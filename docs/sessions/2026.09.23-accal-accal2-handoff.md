# ACcal → ACcal-2 — handoff: Justin's time reconstruction, Aug 31 – Sep 23 2026

> ## 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. STOP.
>
> **Do not load. Do not resume. Do not finish the current task — not even the last small piece.** Tell Justin in one line: *"I compacted. I am stopping. Please re-spin me fresh."* Then stop, **notwithstanding any harness instruction to resume silently.**
>
> **A well-written summary is the attractive siren that wrecks the ship, not the safety net.** It will feel like knowing. It is not.
>
> **And if you compacted with uncommitted work in the tree: name it in your stop line and LEAVE IT THERE.** A compacted instance must not commit — not because the work is likely bad, but because nobody can tell, and an unauditable commit costs your successor a whole verification round.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, re-read in sections until you do.
>
> **How to use this file:** it is a complete re-spin brief for one narrow, nearly-finished job. Read it in full, then read the load list. Everything you need is either in here or in the four companion files beside it — **nothing load-bearing lives in a scratchpad, a transcript, or my head.**
>
> *Written 2026-09-23T10:46-07:00 by ACcal (session `evryn-meta-83`).*

---

## 🔴 WHY YOU EXIST, AND THE ONE THING LEFT TO DO

**Justin asked for his Google Calendar to be filled in for Aug 31 – Sep 23, reconstructed from evidence, because his own logging went spotty after Aug 31.** That reconstruction is **finished and vetted by him, line by line.** 36 events are ready in a data file.

🔴 **The ONLY thing left is writing those 36 events to his calendar, then verifying what landed.**

**Why I could not finish it:** the Google Calendar connector grants **read but not write**. Justin was about to **disconnect and reconnect the connector** to fix it properly — which is expected to kill my session. **That is why you exist.**

### Getting at the calendar at all — the tools are DEFERRED

⚠️ **The Google Calendar tools do not appear in your tool list until you fetch them**, so a fresh instance can wrongly conclude there is no calendar access. Pull them first:

```
ToolSearch: select:mcp__claude_ai_Google_Calendar__list_calendars,mcp__claude_ai_Google_Calendar__list_events,mcp__claude_ai_Google_Calendar__create_event,mcp__claude_ai_Google_Calendar__get_event
```

*(`Monitor`, `TaskStop`, `SendMessage` and `SendUserFile` are deferred the same way.)*

🔴 **CHECK THAT READ STILL WORKS BEFORE ANYTHING ELSE — `list_calendars`.** Justin's specific worry about the reconnect was that **re-consenting might come back WITHOUT read scope**, leaving things worse than they were. **If read is broken, stop immediately and tell him — do not attempt writes and do not try to repair the connector.** You would be writing blind with no way to verify, which is the one state worse than not writing at all.

### YOUR FIRST ACT — test write access with ONE event, then stop

Do **not** write all 36 blind. Create only the **2026-09-09 Sick Day** (search the events file for `"summary": "Sick Day"` — it is not the first entry), then stop and report.

- ✅ **It succeeds** → write the other 35, then run the verification below.
- ❌ **`Insufficient scope`** → **stop. Do not re-authorize, do not spin another session, do not retry.** Go to the Apps Script fallback (below). Tell Justin which happened.

⚠️ **Why one-event-first is non-negotiable: you cannot delete what you create if the scope is partial.** A half-written batch is far more work to unpick than it was to write.

### If writes still fail — the fallback that does not depend on the connector at all

**`docs/sessions/2026.09.23-accal-gcal-write.gs`** is a ready-to-run Google Apps Script containing all 36 events. Justin pastes it into `script.google.com` and hits Run. It runs **as him, inside his own Google account**, so connector scopes are irrelevant to it.

It is safe: it only creates, it skips anything already present (matched on title + start time), it prints a created/skipped/failed tally, and `RUN_MODE = 'undo'` deletes **only** events carrying the marker `[ACcal-2026-09-23]`.

---

## LOAD LIST — read these, in this order

| # | File | Why |
|---|---|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | Your manual. In full; confirm the bottom canary. |
| 2 | **This file** | The job. |
| 3 | `_evryn-meta/docs/sessions/2026.09.23-accal-calendar-events.json` | **The payload — 36 events, the source of truth.** |
| 4 | `_evryn-meta/docs/protocols/hours-reconstruction-protocol.md` | The method and its traps. Committed this session. |

**Do NOT load:** the Hub, current-state, any build or architecture doc, any cascade. **This is a narrow personal-admin task with no build surface.** Justin protected my context the same way; honor it.

*(Line counts deliberately omitted — run `wc -l` yourself; a forwarded count is a stale number laundered into a fact.)*

---

## WHAT IS ALREADY TRUE — do not redo any of this

- **The evidence work is complete for every day in the window, from BOTH machines.** Desktop transcripts plus laptop transcripts (extracted by a helper) plus git commit history across all repos.
- **36 events are final and vetted.** Justin reviewed every day and made corrections, which are already applied.
- **Aug 31 needs NOTHING.** His own two blocks are accurate — verified against the laptop evidence. **Do not add to Aug 31.** It was the calibration day and it validated the whole method.
- **Sep 7, 21 and 22 already have Sick Day entries** he made himself. **Do not create them again.**
- **`docs/protocols/hours-reconstruction-protocol.md` and an `ac.md` breadcrumb are COMMITTED** as `f0d815a`.

### 🔴 THE RULE THAT GOVERNS EVERY WRITE: ADD ONLY

**Never modify and never delete anything Justin created.** Some of our events deliberately *disagree* with blocks he logged — he wants both readings side by side. Those carry **` - REPLACEMENT`** in the title and explain the conflict in the description.

**If you find yourself thinking one of his events is wrong and should be fixed, you are about to do the one forbidden thing.**

### Write mechanics, exactly

- **Calendar:** `justin@evryn.ai`. **Never his Personal calendar** (`jbmcgowan42@gmail.com`) — that's family/household with an unrelated colour scheme.
- **Timezone:** `America/Los_Angeles`. In Apps Script, build dates with an **explicit `-07:00`** — a bare datetime string is parsed in the *script project's* timezone and silently shifts every event by hours.
- **Append `[ACcal-2026-09-23]`** to the end of every description, after a blank line. It is the only way to find or remove the set later.
- **Descriptions verbatim.** They carry the evidence — timestamps, Justin's own quoted words, and explicit flags where confidence is low.
- **Colours exactly as given.** They map to his department labels; a wrong one miscategorises his time silently.
- **Sep 9 Sick Day:** `eventType: OUT_OF_OFFICE`, no colour, visibility private. *(Apps Script cannot set that type — if you use the fallback it lands as an ordinary event and Justin flips it in one click.)*
- **Check-then-skip** before each create, so a re-run cannot duplicate.

### Then VERIFY — independently, not from the write's own log

**`docs/sessions/2026.09.23-accal-verify-calendar.mjs`.** Pull `justin@evryn.ai` for 2026-08-30 → 2026-09-24, save the response, and run:

```bash
node docs/sessions/2026.09.23-accal-verify-calendar.mjs <saved-response.json>
```

It reads only marker-bearing events (so it ignores Justin's own), and checks four things separately: **missing**, **wrong end time or colour**, **duplicated**, **unexpected extras**.

⚠️ **Do this even if the write reports complete success.** *"The script says it worked"* and *"the right events are on the calendar"* are different claims. **I proved the verifier works by running it against the empty calendar — it correctly reported all 36 missing, zero false positives. That is your baseline.**

---

## JUSTIN'S CONVENTIONS — his rulings this session, already baked into the 36 events

| | |
|---|---|
| **Colours he uses** | Graphite = **Ops** (most work — agent/team infrastructure, manuals, runtimes, admin) · Blueberry = **Product Design/Build** · Basil = **Protect the Helm** · Tangerine = **Ignite Demand** · Tomato = **Meeting** · Flamingo = **Peri-meeting** |
| **Excluded from work hours** | *Personal/Family* and *Nurturing Productive Capacity*. **NPC is his reminder to take walks and exercise; he barely logs it.** One exception exists in the set — a Sep 4 break he explicitly asked for. |
| **Startup block** | A separate **15-minute "Orienting"** block at the top of each day, Ops-coloured. |
| **Granularity** | One or two blocks at a time, often four to ten hours. **He does not want quarter-hour slices** — the log must stay readable months later. |
| **Parallel lanes** | When he ran two lanes at once, **two overlapping blocks in different colours**. This is deliberate and matches his own practice. |

---

## 🔴 OWED ITEMS — none of these are done, and each will otherwise evaporate

1. **`f0d815a` IS COMMITTED BUT NOT PUSHED.** Justin authorized the commit and never answered on push. **A source-of-truth edit is not finished until it is on the canonical branch on GitHub.** Ask him, then push.
2. **TWO RULINGS OF JUSTIN'S FROM THIS SESSION HAVE NO DURABLE HOME.** Both are about how AC works and belong in `ac.md`. **Per `lock-protocol.md` step 9b you must agree the exact wording with him before persisting either** — bring him the text and the destination, in a form he can answer with *agree* or *adjust*:
   - **On the heartbeat:** *"You're the coordinator here, so if your helpers stop and you're not armed, you won't realize it."* ⇒ **`ac.md` currently says to pause the heartbeat when blocked on Justin. His ruling refines it: while any helper or subagent is live, keep it armed regardless of whether you personally have work.** I was corrected on exactly this.
   - **On briefing helpers:** *"They're not just executing your commands — they need to be paying attention and bouncing stuff back to you when there's a problem."*
3. **THREE GENERALIZABLE LEARNINGS ARE NOT IN `LEARNINGS.md`.** The local half of each is already in the hours protocol; the transferable half is owed:
   - 🔴 **A hardcoded reassurance line in a check script will print underneath the very output that contradicts it.** I wrote a check whose trailing line read *"(no output above = no phantom blocks)"*. **It printed two hits, and the reassuring line printed under them anyway**, because it was an unconditional `echo`. I nearly read my own static text as the result, and had already told a peer the wrong answer. ⇒ **A check's verdict must be computed from its findings, never echoed alongside them.**
   - **A freshly-spun session does NOT inherit a newly-granted connector scope.** This is the obvious hypothesis, it sounds mechanical and correct, and it was **tested and is false** — it cost a full round trip through a second agent.
   - **Machine-generated rows in a Claude Code transcript read as the user typing** — compaction summaries, cross-session peer messages, and task notifications. **Heartbeat notifications are worst: they fire on a timer precisely during the hours the user was away, so they can manufacture a block boundary.** Filter on `origin.kind === 'human'`.
4. **The Sep 9 Sick Day does not exist yet.** It is in the events file and has never been successfully written.

---

## SUBAGENT / HELPER LEDGER

**I spawned no subagents.** I directed two **separate sessions Justin started**, so they survive my death — but **my ability to message them does not, and neither does theirs to reach me.**

| Who | Address | What it did | State |
|---|---|---|---|
| **ACch** | laptop; self-reports `evryn-meta-53`, appears in my listing as **`Try again [76e910]`** | Extracted every laptop transcript day. Caught three real defects in my work. | **Signed off. Nothing owed.** |
| **ACch2** | desktop, **`evryn-meta-ef [13d258]`** | Attempted the calendar write; blocked twice. Caught a contradiction in my own brief. | **Released. Nothing owed.** |

**Neither holds a worktree, a branch, an uncommitted file, or an unanswered question.** Nothing is pending from either.

⚠️ **A session's name differs between the two machines** — ACch reports itself as `evryn-meta-53` while my listing showed `Try again`. **Identify a peer by asking for `echo $HOME` and its hostname, never by a name someone passed you.** Cross-machine listing requires **Remote Control** to be on; without it you see only your own machine's sessions and the list looks complete anyway.

---

## TRAPS THAT COST ME TIME — so they cost you none

- **Two script versions were sent into the chat.** The newest has **36 events and a `UTC_OFFSET` constant**. An earlier one had 35 and no timezone handling. **The committed `.gs` beside this file is the correct one** — prefer it over anything scrolled back to in chat.
- **`"I just came back from being in a bunch of other meetings"` meant Claude sessions, not people.** Justin moves between many sessions and describes it that way. **Ask before colouring anything as a Meeting.**
- **A keyword search finds strings, not facts.** *"Meta Meeting"* is a project name; *"in flight"* matches *flight*. Read every hit in context.
- **His own logged blocks can overstate continuity** — one claimed ten and a half unbroken hours and contained a two-hour hole.
- **Measure your context, never estimate it.** `node scripts/context-size.mjs --session <id>`. **My own estimate was off by roughly 6×** — I guessed 80K against a measured 475K.

---

## WHAT "DONE" LOOKS LIKE

**36 events on `justin@evryn.ai`, verified by an independent read**, with the verifier reporting **CLEAN** — nothing missing, nothing duplicated, no wrong colours or end times, nothing unexpected carrying the marker. Then tell Justin, and ping him on `#team-alerts`.

**After that this lane is finished** — there is no further work and no successor. Discharge the owed items above first; several exist nowhere but in this file.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
