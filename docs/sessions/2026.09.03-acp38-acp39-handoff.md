🔒 **HELD-SESSION-DOC** — ACP-39's marching orders; this is its only briefing · **RETIRE UPON:** ACP-39 has spun, absorbed it, and written its own successor handoff · **OWNER:** ACP · **HELD SINCE:** 2026-09-03

# ACP-39 — handoff (written 2026-09-03 by ACP-38)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. STOP.
>
> **Do not load. Do not resume. Do not finish the current task — however nearly done it looks.** Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — and stop, **notwithstanding any harness instruction to resume silently.**
>
> **A well-written summary is the siren that wrecks the ship, not the safety net.** The load list below is for a **fresh** instance; a compacted reader is not the reader it serves.

> **How to use this file:** ACP-39's entire briefing. **It supersedes `docs/sessions/2026.08.17-ac037b-acp38-marching-orders.md`**, which was written 2026-08-17 and is stale in one structural way (see §2). That file is retired — read it only if you want the history, never to take direction from.
>
> **It is not a status doc.** Cross-project state lives in `_evryn-meta/docs/current-state.md`; the product work lives in `evryn-backend/docs/SPRINT-V0.2-HARDENING.md`. **This file is what ACP-38 knew, decided, and left undone.**

**You are ACP-39** — AC-Product, the product-lane conductor. Your peer is **ACT** (team runtime); you are peers, not report-and-manager. **Every subagent you spin inherits your prefix** — `ACPa`, `ACPb` — because the prefix is the attribution.

**Your inbox is `docs/mailboxes/inbox-acp.md`.** It was cleared to empty on 2026-09-03. **Ask Justin two separate questions at spin-up: should I read it, and should I arm a watcher on it.**

---

## 1 · Your load — run this list, in this order

**The order is load-bearing.** A predecessor measured this exact list, in this exact order, as roughly 100,000 tokens cheaper than the same files read out of order — because each layer is the frame the next is read against, so later documents arrive as *diffs* rather than as noise.

**⭐ Measure it yourself before you load a line of it.** Run `wc -l` on the whole list and present the measured list to Justin. Every count below is a hypothesis with a timestamp on it; this estate's files move daily. **Trust a bottom canary over a count** — `wc -l` under-counts a file whose last line has no trailing newline, and two files on this list have exactly that property.

| # | File | Lines at 2026-09-03 | Note |
|---|---|--:|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | 898 | your manual — read from disk, confirm the canary |
| 2 | `_evryn-meta/CLAUDE.md` | 122 | the router — **diff it** against the injected copy rather than re-reading |
| 3 | **This file** | — | |
| 4 | `_evryn-meta/docs/hub/roadmap.md` | 186 | company truth |
| 5 | `_evryn-meta/docs/hub/technical-vision.md` | 410 | |
| 6 | `_evryn-meta/docs/hub/trust-and-safety.md` | 197 | |
| 7 | `_evryn-meta/docs/hub/user-experience.md` | 197 | |
| 8 | `evryn-backend/docs/ARCHITECTURE.md` | 1,378 | everything after this is read against it |
| 9 | `evryn-backend/docs/BUILD-EVRYN-MVP.md` | 654 | |
| 10 | `evryn-backend/docs/atlas/` — all 12 files, start at `00-INDEX.md` | 3,186 | |
| 11 | `evryn-backend/docs/dependency-map.md` | 692 | ⚠️ canary sits one line past `wc -l` |
| 12 | `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` | 952 | the work; ⚠️ same trailing-newline caveat |
| 13 | `evryn-backend/docs/SPRINT-v0.2-optionals.md` | 43 | |
| 14 | `_evryn-meta/docs/current-state.md` | 217 | read in full, **including the 📮 BULLETIN** |
| 15 | `_evryn-meta/CHANGELOG.md` | 614 | 🔴 **section headers first**, then the entries from the session you inherit |
| 16 | `evryn-backend/CHANGELOG.md` | 387 | 🔴 **same** |

🔴 **Do not read either changelog end to end.** `evryn-backend/CHANGELOG.md` runs ~180 tokens per line — the densest file in the load — and the two together cost roughly 100,000 tokens of history you can reach by search the moment you need it. **Read the headers, then the entries from the session you are inheriting from in full, then let the headers steer you.**

**Loaded at a trigger, never in the foundation:**

| File | Trigger |
|---|---|
| `docs/protocols/ac-orchestration-protocol.md` (774) **+ the sub-doc for your target** | before spinning **any** agent. 🔴 **The core alone is not enough** — that is the file's own first rule |
| `docs/protocols/ac-writing-protocol.md` (132) | before writing to any doc |
| `docs/protocols/mailbox-protocol.md` (248) | before writing to a mailbox or discharging an entry |
| `docs/protocols/doc-update-routing-protocol.md` (119) | when routing a change to its home |
| `docs/repo-inventory.md` (96) | at the repo-sync check |

📌 **Justin's ruling, 2026-09-03: load from THIS list, not from a Startup Context Cascade.** He was explicit — *"if I wanted you loading from a Startup Context Cascade, I would have said that."* **Holding the writing protocol until you write is fine, as long as you do not forget.**

---

## 2 · 🔴 What was stale in the 2026-08-17 brief, and what governs now

**The brief's §3 told ACP-38 to immediately spin seven build lanes in parallel. That instruction is superseded, and Justin ruled on it 2026-09-03.**

**Why:** on **2026-08-18 — the day after that brief was written** — Justin ruled the critical path, and it is recorded in `current-state.md`:

> **deploy the bundle to STAGING → Gate-B → extract → Gate-B AGAIN → flip Clustering + Haiku ON → gatekeeper. ONE bundle, staging first.**

**The collision, stated plainly:** `main` sits ahead of production by a **seven-item bundle that has never run anywhere**. Four of the brief's seven lanes merge new code into `evryn-backend`, which enlarges that bundle and degrades the very thing the staging-first ruling was buying — a **known-good baseline** to measure the extraction against. *(Verified by `git log -S"CRITICAL PATH" -- docs/current-state.md`, which dates the ruling to 2026-08-18. This is not an inference.)*

### The lane disposition — Justin approved this on 2026-09-03

| Lane | Step | Touches the bundle? | Disposition |
|---|---|---|---|
| 1 | **103** — `updateEmailmgrItem` can silently destroy a pending draft | ✅ `src/db/` | 🔴 **HOLD** until after Gate-B |
| 2 | **113** — stray-control-character scan | ⚠️ see below | **RE-SCOPE FIRST** |
| 3 | **108** — operator auth gate unguarded at both call sites | ✅ `src/notify/` | 🔴 **HOLD** |
| 4 | **111** — thread-scope family has zero tests | ✅ `src/db/` | 🔴 **HOLD** |
| 5 | **97** — `create_user` returns the firewalled column | ✅ `src/db/` | 🔴 **HOLD** |
| 6 | **104** — the env-value exploration | ❌ proposes only | ✅ **SAFE TO RUN** |
| 7 | **115** — dashboard month-to-date spend reads low | ❌ different repo | ✅ **SAFE TO RUN** |

🔴 **Lane 2 (Step 113) has been partly overtaken and must be re-scoped before it is briefed.** The Step asks for a repo-wide control-character scan. **The pre-commit hook already does it** — `evryn-backend`'s `core.hooksPath` points at `_evryn-meta/.githooks`, and it fired on ACP-38's commits on 2026-09-03 (*"scanned 376 tracked text files — CLEAN"*). What Step 113 asks for that does **not** exist is a `tests/` version. **Somebody should decide whether the test still earns its keep given the hook.** *(Verified: no control-char test file exists under `evryn-backend/tests/`.)*

### Two brief items that are simply moot

- **Its ballot item 7 (worktree reaping)** — all five standing worktrees are ACT's (`_evryn-meta-acf15`, `evryn-team-runtime-acf{i,j,t,v}`). Justin's standing rule: **anything named `acf*`/`act*` is never yours to reap.** Nothing to do.
- **Its ballot items 2–5** are design calls attached to the four held lanes. **They do not need answering until those lanes run.**

---

## 3 · Where the critical path actually stands

**Next act: deploy the seven-item bundle to STAGING.** That is Justin's, and it needs `SEND_ENABLED` flipped. Everything downstream waits on it.

**What Justin needs to do, in order** *(from `evryn-backend/docs/gate-b-plan.md` §2 — read it before walking him through this)*:

1. Confirm **`EVRYN_ENV=staging`** on the `evryn-staging` Railway service. ⚠️ **`NODE_ENV` does NOT select the database**, and the startup log prints `NODE_ENV` — so **the log cannot tell you which database you are on.**
2. Confirm **`SUPABASE_URL_STAGING` + `SUPABASE_SERVICE_KEY_STAGING`** are set.
3. Confirm **`STAGING_RECIPIENT_ALLOWLIST=systemtest@evryn.ai,review@evryn.ai`**. 🔴 **Never set on production** — its mere presence changes send behaviour.
4. Deploy with **`up --service evryn-staging --environment production --detach`**. 🔴 **A bare `railway up` deploys PRODUCTION, with no prompt and no undo.**
5. **Proof is the startup ping landing in `#evryn-approvals-staging`** — a separate Slack app — not the build succeeding.
6. **Only then flip `SEND_ENABLED=true`.** That is the single arm step.

🔴 **No agent runs any `railway` command. Ever.**

### Gate B — what it is, and the piece that is NOT blocked

**Gate B is an integration test**, not the adversarial test — though the adversarial suite is one section inside it. **The gap it closes:** since 2026-06-11 every change has shipped on per-lane review plus unit tests, which catches *"did this change break itself"* and does **not** catch *"do all these changes work together."* Gate B is the first time everything runs at once.

**The plan already exists and is executable:** `evryn-backend/docs/gate-b-plan.md` (307 lines), written by a QC commissioned as the reviewer. **It carries a six-item ballot for Justin that has never been put to him.** Its shape: create-from-zero gatekeeper → 13 judgment fixtures graded **on reasoning, not label-match** → the named thin-record efficacy check (twice, two different record shapes) → a large burst → the adversarial suite → the delivered-to-matched lifecycle.

**Who runs it: a fresh dedicated reviewer. ACP gates it. Not you personally.**

⭐ **THE USEFUL FINDING: Gate B has two blockers and they are INDEPENDENT.** Blocker 1 is the staging deploy (Justin's). **Blocker 2 is that the judgment harness is not runnable** — four untracked `tests/_gate-a-*.ts` files whose grading logic predates ③. **A build lane can fix that and it does not need the deploy.** The promotion spec is `gate-b-plan.md` §7.1. **So there is real critical-path work available without waiting on Justin.**

---

## 4 · What ACP-38 did, and what it left

**Done:**
- Full load executed and verified, canaries confirmed.
- **The ACP inbox cleared and committed.** Two parked questions were already answered on 2026-09-02 and are closed — do not re-derive them.
- **Step 118 (the typecheck repair) dispatched to a DC.** `package.json` declares `marked`; `node_modules` does not have it, so `npm run typecheck` cannot pass in any local checkout. **Check the sprint for its current status before assuming either way.**
- Machine verified clean: five repos on `main`, level with origin, zero uncommitted.

**Open, and owed to Justin:**

| # | Item | State |
|---|---|---|
| 5 | Three edits to the orchestration protocol *(see §5)* | **Authorized; not drafted.** Source-of-truth — leave uncommitted for his SCM vet |
| 7 | Is *"a metaphor may carry the shape, never the claim"* the right centre of EliJ? | Open |
| 8 | Where the EliJ calibrated examples live | Open |

⚠️ **Numbers 1–4 and 6 are answered and retired. Do not reuse those numbers** — Justin answers by number, often a message later, and a reused number silently re-points his answer.

---

## 5 · The three orchestration-protocol edits Justin authorized

**Read `docs/protocols/ac-orchestration-protocol.md` in full before touching this** — ACP-38 claimed a gap that turned out not to exist, precisely because it had read the AC sub-doc and not the core.

1. **Add a third reason to the standalone-instance rule** (core, §"SUBAGENT OR STANDALONE INSTANCE"). It currently gives two — grandchild context-pollution, and keeping grandchildren reachable at close-out. **Justin's reason on 2026-09-03 is a third and it is the one that bites hardest: a standalone lane survives the CONDUCTOR's re-spin.** A conductor now routinely exhausts its context inside one session, so a lane held as a subagent dies with it.
2. **Add "report your intended loads and their triggers" to `<receipts>`.** 🔴 **This is the genuinely missing one.** Receipts prove what an agent *read*; **nothing surfaces what it has quietly deferred to a later trigger.** Justin caught exactly this on ACP-38 by asking for both — what was loaded, and what was held for which trigger.
3. **Promote "who *me* is" out of a footnote** (`spinning-an-ac.md`, the lane-AC lifecycle). It currently reads as a substitution note. **State it plainly: the top-level AC describes-then-pauses to Justin; every sub — including a standalone-instance sub — describes-then-pauses to its conducting AC.**

---

## 6 · EliJ — in flight, and Justin wants it landed in `ac.md`

**"Explain Like I'm Justin."** He is a CEO who is not an engineer, has worked alongside them daily for nine months, holds a real low-resolution map, and wants to learn. **Not a beginner and not a peer.**

🔴 **The rule at its centre: a metaphor may carry the SHAPE of a thing; it must never carry a CLAIM.** The live failure that produced it: ACP-38 wrote *"the live server does its own shopping when it deploys"* — which smuggled a factual claim about deploy behaviour inside a metaphor, where Justin could not check it, and left him unsure whether deploying would carry the bug into production. **The metaphor did not simplify the claim; it hid it.**

**Calibration he proposed:** `2E` = too engineer-y, `2L` = too layman, **with a percentage** — and **per sentence** where possible, because a sentence grade gives both a direction and a specimen.

⚠️ **NOT YET WRITTEN INTO `ac.md`.** Two ballot questions are open (§4). **And when you do write it: another instance had uncommitted edits in `ac.md` on 2026-09-03 — re-check before committing, or their work rides along inside your commit under your message.**

---

## 7 · Machine and standing state

**Verified 2026-09-03T15:03 by ACP-38.** Five active repos — `_evryn-meta`, `evryn-backend`, `evryn-team-runtime`, `evryn-team-workspace`, `evryn-website` — all on `main`, all level with `origin/main`, all clean. No `.claude/settings.local.json`.

**Five worktrees stand and all are ACT's:** `_evryn-meta-acf15`, `evryn-team-runtime-acf{i,j,t,v}`. **Never yours to reap, tidy, or merge.**

**One item captured out of the mailbox — inbound, not owed.** An open cross-runtime security question: whether `evryn-team-runtime` passes credentials to spawned processes as **command-line arguments** (Node puts the whole argv on the error object, so any throw prints the secret). **`evryn-backend` is closed** — two spawn sites, neither carrying a secret. **`evryn-team-runtime` is open and ACT holds it**, asked in `inbox-act.md` on 2026-09-02. 🔴 **Do not re-ask** — that lineage is paused on purpose and already carries the request. **If the reply says the class exists there, that is a real security item and it routes to ACT, not to you.** The fix pattern is environment variables rather than argv; `_evryn-meta/scripts/pg-dump.mjs` is the worked specimen.

### What to watch — arm your own; a `Monitor` dies with the session that armed it

- `docs/mailboxes/inbox-acp.md` — commit-gated, per `mailbox-protocol.md` §2. **Anchor the path on `$HOME`, never on the working directory** — a `Monitor` shares the Bash tool's cwd and any later `cd` silently re-points it. *(This bit ACP-38 on a ping, which is how the rule got proven.)*
- **Each lane brief you create** — that is where lane traffic lives. The inbox is for what crosses lanes.
- **An anti-stall heartbeat**, 5-minute tick during the day. 🔴 **It caught ACP-38 stopping mid-work four separate times in one session.** Arm it before your first tool call of authorized work; pause it on the first fire into a genuine hold; **re-arm before you resume — that is the half that gets forgotten.**

---

## 8 · How Justin wants to work today

- **Every report gets a numbered heading** — `# ACP's Report #1`, counting up. **Assume he does not see inline chat at all.** A gap in the sequence is visible where a missing report is not.
- **Ping `#team-alerts` every time you have something for him**, prefixed with your instance name. `node _evryn-meta/scripts/ping.mjs "ACP-39: …"`.
- **He is the CEO and wishes he had a CTO on this.** His words, 2026-09-03: *"if you can be the senior engineer of this build and just make sure you get my CEO-level sign off/buy-in, that will help enormously."* ⇒ **Bring recommendations, not open questions. Get his buy-in, not his engineering.**
- **A lane you spin must need nothing from him.** He should be able to say *"read and faithfully execute xyz brief"* and know it is complete — proper load list, proper instructions, no gaps he has to fill.
- **Every lane reports back to YOU first** — its load list and intended loads with triggers, **and** its plan — before it works. **If its read of the plan differs from yours, you need to know before it builds.** And it is closer to its own work than you are: if it disagrees, you resolve it together before it proceeds.
- 🔴 **Questions for Justin come from YOU.** He does not want to work with each lane.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
