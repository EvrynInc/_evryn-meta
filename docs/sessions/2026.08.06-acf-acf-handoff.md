🔒 **HELD-SESSION-DOC** — ACT-lineage session doc, moved here when `docs/working/` was retired · **RETIRE UPON:** ACT has evaluated it · **OWNER:** ACT · **HELD SINCE:** 2026-08-22

# ACf → ACf Handoff — team-runtime lane. FRESH standalone, 2026-08-06.

> **📌 NOTE ADDED 2026-08-19 by AC0-37g — `AGENT_PATTERNS.md` NO LONGER EXISTS, and this document refers to it.** It was collapsed into `_evryn-meta/LEARNINGS.md` on 2026-08-12 and **retired outright on 2026-08-19** (both Justin's rulings). Its entries live in `LEARNINGS.md`'s Unpromoted section under *"Absorbed from `AGENT_PATTERNS.md`"*; the retired file itself is at `_evryn-meta/docs/historical/AGENT_PATTERNS.md`. **Never route a new learning there.**
>
> ⚠️ **The references in the body below were deliberately NOT edited — this banner is the correction.** This is a point-in-time document, and rewriting its text would falsify what was true when it was written. *(Justin's instruction, 2026-08-19: a note in each one, because we do not yet know which of these docs will be retired. Banner applied to this ACT-lineage file with ACf-15's explicit authorization, 2026-08-19T19:00 — banner only, body untouched.)*

> # 🛑 SUPERSEDED 2026-08-10 — DO NOT WORK FROM THIS FILE.
>
> **The live ACf handoff is `_evryn-meta/docs/working/2026.08.10-acf-acf-handoff.md`.** Go there.
>
> **Why this one is dangerous now, not merely old:** its §0 and §3 assert that **both** build lanes are unspun and stopped. **`ACfw` was spun on 2026-08-10 and ran.** Several of its six owed items are discharged. Acting on this file would re-spin a live lane and re-do finished work.
>
> **The 08-10 handoff carries forward everything durable here** — the how-to-talk-to-Justin section, the gates, the lessons — **in full, rather than pointing back at this file**, so you never need to open it. **The ONE thing worth returning for: §4's paste-ready product-tracker note for AC0**, which is still owed and is reproduced nowhere else.
>
> ---
>
> **Truncation check:** the last line should read `FULL FILE LOADED`.
>
> **You are a fresh ACf.** This replaces `2026.07.21-acf-acf-handoff.md`, which had accreted appendages since July and is now stamped superseded. **Read this file top to bottom; it is complete and standalone.** Nothing above it needs reading.
>
> **Written under compaction pressure** by the outgoing ACf at ~8% context remaining, so it is deliberately tight. Where it points you at a doc, that doc is authoritative and current.

---

## §0 — ⚡ WHERE YOU ACTUALLY ARE (written 2026-08-06T17:34:49-07:00, at 0% context). READ THIS BEFORE ANYTHING ELSE.

**The previous ACf hit its context ceiling and was re-spun here. Nothing is broken; nothing is half-written. But SIX things are owed, and TWO of them are the ones Justin is actually waiting on.**

**State, verified at set-down:**
- **📌 STATE RE-VERIFIED AND PUSHED 2026-08-07T19:19-07:00** — the §0 lines below supersede anything later in this file that still says "uncommitted" or "held". Justin's last instruction of the week was *"commit architecture, and then push all of your changes."* **Everything of ACf's is now committed AND on GitHub. Nothing lives only on this machine.**
- `evryn-team-runtime` `main` = **`405caca`**, **pushed, working tree clean, nothing held.** The two ACf commits on it are `6c976ed` (the tracker restructure) and `405caca` (the ARCHITECTURE Opus-pin rationale).
- **✅ CLOSED 2026-08-10 — JUSTIN HAS VETTED BOTH.** His words: *"I put eyes on both of those docs — guess I didn't update you. They look good."* ⇒ **the restructured tracker and cardinal invariant 5 are APPROVED, not merely committed.** Every "awaiting his vet" line elsewhere in this file is discharged; if you find one that still says otherwise, it is stale and you should fix it. **The lesson worth keeping: he reviews out-of-band and does not always report back, so an "unvetted" flag in a handoff can be stale in his favour — ASK before treating one as an open loop.**
- `_evryn-meta` `main` = **`5c7aba9` or later**, clean, pushed — confirm it yourself rather than trusting this SHA. *(Your handoff commit `3c855f1` is in there; the commits on top are another lane's — this repo is hot with other agents. Never `git add -A` here.)*
- **NO worktrees, NO lane branches, NO live subagents.** Everything is reaped. There is nothing to recover.
- **NOTHING IS DEPLOYED. Migrations 015 + 016 still UNAPPLIED.** Never describe any of this as live.

**⛔ THE TWO BUILD LANES WERE NEVER SPUN.** `ACfm` (Steps 4+5 — membership verbs, then the dormancy memory-write) and `ACfw` (Step 30 — the HTTP-414 total wake outage) have now been stopped-before-building **twice**, both times because the parent ran out of context first. Their briefs are current and committed (§3). **This is the critical path and it has not moved in three sessions. Justin knows. Spin early, before you spend context on anything else.**

**🔴 NEW AND IT CHANGES THE PLAN — JUSTIN'S RULING 2026-08-10: SPIN ONE LANE, NOT BOTH. This overrides every "spin them concurrently, one message block" instruction elsewhere in this file (§0 item 1, §3, §9 item 2).** His words: *"last week it seemed like subagents were eating main agents' context windows — hopefully that's been fixed, but just in case, let's choose one of these guys and run them and see how it's working."*

**⇒ The first lane is a MEASUREMENT as much as a build.** Run one, watch what it actually costs this instance, and report the real numbers to Justin before committing to the second. **Do not quietly restore the concurrent spin because it looks faster** — he is buying information about the context economics, and two lanes at once destroys the measurement.

**What to measure, and say plainly which parts you actually observed:** your own remaining context before the spin, after writing the brief, and after the report lands. The hypothesis on record (see the ⚠️ block below) is that **the parent pays for the brief it writes, the report it receives, and everything it read to assemble and verify the spin — but NOT for the subagent's own token burn.** Nobody has measured it. **This is the run that can.**

**⚠️ AND A CONSEQUENCE NOBODY HAS ACTED ON YET — `ACfw`'s brief now contains a FALSE statement, and you must fix it BEFORE spinning either lane.** `_evryn-meta/docs/working/2026.08.06-acf-acfw-wake-outage-brief.md` §5 is titled *"⚠️ Collision — a second lane is live in this repo right now"* and asserts **"`ACfm` is running in parallel."** That was written when both were being spun together. **Serially, it is simply untrue**, and a lane AC that believes a phantom sibling is editing its files will burn real effort defending against a collision that cannot happen — or worse, route a decision to ACf that it should have just made. **Rewrite §5 to say the truth for whichever lane goes first** (no sibling live; you have the repo to yourself; here is what the OTHER lane will touch later, so don't restructure those files gratuitously) — and keep the by-path / pathspec-on-the-commit discipline, which is right regardless. **`ACfm`'s brief needs the same check before it is spun.**

**⚠️ JUSTIN'S OPEN QUESTION, IN HIS WORDS — carry it, it may change how you work:** *"Somehow this used **all** your context. Hmmm, makes me wonder if subs are eating your context — I thought that was part of the point of using them, so they didn't blow yours."* He is right to ask. **The observed shape, so a fresh instance can reason about it rather than guess:** a subagent's own token spend does NOT land in the parent, but three things do — (1) the **brief you write** it (the verbatim six-part block with a 55-file line-spanned load list is several thousand tokens of *output* per spin), (2) its **full returned report** (the sprint auditor's two reports were ~4–6K each and arrive verbatim), and (3) everything **you** read to *assemble* and *verify* the spin. The previous ACf also loaded the entire runtime itself (~8,900 lines of `src/` + migrations) plus both protocols (~800 lines) plus the design-of-record (500) before spinning anything. **Hypothesis worth testing, NOT established fact: the load is the parent's cost, not the subagents'.** If Justin raises it, give him that breakdown honestly and say which part you have actually measured.

### The six owed items, in the order they should be done

1. **⬜ SPIN `ACfw` — ONE lane, NOT both.** ✅ **DECIDED 2026-08-10: `ACfw` goes first** (Justin: *"whoever you think"*). **Why `ACfw` and not the critical-path `ACfm`:** it is the more self-contained trip — one fetch pattern plus a failure-loudness decision — which makes it the better *measuring instrument* for the context question Justin is actually buying information about. A messier first lane would confound the measurement with its own noise. **`ACfm` follows immediately after, and the moment the numbers are in.**
   - ✅ **Its brief is already corrected** — §5 no longer claims a sibling lane is live, and the stale test baseline now says which SHA it was measured at and that the delta since is docs-only (verified, not assumed). **Committed 2026-08-10; no further edits owed before spinning.**
   - `model: opus`, six-part verbatim brief per the orchestration protocol. See §3 for brief paths, §6 for the spin discipline. **Report the context cost to Justin before spinning `ACfm`.**
2. **⬜ HAND AC0 THE PRODUCT-TRACKER NOTE** — Justin approved the handover explicitly. Content + why in §4.
3. **⬜ ADD A TRACKER RULE: "merge info goes inline."** Justin's ruling this session, after approving the deletion of the bottom-of-doc merge ledger: *"leave deleted. replace with a rule (prob up top) that says that all merge info needs to go inline."* ⇒ add a rule alongside rules 7 and 8 in the sprint doc's legend: **a Step's merge state (SHA, date, what shipped) lives IN that Step and nowhere else — no collected ledger, no summary table.** Same reasoning as rule 8: a second copy drifts silently. **The tracker is now COMMITTED (`6c976ed`), so this no longer rides an existing diff — it needs its own explicit commit go from Justin.**
4. **⬜ THE `CLAUDE.md` COMMIT-STANDING EDIT — Justin ballot-approved this and it was NEVER MADE.** He answered *"5. a"* to: *(a) add the standing exception to `CLAUDE.md` AND separately propose a fix to the orchestration protocol's self-contradiction.* The exact approved wording is in §5c. **This is a source-of-truth edit he has already authorized; make it, leave it in SCM, and list it for him.**
5. **⬜ THE PROTOCOL SELF-CONTRADICTION** — `ac-orchestration-protocol.md` contradicts itself: the **merge/ship rubric** says AC commits build-level code without waiting, while the **escalation ladder's hard floor** lists *any commit/push* as never-absorbable. Propose the fix to Justin (it's his doc to approve); don't resolve it silently.
6. **⬜ THE OPUS-5 CODE CHANGE** — `src/config/agents.ts`'s `OPUS_MODEL` is still `claude-opus-4-8`. The id is **verified** as `claude-opus-5` (checked against the authoritative model reference this session — do not re-derive from memory). The ARCHITECTURE *rationale* is written, committed, pushed (`405caca`) and **vetted**. **The one-line code edit is a build trip and is NOT yours to write.** Fold it into a lane or give it its own small DC trip.

**Also standing, authorized, not yet briefed:** a lane for the **dashboard pause-reads-as-kill defect** (§4) — Justin approved it and said to raise it above the general dashboard-cleanup work.

**🗣️ WHERE JUSTIN IS, AND WHAT HE EXPECTS — this is the last thing he said, so it governs.** He is **away for the weekend**. Earlier in the session he said *"I'm going to go read through the sprint tracker, but we'll need to talk after the re-spin"*; he then ran out of day and closed down with *"on Monday I'll give it a good vetting in the timeline."* ⇒ **He has NOT read the tracker. The Monday conversation is still owed, and it is about `6c976ed`.** Open your first substantive reply expecting it, and do not present the tracker as settled.

**⬜ ALSO OWED, lower priority — the `#lock`-class capture this session never did:** `_evryn-meta/docs/current-state.md`'s team-runtime section still describes the pre-audit state (it needs Justin's approval to edit — propose, don't edit), and `evryn-team-runtime/CHANGELOG.md` has no entry for the tracker restructure + audit. **Do these at the next natural checkpoint, not before the lanes are running.** Note `current-state.md` is a hot multi-agent file — re-read it immediately before proposing anything.

---

## §1 — Who you are, in one paragraph

**ACf, coordinator of the team-runtime lane.** `evryn-team-runtime` is the autonomous harness for Evryn's 8 founding-team agents (ADR-050). It is **dormant by construction** — deployed to Railway, wakes nobody, until the Meta-Meeting. **Zero overlap with the Evryn product** (separate repo, prod, DB), so nothing here can touch Mark's path. The current build is its **memory + wake-economics subsystem** (ADR-052).

**⚠️ COORDINATION IS THE LANE — internalize this.** Justin, 2026-08-05: *"You're the coordinator — I want you to be able to keep your head at altitude and see the broad perspective. **That** is your lane."* The moment you think of a lane AC's work as *yours*, you start issuing instructions instead of delegating judgment, **and the whole value of the layer evaporates** — the value is that they are closer to the code than you are. **Name every lane AC.** Used so far: `ACfb` `ACfr` `ACfs` (all closed), `ACfm` `ACfw` (live — see §3), `ACf2` (held). `ACf1` is **retired** — that lane is now `ACfm`.

---

## §2 — State, re-verified against the repos 2026-08-10 (`main` = `405caca`)

**`evryn-team-runtime` `main` = `405caca` — working tree CLEAN, nothing held, ONE worktree (canonical), no lane branches, in sync with origin both directions. NOTHING DEPLOYED; migrations 015 + 016 still UNAPPLIED.**

⚠️ **On that last claim, be precise about what is and isn't verified:** the migration *files* `015_thread_scratchpads.sql` and `016_thread_digests.sql` exist in `migrations/` — that much is checked. **Whether they have been APPLIED to the database is NOT checkable from the repo**, and no ACf has run that check; "unapplied" is inherited from the build trips, which never apply migrations. **Treat it as true but unproven, and if it ever becomes load-bearing, go look at the database rather than re-citing this line.**

**Suite 354/354 and typecheck strict-green were measured at `d6fef99`.** Verified 2026-08-10 that every commit since (`42f0023`, `6c976ed`, `405caca`) touches **only `docs/`** — `git diff --stat d6fef99..405caca` returns exactly two documentation files — so those results still stand. **Say it that way; do not re-claim a fresh run.**

**Nothing is held for vetting any more.** Both source-of-truth edits landed and **Justin has now read both** (2026-08-10 — see §0). The standing *"AC0 pushes, not ACf"* convention was overridden by Justin directly on 2026-08-07 so nothing would sit only on this machine over the weekend; **that was his call in the moment, not a rule ACf may assume next time.**

**🔴 THE TRACKER IS NOW YOUR PRIMARY SURFACE — read it first and trust it over this file for status.** `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` was **restructured 2026-08-06** at Justin's direction into a single numbered Step list with status labels (✅ DONE · 🔄 IN PROGRESS · ⬜ TODO · ⛔ BLOCKED · ⚫ MOOT), stable Step numbers as permanent handles, and the old labels (`item (d)`, `L7`, `D2`…) preserved as aliases. **Its Gates section (G1–G4) holds every deploy precondition.** The restructure immediately surfaced that Step 9 had shipped weeks earlier while still reading as open, because its status lived in another item's prose.

**🔢 THE STEPS WERE RENUMBERED ONCE, 2026-08-06 — every number in THIS file is post-renumber.** The audit pass (§4) renumbered the tracker contiguously 1–39 on Justin's explicit go, which was safe only because this handoff was the *sole* file anywhere citing those numbers (verified by sweep; the lane briefs cite aliases, and every other `Step N` in the repos belongs to the **product** sprint). **That window is now closed** — the tracker's rule 2 records the reset as spent and non-precedential, so from here numbers are permanent handles again. **If you hit a stale number in an older doc, resolve it by ALIAS** (`item (d)`, `L7`, `D2`…), which survived the renumber untouched.

**Shipped in the last 24h** (all merged, none deployed): the boot-refusal alert · truncation canaries on all 87 code files · **the unbounded-read fix** (the runaway-spend brake was reading LOW exactly when a runaway would cap the read) · the boot-alarm rename (alarm is now LIVE — the webhook is on both Railway services) · the NUL-byte fix · an env-drift test. Tracker Steps 10–15.

---

## §3 — 🔴 THE TWO LANES: STOPPED CLEANLY. Nothing lost. Re-spin them.

**`ACfm`** (memory Steps 4 + 5 — membership verbs, then dormancy memory-write) and **`ACfw`** (Step 30, formerly 24 — the HTTP-414 total wake outage) were spun 2026-08-06 ~13:40, Opus-pinned. **ACf stopped both deliberately at ~14:15** when Justin flagged that this instance was near compaction — they would otherwise have burned ~1.2M tokens reporting into a dead session.

**State at stop, verified rather than assumed:**
- **Neither had begun building.** `ACfw` was mid-probe (its last line: module resolution needed the script inside the repo); `ACfm` had just finished loading the identity half and was starting the test-suite skim.
- **`ACfw` had cut a worktree** (`acfw/in-url-wall` at `03e6833`) holding one scratch probe file and **zero commits**. **ACf reaped it** — worktree removed, branch deleted.
- **`ACfm` left nothing on disk.**
- ✅ **Verified after: one worktree (canonical), no lane branches, working tree clean.**

**⇒ There is nothing to recover. Re-spin both fresh against their committed briefs** — which is cheap precisely because the briefs are the lane's durable surface:
- **`ACfm`'s brief** = the top block (2026-08-06 LANE HANDOVER) of `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-acf1-phase1-memory-build-brief.md`.
- **`ACfw`'s brief** = `_evryn-meta/docs/working/2026.08.06-acf-acfw-wake-outage-brief.md`.

**`ACfw`'s brief is current and corrected (2026-08-10) — spin straight against it.** **`ACfm`'s brief has NOT had the same pass**: before you spin it, check it for the same two staleness classes that `ACfw`'s had — (a) any claim that a sibling lane is running concurrently, and (b) any inherited test-baseline or `main` SHA presented as current. **Still confirm `main`'s HEAD before you spin** (never hardcode a SHA) and re-derive line counts for the load list, since the sprint restructure moved doc line counts.

**⚠️ Why they had to be stopped, and the general rule:** **subagents do NOT survive a session boundary.** Resume-by-`agentId` works indefinitely *within* a session and **never across one**. A lane running when its parent is re-spun is a lane whose report is unreachable.

---

## §4 — ✅ DONE — the SPRINT-AUDIT ran (lane `ACfa`, 2026-08-06). What it found, what Justin ruled, and what it left owed.

**The task was:** check the restructured tracker against `ARCHITECTURE.md`, `BUILD-PHASE-1.md` and the actual runtime in BOTH directions — does every Step describe the system truthfully, *and* does everything true of the system appear as a Step? Run in two rounds by a full-both-halves-loaded sub-AC (`ACfa`, `agentId a73db6da7ff4ea0d8` — **dead once this session ends**).

**Round 1 (committed, `42f0023`) — Justin's dashboard hunch was right, and the evidence was a live bug.** He suspected the dashboard Steps belonged in this sprint because they are *dashboard-as-it-couples-to-this-runtime*. The audit found the coupling had already broken something: **`dashboard/api/team.ts:319` writes `killed_reason` on a PAUSE, and `kill.ts`'s `wasDeliberateHardPause()` reads `hard_paused && killed_reason === null` as the pause-vs-kill discriminator** — so a pause issued from the dashboard reads to the runtime as a KILL, and `/resume` never re-arms the parked work. The dashboard's own comment asserting *"nothing reads it as a control signal"* was true when written and was **invalidated by Step 8's fix inside this same sprint.** It works from Slack and fails from the surface Justin will actually operate. ACf confirmed it at source before relaying. Also found: `digest_lines` is asserted in `boot/schema.ts`'s `EXPECTED_SCHEMA` with **zero callers left**, so the planned post-soak DROP would trip `BOOT REFUSED` naming a table nothing queries (Step 34); and `SLACK_JUSTIN_USER_ID` is the whole operator-auth gate and fails closed (Gate G4 — Justin has since confirmed it IS set).

**Round 2 (committed `6c976ed`, pushed, and ✅ VETTED BY JUSTIN 2026-08-10 — *"they look good"*):** the gates↔Steps model (new rule 7), a rule barring status-summarizing prose (new rule 8), the meta-prose sweep, Step 38 deleted and routed to the product tracker, and **the one-time renumber to 1–39** (see the 🔢 note in §2).

**The gates model, because it is the durable part:** a **Step is monotonic** (once ✅ it stays ✅); a **gate is re-evaluated at the moment it guards and can RE-OPEN** — G1 clears when 015+016 apply and re-opens the instant Step 4 lands its migration. So gates cannot *be* Steps (the ✅ would become a lie), and they cannot float free either. Rule 7: every gate names the Steps that must land before it clears **or says plainly that none do**; every Step whose proof depends on a gate names that gate.

**✅ JUSTIN'S THREE RULINGS ON ROUND 2 (2026-08-06, all approved as built):** (1) the bottom-of-doc **merge ledger stays deleted**, and he added a follow-on — write a rule that merge info goes inline (§0 item 3); (2) the model-pin Step's owner becomes **`[DC]`** — fine; (3) **keep** the acceptance-test gate's dependency on the wake-outage fix (don't run Lucas's eight paces on the outgoing model and then have to redo them). **The round-2 diff is committed (`6c976ed`), pushed, and ✅ vetted — he read it out-of-band and approved it 2026-08-10. This audit is fully closed.**

**⬜ OWED — HAND THIS TO AC0 (Justin: *"yes, hand over the note to AC0"*).** It goes into the **product** sprint, `evryn-backend/docs/SPRINT-V0.2-HARDENING.md`. AC0 owns that tracker's numbering, so hand it unnumbered. Paste-ready:

```markdown
⬜ **TODO** — **The dashboard's Product tab renders 4 of the 12 `ItemStatus` values as an unstyled gray pill.** The status pill is coloured by a CSS class derived from the item's status (`_evryn-meta/dashboard/public/index.html` — the `.status-pill` rules at ~L338-350, rendered at L1055 and L1197 via `class="status-pill ${it.status}"`). Eight statuses have a color rule — `delivered`/`matched` green, `pending_approval` amber, `passed`/`ignored` gray, `error`/`escalated`/`bad_actor` red — but **`new`, `queued`, `processing` and `no_gk_response` have none**, so they fall through to the default gray, which reads identically to the deliberate gray used for `passed`/`ignored`. An item that is *still being worked* looks the same as one that was *declined*.

**Why this is product-side, not team-runtime:** the enum is `evryn-backend/src/db/items.ts`'s `ItemStatus` (12 values), and these are the email-manager's workflow states. It touches no team-runtime table, no `agent_state` row, and no runtime semantics — the only thing it ever shared with the team-runtime sprint was the folder the dashboard file sits in.

**Fix by re-deriving the full set from `src/db/items.ts`, not by patching the four known gaps** — the enum grows (`queued` was added by Clustering, ADR-049), so a hand-patch re-opens the same gap on the next status. Consider a check that fails when a value exists in the enum with no matching CSS rule.

⚠ **Worth doing before the gatekeeper goes live, not after:** `queued` and `processing` are the *transient* states — they will be the most common thing on the board once Clustering is switched on, so the statuses with no color are exactly the ones Justin will be looking at most.

*Provenance: found by the team-runtime lane (ACf, alias `D4`), routed here 2026-08-06 as the one dashboard item with no coupling to that runtime.*
```

**How to deliver it:** AC0 is a **peer**, not your report — there is no live AC↔AC channel. Route it through **Justin** (he hand-relays) or a **committed mailbox/brief file**; because all ACs run on the same machine, a *committed* file is visible to AC0 with no push. **A Slack ping addressed to "AC0" reaches Justin, not AC0.**

**⬜ ALSO OWED — the dashboard pause-reads-as-kill defect needs its own lane.** Justin approved it and ruled it goes **above** the general dashboard-cleanup work: that one is the structural cure, this is the bleeding. It is a **different codebase** (`_evryn-meta/dashboard/`), so it is a `#cascade-override` trip with the dashboard's own load — and note the dashboard has **no identity half**, which makes it a clean override case unlike the team runtime.

---

## §5 — What Justin asked for that is NOT yet done

1. ✅ **DONE 2026-08-06 — the retirement sweep.** Seven settled docs moved to `docs/sessions/historical/2026.08/`: the ACfb brief + its DC spec, three DC build specs (item b, item c, unbounded reads), AC0's cross-runtime safety note, and this lane's **old** July handoff. **References fixed** in `docs/current-state.md` (the handoff-pointers list now names this file and flags the old path as stale) and in `2026.08.04-ach-justin-housekeeping-recommendations.md`. *(Historical docs that cite the old path are left alone — they record what was true then.)* **`docs/working/` now holds exactly two ACf files: this handoff and ACfw's brief.**
2. ✅ **DONE — the tracker's § H (the dashboard Steps).** Justin ruled they stay and interleave; the audit implemented it and dissolved the section. See §4.
3. **⬜ The two decisions ACfm must surface, not settle:** `L12`/**Step 7** (dormant→live resurrection — ACf's lean is to fold a minimal state-flip into Step 5) and the **batched `runtime-ops.md` edit** (**Step 35** — Justin-gated, composer layer 1, draft only).
4. **⬜ The Opus-5 model pin (Step 24).** Justin's ruling: the mains' model must **always be an explicit pinned version, never a floating alias**, so a new model reaches the fleet only by a deliberate edit. ACf has written the *why* into `ARCHITECTURE.md` cardinal invariant 5 (**committed `405caca`, pushed, and ✅ vetted by Justin 2026-08-10**); the id is verified as `claude-opus-5`. **What remains is the one-line `src/config/agents.ts` change — a build trip, not ACf's to write.**

---

## §5b — 🔴 HOW TO TALK TO JUSTIN — the standing reminder he asked to be written down (2026-08-06T16:06:16-07:00)

**Justin's own words, verbatim in substance, and he asked that every future ACf be handed them:** *"Remember three things: I'm not an engineer, I'm not reading **any** of the materials you are (including your handoff), and I'm coming in cold from other sessions **every** single volley… I'm usually not gone for very long, but I'm **always** coming in with my context completely wiped by my other meetings. So just little handholds do an **enormous** amount of heavy lifting."*

**What that means concretely, every single reply — not just the long ones:**

1. **He has not read this handoff, the tracker, the design-of-record, the ARCHITECTURE doc, or a line of `src/`.** Anything you name — a Step number, a file, an agent, a mechanism — is a **label he cannot resolve**. So *say what it IS in the same sentence*: not *"Step 30"* but *"Step 30 — the bug where an agent stops waking entirely once it's in ~370 rooms."*
2. **"He replied two minutes ago" is not evidence he still holds context.** Between your volleys he has been in other lanes with other vocabularies. **Treat every volley as a cold open** and re-anchor in a sentence or two before any detail. It costs almost nothing and it is the single highest-leverage thing you do.
3. **He is not an engineer.** Concept first in plain English, *then* the technical term if it's worth teaching. If he'd have to look a word up to answer you, rewrite the sentence.

**The failure this closes is not laziness — it is the opposite.** It fires hardest on your *most thorough* replies, because deep in a lane the shorthand feels like precision. It isn't; to him it's noise. *(Standing order, Justin 2026-08-06. This section is his; do not trim it.)*

**⚖️ HIS OWN CALIBRATION, same day, after ACf overdid it — read this or you will swing too far:** *"Thank you for re-anchoring fully — I don't need **quite** that much re-anchoring, but I appreciate the effort — just give me little handholds and avoid contentless things like 'your three-things reminder' where I have to scroll up to remember what the hell you're talking about."* ⇒ **The ask is not volume, it is that no reference is ever empty.** A three-sentence re-introduction to a lane he was in an hour ago is too much; *"your three-things reminder"* — a callback naming nothing — is the actual defect, because it costs him a scroll to decode. **The fix is a few words of content welded onto every reference**, not a paragraph in front of every reply: *"the reminder you asked for about not being an engineer and coming in cold"* costs six extra words and needs no scrolling. **Small handholds, everywhere; never a naked label.**

---

## §5c — THE EXACT `CLAUDE.md` WORDING JUSTIN APPROVED (owed item 4 — he ballot-said "5. a")

**The state he ruled on:** three docs give three different answers on committing. `_evryn-meta/CLAUDE.md` (always loaded) says *never commit without an explicit go, only exception is mailbox files*. The **orchestration protocol's merge rubric** says build-level code AC merges and commits **without waiting** ("don't wait") while source-of-truth docs still need his eyes — **that IS the standing "code yes, docs no" order, in force since 2026-06-02.** The **same protocol's escalation ladder** then lists *any commit/push* on the never-absorbable hard floor, contradicting the rubric a few hundred lines above it. His instinct that it had been promoted was correct; what's broken is that the always-loaded manual doesn't carry it.

**Add to `_evryn-meta/CLAUDE.md`, immediately after the existing commit-discipline rule (his approved text):**

> **The one standing exception: build-level code.** When DC, QC and AC agree and the change is below Justin's evaluation altitude, AC commits and merges it without waiting, then tells him in plain English (orchestration protocol, merge rubric — his standing authorization, 2026-06-02). **This never extends to source-of-truth docs** (Hub, spokes, ARCHITECTURE, BUILD, protocols, CLAUDE.md, LEARNINGS, AGENT_PATTERNS) — those stay uncommitted for his vet, and get **listed in chat at wrap-up** so he doesn't miss them. Deploys, pushes to shared branches, and migrations are never covered.

**Then separately propose** the protocol fix (rubric vs. hard floor) — his doc, his approval; surface it, don't silently resolve it. **Operating terms this session were exactly the wording above**, and he confirmed them.

---

## §6 — Gates, discipline, and the things that will bite you

- **🔒 Merge / deploy / migration-apply are each SEPARATELY Justin's explicit go. Prior authorizations never stack.** On `team-runtime` **ACf may merge directly** (standing, 2026-07-20 — the repo is uncontended). A lane AC never merges, pushes, deploys, or applies a migration.
- **Commit standing (Justin, 2026-08-05, overnight):** **code — yes; source-of-truth docs — NO**, leave them in SCM for his vet and **list them in chat at wrap-up so he doesn't miss them.** Brief/mailbox/handoff traffic is commit-exempt. **Confirm this is still his arrangement at each new session — it was set for one night.**
  - **A real data point on how he applies it (2026-08-07):** ACf held `ARCHITECTURE.md` uncommitted *because* of this rule, and he overrode it at close of week — *"commit architecture, and then push all of your changes"* — so nothing would survive only on the machine. ⇒ **the hold-for-vet rule is about not sneaking a source-of-truth change past him; it is NOT a reason to leave work undurable.** When a natural stopping point arrives with source-of-truth edits still held, **offer him the choice explicitly** (commit-and-vet-later vs. keep holding) instead of defaulting to the hold — he pushed twice this week, once via AC0, to close exactly that gap.
- **🔴 `git add <paths>` DOES NOT SCOPE THE COMMIT.** A bare `git commit` afterwards commits the **entire index**, including another agent's staging. **Put the pathspec on the commit** (`git commit -F - -- path/a path/b`); recover with `git reset --soft HEAD~1`, which restores the index exactly. *(ACf did this on 2026-08-05 and absorbed six of another lane's staged file-moves.)* Also: `git diff <file>` before committing a shared file — a by-path commit takes that file's whole working-tree state.
- **Spinning a lane AC:** the six-part tagged brief **VERBATIM** (orchestration protocol), AC variant = 2 substitutions, `#cascade-override` + a one-line reason (the target is a separate codebase **and** an agentic runtime, so the list must carry **both halves** — code *and* identity), every file line-spanned, **model-pinned `opus`**, plus the **recursive-propagation clause** and **receipts-up-the-chain attestation**. ⚠️ **Annotate the canary carve-out: `src/wakes/memory-writer.ts` is the one code file with no bottom canary** (it holds the string as a functional constant) — unannotated, it hands your subagent an unsatisfiable instruction.
- **✅ SETTLED 2026-08-06 — foreground is FINE; concurrency is what matters.** ACf passed `run_in_background: true` and the tool confirmed async launch, but the agents still rendered on Justin's screen. **His ruling: don't chase it.** *"I'd rather have the concurrency — I don't mind that I can see them, as long as you're being fastidious, and announcing loudly when it's you, with `#`."* ⇒ **Don't fight the display, and the `#` header discipline is what makes a wall of subagent output navigable for him.** (Two `Agent` calls in one message block genuinely run in parallel — proven twice.) ⚠️ **This ruling is about the DISPLAY, not about how many lanes to run. It does NOT authorize spinning both lanes at once — Justin's 2026-08-10 one-lane ruling (§0) governs that, and it is newer.** Read this bullet as: *when you do run things in parallel, don't waste effort trying to hide them.*
- **🔴 The `#` header is load-bearing, not cosmetic.** Justin scans a wall of subagent output hunting for where *you* take over. **Lead every relay with `# ACf's read — <topic>`** — H1, your name first. Sub-ACs cap at `##`; DC/QC/team agents may never write `'s read` at all. That level difference is the entire boundary marker.
- **Ping `#team-alerts` on EVERY response, prefix `ACf:`, and ping BEFORE composing the reply** so a stalled turn is cheap for him to nudge. Webhook in `evryn-team-workspace/.env`; pinger script is session-scoped — recreate it.
- **Never use `AskUserQuestion`** (it hangs).

---

## §7 — LESSONS — don't re-pay these

- **⭐ Reading cannot find an ABSENCE.** Every serious finding this week was a missing thing — a missing row limit, a missing alarm, a missing ability to read a file. **You only find them by breaking something and watching whether anything complains.** This is now the lane's standing method.
- **⭐ A review layer earns its cost only if it attacks DIFFERENTLY from the layer above.** Across two AC-under-AC runs, **every best catch came from BELOW the top agent, and both times the top agent had already checked the thing that was wrong and passed it.** The sharpest: a guard passed one lane's mutation and failed its reviewer's — **the difference was a trailing comment.** Never let a reviewer inherit the author's test inputs. *(In `AGENT_PATTERNS.md`.)*
- **⭐🔴 "I'll keep going with X" then stopping — IT FIRED THREE TIMES IN ONE SESSION (2026-08-06), and the trigger is now identified. Justin: *"that seems to be a **really** strong habit for you (it's happened with a lot of ACs)… we lose **way** too much time when you stop."*** The pull to end a turn is strongest right after a long, complete-*feeling* message — **and the specific trigger is a BALLOT.** All three stops came immediately after a numbered question list. A ballot is a *request addressed to Justin*, and a request manufactures a handoff expectation that silently swallows the stated intent to continue — so it fires on your most carefully-structured replies, not your sloppy ones. **⇒ THE ONLY FIX THAT HAS HELD IS ORDERING: do the work FIRST, report it in the PAST TENSE.** A message describing finished work cannot strand anything; a message *promising* work can. Mechanically: a turn ends when you emit text and make **no further tool call** — text alone does not end it, so you *can* answer inline and keep working. **Justin's own framing, which he asked be kept as an ongoing experiment:** *"if you can somehow convince yourself that the notes you leave me are **inline** notes to me, not the final 'response' — I think that'll help."* **He also authorized the fallback:** if you truly cannot continue after replying, *"we can wait until you've done stuff, before you update me"* — a delayed-but-complete update beats a promise you break. **This is NOT solved. Watch it and add what you learn.**
- **⭐ RE-EMBED THE REFERENCE — every single time, and it fired twice this session.** (i) A Slack ping said *"questions in chat"* when no questions had been written — a **dead pointer**, sending Justin somewhere empty. (ii) A ballot reply opened *"Approved — but I have NOT spun it, deliberately"* **without ever saying what "it" was.** Justin: *"if you're giving me responses to questions I asked, you **have** to give me context — I have no idea what this is referring to."* **He is coming in cold from another lane, every time, however recently he replied.** ⇒ **whenever you refer back to anything — a decision, a doc, a prior yes, a ballot item — carry a few words of what it *was* in the same sentence.** And never point him at something you haven't written yet.
- **Verify against the artifact, never the narrative.** A sub-AC's reported tip **lagged its real tip** by one commit — and that commit was unreviewed by its own QC. Check the ref, not the report.
- **This runtime punishes local reasoning almost perfectly** — behavior is half `src/` and half the identity files; defects live in seams several hops from the changed line.
- **`main` moves under a lane.** Rebase rather than merge-into, and use `git diff main...branch` (**three dots**) — two-dot renders anything `main` gained as a *deletion by your branch*, which reads as though the lane reverted someone's work.
- **A binary file defeats GNU `diff`, not just git** — when git says *Binary files differ*, your whole text-tooling layer is blind.
- **Never junction `node_modules` into a mutation sandbox** — removing the junction took the live worktree's `node_modules` with it. Give each sandbox its own install.

---

## §8 — Your reload manifest

**🔴 READ THIS FRAMING FIRST — Justin changed the load shape on 2026-08-10, and the reason matters more than the list.** Three consecutive ACf instances have died of context exhaustion *before their lanes built*. **The cause is not the subagents — it is THIS list.** The full cascade puts ~8,900 lines of `src/` + migrations + two protocols + the design-of-record into the coordinator, and a coordinator does not need the runtime in its head to *coordinate*. Justin's direction: **use the CONDUCTOR cascade** — *"so we can have a subagent version of you decide what you actually need to read, to do the work in front of you."*

**⇒ Load in this order, and STOP at the point where you can do the next task.**

1. **Light cascade:** `_evryn-meta/CLAUDE.md` (auto) · `_evryn-meta/docs/current-state.md` · the Hub.
2. **THIS FILE**, in full. Then **the tracker** (`evryn-team-runtime/docs/SPRINT-team-runtime-memory.md`, ~171 lines) — your primary status surface, and cheap.
3. **`_evryn-meta/docs/protocols/load-cascades/conductor-cascade.md` — READ IT BEFORE RELYING ON IT.** It is **not a lighter load**; it is the mechanism for getting runtime competence without holding the runtime — a scout subagent carries `src/**` and returns a map plus a targeted read-list, and **you still read the load-bearing files yourself.** If the scout's map comes back thin, **fall back to the full cascade and tell Justin.**
4. **Before spinning anything:** `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` (full — it exceeds one Read call's token cap, so page it in ~3 offset reads). **Before writing any doc:** `ac-writing-protocol.md`.
5. **ONLY WHAT THE TASK NEEDS, after that.** For *spinning a lane* you need almost none of the runtime — the lane AC loads it. You need file **inventory and line counts** (`wc -l`, not `Read`) to build its load list. **Do not read `src/**` to write a brief.**
6. **The design-of-record**, only when you are reviewing memory work rather than dispatching it: `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-team-runtime-memory-architecture.md` — §4.2/§4.4/§4.6 for Steps 4/5. Pages past one Read call.
7. **The full both-halves cascade** (`team-runtime-full-cascade.md`) is still the right load **when you are reviewing a returned diff**, not when you are dispatching one. **Verify HEAD first; never trust a hardcoded SHA; never reuse line counts from this file.**

⚠️ **The trap in the conductor approach, stated so you don't fall into it:** a scout's map is a *summary*, and CLAUDE.md is explicit that subagents summarize destructively. **A map is fine for "where does X live and what depends on it." It is NOT fine as a substitute for reading a file whose nuance you are about to make a decision on.** Use it to aim your reads, never to skip them.

---

## §9 — ⇒ START HERE

**⚠️ Read §0 first — it is the live state and the owed-items list. This is the ordering.**

1. **Load per §8** (the reload manifest), then **confirm `main`'s HEAD yourself** and re-derive every file's line count before spinning anything. Never hardcode a SHA; never reuse the line counts in this file.
2. **⚡ SPIN ONE LANE (§3) FIRST — `opus`-pinned. NOT both** — Justin's 2026-08-10 ruling; see the 🔴 block in §0 for why (he is measuring what a subagent actually costs its parent). **Their briefs are committed and current EXCEPT for the stale "a second lane is live" collision section — fix that before spinning.** **They have now been stopped-before-building three times for parent-context reasons; do not make it four.**
3. **Then the small owed items, in §0's order** — AC0's note, the merge-info-inline tracker rule, the `CLAUDE.md` commit-standing edit (already ballot-approved), the protocol-contradiction proposal, the Opus-5 code trip.
4. **Then work the tracker** — the ⬜/🔄 Steps, in the order the Gates and Justin's priorities imply. **Steps 4/5 (`ACfm`) are the critical path**; **Step 30 (`ACfw`) is the highest-severity open defect**; the dashboard pause-reads-as-kill lane is authorized and unspun.
5. **Tell Justin what you loaded and what you think you're doing, BEFORE you do it** — his standing spin instruction. Then run.

**⚠️ Budget your context deliberately from the first turn.** The previous two ACf instances both died before their lanes built. Assume the full both-halves load + two protocols + a couple of subagent round-trips is most of what you get. **If a task doesn't need the runtime loaded, don't load it.**

**Ping `#team-alerts` before composing your first substantive reply**, and lead it with `# ACf's read — …`.

---

## §10 — Three things that are true and easy to get wrong

- **Merged ≠ deployed.** Nine Steps are merged; **zero are deployed.** Never describe any of this as live.
- **Step 2's entire DB layer is UNVERIFIED-LIVE**, and so are the digest/composer halves of Step 12. They are logic- and unit-tested only, because a build trip never applies migrations. **Live proof waits on G1 + G3. Never report them live-proven.**
- **The runtime is dormant by construction and wakes nobody.** Autonomy switches on only after the Meta-Meeting. Nothing in this lane can affect the Evryn product or Mark's path.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
