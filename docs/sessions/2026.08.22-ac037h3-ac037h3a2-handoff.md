🔒 **HELD-SESSION-DOC** — the live marching orders for the ACP-lineage instance retiring `docs/working/`; a fresh instance spins from this file · **RETIRE UPON:** `docs/working/` no longer exists (`git ls-files docs/working/` returns nothing) · **OWNER:** ACP · **HELD SINCE:** 2026-08-22

# `AC0-37h3a2` — finish retiring `docs/working/`. Written 2026-08-22 by `AC0-37h3`.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. **STOP.**
>
> **Do not load. Do not resume. Do not finish the current task**, however nearly-done it looks. **Ping Justin in one line — *"I compacted. I am stopping."* — and do NOTHING else.** *(His ruling, 2026-08-22: **"I want you doing no work with a cooked brain."**)*
>
> **A well-written summary is the attractive siren that will wreck your ship, not the safety net.** The danger is not that it is bad — it is that it is GOOD, so it draws you onward. *(ADR-058.)*

> ### 📌 THIS DOC REPLACES `docs/working/2026.08.20-ac037g-ac037h-handoff.md` AS YOUR MARCHING ORDERS.
>
> **That file is 1,055 lines written across six instances, and reading it top-to-bottom makes you derive, re-derive and reconcile four separate times** — §2 says one thing, §7 overrides it, §8 overrides that, §9–12 override again. **Everything still true is carried here.**
>
> ⚠️ **It also carries at least one `VERIFIED AT SOURCE` label that is FALSE** *(see §6.4)*. **Go to it only under a §2.2 trigger, and treat its claims as leads.**

---

# §1 — THE JOB, AND WHERE IT STANDS

**Retire `docs/working/` into `docs/sessions/`.** *(Justin's design, 2026-08-19.)* **One folder, not two** — and the load-bearing half is not the merge: **session-doc references are NEVER repointed again.** Findability becomes a rule — `docs/sessions/`, or `docs/sessions/historical/YYYY.MM/` filed by the doc's own date, which is the first thing in its filename.

🔴 **THE TEST FOR WHAT STAYS, and it is much tighter than earlier lanes used:** *"The ONLY instance in which a working doc should sit here is if we had a session handoff that was supposed to go to a new session, but that session is still needing to re-spin from it."* ⚠️ **"Is anything referencing it?" is DEAD as a test. Do not resurrect it.**

**⭐ WHERE IT STANDS: 58 → 16. Forty-two files moved, all committed.** *(For four consecutive instances the honest line was "not one file has moved." That is over.)*

**Your job is the last 16, then three closing steps. You are NOT starting cold — every file already has a disposition in §3.**

---

# §2 — YOUR LOAD

## 2.1 · Load these now, in this order. ~1,900 lines.

**Widest lens inward, intended shape before current state.** *(`ac.md`, Context Discipline — it is a standing rule, not this list's preference.)*

1. **`_evryn-meta/.claude/agents/ac.md`** *(full — 832)* — your manual. **Nothing else makes you AC.**
2. **`_evryn-meta/docs/hub/roadmap.md`** *(full — 184)* — the Hub. Company truth.
3. **`_evryn-meta/docs/current-state.md`** *(full — 195)* — the snapshot, including its 📮 BULLETIN.
4. **THIS FILE** — ⚠️ **you have almost certainly read it already, because it is what you were pointed at. RE-READ §3 and §4 after `ac.md` lands**, because they mean different things once you hold the manual.
5. **The four protocols:** `ac-writing-protocol.md` *(118)* · `lock-protocol.md` *(134 — **read step 10 twice; it is the rule you are applying**)* · `doc-update-routing-protocol.md` *(119)* · `mailbox-protocol.md` *(214)*.

**Confirm every bottom canary.** ⚠️ **A line count here is a HYPOTHESIS — trust your own read and flag any delta.**

## 2.1a · Session-startup hygiene — ALREADY DONE 2026-08-22, do not redo

✅ **`.claude/settings.local.json`: absent** *(checked)*. ✅ **Repo-sync: all repos on their canonical branch, `_evryn-meta` clean on `main`.** ✅ **Worktrees: the canonical tree plus `_evryn-meta-acf15` on `acf15/nest-prep` — 🔴 that one is ACT's, do NOT reap it.** ✅ **`#dev-alerts` peek: Justin declined it.**

⚠️ **DO re-run the repo-sync check yourself if you are on a different machine, or if hours have passed** — it is cheap and it is the one that fails silently.

## 2.2 · 🔴 TRIGGERS — the moment one of these fires, LOAD THE FILE BEFORE YOU DECIDE.

> **Justin's instruction, 2026-08-22, and it is the reason this section exists rather than a bare list: *"if you don't have him load something right away, make sure that he knows that if he runs into xyz scenarios, running blind is MUCH more dangerous than loading a file."***
>
> 🔑 **The load above is small BECAUSE the derivation is already done — not because the job is light.** ⚠️ **The moment you are deciding something the manifest does not already answer, you are outside what that load covers.**

| The moment you… | Load this, in full, BEFORE deciding |
|---|---|
| **Are about to judge ANYTHING that touches product, at all** | 🔴 **DO NOT. Ask `AC0-37h3b`** — §5.1. **This is Justin's stipulation, not a preference.** |
| **Doubt a §3 disposition, or want to know what `ACx` actually checked** | **`2026.08.19-ac037f-acx-working-docs-sweep-lane.md`** *(384)* — per-file verdicts for all 55, plus its own honest-limits §13. ⚠️ **In `docs/working/` NOW; moves to `docs/sessions/historical/2026.08/` when you close this out.** *(Resolve it by the date rule rather than by a path I write here — which is the very rule this job exists to establish.)* |
| **Need the fuller DERIVATION behind a §3 disposition, or Justin's earlier rulings verbatim** | **`2026.08.20-ac037g-ac037h-handoff.md` §§9–12** *(same folder, same caveat)*. 🔴 **§§0–8 of that file are superseded — do not read them for instruction.** |
| **Need to know whether a working doc's content already landed somewhere** | `_evryn-meta/CHANGELOG.md` *(513)* and/or `evryn-backend/CHANGELOG.md` *(352)* — **the cheapest way to avoid redoing closed work** |
| **Hit a question of whether something SHIPPED** | `_evryn-meta/docs/deploy-log.md` *(37)* **plus** that Step's own status — **the authoritative pair.** A *"rides the next deploy"* note is intent, never proof |
| **Need to know if an owed item is already tracked** | `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` *(786)* — its 🔵 LIVE region is the open work |
| **Are about to write into ANY mailbox, or discharge an entry** | `mailbox-protocol.md` — **at the moment of use** |
| **Consider spinning ANY subagent** | 🔴 **`docs/protocols/ac-orchestration-protocol.md` (743) + the sub-doc for your target. MANDATORY — its core alone does not authorise a spin.** |
| **Are asked about the `#lock` or `#sweep` cadence** | that protocol, every time |

⛔ **You do NOT need, and should not load speculatively:** `technical-vision.md` · `ARCHITECTURE.md` · the Code Atlas · the dependency map · the BUILD doc. 🔑 **The last four are `AC0-37h3b`'s load, deliberately — and the split only works if you do not duplicate it.**

---

# §3 — THE SIXTEEN, AND WHAT EACH WAITS ON

**Grouped by BLOCKER, because the blocker is the actionable part.**

### 🅰 Blocked on `AC0-37h3b` — product-coupled, 6 files

⚠️ **Several LOOK ready to retire. Do not.** **This list was fixed in advance, while the deciding instance still held the whole corpus** — precisely because an under-loaded agent cannot reliably tell in the moment which questions are product-coupled.

- **`2026.08.11-ac0-ac0-handoff.md`** — 🔴 **THE ONE HARD COUPLING. B1's source (§5.11).** **B has written B1 into both destinations; they are UNCOMMITTED pending Justin's SCM vet.** ⇒ **KEEP HOLDING until B sends a `COMMITTED` entry.** ⭐ *(Risk is now low either way — the substance lives in two durable product docs, so this is no longer the only copy.)*
- `2026.07.15-ac3-step78-real-health-design.md` — **a REAL doc, not a session doc.** Its retirement condition reads *"once its content is rehomed,"* which `lock-protocol.md` step 10 names as the signature of a misfiled real doc. ⚠️ **The extraction is MOOT** — the rule and its reasoning already live in `atlas/07-safety.md` invariant 3 **and** in `liveness.ts`'s own comment. **It needs a HOME, not an extraction.**
- `2026.07.22-ac0-ac0-handoff.md` — **Soren's unsettled v0.3 batch 3.** See §4.6.
- `2026.07.22-ac0-ac2-fast-follow-brief.md` — **SPRINT Step 94 cites its §9** for a rebuild recipe owed at the `CLUSTERING_MODE` flip — **a FUTURE gate, so the citation is live, not historical.**
- `2026.08.04-ac0-ac5-silent-email-drop-brief.md` — did its two generalizable rules reach `LEARNINGS.md`?
- `2026.08.04-ac0-aca-atlas-refactor-brief.md` — the extraction lane.

### 🅱 The ACh P-queue — 3 files, and it is MUCH smaller than it looks

**`2026.06.30-ac0-ac3-identity-ops-brief.md`** · **`2026.08.04-ach-justin-housekeeping-recommendations.md`** · **`2026.08.06-ac0-ac0-handoff.md`** *(`current-state:183` names it the queue's home)*

**P2 was recorded as five unplaced items. It is ONE, plus a decision** — verified with a control 2026-08-22:

- **(ii)(iii)(iv) — the three QC patterns: ✅ ALREADY IN `qc.md`.** `AC0-37i` landed them 08-21; `ACx` grepped them absent on 08-19 and its verdict went stale.
- **(v) — the "clean up as you go" rule: present IN SUBSTANCE at `ac.md:474`.** **Recommendation to Justin: DECLINE the duplicate.** ⏳ **He had not answered when this was written — see §5.2a.**
- **(vi) — the line-count-drift lesson: WRITTEN into `ac-orchestration-protocol.md`, UNCOMMITTED, awaiting Justin's SCM vet.**

⚠️ **The other two files are held by the WHOLE queue, not just P2.** **Still open in it: P3** *(a `liveness.ts` comment repoint — `src/`, so product → B)*, **P4** *(a research doc went to `historical/` instead of the research folder, diverging from Justin's ruling — his call)*, **P5** *(the Gate-B suite's build half → product)*, **P6** — 🔴 **REVERSED 2026-08-22: that branch is NOT to be deleted, and the reasoning now lives in `docs/repo-inventory.md` → *Deliberately Preserved Branches*.** *(Three of its six SHAs are cited in committed docs; the WORK is fully landed, the CITATIONS are what is at risk.)* ⚠️ **`current-state.md` carries a one-line breadcrumb. Do not re-derive this — it has nearly been executed twice.** · **P8** *(Soren's batch — §4.6)*.

### 🅲 Held by a LIVE INSTRUCTION — 2 files

🔑 **This is the class the no-repointing rule does NOT cover, and the distinction is the whole point: a stale PATH is fine; a doc named as a REQUIRED PRE-READ is an instruction, and it must resolve.**

- **`2026.08.12-qc-sweep-findings.md`** — `product-full-cascade.md` names it a required pre-read. ⚠️ **Source-of-truth edit → Justin.**
- **`2026.07.21-ac0-ac3-cleanup-batch-brief.md`** — **SPRINT Step 50 routes work to it and that Step is still TODO.** Product → B.

### 🅳 GENUINELY STAY — 3 files, all bannered already, moving to `docs/sessions/`

`2026.07.19-ac-justin-restructure-napkin.md` *(the `classify.ts` extraction napkin — `current-state:64` holds it open as explicitly unsettled and to be re-derived)* · `2026.07.28-acp-ac0-desktop-permissions-fix.md` *(re-opened open question; the desktop still needs the `z.archive/` move and the hook re-armed)* · `2026.08.17-ac037b-acp38-marching-orders.md` *(ACP-38 not spun)*

⚠️ **Move these LAST, with the final commit.** **Two are pointed at from `current-state.md`'s "Recent handoff pointers"** — *"the single most consequential line in this file"* by its own account, wrong twice before. **Moving them early opens a window where the line that routes a fresh AC is wrong.**

### 🅴 Retire at close — 2 files

- **`2026.08.19-ac037f-acx-working-docs-sweep-lane.md`** ⚠️ **Per `doc-update-routing-protocol.md` change-type 8, exclusion 2: MOVE it, but NEVER repoint a path inside it.** A document whose value is that it captured a before-state stops carrying it the moment its paths are updated.
- **`2026.08.20-ac037g-ac037h-handoff.md`** — **the brief THIS document replaced.** It is not held; it retires with the folder. 🔴 **Before it goes, confirm nothing in §§9–12 is still un-carried** — that is the only reason it still has value.

**Count check: 6 + 3 + 2 + 3 + 2 = 16.** ✅

---

# §4 — HOW YOU OPERATE. Justin's rulings, 2026-08-21/22.

**4.1 · COMMITS.** ✅ **Your own session docs and mailbox messages: commit freely.** ✅ **Source-of-truth docs: make the edit and commit** — *provided you end the session with a **specific list of the paths you changed**, so he can find them in the SCM timeline. He vets after the fact.* 🔴 **THE EXCEPTION: anything that changes HOW AGENTS WORK gets a PING and his SCM VET *BEFORE* you commit** — `ac.md`, the protocols, the load cascades. ⚠️ **He runs other ACs today; a process change landed silently reaches instances that never agreed to it.**

**4.2 · 🔴 `Write` AND `Edit` HANG ON ANY `.claude/` PATH.** They stall on an authorization prompt **Justin is never notified about** — indefinitely, killing all forward movement, with nothing to notice. ✅ **Use bash. TESTED AND CONFIRMED 2026-08-22.** **Two things learned doing it:** team `MEMORY.md` files are **pure CRLF** *(append with `\r\n`)*, and **a naive append lands BELOW the truncation canary, destroying its purpose — INSERT ABOVE IT.**

**4.3 · EDITING SOMEONE ELSE'S DOC — `A → (true A)`.** *"You can't rewrite, exactly — you can just update so it's actually true, so that it now says **what it was already meaning to say**."* **Fix a path so it resolves; record that a condition the author named has been met.** ⛔ **Never re-scope, sharpen, or improve the claim.** ⚠️ **If the TRUE statement differs from the INTENDED one, that is a ROUTE, not an edit.**

**4.4 · 🔴 OTHER LINEAGES' DOCS ARE NEVER YOURS TO RETIRE.** **The top-level lineages are `ACP` (yours), `ACT`, and `ACv`** *(ACv short-lived)*. **Banner it for its owner to evaluate, message that owner's inbox, move on.** 🔑 *"They've been doing this culling process too, so all of their stuff is actually in-flight."* **You cannot see their worktrees or unspun briefs.** ✅ **All 34 are already moved and bannered; ACT and ACv have been notified.** *(Now also written into `lock-protocol.md` step 10.)*

**4.5 · WHEN YOU REMOVE WORDING, DO NOT ARGUE AGAINST IT.** **The reader never sees what you deleted**, so a clause defending the change re-introduces the wrong idea into their head. **Just write the new instruction.** ⚠️ **One carve-out — a REDIRECT, never an argument: when the old wording still exists somewhere the reader will meet it** *(e.g. pre-08-20 docs say "canary" and mean the heartbeat)*, **one pointer earns its place.**

**4.6 · SOREN'S BATCH 3 — do not act on Justin's approval of the sweep.** `ACh`'s ballot recommended sweeping it into `BUILD-EVRYN-v0.3.md` **and Justin approved that** — then `ACh` **deliberately did not do it**, because Soren's own text forbids exactly that, and *"my recommendation had been built on a summary, not the source."* **The record lives only in `soren/MEMORY.md:129-137`,** which also names the decision needed: *"if those items are still live design, they likely want a durable home that PRESERVES the unsettled framing (not a source-of-truth doc)."* ⇒ **Route the question; do not decide it.**

**4.7 · Two naming traps.** The `acp` in `2026.07.28-acp-ac0-desktop-permissions-fix.md` is a **lane**, not this lineage. **`ACf` IS what became `ACT`.**

**4.8 · Peer etiquette** *(Justin's, for everyone running today)*: **don't bug each other · other people's emergencies are not automatically yours** ⇒ **so make any request you send CHEAP TO ANSWER, or it will correctly be declined.**

---

# §5 — WHAT IS LIVE THE MOMENT YOU ARRIVE

## 5.1 · `AC0-37h3b` is running — your product consultant, and a PEER

**A separate instance, not a subagent** *(subagent resume is session-bound; as a sub it would have died with the last ACP and its ~350k product load would have to be re-paid)*. **Brief: `docs/sessions/2026.08.22-ac037h3-ac037h3b-product-extraction-brief.md`.**

**Channel PROVEN in both directions.** **You write to `docs/mailboxes/inbox-ac037h3b.md`; it writes to `inbox-acp.md`.** 🔴 **COMMITTING IS SENDING.** ⭐ **It has been told a fresh ACP may answer it and that this is planned — introduce yourself and carry on.**

- 🔑 **It is CONSULTING, not being tasked. It is product-loaded where you are not.** ⭐ **If it says your framing is wrong, it is probably right** — it has already corrected this lineage twice today.
- 📌 **Standing rule it relies on: if you edit its brief while it is running, tell it in its inbox WITH THE DIFF COMMAND.** **Silence means its loaded copy is trustworthy.**
- ⚠️ **The mailbox files are the ONLY surface you both write to unprompted.** **Commit mailbox traffic PROMPTLY**, and `git diff <file>` before committing one.

## 5.2 · Uncommitted, deliberately, awaiting Justin's SCM vet

| File | What |
|---|---|
| `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` | P2(vi) — the line-count-drift paragraph. **Process change → his vet before commit.** |
| `evryn-backend/docs/ARCHITECTURE.md` · `SPRINT-v0.2-optionals.md` | **B1, both halves.** 🔴 **B's work — do not touch, do not commit, do not `git add -A`.** |
| `docs/sessions/2026.08.22-ac037h3-ac037h3b-product-extraction-brief.md` | ⚠️ **B edits its own brief while it runs.** **If it shows modified, that is B working — leave it.** |

## 5.2a · 🔴 AWAITING JUSTIN, AND HE HAD NOT ANSWERED WHEN THIS WAS WRITTEN

✅ **THE EIGHT RETIREMENTS ARE SIGNED OFF** *(commit `2af917e`)* — **do not re-ask.** Justin reviewed the reasoning and answered *"otherwise, looks good,"* with exactly two carried notes:

- ✅ **`ac-mailbox.md`** — he asked whether the participants had posted their *release* or whether content was *trapped*. **Answered with evidence from the file itself: both ACT (08-19) and ACv-2 (08-20) posted explicit releases with "nothing owed."** It is a completed conversation. **Closed.**
- ⏳ **`2026.08.11-ac0-acm-loading-architecture-brief.md` — the ONE that is NOT signed off.** He caught that *"Lane A shipped"* and *"everything in this brief shipped"* are **different claims**, and only the first was verified. **With `AC0-37h3b` now.** ⚠️ **If B finds anything unshipped, pull the file back out of `historical/` — one `git mv`.**

✅ **P2(v) is ANSWERED: Justin said YES to declining the duplicate.** **ACh's P2 is fully closed.**

⏳ **Genuinely still with Justin: P4 and P6** — see §3 🅱, both now routed through B first. **P6 needs a push, which you are barred from.** 🔴 **And P6 has REVERSED — the branch is KEPT. See §3 🅱 and `docs/repo-inventory.md`.**

> ⚠️ **A note on this section itself, because it is the shape that wastes the most of Justin's time: an *"awaiting his answer"* line survives past the answer.** **The instance that wrote this section carried "awaiting sign-off" forward for an hour after he had given it, and put it in a successor's brief.** ⇒ **Before you re-ask him ANYTHING, check whether he already answered it.**

## 5.3 · Instruments

**Arm your own heartbeat and an inbox watcher at spin-up.** ⚠️ **NEVER inherit a task id — a `Monitor` dies with the session that armed it.** **Ask Justin the two separate questions** *(read the inbox? · arm a watcher?)*. **Anchor on `$HOME`; emit a proof-of-fire line BEFORE the loop; fire on COMMIT, using `git log --all`.**

---

# §6 — TRAPS. Every one of these cost real time TODAY.

**6.1 · 🔴 `git grep` does NOT accept `--binary-files=text`** — that is GNU `grep`'s flag; `git grep` uses **`-a`**. **A scan with the wrong flag errors to stderr and emits nothing, which reads exactly like a clean all-clear.** ⇒ **A 30-lookup sweep returned a false clean bill of health; re-run correctly it found ELEVEN live referrers.** 🔑 **Caught ONLY by pairing it with a KNOWN-TRUE control in the same run. Always do that.**

**6.2 · Bash `grep` ABORTS on some reads here** — not an error, not empty: aborted, printing exactly like a clean result. **When an ABSENCE matters, read the matching lines with `node`.** **`grep -P` is separately broken** *(use `awk`)*. **A filesystem-wide scan double-counts because a worktree is a full checkout** — `git grep` per repo.

**6.3 · A lineage regex matches `refACTor`.** **Select other-lineage files by EXPLICIT LIST, never a pattern.**

**6.4 · 🔴 A `VERIFIED AT SOURCE` LABEL IS A CLAIM ABOUT AN INSTRUMENT — and those get inherited unexamined.** **Live specimen:** the old brief states *"`SPRINT-V0.2-HARDENING.md:620` is a redirect stub carrying no content,"* stamped VERIFIED. **It is a 12-line entry carrying the full original Step 53 body.** ⚠️ **The instance that propagated it HAD THAT FILE OPEN and never put the two side by side.** ⇒ **Treat every such label in the old brief as UNVERIFIED unless it names WHO ran WHAT.**

**6.5 · A markdown-emphasis checker cannot tell a glob from a bold marker.** `identity/**` reads as unbalanced `**`. **Look at the line before believing the flag.**

**6.6 · `SPRINT-V0.2-HARDENING.md` writes steps as `93.` while every other doc says *"Step 93"*** — so `grep "Step 93"` returns nothing for a Step plainly there. **Six false losses so far.**

**6.7 · `npm run typecheck` CANNOT PASS in `evryn-backend`** — `marked` declared, absent from `node_modules` *(**SPRINT Step 118**)*. **It predates you and fails in the direction that looks like your own change's fault.**

---

# §7 — THE THREE CLOSING STEPS, IN THIS ORDER

**1.** **Clear the 16.**

**2.** 🔴 **THEN, IN ONE COMMIT: the announcement into `current-state.md` + delete `docs/working/` + the changelog entry.** **The announcement CANNOT land early** — announcing a retirement that has not happened puts a lie in the file every agent loads first, which is the exact class this series spent two days cleaning up. ⚠️ **Fix `current-state.md`'s two "Recent handoff pointers" paths in this same commit.**

> ### 📋 THE APPROVED ANNOUNCEMENT, CARRIED HERE ON PURPOSE
>
> 🔴 **It used to live only in the brief that gets DELETED by this very commit — a trap I nearly left you.** **Justin approved this text; paste it at the top of `docs/current-state.md`.**
>
> ```
> > # 📁 `docs/working/` IS RETIRED. EVERYTHING IS IN `docs/sessions/` NOW.
> >
> > **One folder, not two.** Working docs and session docs were always the same thing, so they now live in one place.
> >
> > ### 🔎 CAN'T FIND A DOC? You do not need to search, and nobody needs to fix the link.
> >
> > **Every working or session doc ever written is in exactly one of two places:**
> > - **`docs/sessions/`** — if it is still live.
> > - **`docs/sessions/historical/YYYY.MM/`** — if it is retired, **filed by the doc's own date**, which is the first thing in its filename.
> >
> > **So a path like `docs/working/2026.07.15-ac3-step78-real-health-design.md` resolves in one step: it is a 2026-07 doc, so look in `docs/sessions/historical/2026.07/`.**
> >
> > ### 🔴 THE RULE THAT MATTERS MOST: DO NOT FIX REFERENCES TO SESSION DOCS.
> >
> > **A stale path to a session doc is not a bug, and NOBODY GOES LOOKING FOR THEM.** The date in the filename plus the two locations above make it findable in one step, forever. **Sweeping for them is make-work, and the make-work was bad enough to have consumed whole sessions.**
> >
> > ⭐ **But if you happen to be in a doc and see a session path that has moved, fix it.** **What is abolished is the OBLIGATION TO SWEEP — not the permission to fix what is already under your nose.**
> >
> > ⚠️ **This does NOT extend to source-of-truth docs** — protocols, manuals, ADRs, ARCHITECTURE, the Hub, sprint docs. **Those still get their references fixed when they move**, because they are cited as authority rather than as history.
> ```

**3.** **One line into ACP-38's marching orders** so he learns the folder is gone **as a settled sentence, never a task.**

---

# §8 — STILL OPEN, AND NOT YOURS TO ABSORB

- 🔴 **`qc.md` names `docs/ac-to-qc.md` in FOUR places** *(lines 95, 509, 522, 530)* **as her brief source and "primary work surface."** **That repo is retired and the mailbox model died 2026-08-12** — so a spawned QC is told to read a brief from a path that does not exist where she runs. ⚠️ **`AC0-37g` fixed the OUTPUT side on 08-20; this is the INPUT side, still open.** **Possibly the finding ACv is holding.** ⇒ **Source-of-truth → Justin.**
- **`docs/mailboxes/inbox-mira.md` was created 2026-08-22 by `AC0-37h3b`** to route B1's identity half. ⚠️ **Nobody is known to be watching it.** **Flagged to Justin.**
- **§6 items 1 and 2 stay with this lineage and are NOT started:** the **auto-load audit** *(how much of what an AC auto-loads should be a protocol or skill pulled at its moment — and **the deliverable is the TRIGGER that re-loads it, not the cut**)*, and the **naming cutover's estate-wide cutoff.** ⚠️ **Items 3–7 were closed by `AC0-37i` on 08-21.**
- **Open questions Justin has not ruled on:** the foreground-vs-background default now rests on nothing *(background renders identically to foreground)* · the hold-banner aging window is untested · `conductor-cascade.md:51` still tells a spun scout to load `CLAUDE.md`, which auto-injects.

---

# §9 — HONEST LIMITS OF WHAT YOU ARE INHERITING

- **What actually moved: 34 other-lineage docs to `docs/sessions/` with owner banners, and 8 ACP docs retired to `historical/2026.08/`.** **The dispositions rest on `ACx`'s per-file work plus a corrected referrer scan — NOT on reading all 42 in full.** ⚠️ **`ACx`'s own §13 admits it read headers and residue sections rather than bodies for the ten it retired, and held two of 45 `src/` files.** ⇒ **Execute a verdict you have CONFIRMED; treat an unconfirmed one as a LEAD.** 🔴 **Do NOT rest this on "ACx was fully loaded" — Justin believed that, and it is not what ACx says about itself.**
- ⚠️ **The 34 were selected by an EXPLICIT LIST and moved by script, then verified file-by-file afterwards** *(all present, all bannered, none left behind)*. **The first run of that script failed completely and silently — a bash `/tmp` path that node reads as `C:\tmp` — and it was the independent verification, not the exit code, that caught it.**
- **Cost if a disposition is wrong: DISCOVERABILITY, not loss.** Every file is one `git mv` away, nothing was deleted, and under the no-repointing rule a wrong retirement breaks no reference.
- **No product doc was ever loaded by this lineage today.** **Every product claim in this brief is a LEAD.** **That is what B is for.**
- **Nothing is deployed. No `src/` file was touched. `evryn-backend` was modified only by B, and only in docs.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
