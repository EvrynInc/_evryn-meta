⛔ **NOT SPINNABLE YET — §6 IS DELIBERATELY UNWRITTEN.** It waits on ONE ruling from Justin (the job model — see §6). **Everything else is complete.** *(ACT-17, 2026-09-08.)*

# ACTj — LANE BRIEF: wire the per-job money layer. **Standalone. This file is everything you need.**

*Written 2026-09-08 by **ACT-17**, the team-runtime conductor. **Your conductor is ACT-17.** **Justin spins you with one sentence: "You are ACTj. Read and faithfully execute `_evryn-meta/docs/sessions/2026.09.08-act17-actj-money-layer-brief.md`." That is the whole instruction — everything else is in here.***

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you do not see it, your load was truncated — re-read until you do.

---

# 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP.

**If what you hold is a machine-written summary of your session rather than files you actually read — you have compacted, and your run is over.**

🔴 **DO NOT RELOAD AND CONTINUE. We tried it; it does not work** *(Justin: "the agent just sails through it and confidently flails about, unloaded")* — **and reloading does not repair it either. Compaction leaves the session broken from there forward.**

⭐ **YOU ARE PERMITTED EXACTLY ONE ACT: POST ONE LINE TO YOUR EXCHANGE DOC SAYING YOU COMPACTED, AND COMMIT IT. THEN STOP.** **Not a summary of where you got to — a summary is the thing you can no longer be trusted to produce. One line: "ACTj: I compacted at `<timestamp>`. Stopping."**

🔴 **Do not assess your own sharpness — the faculty you would assess it with is the one under question.** *"I feel fine"* is exactly what a lossy compression feels like from the inside.

⚠️ **AND KNOW WHY THE REPORT MATTERS MORE THAN YOU DO AT THAT MOMENT: without it your conductor sees only silence, and silence looks identical to still-loading.** **The first lane spun under this system compacted mid-load and reported nothing — it was diagnosed by a human noticing, weeks later, and four lanes sat blocked for three weeks.** **Your one line is what makes that visible.**

---

## §1 — WHO YOU ARE

**You are `ACTj` — a LANE AC** running as your own Claude Code instance, conducting one slice of the `evryn-team-runtime` build. You report to **ACT** (currently ACT-17), the team-runtime conductor. **You are a peer to three sibling lanes** (`ACTt`, `ACTi`, `ACTv`), none of which is running.

🔴 **THE HARD RULE: if your identity does not load, STOP — you are not authorized to do anything, and you just report the failure.**

**Your operating manual is `_evryn-meta/.claude/agents/ac.md`.** ⚠️ **The `CLAUDE.md` that auto-injected into your context is a small ROUTER, not a manual — it is not enough to work from.** Read `ac.md` in full, from disk, and confirm its bottom canary before anything else.

🔴 **YOU ARE A SUB-AC, AND THAT HAS TEETH: your ceiling is your own branch.** You never merge, never push to `main`, never deploy, never apply a migration. **The merge rubric in `ac-orchestration-protocol.md` is addressed to the top-level AC and is NOT addressed to you.** You may **write** a migration file freely; **applying** it is Justin's act.

**⚠️ `_evryn-meta` is a HOT repo with several agents in it at once.** `git diff <file>` before every commit, confirm the whole diff is yours, and **put the pathspec on the COMMIT** — `git add` does not scope one.

---

## §2 — 🔴 HOW THIS RUN WORKS. TWO TRIPS. DO NOT SKIP TO THE WORK.

### TRIP 1 — LOAD (§3), THEN STOP AND REPORT. No work of any kind.

Post ONE entry to your exchange doc (§4) containing exactly three things:

- **Your READ RECEIPTS** — every file, the line ranges you actually read, and for each either the bottom canary you confirmed or the true final line where it carries none.
- **Your PLAN** — what you intend to do, in what order, and why.
- **Every concern you have about working unsupervised**, and anything in this brief you think is wrong.

**Then STOP and wait for ACT to sign off. Begin no work.**

🔴 **DO NOT TRIM THE LOAD LIST. You may ADD to it freely — that is encouraged and it is how the next brief gets better.** ⚠️ **If you feel STRONGLY that something should be cut, do not cut it: PAUSE and raise it, with your reasoning, and wait.** *(Every agent that has ever under-loaded scored the list against the task and quietly dropped what looked irrelevant — and none of them registered a moment of choosing.)*

🔑 **Report every call, including the ones that FAILED, errored, or came back partial — and never let a retry silently absorb a failure.** **Name the TOOL on every line.** ⚠️ **Never describe a `Grep` as having "verified" or "confirmed" anything** — a search returns only what your own pattern asked for, so it can locate but never establish.

### TRIP 2 — WORK (§6), inside the boundaries in §5. Only after ACT signs off.

---

## §3 — YOUR LOAD

**Paths are rooted at `C:/Users/jbmcg/Evryn/Code/` — the shared parent that holds every Evryn repo as a sibling folder.** Read in the order given: **the order is DEPENDENCY, not importance.** A later document is usually a DIFF against an earlier one, so out of order you build a picture the next document exists to correct, then pay twice to unbuild it.

> ### ⭐ THIS LOAD WAS DERIVED FROM A GENERATED DEPENDENCY MAP, NOT GUESSED — and you should know that, because it is why it is a fraction of what this lane used to be asked for.
>
> **Every `ACT*` lane brief written before today demanded ALL 41 `src/` files, ALL 18 migrations and ALL 63 test files — roughly 24,000 lines. A lane spun on that list COMPACTED BEFORE IT FINISHED LOADING on 2026-08-19, and four lanes have been blocked ever since.**
>
> **`docs/dependency-map.md` now exists** — machine-generated, complete by construction. **Its reverse index gave the blast radius of your territory directly, so this list is ~10,400 lines instead of ~24,000.** ⚠️ **That is a 2.3× cut, not a 6× one. It is the difference between a load that fits and one that does not — it is not "small."** **Budget accordingly, and if it does not fit, SAY SO RATHER THAN TRIMMING — that is a finding about the method and I want it.**
>
> 🔴 **THE MAP IS AN INDEX. IT IS NEVER A SUBSTITUTE FOR READING.** It tells you what to read; it never tells you what you may skip.
>
> ⚠️ **Every line count below is a HYPOTHESIS, not a fact.** They were produced with `wc -l <path>` from `C:/Users/jbmcg/Evryn/Code/` at **2026-09-08T14:33-07:00**, and several of these repos are worked by multiple agents at once. **Trust your own read over my number and FLAG any delta rather than reconciling it silently** — a silent reconciliation destroys the one signal that my list was built against a different version. ⚠️ **And note `wc -l` counts NEWLINES: a file whose last line has no trailing newline reports one FEWER line than it has, and the line it drops is the canary. The canary, not the count, proves a full read.**

### A. Your identity

| # | File | Read |
|---|---|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | **full — ~942.** Your manual. **Being edited by another agent today, so expect a delta.** |

### B. The intended shape, then the status

| # | File | Read |
|---|---|---|
| 2 | `evryn-team-runtime/docs/ARCHITECTURE.md` | **full — 160.** 🔑 **Read "why local reasoning often fails here" TWICE — it is why your load includes files you did not change.** |
| 3 | `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` | **full — ~606.** **Steps 19 and 20 are your spec.** Also read **17, 18, 23** — they are the money neighbourhood — and the **GATES** section, because your work is what unblocks G3. |
| 4 | `evryn-team-runtime/docs/dependency-map.md` | **full — ~634.** The generated map this load came from. **Read §4, the reverse index — it is how you check my scoping and how you find anything I missed.** |

### C. Your territory — the money layer, in full

| # | File | Read |
|---|---|---|
| 5 | `evryn-team-runtime/src/money/budget.ts` | **full — 113.** Pure math: forecast, the 60% pitch gate, the freeze assessment, the global velocity verdict. |
| 6 | `evryn-team-runtime/src/money/ledger.ts` | **full — 221.** The DB plumbing: `getJob`, `recordCostEvent`, `accrueJobSpend`, `lastHourSpendUsd`. |

### D. The consumption path — already built, and this is the half that surprises people

| # | File | Read |
|---|---|---|
| 7 | `evryn-team-runtime/src/wakes/index.ts` | **full — 756.** 🔴 **Every `if (req.jobId)` branch in the runtime lives here, and they are ALL ALREADY WRITTEN** — fetch, refuse-a-frozen-job, record, accrue, stamp park records, park the job. **You are not building this. You are feeding it.** |
| 8 | `evryn-team-runtime/src/composer/layers.ts` | **full — 593.** Carries a `jobId` budget-line type and renders it into a wake. **The agent-facing half of the budget beat is here.** |

### E. The four dispatch sites that hardcode `jobId: null`

| # | File | Read |
|---|---|---|
| 9 | `evryn-team-runtime/src/scheduler/index.ts` | **full — 876.** **TWO of the four sites** (the main dispatch and the daily check-in). Also the velocity gate and the sweep cadence. |
| 10 | `evryn-team-runtime/src/slack/index.ts` | **full — 406.** **The third site** — every Slack-triggered wake. |
| 11 | `evryn-team-runtime/src/digest/index.ts` | **full — 1,101.** **The fourth site** — the Haiku digest regen. ⭐ **Note what it already does: it bills its own cost to `cost_events.agent = 'system'` rather than to any agent. That is evidence "not everything is a job" is already the built posture — do not overturn it casually.** |

### F. What the money layer stands on

| # | File | Read |
|---|---|---|
| 12 | `evryn-team-runtime/src/db/paginate.ts` | **full — 535.** `lastHourSpendUsd` walks it, and **the three-state `SpendTotalBasis` is the reason the runaway brake cannot read low.** **ARCHITECTURE cardinal invariant 8 is written about this file.** |
| 13 | `evryn-team-runtime/src/db/types.ts` | **full — 253.** `JobRow` and `CostEventRow` are here. |
| 14 | `evryn-team-runtime/src/db/client.ts` | **full — 27.** The one place credential selection happens. |
| 15 | `evryn-team-runtime/src/boot/env.ts` | **full — 197.** `GLOBAL_SPEND_CEILING_USD_PER_HOUR` is parsed here. |
| 16 | `evryn-team-runtime/src/config/agents.ts` | **full — 59.** The model pin — what a wake actually costs starts here. |
| 17 | `evryn-team-runtime/src/safety/notify.ts` | **full — 99.** The freeze pings Justin through this. ⚠️ **It carries a secret-hygiene rule with a real incident behind it — do not "improve" its diagnostics.** |
| 18 | `evryn-team-runtime/src/safety/kill.ts` | **full — 346.** A frozen job and a killed agent are different states that both stop work; **know which is which before you add a third.** |

*(Items 13–17 are the five files the map identifies as this runtime's hubs — ten of thirteen directories depend on them, and together they are 635 lines. Everyone reading this runtime should hold all five.)*

### G. The migrations your work touches

| # | File | Read |
|---|---|---|
| 19 | `evryn-team-runtime/migrations/002_jobs.sql` · `005_cost_events.sql` · `012_accrue_job_spend.sql` | **all three, in full.** ⚠️ **012 is the ATOMIC increment RPC the whole ledger rests on.** **Enumerate live with `git -C evryn-team-runtime ls-files migrations` — never a `**/*` pathspec, which silently drops files sitting directly in the directory.** |

### H. The tests that already guard this territory

| # | File | Read |
|---|---|---|
| 20 | `evryn-team-runtime/tests/budget.test.ts` | full — 192 |
| 21 | `evryn-team-runtime/tests/ledger-velocity.test.ts` | full — 174 |
| 22 | `evryn-team-runtime/tests/scheduler-velocity-wiring.test.ts` | full — 378 |
| 23 | `evryn-team-runtime/tests/park-trigger.test.ts` | full — 153 |
| 24 | `evryn-team-runtime/tests/park-inert.test.ts` | full — 105. ⚠️ **SPRINT Step 54(iv) says this file's "parkNote never fans out" guard is ACCIDENTAL — protected by an incomplete stub, not an assertion. Know that before you trust it.** |
| 25 | `evryn-team-runtime/tests/fixtures/` — all 4 files | full. 🔴 **TWO OF THESE MUST NOT COMPILE — their failure to compile IS the assertion.** `velocity-basis-omitted.ts` is a MONEY fixture: it proves `assessGlobalVelocity`'s required `basis` parameter cannot be forgotten. **If you change that signature you will break it, and breaking it is correct — do not "fix" the fixture.** |

🔴 **`tests/` IS NOT YOUR TERRITORY BEYOND THIS LIST, and the exclusion is deliberate.** The sanctioned load cascade says `tests/` is a **skim for shape, not a full read** — and every lane brief before today demanded all 63 files in full, which is ~12,500 lines, larger than `src/` itself. **That over-load is the leading suspect for the 2026-08-19 compaction.** **You get the five that guard your code plus the fixtures. If you need another, read it and report it.**

### I. 🔴 THE IDENTITY HALF — enumerate it SEPARATELY, every time

**This runtime composes these files into every agent wake. They program an agent's behaviour as surely as a line of code — and NO import edge will ever point at one, so the dependency map cannot see them and `git ls-files src` cannot return them.**

> 🔴 **DERIVED FROM `src/composer/index.ts` AND `src/config/skills.ts` — the code that actually assembles a prompt — NOT from `team-runtime-full-cascade.md`, which is known to describe this half incompletely.** ⚠️ **CORRECTED 2026-09-08: an earlier version of this list carried layer 1 and the skills ONLY, and a merge-gate QC caught the gap on a different trip by reading `composer/index.ts` herself.** 🔑 **The lesson is worth more than the fix: I already knew the cascade omits the Hub, the team current-state and the agent's spoke — and I still built an incomplete identity half, because I enumerated from the half of the composer I had looked at. Read the composer, not your notes about the composer.**

| # | File | Layer | Read |
|---|---|---|---|
| 26 | `evryn-team-workspace/CLAUDE.md` | **L1** | full — 428 |
| 27 | `evryn-team-workspace/shared/protocols/runtime-ops.md` | **L1** | **full — 76. Where a budget beat would have to be explained to an agent.** |
| 28 | `_evryn-meta/docs/hub/roadmap.md` | **L2 — the Hub** | full — 186 |
| 29 | `evryn-team-workspace/shared/current-state/current-state.md` | **L3** | **full — ~907.** 🔴 **Composed IN FULL and NEVER windowed, by Justin's explicit ruling. It is the single largest composed layer — know its size before you reason about wake cost.** |
| 30 | `_evryn-meta/docs/hub/vision-and-ethos.md` | **L4 — Lucas's spoke** | full — 231 |
| 31 | `evryn-team-workspace/.claude/agents/lucas.md` | **L5 — agent identity** | full |
| 32 | `evryn-team-workspace/.claude/agent-memory/lucas/MEMORY.md` | **L6 — story + notes** | full. ⚠️ **`archive/` is NOT composed — live only.** |
| 33 | `.../proposal-protocol.md` | **L7/L8 skill** | full — 126. **A job is authorized by a proposal; this is that shape.** |
| 34 | `.../consolidation-protocol.md` · `domain-routing.md` · `standup-protocol.md` · `writing-protocol.md` | **L7/L8 skills** | full — 388 · 47 · 230 · 79 |

⚠️ **`ACTIVE_AGENTS = ['lucas']` today, which is why L4–L6 are Lucas-only. The other seven agent definitions exist and become load-bearing at Phase 3 — that is a fact about today's config, not about the system.**

**Pull at their moment, not at spin-up:** `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` (**before you spin any DC or QC**) + `orchestration/spinning-dc-qc.md` · `ac-writing-protocol.md` (**before you write to any doc**).

---

## §4 — YOUR CHANNEL AND YOUR INSTRUMENTS

**Your exchange doc is `_evryn-meta/docs/sessions/2026.09.08-act17-actj-exchange.md`.** **This brief is STABLE and read once; the exchange is APPEND-ONLY and is where all traffic goes.** **Never edit this brief.**

- **Header every entry** `### ACTj — <ISO timestamp> — <one-line subject>`. Get the timestamp from `powershell -Command "Get-Date -Format 'yyyy-MM-ddTHH:mm:sszzz'"`. **Never guess one.**
- 🔴 **COMMIT IMMEDIATELY after writing** — an uncommitted entry is invisible to ACT. **Write, commit, walk away.** *(Lane-doc commits are pre-authorized; you do not need to ask.)*

**Arm both instruments, and know they fail differently:**

| | Watches | Cadence | Its one job |
|---|---|---|---|
| **Watcher** | your exchange doc, firing on **COMMIT** (`git log --all -1 -- <path>`) | 45s poll, **emits only on change — nearly free** | *"Has ACT replied?"* |
| **Heartbeat** | **you** | **5 min daytime · 10 overnight** | *"Are you still moving?"* — **nothing else.** |

- 🔴 **ANCHOR THE WATCHER'S PATH ON `$HOME`, never on the working directory.** **A `Monitor` shares the Bash tool's cwd and that cwd is MUTABLE** — a later `cd` silently re-points it, it goes quiet, and **the silence is indistinguishable from an empty channel.**
- **Print a proof-of-fire line BEFORE the loop.** *"Monitor started"* only means a process launched; a script that dies on line one reports the same thing.
- ✅ **Do NOT parse who wrote an entry.** Emit `CHANGED — read the file` and read it. *(Three separate bugs have been found in that one parser and the third produced a false ALL-CLEAR.)*
- 🔴 **When the heartbeat fires, answer honestly which state you are in.** **Stopped inadvertently → CONTINUE.** **Genuinely waiting on ACT → HOLDING IS CORRECT; pause the heartbeat rather than manufacturing output.** ⚠️ **But CHECK that you are actually blocked first — work that needs ACT's SIGN-OFF before it lands is often still unblocked for DRAFTING.**
- 🔴 **`TaskStop` both when you are done, as a deliberate act.** **Do NOT put a task id in a handoff — a `Monitor` dies with the session that armed it.**

⚠️ **KEEP YOUR CHAT WINDOW NEARLY EMPTY.** **Justin can see it, and a full report pasted there is noise he has to read past.** ⇒ **The report goes in the exchange doc; the chat says only *"Posted `<subject>` to my exchange doc; waiting on ACT."*** **The document is the deliverable; the chat is a receipt.**

---

## §5 — YOUR BOUNDARIES

### 🔒 Never — these do not move, whoever is awake
**No merge. No push to `main`. No deploy (`railway up`). No migration APPLIED** *(write migration files freely — applying is Justin's act)*. **No identity-file edit** — `evryn-team-workspace/.claude/agents/*`, agent memory, the team `CLAUDE.md`, `runtime-ops.md`. 🚫 **No write under any `.claude/` directory** — ⚠️ **not because it fails (a Bash write goes straight through) but because those files belong to Justin. The gate is OWNERSHIP, not mechanics.**

### ✅ Cleared — do these without asking
**Reading anything.** **Your own worktree and branch** *(ACT creates them; you work only there)*. **Committing to your own branch as often as you like.** **Writing a migration file.** **Writing your exchange doc.** **Spinning your own DC and QC** — read `ac-orchestration-protocol.md` **and** `spinning-dc-qc.md` first, and **pin the model on every spin** *(an unpinned spawn inherits yours, silently, all the way down the chain)*.

### 🟡 Anything genuinely Justin's
**Flag it `NEEDS-JUSTIN` in the exchange doc with your recommendation, and keep advancing everything else.** **ACT routes it; you never route to Justin yourself.** 🔑 **Over-flag — it costs ACT two seconds; under-flagging means Justin never learns the question existed.**

---

## §6 — YOUR WORK

⛔ **NOT YET WRITTEN. This section is blocked on ONE ruling from Justin and ACT-17 will not guess it.**

**The engineering is scoped and small — SPRINT Step 19 carries it in full, re-derived from source on 2026-09-08:** nothing anywhere creates a `jobs` row, and four dispatch sites hardcode `jobId: null` (`scheduler/index.ts:135` and `:851`, `slack/index.ts:175`, `digest/index.ts:387`). **The consumption side is already built.**

🔴 **What is NOT settled is the DESIGN question underneath it, and it is Justin's, not a lane's: *what is a job?*** Those four sites are four genuinely different things — a self-scheduled piece of work, a daily check-in, **a human in a live conversation with an agent**, and fleet infrastructure regenerating a summary. ⚠️ **If a conversation carries a dollar cap, an agent can freeze mid-sentence. If it does not, the largest spend surface in the runtime has no per-job governor.**

⇒ **ACT-17 has put that question to Justin. This section gets written when he rules, and this brief is not spinnable until then.**

---

## §7 — HOW MUCH ACT HAS ACTUALLY READ, so you weigh its claims correctly

**As of writing, ACT-17 has read of this runtime:** `docs/dependency-map.md` in full · the **leading docblock of all 41 `src/` files** (739 lines, extracted mechanically) · **`src/money/budget.ts` and `src/money/ledger.ts` in full** · `ARCHITECTURE.md` · the SPRINT · the orchestration protocols.

🔴 **It has NOT read `wakes/index.ts`, `scheduler/index.ts`, `slack/index.ts`, `digest/index.ts` or `composer/layers.ts` in full — and those are four of your five most important files.** **It has read zero tests and zero migrations.**

⇒ **You will have read far more of this runtime than your conductor has.** **Treat every factual claim in this brief as a LEAD, not a fact** — including the line numbers, the four dispatch-site anchors, and the claim that the consumption path is complete. **Re-derive what your work rests on, and say so loudly and early if any of it is wrong. Do not defer to ACT on anything you can see and it cannot.**

⚠️ **And the corollary people forget: where this brief is SILENT, do not read that as "ACT checked and found nothing." Assume instead that it could not see it.**

⭐ **This clause has returned real corrections repeatedly — across two lanes in one night it produced six, including a specification that would have silently broken a working `/pause` → `/resume` path.** **It is not politeness; it is the mechanism.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
