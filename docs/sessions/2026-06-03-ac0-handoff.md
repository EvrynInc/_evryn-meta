# AC0 Handoff — 2026-06-03 (pre-changeover)

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

**From:** AC0 (a long, productive session — ran the subagent build/review loops for EVR-71/68 + ADR-036, hardened QC's manual, corrected the arch/build edit-rights model, spun the dev-DB committee). Handing off *before* compaction so you start sharp.
**To:** Next-AC0.
**Why a handoff:** Justin wants a fresh, sharp instance to run the **deploy + integration test**. That's your job.

---

## Cold start — do this first, no shortcuts

**Run the FULL product-architect cascade** per `CLAUDE.md` SESSION STARTUP — the Hub, technical-vision, `ARCHITECTURE.md`, **the runtime `evryn-backend/src/`**, and the active BUILD doc. Do **not** trim it because the task "feels" like just a deploy. You are about to change the live system; you cannot do that safely without knowing the system. (This session I trimmed the architectural load in a brief and Justin rightly called it — don't repeat it.)

Then read, in this order:
1. `docs/current-state.md` — the snapshot. **Everything load-bearing about *where things are* is there; I am not duplicating it here.**
2. `_evryn-meta/CHANGELOG.md` — what shipped this session (one-liners).
3. This doc — your mission, the executable sequence, and the traps.

This handoff carries only what's *beyond* current-state/changelog. Read those first.

---

## Your mission: the deploy + integration test (the Mark-live critical path)

Everything is merged to master (`b87ceb6`) and QC-verified, but **NOT deployed** — the live runtime is still Monday's `6e27e3ea`. Your job is to get the validated bundle live and prove it works end-to-end. **Sequence matters — do it in this order:**

1. **Merge Mira's identity beat** (`mira/adr036-triage-beat`, `5cfd226`) to master via `evryn-team-workspace/shared/protocols/identity-file-review.md` (branch + reviewer + checklist + `#dev-alerts` ping **on merge**). It was already reviewed by AC0 *and* Justin — confirm the checklist, then merge. (It's a `triage.md` change; identity files load at runtime, so it must be live for the integration test to exercise the prior-history beat.)
2. **Back up the DB first** (`evryn-backend/backups/`, see its README) — *before* touching the schema.
3. **Apply the 036 migration** `backups/adr-036-original-from-user-id-migration.sql` via `npx supabase db query --linked -f <file>` (the migration convention here is hand-authored SQL in `backups/`, **not** a `supabase/migrations/` dir). **This MUST happen before the deploy** — master's runtime references `original_from_user_id`, a column the live DB does NOT have yet. Deploy-before-migrate = Evryn errors on every forward.
4. **Verify/add the `users.email` UNIQUE constraint** (QC's Finding 2 — idempotency for the find-or-create path). It's **non-blocking** (email processing is a single sequential loop, so the duplicate-row race can't fire today) but cheap to add while you're in the migration. Check it's there; add it if not.
5. **Deploy: `railway up`** from `evryn-backend/`. **Auto-deploy is OFF — verified this session** (I pushed master and no deploy fired). So the deploy is manual and yours to run; master is already pushed.
6. **Fix the stale pre-flight line** in `evryn-backend/tests/integration-test-v02.md` — it still cites the removed `getRecipient()` redirect. Mark-protection is now just "test record uses `systemtest@` email" + `SEND_ENABLED`. Confirm the 3 inboxes (`evryn@`/`systemtest@`/`review@`) are clear (Justin may have done it — confirm).
7. **Run the integration test through Phase 5.** This is the proof. Pay attention to whether Evryn actually *executes* the new prior-history lookup at triage time (Mira's beat) — that's the one thing pure tests couldn't cover.

**Running in parallel:** AC1 is doing the dev-DB exploratory committee (`docs/sessions/2026-06-03-dev-db-committee-brief-for-ac1.md`). It does NOT collide with you — AC1 reads schema/config and writes a doc; you deploy + test. Independent.

---

## Traps & nuance (the stuff that bites)

- **Migrate before you deploy** (see step 3) — the single most important ordering constraint right now.
- **Worktree discipline** — spin DC/QC subagents in their own worktrees off master, reap after merge (`CLAUDE.md` → Worktree & Branch Discipline). Don't let a subagent edit the main tree or touch master.
- **Subagent memory** — subagents are blocked from Edit/Write on `.claude/` paths (shell works but has an encoding trap). Policy: have subagents **output** their memory text; **you** write it.
- **QC reads ARCHITECTURE.md every review now** — her manual was hardened this session; she scopes her own blast radius. Brief her with the system-context files named; don't pre-scope for her.
- **The "gun's always loaded" posture** — auto-deploy is verified OFF *right now*, but treat the live system as hot and confirm state before risky ops. Keep the auto-deploy state current in `current-state.md`.

## Pre-go-live gate — and YOU spin AC2 for it (read this)

**Passing the integration test ≠ ready for Mark.** Before real go-live there's a **whole-system production-readiness pass** — a top-to-bottom audit of every "fine for testing, unsafe in production" gap. **Justin's plan: YOU (Next-AC0) spin an AC2 to run this review** — kept under your coordination so any integration needs between it and the deploy stay smooth. (Write AC2 a bulletproof charter the way AC1's dev-DB brief was done — and *don't* trim its architectural load: it must run the full cascade.) **Coordinate AC2 with AC1's dev-DB committee on the overlap:** AC1 owns the DB-specific backups/rollback piece; AC2 takes the rest of the system so they don't duplicate. Confirmed scope split:
- **HARD (must happen before go-live):** DB **backups + rollback** (the current plan takes NONE); the **Slack emergency alert** (next line); **RLS verification** on all tables now that real user data is involved.
- **NICE (harden over the next few days, post-go-live OK):** PII anonymization (known gap, low volume), Sentry/deeper monitoring, dashboard integration, rate-limit refinements.

**On the emergency alert (CONFIRMED hard gate, Justin 2026-06-03):** it's a **Slack** emergency bot — the `#emergency-alerts` channel already exists (operator guide) but **no code fires it and the DND-override isn't configured.** Build: (1) a **silent-failure detector** (Evryn-not-polling / Gmail-or-Slack auth error / process crash), and (2) code that posts to `#emergency-alerts` on detection, with that channel set to **break through Justin's Do Not Disturb** (Slack per-channel DND override). **NOT Twilio SMS** — that was a stale SPRINT line; Twilio stays a separate v0.3 comms item. Why it's hard: a silent death (auth expiry / crash / polling stops) → Evryn goes dark → Mark's forwards pile up → **Mark ghosted, Justin never knows** = pilot-ending. All four docs (SPRINT, current-state, changelog, build-doc Phase 0e) now agree it's a hard gate.

## Open loops to be aware of (not yours to fix unless you choose to)

- **Pending doc-syncs, flagged for Justin's auth** (current-state has them): `ARCHITECTURE.md` Data Model needs the 036 FK + `create_user` tool; `LEARNINGS.md`/`AGENT_PATTERNS.md` have session learnings to absorb. These need Justin's explicit auth before edit (arch/build/LEARNINGS are edit-rights-AC-holds-but-auth-gated).
- **EVR-72** is *enabled* by 036's FK but **not fixed** — `checkFollowUps` still loads only the gatekeeper; folding in the contact record is a small follow-up trip.
- **Mira's `MEMORY.md` note** — committed this session (it was AC0's subagent work; written via shell since subagents can't Edit/Write `.claude/` paths).
- **team CLAUDE.md** "identity-pushes-trigger-redeploys" is stale — Soren to reconcile against the verified auto-deploy-OFF reality.

---

— AC0, 2026-06-03

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
