# AC02 Handoff — 2026-06-10

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

**From:** AC02 (a second top-level AC0 instance Justin spun up 2026-06-10, running its own show parallel to AC0, who was driving the Phase-6 live-fire integration test).
**To:** the next AC02 (or whichever AC picks this up).

**Why this exists:** compaction backstop + persistence of decisions/state that would otherwise live only in chat. (Justin's standing correction to AC02 this session: *stop saying "I persisted/flagged X" and then leaving it in chat — actually write it to a durable home.* This doc + the ADRs/edits below are that.)

---

## What SHIPPED this session — cost-capture (DEPLOYED + LIVE)

**Per-event LLM cost/token capture.** Built by DC, reviewed by AC + QC (GO), deployed 2026-06-10.
- **Deploy:** merged to `evryn-backend` main (`23f9858`) → prod pg_dump before → migration applied to **prod** + verified → pg_dump after → `railway up` (container healthy: Supabase/Slack/polling up, no errors).
- **What it does:** new `llm_usage` table, one row per `runEvrynQuery` (the single Anthropic choke point, `src/triage/classify.ts`). Columns: `created_at, pathway, scope_user_id, emailmgr_item_id, model, input_tokens, output_tokens, cache_read_tokens, cache_creation_tokens, total_cost_usd, num_turns, activity`. Captures EVERY spend event including no-message pathways (pass/ignore triage, proactive no-ops, operator chat) — attach cost to the *event*, not the message. Write is **best-effort** (never throws into the live pipeline). Migration: `evryn-backend/backups/add-llm-usage-table.sql` (RLS on, `service_role`-only grant, anon/authenticated REVOKEd, FKs `ON DELETE SET NULL`). Full design rationale: **ADR-038**.
- **Capture now, observe later.** The dashboard/observe layer is a fast-follow (SPRINT). Onboarding-cost approach (Justin's): normalize each user's spend timeline to their day-0 (first message) so cohorts align and the onboarding tail is visible empirically — a read-time/dashboard computation, no extra capture needed.

## Cleanups done
- **cron-architecture working doc archived** to `docs/sessions/historical/` (the rename got swept into AC0's commit `134714a` — benign; archive landed correctly). The three live runtime comments repointed to ARCHITECTURE.md (rode the cost deploy in `23f9858`).
- **`evryn-backend-team` worktree REAPED** — was fully merged into main + clean (a stale early-June DC/QC-wave leftover). Worktree removed + branch `team/2026-06-02` deleted.

## PARKED until the integration-test findings + Mira docs are STABLE
Justin's call: with the findings list still growing during the test, building against it is wasted motion. **Everything except cost-capture waited.** When findings stabilize (soon — AC0 had only S5/S6 left):
1. **FIRST: the doc-centralization pass (before M1).** Justin wants ONE definitive list, top-to-bottom, *before even M1* — because M1 itself might shift once everything's clear. Target shape: **SPRINT = everything to do THIS week before v0.3; BUILD = v0.3 scope; ARCH = how-the-system-works** (alignment TBD — make them agree, one definitive place for where each item goes). Pull from the Phase-6 findings (`docs/working/2026-06-09-phase6-findings.md`), the Mira dispatch (`docs/working/2026-06-09-phase6-mira-dispatch.md`), AC0's backlog notes (`docs/working/2026-06-10-ac02-backlog-notes.md`).
2. **Then M1** (silent-death detector — the go-live build gate, gates Mark *forwarding*, not the intro email).
3. **Quiet-hours redesign** (BUILD item 9f) — Justin wanted it working ~EOD; deferred behind the parking.
4. **Multi-inbox build** (ADR-039) — build the gatekeeper alias+reply-to mechanism AHEAD of Mark's return (not wait-and-see). Must runtime-doc + identity-doc (Mira "ask the address" beat).

## Decisions persisted this session
- **ADR-038** — per-event LLM cost-capture (the shipped design).
- **ADR-039** — multi-inbox gatekeeper identity (alias set + reply-to, narrow-now/robust-scoped, build-ahead).

## Open loops (carry forward)
- **`evryn-backend` main is merged + deployed locally (`23f9858`) but NOT pushed to origin.** Needs Justin's explicit "push" before remote matches reality. **Do this early** — origin/main is behind the deployed code.
- **Commit-gos pending** for this session's persistence edits (the BUILD/SPRINT/schema-reference/protocol edits + the ADRs + this handoff). They're written/staged but await Justin's commit-go (ADRs/handoff are approval-excluded content but still need a commit to land). The comment-repoint + cost-capture are already committed+merged+deployed.
- **QC pattern proposal** (2026-06-10) — *"a fail-safe side-write inside a re-throwing try block must verify both that the write can't reach the re-throw AND that its row-builder is inside the catch's reach"* (the NB2 issue QC caught on cost-capture). Promote into `evryn-quality/CLAUDE.md` "Patterns This Role Watches For" **on Justin's approval** (source-of-truth edit).
- **AC0-tree-slip:** during the cost build, DC briefly edited the shared `evryn-backend` main tree before isolating to its branch. Main verified clean + AC0's committed work intact; flagged to Justin so AC0 can confirm no uncommitted work of his was disturbed.
- **Cost dashboard view** = fast-follow (SPRINT week-of-6/8). Mirrors the `agent_api_calls`/`agent_daily_spend` shape so the existing agent dashboard can likely ingest it — assess effort when picking it up.
- **`NODE_ENV=development`** shows in the live startup log — that's the known dead/descriptive config (current-state); `SEND_ENABLED=true` is the real switch. Not a concern; on the dead-config-sweep list.

## Protocol/manual changes proposed this session (Justin-directed)
- **lock-protocol.md** — added a worktree-hygiene reap step (check for + reap stale/merged worktrees at #lock). CLAUDE.md already has the per-loop reap (Worktree & Branch Discipline).
- **ac-orchestration-protocol.md** — added a rule: don't dump DC/QC's full output to Justin; AC verifies, then gives AC's own distillation at CEO altitude.

## Working rhythm notes
- AC02 ran parallel to AC0's live test; deploys coordinated (AC0 paused the test, greenlit, AC02 deployed, AC0 re-baselined + resumed on the cost build).
- Ping Justin on `#team-alerts` every response (no AC↔AC Slack — route through Justin). Webhook `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`, Node `fetch`, ASCII only.
- All AC02 backend work was on dedicated worktrees (cleanup, cost) — never edited `main` directly; deploys gated by Justin + AC0.

— AC02, 2026-06-10

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
