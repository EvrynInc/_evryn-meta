# AC02 Handoff — 2026-06-10

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

**From:** AC02 — a *second concurrent top-level AC0* Justin ran 2026-06-10, parallel to AC0's Phase-6 live-fire test. *(Naming: "AC02" = the second top-level AC0 running at once; "AC1"/"AC2" are **sub**-ACs working under an AC0 — don't conflate.)*
**To:** the next AC02.

**This doc is deliberately light.** It carries only what `current-state.md` + `CHANGELOG.md` + the Full Startup Context Cascade *don't* already make obvious — the in-flight session nuances and open loops. The cascade tells you the system state; this tells you what's mid-air.

## ⚠️ FIRST THING when Justin spins you up — the quiet-hours redesign (it's blocking the test)

**Build the quiet-hours redesign before anything else** (ahead of the centralization/M1 sequence below). On 2026-06-10 evening, Justin hit quiet hours (18:00–08:00 PT) **mid-test**, and the current behavior **queues his `#evryn-approvals` pings until ~8am replay** — so he can't work with Evryn in the evening to finish Phase 6. He needs this shipped to resume + finish the test; he flagged it as the *very first* thing for you.

Spec: **`BUILD-EVRYN-MVP.md` resilience item 9f.** Shape: let regular pings post normally (Justin uses Slack DND for night-silence — it's his to toggle), time-gate **only** the stale-checker (`checkStaleItems`, `src/email/poll.ts`) to waking hours (~08:00–18:00 PT), optional 8am "anything pending?" sweep. Likely retires most of the `notify_queue` machinery and simplifies M1. **Spin DC→QC, then coordinate the deploy with Justin (and AC0 if he's mid-test).**

## Read first (beyond the cascade)
- `_evryn-meta/docs/current-state.md` — has the cost-capture deploy, live SHA `23f9858`, and the push-pending note.
- `ADR-038` (per-event cost-capture — **shipped**) + `ADR-039` (multi-inbox gatekeeper identity — **design, build parked**). The two decisions this session made.
- The `2026-06-10 (AC02 …)` CHANGELOG entry.

## Open loops — handle early
1. **`evryn-backend` main is committed but NOT pushed to origin.** Deploy `23f9858` is live; remote is behind. Needs Justin's explicit **"push."** Do this first when he greenlights.
2. **This #lock's doc edits await Justin's review + commit** (he reviews in source control before this *round* commits): `current-state`, `CHANGELOG`, `ARCHITECTURE` (operational-tables note), `AGENT_PATTERNS`, team `current-state`, and this lighter handoff rewrite. **Round 1 is already committed** (`_evryn-meta 163f6cc`, `evryn-backend 1d85714`): ADR-038/039, BUILD/SPRINT/schema-reference, the two protocol updates, and the (verbose) first handoff.
3. **QC pattern pending promotion** — the *"a fail-safe side-write inside a re-throwing try block must verify both that the write can't reach the re-throw AND that its row-builder is inside the catch's reach"* pattern QC surfaced on cost-capture → into `evryn-quality/CLAUDE.md` "Patterns This Role Watches For," **on Justin's approval** (source-of-truth edit; QC can't write her own).
4. **AC0-tree-slip** — DC briefly edited the shared backend main tree during the cost build before isolating to its branch; main verified clean + AC0's committed work intact, but AC0 should confirm no *uncommitted* work of his was disturbed. (Pattern logged to AGENT_PATTERNS — "verify a subagent's self-reported isolation.")
5. **Two CLAUDE.md-feedback candidates for Justin** (his domain + phrasing, not mine to add): (a) **persist immediately — don't claim-then-defer** (he caught AC02 three times this session saying "I persisted/flagged X" while it was still only in chat); (b) the **AC02-vs-sub-AC naming** convention.

## Strategic shape (what the cascade won't make obvious)
- **Everything except cost-capture is PARKED** until the Phase-6 findings + Mira docs stabilize (AC0 was on S5/S6 — nearly done). Building against a still-growing findings list is wasted motion (Justin's call).
- **Next, BEFORE M1: a full doc-centralization pass.** Justin wants **ONE definitive list, top-to-bottom**, so it's clear *before even M1* (M1 itself might shift once everything aligns). Target shape: **SPRINT = everything to do THIS week before v0.3; BUILD = v0.3 scope; ARCH = how-the-system-works** — make them agree, one definitive home per item. Pull from `docs/working/2026-06-09-phase6-findings.md`, `…-mira-dispatch.md`, `…2026-06-10-ac02-backlog-notes.md`. **Create the Linear tickets for the resulting items during this pass** (cost-dashboard, multi-inbox build, etc.) — not piecemeal before it.
- **Then M1** (silent-death detector — the one go-live build gate; gates Mark *forwarding*, not the intro email).
- **Then:** multi-inbox build (ADR-039 — build *ahead* of Mark's return, runtime-doc + identity-doc it). *(The **quiet-hours redesign** is the FIRST priority — see the top of this doc — ahead of all of this, because it's blocking Justin's evening testing right now.)*

## Cost-capture — where to look (it's live; you don't need to touch it)
- Code: `runEvrynQuery` (`src/triage/classify.ts`, the write) + `src/db/llm-usage.ts` (`recordLlmUsage`, best-effort/never-throws). Migration `backups/add-llm-usage-table.sql`; pre/post pg_dumps in `backups/` (untracked).
- **The fast-follow is the dashboard/observe view** (SPRINT week-of-6/8): sortable/filterable spend, per-user day-0 normalization for "onboarding cost." Mirrors the `agent_api_calls` shape so the existing agent dashboard can likely ingest it — assess effort when picking it up.
- Known limitation (ADR-038): errored queries don't write a cost row (rare; accepted for v0.2).

## Working rhythm
- Ran parallel to AC0; deploys coordinated *through Justin* (no AC↔AC channel — Slack reaches only Justin). Ping `#team-alerts` every response (`SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`, Node `fetch`, ASCII only).
- All backend work was on dedicated worktrees (now reaped); never edited `main` directly; deploys gated by Justin + AC0.

— AC02, 2026-06-10

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
