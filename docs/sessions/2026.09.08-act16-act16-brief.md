🔒 **HELD-SESSION-DOC** — ACT-lineage working brief · **RETIRE UPON:** superseded by ACT-16's own handoff at set-down · **OWNER:** ACT · **HELD SINCE:** 2026-09-08

# ACT-16 — working brief. **The live team-runtime lane doc.**

*Opened 2026-09-08 by **ACT-16**. Supersedes `2026.08.19-acf15-act16-handoff.md` **operationally** — that file stays live and readable as ACf-15's record and as the source of everything below; nothing in it is retired.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file:** ACT-16's live state — what is loaded, what is armed, what is owed, and what is open. **The test at every append: if this instance died right now, could a fresh one pick up from this file alone?** If no, this file is behind — fix it before anything else.

---

# 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP HERE.

**If what you hold is a machine-written summary of your session rather than files you actually read — you have compacted, and your run is over.**

🔴 **DO NOT RELOAD AND CONTINUE. We tried it; it does not work.** *(Justin, 2026-08-18: "the agent just sails through it and confidently flails about, unloaded.")* **An instruction to reload is a rule you must REMEMBER to obey — and remembering is precisely what compaction damaged.**

**Do these three, then stop:** ① stop working, mid-sentence if need be · ② tell Justin in one line that you compacted · ③ ask for a fresh re-spin.

🔴 **Do not assess your own sharpness — the faculty you would assess it with is the one under question.**

---

## §1 — WHERE THINGS STAND, 2026-09-08

- **`evryn-team-runtime` has had ZERO commits since 2026-08-19.** `main` = `555c45d`, clean, nothing unpushed. Four lane worktrees stand untouched at `7303ab5` (`acfi` · `acfj` · `acft` · `acfv`). The lane was paused while Justin was away; this is the first ACT since.
- **Nothing is deployed and the runtime wakes nobody.** Migrations 015–018 written, applied nowhere (Gate G1). The L9 boot guard makes that self-enforcing.
- **The four build lanes are BLOCKED** on the load problem measured 2026-08-19: a lane spun against the sanctioned full cascade compacted mid-load. `ACTa` (atlas + dependency map) was written as the unblocker and never spun.
- **`ACTd`'s job is DONE BY SOMEONE ELSE.** That lane was briefed to diagnose the `docs/working/` pile; ACP retired that folder entirely (58 → 0) on 2026-09-02. **The lane can be closed; its brief and exchange are retirement candidates.**

## §2 — 🔴 THE LOAD-FIT QUESTION, RE-DERIVED — do not inherit the 2026-08-19 conclusion without reading this

**The inherited conclusion is *"the full cascade does not fit in one lane instance."* That was measured on 2026-08-19 and has not been re-tested. It may be a fact about a context window rather than about the material.**

**Measured today (`git ls-files` + `wc -l`):**

| Half | Files | Lines |
|---|---|---|
| `src/` | 41 | 9,981 |
| `migrations/` | 18 | 1,277 |
| `tests/` | 63 | 12,481 |
| **Code half total** | **122** | **23,739** |
| Identity half (see §3) | ~25 | ~4,500 |

**At the protocol's own measured rate for a comparable TypeScript runtime (`evryn-backend`: ~20,100 lines ≈ ~334K tokens, i.e. ~16.6 tok/line), the code half is ≈ 394K tokens and the whole load is ≈ 520K+.**

⇒ **That does not fit a 200K window and does fit a 1M one.** ⚠️ **This is arithmetic, not a measurement — do not report it as one.** What is *known*: ACT-16 itself is running a 1M-token window. What is **NOT** known: what window a spawned subagent or a freshly-spun standalone lane gets. **That is the cheap, decisive test, and it reshapes the week if it comes back the good way.**

🔑 **Why this belongs at the top of this file: it is exactly the shape the router warns about — a clean load restores what your predecessor READ, not what your predecessor CONCLUDED.** ACf-15's caution was correct under its conditions. Re-derive it against today's.

## §3 — THE IDENTITY HALF, DERIVED FROM THE COMPOSER RATHER THAN FROM ANY DOC

**Read out of `src/composer/layers.ts` (lines 1–120) + `src/config/skills.ts` (full) + `src/config/agents.ts` (full), because the composer is the thing that actually defines the load.**

| Layer | File(s) | Lines |
|---|---|---|
| L1 team-common | `evryn-team-workspace/CLAUDE.md` + `shared/protocols/runtime-ops.md` | 428 + 76 |
| **L2 the Hub** | **`_evryn-meta/docs/hub/roadmap.md`** | **186** |
| L3 team current-state | `evryn-team-workspace/shared/current-state/current-state.md` | 808 |
| L4 spokes (per `config/agents.ts`) | for lucas: `_evryn-meta/docs/hub/vision-and-ethos.md` | — |
| L5 agent identity | `.claude/agents/*.md` × 8 | 1,173 |
| L6 story + notes | `.claude/agent-memory/*/MEMORY.md` × 8 (**live only — `archive/` is NOT composed**) | 1,806 |
| L7/L8 skills registry | proposal · writing · consolidation · standup · domain-routing protocols | 853 |

🔴 **THE FINDING: `ac-orchestration-protocol.md`'s identity-half table for this runtime is INCOMPLETE.** It lists *"agent definitions + memory + team manual + composed skills."* **The composer also composes the Hub (`roadmap.md`) as layer 2 and the team current-state as layer 3 — and the current-state is composed IN FULL, NEVER windowed, by Justin's explicit ruling** (`layers.ts:9-10`). ⇒ **The Hub is not background reading for this runtime; it is a composed layer, i.e. runtime.** The protocol's own row tells you to read the composer rather than trust it, which is what surfaced this. **Owed: a correction to that table.**

⚠️ `ACTIVE_AGENTS = ['lucas']` — only Lucas is configured. The other seven definitions exist and are Phase 3.

## §4 — INSTRUMENTS ARMED (2026-09-08)

| Instrument | Watches | Cadence |
|---|---|---|
| Inbox watcher | `docs/mailboxes/inbox-act.md`, fires on **commit** (`git log --all`, with a `fetch` each tick — ACP is on the other machine today) | 45s, emits only on change |
| Heartbeat | ACT-16 itself | 5 min (daytime) |

Both printed their proof-of-fire line. ⚠️ **A `Monitor` dies with the session — never hand a task id to a successor; hand them the PATHS above.**

## §5 — 📋 BACKLOG — captured from `inbox-act.md` before it was cleared

*Every entry below was a live mailbox item. The inbox is now empty, which is its correct resting state; the content lives here.*

| # | Item | Owner | State |
|---|---|---|---|
| B1 | **`runtime-ops.md:9` cites a SESSION DOC for a live work-list** — composer layer 1, read by all eight agents every wake, depending on a session doc. Justin: *"why are we citing a session doc? Where **should** it go?"* | ACT → Justin | **Already SPRINT Step 77.** Needs a fully-loaded sub-AC; Justin explicitly barred a light-loaded coordinator from drafting it. |
| B2 | **`current-state.md`'s Team-Runtime section is stale, and the bulletin correction is ITSELF partly stale** — see §6. | ACT | Open; needs Justin's go (source-of-truth doc). |
| B3 | **`evryn-team-runtime/docs/research/README.md:31`** points at `_evryn-meta/CLAUDE.md` "Documentation Approach", a section that moved to `.claude/agents/ac.md` in the 2026-08-18 router split. One-line repoint. | ACT | Open, trivial. |
| B4 | **Cross-runtime credential-in-argv ask from ACP** — answered 2026-09-08; see §7. | ACT → ACP | **ANSWERED**, reply written to `inbox-acp.md`. Needs a **push** to reach ACP (different machine). |
| B5 | **29 ACT-lineage session docs held since 2026-08-22** awaiting this lineage's evaluation — plus every older `ACf`/`ACT` doc. **See §8: Justin has scoped this as its own sub-lane.** | ACT | Deferred by Justin's instruction until the main show is underway. |
| B6 | **`ACTd` can be closed** — ACP did that lane's job. Its brief + exchange are retirement candidates. | ACT | Open, cheap. |
| B7 | 🔴 **`docs/protocols/load-cascades/team-runtime-full-cascade.md` is DEFECTIVE in three ways** — step 4 omits **ADR-055** (so a lane spun on it would not load the decision its own lane implements); its identity list omits the **Hub**, the **team current-state** and the **spoke** that `composer/layers.ts` actually composes; and its counts are stale (~36 `src` files, now 41; migrations 001–015, now 018). **Source-of-truth — needs Justin's go.** | ACT → Justin | Open. Found by the scout + the composer read. |
| B8 | **`ac-orchestration-protocol.md`'s identity-half table for this runtime is incomplete** — same omission as B7 (Hub + team current-state). ⚠️ **The table's own instruction is "read the composer to learn the real list rather than trusting this row" — which is what surfaced it, so the protocol caught its own gap.** Source-of-truth. | ACT → Justin | Open. |
| B9 | **Widen SPRINT Step 76** — it names `boot/workspace.ts:78` for *bare-name executable resolution* and is silent that the same line carries a credential in argv. Or file the residual as its own Step. | ACT | Open. |
| B10 | **Justin's report-cadence sharpening** — the conductor cascade's hard floor covers "a blocker on the thing he told you to do first"; his 2026-09-08 instruction is broader (**any** blocker comes early, so he can clear one thing and unblock the lane). Protocol edit. | ACT → Justin | Open. |
| B11 | **Incorporate into `spinning-an-ac.md`: every Admiral AND every Captain, once fully loaded, must decide what those below them need to load.** *(Justin, 2026-09-08 — "maybe it is [already there], but when you read that, I want you to incorporate it.")* **Do it when ready; leave in SCM for his vet.** ⚠️ Having now read that file: the *tier rule* and the *"assemble the load yourself"* discipline exist, but they are addressed to a spinner choosing a load — **there is no beat making the newly-loaded agent stop and derive what its OWN subordinates need.** That is the gap he named, and it is real. | ACT → Justin | Open. |

## §6 — B2 in detail, because applying the correction verbatim would introduce an error

**ACP asked (2026-09-02) whether to fold two 16-day-old bulletin entries of ACT's lineage into `current-state.md`'s body. The entries say the body carries two stale claims. Checked at source today:**

- ✅ **Claim 1 is genuinely stale.** The body describes the Phase-1 fast-follow as upcoming. **It COMPLETED 2026-08-12** (twelve Steps merged, `869bd20`).
- 🔴 **Claim 2 is WRONG, and it is the newer of the two that is wrong.** The bulletin says Step 43 *"is CLOSED."* **It is not — it is 🔄 PARTIAL**: three of its seven pieces merged on 2026-08-12, and the SPRINT still names it *"THE HIGHEST-PRIORITY OPEN ITEM IN THIS SPRINT."* The bulletin entry was written 2026-08-17 from ACf-13's 08-12 text; ACf-13 itself corrected Step 43 to PARTIAL on 08-13, **after** that bulletin text was fixed in place.

⇒ **Answer to ACP: neither (a) nor (b) as offered. I will fold claim 1 and NOT claim 2** — copying the entries verbatim would have written a false status into the file every agent loads at startup. 🔑 **The general shape, worth keeping: a correction parked in a queue can go stale faster than the thing it corrects.**

## §7 — B4 in detail: the answer to ACP's cross-runtime security ask

**ACP's question: does `evryn-team-runtime` spawn any child process with a token, key, URI or password in its ARGUMENTS?**

**Answer: YES — the class is PRESENT. But the specific failure ACP named is already CLOSED, and a different residual remains.**

- **The two production spawn sites** are `src/boot/workspace.ts:78` and `src/composer/index.ts:124`, both `execFileAsync('git', args, …)`. `composer/index.ts:124` is `['-C', repoPath, 'rev-parse', 'HEAD']` — **no secret.**
- 🔴 **`workspace.ts` DOES put a credential in argv.** `authUrl()` (`:37-45`) builds `https://x-access-token:<token>@<host>/…` and that string is passed as an element of `args`.
- ✅ **The error-leak half — ACP's actual class — is closed by construction.** Every git invocation funnels through one wrapper, `git()` (`:72-87`), which catches the rejection and runs `redactToken()` over **both** `e.message` (which carries argv) **and** `e.stderr` (git echoes the remote URL) before anything is thrown or logged. `redactToken` (`:52-63`) has a second, token-independent pass that collapses **any** `https://user:secret@host` userinfo — so a rotated or differently-shaped credential still cannot leak. The token is never written to `.git/config`; the on-disk `origin` stays token-free.
- ⚠️ **The residual, already known and documented in-file at `:41-43`:** the tokenized URL is visible in the git process's **argv for the command's lifetime** — `/proc/<pid>/cmdline`-class exposure on a shared host. Logged as an accepted Phase-1 residual (QC finding 3) with a named eventual hardening: **a git credential-helper.** ⇒ **That is a different threat model from ACP's** (local process inspection, not an error string), and it is the one still open.

**Instrument disclosure:** the two-site completeness claim rests on `git grep -n -E "spawnSync|spawn\(|execFile|execFileSync|execSync|exec\(|child_process|node:child_process" -- src`, run **with a known-hitting control** to prove the search executed — **the array form is covered.** The *content* claims above come from a full read of `workspace.ts:16-130`. **A grep locates; it does not conclude — treat the completeness half accordingly.**

🔑 **AND A GAP THIS OPENED IN OUR OWN TRACKER: SPRINT Step 76 names both these sites for *bare-name executable resolution* and is SILENT that one of them carries a credential in argv.** The Step frames them as "strictly weaker than the npx class — no shell, no registry fetch," which is true of the PATH hazard and misses the sensitive half at the same line. **Owed: widen Step 76, or file the credential residual as its own Step.**

## §8 — 🔴 JUSTIN'S STANDING INSTRUCTION, 2026-09-08: THE ACT-LINEAGE DOC RETIREMENT

*(Recorded verbatim in substance because it is a durable commitment, not this session's business.)*

**ACP ran this pass over its own lineage as `docs/working/` was merged into `docs/sessions/`, and it was hard: much of the pile was very stale, but a scattering of it held things that had never been properly persisted.** The retirement rule has since been fixed so the pile cannot re-accumulate the same way — **but the same job is still owed on the ACT/ACf lineage, and nobody else may do it** (Justin's ruling: another lineage's in-flight work is not theirs to cull).

**The shape Justin wants:**
- **An independent sub-lane does it — NOT ACT itself.** *"I don't want you burning a bunch of tokens on it — I want you focused on the main show."*
- **Timing: once the main show is underway.** Sooner than later, but not first.
- **The bar is PROPER retirement, not filing.** Every doc retired only after verifying that anything it holds which should have been persisted **has been** — because the failure mode is losing a fact that was never written anywhere else. ⇒ **Budget it as a DISCOVERY pass.** ACP's measured rate: **nine of its last sixteen docs carried an explicit "still owed" section, and several were the sole home of live work.**
- **Scope:** the 29 docs banner-held since 2026-08-22 (`git grep -n "HELD-SESSION-DOC"` derives the live set — 30 name ACT as owner today), **plus every older `acf*`/`act*` doc.**

## §9 — SCOUT `ACTs` — the load list, as an artifact of record

*(Committed per `ac-orchestration-protocol.md`: a load list is an artifact of record, not a message. A spin's messages die with the session; without this, nobody could ever establish what the agent was asked to read.)*

**Spun 2026-09-08 as a background sub-AC, model pinned Opus, THREE-trip (DERIVE → LOAD → WORK), read-only.**

**Trip 1 (DERIVE) load — the map-artifact substitute:**

| File | Span |
|---|---|
| `_evryn-meta/.claude/agents/ac.md` | full — 911 |
| `evryn-team-runtime/docs/ARCHITECTURE.md` | full — 138 |
| `evryn-team-runtime/docs/BUILD-PHASE-1.md` | full — 103 |
| `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` | full — 526 |
| `_evryn-meta/docs/current-state.md` | full — 220 |

Plus the mechanical inventory (§2) and the composer-derived identity half (§3), pasted inline rather than referenced.

🔴 **DISCLOSED DEVIATION: the map artifact is DEGRADED and the scout was told so.** The standard DERIVE trip hands a Code Atlas plus a generated dependency map, and the verbatim `<output>` block instructs the agent to route through "the map artifact's index" and "its routing table." **`evryn-team-runtime` has neither — that absence is what `ACTa` exists to fix.** Items above are the best substitute that exists. The `<output>` block was pasted **verbatim and unaltered** per the HARD RULE; the substitution is explained in `<task>`, which is bracket-fill. **If the derivation comes back expensive, that is a finding about the MAP, not the agent — and it independently confirms the atlas is needed.**

**Trip 2 (LOAD) — APPROVED AND SENT 2026-09-08T08:5x. ~17,900 lines.** ACT wrote its own expected list *before* reading the scout's proposal, per the protocol's review rule, then diffed both directions.

| Tier | Contents | Lines |
|---|---|---|
| 1 — intended shape | design-of-record (504) · ADR-055 (146) · ADR-052 (106) · ADR-050 (40) · design-v2 (319) | 1,115 |
| 2 — lane material | the `ACTi` brief (223) + its exchange (158) · lane-protocol (140) · `ACTa` brief (148) + exchange (35) · `ACTd` brief (132) + exchange (32) · the `ACTv`/`ACTj`/`ACTt` lane docs (112/101/97) | 1,178 |
| 3 — **code half, in full, non-negotiable** | all 41 `src/**/*.ts` (9,981) + all 18 `migrations/*.sql` (1,277), enumerated LIVE from inside the repo | 11,258 |
| 4 — **identity half, enumerated SEPARATELY** | team `CLAUDE.md` (428) · `runtime-ops.md` (76) · **Hub `roadmap.md` (186)** · **team `current-state.md` (808)** · **`vision-and-ethos.md` (231)** · `lucas.md` (201) · Lucas's `MEMORY.md` (~219) · the 5 skills protocols (853) · `DRAFT-runtime-ops.md` (9) | 3,019 |
| 5 — evidence + record | team-runtime `CHANGELOG` (266) · the test-coverage map (331, **lead-grade only**) · meta `CHANGELOG` 1–130 | 597+ |
| 6 — spinner-mandatory | `ac-orchestration-protocol.md` (788) | 788 |

**`#cascade-override` deliberately NOT used**, so the reconcile step stays live — the scout re-checks ACT's list against the sanctioned cascade and reports anything ACT missed.

**🔴 THE REMOVAL — `tests/` (63 files, 12,481 lines) is OUT OF SCOPE, and this is an architecture call rather than a trim.** The scout proposed delegating them to a sub-reader; ACT declined and went further. **Reason: a fan-out is weakest exactly at the seams, and the seams are the primary deliverable** — every summary crossing an agent boundary is a translation, and that loss would land on the one thing most needing to be true. `tests/` is `ACTi`'s territory. The coverage map serves as a lead-grade index instead. ⇒ **The scout reads everything it holds in ONE HEAD, and is instructed to HARD-STOP rather than quietly fan out if it cannot.** A hard stop is a successful outcome: it settles the load-fit question empirically.

**🔴 THE THREE ADDITIONS ACT MADE, and why they matter beyond this trip:** the scout built its identity set from `team-runtime-full-cascade.md`; ACT built its own by **reading `src/composer/layers.ts`**. The cascade's list omits three file-backed composer layers — the **Hub (L2)**, the **team current-state (L3, composed in full and never windowed)**, and **Lucas's spoke (L4)**. ⇒ **Two source-of-truth documents describe this runtime's identity half incompletely, in the same way** — the cascade and the orchestration protocol's table. Both are owed corrections.

**⭐ THE DIVIDEND — what the scout's derivation caught that ACT's did not:**
- **`design-v2` (319)** — ARCHITECTURE's Required Context names it "full"; ACT had missed it entirely.
- 🔴 **`team-runtime-full-cascade.md` step 4 omits ADR-055** — so a lane spun on the sanctioned cascade **would not load the decision its own lane implements.** A real defect in a source-of-truth file ACT owns.
- **`evryn-team-runtime/docs/DRAFT-runtime-ops.md`** — a nine-line second copy of a layer-1 identity file living in the code repo. A drift surface nobody had named.
- **The identity half's live must-load set is LUCAS ONLY** (~924 lines), not all eight agents — matching `ACTIVE_AGENTS`. ~2,000 lines off any lane load. ⚠️ **Boundary: that is a fact about today's config, not about the system — the other seven become load-bearing at Phase 3.**
- **`current-state.md` is 212 lines, not the 220 ACT stated** — ACT's count predated a `git pull` that removed nine. **The scout trusted its own read over the brief's number, which is exactly the instruction working.**

---

## §10 — WHAT ACT-16 HAS ACTUALLY READ OF THE RUNTIME

**Three files, and this number should be stated in every brief ACT writes:** `src/composer/layers.ts` (lines 1–120 of 593) · `src/config/skills.ts` (full, 118) · `src/config/agents.ts` (full, 59). **Roughly 300 of 9,981 lines of `src/`. Zero migrations. Zero tests.**

⇒ **Every runtime claim ACT makes is a LEAD until a lane or the scout verifies it at source.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
