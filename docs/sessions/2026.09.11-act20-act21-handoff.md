🔒 **HELD-SESSION-DOC** — the LIVE ACT working brief · **RETIRE UPON:** superseded by ACT-21's own handoff at set-down · **OWNER:** ACT · **HELD SINCE:** 2026-09-11

# ACT-20 → ACT-21 — handoff. **Self-sufficient. You need no earlier handoff.**

*Written 2026-09-11 by **ACT-20**, the team-runtime conductor, at `#packout`, on the **LAPTOP**. **ACT-20 did not compact** — it was re-spun deliberately at Justin's direction with ~160K of context left. **SUPERSEDES `2026.09.10-act18-act19-handoff.md` entirely** — that file had reached fifteen layered sections and its own retire condition called for replacement. **It is archived at `docs/sessions/historical/2026.09/`, filename unchanged, on Justin's OK of 2026-09-11.** *(Find it any time with `git ls-files "*act18-act19-handoff*"`.)*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you do not see it, re-read until you do.
>
> **How to use this file:** ACT's live state — what is built, what is running, what is owed, what is waiting. **The test at every append: if this instance died right now, could a fresh one pick up from this file alone?** If no, the file is behind — fix that before anything else.

---

# 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP HERE.

**If what you hold is a machine-written summary of your session rather than files you actually read — you have compacted, and your run is over.**

🔴 **DO NOT RELOAD AND CONTINUE. We tried it; it does not work.** *(Justin, 2026-08-18: "the agent just sails through it and confidently flails about, unloaded.")* **An instruction to reload is a rule you must REMEMBER to obey — and remembering is exactly what compaction damaged.**

**Do these three, then stop:** ① stop working, mid-sentence if need be · ② tell Justin in one line that you compacted · ③ ask for a fresh re-spin.

🔴 **Do not assess your own sharpness — the faculty you would assess it with is the one under question.**

---

## §1 — 🔴 WHAT TO DO FIRST

**The lane in one line: Step 85 (memory grading) is built and fixed and needs one fresh QC before it merges; `ACTj` (the money layer) is LIVE with a design proposal posted and UNREVIEWED — that review is your first real act.**

0. **No machine switch.** ACT-20 ran on the **laptop**; run the ordinary repo-sync check (`ac.md`). **Worktrees that are NOT ACT's, leave alone:** `_evryn-meta-acm`, `_evryn-meta-pgdump`, `evryn-backend-*`.
1. **Load §2. It is complete and it is here.**
2. **Ask Justin the two spin-up questions** — *should I read my inbox?* and *should I arm a watcher on it?* **`inbox-act.md` was EMPTY at set-down** (ACT-20 discharged Lucas's message and cleared it).
3. 🔴 **READ `ACTj`'s DESIGN PROPOSAL AND ANSWER IT. She is parked waiting on you.** It is the entry headed `### ACTj — 2026-09-11T15:22:07-07:00 — DESIGN PROPOSAL: a job is a SCOPE, minted lazily; propose_work cannot be the mechanism…` in `docs/sessions/2026.09.08-act17-actj-exchange.md`. **ACT-20 never read it — it arrived during the packout.** ⚠️ **Read her TRIP-1 entry above it too; it carries the four corrections that reshaped SPRINT Step 19.**
   - **Expect `NEEDS-JUSTIN` items in it — at minimum `cap_usd`'s default, and probably where the job's time ESTIMATE comes from.** **You route those; she never routes to Justin herself.**
   - **Before her BUILD trip: bring her worktree current and NAME what moved** (she asked, and it is owed).
4. 🔴 **SPIN A FRESH QC ON STEP 85 — the branch is `dc/step85-memory-grading` @ `267926c` in worktree `evryn-team-runtime-dcm`**, clean at set-down. **§7 carries what was built, where the DC's own set-down is, and what it could not do.**
   - ⚠️ **`main` HAS MOVED SINCE THAT BRANCH WAS LAST BROUGHT CURRENT** *(ACT-20 merged `main` in at `1d34601`, then committed tracker and changelog work on top of `main` afterwards)*. **`DCn` flagged this itself: expect to bring the branch current before you merge.** **Check with `git -C evryn-team-runtime rev-list --count dc/step85-memory-grading..main`, and read any branch diff with THREE dots (`git diff main...branch`) — two dots will render `main`'s newer commits as deletions by the branch.**
5. **Then the merge** *(build-level code — `ac.md`'s standing authorization)*, **the worktree reap with the `node_modules` count check** *(canonical was **146** at set-down)*, **the changelog entry, and an ARCHITECTURE line** mentioning layer-6 grading — **that last needs Justin's explicit authorization.**
6. **Then SPRINT Step 43** — the scheduler's lifecycle five. ⚠️ **Settle a contradiction at source first: §1 of the old handoff and `current-state.md` call it "the unguarded dormancy cadence gate," while the SPRINT's own Step 43 and the 2026-08-12 changelog say both sweep gates were guarded at `869bd20` and only the lifecycle five remain.** **Read the code before briefing that lane.** **It shares a file region with `ACTj`'s build, so it goes after her.**

---

## §2 — YOUR LOAD LIST

> 🔴 **NO LINE COUNTS APPEAR BELOW, AND THAT IS JUSTIN'S RULING (2026-09-08): *"they're always stale when they get forwarded like this."*** ⭐ **Derive them at read time:** `cd "$HOME/Evryn/Code" && wc -l <paths>`. ⚠️ **`wc -l` counts NEWLINES, so a file with no trailing newline reports one FEWER line than it has — and the line it drops is the CANARY.** ⇒ **Size your last read window PAST the stated count by default** *(`ACTj`'s durable version of that warning, after it caught her, 2026-09-11)*. **Confirm each file's bottom canary.**
>
> 🔴 **NAME THE WORKTREE YOU READ FROM** — each repo's canonical `main` tree unless told otherwise.

**Read in this order. The order is DEPENDENCY, not importance.**

| # | File | Read |
|---|---|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | **full.** Your manual. ⚠️ **NOT auto-loaded** — the injected `CLAUDE.md` is a router. |
| 2 | `_evryn-meta/docs/hub/roadmap.md` | full — the Hub |
| 3 | `_evryn-meta/docs/hub/technical-vision.md` | full |
| 4 | `evryn-team-runtime/docs/ARCHITECTURE.md` | full — **cardinal invariants 8 and 9, and "why local reasoning often fails here"** |
| 5 | `evryn-team-runtime/docs/BUILD-PHASE-1.md` | full |
| 6 | `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` | **full — the Steps, and the status source of truth** |
| 7 | `evryn-team-runtime/docs/dependency-map.md` | **full.** Generated, complete by construction. **Its §4 reverse index is how you derive a lane's load.** |
| 8 | `_evryn-meta/docs/current-state.md` | full |
| 9 | `_evryn-meta/CHANGELOG.md` | **the skim its own header prescribes** — the top ~100 lines plus section headers |
| 10 | `evryn-team-runtime/CHANGELOG.md` | **the same skim.** Read an older entry in full when your work touches what it records. |
| 11 | `_evryn-meta/docs/protocols/mailbox-protocol.md` **+** `_evryn-meta/docs/mailboxes/inbox-act.md` | full, both |
| 12 | **`_evryn-meta/docs/sessions/2026.09.08-act17-actj-money-layer-brief.md`** **+ `…-actj-exchange.md`** | **BOTH — the live lane.** ⚠️ **Her LOAD LIST now lives separately** in `2026.09.11-act20-actj-load-list.md`; read that too if you re-brief her. |
| 13 | `_evryn-meta/docs/sessions/2026.08.19-act-acta-atlas-brief.md` **+ `…-acta-exchange.md`** | **BOTH.** The exchange is the valuable one — the only home of the hub reasoning. |
| 14 | `_evryn-meta/docs/sessions/2026.08.19-act-acti-guard-integrity-brief.md` | **the lane-brief anti-pattern. Read its §3 BANNERS, not its §3 list** (lines 1–90). |
| 15 | `_evryn-meta/docs/decisions/055-thread-as-complete-record.md` | full — **SPRINT §H is entirely this ADR's build** |
| 16 | `_evryn-meta/docs/protocols/orchestration/being-an-admiral.md` | full. **You report to Justin as an Admiral.** ⚠️ **It gained two rules on 2026-09-10 — read the current file, not a memory of it.** |
| 17 | `_evryn-meta/docs/protocols/ac-writing-protocol.md` | full |

> 🔴 **CHANGES FROM THE LIST ACT-20 INHERITED: NONE. Carried VERBATIM.** *(It loaded ACT-20 cleanly: 20 files, 16 canaries confirmed, two deliberate changelog skims, item 14 a deliberate partial read of lines 1–90.)* **Item 12 gained a pointer to `ACTj`'s split-out load list — a pointer, not a change to what you read.**

**Pull at their moment, not at spin-up:** `ac-orchestration-protocol.md` **+** the matching `orchestration/spinning-*.md` *(**before you spin ANYTHING** — the core alone is explicitly not enough)* · `lock-protocol.md` *(at a `#lock`)* · `packout-protocol.md` *(at a `#packout`)* · `qc.md`'s "Non-product build targets" section *(before briefing a QC here — it is how the `#cascade-override` list is justified)*.

### 🔴 NOT on this list, deliberately: the runtime itself.
**You are a CONDUCTOR; the runtime lives in your lanes' heads.** ⭐ **But the cheap docblock pass is recommended and repeatable — see §9.**

---

## §3 — WHERE THE LANE STANDS, 2026-09-11 evening

🔴 **ASK GIT FOR REPO STATE, NEVER THIS FILE.**

- **🔄 SPRINT Step 85 — BUILT, FIXED, UNMERGED.** An agent's memory can no longer compose silently empty or silently damaged — **and as of Justin's ruling today, a memory damaged at rest REFUSES the wake after one re-read.** **Needs a fresh QC, then merges.**
- **🟢 `ACTj` (the money layer) is LIVE** — Justin spun her ~15:05. **Load verified, trip 1 signed off, design proposal posted and awaiting your review.**
- **🆕 Step 89 filed** — the composer carries no per-agent domain index, so a consolidated agent gets a memory pointing at documents it never receives.
- **Nothing is deployed. Migrations 015–018 are unapplied. The runtime wakes nobody.**

---

## §4 — INSTRUMENTS — what to arm, not what was armed

🔴 **A `Monitor` DIES WITH THE SESSION. Nothing below is running. Arm your own; never inherit a task id.**

| Instrument | Watch | Cadence | Its one job |
|---|---|---|---|
| **Heartbeat** | **you** | **5 min daytime · 10 overnight** | *"Are you still moving?"* ⚠️ **Justin's ruling 2026-09-11: waiting on a PROCESS is not a hold. Keep it armed while a subagent, build or watcher is in flight; pause only when blocked on JUSTIN.** |
| **Inbox watcher** | `docs/mailboxes/inbox-act.md`, on **COMMIT** (`git log --all -1 -- <path>`, with a `git fetch` each tick) | 45s, emits only on change | *"Did anything land for me?"* |
| **`ACTj` channel watcher** | `docs/sessions/2026.09.08-act17-actj-exchange.md` — **on COMMIT *and* on MTIME** | 45s | *"Has she posted?"* 🔑 **Watch BOTH: a Captain's own commit can be refused by the permission classifier, and an uncommitted entry would otherwise be invisible.** **This earned itself today — it fired on her post before the commit landed.** |

- 🔴 **ANCHOR EVERY WATCHER ON `$HOME` AND ON THE REPO (`git -C`)** — a `Monitor` shares the Bash tool's cwd and that cwd is MUTABLE. **Print a proof-of-fire line BEFORE the loop.**
- **To observe a subagent's calls, parse its transcript** at `<project-folder>/<session-id>/subagents/agent-<agentId>.jsonl` — ⚠️ **NOT the `tasks/<id>.output` path the Agent tool returns, which can read EMPTY.** Parse with `node`, printing counts only; never `Read` it.

---

## §5 — 📋 BACKLOG — numbers never reused *(carried from ACT-19)*

| # | Item | Owner | State |
|---|---|---|---|
| **B1** | `runtime-ops.md:9` cites a session doc for its open questions — **SPRINT Step 77.** | ACT → Justin | Open. **Needs a FULLY-LOADED sub-AC**; Justin barred a light-loaded coordinator from drafting it. |
| **B5** | ~54 files carry `HELD-SESSION-DOC` *(derive: `git grep -n "HELD-SESSION-DOC"`)* | ACT | **DEFERRED by Justin** until the main show is underway; **budget it as a DISCOVERY pass.** |
| **B13** | `ThreadDormancyWriteRow` referenced nowhere | → **SPRINT Step 47's residue** | Lives there. Recorded so nobody re-files it. |
| **B16** | `evryn-backend-qc-sandbox` detached worktree | ACP | **Not ACT's.** Do not reap it. |
| **B18** | 🔴 **`settingSources` unset in `runWake`** | → **SPRINT Step 86** | **The one-line fix is HELD until `ACTj` is done** — it shares an options block with `maxBudgetUsd`, which is the money layer's. ⚠️ **A live sequencing constraint; do not let a lane land it early.** |
| **B20** | Seven ACT-lineage entries sit unread in `inbox-acp.md` | — | **ACP dormant. Nothing of ACT's is blocked.** |
| **B23** | `src/wakes/memory-writer.ts` carries no truncation canary | — | ✅ **Deliberate, not a defect** — SPRINT Step 11 excluded it because it holds the canary string as a functional constant. **Recorded so nobody re-files it.** |
| **B21** | `npm audit` advisories exist in `evryn-team-runtime`; not investigated | ACT → a DC | Open. **Read `npm audit` before deciding anything; never `audit fix --force` blind.** |
| **B26** | 🆕 **Justin's rulings from 2026-09-11 have no home in `doc-update-routing-protocol.md` (change-type 9)** — they are recorded in `ac.md`, the team manual and the SPRINT, but the routing protocol's own ledger was not updated. | ACT | Open, small. |

---

## §6 — ⏳ WAITING ON

| # | Sent to | What was asked | Status |
|---|---|---|---|
| **W1–W3** | ACP *(2026-09-08)* | the shared-generator question; an ARCHITECTURE completeness question; a generator-provenance question | **Unanswered. ACP dormant. Nothing of ACT's is blocked.** |
| **W4** | ACP *(2026-09-10)* | 🔴 **the `settingSources` class, possibly LIVE IN PRODUCTION on the product side** | ✅ **Put to Justin 2026-09-11. HIS CALL: it stays with ACP — ACT does NOT run the product-side check.** **It waits for that lane to spin.** |

---

## §7 — 🔴 SUBAGENT LEDGER — enumerate, never work from memory

**Subagent resume is SESSION-BOUND. `DCn` became permanently unreachable when ACT-20's session ended.**

### `DCn` — DC, the Step 85 fix trip — `agentId a425bd9d3c5732431` *(unreachable now)*

- **What it was for:** implement Justin's ruling that a memory **damaged at rest REFUSES** the wake rather than composing with a flag, plus his same-day sharpening that **the refusal comes after ONE re-read.**
- **Load trip VERIFIED before the task went out:** 44 of 44 files, every canary, **56 calls declared against 56 observed**, three failed/partial reads disclosed rather than absorbed, nothing deferred. *(It also corrected the brief twice: the two `tsconfig` files are `.json` and fall outside the code-canary carve-out, and the dependency map it held is stamped from the BRANCH commit, not `main`.)*
- **Artifacts:** worktree **`evryn-team-runtime-dcm`** on `dc/step85-memory-grading` @ **`267926c`** *("Step 85: ONE re-read before a memory refusal, and an operator message written for Justin")*, clean at set-down. **KEEP IT — it awaits QC and the merge, and you own it.**
- ✅ **ITS SET-DOWN EXISTS AND IS COMMITTED: `_evryn-meta/docs/sessions/2026.09.11-dcn-act-step85-refusal-setdown.md` @ `94bac06`** (237 lines). 🔴 **READ IT BEFORE BRIEFING THE QC** — it carries the mutation table, the verbatim operator-facing refusal message, the re-read behaviour, the measured numbers, and what it would want a reviewer to look at hardest. *(It committed only its own file and correctly did not sweep ACT-20's uncommitted edits.)*
- ✅ **Sandbox REAPED by `DCn` itself — `node_modules` 146 → 146 on the canonical tree AND on `-dcm`, no `--force`, no `npm ci` needed.** *(It deleted the junction as a LINK rather than recursively, which would have thrown rather than emptied a real directory.)* **Verify anyway with `git -C evryn-team-runtime worktree list`.**
- **Its measured numbers: 533 → 538 tests, 0 fail; typecheck exit 0 on both configs, before and after. SEVEN mutations, ZERO survivors** — including the two demanded: delete the damaged→refuse branch and the refusal tests go RED while **the consolidation control stays GREEN**, and a second mutation proves that control **can** fail, so it is real rather than decorative.
- 🔴 **THE LESSON THAT OUTLIVES THIS STEP, in its own words: its FIRST mutation harness reported ALL SEVEN mutations as survivors.** It had run `node --test` without `--import tsx` and parsed TAP against a spec reporter. ⚠️ **Assert-applied passed throughout and did NOT catch it.** **What caught it was a BASELINE CONTROL THAT HAD TO HIT.** ⇒ **A mutation harness needs a known-GOOD control proving the harness itself can still SEE a failure — proof that the mutation landed is not enough.** *(Candidate for `LEARNINGS.md`; not filed.)*
- **Two corrections it made to ACT-20's brief, both worth carrying:** this checkout is **MIXED LF/CRLF, not 100% CRLF** — and the split falls on the two files this work touches most, which inverts the stated hazard; and **the re-read's real transient window is the workspace GIT SYNC, not the runtime's own note write**, which is atomic (`tmp` + `rename`).
- ⚠️ **What it could NOT do, because `src/wakes/` was fenced off: make the Slack alert genuinely LEAD with plain English.** **~330 characters of `runWake` wrapper sit in front of its message.** **A one-paragraph edit for whoever owns that file — sequence it with Step 86's held one-liner (B18).**
- **Nesting:** it spawned nothing.

### `ACTj` — the money-layer lane — **NOT a subagent. A STANDALONE instance, spun by Justin.**

**She survives ACT-20's session.** **Her channel is the exchange doc; her state is in it.** ⚠️ **You cannot resume or reap her — she is Justin's to close.** **What you owe her: a reply to her design proposal, and a current worktree before her build trip.**

---

## §8 — 🗂 DOC DISPOSITIONS

| Doc | Disposition |
|---|---|
| **`2026.09.10-act18-act19-handoff.md`** | **SUPERSEDED by this file** — its own retire condition said so, and it had reached fifteen layered sections. **Archive to `docs/sessions/historical/2026.09/` with a `SUPERSEDED BY` line, filename unchanged — on Justin's OK.** ⏳ **Not yet given.** |
| **`2026.09.08-act17-actj-*`** *(brief · exchange · `2026.09.11-act20-actj-load-list.md`)* | 🔒 **CARRY — the live lane.** Retire when `ACTj` closes out. |
| **`2026.09.11-dcn-act-step85-refusal-setdown.md`** *(exists, committed `94bac06`)* | 🔒 **CARRY until Step 85 merges and its changelog entry is written**; then retire. |
| **`2026.08.19-act-acta-*` · `2026.08.19-act-acti-*` · `2026.08.17-acf13-*`** | 🔒 **HOLD** — unchanged from ACT-19's dispositions. |
| **`2026.09.10-act18-packout-checklist.md` · the three `2026.09.11-act19*` accounting docs** | 🔒 **HOLD until Justin has reviewed ACT-19's packout; then retire all four together.** |

🔎 **A retired doc is never lost: `git ls-files "*<fragment>*"` finds it wherever it lives.**

---

## §9 — WHAT ACT-20 ACTUALLY READ OF THE RUNTIME

**`src/composer/memory.ts` in full · the complete three-dot `src/` diff of the Step 85 branch (`composer/index.ts`, `composer/layers.ts`) · the layer-4 spoke loop and the composed-layer inventory (for Step 89).** 🔴 **Zero tests, zero migrations, and none of `src/wakes/`, `scheduler/`, `slack/`, `digest/` or `tools/`.** ⇒ **State your own number in every brief you write.**

> ### ⭐ THE DOCBLOCK PASS — still the cheapest real runtime knowledge a conductor can get
> **One `node` script over `git ls-files src`, printing each file's leading comment block — 41 files, ~814 lines, one command, written to a file and read in chunks.** **The recipe is in the archived ACT-18/19 handoff §9** *(`git ls-files "*act18-act19-handoff*"`)* — **it was not re-run this session.**

---

## §10 — JUSTIN'S ORDERS AND RULINGS, 2026-09-11

**Session posture — ask rather than assume it persists:**
1. **ADMIRAL MODE** — be as autonomous as is reasonable; solve it and say what you did; **anything important or hard to reverse, get him in right away.**
2. **PING EVERY TIME** you have something for him.
3. **Routine changes commit; important ones wait UNCOMMITTED for his review in SCM.**
4. **Numbered reports** (`# ACT-N's Report #N`), **inline ballots**, a handhold on every reference, **never a bare Step number.**
5. **Nobody hijacks ACT** — you work for Justin, not for other ACs or team agents. **Default is to surface an inbound ask to him.**
6. **Packouts run like an astronaut's checklist.** **Don't check `#dev-alerts`.**

**Rulings, each with its durable home:**
- 🔴 **A memory DAMAGED AT REST refuses the wake — after ONE re-read.** → **SPRINT Step 85**, verbatim. *("I want this to fail gracefully, but I don't want it to be soft… And holes in your memory? No bueno." / "if it's just a bad load, I don't want anyone crashing if the file is fine.")*
- 🔴 **The heartbeat stays armed while ANY PROCESS is being waited on** — *"waiting on a process — it's not just subs."* → **`ac.md`**, the heartbeat rules.
- 🔴 **Never let one label mean two things in a ballot** — sub-items get NUMBERS, answer options get LETTERS. → **`ac.md`**, the ballot rules.
- 🔴 **When you mean a requirement, write it as one** — *"your wording sounds descriptive — make it prescriptive… way too often you're writing things this way."* → **`ac.md`** (Documentation Approach) **and `evryn-team-workspace/CLAUDE.md`** (Writing discipline), both at his instruction.
- 🔴 **DELEGATE THE NON-ORCHESTRATING WORK.** *"Be careful doing a bunch of non-orchestrating work yourself — I don't want to constantly have to re-spin you… if you can brief a quick sub easier, do that. Use your judgment for edge cases."* → **here, and owed to `doc-update-routing-protocol.md` (B26).** ⚠️ **ACT-20 did today's tracker corrections and manual edits itself; that is the habit this corrects.**
- 🔴 **TELL JUSTIN BEFORE WRITING INTO ANOTHER AGENT'S CHANNEL.** *"I don't know that Lucas has a watcher on his inbox — so don't write to things like that w/o letting me know."* → **here, and owed to the routing protocol (B26).**
- **W4 stays with ACP** *(㉑b)*. **The tracker corrections were committed on his go** *(㉕a)*.
- 🆕 **HIS CALIBRATION OF HOW ACT WRITES TO HIM, 2026-09-11 — and it is owed a home in `ac.md`'s EliJ calibrated-examples list (B26's sibling; ACT-20 did not land it).** On ACT-20's first report: ***"the descriptions you've just given me are just within my ability to parse — you're referring to things without giving much contextualization. I don't need a whole lot more, but give me a little more handhold… just when you first introduce stuff, give me a sentence or two as to what it is."*** ⇒ **And the shape he asked for: *"Just orient me in space, then orient me at the points of contact."*** ⚠️ **He also said his last full re-orientation was around ACT-16 — so a wide-lens pass at the top of a session is not optional politeness; it is the thing that makes the rest parseable.**
- **His own open task, status unknown to ACT:** whether long team memories load fully in interactive sessions, and whether agents ignore the harness's compaction nudges. **Nothing for ACT to do unless he says so.**

---

## §11 — ✅ DOC-CURRENCY GATE, ANSWERED

| Branch | Answer |
|---|---|
| **a · Sprint** | ✅ **CHANGED and committed** (`7371ac5`) — Step 85's ruling, Step 19 re-derived, Step 87 prescriptive, Step 89 filed, Steps 81/82 corrected. |
| **b · Build doc** | ✅ **NO CHANGE.** |
| **c · Architecture** | ✅ **NO CHANGE — but one is OWED AT THE STEP 85 MERGE:** its memory-lifecycle and composer entries should mention layer-6 grading. **Justin-authorized at that time.** |
| **d · Atlas / map** | ✅ **No runtime shape change on `main`.** This repo has no Atlas; the map regenerates. |
| **e · Learnings** | ⚠️ **LOCAL half done** (Steps 85/19/89). **GENERAL half NOT filed** — two candidates for `LEARNINGS.md`, deliberately left for whoever lands them: *a lane's first-trip corrections are worth more than its build* (seven corrections, four of which rewrote a Step), and *a warning in a load list does not prevent the failure it names — the durable fix is a default, e.g. size the last read window past the stated count.* |
| **6 · Identity files** | ✅ `evryn-backend/identity/**` untouched. **The TEAM's:** `CLAUDE.md`'s Writing discipline gained one rule, **on Justin's explicit instruction.** ⚠️ **Composer layer 1 — it invalidates every agent's cached prefix fleet-wide; free today because the runtime is dormant.** |
| **7 · Hub & spokes** | ✅ No change. |
| **8 · Cross-runtime** | ✅ **No hole closed. The `settingSources` class stays with ACP (W4).** |
| **9 · ADRs** | ✅ **None.** Step 89 is a finding; its design pass may produce one. |
| **13 · Linear** | ✅ Deliberately untouched. |
| **16 · Operator guide** | ✅ This runtime has none. |
| **17 · Database** | ✅ **No migration ran.** Newest archival dump 2026-09-10. |

---

## §12 — 🎟 OPEN BALLOTS WITH JUSTIN

**Numbers continue across instances — do not reuse.** **ACT-20 used ⑪–㉗.**

| # | Question | State |
|---|---|---|
| **㉔** | **The verbatim operator-facing refusal message, and whether the alert is deterministic.** *(His question: "How do I get notified? Deterministically in slack? Does it say 'Lucas couldn't launch because [file] couldn't load'?")* | ✅ **ANSWERED AND APPROVED — "the (24) thing is good," 2026-09-11.** **The wording is program output, not prose: `DCn` ran the real `memoryRefusalMessage` and wrapped it exactly as `runWake` and `createNotifier` do. It lives VERBATIM in `DCn`'s set-down (§7), and nothing further is owed on the wording.** 🔴 **What IS owed is the gap behind it — see below.** |
| **㉕** | Commit the tracker corrections. | ✅ **(a) — committed and pushed.** |
| **㉖ · ㉗** | Commit the lock's `current-state` edits; archive the superseded handoff. | ✅ **Both given 2026-09-11 and both done.** |
| **The ARCHITECTURE line** | Authorization to add layer-6 grading to the memory-lifecycle and composer entries at the merge. | ⏳ **OPEN — ask at the merge.** |

> ### 🔴 THE ONE THING ㉔ LEFT BEHIND, AND IT IS NOT A WORDING QUESTION
>
> **`TEAM_ALERTS_WEBHOOK_URL` is read as optional (`?.trim() || null`), not `required()`.** ⇒ **If it is unset on Railway, a memory refusal parks the agent and NOTHING REACHES SLACK** — the alert Justin just approved would simply not arrive. **Gate G2 confirms the EMERGENCY webhook is set; there is no equivalent confirmation for this one, and ACT cannot see deployment config.**
>
> **Recorded in SPRINT Step 85 as owed BEFORE THE FLEET RUNS.** ⇒ **Put it to Justin as a one-minute check on his side — it is the difference between a refusal he hears about and an agent that silently stops waking.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
