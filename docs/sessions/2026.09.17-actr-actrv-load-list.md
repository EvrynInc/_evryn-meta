🔒 **HELD-SESSION-DOC** — the load list of record for `ACT-Rv`, the `ACT-R` lane's first sub-AC · **RETIRE UPON:** `ACT-Rv`'s slice is fully adjudicated in the `ACT-R` ledger · **OWNER:** ACT · **HELD SINCE:** 2026-09-17

# `ACT-Rv` — LOAD LIST OF RECORD

*Written 2026-09-17 by `ACT-R`. **Committed because a load list is an artifact of record, not a message** — once a session ends, nobody can establish what its agents were asked to read unless the list is on disk.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.

---

## What this is, and how it was derived

**`ACT-Rv` is spun as an `ac` SUBAGENT, model pinned `opus`, two trips.** Trip 1 carries this list inside the verbatim `<mandatory_load>` block and tells the agent nothing about its work. **The task arrives only in trip 2**, by resume, naming `2026.09.17-actr-actrv-verification-brief.md`.

**Derived, not copied — per `spinning-an-ac.md`'s load-derivation gate:**
- **The docs half** is the team-runtime Full cascade, steps 1–5 (`docs/protocols/load-cascades/team-runtime-full-cascade.md`), read by `ACT-R` in full.
- 🔴 **The identity half was DERIVED FROM THE COMPOSER, not taken from the cascade's layer list.** `ACT-R` located every file open in `src/composer/index.ts` with `grep` *(`TEAM_COMMON_FILES`, `HUB_REL`, `CURRENT_STATE_REL`, the spokes loop, the identity and memory paths)*, then **READ `src/config/skills.ts`'s registry to conclude it is complete** — exactly five skills. **Defeater closed:** `src/composer/layers.ts` contains **0** file reads, against a control of **10** in `index.ts` in the same run. **Result: composer layers 1–6 for Lucas, plus the five skill bodies — which agrees with the cascade's list.**
- **The code half is delivered TARGETED, at the moment of use** — `spinning-an-ac.md`'s tier rule: *"FULL CASCADE NO LONGER MEANS AND THE ENTIRE `src/` RUNTIME AS A DEFAULT."* **What IS mandatory: the dependency map, its five hub files, and the composer + config files that define the identity half.**
- ⚠️ **This uses `#cascade-override`, and the reason is measured:** the list below is **~600k tokens** *(1,454,369 bytes at a calibrated 2.60 B/tok, plus two changelog skims)*. **The cascade's step 6 — every `src/` file and every migration — would add ~200k before any work, putting the agent at the compaction edge.** **Nothing is dropped from the accountability set; the code half is read by the same agent, at the moment a claim names it.**
- ⚠️ **What the override costs, stated so it is not a surprise: it disables the agent's cascade-reconcile step, which is the one part of loading that can catch a SPINNER's omission.** **Compensated in trip 2**, which explicitly invites the agent to load anything it finds it needs and to report each one.

## 🔴 LINE COUNTS ARE HYPOTHESES. Derive them yourself:

```bash
cd "$HOME/Evryn/Code" && wc -l <path>
```

**`wc -l` counts NEWLINES — a file whose last line has no trailing newline reports one fewer line than it has, and the line it drops is the CANARY.** **Read past the stated count. Flag any delta rather than reconciling it silently.**

---

## THE LIST — in dependency order: intended shape → record → map → both runtime halves → procedure

| # | File | ~Lines | Read |
|--:|---|--:|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | 979 | full — the operating manual |
| 2 | `_evryn-meta/docs/hub/roadmap.md` | 187 | full — the Hub, company truth |
| 3 | `evryn-team-runtime/docs/ARCHITECTURE.md` | 162 | full — the runtime's intended shape and cardinal invariants |
| 4 | `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-team-runtime-memory-architecture.md` | 504 | full — the memory design-of-record (ADR-052); exceeds one `Read` cap, page it |
| 5 | `_evryn-meta/docs/decisions/050-team-runtime-sdk-mains-worker-tier.md` | 40 | full — the foundational runtime decision |
| 6 | `_evryn-meta/docs/decisions/052-team-runtime-memory-wake-economics.md` | 106 | full — the memory decision record |
| 7 | `_evryn-meta/docs/decisions/055-thread-as-complete-record.md` | 146 | full — the thread as the agent's complete record |
| 8 | `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` | 683 | full — the runtime's live tracker; dense, page it |
| 9 | `evryn-team-runtime/CHANGELOG.md` | — | **skim, as its header prescribes:** top ~100 lines + every `## ` header + confirm the canary with `tail -1` |
| 10 | `_evryn-meta/CHANGELOG.md` | — | **skim, as its header prescribes:** top ~100 lines + every `## ` header + confirm the canary with `tail -1` |
| 11 | `_evryn-meta/docs/current-state.md` | 221 | full — the cross-project snapshot |
| 12 | `evryn-team-runtime/docs/dependency-map.md` | 640 | full — **its canary is on line 641; read past the count** |
| 13 | `evryn-team-runtime/src/db/client.ts` | 27 | full — hub file |
| 14 | `evryn-team-runtime/src/db/types.ts` | 282 | full — hub file |
| 15 | `evryn-team-runtime/src/boot/env.ts` | 197 | full — hub file |
| 16 | `evryn-team-runtime/src/config/agents.ts` | 59 | full — hub file; per-agent config incl. spokes |
| 17 | `evryn-team-runtime/src/safety/notify.ts` | 99 | full — hub file |
| 18 | `evryn-team-runtime/src/composer/index.ts` | 575 | full — assembles every wake; the authority for the identity half |
| 19 | `evryn-team-runtime/src/composer/layers.ts` | 593 | full — the composed layer definitions |
| 20 | `evryn-team-runtime/src/config/skills.ts` | 118 | full — the skills registry |
| 21 | `evryn-team-workspace/CLAUDE.md` | 429 | full — composer layer 1 |
| 22 | `evryn-team-workspace/shared/protocols/runtime-ops.md` | 76 | full — composer layer 1 |
| 23 | `evryn-team-workspace/shared/current-state/current-state.md` | 758 | full — composer layer 3, composed whole into every wake; the largest identity file |
| 24 | `_evryn-meta/docs/hub/vision-and-ethos.md` | 231 | full — composer layer 4, Lucas's spoke |
| 25 | `evryn-team-workspace/.claude/agents/lucas.md` | 201 | full — composer layer 5, the live agent's definition |
| 26 | `evryn-team-workspace/.claude/agent-memory/lucas/MEMORY.md` | 402 | full — composer layer 6; memory files truncate silently, confirm the canary |
| 27 | `_evryn-meta/docs/protocols/ac-writing-protocol.md` | 217 | full — how to write anything durable |
| 28 | `_evryn-meta/docs/protocols/lock-protocol.md` | 189 | full — step 10 carries the document dispositions |
| 29 | `_evryn-meta/docs/protocols/doc-update-routing-protocol.md` | 119 | full — where a change of each type belongs |
| 30 | `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` | 828 | full — the protocol for spinning any subagent |

**Deliberately NOT on this list, and read in trip 2 instead because each would reveal the task:** the brief, and its two named inputs.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
