# AC0 Handoff — 2026-06-19: round-2 is staged; YOUR first job is to audit my staging

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

> **From:** the AC0 that ran the 2026-06-18/19 recovery cleanup. **To:** the fresh AC0 Justin spins Monday to run the round-2 wave. **Load the Full Startup Context Cascade** — you're orchestrating a build recovery. **Ping `#team-alerts` every response** (Node `fetch` to `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`; parse the `.env` directly, `dotenv` isn't installed; prefix `AC0:`).

---

## 🚦 YOUR FIRST ACT — audit my staging work BEFORE you trust it (Justin's explicit instruction)

Justin does NOT want you to take my work on faith. The whole point of spinning you fresh is that **you form your own deeply-held opinion** on whether the round-2 staging is clean. So before you spin a single lane:

1. **Read the situation below + the linked docs, then independently verify the clean-vs-junk handling.** The cardinal risk in this whole recovery is **dragging corrupted round-1 output into round 2 as if it were validated.** My claim is that I quarantined the corrupt stuff and framed the rest as research-to-verify. **Don't believe me — check it.** Specifically:
   - Open `docs/sessions/2026-06-19-round2-lanes-brief.md` (the brief Justin will hand AC1a–5a) and judge for yourself: does it lean on round-1 *outputs* as gospel anywhere, or is it appropriately "research, verify independently, re-derive against the real ARCHITECTURE/runtime + clean DC/QC"? If you find a spot that's too trusting, fix it.
   - Confirm the corrupted artifacts are actually quarantined (`docs/working/2026.06.17-corrupted/` holds the 3 lane *handoffs*; the lane *code* is in the on-ice junk worktrees) and that the brief tells AC1a–5a NOT to read them.
   - Sanity-check that all **five** lanes are present (AC1a Lane A, AC2a Lane B, AC3a Lane C/cost, AC4a staging, AC5a M1) and nothing got dropped.
2. **If you disagree with any of my calls, change them** — you own round 2, not me. I tried hard not to lead the witness; the situation below is fact, the judgment is yours to re-make.

---

## SITUATION (the short version; full forensics in the kept docs)
The 2026-06-17 5-AC wave (AC1–5) ran on **lobotomized subagents** — they loaded no startup cascade, so the coordination never fired. **All round-1 lane code is junk; no merge.** Justin caught it pre-merge. Recovery ran 6/18–6/19:
- **ACP (the protocol-fix AC) fixed loading** — `docs/protocols/ac-orchestration-protocol.md` is rewritten (precise file lists + line-spans → two-part receipts → re-run on any partial; a real "Spinning a team subagent" section) and **ADR-042** codifies it. This was THE gate; it's cleared. (ACP's own handoff is in `docs/sessions/historical/2026-06-19-acp-handoff.md`.)
- **Git estate reconciled:** the `evryn-quality` fork is resolved (laptop on the good `main`; trapped QC pattern ported → `b4ce5be`; GitHub default flipped to `main`). `evryn-ops` renamed `master→main` (laptop caught up). **Every repo is now on `main`.** Forensics: `docs/working/2026-06-18-sync-forensics-report.md`.
- **Mira's identity-writing-bible work is merged + pushed** (`7eb6d73`).

## WHAT I STAGED FOR ROUND 2 (verify, don't trust)
- **`docs/sessions/2026-06-19-round2-lanes-brief.md`** — the combined round-2 brief. Spine = **DESCRIBE-THEN-PAUSE** (each AC1a–5a orients Justin on its plan and WAITS for his explicit "go" before building — reversing round-1's post-and-proceed, which is what let them race ahead). Carries the distilled per-lane salvage with `[RE-DERIVE]` tags + a "research, not gospel" discipline. Dev-residue drops baked into AC1a/AC3a.
- **4 r2 worktrees** off `main` `7eb6d73`, `.env` copied into each: `evryn-backend-{ingest,operator,cost,m1}-r2` on branches `r2/lane-{a,b,c}-...` + `r2/m1-silent-death`. (Staging/AC4a is decoupled — no code worktree.) **The old round-1 junk worktrees are intentionally ON ICE** (insurance until round 2 settles — I've harvested all their salvage; reap them once you're confident).
- **Docs cleaned:** 3 lane handoffs → `docs/working/2026.06.17-corrupted/`; spent AC6/AC7/ACP docs + the round-1 parallel-lanes brief → `docs/sessions/historical/`. Kept live as **research**: `m1-stage2-design.md`, `step61-...md`, `staging-launch-space.md`, `staging-validation-playbook.md`, `cache-measurement-plan.md`, the forensics report, + untouched `2026-06-15-mira-guardrail` & `2026-06-12-acf-cost-levers`.

## MONDAY'S JOB (after your audit)
Spin **AC1a–5a** (clean, top-level instances) off the round-2 brief. They follow the **fixed** orchestration protocol when spinning DC/QC (precise-load + receipts). **Naming: AC1a–5a** (Justin's call — clean, collision-safe; supersedes an "AC11–15" idea he floated then dropped). Justin keeps the original AC1–5 sessions alive for Q&A if a lane gets stuck. **Each lane DESCRIBE-THEN-PAUSEs** — do not let them race. Then you converge (assemble branches → resolve cross-lane seams → validate → one review + one deploy-go to Justin).

## OWED LIST (carried — none blocking the spin)
- **Dev-DB suspect residue** (round-1 applied to *dev* only, prod untouched): Lane A's `gatekeeper_inbound_addresses` + `users.outbound_address` + `trg_emailmgr_items_updated_at`; Lane C's `story_versions` + `consolidate_profile`. **Drop-then-apply is baked into the AC1a/AC3a brief sections** (the `IF NOT EXISTS` guards would else leave suspect structure). Verify the dev ref before any DB op.
- **Auth-gated ARCH/BUILD edits** for round 2: Step-61's drafted edits (in its design doc), the Reflection "story is written in v0.2" ARCH update, M1's v0.3 note. Applied via you/Justin after the clean rebuild — don't auto-apply round-1's drafts.
- **Desktop catch-up (Justin's):** `git pull` on the desktop's `_evryn-meta`/`evryn-backend`/`evryn-team-workspace`/`evryn-quality` (un-pulled since 6/16, all clean FFs per AC7) + the `evryn-ops` local `master→main` catch-up.
- **Justin to vet:** the mid-wave `_evryn-meta/CLAUDE.md` edit `83a3ecb` (subagent fresh-spawn/no-resume discipline — likely fine, covered by ADR-042, but it's source-of-truth).
- **Verify-if-shipped:** sprint Step 60 (proportional-research — did it ride the 6/16 Mira batch?).
- **Candidate retire (I left it — your/Justin's call):** `docs/sessions/2026-06-16-ac0-handoff.md` (the go-live deploy handoff, superseded).
- **Could promote** to `LEARNINGS.md`/`AGENT_PATTERNS.md`: the lobotomy → loading-discipline pattern — but it's already durably captured in ADR-042 + the rewritten protocol, so low-priority (source-of-truth edit, Justin approves).
- **The describe-then-PAUSE rule** lives in the round-2 brief; consider whether it should also be codified in `ac-orchestration-protocol.md` for future waves (source-of-truth — propose to Justin).

## NORTH STAR
The recovery is done; round 2 is staged and clean *as far as I can tell* — your job is to confirm that independently, then run the wave the way round 1 should have gone: loaded right, paced right (describe-then-pause), verified by real DC/QC. Don't let anything from round 1 in without re-deriving it.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
