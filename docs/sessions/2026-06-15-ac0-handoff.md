# AC0 Handoff — 2026-06-15 (finish the go-live deploy; resolve the guardrail)

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

**From:** AC0 (this session — assembled + double-vetted the go-live bundle, integrated Mira's identity edits, codified identity-as-runtime). **To:** the next AC0, who finishes the deploy and resolves the open guardrail.

**Read first (don't re-derive — these carry the snapshot):** `docs/current-state.md` (the 2026-06-15 entry + the top "What's Next" bullet) and `CHANGELOG.md` (the 2026-06-15 entry). Then this handoff for the operational nuance. Active sprint: `evryn-backend/docs/SPRINT-V0.2-HARDENING.md`. **Load the Full Startup Context Cascade** — you're finishing a build/deploy.

**Standing rules (Justin, this session):** ping `#team-alerts` **every** response — *actually send it* via Node `fetch` to `SLACK_TEAM_WEBHOOK_URL` (don't just type "pinging"). Deploy is OFF / manual `railway up`, **never without Justin's explicit go.** Everything below is HELD uncommitted for Justin's SCM review — he's fine with you committing/pushing post-compaction.

---

## Machine plan + git topology (added 2026-06-16 — READ before any `git pull`)

> **UPDATE 2026-06-16 (pushed):** `_evryn-meta` (`02b817d` = lock docs + CLAUDE.md codification, plus this handoff's follow-ups) is now **committed + pushed to origin.** Still LOCAL on this machine: the **held bundle** (uncommitted, `evryn-backend-integration`), the `evryn-backend` `main` sprint commits, and `3531fea` (`evryn-team-workspace` current-state — rides Mira's push). The "local-unpushed" framing in the bullets below predates this push.

**Plan: finish the deploy on THIS machine, *then* bounce to the new machine.** Everything you need is already here — do NOT try to reconstruct on a fresh machine mid-flight.
- **This machine holds (all local):** the held bundle (uncommitted, in `evryn-backend-integration`), local-unpushed commits (`02b817d` `_evryn-meta` = lock docs + CLAUDE.md codification; `3531fea` `evryn-team-workspace` = current-state append; the sprint commits on `evryn-backend` `main`), and all the worktrees.
- **Other agents push to origin in parallel:** AC2 (laptop) → `_evryn-meta/dashboard/` (+ possibly a `_evryn-meta/docs/current-state.md` line); Mira (this machine) → `evryn-team-workspace` (team current-state + her v0.3 identity-files backlog). Push order is set: **Mira pushes before AC2; AC2 `git pull`s before pushing** (the team current-state has 3 appenders — AC0/Mira/AC2).
- **DO NOT `git pull` on this machine until the deploy is done.** The deploy is entirely local (the bundle, in `evryn-backend`) and needs nothing from origin; pulling now only risks disturbing the held bundle / worktree state for zero benefit.
- **After deploy + cleanup, but preferably before #lock:** `git pull` each repo to reconcile (AC2's `_evryn-meta` push — clean except a possible append-merge on `docs/current-state.md`; the team current-state), then push everything (the deployed bundle + the local lock/sprint commits), THEN it's safe to bounce to the new machine with a synced origin.

---

## WHERE IT STANDS — one sentence
The v0.2 go-live bundle is **assembled, typecheck-green, double-vetted (QC + DC), and HELD uncommitted; it has NOT deployed.** Your job: resolve one open identity question, run a final QC re-confirm, commit, and get Justin's deploy-go + the M1 phone test.

## THE GIT STATE (precise — this is the part that's easy to get wrong)
- **Bundle branch `bundle/go-live-v0.2`** lives in worktree **`c:/Users/Justin/Evryn/Code/evryn-backend-integration`**, branched off `main` `fdd57f7`.
- **Committed on the bundle branch:** the 4 merges (`d7270e9` morning-sweep-c2 [carries quiet-hours] → `25579e6` m1 → `7c0585a` cost → `3fb075a` mira-phase6) + **`f4083e6`** — an *interim* tag-drop commit that is **SUPERSEDED.** The held edits below restore + evolve what `f4083e6` removed, so when you commit the held bundle, make it **one commit that supersedes `f4083e6`** (note that in the message).
- **HELD (uncommitted) in the integration worktree** — these are the bundle's real final content:
  - `src/triage/classify.ts` — the **pass-stamp rewrite** (`buildPassStamp` new wording + signature `emailmgrItemId`→`gatekeeperUserId`; the new `gatekeeperFirstName` helper). **QC GO + DC build-lens GO.**
  - `tests/test-record-pass.ts` — updated to the new shape; **passes 50/50** (`tsx`, dummy-env per the test header).
  - `identity/activities/triage.md` — Mira's tightened `record_pass` Pass-line.
  - `identity/core.md` — Mira's determination-sharing guardrail. **← placement OPEN (see below).**
- **Committed on canonical `main`** (`c:/Users/Justin/Evryn/Code/evryn-backend`, local + UNPUSHED): sprint commits `e3dc73f` (Steps 53/54), `ef1caed` (53/54 legibility), `c452f96` (Step 54d). The bundle branch does NOT contain these (they're on main, off which the bundle was cut — they'll be present after you merge the bundle into main, no conflict; sprint doc isn't touched by the bundle).
- **HELD (uncommitted) in `_evryn-meta`:** `CLAUDE.md` (my 2 added beats — see Codification — PLUS Justin's own tweaks/enrichment, all uncommitted), the Mira working brief `docs/working/2026-06-15-mira-guardrail-placement-brief.md` (with Mira's appended response), and this lock's `current-state.md` / `CHANGELOG.md` / this handoff.

## THE DEPLOY-CLOSE SEQUENCE (in order)
1. **Resolve the `core.md` guardrail placement *with Justin*.** Mira's guardrail (the determination-sharing beat, paired to the now-`[shareable_with_user]` pass-stamp) was integrated into `core.md`'s **Hard Constraints**, right after "You never disclose your assessments of individuals." **My concern (drove this):** it carves a *judgment-gated exception into the inviolable, no-judgment Hard Constraints section* and softens the adjacent "a non-connection is a non-event — no decline" absolute. Questions: (a) does it muddy the no-judgment section; (b) right home — Hard Constraints vs. the triage activity module vs. pull-to-v0.3; (c) the canary / ADR-010 thread (Nathan). **Mira responded** in `docs/working/2026-06-15-mira-guardrail-placement-brief.md` (a third path: *pull it from v0.2, land it in v0.3*). **Justin DISAGREES with her take** — he had me NOT read it and defer to you. So: read her response **fresh**, form your own view, **re-decide with Justin.** It **doesn't fire in v0.2** (Evryn never tells a passed contact "I decided not to connect you" yet — passes are silent; the personal-pass decline uses neutral "isn't available"), so it is **not deploy-blocking** — but Justin wants it settled. Then build the **unified Nathan brief** (fold Mira's Nathan input — in her response — with your Nathan thoughts → one doc Justin points Nathan to; it's v0.3-scope, not a go-live gate).
2. **Final QC re-confirm** of the held identity edits (triage.md Pass-line + core.md guardrail) *as assembled*. The stamp **code** is already QC + DC GO — **do not redo it.** Scope: do Mira's identity edits cohere with the runtime + the existing canary material (spec-runtime-match on the judgment runtime)? If the guardrail gets pulled per step 1, re-confirm just the Pass-line.
3. **Commit the held bundle** (the 4 files above, minus whatever step 1 pulls) — one commit superseding `f4083e6`.
4. **Justin's explicit deploy-go** (never auto). Then **walk Justin through the M1 setup** — VIP the `#emergency-alerts` Slack bot + a phone-DND smoke test (only he can confirm it breaks his DND); set `SLACK_EMERGENCY_WEBHOOK_URL`. Then `railway up` (from `evryn-backend`).
5. **Post-deploy:** the `notify_queue` **table-DROP** (AFTER the code deploy — old live code SELECTs it; write `drop-notify-queue-table.sql`, dev-first + pre/post `pg_dump`, ADR-037 method); **reap** the 5 go-live branches + the integration worktree (`git worktree remove` + `git branch -d` + `git push origin --delete`); doc absorptions (delete the retired `notify_queue` machinery from `ARCHITECTURE.md`/`schema-reference.md`; update operator-guide quiet-hours sections; update the current-state Slack line).

**The two Gate-B gates** (the only true go-live blockers, both in the bundle): **M1** silent-death channel + the **cost pass-stamp**. Everything else is approval-gate-backstopped.

## DONE THIS SESSION — do NOT redo
- **QC integration-QC GO'd the whole bundle** (first pass, with Mira's full Phase-6 batch merged).
- **QC *and* DC independently vetted the AC-written pass-stamp → both GO** (the recovery for an AC code-slip). DC's 2 minor build-lens should-fixes (empty-name guard in `gatekeeperFirstName`; export+unit-test its branches) are **non-blocking, Mark-unreachable** → bounced to **AC3 (SPRINT Step 54d)**.
- **Mira's 2 identity edits integrated** (Pass-line + guardrail) and her note reaped. She grounded both in the code herself.
- **Codification:** added two beats to AC `CLAUDE.md` (Orchestrating DC and QC) — **"AC conducts — does not write the deterministic code"** (general division-of-labor) and **"identity files *are* runtime"** (mechanics/usage = AC, voice = Mira, QC's spec-runtime backstop covers identity). **QC + DC manuals were already covered → skipped** (Justin's de-dup call — don't re-add).
- **SF1/SF2 + the 3 forward-persistence findings → AC3** (SPRINT Steps 54d + 54). Legibility pass on Steps 53/54 (committed).

## SPIN AFTER DEPLOY
- **AC3** (off the freshly-merged `main`, post-deploy — avoids colliding with the bundle): SPRINT **Step 54** (the 3 forward-persistence findings — **(b) is the keystone**: forwards aren't persisted to `messages`, only the 6-month-TTL `emailmgr_item`; the lead design option is **lazy-backfill** so it doesn't flood the gatekeeper's `getRecentMessages` window — see Step 54b for the full design call; (a) triage prior-history → permanent store; (c) name-capture; (d) pass-stamp UUID→message-id swap), **Step 54d** (SF1/SF2), the **Phase 3–5 build batch**, and **Step 53** (the dedicated composition investigation — make Evryn reliably load the right module + stop the identity prefix growing unbounded). Build properly (design → DC → QC).
- **AC2** (dashboard) — briefed at `docs/sessions/2026-06-12-dashboard-side-ac-brief.md` (pushed); a read-only product-vitals / liveness tab. Justin spins it (likely the other machine). Independent of this thread.

## OWED / DEFERRED (explicit — not lost, just not done this lock)
- **Team current-state append** — a coordination note (AC0 ran the go-live bundle close + the Mira/identity-as-runtime work this session) to `evryn-team-workspace/shared/current-state/` (latest file, signed + timestamped). **Not done** (token squeeze at compaction). Next AC0 or Justin appends post-compaction.
- **identity-as-runtime ADR (candidate, low-priority):** the concept now lives in AC `CLAUDE.md`; an ADR extending ADR-033 (permission/compulsion) + ADR-012 (trigger-composed identity) could formalize it at the v0.3 architecture pass. Not urgent.

## PROCESS NOTES (Justin, this session)
- **Justin keeps HIS work sequential** — don't fragment his attention across many parallel asks; batch decisions into clean numbered ballots (sub-items a/b/c; frame yes/no or named options; section-ballot under each area + a full ballot at the end).
- **AC does not write code** (now in CLAUDE.md). Even small fixes → DC. The stamp this session was the slip that prompted the rule.
- When a doc/instruction is marked **"locked" / carefully-chosen**, a reviewer's flag is a *question to reason through + surface*, not a change to reflexively implement (the pass-stamp `[shareable_with_user]` tag was dropped then reverted this session for exactly this reason — the comment at `buildPassStamp` documents why).

— AC0, 2026-06-15

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
