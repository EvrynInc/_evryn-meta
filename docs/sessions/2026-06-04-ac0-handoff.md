# AC0 Handoff — 2026-06-04

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

**From:** AC0 (2026-06-03 evening session — ran the Mark wipe, pivoted the integration test to create-from-zero, reconciled the go-live docs, cleaned up the ADR-029 phantom controls, restructured QC's pattern home, ran the #lock). Context got heavy near the end; this handoff is written deliberately so you start clean and don't inherit my fatigue.
**To:** the next AC0.
**Why a handoff:** last session ended with the validated build on master but not deployed, and a database move in flight. The move is now **done** (AC1) — so your mission is the **cutover** (point Railway at the new DB, migrate, deploy) and then the **create-from-zero integration test.** Details below.

---

## Cold start — do this first, no shortcuts

**Run the FULL product-architect cascade** per `CLAUDE.md` SESSION STARTUP — the Hub, technical-vision, `evryn-backend/docs/ARCHITECTURE.md`, **the runtime `evryn-backend/src/`** (from the right tree — see **Branch / tree** just below before you load it), and the active BUILD doc. Do **not** trim it because the task "feels" like just a deploy — you're about to change the live system. (Two AC0s in a row, including me, were reminded of this; don't be the third.)

Then read, in this order:
1. `docs/current-state.md` — the snapshot. The Oregon DB move (done), the cutover plan, and the create-from-zero pivot are there.
2. `_evryn-meta/CHANGELOG.md` — the `2026-06-03 evening` entry is this session.
3. This doc — your mission (the cutover + the create-from-zero test) and the one thing to sanity-check before you deploy against the new DB (the system-actor UUIDs below — AC1's migration already handled it, but verify).

---

## Branch / tree — read from the right place (initial load AND during work)

**For the cascade (initial load):** read everything from the **main `evryn-backend` tree, on `master`** — *not* a feature worktree. **Verify the branch first:** `git -C evryn-backend branch --show-current` should say `master`. AC1 and others are active in this shared repo, and a shared working tree can be switched out from under you. Two things about master will look "off" but are correct:
- **Master is ahead of what's live.** Master is the validated-but-undeployed bundle (Mira's beat, ADR-029 cleanup, the QC fixes, the arch sync); the *deployed* runtime is still `6e27e3ea`. So the `src/` you read is **not** what's running in production yet — that's the whole point of the pending cutover.
- **The 036 migration isn't applied yet.** Master's runtime references `emailmgr_items.original_from_user_id`; that column doesn't exist in the DB until you run the migration at cutover. If you read the runtime and then query the DB, the column is absent — **expected, not a bug.**

**Other worktrees exist** (`evryn-backend-mira-036` — now redundant, its beat is merged to master; reap when convenient. `evryn-backend-team` — the team's). The canonical tree is the main `evryn-backend` on `master`.

**During work (later loads + edits):** this is a **shared, multi-agent repo** (AC1 on the DB/cutover, the team in their worktree). **Verify the branch before every edit** (not just every commit). **Stage explicitly by path — never `git add -A`** (you'll sweep others' uncommitted work). For any DC/QC/AC2 subagent you spin, give it its own worktree on its own branch (per `CLAUDE.md` → Worktree & Branch Discipline); don't let it touch the main tree or `master`.

---

## THE DB MOVE IS DONE — here's the cutover (you own the Railway side)

**Done 2026-06-03 eve (AC1).** Prod is migrated to a new us-west-2 Supabase project **"Evryn Product (West Coast)"** — verified faithful copy (identical rows, RLS/policies/comments/citext+vector preserved, system-actor UUIDs intact), and the runtime **connection test passed**. The us-east project was crossing the country on every query (Railway is us-west); this fixes it. Old East (`maruxkjwlfltlmureqkt`) is still live + untouched, to retire after cutover.

**You (AC0) own the Railway cutover** (Justin's call, 2026-06-03). Sequence:
1. **Repoint Railway env** — `SUPABASE_URL` / `SUPABASE_SERVICE_KEY` → the Oregon project (creds are in `backend/.env`). **Remind Justin: re-upload `.env` to Bitwarden after.**
2. **Back up Oregon, then apply the 036 migration to OREGON** (not East — Oregon mirrors East's pre-036 state), BEFORE the deploy: `npx supabase db query --linked -f backups/adr-036-original-from-user-id-migration.sql` (CLI linked to Oregon). + verify/add the `users.email` UNIQUE constraint (QC's idempotency finding — cheap here).
3. **`railway up`** — deploy the validated master onto Oregon.
4. **Confirm healthy on Oregon** (poll cycle, Slack connect, a smoke query) → then **retire old East.**

Coordinate the moment with Justin; the integration test (next) needs him (he plays Mark). AC1 did the DB; the Railway side is yours.

---

## The UUID check (was a trap — AC1's migration resolved it)

The runtime **hardcodes** two user UUIDs as FK anchors: Evryn = `1dc33f28-a803-4558-9bd1-fbaafbb5ea99`, Operator = `5db9d677-ed7e-49c3-9a91-73458e4ba45d` (grep `EVRYN_USER_ID` / `OPERATOR_USER_ID` in `process.ts`/`classify.ts`/`flow.ts`). If Oregon didn't carry these exact UUIDs, the whole pipeline would break (message writes, scope inheritance, approval logging all FK to them). **AC1's migration was a faithful `pg_dump → restore`, which preserves UUIDs — verified (identical rows).** So this is handled. Still cheap to sanity-check once after the cutover: `SELECT id, role FROM users WHERE id IN ('1dc33f28-a803-4558-9bd1-fbaafbb5ea99','5db9d677-ed7e-49c3-9a91-73458e4ba45d');` against Oregon — both present with roles system/admin = good.

---

## Your mission — the cutover + create-from-zero test

The validated bundle is on **master** (QC-verified GO; includes Mira's beat, the ADR-036 arch sync, AC1's Oregon backups), not deployed (live = `6e27e3ea`). Your arc, in order:

1. **Run the cutover** — the full sequence is in the section above ("THE DB MOVE IS DONE — here's the cutover"): repoint Railway env → Oregon, **back up Oregon**, apply the 036 migration **to Oregon** (migrate BEFORE deploy — master's runtime references the `original_from_user_id` column the migration adds), `railway up`, confirm healthy → retire East. Verify the runtime's `.env` and the Supabase CLI target the *same* (Oregon) project (they diverged on me once). `railway up` is the one loaded gun — coordinate the moment with Justin. **Bitwarden re-upload after the env change.**
2. **Mira's beat is already merged** (master `f24aa0e`, reviewed clean against the identity-file checklist) — nothing to do, but know it references the 036 migration column, so it can't be *exercised* until the migration applies (step 1 handles the order). *(The old `mira/adr036-triage-beat` worktree + branch are redundant now; reap when convenient.)*
3. **Run the create-from-zero integration test** — read `tests/integration-test-v02.md` + `tests/fixtures/integration-test-script.md` first (the test was reshaped this session; what changed is in the next section). Justin plays Mark.

---

## What changed about the test (read before running it)

**Mark is wiped to ZERO.** The DB holds only Evryn + Operator + one inert Google-Workspace test lead. There is **no Mark record** — that's intentional.

**The test now exercises CREATE-FROM-ZERO, not look-up-existing:**
- **Phase 2 (create):** Justin introduces a never-seen Mark on Slack → Evryn must **create his record via the `create_user` tool** (the ADR-036 tool; this is its first real test) → then research + draft the intro. Watch the tool she reaches for — `create_user` is right; `supabase_upsert` makes a malformed record. If she tries to draft before creating him, `submit_draft` fails by design (it requires the user to exist).
- **Phase 2b (find):** AFTER onboarding, open a fresh top-level Slack thread about the now-existing Mark → this re-tests the ADR-030 verify-and-lock *find* path. (Create-from-zero alone doesn't exercise it; 2b restores that coverage.)

**Go-live now works the same way** (operator-guide STEP 0 was reconciled): at launch Justin introduces the *real* Mark and Evryn creates his record — no hand-built row, no `TEST_RECIPIENT` flip. The approval gate (Justin sees every recipient before send) is the safety backstop. QC reviewed this model: GO, sound, arguably safer.

**Watch the one thing pure tests can't cover.** When Mark forwards emails in the test (the triage phase), watch whether Evryn actually *executes* the prior-history lookup at triage time — Mira's beat checks `emailmgr_items.original_from_user_id` history *before* classifying (to catch a resubmit-after-a-pass). Unit tests can't verify she really does this in the live loop; this run is the proof. (Requires the 036 migration applied first — your cutover step.)

---

## Traps & nuance (the stuff that bites)

- **Subagent memory.** Subagents are **blocked from Edit/Write on `.claude/` paths** (shell works, but has an encoding trap). So when you spin QC / AC2 / DC and want a memory or learning recorded, have the subagent **output** the text and **you** write it — don't ask it to write its own memory.
- **Briefing QC (you'll spin her for the deploy review).** QC's manual is hardened — she reads `ARCHITECTURE.md` + the full runtime *every* review and scopes her own blast radius. Brief her with the system-context files **named**; **don't pre-scope her** ("only look at these files") — that defeats the independent check she exists to provide.
- **The gun's always loaded.** Auto-deploy is OFF, but treat the live system as hot — confirm state before any risky op (the cutover's `railway up` and the Oregon migration are the loaded ones). If the auto-deploy state ever changes, update `current-state.md`.
- *(Migrate-before-deploy and per-subagent worktree discipline are in the cutover + Branch/tree sections above — don't lose them.)*

---

## Open loops (carry forward — none block the cutover/test mission)

- **Doc-syncs:** `ARCHITECTURE.md` Data Model (036 FK + `create_user`) — **DONE 2026-06-03 eve.** `LEARNINGS.md`/`AGENT_PATTERNS.md` → largely captured already. **team CLAUDE.md** "identity-pushes-trigger-redeploys" line is stale (auto-deploy OFF) — **routed to Soren** (in the team current-state), not yet fixed.
- **QC's pattern home moved this session** — `quality-patterns.md` is dead (never existed, wasn't auto-loaded); QC's patterns now live in a "Patterns This Role Watches For" section in her CLAUDE.md, auto-loaded every trip, and **AC promotes** them there (she can't write her own CLAUDE.md). The pending EVR-71/68 pattern (audit dedup keys for NULL/empty when a fix adds a deliberate re-processing path) was landed there 2026-06-03 eve. Going forward: promote QC-surfaced patterns same-session, don't forward them.
- **Pre-go-live production-readiness pass (you spin AC2).** Passing the integration test ≠ ready for Mark. **HARD gates** (before real go-live): DB **backups + rollback** (none today); the **`#emergency-alerts` Slack bot** — a **silent-death detector** (Evryn-not-polling / Gmail-or-Slack auth error / process crash) that posts to `#emergency-alerts` with a **DND-override** so a 3am silent death (→ Evryn goes dark → Mark's forwards pile up → Mark ghosted, Justin never knows = pilot-ending) actually wakes Justin. **NOT Twilio.** **RLS verification** on all tables. **NICE** (post-go-live OK): PII anonymization, Sentry, dashboard, rate-limits. **Scope split:** AC1 owns the DB-specific backups/rollback piece; AC2 takes the rest, so they don't duplicate. Give AC2 a bulletproof charter (full cascade, don't trim its load), kept under your coordination.
- **EVR-72** (follow-up loads gatekeeper not contact) — enabled by 036's FK, not fixed: `checkFollowUps` still loads only the gatekeeper; folding in the contact record is a small follow-up trip.
- **Session-doc retirement:** I flagged `2026-06-03-ac0-handoff.md` (the handoff that fed *this* session — superseded by this one) to Justin as a retirement candidate. Confirm with him before moving it to `historical/`. Don't move silently.

---

## Protocols you'll need
- `evryn-team-workspace/shared/protocols/identity-file-review.md` — merging Mira's beat (step 2).
- `docs/protocols/ac-orchestration-protocol.md` — spinning DC/QC/AC2 subagents (brief shape + loop + merge rubric + the new "promote QC's patterns" step).
- `docs/protocols/lock-protocol.md` — when Justin says `#lock`.

— AC0, 2026-06-03 evening (written for the 2026-06-04 instance)

---

## SESSION 2 ADDENDUM — 2026-06-04 evening (AC0 #2, for the next AC0)

**The mission above (cutover + create-from-zero test) is DONE.** Everything above is now historical context; read this addendum for the current state, the live-test findings, and what's left. (Written thorough because I'm handing off at ~64%-to-compaction.)

### What got done this session
- **Cutover to Oregon: COMPLETE.** 036 migration rehearsed on dev, then applied to Oregon prod (pre/post `pg_dump` backups in `evryn-backend/backups/oregon-{pre,post}-036-2026-06-04.sql` — **untracked**, commit at next lock). Railway env repointed → Oregon (`SUPABASE_URL`/`SUPABASE_SERVICE_KEY`); master (`1610f3b`) deployed + healthy (Supabase connected, Slack socket up, polling). **Apply method:** full-path PG17 psql (`/c/Program Files/PostgreSQL/17/bin/`) + `SUPABASE_DB_URL_{PROD,DEV,EAST}` from `backend/.env` (see `backups/README.md`). Oregon prod ref `wvaaqwapueycyxyhxdnh`, dev `maqkdesopsskptpxjbqs`. **East still live — AC0 retires it once FULLY confident West is flawless; don't delete yet.**
- **AC2 pre-go-live audit: DONE** (report: `docs/working/ac2-to-ac0.md`). Path is short. **One true build blocker = M1 (silent-death detector).** Backups + RLS already satisfied (RLS live-verified ON, 5 tables, prod+dev). Everything else can-wait. AC2 round-2 staged in `docs/working/ac0-to-ac2.md` (codify the delineation + clean stale docs) — **Justin relays the go.**
- **Integration test Phase 2 (create-from-zero): PASSED + full approval→send validated end-to-end.** Lookup by email → by name → nothing → **`create_user`** (NOT `supabase_upsert` — the core validation) → `set_thread_scope` → deep research (hit the wow bar) → on Justin's confirm, drafted intro → approved → `review@` → signed-off-with-code → **sent to Mark (`systemtest@`), "looks awesome."** Justin replied as Mark; she responded; continuity works.

### Current state (session end)
- **Oregon = live prod** (master, healthy). East = live, untouched, retire-when-confident.
- **Integration test PAUSED mid-onboarding** (Justin went to dinner during volleys). Phase 2 + approval/send DONE; **Phases 3-6 (onboarding depth + triage validation) still to run** — resume later.
- **Real-Mark email NOT confirmed sent** (Justin's call; he was still validating via the test). **If/when he sends it, M1 becomes time-sensitive** (silent-death net before Mark engages).
- `POLL_INTERVAL_MS=10000` (test cadence — slow at Mark-live). `SEND_ENABLED=true` (correct, already on in prod). `TEST_RECIPIENT`/`NODE_ENV=development` = dead config (6/8 sweep).

### The delineation (Justin's date buckets — AC2 codifies into SPRINT/BUILD/ARCH)
- **Today (6/4):** cutover ✓ · int test (paused) · real-Mark email (pending Justin).
- **Tomorrow (6/5):** M1 silent-death detector.
- **Week of 6/8:** adversarial test (Justin wants soon; + dummy/wrong-Mark scenario) · 4 QC resilience items · EVR-72 · dead-config sweep · S2 SSRF mini · written DB-rollback procedure.
- **v0.3 push:** S1/S2 architectural · dashboard · Sentry · PII anonymization · rate-limits · v0.3 RLS policies · web app.

### LIVE-TEST FINDINGS (the meat — route these)

**A. Context/history architecture → SOREN (runtime) + MIRA (identity). The through-line.** Justin wants Evryn **completely out of the context game.**
- Her message to Mark had a "conversation context" block at the bottom AND the runtime *already* attaches prior-message context (`loadConversationContextBlock`, `flow.ts`). **Doubled.**
- Runtime history labeling shows "(user) via email" → should be **"Mark Titus" + the actual email**, harvestable from the incoming From string (don't reinvent).
- **Principle (AC0's synthesis):** context is 100% a RUNTIME concern. Evryn writes the message; the runtime appends history (labeled name+email). Kills the doubling, fixes labeling, cuts compute + failure points, AND keeps her context lean as volleys pile up (the scaling/compaction problem Justin flagged).
- **(1) SOREN/runtime:** runtime owns context attachment + labeling (name+email from incoming string); fire "append previous messages" on `submit_draft` so Justin can verify attachment. **(2) MIRA/identity:** strip Evryn's context-construction instruction entirely. → Draft note: `docs/working/ac0-to-soren-context-architecture.md`.

**B. MIRA dispatch** (`docs/working/ac0-to-mira-dispatch.md`):
- **Verify-and-lock teeth.** `operator.md` HAS the beat (verify→confirm→lock scope) but it doesn't gate **substantive scoped work** — she researched + wrote 2 notes BEFORE the confirm. Fix: confirm gates ALL substantive scoped work; create-from-zero does a LIGHT id-check → confirm → then deep. Likely tier-4 bump (ADR-033). (Justin: "it already says this, just needs teeth.")
- **"Gold" is internal-only**, not customer-facing (gold = raw nugget, not a match until Mark confirms). With users use "match"/"good fit." Mira finds where "gold" appears + ensures internal-only.
- Strip the conversation-context instruction (per A2).
- **Test-vs-script:** integration-test "tell me about your world" doesn't track the script. Mira's eyes — maybe modify the test; gentle elicitation is good. One more pass.

**C. Adversarial:** add a **dummy/wrong-Mark** scenario → `tests/adversarial-test-v02.md` (6/8). (create-from-zero had no wrong-Mark risk — she built from Justin's explicit intro.)

**D. Operator-instruction 3-tier model (for the record):** identity files = permanent · operator profile pending_notes = durable curated · **meta layer = soft toggleable preferences** (carry forward via `getRecentMetaMessages`, easy on/off by instruction). Justin is KEEPING "always first-draft in Slack → iterate → `submit_draft` only when happy (formatting check)" as a meta instruction. **Meta carry-forward is intended, NOT a bug** — the earlier "wipe must clear all meta" framing was WRONG. STEP 0/wipe should preserve real meta instructions; only pure debris (Google notifs, "loaded for volley" dumps) is worth an eventual light tidy.

### Next AC0's mission
1. Resume + finish the integration test (Phases 3-6) — Justin plays Mark.
2. **M1 silent-death** (tomorrow): one VIP'd Slack channel (DND-break) + Railway-native crash trigger + in-process auth/poll-stall detector (Justin's "fewest points of failure"). Spin DC → QC.
3. Route findings: **Mira dispatch** + **Soren** context-architecture note.
4. AC2 round-2 (relay Justin's go).
5. Go-live tunables (poll cadence, dead-config sweep) before real Mark engages.
6. Retire East when confident.
7. Standing #lock-deferred captures: current-state East-retirement-ownership line; CLAUDE.md DB-access pointer for AC+QC; propose "align-before-editing-complex-docs" rule; commit the Oregon backup `.sql`.

### Traps
- **Don't commit without Justin's per-commit go** (he reviews in SC). This session's work is written, NOT committed.
- Oregon backup `.sql` files untracked in `evryn-backend/backups/`.
- Handing off near compaction — hence the thoroughness.

— AC0 #2, 2026-06-04 evening

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
