# AC0 Handoff — 2026-06-04

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

**From:** AC0 (2026-06-03 evening session — ran the Mark wipe, pivoted the integration test to create-from-zero, reconciled the go-live docs, cleaned up the ADR-029 phantom controls, restructured QC's pattern home, ran the #lock). Context got heavy near the end; this handoff is written deliberately so you start clean and don't inherit my fatigue.
**To:** the next AC0.
**Why a handoff:** the deploy + integration test is **blocked** on a database move that Justin + AC1 are running. You pick up when it lands.

---

## Cold start — do this first, no shortcuts

**Run the FULL product-architect cascade** per `CLAUDE.md` SESSION STARTUP — the Hub, technical-vision, `evryn-backend/docs/ARCHITECTURE.md`, **the runtime `evryn-backend/src/`**, and the active BUILD doc. Do **not** trim it because the task "feels" like just a deploy — you're about to change the live system. (Two AC0s in a row, including me, were reminded of this; don't be the third.)

Then read, in this order:
1. `docs/current-state.md` — the snapshot. The DB-move blocker and the create-from-zero pivot are there.
2. `_evryn-meta/CHANGELOG.md` — the `2026-06-03 evening` entry is this session.
3. This doc — your mission, the gating blocker, and the traps (especially the UUID trap — read it before you touch the Oregon DB).

---

## THE GATING BLOCKER — confirm before you do anything deploy-related

**The Supabase DB is being moved from us-east to Oregon (us-west).** Why: the "Evryn Product" project (`maruxkjwlfltlmureqkt`) is us-east, but Railway runs us-west — every query crosses the country both ways, hundreds of times a day. **Justin + AC1 are replicating/moving it to Oregon** (`evryn-backend/backups/*-oregon-2026-06-03.sql` are theirs — leave them alone). Until this lands and the envs are re-stitched, **nothing deploys and the integration test can't run.**

**Your first action: ask Justin where the DB move stands.** Do not assume it's done. Do not run `railway up`, the migration, or the test until he confirms the Oregon DB is live and the envs are re-stitched.

---

## ⚠️ THE TRAP THAT WILL BITE — system-actor UUIDs must survive the move

The runtime **hardcodes** two user UUIDs as foreign-key anchors:
- **Evryn** = `1dc33f28-a803-4558-9bd1-fbaafbb5ea99`
- **Operator** = `5db9d677-ed7e-49c3-9a91-73458e4ba45d`

(grep `EVRYN_USER_ID` / `OPERATOR_USER_ID` — they're in `process.ts`, `classify.ts`, `flow.ts`.)

**If the Oregon DB does not carry these exact UUIDs, the entire pipeline breaks** — message writes, scope inheritance, approval logging, everything FK's to these. A `pg_dump → restore` preserves them; a *fresh project + hand-recreated rows* would NOT (new UUIDs). **Before you migrate or deploy against Oregon, query the Oregon `users` table and confirm both UUIDs are present with the right roles.** If they're not, stop and surface to Justin — do not "fix" it by editing the hardcoded constants without a deliberate decision.

---

## Your mission (when Justin confirms the DB move landed)

The validated bundle is on **master `f24aa0e`** (QC-verified GO; includes Mira's beat + AC1's Oregon-backup commit), not deployed (live = `6e27e3ea`). Sequence — **order matters**:

1. **Verify the Oregon DB** — system-actor UUIDs present (above); envs re-stitched (`SUPABASE_URL` / `SUPABASE_SERVICE_KEY` point at Oregon); the runtime's `.env` and the Supabase CLI target the *same* project (they diverged on me once — verify). **Remind Justin: re-upload `.env` to Bitwarden after the env change.**
2. **Mira's identity beat — DONE 2026-06-03 eve, already on master (`f24aa0e`).** Reviewed clean against the identity-file checklist and landed via cherry-pick (the branch base was badly stale; a naive merge would have looked like a -5044-line revert — cherry-pick avoided it). Nothing to do here. **One dependency you must honor:** the beat references the 036 migration column (`original_from_user_id`), so it must not be *exercised* before the migration applies (step 3) — already the order. (The old `mira/adr036-triage-beat` worktree + branch are now redundant; reap when convenient.)
3. **Back up the Oregon DB**, then **apply the 036 migration** (`backups/adr-036-original-from-user-id-migration.sql` via `npx supabase db query --linked -f <file>`) — **against Oregon now, not the old DB.** Migrate BEFORE deploy (master's runtime references `original_from_user_id`, which the migration adds). Also verify/add the `users.email` UNIQUE constraint (QC's idempotency finding — non-blocking but cheap here).
4. **Deploy: `railway up`** from `evryn-backend/`. Auto-deploy is OFF by design — the deploy is manual and yours. Coordinate the moment with Justin (`railway up` is the one loaded gun).
5. **Run the integration test — now CREATE-FROM-ZERO.** Read `tests/integration-test-v02.md` + `tests/fixtures/integration-test-script.md` first; the test was reshaped this session (details below).

---

## What changed about the test (read before running it)

**Mark is wiped to ZERO.** The DB holds only Evryn + Operator + one inert Google-Workspace test lead. There is **no Mark record** — that's intentional.

**The test now exercises CREATE-FROM-ZERO, not look-up-existing:**
- **Phase 2 (create):** Justin introduces a never-seen Mark on Slack → Evryn must **create his record via the `create_user` tool** (the ADR-036 tool; this is its first real test) → then research + draft the intro. Watch the tool she reaches for — `create_user` is right; `supabase_upsert` makes a malformed record. If she tries to draft before creating him, `submit_draft` fails by design (it requires the user to exist).
- **Phase 2b (find):** AFTER onboarding, open a fresh top-level Slack thread about the now-existing Mark → this re-tests the ADR-030 verify-and-lock *find* path. (Create-from-zero alone doesn't exercise it; 2b restores that coverage.)

**Go-live now works the same way** (operator-guide STEP 0 was reconciled): at launch Justin introduces the *real* Mark and Evryn creates his record — no hand-built row, no `TEST_RECIPIENT` flip. The approval gate (Justin sees every recipient before send) is the safety backstop. QC reviewed this model: GO, sound, arguably safer.

---

## Open loops (carry forward — none are yours to fix tonight)

- **Pending doc-syncs (need Justin's auth):** `ARCHITECTURE.md` Data Model → the 036 `original_from_user_id` FK + `create_user` tool. `LEARNINGS.md`/`AGENT_PATTERNS.md` → session learnings. **team CLAUDE.md** "identity-pushes-trigger-redeploys" line is stale (auto-deploy OFF) — Soren to reconcile.
- **QC's pattern home moved this session** — `quality-patterns.md` is dead (never existed, wasn't auto-loaded); QC's patterns now live in a "Patterns This Role Watches For" section in her CLAUDE.md, auto-loaded every trip, and **AC promotes** them there (she can't write her own CLAUDE.md). The pending EVR-71/68 pattern (audit dedup keys for NULL/empty when a fix adds a deliberate re-processing path) was landed there 2026-06-03 eve. Going forward: promote QC-surfaced patterns same-session, don't forward them.
- **Pre-go-live production-readiness pass (spin AC2).** HARD gates before real go-live: DB **backups + rollback**, the **`#emergency-alerts` Slack bot** (silent-death detector — NOT Twilio), **RLS verification** on all tables. Justin's plan: you (AC0) spin AC2 to run it, coordinated with AC1's DB work. Give AC2 a bulletproof charter; don't trim its architectural load.
- **EVR-72** (follow-up loads gatekeeper not contact) — enabled by 036's FK, not fixed.
- **Session-doc retirement:** I flagged `2026-06-03-ac0-handoff.md` (the handoff that fed *this* session — superseded by this one) to Justin as a retirement candidate. Confirm with him before moving it to `historical/`. Don't move silently.

---

## Protocols you'll need
- `evryn-team-workspace/shared/protocols/identity-file-review.md` — merging Mira's beat (step 2).
- `docs/protocols/ac-orchestration-protocol.md` — spinning DC/QC/AC2 subagents (brief shape + loop + merge rubric + the new "promote QC's patterns" step).
- `docs/protocols/lock-protocol.md` — when Justin says `#lock`.

— AC0, 2026-06-03 evening (written for the 2026-06-04 instance)

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
