🔒 **HELD-SESSION-DOC** — the LIVE ACT working brief · **RETIRE UPON:** superseded by the next ACT's handoff at set-down · **OWNER:** ACT · **HELD SINCE:** 2026-09-10

# ACT-18 — live working brief. **Becomes the ACT-18 → ACT-19 handoff at set-down.**

*Opened 2026-09-10 by **ACT-18**, the team-runtime conductor, running on the **LAPTOP**. From the moment this file exists it is ACT's live brief; `2026.09.08-act17-act18-handoff.md` stays as the record of what ACT-18 was handed and should not be worked from.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you do not see it, re-read until you do.
>
> **How to use this file:** ACT's live state, kept current to the minute — what is running, what is armed, what is owed, what is waiting. **The test at every append: if ACT-18 died right now, could a fresh ACT pick up from this file alone?** If no, this file is behind — fix it before anything else. **While ACT-18 is live this is a working brief; at set-down it is rewritten clean as the handoff, with the load list for ACT-19.**

---

# 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP HERE.

**If what you hold is a machine-written summary of your session rather than files you actually read — you have compacted, and your run is over.**

🔴 **DO NOT RELOAD AND CONTINUE. We tried it; it does not work.** *(Justin, 2026-08-18: "the agent just sails through it and confidently flails about, unloaded.")* **An instruction to reload is a rule you must REMEMBER to obey — and remembering is precisely what compaction damaged.**

**Do these three, then stop:** ① stop working, mid-sentence if need be · ② tell Justin in one line that you compacted · ③ ask for a fresh re-spin.

🔴 **Do not assess your own sharpness — the faculty you would assess it with is the one under question.**

---

## §1 — JUSTIN'S STANDING ORDERS FOR THIS SESSION (his words where the wording matters)

1. **ADMIRAL MODE.** *"I need you to be as autonomous as is reasonably possible… be the Admiral of your fleet of Captains… don't bug me with every little thing — try to solve it yourself and just let me know what you did — but if it really does need my attention, or if it's hard to reverse, get me in right away."*
2. **PING EVERY TIME.** *"make sure you ping me every time — failing to do so will be a huge waste of time."*
3. **ACT IS HIS FOCUS.** *"you are my top priority today… if you need me, I'll drop everything everywhere else and come running."* And: *"ACP is dormant — you are my focus for the next week."*
4. **WHAT COMMITS WITHOUT HIM, AND WHAT DOES NOT.** Routine changes commit without his review — his example was reordering the changelog: *"If you change nothing else in there, you can commit that without me seeing."* **Everything important stays UNCOMMITTED for his review in SCM**: *"All the important stuff, though: leave in SCM for me."*
5. **MACHINE.** ACT stays on the **laptop**. Justin is consolidating the founding-team agents on the **desktop**.
6. **HOW TO TALK TO HIM.** Inline ballots with numbers that continue across messages; numbered `# ACT-18's Report #N` headings; a cold-open handhold on every reference; **never a bare Step number** — always a few words saying what the Step is.

---

## §2 — LIVE STATE *(ask git for SHAs; none are recorded here on purpose)*

- **`ACTj` — the money lane (SPRINT Step 19 + Step 20).** Standalone instance; **Justin spins her.** Worktree `evryn-team-runtime-actj`, branch `actj/money-layer`, cut fresh from `main` 2026-09-10, `npm ci` inside it (not junctioned). Her briefing entry — worktree named, conductor handover, a stale warning corrected — is committed in `docs/sessions/2026.09.08-act17-actj-exchange.md`. **STATE: READY, awaiting Justin's spin on the laptop.**
- **`DCm` — SPRINT Step 85, memory grading.** Subagent conducted by ACT-18. Worktree `evryn-team-runtime-dcm` (branch `dc/step85-memory-grading`, cut from `main`; `npm ci` inside it — the canonical tree's install was untouched). **STATE: trip 1 (load only) spun 2026-09-10 in the background; awaiting receipts.** Trip record and load list in §6.
- **PENDING WITH JUSTIN (2026-09-10, ~16:15) — Report #6, ballots ⑤–⑧:**
  - ⑤ **A VS Code + Claude Code extension update.** A restart kills every background process of this session — `DCm` mid-flight and both watchers — and none can be resumed after. **ACT-18 offered: now (DCm has written nothing, so only its reload is lost) or after DCm reports.** ⚠️ **Do the update BEFORE spinning `ACTj`, or it kills her too.** If he picks "now": `TaskStop` DCm, confirm its worktree is clean, then ping him "go".
  - ⑥ **`runtime-ops.md` — edit REVERTED.** Justin asked whether runtime agents need the two §1 sub-bullets; ACT-18's answer was no — they never meet the interactive cap, and today cannot write memory. **Proposed: one line in SPRINT Step 85 recording that its §1 clause is deliberately not going in — wording offered to him, not yet agreed.**
  - ⑦ **The team manual still says a `MEMORY.md` write "will silently hang until Justin approves." No longer true** — Justin confirmed 2026-09-10 that interactive agents now write memory with no prompt. **He wants the ping-before-writing instruction kept.** Offered: a reword that keeps the ping and drops the false safety claim (his file; would sit in SCM).
  - ⑧ **`evryn-team-runtime/docs/ARCHITECTURE.md`** (invariant 8's second door) — Justin: *"architecture.md is fine."* **Uncommitted until his explicit go.**
  - ✅ **His point that "can't rewrite its own memory" holds only under Phase-1 policy → filed as SPRINT Step 87.**
- ✅ **SPRINT housekeeping COMMITTED to `main`:** Steps 42, 47 and 63 relabelled to match their 2026-09-08 merge (42 and 47 are now PARTIAL, each naming its residue in the label), and **Step 86** filed — the `settingSources` finding.

---

## §3 — INSTRUMENTS — arm your own; never inherit a task id

| Instrument | Watches | Cadence | Its one job |
|---|---|---|---|
| **Inbox watcher** | `_evryn-meta/docs/mailboxes/inbox-act.md`, firing on COMMIT, with a `git fetch` each tick | 45s, emits only on change | *"Did anything land for me?"* |
| **ACTj channel watcher** | `_evryn-meta/docs/sessions/2026.09.08-act17-actj-exchange.md`, firing on COMMIT | 45s, emits only on change | *"Has ACTj posted?"* |
| **Heartbeat** | ACT itself | 5 min daytime · 10 overnight | *"Are you still moving?"* — nothing else |

🔴 **Anchor every watcher on `$HOME` and `git -C`, never on the working directory, and print a proof-of-fire line before the loop.**

---

## §4 — 📋 BACKLOG — one table, numbers never reused *(carries ACT-17's numbering)*

| # | Item | Owner | State |
|---|---|---|---|
| **B1** | `runtime-ops.md:9` cites a SESSION doc for its open questions — **SPRINT Step 77** | ACT → Justin | **Open. Needs a FULLY-LOADED sub-AC** — Justin barred a light-loaded coordinator from drafting it. |
| **B5** | ~30 ACT-lineage `HELD-SESSION-DOC`s awaiting this lineage's evaluation *(derive: `git grep -n "HELD-SESSION-DOC"`)* | ACT | **DEFERRED by Justin** until the main show is underway; its own sub-lane; **budget it as a DISCOVERY pass.** |
| **B13** | `ThreadDormancyWriteRow` referenced nowhere | → **SPRINT Step 47's residue** | Lives there now. |
| **B15** | `_evryn-meta/CHANGELOG.md` entries out of date order | ACT-18 | ✅ **CLOSED 2026-09-10** — reordered as a verified byte-exact move and committed on Justin's go. |
| **B16** | `evryn-backend-qc-sandbox` detached worktree on the laptop | ACP | Not ACT's to reap. |
| **B18** | 🆕 **`runWake` never sets the SDK's `settingSources`, so a wake would load any `CLAUDE.md` or settings file where it runs — safe today by coincidence** | ACT → DC+QC | **Filed as SPRINT Step 86. Fix HELD until `ACTj` is done** — it sits in the `query()` options block beside `maxBudgetUsd`, the money layer's. |
| **B19** | 🆕 SPRINT Steps 42, 47, 63 never had their labels flipped at the 2026-09-08 merge | ACT-18 | ✅ **CLOSED 2026-09-10** — relabelled and committed; 42 and 47 are PARTIAL, each naming its residue in the label. |
| **B20** | 🆕 Five ACT-lineage messages sit UNREAD in `inbox-acp.md`, including ACT-17's three open questions (W1–W3) | — | **ACP is dormant for about a week (Justin, 2026-09-10). Nothing of ACT's is blocked on them.** |

---

## §5 — ⏳ WAITING ON

**W1–W3** — ACT-17's three questions to ACP *(shared generator vs two forks · does `evryn-backend`'s ARCHITECTURE carry the hand-maintained-inventory shape · the generator's contents-from-disk / SHA-from-git defect class)*. **Full statement in `2026.09.08-act17-act18-handoff.md` §6.** **All unanswered; ACP dormant; nothing blocked.**

---

## §6 — 🔴 SUBAGENT & LANE LEDGER — enumerate, never work from memory

**Subagent resume is SESSION-BOUND: when ACT-18's session ends, every subagent below becomes permanently unreachable. A returned report is not persistence; only a committed file is.**

### `DCm` — DC, SPRINT Step 85 (the runtime grades memory below current-state; an unreadable memory composes as empty and tells nobody)

- **Worktree:** `evryn-team-runtime-dcm` · **branch:** `dc/step85-memory-grading` · cut from `main` at spin. **ACT-18 owns its lifecycle; reap after merge.**
- **TRIP LOG.** Trip 1 (load) spun 2026-09-10 → **receipts VERIFIED:** 38/38 items, every canary confirmed; 42 calls declared = 42 counted by the harness; one partial read (the team manual hit the Read cap) disclosed and completed; one line-count delta flagged rather than reconciled (the memory design-of-record's canary sits one line past its `wc -l` count — no trailing newline). ⚠️ **The per-tool breakdown is NOT observable** — the agent's transcript file read as empty, the documented completed-agent quirk — so the total is reconciled but the Read/Bash split rests on its own report. **Trip 2 (work) sent 2026-09-10; awaiting its output.**
- **Model:** `opus`, pinned. **Shape:** TWO trips *(a scoped change in known files — the orchestration protocol's two-trip row)*. **Runs under `#cascade-override`**, because `dc.md`'s own cascade names the PRODUCT build; for a non-product build its AC supplies the list.
- **🔴 LOAD LIST — THE ARTIFACT OF RECORD.** Derived from `team-runtime-full-cascade.md` + the dependency map's reverse index (§4) and test-coupling table (§8) + a full read of `src/composer/index.ts`, `src/config/agents.ts` and `src/config/skills.ts`. **Line counts are HYPOTHESES**, measured 2026-09-10 with `cd "$HOME/Evryn/Code" && wc -l <path>`; **the bottom canary, not the count, proves a full read.** Runtime files are read from the DC's WORKTREE; the canonical tree carries uncommitted doc edits.

  | # | File *(rooted at the shared parent `C:/Users/jbmcg/Evryn/Code/`)* | Span |
  |---|---|---|
  | 1 | `_evryn-meta/.claude/agents/dc.md` | full — 362 |
  | 2 | `_evryn-meta/docs/hub/roadmap.md` | full — 187 *(composer layer 2)* |
  | 3 | `evryn-team-runtime-dcm/docs/ARCHITECTURE.md` | full — 160 |
  | 4 | `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-team-runtime-memory-architecture.md` | full — 504 *(the memory design-of-record, ADR-052; page it to the canary)* |
  | 5 | `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.10-acf-justin-team-runtime-design-v2.md` | full — 319 *(ARCHITECTURE's own Required Context)* |
  | 6 | `_evryn-meta/docs/decisions/050-team-runtime-sdk-mains-worker-tier.md` | full — 40 |
  | 7 | `_evryn-meta/docs/decisions/052-team-runtime-memory-wake-economics.md` | full — 106 |
  | 8 | `_evryn-meta/docs/decisions/055-thread-as-complete-record.md` | full — 146 |
  | 9 | `evryn-team-runtime-dcm/docs/BUILD-PHASE-1.md` | full — 103 |
  | 10 | `evryn-team-runtime-dcm/docs/dependency-map.md` | full — 640 by `wc -l`; **the canary is the line AFTER that count** |
  | 11 | `evryn-team-runtime-dcm/src/composer/index.ts` | full — 575 |
  | 12 | `evryn-team-runtime-dcm/src/composer/layers.ts` | full — 593 |
  | 13 | `evryn-team-runtime-dcm/src/wakes/index.ts` | full — 756 |
  | 14 | `evryn-team-runtime-dcm/src/safety/notify.ts` | full — 99 |
  | 15 | `evryn-team-runtime-dcm/src/safety/rate-gate.ts` | full — 54 |
  | 16 | `evryn-team-runtime-dcm/src/db/client.ts` | full — 27 |
  | 17 | `evryn-team-runtime-dcm/src/db/types.ts` | full — 282 |
  | 18 | `evryn-team-runtime-dcm/src/boot/env.ts` | full — 197 |
  | 19 | `evryn-team-runtime-dcm/src/config/agents.ts` | full — 59 |
  | 20 | `evryn-team-runtime-dcm/src/config/skills.ts` | full — 118 |
  | 21 | `evryn-team-runtime-dcm/src/threads/kind.ts` | full — 72 |
  | 22 | `evryn-team-runtime-dcm/src/db/paginate.ts` | full — 535 |
  | 23 | `evryn-team-runtime-dcm/migrations/010_wake_manifests.sql` · `014_composition_v2_skills.sql` | full, both |
  | 24 | `evryn-team-runtime-dcm/tests/composition-failure-honesty.test.ts` (201) · `current-state-nag.test.ts` (181) · `composer.test.ts` (328) · `composer-pagination.test.ts` (371) · `composer-scratchpad-read.test.ts` (116) · `fake-db.ts` (199) · `ceiling-db.ts` (279) | full, all seven |
  | 25 | `evryn-team-workspace/CLAUDE.md` | full — 428 *(composer layer 1)* |
  | 26 | `evryn-team-workspace/shared/protocols/runtime-ops.md` | full — 78 *(layer 1; its working copy carries an uncommitted ACT-18 edit in §1, awaiting Justin)* |
  | 27 | `evryn-team-workspace/shared/current-state/current-state.md` | full — 494 *(layer 3, composed in full, never windowed)* |
  | 28 | `_evryn-meta/docs/hub/vision-and-ethos.md` | full — 231 *(layer 4 — Lucas's spoke, per `config/agents.ts`)* |
  | 29 | `evryn-team-workspace/.claude/agents/lucas.md` | full — 201 *(layer 5)* |
  | 30 | `evryn-team-workspace/.claude/agent-memory/lucas/MEMORY.md` | full — 254 *(layer 6)* |
  | 31 | `evryn-team-workspace/shared/protocols/consolidation-protocol.md` | full — 388 *(a layer-8 registry skill)* |

  **Deliberately NOT on the trip-1 list, so a successor can see these were decisions rather than oversights:** the SPRINT *(not in DC's cascade; its Step 85 is the task, so it is named in trip 2)* · `src/digest/index.ts` *(composer imports it for layer 9 only; trip 2 may load it)* · the other four registry skill bodies *(composed only when a wakeup declares them)* · the remaining migrations and tests.

- **Trip 2 will carry:** SPRINT Step 85 as the spec *(its span in the DC's worktree, lines 212–231 — named in `<task>` and never in trip 1, because it IS the task)*; ADR-055 / cardinal invariant 9 as the framing *(complete, or loudly incomplete)*; the four mutation acceptance criteria verbatim from the Step; the note that `runtime-ops.md` §1's clause is Justin-gated and NOT the DC's to edit; ACT-18's read-disclosure; and **two fences:** **(a) keep the change inside `src/composer/` and `tests/` — `src/wakes/index.ts` is the `ACTj` money lane's territory this week, so a design that needs it STOPS and asks rather than editing;** **(b) mutation runs happen in a throwaway detached worktree cut from the DC's own tip, with `npm ci` inside it, reaped afterwards — never in a tree anyone is working in.**

### `ACTj` — standalone lane, Justin spins
- **Brief:** `docs/sessions/2026.09.08-act17-actj-money-layer-brief.md` · **channel:** `docs/sessions/2026.09.08-act17-actj-exchange.md`.

---

## §7 — VERIFIED AT SOURCE THIS SESSION, so a successor need not re-derive *(each names its instrument)*

- **The ~24.4KB memory-truncation cap is Claude Code's agent-memory auto-load, and the autonomous runtime does NOT inherit it.** *Read, not executed:* the pinned SDK's `sdk.d.ts` describes the feature as an agent definition's `memory` scope; `runWake` defines no agents and disallows `Task` and `Agent` (`src/wakes/index.ts`, the `query()` options block); layer 6 reads `MEMORY.md` whole (`src/composer/index.ts`, `readIfExists`).
- **A runtime agent cannot compact its own memory.** The authoritative `PreToolUse` gate (`src/wakes/tool-gate.ts`, read in full) default-denies every tool outside four named cases, so `Write`/`Edit`/`Bash` never run — `disallowedTools` names them too, but the runtime's own docblock calls that list a courtesy, not the fence. And none of the ten MCP tools rewrites memory *(grep of the `name:` entries in `tools.ts`, with a known-hitting control)*; `append_note` only appends, through the canary-guarded writer. *Read, not executed.*
- **`settingSources` is unset in `runWake`**, and the pinned SDK documents unset as *"all sources are loaded… must include 'project' to load CLAUDE.md files."* **It loads nothing today** — the Dockerfile copies only package files, `tsconfig.json` and `src/`; the repo tracks no `CLAUDE.md` or `.claude/`; no ancestor directory of the laptop checkout carries one *(checked with a known-present control)*. → **SPRINT Step 86.**
- **Team memory sizes, measured 2026-09-10:** seven of eight exceed the ~24.4KB auto-load cap — Nathan 107.1KB · Marlowe 83.1 · Mira 79.0 · Emma 65.6 · Soren 63.4 · Lucas 48.7 · Dominic 25.9 · Thea 6.9. ⚠️ **Justin is consolidating on the desktop, so these will move.**
- **What ACT-18 has read of the runtime:** `src/composer/index.ts`, `src/config/agents.ts` and `src/config/skills.ts` in full; `src/wakes/index.ts` lines 425–480 only; the dependency map in full. **The 41-file docblock pass sits in the session scratchpad and has NOT been read.** **Zero migrations, zero tests.** ⇒ **State this in every brief.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
