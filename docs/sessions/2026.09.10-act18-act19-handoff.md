🔒 **HELD-SESSION-DOC** — the LIVE ACT working brief · **RETIRE UPON:** superseded by ACT-19's own handoff at set-down · **OWNER:** ACT · **HELD SINCE:** 2026-09-10

# ACT-18 → ACT-19 — handoff. **Written clean. This file is self-sufficient; you need no earlier handoff.**

*Written 2026-09-10 by **ACT-18**, the team-runtime conductor, at `#packout`, on the **LAPTOP**. **Supersedes `2026.09.08-act17-act18-handoff.md` entirely** — that file is the record of what ACT-18 was handed. **Do not work from it.***

> 🟥🟥 **ACT-18 COMPACTED mid-packout (2026-09-10T23:55:45Z). `ACT-19b` — a sub-AC of ACT-19 — FINISHED the packout on 2026-09-11, working from ACT-18's transcript and from git.** **Read §13 first:** it lists every change ACT-19b made, what was verified and with which instrument, what is still a lead, and **every item that is Justin's.** **§§1–11 are corrected in place. §12 is the compacted ACT-18's appendage, kept as written except where §13.2 marks a claim wrong** — 🔴 **treat any §12 line §13.2 does not mark VERIFIED as a lead.**

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you do not see it, re-read until you do.
>
> **How to use this file:** ACT's live state — what is built, what is armed, what is owed, what is waiting. **The test at every append: if this instance died right now, could a fresh one pick up from this file alone?** If no, this file is behind — fix it before anything else.
>
> ### 📖 READ IN THIS ORDER
>
> | Read | Section | What it gives you |
> |---|---|---|
> | 🟥 **0th** | **§13**, then **§12** | 🟥 **§13: how the packout was finished, and the decisions that are Justin's. §12: the compacted ACT-18's appendage, claim by claim marked in §13.2.** |
> | **1st** | **§1** | 🔴 **WHAT TO DO FIRST** |
> | 2nd | **§2** | **YOUR LOAD LIST** — in full, here |
> | 3rd | §3 | Where the lane stands |
> | 4th | §4 | The instruments to arm |
> | 5th | §5 | The backlog |
> | 6th | §6 | ⏳ Waiting on — read before answering any mail |
> | 7th | §7 | The subagent ledger — **the DC's build, and its load list** |
> | 8th | §8 | Doc dispositions |
> | 9th | §9 | What ACT-18 read of the runtime — and the cheap recipe |
> | 10th | §10 | Justin's orders and rulings this session |
> | ref | §11 | The doc-currency gate, answered |

---

# 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP HERE.

**If what you hold is a machine-written summary of your session rather than files you actually read — you have compacted, and your run is over.**

🔴 **DO NOT RELOAD AND CONTINUE. We tried it; it does not work.** *(Justin, 2026-08-18: "the agent just sails through it and confidently flails about, unloaded.")* **An instruction to reload is a rule you must REMEMBER to obey — and remembering is precisely what compaction damaged.**

**Do these three, then stop:** ① stop working, mid-sentence if need be · ② tell Justin in one line that you compacted · ③ ask for a fresh re-spin.

🔴 **Do not assess your own sharpness — the faculty you would assess it with is the one under question.**

---

## §1 — 🔴 WHAT TO DO FIRST

**The lane's one-line state: SPRINT Step 85 (memory grading) is BUILT on a branch and needs its independent QC review; the money lane (`ACTj`) is briefed with a fresh worktree and waits on Justin's spin.**

0. **No machine switch.** ACT-18 ran on the laptop and so do you — Justin kept ACT here on 2026-09-10. **Run the ordinary repo-sync check** (`ac.md`, Repo-sync check); an arrival check is needed only if you are told you are on the desktop. ⚠️ **Justin planned a VS Code + Claude Code extension update right after this packout; a restart kills every background process of a session, so nothing of ACT-18's is still running.** **Two laptop worktrees are NOT ACT's — leave them alone:** `_evryn-meta-acm` (`acm/manual-rubric`) and `_evryn-meta-pgdump` (`dc/pgdump-schema-only`).
1. **Load §2.** It is complete and it is here.
2. **Ask Justin the two spin-up questions** *(separate acts, separate costs)*: **should I read my inbox?** and **should I arm a watcher on it?** *(`inbox-act.md` was EMPTY at set-down.)*
3. 🔴 **SPIN A FRESH QC ON STEP 85 — it is the first thing on the board.** The build is `dc/step85-memory-grading` @ `4d9d09e` (`807dc88` = code + 18 tests; `4d9d09e` = the regenerated dependency map), in worktree `evryn-team-runtime-dcm`. **§7 carries what the DC built, what it found, and the load list it was given — derive QC's list from that.**
   - **QC's load:** `qc.md` in place of `dc.md` · plus `_evryn-meta/docs/current-state.md` and `_evryn-meta/docs/hub/technical-vision.md` (her Tier 0/1) · plus the files the DC changed or added (`src/composer/memory.ts` NEW, `src/composer/index.ts`, `src/composer/layers.ts`, `tests/composer-memory-grading.test.ts` NEW, `tests/composition-failure-honesty.test.ts`, `tests/current-state-nag.test.ts`, the regenerated map) · plus `src/wakes/memory-writer.ts` and its test (the new module imports its `CANARY_LINE`) · **plus what the DC itself had to read on its work trip to build and test this** *(its receipts, transcript rec 919)*: `src/wakes/env.ts` (why test 2c carries a tripwire) · `src/wakes/capabilities.ts` · `src/digest/index.ts` lines 90–139 and 480–569 · `src/tools/tools.ts` lines 420–464 (`append_note` goes through the same writer) · `tests/dormancy-wake.test.ts` (the only other test that calls `composeWake`) · `tests/scratchpad.test.ts` (pins the layer-6 component name) · `tests/digest.test.ts`, `tests/dormancy-lifecycle.test.ts`, `tests/url-414-call-sites.test.ts` (they import `composer/index.ts`) · `tsconfig.json` + `tsconfig.tests.json`. ⚠️ **The team `CLAUDE.md` needs two reads — it is 46 tokens over the `Read` cap** *(DCm's trip-1 receipts, rec 666)*. **For a non-product build QC gets an explicit `#cascade-override` list (`qc.md`, "Non-product build targets").** **Provision her a throwaway sandbox for mutation** — you own that — **and have her re-run the breaks in §7's mutation table rather than inherit its verdicts.**
   - 🟡 **TWO THINGS ARE JUSTIN'S BEFORE THE MERGE, not after:** **(a)** the **damaged-at-rest policy** — the DC built *compose-and-flag* (a memory whose canary is missing still composes, with a warning and an alarm) and recommends keeping it; the alternative is refusing like an unreadable file. **The known cost of its recommendation, in the DC's own words: "the writer refuses notes to such a file"** — a damaged memory composes, but nothing can append to it, so its dormancy closing wakes retry (SPRINT Step 88). **(b)** **two new agent-facing texts** in `src/composer/memory.ts` — `absentMemoryNotice` (*"say so plainly in the thread rather than proceeding as though you remember"*) and `damagedMemoryWarning` (*"treat it as possibly INCOMPLETE … UNKNOWN, not absent — say so rather than inferring"*) — which program the founding-team agents' behaviour, so they are **Justin's to read** *(the team's identity layer is his, not Mira's — `ac.md`, the identity-layer table; the DC suggested "Mira or AC", and ACT-18 correctly routed it to Justin)*. 🔴 **Neither reached Justin before ACT-18 compacted** — the report that would have carried them was never sent (§12.5 D). **ACT-19 takes both to him (§13.4).**
   - **Merge** is yours after QC GO + your review + Justin's two answers *(build-level code — `ac.md`, the standing merge authorization)*. **Then reap the worktree with the `node_modules` count check around it, and write the changelog entry — the DC's changelog-worthy facts are in §7.**
4. **`ACTj` — if Justin has not spun her yet, she is ready.** Worktree `evryn-team-runtime-actj`, branch `actj/money-layer`, cut from `main` at `52edd12` on 2026-09-10, `npm ci` inside it. ⚠️ **`main` has moved since (docs and tracker only). Before she starts, run `git rev-list --count actj/money-layer..main`; her branch holds no commits, so `git -C "$HOME/Evryn/Code/evryn-team-runtime-actj" merge --ff-only main` brings it current.** **Her channel is `docs/sessions/2026.09.08-act17-actj-exchange.md`**; ACT-18's entry there names her worktree and corrects a stale warning. **The spin is one sentence, and Justin types it:** *"You are ACTj. Read and faithfully execute `_evryn-meta/docs/sessions/2026.09.08-act17-actj-money-layer-brief.md`."*
5. **Then SPRINT Step 43 — the lifecycle five.** Still the top open item: **the dormancy cadence-gate wiring is unguarded, and that gate is what stands between a model-stage failure and one ~64K Opus wake every fifteen seconds.** ⚠️ **It COLLIDES with `ACTj`:** the `checkinTask` it must extract sits at `src/scheduler/index.ts:841`, ten lines from the daily check-in dispatch site she must modify at `:851`. **Sequence it after her, or coordinate the file.**
6. ⚠️ **The other three lane briefs (`ACTt`, `ACTi`, `ACTv`) are still WRONG and still unspun** — each demands all 63 test files in full. **Justin's sequencing: ONE lane first (`ACTj`), learn from it, then re-scope the rest. Do not re-scope all three pre-emptively.**

---

## §2 — YOUR LOAD LIST

> 🔴 **NO LINE COUNTS APPEAR BELOW, AND THAT IS JUSTIN'S RULING (2026-09-08): *"they're always stale when they get forwarded like this."*** **The record backs him: `ac.md` has been forwarded as 749, then 911, and measured 940 the same week.**
>
> ⭐ **DERIVE THEM AT READ TIME:** `cd "$HOME/Evryn/Code" && wc -l <paths>`. **For anything enumerated rather than named, enumerate LIVE:** `git -C evryn-team-runtime ls-files src migrations`. ⚠️ **Never a `src/**/*.ts` pathspec — `**/` requires an intervening directory, so it silently drops every file sitting directly in `src`.**
>
> 🔴 **CONFIRM EACH FILE'S BOTTOM CANARY** (`FULL FILE LOADED`). ⚠️ **AND KNOW THE ONE WAY `wc -l` LIES: it counts NEWLINES, so a file with no trailing newline reports one FEWER line than it has — and the line it omits is the LAST one, which is the canary.** ⇒ **Read PAST the count, or `tail -1`. Never conclude "no canary" from a count-bounded read.** *(Live: `evryn-team-runtime/docs/dependency-map.md` reports 634; its canary is line 635.)*
>
> 🔴 **NAME THE WORKTREE YOU READ FROM, not just the paths** — read from each repo's canonical `main` tree unless told otherwise, and say so.

**Read in this order. The order is DEPENDENCY, not importance** — a later document is usually a diff against an earlier one, so out of order you build a picture the next document exists to correct.

| # | File | Read |
|---|---|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | **full.** Your manual. ⚠️ **NOT auto-loaded** — the injected `CLAUDE.md` is a router. |
| 2 | `_evryn-meta/docs/hub/roadmap.md` | full — the Hub |
| 3 | `_evryn-meta/docs/hub/technical-vision.md` | full |
| 4 | `evryn-team-runtime/docs/ARCHITECTURE.md` | full — **cardinal invariants 8 and 9, and "why local reasoning often fails here"** |
| 5 | `evryn-team-runtime/docs/BUILD-PHASE-1.md` | full |
| 6 | `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` | **full — the Steps, and the status source of truth** |
| 7 | **`evryn-team-runtime/docs/dependency-map.md`** | **full.** ⭐ **NEW TO THIS LIST and the reason the lane is unblocked** — generated, complete by construction. **Its §4 reverse index is how you derive a lane's load.** |
| 8 | `_evryn-meta/docs/current-state.md` | full |
| 9 | `_evryn-meta/CHANGELOG.md` | **the skim its own header prescribes** — the top ~100 lines plus section headers |
| 10 | `evryn-team-runtime/CHANGELOG.md` | **the skim: the top ~100 lines plus all section headers.** *(Settled by ACT-19 on 2026-09-11, after reading the whole file at Justin's direction.)* Everything below the top ~100 lines records work whose status and load-bearing detail now live inline in its SPRINT Step (the tracker's rule 9), and whose standing lessons live in ARCHITECTURE and the manuals. **Read an older entry in full when your work touches what it records** — e.g. before a deploy, the 2026-07-14 entry is the record of the runtime's first live shakedown and first Railway deploy. |
| 11 | `_evryn-meta/docs/protocols/mailbox-protocol.md` **+** `_evryn-meta/docs/mailboxes/inbox-act.md` | full, both. ⚠️ **`docs/mailboxes/README.md` DOES NOT EXIST.** |
| 12 | **`_evryn-meta/docs/sessions/2026.09.08-act17-actj-money-layer-brief.md`** **+ its exchange doc** | **BOTH.** ⭐ **NEW — this is the live lane and the first brief derived from the map.** |
| 13 | `_evryn-meta/docs/sessions/2026.08.19-act-acta-atlas-brief.md` **+ `…-act-acta-exchange.md`** | **BOTH.** ⭐ **The exchange is the valuable one — it holds `ACTa`'s delivered analysis and is the only home of the hub reasoning.** |
| 14 | `_evryn-meta/docs/sessions/2026.08.19-act-acti-guard-integrity-brief.md` | **the lane-brief anti-pattern.** ⚠️ **Read its §3 BANNERS, not its §3 list.** |
| 15 | `_evryn-meta/docs/decisions/055-thread-as-complete-record.md` | full — **SPRINT §H is entirely this ADR's build** |
| 16 | `_evryn-meta/docs/protocols/orchestration/being-an-admiral.md` | full. **Added 2026-09-11 on Justin's ruling.** ACT reports to Justin as an Admiral — numbered reports, sole conduit, and the token watch that triggers the `#packout` offer. It is a sub-doc of the orchestration protocol; the rest of that protocol is still pulled at spin time (below). |
| 17 | `_evryn-meta/docs/protocols/ac-writing-protocol.md` | full. **Added 2026-09-11 on Justin's ruling** — `ac.md`'s Light Startup Cascade loads it every session, and an ACT session writes to docs early. |

> 🔴 **CHANGES FROM THE LIST ACT-18 INHERITED: NONE. Carried VERBATIM from ACT-17's handoff.** *(Its "NEW" markers are ACT-17's, relative to its own predecessor; they are left as written rather than silently edited.)* **This list loaded ACT-18 cleanly on 2026-09-10: 19 files, 18 canaries confirmed, item 14 a deliberate partial read** *(lines 1–86 — its §3 banners and the lines above them; transcript rec 259)*.
>
> 📌 **CHANGES BY ACT-19, 2026-09-11, on Justin's rulings:** item 10's reading is now stated outright, and items 16 and 17 are new. ⇒ **ACT-18's own load was items 1–15 as they stood before, with item 10 skimmed** — which is what §12.2 means by *"load exactly as ACT-18 did."*

**Pull at their moment, not at spin-up:** `ac-orchestration-protocol.md` **+** the matching `orchestration/spinning-*.md` *(**before you spin ANYTHING** — the core alone is explicitly not enough)* · `lock-protocol.md` *(at a `#lock`)* · `packout-protocol.md` *(at a `#packout` — it runs the lock as step 0)*.

### 🔴 NOT on this list, deliberately: the runtime itself.
**You are a CONDUCTOR; the runtime lives in your lanes' heads.** ⚠️ **But the map changes what that costs — ACT-17 read the map plus all 41 leading docblocks (739 lines, one script) and that was enough to write a competent lane brief.** ⭐ **That is a cheap, repeatable move and it is recommended.** *(The recipe is in §9.)*

---

## §3 — WHERE THE LANE STANDS, 2026-09-10 evening

🔴 **ASK GIT FOR REPO STATE, NEVER THIS FILE — no `main` SHA appears here on purpose.**

- **🔄 SPRINT Step 85 — BUILT, UNMERGED.** An agent's memory can no longer compose silently empty or silently damaged. **533 tests on the branch; `main` was 515 when measured.** Details in §7.
- **🆕 Three Steps filed** — **86** *(`runWake` never sets the SDK's `settingSources`, so any `CLAUDE.md` where it runs would enter every wake unrecorded; safe today by coincidence; the one-line fix is HELD behind `ACTj` because it shares an options block with `maxBudgetUsd`)* · **87** *(no runtime agent can rewrite its own memory TODAY only because agents are conversation-only — write capabilities must be designed not to reach agent memory; Justin's point)* · **88** *(no runtime path creates `MEMORY.md`, so an agent whose file is absent or damaged can never save a note, and dormancy then retries a closing wake every ~30 minutes; found by the DC)*.
- **✅ ARCHITECTURE cardinal invariant 8 gained its second door** — model-supplied tool arguments — Justin-authorized and committed.
- **✅ SPRINT housekeeping:** Steps 42, 47 and 63 relabelled to their 2026-09-08 merge; Step 85 records that its `runtime-ops.md` clause is deliberately NOT going in.
- **Nothing is deployed. Migrations 015–018 are unapplied. The runtime wakes nobody.**

---

## §4 — INSTRUMENTS — what to arm, not what was armed

🔴 **A `Monitor` DIES WITH THE SESSION. Nothing below is running. Arm your own; never inherit a task id.**

| Instrument | Watch | Cadence | Its one job |
|---|---|---|---|
| **Inbox watcher** | `docs/mailboxes/inbox-act.md`, firing on **COMMIT** (`git log --all -1 -- <path>`), with a `git fetch` each tick | 45s, **emits only on change** | *"Did anything land for me?"* |
| **ACTj channel watcher** *(once she is spun)* | `docs/sessions/2026.09.08-act17-actj-exchange.md`, firing on COMMIT | 45s, emits only on change | *"Has ACTj posted?"* |
| **Heartbeat** | **you** | **5 min daytime · 10 overnight** | *"Are you still moving?"* — nothing else |

- 🔴 **ANCHOR EVERY WATCHER ON `$HOME` AND ON THE REPO (`git -C`)** — a `Monitor` shares the Bash tool's cwd and that cwd is MUTABLE; **the silence of a mis-anchored watcher is indistinguishable from an empty inbox.** **Print a proof-of-fire line BEFORE the loop.**
- **While waiting on a subagent, watch its WORKTREE (`rev-parse HEAD`), not yourself** — and kill that watcher the moment its subject is gone.
- ⚠️ **To observe a subagent's calls, parse its TRANSCRIPT: `C:/Users/jbmcg/.claude/projects/<project-folder>/<session-id>/subagents/agent-<agentId>.jsonl`** — with `node`, printing only counts; never `Read` it. **The `tasks/<agentId>.output` path the Agent tool hands back is NOT the transcript and can read EMPTY** — that is the file ACT-18's parse hit, and why it saw zero lines. *(Parsed correctly on 2026-09-11, `DCm`'s transcript gives trip 1 = 42 calls — 41 `Read`, 1 `Bash` — and trip 2 = 61 — 18 `Bash`, 16 `Read`, 5 `Grep`, 3 `Write`, 19 `Edit` — none errored.)*

---

## §5 — 📋 BACKLOG — one table, numbers never reused *(carries ACT-17's numbering)*

| # | Item | Owner | State |
|---|---|---|---|
| **B1** | `runtime-ops.md:9` cites a SESSION doc for its open questions — **SPRINT Step 77.** ⚠️ **The path it cites (`_evryn-meta/docs/working/2026.08.11-acf-acf-handoff.md`) is also dead** — `docs/working/` was retired 2026-09-02; the file is findable by name. ACT-18 saw this (rec 390) and left it for the Step 77 rewrite. | ACT → Justin | Open. **Needs a FULLY-LOADED sub-AC** — Justin barred a light-loaded coordinator from drafting it. |
| **B5** | ~30 ACT-lineage `HELD-SESSION-DOC`s *(derive: `git grep -n "HELD-SESSION-DOC"`)* | ACT | **DEFERRED by Justin** until the main show is underway; its own sub-lane; **budget it as a DISCOVERY pass.** |
| **B13** | `ThreadDormancyWriteRow` referenced nowhere | → **SPRINT Step 47's residue** | Lives there. |
| **B16** | `evryn-backend-qc-sandbox` detached worktree | ACP | Not ACT's. |
| **B18** | `settingSources` unset in `runWake` | → **SPRINT Step 86** | Fix HELD until `ACTj` is done. |
| **B20** | Seven ACT-lineage entries sit unread in `inbox-acp.md` — W1–W4 plus three that need nothing from ACP | — | **ACP dormant ~a week (Justin, 2026-09-10). Nothing of ACT's is blocked.** |
| **B21** | 🆕 `npm ci` in the DC's worktree ended with npm's *"run `npm audit` for details"* notice *(rec 605; the `ACTj` worktree's captured output, rec 393, shows only a version notice)* — **dependency advisories exist in `evryn-team-runtime`; not investigated** | ACT → a DC | Open. Read `npm audit` before deciding anything; do not run `audit fix --force` blind. |
| **B24** | 🆕 **SPRINT Steps 81 and 82 still name pre-merge branches** — Step 81: *"The port lives on branch `acta/dependency-map` at `22bb127`"* (merged at `7e4f8af`, 2026-09-08); Step 82: *"FIXED ON `dc/db-types-and-bounds` @ `d996e79`"* (merged at `8079621`, 2026-09-08). ACT-18 saw both and left them (rec 481). | ACT | **Drafted change in §13.3 for ACT-19 to apply.** |
| **B22** | 🆕 The two Justin items gating Step 85's merge — damaged-at-rest policy; the two agent-facing texts | Justin | **See §1 step 3.** |
| **B23** | 🆕 **`src/wakes/memory-writer.ts` carries no truncation canary** — re-flagged by the DC | — | ✅ **Deliberate, not a defect:** SPRINT Step 11 excluded it because it holds the canary string as a functional constant. **Recorded so nobody re-files it.** |

⚠️ **Closed this session and deliberately not carried:** B15 *(changelog order — fixed, committed `8d7b92b`)* · B17 *(closed by ACT-17)* · B19 *(SPRINT labels — fixed and committed)*.

---

## §6 — ⏳ WAITING ON — sent, no answer yet

🔴 **READ THIS BEFORE YOU ANSWER ANY MAIL. A reply arriving cold is unreadable without the question that produced it.**

| # | Sent to | What was asked | What their answer means |
|---|---|---|---|
| **W1** | ACP *(2026-09-08)* | Should the dependency-map generator be ONE shared file with per-repo config, instead of two forks? | "Yes" → build the shared version. "No" → **SPRINT Step 81 closes MOOT.** |
| **W2** | ACP *(2026-09-08)* | Does `evryn-backend/docs/ARCHITECTURE.md` enumerate a set, as if complete, that its map or Atlas already derives? | A question, not a defect report. "No" → nothing owed. |
| **W3** | ACP *(2026-09-08T16:42)* | A generator that reads CONTENTS from the working tree but its SHA from git maps disk and stamps the previous commit — does theirs? | "We regenerate from clean" → close it. |
| **W4** | 🆕 ACP *(2026-09-10T16:41, commit `2254341`)* | 🔴 **The `settingSources` class — possibly LIVE IN PRODUCTION on the product side:** `evryn-backend` tracks a `CLAUDE.md`, so if `runEvrynQuery` omits `settingSources` AND that file sits in or above the production working directory, **every live triage query receives developer instructions**; a local live run from the checkout would load it regardless. | **Two one-minute checks settle it.** ⚠️ **ACP is dormant this week. Justin was NOT told before ACT-18 compacted** — the report that would have carried it was never sent (§12.5 D). **ACT-19 takes it to him (§13.4).** |

---

## §7 — 🔴 SUBAGENT LEDGER — enumerate, never work from memory

**Subagent resume is SESSION-BOUND. The agent below became PERMANENTLY UNREACHABLE when ACT-18's session ended. Only a committed file survives — which is why its report is summarised here.**

### `DCm` — DC, SPRINT Step 85 — `agentId a541d906ca97306f6` *(unreachable now)*

- **Artifacts:** worktree `evryn-team-runtime-dcm` on `dc/step85-memory-grading` @ `4d9d09e` — **KEEP IT; it awaits QC and the merge, and you own it.** Its mutation sandbox was created and removed by the DC itself *(canonical `node_modules` counted 146 before and after)*. 🔎 **If `git -C evryn-team-runtime worktree list` shows any detached tree, that reap failed — remove it with the count check around it.** **Its mutation harness (`mutate85.mjs`) and both suite logs are still in ACT-18's session scratchpad (§12.4) as of 2026-09-11** — temp storage that vanishes when it is cleaned; the method is described below.
- **Nesting:** it spawned no agents. **Owed upward — its two `NEEDS-JUSTIN` and one heads-up:** **(1)** the damaged-at-rest policy → §1 step 3(a); **(2)** half a sentence for the `runtime-ops.md` §1 clause → **MOOT**, the clause is not going in (ballot ⑥); **(heads-up)** two new agent-facing texts need reading → §1 step 3(b). 🔴 **None reached Justin before ACT-18 compacted** (§12.5 D) — **ACT-19 carries them (§13.4).**
- **What it built** — layer 6 is graded by a new `src/composer/memory.ts`; **one rule sits under every case: refuse when the agent HAS learned things we cannot deliver; compose with a flag when we can deliver them but cannot vouch they are whole; compose at a lower grade when there is nothing to deliver.**

  | Grade | When | What happens |
  |---|---|---|
  | intact | reads, canary is the last line | composed verbatim, no alarm |
  | damaged | reads, canary missing or not last | composed verbatim; the agent warned in its wake context; a rate-limited alarm; the grade in the manifest name |
  | absent | no file, and no earlier wake composed a memory | an explicit "NONE ON FILE" notice composes as layer 6 (`6-memory/ABSENT`); rate-limited alarm; the wake proceeds |
  | refuse | unreadable (any error but a missing file), empty, or **vanished** (no file, but the last wake composed one) | throws `MemoryCompositionError`; `runWake`'s existing composition-failure path parks it and alerts — **no edit under `src/wakes/`** |

  `readIfExists` keeps identical behaviour for its other callers. The wake-over-wake comparison reuses the previous-manifest read the tray already made, widened to `components` and moved above layer 6. **`composeLayers` itself now refuses an empty memory layer.** For a deliberate memory reset, create the file containing only its canary line.
- **Its proof:** 515 → **533** tests, typecheck exit 0, measured before and after. **11 mutations, every one proven applied (single-line, exactly-once, `git diff --numstat` 1/1), every guard RED, every paired control GREEN.** It disclosed one defect in its own harness (a voided verdict, failed closed, fixed, re-run). **Fences all held:** only `src/composer/`, `tests/` and the map changed *(verified 2026-09-11: `git diff --stat 52edd12 4d9d09e` shows exactly 8 files — `src/composer/{index,layers,memory}.ts`, three tests, the two map files)*. **Its run:** 742,119 tokens, 61 tool uses, ~39 minutes *(harness figures, rec 919)*.
  - **Its mutation table — the break sites, so QC can re-run them** *(all in a throwaway worktree at `807dc88`; each proven applied)*:

    | # | Break (file:line → mutated) | Guards RED | Control GREEN |
    |---|---|---|---|
    | M1 | `memory.ts:81` → `return { kind: 'absent' };` | readFileOutcome · ACCEPTANCE 1 · 2c | ACC 2, ACC 3 |
    | M2 | `memory.ts:81` → `return { kind: 'unreadable', code };` (all failures fatal) | ACC 2 · absent-every-wake · RATE-GATED · VANISHED · readFileOutcome + **7 pre-existing tests** | ACC 1, 2c, ACC 3 |
    | M3 | `index.ts:431` → `if (true) {` | ACC 3 · 4c | 4a, ACC 2 |
    | M4a | `memory.ts:106` → `if (at === -1) return 'terminal';` | 4a · SIBLINGS · memoryCanaryState · gradeMemory · RATE-GATED | 4c, 4b |
    | M4b | `memory.ts:197` → a size-collapse clause | 4c · ACC 3 · gradeMemory | 4a, ACC 2 |
    | M5 | `layers.ts:495` → `if (false) {` | the composeLayers empty-memory guard | ACC 1, 2c |
    | M6 | `memory.ts:193` → `return { grade: 'absent' };` | VANISHED · gradeMemory | ACC 2, ACC 1 |
    | M7 | `memory.ts:120` → prefix `'6-mem/'` | ROUND TRIP · VANISHED · priorMemoryFrom · 4a | ACC 3, ACC 2 |
    | M8 | `index.ts:436` → `if (notify) {` | RATE-GATED | 4a |
    | M9 | `memory.ts:107` → `return 'terminal';` | SIBLINGS · memoryCanaryState · 4b · gradeMemory | 4a |
    | M10 | `layers.ts:590` → `if (false) {` | 4a · 4b | ACC 3, ACC 2 |
  - **Test details QC will want:** test **2c** drives the real `runWake` with an unreadable memory and asserts the park record and alert text; its manifest insert is rigged to fail as a **tripwire**, so a wrong pass can never reach a billed `query()`. `current-state-nag`'s `memoryChars: 0` inputs keep their assertions, relabelled in its header as isolation values. **One cost the DC accepted, belonging to the REFUSE grade (not to the damaged-at-rest choice):** a transient read error (e.g. too many open files) now parks the wake and uses up its trigger — the house position for any composition failure.
  - ⚠️ **Its receipts declared 60 work-trip calls; the transcript shows 61** — the extra one is a `Bash` call (18 observed, 17 declared). Nothing else differs.
- **What it found wrong in its brief** *(all verified by it at source)*: **Step 85's "a new agent has no file until its first write" is false → filed as Step 88**; Step 85 contradicted itself on surfacing a shrink (resolved: the manifest records size, no alarm, so a consolidation stays quiet); the canary check is presence-AND-terminal, matching the writer; every line reference in the brief checked out. **Adjacent, routed into Step 88:** layers 1, 2, 4 and the skill bodies still treat unreadable as absent.
- **Changelog-worthy facts for the merge entry:** layer 6 graded intact / damaged / absent / refuse · the manifest records the grade in the layer-6 name (`6-memory/MEMORY.md`, `6-memory/ABSENT`, `… [NO TERMINAL CANARY]`, `… [CANARY NOT TERMINAL]`) · new alerts `MEMORY ABSENT: …`, `MEMORY INTEGRITY: …` and a park whose cause is `MemoryCompositionError: …`, the two alarms limited to once an hour per agent per grade · 515 → 533 tests, 11 mutations proven · ARCHITECTURE's memory-lifecycle and composer entries could now mention layer-6 grading.
- **ITS LOAD LIST — the artifact of record, for receipts audit and for deriving QC's list.** Ran under `#cascade-override` *(non-product build, both halves)*; runtime files read from the DC's worktree. **38 items, all loaded, all canaries confirmed:** `_evryn-meta/.claude/agents/dc.md` · the Hub · `ARCHITECTURE.md` · the memory design-of-record (`evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-team-runtime-memory-architecture.md`) · design v2 (`…/2026.07.10-acf-justin-team-runtime-design-v2.md`) · ADR-050, ADR-052, ADR-055 · `BUILD-PHASE-1.md` · `docs/dependency-map.md` · `src/composer/index.ts`, `src/composer/layers.ts`, `src/wakes/index.ts`, `src/safety/notify.ts`, `src/safety/rate-gate.ts`, `src/db/client.ts`, `src/db/types.ts`, `src/boot/env.ts`, `src/config/agents.ts`, `src/config/skills.ts`, `src/threads/kind.ts`, `src/db/paginate.ts` · `migrations/010_wake_manifests.sql`, `014_composition_v2_skills.sql` · tests `composition-failure-honesty`, `current-state-nag`, `composer`, `composer-pagination`, `composer-scratchpad-read`, `fake-db`, `ceiling-db` · **identity half, derived from `src/composer/index.ts` + `src/config/skills.ts`:** `evryn-team-workspace/CLAUDE.md`, `shared/protocols/runtime-ops.md`, `shared/current-state/current-state.md`, `_evryn-meta/docs/hub/vision-and-ethos.md` (Lucas's spoke), `.claude/agents/lucas.md`, `.claude/agent-memory/lucas/MEMORY.md`, `shared/protocols/consolidation-protocol.md`. **Its trip-2 task** was SPRINT Step 85 as the spec, with fences: stay inside `src/composer/` and `tests/` (`src/wakes/` is `ACTj`'s this week), mutate only in a throwaway worktree, touch no identity file, no migration unless needed, no SPRINT / ARCHITECTURE / CHANGELOG edits, regenerate the map from a clean tree if the graph moves.

---

## §8 — 🗂 DOC DISPOSITIONS

| Doc | Disposition |
|---|---|
| **`2026.09.08-act17-act18-handoff.md`** | **SUPERSEDED by this file.** Its own condition ("superseded by ACT-18's own handoff at set-down") is met. **Archive to `docs/sessions/historical/2026.09/` with a `SUPERSEDED BY` line, filename untouched — on Justin's OK.** ⏳ **That OK has NOT been given** — ACT-18's request never reached him (§12.5 D). **Awaiting Justin; ACT-19 asks (§13.4).** 🔎 `git ls-files "*act17-act18-handoff*"` shows where it now lives. |
| **`2026.08.19-act-actd-*`** *(brief + exchange)* | 🏁 **LANE-CLOSED, deliberately NOT moved** *(ACT-17's disposition, unchanged — both carry the `LANE-CLOSED` banner on line 1, checked 2026-09-11)*. **Archiving them belongs to the B5 discovery pass**; no rush and no risk, because the banner already stops anyone spinning a moot lane. |
| **`2026.09.08-act17-actj-money-layer-brief.md`** + **`…-actj-exchange.md`** | 🔒 **CARRY — the live lane.** Each carries a `HELD-SESSION-DOC` banner as of this packout: retire upon the `ACTj` lane closing out. |
| **`2026.08.19-act-acta-*`** *(brief + exchange)* | 🔒 **HOLD** *(ACT-17's recommendation, unchanged)* — the map exists and is used; the atlas does not; **the exchange is the only home of the hub reasoning — do not retire it without rehoming that.** |
| **`2026.08.19-act-acti-guard-integrity-brief.md`** | 🔒 **HOLD** — re-scopable, deliberately not re-scoped until `ACTj` reports. |
| **`2026.08.17-acf13-lane-protocol.md`** · the four `2026.08.17-acf13-acf*` lane docs | 🔒 **HOLD** — the channels for the three unspun lanes. |
| **`2026.09.10-act18-packout-checklist.md`** | 🔒 **HOLD** until Justin has reviewed the packout it records; then retire. |
| **ACT-19's packout-finishing lane:** `2026.09.11-act19-act19a-act19b-packout-finish-brief.md` · `2026.09.11-act19a-act19b-act18-session-accounting.md` · `2026.09.11-act19c-act19b-act18-tail-accounting.md` | 🔒 **HOLD, each under its own banner** — they are the evidence behind §12's verification and §13. **ACT-19 dispositions them at its own set-down.** |

🔎 **A retired doc is never lost: `git ls-files "*<filename-fragment>*"` finds it wherever it lives.**

---

## §9 — WHAT ACT-18 ACTUALLY READ OF THE RUNTIME

**`src/composer/index.ts`, `src/wakes/tool-gate.ts`, `src/wakes/capabilities.ts`, `src/config/agents.ts` and `src/config/skills.ts` in full · `src/wakes/index.ts` lines 425–480 only · `docs/dependency-map.md` in full · the leading docblock of all 41 `src/` files.** 🔴 **Zero migrations, zero tests.** ⇒ **State your own number in every brief you write.**

> ### ⭐ THE DOCBLOCK PASS — the recipe, because it is the cheapest real runtime knowledge a conductor can get
>
> ```bash
> cd "$HOME/Evryn/Code/evryn-team-runtime"
> node --input-type=module -e '
> import {readFileSync} from "node:fs"; import {execSync} from "node:child_process";
> for (const f of execSync("git ls-files src",{encoding:"utf8"}).trim().split("\n").filter(x=>x.endsWith(".ts"))) {
>   const L = readFileSync(f,"utf8").split(/\r?\n/); let end=0, inBlock=false;
>   for (let i=0;i<L.length;i++) { const t=L[i].trim();
>     if (t.startsWith("/*")) { inBlock=true; end=i+1; if (t.includes("*/")) inBlock=false; continue; }
>     if (inBlock) { end=i+1; if (t.includes("*/")) inBlock=false; continue; }
>     if (t.startsWith("//")||t.startsWith("*")||t==="") { end=i+1; continue; }
>     break; }
>   console.log(`\n===== ${f} (${L.length} lines) =====\n` + L.slice(0,end).join("\n").trim()); }' > <your-scratchpad>/docblocks.txt
> ```
> **Write it to a file and `Read` it in chunks — printed straight to the Bash tool it runs past the output limit.** 🆕 **The pass grows as the runtime does:** 814 lines on 2026-09-10, and Step 85 adds `composer/memory.ts`.

**Verified at source this session, so you need not re-derive it** *(each read, not executed)*:
- **The ~24.4KB memory-truncation cap is Claude Code's agent-memory auto-load, and the autonomous runtime does NOT inherit it** — the SDK exposes it as an agent definition's `memory` scope; `runWake` defines no agents and disallows `Task`/`Agent`; layer 6 reads `MEMORY.md` whole.
- **A runtime agent cannot rewrite its own memory TODAY** — the authoritative `PreToolUse` gate (`src/wakes/tool-gate.ts`) default-denies every tool outside four named cases; `disallowedTools` is, in the runtime's own words, a courtesy, never the fence; none of the ten MCP tools rewrites memory. **Phase-1 only → Step 87.**
- **Team memory sizes, measured 2026-09-10:** seven of eight exceed the cap (Nathan 107.1KB · Marlowe 83.1 · Mira 79.0 · Emma 65.6 · Soren 63.4 · Lucas 48.7 · Dominic 25.9 · Thea 6.9). ⚠️ **Justin was consolidating on the desktop that evening; these will have moved.**

---

## §10 — JUSTIN'S ORDERS AND RULINGS THIS SESSION

**Session operating orders — his posture on 2026-09-10; he may restate them, so ask rather than assume they persist:**
1. **ADMIRAL MODE.** *"be as autonomous as is reasonably possible… be the Admiral of your fleet of Captains… don't bug me with every little thing — try to solve it yourself and just let me know what you did — but if it really does need my attention, or if it's hard to reverse, get me in right away."*
2. **PING EVERY TIME.** *"make sure you ping me every time — failing to do so will be a huge waste of time."* *(Already standing in `ac.md`.)*
3. **ACT IS HIS FOCUS.** *"ACP is dormant — you are my focus for the next week."* And, the same message (rec 332): *"you are my top priority today. So if you need me, I'll drop everything everywhere else and come running."*
4. **Routine changes commit without his review; important ones stay UNCOMMITTED for his review in SCM** — his example of routine was the changelog reorder.
5. **How to talk to him:** inline ballots whose numbers continue across messages *(ACT-18 used 1 to 8 — drawn as ⓵–⓸, then ⑤–⑧)*; numbered `# ACT-N's Report #N` headings; a handhold on every reference; never a bare Step number. ⚠️ **ACT-18's report numbers ran #1, #2, #4, #5, #6, #7 — no Report #3 was ever sent** (transcript checked, rec 0–1018).
6. **Nobody hijacks ACT** *(rec 275)*: *"no one gets to hijack you, though. If you can quickly answer someone's question with an absolute minimum of distraction and context burn, cool — go ahead. Anything else, just note it in your brief, and get back to it when you can. But let me know — because it might be something important that I need someone else not to block on."* Same message: *"help keep things cognitively light for me … bring me things that have been chewed appropriately before they come to me"* and *"I don't need a top level again — I know who ACT is."*
7. **The packout is done fastidiously, and visibly** *(rec 814)*: *"It was designed very carefully — like an astronaut's checklist — and if you skip steps or skimp on them, it messes things up. So be very fastidious. And put up a todo tool, so I can see you crossing things off."* **The checklist file (§8) is how ACT-18 answered the second half — no todo tool was available.**

**Rulings, each with its durable home** *(lock step 9b)*:
- **The `runtime-ops.md` §1 clause is deliberately NOT going in** — runtime agents never meet the interactive auto-load. → **SPRINT Step 85**, his agreed wording.
- **Memory protection has to be designed before agents get write capabilities.** → **SPRINT Step 87**, his words quoted verbatim *(rec 762)*. ⚠️ **The Step's REQUIREMENT — memory writable only through the guarded append path and a Justin-triggered consolidation, enforced in the gate, with a failing test — is ACT-18's framing and was never put to him** (lock step 9b asks for agreed wording; ACT-18 meant to flag it and compacted first). **Justin item (§13.4).**
- **Keep the ping before a `MEMORY.md` write in the team manual, but it is a notice, not a safeguard** — the write no longer waits on his approval. → **`evryn-team-workspace/CLAUDE.md`**, reworded on his authorization (ballot ⑦a). ⚠️ **Left uncommitted for his review, then committed unreviewed in the post-compaction baseline `6b532f9`** (§12.7). **Justin item: vet the wording (§13.4).**
- **Facts he supplied, recorded so nobody re-derives them** *(rec 762)*: the ~24.4KB memory cap is *"a claude code-only problem"*; interactive agents now write `MEMORY.md` with no approval prompt (*"that seems to have gone away"*) — **so the advice in ACT-18's Report #4 to deny any unexpected memory write was advice about a guard that no longer exists; ACT-18 acknowledged this in Report #6 (rec 777).**
- **His own open task, no durable home, status unknown to ACT** *(rec 332)*: *"we're going to have to verify that they CAN load their memories even if they are too long — and that they don't listen to the harness when it tells them to compact them."* **The runtime half is answered (§9); the interactive half is his desktop work.** ACT-18 routed it to him (fragment sweep, lock step 20) and never recorded it here.

---

## §11 — ✅ DOC-CURRENCY GATE, ANSWERED *(`lock-protocol.md` step 5 and neighbours)*

| Branch | Answer |
|---|---|
| **a · Sprint** | ✅ **CHANGED** — Steps 42/47/63 relabelled; 85 built (label) and its `runtime-ops` clause decided; **86, 87, 88 filed.** |
| **b · Build doc** | ✅ **NO CHANGE** — `BUILD-PHASE-1.md` is the original harness's build-record; nothing this session made it false. |
| **c · Architecture** | ✅ **CHANGED** — invariant 8's second door, committed. ⚠️ **At Step 85's merge, its memory-lifecycle and composer entries could mention layer-6 grading — owed with the merge, Justin-authorized.** |
| **d · Atlas / map** | ✅ **No runtime shape change on `main`.** The DC's branch adds `src/composer/memory.ts` and carries a map regenerated from a clean tree (`4d9d09e`); it lands with the merge. This repo has no Atlas. |
| **e · Learnings** | ✅ **Local half FIRST** (Steps 86, 87), **then the general half** to `LEARNINGS.md` as an un-numbered appendage: *a verified safety property may be incidental to today's configuration* · *an option left unset on a composition path may default to "load everything."* |
| **6 · Identity files** | ✅ `evryn-backend/identity/**` untouched. **The TEAM's identity layer:** the team manual's memory-write bullet was reworded on Justin's authorization; **`runtime-ops.md` was edited (rec 462), then reverted by ACT-18 at 23:15:28Z (rec 767) when Justin asked whether runtime agents need it (rec 762) — net unchanged; Justin then ruled the clause stays out (ballot ⑥a, rec 814).** |
| **7 · Hub & spokes** | ✅ No change. |
| **8 · Cross-runtime** | ✅ **No hole closed; a class FOUND** (`settingSources`) and sent to ACP as **W4** (`2254341`). ⚠️ **Recorded as pending, as the step requires — but NOT flagged to Justin before compaction**, although ACP is dormant (§6, §12.5 D). |
| **9 · ADRs** | ✅ **None** — Steps 86–88 are findings, not changes to how the system works. |
| **13 · Linear** | ✅ **Deliberately untouched** — no ticket belongs to this lane; the board's statuses are non-binding. |
| **16 · Operator guide** | ✅ This runtime has none (SPRINT Step 29); nothing operator-facing shipped. |
| **17 · Database** | ✅ **Fresh archival dump taken** — `evryn-backend/backups/full-public-2026-09-10.sql` (the newest was 8 days old). No migration ran. |

---
---

## 🟥 §12 — APPENDAGE BY A **COMPACTED** ACT-18. VERIFY EVERYTHING IN IT.

> 🟥 **THIS SECTION WAS WRITTEN AFTER ACT-18 COMPACTED.** ACT-18 compacted at **2026-09-10T23:55:45Z**, mid-`#packout`. **Everything in §12 was written afterwards, by the compacted instance, on Justin's explicit instruction** — working from a machine-written summary of the session plus a set of read-only checks against git and the session transcripts. **The checks were real tool calls; the judgment reading them is exactly the faculty compaction degrades.**
>
> ⇒ **Take every line of §12 with a big grain of salt. Verify each claim against git and the transcripts (§12.4) before you rely on it.**
>
> **§§1–11 above were written BEFORE compaction, by a fully loaded ACT-18** — but as the output of an unfinished packout. **§12.5 lists what is already known to be wrong or missing in them.**

### §12.1 — YOUR FIRST JOB: FINISH ACT-18's PACKOUT. **It comes before §1.**

**Justin's order, verbatim (2026-09-10):**

> "I want ACT-19 to start by verifying everything, doing the end of the packout as best he can - including sending two sub-ac's to read the transcript - I want the first one to 1) load up as ACT 18 did, then 2) read the incoming brief, then 3) the read the first 2/3rds of the transcript, reading first-hand any files that were loaded, and then 4) give a *very* detailed accounting of it - enough that the second AC can read the first AC's output and continue on *as* the first AC. And then I want the second sub-AC to load as ACT-18, read the incoming, the first ACs output, then the read the 2nd 2/3rds of the transcript (so, *lots* of overlap), reading any files AC-18 did (for both of them, if the files' full bodies are *in* the transcript, they don't have to re-read them - only if htey are just mentioned in the transcript - one way or another, the full contents need to be read) - so then I want the 2nd AC sub to look at the handoff and make any changes it needs to - using the packout protocol fastidiously to make sure the handoff is solid. Then the 2nd AC sub can give the handoff back to ACT-19."
>
> **And on how to get at the transcript:** "just point to where it is and let ACT-19 get it however he wants it."

**In order:**
1. **Load §2 as usual. Then verify §12** against git (`git -C <repo> log` / `status`, per repo) and the transcripts. **Ask git for state — every SHA in §12 is as of writing.**
2. **Check the budget (§12.3) — BEFORE you spin anything.**
3. **Spin sub-AC 1, the first reader** (§12.2).
4. **Spin sub-AC 2, the second reader and packout finisher,** once sub-AC 1's accounting exists (§12.2).
5. **Review what sub-AC 2 hands back.** Then take Justin the decisions that are his — **the report ACT-18 planned and never sent (§12.5 D)** — and push on his go.
6. **Then §1,** from its step 3 (the fresh QC on Step 85), as written.

⚠️ **Run on the LAPTOP. The transcripts and the DC's scratch files exist only there** (§12.4). *(The DC's branch has been on origin too since the post-compaction push — §12.7 — but its worktree is here.)*

### §12.2 — THE TWO SUB-ACs, OPERATIONALLY

**Both are `ac` subagents, spun per `ac-orchestration-protocol.md` + the matching `orchestration/spinning-*.md`** (two trips, receipts, canaries).

**Both follow the same rule for files:** every file ACT-18 read gets read in full, once. **A full body in the transcript counts; a file the transcript only names, or shows read partially, or shows refused, gets read from disk.** A file the sub-AC already read in its own load need not be read again. ⚠️ **The transcript shows files AS ACT-18 READ THEM, and several have changed since** (the SPRINT, current-state, this handoff) — **where it matters, read the current version too.**

**Sub-AC 1 — the first reader.**
1. **Load exactly as ACT-18 did:** `ac.md`, then §2's list. *(§2 is ACT-17's list carried verbatim — the list ACT-18 loaded.)*
2. **Read the incoming brief** — `2026.09.08-act17-act18-handoff.md`, ACT-18's own starting brief *(`git ls-files "*act17-act18-handoff*"` finds it)*.
3. **Read the first two-thirds of ACT-18's transcript,** by the rule above.
4. **Write a VERY detailed accounting** — detailed enough that **sub-AC 2 can read it and carry on AS sub-AC 1.** 🔎 **Recommended: a committed file in `_evryn-meta/docs/sessions/`, named by you,** so sub-AC 2 reads it first-hand and it outlives both sessions.

**Sub-AC 2 — the second reader, and the packout finisher.**
1. **Load exactly as ACT-18 did.**
2. **Read the incoming brief.**
3. **Read sub-AC 1's accounting, in full.**
4. **Read the last two-thirds of the transcript** — deliberately overlapping sub-AC 1's middle third, and **including the tail after the compaction boundary**, which is a compacted ACT-18 and unverified by definition.
5. **Then make the handoff solid:** read `packout-protocol.md` and `lock-protocol.md` in full *(and `ac-writing-protocol.md` before writing)*, and **run the packout's unfinished steps on this handoff, fastidiously.** Work from the packout checklist (§12.6). Start from §12.5's gaps, add whatever the transcript shows is missing, and correct what is wrong.
6. **Hand the handoff back to you,** listing every change it made and every item that is Justin's.
- **Not its call:** the push · archiving ACT-17's handoff · any code · the Step 85 merge · spinning anything.

### §12.3 — ⚠️ BUDGET: MEASURE BEFORE YOU SPIN

**ACT-18's rough arithmetic, from the transcript's own token-usage fields** *(compacted — verify)*:
- **ACT-18's context at its last call before compaction: ~958K tokens.**
- **In context when its load finished** (Report #1, 21:47Z): **~400K** — so the load itself (the incoming brief + §2) was ~355K on top of a ~45K baseline.
- **The work after the load: ~560K.** The tail after compaction adds ~75–105K more, and it grows until this session is closed.
- **The transcript also holds ACT-18's reasoning blocks, most of which were not in its context at the time** — about 15% of the text. **Reading them costs extra.**

| | Load | Then reads | Projected |
|---|---|---|---|
| **Sub-AC 1** | ~400K | the first 2/3, minus the load bodies it has already read: ~250K+ | **~650K+** |
| **Sub-AC 2** | ~400K | sub-AC 1's accounting + the last 2/3 (~560K+) + the protocols + the handoff work | 🔴 **~1M+ — past the point where ACT-18 itself compacted** |

⇒ **Measure it yourself before spinning.** **If either projects past ~700K** — room has to be left for the actual work — **bring Justin the numbers and an alternative** (for example, a third reader, so the finisher reads less) **before you spin.** 🔴 **Do not spin an agent into a known compaction.** **And tell each sub-AC: if you near your limit, stop at a clean boundary and write down exactly where you stopped — never read on into compaction.**

### §12.4 — WHERE THE TRANSCRIPTS ARE *(the laptop only — in the user profile, not in git)*

| What | Where |
|---|---|
| **ACT-18's session** — session id **`ed8ebebb-d415-4f93-a114-afd076364b7b`** | `C:\Users\jbmcg\.claude\projects\c--Users-jbmcg-Evryn-Code--evryn-meta\ed8ebebb-d415-4f93-a114-afd076364b7b.jsonl` — JSONL, one event per line; ~6.7MB and ~1,190 records at writing, **still growing until Justin closes the session.** **The compaction boundary is the line whose `subtype` is `compact_boundary`** (record ~1019, 23:55:45Z). 🟥 **Everything after it is the compacted ACT-18.** |
| **The DC's session** — `DCm`, agentId **`a541d906ca97306f6`** | the same folder: `ed8ebebb-d415-4f93-a114-afd076364b7b\subagents\agent-a541d906ca97306f6.jsonl` — 342 records, both of its trips. **Its final report is also in ACT-18's transcript,** as a queued task-notification at 23:42:54Z. |
| **Session scratch files** — **session temp: not in git, and may vanish** | `C:\Users\jbmcg\AppData\Local\Temp\claude\c--Users-jbmcg-Evryn-Code--evryn-meta\ed8ebebb-d415-4f93-a114-afd076364b7b\scratchpad\` — `mutate85.mjs` (the DC's mutation harness) · `test-run-1.txt`, `test-run-final.txt` (its suite logs) · `docblocks.txt` (ACT-18's docblock pass, §9) |

### §12.5 — WHAT THE COMPACTED ACT-18 VERIFIED *(leads, not facts)*

**A. The timeline.**
- **Three changes that look post-compaction were made BEFORE it, with full context:** the §5 B20 line (23:51:55Z) · the checklist ticks for lock steps 3–21 (23:52:12Z) · commit `76d0ec7`, the `HELD` banners on the two `ACTj` docs (23:52:20Z). *(The compacted ACT-18 first told Justin the opposite, in chat and in a ping. That was wrong.)*
- **ACT-18's last chat message before compacting was at 23:34:51Z** (acknowledging ballots ⑤–⑧ and starting the packout); **its last ping, 23:45:03Z** (the DC finished; don't restart VS Code yet). **The last numbered report sent was #7** (23:20Z).
- **After compaction:** one ping · the heartbeat stopped · read-only checks · baseline commits at Justin's order (§12.7) · this section.

**B. The DC's work — `DCm`, SPRINT Step 85.**
- **The build is safe:** `dc/step85-memory-grading` @ `4d9d09e` in worktree `evryn-team-runtime-dcm`, clean. Eight files changed, all inside `src/composer/`, `tests/` and the dependency map. ✅ **Pushed to origin at Justin's order after compaction (§12.7)** — until then it existed only on the laptop.
- **Captured in §7 and in the SPRINT:** the four grades · the main design points · the proof (515 → 533 tests, 11 mutations, the harness defect it disclosed, its fences) · its four corrections to its brief, including Step 88, which also carries the layers-1/2/4 adjacency · its changelog facts · its trip-1 load list · both of its NEEDS-JUSTIN items *(its second, the `runtime-ops.md` clause, is settled by Justin's ballot ⑥)*.
- **NOT captured — carry these:**
  - ~~**A cost it accepted, which bears on Justin's damaged-at-rest call:**~~ ❌ **MISATTRIBUTED (ACT-19b, rec 919):** a transient read error (e.g. "too many open files") now parks the wake and uses up its trigger — **but that is the cost of the REFUSE grade, not of the damaged-at-rest choice.** **The cost the DC tied to its damaged-at-rest recommendation is that the writer refuses notes to a damaged file (Step 88).** Both are now in §1 step 3 and §7.
  - **Files it read on its work trip that belong in QC's load** — §1 step 3 names only `memory-writer.ts` and its test: `src/wakes/env.ts` (why one test has a tripwire) · `tests/dormancy-wake.test.ts` (the only other test that calls `composeWake`) · `tests/scratchpad.test.ts` (pins the layer-6 component name) · `src/wakes/capabilities.ts` · `src/digest/index.ts` (two ranges) · `src/tools/tools.ts` 420–464.
  - **Test details QC will want:** test 2c drives the real `runWake`, with a tripwire so a wrong pass cannot reach a billed call · `current-state-nag`'s zero memory sizes are relabelled as isolation values.
  - **The full 11-row mutation table** — break site, guards that went red, controls that stayed green. It is in the DC's report.
- **Two lines above are wrong:** §4 says a finished subagent's transcript "can read as EMPTY" — **the DC's parses fine** (trip 1: 42 tool calls, matching the reconciliation; trip 2: 61, against 60 in its receipts — one Bash call unlisted). §7 says its mutation harness "is gone" — **it is still in the scratchpad** (§12.4), until that temp folder is cleaned.

**C. Where the lock and the packout stood.**

| Step | State |
|---|---|
| Lock, pre — re-read the writing protocol | ❌ skipped deliberately; flagged in the checklist |
| Lock 1–21 | ✅ ticked before compaction — 🔴 **but two ticks are wrong.** **Step 8** says W4 was flagged to Justin "in Report #8": **Report #8 was never sent, and no ping mentioned W4.** **Step 10** says the `actd` lane docs were left as ACT-17 left them: true on disk, **but §8 has no `actd` row.** |
| Lock 22 — Justin's commit/push go | ✅ given by Justin after compaction (§12.7) — never asked for as the lock intended |
| Lock 23 — commit and push | ✅ done at Justin's order after compaction (§12.7): **everything committed and pushed** |
| Packout 1 — the handoff | ✅ done (`7e0fd03`); box unticked |
| Packout 2 — second fragment sweep | ✅ in substance; box unticked |
| Packout 3 — subagent close-out | ✅ in substance (one subagent, `DCm`); box unticked; **gaps in B above** |
| Packout 4 — re-read both briefs | 🔄 **in progress at compaction.** It found four fixes and applied one (B20). **Unapplied:** (a) a §1 line saying the `_evryn-meta-acm` and `_evryn-meta-pgdump` worktrees belong to other agents · (b) the `actd` row restored to §8 · (c) a pointer saying ACT-17's rulings (e.g. what counts as a "job") live in the `ACTj` brief's §6 |
| Packout 5 — a fresh-eyes pass | ❌ |
| Packout 6 — archive ACT-17's handoff | ❌ needs Justin's OK |
| Lane-ending test | ❌ *(trivially: the lane is not ending)* |

**D. Never sent to Justin before compaction — the "Report #8" ACT-18 planned.** **Put these to him:**
- **⑨** archive `2026.09.08-act17-act18-handoff.md` to `docs/sessions/historical/2026.09/` with a `SUPERSEDED BY` line?
- **⑩** Step 85's damaged-at-rest policy — the DC built compose-and-flag and recommends keeping it *(weigh it with the cost in B)*.
- **⑪** the two new agent-facing texts, `absentMemoryNotice` and `damagedMemoryWarning` in `src/composer/memory.ts` — his to read, now or alongside QC.
- ~~**⑫** the push go~~ — **settled:** Justin ordered the commit and push after compaction (§12.7).
- **W4** (§6) — ACT-18 planned to recommend a quick product-side check by someone other than the dormant ACP, with Justin's OK. *(Justin first heard of W4, and of the DC's two items, from the compacted ACT-18's chat — nothing before.)*
- ~~**A fragment:** the ping at 23:19:59Z went out with unexpanded placeholders — *"$C commit(s), $D changed file(s), $S test sandbox(es)"*.~~ ❌ **CONTRADICTED by the transcript (ACT-19b):** `$C`, `$D` and `$S` were assigned in the same Bash call as the ping (rec 801), inside double quotes, and **that call's own echo printed them expanded — "0 commit(s) … 0 changed file(s) … 0 detached sandbox(es)" (rec 806).** The ping almost certainly read "0 / 0 / 0". *(The Slack message itself was not observed — nothing for Justin to act on.)*

### §12.6 — THE PACKOUT CHECKLIST *(Justin asked whether the handoff mentions it)*

**`docs/sessions/2026.09.10-act18-packout-checklist.md` is the running record of this packout** — each step, ticked with its evidence. **Before §12, the handoff named it only in §8, as a doc to hold — not as the instrument for finishing the packout.** ⇒ **Sub-AC 2 works from it:** correct the two wrong ticks (lock 8 and 10), tick packout 1–3 with their evidence, **tick lock 22 and 23 (both closed after compaction — §12.7)**, and tick the rest as they close. **It stays `HOLD` until Justin has reviewed the packout it records.**

### §12.7 — COMMITS AND PUSHES MADE AFTER COMPACTION *(ask git)*

**At Justin's order — "commit current files *before* you change *anything*" — the compacted ACT-18 committed everything outstanding, unchanged, as a baseline:** `_evryn-meta` **`5d00bbf`** (meta current-state, `LEARNINGS.md`, the B20 line, the checklist ticks) · `evryn-team-runtime` **`641e85c`** (`CHANGELOG.md`) · `evryn-team-workspace` **`6b532f9`** (the team manual's memory-write bullet, and ACT's current-state appendage). 🔴 **None of those had been reviewed by Justin as the lock intended — they are a baseline, not a sign-off.** **Then, at Justin's order — "commit your handoff, then push everything":** §12 was committed (`_evryn-meta` **`6e95727`**) and **everything was pushed** — all three repos' `main` left even with origin, and **the DC's branch `dc/step85-memory-grading` @ `4d9d09e` went to origin as a new branch.** *(The three baseline commits were already on origin when ACT-18 fetched — pushed by something other than ACT-18, probably Justin's editor sync.)* **Deliberately NOT pushed:** `actj/money-layer` (it holds no commits of its own) · `acm/manual-rubric` and `dc/pgdump-schema-only` (other agents' branches). **A follow-up commit (`44bd827`), also pushed, corrected the §12 lines that push made stale** — merged on the laptop with three of Justin's desktop commits (`a25ec35`). **§12.8 was added after that, at Justin's order; `git log` shows its commit.**

### §12.8 — TWO HEADS-UPS FROM THE FINAL PUSH *(added at Justin's order — verify; the first is now resolved)*

1. ✅ **The laptop's `evryn-team-workspace` WAS behind origin — ACT-18 pulled it, at Justin's order.** It held Justin's desktop consolidation round (2026-09-10, from ~15:46 PT): the team consolidation briefs and logs, Nathan's and Lucas's memory work, and a pass over the team current-state — none of it ACT-18's. **A clean fast-forward: 45 commits, `6b532f9` → `3a64dcc`.** **ACT's own team current-state appendage survived it** — the `ACT — 2026-09-10T16:41:27-07:00` entry was still present and the file's canary intact *(checked by grep, not re-read)*. ⇒ **Your repo-sync check should find this repo current, unless the desktop has pushed since.** *(In the same pass ACT-18 fetched every repo on the laptop: every canonical repo was then even with origin, and no worktree had anything to pull. `evryn-team-runtime-actj` is behind local `main` — that is §1 step 4's catch-up, not a pull.)*
2. **Justin's desktop commits changed ACT's protocols AFTER §§1–11 were written** — `86d0e17` and `9367a9c`, merged on the laptop in `a25ec35`: `docs/protocols/packout-protocol.md` (a new "universal packout Step 0") · `docs/protocols/ac-orchestration-protocol.md` · `.claude/agents/ac.md` (two lines) · `docs/repo-inventory.md` · and a **NEW** `docs/protocols/orchestration/being-an-admiral.md`. 🔴 **ACT-18 has read none of these changes.** ⇒
   - **You and both sub-ACs read the current versions from disk.**
   - **§2 does not name `being-an-admiral.md`.** ACT-18 ran in "Admiral mode" (§10), so it may now be owed at load or before you spin — **ask Justin whether it belongs on your load list.** ✅ **Answered 2026-09-11 (Justin): yes — it is now §2 item 16.**
   - 🔴 **The packout checklist mirrors the packout protocol as ACT-18 had it.** **Sub-AC 2 must finish the packout against the CURRENT protocol, including its new Step 0, and reconcile the checklist to it** — not trust the checklist's step numbers.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
