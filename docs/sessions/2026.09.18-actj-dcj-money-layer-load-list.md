🔒 **HELD-SESSION-DOC** — `DCj`'s LOAD LIST · **RETIRE UPON:** the `DCj` build closes out · **OWNER:** ACT (written by ACTj) · **HELD SINCE:** 2026-09-18

# DCj — YOUR LOAD. Read every file here, in full unless a span is given, BEFORE you read your brief.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you do not see it, re-read until you do.
>
> **How to use this file:** this is the list of what `DCj` (the Developer Claude building the team runtime's per-job money layer) loads before it is told its task. **It says what each file IS, never what it is for — your brief explains the for, after you are loaded.** Written by `ACTj` (the money-layer lane AC, who has read every file below), because the person who read the code is the one who should derive what a builder needs from it.

---

## Why load first, and the three rules

**You load first and read your brief second.** An agent that knows its task while it loads scores each file against that task and quietly drops what looks irrelevant — without ever noticing it chose.

1. 🔴 **DO NOT TRIM THIS LIST. ADD to it freely**, and report what you added. **If you feel strongly that something should be cut, do not cut it — say so with your reasoning and wait.**
2. **Read in the order given. The order is DEPENDENCY, not importance** — later files are easier to read correctly once the earlier ones are held.
3. **Every line count below is a HYPOTHESIS** — measured with `wc -l` at 2026-09-18 12:34 PT against `0f0d6c2`. **Re-derive any you care about and FLAG a delta rather than reconciling it silently.** ⚠️ **`wc -l` counts newlines, so a file with no trailing newline reports one line fewer than it has — and the line it drops is the canary. The bottom canary, not the number, proves a full read. Size your last read window PAST the stated count.**

### Where to read from

- **Paths are rooted at the shared parent that holds every Evryn repo as a sibling folder.**
- 🔴 **Every `evryn-team-runtime/...` path below is read from YOUR worktree, `evryn-team-runtime-dcj`** (branch `dcj/money-layer`, which your conductor cuts from `actj/money-layer` at `0f0d6c2`). **Never from the canonical `evryn-team-runtime` tree.**
- **`_evryn-meta/` and `evryn-team-workspace/` files are read from those repos' `main`.**

---

## A. Your identity

| # | File | Read | What it is |
|---|---|---|---|
| 1 | `_evryn-meta/.claude/agents/dc.md` | full — 362 | **Your operating manual.** The `CLAUDE.md` auto-injected into your context is a small router, not a manual. |

## B. The map

| # | File | Read | What it is |
|---|---|---|---|
| 2 | `evryn-team-runtime/docs/dependency-map.md` | **the header (through "Generated:"), §4 "Reverse index" rows for every `src/` file in sections C–F below, §8 "Test coupling" rows for the same files, and "Limits of this map"** | The **generated** map of what imports what — complete by construction. **It is an index, never a substitute for reading.** ⚠️ **Read each file's §4 row IN FULL — its importers are the blast radius of any signature you change.** |

## C. Money

| # | File | Read | What it is |
|---|---|---|---|
| 3 | `evryn-team-runtime/src/money/budget.ts` | full — 113 | Pure budget math: the forecast, the 60%-of-cap pitch gate, the freeze assessment, the per-wake backstop, the fleet velocity verdict. |
| 4 | `evryn-team-runtime/src/money/ledger.ts` | full — 221 | The DB plumbing beneath it: `getJob`, `recordCostEvent`, `accrueJobSpend`, `lastHourSpendUsd`. |

## D. The wake

| # | File | Read | What it is |
|---|---|---|---|
| 5 | `evryn-team-runtime/src/wakes/index.ts` | full — 756 | `runWake` — the SDK `query()` harness: the wake request type, the job/budget handling, cost capture, the park paths, `parkNote`. |

## E. Dispatch, and the surfaces that reach it

| # | File | Read | What it is |
|---|---|---|---|
| 6 | `evryn-team-runtime/src/scheduler/dedup.ts` | full — 442 | The pure planner: trigger kinds, candidate builders, coalescing, per-agent serialization, sender priority. |
| 7 | `evryn-team-runtime/src/scheduler/index.ts` | full — 876 | The 15-second loop: `buildPlannedWakeRequest`, `dispatch`, `cycle()`, the velocity gate, the sweep gates, the daily check-in cron. |
| 8 | `evryn-team-runtime/src/slack/index.ts` | full — 406 | Slack ingress: mention-gated wakes, the DB mirror, the `/dismiss` `/pause` `/resume` commands. |

## F. What those stand on

| # | File | Read | What it is |
|---|---|---|---|
| 9 | `evryn-team-runtime/src/db/types.ts` | full — 282 | The hand-maintained row types, one interface per migration table — `JobRow` and `JobMetadata` among them. |
| 10 | `evryn-team-runtime/src/db/client.ts` | full — 27 | The Supabase client and `assertOk`. |
| 11 | `evryn-team-runtime/src/db/paginate.ts` | full — 535 | Cursor pagination plus URL-byte chunking. **ARCHITECTURE cardinal invariant 8 is written about this file.** |
| 12 | `evryn-team-runtime/src/boot/env.ts` | full — 197 | Env parsing into `RuntimeConfig`; every branch refuses loudly. |
| 13 | `evryn-team-runtime/src/config/agents.ts` | full — 59 | Per-agent configuration, including the explicit model pin. |
| 14 | `evryn-team-runtime/src/safety/notify.ts` | full — 99 | The Slack notifier every kill/park/freeze ping goes through. ⚠️ **It carries a credential-hygiene rule with a real incident behind it — do not "improve" its diagnostics.** |
| 15 | `evryn-team-runtime/src/safety/kill.ts` | full — 346 | Kill switches, pause states, the dispatch gate, and `reArmParkedWork` with its one re-armable park tag. |
| 16 | `evryn-team-runtime/src/threads/kind.ts` | full — 72 | The room-vs-pseudo thread-kind union, declared at dispatch sites and never inferred. |
| 17 | `evryn-team-runtime/src/boot/schema.ts` | full — 227 | The boot-time schema assertion: refuses to boot on a missing relation the runtime queries. |
| 18 | `evryn-team-runtime/src/boot/singleton.ts` | full — 210 | The runtime singleton lock and its stale-lock reclaim. |

## G. Read-only neighbours — spans only

| # | File | Read | What it is |
|---|---|---|---|
| 19 | `evryn-team-runtime/src/composer/layers.ts` | **:1–34** (header), **:86–94** (`BudgetLine`), **:419–434** (`renderBudgetLine`) | How a budget line is rendered into a wake. ⚠️ **Another build is landing in `src/composer/` — this is read-only for you.** |
| 20 | `evryn-team-runtime/src/tools/tools.ts` | **:558–589** | The `get_job_status` MCP tool — the agent-facing read of the jobs ledger. |
| 21 | `evryn-team-runtime/src/digest/index.ts` | **:1–21** (header), **:245–298** (the Haiku call), **:312–413** (`regenerateThreadDigest`, including its cost capture) | The shared per-thread digest's regeneration path. |

*(If a span's boundary has moved, find it by the quoted identifier — never by the number alone.)*

## H. Migrations

| # | File | Read | What it is |
|---|---|---|---|
| 22 | `evryn-team-runtime/migrations/002_jobs.sql` | full — 53 | The `jobs` table: caps, spend, forecast, status, cap history, `metadata`. |
| 23 | `evryn-team-runtime/migrations/005_cost_events.sql` | full — 52 | The `cost_events` table. |
| 24 | `evryn-team-runtime/migrations/012_accrue_job_spend.sql` | full — 23 | The atomic spend-accrual RPC the ledger rests on. |

⚠️ **Enumerate the full migration set live if you need it — `git -C evryn-team-runtime-dcj ls-files migrations`. Never a `**/*` pathspec: it silently drops files sitting directly in the folder.**

## I. Tests — the ones your work will meet

| # | File | Read | What it is |
|---|---|---|---|
| 25 | `evryn-team-runtime/tests/park-trigger.test.ts` | full — 153 | The thread-kind chain: candidate → planner → wake request → scratchpad gate. |
| 26 | `evryn-team-runtime/tests/scheduler-velocity-wiring.test.ts` | full — 378 | Source-level wiring assertions over comment-stripped source, **with the anti-vacuity proof that the stripping is real.** |
| 27 | `evryn-team-runtime/tests/park-inert.test.ts` | full — 105 | Guards that park records are written inert. ⚠️ **SPRINT Step 54(iv) records that its "park note never fans out" property is protected by an incomplete stub, not an assertion.** |
| 28 | `evryn-team-runtime/tests/ledger-velocity.test.ts` | full — 174 | The ledger's trailing-hour read, run against a fake DB that simulates the server's row ceiling. |
| 29 | `evryn-team-runtime/tests/ceiling-db.ts` | full — 279 | A shared test fake: a DB that enforces a row ceiling. |
| 30 | `evryn-team-runtime/tests/fake-db.ts` | full — 199 | A shared test fake DB. |
| 31 | `evryn-team-runtime/tests/fixtures/` — `README.md` (40), `velocity-basis-omitted.ts` (14), `velocity-basis-control.ts` (7) | full | 🔴 **Compile-time guards as files on disk. `velocity-basis-omitted.ts` MUST NOT COMPILE — its failure to compile IS the assertion. Do not "fix" it.** |

**`tests/` beyond this list is a skim for shape, not a full read.** **If you need another test file, read it and report that you did.** ⚠️ **§8 of the map (item 2) names every test that imports a file you change — those are the ones to run and read when a signature moves.**

## J. The identity half

**This runtime composes identity files into every agent wake; no import edge points at one, so the map cannot see them.**

| # | File | Read | What it is |
|---|---|---|---|
| 32 | `evryn-team-workspace/shared/protocols/runtime-ops.md` | full — 76 | Composer **layer 1**: what every autonomous agent is told about wakes, tools, **money (its §3)** and messages. ⚠️ **Justin's file — read-only.** |

---

## When your load is complete

1. **Read the brief your spin names.**
2. **Post ONE entry where your brief says** with: your **read receipts** — every file, the line ranges you actually read, and the bottom canary you confirmed (or the true final line where there is none) — **naming the TOOL on every line**; your **plan**; and **every concern**, including anything in the brief you think is wrong.
3. **Then STOP and wait for sign-off. Begin no work.**

🔑 **Report every call, including any that FAILED, errored or came back partial — never let a retry silently absorb a failure.** ⚠️ **Never describe a search as having "verified" or "confirmed" anything** — a search returns only what your own pattern asked for, so it can locate but never establish.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
