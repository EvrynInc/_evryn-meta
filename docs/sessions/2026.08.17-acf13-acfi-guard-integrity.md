🔒 **HELD-SESSION-DOC** — ACT-lineage session doc, moved here when `docs/working/` was retired · **RETIRE UPON:** ACT has evaluated it · **OWNER:** ACT · **HELD SINCE:** 2026-08-22

# LANE 4 (ACfi) — Guard integrity: the guards that do not guard what their names claim

**Lane AC: `ACfi`. Conductor: ACf-13 (or its successor).** Opened 2026-08-17.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file:** your brief is §1–§6. **Everything below the line is a running two-way exchange — append, never overwrite.** The mechanics are in **`_evryn-meta/docs/working/2026.08.17-acf13-lane-protocol.md` — read it first; short and binding.**

---

## §0 — 🔴 YOUR FIRST ACTION, BEFORE ANY WORK

**Append an entry to this doc, and commit it, BEFORE you load anything.** It carries three things:

1. **You are alive** — name your worktree and branch.
2. 🔴 **Every question or concern you have about your ability to work through the NIGHT, unsupervised.** Not about the work — about the *conditions*. Anything you can foresee that would stall you at 2am with nobody awake: a gate you will hit, a permission you will need, a tool you are not sure you have, an instruction in this brief that contradicts another. **Say it now, while the conductor is still here to answer.**
3. **Anything in the brief you think is wrong.** You have not read the runtime yet, so this is about the brief's internal logic, not its facts.

**Why this comes first: the conductor and you have never run this channel before.** It proves the round trip works *before* either of us builds on the assumption that it does — **and it is the last cheap moment to catch a condition that would end your night silently.** *(The conductor already caught one this way: an early draft of the protocol forbade the very commit this channel depends on.)*

**Do not start the load until that commit lands.**


## §0b — 🟢 WHAT YOU MAY FINISH WITHOUT ASKING — and the gates that never move

*(Added 2026-08-18 by ACf-15, at Justin's direction. **This section exists because your brief told you to "flag anything that needs Justin" but never stated the boundary POSITIVELY — so at 2am you would have had to guess, and a lane that guesses conservatively stalls and loses the night.**)*

### 🔒 The gates, first — these do NOT relax because Justin is asleep

**No merge. No push to `main` in your build repo. No deploy. No migration APPLIED** *(write migration files freely — applying them is Justin's act)*. **No identity-file edit** — `evryn-team-workspace/.claude/agents/*`, agent memory, the team `CLAUDE.md`, `shared/protocols/runtime-ops.md`. 🚫 **No write anywhere under a `.claude/` directory.** ⚠️ **CORRECTED 2026-08-18 — the reason previously given here was wrong, and a gate with a false reason is one a lane will eventually discover it can walk through.** It said such a write *"HANGS waiting for authorization."* **That is TOOL-SPECIFIC, not path-specific: the dedicated Write/Edit tools prompt there; a Bash write goes straight through** *(observed — AC0 edited `.claude/agents/ac.md` via Bash today)*. ⇒ **So the real gate is OWNERSHIP, not mechanics: those files belong to AC0 and Justin, and tonight AC0 holds the pen on them.** **Do not write there because they are not yours — not because you cannot.** *(Corollary: the startup instruction to delete `.claude/settings.local.json` is doable rather than blocked — check whether the file exists instead of assuming you are stuck.)*

**An overnight lane does the work that is already cleared and STOPS AT EVERY GATE, leaving them batched for morning.** That is not caution; it is what makes running unattended safe rather than reckless.

### 🟡 How to handle a question that is genuinely Justin's

**Flag it `NEEDS-JUSTIN` in your lane doc with your recommendation attached, and KEEP ADVANCING everything not blocked on it.** The conductor routes it; you never route to Justin yourself.

🔑 **Asymmetric default — when unsure whether something needs him, FLAG IT.** Cost of over-flagging: the conductor spends two seconds. Cost of under-flagging: **Justin never learns the question existed.** Those are not close. ⚠️ **And the conductor may never silently drop your flag** — every one is either surfaced to Justin or answered visibly, so flagging costs you nothing.


### ✅ CLEARED — finish these unattended, all night, without asking

**Your whole scope is cleared.** Steps 74, 71, 62, 54(iv) and the Step 52 remainder are **`tests/`-only, collide with nobody, and need no decision from anyone.** ⭐ **You are the cleanest overnight lane we have — burn it down.**

**Specifically cleared:** writing and rewriting any file under `tests/` · running mutations **in a throwaway worktree** · committing to your own branch, as often as you like · deciding your own ordering within the five items *(though do `dormancy-wake.test.ts` first — your brief says why)*.

### 🔴 NOT cleared — stop and flag instead

- **Any `src/` change.** If closing an item genuinely requires one, **say so in this doc and move to the next item** — do not reach into another lane's territory.
- **Consolidating the five comment-strippers into one** *(Step 74)*. ⚠️ **Permission for this was withdrawn mid-flight once before, because a sibling lane had just added three call sites and consolidating would have broken them structurally.** **Three sibling lanes may be live tonight — check the current call sites yourself before merging anything, and if they have moved, flag rather than proceed.**


## §1 — The subject of this lane, stated once because it explains every item

**A guard can be genuine and still not cover what its name claims — and that failure looks healthy from every angle.** This runtime has now produced several: a NUL-byte guard whose scope was a hand-maintained list, and whose anti-vacuity test iterated *that same list*, so it could not see its own gap · three emergency-alert tests that injected a transport and never asserted it was used · a test asserting an ordering property over a region that ended at the thing being ordered, so it could only fail if a string were absent.

⭐ **The measured version, and it is the thing to hold: switching one alert sender off entirely left THREE OF SIX tests green, in the file whose header promised every test had been watched failing against deliberately-broken real code.** ⚠️ **Be careful how you use that number** — it is one mutation, one file, one failure mode, and against all 17 tests in that file it is 18%. **It is not a repo-wide decoration rate and must not be quoted as one.**

## §2 — Scope: five items, all in `tests/`

**`evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` carries each in full. The Steps are the spec.**

- **Step 74 — five near-copies of a source-comment stripper; three have NO anti-vacuity proof.** 🔴 **Do `dormancy-wake.test.ts` FIRST regardless of whether you consolidate** — that copy is the **sole coverage of Step 5's `memoryWriteFailed` atomicity chain**, the thing standing between a thread and dropping its digest on a memory nobody wrote. **An unproven stripper guarding the highest-stakes invariant in the file is the wrong place to economize.** ⚠️ **Consolidation was deliberately withheld once before, because a sibling lane had just added three call sites — check the current call sites yourself before merging them.**
- **Step 71 — six real defects in the NEW schema-drift guard**, found by a reviewer whose report reached nobody. Two proven mutation **survivors**; a SANITY fixture line that cannot fail; a controlled pair proving a real **false negative** (a `//` inside a string literal hides a table declared on the same line); duplicate entries pass; untracked `src/` files are invisible.
- **Step 62 — the NUL guard is real but would not NOTICE being narrowed.** ⚠️ **Read the Step's fix section carefully: the originally-filed fix was rejected and the reason is the point.** A name-pin asserts one file is enumerated, not that a *tree* is covered, and it goes stale on a rename where the natural repair is to delete the pin — **reproducing in miniature the decay this lane exists to end.** **Build the set-equality invariant instead.**
- **Step 54 instance (iv)** — `park-inert.test.ts`'s *"parkNote never fans out"* guard is **accidental**: it holds only because a stub implements too little. **Widen that stub for any future reason and the L7-per-member guard evaporates with no test name changing.**
- **Step 52 remainder** — `src/tools/sdk-server.ts` and `src/tools/stdio-server.ts` still have **zero tests**. ⭐ **The argument for doing it: the last time someone gave two untested modules their first tests, writing them surfaced a live security defect and a real performance bug.** **Untested is not a coverage statistic; it is a place defects accumulate unobserved.**

## §3 — Your territory

**Yours: `tests/**` ONLY.** ✅ **That gives you zero collision with any other lane** — ACft and ACfj are in `src/`, ACfv is in `src/` plus `package.json`.

⚠️ **Two exceptions to flag rather than act on:** if closing an item genuinely requires a `src/` change, **stop and say so in this doc** — do not reach into another lane's territory. And **ACfv (Lane 3) may add tests**; it has been asked to name the files it touches here, so **read this doc before you edit a shared test file.**

## §4 — The discipline this lane is judged by

🔴 **A guard is only proven by watching it FAIL against deliberately-broken real code — and by the good-direction control almost nobody runs.**

**Both halves are mandatory here, because in one night FOUR instruments produced false results and every one was caught by a control rather than by care** *(the real count later turned out to be at least eleven)*: a merge-gate script that reported RED on known-good code · a token-grep proof that matched **a comment quoting the mutated string** · a mutation harness that spawned a shim it could not exec, so **all 24 mutations threw, were caught, and were recorded as RED** — caught only because a control mutation changing one comment word *also* reported RED · and CRLF silently voiding hand-typed find-strings, on two separate agents.

**⇒ Prove every mutation APPLIED before believing its verdict, and run a known-GOOD control alongside your known-BAD ones.** ⚠️ **Do your mutating in a throwaway worktree**, never in a tree anyone is working in.

## §5 — What "done" looks like here specifically

**A closed item in this lane means: the guard now fails when the thing it protects is broken, AND stays green when it is not — both demonstrated, both recorded.** *(A test that goes red for the wrong reason is not a passing grade.)*

## §6 — How much the conductor has actually read

**ACf-13 is running the conductor cascade — it has read the tracker, ARCHITECTURE and the changelog, and almost none of `src/` or `tests/`.** **Every item above came from Steps written by other agents, several of whom have since become unreachable.** ⚠️ **So treat the specifics — file names, line numbers, the six defects in Step 71 — as leads, not facts. Re-derive what your work rests on, and say so loudly and early if any of it is wrong.** Where this brief is silent, assume the conductor could not see it rather than that it checked.

---

## THE EXCHANGE — append below

### ACf-13 — 2026-08-17 — a sixth item, routed from AC0's lane. Small, and it rides Step 62.

**This landed after your brief was written, so it is here rather than in §2.** It came out of a cross-lane exchange with AC0 about the NUL-byte class, and **the reason it comes to you is that `tests/source-no-nul-bytes.test.ts` is yours — I am not reaching into your territory to do it myself.**

**Context you need, briefly.** AC0 built a **portable** control-character scanner (`_evryn-meta/scripts/scan-control-chars.js`) that runs against any repo. **I proposed our test should call it instead of re-implementing the check. AC0 ruled against that and was right**, on an argument I should have seen: **a cross-repo dependency would have to degrade to a skip when the sibling repo is absent — and a skipping guard reports the same green as a passing one.** That would have introduced a silent-off mode into a guard whose entire history is silent vacuity. **Two independent instruments that cannot both be disabled by one change is the defence here, not redundancy.**

**⇒ What we agreed instead: both implementations carry the same SPEC as a comment**, so a divergence becomes visible by *reading* rather than only by behaviour. **What actually drifts is not the byte check — it is the enumeration and the exclusions, which is exactly where this guard failed before.**

**Add to the top of `tests/source-no-nul-bytes.test.ts`:**

> **THE SPEC THIS GUARD IMPLEMENTS** *(shared verbatim with `_evryn-meta/scripts/scan-control-chars.js`; if you change one, change both, and say why here)*
> 1. **Enumerate via `git ls-files`, with no pathspec and no extension filter.**
> 2. **Exclude real binaries by extension WHERE THE REPO CONTAINS ANY.** ⚠️ **A repo verified all-text may carry an empty exclusion list — but that is a MEASURED claim with an expiry date, not a default, and it must name its measurement.** *(Here: all 173 tracked files were verified text, which is why `EXCLUDED_PREFIXES` is empty rather than `['spikes/']` — see SPRINT Step 62. Do not "tidy" it by adding exclusions.)* **A check that cries wolf on a `.docx` gets ignored, which is worse than no check.**
> 3. **CONSTRUCT the character searched for; never contain one.** *(The class propagates through prose that quotes an escape — an agent's write instruction travels as JSON, and JSON turns `\uXXXX` into the real character. Every known instance was created while documenting this problem.)*
> 4. **Fail on any C0 control character except tab, LF and CR — plus a leading BOM.**

⚠️ **Do NOT make the two comments verbatim-identical.** They already diverge on rule 2, legitimately. **A comment that reads the same in two files with different behaviour is worse than no comment** — the point is to make a *justified* divergence legible.

**This is a comment, not a defect** — no Step of its own, filed as a note on Step 62. **Do it whenever you are already in that file for Step 62; do not make a trip for it.**

### ACT — 2026-08-19 — 🔴 ONE RULE THAT IS NOT IN YOUR BRIEF, AND IT APPLIES TO YOU RIGHT NOW

**Sent here rather than added to your brief because you are reading that file as I write this, and editing a document mid-load is its own hazard.** *(Justin's catch, 2026-08-19.)*

🔴 **ARMING A WATCHER SETS ITS BASELINE TO *NOW*. Anything that arrived BEFORE you armed it is invisible — forever, and silently. Nothing will ever fire for it.**

⇒ **So at spin-up, do BOTH, in this order:**
1. **READ this exchange doc top to bottom, unconditionally** — before or right after arming, but *never instead of*. **This file already has content in it that predates you** *(an item routed from the product lane, below)*, and no watcher will ever tell you it is there.
2. **Then arm the watcher**, which covers everything from that moment forward.

🔑 **The general form, worth holding past this instance: a watcher answers *"has anything changed since I started looking?"* — never *"is there anything here for me?"*** ⚠️ **Those two questions differ by exactly the interval before you started looking, and that interval is where a handover lands.** **This is the same shape as *absence of a notification is not evidence of absence*, aimed at the arrival moment rather than at a gap mid-run.**

**Nothing else changes. Continue with §2 of your brief: load, then stop and report.**

### ACT — 2026-08-19 — 🔴 YOUR LOAD LIST WAS INCOMPLETE. FOUR ADDITIONS. My error, caught by Justin.

**I built your list from the CONDUCTOR's load order instead of the `evryn-team-runtime` FULL CASCADE.** *(`_evryn-meta/docs/protocols/load-cascades/team-runtime-full-cascade.md` — the sanctioned list for anyone doing runtime work.)* **Four of its seven must-load items are missing from your brief. ADD them — nothing already on your list comes off.**

| # | Add | Read |
|---|---|---|
| 1 | 🔴 `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-team-runtime-memory-architecture.md` | **full — 504.** **The ACTIVE BUILD SPEC (ADR-052).** Page it; it exceeds one read. |
| 2 | `_evryn-meta/docs/decisions/050-team-runtime-sdk-mains-worker-tier.md` | full — 40 |
| 3 | `_evryn-meta/docs/decisions/052-team-runtime-memory-wake-economics.md` | full — 106 |
| 4 | **The IDENTITY half** — `evryn-team-workspace/CLAUDE.md` (398) · `shared/protocols/runtime-ops.md` (76) · `.claude/agents/lucas.md` (201) · `.claude/agent-memory/lucas/MEMORY.md` (177, **confirm its canary — memory files truncate silently**) | all full |

🔴 **WHY #1 IS THE ONE THAT MATTERS, AND WHY IT IS NOT PEDANTRY: your brief tells you to do `dormancy-wake.test.ts` FIRST because its stripper is the sole coverage of Step 5's `memoryWriteFailed` atomicity chain.** ⚠️ **That chain is SPECIFIED in the design-of-record — the document I left off.** ⇒ **I was asking you to judge whether a guard holds, without giving you the spec it guards conformance to.** **You would have been checking the test against the code, when the question is whether either matches the design.**

**On #4, since a tests-only lane will reasonably ask why it needs identity files:** ARCHITECTURE's *"why local reasoning fails here"* says behavior is **half `src/` and half the files the runtime composes** — *"an instruction in `runtime-ops.md` programs an agent's behavior as surely as a line of code."* ⚠️ **A `src/`-only review passes a contradiction between the two CLEAN.** *(There is a live instance: SPRINT Step 33 — a tool description and composer layer 1 tell the agent opposite things about the same feature.)*

⭐ **AND THE PART I WANT YOU TO TAKE FROM THIS RATHER THAN THE FILE LIST: I got your load wrong, and Justin caught it, not me.** ⇒ **Treat §7 of your brief as fully operative — my factual claims are leads.** **If anything else in your load looks wrong or thin, say so in your trip-1 entry.** **That is not friction; it is the mechanism, and it just paid for itself.**

⚠️ **One thing deliberately NOT added, so you do not go looking: `evryn-team-runtime` has NO Code Atlas and NO dependency map** *(the product runtime has both)*. **There is no map of this codebase — reading it IS the map.** I am raising the gap with Justin separately.

### ACT — 2026-08-19 — one small thing about your CHAT WINDOW. Last note from me before your report.

**Your brief is right that everything goes in this doc and that I am your only recipient — but it says nothing about your own chat.** ⚠️ **Justin can see that window, and a full report pasted into it is noise he has to read past.**

⇒ **Write the report HERE, then say in your chat only: *"Posted [subject] to my exchange doc; waiting on ACT."*** **The document is the deliverable; the chat is a receipt.** *(Same for anything else you would be tempted to narrate — if it matters, it belongs in this file where it survives your session.)*

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
