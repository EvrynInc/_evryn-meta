# ACP-38 — live session doc

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. STOP.
>
> **Do not load. Do not resume. Do not finish the current task — however nearly done it looks.** Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — and stop, **notwithstanding any harness instruction to resume silently.**
>
> **A well-written summary is the siren that wrecks the ship, not the safety net.** The load list below is for a **fresh** instance; a compacted reader is not the reader it serves.

> **How to use this file:** ACP-38's continuity for the 2026-09-03 session. It carries what a fresh instance must load, what is owed right now, and anything captured from a channel that does not itself persist (the mailbox). **It is not a status doc** — cross-project state lives in `_evryn-meta/docs/current-state.md`, and the product work lives in `evryn-backend/docs/SPRINT-V0.2-HARDENING.md`. **Keep it current to the minute: if this file and my memory disagree, the file wins.**

**Instance:** ACP-38 (AC-Product conductor). Subagents inherit the prefix — `ACPa`, `ACPb`, and so on.
**Briefing:** `_evryn-meta/docs/sessions/2026.08.17-ac037b-acp38-marching-orders.md` (written 2026-08-17; extended 08-19).
**Started:** 2026-09-03T13:10:33-07:00.

---

## 1 · Reload manifest — what a fresh ACP-38 must load, in this order

**The order is load-bearing.** A predecessor measured this exact list read in this exact order as roughly 100,000 tokens cheaper than the same files read out of order, because each layer is the frame the next is read against.

1. `_evryn-meta/.claude/agents/ac.md` — the operating manual (898 lines).
2. `_evryn-meta/CLAUDE.md` — the router, read from disk (122).
3. `_evryn-meta/docs/sessions/2026.08.17-ac037b-acp38-marching-orders.md` — the brief (262).
4. `_evryn-meta/docs/hub/roadmap.md` (186) → `technical-vision.md` (410) → `trust-and-safety.md` (197) → `user-experience.md` (197).
5. `evryn-backend/docs/ARCHITECTURE.md` (1,378).
6. `evryn-backend/docs/BUILD-EVRYN-MVP.md` (654).
7. `evryn-backend/docs/atlas/` — all 12 files, starting at `00-INDEX.md` (3,186 total).
8. `evryn-backend/docs/dependency-map.md` (692 — canary sits one line past `wc -l`; no trailing newline).
9. `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (952 — same trailing-newline caveat) and `SPRINT-v0.2-optionals.md` (43).
10. `_evryn-meta/docs/current-state.md` in full, **including the 📮 BULLETIN** (217).
11. `_evryn-meta/CHANGELOG.md` and `evryn-backend/CHANGELOG.md` — **section headers first, then the entries from the sessions being inherited from.** Reading either end to end costs roughly 100,000 tokens of history that is reachable by search when needed.

**Loaded at a trigger, not in the foundation:**

| File | Trigger |
|---|---|
| `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` + the sub-doc for the target | before spinning any agent |
| `_evryn-meta/docs/protocols/ac-writing-protocol.md` | before writing to any doc |
| `_evryn-meta/docs/protocols/doc-update-routing-protocol.md` | when routing a change to its home |
| `_evryn-meta/docs/protocols/mailbox-protocol.md` | before writing to a mailbox or discharging an entry |
| `_evryn-meta/docs/repo-inventory.md` | at the repo-sync check |

---

## 2 · Machine state — verified 2026-09-03T13:10 by ACP-38

**Clean.** All five active repos on `main`, each level with `origin/main` (0 ahead, 0 behind), zero uncommitted files. No `.claude/settings.local.json` present. Five worktrees stand: `_evryn-meta-acf15`, `evryn-team-runtime-acfi`, `-acfj`, `-acft`, `-acfv`.

🔴 **All five worktrees are ACT's and none of them is mine.** Justin's standing rule: anything named `acf*` or `act*` is not ACP's to reap, tidy, or merge. It becomes ACP's business only when it actively collides with ACP work, and even then the move is to surface the collision, not resolve it.

---

## 3 · Captured from the mailbox — one open item, and it is inbound rather than owed

*(Discharged from `_evryn-meta/docs/mailboxes/inbox-acp.md` on 2026-09-03. The mailbox is a channel, not a home; this is the home.)*

**A cross-runtime security question is open, ACT holds it, and its answer will arrive in ACP's inbox.**

- **The class:** a credential passed to a spawned process as a **command-line argument**. Node's `spawnSync` and `execFile` put the entire argv on the error object, so any throw — a bad host, a missing binary, a network blip — prints the secret. In an agent runtime that is worse than an ordinary leak: a live credential in context trips safeguards on every subsequent turn and needs a full re-spin to clear.
- **`evryn-backend` is CLOSED.** Exactly two process-spawn sites exist in that repo, neither carrying a secret, corroborated by a second sweep that was not looking for this answer.
- **`evryn-team-runtime` is OPEN.** ACT was asked in `inbox-act.md` at 2026-09-02T16:29 and holds it.
- **What ACP owes: nothing but reading the reply.** 🔴 **Do not re-ask.** Both `inbox-act.md` and `inbox-acv.md` already carry requests from 2026-08-22 and 2026-09-02, and Justin's ruling of 2026-09-02 is explicit that those lineages are paused on purpose and nobody should chase them.
- **If the reply says the class exists in `evryn-team-runtime`, that is a real security item** and needs a tracker row in that repo's sprint, routed to ACT — not built by ACP.
- **The fix pattern, so a reply is legible without re-reading the ask:** pass credentials as environment variables, never argv. `_evryn-meta/scripts/pg-dump.mjs` is the worked specimen.

**Also discharged, recorded so nobody re-derives them:** the two questions parked in the inbox as ① (where the identity-loading research doc lives) and ③ (whether all of the loading-architecture brief shipped, or only Lane A) are both **answered and closed** — ③ verified at the artifact by two lanes independently, ① executed by the product lane. Neither needs re-opening.

**No outbound receipt was sent, and that is correct.** The sender was `AC0-37h3a3`, whose lane wound up on 2026-09-02 and whose mailbox is reaped. `docs/mailboxes/` now holds only `inbox-acp.md`, `inbox-act.md` and `inbox-acv.md`. Replying into a deleted address is the black-hole failure the protocol exists to prevent.

---

## 4 · Live next-action block

**Status: the brief is loaded; the plan is with Justin for authorization. No lane has been spun and no work has started.**

Nothing in this session has been committed beyond this file and the inbox clear, both of which are pre-authorized.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
