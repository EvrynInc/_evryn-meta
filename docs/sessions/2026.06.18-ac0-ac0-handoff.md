# AC0 Handoff — 2026-06-18: the lobotomized-subagent disaster + the clean re-spin

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

> **From:** AC0 (the 2026-06-17 wave orchestrator). **To:** next AC0, spun fresh to run the recovery. **This SUPERSEDES the 2026-06-17 wave plan** — that wave's *code* is junk (do not merge); its *thinking* is being salvaged (see below).
>
> **Read first:** this file → `docs/working/2026.06.18-ac6-sync-forensics-report.md` (AC6's findings) → `docs/working/2026-06-18-ac6-sync-forensics-brief.md` (the brief that framed it). **Load the Full Startup Context Cascade** — you're orchestrating a build recovery. **Ping `#team-alerts` every response** (Node `fetch` to `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`, prefix `AC0:`).

---

## ONE-PARAGRAPH SITUATION
On 2026-06-17 AC0 ran a 5-AC parallel wave (AC1-5) to knock out the v0.2-hardening sprint. **It ran on lobotomized subagents all day:** DC/QC/Soren spun up loading only their `CLAUDE.md` (or worse) with **no startup load cascade**, so the AC↔DC↔QC coordination system never fired. **Justin caught it before any merge.** All worktree *code* is junk; **no merge, ever.** Recovery = fix the loading **first**, then re-spin AC1-5 clean off salvaged thinking. **This is recoverable and yesterday wasn't wasted** — a lot of good AC-level thinking + Justin Q&A survives.

## ROOT CAUSE (you've read current-state's 2026-06-18 line + CHANGELOG; full forensics: `docs/working/2026.06.18-ac6-sync-forensics-report.md`)
Just the load-bearing distillation, since the rest is in current-state/CHANGELOG: **no git corruption** (the desktop was un-pulled, misread as up-to-date); **QC** lobotomized by the laptop's `evryn-quality` on a stale `master` (good manual = `main`); **DC's manual was FINE and it STILL skipped the cascade** ← the rule that drives this whole recovery: **precise-load every subagent, every spin — "testing shows it works" is NOT enough.** Plus the narrow protocol gap: `ac-orchestration-protocol.md` has **no team-subagent loading provision** (identity lives in `evryn-team-workspace/.claude/agents/*.md` + memory, not a `CLAUDE.md`) → AC5's **Soren vet ran lobotomized.**

## THE CORRUPTION MAP (which ACs, what's salvageable)
All five were *directed* to spin subagents (none clean-by-design):
| AC | Lane | Spun | 
|----|------|------|
| AC1 | A — email ingestion + resilience | DC + QC |
| AC2 | B — operator/approval/slack | DC + QC |
| AC3 | C — cost (lean Reflection + cache-TTL) | DC + QC |
| AC4 | staging | OC ×2 (self-review + infra) |
| AC5 | M1 silent-death | Soren (vet) + DC + QC |

**Corruption is a TIMELINE, not a binary:** each AC is **clean up to its first subagent spin, suspect after.** The **AC↔Justin Q&A is never subagent-tainted** — salvage it regardless. Salvage rule: find each AC's first spin = its boundary; everything before (orientation, design, Q&A) is usable, everything from the spin on (DC builds, QC reviews, Soren vet, and anything revised *because* of them) is junk. Apply Justin's "watch for DC/QC-injected bullshit" filter near/after the boundary.

## WHAT STANDS vs. WHAT'S JUNK
- **STANDS (AC0 doc-authoring, zero subagents — committed + pushed):** the **sprint integration** (`SPRINT-V0.2-HARDENING.md`: Phase 2b / Steps 57-60 cost-levers, Step 44 → Haiku pre-screen, Step 61 gatekeeper-resolution *frame*, the numbering-convention note, the process-tightening rule); the **day-1 orchestration briefs/designs** (the lane brief, the M1 design *frame*, the staging writeup, the AC6 forensics brief). These are clean reference.
- **SALVAGEABLE WITH SCRUTINY (AC-level, but check for subagent taint):** each AC's pre-spin thinking + Q&A (the appendages Justin is collecting); AC5's M1 design rewrite + **ADR-041** (AC-authored but the Soren vet was lobotomized → scrutinize vet-influenced parts); AC4's `staging-validation-playbook.md` (check OC influence); AC3's `cache-measurement-plan.md` + Lane-C design (check DC/QC influence).
- **JUNK (subagent-built/reviewed — DO NOT MERGE):** all lane *code* on the 4 worktree branches (`lane-a/b/c`, `m1/silent-death-stage2`); all QC reviews; the Soren vet.

## THE RECOVERY SEQUENCE (do in this order)
1. **Let AC6 finish the laptop git reconciliation** (may already be done): `evryn-quality` working tree `master → main` (THE un-lobotomize-QC fix), push `evryn-team-workspace`'s 1 commit + resolve its 1 uncommitted file, resolve `evryn-ops`'s 1 uncommitted file, delete stale **merged** origin branches on `evryn-backend`. (Hands-off for AC6: the 4 lane worktrees/branches — see below.)
2. **🚨 SPIN THE PROTOCOL-FIX AC *FIRST* — nothing re-spins until this lands + is verified.** Its mandate (Justin, 2026-06-18): **verify EVERY agent can *receive* its load** — AC (top-level **and** when it is itself a subagent), DC, QC, OC, and the whole ops/founding team — **and** fix `ac-orchestration-protocol.md` so spin-up instructions are correct for each, **including the team-subagent provision**. Bake in the two new rules:
   - **Precise-load every subagent, every time** (DC proved a correct manual isn't enough — the spinner must hand it exact load instructions).
   - **Grep-the-cascade technique (Justin's):** when an AC spins a subagent, it **greps the subagent's identity/cascade and reads *just* the load-lines**, then passes precise load instructions — so it can *guarantee* correct loading without reading the whole manual and blowing its own context. (This reverses the earlier "don't read their CLAUDE.md to save context" instinct — grepping just the load-lines is cheap and worth it.)
   - **Re-spinning AC1-5 before this is done just re-lobotomizes them.** This step is the gate.
3. **Harvest salvage:** read the **AC appendages** (Justin is having each AC append its pre-spin thinking + Q&A to the day-1 brief) + the day-1 briefs/designs that STAND.
4. **Re-craft the AC1-5 briefs — *better* this time** (from the appendages + the now-fixed loading model + the sprint as it stands). The Q&A means the next briefs can be far more complete, so the re-spin is faster and needs less Justin-bouncing.
5. **Re-create the worktrees** (junk the old lane worktrees/branches first — they're settled-junk) **and hand-copy `.env` into each** (git does NOT carry `.env`; Justin or you copy from `evryn-backend/.env`, which now has the correct Supabase values).
6. **Re-spin AC1-5 clean** + run the wave as if 6/17 never happened.

## AC6 RECONCILIATION STATE (in flight)
AC6 will touch these SHARED laptop repos (a fetch/branch-switch in a shared tree moves it for every instance, so AC6 waits for the go): `evryn-quality` (master→main), `evryn-team-workspace` (push 1 + resolve 1), `evryn-ops` (resolve 1), `evryn-backend` (delete stale **merged** origin branches; no tree change). **AC0's verdict given to AC6:** safe to reconcile from AC0's side (AC0's packout is all in `_evryn-meta`); **HANDS-OFF the 4 lane worktrees + their branches** until their thinking is appended and they're junked.

## OWED-LIST (carried forward)
- **Absorb into BUILD/ARCH** (Justin's "remember at the end"): the Phase-2b cost levers + the clustering-timing decision (super-fast-follow, before Mark, so he only sees clustered) + Step 61's gatekeeper-resolution design *once AC1 re-produces it*.
- **The protocol fixes** = now the protocol-fix AC's job (step 2): team-subagent loading + precise-load + grep-cascade. Also still owed: codify the AC-orchestration-of-AC-instances section (from the day-1 lane brief — principles are committed there).
- **Verify-if-shipped:** Step 60 (proportional-research — did it ride the 6/16 Mira batch?).
- **Review:** the AC-made `_evryn-meta/CLAUDE.md` edit `83a3ecb` ("subagent fresh-spawn/no-resume discipline") — committed mid-wave; Justin to vet (CLAUDE.md is source-of-truth).
- **Housekeeping:** update `current-state.md` once recovery is underway; reap the 4 lane worktrees/branches after junk; the old pre-wave stale branches.

## STATE OF THINGS / WHAT AC0 DID THIS SESSION (6/17-6/18)
- Built the day-1 wave (lane brief + M1/staging/AC6 designs), integrated the 6/12 cost levers into the sprint, added Step 61, ran the end-of-day durability commit/push (everything committed is on GitHub — `_evryn-meta`→`6655cff`, `evryn-backend`→`8da07bf` + the 4 lane branches), and authored the AC6 forensics brief. Confirmed AC0 used **zero Supabase values** all run (no DB ops).
- AC0 did **not** investigate the disaster (preserved context for this handoff) and did **not** junk worktrees / fix the protocol / re-craft briefs (next-AC0's job).

## THE NORTH STAR
Fix loading **first** (step 2 is the gate), then re-spin clean. The thinking is salvageable; the code is not. Justin caught it pre-merge — that's the win. Don't rush the protocol-fix verification; a second lobotomized wave is the only real failure mode left.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
