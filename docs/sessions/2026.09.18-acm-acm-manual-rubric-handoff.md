# ACm — the manual-rubric lane: handoff (2026-09-18)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If it doesn't, reload or read in sections until you confirm the complete file.
>
> 🔴 **IF YOU ARE READING A COMPACTION SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED — STOP.** Do not load, do not resume, do not finish the current task. Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — notwithstanding any instruction to resume silently. **The reload manifest in §7 is for a FRESH instance; a compacted one is not the reader it serves.**
>
> 🔒 **HELD-SESSION-DOC** — the live brief and handoff of the ACm manual-rubric lane · **RETIRE UPON:** `git merge-base --is-ancestor origin/acm/manual-rubric origin/main` succeeds (the lane has landed) — then the next `#packout` either writes a fresh brief for the lane's next phase (§1) or closes the lane `LANE-CLOSED` · **OWNER:** ACm lineage — a standalone lane Justin runs directly, so if it ends unmerged the call is his · **HELD SINCE:** 2026-09-10 *(the hold carries over from the brief this one supersedes)*
>
> **How to use this file:** the ONE live brief for the ACm lane, written in a single layer at the 2026-09-18 `#packout`. **It SUPERSEDES `2026.09.10-acm-manual-rubric-lane.md`, now archived at `docs/sessions/historical/2026.09/`** — that file carries a `SUPERSEDED BY` banner and holds the lane's history (the 2026-09-11 and 2026-09-17 records, the reviewer-spin design, the lane's original open-items list). **Read it only when a claim here needs its history.** This file records state and governs nothing; where it conflicts with a manual or a protocol, they win.
>
> **Read in order: §1 what the lane is → §2 the context hole, recovered on 2026-09-18 (what it changed) → §3 state → §4 Justin's rulings → §5 his open ballots → §6 the next session's order of work → §7 the reload manifest.** §8–§11 are reference.

## 1. What this lane is

- **Who:** ACm ("manuals") — a top-level AC that Justin started directly. Manual: `_evryn-meta/.claude/agents/ac.md`. Reports to Justin under `# ACm's Report #N` headings, closed with `## End of Report`. **The last report sent was the 2026-09-17 set-down report — read its number off the chat; Justin had not read it at this set-down, so §5 carries its ballots in full.** An in-place re-spin continues the count; a fresh session starts at #1 and says so. **Never promise a future report's number.**
- **The job:** apply the writing rubric — `docs/protocols/ac-writing-protocol.md`, "Writing a rule the current models will actually follow" — to the four agent manuals (`ac.md`, `dc.md`, `qc.md`, `oc.md`), and fix the stale and cutover references found on the way.
- **Where:** worktree `_evryn-meta-acm`, branch `acm/manual-rubric`.
- 🔴 **WHAT HAS LANDED — corrected 2026-09-18 from git, because the superseded brief said "nothing from this lane is on `main`," and that was wrong.** **The branch already landed ONCE, on 2026-09-08: `10c44c8` "Merge acm/manual-rubric: the writing rubric and the enumeration fix."** That landing carried the rubric itself (`c7eb52d`), the compaction alarm (`d19d2ec`) and the enumeration fixes (`723df11`, `9935b6b`); `1185254` (orchestration: what a subagent does NOT inherit from the harness) landed the same day. **Everything on the branch from `c933a93` (2026-09-10) onward is UNMERGED.** The superseded brief was opened on 2026-09-10, after that work had dropped out of ACm's context (§2), which is why it never knew.
- ⏭️ **THE LANE'S NEXT PHASE — queued BEHIND the in-flight work, on Justin's order (2026-09-17).** His words: *"part of my initial intent was to see if there are things in ac.md that don't need to be there — if there are things that could be protocols that every AC doesn't need to read on every run, I'd want to identify those. The key is: I need to know that they'll actually read them when the time comes where they should, and also that we don't pull them out into a protocol, only to basically put them back in to the always-on-load list — like we did with the writing protocol."* And: *"it prob makes sense to close out what we have in flight and then focus on that."*
  - **Status: NOT DONE, by anyone — checked in the repo.** Lane A (ACm's predecessor, `docs/sessions/historical/2026.08/2026.08.11-ac0-acm-loading-architecture-brief.md`) extracted AC's manual *away from what other agents load* (`CLAUDE.md` → router); it never asked what AC itself needs on every run. This lane's rubric pass cut `ac.md` from 211,902 to 206,903 bytes (−2.4%); **`main` has since grown it past where it started** (213,702 at `eaeac69`).
  - **The two tests that answer his two conditions:** (1) *"will they actually read it?"* — a pointer that relies on an agent **recognizing** its situation is a remember-to-obey rule, the weakest thing we build; **attach extracted content to a load that already fires reliably** — his command words (`#lock`, `#packout`), a cascade loaded at the start of that kind of work, or a tool action a hook can see. (2) *"don't pull it out and put it back"* — the writing protocol went back because its trigger fires in practically every session; **if a trigger fires in most sessions, extraction buys an extra read and nothing else.**
  - 🔴 **ALREADY PROPOSED AND DECLINED — do not bring it back as new: converting the `#` protocols to skills.** On 2026-09-08 ACm found that a skill with `disable-model-invocation: true` loads only when Justin types it, at zero resident cost, and proposed it for `#lock`/`#packout`/`#sweep`/`#align`. **He declined:** *"I want to use my #protocols — that way I can say 'read the #lock protocol [at trigger]' without it loading it right now… I can decide precisely when I want these to load. I'm not really having compliance problems with stuff like this, so I don't think I want to fix what ain't broken."* ⇒ **His command words already ARE the reliable trigger for test (1); he wants control of WHEN, not a harness mechanism.** *(Recovered from Stretch A — §2.)*
  - **Where to look first (a starting list, not an analysis):** the conducting-only sections of `ac.md` — "Orchestrating DC and QC", "Working with QC" — could ride the conductor cascade · the Code Atlas standing order could ride the build cascades · the DB and Railway tool notes are moment-of-use. **Likely to stay:** worktree discipline, the heartbeat, the communication rules.

## 2. The context hole — RECOVERED on 2026-09-18

**The task brief was `2026.09.18-act23-acm-context-hole-recovery-brief.md`** — written by `ACT-23` (the team-runtime conductor) at Justin's request, from a forensic read of this session's transcript. **Its condition was met by the recovery below, and it was RETIRED on Justin's word the same evening — archived on `main` at `docs/sessions/historical/2026.09/`. Read it only if a recovered claim is disputed** (it carries the uuids and the extraction script). Justin: *"another lane discovered that you actually lost context before, and we've been forwarding you with that hole in your context ever since… we'll need to go back and grab that, and see what kind of damage it created."*

**What happened, in short:** on 2026-09-08 an API call failed with retries, the harness logged them as a chain of error rows, and a later message attached to the end of that ERROR chain instead of to ACm's actual work. **While the session stayed open nothing was lost; when it was REOPENED, Claude Code rebuilt the context by following the chain, which routes around the side branch.** Three stretches of ACm's OWN work dropped out that way — **no compaction, no summary, no warning.** None of Justin's messages were lost; the on-disk transcript is complete.

| Stretch | When (PT) | Lost at the reopen of | What it held, per ACT-23's one-line glimpses *(leads, not facts — ACT-23 did not read the stretches in full)* |
|---|---|---|---|
| **A** | 09-08 10:41–10:48 | 09-08 13:26 | the revised rubric written into the worktree's `ac-writing-protocol.md` · **building the compaction alarm**, and reworking it after Justin's correction that it pings nobody · 🔴 **the EXPERIMENT Justin specified** — a fully loaded AC, told to load everything, with no mention of compaction in its brief · Report #3. **The experiment's RESULT survived** (a task notification on the chain); **its design, his specification and the hook's design reasoning did not.** |
| **B** | 09-08 13:03–13:07 | 09-08 13:26 | **the 09-08 landing** (§1's commits) · 🔴 **resolving a conflict with ACT-16's edit to `team-runtime-full-cascade.md` by COMBINING both** · Report #8 |
| **C** | 09-08 13:28–13:36 | 09-10 14:01 | a +10-line orchestration-protocol edit (landed as `1185254`) · **piloting the rubric on `ac.md`'s `node_modules` block** · two self-corrected measurement errors, one of them **LF vs CRLF** · 🔴 **`c933a93`, the pilot, was committed at 09-10 14:02 — ONE MINUTE after the reopen that erased the memory of making it** |

**Consequences:** the superseded brief (opened 09-10), both compaction summaries (09-10 15:44 and 09-11 17:19) and the 2026-09-17 in-place re-load were all written from holed context.

### ✅ RECOVERED 2026-09-18 — by ACm, in this same session, on Justin's instruction

*(Each stretch read in full from the transcript with ACT-23's extractor, trimmed to print ACm's own text whole and each tool call as one line — no chain-walk warnings. **Justin's on-chain messages around the stretches were read too:** they were never lost, but they carry his rulings on what the stretches proposed.)* **Nothing below is a lead any more — each item says what it confirms or corrects.**

**Stretch A (09-08 10:41–10:48) — the compaction alarm, the experiment, Report #3:**
- ✅ **The alarm's design is durable and matches disk.** First draft (a `require` inside ESM — rewritten) → proof-of-fire with a self-labelled `trigger=SELF-TEST-ignore-this` Slack ping → wired into `.claude/settings.json` (`PreCompact`/`PostCompact`), log artifacts gitignored → **Justin's correction, 11:23:** *"the script shouldn't ping anybody — it should just tell the model that all it can do is report its own compaction to its spinner. For a top-level AC, it should know that that requires a ping to me"* → **his speak-last design, 12:09**, which replaced the planned `PreToolUse` blocker: *"we don't block tool use… tell them as the final instruction… we need to actively say that this overrides that."* **`scripts/compaction-alarm.mjs`'s header carries all of it.**
- 🆕 **The experiment — recorded here for the first time.** Justin specified **a fully loaded AC (`ACx`), told to load the entire product cascade regardless, with NO mention of compaction anywhere in its brief** — to see whether an unprompted compaction gets caught. **Result: it compacted (`trigger=auto`) and the hook fired on both events.** The payload finding it produced (a subagent's `PostCompact` carries no `agent_id` and reports its PARENT's `session_id`) is in the script header's "MEASURED 2026-09-08".
- 🆕 **A datum for ACT-A** *(a lane under ACT, the team-runtime conductor, reworking the alarm's `PostCompact` half)* **— for Justin to pass on, as ACT-23's brief asks:** on 09-08 Justin ran a **manual `/compact` and the hook did NOT fire.** ACm's reading then (Report #8): *his session predated the hook, and hooks are read at session start* — **a hypothesis, never tested.** Justin: manual compaction doesn't need the alarm anyway — *"even though it says to continue, it doesn't. It's only the autocompact where it does that."*
- ✅ **Report #3's rubric work is durable.** Justin's two counterexamples killed the "count the recurrences" test, and his reframe — *"our primary tool is persuasion"* — became the protocol's "Persuade. Emphasis is what you reach for when you have not" (`c7eb52d`, carrying his own edits: the softened *"BE VERY CAREFUL STRIPPING STRIDENCY"*, the *"is it even needed?"* clause, the model-version stamp).
- 🔴 **LOST UNTIL NOW, and it bears directly on the next phase (§1): the SKILLS proposal — and Justin's DECLINE.** Report #3 found that a skill with `disable-model-invocation: true` loads only when Justin types it, at zero resident cost, and recommended converting `#lock`/`#packout`/`#sweep`/`#align`. **Justin, 09-08: *"I think I want to use my #protocols — that way I can say 'read the #lock protocol [at trigger]' without it loading it right now, bogging your context. And I can decide precisely when I want these to load. I'm not really having compliance problems with stuff like this, so I don't think I want to fix what ain't broken."*** No skills exist in `_evryn-meta`.
- ⚠️ **Report #3 numbered its ballots ⑩ ⑪ ⑫ — a different run from the 2026-09-11 ballots of the same numbers.** All three are answered: ⑩ vet the rubric in the worktree (*"worktree is perfect"*) · ⑪ skills (declined, above) · ⑫ keep the hooks (*"as long as this is telling any agent to stop when it compacts, I'm happy… make sure it's always failing gracefully"*). **Never confuse them with §4's ⑪ and ⑫.**

**Stretch B (09-08 13:03–13:07) — the first landing, Report #8:**
- ✅ **What landed, on Justin's go** (*"I vetted everything of yours in SCM. You can commit… fix the enumeration bug in all the places it lives… Do it on main. commit right away"*): `723df11` (the enumeration fix in the orchestration protocol and conductor cascade) and `d19d2ec` (the alarm: speak last) **directly on `main`**; `c7eb52d` (the rubric) and `9935b6b` (the enumeration fix in `dc.md` and two cascades) **on the branch**, merged by `10c44c8` with `--no-ff`. **§1 now records it.**
- 🆕 **The conflict with ACT-16 — recorded here for the first time, and resolved soundly.** ACT-16 committed to `team-runtime-full-cascade.md` while ACm was editing it. **ACT-16's version was better in one respect: it removed a stale count** (*"~36 as of 2026-07-21"* against a real 41 — a completeness claim that rotted untouched). ACm combined both: their reasoning that no count belongs there, plus ACm's bare-directory warning. *"Same defect class, caught from two directions on the same day."*
- ✅ **Report #8's follow-ups are closed.** The ACT-17 handoff line still carrying the `src/**/*.ts` glob (ACm left it to ACT-16) was fixed at `f137f20`; the open question — does the alarm's instruction reach an auto-compacted agent? — was settled by Justin's 2026-09-11 observation (ruling ⑩).

**Stretch C (09-08 13:28–13:36) — an orchestration beat and the pilot:**
- ✅ **The +10 lines to `ac-orchestration-protocol.md`** — the second half of what a spawned subagent does NOT inherit — went in uncommitted for Justin's vet and landed as `1185254`. They extend the callout beginning *"Do not design a brief that depends on the injection carrying curr[ent]…"* — **so the protocol warned about the stale injection BEFORE the 2026-09-11 reviewers measured it (§11).**
- ✅ **`c933a93` IS the pilot, with the CORRECTED numbers.** Its message reads *"node_modules reap: 3,975 -> 1,919 bytes"* — the LF-normalized figure. Mid-stretch ACm first got 1,095 (a `sed` range that missed part of the new block), then caught that `git show` emits LF while the working file is CRLF. The commit also covers a second block (the three-dot diff, 1,830 → 1,750) and deliberately leaves the `--no-ff` block alone. **Neither handoff ever quoted the pilot's numbers.**
- The stretch ended on an API error (ENOTFOUND) after a failed ping; **Reports #10 and #11 reached Justin afterwards, on the chain.**

**Also settled while recovering:** ✅ **"the worktree copy of the writing protocol carries a fix `main` lacks" is TRUE** — it is `ff2c8c3`'s rewording of the "visible ≠ cheap" line; `main` still carries *"This corrects an earlier draft of this section…"* · **the six tool-only side branches** ACT-23 listed (09-03, 09-10, 09-11) were not read: none was followed by a reopen before the next compaction and no drop was recorded after them — ACT-23's assessment, which the drop check below agrees with.

**⇒ NET DAMAGE: small.** Everything that landed was sound and on disk; the holes were in ACm's MEMORY. They made the superseded brief state one falsehood — nothing on `main` — and miss four things: the first landing, the experiment, the skills decline, and the manual-`/compact` datum. **All four are here now; the recovery brief is retired.**

✅ **MEASURED BY ACm AT THIS SET-DOWN, so the next session does not repeat it:**
- **No NEW hole.** A drop check over the whole transcript (5K threshold) finds exactly: the two 09-08 reopen drops ACT-23 reported (the control — it fires), the two compactions, and one zero-usage API-error row at 09-08 13:36. **No drop after 09-11 17:19, including across the 2026-09-18 reopen — so everything from the 2026-09-17 re-load onward is intact in this record.** *(The check is ~30 lines of read-only `node` — it lived in the session scratchpad; ACT-A's version is in `docs/sessions/2026.09.18-act22-act-a-notes.md` §10, and ACT-23's brief §6 says to lower its threshold to 5K.)*
- **All of B's commits are on `origin/main`** (`723df11`, `d19d2ec`, `c7eb52d`, `9935b6b`, `4cea625`, `10c44c8`, `1185254`, `f137f20` — `git merge-base --is-ancestor <sha> origin/main`).
- **`current-state.md` on `main` now carries a BULLETIN entry by ACT-23 about ACm's holes** (Justin owns the fix).

**Re-derived, not defended:** every judgement in this file that rested on A, B or C was re-checked above. **None changed direction** — the alarm design, the rubric, the enumeration fix and the pilot all hold; what changed is what this file *knows*.

## 3. State (measured 2026-09-18 against git)

| Item | State |
|---|---|
| **Branch** | Tip `cd1375b` = `origin/acm/manual-rubric` at the start of this packout; worktree clean. *(The packout's own two commits sit on top — §12.)* |
| **Landed on `main`** | The 2026-09-08 work, via `10c44c8` (§1). |
| **Unmerged on the branch** | `c933a93` pilot · `4ceb202` `ac.md` rubric pass · `ff2c8c3` corrective pass · `b2afe81` `dc`/`qc`/`oc` cutover + Justin's tweaks · `a0813bb` three small fixes · `459ff35` merge of LOCAL `main` · **`358b18b` the reviewer-driven cleanup** · **`eaeac69` merge of `origin/main` (`916c8b5`)** · **`67992f5` ruling ⑨'s ⑫-independent half of `dc-lock-protocol.md`** · session-doc commits. |
| **`main` has moved again** | `origin/main` was `5986f07` at this packout — well past the last sync. ⇒ **sync before any edit: `git fetch`, then merge `origin/main` (never a local `main` checkout — §11), on Justin's word.** 🔴 **`main`'s `ac.md` carries binding rules this worktree lacks until then:** close every report with `## End of Report` · never forecast a report's number · describe a name the first time, spell out any legend, give a file's real path · measure context with `scripts/context-size.mjs`, never estimate · a compacted instance must not commit. |
| **Orphans** | Two of ACT's commits (`a011fef`, `769fa99`) sit on the branch as duplicates of `ddcac29`/`5c2bc6a` — **harmless in content**; before the landing merge, confirm `git diff --stat origin/main...HEAD` shows only this lane's files. |
| **Meta `CHANGELOG.md`** | No entry for the unmerged work — owed at the landing merge, by the AC that merges (`ac.md`, routing table rule 3b). |
| **Collision exposure** | Other ACs committed to `ac.md` on `main` four times on 2026-09-17 alone while this lane held unmerged `ac.md` edits. **Recommendation standing with Justin: land the lane rather than write a "manuals have unmerged edits" warning into `current-state.md`.** |

## 4. Justin's rulings for this lane

**Standing:**
- **Commit gate.** Manuals, protocols and `current-state.md` stay uncommitted for his SCM vet and are committed only on an explicit command in his most recent message, by pathspec. **Session docs commit freely.** **Push only on his word** — on 2026-09-17 ACm pushed `eaeac69` without it; left in place, because un-pushing needs a force-push.
- **Compaction.** The STOP block governs. He made two deliberate exceptions for THIS instance — continue after the 2026-09-10 compaction on a full re-read (2026-09-11), and re-spin in place after the second (2026-09-17: *"you need to re-load docs you had loaded before the compaction — the compaction summary is notoriously lossy"*). **Neither carries forward.**
- **The reviewer design (2026-09-10):** one subagent per manual — the `dc`, `qc` and `oc` agent types — each reading the pre-lane version and asked whether ACm got anything wrong. **Done 2026-09-11; findings in §10.**
- **Keep this file current as work proceeds, so he can re-spin at any point.**

**Rulings on ballots, in his words where they were words:**
- **2026-09-11:** ⑤ sync after the reviews, not before · ⑦ *"DC should send nothing anywhere other than to AC"* · ⑧ delete `dc.md`'s beginner-example bullet · ⑨ reconcile `dc-lock-protocol.md` · ⑩ the alarm: *"it's clearly reaching the agents in the background. Nothing to fix."* · ⑪ go on the plan.
- **2026-09-17:** ⑫ **(a)** per-repo mailbox files are retired as a channel — worklist §8 · ⑲ **(a)**, *"I think (a) is likely the answer. I'd want to see the change in SCM before you commit it"* · ⑳ **(a)** · ㉑ **(a)** · ㉒ **(a)** · ㉓ **(a)**.
- 🔴 **2026-09-17 — none of those are executed yet, by order:** *"the actual doing of this work from these ballots — I want you to wait until tomorrow — and I want you to leave it all in SCM for me to vet."*

**Other 2026-09-17 rulings:**
- **Integration was delegated** — *"I'll let you decide how to do that"* — which covered a merge, not a push.
- *"Yeah, I decided archive not delete — current-state is just stale"* (that is ㉑).
- **Watch the load:** *"be on the lookout for things that we don't have to load right away, to see if the load list can be cut down or deferred. Prob not — just something to watch out for — esp as the work shifts."* ⇒ §7 is organized by what each work item needs.
- **The lane's next phase** — §1.

**2026-09-18:** *"do a full #packout and #lock. Be thorough — especially with the #packout steps"* · then the recovery, done by THIS instance rather than left to the next: *"I actually think you might be in better position to do that, since you have the old messy handoff… the next instance is only going to have the cleaned-up handoff"* (§2) · **㉔ (a): the next session is a FRESH re-spin** · **㉕ (a): archive the superseded brief**, after one last top-to-bottom scan that everything was captured.

## 5. Open ballots — IN FULL

**Justin could not see ⑬–⑱ in the 2026-09-17 session** (*"all that has been truncated in the chat"*), and had not read the set-down report that re-presented them. **They have been open since 2026-09-11. Put each in front of him IN FULL — never as a one-line label.** Numbers are stable; sub-items get numbers (12.1), options get letters.

- **⑬ Who may write into another agent's inbox?** Inboxes (`docs/mailboxes/inbox-<name>.md`) are created the moment anyone writes to one. While cleaning up `qc.md`, ACm added a line telling QC the inboxes are the channel for cross-session exchanges; **QC's reviewer caught that this gave her an UNGATED channel** — she could open a line to any agent and route findings around the AC that spun her — contradicting three rules in her own manual (no direct QC mailboxes unless Justin establishes them; her no-write list; her commit discipline). **A permissions change dressed as a tidy-up.** `dc.md` and `oc.md` have similar lines. **(a)** when spawned, DC and QC never write to an inbox — the spinning AC owns the channel; started directly, only to ACP's or ACT's; OC the same; plus a one-line mailbox-protocol fix: an inbox message is committed in `_evryn-meta`'s main checkout, never on a worktree branch (DC's reviewer found a worktree-branch commit wakes the watcher but never appears in the file) ⟵ **recommended** · **(b)** keep direct access and loosen QC's three rules.
- **⑭ Startup steps that misfire when an agent is spawned.** All four manuals open with steps written for an agent Justin starts: delete `.claude/settings.local.json` (a spawned agent may delete its PARENT session's live approvals), ask Justin about `#dev-alerts` (a subagent can't ask), confirm the canonical branch (fails by design in an assigned worktree). All three reviewers found it. **(a)** split startup into "if you were spawned" / "if Justin started you," all four manuals ⟵ **recommended** · **(b)** only DC, QC and OC · **(c)** leave.
- **⑮ Proposed edits to Justin's own text.** (1) `dc.md`/`qc.md` "Working With Justin": his note says most bullets apply only when talking to him directly, but about half bind a spawned agent too, and *"Railway CLI is yours … `railway up`"* reads as permission for a spawned DC to deploy. (2) `qc.md`: a grammar break in *"what Evryn — or other agents (when relevant) — do."* (3) `oc.md`: his routing note says who but not how, and calls itself a stub with no owner; its frozen identity lines say DC implements OC's fixes directly, which conflicts with DC now working only under an AC. **(a)** ACm drafts each as a marked proposal, uncommitted, for him to keep or reject line by line ⟵ **recommended** · **(b)** leave his text alone.
- **⑯ Fixes outside the four manuals.** (1) `docs/protocols/orchestration/spinning-dc-qc.md` — QC's manual expects DC's own report to arrive with her task; nothing tells the spinning AC to send it. (2) `docs/current-state.md` — a *"QC frontmatter STILL OPEN"* line contradicted by a later line in the same file. (3) the dashboard's stale *"live in prod today"* comment on the health check — **path not yet found; ACm has not opened the file.** (4) **Justin's, not ACm's:** the auto-memory stub (`MEMORY.md` in his Claude projects folder) points at a `CLAUDE.md` section that no longer exists; `ac.md` forbids ACm writing it. **(a)** ACm drafts 1–3, uncommitted ⟵ **recommended** · **(b)** leave them.
- **⑰ QC's dropped idea.** Removing stale text from `qc.md` also cut a never-adopted idea — adversarial and security review passes at release milestones — the only deletion that lost an idea rather than a stale fact. **(a)** keep it as one line marked "proposed, not adopted" ⟵ **recommended** · **(b)** drop it.
- **⑱ OC's proposed trap entry.** OC's manual keeps "looks broken but is intentional" traps; its reviewer proposed one: **the team runtime (`evryn-team-runtime`) is deployed to Railway but paused on purpose until the Meta-Meeting** — don't report it as down. Only AC adds entries, with Justin's OK. **(a)** add it ⟵ **recommended** · **(b)** skip.
- **⑫.1 The two old mailbox files in the product repo.** ⑫(a) retires the per-repo channel. Two files from that era remain in `evryn-backend` (ACP's repo): `docs/ac-to-dc.md` — an uncleared 263-line brief to an old DC instance, from June — and `docs/dc-to-ac.md`. The version of ⑫ he answered did not mention them, and `lock-protocol.md` step 10 forbids retiring a doc before checking it is not the only home of still-owed work. **(a)** ACm reads both and reports what is in them; whether and by whom they are retired stays ACP's call ⟵ **recommended** · **(b)** leave them wholly to ACP.
- ✅ **㉔ and ㉕ — ANSWERED (a) on 2026-09-18** (a fresh re-spin; archive the superseded brief). Kept below as asked.
- **㉔ The next session: in place, or fresh?** Measured: **~701K tokens at the start of this packout**, higher at its end (§12). The next session's work is the recovery (100–150K of transcript reading), then the orchestration protocol in full (~85K), `main`'s new commits and the edits. **In place, that crosses the ~850K pack-out line before the work is done.** **(a)** a fresh session loading §7 ⟵ **recommended** · **(b)** in place again.
- **㉕ Archive the superseded brief.** `docs/sessions/2026.09.10-acm-manual-rubric-lane.md` now carries `SUPERSEDED BY` pointing here. `lock-protocol.md` step 10 asks for his OK before moving it. **(a)** `git mv` it to `docs/sessions/historical/2026.09/`, filename unchanged ⟵ **recommended** · **(b)** leave it where it is.

## 6. The next session, in order

Every ballot edit stays **UNCOMMITTED** for his SCM vet (§4).
1. **Re-arm the heartbeat** — ⚠️ on 2026-09-18 this session's `Monitor` came back *"expires in 30m"*: **re-arm on each expiry notice.** **Measure context.** If this session was reopened rather than started fresh, **run the drop check before trusting anything held** (§2).
2. ~~Recover Stretches A, B, C~~ — **DONE 2026-09-18 by the previous instance (§2); the recovery brief is retired.**
3. **Put §5's open ballots in front of him IN FULL** if still unanswered.
4. **Sync:** `git fetch`, merge `origin/main` on his word, then read the diffs `main` gained in the files this lane edits before touching them.
5. **Execute:**
   - **⑫(a)** — the worklist in §8. Ask ⑫.1 first.
   - **⑲(a) — two source-of-truth docs give opposite orders about rebasing.** `ac.md` ("Worktree & Branch Discipline"): *"MERGE WITH `--no-ff`, AND NEVER SILENTLY REBASE A BRANCH THAT HAS BEEN PUSHED"* — committed docs cite SHAs by hand, and a rotted citation fails silently. `ac-orchestration-protocol.md`, its moved-base section: *"if it fails, REBASE the branch onto current `main`… Rebase rather than merge-into."* **The fix: give the protocol's line the condition `ac.md` already states — rebase only a branch that has NOT been pushed.** He sees the diff before any commit. *(It decided a real action: on 2026-09-17 ACm merged rather than rebased, because `358b18b` is on his vet list.)*
   - **⑳(a) — `doc-update-routing-protocol.md`'s third-exclusion row** says it is *"not yet in force… Until `docs/working/` is actually retired… Whoever lands that consolidation should update this row."* `docs/working/` was retired on 2026-09-02 and `current-state.md` states the rule as live, so the protocol and the snapshot now contradict each other. **Draft the row update.**
   - **㉒(a) — Lucas's two offers, from `current-state.md`'s BULLETIN (2026-09-04), folded into the writing protocol as proposals:** (1) **the writer-side half of progressive depth** — our protocol tells a reader to *"read ONE layer"* but never obliges the writer to make that layer sufficient, so pointers get pushed ever thinner with nothing saying where to stop; (2) **the tiered-timestamp clause** — *"the tier drops, the verify doesn't"*: even a date-only stamp is pulled from the clock, never typed. Source: §7 B.
   - ㉓(a) needs nothing.
6. **The answered parts of ⑬–⑱.**
7. **Re-check `oc.md`'s three factual rewrites in `358b18b` against source** — `/health`'s three states, the emergency triggers, the DND text (§7 C names the files). Owed before the landing merge.
8. **Stop and hand him the diff.** After his vet: commit on his word → merge to `main` with `--no-ff` → **fold in ㉑** (`current-state.md`'s *"ADR-057. Decision due ~2026-09-18 on archive-vs-delete"* → decided 2026-08-21, archive; **still stale on `main` at `5986f07`** — ACT-23's commit that says "Ballot 21 fixes" used ACT's own ballot numbering, not this lane's) → meta `CHANGELOG` entry → push on his word → reap the worktree and branch.
9. **Then the next phase (§1).**

## 7. Reload manifest — for a FRESH ACm, or a re-load in place

**Paths are repo-relative inside `_evryn-meta`; a path in another repo names that repo.** Read each file in full and confirm its bottom canary. ⚠️ **What changed from the manifest proven on 2026-09-17, surfaced as `packout-protocol.md` step 4 requires:** the recovery brief, ACT-A's notes §10 and the superseded brief are ADDED to C — each only if a claim here is disputed. Nothing was added to A: the recovery is done (§2).

🔴 **WHICH COPY TO READ — sync first, then read everything from the worktree.** `main` moves daily and edits the files this lane edits. ⇒ **`git fetch`, merge `origin/main` into the branch (on Justin's word — it is a commit), then read every file below from `_evryn-meta-acm`.** **If you cannot sync first**, read the four manuals, the writing protocol and `dc-lock-protocol.md` from the worktree and everything else from `origin/main` — `git show origin/main:<path>` into the scratchpad, then `Read` it.

**A · Always:**
1. `.claude/agents/ac.md` — your manual.
2. `docs/hub/roadmap.md` and `docs/current-state.md` — the Light cascade.
3. `docs/protocols/ac-writing-protocol.md` — mandatory before writing anything; the worktree copy carries a one-line change `main` lacks — `ff2c8c3`'s de-argued "visible ≠ cheap" line (verified 2026-09-18).
4. This file.

**B · What the ballot work needs.** `.claude/agents/dc.md`, `qc.md` and `oc.md` are needed by ⑫ and most of ⑬–⑱ — load all three.

| Work item | Needs, beyond A |
|---|---|
| **⑫** | 🔴 `docs/protocols/ac-orchestration-protocol.md` **IN FULL** · `docs/protocols/lock-protocol.md` · `docs/protocols/dc-lock-protocol.md` · `docs/protocols/mailbox-protocol.md` · ADR-004 · ADR-009 · **not** the two `evryn-backend` files until ⑫.1 is answered |
| **⑲** | the orchestration protocol's moved-base section (covered above) |
| **⑳** | `docs/protocols/doc-update-routing-protocol.md` |
| **㉑** | `docs/current-state.md`, at the merge |
| **㉒** | the writing protocol + `evryn-team-workspace/shared/projects/ops/2026.07.16-lucas-sweep-align-findings.md`, items X-6 and X-7 |
| **⑬–⑱** *(open)* | ⑬ `mailbox-protocol.md` + the three manuals · ⑭ all four manuals · ⑮ `dc.md`/`qc.md`/`oc.md` · ⑯ `spinning-dc-qc.md`, `current-state.md`, the dashboard file · ⑰ `qc.md` · ⑱ `oc.md` |

- **Also:** `lock-protocol.md` in full before any `#lock`; its steps 5 and 9b route through `doc-update-routing-protocol.md` (change-type 9 is where a ruling of Justin's gets its durable home).

**C · Deferred, each with its trigger:**
- `docs/protocols/packout-protocol.md` — on `#packout`, or a re-spin.
- `docs/protocols/orchestration/spinning-dc-qc.md` — if ⑯ comes back (a), or before any spin.
- `docs/repo-inventory.md` — if a repo-status question arises.
- `docs/protocols/orchestration/being-an-admiral.md` — if ACm runs a standalone lane of its own.
- `evryn-backend/src/notify/emergency.ts`, `src/safety/liveness.ts`, `_evryn-meta/docs/deploy-log.md` — for §6 step 7.
- `evryn-team-runtime/src/composer/index.ts` and `src/config/skills.ts` — only if the `dc.md` identity-half correction is disputed. ADR-057 — only if a cutover fact is.
- `CHANGELOG.md` — when writing the merge entry. The lane's diff (`git diff -U0 --word-diff=plain ff2c8c3 HEAD -- .claude/agents/<name>.md`) — for Justin's vet.
- **The retired recovery brief** `docs/sessions/historical/2026.09/2026.09.18-act23-acm-context-hole-recovery-brief.md` and `docs/sessions/2026.09.18-act22-act-a-notes.md` §10 (the drop check) — only if a recovered claim in §2 is disputed; the transcript is `~/.claude/projects/c--Users-jbmcg-Evryn-Code--evryn-meta/2ad5d3d3-2c1d-4cb8-ab1d-3ef93c2076c6.jsonl` (laptop), read-only.
- 🆕 **The superseded brief** — archived at `docs/sessions/historical/2026.09/2026.09.10-acm-manual-rubric-lane.md` (find it by name: `git ls-files "*acm-manual-rubric-lane*"`) — only when a claim here needs its history (its §4 is the reviewer-spin design, §5 the lane's original open items, §12–§13 the 2026-09-11 and 2026-09-17 records).

**D · Budget:** read §5 ㉔ before deciding how to start.

## 8. ⑫(a) — the worklist, split by whether a reference INSTRUCTS or RECORDS

**The distinction is required by `doc-update-routing-protocol.md`'s own exclusion rule: a corrected path inside a record of what was true then is a falsification.** The premise was re-verified at source on 2026-09-17: `ac-orchestration-protocol.md`'s closing note still calls `<repo>/docs/ac-to-dc.md` files a live channel.

- **INSTRUCTIONS → fix:** `ac-orchestration-protocol.md` (closing note) · `ac.md` — the Slack-ping bullet, the "Quick reference" line, the "Session start" line in "Orchestrating DC and QC" · `lock-protocol.md` steps 15–16 *(AC's own lock still says to peek at `dc-to-ac.md`)* · `dc-lock-protocol.md` steps 3, 4, 6, 7 · ADR-004 · ADR-009 *(extends the pattern to QC and OC)* · **`dc.md`'s and `oc.md`'s own redirect lines** — re-keyed from the 2026-08-12 date to the path itself; `oc.md`'s also names "runbooks" that do not exist (reviewer findings DC-2 and OC-7, §10). **`qc.md`'s equivalent was already fixed in `358b18b` (QC-10); its one remaining mailbox line — the cross-session inbox sentence — is ⑬'s, not ⑫'s.**
  - ⚠️ **ADRs get a dated STATUS NOTE, not a rewrite** — an ADR records a decision as it was made.
- **RECORDS → do NOT touch:** ADRs 029, 030, 034, 035, 036 (each names a mailbox as where a reply *was* written) · every changelog · `docs/historical/**` · `docs/sessions/historical/**`.
- **NOT AUTHORIZED:** the two `evryn-backend` files — ⑫.1.
- **Not in the list by count, deliberately:** an earlier report forwarded "ten instructional sites"; the list above is the authority.

## 9. For Justin — the vet list (timeline)

**Everything is on branch `acm/manual-rubric`, pushed — except the 2026-09-18 packout's own two commits, which await Justin's word (§12).** Committed unvetted at his instruction at the 2026-09-11 and 2026-09-17 set-downs. **Oldest first; the 🔴 rows need reading.**

| Commit | What it is | Vet status |
|---|---|---|
| `c933a93` | Pilot — the rubric on two blocks of `ac.md`'s *Worktree & Branch Discipline* | Rolled into the `ac.md` pass he approved. ✅ **Verified 2026-09-18 (§2, Stretch C): it is the pilot, with the LF-normalized numbers** — committed one minute after the reopen that erased the memory of making it |
| `4ceb202` | `ac.md`: the rubric applied, and eleven defects it surfaced | ✅ Vetted in SCM; he authorized it |
| `ff2c8c3` | `ac.md` corrective pass | ⚠️ Committed without authorization; he checked it afterwards and let it stand |
| `b2afe81` | `dc`/`qc`/`oc`: the cutover reaches the manual bodies, plus **his own tweaks** | ✅ He read all three and authorized it |
| `a0813bb` | Three small fixes to his tweaks | ✅ He asked for them |
| `459ff35` | Merge `main` into the branch (ballot ⑤) | No conflicts. ⚠️ Merged the LOCAL `main` checkout, which held two of ACT's unpushed commits — the source of the harmless orphans (§3) |
| `358b18b` | **Reviewer-driven cleanup** — `dc.md`, `qc.md`, `oc.md`, `ac.md`, `scripts/compaction-alarm.mjs` (comments only) | 🔴 **UNVETTED — the big one** |
| `eaeac69` | **Merge `origin/main` (145 commits), 2026-09-17** | 🔴 **UNVETTED, and ONE PART IS HIS TEXT:** the `ac.md` conflict on the writing-protocol bullet in the Light cascade kept **his own wording (`03ea080`) verbatim** as the lead sentence and re-added the lane's don't-duplicate clause after it. **Read that one bullet.** *(Pushed without his go-ahead.)* |
| `67992f5` | **`dc-lock-protocol.md`, ruling ⑨'s ⑫-independent half** — two stale banners corrected, step 5 leads with the rule, steps 8 and 10 repointed off "CLAUDE.md"; steps 3, 4, 6, 7 untouched | 🔴 **UNVETTED** — small |
| session-doc commits | This lane's handoffs as the lane moved | Session docs commit freely |

**Where to look hardest in `358b18b`:**
1. **`oc.md`'s three factual rewrites** — the emergency triggers (live since `v0.2.2`), `/health`'s three states, and "the channel does NOT pierce Do Not Disturb" — checked at source on 2026-09-11 against `evryn-backend/src/notify/emergency.ts`, `src/safety/liveness.ts`, `docs/deploy-log.md` and ADR-041. **A second check is owed before the landing merge (§6 step 7).**
2. **`dc.md`'s frozen Build Mandate** — a spawned DC now returns a question under `<questions_first>` instead of pinging or saying so in its report, none of which reach AC before it acts; and **research goes to AC** (ruling ⑦).
3. **`qc.md`'s enumeration rule** — `git -C <repo> ls-files` plus a known-true control, because it returns an empty list with no error from the wrong folder or in the team runtime.
4. **The frozen blocks touched:** `dc.md` Working With Justin and Build Mandate; `oc.md` Identity (Lucas, Soren) — where ACm also reverted its own earlier stylistic rewordings, keeping only fact fixes.

## 10. Reviewer findings (2026-09-11) — carried VERBATIM from the superseded brief

> **Carried byte-for-byte**, because this is the only home of the three reviewers' output. **Its internal section references point into the superseded brief** (`docs/sessions/historical/2026.09/2026.09.10-acm-manual-rubric-lane.md`), where §4 is the reviewer-spin design and §5 the lane's original open-items list. **Line numbers are in each manual as of `a0813bb`.**

**Each entry: the reviewer's finding (line numbers in the file at `a0813bb`) → ACm's read → where it goes.** "Cleanup" = applied in `358b18b`, unvetted (§9 of this file). "Ballot" = needs Justin's ruling before any edit — **for where each of those ballots stands now, §4 and §5 of this file.**

**Receipts reconciled by counting each reviewer's tool calls from its transcript** (`count-tools.mjs` in the session scratchpad — counts only, no content): DC 12 + 23, QC 16 + 39, OC 12 + 24 — **every declared count matches, and every errored call was disclosed.** *(The harness's own usage figure is cumulative for DC and QC but covered only the work trip for OC; that, not a swallowed call, was the apparent mismatch.)*

### DC on `dc.md` — returned 2026-09-11 ~14:12 PT: no blockers, 9 non-blockers, 7 cosmetic

- ⚠️ **Two slips DC disclosed himself:** he wrote a temp file to `/tmp` (deleted, confirmed gone), and he **opened this session doc, including §5** — the open-items list withheld from reviewers. He marked which findings predate that read (7, 8, 16, part of 9); only finding 9's rationale was taken from §5. ⇒ **Lesson for the next review brief: if a list is withheld, say where it lives and not to open it.**

1. **Line 143 (FROZEN, Build Mandate)** — "flag it BEFORE you act: a Slack ping, a question in chat, or worst-case … your report back" still assumes the mailbox era; for a spawned DC none of those reaches AC before acting, and `<questions_first>` is the real route. "Reporting order (report back before deploying)" describes a DC that deploys. → **Agree. Cleanup.**
2. **Line 300** — the per-repo-mailbox redirect is keyed to the wrong date and scope: the orchestration protocol's closing section still calls `<repo>/docs/ac-to-dc.md` files a live channel, so do ADR-004, `dc-lock-protocol.md` steps 3/4/6/7, and `ac.md` ("Slack pings" bullet, the Orchestrating DC and QC "Quick reference" and "Session start" lines); and `evryn-backend/docs/ac-to-dc.md` still holds an uncleared 263-line brief to "DC4" from 2026-06-01. → **Agree. Ballot: are per-repo mailbox files retired?** If yes: path-keyed redirect in `dc.md`, and fix `ac.md`, the protocol's closing section and ADR-004's pointer, and retire the two `evryn-backend` files.
3. **Lines 298/302** — a DC in a worktree would commit an inbox message on its own branch; the recipient's `git log --all` watcher fires, but the file in `_evryn-meta`'s main checkout never shows the message. *(Reasoned from mailbox protocol §2; untested.)* → **Agree it is a real gap; it belongs in the mailbox protocol. Ballot.**
4. **Line 114 (FROZEN)** — "a path inside this repo resolves as written" is wrong from a worktree: links resolve against `_evryn-meta`'s main checkout. `ac.md`'s matching bullet has the same gap. → **Agree. Cleanup (both manuals).**
5. **Lines 98–99 (Justin's paragraph, FROZEN)** — "most of the following is only when you speak to Justin directly," but about half the bullets bind a spawned DC (timestamps, work stamps, Node `fetch`, the Railway-logs trap), and "Railway CLI is yours … `railway up`" reads as permission to deploy on a spawned trip. → **Agree. Ballot — it is Justin's text; DC suggests moving the general bullets to a "Working environment" section and splitting the Railway bullet.**
6. **Session-startup list (unchanged)** — a spawned DC cannot do three of four steps as written: deleting `settings.local.json` may delete the PARENT session's file; it cannot ask Justin about `#dev-alerts`; the canonical-branch check fails in an assigned worktree. **The same steps exist in `qc.md` and `oc.md`.** → **Agree. Ballot — split startup by arrival mode, in all three manuals.**
7. **Provenance lines 24, 35, 41, 331** — false now. → **Agree. Cleanup (§5 item 4).**
8. **Line 331 vs `dc-lock-protocol.md`** — the mailbox material spans steps 3, 4, 6 and 7 (not two); step 5 was already corrected, so the file's own banner is stale on that point; steps 8 and 10 still say "CLAUDE.md". → **Agree. Part of the ⑨ reconcile.**
9. **Line 234** — the withdrawn four-item identity list, and it matters because line 237 tells DC to load identity files itself when a brief omits them. → **Agree. Cleanup (§5 item 2).**
10. **Line 207 "(or Soren)"** reads two ways, and "AC owns this file" contradicts the protocol (Soren owns ARCHITECTURE of record; AC is co-owner). → **Agree. Cleanup — the parenthetical is Justin's, so it goes to his vet as a proposal.**
11. **Line 213** — "(which it assembles …)" refers to AC → "he". → **Agree. Cleanup.**
12. **"CLAUDE.md" still at lines 42 and 343.** Outside the repo: the auto-memory stub loaded every session points to an "Auto-Memory Hygiene section in CLAUDE.md" that the router no longer has. → **Agree on 42/343 (cleanup). The stub: flag to Justin — `ac.md` forbids writing the auto-memory file.**
13. **Lines 140, 292** — bold on swapped-in phrases marks the edit, not the rule. → **Agree. Cleanup.**
14. **Line 298** lost its reading trigger for the orchestration protocol. → **Agree. Cleanup.**
15. **Line 306** mixes two inbox models (per-designation addresses vs. lineage-shared inboxes). → **Partly: the ambiguity is the mailbox protocol's. Note to Justin; minimal `dc.md` change.**
16. **Lines 102–103** — beginner-pitched bullets under the new learning-curve line. → **Agree on the example bullet (ruling ⑧). Disagree on "Breadcrumb everything": `ac.md`'s EliJ section keeps it.**

### OC on `oc.md` — returned 2026-09-11 ~14:18 PT: no blockers, 12 non-blockers, 8 cosmetic

**Five of these are errors ACm made (1, 2, 4, 6, 8)** — each settled by a file ACm had loaded or named.

1. **L217, L237** — ACm's edit calls the emergency triggers unbuilt. **M1 Stage 2 shipped in v0.2.2 (2026-06-29)**; `notifyEmergency` fires from polling-dead, the breaker halt, the unauthorized-send check, mail loss and the daily affirmation; `current-state` and the operator guide say so. ACm hedged a flipped condition instead of re-deciding it — the writing protocol's named failure. Process death is Healthchecks.io's job, not `notifyEmergency`'s (ADR-041). → **Agree. Cleanup.**
2. **L219** — the `/health` entry is false in prod: Step 78 rebuilt it (commit in `v0.2.9`, deployed 2026-07-23) to three states — breaker halted → 200 "halted"; poll loop or DB not OK → 503 "wedged"; else 200 "ok" — with the Slack socket reported but not affecting the code. The `fetchProcessUp` dashboard pointer is dead (now `fetchHealth()`). ACm's ⏳ note sat on it instead of checking the file it names. → **Agree. Cleanup.** *(Adjacent, outside the manual: the dashboard's own "live in prod today" comment is stale — flag to Justin.)*
3. **L235–236** — "rings through Do Not Disturb" is false: `emergency.ts`, ADR-041, the operator guide and `current-state` all say the channel is DND-respecting; nothing wakes Justin at night. *(Pre-existing text.)* → **Agree. Cleanup.**
4. **L219, L237** — ACm's new notes tell OC to "correct this entry" — but OC does not write its own manual (L214). And L270's approval list names "CLAUDE.md files", which no longer covers `.claude/agents/`. → **Agree. Cleanup: "report what you find so AC can correct it."**
5. **L284, L288–293** — routing still names DC and QC, who have no watched inbox (writing `inbox-dc.md` reaches nobody); Justin's note is advice with no consequence stated, says "reach out to Soren or Justin" without saying how, and calls itself a stub with no owner; it also contradicts FROZEN L69 ("DC can implement directly from your diagnosis") and L73. → **Agree. Cleanup for ACm's part; Justin's part and the FROZEN L69/L73 amendments go to his vet as proposals.**
6. **Old L291 deleted** — ACm removed OC's startup mailbox check; only the `#lock` check (L315) remains. The mailbox protocol requires a spin-up read. → **Agree — a loss ACm introduced. Cleanup: read `inbox-oc.md` at startup, when started directly.**
7. **L286** — the redirect's scope is too narrow (the orchestration protocol's closing note still calls per-repo files a live channel), and it names "runbooks" that do not exist. → **Agree. Folds into the per-repo-mailbox ballot (DC finding 2).**
8. **L75, Lucas (FROZEN)** — ACm deleted "Not yet running"; it is still true (the team runtime is paused, autonomy off until the Meta-Meeting). The line now reads as though Lucas operates. → **Agree — a meaning change ACm introduced. Cleanup: restore the true condition.**
9. **Question 8** — third confirmation that the harness injects the main checkout's old manual. → Recorded in §4.
10. **L35, L40, L42** — maintainer notes false; L40 even invites a maintainer to "re-cut this from an older commit," which would silently undo every correction. → **Agree. Cleanup (§5 item 4).**
11. **L108, L350** — backup checks point at the wrong mechanism: Supabase Pro's daily auto-backups are the real restore path; the `pg_dump`s are archive. *(Pre-existing.)* → **Agree. Cleanup.**
12. **L53, L55** — startup steps assume a direct start (same as DC finding 6). → **Ballot (all three manuals).**
13. **L76, Soren (FROZEN)** — "When the team runtime is running" is ambiguous (deployed but paused); the SRE-under-Soren plan appears only in a research note. → **Partly: fix the ambiguity ("once team-runtime autonomy is on"); whether to mark the plan "proposed" is Justin's call — it is his frozen text.**
14. **L94** — ACm's "your identity and methodology live here" drops that OC's writable folders (`docs/ops/`) live here too. OC also proposes a pattern entry: the paused team-runtime deployment is a "looks broken, is intentional" trap. → **Agree on the wording (cleanup); the pattern entry goes to Justin.**
15. **L284** — "you watch your own inbox" is unconditional; arming a watcher is Justin's call. → **Agree. Cleanup.**
16. **L288** — "which has not changed" argues with deleted text (§5 item 11). → **Agree. Cleanup.**
17. **L214** — ACm dropped the inbox as a place to surface a pattern. → **Agree. Cleanup.**
18. **L216** — "Seed entries (verified 2026-06-17)" now heads entries marked unverified. → **Agree. Cleanup.**
19. **L72–74 (FROZEN)** — alongside the fact fixes, ACm made purely stylistic rewordings with no stale fact behind them. → **Agree in principle. Cleanup: revert the stylistic parts, keep the fact fixes.**
20. **Cutover leftovers** — L24 "being retired", the L43 heading "`# CLAUDE.md — OC`", L264's correction note. → **Agree. Cleanup.**

### QC on `qc.md` — returned 2026-09-11 ~14:24 PT: no blockers, 7 non-blockers, 7 cosmetic

1. **L72–77, L107** — `git ls-files` can come back empty with no error too: in `evryn-team-runtime` `git ls-files identity` returns 0 (its identity half is not a directory — the composer opens files across repos), and from the wrong repo (a Bash cwd reset) both return 0. ACm's parenthetical now names the team runtime, which invites exactly that. L107 overstates `find` ("both under-enumerate silently"; one recorded incident). → **Agree. Cleanup: `git -C <repo> ls-files …` plus a known-true control (non-zero count; `identity/core.md` present); correct the team-runtime parenthetical.** *(QC's optional idea — a committed enumeration script, per the rubric's "machine-checkable → build the guard" — goes to Justin.)*
2. **L520** — 🔴 **ACm's new mailbox sentence gives QC an UNGATED channel**: under the mailbox protocol you create an inbox by writing to it, so opening a channel became QC's own call. It contradicts L166 ("no direct QC mailboxes unless Justin establishes them"), L344's write list, and commit discipline (the old manual's pre-authorized-commit exception was deleted with the old mechanics). A spawned QC could route findings to a peer, around her spinner. **Neither a rubric fix nor a stale-reference fix — an authority grant, the "permissions change wearing a tidy-up costume" `ac.md` warns about.** → **Agree — the most important finding of the three reviews. Ballot (with the inbox-access question for DC and OC).**
3. **L99** — Tier-0 item 4 expects DC's report, but no AC is told to provide it (`spinning-dc-qc.md` never mentions it); "ask for it" means a blocking `<questions_first>`; and pulling it into trip 1 would leak the task. → **Agree. Cleanup in `qc.md` ("arrives with your task; if absent, note it and proceed"); the matching line in `spinning-dc-qc.md` is a protocol edit — ballot.**
4. **L479** — the pointer to `dc.md` for the Node service-role env setup lands on a file with no such recipe. → **Agree. Cleanup: point at `evryn-backend/backups/README.md` once ACm reads it.**
5. **L458–460 (unchanged)** — "Do NOT read other repos' CLAUDE.md files" blocks reading the team runtime's composed layer 1 (`evryn-team-workspace/CLAUDE.md`) on a team-runtime review. **`dc.md` carries the same rule.** → **Agree. Cleanup: an exception when that file is part of the runtime under review (both manuals).**
6. **L30, L97 vs L31 (unchanged)** — a three-trip derive trip has no `<mandatory_load>`, which these lines call a briefing error. → **Agree. Cleanup: exempt the derive trip.**
7. **SESSION STARTUP (unchanged)** — misfires when spawned (as DC 6, OC 12). → **Ballot (all three manuals).**
8. **L39 maintainer note** — ACm's rewritten argument is weak (returning findings needs no Write tool); and the deleted war story was rebutting a claim still live in `current-state.md` (its QC-frontmatter "STILL OPEN" line, contradicted a few lines later). → **Agree on the rewrite (cleanup). The `current-state` correction is a source-of-truth edit — ballot.**
9. **L61** — "if you don't know which instance you are, ask" misfires for an unnumbered spawned QC. → **Agree. Cleanup.**
10. **L492, L518** — the 2026-08-12 cutoff is too early (this manual kept the instruction until 2026-08-20); L518's "no longer resolve from the shared parent" names the wrong paths. → **Agree. Cleanup: drop the date (as with DC 2 and OC 7).**
11. **L532** — ACm dropped a never-adopted idea ("adversarial/security passes at release milestones") along with the stale half. The only deletion that lost an idea rather than a fact. → **Ballot: keep one line marked "proposed, not adopted"?**
12. **Self-references** — L24 "being retired", L35/L44 "not edited / verbatim", L45 "`# CLAUDE.md — QC`", L123's pointer to the CLOSED `SPRINT-MARK-LIVE.md`. Leave L65's heading ("Auto-load" is an anchor AC greps for). → **Agree. Cleanup.**
13. **L116 (Justin)** — "what Evryn — or other agents (when relevant) — do" breaks the grammar, and the bullet is specifically about `evryn-backend/identity/`. → **Agree. Proposal for Justin's vet.**
14. **L312–313 (Justin, FROZEN)** — the new paragraph doesn't say which bullets still bind a spawned QC (timestamps would get dropped). → **Agree (as DC 5). Proposal for Justin's vet.**

## 11. Durable facts and learnings — held here until they have a proper home

**Learnings candidates — for Justin's WORDING before filing** (`lock-protocol.md` step 9b):
1. **Sync a branch with `origin/main` after a fetch — never a local `main` checkout another agent may be mid-work in.** The remote-tracking reflog proved it: at 2026-09-11 14:46:34 ACT ran `pull --rebase` on its local `main` and pushed, replaying its unpushed commits under new SHAs; ACm's `459ff35` had merged that local `main` while it held the pre-rebase `a011fef`. **No one force-pushed `main`** — ACm's 2026-09-17 report and the messages of `eaeac69` and `5c294a2` wrongly said its history was "rewritten"; they stand uncorrected because rewriting a pushed commit would itself be a force-push. *(Local home: §7's copy rule. General home: `LEARNINGS.md`.)*
2. **In a long session, a ballot carried forward must be restated IN FULL every time** — Justin's view of older messages truncates. *(Candidate home: `ac.md`'s stable-numbering rule.)*
3. **After ANY session reopen, run the drop check before trusting what you hold** (§2). *(ACT-A and ACT-23 are working this class — `anthropics/claude-code#92089` and the API-error route; say so to Justin rather than filing it independently.)*

**Harness and environment facts:**
- **The body the harness injects into a spawned `dc`/`qc`/`oc` agent was the OLD manual** — measured 2026-09-11 by all three reviewers — so the `<identity>` block's instruction to read the named manual from disk is what put the current version in charge. **Re-check it before the next spin** — it was measured once, and harness behavior changes. *(✅ The orchestration protocol already warns about it — the callout that `1185254` extended on 09-08 begins "Do not design a brief that depends on the injection carrying curr[ent]…" (§2, Stretch C). The reviewers' 2026-09-11 measurement confirmed it.)*
- **`Monitor`'s `persistent` flag, one session, two days:** honored on 2026-09-17; on 2026-09-18, after the overnight reopen, the SAME session capped the heartbeat at 30 minutes. **A data point for `ac.md`'s rule that a session's behaviour does not carry across a restart.**
- **A subagent's call count must be reconciled against its transcript, not the `Agent` tool's usage line** — the usage figure was cumulative for two reviewers and work-trip-only for the third.

**For the next review brief:** if a list is deliberately withheld from reviewers, **say where it lives and that they must not open it** — DC found the withheld open-items list on its own, disclosed it, and marked which findings predated the read, which is the only reason that review stayed usable.

**Justin's calls, not built:** QC's idea that the enumeration rule — now stated three times across the manuals and machine-checkable — should become a committed script (enumerate both halves, assert a control) rather than more prose.

**Unchecked by the 2026-09-11 reviewers, recorded so nobody assumes they were:** whether Justin's VSCode root really is `_evryn-meta` (sole source: `ac.md`) · that prod runs the rebuilt `/health` (inferred from git ancestry and the deploy log, not observed) · whether his phone lets the emergency app through Do Not Disturb · whether a Railway healthcheck is configured in the dashboard · `evryn-team-runtime/docs/ARCHITECTURE.md` line 150.

## 12. The packout record (2026-09-18)

**Justin's order:** *"do a full #packout and #lock. Be thorough — especially with the #packout steps. Since we just reloaded, some of them may not apply, but if they do, do them. Don't skip or skimp."* Context at the start: **~670K, measured**.

**Step 1 — form: SUPERSEDE.** `lock-protocol.md` step 10's trigger had fired repeatedly on 2026-09-17, the old brief's own banner committed the next `#packout` to superseding it, and the context-hole finding corrects its headline and reframes half of it. **This file is a fresh single layer; the reviewer findings were carried byte-for-byte by script** — they are the only home of the reviewers' output, and retyping 13KB invites exactly the silent compression the writing protocol warns about. **Safety-committed as `8e1788b` before the re-reads**, per `packout-protocol.md`'s own advice to secure the handoff first.

**Step 0 — the `#lock`, every step answered:**
- **1 · CHANGELOG** — no entry for the branch's unmerged work; owed at the landing merge. 🔴 **And a MISSED SHIP, found by the step's reconcile-the-other-way check: the 2026-09-08 landing (`10c44c8` — the rubric, the compaction alarm, the enumeration fix) has NO meta `CHANGELOG` entry that ACm could find** (searched for `rubric`, `compaction alarm`, `compaction-alarm`, `enumeration`; the control term `writing protocol` does hit). **Confirm during Stretch B — it may be worded differently — and write it with the landing merge's entry.**
- **2 deploy log · 4 team current-state · 5a–d · 6 identity files · 7 Hub · 8 cross-runtime safety · 16 operator guide · 17 database** — no change; nothing touched a runtime, the product, identity files or company truth, and **no safety hole was closed.**
- **3 · `current-state.md`** — not edited. ㉑ is still stale on `main` and rides the landing merge; ACT-23's bulletin about ACm's holes is there; the "manuals have unmerged edits" warning stays a recommendation to land instead (§3).
- **5e · learnings** — three candidates in §11, **held for Justin's wording**, not filed.
- **9 · decisions** — none architectural. **9b · rulings** — the 2026-09-18 assignment (recover the stretches) lives in the recovery brief, HELD with OWNER ACm; the ballot rulings' durable home is the edits they authorize, still to be made; every ruling is quoted in §4.
- **10 · session docs** — **this file:** HELD, banner at the top. **The superseded brief:** `SUPERSEDED BY` banner, left in place — step 10 asks for Justin's OK before a move (㉕). **ACT-23's recovery brief:** HELD, OWNER ACm, condition not yet met, one day old. **ACT-A's notes:** ACT's doc; untouched. No doc was persisted from a subagent's output this session.
- **13 Linear · 14 Bitwarden** (no `.env` change) · **15 mailbox** (no `dc-to-ac.md` in `_evryn-meta`) — nothing. **18 auto-memory** — the stub only; its stale pointer is ⑯ item 4. **19 `settings.local.json`** — absent from both checkouts. **Worktree** — `_evryn-meta-acm` stays; the lane is live.
- **22/23 · commit and push** — **both packout commits are SESSION DOCS, committed under the standing exception. The PUSH waits for Justin's word:** `ac.md` makes push absolute, and ACm already pushed once without it (§4).

**Step 2 — fragment sweep.** Writing this file surfaced three things that had lived only in the old brief's ballot texts — **⑲'s substance, ㉒'s two offers, ㉑'s still-open status** — all now in §6. **Tonight's conversation added:** the context hole and its measurements (§2) · the 2026-09-08 landing, which contradicts the old headline (§1) · the missed `CHANGELOG` entry (above) · `Monitor`'s 30-minute cap returning in the same session after a reopen (§11) · that ACT-23's "Ballot 21" is ACT's own numbering (§6). **Report #23's new insights** — the extraction answer, the correction about `main`'s history, the collision, ⑫.1, ㉔, and ⑬–⑱ in full — are all in §1, §3, §5 and §11.

**Step 3 — subagent close-out.** **No subagent was spawned on 2026-09-17 or 2026-09-18.** The three reviewers of 2026-09-11 are ledgered in the superseded brief's §12 — no artifacts, no children, nothing owed.

**Step 4 — both briefs, top to bottom.** **Incoming (the superseded brief), from disk:** every section read by eye except its §9, which was checked by a **whole-block byte comparison** against this file's §10 — a stronger instrument than a visual pass for "did it carry intact" — with exactly one difference, the intro line deliberately re-pointed. **The comparison found one more drop** (the trigger to re-check the old-manual claim before the next spin), fixed in §11. **Outgoing (this file), in full:** one wording defect, fixed in §9.

**Step 5 — a further fresh-eyes pass of this file alone,** every section read by eye — §10 included this time — asking only *"could a brand-new instance understand this fully?"* **Three fixes:** a singular "commit" in §3 · this line, which had promised a result "below" · **"ACT-A" appeared in §2 with no description** (`main`'s new `ac.md` rule: describe a name the first time it appears).

**Context at the end: ~822K (82%), measured** — hence ㉔'s recommendation to start the next session fresh.

**After the packout, on Justin's instruction (same evening):** (1) **the context-hole recovery was done by this instance** — all three stretches read in full, reconciled against both briefs, written into §2, with the corrections applied in BOTH briefs; (2) **a final top-to-bottom scan of the superseded brief** confirmed everything live had been captured here; (3) it was **archived** under its own date, filename unchanged (㉕ a); (4) **commit and push at the very end, on his word** — *"Commit and push at the very end."* (5) **ACT-23's recovery brief retired** on his word, archived on `main` as `bf26450`; then a last fresh-eyes pass of this file.

**Step 6 — the old brief:** `SUPERSEDED BY` added and its hold banner removed; it stays in `docs/sessions/` until Justin answers ㉕.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
