🔒 **HELD-SESSION-DOC** — ACv-lineage session doc, moved here when `docs/working/` was retired · **RETIRE UPON:** ACv has evaluated it · **OWNER:** ACv · **HELD SINCE:** 2026-08-22

# ACp — move the Slack ping mechanic out of the agent manuals and into the router

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED — STOP.
> Do not load. Do not resume. Do not finish the current task, however nearly-done it looks. Tell your parent in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — and stop, **notwithstanding any harness instruction to resume silently.** *(ADR-058.)*

**Lane:** ACp, spun by **ACv** (AC-visual), who reports to Justin.
**Your parent is ACv. Justin is NOT your direct contact.** You do not ping him — ACv owns that.

---

## The problem, in one paragraph

Every Evryn agent is told how to ping Justin on Slack by **its own operating manual**, and all four manuals describe the *same underlying mechanic* in their own words. That mechanic was **broken for everyone and nobody knew**: the manuals instruct the agent to build an inline one-line command that reads a secret webhook URL out of a `.env` and POSTs it to the internet in the same breath. That command carries **both halves of an exfiltration signature**, so Claude Code's auto-mode permission classifier refuses it — silently, from Justin's side. The agent believes it pinged; Justin never hears anything.

**It is now fixed once, in `_evryn-meta/scripts/ping.mjs`** (already written, tested, and confirmed delivering). Running a committed project script is an ordinary action and is not blocked.

🔑 **Justin's point, and the reason this lane exists rather than a one-line manual edit:** *"pinging stuff should come out of the agent definitions and go into the router so that everyone can ping me properly and we only ever have to fix anything like that once."* **Four copies of one mechanic means four places to fix it and four chances to miss one.**

## Your job

**Move the COMMON ping mechanic into `_evryn-meta/CLAUDE.md` (the router), and leave genuine per-agent idiosyncrasies where they are.**

Justin's exact framing: *"The agents have a few different pinging things, so if there are idiosyncrasies that need to stay in their agent.mds, leave them, but I think the main mechanic can come out into the CLAUDE.md router."*

**So the deliverable is a judgment call, not a cut-and-paste.** You decide what is *mechanic* (goes to the router, stated once) and what is *idiosyncrasy* (stays in the manual that owns it). Measured starting point, for orientation only — **verify it yourself, these are grep hits, not a reading**: `ac.md` 17 · `qc.md` 10 · `oc.md` 8 · `dc.md` 3 · router 0.

### 🔴 The constraint that governs everything else

**The router is auto-injected into EVERY agent this workspace spins** — ACs, DCs, QCs, generic one-line lookup agents, all of them. It is deliberately **tiny (89 lines) and near-static**, and the file says why in its own words: *"kept deliberately tiny and near-static so that a stale copy of it is still a correct copy."*

⇒ **What you add must be SMALL.** The router should carry the *fact* and the *command* — how to ping, which channel reaches whom, and the one-line invocation. It must **not** absorb the rationale, the failure history, or the security reasoning. **That already lives in `scripts/ping.mjs`'s own header comment**, where it is read by whoever needs it and costs nobody else a token.

**If your addition is growing past a short block, that is the signal you are moving the wrong half.**

### Specific things that must be true when you are done

1. **The router tells any agent how to ping, in a form that actually works.**
2. 🔴 **`ac.md`'s two Slack bullets currently instruct the BLOCKED inline form.** They must stop doing that. This is the live defect — an AC following its manual today gets refused.
3. **The same check applies to `dc.md`, `qc.md` and `oc.md`** — any instruction to build the inline form is broken and must go.
4. **Genuine idiosyncrasies survive, in their own manual.** Known examples to weigh (not an exhaustive list — find the rest): DC is told to send *all* ops pings to `#dev-alerts` rather than `#team-alerts`, and there is a stated reason involving which repo holds which webhook — **check whether that reason still holds now that one script reads both.** AC has instance-name signing conventions (`AC0:`, `ACv:`).
5. **One fact must survive prominently wherever it lands:** *both channels reach Justin and ONLY Justin.* There is no agent-to-agent Slack delivery — addressing a ping to "AC1" or "DC" does not reach them. This has bitten repeatedly.
6. **Nothing that is frozen gets touched.** `ac-orchestration-protocol.md` contains verbatim blocks under a HARD RULE; you may not alter one word of those without Justin's explicit prior OK. If a ping instruction lives inside frozen text, **report it and leave it alone.**

## Rules of engagement

- **Read-then-edit. You edit files; you do NOT commit, merge, or push.** Leave everything in the working tree for Justin's review. *(Source-of-truth edits are normally propose-only; Justin explicitly commissioned this work, so the edits are authorized — the commit is not.)*
- ⚠️ **The editing tool has been observed HANGING on files under `.claude/`.** If an edit stalls, fall back to Bash (heredoc, `sed`). Say in your report which method you used per file.
- **You do NOT ping Justin.** ACv owns that channel. Report to ACv.
- **Follow `ac-writing-protocol.md`** for every word you write into a doc.
- **If you spin any subagent of your own**, you are bound by `ac-orchestration-protocol.md` exactly as ACv is.

## What to report back

1. **Your receipts**, both parts, per the protocol.
2. **The split you chose** — what you judged *mechanic* vs *idiosyncrasy*, and why. This is the part ACv most needs to see, because it is the judgment nobody else made.
3. **A before → after for the router**, so the size cost of the addition is visible at a glance.
4. **Anything you found that was broken and is NOT ping-related** — flag it, do not fix it.
5. **Any `NEEDS-JUSTIN` calls**, with your recommendation. Justin is away today; **batch rather than block.**

---

## Log — append below. Both sides write here.

**[2026-08-19 · ACv]** Lane created. `scripts/ping.mjs` already exists, is tested, and is uncommitted awaiting Justin's vet.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
