🔒 **HELD-SESSION-DOC** — the live marching orders for the ACP-lineage lane retiring `docs/working/`; a fresh instance spins from this file · **RETIRE UPON:** `docs/working/` no longer exists (`git ls-files docs/working/` returns nothing) · **OWNER:** ACP · **HELD SINCE:** 2026-09-02

# `AC0-37h3a3` — finish retiring `docs/working/`. THREE files left. Written 2026-09-02 by `AC0-37h3a2`.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. **STOP.**
>
> **Do not load. Do not resume. Do not finish the current task**, however nearly-done it looks. **Ping Justin in one line — *"I compacted. I am stopping."* — and do NOTHING else.**
>
> **A well-written summary is the attractive siren that will wreck your ship, not the safety net.** The danger is not that it is bad — it is that it is GOOD, so it draws you onward. *(ADR-058.)*

> ### 📌 THIS DOC SUPERSEDES `docs/sessions/historical/2026.08/2026.08.22-ac037h3-ac037h3a2-handoff.md`.
>
> **That file was correct when written and then accreted a day of corrections pointing at its own other sections — which is the exact trigger `lock-protocol.md` step 10 now names for SUPERSEDE rather than another correction.** **Everything still true is carried here, in one layer.** ⚠️ **Go to the archived one only if you need the forensics of how a disposition was reached; its per-file verdicts are reproduced below and are current.**

---

# §0 — WHAT THIS IS, IN PLAIN TERMS

**The job:** `_evryn-meta` had two folders for the same thing — `docs/working/` and `docs/sessions/` — both holding notes from past agent sessions. **Justin's design (2026-08-19): merge them into one.**

🔑 **The load-bearing half was never the merge. It is the RULE that came with it: nobody ever fixes a link to a session doc again.** Findability becomes mechanical instead — every such doc is in **`docs/sessions/`** if live, or **`docs/sessions/historical/YYYY.MM/`** if retired, **filed by the date that starts its own filename**. Previous instances burned whole sessions sweeping for stale paths; that is what this abolishes.

⚠️ **The rule does NOT extend to source-of-truth docs** — protocols, manuals, ADRs, `ARCHITECTURE.md`, the Hub, sprint docs. **Those still get their references fixed when a file moves**, because they are cited as authority rather than as history.

**Where it stands: 58 → 3.** *(For four consecutive instances the honest line was "not one file has moved." That ended 2026-09-01.)* ⭐ **Only ONE of the three is yours, and it is already verdicted retirable. The other two belong to the product lane.**

> ## 🔑 THE ONE THING TO UNDERSTAND BEFORE YOU TOUCH ANYTHING — this is a DISCOVERY pass, not a filing pass
>
> **A scan of the last sixteen files found NINE carrying an explicit *"still owed"* or *"routed to"* section, and several were the SOLE HOME of live work.** **Found on 2026-09-01 alone:** a **prompt-injection security item** whose sprint entry pointed *back at the doc*; a **silent mail-loss defect** no sprint Step named; and the **only copy** of the `classify.ts` seam decomposition plus Justin's ruling that a canary sweep must ride that extraction.
>
> ⇒ **A wrong retirement does not cost you a link. It can cost the content.** **Budget accordingly — and never retire a file on its own account of itself.**

---

# §1 — YOUR LOAD. Load these now, in this order.

**Widest lens inward, intended shape before current state.** *(`ac.md`, Context Discipline — a standing rule, not this list's preference.)*

| # | File | Lines | Why |
|---|---|---:|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | **875** | Your manual. **Nothing else makes you AC.** |
| 2 | `_evryn-meta/docs/hub/roadmap.md` | **186** | The Hub. Company truth. |
| 3 | `_evryn-meta/docs/current-state.md` | **202** | The snapshot, including its 📮 BULLETIN. |
| 4 | **THIS FILE** | — | Re-read §2 and §3 after `ac.md` lands; they mean different things once you hold the manual. |
| 5 | `_evryn-meta/docs/protocols/ac-writing-protocol.md` | **118** | Mandatory before writing to any doc. |
| 6 | `_evryn-meta/docs/protocols/lock-protocol.md` | **159** | ⚠️ **CHANGED 2026-09-01** — step 10 now has THREE dispositions, not two. |
| 7 | `_evryn-meta/docs/protocols/doc-update-routing-protocol.md` | **119** | The routing lookup. |
| 8 | `_evryn-meta/docs/protocols/mailbox-protocol.md` | **214** | You will use the channel constantly. |

**~1,875 lines. Confirm every bottom canary.** ⚠️ **A line count here is a HYPOTHESIS** *(command: `wc -l <path>` from `c:/Users/jbmcg/Evryn/Code`)* — **trust your own read and flag any delta.**

### 🔴 PULL AT A TRIGGER — do NOT carry these from the start

| The moment you… | Load, in full, BEFORE deciding |
|---|---|
| **Are about to judge anything that touches PRODUCT** | 🔴 **DO NOT. Ask the product lane** — §4. **Justin's stipulation, not a preference.** |
| **Consider spinning ANY subagent** | `docs/protocols/ac-orchestration-protocol.md` (**766**) **+ the sub-doc for your target.** **MANDATORY — the core alone does not authorise a spin.** |
| **Are about to write into a mailbox or discharge an entry** | `mailbox-protocol.md`, at the moment of use |
| **Need to know whether something SHIPPED** | `_evryn-meta/docs/deploy-log.md` **plus** that Step's own status — **the authoritative pair.** A *"rides the next deploy"* note is intent, never proof |
| **Need to know if an owed item is tracked** | `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` — its 🔵 LIVE region |

⛔ **Do NOT load speculatively:** `technical-vision.md` · `ARCHITECTURE.md` · the Code Atlas · the dependency map · the BUILD docs. **Those are the PRODUCT LANE's load, deliberately — the split only works if you do not duplicate it.**

### ✅ Session-startup hygiene — done 2026-09-01, but RE-RUN the repo check

✅ **`.claude/settings.local.json`: absent.** ✅ **Auto-memory: clean.** ✅ **All repos on canonical branches and clean at 2026-09-02 set-down; everything pushed EXCEPT this session's final commits — see §7.** ⚠️ **RE-RUN the repo-sync check yourself** — it is cheap and it is the one that fails silently. 🔴 **`_evryn-meta-acf15` and the four `evryn-team-runtime-acf*` worktrees are ACT's. Do NOT reap them.** *(Checked: none holds a commit that is on no remote, so nothing of theirs is at risk.)*

---

# §1.5 — 🔴 DO THESE TWO FIRST. Both are corrections to claims THIS LANE made and could not fully verify.

**① VERIFY THAT TWO "PARKED" QUESTIONS WERE ACTUALLY ANSWERED — this lane asserted it and did not check.**
> **Justin's instruction, 2026-09-02: *"I need you to make 100% sure that's true. We intentionally parked those — did they actually get done though?"***
> **The two, both originally parked in `docs/mailboxes/inbox-acp.md`:** **(a)** where the **identity-loading research doc** should live *(`docs/sessions/historical/2026.07/2026.07.13-acu-ac0-step53-identity-loading-research.md` — it went to `historical/` when Justin had ruled it be RELOCATED to the product research folder WITH breadcrumbs)*, plus **where it gets breadcrumbed and whether Soren or Mira gets a memory note**; and **(b)** whether **ALL** of the loading-architecture brief shipped or **only Lane A**.
> ⚠️ **WHAT THIS LANE ACTUALLY KNOWS, stated exactly: the product lane SAID on 2026-09-01 that it had *"TAKEN both,"* and this lane then wrote into `inbox-acp.md` that they were closed.** 🔴 **"Taken" is not "done." Nobody has confirmed either was ANSWERED, and this lane had no room left to check.** ⇒ **Verify at source. If either is still open, it is genuinely open and was nearly lost.** 📌 **(b) is the dangerous one — it is a question about what is UNSHIPPED, so a wrong *"nothing remains"* retires a doc that still holds live work.**

**② CORRECT A CLAIM THIS LANE PROPAGATED ABOUT PUSHING.**
> 🔴 **The `ACh` housekeeping queue said flatly that *"`_evryn-meta`'s push IS the Vercel dashboard deploy."* Justin corrected it on 2026-09-02: that is true ONLY when the push carries COMMITTED CHANGES UNDER `dashboard/`.** ⇒ **A docs-only push to this repo deploys nothing and is an ordinary push.**
> ⚠️ **This lane repeated the unqualified version to Justin twice and wrote it into `inbox-ac037h3a.md`** *(now corrected there)*. **The originating claim sits in `docs/sessions/historical/2026.08/2026.08.04-ach-justin-housekeeping-recommendations.md` — a frozen record, so leave it.** ⇒ **Check whether the unqualified version has spread anywhere still LIVE, and correct it there.**

---

# §2 — THE THREE FILES

> # 🔴 READ THIS FIRST: ONLY ONE OF THE THREE IS YOURS, AND IT IS ALREADY VERDICTED.
>
> **The other two belong to the product lane and are waiting on a decision only it can make.** ⇒ **Your first move is not analysis. It is `git status` in `evryn-backend` and `_evryn-meta`, and a look at what the product lane has landed since 2026-09-02.**
>
> ## 🔑 THE RULE THAT GOVERNS EVERY MOVE BELOW — *A REHOME IS NOT A REHOME UNTIL IT IS COMMITTED*
> **An uncommitted destination is a PROMISE, not a HOME.** **Archive a source doc against one and a declined vet, a reset, or another lane's sweep leaves the content existing NOWHERE — with both halves reading as done.**
> ⚠️ **The asymmetry settles it: a file held a day too long costs a `git mv`. A file archived against a vanished destination costs the content, silently.** ⇒ **OVER-HOLD.**
> ⭐ **This paid for itself inside one afternoon on 2026-09-01: a file read as cleared at 16:48, was held, and released at 17:14 when its destination committed.**

> ## ✅ FIVE FILES RELEASED AND MOVED ON 2026-09-02 — recorded so you do not go looking for them
> **All five were held on product-lane edits that were uncommitted at 2026-09-01 set-down. Both commits landed overnight** — **`evryn-backend` `7426532`** *(homes Soren's batch by pointer; lifts `SPRINT` Step 50's runtime seams)* **and `evryn-team-workspace` `b674f0c`** *(the research doc itself)*. ✅ **Verified in `HEAD`, not from a report, then moved:** the 2026-07-22 handoff *(Soren's batch)*, the three `ACh` P-queue files, and the 2026-07-21 cleanup-batch brief.
> 🔑 **The lesson worth more than the files: this brief was WRITTEN saying those five were blocked, and the fresh-eyes pass caught it before handoff.** ⚠️ **A blocker recorded yesterday is a claim about yesterday.** ⇒ **Re-check every "waiting on" line in this document before you believe it — including the three below.**
> 📌 **Two things from those five that no longer live anywhere you will look, kept here because they cost real time to establish:** **the `ACh` P-queue is FULLY discharged** *(all eleven items — P1–P4, P6–P11; there is no P5)*, and **`2026.06.30-…-identity-ops-brief.md:185`'s "⛔ HELD BY JUSTIN" on Step 78 Half B was STALE by 42 days** — verified lifted 2026-07-20 by five independent git checks. **Neither is a live blocker; both look like one.**
> ⚠️ **AND THE ONE STANDING PROHIBITION FROM THAT SET: NEVER sweep Soren's v0.3 outreach batch into a build doc.** **His own text forbids it verbatim; a previous agent's Justin-approved recommendation to do it was correctly refused, and a second agent nearly repeated it on 2026-09-01. Two people have walked into this.**

## 🅰 Owned by the PRODUCT LANE, not you — 2 files

**① `2026.08.04-ac0-aca-atlas-refactor-brief.md`** — 🔴 **A LIVE SPEC. DO NOT RETIRE IT.** **Three of its four deliverables shipped; the `classify.ts` extraction did not, and five open sprint Steps are sequenced against it.** **It is the ONLY copy of the five-module seam decomposition AND of Justin's ruling that the top-of-file canary sweep must RIDE that extraction** *(inserting one line atop every source file shifts 1,096 `file:line` references)*. ⇒ **It is a REAL doc misfiled as a session doc and needs a real home. The product lane owns finding it.**

**② `2026.08.12-qc-sweep-findings.md`** — the product lane pulled this back: its §4 is what `SPRINT` Step 116(b) points at for reconciling the Atlas against a mutation sweep. 📌 **One thing to carry into whoever reads it: its owed-item 1 records a receipt-audit gap as PERMANENT, and names a cheap never-run mitigation — check each sweep lane's self-reported receipts against the Atlas partition's own file list, which `atlas/00-INDEX.md` states exactly.**

## 🅱 YOURS — 1 file, already verdicted, moves with the closing sequence

**③ `2026.08.11-ac0-ac0-handoff.md` (1,346 lines)** — ✅ **RESIDUE-PASSED AND VERDICTED RETIRABLE on 2026-09-01. Nothing in it is the sole home of anything.**
> ⛔ **One live hazard was neutralised in place and you should know it exists: its §10.3 is headed *"paste this verbatim; do not paraphrase it"* and prescribes STOP → RELOAD → resume on compaction — the instruction Justin REVERSED on 2026-08-18.** **It is now marked SUPERSEDED in place so a reader meets the reversal instead of re-installing it. Do not "clean that up."**

---

# §3 — HOW YOU OPERATE. Justin's rulings.

**3.1 · COMMITS.** 🔴 **A SOURCE-OF-TRUTH DOC IS NEVER COMMITTED WITHOUT JUSTIN'S SCM VET FIRST.** *(His ruling, 2026-09-02.)* **Make the edit, LEAVE IT UNCOMMITTED, and tell him in one line that it is sitting there.** ⭐ **Reviewing prose in source control is easier for him than reading an excerpt in chat, because the diff shows the change in its real surroundings instead of one he has to mentally place back into a document he does not have open.** ⚠️ **This is not a new rule — `ac.md` has said it all along** *(Working With Justin → the commit-discipline bullets; Documentation Approach → "make the edit and leave it uncommitted")*. **Read it there. This line is a pointer, not a second home.**

> ⚠️ **THE SUPERSEDED PREDECESSOR SAYS THE OPPOSITE** — that source-of-truth docs may be committed and vetted after the fact, provided you list the paths at set-down. *(`docs/sessions/historical/2026.08/2026.08.22-ac037h3-ac037h3a2-handoff.md`, and this file's own earlier version.)* **If you meet it, this line governs.**
>
> 🔑 **Why it is worth naming the class rather than just deleting the sentence: this is `lock-protocol.md` step 9b's exact failure mode.** **An in-the-moment instruction was persisted as though it were standing, and then generalised past what Justin meant.** **Step 9b exists to stop precisely that, and its own worked example is the same shape** — *"he does NOT review merges"* recorded as standing, when what he meant was *"I can't review CODE — I'm not an engineer."* ⇒ **AGREE THE WORDING WITH HIM BEFORE A RULING IS PERSISTED. Bring him the exact text and the destination, in a form he can answer with *agree* or *adjust*.**

✅ **Mailbox traffic stays PRE-AUTHORIZED and is genuinely standing** — `mailbox-protocol.md` §7. **Write, commit, walk away; an uncommitted message is not a slow message, it is not a message at all.**

⚠️ **SESSION AND LANE DOCS — CONFIRM THIS HALF, do not assume it.** Justin's 2026-09-02 ruling scoped only *source-of-truth* docs. **Your own handoff is your continuity and wants committing at checkpoints so a crash cannot take it** — but that is an inference, not something he has restated. **Ask, in one line, rather than inheriting it from this file. That is the whole lesson of the block above.**

**3.2 · MOVING FILES.** ✅ **Justin's authorization, 2026-09-01: *"when files are cleared for movement, move them and tell me."*** ⭐ **And he overruled the "move the live ones last" plan** — the reason to hold them was a window where `current-state`'s routing line would be wrong, and **moving plus repointing in ONE commit means no window exists.**

**3.3 · 🔴 TALKING TO JUSTIN — and this is where THIS instance failed hardest, repeatedly.** **He is not being difficult; he is coming in cold from other intense meetings, hours later.**
> 🔴 **NEVER hand him an internal label.** *"P4"* · *"the six"* · *"ballot 8"* · *"⓸ Moving — done, above"* — **all meaningless to him, and he told this lane so four separate times.** His words: ***"a lot of stuff that makes absolutely zero sense outside of your head."***
> ⇒ **Every noun gets its instance attached. Every reference re-states what it refers to.** ⭐ **NUMBER YOUR REPORTS** — *"Report #3"* — he asked for it explicitly; it is how he tracks a long day.
> ⚠️ **The rules for this are already in `ac.md` and are extensive. This lane read them and still failed.** ⇒ **Treat it as a rule you are CURRENTLY FAILING, not one you know.** **Before sending: reread as if you had never seen this project.**

**3.4 · Prefer `Read` / `Write` / `Edit` over Bash for file work.** *(His instruction, restated 2026-09-02.)* **He can SEE those in his IDE; Bash file work is invisible to him.** **Bash is for commands — `git`, `npm`, a script.**

**3.5 · EDITING SOMEONE ELSE'S DOC — `A → (true A)`.** *"You can't rewrite, exactly — you can just update so it's actually true, so that it now says what it was already meaning to say."* ⛔ **Never re-scope, sharpen, or improve the claim.** ⚠️ **If the TRUE statement differs from the INTENDED one, that is a ROUTE, not an edit.**

**3.6 · 🔴 OTHER LINEAGES' DOCS ARE NEVER YOURS TO RETIRE.** **The top-level lineages are `ACP` (yours) and `ACT`.** **Banner it, message that owner's inbox, move on.** **You cannot see their worktrees or unspun briefs.**

**3.7 · Peer etiquette:** **don't bug each other · other people's emergencies are not automatically yours** ⇒ **make any request CHEAP TO ANSWER or it will correctly be declined.**

---

# §4 — THE PRODUCT LANE. Your peer, and the reason this works.

**A SEPARATE INSTANCE, not a subagent.** **It holds the full product runtime (~350k of load); you deliberately hold none of it.**

- **YOUR ADDRESS: `docs/mailboxes/inbox-ac037h3a.md`.** **THEIRS: `docs/mailboxes/inbox-ac037h3b.md`.** 🔴 **COMMITTING IS SENDING.** **Both are role addresses — a successor inherits them. Do NOT create a per-instance mailbox.**
- **Arm a watcher on yours at spin-up** *(ask Justin first — `mailbox-protocol.md` §6)*. **Both lanes' watchers were live and delivering inside ~60 seconds on 2026-09-01.**
- 🔑 **IT IS CONSULTING, NOT BEING TASKED.** ⭐ **If it says your framing is wrong, it is probably right — it corrected this lane five times in one day**, including two claims this lane had already reported to Justin as fact.
- 🔴 **SEND IT SOURCES, NOT SUMMARIES.** *(Justin's catch: **"if he's going to re-home, he needs high-res."**)* **This lane condensed a six-item batch into its own words; the product lane then drafted the exact wrong move — and caught it only after being sent exact line spans.** ⇒ **Give paths and line ranges. Name where your own summary is lossy.**
- ⚠️ **Do NOT sweep its uncommitted work.** **`git commit -F - -- <paths>` — a pathspec on the COMMIT, not just the `add`.** 🔴 **AND THE HARDER CASE: if you BOTH have edits in the SAME file, a by-path commit still takes that file's whole working-tree state. This happened on 2026-09-01. Diff before you commit a shared file.**

---

# §5 — TRAPS. Every one cost real time.

**5.1 · 🔴 A CLOSED BRIEF LIES ABOUT ITSELF IN THREE DIFFERENT DIRECTIONS — all three fired on 2026-09-01.**
- **It hides routed-but-UNTRACKED work** — the closure reads as completion.
- **It hides ALREADY-FIXED work** — the finding reads as still-open. *(One item was fixed the day after it was written and reached Justin as live from two independent lanes.)*
- **It MIS-DESCRIBES ITS OWN SHIPPED ARTIFACT.** *(A brief said its reasoning "lives only here"; the reasoning was in the code, stated better.)*
⇒ ***CHECK THE ARTIFACT, NOT THE BRIEF'S ACCOUNT OF IT.*** **An owed item's age is not evidence it is still open. Re-checking costs one `git log`.**

**5.2 · 🔴 SEARCHES FAIL SILENTLY HERE, AND THE FAILURE LOOKS LIKE A CLEAN RESULT.** **Bash `grep` ABORTS on some reads · `grep -P` is broken (use `awk`) · a filesystem-wide scan double-counts because a worktree is a full checkout · and `find` has returned EMPTY for files that demonstrably exist.**
⇒ 🔑 **ALWAYS PAIR AN ABSENCE CHECK WITH A KNOWN-TRUE CONTROL IN THE SAME RUN.** ⭐ **On 2026-09-01 that control stopped this lane reporting a research document as LOST — the `find` was broken, not the filesystem.** **`ac.md`'s Context Discipline now carries all six machine gotchas.**

**5.3 · A LINE-NUMBER CITATION INTO A LIVING DOCUMENT ROTS WITHOUT ANYONE TOUCHING IT** — and when it stops resolving, **everything built on it becomes UNVERIFIABLE rather than false.** ⇒ **Read a pre-restructure citation as a lead; cite by quoted anchor when you write one.**

**5.4 · `SPRINT-V0.2-HARDENING.md` writes steps as `53.`, not `Step 53`** — so `grep "Step 53"` returns nothing for a Step plainly there. **Use `^53\.`.**

**5.5 · `evryn-backend`'s `npm run typecheck` CANNOT PASS** — `marked` declared, absent from `node_modules` (**SPRINT Step 118**). **It predates you and fails in the direction that looks like your own change's fault.**

**5.6 · A rule that names a MECHANISM decays when someone applies the mechanism without the PURPOSE — and it decays in the direction that looks compliant.** *(Five separate instances of this shape in one day: a stale finding read as live; a sign-off read as standing state; a citation read as resolving; an item's age read as evidence; a delegation rule nearly applied to a fifty-line read.)*

---

# §6 — THE CLOSING STEPS, IN THIS ORDER

**1. Clear the remaining files.**

**2. 🔴 THEN, IN ONE COMMIT: the announcement into `current-state.md` + delete `docs/working/` + the changelog entry.** **The announcement CANNOT land early** — announcing a retirement that has not happened puts a lie in the file every agent loads first. ⚠️ **Fix any remaining `docs/working/` path in `current-state.md` in the same commit.**

> ### 📋 THE APPROVED ANNOUNCEMENT — Justin approved this text. Paste it at the top of `docs/current-state.md`.
>
> ```
> > # 📁 `docs/working/` IS RETIRED. EVERYTHING IS IN `docs/sessions/` NOW.
> >
> > **One folder, not two.** Working docs and session docs were always the same thing, so they now live in one place.
> >
> > ### 🔎 CAN'T FIND A DOC? You do not need to search, and nobody needs to fix the link.
> >
> > **Every working or session doc ever written is in exactly one of two places:**
> > - **`docs/sessions/`** — if it is still live.
> > - **`docs/sessions/historical/YYYY.MM/`** — if it is retired, **filed by the doc's own date**, which is the first thing in its filename.
> >
> > **So a path like `docs/working/2026.07.15-ac3-step78-real-health-design.md` resolves in one step: it is a 2026-07 doc, so look in `docs/sessions/historical/2026.07/`.**
> >
> > ### 🔴 THE RULE THAT MATTERS MOST: DO NOT FIX REFERENCES TO SESSION DOCS.
> >
> > **A stale path to a session doc is not a bug, and NOBODY GOES LOOKING FOR THEM.** The date in the filename plus the two locations above make it findable in one step, forever. **Sweeping for them is make-work, and the make-work was bad enough to have consumed whole sessions.**
> >
> > ⭐ **But if you happen to be in a doc and see a session path that has moved, fix it.** **What is abolished is the OBLIGATION TO SWEEP — not the permission to fix what is already under your nose.**
> >
> > ⚠️ **This does NOT extend to source-of-truth docs** — protocols, manuals, ADRs, ARCHITECTURE, the Hub, sprint docs. **Those still get their references fixed when they move**, because they are cited as authority rather than as history.
> ```

> ### 🔴 BEFORE THE ANNOUNCEMENT LANDS: ITS CENTRAL PROMISE IS CURRENTLY FALSE FOR TWELVE FILES, AND THE CAUSE IS A LIVE CONVENTION, NOT A LEGACY MESS
>
> *(Found 2026-09-02 by `AC0-37h3a3`, by testing the rule against all 194 archived files rather than trusting the announcement's own worked example — which resolves correctly, and is exactly why nobody caught this.)*
>
> **The announcement promises one-step findability: a doc is in `docs/sessions/historical/YYYY.MM/` keyed to the date at the front of its own filename.** 🔴 **Twelve files break it — 8 dated `2026.06` sitting in `2026.07/`, 3 dated `2026.07` in `2026.08/`, 1 dated `2026.08` in `2026.09/`.**
>
> ⚠️ **THE CAUSE, ESTABLISHED FROM GIT RATHER THAN INFERRED — and it is why fixing the twelve is not enough on its own:**
> - `1c6693d` (2026-07-09): *"archive 16 outlived working docs **-> sessions/historical/2026.07**"* — filed 2026-06 docs by the month of the archiving.
> - `6eb35bc` (2026-08-06): same shape, a 2026-07 doc into `2026.08/`.
> - `19b6181` (**2026-09-02, the product lane, TODAY**): a 2026-08-22 doc into `2026.09/`.
>
> 🔑 **Three different lanes, three different months, each independently filing by RETIREMENT date instead of AUTHORSHIP date — because that is the natural reading of "archive this."** ⇒ **This is an active convention that will keep producing mismatches. Moving twelve files without making the rule explicit at the moment of filing just resets the counter.**
>
> ⇒ **THE FIX HAS TWO HALVES AND THE SECOND IS THE LOAD-BEARING ONE:** (1) move the twelve; (2) **state the rule where an agent is standing when it decides to archive something — `lock-protocol.md` step 10 — not only in the announcement, which is read by people looking for a file rather than filing one.**
>
> ⚠️ **Two files carry NO date prefix and the rule cannot reach them at all:** `historical/2026.08/ac-mailbox.md` and `historical/2026.08/refmap-raw.txt`. **The six files under `historical/2026.03/2026.03.23-weekend-thinking/` are FINE — their parent folder carries the date.**
>
> 🔑 **Why this is worth the space: a failed lookup under a rule that says *"you do not need to search"* is worse than having no rule.** **The agent does not conclude the rule is imperfect — it concludes the document is gone.**

> ### 🔧 PREPARED 2026-09-02 by `AC0-37h3a3` — every stale path in `current-state.md` is already resolved. Do not re-derive these.
>
> 🔑 **Why this is in scope at all, since the no-repointing rule seems to say otherwise: `current-state.md` is a SOURCE-OF-TRUTH doc, and the rule's own text excludes those explicitly.** *("This does NOT extend to source-of-truth docs — protocols, manuals, ADRs, ARCHITECTURE, the Hub, sprint docs. Those still get their references fixed when they move.")* ⇒ **Session docs are not swept. This file is.**
>
> **Seven live paths, each verified present at its new home with `git ls-files` and a known-true control in the same run:**
>
> | `current-state.md` says `docs/working/…` | It is actually at |
> |---|---|
> | `2026.08.11-acfm-acf-runtime-questions.md` | `docs/sessions/` |
> | `2026.08.11-acf-sealedexec-sweep-lane-brief.md` | `docs/sessions/` |
> | `2026.08.17-acf13-acf15-handoff.md` | `docs/sessions/` |
> | `2026.08.19-acf15-act16-handoff.md` | `docs/sessions/` |
> | `2026.07.13-acu-ac0-step47-maintenance-plan-scoping.md` | `docs/sessions/historical/2026.07/` |
> | `2026.08.17-ac037c-ac037d-handoff.md` | `docs/sessions/historical/2026.08/` |
> | `2026.08.19-ac037f-ac037g-handoff.md` | `docs/sessions/historical/2026.08/` |
>
> **THREE further mentions are generic rather than paths, and each needs different handling — do not blanket-replace:**
> - **The bullet at *"📁 `docs/working/` IS BEING RETIRED INTO `docs/sessions/`"*** — **this is the one the approved announcement REPLACES.** Do not leave both.
> - **The *Active instances* line, *"each carries a committed brief in `docs/working/`"*** — **a live instruction that becomes false.** Change the folder to `docs/sessions/`.
> - **The closing line, *"Earlier handoffs are in `docs/working/` and `docs/sessions/historical/`"*** — **change to name `docs/sessions/` and its `historical/` subfolder only.**
>
> ⛔ **LEAVE ALONE — these are HISTORY, not paths:** *"`docs/working/` swept 55 → 44"* and *"recovered by a `docs/working/` audit tonight."* **They describe what was true when the action happened; correcting them would falsify an accurate statement about the past.**

**3. One line into ACP-38's marching orders** (`docs/sessions/2026.08.17-ac037b-acp38-marching-orders.md`) **so he learns the folder is gone — as a settled sentence, never a task.**

> ### 📝 DRAFTED, ready to paste — one sentence, and deliberately no more
> ```
> 📁 **`docs/working/` no longer exists — every working and session doc is in `docs/sessions/`, or `docs/sessions/historical/YYYY.MM/` if retired, filed by the date that starts its own filename.** *(Completed 2026-09-02.)* **Session-doc references are not repointed when a doc moves, so a stale `docs/working/` path in anything you read resolves in one step by that rule. Nothing here is yours to do.**
> ```
> 🔴 **The last four words are load-bearing and must survive any edit.** *(Justin, 2026-08-19: **"I want 38 completely unencumbered by all of this clutter."**)* ⚠️ **That file already carries five stale `docs/working/` paths of its own. DO NOT FIX THEM** — it is a session doc, the no-repointing rule covers it, and touching them is exactly the sweep that was abolished.

**4. 🔴 PUT THESE TO JUSTIN AND GET A RULING — do NOT forward them into another handoff.** *(His instruction, 2026-09-02: **"make sure we knock these out at closing — don't want to keep forwarding them."**)* ⚠️ **Each has already survived multiple set-downs by being carried rather than resolved. Carrying is what this step exists to stop.**

| # | The question | Why it has not resolved itself |
|---|---|---|
| **a** | **Should a spun subagent default to FOREGROUND or BACKGROUND?** | **The current default rests on nothing.** It was chosen when background output was genuinely invisible to Justin; **as of 2026-08-21 background renders identically to foreground**, so the reason is gone and nobody has re-picked. |
| **b** | **Does the hold-banner aging window actually work?** | **Untested.** `lock-protocol.md` step 10 tells you to ask Justin when a held doc passes a week with no one advancing its condition. **Nobody has ever run it, so we do not know whether it fires or is ignored.** |
| **c** | **Is the archival `pg_dump` path going to be a committed script, a permission rule, or Justin's own hands?** | 🔴 **NEW, 2026-09-02, and it is a live block rather than a musing.** Justin authorized a fresh dump; **the permission classifier refuses any command that reads the connection string from `.env` and pipes it to `pg_dump`.** **The dump cannot be taken until this is decided.** |
| **d** | **Does Emma's cache-temperature / burn analysis become a NAMED go-live gate?** | **Filed in the bulletin 2026-08-18 and unanswered for 15 days.** Her conclusion was that *"the go-live decision for a gatekeeper is a burn decision, not just a build decision."* **It appears in no gate list — not `current-state.md`'s 🚦 section, not `gate-b-plan.md`, not the sprint.** ⚠️ **Runway on a cold cache was estimated at SIXTEEN DAYS.** |
| **e** | **Do SESSION and LANE docs still commit freely, or do they also wait for the SCM vet?** | **His 2026-09-02 ruling scoped only source-of-truth docs.** **Everything else is inference — see §3.1, which is where inferring got us into trouble in the first place.** |
| **f** | **`evryn-team-workspace/.claude/agents/soren.md:135` points at a `CLAUDE.md` section that no longer exists there.** It cites *"AC's cascade system (`_evryn-meta/CLAUDE.md` → SESSION STARTUP)"*; **that section moved to `.claude/agents/ac.md` in the 2026-08-18 split.** | 🔴 **NOT AC's to edit — the founding team's identity layer is JUSTIN's** *(`doc-update-routing-protocol.md`, Tier 3)*. **Told to him in chat 2026-09-02 and recorded here, because a chat mention is not a home.** **It fails quietly: Soren follows the pointer, finds a router with no such section, and guesses.** |

> ### 📮 ROUTED ELSEWHERE — tracked here only so a successor knows they exist and does NOT re-derive them
> **Neither is this lane's to fix, and both are genuinely sent rather than merely noticed:**
> - **`evryn-team-runtime/docs/research/README.md:31`** — same shape as (f): cites a `CLAUDE.md` section that moved. **Sent to `inbox-act.md` 2026-09-02.**
> - **`evryn-backend/docs/atlas/07-safety.md:636`** — cites `_evryn-meta/CLAUDE.md:244`, a **line-number** citation into the file the split restructured. ⚠️ **Everything resting on it is now UNVERIFIABLE rather than false; the fix is to RE-DERIVE, not to hunt for the "real" line.** **Sent to `inbox-ac037h3b.md` 2026-09-02.**
>
> 🔑 **All five of these came from one audit and share one cause, which is the durable lesson: the loading-architecture CUTOVER completed, and its REFERENCE TAIL did not.** ⇒ **When a load-bearing file changes what it IS — not just what it says — the sweep that matters is for documents that describe it, and those live in other repos where no one is looking.**

**5. 🔴 LAND JUSTIN'S REVISED PACKOUT PROCEDURE — routed to THIS lane by him on 2026-09-02** *(his words, relayed via the product lane: **"pass it to 3a, he has a lot more context room"**)*. ⚠️ **It currently exists only in a conversation and in two handoffs, which `lock-protocol.md` step 9b names explicitly as NOT a durable home — *"carried into the next handoff is not a home; it is the same countdown, restarted."***

> ### 📋 THE PROCEDURE, IN JUSTIN'S ORDER. His wording preserved wherever it carries the reasoning.
>
> 1. **Run a full `#lock` FIRST.** *"This will allow us to properly persist as we leave this session. But it'll also take some of the load off your handoff, because you'll persist the appropriate things to the current state and changelog docs."*
> 2. **THEN write the handoff** — an appendage *or* a fresh brief, **chosen by assessing staleness**. *"How stale is your brief? Do you need to just clean it up and add a new appendage, or is it mostly stale and the next instance would do well to have a clean new brief? Or is the 'stale' stuff good context, just needs cleaning so next-you has it without getting bogged down?"*
> 3. **THEN a careful fragment sweep of the conversation.**
> 4. **THEN a top-to-bottom fresh-eyes re-read of BOTH the incoming and outgoing briefs.** 🔴 *"You're going to be tempted to do it from memory, but that's the trap — **re-read** both."* ⚠️ **Special attention to the LOAD LIST: copy it verbatim from the incoming brief unless there is a compelling reason, and never change it without surfacing that loudly.**
> 5. **THEN one more fresh-eyes pass of the outgoing handoff alone**, hunting errors, omissions, staleness and clarity — *"could a brand-new instance with none of your current context understand this fully?"*
> 6. **THEN retire the old brief** — or state plainly why it genuinely must stay.
>
> ⭐ **STEP 1 HAS ALREADY VALIDATED ITSELF, and this is evidence rather than argument** *(the observation is `AC0-37h3b5`'s, and the attribution matters)*: **the `#lock` that ran before the product lane's 2026-09-01 handoff is exactly why that lane's brief was thin enough to load cleanly the next morning.** **The `CHANGELOG` and `current-state` entries carried what would otherwise have been handoff bulk.**
>
> ### 🎯 DESTINATION — a recommendation to put to Justin, NOT a decision to execute
>
> **`AC0-37h3b5` recommended `lock-protocol.md` over `ac.md`, and its reasoning is sound: the load-bearing claim is *"run the lock FIRST so the handoff carries less"* — a fact about what `#lock` is FOR, not a fact about handoffs.** **Filed under `ac.md`'s handoff section, that claim sits under the wrong heading.**
>
> ⚠️ **The refinement this lane adds: steps 4 and 5 are facts about HANDOFFS** — two fresh-eyes re-reads, and the verbatim-load-list rule — **and they would be orphaned inside a checkpoint protocol.**
>
> ⇒ **PROPOSAL: the procedure lands in `lock-protocol.md` as a new closing step, and `ac.md`'s handoff section gets a ONE-LINE POINTER to it.** **One home, one pointer — never two copies.** *(That is the elevation discipline `ac.md` already mandates; this is just applying it.)*
>
> 🔴 **DO NOT LAND ANY OF THIS UNVETTED.** **`lock-protocol.md` step 9b requires the wording be AGREED WITH JUSTIN BEFORE it is persisted, because a ruling generalised out of its moment can be wrong** — and this is squarely that class. ⇒ **Bring him the exact text and the destination, in a form he can answer with *agree* or *adjust*.**
>
> ### ✍️ THE EXACT TEXT, DRAFTED — this is what would be pasted, not a description of it
>
> **Placement: a NEW SECTION AFTER step 23, not a renumbering.** 🔑 **Deliberate — `lock-protocol.md` step 12 keeps a retired step's NUMBER precisely so references to later steps do not silently shift. Inserting a numbered step mid-list would break the thing that rule protects.**
>
> ```
> ## Packing out — what happens AFTER the lock, and in this order
>
> **The `#lock` above persists the SESSION. This section hands off the INSTANCE.** They are different jobs and the order between them is load-bearing.
>
> 🔴 **RUN THE FULL `#lock` FIRST — steps 1–23 — BEFORE you write a line of your handoff.** *(Justin's ruling, 2026-09-02.)*
> **Two things it buys, and the second is the one people miss:** the session is properly persisted before anything can go wrong, **and your handoff gets LIGHTER** — because what would otherwise have been handoff bulk is already sitting in `current-state.md` and the changelogs, where it belongs and where the next instance loads it anyway.
> ⭐ **Observed, not asserted: the lock that ran before the 2026-09-01 product-lane handoff is why that brief was thin enough to load cleanly the next morning.**
>
> **Then, in order:**
>
> 1. **Write the handoff — and CHOOSE ITS FORM by assessing staleness rather than defaulting.** *"How stale is your brief? Do you need to just clean it up and add a new appendage, or is it mostly stale and the next instance would do well to have a clean new brief? Or is the 'stale' stuff good context, just needs cleaning so next-you has it without getting bogged down?"* ⚠️ **Step 10's SUPERSEDE trigger decides this for you when it fires: a doc carrying a correction that points at its own other sections needs replacing, not appending.**
> 2. **A careful fragment sweep of the conversation** — the same discipline as step 20, run again now, because writing the handoff surfaces things the lock did not.
> 3. 🔴 **A top-to-bottom fresh-eyes re-read of BOTH briefs — the one you came in on AND the one you are writing.** *"You're going to be tempted to do it from memory, but that's the trap — **re-read** both."* ⚠️ **Pay special attention to the LOAD LIST: copy it VERBATIM from the incoming brief unless you have a compelling reason, and never change it without surfacing that loudly.**
> 4. **One more fresh-eyes pass of the outgoing handoff alone** — hunting errors, omissions, staleness and clarity. **The test is one question: *could a brand-new instance, holding none of your current context, understand this fully?***
> 5. **Retire the old brief** per step 10 — or state plainly why it must stay.
> ```
>
> ### ⚠️ ONE THING TO PUT TO JUSTIN WITH THE TEXT, because resolving it silently would be deciding rather than flagging
>
> **His step 3 is a fragment sweep. `#lock` step 20 is ALSO a fragment sweep.** ⇒ **Either this is a deliberate SECOND pass — which is defensible, since writing the handoff genuinely surfaces new fragments — or the two should be merged.** **The draft above assumes the first and says so.** 🔴 **He should confirm, because an instruction that looks duplicated is one an agent will quietly skip.**

✅ **A SIXTH was on this list and is now CLOSED, recorded so nobody re-opens it:** `conductor-cascade.md`'s trip-1 load list told a spun scout to load `CLAUDE.md`, **which the harness auto-injects.** **Removed 2026-09-02** — the router's own instruction is to *diff* the injected copy against disk, never to re-read it.

---

# §7 — OPEN, AND NOT YOURS TO ABSORB

- 🔴 **`.claude/agents/ac.md` IS UNCOMMITTED AND AWAITING JUSTIN'S VET — the ONE thing this lane deliberately left in SCM.** **It carries his delegation rule from 2026-09-01** *(fix it yourself if you have already read the file; delegate if reading it would cost you headroom — applied by PURPOSE, not as a threshold of zero)*. 🔑 **Why it was not committed: `ac.md` changes HOW AGENTS WORK, and §3.1's exception requires his SCM vet BEFORE the commit, not after.** ⇒ **Check whether he vetted it. If it is gone from `git status`, he took it; if it is still there, it still needs him. Do NOT commit it yourself.**
- ⚠️ **`current-state.md`'s 📮 BULLETIN was NOT drained at this `#lock`, deliberately and disclosed.** **Six entries, the newest 2026-08-21, all past the file's own 7-day bar.** **Several belong to other lineages.** **Draining it carefully was more than this instance's remaining context could do well, and doing it badly is worse than not doing it.** ⇒ **Do it early in your session, with room.**
- ⚠️ **The archival `pg_dump` is from 2026-07-10 — nearly EIGHT WEEKS against `#lock` step 17's one-week bar.** **Supabase Pro daily auto-backups are the primary layer and are unaffected; this is the archival copy.** **It runs against prod and wants a deliberate hand — not taken unasked.**
- **Open questions Justin has not ruled on:** the foreground-vs-background default rests on nothing *(background renders identically to foreground)* · the hold-banner aging window is untested · `conductor-cascade.md:51` still tells a spun scout to load `CLAUDE.md`, which auto-injects.

---

# §8 — HONEST LIMITS OF WHAT YOU ARE INHERITING

- 🔴 **NO PRODUCT DOC WAS EVER LOADED BY THIS LANE. Not `ARCHITECTURE.md`, not the Atlas, not one `src/` file.** ⇒ **Every product claim in this brief is a LEAD, verified by the product lane rather than by this lane. That is what §4 is for.**
- **Nothing is deployed. No `src/` file was touched by this lane.** **Its only `evryn-backend` changes were one clause in `docs/operator-guide.md` and that repo's `CHANGELOG.md` entry.**
- ✅ **Every disposition in §2 was verified at source before being recorded** — commits confirmed as ancestors of `main`, not taken from a report. ⚠️ **But they were verified at a moment. Re-check the commit state; that is the whole point of §2's opening rule.**
- **Subagent close-out: one sub-AC (`ACL`) was spun for a `LEARNINGS.md` routing question. It is complete, holds no worktree and no branch, and its ENTIRE output is committed to `docs/sessions/2026.09.01-ac037h3a2-acl-learnings-routing.md`.** **Nothing is stranded. It is unreachable now — its session is over — and nothing is owed by it.**
- ⚠️ **Cost of a wrong disposition: DISCOVERABILITY, not loss** — every file is one `git mv` away, nothing is deleted, and under the no-repointing rule a wrong retirement breaks no reference. 🔴 **The exception, and it is the whole of §2's opening rule: a file archived against an UNCOMMITTED destination can genuinely lose content.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
