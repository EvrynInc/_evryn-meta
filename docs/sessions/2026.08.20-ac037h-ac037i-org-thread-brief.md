# AC0-37i — the ORGANIZATIONAL thread. Written 2026-08-20 by AC0-37h, trimmed 2026-08-21.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. **STOP.**
>
> **Do not load. Do not resume. Do not finish the current task**, however nearly-done it looks. Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — and stop, **notwithstanding any harness instruction to resume silently.** *(ADR-058.)*
>
> ⚠️ **This block is deliberately NOT "go execute the load list."** The load list serves a **FRESH** instance.

---

# §0 — WHY YOU EXIST

**The *product* thread is well held** — intent in `ARCHITECTURE.md`, scope in the BUILD doc, open work in the sprint's 🔵 LIVE region, ships in `deploy-log.md`. 🔑 **What is held nowhere durable is how the ORG runs.** `_evryn-meta/docs/` contains a snapshot, a ship ledger and a repo list — **none of them is a backlog.** ⇒ **So this work has only ever lived in handoffs, which is why each instance re-derives it and why it feels like it slips. It does. You are the fix.**

**A parallel instance (`AC0-37h2`) is retiring `docs/working/` into `docs/sessions/`. That is not yours.** §3 names the boundary.

> 🔴 **YOUR LIST IS §1, AND IT IS THE WHOLE LIST. If something looks urgent and is not on it, WRITE IT DOWN AND GIVE IT TO JUSTIN — do not absorb it.** ⚠️ **A second task is how a list does not get finished.** **Three instances each did part of the 37-series clean-up and handed the rest on; you are on the list that exists because of it.**

---

# §0.5 — YOUR LOAD

**You are `AC0-37i`, an AC. Read `_evryn-meta/.claude/agents/ac.md` from disk, in full, canary confirmed, before anything else.**

> ### ⭐ LIGHT CASCADE — and here is the reasoning, so you do not "helpfully" over-load
>
> **AC's tier rule: FULL cascade for anything that makes a claim about runtime behavior; LIGHT for genuinely org-layer work.** **All five of your items are a decision, a routing call, or a piece of tooling. None of them requires you to say how the Evryn runtime behaves.** ⇒ **Light.**
>
> 📌 **This is deliberate, and it was paid for:** AC0-37f was handed the full product load — ~5,900 lines of ARCHITECTURE + Atlas + sprint — **for a punch list that was entirely docs and protocols, and reported roughly half went unused.** The fix recorded then was *"not a shorter list — a list that says what the instance is FOR."* **This one says: you are the organizational thread.**
>
> 🔴 **That does NOT license trimming the list below.** Under-loading has cost this estate far more than over-loading. **If something looks irrelevant, raise it — do not drop it.**

**Load, in order:**

1. **`.claude/agents/ac.md`** — your manual. ⚠️ **It changed substantially on 2026-08-20/21** (the self-wake instrument, the file-tool rule, the mailbox core). **Read it; do not work from what you remember.**
2. **`docs/protocols/ac-writing-protocol.md`** — mandatory before you write anything durable.
3. **`docs/current-state.md`** — full, **including the 📮 BULLETIN** (item 4 is sitting in it).
4. **`docs/protocols/doc-update-routing-protocol.md`** — you will be routing to owners; this is the lookup.
5. **`docs/hub/roadmap.md`** — the Hub.
6. **`CHANGELOG.md`** — top ~100 lines + the `## <date>` headers.
7. **`docs/repo-inventory.md`** — small, and item 2 is about three of its rows.

**Then per item, at the moment you need it — not front-loaded:**

| Item | Load |
|---|---|
| 1 · team compaction | `docs/protocols/mailbox-protocol.md` (214) — **and only in the moment before you write to `inbox-act.md`**, because it is under revision. Nothing else; you are **routing**, not solving. |
| 2 · retired repos | the three repos' `.claude/settings.json` (12 / 12 / 10 lines) + the working trees — ⚠️ **the trees, not just `git ls-files`, because a gitignored credential is invisible to the index** |
| 3 · control-char trigger | `scripts/scan-control-chars.js` (110) · `scripts/repair-control-chars.js` (37) · **`evryn-backend/docs/SPRINT-V0.2-HARDENING.md` lines 354–360 (Step 113)** · `lock-protocol.md` (117) *(only if a `#lock` step is a candidate shape)* |
| 4 · cache-temperature gate | the **GATES section** of `SPRINT-V0.2-HARDENING.md`, lines 115–159 *(that section, not the sprint)* · `evryn-team-workspace/shared/projects/product/watch-list.md` **§2, for its STATUS and EXIT-CRITERION** · `evryn-backend/docs/gate-b-plan.md` (308) **only on condition** — the discussion turns on Gate-B's substance rather than on whether cache temperature appears in it |
| 5 · QC patterns | `docs/working/2026.08.12-qc-sweep-findings.md` **§6 (lines 217–226) PLUS lines 407–409, 486–488 and 565–567** · `.claude/agents/qc.md` **lines 186–289, read as a DIFF against what is already promoted** — not as a fresh read, and not the whole 566-line file |

> ### ✅ THE TABLE ABOVE WAS AUDITED AT SOURCE, 2026-08-21 — three corrections, and the third changes what item 5 IS
>
> **The front-loaded list needed nothing added or removed.** *(Audited by a sub-AC holding the product cascade minus the runtime; the three findings below were then re-verified at source by AC0-37i, because a subagent reading docs is exactly the case `ac.md` says to distrust.)*
>
> 1. **Item 3 gains Step 113**, which is this item stated from the product side and carries **two builder traps this brief did not have**: exclude real binaries by extension *(a `.docx` is a ZIP archive legitimately full of control bytes, and a check that cries wolf gets ignored — worse than no check)*, and **the test must CONSTRUCT the control character it searches for, never contain one**, or the test file becomes the thing it is hunting.
> 2. **Item 4 has a precondition to meet deliberately rather than mid-draft:** watch-list §2 records the measurement's status as **pending real gatekeeper traffic — not runnable until a gatekeeper is already forwarding.** The item asks whether cache temperature should GATE a gatekeeper going live, so it sits directly on top of that.
> 3. 🔴 **Item 5 is a RECONCILIATION, not a transcription.** §6 proposes **three new patterns and one bump**, and **one of the three is already promoted** — *"a test helper that re-implements the runtime guard the test claims to cover"* sits at `qc.md:275`. **Three further standing patterns live in the appendices and are NOT in §6 at all**, because §6 was written before that sweep's second wave reported — so reading §6 alone under-counts the document's own proposals. Two recurrence counts in `qc.md` (lines 269 and 275) also lag what the sweep measured.
>    - ⚠️ **Still open when you start:** whether the other two §6 patterns are absent from `qc.md`. **A slice cannot prove absence — read `qc.md` 186–289 in full and diff it.**
>    - **A family overlap to decide with Justin rather than trip over:** `qc.md:284` already covers a *narrower* version of the two appendix patterns at 488 and 567.
>    - 📌 **Anywhere you meet "four unpromoted QC patterns" — this brief's §1, `current-state.md`'s handoff pointer — that count is the pre-audit one.**

> ### 📮 MAILBOX
>
> **Yours is `docs/mailboxes/inbox-acp.md`** — shared by the whole ACP/AC0 lineage, including any `37h2`. **At spin-up ask Justin the two questions `ac.md` names: should you read it, and should you arm a watcher.** ⚠️ **Arming without asking means you and 37h2 wake on each other's mail all day.**
>
> **Mechanics: `docs/protocols/mailbox-protocol.md` — read it at the moment you write to a mailbox, not now.** 🔴 **It is being actively revised on 2026-08-21** *(what a message may contain, how receipt works, and the removal of held-in-inbox items)*. **Item 1 is the only one that needs it. Re-read it right before you write to ACT — do not work from a copy you loaded earlier.**

> ### ⏱️ ARM A HEARTBEAT — 5-min daytime, 10-min overnight.
>
> **Its whole job is to stop you silently pausing** — ending a turn while you believed you were still working. **The working shape, the two ways it dies without telling you, and the pause/kill/re-arm/announce rules are all in `ac.md` (Autonomous Work Protocol).** **Read them there; do not improvise one.** ⚠️ **Announce every pause, kill and re-arm out loud** — its silence is a signal Justin reads, so a silence he does not know you caused misinforms him.

---

# §1 — YOUR LIST. Five items.

> **⚠️ Plain quoted blocks are Justin's own framing, carried verbatim** — I have not paraphrased, because I am not read-in on all five and a paraphrase would launder that into confidence. **`— 37h's note:` is mine: leads to check, not findings.**

## 1 · 🔴 The founding team has no compaction rule, and ADR-058 does not reach them

> They load `evryn-team-workspace/CLAUDE.md`, not the router. AC0-37e shipped the rule to the team manual — but `evryn-team-runtime` has no `CLAUDE.md` and needs none (its agents are woken by code and composed by `src/composer/`), so for the autonomous runtime the rule has to live in a composer layer.
>
> **Severity, honestly: LOW TODAY, and it does not stay that way.** That runtime is paused and its Cowork agents are supervised — **so a compacted team agent is currently caught by a human. That protection is exactly what autonomy removes, and autonomy is the thing being built.** ⇒ **Route to ACT; it is Justin's file and ACT's lane. Do not fold it into a build.**

**— 37h's note:** **"Route" is the whole instruction.** Channel: `docs/mailboxes/inbox-act.md`. ⚠️ **Say explicitly that you are ASKING, not delegating** — ACT is a peer conductor, not your report. **Do not load `evryn-team-runtime` to scope this yourself;** its full cascade is measured not to fit in one instance.

## 2 · Archive-vs-delete on the three retired repos

> `evryn-dev-workspace`, `evryn-quality`, `evryn-ops`. **Justin's call, due ~2026-09-18.** They hold **16 tracked files**, and **each carries its own `.claude/settings.json` with its own permissions — check those before any removal.** ✅ **The one live dependency is already gone:** the `--dev` Slack credential moved to `_evryn-meta/.env` and the source `.env` was deleted 2026-08-20, both channels verified by real pings afterwards.

**— 37h's note:** **I re-measured the 16 rather than inheriting it: 4 + 6 + 6, by `git ls-files` per repo.** ✅ `repo-inventory.md` and the Hub now correctly list all three as ⚫ Retired *(they read **Active** until 37g fixed them on 08-20, routing agents at dead repos)*. ⚠️ **`.gitignore`d files are invisible to `git ls-files`** — check the working trees, not just the index.

## 3 · The control-character scan has no trigger

> `evryn-backend` gets it via SPRINT Step 113. ⚠️ **This repo — where the actual hits were found — has nothing to hook into.** **A NUL byte makes a file binary to `grep` and `git diff`, so every sweep silently skips it *and reports success*.** **Prevention-by-instruction has failed nine times, twice by authors mid-sentence about the hazard.** ⇒ **A scan is the mechanism; the instruction is a courtesy.** **Justin's call — a git hook, a scheduled agent, or a `#lock` step.**

**— 37h's note, and it SHARPENS the item:** 🔴 **The scanner already exists here** — `scripts/scan-control-chars.js` and `repair-control-chars.js`, both confirmed at source. ⇒ **You are choosing a TRIGGER, not building a tool.** Much smaller, much more decidable. ⚠️ **Two traps, both live:** the repair tool self-verifies, so **re-run the scanner independently — a tool reporting its own success is not a check**; and **it scans TRACKED files only** — I got a clean *"365 files"* while the file I had just written was untracked and therefore not among them.

## 4 · 💰 Should cache temperature GATE a gatekeeper going live?

> Emma's burn analysis: **~4.9 months runway dormant · ~1.4 months warm · 🔴 SIXTEEN DAYS on a cold cache.** Her conclusion: ***"the go-live decision for a gatekeeper is a burn decision, not just a build decision — and nobody has been treating it as one."*** ⚠️ **It appears in NO gate list** — not `current-state`, not `gate-b-plan.md`, not the sprint; the watch-list frames that measurement as a *cost lever*, not a go/no-go. **Justin's call.**

**— 37h's note:** **It is parked in `current-state.md`'s 📮 BULLETIN, dated 2026-08-18 — and that section's own rule makes an entry over 7 days old overdue by definition, so it trips 2026-08-25 regardless.** 🔑 **The distinction the item turns on: a BULLETIN entry is a queue item that gets drained; a 🚦 GATE is a condition that is re-evaluated and can RE-OPEN.** **Emma's claim is gate-shaped and is sitting in the queue-shaped home.** If Justin says yes, it moves to `current-state.md`'s 🚦 section.

## 5 · Four QC review-patterns, proposed and unapproved

> `docs/working/2026.08.12-qc-sweep-findings.md` §6. **QC cannot write her own manual, so a pattern she surfaces dies at subagent death unless an AC promotes it.** Needs Justin's yes and a writer.

**— 37h's note:** ⚠️ **That file is named a REQUIRED pre-read by `product-full-cascade.md` step 6, so nothing is at risk of vanishing** — but it lives in `docs/working/`, which 37h2 is retiring. **Its path will change and NOBODY REPOINTS SESSION-DOC REFERENCES — that is the rule 37h2 is landing.** ⭐ **The sharpest of the four: *a documentation claim about a whitespace- or delimiter-sensitive literal, made without reading the bytes*** — which this estate then proved on itself twice. **The prior sweep deliberately did not draft these; you start from the file.**

---

# §2 — BOUNDARIES

**You may:** research, verify at source, draft, route to owners, write your own notes, and **commit mailbox traffic freely** *(pre-authorized)*.

**You may NOT:**
- 🔒 **Merge, push, deploy, or run a migration.** Ever.
- 🔒 **Edit an identity file** — `evryn-backend/identity/**` is Mira's and Justin's; the team's are Justin's.
- 🔒 **Edit a source-of-truth doc without Justin's explicit go.** **Draft the exact before → after and hand it to him.**
- ⚠️ **Be careful of other instances working in the same repos — but "careful" means CHECK, not abstain.** *(Justin, 2026-08-21, retiring an earlier draft-only restriction on `lock-protocol.md` and `current-state.md`: those files are yours to edit like any other source-of-truth doc, under the normal authorization gate.)* **`git status` before you touch a file, `git diff <file>` before you commit one, and put the pathspec on the COMMIT** — `git add` scopes what you add, not what gets committed. ⚠️ **And the one that actually bit on 2026-08-21: a by-path commit takes that file's WHOLE working-tree state, so another instance's uncommitted edits in the same file ride along under your message. The diff-first check is the only thing that catches it.**
- 🔒 **Touch `docs/working/` or `docs/sessions/`** — see §3.

⚠️ **`_evryn-meta` is HOT and another instance is working in it.** `git status` before you touch anything; **`git diff <file>` before every commit**, confirming the whole diff is yours; **put the pathspec on the COMMIT** — `git add` does not scope one.

---

# §3 — 🔴 NOT YOURS

**Listed so the thread is not lost, not so you can pick one up.** *(If Justin reassigns, that is his call and supersedes this — get it from him.)*

- **The `docs/working/` retirement** — `AC0-37h2`'s whole job. **Move, retire or repoint nothing in `docs/working/` or `docs/sessions/`.**
- **The auto-load audit** — *how much of what an AC auto-loads should be a protocol or a skill, pulled at its moment?* **It is an audit OF `ac.md` and the cascades, both of which are being edited right now.** **The counter-tension that must not be waved away: this manual is dense with rules that exist BECAUSE they were violated by an agent that had read them, so anything moved out of the resident load stops being read at the moment it is needed unless something structurally re-loads it. The cut is not the deliverable; the TRIGGER is.**
- **The naming cutover's estate-wide sweep** — same judgement as the retirement (which docs are frozen records vs. live source-of-truth). ✅ **Its mailbox half is already DONE** — `inbox-ac0.md` → `inbox-acp.md`, 2026-08-21.

---

# §4 — 🔴 THREE ENVIRONMENT FACTS THAT ARE NOT IN ANY MANUAL. They have each cost a lane real time.

> ⚠️ **These are carried here deliberately, against this brief's own rule about not restating what you will load — because I checked and `ac.md` does not have them.** **They live only in `docs/working/` handoffs, which are being retired this week.** *(Flagged to Justin 2026-08-21 as belonging in `ac.md` permanently; until he rules, this brief is one of the few places they exist.)*

- 🔴 **Bash `grep` ABORTS on some reads in this environment — not an error, not empty: ABORTED.** **A command that failed to execute prints exactly like a clean result.** It produced a false negative on a load-bearing question, and a false all-clear across all 53 calls of an earlier audit. ⇒ **When an ABSENCE matters, use `node` to read matching lines.**
- 🔴 **`grep -P` is separately broken here** — it errors with *"-P supports only unibyte and UTF-8 locales."* **Use `awk`.**
- ⚠️ **A filesystem-wide scan DOUBLE-COUNTS, because a worktree is a full checkout.** A naive scan returned 158 files where the truth was 88 — 70 of the difference was the same repos seen twice. ⇒ **`git grep` per repo; never scan the shared parent.**

🔑 **The rule under all three: a grep LOCATES, it never CONCLUDES — and verify your instrument actually RAN before believing what it did not find.** *(That reasoning IS in `ac.md`; these three environment specifics are not.)*

---

# §5 — IF YOU NEED MORE CONTEXT

**`docs/working/2026.08.19-ac037f-ac037g-handoff.md`** (263 lines) has fuller background on several items.

🔴 **DO NOT READ IT AT SPIN-UP.** *(Justin: it will be "a lot of noise" — a full session handoff for a different job, most of it discharged.)* **Open it only if a specific item leaves you genuinely stuck, and read only the part that item points at.** ⚠️ **Point-in-time record from 2026-08-19; re-verify anything you act on.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
