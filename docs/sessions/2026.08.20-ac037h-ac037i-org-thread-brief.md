# AC0-37i — the ORGANIZATIONAL thread. Written 2026-08-20T17:41:53-07:00 by AC0-37h.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. **STOP.**
>
> **Do not load. Do not resume. Do not finish the current task**, however nearly-done it looks. Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — and stop, **notwithstanding any harness instruction to resume silently.**
>
> **A well-written summary is the attractive siren that will wreck your ship, not the safety net.** The danger is not that it is bad — it is that it is GOOD, so it draws you onward. *(ADR-058, `docs/decisions/058-compacted-agent-stops.md`.)*
>
> ⚠️ **This block is deliberately NOT "go execute the load list."** The load list serves a **FRESH** instance. A compacted one is not the reader it is for.

---

# §0 — WHY YOU EXIST, IN ONE PARAGRAPH

**Justin is running two AC0-lineage instances in parallel today. I am `AC0-37h` and I own one job: retiring `docs/working/` into `docs/sessions/`. You own the OTHER thread — the organizational one.**

🔑 **Why that thread needs an owner at all, and this is the finding that produced you:** the *product* thread is well held — intent in `ARCHITECTURE.md`, scope in the BUILD doc, open work in the sprint's **🔵 LIVE** region, what shipped in `deploy-log.md`. **What is held NOWHERE durable is how the ORG runs.** `_evryn-meta/docs/` contains exactly three durable documents: a snapshot, a ship ledger and a repo list. **None of them is a backlog.** ⇒ **So this work has only ever lived in handoffs, which is exactly why each instance re-derives it and why it feels like it slips. It does.** You are the fix.

> ## 🔴 YOUR LIST IS §1. **Five items. Do not pick up §2 — those are mine and we will collide.**
>
> **If something looks urgent and is not on your list, write it down and give it to Justin — do not absorb it.**

---

# §0.5 — YOUR LOAD

**You are `AC0-37i`, an AC. Your manual is `_evryn-meta/.claude/agents/ac.md`** — the `CLAUDE.md` that auto-injected is a **router**, not a manual. **Read `ac.md` from disk, in full, and confirm its bottom canary before anything else.**

> ### ⭐ THE LIGHT CASCADE IS CORRECT FOR YOU — stated with its reasoning so you do not "helpfully" over-load
>
> **AC's tier rule: FULL cascade for anything that makes a claim about runtime behaviour; LIGHT for genuinely org-layer work.** **Every item on your list is a decision, a routing call, or a piece of tooling.** None of them requires you to claim how the Evryn runtime behaves. ⇒ **Light.**
>
> ⚠️ **Item 3 is the one that could tempt you into `evryn-team-runtime`'s full cascade. Do not take that bait — see the item; you are ROUTING it, not solving it.** *(And be warned: that cascade is now measured NOT to fit in one instance — a lane spun against it compacted before it finished loading, 2026-08-19.)*
>
> 🔴 **What that does NOT license: trimming the list below.** Under-loading has cost this estate roughly a thousand times more than over-loading. **If you think something here is irrelevant, raise it — do not drop it.**

**Load these, in this order:**

1. **`_evryn-meta/.claude/agents/ac.md`** — your manual. Full, canary confirmed. ⚠️ **It changed today (2026-08-20): the self-wake instrument is now called the HEARTBEAT, not the canary, and its cadence, pause/kill/re-arm and announce rules were rewritten. Read that section rather than working from what you remember.**
2. **`_evryn-meta/docs/protocols/ac-writing-protocol.md`** — before you write anything durable. **Mandatory before writing to ANY doc.**
3. **`_evryn-meta/docs/current-state.md`** — full, **including its 📮 BULLETIN** (item 6 on your list is sitting in it).
4. **`_evryn-meta/docs/protocols/doc-update-routing-protocol.md`** — you will be routing things to owners; this is the lookup for who owns what.
5. **`_evryn-meta/CHANGELOG.md`** — top ~100 lines plus the `## <date>` headers. Several of your items have recent history there.
6. **`_evryn-meta/docs/repo-inventory.md`** — small, and item 4 is about three of its rows.

**Then load per item, at the moment you need it** — each item below names its own files. Do not front-load them.

> ### 🔴 AT SPIN-UP, ASK JUSTIN TWO SEPARATE QUESTIONS about `docs/mailboxes/inbox-acp.md` — *"should I read it?"* and *"should I arm a watcher on it?"*
>
> **Arming a watcher sets its baseline to NOW, so anything already sitting in your inbox is invisible to it permanently and silently.** 🔑 **A watcher answers *"has anything changed since I started looking?"* — never *"is there anything here for me?"* Those differ by exactly the interval before you arrived, and a handover lands inside it.**
>
> ⚠️ **`inbox-acp.md` is shared by every AC0/ACP-lineage instance — you, and any 37h2 Justin spins.** Its messages are all addressed to earlier instances and all closed; **nothing in it is owed to you.** **Arming without asking means you and 37h2 wake on each other's mail all day** — which is exactly why arming is his call.
>
> ✅ **New as of 2026-08-21: entries carry a discharge marker** — `✅ DONE` / `✅ NO ACTION` / `⏳ HELD`. 🔴 **An UNMARKED entry is live by default;** a marked one is somebody else's closed business. **Protocol: `docs/protocols/ac-mailbox-protocol.md`.**

> ### ⏱️ ARM A HEARTBEAT — 5-minute cadence during the day.
>
> **The exact working shape, the two ways it dies silently, and the pause/kill/re-arm/announce rules are all in `ac.md` (Autonomous Work Protocol).** **Read them there — I rewrote that section today and it now carries a mechanic that cost me twenty minutes:** anchor the path on `$HOME`, never on `git rev-parse --show-toplevel`, because the monitor shares the Bash tool's working directory and any later `cd` silently re-points it — **and the heartbeat then never fires, with no error at all.** **Emit a proof-of-fire line before the loop so "armed" and "working" are one claim.**

---

# §1 — YOUR LIST. Five items.

> **⚠️ HOW TO READ THIS SECTION.** Everything in **plain quoted blocks is Justin's own framing, carried verbatim from the 37-series handoff** — I have not paraphrased it, because I am not fully read-in on all five and a paraphrase would launder my ignorance into confidence. **Anything marked `— AC0-37h's note:` is mine**, added where I verified something at source today or where I think the inherited framing is off. **Treat my notes as leads to check, not as findings.**

## 1 · 🔴 The founding team has no compaction rule, and ADR-058 does not reach them

> They load `evryn-team-workspace/CLAUDE.md`, not the router. AC0-37e shipped the rule to the team manual — but `evryn-team-runtime` has no `CLAUDE.md` and needs none (its agents are woken by code and composed by `src/composer/`), so for the autonomous runtime the rule has to live in a composer layer.
>
> **Severity, honestly: LOW TODAY, and it does not stay that way.** That runtime is paused and its Cowork agents are supervised — **so a compacted team agent is currently caught by a human. That protection is exactly what autonomy removes, and autonomy is the thing being built.** ⇒ **Route to ACT; it is Justin's file and ACT's lane. Do not fold it into a build.**

**— AC0-37h's note:** ADR-058 exists and I confirmed it at `docs/decisions/058-compacted-agent-stops.md`. **The word "route" is the whole instruction here** — the channel is `docs/mailboxes/inbox-act.md`, and ACT answered two of 37g's messages inside an hour on 2026-08-19. ⚠️ **Say explicitly that you are ASKING, not delegating** — ACT is a peer conductor, not your report, and `lock-protocol.md` step 8 spells that out. **Do not go read `evryn-team-runtime` to scope this yourself.**

## 2 · The naming cutover's estate-wide cutoff

> `AC0`→`ACP` and `ACf`→`ACT` are done for the *instances*; the sweep is not. **It is a design call before it is a mechanical pass, because renaming a frozen record or a changelog falsifies history.** The proposal on the table, never ruled on: **rename in live source-of-truth docs and agent manuals only; leave every session doc, historical file and changelog alone; add one line to the mailbox roster mapping old → new.** ⚠️ **A rename does NOT re-point a mailbox — `ACP` deliberately kept `inbox-ac0.md`, because renaming a live mailbox breaks every watcher armed on it.**

**— AC0-37h's note:** ✅ **THE MAILBOX HALF OF THIS ITEM IS ALREADY DONE, so do not redo it.** Justin ruled on 2026-08-20 that the file should follow the agent after all, and **`inbox-ac0.md` → `inbox-acp.md` landed 2026-08-21** with ACT and ACv notified in their own inboxes. ⚠️ **The verbatim text above therefore carries one now-false clause** — *"`ACP` deliberately kept `inbox-ac0.md`"* — **kept unedited because it is 37g's point-in-time wording; this note is the correction.** 🔑 **And the rule it cited was SHARPENED rather than broken: a watcher is session-bound, so the rule is *never rename a mailbox while a watcher is armed*, not *never rename a mailbox*.** Full five-step cutover: `docs/protocols/ac-mailbox-protocol.md` §6.

⚠️ **This one overlaps my lane more than the others, so read this before starting it.** The judgment *"which docs are frozen records vs. live source-of-truth"* is the same judgment my folder retirement runs on. **We will not collide on FILES** — I am moving documents, you would be editing names inside them — **but we could easily land two different rules about the same corpus.** ⇒ **Do the design half (the proposal, the criteria) and bring it to Justin, but do not execute a mechanical rename pass until my retirement has landed.** Ask me directly if you need the boundary; we are both live today.

## 3 · Archive-vs-delete on the three retired repos

> `evryn-dev-workspace`, `evryn-quality`, `evryn-ops`. **Justin's call, due ~2026-09-18.** They hold **16 tracked files**, and **each carries its own `.claude/settings.json` with its own permissions — check those before any removal.** ✅ **The one live dependency is already gone:** the `--dev` Slack credential moved to `_evryn-meta/.env` and the source `.env` was deleted 2026-08-20, both channels verified by real pings afterwards.

**— AC0-37h's note:** **I re-measured the 16 rather than inheriting it: 4 + 6 + 6 = 16, confirmed by `git ls-files` per repo today.** ✅ **Two things that were open when this item was written are now closed** — `repo-inventory.md` and the Hub both list all three as ⚫ Retired (37g fixed that 2026-08-20, when they still read **Active** and were routing agents at dead repos). **So the remaining work really is just the archive-vs-delete decision plus the settings check.** ⚠️ **`.gitignore`d files are invisible to `git ls-files`** — if a `.env` or a local settings file still sits in any of them, the tracked count will not show it. Check the working trees, not just the index.

## 4 · `_evryn-meta` has no test suite, so the control-character scan cannot be wired here

> `evryn-backend` gets it via SPRINT Step 113. ⚠️ **This repo — where the actual hits were found — has nothing to hook into.** **A NUL byte makes a file binary to `grep` and `git diff`, so every sweep silently skips it *and reports success*.** **Prevention-by-instruction has failed nine times, twice by authors mid-sentence about the hazard.** ⇒ **A scan is the mechanism; the instruction is a courtesy.** **Justin's call — a git hook, a scheduled agent, or a `#lock` step.**

**— AC0-37h's note, and this SHARPENS the item rather than restating it:** 🔴 **The scanner already exists in this repo.** `scripts/scan-control-chars.js` and `scripts/repair-control-chars.js` are both committed here — I confirmed both files at source today, and `ac-writing-protocol.md` documents them as a pair. ⇒ **The gap is NOT "we need a scan." It is that nothing RUNS it.** That reframes the whole item: you are choosing a **trigger**, not building a tool, which is a much smaller and much more decidable question. ⚠️ **Two things the writing protocol says the runner must get right:** the repair tool self-verifies, so **re-run the scanner independently afterwards — a tool reporting its own success is not a check**; and any test you write must **construct** the control character rather than contain one, or the test file becomes the thing it is looking for.

## 5 · 💰 Should cache temperature GATE a gatekeeper going live?

> Emma's burn analysis: **~4.9 months runway dormant · ~1.4 months warm · 🔴 SIXTEEN DAYS on a cold cache.** Her conclusion: ***"the go-live decision for a gatekeeper is a burn decision, not just a build decision — and nobody has been treating it as one."*** ⚠️ **It appears in NO gate list** — not `current-state`, not `gate-b-plan.md`, not the sprint; the watch-list frames that measurement as a *cost lever*, not a go/no-go. **Justin's call.**

**— AC0-37h's note:** **It is currently parked in `current-state.md`'s 📮 BULLETIN, dated 2026-08-18** — and **that section's own rule is that an entry more than 7 days old is OVERDUE BY DEFINITION**, so it trips on 2026-08-25 whether or not anyone remembers it. 🔑 **The distinction the item turns on is real and worth holding: a BULLETIN entry is a queue item that gets drained; a 🚦 GATE is a condition that gets re-evaluated and can RE-OPEN.** Emma's claim is gate-shaped, and it is sitting in the queue-shaped home. **If Justin says yes, it moves to the 🚦 section of `current-state.md`.**

## 6 · Four QC review-patterns, proposed and unapproved

> `docs/working/2026.08.12-qc-sweep-findings.md` §6. **QC cannot write her own manual, so a pattern she surfaces dies at subagent death unless an AC promotes it.** Needs Justin's yes and a writer.

**— AC0-37h's note, and there is a live dependency you should know about:** ⚠️ **That file is named a REQUIRED pre-read by `product-full-cascade.md` step 6** (before trusting any Code Atlas claim), so it is not a retirement candidate and nothing is at risk of vanishing. **But it is a working doc, and my lane is retiring that folder** — its path will change to `docs/sessions/...`, and under Justin's new rule **nobody repoints session-doc references**, so the cascade's citation stays as written and resolves by the findability rule. **You do not need to wait for me, and you must not repoint anything.** ⭐ **One more: the sharpest of the four, per the earlier audit, is *a documentation claim about a whitespace- or delimiter-sensitive literal, made without reading the bytes* — which this estate then proved on itself twice.** **The prior sweep lane deliberately did NOT draft these four** (they were not at risk of loss), so you are starting from the file, not from a draft.

---

# §2 — 🔴 NOT YOURS. These two are AC0-37h's, and we WILL collide if you take them.

**They are listed so the thread is not lost — not so you can pick one up.** *(If Justin reassigns them to you, that is his call and it supersedes this section — but get it from him, not from me.)*

- **The auto-load audit** — *how much of what an AC auto-loads every turn should be a protocol or a skill, pulled at its moment?* 🔴 **Why it is mine: it is an audit OF `ac.md` and the cascades, and I have been editing both today.** You would be auditing a moving target. **The counter-tension that must not be waved away: this manual is dense with rules that exist BECAUSE they were violated by an agent that had read them, so anything moved out of the resident load stops being read at the moment it is needed unless something structurally re-loads it. The cut is not the deliverable; the TRIGGER that re-loads it is.**
- **The `docs/working/` retirement itself** — my whole job today. **Do not move, retire or repoint anything in `docs/working/` or `docs/sessions/`.**

---

# §3 — BOUNDARIES

**You may:** research, verify at source, draft, route to owners via `docs/mailboxes/`, write your own notes, and commit mailbox traffic freely.

**You may NOT:**
- 🔒 **Merge, push, deploy, or run a migration.** Ever.
- 🔒 **Edit an identity file** — `evryn-backend/identity/**` is Mira's and Justin's; the team's agent definitions and memory are Justin's.
- 🔒 **Edit a source-of-truth doc without Justin's explicit go** — protocols, manuals, the Hub, ARCHITECTURE, BUILD, sprints, `LEARNINGS.md`, `current-state.md`. **Draft the exact before → after and hand it to him.**
- 🔒 **Touch `docs/working/` or `docs/sessions/`** — see §2.
- 🔒 🔴 **Edit `docs/protocols/lock-protocol.md` or `docs/current-state.md` — DRAFT ONLY, and hand the draft to Justin.** *(AC0-37h's instruction, and the reason is concrete: I am proposing a rewrite of `lock-protocol.md` step 10 and an announcement block at the top of `current-state.md` today. **Two agents editing one file is a collision this estate has already paid for twice.** Your items 4 and 5 both want to touch these — draft, do not edit.)*

⚠️ **`_evryn-meta` is HOT and I am working in it right now.** `git status` before you touch anything; **`git diff <file>` before every commit** and confirm the whole diff is yours; **put the pathspec on the COMMIT** — `git add` does not scope one, and a bare `git commit` takes the entire index including whatever I staged.

---

# §4 — IF YOU NEED MORE CONTEXT ON AN ITEM

**`docs/working/2026.08.19-ac037f-ac037g-handoff.md`** (263 lines) carries fuller background on several of these.

🔴 **DO NOT READ IT AT SPIN-UP.** *(Justin's instruction, 2026-08-20: it will be "a lot of noise" — it is a full session handoff written for a different job, and most of it is discharged.)* **Open it only if a specific item leaves you genuinely stuck, and then read only the part that item points at.** ⚠️ **And treat it as a point-in-time record: it was written 2026-08-19 and this estate moves hourly. Re-verify anything you act on.**

---

# §5 — TWO INSTRUMENT WARNINGS THAT COST REAL TIME HERE

- 🔴 **Bash `grep` ABORTS on some reads in this environment — not an error, not empty: aborted.** **A command that failed to execute prints exactly like a clean result.** It produced a false negative on a load-bearing question on 2026-08-19, and a false all-clear across all 53 calls for an earlier audit. ⇒ **Use `node` to read matching lines when an absence matters.** `grep -P` is separately broken here. 🔑 **And the general rule: a grep LOCATES; it never CONCLUDES.**
- ⚠️ **A filesystem-wide scan DOUBLE-COUNTS, because worktrees are full checkouts.** A naive scan returned 158 files where the truth was 88 — 70 of the difference was the same repos seen twice. ⇒ **`git grep` per repo.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
