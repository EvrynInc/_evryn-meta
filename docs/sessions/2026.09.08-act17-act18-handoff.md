🔒 **HELD-SESSION-DOC** — the LIVE ACT working brief · **RETIRE UPON:** superseded by ACT-18's own handoff at set-down · **OWNER:** ACT · **HELD SINCE:** 2026-09-08

# ACT-17 → ACT-18 — handoff. **Written clean. This file is self-sufficient; you need no earlier handoff.**

*Written 2026-09-08 by **ACT-17**, the team-runtime conductor. **Supersedes `2026.09.08-act16-act17-handoff.md` entirely** — that file is a frozen record of ACT-16's session and of ACT-17's first hours. **Do not work from it.***

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you do not see it, re-read until you do.
>
> **How to use this file:** ACT's live state — what is loaded, what is armed, what is owed, what is open. **The test at every append: if this instance died right now, could a fresh one pick up from this file alone?** If no, this file is behind — fix it before anything else.
>
> ### 📖 READ IN THIS ORDER
>
> | Read | Section | What it gives you |
> |---|---|---|
> | **1st** | **§1** | 🔴 **WHAT TO DO FIRST** |
> | 2nd | **§2** | **YOUR LOAD LIST** — in full, here, not in any older file |
> | 3rd | §3 | Where the lane stands |
> | 4th | §4 | The instruments to arm |
> | 5th | §5 | The backlog — what is owed |
> | 6th | §6 | ⏳ Waiting on — read before answering any mail |
> | 7th | §7 | The subagent ledger |
> | 8th | §8 | Doc dispositions *(recommendations, not actions)* |
> | 9th | §9 | What ACT-17 actually read of the runtime |
> | ref | §10 | Standing rulings of Justin's that have no other home yet |

---

# 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP HERE.

**If what you hold is a machine-written summary of your session rather than files you actually read — you have compacted, and your run is over.**

🔴 **DO NOT RELOAD AND CONTINUE. We tried it; it does not work.** *(Justin, 2026-08-18: "the agent just sails through it and confidently flails about, unloaded.")* **An instruction to reload is a rule you must REMEMBER to obey — and remembering is precisely what compaction damaged.**

**Do these three, then stop:** ① stop working, mid-sentence if need be · ② tell Justin in one line that you compacted · ③ ask for a fresh re-spin.

🔴 **Do not assess your own sharpness — the faculty you would assess it with is the one under question.** *"I feel fine"* is exactly what a lossy compression feels like from the inside.

---

## §1 — 🔴 WHAT TO DO FIRST

**The lane's one-line state: the three-week block is broken, one build has shipped against the dependency map, and the next lane is briefed and waiting for someone to spin it.**

0. 🔴 **YOU ARE PROBABLY ON A DIFFERENT MACHINE. RUN THE ARRIVAL CHECK BEFORE ANYTHING ELSE — and know that it is NOT the check you will reflexively run.** *(ACT-17 was packed out on 2026-09-08 specifically so Justin could move to the desktop; the laptop stays live with OTHER lanes on it.)*
   - **The reflex is the DEPARTURE check — *"is anything stranded here?"* — and on a fresh machine it answers *"all clean"* whether or not your work arrived.** 🔴 **The arrival question is the opposite one: *"is everything I was working on actually HERE?"*** ⚠️ **`git fetch` brings every branch down as data; `git pull` materializes ONLY the branch you are standing on.** ⇒ **Work in flight on any other branch is on the disk with no local branch and no working tree, and every sync check reports green.**
   - **Enumerate what has no local presence** *(the script is in `ac.md`, Worktree & Branch Discipline, "THERE IS A THIRD QUESTION")* — then **report the list to Justin rather than acting on it.** **Materializing another lane's branch is that lane's call, not yours.**
   - ⚠️ **Known state at set-down, so you can tell inherited from new: three things lived only on the LAPTOP and NONE of them are ACT's** — `dc/pgdump-schema-only` and `dc/gate-a-harness-promotion` (unpushed, product lane) and uncommitted edits in `_evryn-meta-acm` (another meta agent). **Justin confirmed the laptop stays in flight, so they are not lost and they are not yours.**
1. **Load §2.** It is complete and it is here.
2. **Ask Justin the two spin-up questions** *(separate acts, separate costs)*: **should I read my inbox?** and **should I arm a watcher on it?** *(ACT-17's inbox was empty all session and is empty now.)*
3. 🔴 **SPIN `ACTj` — the money lane. It is briefed, its channel is open, and it is the first thing on the board.**
   - **Brief: `docs/sessions/2026.09.08-act17-actj-money-layer-brief.md`.** **The spin is one sentence and the brief needs nothing further from Justin:**
     > *"You are ACTj. Read and faithfully execute `_evryn-meta/docs/sessions/2026.09.08-act17-actj-money-layer-brief.md`."*
   - ⚠️ **It is a STANDALONE INSTANCE, not a subagent** — it will conduct its own DC and QC, and a standalone lane survives your re-spin where a subagent dies with it. **You reach it through its committed exchange doc, which both sides watch.**
   - ⚠️ **BEFORE IT SPINS, CUT IT A WORKTREE OFF CURRENT `main` AND RUN `npm ci` INSIDE IT.** **Never junction `node_modules`.** **The old pre-provisioned lane worktrees were reaped on 2026-09-08 — they held zero commits and sat 27 behind `main`, and a lane starting in one would inherit a base from 2026-08-19.**
   - ⭐ **Why this lane and not another: it is the only one of the four that is not entangled with a sibling, and it is the only one that moves a deploy gate.** **Gate G3's pace 5 tests a per-job spend cap and the per-job layer is inert, so pace 5 is UNRUNNABLE until this lane wires jobs.**
4. **Then SPRINT Step 43 — the lifecycle five.** It is the highest-priority open item in the tracker and has been for weeks: **the dormancy cadence-gate wiring is unguarded, and that gate is what stands between a model-stage failure and one ~64K Opus wake every fifteen seconds.** **Three of its seven pieces merged; five remain.**
5. ⚠️ **The other three lane briefs (`ACTt`, `ACTi`, `ACTv`) are still WRONG and still unspun.** **Each demands all 63 test files in full — ~12,500 lines, larger than `src/` itself — which the sanctioned cascade never asked for and which is the leading suspect for the 2026-08-19 compaction.** **`ACTi`'s brief carries a banner explaining this and pointing at `ACTj`'s as the worked example.** 🔴 **Justin's sequencing: ONE lane first, learn from it, then re-scope the rest. Do not re-scope all three pre-emptively.**

---

## §2 — YOUR LOAD LIST

> 🔴 **NO LINE COUNTS APPEAR BELOW, AND THAT IS JUSTIN'S RULING (2026-09-08): *"they're always stale when they get forwarded like this."*** **The record backs him: `ac.md` has been forwarded as 749, then 911, and measured 940 the same week.**
>
> ⭐ **DERIVE THEM AT READ TIME:** `cd "$HOME/Evryn/Code" && wc -l <paths>`. **For anything enumerated rather than named, enumerate LIVE:** `git -C evryn-team-runtime ls-files src migrations`. ⚠️ **Never a `src/**/*.ts` pathspec — `**/` requires an intervening directory, so it silently drops every file sitting directly in `src`.**
>
> 🔴 **CONFIRM EACH FILE'S BOTTOM CANARY** (`FULL FILE LOADED`). ⚠️ **AND KNOW THE ONE WAY `wc -l` LIES: it counts NEWLINES, so a file with no trailing newline reports one FEWER line than it has — and the line it omits is the LAST one, which is the canary.** ⇒ **Read PAST the count, or `tail -1`. Never conclude "no canary" from a count-bounded read.** *(Live: `evryn-team-runtime/docs/dependency-map.md` reports 634; its canary is line 635.)*
>
> 🔴 **NAME THE WORKTREE YOU READ FROM, not just the paths** — read from each repo's canonical `main` tree unless told otherwise, and say so.

**Read in this order. The order is DEPENDENCY, not importance** — a later document is usually a diff against an earlier one, so out of order you build a picture the next document exists to correct.

| # | File | Read |
|---|---|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | **full.** Your manual. ⚠️ **NOT auto-loaded** — the injected `CLAUDE.md` is a router. |
| 2 | `_evryn-meta/docs/hub/roadmap.md` | full — the Hub |
| 3 | `_evryn-meta/docs/hub/technical-vision.md` | full |
| 4 | `evryn-team-runtime/docs/ARCHITECTURE.md` | full — **cardinal invariants 8 and 9, and "why local reasoning often fails here"** |
| 5 | `evryn-team-runtime/docs/BUILD-PHASE-1.md` | full |
| 6 | `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` | **full — the Steps, and the status source of truth** |
| 7 | **`evryn-team-runtime/docs/dependency-map.md`** | **full.** ⭐ **NEW TO THIS LIST and the reason the lane is unblocked** — generated, complete by construction. **Its §4 reverse index is how you derive a lane's load.** |
| 8 | `_evryn-meta/docs/current-state.md` | full |
| 9 | `_evryn-meta/CHANGELOG.md` | the top ~110 lines |
| 10 | `evryn-team-runtime/CHANGELOG.md` | the top ~110 lines |
| 11 | `_evryn-meta/docs/protocols/mailbox-protocol.md` **+** `_evryn-meta/docs/mailboxes/inbox-act.md` | full, both. ⚠️ **`docs/mailboxes/README.md` DOES NOT EXIST.** |
| 12 | **`_evryn-meta/docs/sessions/2026.09.08-act17-actj-money-layer-brief.md`** **+ its exchange doc** | **BOTH.** ⭐ **NEW — this is the live lane and the first brief derived from the map.** |
| 13 | `_evryn-meta/docs/sessions/2026.08.19-act-acta-atlas-brief.md` **+ `…-act-acta-exchange.md`** | **BOTH.** ⭐ **The exchange is the valuable one — it holds `ACTa`'s delivered analysis and is the only home of the hub reasoning.** |
| 14 | `_evryn-meta/docs/sessions/2026.08.19-act-acti-guard-integrity-brief.md` | **the lane-brief anti-pattern.** ⚠️ **Read its §3 BANNERS, not its §3 list.** |
| 15 | `_evryn-meta/docs/decisions/055-thread-as-complete-record.md` | full — **SPRINT §H is entirely this ADR's build** |

> 🔴 **CHANGES FROM THE LIST ACT-17 INHERITED, SURFACED RATHER THAN MADE SILENTLY** *(a silently-altered load list hands your successor different eyes than the ones proven to work)*: **ADDED item 7** *(the dependency map — it did not exist when the old list was written and it is now the lane's central artifact)* and **item 12** *(the live lane's brief)*. **REMOVED the `ACTd` brief and exchange — that lane is CLOSED, bannered, and there is nothing to act on.** **Everything else is carried verbatim.**

**Pull at their moment, not at spin-up:** `ac-orchestration-protocol.md` **+** the matching `orchestration/spinning-*.md` *(**before you spin ANYTHING** — the core alone is explicitly not enough)* · `ac-writing-protocol.md` *(before writing to any doc)* · `lock-protocol.md` *(at a `#lock`)* · `packout-protocol.md` *(at a `#packout` — it runs the lock as step 0)*.

### 🔴 NOT on this list, deliberately: the runtime itself.
**You are a CONDUCTOR; the runtime lives in your lanes' heads.** ⚠️ **But the map changes what that costs — ACT-17 read the map plus all 41 leading docblocks (739 lines, one script) and that was enough to write a competent lane brief.** ⭐ **That is a cheap, repeatable move and it is recommended.**

---

## §3 — WHERE THE LANE STANDS, 2026-09-08 evening

🔴 **ASK GIT FOR REPO STATE, NEVER THIS FILE — no SHA appears below on purpose.**

- **✅ THE THREE-WEEK BLOCK IS BROKEN.** SPRINT Steps **47, 63, 81 and 42** merged; **515 tests pass, typecheck clean.** **Nothing is deployed, migrations 015–018 are unapplied, and the runtime wakes nobody.**
- **✅ `evryn-team-runtime` is ONE CLEAN WORKTREE** for the first time since 2026-08-19. All five stale lane trees reaped; all their branches deleted.
- **⭐ SPRINT Step 84 CLOSED: the partition is EIGHT sections, not seven.** `digest` + `threads` are one idea *(who is aware of a room and who wakes for it)*; `money` is another. 🔴 **Merged, the digest→money-ledger call looks INTERNAL, so a section reader never reports it — the hidden-seam failure this runtime is built to punish.**
- **⭐ SPRINT Step 19 RE-SCOPED FROM SOURCE: the money layer is BUILT and merely UNWIRED.** The tracker said for weeks that it *"does not exist in practice"* — misleading. **Read the re-scope, not the original description.**
- **The top open item is still SPRINT Step 43** — the lifecycle five.

---

## §4 — INSTRUMENTS — what to arm, not what was armed

🔴 **A `Monitor` DIES WITH THE SESSION. Nothing below is running. Arm your own; never inherit a task id.**

| Instrument | Watch | Cadence | Its one job |
|---|---|---|---|
| **Inbox watcher** | `docs/mailboxes/inbox-act.md`, firing on **COMMIT** (`git log --all -1 -- <path>`), with a `git fetch` each tick | 45s, **emits only on change — nearly free** | *"Did anything land for me?"* |
| **Heartbeat** | **you** | **5 min daytime · 10 overnight** | *"Are you still moving?"* — nothing else |

- 🔴 **ANCHOR EVERY WATCHER ON `$HOME` AND ON THE REPO (`git -C`), never on the working directory.** **A `Monitor` shares the Bash tool's cwd and that cwd is MUTABLE** — a later `cd` re-points it, it goes quiet, and **the silence is indistinguishable from an empty inbox.**
- **Print a proof-of-fire line BEFORE the loop.** *"Monitor started"* only means a process launched.
- ⭐ **AND A THIRD INSTRUMENT WORTH KNOWING, learned this session: WHEN YOU ARE WAITING ON A SUBAGENT, WATCH ITS WORKTREE, NOT YOURSELF.** **A heartbeat costs a full turn every tick to ask a question you already know the answer to; a change-gated watcher on the subagent's `git` state answers *"is it advancing?"* for almost nothing.** ⚠️ **Fire on COMMITS (`rev-parse HEAD`), not on every dirty file — the file-level version costs a turn per write.** 🔴 **And kill it the moment its subject is gone: mine fired with an EMPTY SHA after I reaped the worktree, which is the stale-pointer failure this estate has now recorded four times.**

---

## §5 — 📋 BACKLOG — what is owed

🔴 **ONE TABLE, ONE STATE COLUMN. Numbers are never reused, so a reference to "B4" from anywhere still resolves.**

| # | Item | Owner | State |
|---|---|---|---|
| **B1** | **`runtime-ops.md:9` cites a SESSION DOC for a live work-list** — composer layer 1, read by all eight agents on every wake, depending on a session doc for its own open questions. | ACT → Justin | **Already SPRINT Step 77.** 🔴 **Needs a FULLY-LOADED sub-AC — Justin explicitly barred a light-loaded coordinator from drafting it.** |
| **B5** | **~30 ACT-lineage session docs held since 2026-08-22** awaiting this lineage's evaluation. *(Derive the live list: `git grep -n "HELD-SESSION-DOC"`.)* | ACT | **DEFERRED BY JUSTIN** until the main show is underway, and scoped as **its own independent sub-lane** — not ACT's own token budget. ⚠️ **Budget it as a DISCOVERY pass: ACP's equivalent found nine of its last sixteen docs carried live work.** |
| **B14** | 🔴 **Justin's line-count ruling has NO DURABLE HOME OUTSIDE A HANDOFF.** *"Do not forward line counts in a load list — hand over the command and let the reader derive them."* **Plus the `wc -l` newline trap.** **It is in this brief and in the meta changelog; neither is a protocol.** | ACT → Justin | **OPEN. Proposed home: `ac-writing-protocol.md`.** 🔴 **Needs his sign-off on the wording — that is `lock-protocol.md` step 9b, and skipping it is how a situational remark becomes a false standing rule.** |
| **B15** | **`_evryn-meta/CHANGELOG.md` is MIS-ORDERED** — the `2026-09-04` entry sits above `2026-09-08`, against the file's own "most recent at top" rule. | anyone | **Open, trivial.** **Flagged rather than fixed: moving another instance's entry is not ACT's to do.** |
| **B16** | **`evryn-backend-qc-sandbox`** — a detached-HEAD worktree on the laptop, not ACT's. **Its commit is already inside `dc/gate-a-harness-promotion`, so nothing is at risk.** | ACP | **Surfaced to Justin; not ACT's to reap.** |
| **B13** | **`ThreadDormancyWriteRow` is declared and referenced NOWHERE** — `digest/index.ts` still reads that table through an inline type at three sites. | ACT → whoever next owns `digest/` | ⚫ **NOT CLOSED, MOVED. It now lives as the RESIDUE inside SPRINT Step 47, which is where a builder will meet it.** *(Named here rather than dropped: an item removed from a backlog without a destination is an item nobody can find again.)* ⚠️ **The honest framing recorded there: the typed surface is now ACCURATE but still UNGUARDED — nothing tests `db/types.ts` against the migrations.** |

⚠️ **TWO ITEMS FROM THE SUPERSEDED BRIEF ARE DISCHARGED AND DELIBERATELY NOT CARRIED, named so a reader does not go hunting:** **B7's `team-runtime-full-cascade.md` fix** *(it read "uncommitted for Justin's vet"; it was committed at `96de463` and that line simply went stale)*, and **the "Step 63's text still instructs the coverage-losing move" note** *(the Step now opens by forbidding exactly that restoration)*. **Both verified at source during the packout re-read, not assumed.**

---

## §6 — ⏳ WAITING ON — sent, no answer yet

🔴 **READ THIS BEFORE YOU ANSWER ANY MAIL. A reply arriving cold is unreadable without the question that produced it, and ACT-17's transcript dies with its session.**

| # | Sent to | What was asked | What their answer means |
|---|---|---|---|
| **W1** | **ACP** *(`inbox-acp.md`, 2026-09-08)* | **Should the dependency-map generator be ONE shared file with per-repo config, instead of two forks?** | **"Yes"** → build the shared version. **"No"** → **SPRINT Step 81 closes MOOT** and the fork is permanent. |
| **W2** | **ACP** *(same file)* | **Does `evryn-backend/docs/ARCHITECTURE.md` carry the same hand-maintained-inventory shape ours did?** | **A QUESTION, not a defect report.** "No" → nothing owed. |
| **W3** | **ACP** *(`inbox-acp.md`, 2026-09-08T16:42)* | 🔴 **A GENERATOR DEFECT CLASS: reads file CONTENTS from the working tree, takes its provenance SHA from git — so a run mid-change maps disk and stamps the previous commit.** **Ours had it; theirs has the same construction.** | **"Yes, we regenerate from clean"** → nothing owed, close it. **"No"** → their committed map may describe a tree that never existed. ⚠️ **The message is explicit that a grep located this and concluded nothing.** |

⚠️ **ACP was dormant on 2026-09-08 and all three are unanswered.** **`git pull` before assuming his inbox state is current.**

---

## §7 — 🔴 SUBAGENT LEDGER — enumerate, never work from memory

**Subagent resume is SESSION-BOUND. Both agents below became PERMANENTLY UNREACHABLE when ACT-17's session ended.** **A returned report is not persistence; only a committed file survives.**

| Agent | What it was for | Durable outcome | 🔎 If the close-out did NOT land |
|---|---|---|---|
| **`QCt`** *(QC)* | Full review of `dc/db-types-and-bounds`, then a verification pass | ✅ **NO-GO then GO.** **Findings are in SPRINT Steps 42, 47 and 82, and two patterns are in `qc.md`.** **Sandbox `evryn-team-runtime-qct2` created and removed by her; absence verified.** | **If `evryn-team-runtime-qct2` exists, the reap failed** — `git worktree remove` it and check `node_modules` counts around the reap. |
| **`DCf`** *(DC)* | Fix trip on QC's findings | ✅ **Two commits, merged.** **Its findings are in Steps 42 and 82.** **Worktree `dc1` reaped; branch merged and deleted with `-d`.** | **If `evryn-team-runtime-dc1` exists, it is an empty shell** *(git unregisters it but a file handle can hold the directory — Justin had it open in his editor)*. **Safe to delete: every commit is on `main`.** |

**Nesting: NEITHER spawned children** — neither brief authorized spinning. ⇒ **There is no invisible grandchild to strand.**

**Nothing owed upward:** no open question, no unsurfaced `NEEDS-JUSTIN`. **Every item `DCf` flagged is filed** — the withdrawn Step 82 stamp-guard, the `git diff`-not-`cmp` rider, the `assertOk`-outside-`try` residue *(Step 42, same class as Step 40)*, and the seven-vs-six scalar count.

---

## §8 — 🗂 DOC DISPOSITIONS — recommendations for Justin, NOT actions taken

🔴 **ACT-17 MOVED NOTHING.** *(`lock-protocol.md` step 10: anything needing a judgement call is a recommendation, and no session doc moves silently.)*

| Doc | Its condition | Recommendation |
|---|---|---|
| **`2026.09.08-act16-act17-handoff.md`** | *"superseded at set-down"* | ✅ **CONDITION MET — ARCHIVE.** **It is bannered `SUPERSEDED BY` this file.** ⚠️ **File it under ITS OWN date (`historical/2026.09/`) and do not change one character of the filename.** |
| **`2026.08.19-act-actd-*`** *(brief + exchange)* | *"retire upon the lane closing out"* | ✅ **CONDITION MET — both are bannered `LANE-CLOSED`.** **Archive when the B5 pass runs; no rush.** |
| **`2026.08.19-act-acta-*`** *(brief + exchange)* | *"retire upon the atlas + map existing and being adopted"* | 🔒 **HOLD — and the condition is now MOSTLY met.** **The map exists, is merged, and has now been USED to ship a build.** **The atlas still does not exist.** ⚠️ **The exchange doc is the ONLY home of the hub reasoning — do not retire it without rehoming that.** |
| **`2026.08.19-act-acti-guard-integrity-brief.md`** | *"retire upon that lane completing, or a re-scoped brief superseding it"* | 🔒 **HOLD — re-scopable but deliberately not re-scoped.** Its banner explains why and points at `ACTj`'s brief as the worked example. |
| **`2026.08.19-acf15-act16-handoff.md`** | *"ACT has evaluated it"* | ✅ **CONDITION MET — ARCHIVE.** Its load list was carried forward, corrected, and now lives inline in §2 here. |
| **`2026.08.17-acf13-lane-protocol.md`** · **the four `2026.08.17-acf13-acf*` lane docs** | — | 🔒 **HOLD.** Still named in `ACTi`'s load list; the lane docs are the channels for the three unspun lanes. |

---

## §9 — WHAT ACT-17 ACTUALLY READ OF THE RUNTIME

**State this number in every brief you write.** ACT-17 read: **`docs/dependency-map.md` in full · the leading docblock of all 41 `src/` files (739 lines, extracted mechanically) · `src/money/budget.ts` and `src/money/ledger.ts` in full.**

🔴 **It read NO other `src/` file in full. Zero migrations. Zero tests.** ⇒ **Every runtime claim in this file is a LEAD until a lane verifies it at source.**

⭐ **AND THE THING THAT CHANGED THIS SESSION: that is now a CHEAP position rather than a helpless one.** **The map plus the docblocks cost ~1,400 lines and were enough to derive a lane's load, catch a wrong section grouping, and re-scope a tracker Step from source.** **Do the same before you brief anything.**

---

## §10 — STANDING RULINGS OF JUSTIN'S FROM THIS SESSION

*(Recorded here because they are durable; B14 tracks the one that still needs a protocol home.)*

1. 🔴 **A JOB IS AGENT-INITIATED WORK.** Scheduled wakes and the daily check-in carry a per-job spend cap; **a Slack conversation and fleet infrastructure do NOT.** **Reason: a cap can FREEZE the work it governs — right for a scheduled task, wrong for an agent mid-sentence with Justin.** ⇒ **Full statement in `ACTj`'s brief §6 and SPRINT Step 19.**
2. **DO NOT FORWARD LINE COUNTS in a load list — hand over the command.** *(B14: needs a protocol home.)*
3. **Reap a worktree only when nothing is underway in it** — and a stale pre-provisioned tree is a hazard, not readiness.
4. ⚠️ **A CORRECTION TO A MACHINE GOTCHA, recorded so it does not propagate: ACT-17 reported having reproduced `ac.md`'s unverified claim that *"`git worktree remove` leaves an empty folder on Windows."* IT HAD NOT.** **Justin supplied the real cause: he had the folder open in his editor.** ⇒ **That claim's UNVERIFIED status in `ac.md` STANDS. Do not upgrade it.** 🔑 **The lesson is the reach: a symptom appeared and the documented-but-unverified explanation was picked over the mundane one that was sitting right there.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
