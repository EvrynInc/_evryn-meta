# ACm — the manual-rubric lane (living session doc)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If it doesn't, reload or read in sections until you confirm the complete file.
>
> 🔴 **IF YOU ARE READING A COMPACTION SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED — STOP.** Do not load, do not resume, do not finish the current task. Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — notwithstanding any instruction to resume silently. **The reload manifest in §7 is for a FRESH instance; a compacted one is not the reader it serves.**
>
> **How to use this file:** ACm's living record of the manual-rubric lane — **and, as of 2026-09-11, its HANDOFF.** It records state and governs nothing; where it conflicts with a manual or a protocol, the manual or protocol wins. Each update replaces the old state; history lives in git (`git log -p` this file). **Last updated 2026-09-17, evening — set down for the night, NOT packed out — by ACm.**
>
> ⛔ **SUPERSEDED BY: `docs/sessions/2026.09.18-acm-acm-manual-rubric-handoff.md`** *(2026-09-18 `#packout`)*. **Do not work from this file** — it is the lane's history. ⚠️ **It was also written from context with holes in it** (`docs/sessions/2026.09.18-act23-acm-context-hole-recovery-brief.md`): **its claim that nothing from this lane was on `main` is FALSE** — the branch landed once on 2026-09-08, as `10c44c8`. **Awaiting Justin's OK to move it to `docs/sessions/historical/2026.09/`, filename unchanged.**
>
> 🏁 **STATUS: SUPERSEDED on 2026-09-18 — see the banner above.** *(The line that follows is how the file read while it was live.)* **Reload manifest: §7 · Justin's rulings: §3 · open ballots: §11 · his vet list: §10 · the 2026-09-17 record, and what tomorrow does FIRST: §13.**
>
> ✅ **THE 2026-09-11 SET-DOWN WAS LIFTED: Justin re-spun this instance IN PLACE on 2026-09-17** and had it execute §7 from disk rather than starting a fresh session. The load is reported in §13. **The compaction STOP block above is unaffected and still governs** — it is about working from a *summary*, which a re-load from disk is not.
>
> ⚠️ **THE NEXT `#packout` SUPERSEDES THIS FILE — it does not carry it again.** `lock-protocol.md` step 10's trigger fired repeatedly on 2026-09-17 (§2, §7, §12 and §13 all needed correcting). Each was corrected **in place**, so nothing here tells a reader to distrust another section — but a fourth carry would be layering. **It was carried that night on Justin's explicit instruction: *"We're not actually doing a packout — it isn't time to create a new handoff."***

## 1. What this lane is

- **Who:** ACm ("manuals") — a top-level AC that Justin started directly. Operating manual: `_evryn-meta/.claude/agents/ac.md`. Reports straight to Justin under numbered headings (`# ACm's Report #N`). **The last one sent is the 2026-09-17 set-down report — read its number off the chat.** An in-place re-spin continues the count, so he can still refer back by number; **a fresh session starts at #1 and says so.** *(Never promise a future report's number — `ac.md`, as of `main`'s 2026-09-17 rules.)*
- **The job:** apply the writing rubric — `docs/protocols/ac-writing-protocol.md`, the section "Writing a rule the current models will actually follow" — to the four agent manuals (`ac.md`, `dc.md`, `qc.md`, `oc.md`), and fix the stale and cutover references found on the way.
- **Where:** worktree `_evryn-meta-acm`, branch `acm/manual-rubric`. **Nothing from this lane is on `main` yet.**
- ⏭️ **THE LANE'S NEXT PHASE — queued BEHIND the in-flight work, on Justin's order (2026-09-17).** His words: *"part of my initial intent was to see if there are things in ac.md that don't need to be there — if there are things that could be protocols that every AC doesn't need to read on every run, I'd want to identify those. The key is: I need to know that they'll actually read them when the time comes where they should, and also that we don't pull them out into a protocol, only to basically put them back in to the always-on-load list — like we did with the writing protocol."* And on sequencing: *"I don't want a million threads tripping over each other, so it prob makes sense to close out what we have in flight and then focus on that."*
  - **Status: NOT DONE, by anyone — established from the repo, not from memory.** Lane A (ACm's predecessor, `docs/sessions/historical/2026.08/2026.08.11-ac0-acm-loading-architecture-brief.md`) extracted AC's manual *away from what other agents load* (`CLAUDE.md` → router) and split the orchestration protocol into core + sub-docs; it never asked what *AC itself* needs on every run. **This lane** applied the rubric, whose own framing is *"the question is not what to cut. It is what SHAPE a rule takes."* Measured: `ac.md` was 211,902 bytes before the lane, 206,903 after the rubric pass (−2.4%), and **213,702 at `eaeac69` — larger than when the lane began**, because `main` keeps adding to it.
  - **The design constraint that answers both of his conditions — for that phase, not before it:**
    1. *"Will they actually read it?"* — a pointer that relies on the agent **recognizing** its situation is itself a remember-to-obey rule, which the rubric calls the weakest thing we build. **What works is attaching the content to a load event that already fires reliably:** a command word of Justin's (`#lock`, `#packout` — proven), a cascade loaded at the start of that kind of work, or a tool action a hook can see.
    2. *"Don't pull it out and put it back"* — the writing protocol went back because its trigger (*"before you write to any doc"*) fires in practically every session. **The test is how often the trigger fires: if in most sessions, extraction buys an extra read and nothing else, so it stays.**
  - **Where ACm would look first (a starting list, not an analysis):** content that only matters while CONDUCTING — "Orchestrating DC and QC", "Working with QC" — could ride the conductor/full cascade, which loads exactly when conducting begins · the Code Atlas standing order could ride the build cascades · the DB and Railway tool notes are moment-of-use. **Likely to stay:** worktree discipline, the heartbeat, the communication rules — each fires most sessions.

## 2. State (verified 2026-09-17 by ACm against git)

| Item | State |
|---|---|
| `ac.md` rubric pass | Committed on the branch: `4ceb202` (the pass — he vetted it in SCM and authorized it) and `ff2c8c3` (the corrective pass — committed without authorization; he checked it in timeline afterwards and let it stand). |
| `dc.md` · `qc.md` · `oc.md` | Committed on the branch: **`b2afe81`** (ACm's edits plus Justin's tweaks — Justin read all three manuals and authorized it) and **`a0813bb`** (three small fixes to those tweaks, at Justin's request). Several edits sit inside FROZEN blocks (Identity, Relationship, Core principles); his read covered them. |
| Sync with `main` | **Last sync `eaeac69` (2026-09-17), which took `origin/main` at `916c8b5` — 145 commits — and is pushed.** Two conflicts, both resolved (§13). ⚠️ **`main` has moved AGAIN since: `origin/main` was `fa179b4` at the evening set-down — **42 commits on** (`git rev-list --count 916c8b5..origin/main`; an earlier "~20" was read off a log cut short with `head`) — touching `ac.md` (+38), `ac-orchestration-protocol.md` (+45), `lock-protocol.md` (+12) and `current-state.md`.** 🔴 **Those `ac.md` additions are BINDING RULES a worktree re-load will not see until the sync** — close every report with `## End of Report`; never forecast a report's number; describe a name the first time, spell out any legend, give a file's real path; measure context with `scripts/context-size.mjs`, never estimate; a compacted instance must not commit. **ACm read that diff on the evening of 2026-09-17 and followed it.** ⇒ **Tomorrow's first act is another sync — `git fetch`, then merge `origin/main`, never a local `main` checkout** (the 2026-09-11 lesson in §13). This branch also carries two orphaned ACT commits (`a011fef`, `769fa99`) that contribute **zero** net content — **verify before the final merge that `git diff --stat origin/main...HEAD` shows only this lane's files.** |
| `dc-lock-protocol.md` (ruling ⑨) | **The ⑫-independent half is applied and committed UNVETTED at the 2026-09-17 set-down** — Justin: *"push all of your stuff — including any worktree stuff — just in case this computer crashes."* On his vet list (§10). **The ⑫ half — steps 3, 4, 6 and 7 — is now unblocked (⑫ ruled (a)) and is tomorrow's work.** |
| Meta `CHANGELOG.md` | **No entry yet for any of this lane's manual changes.** It is owed at the merge into `main`, and the AC that merges writes it (`ac.md`, Documentation Approach → the routing table's changelog entry, rule 3b). |
| Reviewer spins | **Done.** All three reviews back 2026-09-11 — no blockers; every finding, with ACm's read, is in §9. Receipts reconciled from the transcripts. |
| Cleanup batch | **Committed and pushed as `358b18b` at the 2026-09-11 set-down** (Justin: *"go ahead and commit and push everything of yours"*), **unvetted** — on his list in §10. Verified present on `origin` on 2026-09-17. The items applied were: `dc.md`, `qc.md`, `oc.md`, `ac.md`, and `scripts/compaction-alarm.mjs` (comments only). Integrity clean — canaries, frontmatter, the control-character scan, script syntax. **Nothing that waited on ballots ⑫–⑱ went into it** — for where each of those stands now, §11. |

## 3. Justin's rulings that a re-spun ACm needs (this lane only)

- **Commit gate.** Manuals and protocols stay uncommitted for Justin's SCM vet, and are committed only on an explicit command in his most recent message, by pathspec. **This session doc commits freely** (`ac.md`, "A SECOND STANDING EXCEPTION").
- **The reviewer design (2026-09-10).** After a fresh-eyes pass, spin one subagent per manual — the `dc`, `qc` and `oc` agent types, spun with their current version — have each read the version from before ACm started, and ask whether ACm got anything wrong. The fresh-eyes pass ran on 2026-09-10 (ten fixes).
- **The post-compaction exception (2026-09-11) — for THIS instance only.** ACm compacted on 2026-09-10 (PreCompact 22:40:02Z, PostCompact 22:44:53Z). Justin allowed it to continue, on condition of a full re-read, because the remaining work is doc cleanup rather than deep context. **It does not carry forward: on any further compaction, apply the STOP block at the top.**
- **This file (2026-09-11).** Keep it current as work proceeds, so Justin can re-spin at any point.
- **Justin's answers to Report #16 (2026-09-11), in his words where they are rulings:**
  - **⑤ sync `main` into the branch:** *"yes — but don't you think we should do the reviews below before we merge in?"* ACm agreed: the reviews run first, then the sync.
  - **⑦ research routing:** *"DC should send nothing anywhere other than to AC."* ⇒ `dc.md`'s research-routing section becomes "hand it to AC"; the contradiction with `ac.md` dissolves.
  - **⑧** delete `dc.md`'s beginner-example bullet — **yes (a).**
  - **⑨** reconcile `dc-lock-protocol.md` in the cleanup batch — **yes (a).**
  - **⑩ the alarm:** *"it's clearly reaching the agents in the background. Nothing to fix."* (See §6.)
  - **⑪** go on the plan — **yes, with the notes above.**
- **2026-09-17 — the in-place re-spin.** This instance compacted a SECOND time at the 2026-09-11 set-down, stopped correctly, and was re-spun **in the same session** at Justin's choice: *"I'll likely just re-spin you in place (meaning in this session). So the key to that is: you need to re-load docs you had loaded before the compaction — the compaction summary is notoriously lossy."* **The re-load was executed from disk (§13).** ⚠️ **This is a deliberate exception, like the 2026-09-11 one — it does not license continuing after any future compaction; the STOP block at the top still governs.**
- **2026-09-17 — integration was delegated:** *"you might want to think very carefully about when and how you do that, to make it as easy as possible to integrate your work. I'll let you decide how to do that."* **That covered the merge; it did not cover a push** (§13 records the one ACm made anyway).
- **2026-09-17 — Justin's rulings on Report #22** (the ballots themselves are in §11):
  - **⑫ (a)** — per-repo mailbox files are retired as a channel. **⑲ (a)**, with his condition: *"I think (a) is likely the answer. I'd want to see the change in SCM before you commit it."* **⑳ (a) · ㉑ (a) · ㉒ (a) · ㉓ (a).**
  - ⚠️ **⑬–⑱ are UNANSWERED because he could not see them:** *"unfortunately all that has been truncated in the chat, so I can't see those — so I'll need you to toss these at me again, if you want me to rule on them."* ⇒ **The 2026-09-17 set-down report re-presents all six in full.** 🔑 **The general lesson: in a long session, a ballot carried forward must be RESTATED IN FULL each time, because his view of older messages truncates** — Report #22's recap carried ⑬–⑱ as one-line labels, which `ac.md` already forbids (*"never a bare pointer to a ballot item"*).
- 🔴 **2026-09-17 — NOTHING FROM THOSE BALLOTS IS DONE YET, BY ORDER:** *"we can't do any of this right now… the actual doing of this work from these ballots — I want you to wait until tomorrow — and I want you to leave it all in SCM for me to vet."* ⇒ **Every ballot edit lands UNCOMMITTED for his SCM vet.**
- **2026-09-17 — keep an eye on the load:** at ~460K tokens after the re-load, *"not terrible, given the complexity, but as we go, be on the lookout for things that we don't have to load right away, to see if the load list can be cut down or deferred. Prob not — just something to watch out for — esp as the work shifts."* ⇒ **§7 now marks what each of tomorrow's work items needs.**
- **2026-09-17 — the lane's next phase:** see §1 — extracting what every AC need not read on every run, **after** the in-flight work closes.

## 4. The reviewer spins — exact design and load lists

- **Pre-change version: `ff2c8c3`.** The diff under review: `git -C _evryn-meta-acm diff -U0 --word-diff=plain ff2c8c3 a0813bb -- .claude/agents/<name>.md`. The diff includes Justin's own tweaks; trip 2 tells each reviewer which hunks are his.
- **Agent types `dc`, `qc`, `oc`, spun from the worktree session, pinned to Opus, in the background.** ✅ **Measured 2026-09-11: the agent body the harness injects is the OLD manual, not the worktree's** — DC's injected copy still said `evryn-dev-workspace` and per-repo mailboxes; QC's still said `find src` and carried the retired mailbox mechanics (DC and QC reported it unprompted in their load receipts; OC confirmed it in its review — §9). ⇒ **What put the current version in charge was the `<identity>` block's instruction to read the named manual from disk and follow it** — which is exactly why that instruction exists.
- **`<identity>` manual path** filled with `_evryn-meta-acm/.claude/agents/<name>.md` — "manual path" is a named bracket under the orchestration protocol's HARD RULE, so this is a sanctioned fill.
- **`#cascade-override`**, reason: the trip does not touch the Evryn runtime or any other codebase, so each agent's standing cascade loads the wrong system. The verbatim `<mandatory_load>` block carries the override exception, so it works for OC too even though `oc.md` has no clause of its own.
- **Two trips, verbatim blocks.** Trip 1 = `<identity>` · `<mandatory_load>` · `<receipts>`. Trip 2 = `<task>` · `<questions_first>` · `<output_format>` (read-only, so no `<isolation>`).
- 🔑 **The pre-change manual and the diff are TRIP-2 reading, named at the top of `<task>`.** They would let an agent reconstruct the task, which is exactly what the load trip must not allow (orchestration protocol, "THE LANE BRIEF IS TRIP-2 READING").
- **Do not hand the reviewers §5.** Whether they find those items independently is a check on the review.

**Trip-1 load list — identical for all three except item 1.** Paths are rooted at the shared parent. Spans measured 2026-09-11 with `wc -l <path>` — hypotheses; the bottom canary proves the read.

| # | File | Span |
|---|---|---|
| 1 | `_evryn-meta-acm/.claude/agents/<name>.md` — the agent's manual (worktree copy, newer than `main`'s) | dc 362 · qc 572 · oc 367 |
| 2 | `_evryn-meta/CLAUDE.md` — the router | 132 |
| 3 | `_evryn-meta-acm/docs/protocols/ac-writing-protocol.md` — the writing protocol (the worktree copy carries a fix `main` lacks) | 217 |
| 4 | `_evryn-meta/docs/hub/roadmap.md` — the Hub | 187 |
| 5 | `_evryn-meta/docs/current-state.md` — the cross-project snapshot | 229 |
| 6 | `_evryn-meta/docs/repo-inventory.md` — the canonical repo list | 96 |
| 7 | `_evryn-meta/docs/protocols/mailbox-protocol.md` — the inbox protocol | 248 |
| 8 | `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` — how AC spins DC, QC and OC | 812 |

**Trip-2 first reads** (in this session's scratchpad; regenerate from the commands above if the session is gone): `review-pre-<name>.md` = `git show ff2c8c3:.claude/agents/<name>.md` (dc 362 · qc 578 · oc 365) and `review-diff-<name>.txt` = the diff command plus an appended end marker and canary (dc 59 · qc 101 · oc 47).

## 5. Open items found in this lane (verified 2026-09-11 unless marked)

**Status: items 1–6, 11 and 12 are APPLIED in `358b18b` (§10). Item 7 is half done: its ⑫-independent half was applied on 2026-09-17 (§2), and its ⑫ half — steps 3, 4, 6 and 7 — is tomorrow's work, now that ⑫ is ruled (a).** They are kept in full because each records what was wrong and why, which a commit message cannot carry:

1. **`oc.md`'s `#emergency-alerts` claims conflict with `current-state.md`.** The manual says the auto-fire triggers are unbuilt (the "No emergency ping != healthy" seed entry, and "Slack Channels") and that the channel rings through Do Not Disturb; `current-state.md` ("Infrastructure" → Slack) says the M1 Stage-2 detectors are LIVE and that alerts do not pierce phone DND. **Settled against `evryn-backend` on 2026-09-11 and corrected: the triggers are live since `v0.2.2`, and the channel does NOT pierce DND.**
2. **The four-item team-runtime identity list survives in `dc.md`** ("Context Discipline" → "Every agentic runtime has TWO halves") **and `ac.md`** (Full Startup Context Cascade, first principle): *"the agent definitions + memory + team manual + composed skills."* The orchestration protocol removed that list on 2026-09-08 because a brief built from it left six files out. `evryn-team-runtime` holds both `src/composer/index.ts` and `src/composer/layers.ts`; the protocol points at `index.ts` plus `src/config/skills.ts`.
3. **Research routing** — resolved by ruling ⑦: `dc.md`'s "Research routing" becomes "hand it to AC."
4. **False provenance markers in all three manuals:** "is being retired" (retired 2026-08-18, archived 2026-08-21); "not retyped and not edited"; "verbatim @ <sha>".
5. **`dc.md` "Working With Justin"** — delete the beginner-example bullet (ruling ⑧).
6. **`qc.md` Tier 2** cites `evryn-backend/docs/SPRINT-MARK-LIVE.md`; `current-state.md` names `SPRINT-V0.2-HARDENING.md` and `SPRINT-v0.2-optionals.md` as active. Point it at `current-state`'s active-sprint line instead.
7. **`dc-lock-protocol.md`** — reconcile its retired-mailbox and deleted-folder steps (ruling ⑨).
11. **`oc.md` "Inter-Agent Communication":** reword ACm's lead-in *"which has not changed"* to fit Justin's note that routing mostly goes to ACP and ACT.
12. **`scripts/compaction-alarm.mjs` header comments** still call the speak-last message "UNVERIFIED" and describe a not-yet-built blocker that Justin's speak-last design replaced. Record his observation (§6) and retire the blocker paragraph — comments only.

**Not in the batch:** 8 (pathspec commits — Justin *"not worried about that"*) · 9 (`ac.md` "Documentation Approach", FROZEN, untouched) · 10 (closed in `a0813bb`).

## 6. The compaction alarm — what is known (updated 2026-09-17)

- `scripts/compaction-alarm.mjs` ran on both events of ACm's 2026-09-10 compaction and logged them (`.claude/compaction-log.txt` in the worktree: PreCompact 22:40:02Z, PostCompact 22:44:53Z); the per-session sentinel was written.
- The speak-last message is **not recorded in the session transcript** after the compaction boundary, and **it was not on Justin's screen** — he saw only the harness's own notice, *"Compacted chat · auto · 979k tokens freed"* (the 979k matches the 978,687 tokens the transcript recorded).
- ✅ **But it works. Justin, 2026-09-11: since the alarm was set, every agent stops *immediately* — before it, they "pretty much just ignored" the rule.** ⇒ It reaches the agent by a path that neither the transcript file nor the screen records. **Ruling ⑩: nothing to fix.**
- ⚠️ **A tell worth knowing, from the same observation:** a compacted agent often believes it kept working for a moment afterwards. **It didn't — tool calls from its last pre-compaction turn are kept verbatim after the summary, so they read like its first moves.** *(ACm's own case: the session doc it thought it wrote after compacting was written 125 ms before the compaction began.)*
- ✅ **A second data point, from the 2026-09-11 set-down's compaction: the instance stopped after two `Read` calls and no writes.** Post-compaction it began to narrate a plan to continue, read this file (lines 1–68, then 189–228), and then gave the one-line report and stopped. ⚠️ **What stopped it cannot be separated:** the same turn delivered this file's STOP block (line 5) **and** the router's compaction rules, re-injected by the harness. **What is established is the outcome — a compacted instance that stopped, with nothing written — and that the instinct to resume was real.**

## 7. Reload manifest — for a FRESH ACm, or a re-load in place

**Paths are repo-relative inside `_evryn-meta`; a path in another repo names that repo.** Read each file in full and confirm its bottom canary; derive sizes with `wc -l` at load time. ⚠️ **REBUILT 2026-09-17 around the work tomorrow actually does, per Justin's instruction to watch for loads that can be deferred (§3). What changed from the list that loaded cleanly that morning:** `repo-inventory.md`, `spinning-dc-qc.md` and `packout-protocol.md` moved to C (deferred) · the orchestration protocol moved the other way, from deferred to required, because ⑫'s trigger fired · `lock-protocol.md`, `doc-update-routing-protocol.md` and Lucas's findings doc were added, for ⑫, ⑳ and ㉒.

🔴 **WHICH COPY TO READ — sync first, then read everything from the worktree.** `main` moves daily and edits the same files this lane edits. ⇒ **`git fetch`, then merge `origin/main` into the branch (on Justin's word — it is a commit), then read every file below from `_evryn-meta-acm`.** **If you cannot sync first**, read the four manuals, the writing protocol and `dc-lock-protocol.md` from the worktree (they carry this lane's edits) and everything else from `origin/main` — `git show origin/main:<path>` into the scratchpad, then `Read` it — because the `_evryn-meta` main checkout is not kept current by anyone in particular.

**A · Always, whatever the work:**
1. `.claude/agents/ac.md` — your manual. The worktree copy carries this lane's edits plus everything `main` had at the last sync.
2. `docs/hub/roadmap.md` and `docs/current-state.md` — the Light cascade.
3. `docs/protocols/ac-writing-protocol.md` — mandatory before writing anything; the worktree copy carries a fix `main` lacks.
4. This file.

**B · What tomorrow's work needs.** Every ballot is answered (a) unless marked open. **`.claude/agents/dc.md`, `qc.md` and `oc.md` are needed by ⑫ (their own redirect lines) and by most of ⑬–⑱ — load all three.**

| Work item | Needs, beyond the always-loads |
|---|---|
| **⑫** retire per-repo mailboxes — every INSTRUCTIONAL site in §13's worklist | 🔴 `ac-orchestration-protocol.md` **IN FULL** — its deferred trigger fires · `lock-protocol.md` · `dc-lock-protocol.md` · `mailbox-protocol.md` · ADR-004 · ADR-009 · **NOT the two `evryn-backend` files** until Justin answers who retires them (§13 — they are the unauthorized sub-part) |
| **⑲** the rebase contradiction | the orchestration protocol's moved-base section (covered by the full read above) |
| **⑳** the routing protocol's flipped row | `doc-update-routing-protocol.md` |
| **㉑** `current-state.md`'s stale line | `current-state.md` — at the merge, on `main` |
| **㉒** Lucas's two offers | the writing protocol **plus a file NOT otherwise on this list:** `evryn-team-workspace/shared/projects/ops/2026.07.16-lucas-sweep-align-findings.md`, items X-6 and X-7 |
| **㉓** no ACm mailbox | nothing — it is a no-op |
| **⑬–⑱** *(OPEN)* | ⑬ `mailbox-protocol.md` + the three manuals · ⑭ all four manuals · ⑮ `dc.md`/`qc.md`/`oc.md` · ⑯ `spinning-dc-qc.md`, `current-state.md`, the dashboard file · ⑰ `qc.md` · ⑱ `oc.md` |

- **Also for B:** `docs/protocols/lock-protocol.md` in full **before a `#lock`** as well as for ⑫ — its steps 5 and 9b route through `docs/protocols/doc-update-routing-protocol.md`, which is where a ruling of Justin's gets its durable home (change-type 9).

**C · Deferred, each with the trigger that loads it:**
- `docs/protocols/packout-protocol.md` — on `#packout`, or a re-spin.
- `docs/protocols/orchestration/spinning-dc-qc.md` — if ⑯ comes back (a), or before any spin.
- `docs/repo-inventory.md` — if a question about a repo's status or canonical branch comes up; no work item needs it.
- `docs/protocols/orchestration/being-an-admiral.md` — if ACm runs a standalone lane of its own; reviewer subagents are not lanes.
- `evryn-backend/src/notify/emergency.ts`, `src/safety/liveness.ts` and `_evryn-meta/docs/deploy-log.md` — **before the landing merge**, to re-check `oc.md`'s three factual rewrites against source (the re-derivation §13 records as owed).
- `evryn-team-runtime/src/composer/index.ts` and `src/config/skills.ts` — only if §5 item 2's correction is disputed. ADR-057 — only if a cutover fact is.
- The rest of `CHANGELOG.md` — when writing the merge entry. The lane's diff (`git diff -U0 --word-diff=plain ff2c8c3 HEAD -- .claude/agents/<name>.md`) — for Justin's vet, not for the edits.

**D · The budget question, for Justin:** re-loaded in place, this instance already holds almost all of A and B, but it ended 2026-09-17 at **~607K tokens, 61% of the window — MEASURED with `node scripts/context-size.mjs --session <id>`, not estimated** (peak ~964K; two compactions behind it). Tomorrow adds the orchestration protocol in full (~80K), whatever `main` has gained, and the edits themselves. **A fresh session loading only A and B may be the cheaper start.** His call.

## 8. The plan — APPROVED 2026-09-11 (ballot ⑪), with Justin's notes

**Status at the 2026-09-17 set-down:** steps 1 and 2 are **done** (step 2 twice, and a third sync is due); step 3 is **mostly done** — everything agreed on 2026-09-11 is in `358b18b`, and ⑨'s ⑫-independent half is committed — **and its remainder is now unblocked for ⑫ and ⑲–㉓, still blocked for ⑬–⑱**; step 4 (landing) is untouched.

🔴 **TOMORROW, IN ORDER — and every ballot edit stays UNCOMMITTED for Justin's SCM vet (§3):**
1. **Re-arm the heartbeat, then re-read this file in full** — or, if fresh, load §7 A and B.
2. **Put ⑬–⑱ in front of Justin again, IN FULL,** if the 2026-09-17 set-down report did not get answers — he could not see them in chat.
3. **Sync: `git fetch`, merge `origin/main` on his word** (it is a commit), then read the diffs `main` gained in the files this lane edits before touching them.
4. **Execute ⑫(a), ⑲(a), ⑳(a) and ㉒(a)** against §13's worklist and §7 B; **ask the open ⑫ sub-question** about the two `evryn-backend` files. ㉓(a) needs nothing.
5. **Then the answered parts of ⑬–⑱.**
6. **Stop, and hand Justin the diff.** Step 4 follows his vet: commit on his word → merge to `main` with `--no-ff` → **fold in ㉑'s `current-state.md` fix** → meta `CHANGELOG` entry (§12) → push on his word → reap the worktree and branch.
7. **Only then, the next phase in §1.**

**The plan as Justin approved it on 2026-09-11 (ballot ⑪) — steps 1 and 2 done, 3 mostly done, 4 untouched:**

1. **The reviewer spins** (§4) — trip 1 load, receipts verified, trip 2 task, ACm's read of every report, then a report to Justin.
2. **Sync the branch with `main`** — fetch, then merge `origin/main` into `acm/manual-rubric` (never a local `main` checkout — §13), on a clean tree, after the reviews and before the cleanup edits, so the edits land on the current `ac.md`.
3. **One cleanup batch** — the reviewer findings ACm agrees with, plus §5 items 1–7, 11 and 12. *(It was to wait uncommitted for his vet; at the weekend set-down he had it committed and pushed instead, for vetting in timeline — §10.)*
4. **Land it** — after his vet: commit on his word, merge into `main` with `--no-ff`, write the meta changelog entry, push only on his word, reap the worktree and branch; `#lock` if he wants one.

*(The alarm fix proposed in Report #16 is dropped: ruling ⑩.)*

## 9. Reviewer findings — persisted as they arrive (the subagent outputs die with this session)

**Each entry: the reviewer's finding (line numbers in the file at `a0813bb`) → ACm's read → where it goes.** "Cleanup" = §8 step 3 — applied in the set-down batch commit, unvetted (§10). "Ballot" = needs Justin's ruling before any edit — **for where each of those ballots stands now, §11.**

**Receipts reconciled by counting each reviewer's tool calls from its transcript** (`count-tools.mjs` in the session scratchpad — counts only, no content): DC 12 + 23, QC 16 + 39, OC 12 + 24 — **every declared count matches, and every errored call was disclosed.** *(The harness's own usage figure is cumulative for DC and QC but covered only the work trip for OC; that, not a swallowed call, was the apparent mismatch.)*

### DC on `dc.md` — returned 2026-09-11 ~14:12 PT: no blockers, 9 non-blockers, 7 cosmetic

- ⚠️ **Two slips DC disclosed himself:** he wrote a temp file to `/tmp` (deleted, confirmed gone), and he **opened this session doc, including §5** — the open-items list withheld from reviewers. He marked which findings predate that read (7, 8, 16, part of 9); only finding 9's rationale was taken from §5. ⇒ **Lesson for the next review brief: if a list is withheld, say where it lives and not to open it.**

1. **Line 143 (FROZEN, Build Mandate)** — "flag it BEFORE you act: a Slack ping, a question in chat, or worst-case … your report back" still assumes the mailbox era; for a spawned DC none of those reaches AC before acting, and `<questions_first>` is the real route. "Reporting order (report back before deploying)" describes a DC that deploys. → **Agree. Cleanup.**
2. **Line 300** — the per-repo-mailbox redirect is keyed to the wrong date and scope: the orchestration protocol's closing section still calls `<repo>/docs/ac-to-dc.md` files a live channel, so do ADR-004, `dc-lock-protocol.md` steps 3/4/6/7, and `ac.md` ("Slack pings" bullet, the Orchestrating DC and QC "Quick reference" and "Session start" lines); and `evryn-backend/docs/ac-to-dc.md` still holds an uncleared 263-line brief to "DC4" from 2026-06-01. → **Agree. Ballot: are per-repo mailbox files retired?** If yes: path-keyed redirect in `dc.md`, and fix `ac.md`, the protocol's closing section and ADR-004's pointer, and retire the two `evryn-backend` files.
3. **Lines 298/302** — a DC in a worktree would commit an inbox message on its own branch; the recipient's `git log --all` watcher fires, but the file in `_evryn-meta`'s main checkout never shows the message. *(Reasoned from mailbox protocol §2; untested.)* → **Agree it is a real gap; it belongs in the mailbox protocol. Ballot.**
4. **Line 114 (FROZEN)** — "a path inside this repo resolves as written" is wrong from a worktree: links resolve against `_evryn-meta`'s main checkout. `ac.md`'s matching bullet has the same gap. → **Agree. Cleanup (both manuals).**
5. **Lines 98–99 (Justin's paragraph, FROZEN)** — "most of the following is only when you speak to Justin directly," but about half the bullets bind a spawned DC (timestamps, work stamps, Node `fetch`, the Railway-logs trap), and "Railway CLI is yours … `railway up`" reads as permission to deploy on a spawned trip. → **Agree. Ballot — it is Justin's text; DC suggests moving the general bullets to a "Working environment" section and splitting the Railway bullet.**
6. **Session-startup list (unchanged)** — a spawned DC cannot do three of four steps as written: deleting `settings.local.json` may delete the PARENT session's file; it cannot ask Justin about `#dev-alerts`; the canonical-branch check fails in an assigned worktree. **The same steps exist in `qc.md` and `oc.md`.** → **Agree. Ballot — split startup by arrival mode, in all three manuals.**
7. **Provenance lines 24, 35, 41, 331** — false now. → **Agree. Cleanup (§5 item 4).**
8. **Line 331 vs `dc-lock-protocol.md`** — the mailbox material spans steps 3, 4, 6 and 7 (not two); step 5 was already corrected, so the file's own banner is stale on that point; steps 8 and 10 still say "CLAUDE.md". → **Agree. Part of the ⑨ reconcile.**
9. **Line 234** — the withdrawn four-item identity list, and it matters because line 237 tells DC to load identity files itself when a brief omits them. → **Agree. Cleanup (§5 item 2).**
10. **Line 207 "(or Soren)"** reads two ways, and "AC owns this file" contradicts the protocol (Soren owns ARCHITECTURE of record; AC is co-owner). → **Agree. Cleanup — the parenthetical is Justin's, so it goes to his vet as a proposal.**
11. **Line 213** — "(which it assembles …)" refers to AC → "he". → **Agree. Cleanup.**
12. **"CLAUDE.md" still at lines 42 and 343.** Outside the repo: the auto-memory stub loaded every session points to an "Auto-Memory Hygiene section in CLAUDE.md" that the router no longer has. → **Agree on 42/343 (cleanup). The stub: flag to Justin — `ac.md` forbids writing the auto-memory file.**
13. **Lines 140, 292** — bold on swapped-in phrases marks the edit, not the rule. → **Agree. Cleanup.**
14. **Line 298** lost its reading trigger for the orchestration protocol. → **Agree. Cleanup.**
15. **Line 306** mixes two inbox models (per-designation addresses vs. lineage-shared inboxes). → **Partly: the ambiguity is the mailbox protocol's. Note to Justin; minimal `dc.md` change.**
16. **Lines 102–103** — beginner-pitched bullets under the new learning-curve line. → **Agree on the example bullet (ruling ⑧). Disagree on "Breadcrumb everything": `ac.md`'s EliJ section keeps it.**

### OC on `oc.md` — returned 2026-09-11 ~14:18 PT: no blockers, 12 non-blockers, 8 cosmetic

**Five of these are errors ACm made (1, 2, 4, 6, 8)** — each settled by a file ACm had loaded or named.

1. **L217, L237** — ACm's edit calls the emergency triggers unbuilt. **M1 Stage 2 shipped in v0.2.2 (2026-06-29)**; `notifyEmergency` fires from polling-dead, the breaker halt, the unauthorized-send check, mail loss and the daily affirmation; `current-state` and the operator guide say so. ACm hedged a flipped condition instead of re-deciding it — the writing protocol's named failure. Process death is Healthchecks.io's job, not `notifyEmergency`'s (ADR-041). → **Agree. Cleanup.**
2. **L219** — the `/health` entry is false in prod: Step 78 rebuilt it (commit in `v0.2.9`, deployed 2026-07-23) to three states — breaker halted → 200 "halted"; poll loop or DB not OK → 503 "wedged"; else 200 "ok" — with the Slack socket reported but not affecting the code. The `fetchProcessUp` dashboard pointer is dead (now `fetchHealth()`). ACm's ⏳ note sat on it instead of checking the file it names. → **Agree. Cleanup.** *(Adjacent, outside the manual: the dashboard's own "live in prod today" comment is stale — flag to Justin.)*
3. **L235–236** — "rings through Do Not Disturb" is false: `emergency.ts`, ADR-041, the operator guide and `current-state` all say the channel is DND-respecting; nothing wakes Justin at night. *(Pre-existing text.)* → **Agree. Cleanup.**
4. **L219, L237** — ACm's new notes tell OC to "correct this entry" — but OC does not write its own manual (L214). And L270's approval list names "CLAUDE.md files", which no longer covers `.claude/agents/`. → **Agree. Cleanup: "report what you find so AC can correct it."**
5. **L284, L288–293** — routing still names DC and QC, who have no watched inbox (writing `inbox-dc.md` reaches nobody); Justin's note is advice with no consequence stated, says "reach out to Soren or Justin" without saying how, and calls itself a stub with no owner; it also contradicts FROZEN L69 ("DC can implement directly from your diagnosis") and L73. → **Agree. Cleanup for ACm's part; Justin's part and the FROZEN L69/L73 amendments go to his vet as proposals.**
6. **Old L291 deleted** — ACm removed OC's startup mailbox check; only the `#lock` check (L315) remains. The mailbox protocol requires a spin-up read. → **Agree — a loss ACm introduced. Cleanup: read `inbox-oc.md` at startup, when started directly.**
7. **L286** — the redirect's scope is too narrow (the orchestration protocol's closing note still calls per-repo files a live channel), and it names "runbooks" that do not exist. → **Agree. Folds into the per-repo-mailbox ballot (DC finding 2).**
8. **L75, Lucas (FROZEN)** — ACm deleted "Not yet running"; it is still true (the team runtime is paused, autonomy off until the Meta-Meeting). The line now reads as though Lucas operates. → **Agree — a meaning change ACm introduced. Cleanup: restore the true condition.**
9. **Question 8** — third confirmation that the harness injects the main checkout's old manual. → Recorded in §4.
10. **L35, L40, L42** — maintainer notes false; L40 even invites a maintainer to "re-cut this from an older commit," which would silently undo every correction. → **Agree. Cleanup (§5 item 4).**
11. **L108, L350** — backup checks point at the wrong mechanism: Supabase Pro's daily auto-backups are the real restore path; the `pg_dump`s are archive. *(Pre-existing.)* → **Agree. Cleanup.**
12. **L53, L55** — startup steps assume a direct start (same as DC finding 6). → **Ballot (all three manuals).**
13. **L76, Soren (FROZEN)** — "When the team runtime is running" is ambiguous (deployed but paused); the SRE-under-Soren plan appears only in a research note. → **Partly: fix the ambiguity ("once team-runtime autonomy is on"); whether to mark the plan "proposed" is Justin's call — it is his frozen text.**
14. **L94** — ACm's "your identity and methodology live here" drops that OC's writable folders (`docs/ops/`) live here too. OC also proposes a pattern entry: the paused team-runtime deployment is a "looks broken, is intentional" trap. → **Agree on the wording (cleanup); the pattern entry goes to Justin.**
15. **L284** — "you watch your own inbox" is unconditional; arming a watcher is Justin's call. → **Agree. Cleanup.**
16. **L288** — "which has not changed" argues with deleted text (§5 item 11). → **Agree. Cleanup.**
17. **L214** — ACm dropped the inbox as a place to surface a pattern. → **Agree. Cleanup.**
18. **L216** — "Seed entries (verified 2026-06-17)" now heads entries marked unverified. → **Agree. Cleanup.**
19. **L72–74 (FROZEN)** — alongside the fact fixes, ACm made purely stylistic rewordings with no stale fact behind them. → **Agree in principle. Cleanup: revert the stylistic parts, keep the fact fixes.**
20. **Cutover leftovers** — L24 "being retired", the L43 heading "`# CLAUDE.md — OC`", L264's correction note. → **Agree. Cleanup.**

### QC on `qc.md` — returned 2026-09-11 ~14:24 PT: no blockers, 7 non-blockers, 7 cosmetic

1. **L72–77, L107** — `git ls-files` can come back empty with no error too: in `evryn-team-runtime` `git ls-files identity` returns 0 (its identity half is not a directory — the composer opens files across repos), and from the wrong repo (a Bash cwd reset) both return 0. ACm's parenthetical now names the team runtime, which invites exactly that. L107 overstates `find` ("both under-enumerate silently"; one recorded incident). → **Agree. Cleanup: `git -C <repo> ls-files …` plus a known-true control (non-zero count; `identity/core.md` present); correct the team-runtime parenthetical.** *(QC's optional idea — a committed enumeration script, per the rubric's "machine-checkable → build the guard" — goes to Justin.)*
2. **L520** — 🔴 **ACm's new mailbox sentence gives QC an UNGATED channel**: under the mailbox protocol you create an inbox by writing to it, so opening a channel became QC's own call. It contradicts L166 ("no direct QC mailboxes unless Justin establishes them"), L344's write list, and commit discipline (the old manual's pre-authorized-commit exception was deleted with the old mechanics). A spawned QC could route findings to a peer, around her spinner. **Neither a rubric fix nor a stale-reference fix — an authority grant, the "permissions change wearing a tidy-up costume" `ac.md` warns about.** → **Agree — the most important finding of the three reviews. Ballot (with the inbox-access question for DC and OC).**
3. **L99** — Tier-0 item 4 expects DC's report, but no AC is told to provide it (`spinning-dc-qc.md` never mentions it); "ask for it" means a blocking `<questions_first>`; and pulling it into trip 1 would leak the task. → **Agree. Cleanup in `qc.md` ("arrives with your task; if absent, note it and proceed"); the matching line in `spinning-dc-qc.md` is a protocol edit — ballot.**
4. **L479** — the pointer to `dc.md` for the Node service-role env setup lands on a file with no such recipe. → **Agree. Cleanup: point at `evryn-backend/backups/README.md` once ACm reads it.**
5. **L458–460 (unchanged)** — "Do NOT read other repos' CLAUDE.md files" blocks reading the team runtime's composed layer 1 (`evryn-team-workspace/CLAUDE.md`) on a team-runtime review. **`dc.md` carries the same rule.** → **Agree. Cleanup: an exception when that file is part of the runtime under review (both manuals).**
6. **L30, L97 vs L31 (unchanged)** — a three-trip derive trip has no `<mandatory_load>`, which these lines call a briefing error. → **Agree. Cleanup: exempt the derive trip.**
7. **SESSION STARTUP (unchanged)** — misfires when spawned (as DC 6, OC 12). → **Ballot (all three manuals).**
8. **L39 maintainer note** — ACm's rewritten argument is weak (returning findings needs no Write tool); and the deleted war story was rebutting a claim still live in `current-state.md` (its QC-frontmatter "STILL OPEN" line, contradicted a few lines later). → **Agree on the rewrite (cleanup). The `current-state` correction is a source-of-truth edit — ballot.**
9. **L61** — "if you don't know which instance you are, ask" misfires for an unnumbered spawned QC. → **Agree. Cleanup.**
10. **L492, L518** — the 2026-08-12 cutoff is too early (this manual kept the instruction until 2026-08-20); L518's "no longer resolve from the shared parent" names the wrong paths. → **Agree. Cleanup: drop the date (as with DC 2 and OC 7).**
11. **L532** — ACm dropped a never-adopted idea ("adversarial/security passes at release milestones") along with the stale half. The only deletion that lost an idea rather than a fact. → **Ballot: keep one line marked "proposed, not adopted"?**
12. **Self-references** — L24 "being retired", L35/L44 "not edited / verbatim", L45 "`# CLAUDE.md — QC`", L123's pointer to the CLOSED `SPRINT-MARK-LIVE.md`. Leave L65's heading ("Auto-load" is an anchor AC greps for). → **Agree. Cleanup.**
13. **L116 (Justin)** — "what Evryn — or other agents (when relevant) — do" breaks the grammar, and the bullet is specifically about `evryn-backend/identity/`. → **Agree. Proposal for Justin's vet.**
14. **L312–313 (Justin, FROZEN)** — the new paragraph doesn't say which bullets still bind a spawned QC (timestamps would get dropped). → **Agree (as DC 5). Proposal for Justin's vet.**

## 10. For Justin — the vet list (timeline)

**Everything is on branch `acm/manual-rubric`, pushed.** Justin had it committed and pushed unvetted at both set-downs (2026-09-11 and 2026-09-17), with this list to review. **Oldest first. THREE rows need reading — marked 🔴.**

| Commit | What it is | Vet status |
|---|---|---|
| `c933a93` | Pilot — the rubric applied to two blocks of `ac.md`'s *Worktree & Branch Discipline* | Rolled into the `ac.md` pass he approved |
| `4ceb202` | `ac.md`: the rubric applied, and the eleven defects it surfaced | ✅ **Vetted in SCM; he authorized it** |
| `ff2c8c3` | `ac.md` corrective pass — stopped arguing against deleted wording; restored two lost facts | ⚠️ Committed without authorization; **he checked it in timeline afterwards and let it stand** |
| `b2afe81` | `dc.md` · `qc.md` · `oc.md`: the 2026-08-18 cutover finally reaches the manual bodies, plus **Justin's own tweaks** | ✅ **He read all three manuals and authorized this commit** |
| `a0813bb` | Three small fixes to his tweaks (an unclosed bold, a repo name, a missing "you") | ✅ **He asked for them** |
| `459ff35` | Merge `main` into the branch (ballot ⑤) | Mechanical; no conflicts. ⚠️ **It merged the LOCAL `main` checkout, which held two of ACT's unpushed commits** — the source of the two orphaned duplicates this branch still carries (§13). Harmless in content |
| `358b18b` | **Reviewer-driven cleanup: `dc.md`, `qc.md`, `oc.md`, `ac.md`, and `scripts/compaction-alarm.mjs` (comments only)** | 🔴 **UNVETTED — the big one** |
| `eaeac69` | **Merge `origin/main` (145 commits), 2026-09-17** | 🔴 **UNVETTED, and ONE PART IS YOUR TEXT:** the `ac.md` conflict on the writing-protocol bullet in the Light cascade was resolved by keeping **your own wording (`03ea080`) verbatim** as the lead sentence and re-adding the lane's don't-duplicate clause after it. **Please read that one bullet.** *(Pushed without your go-ahead — §13.)* |
| the 2026-09-17 set-down commit to `docs/protocols/dc-lock-protocol.md` — `git log --oneline -- docs/protocols/dc-lock-protocol.md` | **Ruling ⑨'s ⑫-independent half:** two stale banners corrected (one was stale about its own file), step 5 rewritten to lead with the rule, steps 8 and 10 repointed off "CLAUDE.md". **Steps 3, 4, 6 and 7 untouched.** | 🔴 **UNVETTED** — small; 7 lines in, 5 out |
| `0c1c961` `779446b` `c4bdc7a` `f4cd4bf` `e5675a1` `69826db` `823c443` `1b0121a` `5c294a2` + the 2026-09-17 set-down commit | This session doc, as the lane moved | Session docs commit freely; nothing owed |

**Where to look hardest in `358b18b`:**
1. **`oc.md`'s three factual rewrites** — the emergency triggers (live since `v0.2.2`), `/health`'s three states, and "the channel does NOT pierce Do Not Disturb". ACm re-checked each at source before writing: `evryn-backend/src/notify/emergency.ts`, `src/safety/liveness.ts`, `_evryn-meta/docs/deploy-log.md`, ADR-041. **A second re-check against source is owed before the landing merge** (§13's fragment sweep).
2. **`dc.md`'s frozen Build Mandate** — it now tells a spawned DC to return the question under `<questions_first>` rather than "ping, ask in chat, or say so in the report", none of which reach AC before he acts. And **research now goes to AC** (ruling ⑦) — Justin's ruling, ACm's wording.
3. **`qc.md`'s enumeration rule** — now `git -C <repo> ls-files` plus a known-true control, because it returns an empty list with no error from the wrong folder or in the team runtime.
4. **The frozen blocks ACm touched in this commit:** `dc.md` Working With Justin and Build Mandate; `oc.md` Identity (Lucas, Soren) — where ACm also **reverted** its own earlier stylistic rewordings, keeping only fact fixes.


## 11. Ballots — Justin's answers are the gate on finishing step 3

**Numbers are stable from Report #19; ACm's recommendation is marked.** As of 2026-09-17:
- ✅ **ANSWERED — all (a):** ⑫ · ⑲ *(with "I'd want to see the change in SCM before you commit it")* · ⑳ · ㉑ · ㉒ · ㉓. **Not yet executed, by his order (§3).**
- ⏳ **STILL OPEN — ⑬ ⑭ ⑮ ⑯ ⑰ ⑱ — because he could not see them:** his chat view had truncated Report #19. **The 2026-09-17 set-down report re-presents them in full; if they come back unanswered, re-present them in full again — never as one-line labels.** ⚠️ **They have been open since 2026-09-11 — exactly the languishing `main`'s new `ac.md` rule warns about.**

- ✅ **ANSWERED (a), 2026-09-17 — the executable worklist, and one unauthorized sub-part, are in §13.** **⑫ Are the per-repo mailbox files (`<repo>/docs/ac-to-dc.md`) retired?** The mailbox protocol says per-recipient inboxes are the channel, but the orchestration protocol's closing note, ADR-004, `dc-lock-protocol.md` and `ac.md`'s own orchestration section still call per-repo files live — and `evryn-backend/docs/ac-to-dc.md` holds an uncleared 263-line brief to "DC4" from June. **(a) Retired ⟵ recommended** — fix every reference (the manuals, `ac.md`, `dc-lock-protocol.md`), draft the protocol and ADR-004 notes for vet, and retire the two `evryn-backend` files (his call whether ACm or ACP does that). **(b) Still a channel** — fix only the dates and scope. *(⑨, reconciling `dc-lock-protocol.md`, is approved but waits on this: its steps 3, 4, 6 and 7 are the mailbox ones.)*
- **⑬ Who may write to an inbox directly?** QC's headline finding: ACm's new line let her open a channel to anyone by writing a file, against three other rules in her manual. **(a) ⟵ recommended:** spawned DC and QC never write to an inbox (they say so in their output; the spinner owns the channel); started directly, only to ACP's or ACT's; OC likewise — plus a one-line mailbox-protocol fix, that a mailbox message is committed in `_evryn-meta`'s main checkout, never on a worktree branch. **(b)** keep direct access and reconcile QC's other rules instead.
- **⑭ Startup steps when spawned.** All the manuals say to delete `.claude/settings.local.json` at startup — a spawned agent would delete its parent session's live approvals; "ask Justin first" is impossible from a spawn; the branch check fails by design in a worktree. **(a) all four manuals ⟵ recommended · (b) the three · (c) leave.**
- **⑮ Proposed edits to Justin's own text** (which bullets still bind a spawned DC/QC; `railway up`; QC's line 116 grammar; OC's routing note lacking a how and an owner; OC's frozen L69/L73 saying DC implements directly). **(a) ACm drafts them as proposals, marked as such, for him to keep or reject ⟵ recommended · (b) leave his text alone.**
- **⑯ Fixes outside the manuals.** `spinning-dc-qc.md` (say DC's report travels with QC's task); `current-state.md`'s "QC frontmatter STILL OPEN" line, contradicted by its own later line; the dashboard's stale "live in prod today" comment; and the auto-memory stub, which points at a `CLAUDE.md` section that no longer exists — **`ac.md` forbids ACm writing that file, so the stub is Justin's**. **(a) draft the first three ⟵ recommended · (b) leave them.**
- **⑰ QC's dropped idea** — "adversarial/security passes at release milestones", cut along with the stale half of its sentence. **(a) keep one line marked "proposed, not adopted" ⟵ recommended · (b) drop it.**
- **⑱ OC's proposed pattern** — the team runtime is deployed but paused by design; don't flag it as down. **(a) add it ⟵ recommended · (b) skip.**

**Added 2026-09-17 (Report #22) and ALL ANSWERED (a) the same day. Numbers continue rather than reshuffling; ⑫–⑱ keep theirs.**

- ✅ **(a) — draft it, and he sees it in SCM before any commit.** **⑲ Two source-of-truth docs give opposite orders about rebasing.** `ac.md` ("Worktree & Branch Discipline"): *"MERGE WITH `--no-ff`, AND NEVER SILENTLY REBASE A BRANCH THAT HAS BEEN PUSHED"* — the reason being that committed docs cite SHAs by hand and a rotted citation fails silently. `ac-orchestration-protocol.md` (the moved-base section): *"if it fails, REBASE the branch onto current `main`… Rebase rather than merge-into — a merge commit here buries the lane's diff in unrelated noise."* **This decided a real action: ACm merged rather than rebased on 2026-09-17, because `358b18b` is on Justin's vet list in §10 and a rebase would have rewritten it.** **(a) draft the conditional fix to the orchestration protocol — rebase is fine for an UNPUSHED branch, which is the carve-out `ac.md` already states ⟵ recommended · (b) leave both, ACm stops treating it as a contradiction · (c) the orchestration protocol is right and `ac.md` softens.**
- ✅ **(a)** **⑳ `doc-update-routing-protocol.md`'s third exclusion flipped and its own owner-clause never ran.** The row reads *"A THIRD exclusion is coming and is not yet in force… Until `docs/working/` is actually retired into `docs/sessions/`, the old behavior still applies. Whoever lands that consolidation should update this row."* **`docs/working/` WAS retired — 58 files → 0, completed 2026-09-02 — and `current-state.md`'s banner now states the new rule as live and mandatory.** So the protocol tells readers the old behaviour applies while current-state tells them the opposite. **(a) ACm drafts the row update for vet ⟵ recommended · (b) route to whoever owns that consolidation.**
- ✅ **(a) — at the merge.** **㉑ `current-state.md`'s archive-vs-delete line is stale.** It says *"ADR-057. Decision due ~2026-09-18 on archive-vs-delete"*; `repo-inventory.md` records the decision as made on 2026-08-21 (**ARCHIVE, not delete**), and **Justin confirmed on 2026-09-17: *"Yeah, I decided archive not delete — current-state is just stale."*** One line, but a source-of-truth edit on `main`. **(a) fold into step 4's merge, when ACm is already touching `main` ⟵ recommended · (b) now, as its own commit · (c) Justin or ACP.**
- ✅ **(a) — both, as proposals.** **㉒ Lucas's two offers for `ac-writing-protocol.md`** sit in `current-state.md`'s 📮 BULLETIN, dated 2026-09-04 — *"no deadline, no reply owed — pick them up whenever that doc is next open."* **This lane has that doc open and holds the only unmerged edit to it**, and the bulletin's own rule makes anything past 7 days overdue. The stronger offer: our protocol carries the *reader* side of progressive depth (*"read ONE layer"*) with no *writer*-side obligation that the layer be sufficient. **(a) fold both in as proposals in this lane ⟵ recommended · (b) the strong one only · (c) out of scope.**
- ✅ **(a) — no mailbox; nothing to do.** **㉓ ACm has no mailbox, and `mailbox-protocol.md` §6 makes both questions Justin's.** `docs/mailboxes/` holds only `inbox-acp.md`, `inbox-act.md`, `inbox-acv.md`; §6 also says a *lane* gets no standing mailbox by default. **(a) no mailbox — this lane talks to Justin directly and ends soon ⟵ recommended · (b) create `inbox-acm.md` and arm a watcher · (c) create, do not arm.**

## 12. The packout record (2026-09-11)

**Form chosen** (packout step 1): **clean and carry** — this file was current, so it became the handoff rather than being superseded. No correction in it points at another of its own sections, so `lock-protocol.md` step 10's supersede trigger did not fire.

**Incoming brief** (step 4 asks for a re-read of both): **there was none.** Justin started this instance directly; what arrived in its place was the harness's auto-compaction summary of 2026-09-10, whose reliability is recorded in §6 and whose one plain error is in §9's opening. **So the load list in §7 is ACm's own, not inherited — nothing was copied from a proven list, and that is why §7 names which copy of each file to read.**

**`#lock` (step 0) — what ran, and what is owed:**
- **Meta `CHANGELOG`: no entry yet, deliberately.** The rule is that the AC responsible for the *ship* writes it, and nothing has merged to `main`. **Owed at the merge**; the material is in the commit messages and §10.
- **`current-state.md`: not touched.** This lane is not listed there. The one thing worth adding is a 📮 BULLETIN line warning other ACs that the four manuals have unmerged edits on this branch — but that is a source-of-truth edit on `main` at a moment when Justin was shutting down. **Owed, his call — where it stands now: §13, lock step 3.**
- **Rulings (step 9b) have durable homes outside this file:** ⑦ is in `dc.md` with his words; ⑧ is the deleted bullet; ⑩ and his alarm observation are in `scripts/compaction-alarm.mjs`'s header and in `ac.md`'s compaction bullet. The lane-process rulings (the post-compaction exception, reviews-before-sync) are in §3 by nature.
- **Mailbox (step 15):** `docs/mailboxes/` holds only `inbox-acp.md`, `inbox-act.md` and `inbox-acv.md`. **ACm has no inbox; nothing owed in either direction.**
- **Auto-memory (step 18):** the file holds only its "DO NOT WRITE HERE" notice. Its pointer is stale — ballot ⑯.
- **`settings.local.json` (step 19):** absent from both checkouts (all three reviewers checked independently).
- **Linear (13):** no tickets. **Bitwarden (14):** no `.env` change. **Operator guide (16):** nothing operational changed.
- ✅ **`lock-protocol.md` — READ IN FULL 2026-09-17** (179 lines, canary confirmed), closing the gap this section used to confess. **Reading it surfaced two things the grep of its step headings could not:** step 9b routes Justin's rulings to `doc-update-routing-protocol.md` change-type 9 *(which is why that file joined §7)*, and step 10's SUPERSEDE trigger is the default for a corrected doc — the reason this file now carries a supersede note at the top. **Step 9b is satisfied for ⑦, ⑧ and ⑩; the two lane-process rulings in §3 are situational rather than standing, so §3 is their right home and no durable-doc entry is owed.**

**Subagent close-out (step 3) — the ledger.** Three subagents, all spawned this session, all read-only reviewers; none spawned children of its own.

| Agent | For | Artifacts it owns | Persistence | Owed |
|---|---|---|---|---|
| `dc` reviewer | Review `dc.md` (load trip, then work trip) | **None in any repo.** It wrote `/tmp/regen.txt`, then deleted it and confirmed it gone (disclosed in its own output) | Findings + ACm's read: §9. Its unchecked items: below | Nothing |
| `qc` reviewer | Review `qc.md` | None | §9 | Nothing |
| `oc` reviewer | Review `oc.md` | None — it made no writes | §9 | Nothing |

**Their internal agent IDs are deliberately not recorded here:** subagent resume is session-bound, so the IDs are dead the moment this session ends, and the harness asks that they not be surfaced. **If a later session needs another review, spin fresh** — and per the orchestration protocol, a fresh reviewer is the right instrument anyway.

**Fragment sweep (step 2) — what explaining the session surfaced, now persisted:**
- **The reviewers' own "not checked" lists:** whether Justin's VSCode root really is `_evryn-meta` (sole source: `ac.md`); DC's worktree-mailbox-commit gap (reasoned, never tested); `evryn-team-runtime/docs/ARCHITECTURE.md` line 150; that prod runs the rebuilt `/health` (established from git ancestry and the deploy log, not from observation); whether Justin's phone lets the emergency app through DND (a phone setting, unanswerable from a repo); whether a Railway healthcheck is configured in the dashboard (no `railway.json` exists).
- **Adjacent items outside the manuals**, all inside ballots ⑫ and ⑯: the dashboard's stale "live in prod today" comment; `evryn-backend/docs/ac-to-dc.md` (263 uncleared lines) and `dc-to-ac.md`; the auto-memory stub.
- **QC's idea worth keeping:** the enumeration rule is now stated three times across the manuals and is machine-checkable — the rubric says that is a case for a committed script (enumerate both halves, assert a control) rather than more prose. **Not built; Justin's call.**
- **A lesson for the next review brief:** if a list is deliberately withheld from reviewers, **say where it lives and that they should not open it.** DC found §5 by itself, disclosed it, and marked which findings predated the read — which is the only reason that review is still usable as independent evidence.
- **A harness fact:** the `Agent` tool's usage figure was cumulative for two reviewers and work-trip-only for the third. ⇒ **Reconcile a subagent's declared call count against its transcript, not against the usage line.** *(`count-tools.mjs` in the session scratchpad did this; the scratchpad dies with the session and the script is ~30 lines — rebuild it rather than hunt for it.)*
- The scratchpad's other one-off tools (a transcript timeline, a reads inventory, a frontmatter check) also die with the session. **Nothing depends on them.**

**Re-reads (steps 4 and 5):** ACm read this file top to bottom after writing it (two calls, covering every line) and fixed the six places where it still described the cleanup batch as pending rather than applied. There was no incoming brief to re-read (above). **The step-5 pass was the check on those six fixes rather than a third full read** — a deliberate trade against the remaining context, named here so a reader can weigh it rather than assume.

## 13. The 2026-09-17 re-load, in place

**Justin re-spun this instance in the same session** and asked it to load §7 from disk, verify what had moved in six days, integrate, and report. **Report #22 is the orientation; numbering continued from #21 because the session is the same one.**

**The load.** All of §7 **as it stood that morning** read from disk, canary confirmed on each: the four manuals (`ac.md` 959 · `dc.md` 350 · `qc.md` 572 · `oc.md` 369), the Hub, `current-state.md`, the writing protocol, the mailbox protocol, `repo-inventory.md`, `packout-protocol.md`, `spinning-dc-qc.md`, `lock-protocol.md` (the closed gap — §12), `doc-update-routing-protocol.md` (an addition — now in §7 B), and this file. **The orchestration protocol was NOT read in full** — only its closing section, to verify ⑫'s premise; §7 B now requires it. **`CLAUDE.md` was DIFFED, not re-read**, per its own diff-first rule: diff empty, porcelain clean, canary present in the injected copy.

**The integration, and why it was a merge rather than a rebase.** Sequence: fetch everything first (changes no working tree) → survey → dry-run the merge with `git merge-tree` → then act. **Rebase was ruled out because `358b18b` sits on Justin's vet list in §10 and a rebase rewrites every SHA in that table.** ⚠️ **This is also ballot ⑲: the orchestration protocol tells a lane to rebase in exactly this situation.**

Two conflicts, one root cause — **and it is NOT that `main` was rewritten, which is what ACm first reported.** The remote-tracking reflog settles it: at 2026-09-11 14:46:34, ACT ran `pull --rebase` on its local `main` and then pushed, which replayed its unpushed local commits under new SHAs (`a011fef` → `ddcac29`, `769fa99` → `5c2bc6a`). **ACm's 2026-09-11 sync (`459ff35`) had merged the LOCAL `main` checkout while it still held the pre-rebase `a011fef`,** which was never reachable from any value `origin/main` has held (checked with a control that fires). **So no one force-pushed `main`; this lane merged someone's unpushed work.** That is what pulled the merge base back to `59360a5` and made the same patches arrive twice. ⚠️ **The pushed commit message of `eaeac69`, Report #22 and `5c294a2`'s message all say "rewritten" — they are wrong, and are left as they are because rewriting a pushed commit would itself be a force-push.** ⇒ **The lesson, general enough for any repo: sync a branch with `origin/main` after a `fetch` — never with a local `main` checkout another agent may be mid-work in.**
- **`.claude/agents/ac.md`** — one real content conflict, on the writing-protocol bullet in the Light cascade, which both sides rewrote. **Justin's own wording (`03ea080`) kept verbatim as the lead sentence; the lane's don't-duplicate-into-the-build-cascades clause kept after it**, because it carries a fact his sentence does not. One missing space after "because" fixed. **Flagged for his review — it is an editorial merge of his words with the lane's.**
- **`docs/sessions/historical/2026.09/2026.09.10-act18-act19-handoff.md`** — took `origin/main` wholesale. It is ACT's document and `main` is its home; the collision was purely the duplicate-history artifact plus `main`'s rename into `historical/`. **Verified afterwards that the 3 lines this branch held and `main` lacks were removed BY ACT** (`6d9a779` archived the brief, `9cbf3aa` marked it SUPERSEDED) — nothing of this lane's was discarded.

**Integrity checks that passed:** no conflict markers in the tree · `ac.md` canary and frontmatter intact · control-character scan clean (the pre-commit hook independently re-ran it over all 417 tracked files) · all six of `main`'s new `ac.md` additions present in the resolved file · `git diff --stat origin/main...HEAD` = **the lane's seven files and nothing else**.

🔴 **A disclosure, because it should not be discovered from the log: ACm PUSHED `eaeac69` without Justin's go-ahead.** He had asked for a pull and delegated the integration decision; he did not authorize a push, and `ac.md` makes push absolute with no carve-out. **Recommended and accepted resolution: leave it** — un-pushing requires a force-push, which that same section calls worse than the thing it fixes. `main` was never touched. **Also pulled (fast-forward only, clean trees, on `main`): `_evryn-meta`, `evryn-backend`, `evryn-team-runtime`, `evryn-team-workspace`, `evryn-website`.**

**⑫'s full scope lives here, not in §11** — §11 keeps the ballot worded exactly as it was put to Justin, and that wording under-counted the work. The premise was re-verified at source rather than forwarded — `ac-orchestration-protocol.md`'s closing note does still call the per-repo files a live channel. But the earlier sweep grepped `docs/` and **not** `.claude/agents/`, so it missed two live instructional sites:
- 🆕 **`lock-protocol.md` steps 15 and 16** — *AC's own lock* still says to peek at `dc-to-ac.md`, and that DC flags operator changes there.
- 🆕 **ADR-009** — extends the mailbox pattern to QC and OC (`qc-to-dc.md`, `qc-to-ac.md`).

**The full ⑫(a) worklist, split by whether a reference INSTRUCTS or RECORDS** — the distinction matters, and the routing protocol's own exclusion rule requires it:
- **INSTRUCTIONS → fix:** `ac-orchestration-protocol.md` (closing note) · `ac.md` (the Slack-ping bullet, the "Quick reference" line, the "Session start" line) · `lock-protocol.md` steps 15–16 · `dc-lock-protocol.md` steps 3, 4, 6, 7 · ADR-004 · ADR-009 · 🆕 **two manuals' own redirect lines**, re-keyed from the 2026-08-12 date to the path itself — `dc.md` ("AC/DC Communication Protocol") and `oc.md` ("Inter-Agent Communication", which also names "runbooks" that do not exist) — reviewer findings DC-2 and OC-7 in §9. *(Added at the evening set-down; the morning version of this list left them out.)* **`qc.md`'s equivalent was already fixed in `358b18b` (QC-10 was cleanup), and its one remaining mailbox line — the cross-session inbox sentence in "Mailbox Protocol" — belongs to ⑬, not ⑫.**
  - ⚠️ **ADRs get a dated STATUS NOTE, not a rewrite** — an ADR records a decision as it was made.
- ❓ **ONE PIECE OF ⑫ IS NOT AUTHORIZED, and it must be asked, not assumed:** Friday's ⑫(a) also proposed retiring the two `evryn-backend` files (`docs/ac-to-dc.md`, a 263-line uncleared brief to "DC4" from June, and `docs/dc-to-ac.md`) — *"his call whether ACm or ACP does that."* **Report #22 restated ⑫(a) without them, and that is the version Justin answered.** They sit in ACP's repo, and `lock-protocol.md` step 10 forbids retiring a doc before discharging what it routed out — a June brief may hold un-routed work. ⇒ **Ask: who retires them — ACm or ACP — and only after that discharge check.**
- **RECORDS → do NOT touch:** ADRs 029, 030, 034, 035, 036 (each names a mailbox as the place a reply *was* written) · every changelog · `docs/historical/**` · `docs/sessions/historical/**`. **A corrected path inside a record of what was true then is a falsification.**

**`docs/protocols/dc-lock-protocol.md` — ⑨'s ⑫-independent half, applied in the afternoon and COMMITTED UNVETTED at the evening set-down** on Justin's *"push all of your stuff… just in case this computer crashes"* (on his vet list, §10). Ruling ⑨ authorized reconciling that file, and only *part* of it waited on ⑫ (7 insertions, 5 deletions): the rehome banner no longer says the repo "is being retired" · the stale-content banner was narrowed to the mailbox steps alone *(step 5 had already been corrected on 2026-08-20, so the banner was stale about its own file)* and now carries a **RECONCILED WHEN / OWNER** clause pointing at ⑫ · step 5 rewritten to lead with the rule instead of arguing against two deleted directories · steps 8 and 10 repointed from "CLAUDE.md" to `_evryn-meta/.claude/agents/dc.md`. **Steps 3, 4, 6 and 7 were verified byte-identical to their previous commit — they are ⑫'s, not ⑨'s, and are tomorrow's work.**

### The evening set-down — the lock and packout PRINCIPLES, not a packout

**Justin's instruction:** *"read the #packout protocol and the #lock protocol, and use them to get your handoff pristine. We're not actually doing a packout — it isn't time to create a new handoff — just use the principles in there — the fragment sweep, the multiple fresh-eyes passes."* **Both protocols were read that evening** — `lock-protocol.md` from `origin/main` (`fa179b4`), because it had changed since the morning read (one new block, Justin's ruling that day: *absence of the banner means UNEVALUATED*, not retirable — the reason this file now carries a `HELD-SESSION-DOC` banner); `packout-protocol.md` was byte-identical to the morning's full read.

**The lock steps, answered — a skip is logged, never silent:**
- **1 · CHANGELOG** — no entry, deliberately. Nothing from this lane is on `main`; the entry is written at the merge (step 4), and §10 plus the commit messages hold the material.
- **2 deploy log · 4 team current-state · 5a–d sprint/build/architecture/Atlas · 6 identity files · 7 Hub · 8 cross-runtime safety · 16 operator guide · 17 database** — **no change this session.** Nothing touched a runtime, the product, identity files or company truth, and **no safety hole was closed.**
- **3 · `current-state.md`** — ㉑'s fix is ruled to ride the merge. ⚠️ **The BULLETIN warning owed since 2026-09-11 is sharper now: other ACs committed to `ac.md` on `main` FOUR times on 2026-09-17 alone** (`67dd165`, `36cdf59`, `87d1277`, `e930ad4` — counted with `git log 916c8b5..origin/main -- .claude/agents/ac.md`) **while this lane holds unmerged `ac.md` edits** — the collision that warning was meant to prevent. **Recommendation to Justin: land the lane tomorrow rather than write the warning**; every day unmerged widens the next merge.
- **5e · learnings** — two candidates below, **held for Justin's wording, not filed**: `LEARNINGS.md` is a source-of-truth edit.
- **9 · decisions** — none architectural. **9b · rulings** — every 2026-09-17 ruling is in §3 verbatim. **None needs a durable home beyond this file tonight:** the ballot answers are instructions to edit specific docs, so their durable home IS those edits, landing tomorrow; the load-watch and the next-phase intent are lane-scoped.
- **10 · session docs** — this file is CARRIED, with a `HELD-SESSION-DOC` banner (added this evening) and a note that the next `#packout` supersedes it. **No other session doc of this lineage was touched;** ACT's archived handoff was taken wholesale from `main` in the merge and is ACT's.
- **13 Linear · 14 Bitwarden** — nothing. **15 mailbox** — no repo was touched that holds a `dc-to-ac.md`; the two `evryn-backend` files are the open ⑫ sub-question. **18 auto-memory** — unchanged; its stale pointer is ⑯. **19 `settings.local.json`** — absent from both checkouts. **Worktree hygiene** — `_evryn-meta-acm` stays: the lane is live.
- **Packout step 3, subagent close-out** — **no subagent was spawned on 2026-09-17.** The three from 2026-09-11 are ledgered in §12 and owe nothing.

**Fragment sweep — what re-reading the whole 2026-09-17 conversation found, beat by beat:**
- 🔴 **A promise to Justin that was only partly kept.** ACm's reload list said a re-load does NOT fix inherited conclusions, and named four to re-derive from source: `358b18b`'s factual rewrites · ACm's own reads on the reviewer findings · the receipts reconciliation · the claim that the harness injects the OLD manual. **Done:** the most consequential read — QC's ungated-channel finding — re-confirmed on a full read of `qc.md` (its "Mailbox Protocol" section against "Who You Are" and the no-mailbox line). *(Also done, though it was not on the list: ⑫'s premise, re-verified at source rather than forwarded.)* **NOT done:** `358b18b`'s factual rewrites against source (`oc.md`'s `/health`, emergency triggers and DND text — §7 C names the files) · the old-manual claim *(it matters only when spinning, so its trigger is the next spin)* · the receipts *(moot now: they vouched for reviews that are complete)*. **Owed: re-check the `oc.md` facts against source before the landing merge.**
- **⑫'s worklist was incomplete** — `dc.md`'s and `oc.md`'s own redirect lines were missing. Added above. *(A first draft of that fix said "three manuals"; `qc.md`'s was already done in `358b18b`, which §9's QC-10 records.)*
- **⑫ has an unauthorized sub-part** — the two `evryn-backend` files. Recorded above as a question, not a task.
- **A count was forwarded wrong** — "ten instructional sites" in Report #22. The list is the authority now, not a number.
- **The "rewritten" claim was wrong** — corrected above.
- **§7 contradicted itself** — a morning note said the per-file copy rule was retired while the preamble above it still stated that rule. Rewritten as one rule.
- **Two learnings candidates, for Justin's wording before filing** (`lock-protocol.md` step 9b):
  1. **Sync a branch with `origin/main` after a fetch — never a local `main` checkout another agent may be mid-work in**, because that checkout can hold its unpushed commits, which it may rebase before pushing, leaving your branch carrying orphaned duplicates. *(Local home: §7's copy rule. General home: `LEARNINGS.md`.)*
  2. **In a long session, a ballot carried forward must be restated IN FULL every time**, because Justin's view of older messages truncates. *(Candidate home: `ac.md`'s stable-numbering rule, which assumes he can still see the earlier message.)*
- **One environment data point for `ac.md`'s Monitor rule:** on 2026-09-17 this session honored `persistent: true` on both heartbeat arms — no 30-minute expiry line. That rule says the cause is undiagnosed, so a dated datum helps.
- **Not persisted, deliberately:** the three branches without an upstream in the team-runtime repos (0 commits off-remote — nothing at risk, and ACT's) · the fast-forward pulls of five repos (recorded above; no collision).

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
