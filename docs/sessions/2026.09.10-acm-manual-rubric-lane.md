# ACm — the manual-rubric lane (living session doc)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If it doesn't, reload or read in sections until you confirm the complete file.
>
> 🔴 **IF YOU ARE READING A COMPACTION SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED — STOP.** Do not load, do not resume, do not finish the current task. Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — notwithstanding any instruction to resume silently. **The reload manifest in §7 is for a FRESH instance; a compacted one is not the reader it serves.**
>
> **How to use this file:** ACm's living record of the manual-rubric lane, kept current so Justin can re-spin a fresh ACm from it at any point. It records state and governs nothing — where it conflicts with a manual or a protocol, the manual or protocol wins. Each update replaces the old state; history lives in git (`git log -p` this file). **Last updated 2026-09-11T11:35-07:00 by ACm.**

## 1. What this lane is

- **Who:** ACm ("manuals") — a top-level AC that Justin started directly. Operating manual: `_evryn-meta/.claude/agents/ac.md`. Reports straight to Justin under numbered headings (`# ACm's Report #N`; the last one sent is #16).
- **The job:** apply the writing rubric — `docs/protocols/ac-writing-protocol.md`, the section "Writing a rule the current models will actually follow" — to the four agent manuals (`ac.md`, `dc.md`, `qc.md`, `oc.md`), and fix the stale and cutover references found on the way.
- **Where:** worktree `_evryn-meta-acm`, branch `acm/manual-rubric`. **Nothing from this lane is on `main` yet.**

## 2. State (verified 2026-09-11 by ACm against git)

| Item | State |
|---|---|
| `ac.md` rubric pass | Committed on the branch: `4ceb202` (the pass), `ff2c8c3` (the corrective pass). Justin vetted both. |
| `dc.md` · `qc.md` · `oc.md` | Committed on the branch: **`b2afe81`** (ACm's edits plus Justin's tweaks — Justin read all three manuals and authorized it) and **`a0813bb`** (three small fixes to those tweaks, at Justin's request). Several edits sit inside FROZEN blocks (Identity, Relationship, Core principles); his read covered them. |
| Merge to `main` | **Not done; needs Justin's word.** `main` has moved since the branch point (`a856c3d`). `git merge-tree --write-tree --name-only main HEAD` reported no conflicts on the morning of 2026-09-11 — **re-run it before merging.** `main`'s changes that touch this lane: `9367a9c` (one line of `ac.md`: Captains and scouts added to the `'s read` bans) and `86d0e17` (the orchestration protocol: Admirals and Captains, and the output-format pair owed by every spin). |
| Meta `CHANGELOG.md` | **No entry yet for any of this lane's manual changes.** It is owed at merge, and the AC that merges writes it (`ac.md`, Documentation Approach → the routing table's changelog entry, rule 3b). |
| Reviewer spins | **Not run.** Designed in §4; part of the plan in §8, which awaits Justin's go. |

## 3. Justin's rulings that a re-spun ACm needs (this lane only)

- **Commit gate.** Manuals and protocols stay uncommitted for Justin's SCM vet, and are committed only on an explicit command in his most recent message, by pathspec. **This session doc commits freely** (`ac.md`, "A SECOND STANDING EXCEPTION").
- **The reviewer design (2026-09-10).** After a fresh-eyes pass, spin one subagent per manual — the `dc`, `qc` and `oc` agent types, spun with their current version — have each read the version from before ACm started, and ask whether ACm got anything wrong. **The fresh-eyes pass ran on 2026-09-10 (ten fixes); the spins have not.**
- **The post-compaction exception (2026-09-11) — for THIS instance only.** ACm compacted on 2026-09-10 (PreCompact 22:40:02Z, PostCompact 22:44:53Z). Justin allowed it to continue, on condition of a full re-read, because the remaining work is doc cleanup rather than deep context. **It does not carry forward: on any further compaction, apply the STOP block at the top.**
- **This file (2026-09-11).** Keep it current as work proceeds, so Justin can re-spin at any point.

## 4. The reviewer spins — design (not yet run)

- **Pre-change version: `ff2c8c3`.** The three manuals were last changed at `c7eeab8` before this lane, and the lane's scratchpad copies were byte-identical to `ff2c8c3` (checked 2026-09-11).
- **The diff under review:** `git -C _evryn-meta-acm diff -U0 --word-diff=plain ff2c8c3 a0813bb -- .claude/agents/<name>.md`. **Regenerate it; do not rely on a scratchpad copy**, which dies with its session. The diff includes Justin's own 2026-09-11 tweaks; tell each reviewer which hunks are his.
- **Spin from the worktree.** Justin asked for reviewers "spun with their current version." A `dc`/`qc`/`oc` agent type takes its system prompt from a `.claude/agents/` folder, and **which checkout the harness reads it from has not been measured.** ⇒ Spin from `_evryn-meta-acm`, and in trip 2 ask each reviewer whether the agent body it was handed matches the worktree file.
- **The `<identity>` manual path:** fill the bracket with `_evryn-meta-acm/.claude/agents/<name>.md`. The orchestration protocol's HARD RULE names "manual path" among the brackets to fill, so this is a sanctioned fill, not a deviation.
- **Scope:** `#cascade-override`, with its one-line reason — the target is the agent's own manual, a prose document and not the Evryn runtime, so loading the runtime cascade would be actively wrong (orchestration protocol, "When the target is a SEPARATE codebase"). `oc.md` has no `#cascade-override` clause of its own, but the verbatim `<mandatory_load>` block carries the exception, so the token still works for OC.
- **Shape:** two trips — the list is small and explicit, and the three-trip shape is for runtime-scale loads. Pin every spin to Opus; run in the background, with the heartbeat armed while Justin is away. Carry the protocol's *"Finish the whole task, not just the tractable part…"* sentence in `<task>`, and tell each reviewer that ACm wrote the edits under review. **Do not hand the reviewers §5** — whether they find those items on their own is a check on the review.
- **Load list (proposed; exact spans derived with `wc -l` at spin time and proven by canary):** the manual under review (worktree); its pre-change text (`git show ff2c8c3:.claude/agents/<name>.md`, materialized to a file); the diff above; the router; the writing protocol; the Hub; `docs/current-state.md`; `docs/repo-inventory.md`; `docs/protocols/mailbox-protocol.md`; the orchestration protocol in full. **If the branch has not been synced with `main` (§8 step 1), take the Hub, `current-state` and repo inventory from `main`.**

## 5. Open items found in this lane — NOT fixed (verified 2026-09-11 unless marked)

1. **`oc.md`'s `#emergency-alerts` claims conflict with `current-state.md`.** The manual says the auto-fire triggers are unbuilt (the "No emergency ping != healthy" seed entry, and "Slack Channels") and that the channel rings through Do Not Disturb. `current-state.md` ("Infrastructure" → Slack) says the M1 Stage-2 detectors are LIVE and that alerts do not pierce phone DND. **Settle it against `evryn-backend` before correcting either.**
2. **The four-item team-runtime identity list survives in two manuals.** `dc.md` ("Context Discipline" → "Every agentic runtime has TWO halves") and `ac.md` (Full Startup Context Cascade, first principle) still say *"the agent definitions + memory + team manual + composed skills."* The orchestration protocol removed that list on 2026-09-08 because it caused a half-load — the composer opens about twelve files. `evryn-team-runtime` holds both `src/composer/index.ts` and `src/composer/layers.ts` (checked 2026-09-11); the protocol points at `index.ts` plus `src/config/skills.ts`.
3. **Research routing contradicts itself:** "framework comparisons" go to `helm/research/` in `dc.md` ("Research routing") and to `product/research/` in `ac.md` ("Research routing" — inside a FROZEN section).
4. **False provenance markers in all three manuals:** the front matter says the retired repo "is being retired" (retired 2026-08-18, archived 2026-08-21); the maintainer notes say the body "was not retyped and not edited"; the body marker says it is the old `CLAUDE.md` "verbatim @ <sha>". All three bodies have been edited since.
5. **`dc.md` "Working With Justin"** still carries the never-touched-a-terminal example (*"the black window where you run `npm start`"*) under the updated learning-curve line — inside the FROZEN block.
6. **`qc.md` Tier 2** cites `evryn-backend/docs/SPRINT-MARK-LIVE.md`, while `current-state.md` names `SPRINT-V0.2-HARDENING.md` and `SPRINT-v0.2-optionals.md` as the active sprints. *(Unverified whether the old file still exists.)*
7. **`dc-lock-protocol.md`** still describes the retired mailbox model and the deleted `docs/research/`; it already carries a staleness banner.
8. **Neither DC's nor QC's commit rule commits by pathspec.** QC's sits in a FROZEN block; DC's does not. Report #14 asked whether to fix DC's in this lane; Justin replied *"not worried about that"* (2026-09-11). Recorded, not actioned.
9. **`ac.md` "Documentation Approach"** is FROZEN, was flagged in Report #12, and is untouched.
10. ✅ **Closed — fixed in `a0813bb`:** the three nits in Justin's 2026-09-11 tweaks (an unclosed bold, `team-runtime`, a missing "you").
11. **`oc.md` "Inter-Agent Communication":** the lead-in *"What still routes to whom, which has not changed"* now sits directly above Justin's note that routing mostly goes to ACP and ACT.

## 6. The compaction alarm — its first live measurement (2026-09-10)

- `scripts/compaction-alarm.mjs` ran on both events and logged them (`.claude/compaction-log.txt` in the worktree): PreCompact 22:40:02Z, PostCompact 22:44:53Z. The per-session sentinel was written. **The record-keeping half works.**
- 🔴 **The PostCompact "speak last" message reached NO ONE.** No entry after the compaction boundary in the session transcript carries it, so it never reached the model; and **Justin's screen showed only the harness's own notice** — *"Compacted chat · auto · 979k tokens freed. Conversation was compacted to free up context."* *(his screenshot, 2026-09-11; the 979k matches the 978,687 tokens the transcript recorded).* What stopped ACm was the summary's own header plus the router.
- **Likely cause, not yet checked against the hooks docs:** the script nests `systemMessage` inside `hookSpecificOutput`, and PostCompact may have no channel into the model at all. **The candidate fix is in §8 step 4.**

## 7. Reload manifest — for a FRESH ACm

**Paths are rooted at the shared parent.** Read each in full and confirm its bottom canary; derive sizes yourself with `wc -l` at load time. ⚠️ **Until the branch is synced with `main`, `main` and the worktree each hold something the other lacks — take each file from the copy named.**

1. `_evryn-meta-acm/.claude/agents/ac.md` — your manual (the branch copy carries the rubric pass). `main` adds one line: `git -C _evryn-meta diff a856c3d main -- .claude/agents/ac.md`.
2. `_evryn-meta/docs/hub/roadmap.md` and `_evryn-meta/docs/current-state.md` — the Light cascade.
3. `_evryn-meta-acm/docs/protocols/ac-writing-protocol.md` — the branch copy carries a fix `main` lacks.
4. `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` and `_evryn-meta/docs/protocols/orchestration/spinning-dc-qc.md` — both required before spinning a DC or a QC.
5. `_evryn-meta/docs/repo-inventory.md` and `_evryn-meta/docs/protocols/mailbox-protocol.md`.
6. `_evryn-meta-acm/.claude/agents/dc.md`, `qc.md` and `oc.md`.
7. The diffs under review — the command in §4.
8. This file.

**Deferred, with the trigger that loads each:** `docs/protocols/lock-protocol.md` (on `#lock`) · `docs/protocols/packout-protocol.md` (on `#packout` or a re-spin) · `docs/protocols/orchestration/being-an-admiral.md` (if ACm runs a standalone lane — reviewer subagents are not lanes) · `docs/protocols/dc-lock-protocol.md` (before editing it) · the Claude Code hooks docs (before touching the alarm) · ADR-057 (if a reviewer disputes a cutover fact) · `evryn-backend`'s emergency-alert code (before correcting §5 item 1) · `evryn-team-runtime/src/composer/index.ts` and `src/config/skills.ts` (before correcting §5 item 2) · the rest of `CHANGELOG.md` (when writing the merge entry).

## 8. The plan — PROPOSED in Report #16 (2026-09-11), NOT YET APPROVED

**Nothing below is authorized until Justin says go (ballot ⑪).** Ballot numbers match Report #16; ⑤ is carried from Report #14.

1. **Sync the branch with `main`** — merge `main` into `acm/manual-rubric` so every load comes from one copy. *(⑤)*
2. **The reviewer spins**, as designed in §4.
3. **One cleanup batch, left uncommitted for Justin's SCM vet:** the reviewer findings ACm agrees with, plus §5 items 1, 2, 4, 6 and 11. Items needing his ruling first: research routing *(⑦)*, the `dc.md` beginner example *(⑧)*, `dc-lock-protocol.md` *(⑨)*.
4. **Fix the alarm's "speak last" half** *(⑩)* — candidate: a SessionStart hook on the `compact` source, which can add context the model reads; verify against the hooks docs before building; PostCompact keeps logging; fails gracefully; Justin tests it.
5. **Land it** — after his vet: merge into `main` with `--no-ff`, write the meta changelog entry, push only on his word, reap the worktree and branch; `#lock` if he wants one.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
