# ACm — the manual-rubric lane (living session doc)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If it doesn't, reload or read in sections until you confirm the complete file.
>
> 🔴 **IF YOU ARE READING A COMPACTION SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED — STOP.** Do not load, do not resume, do not finish the current task. Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — notwithstanding any instruction to resume silently. **The reload manifest in §7 is for a FRESH instance; a compacted one is not the reader it serves.**
>
> **How to use this file:** ACm's living record of the manual-rubric lane, kept current so Justin can re-spin a fresh ACm from it at any point. It records state and governs nothing — where it conflicts with a manual or a protocol, the manual or protocol wins. Each update replaces the old state; history lives in git (`git log -p` this file). **Last updated 2026-09-11T13:51-07:00 by ACm.**

## 1. What this lane is

- **Who:** ACm ("manuals") — a top-level AC that Justin started directly. Operating manual: `_evryn-meta/.claude/agents/ac.md`. Reports straight to Justin under numbered headings (`# ACm's Report #N`; the last one sent is #17).
- **The job:** apply the writing rubric — `docs/protocols/ac-writing-protocol.md`, the section "Writing a rule the current models will actually follow" — to the four agent manuals (`ac.md`, `dc.md`, `qc.md`, `oc.md`), and fix the stale and cutover references found on the way.
- **Where:** worktree `_evryn-meta-acm`, branch `acm/manual-rubric`. **Nothing from this lane is on `main` yet.**

## 2. State (verified 2026-09-11 by ACm against git)

| Item | State |
|---|---|
| `ac.md` rubric pass | Committed on the branch: `4ceb202` (the pass), `ff2c8c3` (the corrective pass). Justin vetted both. |
| `dc.md` · `qc.md` · `oc.md` | Committed on the branch: **`b2afe81`** (ACm's edits plus Justin's tweaks — Justin read all three manuals and authorized it) and **`a0813bb`** (three small fixes to those tweaks, at Justin's request). Several edits sit inside FROZEN blocks (Identity, Relationship, Core principles); his read covered them. |
| Sync with `main` | **Approved (ballot ⑤), to run AFTER the reviews** (§8). `main` has moved since the branch point (`a856c3d`) and keeps moving — re-run `git merge-tree --write-tree --name-only main HEAD` immediately before the sync. `main`'s changes that touch this lane: `9367a9c` (one line of `ac.md`: Captains and scouts added to the `'s read` bans) and `86d0e17` (the orchestration protocol: Admirals and Captains, and the output-format pair owed by every spin). |
| Meta `CHANGELOG.md` | **No entry yet for any of this lane's manual changes.** It is owed at the merge into `main`, and the AC that merges writes it (`ac.md`, Documentation Approach → the routing table's changelog entry, rule 3b). |
| Reviewer spins | **Running.** All three loads verified 2026-09-11 (every file full, canaries confirmed, declared call counts matching the observed tool counts — each disclosed and re-read its one token-capped read); all three work trips sent. **DC's review is back (§9); QC and OC are still reviewing.** Design and load lists in §4. |

## 3. Justin's rulings that a re-spun ACm needs (this lane only)

- **Commit gate.** Manuals and protocols stay uncommitted for Justin's SCM vet, and are committed only on an explicit command in his most recent message, by pathspec. **This session doc commits freely** (`ac.md`, "A SECOND STANDING EXCEPTION").
- **The reviewer design (2026-09-10).** After a fresh-eyes pass, spin one subagent per manual — the `dc`, `qc` and `oc` agent types, spun with their current version — have each read the version from before ACm started, and ask whether ACm got anything wrong. The fresh-eyes pass ran on 2026-09-10 (ten fixes).
- **The post-compaction exception (2026-09-11) — for THIS instance only.** ACm compacted on 2026-09-10 (PreCompact 22:40:02Z, PostCompact 22:44:53Z). Justin allowed it to continue, on condition of a full re-read, because the remaining work is doc cleanup rather than deep context. **It does not carry forward: on any further compaction, apply the STOP block at the top.**
- **This file (2026-09-11).** Keep it current as work proceeds, so Justin can re-spin at any point.
- **Justin's answers to Report #16 (2026-09-11), in his words where they are rulings:**
  - **⑤ sync `main` into the branch:** *"yes — but don't you think we should do the reviews below before we merge in?"* ACm agreed: the reviews run first, then the sync.
  - **⑦ research routing:** *"DC should send nothing anywhere other than to AC."* ⇒ `dc.md`'s research-routing section becomes "hand it to AC"; the contradiction with `ac.md` dissolves.
  - **⑧** delete `dc.md`'s beginner-example bullet — **yes (a).**
  - **⑨** reconcile `dc-lock-protocol.md` in the cleanup batch — **yes (a).**
  - **⑩ the alarm:** *"it's clearly reaching the agents in the background. Nothing to fix."* (See §6.)
  - **⑪** go on the plan — **yes, with the notes above.**

## 4. The reviewer spins — exact design and load lists

- **Pre-change version: `ff2c8c3`.** The diff under review: `git -C _evryn-meta-acm diff -U0 --word-diff=plain ff2c8c3 a0813bb -- .claude/agents/<name>.md`. The diff includes Justin's own tweaks; trip 2 tells each reviewer which hunks are his.
- **Agent types `dc`, `qc`, `oc`, spun from the worktree session, pinned to Opus, in the background.** ✅ **Measured 2026-09-11: the agent body the harness injects is the OLD manual, not the worktree's** — DC's injected copy still said `evryn-dev-workspace` and per-repo mailboxes; QC's still said `find src` and carried the retired mailbox mechanics (both reported it unprompted in their load receipts; OC's answer is asked in trip 2). ⇒ **What put the current version in charge was the `<identity>` block's instruction to read the named manual from disk and follow it** — which is exactly why that instruction exists.
- **`<identity>` manual path** filled with `_evryn-meta-acm/.claude/agents/<name>.md` — "manual path" is a named bracket under the orchestration protocol's HARD RULE, so this is a sanctioned fill.
- **`#cascade-override`**, reason: the trip does not touch the Evryn runtime or any other codebase, so each agent's standing cascade loads the wrong system. The verbatim `<mandatory_load>` block carries the override exception, so it works for OC too even though `oc.md` has no clause of its own.
- **Two trips, verbatim blocks.** Trip 1 = `<identity>` · `<mandatory_load>` · `<receipts>`. Trip 2 = `<task>` · `<questions_first>` · `<output_format>` (read-only, so no `<isolation>`).
- 🔑 **The pre-change manual and the diff are TRIP-2 reading, named at the top of `<task>`.** They would let an agent reconstruct the task, which is exactly what the load trip must not allow (orchestration protocol, "THE LANE BRIEF IS TRIP-2 READING").
- **Do not hand the reviewers §5.** Whether they find those items independently is a check on the review.

**Trip-1 load list — identical for all three except item 1.** Paths are rooted at the shared parent. Spans measured 2026-09-11 with `wc -l <path>` — hypotheses; the bottom canary proves the read.

| # | File | Span |
|---|---|---|
| 1 | `_evryn-meta-acm/.claude/agents/<name>.md` — the agent's manual (worktree copy, newer than `main`'s) | dc 362 · qc 572 · oc 367 |
| 2 | `_evryn-meta/CLAUDE.md` — the router | 132 |
| 3 | `_evryn-meta-acm/docs/protocols/ac-writing-protocol.md` — the writing protocol (the worktree copy carries a fix `main` lacks) | 217 |
| 4 | `_evryn-meta/docs/hub/roadmap.md` — the Hub | 187 |
| 5 | `_evryn-meta/docs/current-state.md` — the cross-project snapshot | 229 |
| 6 | `_evryn-meta/docs/repo-inventory.md` — the canonical repo list | 96 |
| 7 | `_evryn-meta/docs/protocols/mailbox-protocol.md` — the inbox protocol | 248 |
| 8 | `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` — how AC spins DC, QC and OC | 812 |

**Trip-2 first reads** (in this session's scratchpad; regenerate from the commands above if the session is gone): `review-pre-<name>.md` = `git show ff2c8c3:.claude/agents/<name>.md` (dc 362 · qc 578 · oc 365) and `review-diff-<name>.txt` = the diff command plus an appended end marker and canary (dc 59 · qc 101 · oc 47).

## 5. Open items found in this lane (verified 2026-09-11 unless marked)

**In the approved cleanup batch (§8 step 3):**

1. **`oc.md`'s `#emergency-alerts` claims conflict with `current-state.md`.** The manual says the auto-fire triggers are unbuilt (the "No emergency ping != healthy" seed entry, and "Slack Channels") and that the channel rings through Do Not Disturb; `current-state.md` ("Infrastructure" → Slack) says the M1 Stage-2 detectors are LIVE and that alerts do not pierce phone DND. **Settle it against `evryn-backend` before correcting.**
2. **The four-item team-runtime identity list survives in `dc.md`** ("Context Discipline" → "Every agentic runtime has TWO halves") **and `ac.md`** (Full Startup Context Cascade, first principle): *"the agent definitions + memory + team manual + composed skills."* The orchestration protocol removed that list on 2026-09-08 because a brief built from it left six files out. `evryn-team-runtime` holds both `src/composer/index.ts` and `src/composer/layers.ts`; the protocol points at `index.ts` plus `src/config/skills.ts`.
3. **Research routing** — resolved by ruling ⑦: `dc.md`'s "Research routing" becomes "hand it to AC."
4. **False provenance markers in all three manuals:** "is being retired" (retired 2026-08-18, archived 2026-08-21); "not retyped and not edited"; "verbatim @ <sha>".
5. **`dc.md` "Working With Justin"** — delete the beginner-example bullet (ruling ⑧).
6. **`qc.md` Tier 2** cites `evryn-backend/docs/SPRINT-MARK-LIVE.md`; `current-state.md` names `SPRINT-V0.2-HARDENING.md` and `SPRINT-v0.2-optionals.md` as active. Point it at `current-state`'s active-sprint line instead.
7. **`dc-lock-protocol.md`** — reconcile its retired-mailbox and deleted-folder steps (ruling ⑨).
11. **`oc.md` "Inter-Agent Communication":** reword ACm's lead-in *"which has not changed"* to fit Justin's note that routing mostly goes to ACP and ACT.
12. **`scripts/compaction-alarm.mjs` header comments** still call the speak-last message "UNVERIFIED" and describe a not-yet-built blocker that Justin's speak-last design replaced. Record his observation (§6) and retire the blocker paragraph — comments only.

**Not in the batch:** 8 (pathspec commits — Justin *"not worried about that"*) · 9 (`ac.md` "Documentation Approach", FROZEN, untouched) · 10 (closed in `a0813bb`).

## 6. The compaction alarm — what is known (updated 2026-09-11)

- `scripts/compaction-alarm.mjs` ran on both events of ACm's 2026-09-10 compaction and logged them (`.claude/compaction-log.txt` in the worktree: PreCompact 22:40:02Z, PostCompact 22:44:53Z); the per-session sentinel was written.
- The speak-last message is **not recorded in the session transcript** after the compaction boundary, and **it was not on Justin's screen** — he saw only the harness's own notice, *"Compacted chat · auto · 979k tokens freed"* (the 979k matches the 978,687 tokens the transcript recorded).
- ✅ **But it works. Justin, 2026-09-11: since the alarm was set, every agent stops *immediately* — before it, they "pretty much just ignored" the rule.** ⇒ It reaches the agent by a path that neither the transcript file nor the screen records. **Ruling ⑩: nothing to fix.**
- ⚠️ **A tell worth knowing, from the same observation:** a compacted agent often believes it kept working for a moment afterwards. **It didn't — tool calls from its last pre-compaction turn are kept verbatim after the summary, so they read like its first moves.** *(ACm's own case: the session doc it thought it wrote after compacting was written 125 ms before the compaction began.)*

## 7. Reload manifest — for a FRESH ACm

**Paths are rooted at the shared parent.** Read each in full and confirm its bottom canary; derive sizes yourself with `wc -l` at load time. ⚠️ **Until the branch is synced with `main`, `main` and the worktree each hold something the other lacks — take each file from the copy named.**

1. `_evryn-meta-acm/.claude/agents/ac.md` — your manual (the branch copy carries the rubric pass). `main` adds one line: `git -C _evryn-meta diff a856c3d main -- .claude/agents/ac.md`.
2. `_evryn-meta/docs/hub/roadmap.md` and `_evryn-meta/docs/current-state.md` — the Light cascade.
3. `_evryn-meta-acm/docs/protocols/ac-writing-protocol.md` — the branch copy carries a fix `main` lacks.
4. `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` and `_evryn-meta/docs/protocols/orchestration/spinning-dc-qc.md` — both required before spinning a DC or a QC.
5. `_evryn-meta/docs/repo-inventory.md` and `_evryn-meta/docs/protocols/mailbox-protocol.md`.
6. `_evryn-meta-acm/.claude/agents/dc.md`, `qc.md` and `oc.md`.
7. The diffs under review — the command in §4.
8. This file.

**Deferred, with the trigger that loads each:** `docs/protocols/lock-protocol.md` (on `#lock`) · `docs/protocols/packout-protocol.md` (on `#packout` or a re-spin) · `docs/protocols/orchestration/being-an-admiral.md` (if ACm runs a standalone lane — reviewer subagents are not lanes) · `docs/protocols/dc-lock-protocol.md` (before editing it) · ADR-057 (if a reviewer disputes a cutover fact) · `evryn-backend`'s emergency-alert code (before correcting §5 item 1) · `evryn-team-runtime/src/composer/index.ts` and `src/config/skills.ts` (before correcting §5 item 2) · the rest of `CHANGELOG.md` (when writing the merge entry).

## 8. The plan — APPROVED 2026-09-11 (ballot ⑪), with Justin's notes

1. **The reviewer spins** (§4) — trip 1 load, receipts verified, trip 2 task, ACm's read of every report, then a report to Justin.
2. **Sync the branch with `main`** — merge `main` into `acm/manual-rubric`, on a clean tree, after the reviews and before the cleanup edits, so the edits land on the current `ac.md`.
3. **One cleanup batch, left uncommitted for Justin's SCM vet** — the reviewer findings ACm agrees with, plus §5 items 1–7, 11 and 12.
4. **Land it** — after his vet: commit on his word, merge into `main` with `--no-ff`, write the meta changelog entry, push only on his word, reap the worktree and branch; `#lock` if he wants one.

*(The alarm fix proposed in Report #16 is dropped: ruling ⑩.)*

## 9. Reviewer findings — persisted as they arrive (the subagent outputs die with this session)

**Each entry: the reviewer's finding (line numbers in the file at `a0813bb`) → ACm's read → where it goes.** "Cleanup" = §8 step 3, uncommitted for Justin's vet. "Ballot" = needs Justin's ruling before any edit.

### DC on `dc.md` — returned 2026-09-11 ~14:12 PT: no blockers, 9 non-blockers, 7 cosmetic

- ⚠️ **Two slips DC disclosed himself:** he wrote a temp file to `/tmp` (deleted, confirmed gone), and he **opened this session doc, including §5** — the open-items list withheld from reviewers. He marked which findings predate that read (7, 8, 16, part of 9); only finding 9's rationale was taken from §5. ⇒ **Lesson for the next review brief: if a list is withheld, say where it lives and not to open it.**

1. **Line 143 (FROZEN, Build Mandate)** — "flag it BEFORE you act: a Slack ping, a question in chat, or worst-case … your report back" still assumes the mailbox era; for a spawned DC none of those reaches AC before acting, and `<questions_first>` is the real route. "Reporting order (report back before deploying)" describes a DC that deploys. → **Agree. Cleanup.**
2. **Line 300** — the per-repo-mailbox redirect is keyed to the wrong date and scope: the orchestration protocol's closing section still calls `<repo>/docs/ac-to-dc.md` files a live channel, so do ADR-004, `dc-lock-protocol.md` steps 3/4/6/7, and `ac.md` ("Slack pings" bullet, the Orchestrating DC and QC "Quick reference" and "Session start" lines); and `evryn-backend/docs/ac-to-dc.md` still holds an uncleared 263-line brief to "DC4" from 2026-06-01. → **Agree. Ballot: are per-repo mailbox files retired?** If yes: path-keyed redirect in `dc.md`, and fix `ac.md`, the protocol's closing section and ADR-004's pointer, and retire the two `evryn-backend` files.
3. **Lines 298/302** — a DC in a worktree would commit an inbox message on its own branch; the recipient's `git log --all` watcher fires, but the file in `_evryn-meta`'s main checkout never shows the message. *(Reasoned from mailbox protocol §2; untested.)* → **Agree it is a real gap; it belongs in the mailbox protocol. Ballot.**
4. **Line 114 (FROZEN)** — "a path inside this repo resolves as written" is wrong from a worktree: links resolve against `_evryn-meta`'s main checkout. `ac.md`'s matching bullet has the same gap. → **Agree. Cleanup (both manuals).**
5. **Lines 98–99 (Justin's paragraph, FROZEN)** — "most of the following is only when you speak to Justin directly," but about half the bullets bind a spawned DC (timestamps, work stamps, Node `fetch`, the Railway-logs trap), and "Railway CLI is yours … `railway up`" reads as permission to deploy on a spawned trip. → **Agree. Ballot — it is Justin's text; DC suggests moving the general bullets to a "Working environment" section and splitting the Railway bullet.**
6. **Session-startup list (unchanged)** — a spawned DC cannot do three of four steps as written: deleting `settings.local.json` may delete the PARENT session's file; it cannot ask Justin about `#dev-alerts`; the canonical-branch check fails in an assigned worktree. **The same steps exist in `qc.md` and `oc.md`.** → **Agree. Ballot — split startup by arrival mode, in all three manuals.**
7. **Provenance lines 24, 35, 41, 331** — false now. → **Agree. Cleanup (§5 item 4).**
8. **Line 331 vs `dc-lock-protocol.md`** — the mailbox material spans steps 3, 4, 6 and 7 (not two); step 5 was already corrected, so the file's own banner is stale on that point; steps 8 and 10 still say "CLAUDE.md". → **Agree. Part of the ⑨ reconcile.**
9. **Line 234** — the withdrawn four-item identity list, and it matters because line 237 tells DC to load identity files itself when a brief omits them. → **Agree. Cleanup (§5 item 2).**
10. **Line 207 "(or Soren)"** reads two ways, and "AC owns this file" contradicts the protocol (Soren owns ARCHITECTURE of record; AC is co-owner). → **Agree. Cleanup — the parenthetical is Justin's, so it goes to his vet as a proposal.**
11. **Line 213** — "(which it assembles …)" refers to AC → "he". → **Agree. Cleanup.**
12. **"CLAUDE.md" still at lines 42 and 343.** Outside the repo: the auto-memory stub loaded every session points to an "Auto-Memory Hygiene section in CLAUDE.md" that the router no longer has. → **Agree on 42/343 (cleanup). The stub: flag to Justin — `ac.md` forbids writing the auto-memory file.**
13. **Lines 140, 292** — bold on swapped-in phrases marks the edit, not the rule. → **Agree. Cleanup.**
14. **Line 298** lost its reading trigger for the orchestration protocol. → **Agree. Cleanup.**
15. **Line 306** mixes two inbox models (per-designation addresses vs. lineage-shared inboxes). → **Partly: the ambiguity is the mailbox protocol's. Note to Justin; minimal `dc.md` change.**
16. **Lines 102–103** — beginner-pitched bullets under the new learning-curve line. → **Agree on the example bullet (ruling ⑧). Disagree on "Breadcrumb everything": `ac.md`'s EliJ section keeps it.**

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
