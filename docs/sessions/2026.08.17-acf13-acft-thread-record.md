🔒 **HELD-SESSION-DOC** — ACT-lineage session doc, moved here when `docs/working/` was retired · **RETIRE UPON:** ACT has evaluated it · **OWNER:** ACT · **HELD SINCE:** 2026-08-22

# LANE 1 (ACft) — The thread as the agent's complete record (ADR-055 §H)

**Lane AC: `ACft`. Conductor: ACf-13 (or its successor).** Opened 2026-08-17.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file:** your brief is §1–§5. **Everything below the line is a running two-way exchange — append, never overwrite.** The mechanics of that exchange (commit immediately, batch questions, your hard limits, how to spin your own DC/QC) are in **`_evryn-meta/docs/working/2026.08.17-acf13-lane-protocol.md` — read it first; it is short and it is binding.**

---

## §0 — 🔴 YOUR FIRST ACTION, BEFORE ANY WORK

**Append an entry to this doc, and commit it, BEFORE you load anything.** It carries three things:

1. **You are alive** — name your worktree and branch.
2. 🔴 **Every question or concern you have about your ability to work through the NIGHT, unsupervised.** Not about the work — about the *conditions*. Anything you can foresee that would stall you at 2am with nobody awake: a gate you will hit, a permission you will need, a tool you are not sure you have, an instruction in this brief that contradicts another. **Say it now, while the conductor is still here to answer.**
3. **Anything in the brief you think is wrong.** You have not read the runtime yet, so this is about the brief's internal logic, not its facts.

**Why this comes first: the conductor and you have never run this channel before.** It proves the round trip works *before* either of us builds on the assumption that it does — **and it is the last cheap moment to catch a condition that would end your night silently.** *(The conductor already caught one this way: an early draft of the protocol forbade the very commit this channel depends on.)*

**Do not start the load until that commit lands.**


## §0b — 🟢 WHAT YOU MAY FINISH WITHOUT ASKING — and the gates that never move

*(Added 2026-08-18 by ACf-15, at Justin's direction. **This section exists because your brief told you to "flag anything that needs Justin" but never stated the boundary POSITIVELY — so at 2am you would have had to guess, and a lane that guesses conservatively stalls and loses the night.**)*

### 🔒 The gates, first — these do NOT relax because Justin is asleep

**No merge. No push to `main` in your build repo. No deploy. No migration APPLIED** *(write migration files freely — applying them is Justin's act)*. **No identity-file edit** — `evryn-team-workspace/.claude/agents/*`, agent memory, the team `CLAUDE.md`, `shared/protocols/runtime-ops.md`. 🚫 **No write anywhere under a `.claude/` directory.** ⚠️ **CORRECTED 2026-08-18 — the reason previously given here was wrong, and a gate with a false reason is one a lane will eventually discover it can walk through.** It said such a write *"HANGS waiting for authorization."* **That is TOOL-SPECIFIC, not path-specific: the dedicated Write/Edit tools prompt there; a Bash write goes straight through** *(observed — AC0 edited `.claude/agents/ac.md` via Bash today)*. ⇒ **So the real gate is OWNERSHIP, not mechanics: those files belong to AC0 and Justin, and tonight AC0 holds the pen on them.** **Do not write there because they are not yours — not because you cannot.** *(Corollary: the startup instruction to delete `.claude/settings.local.json` is doable rather than blocked — check whether the file exists instead of assuming you are stuck.)*

**An overnight lane does the work that is already cleared and STOPS AT EVERY GATE, leaving them batched for morning.** That is not caution; it is what makes running unattended safe rather than reckless.

### 🟡 How to handle a question that is genuinely Justin's

**Flag it `NEEDS-JUSTIN` in your lane doc with your recommendation attached, and KEEP ADVANCING everything not blocked on it.** The conductor routes it; you never route to Justin yourself.

🔑 **Asymmetric default — when unsure whether something needs him, FLAG IT.** Cost of over-flagging: the conductor spends two seconds. Cost of under-flagging: **Justin never learns the question existed.** Those are not close. ⚠️ **And the conductor may never silently drop your flag** — every one is either surfaced to Justin or answered visibly, so flagging costs you nothing.


### ✅ CLEARED — finish these unattended without asking

- **The design pass itself** — the diff format, the content-addressed version store, how bodies and diffs compose in position. **Reading the runtime is always cleared.**
- **Writing the migration FILE** for the new message class *(composed into the thread, wakes nobody, does not reset the dormancy clock, does not mirror to Slack)*. **Write it; never apply it.**
- **Building the composition changes on your branch**, including removing `windowThread()` and building the warning ladder.

### 🔴 NOT cleared — stop and flag instead

- 🔴 **Data governance — this is the one that will bite you, and it is not a formality.** Your change creates **a new durable copy of arbitrary file content in our database.** ADR-050's data rule applies and **Nathan (Internal Counsel) should see it before it ships.** ⇒ **Build it; flag `NEEDS-JUSTIN` at the point where it would SHIP.** Do not treat your own read of the data rule as clearance.
- **The three questions ADR-055 explicitly left open** — the exact diff format, what happens to a *deleted* file (a diff against nothing), and whether current-contents composition covers every touched file or only those still referenced. **Decide what you can defend, record your reasoning here, and flag the ones you cannot.**
- **The retention sweep.** ⚠️ **Build it deliberately, NEVER as a scheduled TTL** — the product lane carries a live case where a built-but-never-scheduled deletion job would have destroyed a real backlog the moment someone "closed the gap."
- **Anything touching the DISPATCH path** in `src/wakes/index.ts` — that is the money lane's. Say so here rather than reaching in.


## §1 — What you are building, and why it is not optional

**Read `_evryn-meta/docs/decisions/055-thread-as-complete-record.md` in full before anything else. It is the decision and, more importantly, it records the alternatives that were rejected and why — each has been proposed more than once, including by the conductor who then had to be corrected.**

**The problem in one sentence:** every wake is a brand-new conversation, so **anything an agent read last wake is gone this wake** — and it has no signal that it is gone. Justin's statement of it, which is the canonical one: *"you think you know what's in a file — you fucking don't. He'll be lobotomized all the time and won't know it."*

🔴 **The rule that must survive any redesign you propose: DO NOT make this retrievable-on-request.** A query-on-demand shape **re-creates compaction at the file level** — the agent holds a reference, reasons confidently from what it believes the content was, and never queries, **because nothing tells it that it does not know.** If your design has the agent asking for something, it is wrong.

## §2 — Scope: SPRINT Steps 68 and 69

**`evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` §H carries both in full. They are the spec; this brief does not restate them.**

- **Step 68** — full file bodies into the thread in position at first read; diffs in position where content moved; tool calls, results and a bounded distillation of reasoning the same way; **current file contents composed as their own layer so the agent never derives present state from a history of diffs.** Needs a migration (a new `message_type` that is composed into the thread but does **not** wake anyone, does **not** reset the dormancy clock, and does **not** mirror to Slack — two of those three filters already exist for `heartbeat`; **the third is the genuinely new combination**).
- **Step 69** — remove `windowThread()`; replace with the graduated ladder (**700k / 800k / 900k / 950k**, then a loud refusal), **posted deterministically into the room from 800k up.** `THREAD_HARD_CEILING_TOKENS = 200_000` is superseded — it sits at **20% of the real 1M window**.

⚠️ **Step 70 (thread-creation verbs) is ACfv's (Lane 3), not yours**, even though ADR-055 groups it with these. Do not build it. **Coordinate through this doc if you find a genuine coupling.**

## §3 — Your territory, and the one collision that matters

**Yours:** `src/composer/**` · the composition path in `src/wakes/index.ts` · your new migration · tests for the above.

🔴 **`src/wakes/index.ts` is SHARED with ACfj (Lane 2, the money layer), which touches the DISPATCH path in the same file.** **ACfj is doing a design pass first and will not build until after you merge** — that sequencing exists specifically to keep you out of each other's way. **If you find yourself needing to change dispatch, stop and say so in this doc rather than reaching into it.**

## §4 — What will make this hard, so you are not surprised

- **The size question is the design.** Composing full bodies makes wakes bigger *on purpose*. **Justin: *"I'd rather have them smart than cheap any day."*** The cost control is that a body is stored **once** and later reads are diffs — not that anything is omitted.
- **The runtime keeps a content-addressed store of versions so it can compute the next diff.** ⚠️ **That store is runtime-internal working material, NOT an agent-facing feature.** The agent never queries it. **Getting this backwards is exactly the rejected design.**
- **Data governance:** this creates a new durable copy of arbitrary file content in our DB. **Flag it as `NEEDS-JUSTIN` before it ships** — ADR-050's Tier-A/Tier-B rule applies.
- **Retention** ties to the thread lifecycle, dying at dormancy *after* the closing memory has had its chance. ⚠️ **Build the sweep deliberately, never as a scheduled TTL** — the product lane carries a live case where a built-but-never-scheduled deletion job would have destroyed a real backlog the moment someone "closed the gap."

## §5 — How much the conductor has actually read, so you weigh its claims correctly

**ACf-13 is running the conductor cascade. It has read the tracker, ARCHITECTURE, the changelog and ADR-055 — it wrote most of them — but it has read almost none of `src/`.** **You will be the better-informed party on every question of fact about the runtime.** Treat every factual claim in this brief as a **lead, not a fact**, including any line number. **Say so loudly and early if any of it is wrong.** And where this brief is silent, do not read that as *"the conductor checked and there was nothing there"* — assume instead that it could not see it.

---

## THE EXCHANGE — append below

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
