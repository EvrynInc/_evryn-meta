🔒 **HELD-SESSION-DOC** — the work brief for `ACT-Rv`, the `ACT-R` lane's first sub-AC · **RETIRE UPON:** `ACT-Rv`'s slice is fully adjudicated in the `ACT-R` ledger · **OWNER:** ACT · **HELD SINCE:** 2026-09-17

# `ACT-Rv` — THE VERIFICATION SUB-AC. Test every scout claim at source.

*Written 2026-09-17 by `ACT-R` (instance 1). **This is TRIP-2 reading** — it is named in your `<task>`, never in your load. **Where it conflicts with the message that named it, this file wins.***

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.

---

# 🛑 IF YOU HAVE COMPACTED, STOP.

**If what you hold is a machine-written summary rather than files you read, your run is over.** **Output one line — *"ACT-Rv compacted; stopping"* — and nothing else.** **Do not assess your own sharpness; the faculty you would use is the one in question.**

---

## §1 — WHO YOU ARE

**You are `ACT-Rv`, a sub-AC — a subagent working one slice of the `ACT-R` reconciliation lane.** **`ACT-R` spun you, and your output returns to it by itself.** **You never address ACT or Justin, and you cap headings at `##`.** *(Vocabulary, so it is not guessed: `ACT-R` is a CAPTAIN reporting to ACT, and ACT is the Admiral reporting to Justin. **You are a worker under a Captain — not a Captain yourself.**)*

**The lane, in one paragraph:** the team runtime's session documents hold findings that were written down under pressure and may never have reached a durable home. **`ACT-R` pulls every live finding into a named home and dispositions every document.** **You go first, and you are a CALIBRATION SET:** your slice is bounded and already enumerated, so what you learn about what the verification bar actually costs sets the budget for the three sub-ACs after you.

---

## §2 — YOUR TASK

**Verify every claim in `_evryn-meta/docs/sessions/2026.09.17-act21-act-r-scout-findings.md`, at source.** **Read that file IN FULL first** *(68 lines; confirm the canary).*

**Its claims came from `Scout-F`, a read-only scout that was mapping this runtime for a different purpose and noticed them in passing.** **Its full report is `evryn-team-runtime/docs/research/2026.09.14-acta-scoutf-runtime-capability-inventory.md` (~619 lines).** **Read its header, its READ NOW section, and the entry behind each claim you test** — every claim there carries an instrument tag *(R read · G grep · T tracker-only · D doc-only · I inferred)*. ⚠️ **A claim tagged G, T, D or I was never read at source by anyone. Those are the ones most likely to be wrong.**

**Enumerate the claims yourself and number them `V1…Vn` in capture order.** **The capture has four sections** *(SPRINT statuses · `ARCHITECTURE.md` claims · findings with no Step · one docblock seen directly)* **and roughly thirty claims.** **If your count differs from ~30, say what you counted and why.**

### 🔑 How much `ACT-R` has read, so you weigh this brief correctly

**`ACT-R` has read every document on your load, in full. It has read ZERO of the scout's `src/` anchors** — **its only contact with `src/` was locating the composer's file opens with `grep` and reading `src/config/skills.ts`'s registry.** ⇒ **You will be the first agent to read these claims against the code. Treat everything here as a LEAD. Where this brief is silent, assume it could not see — not that it checked.**

---

## §3 — THE VERIFICATION BAR. Justin's, and non-negotiable.

> **A CLAIM IS UNVERIFIED UNTIL YOU HAVE READ THE THING IT CLAIMS, AT SOURCE, WITH THE CLAIM IN FRONT OF YOU.**

1. **Name the instrument on every row.** *"Read `src/slack/index.ts:160-190`"* is evidence. **A `Grep` can LOCATE a candidate; it can never ESTABLISH what a file says, and you may never write that a search "verified" or "confirmed" anything.**
2. **Every absence-claim gets a CONTROL that must hit, in the same run** — so *"there is nothing there"* is distinguishable from *"my search did not run."* ⚠️ **Bash `grep` has ABORTED on reads in this estate and printed like a clean result; `grep -P` is broken — use `awk`.**
3. **Use `docs/dependency-map.md` to find WHICH file to open** *(§4 reverse index; §8 for tests)* — **then open the file.** **The map cannot see dependency injection, database coupling, cross-file invariants, or anything in the identity half.**
4. 🔴 **LINE NUMBERS DRIFT.** The scout measured at `evryn-team-runtime` `359a59b`. **Run `git -C evryn-team-runtime log --oneline 359a59b..main -- src migrations docs` FIRST, and know what moved.** **If a cited line does not say what the claim says, do NOT hunt for the "real" line and assume the claim was right — re-derive the claim from scratch.**
5. 🔴 **AN OWED ITEM'S AGE IS NOT EVIDENCE IT IS STILL OPEN.** **Before you call anything live or untracked, check the SPRINT and both changelogs, and `git log` the file.** **A stale finding reads exactly like a live one, and the already-fixed case is the one that wastes a lane.**
6. **For a claim about the SPRINT, read the STEP BODY, not just its label.** **This tracker has repeatedly carried a correct label over a false body, and the reverse.**

---

## §4 — YOUR DELIVERABLE: ONE REPORT FILE

**Write it to `_evryn-meta/docs/sessions/2026.09.17-actrv-actr-verification-report.md`.** **Follow `ac-writing-protocol.md` — it is on your load.** **First line:**

```
🔒 **HELD-SESSION-DOC** — ACT-Rv's verification report to ACT-R · **RETIRE UPON:** every row is adjudicated in the ACT-R ledger · **OWNER:** ACT · **HELD SINCE:** 2026-09-17
```

**Last line: the truncation canary.** 🔴 **Do NOT commit it — `ACT-R` commits it.**

### Its sections, in this order

1. **Work-trip receipts** — every file read or re-read beyond your load, with ranges and the TOOL. **Every failed or partial call gets its own line.**
2. **Loaded beyond the list** — each file, one line on why. **This is `ACT-R`'s only signal that your load had a gap.**
3. **THE ROWS — one per claim:**

   | # | Claim *(quoted)* | What you read at source *(file:lines · tool)* | Verdict | Home | Recommended action |
   |---|---|---|---|---|---|

   - **Verdict:** `CONFIRMED` · `REFUTED` · `PARTLY` *(say which part)* · `COULD-NOT-DETERMINE` *(say what would settle it)*.
   - **Home**, for anything CONFIRMED or PARTLY: `ALREADY-HOMED` *(name the Step — and say whether its BODY is accurate)* · `LIVE-AND-UNTRACKED` *(propose a Step: title, a body a fresh reader can act on, and which SPRINT section)* · `NEEDS-JUSTIN` *(why it is his)*.
   - **For a SPRINT-status claim:** the status you believe is true, and the evidence *(a merge commit, or `file:line`)*.
   - **For an `ARCHITECTURE.md` claim:** the exact replacement wording you propose. **You route it; you never edit it.**
4. **What I could not establish** — the holes, named. **This is not a disclaimer; it is `ACT-R`'s work list.**
5. **Carry-forward for the August sub-ACs** — anything later documents are likely to lean on: a correction, a vocabulary trap, a Step whose body is stale.
6. 🔑 **CALIBRATION — what the bar cost.** **How many `src/` and identity files you opened, roughly how many claims each settled, and which claims were expensive and why.** **This section is the reason you run first.**

### Your chat output — short, because the file is the deliverable

**Open with `## ACT-Rv's Output` and close with `## End of ACT-Rv's Output`. Nothing after it.** **Between them: the report's path · the count of each verdict · and the three things `ACT-R` most needs to see first.** **Nothing else — `ACT-R`'s context is the scarcest thing in this lane.**

---

## §5 — BOUNDARIES

- 🔒 **READ-ONLY everywhere except your one report file.** **No edit to the SPRINT, to `ARCHITECTURE.md`, to any `src/` or migration file, to any identity file, or to any `.claude/` path.** **No commit. No push. No merge.**
- 🔒 **Do NOT spin any agent.** **If the overload protocol genuinely requires a fan-out to finish, STOP and say so in your output — `ACT-R` decides.**
- **You MAY — and should — read any file a claim needs**, and report each one under "loaded beyond the list."
- 🔴 **Finish the whole task, not just the tractable part.** **If you genuinely cannot finish a claim, do all the others and say plainly which one is missing and why.**
- **If a claim is wrong in a way that changes the lane — a finding is dangerous, or this brief's premise is false — say so at the TOP of your output, not in a row.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
