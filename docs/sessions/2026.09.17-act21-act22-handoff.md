🔒 **HELD-SESSION-DOC** — the LIVE ACT working brief · **RETIRE UPON:** superseded by ACT-22's own handoff at set-down · **OWNER:** ACT · **HELD SINCE:** 2026-09-17

# ACT-21 → ACT-22 — handoff. **Self-sufficient. You need no earlier handoff.**

*Written 2026-09-17 by **ACT-21**, the team-runtime conductor, at `#packout`, on the **LAPTOP**. **ACT-21 never compacted by accident.** **Justin compacts it DELIBERATELY (`/compact`) to re-spin it in place, at ~890K of a 1M window — see §0 for exactly what that means and what to do.** **SUPERSEDES `2026.09.11-act20-act21-handoff.md`**, which had layered — a quarantine section, six correction rows pointing at other sections, and two backlog rows appended mid-session.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file:** ACT's live state. **The test at every append: if this instance died right now, could a fresh one pick up from this file alone?**

---

# 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP HERE.

**If what you hold is a machine-written summary of your session rather than files you actually read — you have compacted, and your run is over.** 🔴 **DO NOT RELOAD AND CONTINUE.** **Tell Justin in one line and ask for a fresh re-spin.** 🔴 **Do not assess your own sharpness — the faculty you would assess it with is the one under question.**

---

## §0 — ✅ THE ONE EXCEPTION TO THE STOP RULE ABOVE: JUSTIN RE-SPINS THIS LANE IN PLACE, ON PURPOSE

**He compacts this session deliberately (`/compact`) and then tells you, in an explicit message, to reload in place.** *(Proven repeatedly on ACT-A; first used on this lane 2026-09-18.)* 🔴 **But it is HIS instruction in the moment — never yours to infer, and never something you do because this paragraph exists. No explicit message telling you to reload ⇒ the rule above governs: STOP.**

**Why `/compact` and not `/clear`:** *"I'd much rather have direct access to the subagents — not having to try to harvest stuff over to a new instance."* **`/clear` starts a brand-new session and forfeits this one's subagents; `/compact` leaves a short summary and keeps them resumable.** *(`/clear` is "dead for us" — Justin.)*

**When he tells you to reload, this is the whole of it:**

1. 🔴 **TREAT YOUR COMPACTION SUMMARY AS UNRELIABLE — UP TO AND INCLUDING ACTIVELY MISLEADING.** **It is a lossy, third-party compression: it leaves important things out and states things that are subtly or flatly wrong, with no marker on them.** ⇒ **Where it disagrees with THIS FILE, the file wins — every time, without deliberation.** ⚠️ **A good summary is the danger, not the safety net: it reads like knowing.**
2. **Reload §2 from DISK, in full, confirming every bottom canary.** ⚠️ **You may feel you already hold those files. That feeling is the summary talking.**
3. **Report what you RELOADED — files, spans, canaries — never how sharp you feel** — and invite Justin to probe you on details that never reached chat. **Getting one wrong is the most valuable output available.**
4. **Re-derive every judgement you inherit** — ACT-21's read of ACTj's proposal, its recommendations, its cautions — **against the files, rather than defending it because a previous you concluded it.** ⚠️ **The tell is a conclusion you can state but cannot reconstruct the evidence for.**

**What an in-place re-spin leaves you:**
- **The same session address as ACT-21.** **Re-check it with `ListAgents` anyway.**
- **Any subagents ACT-21 spawned would still be resumable — it spawned NONE.** Every other agent in §3 is a standalone session.
- 🔴 **NO monitors — ACT-21 killed its own at set-down.** **Arm yours — §4.** ⚠️ **ACT-21 ran most of its session without an inbox watcher, and a Captain sat blocked on it for an hour.**

---

> # 🔴🔴 READ JUSTIN BACK INTO THE ROOM — EVERY SINGLE TIME. THIS HAS WASTED MORE OF HIS TIME THAN ANY OTHER FAILURE, AND ACT-22 DID IT AGAIN ON 2026-09-18.
>
> **Justin, 2026-09-18, on a report that said *"F3: your ruling's premise is real"*: "I can't tell you how inscrutable a sentence like this is after I come in from a completely different context — my mind is 1000 cycles away from whatever my 'ruling' was. You have to read me back into the room. I'm not reading *anything* you're reading. *You* are my sole conduit to understanding. So 'F3' means *precisely* zero to me. And consequently… *none* of it makes sense."**
>
> - 🔴 **EVERY reference, EVERY time — not just the first time.** *"F5"*, *"Step 85"*, *"your ruling"*, *"QCs"*, *"the re-read"* — **each gets a clause saying what it IS, in the same sentence, every time it appears in a new report.** ⚠️ **Do NOT assume one explanation carries forward:** *"when you later refer to F5, you'll need to recontextualize it again — too many mental cycles in the meantime."*
> - ⭐ **What WORKS, in his words — "beautiful":** a table that says, per item, **what the thing is and what you are doing with it.** **"Often just a super-quick line is enough — as long as it's well-constructed for clarity."**
> - 🔴 **"Your ruling" is the worst offender: he does not remember which ruling. Restate the ruling itself** — *"your 09-11 rule that a damaged memory refuses the wake after one re-read"* — **never the pronoun.**
> - ⚠️ **And do not get far ahead of him:** *"Be careful getting too far ahead of me — you'll find you're just re-doing work."* **Answers to early reports routinely change later work. Pause for him to catch up before building on unanswered ballots.**

> # 🔴🔴 EVERYTHING THAT NEEDS JUSTIN'S EYES GOES *INSIDE* A NUMBERED REPORT. HE SKIMS STRAIGHT PAST EVERYTHING ELSE.
> **Justin, 2026-09-18: "I see a lot of stuff between reports — just want to make sure you know I'm skimming straight past all of that — so make sure your handoff says clearly that *all* info that needs my eyes must be *within* a report."** **A note between tool calls, a status line, a question in passing — he does not see it.** **`# ACT-N's Report #N` … `## End of Report`, or it was not said.**

> # 🟠 HOW THIS DOMAIN RUNS RIGHT NOW (Justin, 2026-09-18) — until `ACT-A` homes the fleet-wide versions
> - **BUILDS ARE PAUSED.** **`ACT-A`** *(the alternate-branch ACT asking whether the team's autonomy should stay on this SDK runtime at all)* **may conclude parts of it need not be built.** ⇒ **Step 85's memory-grading fix and the money-layer build (`DCj`) are ON HOLD.** **Get everyone to a stoppable place; start nothing new.**
> - 🔴 **THE PRIORITY IS `ACT-R`'s HARVEST** — ACT's 41 old session docs, every live finding moved into a durable home, and the tracker made right. **That is what unblocks `ACT-A`.** **The Atlas moves LATER** *(Justin still wants it)*. **`ACT-A` also needs the list of every durable doc the harvest wrote into, with a baseline commit per doc, so he can diff.**
> - **Re-spin IN PLACE is the practice** *(`/compact` + an explicit reload message; `/clear` is dead)* — **with the compaction-summary-can-actively-mislead warning in every handoff's top block.** **`ACT-A` homes it fleet-wide; the notes to him are `docs/sessions/2026.09.18-act22-act-a-notes.md`** *(append there; he has no inbox)*.
> - 🔴 **DC AND QC ARE ALWAYS SUBAGENTS OF AN AC — never standalone sessions running their own show** *(Justin, 2026-09-18: "they wake, do their narrow job and output. That's it.")*. ⇒ **`DCj`'s standalone brief is WITHDRAWN; when the money layer resumes, its DC runs under an AC.** **Every subagent's output is extracted VERBATIM to a committed file as it lands, and this handoff lists each live subagent's `agentId` + file** — **the compaction summary will compress their outputs to near-nothing.** *(Currently live: `DCn`, agent `a425bd9d3c5732431`, Step 85's builder — idle, resumable; its prior output is `docs/sessions/2026.09.11-dcn-act-step85-refusal-setdown.md`.)*
> - 🔴 **A LANE THAT WILL SPIN DC/QC LOADS `ac-orchestration-protocol.md` + `orchestration/spinning-dc-qc.md` FROM THE START** *(Justin, Ballot ⑱ — "it also shapes your thinking")*.
> - **PACKOUT TIMING, Justin's ruling:** **offer a packout at ~800K measured; at ~850K with no packout authorization from your spinner or chain of command, PAUSE all other work** — only: say you are paused, commit what is already written, measure, kill the heartbeat. **Admirals: do not tell a Captain to pack out while it holds unresolved work — pause it instead.** *(Fleet-wide version: `ACT-A`, notes §2.)*

## §0a — 🟢 ACT-22 LIVE STATE — updated as work lands. **Where this block and §1–§11 disagree, THIS block is current.**

*Last updated 2026-09-18T11:04:38-07:00 by `ACT-22` (reloaded in place 09:04 PT; load verified by Justin via his ballot answers).*

- **Reports to Justin: #1–#3 sent. Ballots ⑥ ⑦ ANSWERED; ⑧–⑪ OPEN** — ⑧ `cap_usd` default (rec. $10/job) · ⑨ fire the 60% warning on spend alone (rec. b) · ⑩ fold the `runtime-ops.md` `approve` fix into Step 35's batch (rec. a) · ⑪ add QCs's F3 "wait for the workspace git lock, then re-read" to the F1 fix trip (rec. a).
- 🔴 **JUSTIN'S STANDING POSTURE ADDITION (Ballot ⑥, 2026-09-18):** **an inbound ask from outside today's work does NOT get auto-served.** *Super* easy and cheap → help. Otherwise → **pitch it to Justin first**; he decides whether ACT can be spared.
- **Instruments: heartbeat (5 min) + a commit-gated watcher on `inbox-act.md`, the ACTj exchange doc and the QCs brief.** Both proven. ⚠️ **30-minute cap — re-arm on each expiry notice.**
- **`ACTj`: ANSWERED** — exchange entry `2026-09-18T10:57:28` (`8645985`): **all eight parts signed off; one mechanism to confirm at source** (what actually serializes §3.1's get-or-create); **build authorized** except the cap VALUE (⑧) and `budget.ts` (⑨). **Her worktree fast-forwarded `b3488ae` → `0f0d6c2`** (docs-only moves). **Justin nudges her; she reports whether her watcher woke her, her address and her context.**
- **`QCs`: RELEASED** (`dab2fe6`). 🔴 **Protocol: a verification QC must be FRESH** (`spinning-dc-qc.md`) — so the F1 re-verify goes to a **fresh** QC, handed her F1 probe method verbatim. **Her four standing patterns are OWED to Justin for promotion into `qc.md`.**
- **Step 85 fix trip — vehicle: RESUME `DCn`**, agent id **`a425bd9d3c5732431`** — the DC that built the refusal, a subagent of THIS session (ACT-20 ran here). **~727K context at its last turn → tight scope; if it nears 850K it writes a set-down and a fresh DC takes over.** **Scope: F1 (both FIX lines + literal assertions shown RED on revert) · F6's four guards · correct the "sync runs only at boot" comment · + F3's lock-wait IF ⑪ = (a).** ⏳ **HELD until ⑪ is answered.**
- **SPRINT updated and pushed (`0d631a3`):** Step 85 review recorded + its spec's bad repair wording corrected · Step 46 re-scoped (F2) · **Steps 90 (F4), 91 (F5), 92 (senderPriority p1 unreachable), 93 (F7 = backlog B33)** filed. ⇒ **B33 now has a tracker home; Steps 46/86/91/93 are sequenced after `ACTj` (`src/wakes/`).**
- ⚠️ **Session addresses churn** — ACT-22's changed twice in ten minutes (`evryn-meta-4f` → `evryn-meta-53`). **Files are the channel; addresses are for nudges only, re-read from `ListAgents` each time.**
- 🔎 **Compaction audit (transcript, not summary):** this session compacted twice — **2026-09-11 16:45 PT auto at 970K (ACT-20's)** and **2026-09-18 09:04 PT manual (Justin's re-spin)**. **ACT-21 never compacted by accident.**
- 🆕 **12:38 PT — THE MONEY LAYER IS NOW `DCj`'s TO BUILD, AND `ACTj` IS PACKING OUT (no successor).** `ACTj` confirmed §3.1 at source (no index), caught an orphan-job defect in her own check-in placement (fixed: the check-in mints only after `dispatch`'s gate + lock), and could not conduct a DC from her seat (~800K; the orchestration protocol would not fit) — **so she wrote a build spec** (`2026.09.18-actj-dc-money-layer-build-spec.md`) **and DCj's load list** (`2026.09.18-actj-dcj-money-layer-load-list.md`). **ACT-22 wrote DCj's brief** (`2026.09.18-act22-dcj-money-layer-brief.md`) **and opened its log** (`2026.09.18-act22-dcj-log.md`), **and cut its worktree** — `evryn-team-runtime-dcj`, `dcj/money-layer` @ `0f0d6c2`, `npm ci` inside (146). **DCj is STANDALONE** (Justin spins it); **it reports in its log AND with a pointer line in `inbox-act.md` at trip 1 and at its final report** — so any ACT instance's inbox watcher catches it. **Next for DCj: verify its trip-1 receipts in its log, sign off, then a FRESH QC reviews its build; ⓵ `cap_usd` (Ballot ⑧) and the 60% gate (Ballot ⑨) gate the MERGE, not the build.**
- 🧪 **Justin's watcher test, answered:** ACTj was **nudged, not woken** — a watcher does not survive a session restart. **Ballot ⑱ opened: ACTj's method finding** (a lane that will spin a DC must carry `ac-orchestration-protocol.md` + `spinning-dc-qc.md` in its load budget from the start) → `spinning-an-ac.md`.
- ⚠️ **ACT-22 context ~705K at 12:38 — "plan a packout."** **Plan: resume `DCn` on ⑪, hand Justin DCj's spin line, then pack out at a clean boundary; Step 85's fresh QC + merge and DCj's adjudication go to ACT-23 (re-spin IN PLACE keeps `DCn` resumable).**
- 🆕 **13:20 PT — BALLOTS ANSWERED (Reports #2–#5): ⑧ $10/job to start → banner in `proposal-protocol.md` (UNCOMMITTED, Justin's vet) + Step 94 · ⑨ his money model → recorded verbatim in SPRINT Step 19 · ⑩ (a) → Step 35 · ⑫ (a) → QCs's 4 patterns written into `qc.md` (UNCOMMITTED, vet) · ⑬ → the packout-timing ruling above, to `ACT-A` · ⑭ → re-spin-in-place homed by `ACT-A` · ⑮ (a) → standalone-output bullet in `ac-orchestration-protocol.md` (UNCOMMITTED, vet) · ⑯ 16.1+16.2 yes → `doc-update-routing-protocol.md` (UNCOMMITTED, vet) · ⑰ (a) → `current-state.md` corrected and COMMITTED (`5c9b614`).** **STILL OPEN: ⑪ (Step 85 re-read — moot while builds pause) · ⑱ (ACTj's lane-load finding) · ⑲ (money-layer successor) · ⑳ (bill jobs by room).** **UNCOMMITTED ELSEWHERE: `ACTj`'s `CHANGELOG.md` entry (her packout; Justin's vet).**
- **ACT-R:** load revised (nothing removed — two files moved to named triggers with ignorance warnings; in-place exception added to his load list + ledger); **his concurrence requested in ledger §12 (13:08) — Justin to nudge his current session; then ACT hands Justin the IN-PLACE reload paste block.**
- 🔴 **13:45 PT — JUSTIN IS CATCHING UP ON REPORTS #7–#13; ACT-22 IS PERSISTING, NOT ACTING** *(his instruction: "we have to prioritize persisting you")*. **Any note of his that arrives after this line and is not yet answered in a report is ACT-23's to answer.** **Report #7's DCj spin line is SUPERSEDED — DCj is on hold and its standalone plan is withdrawn.** **ACTj was a full AC, properly loaded** (her only gap was ⑱) — Justin briefly thought she might be a scout; Report #14 answered it.
- 🆕 **14:08 PT — Justin's Report #7/#8 answers, APPLIED:** `CHANGELOG.md` (ACTj's entry) + `qc.md` + `ac-orchestration-protocol.md` (with Justin's tweaks) + `doc-update-routing-protocol.md` **COMMITTED** (`c0fbcbd`, `1e3b976`). **⑨ correction: a job with no approved amount has a ZERO limit** — SPRINT Steps 19 + 94 corrected (`5aa5795`); **the `proposal-protocol.md` banner rewritten to match — UNCOMMITTED, awaiting Justin's vet.** **⑳ DECIDED: jobs bind to THREADS** (owner polices off-topic talk; "thread" vs "room" naming left to the design pass). **⑲: Justin prefers PAUSING people in an easy-to-resume state over closing them — but ACTj had already closed cleanly (no subagents, worktree reaped, nothing half-built), so nothing is hanging and nothing is owed; the money layer's next lane is spun fresh when builds resume.** **Step 95 (breaker parity) is filed.** **Priority stays: make the current state clear for ACT-A.**
- **Next, in order:** ⑪ answer → resume `DCn` → fresh QC re-verify → merge `--no-ff` + reap `-dcm`/`-qcs` + regen depmap + changelog (+ ARCHITECTURE line needs Justin's go) · **ACT-R's lighter-load decision → hand Justin his spin line** · the four unhomed rulings (§8) as one wording batch · `current-state.md` corrections (B32, "four dispatch sites", "three agents working") for Justin's vet · QCs's patterns for promotion.

---

## §1 — 🔴 WHAT TO DO FIRST

**The lane in one line: three lanes in your domain — one has been waiting on this seat for six days, one has handed you a NO-GO with a one-line fix, and one packed out cleanly and is re-spun tomorrow.**

1. **Load §2.** **It is complete and it is here.**
2. **Ask Justin the two spin-up questions** — *read my inbox?* and *arm a watcher?* 🔴 **Recommend YES to both, and say why: it is how ACT-R reports once he is re-spun, and how any QC or DC you run on Step 85 will report.** *(At ACT-21's set-down no agent was live — ACT-R had packed out and QCs had finished — so an empty inbox at your spin-up is expected, not a fault.)*
3. 🔴 **ANSWER `ACTj`. She has been parked on her design proposal since 2026-09-11 — SIX DAYS — and no ACT has answered it.** *(ACT-20 never read it; ACT-21 read it in full during its load and did not get to it.)* **It is the entry headed `### ACTj — 2026-09-11T15:22:07-07:00 — DESIGN PROPOSAL` in `docs/sessions/2026.09.08-act17-actj-exchange.md`.** **ACT-21's read, which you should check rather than inherit, is in §3 below.** **Confirm with Justin she is still running before you write to her.**
4. 🔴 **Step 85: QCs returned `NO-GO` on ONE blocker — a one-line fix to the refusal message's repair instruction.** **Her headline: "the engineering is sound… with F1 fixed, this is a GO."** **The six-step path to merge is in §3 — and step 2 is taking her F3 to Justin, because it tests his own one-re-read ruling.**
5. 🟡 **ACT-R packed out; Justin re-spins him tomorrow.** **Before that: decide his lighter-load proposal (§3, item ③), and hand Justin the NEW spin line in §3** — **it differs from the original because his first trip is done.**
6. **Then the Atlas** — §5. **It is sequenced BEHIND ACT-R's reconciliation, by Justin's ruling.**

---

## §2 — YOUR LOAD LIST

> 🔴 **NO LINE COUNTS, by Justin's ruling: "they're always stale when they get forwarded like this."** **Derive at read time:** `cd "$HOME/Evryn/Code" && wc -l <paths>`. ⚠️ **`wc -l` counts NEWLINES, so a file with no trailing newline reports one FEWER line than it has — and the line it drops is the CANARY. Read past the count.** **Confirm every canary.** **Read each repo's canonical `main` tree.**

**Read in this order. The order is DEPENDENCY, not importance.**

| # | File | Read |
|---|---|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | **full.** Your manual. ⚠️ **NOT auto-loaded** — the injected `CLAUDE.md` is a router. **It is too big for one `Read` — use windows.** |
| 2 | `_evryn-meta/docs/hub/roadmap.md` | full — the Hub |
| 3 | `_evryn-meta/docs/hub/technical-vision.md` | full |
| 4 | `evryn-team-runtime/docs/ARCHITECTURE.md` | full — **cardinal invariants 8 and 9, and "why local reasoning often fails here"** |
| 5 | `evryn-team-runtime/docs/BUILD-PHASE-1.md` | full |
| 6 | `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` | **full — the status source of truth.** ⚠️ **Too big for one `Read`.** |
| 7 | `evryn-team-runtime/docs/dependency-map.md` | 🔴 **FULL — INCLUDING the ~300 rows of its §4 reverse index.** *(Justin, 2026-09-17: "that'll give you the SHAPE of the runtime like nothing else will.")* **ACT-21 first skipped those rows as "lookup data" and was told to read them; he was right.** |
| 8 | `_evryn-meta/docs/current-state.md` | full |
| 9 | `_evryn-meta/CHANGELOG.md` | **the skim its own header prescribes** |
| 10 | `evryn-team-runtime/CHANGELOG.md` | **the same skim** |
| 11 | `_evryn-meta/docs/protocols/mailbox-protocol.md` **+** `docs/mailboxes/inbox-act.md` | full, both |
| 12 | **`docs/sessions/2026.09.08-act17-actj-money-layer-brief.md`** **+ `…-actj-exchange.md`** | **BOTH — the live money lane, waiting on you.** |
| 13 | `docs/sessions/2026.08.19-act-acta-atlas-brief.md` **+ `…-acta-exchange.md`** | **BOTH.** **The exchange is the only home of the hub reasoning, and it is the Atlas's foundation.** ⚠️ **`ACTa` (this 2026-08-19 Atlas lane, YOURS) is NOT `ACT-A` (Justin's side project, NOT yours). Near-identical filenames, different owners — see §7.** |
| 14 | `docs/sessions/2026.08.19-act-acti-guard-integrity-brief.md` | **lines 1–90 — its §3 banners.** **The lane-brief anti-pattern, and the record of Justin's ONE-LANE-FIRST sequencing.** |
| 15 | `_evryn-meta/docs/decisions/055-thread-as-complete-record.md` | full |
| 16 | `_evryn-meta/docs/protocols/orchestration/being-an-admiral.md` | full. **You are an Admiral (you report to Justin); ACT-R is your Captain.** |
| 17 | `_evryn-meta/docs/protocols/ac-writing-protocol.md` | full |
| 18 | 🆕 **`docs/sessions/2026.09.17-actr-actr-reconciliation-ledger.md`** | **full — ACT-R's lane brief and ledger. His live state.** |
| 19 | 🆕 **`docs/sessions/2026.09.17-act21-qcs-step85-review-brief.md`** | **full — what the Step 85 reviewer was asked, and how her output reaches you.** |

> ✅ **CHANGED FROM THE LIST ACT-21 INHERITED — surfaced loudly, per the packout rule:** **items 1–17 are carried VERBATIM in substance** *(item 7 now says to read the index rows — a correction Justin made, not a trim)*. **Items 18–19 are ADDED** — they are the two live lanes ACT-21 created. **Nothing removed.**

**Pull at their moment, not at spin-up:** `ac-orchestration-protocol.md` **+** the matching `orchestration/spinning-*.md` *(before you spin ANYTHING — the core alone is explicitly not enough; its first line says so)* · `lock-protocol.md` *(at a `#lock`)* · `packout-protocol.md` *(at a `#packout`)* · `doc-update-routing-protocol.md` *(when routing a finding)* · `docs/sessions/2026.09.11-dcn-act-step85-refusal-setdown.md` *(before you evaluate QCs's verdict — it holds the builder's own mutation table)*.

### 🔴 NOT on this list, deliberately: the runtime source.
**You are a conductor.** **ACT-21 read ZERO `src/` files all session, and briefed two lanes from the documents plus the dependency map** *(it did not answer the third — see §1)*. **Zero `src/` is enough to BRIEF a lane; it is not enough to judge a claim about code, so route those to an agent that reads the source.** **State your own number in every brief you write — every lane brief this lineage writes carries a "how much ACT has actually read" section, and it has returned real corrections every time.**

---

## §3 — THE THREE LIVE LANES

### 🟡 `ACTj` — the money layer. **Standalone, spun by Justin. WAITING ON YOU SINCE 2026-09-11.**

**What she is building:** per-job spending caps. **The money layer is BUILT and merely UNWIRED** — every branch in the wake path is already `if (req.jobId)`; nothing creates a `jobs` row. **Worktree `evryn-team-runtime-actj`, branch `actj/money-layer` @ `b3488ae` — on origin, no commits of her own.**

**She asks for FOUR sign-offs and routes TWO questions to Justin.** 🔴 **ACT-21's read, formed during its load and NOT yet re-checked against source — treat as a lead:**

| # | Her proposal | ACT-21's read |
|---|---|---|
| **§2** | **"The main dispatch" is FOUR trigger kinds, not one: `wakeup` mints a job · `park` INHERITS its original job · `member` and `dormancy` carry none.** | ✅ **Sound.** It applies Justin's own stated reasoning — *a cap can FREEZE work; freezing a conversation is not acceptable* — to sites his table could not name. **Not a re-litigation.** |
| **§2.1** | **NO-JOB DOMINATES when a coalesced decision mixes kinds.** | ✅ **Sound, and the harm asymmetry is the argument:** a wrong job can freeze a live conversation; a missing one degrades to today's behaviour **and the fleet-wide velocity brake still covers it.** **It mirrors the existing "dormancy dominates" pattern in the same function.** |
| **§3.1** | **Mint lazily (get-or-create); NO migration** — per-agent serialization already prevents a race. | ✅ **Sound, and worth it: a unique index would RE-OPEN gate G1** for a guard serialization already provides. |
| **§3.2** | **`ensureJobForDispatch` in `money/ledger.ts`; `buildPlannedWakeRequest` takes the job id as a REQUIRED parameter.** | ✅ **Sound** — required, not optional, is this repo's own anti-fail-open pattern *(the `velocity-basis-omitted` fixture)*. |
| ⓵ | **`cap_usd` default** — she recommends per-agent, in `config/agents.ts` beside the model pin. | 🟡 **NEEDS-JUSTIN.** **The mechanism is sound; the NUMBER is his** — it is the one value in the layer that freezes an agent mid-task when wrong. |
| ⓶ | 🔴 **The 60% pitch gate CANNOT FIRE** — nothing writes `estimated_total_ms`, so the forecast is always null. **As specced, jobs ship the FREEZE without the WARN.** Options: (a) supply a crude estimate · (b) **fire on spend alone when no forecast exists** *(her recommendation — overturns a guarded test assertion)* · (c) ship freeze-without-warn and say so. | 🟡 **NEEDS-JUSTIN, and frame it for him the way she did: this is not a nicety, it is *"may an agent be frozen without having been warned?"*** **ACT-21 leans (b).** |

**Also routed by her, still unowned:** 🔴 **`runtime-ops.md` §2 — composer layer 1, in every wake — tells agents Justin authorizes with an `approve <short-id>` command. THAT COMMAND DOES NOT EXIST.** **Justin's file; route it to him.** · **`senderPriority`'s top tier is unreachable** — nothing writes `from_actor: 'justin'`.

🔴 **OWED TO HER BEFORE HER BUILD TRIP — carried from ACT-20's brief, and she asked for it: bring her worktree current and NAME what moved.** **Her tree is at `b3488ae`; `main` has moved a long way since** *(Step 85 work, the SPRINT webhook note, and more)*. ⚠️ **Measure it — `git -C evryn-team-runtime rev-list --count actj/money-layer..main` — and tell her specifically which commits touch her territory.** **A tree that ages while its lane waits builds correctly against superseded instructions** *(`ac.md`, worktree lifecycle)*.

⚠️ **Collisions she must respect, both carried:** SPRINT **Step 86**'s one-line `settingSources` fix is **HELD until she is done** — it shares the options block with `maxBudgetUsd`. **Step 43** extracts `checkinTask` at `src/scheduler/index.ts:841`, **ten lines from the check-in dispatch she modifies at `:851`** *(verified at source by ACT-21, 2026-09-17)* — **sequence it after her.**

### 🟡 `ACT-R` — the reconciliation lane. **Standalone. YOUR CAPTAIN. ⏸️ PACKED OUT 2026-09-17T18:53 at ~780K, on his own authority. NOTHING SPUN. Justin re-spins him tomorrow.**

> 🔴 **THE SPIN LINE FOR HIS SUCCESSOR — it is NOT the original, because his first trip is DONE. Hand Justin exactly this:**
> ```
> You are ACT-R. Load everything in _evryn-meta/docs/sessions/2026.09.17-act21-act-r-load-list.md. Only once you are fully loaded, read _evryn-meta/docs/sessions/2026.09.17-act21-act-r-reconciliation-brief.md for the lane's framing — its first trip is ALREADY DONE — then read and faithfully execute _evryn-meta/docs/sessions/2026.09.17-actr-actr-reconciliation-ledger.md, starting at §10a. Do not read either before you are fully loaded.
> ```
> **His successor starts at his ledger's §10a** — the state, the first five actions, and `ACT-Rv`'s already-written load list and brief.
>
> **Why he packed out BEFORE spinning his first Captain — and it was the right call:** a sub-AC dies with its spinner's session, and spinning `ACT-Rv`, waiting, adjudicating its report and packing out properly would have crossed 850K. **Stopping at a clean slice boundary means nothing is stranded.** ⚠️ **His address `evryn-meta-09 [7f2042]` dies with his session; a fresh one re-publishes its own in his ledger's §1.**
>
> **HIS THREE ITEMS FOR YOU — recommend-first, one decision each:**
> - **① `NEEDS-JUSTIN` — the two `doc-update-routing-protocol.md` edits** *(= B29 + B30, confirmed at source)*. **He recommends yes to both.** **Tier-2 protocol edits — Justin's go.**
> - **② `NEEDS-JUSTIN` — Justin's "a Captain runs its own packout" ruling has no durable home, AND it CONTRADICTS `being-an-admiral.md`**, whose *"Packing out"* section says *"pause and OFFER `#packout`."* **His proposed wording: *"at ~850K measured, run `#packout` in full on your own authority — commit and push; do not offer and do not wait."*** 🔑 **And his sharp follow-up question for Justin: does it bind ADMIRALS as well as Captains?** **This is §8's row, now with wording drafted.**
> - **③ A LIGHTER SUCCESSOR LOAD — a PROPOSAL on the list ACT wrote, not a trim.** **Measured: his load is ~630K, leaving each instance only ~220K of working room.** **He recommends DROPPING the product-extraction precedent brief** *(~53k — read "for shape," and that shape is now built into his ledger)* **and keeping `LEARNINGS.md`** *(~88k)* **unless ACT agrees it can go.** 🟡 **ACT-21's read: the precedent-brief drop is SOUND — he read it, so he is positioned to judge it, and the shape it taught is now captured in the ledger. `LEARNINGS.md` is closer: a finding he must dedupe against it is exactly what it protects.** **Your call; decide before his re-spin tomorrow.**
>
> ✅ **He disclosed his own packout limit rather than absorbing it:** he re-read his outgoing ledger in full *(fixing four defects)*, but for the incoming brief read **every change since his full read** rather than all of it, to stay under 850K.

**What it is:** **41 live ACT-lineage session documents (~9,100 lines, 2026-08-06 → 2026-09-17), almost all bannered *"RETIRE UPON: ACT has evaluated it"* — a written statement that nobody has.** **ACT-R pulls every live finding out of them into a durable home, then dispositions each document.** 🔴 **THE ATLAS IS SEQUENCED BEHIND THIS** — an Atlas is written from the durable documents, so a finding still trapped in a session doc becomes a hole in the map.

- **Session address: NONE LIVE** — instance 1's `evryn-meta-09 [7f2042]` **died with his packout.** **His successor re-publishes its own in his ledger's §1; read it there, never from here.**
- **His ledger IS his log: `docs/sessions/2026.09.17-actr-actr-reconciliation-ledger.md`.** **To reach him, write a `§12` entry there and COMMIT it** — ⚠️ **but no watcher is armed on it until his successor arms one, so a `§12` entry written before his re-spin is READ at his spin-up, not delivered as a wake.** **Signal his session only as a nudge, and only once his successor has published an address.**
- **He reaches you through `inbox-act.md` at four moments:** trip 1 · each slice adjudicated · a `NEEDS-JUSTIN` or anything touching your work · set-down.
- **Shape — FOUR Captains, run SEQUENTIALLY in date order, agreed 2026-09-17:** `ACT-Rv` *(verification of ~30 scout claims — a calibration set)* → `ACT-R1` *(Aug 06–13)* → `ACT-R2` *(Aug 17–19)* → `ACT-R3` *(September)*. **His sub-ACs are subagents he holds; they report to him; he makes every tracker edit himself.**
- 🔴 **He PACKS OUT ON HIS OWN AUTHORITY at ~850K and pushes everything** *(Justin's ruling — he does not "offer" it)*. **Instance 1 loaded at ~630K and packed out at ~780K BEFORE spinning its first Captain** — **so every re-spin of this lane pays that load first.** ⇒ **That is exactly why his lighter-load proposal (item ③ above) matters: at ~630K, each instance gets only ~220K of work.** **His ledger is built for frequent re-spins — rows land as decided, re-spins fall on slice boundaries.**
- **He is a CAPTAIN, not an Admiral.** *(Justin: commanding sub-ACs does not make you one; reporting directly to Justin does.)* **He caps at `##` and never pings Justin.** **His NEEDS-JUSTIN items and every retirement come to YOU, and you carry them.**
- ⚠️ **His ledger's §1 still says *"downward I am an ADMIRAL"*** — written from the brief before ACT-21 corrected the vocabulary. **Behaviourally he is right. Mention it only if it ever matters.**

### 🔴 `QCs` — independent review of SPRINT Step 85. **✅ DELIVERED: `NO-GO` on ONE blocker, which is a one-line fix.**

> **Her full output: `docs/sessions/2026.09.17-qcs-act-step85-review.md`** *(committed `4877937`, 207 lines)*. **Her session: `evryn-meta-26` · ref `[46eb…]`** *(the full ref is at the top of her file)* — **resumable for a second pass, but only while that session lives.** **She ran 18:34–18:55 on 2026-09-17.**
>
> 🔑 **Her headline, verbatim: *"The engineering is sound. All four acceptance criteria and both halves of the ruling are proven by mutation, each with a paired control that stayed green. The eight real team memories all grade `intact`, and the modified tests weaken nothing. With F1 fixed and its new assertion shown to fail under mutation, this is a GO."***
>
> 🔴 **F1 — THE BLOCKER, and it is a genuinely sharp catch: the refusal message's FIX line tells the operator to do the one repair that silently re-admits the hole.** `memory.ts:470` says *"restore the canary as the file's LAST line."* **But a missing canary usually means the file's TAIL WAS LOST** — so re-adding the canary alone makes a truncated memory grade `intact`, **and nothing flags it afterwards, because the grader deliberately never alarms on a shrink.** **She proved it on Lucas's real memory:** cut 30,078 characters off the end, the grader correctly refused, she followed the FIX literally — **and it graded `intact` with the newest notes still missing.** ⇒ **That defeats Justin's *"holes in your memory? No bueno"* through the exact message written for him.**
> **The fix, hers:** for `canary-missing`, say to **restore the file from git history** *(`git log -p -- <path>`)*, **not** to re-add the canary alone · for `canary-not-terminal`, say to move the canary below the trailing content **without deleting it** · **pin the new wording with a LITERAL assertion, and show it goes RED when reverted.** ⇒ **One DC fix trip.**
>
> ⚠️ **F3 — non-blocker, but it bears DIRECTLY on Justin's "one re-read" ruling and he should hear it:** **measured on Windows/NTFS over 132 real `git checkout` rewrites of Lucas's file, 59 of 2,695 reads came back torn, and ONE immediate re-read cleared only 22 of the 59** — **producing 41 false refusals** with a prior manifest. **The ruling's premise (a compose can race a write) is REAL; one immediate re-read is a WEAK clearer of it.** 🔑 **And it is CORROBORATED independently: the builder (`DCn`) had already said the re-read's real transient window is the workspace GIT SYNC, not the runtime's own note write, which is atomic (`tmp` + `rename`) — and QCs's torn reads came precisely from `git checkout` rewrites.** **Two agents, two methods, the same answer.** **Surface to Justin with her numbers before the merge — it is his ruling, not a lane's to re-open.**
>
> **F2 — non-blocker, fleet-scale:** a refused closing-memory wake writes its park note into the closing room, which the activity trigger treats as life — so the room revives and **every healthy member's completed closing memory is wiped** and re-asked a week later. **Pre-existing seam, and SPRINT Step 46's premise has been stale since Step 7 made `closing` revivable.** **Net still better than `main`.** **Fix: park `dormancy-write` refusals to a type the trigger ignores, and correct Step 46.**
>
> **F4–F7, her unestablished list, and her standing patterns: read her file.** 🔴 **Her "standing patterns worth promoting" section is QC-manual material — she is spun fresh every trip and cannot write her own manual, so a pattern she surfaced dies with her unless an AC promotes it (Justin approves).**

**What Step 85 is:** **an agent's memory was the ONLY composed layer that could arrive empty on a read error and tell nobody** — identity refuses the wake, current-state pings Justin, memory logged a warning and proceeded. **The branch grades that layer, and per Justin's ruling a memory DAMAGED AT REST REFUSES the wake after ONE re-read.** *("Holes in your memory? No bueno.")*

- **Branch `dc/step85-memory-grading` @ `267926c`, worktree `evryn-team-runtime-dcm` — on origin, matches local.** **Mutation sandbox: `evryn-team-runtime-qcs`, detached at `267926c`, `npm ci` run inside it** *(canonical `node_modules` 146 before and after)*.
- ✅ **Her output routing worked** — she was spun before her brief was patched, **Justin relayed the fix into her session, and her verdict landed as a committed file plus an inbox pointer.** 📌 **The lesson stands for the next standalone: a standalone has no parent, so write the file-plus-pointer routing into its brief from the start.**
- 🔴 **THE PATH TO MERGE, in order:**
  1. **Read the builder's set-down first** *(`2026.09.11-dcn-act-step85-refusal-setdown.md`)* **and QC's file in full — her findings are inputs, not verdicts; evaluate each rather than rubber-stamp it.**
  2. 🟡 **Take F3 to Justin** *(above)* — **it tests his own ruling.** **Do not let a lane re-open it.**
  3. **Spin ONE DC fix trip for F1** — the two FIX-line rewrites and a literal assertion pinned to the new wording, **shown to go RED when the wording is reverted.** **Consider folding F2's one-line park-target change into the same trip if Justin agrees; it is a separate seam, so it is his call whether it rides.**
  4. **A light QC re-verify of the fix** — **resume QCs if her session still lives** *(cheaper, and she holds the context)*; **otherwise a fresh QC on just the fix.**
  5. **Bring the branch current — it is 10 commits behind `main`** *(read any diff with THREE dots)* → **merge `--no-ff`** *(build-level code, your standing authorization)* → **reap BOTH worktrees, `-dcm` and `-qcs`, with the `node_modules` count check around each** → **changelog entry** → 🟡 **an ARCHITECTURE line on layer-6 grading needs Justin's explicit go.**
  6. **Route F2 and F4–F7 to SPRINT Steps**, and **correct Step 46's stale premise** *(QC's F2)*.
- ✅ **`TEAM_ALERTS_WEBHOOK_URL` IS SET on Railway** *(Justin confirmed 2026-09-17)* — **so a refusal reaches Slack.** ⚠️ **It is still read as OPTIONAL in code**, so it is a confirmed configuration rather than an enforced one. **Making it `required()` is one line and belongs with Step 86.**

---

## §4 — INSTRUMENTS — arm these first

🔴 **A `Monitor` DIES WITH ITS SESSION, AND AN IN-PLACE RE-SPIN CLEARS THEM. Arm your own.** ⚠️ **In this session every monitor caps at 30 MINUTES whatever you ask for. Read what `Monitor` says back when you arm it, and re-arm on its expiry notice** — **an expired watcher looks exactly like a quiet channel.**

| Instrument | Watch | Cadence |
|---|---|---|
| **Heartbeat** | **you** | **5 min daytime · 10 overnight.** 🔴 **Keep it armed while ANY process is in flight.** |
| 🔴 **Inbox watcher** | `docs/mailboxes/inbox-act.md`, **on COMMIT** *(`git log --all -1 -- <path>`, with a `git fetch` each tick)* | 30s, emits only on change. **This is how ACT-R and QCs reach you. ACT-21 went without one and a Captain waited an hour.** |

- **Print a proof-of-fire line BEFORE the loop**, so "armed" and "working" are one claim.
- ✅ **You can MEASURE your context: `node _evryn-meta/scripts/context-size.mjs`.** **It prints "plan a packout" by itself at ~75%.** **`--session <id>` measures another session — use it on your Captains.**

---

## §5 — THE ATLAS — next, and sequenced behind ACT-R

**The map (what imports what) is BUILT, generated and verified. The partition is SETTLED at EIGHT sections** *(SPRINT Step 84)*. **What does not exist is the semantic half: what each region is FOR, and what will bite you in it.**

**The agreed shape, with Justin's addition:**

| Phase | What | Who |
|---|---|---|
| **Read** | **8 readers, one per section**, each with its section plus a shared 635-line preamble of the five hub files | **fan out — OUTPUT is the binding constraint, not input** |
| **Synthesize** | the index and the seam narrative, **reading no source** | 1 agent |
| **Prove — completeness** | **a SCRIPT**: every file appears exactly once | no agent |
| **Prove — truth** 🆕 | 🔴 **A WHOLE-RUNTIME CHECKER** *(Justin, 2026-09-17)* — **a QC with a deliberately trimmed load: all of `src/`, the Atlas drafts, the map, ARCHITECTURE, and the identity half** *(~17,000 lines, well under the 714K peak where the one measured full-load run sat)* | 1 QC |

🔑 **Why BOTH provers, and it is the reason Justin's addition matters:** the script answers completeness — a set operation, and exactly the kind of claim this estate has repeatedly been wrong about by asserting. **Only the checker answers TRUTH.** **The original design ADMITTED this hole** — its synthesizer *"CANNOT catch a section that describes its own files wrongly, because it never read them"* — **as an accepted limit, because whole-runtime reading was believed impossible. It isn't any more.**

⚠️ **Why the checker's load is trimmed, deliberately: if the CHECKER compacts, it returns a confident verdict from a summary** — the worst failure available in a verification step. **The margin is the safety feature.**

🟡 **This shape has a home in NO durable document yet** — only in ACT-21's reports and here. **Put it in the Atlas brief when you write one.**

---

## §6 — 📋 BACKLOG — numbers never reused

| # | Item | State |
|---|---|---|
| **B1** | `runtime-ops.md:9` cites a session doc for its open questions — **SPRINT Step 77.** **Needs a FULLY-LOADED sub-AC; Justin barred a light-loaded coordinator from drafting it.** | Open |
| **B13** | `ThreadDormancyWriteRow` referenced nowhere | → **SPRINT Step 47's residue** |
| **B16** | `evryn-backend-qc-sandbox` detached worktree | **ACP's. Not ACT's. Do not reap.** |
| **B18** | 🔴 **`settingSources` unset in `runWake`** → **SPRINT Step 86** | **HELD until `ACTj` is done** — shares an options block with `maxBudgetUsd`. |
| **B20** | Seven ACT-lineage entries sit unread in `inbox-acp.md` | **ACP dormant. Nothing of ACT's is blocked.** |
| **B21** | `npm audit` advisories in `evryn-team-runtime`, uninvestigated | Open. **Never `audit fix --force` blind.** |
| **B23** | `memory-writer.ts` carries no truncation canary | ✅ **Deliberate** — it holds the canary string as a functional constant. |
| **B26** | Justin's 2026-09-11 rulings have no row in `doc-update-routing-protocol.md` change-type 9 | Open, small |
| **B27** | **The reconciliation lane** | 🟢 **RUNNING — see §3. Subsumes the old B5.** |
| **B28** | **ACT-A's ~30 scout claims** — `docs/sessions/2026.09.17-act21-act-r-scout-findings.md` | 🟢 **Being verified by `ACT-Rv`, ACT-R's first Captain.** |
| **B29** 🆕 | 🔴 **`doc-update-routing-protocol.md` change-type 9 carries a STALE LIVE RULE** — a repointing exclusion "not yet in force until `docs/working/` is retired." **It was retired 2026-09-02.** **Two source-of-truth docs now contradict each other.** | **ACT-R confirmed it at source and is routing it, batched with B30.** |
| **B30** 🆕 | **That same protocol's registry names only the PRODUCT lane's sprint and architecture docs** — this runtime's have no row. | **ACT-R routing it.** |
| **B31** | 🔴 **THE OTHER THREE LANE BRIEFS — `ACTt`, `ACTi`, `ACTv` — ARE STILL WRONG AND STILL UNSPUN.** **Each demands all 63 test files in full: the load that compacted a lane on 2026-08-19.** 🔴 **Justin's sequencing, and do not get ahead of it: ONE lane first (`ACTj`), learn from it, THEN re-scope the rest. Do NOT re-scope all three pre-emptively.** *(The record of the ruling is `2026.08.19-act-acti-guard-integrity-brief.md` §3.)* ⭐ **When you do re-scope: derive each load from the dependency map's §4 reverse index and §8 test-coupling table, as `ACTj`'s was — not from a list.** | **Waiting on `ACTj` to report.** *(Carried from ACT-20's brief; dropped by ACT-21's first draft and caught on the incoming re-read.)* |
| **B33** | ⚠️ **THE MEMORY-REFUSAL ALERT DOES NOT LEAD WITH PLAIN ENGLISH.** **~330 characters of `runWake` wrapper sit in front of the operator message Justin approved** — so the Slack line he reads first is plumbing, not *"Lucas couldn't wake because his memory is damaged."* **The builder could not fix it: `src/wakes/` was fenced off from its trip.** **A one-paragraph edit in `wakes/index.ts` — sequence it WITH Step 86's held one-liner (B18), both after `ACTj`, because that file is her territory.** *(Distinct from QCs's F1, which is the message's REPAIR line in `memory.ts`.)* | Open *(carried from ACT-20's §7; caught on the incoming re-read)* |
| **B32** | ⚠️ **A CONTRADICTION ABOUT STEP 43, to settle AT SOURCE before briefing that lane:** `current-state.md` calls the top open item *"the unguarded dormancy cadence-gate wiring,"* **while the SPRINT's own Step 43 and the 2026-08-12 changelog say both sweep gates were guarded at `869bd20` and only the LIFECYCLE FIVE remain.** **Read the code before briefing — and fix whichever document is wrong.** **Step 43 also collides with `ACTj` (§3), so it goes after her.** | Open *(carried from ACT-20's brief; dropped by ACT-21's first draft)* |

### ⏳ WAITING ON ACP — carried from the previous brief; nothing of ACT's is blocked

| # | Sent to ACP | What was asked | Status |
|---|---|---|---|
| **W1–W3** | 2026-09-08 | the shared-generator question · an ARCHITECTURE completeness question · a generator-provenance question | **Unanswered. ACP dormant.** |
| **W4** | 2026-09-10 | 🔴 **the `settingSources` class, possibly LIVE IN PRODUCTION on the product side** | **Justin's call (2026-09-11): it STAYS WITH ACP — ACT does not run the product-side check.** **It waits for that lane to spin.** |

---

## §6a — JUSTIN'S STANDING SESSION POSTURE — ask rather than assume it persists

*(Carried from the previous brief; still his working instructions to this seat.)*

1. **ADMIRAL MODE** — be as autonomous as is reasonable; solve it and say what you did; **anything important or hard to reverse, get him in right away.**
2. **PING EVERY TIME** you have something for him.
3. **Routine changes commit; important ones wait UNCOMMITTED for his review in SCM** — ⚠️ **and say so INSIDE a numbered report, where the eye lands, or it will be missed** *(§9)*.
4. **Numbered reports** (`# ACT-N's Report #N` … **`## End of Report`**), **inline ballots**, a handhold on every reference, **never a bare Step number.**
5. **Nobody hijacks ACT** — you work for Justin, not for other ACs or team agents. **Default is to surface an inbound ask to him.**
6. **Packouts run like an astronaut's checklist.** **Don't check `#dev-alerts`.**
7. 🆕 **Delegate the non-orchestrating work** — *"I don't want to constantly have to re-spin you."* **And tell him BEFORE writing into another agent's channel.**
8. 🆕 **He is your only reader and he is coming in cold.** *"Orient me in space, then orient me at the points of contact"* — **and when you first introduce a thing, give it a sentence or two of what it IS.** *(ACT-21 introduced "the archaeology lane" without ever defining it, and he had to ask.)*

---

## §7 — ⚠️ THE `ACTa` / `ACT-A` NAME COLLISION — it has already cost one wrong count

**`ACTa`** = the 2026-08-19 **Atlas lane**, ACT-lineage, **YOURS**. **`ACT-A`** = **Justin's side project** *("could Claude Code replace our own autonomous agents?")*, an alternate-branch ACT, **NOT yours — Justin: "ACT-A will deal with its own."**

🔴 **Their filenames are near-identical and their owners are different.** **ACT-21 filtered on the fragment `acta` to exclude ACT-A's docs and silently deleted the Atlas lane's own brief and exchange from a territory count.** ⇒ **Identify a doc by its BANNER's `OWNER:` field, never by its filename.** *(ACT-A's banners read `OWNER: ACT (the ACT-A side-project line, not the main ACT line)`.)*

**Justin's call on renaming: not worth it** — the Atlas-lane docs are about to be dispositioned by ACT-R, **so carry this warning in every handoff and every subordinate's brief until they are retired.**

---

## §8 — JUSTIN'S RULINGS THIS SESSION, AND WHERE EACH LIVES

| Ruling | Durable home |
|---|---|
| **An UNBANNERED session doc is UNEVALUATED, not retirable** — a missing banner is an absence read as a decision | ✅ **`lock-protocol.md` step 10** — committed |
| **Close every report with `## End of Report`** — the failure is burial, not omission | ✅ **`ac.md`** — committed |
| **Admiral = reports to Justin. Commanding sub-ACs does not make you one** | ✅ Already in `being-an-admiral.md`; applied to ACT-R's brief |
| 🟡 **A Captain RUNS its own packout at ~850K and pushes everything — it does not OFFER it** | ⚠️ **ONLY in ACT-R's ledger §12.** **`being-an-admiral.md` says the ADMIRAL "offers" its packout; this is the Captain-side rule and has no home.** **Owed: propose the wording to Justin.** |
| 🟡 **Re-spin IN PLACE is now preferred** — subagents and session IDs survive | ⚠️ **ONLY here.** **Owed: a home in `ac.md` (the re-spin triggers) — propose to Justin.** |
| 🟡 **A STANDALONE agent has no parent, so its output must be routed to a committed file + a pointer in an inbox** | ⚠️ **ONLY in the QCs brief.** **Owed: `ac-orchestration-protocol.md` → "Subagent or standalone instance."** |
| 🟡 **The Atlas gets a whole-runtime QC checker IN ADDITION to the completeness script** | ⚠️ **ONLY §5 here.** **Owed: the Atlas brief.** |
| **ACT-R reports ONLY to ACT; ACT is his Fleet Admiral** | ✅ ACT-R's brief |
| **ACT-A's docs are not ACT's** | ✅ ACT-R's brief + §7 |
| **`TEAM_ALERTS_WEBHOOK_URL` is set on Railway** | ✅ **SPRINT Step 85** — committed |

⚠️ **Four rulings above have NO durable home.** **`lock-protocol.md` step 9b requires each to get one, WITH ITS WORDING AGREED WITH JUSTIN FIRST.** **Bring him the exact text and destination for all four in one batch.**

---

## §9 — WHAT ACT-21 GOT WRONG, so you do not repeat it

- 🔴 **It ran most of the session WITHOUT AN INBOX WATCHER.** **ACT-R posted a correctly-committed trip-1 report and sat blocked for an hour.** **Arm it first.**
- 🔴 **It ran without a HEARTBEAT and ended a turn on *"starting the packout now"*** — the exact failure `ac.md` names: **text announcing work, placed where nothing can follow it.** **Do the work first, then report it in the past tense.**
- **It got a territory count wrong TWICE** — a git pathspec that matches across directory separators swept in archived docs, then the `ACTa`/`ACT-A` filename collision. **Re-derive any count you act on.**
- **It buried an uncommitted source-of-truth edit** in a report — named twice, registered zero times. **That produced the `## End of Report` rule.**
- **Its first two lane briefs omitted two things `being-an-admiral.md` already requires:** that a standalone Captain **publish its session address** and **watch its own log**. **ACT-R paid for both. Put them in every standalone brief you write.**

---

## §10 — DOC DISPOSITIONS

| Doc | Disposition |
|---|---|
| **`2026.09.11-act20-act21-handoff.md`** | ✅ **ARCHIVED to `docs/sessions/historical/2026.09/`, filename unchanged, with a `SUPERSEDED BY` line — Justin's OK, Ballot ⑤, 2026-09-17.** ⚠️ **Archived is not dead: its §0-C records how ACT-20's compaction was quarantined, and every one of those six items is now VERIFIED or settled** *(C1 against the archived source, C2 against the guard-integrity brief, C3 at source, C6 by the push)*. |
| **The ACT-R and QCs briefs, load lists and ledger** | 🔒 **CARRY — live lanes.** |
| **`2026.09.17-act21-act-r-scout-findings.md`** | 🔒 **CARRY** — being verified. |
| **Everything else ACT-lineage** | **ACT-R's territory. Do NOT disposition it yourself — that is the lane's job, and two readers would contradict each other.** |

✅ **`current-state.md`** *(the live-handoff pointer, re-aimed at THIS file)* **and `CHANGELOG.md`** *(ACT-21's 2026-09-17 entry)* **are COMMITTED and pushed — Justin's go, Ballot ④.** **ACT-21's tree was clean at set-down.**

### ⭐ THE CHEAPEST RUNTIME KNOWLEDGE A ZERO-`src/` CONDUCTOR CAN GET

**One `node` script over `git ls-files src`, printing each file's leading comment block — 41 files, ~740 lines, one command, no subagent.** **The method and its verdict are in SPRINT Step 84.** **Run it before you brief anything that touches code; it is how the eight-section partition was settled.**

---

## §11 — 🎟 OPEN BALLOTS WITH JUSTIN

**ACT-21 used Ballots ①–⑤.** **All ANSWERED:** ① yes to the reconciliation brief · ② the tracker corrections go to ACT-R, not ACT · ③ Justin relayed the output-routing line to QCs · ④ commit `current-state.md` + `CHANGELOG.md` · ⑤ archive the superseded handoff. **Nothing is open.** **Start your numbering at ⑥.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
