# ACc (AC-cleanup) Launch Brief — round-2 convergence build arm (2026-06-24)

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

> **How to use this file:** AC0 wrote this for **Justin** to launch **ACc** — a fresh, lane-style AC instance that is the *build arm* during round-2 convergence. Launch ACc from the **`_evryn-meta` seat** (so AC's `CLAUDE.md` auto-loads — ACc is a peer AC, not a subagent). The copy-paste launch block is below; the scope detail + per-build specs follow it. **AC0 stays the conductor** (convergence/merge/deploy); ACc does the net-new builds + the small fix-ups convergence surfaces, each to QC-GO on a branch, handed back to AC0. *Authored 2026-06-24T11:02-07:00 by AC0.*

---

## Why ACc exists (the division of labor)
The 3 closed lanes (A/C/M1) are near compaction — we don't run compacted ACs on this. AC0 must stay light + in orchestrator mode (convergence, seam-merge, prod migrations, deploy coordination). So a **fresh ACc** owns the *build* work that convergence needs:
1. **Step 21 — durable email cursor** (Justin: build it). A required M1 interlock that didn't get built. Without it, M1's lossless-restart guarantee fails on a process restart (the cursor is in-memory today → cold restart falls to latest-~10 → mid-window mail can drop on exactly M1's recovery path).
2. **The register tool** (Justin: scope the small tool) — so **Evryn registers a gatekeeper's inbound lanes + outbound destination herself**, not Justin-by-hand SQL. No mechanism exists today (confirmed: `gatekeeper_inbound_addresses` is read-only in `src`; `outbound_address` has no Evryn-facing setter).
3. **Convergence dribs & drabs** — the small cross-lane integration fixes / QC-flagged blockers that surface once AC0 starts merging. AC0 hands ACc specific fix tasks as they arise.

**ACc does NOT merge to `main` or deploy** — that stays with AC0 + Justin. ACc builds to QC-GO on branches and hands back.

---

## The launch block (paste into a fresh `_evryn-meta` session)

```
You are ACc (AC-cleanup), a lane-style Architect-Claude instance and the BUILD ARM for round-2 convergence of the v0.2-hardening wave. The CLAUDE.md that auto-loaded is AC's operating manual — that's yours; you orchestrate DC/QC (and coordinate Mira for identity) as subagents. AC0 remains the conductor: AC0 converges/merges/deploys; you build the net-new pieces + convergence fix-ups to QC-GO on branches and hand back to AC0. You do NOT merge to main and do NOT deploy.

LOAD NOW — your Full Startup Context Cascade (build-level work; load every file IN FULL):
- _evryn-meta/docs/current-state.md
- _evryn-meta/docs/hub/roadmap.md
- _evryn-meta/docs/hub/technical-vision.md
- evryn-backend/docs/ARCHITECTURE.md (full)
- evryn-backend/docs/BUILD-EVRYN-MVP.md (full)
- evryn-backend/docs/SPRINT-V0.2-HARDENING.md (full — your tie-back step-list)
- evryn-backend/src/** on main (the runtime you build against; you'll also read the closed lane branches per the task)
- _evryn-meta/docs/protocols/ac-orchestration-protocol.md (you spin DC/QC + coordinate Mira — follow it EXACTLY; the HARD RULE: paste the verbatim load blocks character-for-character when you spin)
- _evryn-meta/docs/protocols/ac-writing-protocol.md (before you write any doc)
- AC0's CONVERGENCE PLAN: _evryn-meta/docs/working/2026.06.24-ac0-ac0-convergence-plan.md (your tasks are GAP 1 + GAP 2 there; read the whole plan for the seam map + how your builds fold into convergence)
- THE ROUND-2 BRIEF: _evryn-meta/docs/sessions/2026.06.19-ac0-round2-lanes-brief.md — read the shared preamble + the TWO CHANGED RULES + Lane A §4 (the Step 21 design) + Lane A §6/Step 61 (context for the register tool's tables); skim the other lanes.
- THE LANE-A HANDOFF: _evryn-meta/docs/working/2026.06.23-ac1a-ac0-handoff.md (Step 61 build detail + the gatekeeper-address tables your register tool writes; + the registration-mechanism note Justin flagged).
- THE LANE-M1 HANDOFF: _evryn-meta/docs/working/2026.06.23-ac5a-ac0-lane-m1-r2-handoff.md (why Step 21 is M1's keystone interlock — the dedup-resume keys on the cursor surviving restart).

THE TWO RULES (non-negotiable):
1. DESCRIBE-THEN-PAUSE. Your FIRST action after loading is to orient Justin on your plan (both builds — your worktree topology, the Step 21 approach, the register-tool shape + the Mira identity coordination) in CEO-accessible terms, then STOP and WAIT for his explicit "aligned, go" before building or spinning anything. Hard gate.
2. When you spin DC/QC, follow the fixed ac-orchestration-protocol EXACTLY — precise-load + line-spans + two-part receipts + re-run on any partial — and PASTE the verbatim <identity>/<mandatory_load>/<pre_task>/<task>/<questions_first>/<receipts> blocks character-for-character (the HARD RULE: no one-word deviation without Justin's prior OK). The register tool's identity beat is Mira's — coordinate her in via the team-subagent variant (or Justin relays); identity files go through identity-file-review.md.

YOUR BUILDS (full specs in AC0's convergence plan, GAP 1 + GAP 2):
- Step 21 (durable email cursor): the design is in the round-2 brief Lane A §4 — durable cursor via a lazy provider (cold-start / historyId-expiry reads newest emailmgr_items.created_at minus a margin; client.ts stays DB-free, receives the closure; larger page; durable external_id dedup absorbs overlap). Build it so it composes with Lane A's poll.ts/client.ts resilience work (suggest cutting your branch FROM r2/lane-a-ingest-resilience so Step 21 + Lane A are one coherent unit — propose your topology in your plan). DC + QC.
- Register tool: a small dedicated MCP tool so Evryn registers a gatekeeper's inbound lanes (INSERT gatekeeper_inbound_addresses) + sets the gold destination (users.outbound_address) — via TYPED db/users.ts helpers, NOT the generic RLS-bypassing supabase_upsert. Pairs with Lane A's existing gather-at-onboarding "Setup Check-In" beat (Evryn already gathers the addresses; this lets her WRITE them instead of handing them to Justin). Mira identity beat for when/how she calls it. NOTE the cross-lane seam: this touches the classify.ts MCP-tools region, which Lane B (AC2a) also touches — coordinate the region with AC0 (Lane B may still be in flight). DC + QC + Mira.

HARD CONSTRAINTS: work ONLY in your worktree(s); verify branch before every edit; build to QC-GO; do NOT merge to main, do NOT deploy; commit only your files by pathspec (never git add -A); identity files are Mira's (coordinate); tests are part of "done"; tie every deliverable to a numbered SPRINT Step (Step 21 + a new step for the register tool — add it + flag AC0).

WHEN DONE (per build, or batched): (1) low-res summary for Justin in chat; (2) a high-res handoff working doc for AC0 (docs/working/2026.06.24-acc-ac0-<build>-handoff.md); (3) ping #team-alerts ("ACc: <build> QC-GO on <branch> — handoff at …"), then stop. Ping #team-alerts whenever you have something for Justin — every time; your FIRST ping is your plan, then you WAIT. For convergence fix-ups AC0 hands you mid-stream, same loop: build → QC → hand back.
```

---

## Sequencing notes (for AC0 + ACc)
- **The two builds can start now** — they're independent net-new work, not blocked on B/staging landing. ACc builds them in parallel with AC2a/AC4a finishing.
- **Convergence still waits for ALL code lanes** (Justin's ballot 3: wait + converge everything). So: ACc builds Step 21 + register tool → B + staging land → AC0 converges the whole bundle (C → A → Step 21 → M1 → B + register tool) → one review → Justin's deploy-go.
- **Register-tool ↔ Lane B seam:** both touch the `classify.ts` MCP-tools region. The register tool is additive (a new `tool(...)` block + array entries), so low-conflict, but same region — AC0 sequences the merge. If Lane B lands first, ACc can build the register tool on top of B's branch; if not, off `main` and AC0 reconciles. ACc proposes in its plan.
- **Step 21 ↔ M1:** Step 21 must be in the same converged bundle as M1, or M1's lossless guarantee is incomplete. Building Step 21 off the Lane A branch keeps it a coherent unit with the resilience work M1 also depends on.

## What stays with AC0 (not ACc)
Merging branches to `main`; resolving pure-merge seams (no new code); prod migrations (with Justin); ARCH/BUILD doc absorptions (auth-gated, with Justin); the deploy-go; sprint DONE-marking on merge; promoting M1's QC pattern (Justin approval).

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
