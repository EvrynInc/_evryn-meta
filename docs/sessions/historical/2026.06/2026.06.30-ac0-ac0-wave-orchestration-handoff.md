# Handoff — AC0 → next AC0 — the live 4-lane v0.2-hardening WAVE (2026-06-30)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** START HERE if you're a fresh AC0 picking up the v0.2-hardening wave. You are the **conductor** of a 4-lane parallel build. This doc gives you the operating model, the state of every lane, and the one open thread you must carry. Read it, then load the Light Context Cascade (you do NOT need the full runtime to orchestrate — the *lanes* hold the runtime; you reconcile). Working doc — retire when the wave deploys.
>
> *Authored 2026-06-30 by the outgoing AC0 (hit ~10% compaction mid-wave). Companion: `docs/current-state.md` for the cross-project snapshot.*

---

## ⚡ What you are doing (one paragraph)

Justin and AC0 (you) are running a **4-lane parallel v0.2-hardening wave** on `evryn-backend`. Justin spins each lane as its own AC instance (AC1/AC2/AC3/AC4); **you do NOT write code** — each lane's DC/QC build. Your job: read each lane's report, reconcile/decide, reply, keep the lanes coordinated, and keep Justin informed at CEO-altitude. Round-2 v0.2-hardening is already DEPLOYED + LIVE (`main` `d198ef1`, 2026-06-29); this wave is the *next* batch off that.

---

## 🛠 The operating model — Justin set this, follow it EXACTLY

1. **Each AC appends its report to the TAIL of its own brief. The brief IS the AC↔AC0 mailbox.** You read the tail, append your response as a `## ═══ AC0 → ACn · date · summary ═══` banner *before* the file's truncation canary, and **COMMIT + PUSH the brief IMMEDIATELY** (`git -C <repo> add <brief> && commit && push`). This is **pre-authorized** (mailbox rule) — do NOT wait for Justin's go. He explicitly wants briefs committed between rounds. (Pattern: fetch → if `behind==0`, stage-by-path → commit w/ `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>` → push.)
2. **Justin does NOT read the briefs.** Every time you respond to a lane, give Justin the **CEO-altitude version in chat** — plain English, enough handholds that he can follow after 45 min on a different instance. **NEVER throw a bare "Step 83" at him** — it means *nothing* to him cold; always describe the item in plain terms. (He told you this directly, twice.)
3. **PING every final response** on `#team-alerts`: Node `fetch` to `SLACK_TEAM_WEBHOOK_URL` (in `evryn-team-workspace/.env`), prefix `AC0:`. **Plain Node fetch works** — do NOT add `dangerouslyDisableSandbox` to the Bash call (that flag triggers a permission prompt; the plain fetch does not). He is ALWAYS bouncing between instances — ping even when he seems present.
4. **Each AC's FIRST task is "judge-the-set"** — independently judge its assignment against the LIVE runtime before building, and bounce reframes back to you. You are the reconciliation point. (Why: much of the sprint was added mid-build; the system has shipped a lot since, so some items are stale/done/reshapeable. This is *why* the work is split to focused ACs instead of you doing it.)
5. **Commit/deploy gates are Justin's.** The lanes build/edit freely but pause for his explicit go before **commit** and before **deploy** (`railway up`). The brief-mailbox commits (#1) are the *only* pre-authorized commits.

---

## The 4 lanes + their briefs (all committed on `main`, in `_evryn-meta/docs/working/`)

| Lane | Brief file | What it does | Deploy slot |
|------|-----------|--------------|-------------|
| **AC1** | `2026.06.30-ac0-ac1-cleanup-batch-brief.md` | Mechanical cleanup — tests, comment fixes, small refactors/guards, a DB security pin | **1st** |
| **AC4** | `2026.06.30-ac0-ac4-security-bulkheads-brief.md` | Security bulkheads — WebFetch exfil-door, DB-read lock, send-door guard, email fence | **2nd** |
| **AC2** | `2026.06.30-ac0-ac2-cost-lane-brief.md` | Cost — Haiku pre-screen → clustering → runtime-bookkeeping (Step 57) | **3rd** |
| **AC3** | `2026.06.30-ac0-ac3-identity-ops-brief.md` | Identity/Mira + investigations + staging + ops (5 sub-streams; Justin-heavy) | alongside |

**Deploy order: AC1 → AC4 → AC2, AC3 alongside.** Staged *separate* deploys, NOT one convergence — this was the deliberate choice to avoid round-2's painful merge tsunami. The order matters because AC1/AC4/AC2 all touch `classify.ts`'s tool layer: smallest/urgent first; AC2's big Step-57 rewrite *last* so it rebases onto the others' guards (not vice versa). Each lane deploys its own bundle on Justin's go.

---

## 📍 Where each lane stands RIGHT NOW

### AC1 (cleanup) — PROCESSED, BUILDING
- Judge-the-set done; **I reconciled + cleared him to build** (my sign-off is appended to his brief). His batch **shrank by 7**: 6 items were already shipped in round-2 (I flipped them DONE in the sprint — Steps 32/33/67/84/85), and 1 was dead-on-arrival (Step **24** → MOOT: the code it targeted was deleted ~6wk before the step was written).
- **Two reshapes:** Step **83** → a *per-tool* role policy, not a uniform gate (a blanket admin-reject would break how Evryn writes to the Operator's own profile, ADR-030) — he's building that version. Step **76b** → dead-as-placed; its real version is a security guard I **routed to AC4** (see below).
- He's building DC trip 1 (mechanical) + trip 2 (behavior+tests), QC ×2, on `ac1/cleanup-batch`. **Pauses before commit/deploy for Justin.** Deploys 1st.

### AC4 (security) — PROCESSED, building Bundle 1; **Bundle 2 HELD on the broker (below)**
- Design signed off (appended to his brief). He settled the SDK mechanism himself: the Agent SDK has a `PreToolUse` deny-hook that fires *even under* `bypassPermissions`, so WebFetch can be gated structurally without rebuilding it.
- **Building Bundle 1 now:** ① WebFetch/WebSearch **deny-by-shape hook** (refuse SSRF/internal targets incl. the Railway `169.254.169.254` metadata endpoint + non-http(s) + data-exfil-shaped URLs; deny-with-reason + `notifyDev` on a deny) + ③ email-body untrusted-content fence + ④ the 41a identity beat (Mira voices it via Justin) + the **`sendEmail`-provenance guard** I routed from AC1's 76b (refuse + emergency-alert any external send not coming through the approval gate; it's the outbound-egress sibling of the WebFetch door).
- **Bundle 2 = ② the `service_role` generic-read lock — HELD.** It shares surface with AC2's Step 57. Deploys 2nd.

### AC2 (cost) — NOT YET SPUN
- Brief ready. Three items, built **sequentially** (they rewrite the same files): **① Haiku pre-screen** (on top — a cheap Haiku call gating whether Opus runs; **validation is ~a week of SHADOW-MODE** logging, so build + ship the shadow-logging ASAP to start the clock; flip-on gated on ~0 false-pass) → **② clustering** (twice-daily ~10am + ~4:30pm PT, Justin manually triggers the first few — LOCKED) → **③ runtime-bookkeeping (Step 57)** (runtime pre-loads + persists, Evryn returns a verdict; removes her raw `supabase_upsert` write tool). Deploys 3rd (rebases onto AC1+AC4).

### AC3 (identity/ops) — NOT YET SPUN
- Brief ready. The broadest lane, **needs the most from Justin** — its §3 lists his action-items (the staging-standup account actions, the GA disable, the forwards→messages design call, the adversarial-test character pick, Mira coordination, the Step-53 spin decision, operator-visibility input). 5 sub-streams; let Justin sequence it.

---

## 🔴 THE ONE OPEN THREAD YOU MUST CARRY — broker AC4 ↔ AC2 when AC2 spins

AC4's **Bundle 2** (the `service_role` generic-**read** lock — stops an injection from bulk-dumping the `users` table) is **blocked on a co-design with AC2's Step 57** (which removes Evryn's raw `supabase_upsert` **write** tool). **When AC2 spins and reports, broker ONE agreed tool-surface end-state between them and relay it to both.** The principle AC4 and I already agreed (hold to it): *after Step 57, Evryn's DB tools = {typed tools} + {AC4's constrained read (table-allowlist + `users` requires an id/email filter + row-cap + log)} + NO raw `supabase_upsert`.* AC4 will NOT build ② until you relay the agreed end-state. **This is the single most important coordination item in the wave — don't drop it.**

---

## Other pending / owed (lower priority)
- **AC2 + AC3 not yet spun** — Justin spins them; process their reports via the loop in "operating model" (read tail → reconcile → append → commit-immediately → CEO-altitude → ping).
- **Mira coordination** (via Justin): AC4's 41a beat + AC2's `triage.md`/onboarding beats + AC3's Mira stream all touch identity files Mira voices.
- **Deploys** (AC1→AC4→AC2, each on Justin's go) + **AC1's Step-72 prod migration** (dev-first + pre/post `pg_dump` → prod-apply gated on Justin at deploy, ADR-037 method).
- **Step 86 (the security lane)** now has sub-items a–e in the sprint, incl. the routed `sendEmail`-provenance guard.

---

## Repo / commit state (as of this handoff — will advance as lanes commit)
- **On Justin's DESKTOP.** All 10 repos were synced at session start (main, `HEAD==origin`, clean); I verified + ff-pulled the 2 that were behind. **You're on `main` everywhere — no worktrees for AC0** (the build ACs use their own).
- `evryn-backend` `main` = **`8adcff5`** (sprint reconciliation flips). `_evryn-meta` `main` = **`868ff6a`** (AC4 brief append). Both pushed. *(The lane ACs commit their own builds to their own branches; the briefs + sprint are on `main`.)*

---

## Key context docs (don't re-derive — read these)
- **The sprint:** `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` — the canonical Step list. Note the new **judge-the-set top banner**, the **Step 86 security lane**, the AC1 DONE/MOOT flips, the reshaped Step 83, and **11a (cache-TTL) → MOOT** (clustering makes the 5-min cache warm; the SDK exposes no cache controls anyway). The watch-list need is routed to Step 47.
- **The 4 lane briefs** (above) — each carries its lane's full assignment + my appended sign-off (the mailbox thread).
- **Security/bulkhead context:** Justin's directive — *multiple* bulkheads between malicious input and our data; "behind every door, another door"; don't rely on "they can't reach this layer." My boundary audit (in the AC4 brief): the main paths are well-bulkheaded (email→`prompt`-not-`systemPrompt`; the hard-constraint untrusted instruction in `core.md`/`triage.md`; the no-send-tool approval gate); the two single-door spots became AC4's lane.
- **CLAUDE.md** (`_evryn-meta/CLAUDE.md`) — auto-loads; the orchestration protocol (`docs/protocols/ac-orchestration-protocol.md`) governs how the lanes run DC/QC.

---

## First moves for the new AC0
1. Load the Light Context Cascade (CLAUDE.md auto-loads; read `current-state.md` + the Hub). You do NOT need the full runtime to orchestrate.
2. Re-read this handoff + skim the 4 lane briefs' tails (the latest mailbox state).
3. Send Justin a CEO-altitude "I'm up, here's the board" + ping, and ask which lane needs you next (likely: process AC2's or AC3's first report, or a lane's QC-GO/deploy-ready).
4. **Carry the AC4↔AC2 broker** (above) the moment AC2 reports.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
