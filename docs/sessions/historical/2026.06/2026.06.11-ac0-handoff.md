# AC0 Handoff — 2026-06-11 (the go-live-gate campaign)

> ⚠️ **EDIT TO ADD — NEW PATHWAY (2026-06-13, AC1).** The sprint-doc restructure (the "⚠️ FIRST TASK" below) is **DONE** — don't re-do it. The docs were reorganized to one-home-per-item:
> - **`SPRINT-V0.2-HARDENING.md` is now the single live v0.2 sprint** (one status-driven Steps list) — **read it for the live go-live plan + the M1 / cost-pass-stamp Gate-B gates.**
> - **`SPRINT-MARK-LIVE.md` is gutted to pure history** — its "Go-Live Delineation — three gates by moment" is now a short **"Go-Live: what happened"** section. So any reference below that points you to *"SPRINT-MARK-LIVE → Go-Live Delineation"* now lands on the **historical** doc, not the live plan — go to `SPRINT-V0.2-HARDENING.md` instead.
> - **`SPRINT-V0.3-CANDIDATES.md`** (new) holds the granular v0.3 candidate items.
>
> **For current state, `_evryn-meta/docs/current-state.md` + `CHANGELOG.md` (both updated at the 2026-06-13 #lock) are authoritative.** Read the FIRST-TASK section below only as the historical spec of what got built, not as work to do.

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

**From:** AC02 (2026-06-11) — ran the **quiet-hours rebuild** + the **cost analysis** + the **SPRINT gate-restructure**, parallel to AC0 completing Phase 6.
**To:** AC0 — you pick up the **go-live-gate campaign: M1 + the cost-fix + the go-live deploy**. (With AC02 and the prior AC0 both stood down, the next instance is just AC0 again — the boundary is clean.)

**Deliberately light.** The Full Cascade + the restructured SPRINT + the cost report carry the *state*; this carries the in-flight nuances and open loops.

> **You're AC0.** Same role — the fresh instance for the v0.2-hardening campaign. Your **first task is the sprint-doc restructure below**, then you run the campaign off the clean list.

---

## ⚠️ FIRST TASK — restructure the sprint doc (Justin's spec, 2026-06-11)

**We passed go-live** (Mark's "we're ready" email goes out today). So **draw a line: close `evryn-backend/docs/SPRINT-MARK-LIVE.md` and create `evryn-backend/docs/SPRINT-V0.2-HARDENING.md`** as the new active sprint (build-focused, post-go-live). Get the *model* right — it's the discipline that kills the intra-doc drift we have now. Justin's spec:

- **ONE canonical list, status-driven.** The body is a single master list of **Steps** (modeled on SPRINT-MARK-LIVE's "Build Record Steps 1-6" — Justin's favorite shape): each item is a Step with a **status prefix** (`DONE` / `IN PROGRESS` / `TODO` / `BLOCKED`) at the front, **ordered by the believed timeline**. **Exactly one entry per thing** — no item in two places, no overlapping lists. Status is the *only* signal of where we are; when something's done you flip its status **in place** (never duplicate into a separate "done" section — that intra-doc drift is the exact thing Justin is killing). Scan down → see what's done vs. not.
- **A VERY light "Current State" header — with explicit instructions written into the section.** The rules (put them in the section): *never a list; never mark anything DONE here; just 2-4 lines of "what just shipped / what's in progress / what's next."* It's a low-res pointer, not the work list, so it can't become a competing list.
- **Everything gets a home in the ordered timeline** (the order we believe today; re-orderable). Group the Steps into phases.
- **Link back to `SPRINT-MARK-LIVE.md`** at the bottom ("where we came from — the go-live sprint, closed"). Lose the bloat, keep the pointer.
- **Consolidate INTO the one list (de-dup as you go):** SPRINT-MARK-LIVE's 3-gate Go-Live Delineation (now mostly DONE) + the v0.2 Hardening bucket + the Backlog · BUILD's resilience backlog (items 1-11) · the Phase-6 findings + Mira dispatch (working docs) · the **7 cost levers** (v2 report) · **Reflection (v0.2-lean)** · **M1** (Stage 1 VIP bot / Stage 2 conditions + operator-visibility) · the quiet-hours **deploy + table-drop + morning-sweep + C2** · the **cache-hygiene pass** · the **gatekeeper-onboarding runbook** · the v0.2 maintenance plan · the adversarial test. **Reconcile BUILD ↔ sprint:** BUILD keeps v0.3 scope + architecture; the v0.2-hardening *execution* items move to the sprint (one home each).
- **Then run the campaign off that clean list** (the Gate B / Hardening work below feeds the Steps).

## Cost — refined thinking (Justin + AC02, late 2026-06-11)

- **We'll mostly run WARM cache → closer to the ~$2k v0.2 floor than $6.5k.** At ~200/day clustered in active hours the 5-min cache mostly stays warm; **switching to Anthropic's 1-HOUR cache TTL makes cold pockets basically never happen** (verify whether the runtime sets it — likely default 5-min; it's a config change). Still high — keep pressing.
- **Is the $2-6.5k before or after Reflection?** The v2 report's §0 floor *includes* Reflection, **but** the cold/warm × Reflection accounting is tangled (the per-turn coefficient was fit on bloated-profile data) → treat the split as **soft**. Key nuance: **Reflection's $-value is biggest when the cache runs COLD** (it shrinks the expensive cold-write intercept + the per-turn re-creates); in the warm regime its $-impact is smaller — it's a hedge for cold pockets + it cuts the cold-write. So *warm cache + Reflection together → the low end.* Resolve the exact numbers with the two week-one measurements.
- **Cache hygiene = a dedicated pass:** (a) prefix byte-ordering (mostly done — the byte-identical common prefix), (b) **the 1-hr cache TTL** (the big lever for cold-pocket avoidance), (c) verify nothing per-call breaks prefix byte-identity, (d) **measure cache-create-vs-read ratio week one** (resolves the $2k↔$6.5k uncertainty — the single highest-value measurement).
- **Adversarial test = the Stage-2 cost-validation batch.** The adversarial fixtures run in DEV against a fictional gatekeeper on a *clean profile* — exactly the cleaned-profile cost-measurement environment. Run them, capture the `llm_usage` rows → real post-cleanup cost data. Combine the two.

---

## Read first (beyond the cascade)
- **`evryn-backend/docs/SPRINT-MARK-LIVE.md` → "Go-Live Delineation — three gates by *moment*"** — the **source of truth** for what gates go-live now: **Gate A** (before Justin's invite email) / **Gate B** (before Evryn first contacts Mark) / **v0.2 Hardening**. Built this session around a first-principles spine: *the approval gate backstops anything Evryn would **say** wrong, so the only hard blockers are the three it can't catch — silent death (M1), runaway cost (cost-fix), contaminated state (the wipe, done).*
- **`evryn-team-workspace/shared/projects/product/research/2026.06.11 evryn-cost-analysis.md`** — the cost analysis (canonical = the v2 synthesis; fitted cost model, n~27 extrapolation, **levers table** as centerpiece). Headline: **~$11k/mo today → ~$2k (warm)–$6.5k (cold) v0.2 → ~$0.6–1.6k v0.3 → ~$200/mo irreducible** — ~95% avoidable. The v0.2 spread is *cache temperature* (measure cache-create-vs-read week one). n=4 HARD + 23 MODELED, tagged throughout.
- **`_evryn-meta/docs/decisions/040-quiet-hours-notification-redesign.md`** — the quiet-hours decision (committed `8d595e1`, **not pushed**).
- The Phase-6 working docs (`_evryn-meta/docs/working/2026-06-09-phase6-{findings,mira-dispatch}.md`) — AC0 formalized Findings 13–22 + Mira items 10–22.

## The campaign — Gate B (before Evryn contacts Mark)
1. **M1 — `#emergency-alerts` silent-death detector + DND override.** *Not the long pole.*
   - **Stage 1:** a **dedicated VIP'd Slack emergency bot** — Justin confirmed this is the *only* DND-break-through path (he VIPs it). Add `notifyEmergency()` (new `src/notify/emergency.ts` or in `dev.ts`) + `SLACK_EMERGENCY_WEBHOOK_URL` → `#emergency-alerts`. Smoke-test that it breaks Justin's DND (only he can confirm, on his phone).
   - **Stage 2 (design → brief → Justin spins a *fresh Soren* to vet):** which conditions fire — polling-dead after retries exhausted · hard auth failure (401/403, not transient) · **process-crash (needs an *external* watchdog — a dead process can't alert itself; Railway healthcheck-failure alerting is the minimal path)** · volume/loop anomaly · any send bypassing `SEND_ENABLED`/the gate. **Pair with the operator-visibility design** (a dashboard/feed so a *correct* silent pass isn't indistinguishable from an outage — the open-Q AC0 assigned to AC02; M1 detects death, the feed shows liveness — two halves of "Justin knows the system's alive").
2. **Cost — the long pole (refined 2026-06-11 with Justin).** It splits into *prevent* (Gate B, before forwarding) and *bound* (early v0.2 Hardening):
   - **PREVENT (Gate B):** the **deterministic pass-stamp** (runtime writes `<contact-uuid> passed <ts>`, contact-record only, **no LLM note** — kills note-writing on the ~5,700/mo pass majority; ~$5–7k/mo, low risk: removes *writing*, not *judgment*) + the **Mira note-discipline** (tight notes / no cross-contact maps / no pass-notes / clear bindings — **Mira leads this**; it's the cheapest, fastest lever on *new* bloat) + binding-hygiene. Mira note-template in progress (Justin + Mira).
   - **BOUND (v0.2 Hardening — now in SPRINT, top of that bucket):** **lean-Reflection consolidation — PULLED FROM v0.3 to v0.2** (Justin's call; the cost case is decisive). Profile bloat is a *recurring re-read tax* (force-loaded + re-read every turn; test-Mark hit ~28k tokens). A periodic per-user consolidation (accumulated notes → tight `story`, archive the raw) cuts cache-read across *all* ~200/day queries — a no-brainer at volume. **LEAN scope** (token-cutting only; the full matching-aware Reflection — ADR-019/027 — stays v0.3); a fast-follow, **not** a launch gate (clean post-wipe profile + the pass-stamp buy the runway).
   - **TWO STAGES (Justin):** Stage 1 = burn down the bloat (pass-stamp + Mira-discipline + lean-Reflection). Stage 2 = *then* a **synthetic cost-validation batch** (cheap on a clean profile; measures real go-forward cost) — do NOT run it on bloat.
   - **Tiering** the pass-decision (Opus → lighter model) = a must-run experiment but **post-bloat-cleanup** (double-read on escalations + quality-miss risk; don't test on bloat).
   - **Reports:** `evryn-team-workspace/shared/projects/product/research/2026.06.11 evryn-cost-analysis.md` (v1) — and AC dispatched a **v2** (extrapolated to n~27 with confidence bands + a **levers table**, per Justin) that AC is consolidating into the canonical file. **Validate the pass-floor with a real cleaned-pass run IN DEV** as you build — not in the wiped prod. DC builds → QC reviews.
3. **Real auto-forward format validation** (Finding 10) — at forwarder setup, push one real Gmail auto-forward through; confirm `detectForward`/`splitForwardBody`/original-sender extraction fire.
4. *(soft, approval-gate-backstopped)* early Mira teeth — dispatch items 6 (no internal "gold" to contacts) + 7 (don't over-validate).

## Quiet-hours rebuild — DONE, but NOT deployed (intentional)
- **BUILT + QC-GO** on branch **`dc/quiet-hours-redesign`** (commit `91cc963`); worktree at `c:/Users/Justin/Evryn/Code/evryn-backend-dc-quiet-hours`. **NOT merged, NOT deployed, worktree NOT reaped — deliberate:** its urgency evaporated when Phase 6 finished, so it bundles with the go-live deploy.
- **Deploy plan (Justin delegated timing to AC):** bundle **quiet-hours + M1 + cost-fix + morning-sweep + C2 → ONE go-live deploy + smoke test → then the `notify_queue` table-drop.**
- **Pieces that ride the bundle:**
  - **(a) Morning sweep (decision 2a):** a once-per-morning "N drafts awaiting approval" digest at window-open, silent if zero (~15 lines — Justin wants the predictable clean ping). Build it on the quiet-hours branch (or a fresh one) before the bundle deploy.
  - **(b) C2 fix:** `src/triage/classify.ts` `notify_slack` tool comment still describes the retired "queued-then-drained" path — trim to immediate-post. One-liner.
  - **(c) `notify_queue` table-drop (decision 1a):** DROP the table **after** the code deploy (the *old* live code SELECTs it every poll cycle, so dropping before deploy spams errors). Dev-first + pg_dump pre/post (ADR-037 method). The CREATE migration is `backups/add-notify-queue-table.sql`; write a `drop-notify-queue-table.sql`.
  - **(d) Quiet-hours doc absorptions (at the deploy #lock):** **state-docs → delete** the retired machinery (`ARCHITECTURE.md` lines ~233 [notify_queue row] + ~1182 [runtime summary tail]; `schema-reference.md` lines ~118–130 [notify_queue section]). **work-records → mark in place** (`BUILD` item 9f → shipped, resilience item 5 → moot). **operator-guide** quiet-hours sections (lines ~30–32, 112, 315) → update to "pings post immediately; DND for night-silence; only the stale-checker is gated." **current-state** Slack line → update *when it deploys* (until then the old queue+replay code is what's live, so that line is still accurate).

## Onboarding-gatekeeper runbook — to create (Justin asked)
A **standalone operator runbook** for onboarding *any* gatekeeper (reusable beyond Mark), linked from `ARCHITECTURE.md`. Generalize the Mark go-live steps (SPRINT "Pre-Go-Live Cleanup" wipe/create-from-zero + operator-guide go-live checklist + the BUILD onboarding workflow). Proposed home: `evryn-backend/docs/gatekeeper-onboarding.md`. Not a hard blocker; in v0.2 Hardening.

## Open loops / reconciliation
- **BUILD Status + current-state still say "M1 the one remaining gate"** (AC0's Phase-6 lock framing). SPRINT now correctly carries **M1 + the cost-fix** as the two Gate-B gates — Justin and I established the cost-fix as a genuine go-live gate this session. **Reconcile BUILD Status + current-state to match** (light — just acknowledge the cost-fix gate).
- **SPRINT restructure: edited, pending Justin's Source-Control review + my commit go** (may already be committed by handoff time — it's in the working tree regardless).
- **ADR-040 committed (`8d595e1`) but NOT pushed** — push with the next batch.
- Two untracked **`evryn-backend/backups/llm-usage-*.sql`** = AC0/earlier (NOT mine — leave them).
- **✅ DONE (2026-06-11):** retired the 2026-06-10 AC02 handoff + the 2026-06-10 backlog-notes to `docs/sessions/historical/` (Justin OK'd); codified the worktree-isolation pattern in `AGENT_PATTERNS.md` (the Agent `isolation:worktree` is cwd-repo-scoped, so a subagent building in a *sibling* repo makes its own worktree there).

## Working rhythm
- Ping `#team-alerts` every response (Node `fetch`, `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`, ASCII-only). **AC0 is done** (Phase 6 complete + locked). **Deploys gated by Justin** (he delegated *timing* to AC for this campaign, but `railway up` is still his coordination — give a heads-up). **Commit discipline:** no commit without his explicit go in the immediately-preceding turn.
- DC/QC as subagents per `docs/protocols/ac-orchestration-protocol.md`.

— AC02, 2026-06-11

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
