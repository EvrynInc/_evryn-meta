# AC0 Handoff — 2026-06-09 (evening)

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

**From:** AC0 #5 (2026-06-09 evening — ran Phase 6 S3b live-fire with Justin, collected + routed the findings, did the #lock, wrote this).
**To:** the next AC0.

**This handoff is deliberately lean.** `current-state.md` + the top `CHANGELOG.md` entry carry the state; this doc carries what they *don't* make obvious. It replaces the heavy `2026-06-04-ac0-handoff.md` (4 addenda; now retired to `docs/sessions/historical/`).

## Cold start — read in this order
1. **The full product-architect cascade** per `CLAUDE.md` SESSION STARTUP — Hub, technical-vision, `evryn-backend/docs/ARCHITECTURE.md`, **the full runtime `evryn-backend/src/`** (every file), the active BUILD doc. **Don't trim it** because the task "feels" small — you're operating on a live system about to go to a real user.
2. `_evryn-meta/docs/current-state.md` — the live snapshot (Phase 6 status + the go-live sequence).
3. `_evryn-meta/CHANGELOG.md` — the top entry is this session.
4. This doc.

## Your mission — finish Phase 6, then the go-live sequence
Phase 6 (the last test before Mark) is **mid-run and going well.**
1. **S1 (Nadia) is gold-drafted + queued.** The approval ping was quiet-hours-queued and **replays ~8am PT**. When Justin approves, he replies as Mark ("yes, connect us") → expect `delivered → matched`. **This is the first clean gold — check the three context-fix signals on her draft before approving** (no doubled context block; history labeled `Mark Titus <systemtest@evryn.ai>`, NOT "(user)"/UUID; history-count shown at submit). *(I queued this validation; it hasn't been run yet.)*
2. **Run order: S1 → S2 → S7 → S8 → S4 → S5 → S6.** Run sheet: `evryn-backend/tests/phase6-live-fire-v02.md`. Justin plays Mark from `systemtest@evryn.ai`. **Justin now drops the gatekeeper note** ("forwarding this one") — real production is note-less auto-forwards, so that's the realistic test. ⚠️ **S8 = the REAL Amy Lillard — do NOT approve any outbound to her** (it's a gold-call test only; Mark follows up with Amy himself).
3. **Then the go-live sequence:** Pre-Go-Live wipe (`SPRINT-MARK-LIVE.md` / operator-guide STEP 0 — wipe test-Mark + every Phase-6-created sender, Evryn create-from-zero of the *real* Mark, **Justin verifies the DB is clean with his own eyes**) → Justin's **"we're ready" email** to Mark (can go out) → **M1** → Mark goes live / actively forwards.

## The non-obvious (what the state docs won't make obvious)
- **M1-sequencing — get this right.** **M1** (the `#emergency-alerts` silent-death detector — the one hard build gate, NOT built) gates Mark **actually forwarding/conversing**, NOT the "we're ready" intro email. **The email precedes M1.** (This was conflated in current-state + BUILD earlier this session and corrected; SPRINT always had it right. If you ever see a doc say "M1 before emailing Mark," it's stale — fix it.) M1 is two parts: the **detector** (a crashed process can't alert itself → needs an external watchdog, e.g. Railway healthcheck-failure alerting) + the **break-through** (post immediately via the existing `bypassQuietHours` hook + a DND-override emergency channel). Spin DC→QC. Spec: `SPRINT-MARK-LIVE.md` Backlog (M1 entry) + the 2026-06-04 pre-go-live audit.
- **Bug-routing delineation — triage every finding into ONE of three buckets** (this is how Justin wants bugs routed going forward):
  1. **Fix TODAY** — only what blocks the wipe / create-from-zero / the "we're ready" email itself. (Usually nothing.)
  2. **Week of 6/8** (`SPRINT-MARK-LIVE.md` Go-Live Delineation, M1 at top) — blocks *actual* Mark-forwarding (operational safety net); can land after the email. **A DC batch.**
  3. **v0.3** (`BUILD-EVRYN-MVP.md`) — autonomous-scale hardening; bites when no operator is watching every cycle.
- **This session's findings are routed:** `BUILD-EVRYN-MVP.md` hardening **item 9** (the 12-item Phase-6 batch, bucket-2 + bucket-3) + `SPRINT` week-of-6/8 + Backlog→Mira/identity. Full detail: `_evryn-meta/docs/working/2026-06-09-phase6-findings.md`. **A Mira identity trip is pending** — brief: `_evryn-meta/docs/working/2026-06-09-phase6-mira-dispatch.md` (6 items; gold-needs-teeth / don't-over-validate / gentle-brokering are the early ones). *(Both working notes can be cleaned at #sweep once picked up.)*
- **Quiet-hours redesign is QUEUED, not shipped** (BUILD item 9f): let regular pings post and rely on Slack DND for night-silence; time-gate *only* the stale-checker to waking hours; optional 8am "anything pending?" sweep. Current behavior (queue + ~8am replay) is unchanged until DC builds it.
- **Mark's record is now "Mark Titus"** (display_name + full_name persisted this session via Justin's last-name tell).
- **Verify live state; don't trust a stale doc.** This session: the run sheet said an item was "parked" in `pending_approval`; the live DB showed it `delivered` (already sent). When it matters, query prod — don't infer from a doc.

## The working rhythm (AC0 + Justin, during the test)
- **Justin wakes you each time** — no polling loop (he decided against it; some lag is fine). When he pings, **pull both Railway logs and the DB before any approval.** Railway: `railway logs --json -n 80` or `--since 5m` (both disable streaming — no hang). DB (full-path PG17 + the admin URL from `evryn-backend/.env`):
  ```
  DBURL="$(grep -E '^SUPABASE_DB_URL_PROD=' c:/Users/Justin/Evryn/Code/evryn-backend/.env | cut -d= -f2- | sed -E 's/^"//; s/"$//')"
  "/c/Program Files/PostgreSQL/17/bin/psql.exe" "$DBURL" -c "SELECT ..."
  ```
  (Useful IDs: Evryn `1dc33f28-…`, Operator `5db9d677-…`, test-Mark `7497d231-…`.)
- **Ping Justin on `#team-alerts` on every response** (CLAUDE.md lines 163-165 — no exceptions unless he opts out; a mid-flight ping never replaces the end-of-turn one). Webhook `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`; Node `fetch`, ASCII only (no em-dashes/smart quotes).

## Traps
- **No commit without Justin's per-commit go** (mailbox excepted) — he reviews diffs in Source Control during the pause. **This #lock's edits are UNCOMMITTED, pending his SC review.**
- **Verify branch before edits in the shared repos** (`evryn-backend`, `evryn-team-workspace`); stage by path, never `git add -A` (other agents share these trees).
- **The gun's always loaded** — `railway up` is the live one; confirm git + Railway state first.
- Quiet hours (18:00–08:00 PT) queue Slack approval pings → they replay ~8am. An evening draft won't ping until morning (validated working — it's the night-protection, not a bug).

## Open loops (carry forward)
- **Committed + pushed (2026-06-09 eve):** the #lock batch (`_evryn-meta d9c9d3d`, `evryn-backend 30ac66e`, `evryn-team-workspace 4f9cbbd`), the run-sheet fix (`7ca9e6b` — resume banner now S1 + Justin's S3b tweak), the kill of the stale 6/4 `ac0-to-mira-dispatch` (`1755efb`), and the AGENT_PATTERNS *Cross-Scope Resolution Seam* entry (in `d9c9d3d`). The 6/4 handoff is retired to `docs/sessions/historical/`.
- **UNCOMMITTED — left for Justin's morning review** (per his "leave everything uncommitted"): `evryn-backend/docs/BUILD-EVRYN-MVP.md` — item 9 reformatted to bucket-2 (week-of-6/8) only + the new "v0.3 Hardening / Polish (Phase-6 deferred)" section holding the bucket-3 items; plus this handoff's open-loops refresh.
- **Soren context-arch dispatch — archived (done, 2026-06-10).** Content was fully shipped (ADR-034 amendment + de-dup + name/email labeling + visible-at-`submit_draft`; who-leads resolved). Moved to `docs/sessions/historical/ac0-to-soren-context-architecture.md`; Soren's agent-memory pointer updated to the new path.
- **`docs/working/cron-architecture-and-cross-loading.md` — next-AC0 to handle.** Kept *only* because two live runtime comments still point to it: `evryn-backend/src/db/messages.ts:86` + `src/triage/classify.ts:231` ("Bug B / Spec 2 per …cron-architecture-and-cross-loading.md"). **To finish:** repoint those two comments to `evryn-backend/docs/ARCHITECTURE.md` (Identity Composition → the *Operator-about-user auto-load*, where Bug B lives permanently), then archive the working doc to `docs/sessions/historical/`. Small, deliberate evryn-backend trip — don't break the links or rush it.
- **Nadia context-fix-signal validation** — run it when approving S1 (first clean gold).

## Protocols you'll need
- `docs/protocols/lock-protocol.md` (#lock) · `docs/protocols/ac-orchestration-protocol.md` (spinning DC/QC for M1 + the week-of-6/8 batch).

— AC0 #5, 2026-06-09 evening

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
