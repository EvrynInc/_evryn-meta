# Handoff — AC0 → next AC0 — 2026-07-15 (proper handoff: ③ ratified+built+QC-GO/unmerged · 54a/54c merged · AC7 landed · bundle staged behind Gate-A/B)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** START HERE if you're a fresh AC0. You conduct the **product wave** (AC2 = cost / Step 57 "③", AC3 = identity/ops) under **ACU** (a meta-coordinator one level up — his `docs/working/2026.07.13-acu-uber-week-plan.md` shows the whole week; context, not gospel). You orchestrate, gate merges/deploys, reconcile cross-lane collisions, keep housekeeping current, and translate to Justin at CEO altitude. **You do NOT write runtime code.** This handoff is **self-sufficient for the conductor view** — read a lane's brief in full on-demand when that lane surfaces work.

---

## 🚀 First moves (fresh AC0, on boot)
1. **`git fetch` all repos** and confirm each is on its canonical branch, `main == origin/main`, tree clean (per `docs/repo-inventory.md` + the CLAUDE.md repo-sync ritual). SHAs in this doc are as-of-write and WILL have moved.
2. **Load the Light cascade** (`_evryn-meta/CLAUDE.md` auto-loads → `docs/current-state.md` → Hub `roadmap.md`). Load the **full `src/` runtime** if Justin calls the full cascade OR when you're about to gate **③** (it's a runtime rewrite — you can't gate it blind). ARCH+BUILD are reconciled+accurate (AC6, 7/09) — a trustworthy on-ramp, **NOT license to skip `src/`.**
3. **Remake your ping helper** (⚡ below) and **ping `#team-alerts` "I'm up"** (`AC0:` prefix). **Ping every response — no exceptions** (Justin corrected this hard; I dropped it twice early-session).
4. **Nothing is hot-blocked on you at handoff.** The live thread: AC2 finishes ③'s live-path DEV exercise, then pings you for **Gate-A**. Be ready to gate, reconcile, keep records current.

---

## ⚡ Operating model (this stretch — CARRY THESE)
- **You conduct; you do NOT write runtime code.** Lanes' DC/QC build; you gate merges/deploys, reconcile cross-lane collisions + sequencing, keep Justin at CEO altitude, keep housekeeping current (current-state, CHANGELOG, deploy-log, SPRINT marks).
- **Ping helper (remake it — the path won't persist):** a Node script POSTing `{text}` to `SLACK_TEAM_WEBHOOK_URL` (read that var's LINE from `C:/Users/Justin/Evryn/Code/evryn-team-workspace/.env`; use the `C:/Users/...` path in Node, NOT git-bash `/c/...`). Slack pings via Node (Windows mangles curl/PowerShell).
- **Lanes take recommend-first ballots STRAIGHT to Justin — NOT routed through AC0** (Justin's steer, 2026-07-14: routing through you adds cognitive-switching + "you have mail" overhead). You are conductor for **merges / collisions / housekeeping**, not a question-router. The AC2/AC3 brief tails (2026-07-14) drill the time-discipline: answer-yourself, recommend-first ballots, batch, never idle, ping every response.
- **Model = Opus-default + per-turn Fable escalation** (`⚡ FABLE TURN NEXT: [what] — flip me, then say go`; flip back after). Reach for Fable on: irreversible/expensive calls (merge-gates, deploy sequencing), many-interlocking-constraint decisions, heterogeneous synthesis, adversarial review of high-stakes work, framing-ambiguity. **Lane ACs stay Opus**; AC2 had a one-turn Fable option for the ③ design synthesis (used it); **the ③ merge-gate gets a dedicated Fable review** (ratified).
- **Deploys: Justin does NOT run `railway up`** ("beyond my pay grade", 2026-07-15) — **a lane or AC0 runs it, on Justin's explicit go.** You assemble + gate the bundle. ⚠ A **bare `railway up` deploys PROD**; staging is `railway up --service evryn-staging --environment production`. Also ⚠ **a Vercel push = a live deploy** (the dashboard in `_evryn-meta/dashboard/` + `evryn-website` auto-deploy on push to main) — so merging a dashboard/website branch IS a deploy → Justin-gated.
- **Commit/deploy gates are Justin's, per-action, don't stack.** Lanes build/commit on their own branches freely; PAUSE for his explicit go on **merge-to-main / any deploy / any prod migration**. **STANDING EXCEPTION (yours):** housekeeping-doc commits+pushes (current-state, CHANGELOG, deploy-log, SPRINT marks) + mailbox/brief + working-doc/handoff — you commit + push freely. **Stage by explicit path, never `git add -A`** (shared repos; ACU's `ac-orchestration-protocol.md`/`uber-week-plan.md` and ACf's `evryn-team-workspace` files are often dirty — never sweep them).
- **Part A doc-currency (standing):** keep ARCH/BUILD/SPRINT current at each checkpoint; ARCH edits are Justin-SCM-vetted before you commit. **AC7's #lock doc-currency gate + `doc-update-routing-protocol.md` are now LIVE** — use the routing protocol (change-type → doc-home) so this is mechanical.

---

## 📊 LIVE MAP (authoritative — the conductor's picture)
- **Prod DEPLOYED = `v0.2.7`** (`evryn-backend` `517014d`; Clustering + Haiku both live-**OFF**/dormant). **`evryn-backend` main = `c9813a0`, AHEAD of deployed prod** — it carries 54a/54c code + AC7's ARCH/BUILD doc headers, all **deploy-pending**.
- **⭐ Step 57 / ③ (Runtime Bookkeeping) = RATIFIED + BUILT + QC-GO, UNMERGED — big advance overnight (7/14).** State: design (`docs/working/2026.07.14-ac2-step57-runtime-bookkeeping-design.md`) → **Justin ratified both ballot calls** → **ADR-051** written (+ amendments to ADR-018/033/036) → DC-built → rebased onto `2572dbc` → AC-review found 2 defects (both fixed) → **QC = GO, ZERO blockers/non-blockers** (fresh fully-loaded, heightened). On branch **`ac2/step57-runtime-bookkeeping`** (tip `3266325`, pushed to origin). **What ③ does:** the runtime pre-loads context + owns status/lifecycle writing; Evryn returns a structured verdict + does only judgment (cuts a pass ~15→2-4 turns). **The two ratified calls:** (1) **user-creation moves to VERDICT-time** (none created for `ignore`); (2) **raw `supabase_upsert` REMOVED wholesale** (also deletes Step-18b routing + the note-turn upsert guard) → the tool-surface end-state = `{typed verdict/outcome tools}` + a NEW **operator-channel-only `correct_user_field` tool** (conditional-registration like `set_thread_scope`; field-allowlisted on `users`: `display_name`/`full_name`/`email`/`outbound_address` — never `profile_jsonb`/`cross_user_notes`/`status`/other tables) + `{AC4's constrained supabase_read}` + **NO raw `supabase_upsert` anywhere**. **③ ships switch-less, ZERO prod migration** (triage_result CHECK already allows gold/edge; `messages.metadata` exists). In-scope folded in: 54b (forward-persistence, verdict-keyed option C) + the user-creation-at-verdict question (ratified) + the note-turn `record_pass` residual. **Remaining before merge:** live-path DEV exercise (AC2 runs — breaker off, real model) → **Gate-A (Step 88, WITH AC0)** → Mira pass → Fable merge-review → Justin's merge. **Full live handoff (reload manifest + the QC-enumerated Mira divergence list):** `docs/working/2026.07.14-ac2-step57-dc-build-review.md`.
- **54a + 54c = BUILT + MERGED** to `evryn-backend` main `2572dbc` (QC-GO one pass; typecheck green; identity-file-review 7/7 on 54a). **NOT deployed** — ride the ③ bundle; the two live behaviors verify at Gate-B. **54a** = re-points Evryn's "have I met this person?" triage check at the CONTACT's permanent record (Mira `triage.md` beat applied). **54c** = captures the real forwarded-sender name (`detect.ts`/`process.ts` + Mira Opus-correction beat). *(My night handoff wrongly called 54a "Mira-beat-ready, Justin hands to Mira" — it's DONE + merged.)*
- **AC7 = CLOSED + LANDED** across 3 repos (`_meta a9a34c0` / `backend c9813a0` / `team-ws c1d9ba8`), reaped, changelogged (`4dae58a`). The **#lock doc-currency gate + `doc-update-routing-protocol.md` + ARCH/BUILD doc-currency headers + soren/mira "docs-current-is-part-of-done" beats are LIVE.**
- **CHANGELOG is current through AC7 (7/14).** 54a/54c + ③ are correctly NOT changelogged (no-changelog-for-unshipped) — the next CHANGELOG entry is at the ③ bundle deploy.

## 🛣 Critical path to a gatekeeper
**③ live-path DEV → Gate-A (Step 88, WITH AC0, breaker OFF) → Mira pass → Fable review → Justin merge → deploy THE BUNDLE → Gate-B (mandatory pre-gatekeeper staging create-from-zero live-fire, everything active together) → flip Clustering + Haiku on → Justin account setups → gatekeeper.**
- **⚠ ③ is an interactive/tool-calling + money-path rewrite → the process lesson:** a breaker-tripped/stubbed/mocked test proves the *plumbing*, not the behavior. Gate-A must be a **live-path DEV exercise (breaker off, real model) BEFORE ③ merges.**
- **Gatekeeper:** Mark radio-silent-not-lost; a 2nd gatekeeper warming. Hold as "ready when *a* gatekeeper shows."

## 📦 THE BUNDLE DEPLOY (one coordinated `railway up` — AC0 assembles/gates; lane-or-AC0 runs it, NOT Justin)
Carries **③ + 54a/54c + the Anthropic key-split env swap + the dead `_PROD` var cleanup**. **ZERO prod migration** (③ needs none). Gated Gate-A → merge → Gate-B. At this deploy AC0 runs the consolidated doc-currency + CHANGELOG + deploy-log + current-state pass, including the **ARCH ③-absorption** (from ADR-051; Justin-SCM-vetted). *(OPEN, Justin hasn't answered: draft the ARCH absorption ahead of the deploy vs at-deploy — my rec: **at-deploy**, ARCH should describe what's live.)*
- **Key-split** (runbook `docs/working/2026.07.13-acu-ac0-anthropic-key-split-runbook.md`): AC0-owned rider; **value-only env swap** on `evryn-backend` prod (`ANTHROPIC_API_KEY` → a dedicated Evryn-Production-workspace key), NO code. Justin ~10 min console (J1–J5). No ACU questions — self-sufficient; team-runtime is subscription-auth, untouched.
- **`_PROD` cleanup:** dead unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` on Railway prod; `railway variable delete` can't `--skip-deploys` → rides this window.
- **Staging for Gate-B:** UP + healthy (Step 80 DONE). **DECIDED (AC0):** it's one migration short (the clustering `queued` CHECK-widen) — fold it into a single **pre-Gate-B fresh staging re-seed** from a current zero-PII prod-shape dump (closes the gap + absorbs any migration + gives the live-fire a clean create-from-zero slate + keeps the seed files canonical). **Skip a one-off apply** (nothing needs staging before Gate-B). Deploy staging via `railway up --service evryn-staging --environment production`; `SEND_ENABLED=false` until the run (Justin flips it); Megan-Griffiths fixtures (`evryn-backend/tests/fixtures/emails/megan/` — a subdir, point the runner at it) feed it.

## LANES
### AC2 — cost / ③ (brief: `docs/working/2026.06.30-ac0-ac2-cost-lane-brief.md`)
- **③ is through review (QC-GO, zero blockers).** Next: AC2 runs the live-path DEV exercise, then loops you for **Gate-A**. Seams AC2 flagged for you:
  1. **F2 metadata-shape (coordinate w/ AC3):** ③ stashes `original_from_name` in `emailmgr_items.metadata` (additive, alongside `original_subject`; self-contained to ③'s files, no 54a/54c change — just ensure 54a/54c + ③ agree on the shape).
  2. **Mira-coordinate-ONCE:** AC3's 54a `triage.md` "history" beat and ③'s Mira pass BOTH touch `triage.md`'s Phase-1 history beat — voice it ONCE against ③'s pre-loaded shape (under verdict-time user-creation the FK may not exist pre-verdict → the pre-load falls back to email-match). The **QC-enumerated divergence list in `2026.07.14-ac2-step57-dc-build-review.md` is the exact Mira input.** This is my "Mira-coordinate-once" tracked item.
  3. **AC4 read-lock:** no read-guard changes needed; keep the `emailmgr_items.original_from_user_id` scope key (still load-bearing for operator/edge reads). One tool-surface end-state (③ removes the raw *write*; AC4 locked the *read*; + the new `correct_user_field`).
  4. **Step 71** (Reflection binding safety-net) is SPLIT OUT of ③ — its own small trip, may ride the same deploy window. **Step 58a** rode ③ (DONE — ③ wired `cluster now` into `catchUpOnReconnect`).

### AC3 — identity/ops (brief: `docs/working/2026.06.30-ac0-ac3-identity-ops-brief.md`)
- **54a + 54c DONE + merged** (see LIVE MAP). AC3's last session was a boot-and-audit that routed me the SPRINT marks (done below).
- **OPEN (for Justin, low-priority): "Steps 78/79 vs standby."** AC3 asked whether to fill on the dashboard extensions — **Step 78** (real `/health` 503-when-wedged + 5 per-subsystem status lights) + **Step 79** (staging dashboard tab) — or stand by. These are DC/OC + a Vercel deploy (Justin-gated). Surface it as a fill-vs-standby call when he has a moment.
- **Do NOT start without Justin's go:** the Mira decline-*offer* runtime+identity pair (Step-55 routed-back) · Steps 78/79 dashboard extensions · DEFER Step 51 (compulsion audit — touches `classify.ts`, collides with ③).
- **Partials:** Step 43 Megan fixtures WRITTEN, the RUNS are the Gate-B; Step 47 watch-list done, the Soren maintenance-plan half is owed (Justin held it).

### AC7 — doc-accuracy process (CLOSED, landed 7/14)
Fully landed across 3 repos + changelogged; instance closed. The gate + routing protocol are live — you'll run the next #lock (the ③ deploy) under them.

## ✅ SPRINT marks done this handoff (AC3 routed 6 + AC2 flagged 2)
Flipped in `evryn-backend/docs/SPRINT-V0.2-HARDENING.md`: **48/49/56/80 → DONE** (AC3 ops: gatekeeper-onboarding runbook `5f2c019`, DB-rollback in operator-guide `5f2c019`, Nathan legal glance `036fbd7`, staging standup), **41b → DONE** (curiosity onboarding beat, live in onboarding.md), **45 → MOOT** (subsumed by Step 78), **58a → DONE** (③ wired `cluster now` into catch-up replay), **minted Step 90** (reply-item doctrine — deferred from ③ §2). **54a/54c held-to-deploy** (DONE+merged but mark them at the coordinated deploy, per no-changelog-for-unshipped — AC3's instruction).

## 📌 AC0 OWED (standing — carry these)
1. **Gate-A WITH AC2 before ③ merges** — DEV live-path (breaker off). AC2 pings you after its own DEV exercise.
2. **Gate-B orchestration** — the staging create-from-zero live-fire (pre-Gate-B reseed [decided above] + Justin's `SEND_ENABLED` flip + Megan fixtures).
3. **Part A doc-currency at the ③ bundle deploy:** ARCH ③-absorption (ADR-051; Justin-SCM-vetted) + consolidated CHANGELOG + deploy-log + current-state + team-cs + flip 54a/54c/③ SPRINT steps DONE.
4. **Cross-lane:** the **Mira-coordinate-once** point (③'s Mira pass ↔ AC3's 54a `triage.md` pass, voiced once) + the **F2 `original_from_name` metadata-shape** agreement (③ ↔ 54a/54c).
5. **Two CLAUDE.md-lesson candidates AC3 re-flagged (propose to Justin on his go):** (a) *distinguish what a subagent ACTUALLY DID vs RECOMMENDED — verify, don't conflate*; (b) *before routing an item into another agent's domain doc, check if it's already in THAT agent's memory as their owed self-note*.
6. **Housekeeping as-you-go** (CHANGELOG at every ship — top priority); **ping every response.**

## ✅ On Justin's plate
- His go on the ③ merge + the bundle deploy. Staging `SEND_ENABLED` flip at Gate-B. The Clustering + Haiku prod flips + account setups before a gatekeeper forwards. The ARCH-absorption-timing call (rec: at-deploy). The AC3 78/79-vs-standby call. The 2 CLAUDE.md-lesson proposals.

## 🧭 Key docs
`docs/current-state.md` (7/15 line is the latest) · `_evryn-meta/CHANGELOG.md` (current through AC7) · `docs/deploy-log.md` · `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` + `docs/decisions/051-*` (ADR-051, ③) · ③ docs `docs/working/2026.07.14-ac2-step57-runtime-bookkeeping-design.md` + `-dc-build-review.md` · lane briefs `docs/working/2026.06.30-ac0-ac2-cost-lane-brief.md` + `-ac3-identity-ops-brief.md` · `docs/working/2026.07.13-acu-uber-week-plan.md` + `-ac0-anthropic-key-split-runbook.md` · `evryn-backend/docs/staging-runbook.md` · `_evryn-meta/CLAUDE.md` + `docs/protocols/{ac-orchestration-protocol,lock-protocol,doc-update-routing-protocol}.md`.

## 🔧 Repo state (as of this handoff-write — `git fetch` on boot; SHAs move)
- `_evryn-meta` main = `07bdb4a` at write (advancing as this handoff's own commit + others land). `evryn-backend` main = `c9813a0` (ahead of deployed prod: 54a/54c + AC7 headers). `evryn-team-workspace` main = `d6b97b9`.
- **③ branch `ac2/step57-runtime-bookkeeping` (tip `3266325`) is on origin** (I pushed it for durability 7/14 — AC2 will force-push after any further rebase; normal).
- **Not mine — do NOT sweep:** ACU's `ac-orchestration-protocol.md` / `uber-week-plan.md` (when dirty); ACf's `evryn-team-workspace` in-flight files. The `evryn-backend-step57` worktree is AC2's active ③ tree.
- **This handoff-session's housekeeping commits** (SPRINT marks + both current-states + this handoff) are pre-authorized — committed + pushed at #lock close.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
