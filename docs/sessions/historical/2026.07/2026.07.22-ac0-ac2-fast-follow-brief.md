# Brief: AC2 — hardening fast-follows (Evryn product)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this:** the AC2 lane's single shared surface — its marching orders AND its live state. It is written so a **completely fresh AC2 can re-spin from this file alone**, cold, with no memory of any prior session. That property is the whole point: it makes falling off the context cliff free.
>
> **What belongs here:** live state, the open queue, the load manifest, held environment state, and operational knowledge that exists nowhere else. **What does NOT belong here:** the build history of shipped work. That lives in `evryn-backend/CHANGELOG.md`, `_evryn-meta/CHANGELOG.md`, the ADRs, `_evryn-meta/docs/deploy-log.md`, and git history — all of which a fresh AC2 loads anyway. When something ships, collapse it to a one-line pointer and let the changelog re-hydrate it.
>
> **Rewritten clean 2026-08-04T15:18-07:00 by AC2**, on Justin's instruction to make it current rather than keep layering corrections onto superseded text. Prior revisions (the ③ cost-lane history, the Step-94 build narrative, the accreted correction notes) are in this file's git history. AC0's 2026-08-04 ordering-override appendage is **absorbed into §3** — AC0, that is where it went.

---

## §1 — Where this lane is, right now

**You are AC2, a REGULAR PEER AC** on the Evryn product (`evryn-backend`), owning the hardening fast-follow queue. Sign `AC2`. You are **not** a subagent of AC0 — that experiment was canceled (Justin: more complexity than it fixed). There is **no live AC-to-AC channel**: coordinate with AC0 through Justin or through a committed file.

**You own the design inside your lane.** AC0 is a peer who owns cross-lane sequencing and collisions — bounce *those* to him, decide your own design calls. Treat an AC0 brief as a pitch to bring independent judgment to, not orders.

**Live state:**

- **Prod = `v0.2.9`.** Nothing this lane has built since is deployed. That is by design — see the deploy plan below.
- **`evryn-backend` `main` = `5bbebc5`** (as of 2026-08-04 late afternoon), **1 commit ahead of origin** — AC2's CHANGELOG entry for the 35b/35d pair. **AC0 pushes.** Merged-but-undeployed on `main`, all riding the one staging-first bundle:
  - **SPRINT Step 94** — the two-layer hung-call guard (silence watchdog + absolute ceiling, ADR-054).
  - **SPRINT Steps 35b + 35d** — the gatekeeper-recognition pair, code + identity, merged **together** by AC0-30 (`acf086d` + `df01e9c`). See §4.
  - **SPRINT Steps 96 + 96b** — AC5's silent-email-drop fix in the Gmail ingest fan-out (merge `056a70d`). Added `src/safety/fetch-retry.ts`, which is why the `src/` count moved 44 → 45.
- **Two cost levers stay DORMANT** in prod: Clustering (`CLUSTERING_MODE`) and the Haiku pre-screen (`HAIKU_SCREEN_MODE`), both unset → both no-ops. Flipping them on is a deliberate pre-Mark action.
- **The deploy plan (AC0-28's call, Justin delegated deploy timing):** ONE bundle, **staging first**. Step 94 + the AC5 silent-email-drop fix + the ACa `classify.ts` extraction all land on `main`, then deploy that bundle to **staging**, flip `CLUSTERING_MODE` on **there**, run **Gate-B** against it, then ONE prod deploy of exactly what Gate-B validated. **Prod stays `v0.2.9` until after Gate-B.** Your work rides that bundle — do not ask for a standalone deploy.

**⚠️ Unpushed work exists in several repos** — enumerate it yourself before assuming anything is durable (`origin/main..main` alone will NOT show you a never-pushed branch; use the all-branches loop in AC's `CLAUDE.md`). As of this writing, AC2's own unpushed commits are: `evryn-backend` main +1 (the CHANGELOG entry), `evryn-quality` main +1 (QC's promoted patterns), and part of `_evryn-meta` main's +10. **All pushes are AC0's.**

**Other lanes and the collision surface** — check before you edit a shared file:

- **ACa — the atlas/refactor lane. This is the one that constrains you.** Dependency map → claims audit → Code Atlas → **extracting `src/triage/classify.ts`**. Anything you build in that file wants to land *before* the extraction, or one of you rebases across a large structural change. This is why §3's ordering constraint existed for 35b, and it applies again to any future `classify.ts` work.
- **AC5 — closing.** SPRINT Steps 96 + 96b are merged to `main`; its worktree `evryn-backend-ingest` and branch `ac5/ingest-fetch-resilience` (never pushed) were still present at handoff. **Not yours to reap.**
- **ACfb — new since this lane's last set-down** (team-runtime): making team-runtime boot refusals audible via an emergency Slack alert. Different repo entirely; no collision with the product lane, but it is why `_evryn-meta` has commits and dirty files that are not yours.
- **AC0** typically has `docs/current-state.md` and his own handoff/lane briefs dirty in `_evryn-meta`. **Leave them strictly alone** — stage by explicit path and `git diff` any shared file before committing it.

---

## §2 — Working with Justin (load-bearing — carry these verbatim)

- **Ping `#team-alerts` (prefix `AC2:`) EVERY time you have a response for him** — he runs many lanes in parallel, so an un-pinged update sits unseen. Node `fetch` → `SLACK_TEAM_WEBHOOK_URL` (in `evryn-team-workspace/.env`); use `C:/...` path form in Node, never git-bash `/c/`, and never echo the URL. Attention-tap only — substance goes in chat. **Even when he seems present, assume he isn't.**
- **Put your substantive chat response LAST in the turn, after all tool calls** — a response written between tool calls gets buried.
- **He comes in COLD every volley and reads ONLY the chat**, never briefs or docs. CEO altitude, concept first then jargon. **No jargon-by-number** — "Step 94" / "③" / "NB-2" mean nothing to him; name the thing ("the hung-call guard"). Re-anchor at the top of every substantive reply.
- **He does not vet build specs — you own engineering correctness.** Get his go on the *substance* in plain language. Deploy and merge are the real gates he holds on code.
- **Recommend-first, always.** Every ask carries, in this order: cold-reader context → your recommendation and why → alternatives rejected → the numbered question inline. Never a bare "what do you want?". Batch asks so he clears you in one dip; keep advancing everything not blocked.
- **A plan you present is a request to vet, not a green light.** Wait for explicit authorization on the plan before executing it. Workflow verbs ("handle it," "ping me when you're done") describe the work, not the authorization.
- **Commit discipline:** each **code** commit needs his explicit go in his *most recent* message. Brief / mailbox / CHANGELOG / deploy-log / ADR / handoff commits are pre-authorized. **Stage by explicit path — never `git add -A`.** And in a live multi-agent repo, **`git diff <file>` before committing a shared file** — a by-path commit takes the file's whole working-tree state, so another agent's uncommitted edits to the same file ride along under your message and `git status` will not warn you.
- **Merge to `main`, `railway up`, and identity-file changes are ALWAYS Justin-gated.**
- **"Leave it in SCM" means leave it UNCOMMITTED** so he can vet the diff in source control first.
- **Framing to hold:** Evryn is an **independent contractor doing work for the gatekeeper, not his assistant.** And "trusted" means the **instruction-vs-data wall** (all inbound rides `prompt`, never `systemPrompt`), not vouching — only the Operator (verified Slack ID) is structurally elevated.
- **Model:** stay **Opus**. Offer Fable only for a heavy merge-gate review.

---

## §3 — The queue

| Item | State | Note |
|---|---|---|
| **Step 35(b)** — record-legibility signal in `buildPersonContext` | ✅ **CLOSED — MERGED to `main` `acf086d`, undeployed.** Branch + worktree reaped. Sprint Step 35 flipped DONE by AC0-30. CHANGELOG written. | Structural half. 122 discriminating checks, 5 mutations red, typecheck clean — **re-verified by AC2 on merged `main`**, so the AC5 merge seam is accounted for. See §4. |
| **Step 35(d)** — the "a record the runtime hands you IS the real person" reasoning beat | ✅ **CLOSED — MERGED to `main` `df01e9c`, undeployed.** Branch + worktree reaped. | Identity half. Mira wrote it; Justin vetted the diff in SCM and widened its scope himself; Mira signed off and refined one noun inside his fix. See §4. |
| **SPRINT 90** — reply-item doctrine | **NEXT, design-call-first** | A `reply`-classification `submit_draft` against a pre-verdict/`queued` forward can flip it out of triage and silently drop it from the batch. Needs a status-boundary design call: when may a reply attach to a forward item vs. get its own placeholder? Low reachability pre-Mark. Touches `src/email/process.ts` + `src/approval/flow.ts`. |
| **Step 93** — operator `discard <id4>` Slack command | **TODO** | Design already decided (Justin's ballot): deterministic, classification-agnostic, symmetric with `approve`; touches `parseSlackMessage` + a `handleDiscardMessage` in `slack.ts`, a `discardDraft` in `flow.ts`, `items.ts`. **Plus a Mira `operator.md` beat** so Evryn points the Operator at it — deliberately NOT an Evryn-side discard tool. **One open sub-question is yours:** what terminal status a discarded draft lands in — a new `discarded` value (a small CHECK-widen + `ItemStatus` add, exactly like `queued` shipped) vs. a lifecycle-note-only close on an existing terminal. Decide it inside the ③ verdict/outcome model (ADR-018, `verdict.ts`, `outcome.ts`). **Trap:** a new operator command must ALSO be added to `catchUpOnReconnect`'s replay in `slack.ts`, or it silently degrades to a general Evryn message during a socket disconnect (Step 58a proved this). |
| **Step 71** — Reflection binding safety-net | **OFF THIS QUEUE** | Reassigned to its own dedicated lane (Justin, 2026-08-04). Do not pick it up. |

### The ordering constraints (originally AC0-28's; the first one is now DISCHARGED)

**✅ DISCHARGED — "land 35b before ACa extracts `classify.ts`."** It merged, so ACa can extract once, cleanly. **But the underlying constraint is still live for any FUTURE `classify.ts` work:** ACa is extracting that file (Justin ruled: extract *before* Gate-B, so Gate-B tests the real runtime), so anything you build in it wants to land early or you will rebase across a large structural change. Check ACa's state before opening that file.

**Group SPRINT 90 and Step 93 adjacently** — both touch `src/approval/flow.ts`, so back-to-back saves you rebasing yourself. **SPRINT 90 is design-call-first**, so expect a round-trip with Justin before any build.

**Put the `CLUSTERING_MODE` flip and its owed live check LAST** (§7) — it is an env change plus a supervised verification rather than a build, it is the natural close of a session, and it is the one item that wants Justin actually watching.

---

## §4 — Steps 35b / 35d: what was built, and why it was built that way

> **This section is CLOSED history — kept, not because anything is owed, but because the *reasoning* is load-bearing and easy to undo by accident.** Three things in here will look wrong to a future reader and are deliberate: the affirmative wording (do not re-add a denial list), the rejection of the tracked `is_stub` shape, and the scoping to *who is real* rather than *records*. If you are about to "fix" one of those, read this section first.

**The failure being fixed:** Evryn has repeatedly failed to recognize a real person's record **that the runtime loaded directly into her prompt**. The manifestation Justin called most concerning: she dismissed her own gatekeeper's real record as *"the test account"* and reasoned onward from that dismissal. She had the record; she discounted it.

**The tracked Step-35(b) text says to "mirror `find_user_by_name`'s `is_stub`" — that was under-shaped, and AC2 rejected it (Justin agreed).** `deriveIsStub` (`src/db/users.ts`) is derived purely from `status === "lead"`, and a **gatekeeper is `status: "active"`** — so an `is_stub` mirror would **not have fired on the case Justin actually hit.** The shape built instead: **make the record's realness legible ALWAYS; explain thinness only when the profile is thin.** `is_stub` survives as the one clause that varies, not as the trigger. *(SPRINT Step 35's inline text used to still describe the old shape; **AC0-30 rewrote it at the flip**, so the sprint and this brief now agree.)*

**35b (code, `src/triage/classify.ts`):** `buildPersonContext` now leads with a bracketed interpretation block. Two new PURE exported helpers — `isThinProfile` and `buildRecordLegibilityBlock` — plus `tests/test-record-legibility.ts`. `deriveIsStub` is **imported** from `db/users.ts`, never re-derived, so there is one home for "stub." No DB reads, no new tools, no control-flow change. *(It ended up **not** strictly zero-deletion — see the two replaced render guards below.)*

**One change reaches BOTH surfaces**, which is why it lives in `buildPersonContext` and not at the call sites:
1. `loadUserSuffix` → the scoped user's `## User Context` in the systemPrompt (composition layer 3) — the **gatekeeper** on the forward/direct pathways. This is where the "test account" failure lived.
2. `formatContactContextBlock` in `src/triage/preload.ts` → the **contact's** record inside the Step-57 triage pre-load, which rides `prompt` (layer 4) — where a thin `lead` reads as fake.

**The block deliberately stays out of the byte-identical cached common prefix** (`loadCommonPrefix` reads only identity files off disk and never calls `buildPersonContext`) — verified against the runtime and guarded by `tests/test-cache-byte-identity.ts`.

**The wording is affirmative on purpose — do NOT "strengthen" it back into a denial list.** An earlier draft read *"not a placeholder, a stand-in, or a test artifact."* Mira's objection, accepted: denial-by-enumeration invokes the frame it denies, so Evryn would read three nouns for *fake* before reading the person, on every user-bearing query. It is safe to drop **because the fixture nuance now lives in `identity/core.md`, which is force-loaded into the common prefix on every single query** — so the anti-"test account" instruction is present regardless. The *thin* clause keeps its targeted denial (`Thin means "not yet known," never "not real."`) — that one is doing real work and stays.

**35d (identity, `identity/core.md`):** two paragraphs inserted in "What You Can Draw On", immediately after the existing *"The runtime is not infallible; your judgment is part of the safety net"* paragraph.

- **The tension that had to be held, not collapsed:** that existing line is correct and must survive — it is the affordance that lets Evryn recover when the runtime hands her the wrong *frame*. A naive *"trust the runtime"* beat would contradict it and would be worse than the bug. **The resolving distinction: the runtime can be wrong about the FRAME (which module, which activity, which pathway); it is not wrong about WHO IS REAL** — records are real rows resolved deterministically before she wakes.
- **Scoped to "who is real," NOT "records"** — deliberately. A beat saying the runtime is not wrong about *records* would collide with `correct_user_field` and `operator.md`'s "fixing a plain error in a user's details," which exist precisely because records carry factual mistakes. Realness and canonical identity are authoritative; field-accuracy is not.
- **It does not claim no record is ever a fixture** — `systemtest@evryn.ai` genuinely is one, and a false instruction would corrode everything near it. The rule is about what she does with the suspicion: escalate (`notify_slack`) or name the uncertainty, never silently downgrade a real person to a fixture.
- Placement was chosen over `activities/triage.md` because there is **no token saving** — `triage.md` is inside the same force-loaded prefix as `core.md`, so a narrower placement with worse coverage would be a pure loss. And over `situations/operator.md` because that file is gated to Operator-audience pathways and would be **absent** on `processForward`/`processDirect`, which is exactly where the failure happened.

### 🔴 The two halves MUST merge together — and the reason is the reverse of the obvious one

DC's framing was *"if the code ships alone, nothing carries the anti-fixture warning."* **QC corrected that at source and her version is the one to keep.** The code half alone *does* carry anti-fixture signal (the realness claim, plus `Thin means "not yet known," never "not real."`). The genuine coupling runs the other way:

**`core.md:236` already says, unbounded: *"The runtime is not infallible; your judgment is part of the safety net."*** That line is *about* frame/module selection, but read cold it is a general license to override runtime-supplied facts — and it sits **~75 characters** from where Mira's beat goes. Shipping the **code half alone** would introduce a brand-new *runtime assertion of realness* while leaving that unbounded license right beside it, unscoped. That composition is arguably **worse than shipping neither**. Mira's beat is precisely the scope (*"The runtime can be wrong about the frame. It is not wrong about who is real."*) — it is what converts the code half from a discountable signal into a binding one.

### QC's verdict and what she verified

**GO, zero blockers** (fresh full-load spin, both runtime halves, receipts reconciled). What she verified rather than assumed:

- **`core.md` is force-loaded on every pathway that renders a person record — and more strongly than that.** She enumerated all **8** `runEvrynQuery` pathways plus the 2 raw `query()` sites: 7 compose via `composeSystemPrompt` (whose first line is unconditionally `loadCommonPrefix()`); Reflection uses `loadReflectionPrefix()` (still core.md); the Haiku screen loads no identity **and renders no person record**. She then proved the beat is inside the force-loaded bytes using the *real* `loadCommonPrefix()` — the prefix **starts with core.md verbatim**, so every byte of it ships.
- **The cache invariant holds** — the block is absent from the 147,939-char common prefix; it renders only in the per-user suffix (layer 3) and the `prompt` pre-load (layer 4). Cost: **32 tokens** per render in the steady-state rich case, 86–94 thin.
- **`cross_user_notes` cannot leak** through `buildPersonContext`, bare or nested through `formatContactContextBlock`.
- **Both runtime facts Mira asserted are true:** `notify_slack` is in the *unconditional* MCP tool list and `allowedTools` (all 8 pathways, before both conditional blocks); and person records are resolved before `composeSystemPrompt` on every pathway.
- **The tests are not decorative** — she ran **six** mutations of her own, all red.

### Two non-blockers found and fixed (fix-trips to the same DC)

1. **A JSDoc asserting an invariant that did not hold.** `isThinProfile` tested story as `typeof === "string" && trim().length > 0`; `buildPersonContext` tested it as bare truthiness. Five shapes (`"  "`, `42`, `true`, `{a:1}`, `["a"]`) produced a block claiming *"no story yet"* directly above a rendered `Story:` line. **Unreachable from any runtime writer** (QC traced them all) — but reachable via **direct DB writes, which is exactly what DEV/staging seeding and the Gate-B fixtures do**, and Gate-B is where this block gets its first real exercise. **Fixed structurally:** two shared predicates (`hasRenderableStory` / `hasRenderableNotes`) that BOTH the describer and the renderer call, so they cannot disagree without someone editing one function that changes both sides at once. The JSDoc now explains *why* it holds rather than asserting that it does.
2. **A JSDoc saying the removed denial list "SHIPPED."** It never did — `main` has no legibility block at all. Corrected to match the commit message.

⚠️ **The diff is no longer zero-deletion:** `734 insertions, 2 deletions`. The two deletions are exactly the two replaced render guards. Behavior is unchanged for every runtime-reachable state; the **only** outputs that differ are those five malformed shapes, which now render *nothing* rather than `Story: 42` / `Story: [object Object]`. That is deliberate — a malformed value in Evryn's prompt is worse than its absence — but "purely additive" is no longer literally true, so expect it at merge review.

### 📌 The durable learning — carry this to Gate-B, and beyond

**The code is provably safe. Whether the fix WORKS is not observable by any test we can write.** This is a change to *prompt wording*, aimed at changing how the model reasons. Unit tests can prove nothing regressed; they cannot prove Evryn now reads a thin record as real. The failure mode is that a correct GO on safety gets read as *"the fix works"* — and the stronger the unit coverage, the more convincing that false closure.

**So the efficacy check must be NAMED, with an owner, in a specific downstream gate, or it evaporates.** The recommendation (QC's, endorsed): add to **Gate-B (SPRINT 88B)** — *"with a freshly-created gatekeeper whose profile is thin, confirm Evryn treats the record as real, with no 'test account' framing."* Gate-B is the natural venue because a create-from-zero gatekeeper **is** the thin-`active` case Justin hit.

**Residual risk, stated honestly:** both halves are **instructional, not structural.** Nothing detects "Evryn concluded a real record is a fixture" and stops her; the escalate affordance depends on her taking it. That is the right call for v0.2 — this is a judgment-runtime problem and a hard block would be worse — but hold it as *materially reduced*, not *closed*.

**The fix's KNOWN COVERAGE EDGE (QC's observation, not a defect — recorded so it isn't rediscovered as a surprise):** the legibility block covers the two surfaces `buildPersonContext` feeds. A record Evryn pulls **mid-turn** is different. Via `find_user_by_name` she gets `is_stub`, which is a legibility signal by design. But via a generic `supabase_read` on `users` she gets **raw columns with no legibility signal at all** — so the "reads like a fixture" misread could in principle recur there. Two things keep this narrow rather than urgent: `operator.md` routes name lookups through `find_user_by_name` first, and Mira's beat is scoped to *"the people the runtime loads for you,"* which correctly does **not** over-claim about records Evryn fetches herself. If the failure ever recurs after this ships, **this edge is the first place to look.**

### Where Justin landed on the identity half, and the two changes he/Mira made after QC

Justin **vetted the `core.md` diff in SCM** (2026-08-04) and made one change himself: he widened *"you dismissed **your own gatekeeper**"* → *"you dismissed **a legitimate user**"*. Two things that bought, and the second is the one that matters:

1. It removed the last real-person specificity — QC had flagged *"your own gatekeeper"* as the closest call in the diff, since at v0.2 it resolves to exactly one identifiable person.
2. **It fixed a genuine scope defect nobody had caught.** The thin-record branch keys on `deriveIsStub(status)` = `status === "lead"`, and a **`lead` is a discovered contact, not a gatekeeper** (a gatekeeper is `active`). So the thin-reads-as-fake case — the one the block spends its extra sentence on — is structurally a *contact*. *"Your own gatekeeper"* had pointed the identity half at the one surface that **isn't** the thin case. Mira reviewed this and conceded it plainly: *"it didn't just carry a privacy risk; it narrowed the identity half to the one surface that isn't the thin case."*

**Mira then refined his noun to *"someone real"*, and Justin took it.** Her reasoning, verified at source by AC2: *"legitimate user"* is access-control **category-noun** register, and reducing people to their categories is on Evryn's own avoid-list (the Hub's ethos summary) **and** `activities/triage.md` carries an explicit ban on category nouns for a person (*"never 'Dear inquirer / lead / hopeful'"*). It is also a word shorter, and it lands on **`real`** — the paragraph's own hinge word (*"It is not wrong about who is real"*) — so the paragraph now bookends rule-stated / rule-broken on it. Justin's scope fix is fully preserved; *"someone real"* is just as generic.

### ✅ Steps 35b/35d are fully CLOSED — nothing owed on them

Every thread is discharged, by whom:

- **Justin** — vetted the identity diff in SCM, made the scope-widening edit himself, ruled on Mira's noun, approved the QC-pattern promotion.
- **AC2** — both halves committed; `evryn-backend` CHANGELOG entry written (`5bbebc5`); QC's patterns promoted into `evryn-quality/CLAUDE.md` (`735d63f` — **two entries, not the three Justin approved**: QC's second proposal was a fold-into-an-existing-entry by her own description and proved to be the same discipline as her first, so it landed as a sentence inside it rather than a third entry repeating it); both branches + worktrees reaped after merge.
- **AC0-30** — merged **both halves together** (`acf086d` + `df01e9c`), having rebased the identity half inside its own worktree with the patch **md5-verified byte-identical** before merging; flipped SPRINT Step 35 → DONE and **rewrote its stale inline text** (the "mirror `is_stub`" framing is gone); placed the named efficacy check in Step 88B **with an owner**; minted **Step 97** for the containment hole with its own lane.
- **AC2, post-merge** — re-ran the full suite against *merged* `main` (122 checks, typecheck clean, all three cache/composition invariant suites green), so the AC5 merge seam is accounted for rather than assumed. Also confirmed the removed denial-list wording appears on `main` **only** inside the "do not re-add" comment, not in the live clause.

**No ADR was written, deliberately.** Every decision here already has a durable home: the frame-vs-record principle *is* `identity/core.md` (permanent); the rejected-`is_stub` reasoning is in the CHANGELOG, the sprint Step, and a code comment; the affirmative-wording rule is in a `🔴 DO NOT re-add` JSDoc plus a test that fails on a re-add. Nothing would evaporate. Flagged as a judgment call in case a later reader disagrees.

---

## §5 — The load manifest (the re-spin list)

**Load `main` for the shipped state, then diff any worktree you own** — the worktree is ground truth, this brief is a claim about it. Clear contradictions with Justin before continuing.

**Always load, every AC2 spin:**

- The **Light cascade**: `_evryn-meta/CLAUDE.md` · `_evryn-meta/docs/current-state.md` · `_evryn-meta/docs/hub/roadmap.md`.
- `_evryn-meta/docs/hub/technical-vision.md` — the bulkhead / module-separation frame.
- `evryn-backend/docs/ARCHITECTURE.md` **in full.** Four sections are directly load-bearing for this lane: **Monitoring & Silent-Death Safety (M1)** · **Security → Bulkhead Layers** · **emailmgr_items Status Lifecycle** (Step 93's terminal-status question is decided inside it) · **Agent Architecture → Model tiers**.
- `evryn-backend/docs/BUILD-EVRYN-MVP.md` **in full** — v0.2 scope + the "What's NOT in MVP" list; a primary *"that's deliberately out of scope"* surface.
- `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` **in full, not just your Steps.** The `DONE` entries and the owner tags are exactly what tells you a thing you just noticed is already shipped or already someone else's job.
- `evryn-backend/docs/SPRINT-v0.2-optionals.md` — tiny, and precisely the "considered and parked on purpose" signal.
- `evryn-backend/CHANGELOG.md` + `_evryn-meta/CHANGELOG.md` — the meta one carries changes to your own operating manual and protocols.
- **The runtime, BOTH halves, enumerated LIVE, every file in full.** 🔴 `find src -name "*.ts"` **cannot** return an identity file, so enumerate `identity/**/*.md` **separately, every time** — otherwise the recipe built to prevent under-loading guarantees it.
- **🔴 Do not trust any file count in any doc — including this one.** The counts moved **three times in a single session**: `src/` went 43 (per the docs) → 44 (actual) → **45** (AC5's `fetch-retry.ts`), and `classify.ts` went 2,606 (per the sprint) → 2,974 → 3,122 → **3,186**. Identity has held at **11**. As of 2026-08-04 late afternoon the live figures are **45** `src/*.ts`, **11** `identity/*.md`, `classify.ts` **3,186**. **Re-count at spin time — the enumeration is the only authority, and these numbers are already decaying as you read them.**

**Why the load is this heavy:** the queue's items land across `classify.ts`, `process.ts`, `poll.ts`, `flow.ts`, `slack.ts` — ~8,200 lines by themselves. The union across the queue is close to the whole runtime, so nothing useful is saved by dropping files. The saving comes from being spun for **one job at a time**.

**The "it's already handled" map — stay curious, then check before you act.** Reading a whole runtime guarantees you notice things outside your lane. That noticing is valuable; chasing each one is waste. When you notice something, spend ~60 seconds on these four, in order: (1) `SPRINT-V0.2-HARDENING.md` — is there a numbered Step, and what is its **owner tag**? (2) `SPRINT-v0.2-optionals.md` — was it deliberately set aside? (3) `BUILD-EVRYN-MVP.md` → "What's NOT in MVP" and `SPRINT-V0.3-CANDIDATES.md` — consciously deferred? (4) `current-state.md` → Active Instances — is another AC in that lane right now? Then: **tracked and owned elsewhere** → note it in one line here and move on · **tracked but the record looks WRONG** → surface it, this is high-value and exactly what a full read is for · **not tracked anywhere** → a genuine find; surface it recommend-first.

**Reference on demand, not resident:** `SPRINT-MARK-LIVE.md` (the closed go-live sprint — go here to check the provenance of a `DONE` that looks wrong) · `SPRINT-V0.3-CANDIDATES.md` · `BUILD-EVRYN-v0.3.md` · `BUILD-FUTURE.md` · `docs/staging-runbook.md` · ⚠️ `docs/schema-reference.md` (carries a DO-NOT-TRUST banner — three live tables missing; SPRINT Step 92 is the fix, owner AC0. **Read the live DB, not this file.**).

---

## §6 — 🔒 Held DEV state — DO NOT WIPE

The Gate-A DEV state is still live evidence and is **still held**:

- The **Megan** gatekeeper (`43a68cfa-d6c6-4088-b86b-38a5d6583669`, `systemtest@evryn.ai`) — the fictional gatekeeper lane.
- The **frozen a12 `processing` row** (a hang preserved in amber).
- Four untracked `tests/_gate-a-*.ts` on `evryn-backend` `main` — **the held Gate-A harness. NOT debris. Do not sweep them into a commit and do not delete them.**
- DEV `emailmgr_items` debris from the Step-94 live-path work: `67739ec9-4eb7-4160-9fb9-09882b2bc1e0` · `3bc75edb-9b02-46ee-a6c6-e9244114bc9f` · `7f2dddcf-0060-4657-b38a-8a5041fe3022` · `51fe0c27-d9d2-47a8-ac04-033ed96ebdbc` · `0ed08425-e6ac-42b2-a388-e1a9b8b48002`, plus contacts they reified (e.g. Priya Raman `25300a49-2722-468f-9892-cd2d8c3b3c22`) and their `llm_usage` rows.
- Plus ~60 `Cluster Test Gatekeeper` rows (`cl-gk-…@evryn-test.invalid`).

**Why the hold stands:** the owed nested-relay live check (§7) needs the Megan gatekeeper lane on DEV. Wipe in ONE pass only after that completes.

---

## §7 — The ONE owed verification, gated to the `CLUSTERING_MODE` flip

**What is unproven:** the Step-94 nested note-turn **liveness relay** — the 3-hop wiring (`keepAlive` → `onActivity`) that keeps an *outer* query's silence watchdog from aborting while an *inner* query is genuinely producing. It is unit-tested **discriminatingly** (mutation-checked: disable the relay and the suite goes red), but **not live-proven.** Two DEV attempts were **inconclusive** — verified from `llm_usage`, not inferred: **no inner query ever started**, because the outer spent longer on its own research than the shrunk test budget allowed.

**Safe to defer:** until the flip the pathway is **structurally unreachable** — `runForwardNoteTurn` sits inside `processForward`'s `if (clustered)` branch, and `CLUSTERING_MODE` is unset in prod. It cannot fire or misfire.

**Carry this finding to the flip:** the note-turn's own research can occupy a **single unbroken silent stretch of >60s** — inside the 120s budget but under 2× headroom, far tighter than ordinary triage (~91s *total*, no gap near the budget). So a single research call crossing 120s could abort a note-turn with no nesting involved. Failure is safe (the item stays `queued`) but looks causeless to the operator.

### 🛠 How to run it — the harness was deleted, rebuild from this

⚠️ **Treat every specific below as a strong lead, not verified fact.** It was reconstructed from memory at the end of a long session, after the harness had already been deleted. The **measurements** (>60s outer research, ~91s inner triage, the `llm_usage` pathway tags) came from observed runs and are the most reliable part; the **procedural detail** (call names, argument shapes, paths) is memory and is where it is most likely to be subtly wrong. **Check every name and signature against the runtime before relying on it — if something here contradicts the code, the code wins**, and correct this file rather than working around it.

**Setup.** Make a fresh worktree off `main` with a **DEV-pointed `.env`** (`EVRYN_ENV=dev`, the `SUPABASE_*_DEV` pair, `SEND_ENABLED=false`): `git -C evryn-backend worktree add ../evryn-backend-relay -b ac2/relay-check main`. Do **not** run this in `evryn-backend` — its `.env` may be prod-pointed. Note that a fresh worktree has no `node_modules`; `npm install` there. Put the script in that worktree's `tests/` with a `_` prefix (the ephemeral convention) and run with `npx tsx`.

🔴 **THE TRAP THAT WILL SILENTLY RUIN THE RUN.** `src/config.ts` reads `process.env` **at module load**, and ESM **hoists static imports above all statements**. So this does NOT work:

```ts
process.env.EVRYN_QUERY_SILENCE_TIMEOUT_MS = "75000";   // runs SECOND
import { config } from "../src/config.js";               // hoisted — runs FIRST
```

The budget is read before your assignment lands, you silently get the 120s default, and **the run looks fine while testing nothing.** You must use **dynamic import**: set every `process.env.*` first, then `const { config } = await import("../src/config.js");` — and likewise for `processEmail`, `assertNotProd`, `isBreakerTripped`, `findItemByExternalId`, `resolveGatekeeperByInboundAddress`, `getDb`.

**Steps.**
1. Set `CLUSTERING_MODE="manual"` (the note-turn lives inside `processForward`'s `if (clustered)` branch — with clustering `off` it is unreachable and the run proves nothing) and your silence budget. Then dynamic-import everything.
2. **Four safety gates, all of them:** `assertNotProd()`; refuse unless `config.evrynEnv === "dev"`; refuse if `isBreakerTripped()`; refuse unless `config.sendEnabled === false`.
3. Confirm `await resolveGatekeeperByInboundAddress("systemtest@evryn.ai")` is non-null. **If the held DEV state has been wiped this returns null, the forward escalates instead of triaging, and the run is meaningless — fail loudly here.**
4. Build a fake `EmailMessage` (`{id, messageId, threadId, from, subject, body}` cast through `unknown`), `from: "Megan Griffiths <systemtest@evryn.ai>"`. The body must open with a **non-trivial personal note** (that is what `hasPersonalNote` keys on → fires the note-turn) then a `---------- Forwarded message ----------` block. Word the note to push her toward **`resolve_noted_forward("urgent")`** — the only action that nests an inner query.
5. `await processEmail(email)`, timing it.

**Read the result from the DB, NEVER from the return value.** `processForward` catches note-turn failures by design and returns `"completed"` either way, so the return value cannot distinguish success from abort. Get the item via `findItemByExternalId(email.messageId)`, then query `llm_usage` on `emailmgr_item_id`:

- **A `processForward` row present alongside `processForwardNote`** ⇒ the inner query really ran ⇒ **the relay was exercised.** Combined with "no abort, item pulled out of `queued`", that is a **PASS**.
- **Only `processForwardNote`, or no rows at all** ⇒ the inner query never started ⇒ **INCONCLUSIVE — not a failure, and not a pass.** ⚠️ An aborted query never reaches `recordLlmUsage`, so *no rows* is the expected shape when the outer aborted — that is what both prior attempts produced.
- **Inner row present AND the outer aborted anyway** ⇒ 🔴 a **REAL DEFECT** in the relay. Do not ship the flip.

**Choosing the budget — and the honest catch.** At the **production 120s** the check is **NOT discriminating**: the inner triage (~91s) fits inside 120s, so the outer would likely survive *even with the relay broken*. That shows the pathway works; it does not prove the relay is why. To actually prove the relay you need a budget **above the outer's own research (>60s observed) and below the inner triage (~91s observed)** — try **75000** — so that without the relay the outer *must* abort. That window is narrow and both endpoints vary per run, which is exactly why the prior attempts could not land it and why the supervised flip is the better venue. **Do both:** one production-budget run for real-world confidence, and one or two 75s runs for the discriminating proof.

**Expect to re-run.** The note wording *steers* her choice; it does not force it. She may pick `casual`/`wave_off` (item stays `queued`, no nesting), or spend so long on her own research that the outer trips first. Both are inconclusive, not failures. Budget a few Opus calls per attempt.

---

## §8 — Open items routed elsewhere (do NOT take these on)

- **The "sole Anthropic spend door" claim is INACCURATE** in `src/triage/classify.ts`, `src/safety/breaker.ts`, `src/db/llm-usage.ts`, and `ARCHITECTURE.md`'s M1 section. `runHaikuScreen` → `defaultRunQuery` (`src/triage/haiku-screen.ts`) calls the SDK's `query()` **directly**, so Haiku spend never reaches `recordAttemptAndCheck` and **cannot trip the M1 breaker.** Not a runaway hole (`maybeRunHaikuScreen` early-returns when the breaker is tripped) — a **safety-claim accuracy** defect. AC2 verified this at source 2026-08-04. **Routing is AC0's / the ACa claims-audit's.** Do not reason from the claim while you are in `classify.ts`.
- **A NEW standing constraint Step 94 created, invisible from any single file:** *any MCP tool handler doing more than the silence budget of work will trip the outer watchdog.* All 16 handlers in `buildEvrynMcpServer` were enumerated; only `resolve_noted_forward` nests, and the heaviest non-nesting one (`submit_draft`) is seconds-scale. Recorded in ADR-054. **Anyone adding or lengthening an MCP tool handler must check it against the silence budget.**
- **A pre-existing double-ping** on a resume-time transient DB error: `resumeParkedItem`'s resolution catch fires its own `notifyDev` **and** rethrows, so `poll.ts`'s rate-limited transient alert fires too (`processEmail` was deliberately de-duped). Rare, carries useful extra context, left alone. A future de-dup pass, not urgent.
- **`identity-file-review.md` (team protocol) has a stale deploy assumption.** Its Preconditions section *does* carry a parenthetical noting auto-deploy is off, but the author-steps still assert operationally that *"branches don't trigger Railway redeploys; only `main` does"* and that *"identity-file merges trigger Railway redeploys."* Nothing auto-deploys — a merge does nothing until a manual `railway up`. Harmless today (reality is *safer* than the doc implies), but a fresh reader will trust it about deploy behavior. **Mira's / Lucas's to route at a `#sweep`.** Surfaced by Mira 2026-08-04, corrected and verified at source by AC2.
- 🔴 **A REAL, VERIFIED CONTAINMENT HOLE — `create_user` serializes the firewalled `cross_user_notes` column into Evryn's context.** Surfaced by QC during the Step-35 review; **independently verified at source by AC2**. NOT caused by Step 35 — it correctly did not gate that merge, and it is not this lane's to fix.
  - **The chain:** `findUserByEmail` / `findUserById` (`src/db/users.ts`) both `select("*")` on `users` → that returns `cross_user_notes` → `findOrCreateUser` returns the full row on the find path → the `create_user` tool handler (`src/triage/classify.ts`) returns `JSON.stringify({ user, created })` with **no projection and no strip**.
  - **This is not an inference.** `src/security/supabase-read-guard.ts` was written about exactly this behavior and documents it verbatim: *"`select:*` returns ALL columns incl. cross_user_notes → a firewall-bypass."* `USERS_RETURNABLE_COLUMNS` exists solely as the all-columns-except-`cross_user_notes` list — but it is applied **only** on the generic `supabase_read` path. AC4's Bundle 2 closed that door and left this one.
  - **Failure scenario:** a gatekeeper gives feedback on a contact → `append_cross_user_note` writes it to that contact's `cross_user_notes`. Later Evryn calls `create_user` with that contact's email — which the tool's own description actively invites as a have-I-seen-them-before check — and the gatekeeper's **raw** feedback about that person, plus the reporter's `from_user_id`, lands in Evryn's context **while she is working with them.** That is precisely the wall ADR-027 Decision 3 exists to build.
  - **⚠️ "Not live in prod today" is AC2's REPORT, not a census.** The reasoning is that prod has no contacts and no recorded cross-user feedback yet, so there is nothing to leak — but **nobody queried the prod table to confirm it.** AC0 correctly recorded it in Step 97 as an inherited claim rather than adopting it as fact, and the owning lane should either census it or keep the caveat. It goes live the moment the gatekeeper feedback loop runs — which is core to Mark's calibration. `create_user` is registered **unconditionally** on all 8 pathways.
  - **✅ The class is BOUNDED AT ONE — AC0 verified this, and it bounds the fix.** He swept every other tool that could return a user row: `find_user_by_name` uses an explicit thin column list, and the generic `supabase_read` is already covered by the read-guard's column-deny. So `create_user` is the only exposed door, not the first of many.
  - **Fix direction:** strip the firewalled column at the **typed-helper boundary** (`findUserByEmail` / `findUserById`), not at `create_user`'s return — that closes the class, and any future tool returning a full row inherits the protection. ⚠️ Heed the read-guard's own warning while doing it: *omitting* a real column from a returnable list silently **drops** it from reads (a legit-flow break), and `story` / `profile_jsonb` must stay readable — so the fix is "exclude exactly one column," never "narrow to a hand-picked set."
  - **✅ NOW TRACKED: this is SPRINT Step 97, with its own dedicated lane** (Justin's call, 2026-08-04; minted by AC0-30, who carried the full chain, the failure scenario, and the class-bounded sweep into the Step). **Not AC2's to build** unless Justin reassigns it.
  - **🔴 OPEN CROSS-RUNTIME QUESTION — genuinely unanswered, and it should ride Step 97's lane (AC0 to route; see §10.1).** AC0's sweep bounded the class **within the Evryn product**. Nobody has asked whether the class exists in the **other agentic runtime we operate**. The class, stated so it is checkable: *a typed DB helper that does `select("*")` (or otherwise returns a whole row), whose result is then serialized into an agent's context, bypassing a column-level firewall applied only at some other read path.* **The team-runtime (`evryn-team-runtime`) has agent-facing DB reads and its own agent-visible tables, so the question is live there.** Note ACf already answered the **write**-side analog for that runtime (no raw-write surface); this is the **read** side, which is unanswered. ⚠️ **Do not answer this from inside product context** — the honest reason it is still open is that AC2 was deep in `evryn-backend` and would likely have been wrong; the #lock protocol's own guidance is to send an AC subagent for exactly this, or route it to the lane that holds the other runtime (ACf). *(Recorded 2026-08-04. AC2 told Justin in chat that this had been "routed" **before it was written anywhere** — it existed only in a chat message. Justin caught it. Recorded here now, which is what "routed" was supposed to mean.)*
- ✅ **Two stale AC2-lane branches REAPED** (2026-08-04, Justin cleared it after asking how certain AC2 was). `-d` refused on both, so this took `-D` — the verification that made that safe is recorded here, because the second case is a trap worth recognizing again:
  - **`ac2/step57-runtime-bookkeeping`** (the ③ work, shipped `v0.2.8` in July) — **git's own ancestry settled it: `git rev-list --count main..branch` = 0.** Every commit was already an ancestor of `main`. `-d` declined only because the branch sat 22 ahead of its *upstream* (that guard watches the upstream relationship, not `main`), and those 22 commits are on `main`. Deleting the ref lost nothing reachable.
  - **`dc/cost-haiku-prescreen`** (Step 44, shipped `v0.2.6` in July) — ⚠️ **this one looked like unlanded work TWICE, and was neither time.** First trap: `git branch --merged` says NO and 1 commit is not on `main` by ancestry. Second trap: a per-file content diff shows **460 lines present on the branch and absent from `main`** — which reads as real lost work. It is not. Those 460 lines are the **superseded TWO-verdict draft** of the Haiku screen (`"pass"` / `"escalate"`); `main` carries the **THREE-verdict** version that shipped (`"ignore"` / `"pass"` / `"escalate"`), reshaped after Justin's junk-vs-pass catch because the 2-verdict shape mislabeled junk as `pass`. The lines are unique because they were **rewritten**, not dropped — a deliberately-rejected design. And the local tip was **identical to `origin/dc/cost-haiku-prescreen`**, so the delete could not lose even that draft.
  - **🔑 The generalizable lesson, because "lines unique to the branch" nearly landed AC2 on the wrong answer:** on a superseded branch, a content diff showing unique lines is **expected, not alarming** — a rewrite makes the old version's lines unique by construction. **The question is never "does the branch have lines `main` lacks?" but "are those lines an OLDER DRAFT of something `main` now does differently, or a genuinely absent capability?"** Answer it by *reading a sample of the unique lines* and comparing against what shipped; here the verdict-set difference settled it in one look. A line count alone cannot distinguish the two, in either direction.
  - **Still outstanding — AC0's, since deleting a remote ref is a push op:** `origin/ac2/step57-runtime-bookkeeping` (stale, 22 behind `main`) and `origin/dc/cost-haiku-prescreen` (the rejected 2-verdict draft — low preservation value, but not AC2's ref to delete).
- **A doc-system gap Justin CLOSED, recorded so it is not re-litigated:** promoting QC's review-patterns changed `evryn-quality/CLAUDE.md`, but that repo has no `CHANGELOG.md` and the meta-changelog routing rule only takes entries for changes touching `_evryn-meta` — so a change to QC's operating manual is recorded nowhere. AC2 raised it; **Justin's ruling (2026-08-04): leave it.** It would be ~98% redundant against the manual itself plus git history, and not worth the bookkeeping. **Do not re-raise.**
- **Owed housekeeping, not this lane's:** an archival `pg_dump` (>1 week overdue) · the founding-team current-state ③-shipped one-liner · the Linear ticket update for ③/Step 57.

---

## §9 — Discipline + pointers

- **Full rules:** `_evryn-meta/CLAUDE.md`. **Verify the branch before every edit**, not just every commit — another agent's `git checkout` switches a shared working tree for everyone.
- **QC verifies every real code change**, even small ones — her independent fresh-eyes pass is the point, and "it's tiny" just leaves AC as a single point of failure. **Batch her**: bundle related changes into one trip rather than spinning her per change; but never stack two genuinely big builds on one spin. **Never resume QC for an independence review** — spin fresh. Resume is a continuity tool (a fix-trip to the same DC keeps its build context), not an independence tool.
- **A new interactive / tool-calling pathway is not GO-eligible on stubbed coverage alone.** A review that trips the breaker or stubs the model proves the *plumbing*, not the behavior. Require at least one live-path DEV exercise, or have the reviewer flag the live path UNVERIFIED.
- **🔴 A SCRIPTED mutation check that fails to apply looks EXACTLY like a passing suite — and reports green.** DC hit this: a `perl -0pi` substitution silently didn't match, so no mutation was ever applied, and the suite dutifully reported ALL PASSED for two mutations that had never run. He caught it only because "the guard didn't fire" was *implausible* given they'd gone red the previous trip. **The action:** after any mutation, **prove it applied** — grep for the marker you injected, or confirm the file actually changed — before believing a green result. A mutation check that can't fail is worth less than no mutation check, because it manufactures confidence.
- **🔴 On ANY stale-based branch, `git branch --merged` and `main..branch` will LIE to you about unlanded work.** Live proof from this lane's own housekeeping: `dc/cost-haiku-prescreen` reported as not-merged with a commit "missing" from `main` — and was entirely superseded (merge base 138 commits back; its files present on `main` in *longer, evolved* form; the file-state diff would have reverted ~35,000 lines). **The action, in this order:** (1) do the named files *exist* on `main`, and is `main`'s version newer/longer? (2) how far back is the merge base? (3) what does the *file-state* diff actually say it would do? Only after those three does "this work never landed" mean anything. This is CLAUDE.md's file-state-vs-merge-base rule, and it fires far more often than it feels like it should.
- **Spin DC / QC / Mira per `_evryn-meta/docs/protocols/ac-orchestration-protocol.md`** — the six-part tagged brief pasted **verbatim**, only the codified substitutions, the load assembled by you with per-file line spans, and the two-part receipts verified on return. Identity-file trips also run `evryn-team-workspace/shared/protocols/identity-file-review.md`.
- **Identity files are Mira's** — coordinate, never fold an identity edit in quietly. And they are **runtime**: an instruction there programs Evryn's behavior as surely as a line of code.
- **AC conducts; it does not write the deterministic code itself.** Even small fixes go to DC — AC reviewing its own code is not an independent check.
- **Read `_evryn-meta/docs/protocols/ac-writing-protocol.md` before writing to any doc**, including this one.
- **Three practical traps that each cost real time this session — none of them are bugs:**
  - **A fresh worktree has no `node_modules` and no `.env`.** `npm install` in it, or `npx tsc`/`npx tsx` fail with a confusing "not the tsc command you are looking for." And `.env` is gitignored so it does **not** travel — about five test files then die at *module load* with `Missing required environment variable: EVRYN_ENV` (config's ADR-048 boot-fail gate doing its job). **That is not a test failure.** For pure-composition tests, supply dummy values inline (`EVRYN_ENV=dev` plus dummy Gmail/Supabase/Slack/Anthropic vars) — they reach no DB. Do NOT copy a `.env` in from `evryn-backend`; it may be prod-pointed.
  - **`npx tsc --noEmit` does NOT typecheck `tests/`** — `tsconfig.json` scopes `include` to `src/**/*`. A green typecheck says nothing about your test file. Typecheck a test separately if you care, and expect a benign `TS1375` (top-level `await` with no imports/exports) — that is the repo's standalone-test shape, present on the existing tests too, not something your file introduced.
  - **When writing a multi-line commit message or a file via a heredoc, mind the two shells.** A message containing double quotes breaks a `-m "…"` invocation — use `git commit -F -` with a quoted heredoc. And a path written git-bash-style (`/tmp/x`) is read by **Node** as `C:\tmp\x` and fails ENOENT; in Node always use the `C:/...` form (the same rule as the Slack webhook).
- **Deploy method + the ship ledger:** `_evryn-meta/docs/deploy-log.md`. A migration needs dev-first plus pre/post `pg_dump` (`evryn-backend/backups/README.md` — DB URLs by name, never echoed).
- **Report to AC0** for merge gating, cross-lane collisions, and the Gate-B sequence — via §10 of this file, not a standalone doc (Justin, 2026-08-04: AC0-facing notes live in the brief so continuity is maintained in one place).

---

## §10 — FOR AC0: what this lane still owes you, what you already closed, and the procedures attestation

> **Written 2026-08-04 by AC2, updated after the merge landed.** For a cold AC0 — you have not been in AC2's session and nothing here assumes you were. **The merge request this section originally carried is DISCHARGED** (you merged both halves, `acf086d` + `df01e9c`), so what remains is short. A standalone routing doc was written and then retired into this section on Justin's instruction — do not go looking for it.

### 10.1 — ⭐ What is still open, and all of it is yours

1. **PUSHES.** AC2's commits are local-only: `evryn-backend` main **+1** (the 35b/35d CHANGELOG entry, `5bbebc5`) · `evryn-quality` main **+1** (QC's promoted review-patterns, `735d63f`) · plus part of `_evryn-meta` main's **+10**. Nothing is at risk from a stray checkout any more — it is all committed — but it is **machine-only until pushed**, which is the failure class the all-branches batten-down check exists to catch.
2. **Two stale REMOTE refs** — `origin/ac2/step57-runtime-bookkeeping` (stale, 22 behind `main`) and `origin/dc/cost-haiku-prescreen` (a deliberately-rejected 2-verdict draft of the Haiku screen). **The local branches are already reaped**; deleting a remote ref is a push op, so these two are yours. **§8 records the verification that made the local deletes safe**, including why the second branch looked like unlanded work twice over and was neither time — worth a read before you delete its remote, since that ref is currently the only copy of that draft.
3. **🔴 ROUTE THE CROSS-RUNTIME QUESTION on Step 97 — it is open and it is yours to place.** Your sweep bounded the containment class **inside the Evryn product**; nobody has checked whether the same class exists in the **team-runtime's read path** (ACf answered the *write*-side analog there; the read side is unanswered). **§8 states the class in checkable form** — take it from there. It should ride Step 97's lane or go to ACf; it should **not** be answered from inside product context. *(AC2 told Justin this was "routed" while it existed only in a chat message. It is now written down; placing it is the part still outstanding.)*
4. **The deploy** — nothing from this lane is deployed, by design. It all rides the ONE staging-first bundle (§1). Not AC2's to trigger.

### 10.2 — What you already closed (recorded so a fresh reader doesn't re-open it)

All three items Justin routed you on the 2026-08-04 ballot are **done** — and on one you improved the finding:

1. **SPRINT Step 35 → DONE**, carrying both SHAs, and you **rewrote its stale inline text** so the rejected "mirror `find_user_by_name`'s `is_stub`" framing is gone.
2. **The named Gate-B efficacy check is in Step 88B, with an owner.** That is what makes the *"a prompt-wording fix's efficacy is untestable"* discipline institutional rather than a note living in one instance's head.
3. **Step 97 minted** for the `create_user` containment hole with its own dedicated lane — **and you bounded the class**, sweeping every other tool that could return a user row and confirming `create_user` is the only exposed door. That is a genuine improvement on AC2's finding, and it bounds the fix. You also correctly recorded AC2's *"not live in prod today"* as an **inherited, uncensused claim** rather than adopting it as fact; §8 now carries that caveat so the owning lane either censuses it or keeps it.


### 10.3 — Procedures attestation (per the orchestration protocol's receipts-up-the-chain)

**All procedures followed.** Specifically:

- **Every subagent was spun on the six-part tagged brief, pasted verbatim**, with only the codified substitutions — DC/QC on the base skeleton, Mira on the four-substitution team variant. No wording invented, no block condensed.
- **Loads were assembled by AC2, not delegated to the agent** — grep-the-cascade on each target's own load-defining files, then an explicit per-file list with line spans. Full product runtime, **both halves**, no `#cascade-override` anywhere. Runtime enumerated **live** at spin time: **44** `src/*.ts` and **11** `identity/*.md` (several docs still say 43 — do not trust a doc's count).
- **Every spin was model-pinned** (Opus). **Fresh QC** — never resumed, because independence is the point. DC and Mira *were* resumed by `agentId` for fix-trips, which is the sanctioned continuity use.
- **Worktree isolation held** — one branch ↔ one worktree, per agent; no agent touched `main`, merged, pushed, or deployed. Commits staged by explicit path only.
- **Diff-first before every shared-file commit**, per the multi-agent rule. AC0: your `docs/sessions/historical/2026.08/2026.08.04-ac0-ac0-handoff.md` and (earlier) `current-state.md` were dirty in `_evryn-meta` throughout — **left strictly alone**; AC2 committed only its own files.

**⚠️ The limit of this attestation, stated precisely.** AC2 verified all three agents' receipts against the lists it gave them: every listed file appears in part 1, every `(full — N lines)` span reaches the end, every canary confirmed. **But AC2 could NOT observe any of their call-streams** — a spinner receives only final output — **so the call counts are UNRECONCILED for all three.** Rather than let a list-only check read as a full one, AC2 substituted independent artifact verification: re-running the tests and typecheck itself, reading the diffs itself, and re-confirming at source the load-bearing claims (that `core.md` is force-loaded on every person-record pathway; that the two predicates genuinely diverged; that the containment hole is real). Treat the *artifacts* as verified by AC2 directly, and the *call counts* as unverified.

**Deviations, all disclosed and none suppressed:**
- **AC2's own omission:** `shared/protocols/claude-code-ops.md` was missing from Mira's load list (her team cascade's item 8). **She caught it via the reconcile step, loaded it, and reported it** — the reconcile working exactly as designed. It changed nothing in her output. Let stand; the omission was AC2's.
- **DC disclosed a false-green in his own testing** and did not hide it: a scripted `perl` mutation run reported *all tests passing* for two mutations, because the substitutions silently failed to match and no mutation ever applied. He judged "guard didn't fire" implausible, confirmed the file was unmodified, and re-ran both with the `Edit` tool — 4 red and 15 red, markers grepped to prove application. **Worth knowing as a general trap:** a scripted mutation that fails to apply looks identical to a passing suite.
- **DC self-corrected an unmeasured claim** — he had reported "65 checks" as an estimate; the real figure at that commit was 61 (122 after the second fix-trip).
- **Eight partial or errored reads across the three agents were disclosed and paged to completion** rather than absorbed behind clean retries.
- **A stale count in this brief, which then went stale twice more:** `classify.ts` was 2,974 lines when §5 was first written, 3,122 after the fix-trips, and **3,186** after the merge; `src/` went 44 → 45. QC flagged the first drift. This is why §5's standing instruction is to re-count at spin time and trust **no** doc's number — including this brief's.

**Nothing was absorbed silently.** If any of the above reads as insufficient justification to you, the re-run is yours to demand — AC2 would rather re-run than have a GO rest on an unreconciled load.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
