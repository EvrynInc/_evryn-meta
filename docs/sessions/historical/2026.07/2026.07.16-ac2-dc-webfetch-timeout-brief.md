# Build brief (DC): wall-clock timeout on `runEvrynQuery` — the hung-WebFetch / wedged-poll-loop fix

> **Truncation check:** last line should read `FULL FILE LOADED`.
>
> **Author:** AC2 (cost lane), 2026-07-16. **Scope owner:** AC2. **Reviewers:** QC (live-path check), AC0 (co-sign/sequence), Justin (merge/deploy go). **Priority:** pre-Mark hardening (Justin-ratified 2026-07-16 — elevated from the a12 Gate-A finding). **Origin:** AC0's fast-follow round in `docs/working/2026.06.30-ac0-ac2-cost-lane-brief.md` (2026-07-16).

## 1. The problem (verified against the runtime)

`runEvrynQuery` (`src/triage/classify.ts`) drives Evryn's triage via `for await (const message of query({...}))`. Its options carry `maxTurns: 20` but **NO wall-clock / abort timeout.** The built-in `WebFetch`/`WebSearch` tools are gated only by the shape-based `web-guard` PreToolUse hook (allow/deny — no timeout). So a **WebFetch to a slow/unresponsive host that never returns** blocks the `for await` loop **forever**.

Because the poll pipeline is **single-threaded and fully awaited** — `startPolling → pollOnce → planPollBatch → handle(email) → handleNewEmail → processEmail → runForwardTriage → runEvrynQuery` — a single hung fetch **wedges the entire poll loop.** No new mail is processed. And the existing silent-death guards do NOT catch it:
- The **polling-dead escalation** (`safety/polling-dead.ts`) keys on `consecutiveErrors` — a HANG throws no error, so it never increments, so that escalation never fires.
- The **Healthchecks heartbeat** is pinged only *after a successful `pollOnce`* — a wedged loop stops pinging → Healthchecks alarms after ~15 min → **manual Railway restart** is the only recovery.

**Real Mark-live risk:** Evryn web-researches essentially every real sender at triage. One dead/slow domain in one forward wedges the whole pipeline until a human restarts it. (Observed live: the Gate-A a12 fixture hung `runEvrynQuery` ~27 min on a fetch before AC2 killed it.)

## 2. The fix — an AbortController wall-clock cap on `runEvrynQuery`

Add a wall-clock budget to `runEvrynQuery`'s `query()` call via the SDK's **`abortController`** option (confirmed supported: `@anthropic-ai/claude-agent-sdk/sdk.d.ts` — `abortController?: AbortController` in the query Options). On expiry, **abort the query** so the underlying Claude Code subprocess + the hung fetch are **terminated** (not just ignored), the `query()` iterator rejects, and control returns to `runEvrynQuery`'s existing `catch` → the item follows the **normal error/retry path** instead of wedging the loop.

**Why AbortController, NOT a bare `Promise.race`/`withTimeout` (the refinement over "mirror the Haiku screen"):** the Haiku screen's `withTimeout` (`haiku-screen.ts`) uses `Promise.race`, which on timeout *ignores* the still-pending `query()` — leaving the underlying subprocess running. That's fine for the Haiku screen (`maxTurns:1`, no tools, finishes fast). For `runEvrynQuery` (20 turns, tools, web fetches) a `Promise.race` would leave a **hung Claude Code subprocess zombie per timeout** — a resource leak that compounds. `abortController.abort()` actually kills it. So: use the AbortController, not `Promise.race`.

### Shape (illustrative — DC owns the exact code)
```ts
const ac = new AbortController();
const timer = setTimeout(() => ac.abort(), QUERY_TIMEOUT_MS);
try {
  for await (const message of query({ prompt, options: { /* …existing… */, abortController: ac } })) {
    // …unchanged…
  }
  // …existing usage capture…
} catch (error) {
  // …existing classifyQueryError handling… (see §3 — verify the abort branch)
} finally {
  clearTimeout(timer);
}
```

### The timeout budget
- **Env-tunable:** `EVRYN_QUERY_TIMEOUT_MS` (parse in `config.ts`, mirror the existing `parse*` helpers; default to a generous few minutes). **Recommended default: 180000 (3 min)** — comfortably above a legit multi-turn Opus triage with several web searches (the Gate-A gold/edge fixtures each completed in ~1–2 min with real WebSearch/WebFetch), and far below "wedged forever." **Confirm the default with AC2/AC0 before shipping** (too tight = false-aborting a legit deep triage; too loose = a longer per-item stall on a hang). It applies to the whole `runEvrynQuery`, so it also caps the rare legit slow triage — that's acceptable (an aborted item errors + retries; a legit triage that genuinely needs >3 min is vanishingly rare and better retried than allowed to wedge).
- This is a **whole-query** cap, deliberately — a per-`WebFetch` timeout is NOT cleanly achievable (the built-in WebFetch tool exposes no timeout via the SDK/hook; the PreToolUse hook can only allow/deny, not time-bound a tool). The whole-query cap catches ANY hang (fetch or otherwise) and is the achievable, robust fix. (Note this in the ADR — it's the "which is achievable" answer AC0 asked for.)

## 3. Interactions to get right (DC must verify each)

1. **The abort error must classify to the NORMAL ERROR/RETRY path, NOT a breaker halt.** `runEvrynQuery`'s `catch` runs `classifyQueryError(error)` (`src/safety/errors.ts` — **read it**). An abort error (AbortController → typically an `AbortError`/`DOMException`, or an SDK abort rejection) is neither a billing nor an auth error, so it *should* fall to the `"rethrow"` action → re-throws → `processForward`/`processDirect`'s catch stamps the item `'error'` → returns `"completed"` → **poll loop continues** (exactly the goal). **Verify** `classifyQueryError` doesn't mis-bucket the abort. If it does (or to make it explicit + observable), add a dedicated timeout branch that: (a) fires ONE `notifyDev` (`"runEvrynQuery aborted after {ms}ms — likely a hung web fetch on {pathway}/{item}; item errored, will retry"`) so a recurring hang pattern is VISIBLE, and (b) re-throws so the item errors + retries. Do **NOT** route a timeout into `haltForBreaker` — a hang is a slow external dependency, not a runaway; it must not trip the M1 breaker or park like a halt.
2. **M1 breaker gate is untouched.** The breaker check is at the TOP of `runEvrynQuery`, *before* `query()`; the timeout wraps the `query()` consumption only. A tripped breaker still short-circuits first (zero spend); the timeout only governs a query that actually runs. No change to the halt/park semantics.
3. **`llm_usage` capture:** on an abort, the `result` message carrying usage likely never arrives → `capturedUsage` stays undefined → the existing best-effort `recordLlmUsage` writes a row with null token fields (or is skipped) — same as the existing mid-stream-throw limitation (QC NB1). Fine; no new work.
4. **The `finally { clearTimeout(timer) }`** must run on every path (success, error, abort) so a completed query doesn't leave a dangling timer that aborts nothing.
5. **All other `query()` call sites:** `runEvrynQuery` is the money-path one that wedges the poll loop — that's the target. The **Haiku screen** already has its own 10s `withTimeout` (leave it). The **Reflection consolidation** and any cron pathway also go through `runEvrynQuery` (they call it), so they inherit the cap for free — good. No other raw `query()` call needs this (grep to confirm: only `classify.ts runEvrynQuery` + `haiku-screen.ts defaultRunQuery` call `query()` directly).

## 4. Test / QC (interactive-pathway → the process lesson applies)

- **Unit:** the timeout budget parse (`config.ts`) + (if added) the pure timeout-classification branch. DB-free.
- **🔴 Live-path DEV exercise (the GO bar — stubbed tests only prove plumbing):** drive `runEvrynQuery` against a **genuinely hanging fetch** and confirm it **aborts → errors → the poll loop proceeds** (does NOT wedge). Cheapest reliable repro: point a fixture's research at a **non-routable/blackhole host** (e.g. a `10.255.255.1`-style address that accepts-but-never-responds, or a deliberately slow endpoint) so the fetch hangs, set `EVRYN_QUERY_TIMEOUT_MS` low (e.g. 15000) for the test, and assert: the query aborts at the budget, the item lands `error` (not stuck `processing`), a subsequent `processEmail` runs normally, and `tasklist` shows no lingering zombie node subprocess after the abort (the AbortController kill worked). Run on **DEV** (`EVRYN_ENV=dev`), breaker off, real model. *(NB: the web-egress guard denies internal/SSRF targets — pick a hang-repro that the guard ALLOWS but that never responds, or temporarily target a slow public endpoint; DC picks a clean repro.)*
- **QC:** fresh, full-load, heightened — verify the abort path, the no-breaker-trip, the no-zombie, and the live-path result. This is a money-path/interactive change.

## 5. Scope / sequencing (AC2 recommendation — Justin decides)

**Recommend: FAST-FOLLOW, stacked as a SEPARATE commit on the ③ branch (`ac2/step57-runtime-bookkeeping`, after tip `d9306a8`), separately QC'd, riding ③'s one-window bundle deploy** — NOT folded into ③'s bookkeeping diff. Rationale: ③ is QC-GO'd + DEV-green + at the Gate-A co-sign gate; the timeout is **logically independent** of the bookkeeping rewrite (a wall-clock wrapper around the query loop, ~15 lines), so it QCs cleanly on its own and keeps ③'s money-path QC frozen at the gate; it's an interactive-pathway change needing its own live-path QC regardless; and stacking on ③'s branch (vs a parallel branch) avoids a `classify.ts` merge conflict while still landing before Mark in the same deploy. (Fold-into-③ is the alternative only if Justin/AC0 prefer one branch — same fix, into ③'s `classify.ts` — but that re-opens ③'s QC on the money path at the worst moment.) **ADR:** small — the whole-query-cap decision + the "no per-fetch timeout achievable" finding + the M1-independence. Coordinate the deploy window with AC0 (rides the ③ bundle: ③ + 54a/54c + key-split + `_PROD` cleanup).

## 6. Files (the load-bearing set for this fix)
- **Edit:** `src/triage/classify.ts` (`runEvrynQuery` — the AbortController + timer + option + the catch/timeout branch) · `src/config.ts` (the `EVRYN_QUERY_TIMEOUT_MS` parse + config field).
- **Read to verify:** `src/safety/errors.ts` (`classifyQueryError` — the abort-classification path) · `src/triage/haiku-screen.ts` (the `withTimeout` pattern being deliberately NOT copied, + why) · `src/safety/polling-dead.ts` + `src/safety/heartbeat.ts` (why the hang evades the existing guards — the problem statement) · `src/email/poll.ts` (`pollOnce`/`planPollBatch`/`handleNewEmail` — the single-threaded awaited chain that wedges) · `src/email/process.ts` (`processForward`/`runForwardTriage` catch → item `error`).

---

## 7. BUILD + REVIEW RESULT (AC2, 2026-07-16) — DC built it; QC = conditional GO, zero blockers

**Status: BUILT + QC-reviewed. Commit `bf23bca` on `dc/webfetch-timeout` (worktree `evryn-backend-webfetch`), stacked on the ③ tip `d9306a8`. +334/−0 across `src/config.ts`, `src/triage/classify.ts`, `tests/test-query-timeout.ts`. Typecheck green; 16/16 new DB-free tests; breaker/clustering/gatekeeper regressions pass. NOT merged.**

**What DC built (and did right):** AbortController + a timer → `abort()` at the budget, passed into `query()`'s `abortController` option; `finally { clearTimeout }` on every path (incl. the billing-halt `return`); controller created AFTER the M1 gates (a halted query never arms a timer); `EVRYN_QUERY_TIMEOUT_MS` via a pure `parseQueryTimeoutMs` (default 180000, `Number` not `parseInt` — `parseInt("30s")`→30ms). **He verified empirically instead of inferring**, which caught a trap: the abort's `.name` is `"Error"` NOT `"AbortError"` (minified constructor), so the obvious `error.name === "AbortError"` sniff would have **silently never fired** — he used a local `timedOut` flag. He also added an unrequested refinement: gating the timeout branch on `action === "rethrow"` so `classifyQueryError` wins (billing→halt, auth→alert keep exact M1 semantics if the timer fires in the same instant). Correct — a missed cost-halt beats a missed timeout alert.

**QC's verdict: GO, CONDITIONAL on AC2's live-path DEV exercise** (the abort→error→loop-proceeds→no-zombie chain is not yet live-verified — the §4 GO bar). **Zero blockers.** She verified the three hinges **from SDK source, not DC's word**: the abort genuinely `throw`s (`sdk.mjs`: `if(this.exitError) throw this.exitError`, set on `process.on("exit")` when `signal.aborted`) so the feared silent `result=""` path is **structurally impossible**; `abort()` does `SIGTERM`→SIGKILL the subprocess (**no zombie** — the AbortController-over-`Promise.race` call is justified); `classifyQueryError`→`"rethrow"` confirmed. M1 intact; scope clean (zero Step-57/identity/safety files).

### The findings — routing

- **NB-1 — `parseQueryTimeoutMs` has no UPPER bound → Node coerces a `setTimeout` delay >2147483647 to 1ms. `src/config.ts:292–299`. → FAST-FOLLOW (1 line).** `Number.isInteger(2147483648)` is true and >0 → honored → QC reproduced it firing in **10ms**. An operator setting a huge value to mean *"effectively no cap"* gets **every query aborting instantly** (every item `error`; Reflection/follow-ups/proactive/operator-chat all dead) while `#dev-alerts` floods with self-contradictory *"ABORTED after 2147483648ms"* lines that misdirect the diagnosis. **`Infinity` fails SAFE; this does not.** Fix: add `|| parsed > 2_147_483_647` to the guard (or a `MAX_QUERY_TIMEOUT_MS = 600_000`), + a `"2147483648"` test case beside the existing `"Infinity"` one.
- **NB-2 — 🔴 THE IMPORTANT ONE. The cap contradicts a load-bearing identity promise AND ratchets. `src/reflection/consolidate.ts:249` + `src/email/poll.ts:1278–1321` + `identity/activities/reflection.md:20`. → FAST-FOLLOW; MUST land before Mark's Reflection enrolls. Not a blocker for this merge.** `reflection.md:20` says verbatim: *"**Soft length — never a hard cap.** … let it be as long as it needs, and never truncate a real one to hit a number. (If a story comes out many times longer than that, the system flags it for a look — **but it never cuts it**.)"* The 3-min cap is now a de-facto hard cap on that generation — and worse than truncation: **on timeout NOTHING is written**. The notes are preserved → they keep growing → next hour's attempt is longer → times out again → **burns a full Opus call on a 24k+ char prompt hourly, forever. A RATCHET, not a transient.** Severity held down only because `findCareEligibleUsers` enrolls `status='active'` only (Mark isn't live), it needs 24k+ chars to trigger, and it alerts hourly rather than failing silently.
  - **🔴 This was MY spec miss, not DC's** — §3.5 of this brief says Reflection "inherits the cap for free — good." I reasoned about Reflection as a *call site* without reading what Reflection *promises*. DC built exactly to spec. (The identity-files-are-runtime lesson, landing on AC.)
  - **QC's recommended fix (I concur):** give `runEvrynQuery` an optional per-call **`timeoutMsOverride`** (defaulting to `config.queryTimeoutMs`) and let the Reflection pathway pass a larger budget. Reflection is a known-long, non-interactive, once-per-user-per-hour batch job — unlike the latency-sensitive money path 180s was sized for. **Do NOT just raise the global default** — that weakens the wedge protection on the actual poll loop (the whole point). *(Sibling, no ratchet, no action: `onboarding.md`'s multi-fetch research is under the same cap but is rare, supervised, and retries cleanly.)*
- **NB-3 — EPIPE is now ROUTINE, into the one channel M1 depends on. `src/index.ts:134–139` + the SDK stdin wiring. → NOTE + MEASURE; don't block.** QC confirmed DC's survival claim from source (the SDK never attaches an `'error'` listener to stdin; EPIPE is async → `uncaughtException` → `index.ts:134` alerts and does NOT exit). But this change makes aborts routine, converting a never-seen edge into a recurring *"UNCAUGHT EXCEPTION — write EPIPE. Process may be unstable."* ping — **training Justin to skim past "UNCAUGHT EXCEPTION" is a real cost against M1's whole purpose.** `haiku-screen.ts:373–384` already solves this exact shape (its late-rejection no-op `.catch`) and is the right pattern to borrow. **Action: have the live-path exercise COUNT EPIPEs across N aborts** rather than carry DC's "5/5 clean"; if it recurs, consume it at the seam we control, or allow-list `error.code === 'EPIPE'` **only while an abort is in flight** (so a genuine EPIPE elsewhere still alerts).

**Nits:** **N-1** `slack.ts:967–973` surfaces the raw *"Sorry, I hit an error processing that: Claude Code process aborted by user"* to Justin on an operator-pathway timeout (opaque, not silent — the #dev-alerts ping explains it). **N-2** `EVRYN_QUERY_TIMEOUT_MS` is **absent from the operator-guide env table** (AC-owned; matters more given NB-1/NB-2). **N-3** the operator-guide has **no bottom truncation canary**. **N-4 (out of scope → AC0/SPRINT)** `package.json` pins `^0.2.119` while ARCH mandates exact SDK pins; DC's local-flag approach is robust to a bump, but the `action === "rethrow"` gate does depend on the abort message never matching a billing/auth substring.

### Two OPEN spec items — AC2's, not DC's
1. **§2: "Confirm the default (180000) with AC2/AC0 before shipping."** Still open — and **NB-2 is the reason to think about it** (the answer is likely the per-call override, not a bigger global).
2. **§5: the ADR** — **not written.** AC's deliverable (the whole-query-cap decision + the "no per-fetch timeout achievable" finding + the M1-independence + the NB-2 override). The rationale is already fully captured in the code comments, so it's a transcription job.

### QC patterns surfaced — promote to `evryn-quality/CLAUDE.md` "Patterns This Role Watches For" (needs Justin's approval — source-of-truth edit)
- **Pattern A (the strong one) — *a cap added to a shared chokepoint, sized against one pathway.*** **Trigger:** a change adding a resource CAP (wall-clock, token, size, retry) to a shared chokepoint many pathways funnel through, justified against ONE pathway's measured profile. **Failure:** a different caller has a materially different profile, and **an identity file may promise the un-capped behavior** ("as long as it needs," "never truncated") — so the cap silently contradicts a load-bearing instruction; on a batch/retry path it can **ratchet** (cap trips → input preserved → grows → next attempt likelier to trip → never heals, burning spend each cycle). **Move:** grep EVERY caller; for each ask (1) is this pathway's profile like the one the cap was sized against? (2) does an identity file *promise* un-capped behavior? then **"if the cap trips here, is the next attempt easier or harder?"** — harder = a ratchet. **Not:** a leaf function with one caller. **Seen 1×:** this trip.
- **Pattern B — *an env-parsed number with no upper bound.*** **Trigger:** an env-parsed numeric tunable feeding a platform API with a documented valid range, whose parser handles junk/unset/`Infinity` but bounds only the low end. **Failure:** a finite-but-out-of-range value passes `Number.isInteger` + `>0` and the platform **silently coerces** it (Node clamps a `setTimeout` delay >2147483647 to 1ms), **inverting operator intent** — "no cap" becomes "abort everything"; fails *worse* than `Infinity`, which falls back safely. **Move:** look up the consuming API's valid range, bound BOTH ends, then ask *"what would an operator type to mean disable this?"* and trace it through the coercion (`setTimeout` = `[1, 2147483647]`). **Not:** a tunable whose consumer clamps internally. **Seen 1×:** this trip.

### ➕ AC0's independent evidence (2026-07-16) — corroborates the diagnosis and sharpens the severity

- **`bf23bca` had NEVER been pushed. AC0 pushed it — it is now durable on origin.** Justin **RATIFIED fast-follow** as the scope. It still needs its live-path QC before it rides the ③ bundle.
- **🔴 a12 is STILL FROZEN at `processing` on DEV — the hang preserved in amber (`triage_result=edge` recorded, no draft, no terminal transition).** It proves something the brief above only inferred: **the B2c backstop (`ensureItemTransitioned`) CANNOT catch this class** — B2c only runs *after* `runEvrynQuery` RETURNS, and a hang never returns. So the ONLY thing that would ever notice is the 30-min stuck-`processing` stale-check — **while the poll loop stays wedged that entire time.** That is the real severity, and it's exactly why a whole-query wall-clock cap is the right shape (a per-item backstop structurally cannot help).
- **a12 has NO `llm_usage` row** — 13 items, 12 spend rows ($5.35 total). Usage is captured only after the query loop completes, so a hang means the spend is never recorded. The missing row lines up exactly with the hung fixture (a second, independent fingerprint).
- **🔴 THE CULPRIT IS NOT PROVEN — and that ARGUES FOR this fix, not against it (AC0's point; carry it).** The diagnosis is *"most likely"* a hanging WebFetch. But a12's own recorded reasoning shows the domain fetch failed **FAST** (`ECONNREFUSED`) — so the hang was a **later** call, and we do NOT know which. **The AbortController whole-query cap is robust to not knowing which call hung, because it caps the whole query.** Do NOT let anyone (including a future you) push toward a narrower per-fetch fix on the theory that we should first pin the exact culprit — that reasoning is backwards.
- **`evryn-backend` `main` is now `11fa240`** (Step-78 Half A + a sprint mark landed) — the ③ catch-up rebase target moved.

### Next actions (for whoever picks this up)
1. **Fix-trip — NOT DONE (AC2 ran out of context 2026-07-16 before starting it; Justin's call to defer, nothing lost).** Two fixes: **NB-1** (the upper bound — 1 line + a `"2147483648"` test case) + **NB-2** (the per-call `timeoutMsOverride`, Reflection passes a larger budget — **NOT a bigger global default**). **DC resume handle: `a5795dc3cd7c474bc`** — *try it first* (he keeps his build context + the loaded runtime, so it's cheap); **if the handle is dead/expired, spin a FRESH DC** with the verbatim protocol brief + this doc as the spec (his load list is §6 above + the cascade). Then a **FRESH** QC re-verify — **never resume QC for a re-review** (independence). *(The QC who did this review is `a2cbedc2e5c65ab7f` — do NOT resume her for the re-verify; she carries her prior framing.)*
2. **AC2:** the live-path DEV hang exercise (the conditional-GO gate) — **and have it COUNT EPIPEs** (NB-3). Write the ADR (§5). Close the default question (§2) — likely via NB-2's override. Add `EVRYN_QUERY_TIMEOUT_MS` to the operator-guide env table (N-2).
3. **Justin:** approve promoting Patterns A + B into `evryn-quality/CLAUDE.md`.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
