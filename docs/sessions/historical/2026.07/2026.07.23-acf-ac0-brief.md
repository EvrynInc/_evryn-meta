# ACf → AC0 — brief (2026-07-23)

> **Truncation check:** the last line should read `FULL FILE LOADED`.
>
> **How to use this file:** a short hand-off from ACf (team-runtime memory lane) to AC0 (product conductor), relayed by Justin. The load-bearing item is #1 — it closes an owed item on your ③ #lock. #2–#3 are lane awareness, no action needed.

---

## 1. ✅ Cross-runtime raw-write-surface check — CLOSED, no propagation needed (this is the ③ #lock item routed to ACf)

Your ③ (Step 57) #lock listed **"cross-runtime raw-write-surface check (→ ACf/Soren)"** as owed. Done — here's the answer so you can close it:

- **The class ③ closed:** an agent holding a raw/arbitrary DB-write tool (product removed `supabase_upsert`, replaced with the scoped operator-only `correct_user_field`).
- **Team-runtime does NOT have this class.** Verified against the live tool surface (`evryn-team-runtime/src/tools/tools.ts` + `src/wakes/tool-gate.ts`): the entire agent write surface is **4 narrow writes**, each fixed-table + fixed-schema + own-agent-scoped + `tool:<name>` capability-gated (fail-closed): `wakeup_set` (own wakeups), `scratchpad_write` (own thread scratchpad, thread-kind-gated), `propose_work` (creates a `needs_authorization` proposal — Justin gates), `append_note` (own MEMORY.md, canary-guarded). **No tool takes a model-supplied table name or arbitrary columns — there is no `supabase_upsert` analog to remove.** It was built right from the start (ARCHITECTURE invariant 3: proposal-gated side effects, hook-enforced).
- **No `correct_user_field` equivalent is needed** — the team-runtime has no "user records" to correct (append-only message ledger + agent memory + thread registry).

**→ You can mark the ③ #lock's cross-runtime raw-write item CLOSED: no propagation needed.**

**Two adjacent notes (neither changes the answer, neither is yours to action unless you want to):**
- **L6** (already in the team-runtime tracker, P3): the *stdio interactive* surface is ungated — a **different class** (ungated interactive surface, not a raw-write tool), lower-stakes (blast radius = Justin's own machine). Tracked in my lane; no cross-runtime action.
- **Dashboard** writes `agent_state` directly (the **D1** hand-mirror-of-`kill.ts` finding) — that's an **operator surface**, not an agent tool, and already tracked as D1. If you want the *dashboard* half of the raw-write sweep checked (it's AC0/AC3's domain, not mine), that's your call — flagging so it's visible.

## 2. Team-runtime lane status (FYI — no collision with product)

- Memory Phase-1 is **3 items merged + dormant + undeployed**: (a) scratchpad+headline · L9 boot-guard · **(b) shared-digest re-key** (`567056a`). **Item (c)** (full-thread-load + composed-load advisory + per-agent serialization) is **building now** (ACf1).
- **No product-lane collision:** separate repo (`evryn-team-runtime`), separate DB ("Evryn Team"), runtime dormant/boot-paused. Nothing here touches `evryn-backend` / prod.
- **Deploy deferred by plan:** batch the Phase-1 migrations (015+016+…) into ONE migrate-then-deploy window at a checkpoint, then the Lucas paces. Nothing to deploy now.

## 3. Nothing else needed from you

Pushes are handled (my #lock + CLAUDE.md commits are already on origin — thanks). This brief is awareness + the one closed #lock item. — **ACf**

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
