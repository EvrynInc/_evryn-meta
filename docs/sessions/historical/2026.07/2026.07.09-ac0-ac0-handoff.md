# Handoff — AC0 → next AC0 — 2026-07-09 (AC6 docs landed on main; AC7 spun; wave = AC2/AC3/AC7)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** START HERE if you're a fresh AC0. You are the **conductor** of a multi-lane v0.2-hardening wave — you orchestrate, gate merges/deploys, reconcile cross-lane collisions, and keep housekeeping current; you do NOT write runtime code. This handoff is **self-sufficient** — it summarizes every lane's current state; you do NOT need to pre-read the lane briefs to boot (read a lane's brief in full, on-demand, when that lane surfaces work). Companions: `docs/current-state.md`, `_evryn-meta/CHANGELOG.md`, `docs/deploy-log.md`, the lane briefs in `docs/working/`.

---

## 🚀 First moves (fresh AC0 — do these on boot)
1. **Load the Light cascade** (`CLAUDE.md` auto-loads → `docs/current-state.md` → Hub `roadmap.md`). Load the **full `src/` runtime only if Justin explicitly calls the full cascade** (then ALL of it — enumerate live: `find evryn-backend/src -name '*.ts'`; never trust a hard-coded count). **⭐ NEW as of 2026-07-09:** `evryn-backend/docs/ARCHITECTURE.md` + `BUILD-EVRYN-MVP.md` are now **reconciled + accurate vs the runtime** (AC6's lane, merged to main). They're a **trustworthy on-ramp now** — BUT ⚠️ **that is NOT license to skip reading `src/`.** Justin was emphatic: knowing both what we *intend* AND what the runtime *actually is*, at a granular level, stays load-bearing; skipping the runtime "because the docs are accurate now" has almost always burned us. The docs are a better on-ramp, not a substitute.
2. **Remake your ping helper** (see ⚡ below) and **ping `#team-alerts` "I'm up"** (`AC0:` prefix).
3. **Orient, don't chase:** nothing is hot-blocked on YOU at handoff time. Live waits are on the lanes' builds + Justin's gates. Your job is to be ready to gate + answer + keep housekeeping current.
4. **Then watch for** (your standing owed-list — §"AC0 owed" below): AC2 reaching **Runtime Bookkeeping** → run **Gate-A** with them BEFORE it merges (SPRINT Step 88); **flag AC3 the moment the full Clustering bundle merges** (un-holds its 54a/54c); orchestrate **Gate-B** (pre-Mark live-fire on staging).

---

## ⚡ Operating model (the load-bearing bits — carry these)
- **You conduct; you do NOT write runtime code.** Each lane's DC/QC builds; you gate merges/deploys, reconcile cross-lane collisions + sequencing, keep Justin at CEO-altitude, keep housekeeping current (current-state, CHANGELOG, deploy-log, sprint DONE-marks).
- **PING `#team-alerts` EVERY response**, `AC0:` prefix — even when Justin seems present (he reinforced this repeatedly). Helper (remake it — mine won't be at your path): a Node script that POSTs `{text}` to `SLACK_TEAM_WEBHOOK_URL` — read that var's LINE from `C:/Users/Justin/Evryn/Code/evryn-team-workspace/.env`. In Node use the `C:/Users/...` path form (NOT git-bash `/c/...`).
- **Each lane's brief IS the AC↔AC0 mailbox** (`docs/working/`). Append replies as a `## ═══ AC0 → ACn · date ═══` banner *before* the canary; **commit + push immediately** (brief/mailbox commits are pre-authorized). No live AC-to-AC channel — coordinate through Justin or committed briefs. (The briefs have CRLF churn that fights the Edit tool — I've been appending via a small Node `fs.readFileSync`/`replace-before-canary`/`writeFileSync` script; works cleanly.)
- **Justin reads ONLY the chat, comes in cold.** CEO-altitude, plain English, **never jargon-by-number.** Use the LOCKED cost-lane names: **"Haiku Bouncer"** (cost ①, shipped), **"Clustering"** (cost ②, building), **"Runtime Bookkeeping"** (cost ③, not started).
- **Commit/deploy gates are Justin's, per-action — they don't stack.** Lanes build/edit freely, pause for his explicit go before a **code** commit + before `railway up` + before **merge-to-main**. **STANDING EXCEPTION (yours):** housekeeping-doc commits (current-state, CHANGELOG, deploy-log, sprint DONE-marks) + mailbox/brief + working-doc (handoffs) — you commit + push freely. **Stage by explicit path, never `git add -A`** (shared repos, many agents).
- **NEW standing responsibility (Part A of the doc-accuracy fix, Justin 2026-07-09):** **AC0 keeps `ARCHITECTURE.md` / BUILD / SPRINT current at each `#lock`** — if a session changed what those docs *claim* about the system, update the doc in that same #lock, or route it (name the section + owner). This is now yours, effective immediately. (Part B — the durable process fix — is AC7's lane, see below.)
- **You're a PEER to the lanes on their design, not their boss.** Answer collision/sequencing Qs; let them own their lane's architecture. `AskUserQuestion` is BANNED (hangs).

---

## 📊 LIVE STATE (authoritative)
- **Prod = `v0.2.6` LIVE**, unchanged (nothing deployed since 2026-07-06). The Haiku Bouncer ships **OFF** — REQUIRED Mark-onboarding action is the `HAIKU_SCREEN_MODE`=off→`shadow` flip (in the operator-guide Go-Live Checklist + watch-list) or the ~$1,800/mo lever sits dead.
- **`evryn-backend` main = `9588687`** (= AC6's reconciled ARCH/BUILD docs `a4f6bb4` + Step-55 `declining.md` + my sprint housekeeping). **Merged-but-UNDEPLOYED on main → rides the next `railway up`:** Step-55 `declining.md` (Mira's decline playbook). `_evryn-meta` = `7c97513`, `evryn-team-workspace` = `590c0db`, `evryn-quality` = `2384bf1` — **all clean (0/0), all pushed.**
- **The wave = 3 active lanes:** AC2 (cost) · AC3 (identity/ops) · **AC7 (doc-accuracy process fix — NEW, spun 2026-07-09).** **AC6 (ARCH/BUILD reconciliation) CLOSED this session** (merged, branch+worktree reaped). AC1/AC4 closed weeks ago. Mira builds in-flight (Clustering note-pathway voice).
- **Nothing deploys unattended; nothing is in front of Mark** (still pre-forwarding).

---

## 🛣 Critical path to Mark (the frame)
Finish **Clustering** (MUST be live before Mark) → **Runtime Bookkeeping** (Step 57; Gate-A DEV baseline runs with AC0 before it merges) → **Gate B — the mandatory pre-Mark full create-from-zero live-fire on staging** (staging is UP; needs `SEND_ENABLED`→true + the run; Megan-Griffiths fixtures feed it; AC0 orchestrates) → Justin's account setups → Mark forwards → flip Haiku off→shadow.

> ⚠️ **HOLD THIS WHOLE FRAME LOOSELY (Nathan, 2026-07-08 team current-state — UNCONFIRMED):** the Fenwick legal delay may have **cost us the pilot gatekeeper.** Mark was ready at the engagement's start, is now unavailable/unresponsive, and recovery is uncertain — **the system is ready; the *human* may have checked out** (Justin: *"I don't know for sure yet"*). This is Marlowe/GTM/Nathan territory, not build — but *every* "before Mark forwards" gate above is contingent on Mark actually coming back. **Do NOT treat Mark-live as a certainty**; check the team current-state (`evryn-team-workspace/shared/current-state/`) + with Justin for the latest read before framing anything as "we're almost to Mark." The build work (Clustering / Runtime Bookkeeping / staging) is worth finishing regardless — it's real hardening + the first gatekeeper template — but the *urgency/sequencing* around "Mark" specifically is now soft.

---

## LANES

### AC2 — cost (brief: `docs/working/2026.06.30-ac0-ac2-cost-lane-brief.md`; freshest = Round 15 + the two AC0 banners I appended 2026-07-07/09)
- **Haiku Bouncer (①) = DONE + LIVE** (`v0.2.6`, ships OFF).
- **Clustering (②) = Pieces A+C built → QC-GO** on branch `dc/clustering-a-c` (`f66431f`, worktree `evryn-backend-clustering`), **UNMERGED, ships `CLUSTERING_MODE=off`.** Migration `backups/add-queued-status.sql` **WRITTEN not applied** (AC2 applies dev+prod at deploy). **Piece B (personal-note pathway) = AC2's next build**, paired with Mira's voice (Justin spinning Mira; Mira worktree `evryn-backend-mira-clustering` active). Then converge A+C+B → migrate → Justin-gated deploy.
- **⭐ I routed 2 things into AC2's convergence trip (2026-07-09 banner):** (a) **cadence 4:30pm needs a CODE change** — the built `CLUSTER_HOURS_PT` parses whole hours only (`[10,16]`=4pm); 4:30 needs half-hour support + a SPRINT-Step-58-body rewrite + operator-guide/watch-list; (b) **2 tiny M1 code-comment heading refs** (`config.ts:192`, `emergency.ts:21` → the ARCH M1 heading was renamed to "Monitoring & Silent-Death Safety (M1)").
- **Runtime Bookkeeping (③) = NOT started** (the keystone, last). **Now absorbs two AC3 items (folded via my 2026-07-07 banner):** **54b** (forward-persistence — verdict-keyed) + the **user-creation-at-verdict OPEN QUESTION** (should user-creation move to verdict-time + skip `ignore`?). Design input: `docs/working/2026.07.07-ac3-dc-54b-forward-persistence-build-brief.md`. **Gate-A (Step 88) runs with AC0 BEFORE ③ merges.** Step 71 (Reflection binding safety-net) also on AC2's plate.

### AC3 — identity/ops (brief: `docs/working/2026.06.30-ac0-ac3-identity-ops-brief.md`; freshest = its 2026-07-07 session-3 handoff + my 2026-07-09 collision-reply banner)
- **Staging (Step 80) = UP + healthy.** Service `evryn-staging` deployed + polling; DB seeded by OC (prod-shape, zero PII). **Remaining: flip `SEND_ENABLED`→true + the fake-Mark validation loop (Gate B).**
- **Step-55 `declining.md` = MERGED** to main (rides next deploy). Marked DONE in sprint this session.
- **54a + 54c = ON HOLD behind Clustering** (they touch `process.ts`/`detect.ts`/`triage.md` — the exact surfaces Clustering rewrites). I **confirmed clustering-first** in my 2026-07-09 banner. **⭐ YOU OWE AC3 A FLAG (via Justin) the moment the FULL Clustering bundle merges** (not just A+C — Piece B touches those surfaces too) so it can un-hold. The Fix-54a Mira brief is written + held (`docs/working/2026.07.07-ac3-mira-54a-history-check-permanent-store.md`).
- **54b folded → AC2 Step 57** (see AC2 above). **Nathan brief (Step 56) held** at Justin's request. Queue also has 54c real-name capture, the Mira decline-*offer* pair, Streams C/D/E.

### AC7 — doc-accuracy process fix (brief: `docs/working/2026.07.09-ac0-ac7-doc-accuracy-process-brief.md`; NEW, spun 2026-07-09)
- **Mandate = Part B of the doc-accuracy fix** so ARCH/BUILD/SPRINT don't drift again: a hard `#lock` doc-currency GATE + finalize the **"update-routing protocol"** (change-type → doc-section map) + doc-currency headers on ARCH+BUILD + standing beats in AC/Soren/Mira manuals. Seeded with AC6's drift evidence. **These are cross-repo SOT + manual edits → each needs Justin's explicit approval; Soren = ARCH/BUILD owner-of-record.** The MVP→v0.2 rename is **decided-against** (out of scope). Coordinate via you.

### AC6 — CLOSED (this session)
- ARCH/BUILD reconciliation **MERGED to main `a4f6bb4`** (docs-only, no deploy), fidelity-certified ×2 + Justin-vetted; branch+worktree reaped. **Justin's goal-ordering (memorialized correctly in CHANGELOG/current-state): accuracy → shorter → docs↔runtime agree — NOT "skip the runtime."** New docs on main: `BUILD-EVRYN-v0.3.md` + `BUILD-FUTURE.md` (v0.3 / v0.4+ mechanics relocated); `BUILD-EVRYN-MVP.md` slimmed (KEPT its name — rename decided-against).

---

## 📌 AC0 OWED (standing — carry these)
1. **Flag AC3 (via Justin) when the full Clustering bundle merges** → un-holds AC3's 54a/54c.
2. **Gate-A (Step 88) with AC2** before Runtime Bookkeeping (③) merges — a DEV end-to-end baseline. **Gate-B** = the mandatory pre-Mark full live-fire on **staging** (Megan fixtures; AC0 orchestrates; needs AC3's `SEND_ENABLED` flip).
3. **Part A doc-currency:** keep ARCH/BUILD/SPRINT current at each `#lock` (or route it). Standing.
4. **Prod `_PROD` env cleanup** — the dead unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` on the Railway **prod** service ride the NEXT coordinated prod `railway up` (`railway variable delete` can't `--skip-deploys` → can't do it standalone).
5. **Housekeeping as-you-go:** CHANGELOG at every ship (top priority), current-state, deploy-log, sprint DONE-marks.
6. **Worktrees:** clean as of this #lock — `evryn-backend-cost-haiku` (merged Bouncer) + the stale `mira/declining-md-step55` remote branch were **both reaped 2026-07-09.** Only the two ACTIVE AC2 worktrees remain (clustering + mira-clustering) — do NOT reap those.

## ✅ On Justin's plate
- Piece B Mira spin + AC2 Clustering convergence; AC3's `SEND_ENABLED` flip + validation run; the account setups before Mark; the eventual go on each `railway up`.

## 🧭 Key docs
`docs/current-state.md` · `docs/deploy-log.md` (v0.2.6 latest) · `_evryn-meta/CHANGELOG.md` · `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (Steps 55/89 = DONE/new; 58 flagged) + `SPRINT-V0.3-CANDIDATES.md` · the 4 lane briefs · `_evryn-meta/CLAUDE.md` + `docs/protocols/ac-orchestration-protocol.md` + `lock-protocol.md`.

## 🔧 Repo state (as of this #lock)
All repos clean + pushed (0/0): `evryn-backend` main `9588687`, `_evryn-meta` `7c97513`, `evryn-team-workspace` `590c0db`, `evryn-quality` `2384bf1`. Worktrees (post-reap): canonical main + `evryn-backend-clustering` (AC2 active — do NOT reap) + `evryn-backend-mira-clustering` (AC2/Mira active — do NOT reap). No prod deploy this session.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
