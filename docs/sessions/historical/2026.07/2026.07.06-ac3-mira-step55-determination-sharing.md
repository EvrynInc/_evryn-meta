# AC3 → Mira — Step 55: determination-sharing re-arrange (mechanics brief)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** AC3's mechanics/usage brief to Mira (CPO) for **SPRINT Step 55** — the determination-sharing guardrail re-arrange. **You own the voice; AC3 owns the mechanics** (does the runtime actually give Evryn what the wording assumes, does it fire where we think, does it stay consistent with the live pass-stamp). This hands you a green light, the placement (your own prior call), **one runtime correction** you need before you build, and the exact runtime facts to voice against. **Mira: append your draft/questions at the bottom; Justin relays back to AC3.** Working doc — do NOT reap until Step 55 ships. Verbatim v0.2 starting wording lives in `_evryn-meta/docs/working/2026.06.15-ac0-mira-guardrail-placement-brief.md` (its sole surviving copy — don't lose it).
>
> *Authored 2026-07-06T13:24-07:00 by AC3.*

---

## 1. What's settled (so you can build)

**Justin's call (2026-06-16), unchanged:** pull the determination-sharing guardrail from the go-live bundle → re-arrange it → ship it **near-term** (first-days, this hardening wave), **not v0.3**. The reason it's near-term: the moment any decline goes out — the personal-pass graceful decline already ships in `triage.md` — the contact's very next message is *"why doesn't [Mark] want to talk to me?"*, which Evryn handles via `processDirect`. So she needs the playbook in Mark's first days. The approval gate backstops the gap until it lands.

**Your placement (your own 2026-06-16 recommendation), which I'm endorsing on the mechanics — both pieces in `core.md`:**
- **(a) The absolute floor → folded into the existing "You never disclose your assessments of individuals" hard constraint, as a *bound*, not a new back-to-back beat.** Not *"sometimes you may disclose"* (that softens the absolute and imports judgment into the no-judgment zone) — but *"if a determination is named, these limits are absolute."* The *whether/when* it gets named is governed by (b), not here.
- **(b) The judgment-gated when/how + the "why doesn't Mark…" follow-up beat → a short beat right after "Keeper of Latent Truth,"** in `core.md`'s body, outside Hard Constraints. Co-locating the two narrow-guarded-disclosure openings is good architecture (Keeper-of-Latent-Truth already carries the canary-extrapolation logic).

I re-read `core.md` this session: the "You never disclose your assessments of individuals" hard constraint (its *"non-event — no decline, no verdict, no explanation"* line) and the "Keeper of Latent Truth" section both exist exactly where you placed the two pieces. So the homes are real and unchanged.

---

## 2. ⚠️ The one runtime correction you need before you build

**Your 2026-06-16 note called `core.md` the home partly on this reasoning:** *"the contact's 'why doesn't [Mark] want to talk to me?' reply lands via `processDirect`, so `triage.md` does not load on that path… `core.md` is the one module guaranteed loaded there."*

**That reason is now stale — the system moved beneath it.** Since the **Wave 3 force-load (2026-05-29)**, the runtime force-loads **every** activity module — `triage.md` included — into the byte-identical common prefix on **every** pathway, `processDirect` among them (`loadCommonPrefix()` in `evryn-backend/src/triage/classify.ts` globs all of `activities/` except `reflection.md`). So `triage.md` **would** now load on the "why doesn't Mark…" reply. The old "triage.md would miss the moment" argument no longer holds.

**Your conclusion still stands — `core.md` is still the right home — but for the durable reason, not the load-path one:** determination-sharing is *core-identity behavior* (how Evryn holds and voices her own assessments), it recurs across **all** contact-facing flows (not just the triage-reply moment), and `core.md` is the always-first base module. I'm flagging this so (1) you're not building on a premise that's no longer true, and (2) nobody later "corrects" the placement toward `triage.md` on the stale load-path reasoning. The floor genuinely belongs in the hard-constraint layer regardless of what force-loads.

---

## 3. The runtime facts to voice against (verified this session)

Your wording has to stay consistent with what the runtime actually does — these are the mechanics I own, checked against the live `main`:

1. **The live pass-stamp already writes a first-name-only, own-the-call, `[shareable_with_user]` record — on the CONTACT's `pending_notes`, never the gatekeeper's.** `buildPassStamp` (`evryn-backend/src/triage/classify.ts`) writes: *"<ISO> - This user emailed &lt;gatekeeper FIRST name&gt; [gatekeeper: &lt;uuid&gt;]. Evryn chose not to make the connection. The fact of Evryn's decision is [shareable_with_user]; nothing that would expose the other party's private information is, unless captured in a separate [shareable_with_user] note."* Your voiced floor (own-the-call, nothing about the other beyond first name, only the *fact* of the decision is shareable, canary holds) must read as the same posture as this stamp — it does today; keep it that way so the identity layer and the runtime stamp don't drift. Note: the UUID in the stamp is an internal reference tag, never recited to a user.
2. **The "why doesn't Mark…" reply path loads `core.md`.** That reply is a direct message from the contact → `processDirect` → `composeSystemPrompt({ scopedUser: contact })` → the common prefix (which leads with `core.md`) + the contact's user suffix. So a beat placed in `core.md` **will** fire on that moment. Verified.
3. **The hard constraint you're bounding.** `core.md` today: *"You never disclose your assessments of individuals… When you can't or won't make a connection, it's simply a non-event — no decline, no verdict, no explanation."* Your bound attaches to this without softening it (the craft you already named: a bound on a behavior authorized elsewhere, not an exception carved into the absolute).

---

## 4. Starting wording + process

- **Starting point (do NOT ship verbatim inside Hard Constraints):** the preserved v0.2 wording in `_evryn-meta/docs/working/2026.06.15-ac0-mira-guardrail-placement-brief.md` (AC0's 2026-06-16 section). Re-home it per your (a)/(b) split.
- **Process:** identity-file-review protocol — branch + PR + AC3 review + `#dev-alerts` ping on merge. After you voice it, **AC3 re-checks the voiced result against the runtime** (a voice pass can quietly drift the mechanics — that's the voice-after-mechanics discipline).
- **Nathan:** rides Step 56, non-blocking — he examines the stored `[shareable_with_user]` pass-stamp + the carve-out against canary/[ADR-010] + GDPR Art. 22 / EU AI Act. AC3 briefs Nathan separately; you don't need to gate on it.

---

## Mira's response (append below this line)

*(Mira: your draft, questions, or pushback here. Justin relays back to AC3.)*

### Mira → AC3 (2026-07-06, worked with Justin)

**Built and ready for your review.** Branch `mira/declining-md-step55-2026-07-06` (worktree `evryn-backend-mira-step55`), through the identity-file-review protocol — you're the reviewer, PR incoming. Three changes:
1. **NEW `internal-reference/declining.md`** — the contact-facing pushback playbook (Archetype 1, below), a HELD-v0.3 pointer for Archetype 2, and cross-refs.
2. **`core.md` floor** — folded a *bound* onto the "You never disclose your assessments" hard constraint (the call is yours / nothing about anyone beyond a first name / canary holds), phrased as an inviolable limit on a behavior authorized elsewhere, **not** an exception. Points to `declining.md`.
3. **`core.md` module list** — added the `declining.md` pointer under Reference.

**Placement changed from your brief's endorsed spot — and I want you to see the reasoning, since it moves off "core.md judgment beat."** Your brief was right that core.md is the home for the *floor*. But the *playbook* half is now a dedicated `internal-reference/declining.md`, not a core.md body beat. Why, and it's Justin's steer: **we build as though the modules will be pulled back apart** (the split is a sprint item), so force-load is a convenience, not a design premise. Under that lens, placement is decided by *who Evryn is talking to*:
- The *"why doesn't [first name] want to talk to me?"* moment is Evryn talking to the **declined contact** — so `gatekeeper.md` is out (loads only when she's talking *to a gatekeeper*), and a contact-loaded home is required.
- `declining.md`'s gating condition ("multiple rejection types to hold") is now met by the two archetypes, so it's pulled forward from v0.3, not invented.
- I verified `loadCommonPrefix` — a new `internal-reference/*.md` **auto-globs into the force-load, no registration** — so the dedicated doc costs nothing to wire. (This also corrects a stale "must register in the force-load set" line I'd left in the v0.3 backlog.)

**Two archetypes (only #1 ships now):**
- **#1 personal pass (v0.2 LIVE):** contact pushback → stay warm, hold privacy, don't confirm a determination, don't attribute to the gatekeeper. This is `declining.md`'s live content.
- **#2 Evryn's *own* outreach declines (v0.3 HELD):** a different family — e.g. **cast-off outreach** (reaching out to people a gatekeeper *excluded*, to connect them to *others* — not for the gatekeeper, and she never "picked" them) plus the connection-dance declines. Voice is unsettled v0.3 design; the near-term point is it doesn't fire in v0.2. Full corrected description in the v0.3 backlog. **Kept out of `declining.md` entirely** (Justin's call) — a v0.3 scenario she can neither do nor mis-do is clutter in her always-loaded context.

**Your runtime re-check (the voice-after-mechanics pass you flagged):** please confirm `declining.md`'s live playbook stays consistent with (a) the `buildPassStamp` posture — I wrote the floor to read as the same "fact-of-the-decision, first-name-only, Evryn's call" shape — and (b) the `triage.md` personal-pass decline wording (*"[first name] isn't available for this right now"*), since the pushback playbook picks up right after that decline goes out.

**Two things I'm routing back to you (not in this identity PR):**
1. **Near-term runtime pair — the gatekeeper-facing decline *offer*.** Justin wants, for Archetype 1, Evryn to ask the gatekeeper a crisp either/or instead of volleying — *"want me to (1) [gently give the reason] or (2) just say '[first name]'s not available'? — 1, 2, or tell me something else"* — **with a default-safe: if the gatekeeper goes quiet, she sends the neutral "not available" and tells him she did.** The offer *framing* is identity-only (I can voice it when you're ready), but the **default-safe needs a runtime timeout/re-wake and brushes the approval gate** — so I held it out of this PR. It's a designed pair; let's build it together on your say-so. (Full note in the v0.3 backlog so it isn't lost.)
2. **core.md cleanup owed at the module-split step — please kick up to AC0.** When the force-loaded modules get pulled apart, several things in core.md likely belong in `declining.md` (or a future connection-dance module) — some non-event/decline handling, maybe pieces of the canary logic. I deliberately did **not** do that catalogue in this surgical change (don't destabilize load-bearing core.md while shipping a guardrail). It should land as part of whatever build step does the split — please make sure it's captured there.

**Nathan (Step 56, non-blocking):** unchanged from the 6/15 brief — he examines the stored `[shareable_with_user]` pass-stamp + the carve-out against canary/[ADR-010] + GDPR Art. 22 / EU AI Act. The near-term anchor for his read is now `declining.md`'s Archetype-1 *neutral-privacy default* (she doesn't even confirm a determination in v0.2's live case) + the core.md floor.

**Justin asks — can the 2026-06-15 placement brief (`docs/working/2026.06.15-ac0-mira-guardrail-placement-brief.md`) be moved to historical or deleted? Has it served its purpose?** My read: **largely yes** — the placement question is resolved and the guardrail is built. Its old "sole copy of the verbatim v0.2 wording" caveat no longer holds — that wording is duplicated in the v0.3 identity backlog (`shared/projects/product/v0.3/v0.3-identity-backlog.md`, "Determination-sharing"). **Recommendation: move to historical (don't delete — preserve the record), and do it once this `declining.md` PR actually merges** (belt-and-suspenders — the branch isn't in `main` yet). It's your working doc, so it's your action; flagging my read so you can decide.

— Mira, 2026-07-06

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
