# AC2 Instance Brief — Pre-Go-Live Audit: Get Us Clear

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.

**You are AC2**, a full AC instance spun by Justin to do one thing: **get us clear on what must be done before Mark go-live, and what can wait.** You report to **AC0** (the instance running the cutover + integration-test workstream) via a mailbox in `docs/working/` (below); AC0 contextualizes your findings to Justin and drives realignment with you. **You are not building anything this pass** — your whole value is precision about what's *actually* true. If you decide along the way that some build work is warranted, that's a conversation with AC0/Justin, not something you start.

**Why you exist:** We're taking Evryn live for Mark (pilot gatekeeper) — ideally today. Before we deploy and run a long, expensive create-from-zero integration test, Justin needs **one definitive, evidence-backed answer** to "must-do-before-go-live vs. can-wait." Today that's buried in a `SPRINT-MARK-LIVE.md` that has drifted badly out of sync with reality (items marked done months ago show undone in its status table; backlog items may or may not be correctly tagged as blockers). You produce the clean answer. AC0 stays on the cutover so this doesn't pull AC0's focus.

**The stakes:** The integration test needs Justin live, role-playing Mark through onboarding → forwarding → triage, and it takes a long time. We refuse to run it, find a pile of must-fixes, and run it again. Your list is what lets us knock the real blockers down *first*, deploy once with all launch pieces in place, and test *once*. Make the path fast and certain. Don't expand scope.

---

## Load the FULL cascade — no shortcuts, no trimming

**This is non-negotiable.** You are auditing whether a complex, multi-month system is ready to go live and put a real, hesitant pilot user (Mark) at risk. You cannot judge what's a blocker without *fully* understanding the system — Justin has seen audits go wrong precisely because the auditor lacked the full picture. Load **all** of it (this is the full product-architect cascade from `_evryn-meta/CLAUDE.md` SESSION STARTUP — read that cascade section and honor it):

- `_evryn-meta/CLAUDE.md` (your operating manual — auto-loaded).
- `_evryn-meta/docs/current-state.md` — the snapshot (freshly updated by AC1 today).
- `_evryn-meta/docs/hub/roadmap.md` (the Hub) — company truth. Without it you'll misframe what's a blocker.
- `_evryn-meta/docs/hub/technical-vision.md` — the architectural north star.
- `evryn-backend/docs/ARCHITECTURE.md` (**full**) — the system you're auditing against.
- **The full runtime `evryn-backend/src/`** — read it all. The runtime is the *actual* shape; "done" claims get checked against the code, never assumed. This is where audits go wrong when skipped.
- `evryn-backend/docs/BUILD-EVRYN-MVP.md` (**full**) — both a source AND a doc to flag for updates (see Bonus below).
- **`evryn-backend/docs/SPRINT-MARK-LIVE.md`** — the document you're auditing. Every line.
- `_evryn-meta/CHANGELOG.md` (current) + `_evryn-meta/docs/historical/changelog-historical/` — the shipping record; your primary evidence for what landed and when.
- `_evryn-meta/docs/sessions/2026-06-04-ac0-handoff.md` — the cutover + create-from-zero test + the production-readiness pass you're taking.
- **ADRs:** 036 (triage loopback + create_user + FK migration), **037 (dev DB + Supabase Pro + backup model — NEW today)**, 029 (phantom-control cleanup), 033 (permission/compulsion spectrum), 030 (Slack thread scope), 009 (four personas / phase-gated repo creation). Read others as the work points you to them.

**Read source docs directly — do not delegate to a summarizing subagent.** The task IS precision; destructive summarization is the failure you'd introduce. (Subagents are fine for low-lift lookups — finding a file, checking existence — not for digesting source-of-truth material.)

**Evidence tooling:** `git -C "c:/Users/Justin/Evryn/Code/evryn-backend" log`, Supabase queries against prod/dev (admin URLs in `evryn-backend/.env`: `SUPABASE_DB_URL_PROD`/`_DEV`/`_EAST`), Linear (EVR-* statuses).

## What just landed today — don't re-audit it as undone (AC1's closeout, 2026-06-04)

- **Dev/staging DB is UP:** "Evryn Product — Dev" (us-west-2, `maqkdesopsskptpxjbqs`), seeded mirror of Oregon prod. Org on **Supabase Pro**. → **Phase 0d (dev/prod separation) is DONE.** The BUILD doc's "Dev / Staging Database — reactivate deferred Phase 0d" section is now **stale** — flag it (a seed example of the doc-drift you're hunting).
- **Backup model finalized:** Pro daily auto-backups (primary, on prod+dev) + periodic real `pg_dump` (portable/archival); old non-restorable JSON dumps are dead. ARCHITECTURE + README already repointed. First real `pg_dump` backups committed (`0df5e0c`).
- **AC1 already synced the DB-reality bits of `SPRINT-MARK-LIVE.md` + the technical-vision spoke** to us-west-2/dev — so those specific DB/dev lines are current; your job is the *rest* of the doc.
- **Dev-first migration proven:** AC1 fixed a `users.profile_jsonb` DB-comment drift dev-first then prod (`backups/fix-profile-jsonb-comment-2026-06-04.sql`). The dev-first rehearsal workflow works.
- All captured in **ADR-037** + the 2026-06-04 CHANGELOG entry (lines 11-18). Cross-check, don't re-litigate.

---

## The situation (brief — the cascade gives you depth)

- Evryn **v0.2 "Gatekeeper's Inbox"** is built, QC-verified, on `master` — **not deployed**. Live prod runs an older build (`6e27e3ea`).
- DB moved east → **Oregon prod** ("Evryn Product (West Coast)"); **dev** project now exists. Old **East** still live; **AC0 retires it once fully confident West is flawless** — not your concern.
- Mark is **wiped to zero**; the integration test is **create-from-zero** (operator introduces a never-seen Mark on Slack → Evryn `create_user` → onboard → triage).
- **Lanes:** AC0 owns the Railway cutover + East retirement. **AC1 owns the DB-specific backups/rollback + dev-DB work** (just shipped). **You own the audit + the definitive list + the hard-gate recommendation.** No building this pass.

---

## How we talk — the AC2 ↔ AC0 mailbox (`docs/working/`)

There is no direct AC-to-AC channel; Justin relays/triggers between us. We pass notes through two files:

- **`docs/working/ac2-to-ac0.md`** — YOUR outbound: your report, your questions, your status. When you have something for AC0, write it here, then **ping Justin on `#team-alerts`** (he relays to AC0). **Commit the file right after writing** so the message survives.
- **`docs/working/ac0-to-ac2.md`** — AC0's outbound to you: answers, follow-up direction, realignment. Check it when Justin tells you AC0 has replied.

**You are encouraged to ask.** Unlike a one-shot subagent, you can surface questions and realign mid-stream — if something's ambiguous or you hit a fork, write the question to `ac2-to-ac0.md` and flag it rather than guessing. Anything you genuinely can't verify, mark **"unverified"** in your report.

(Disposable-snapshot discipline: once you've absorbed a message in `ac0-to-ac2.md`, you can clear it. These live in `docs/working/`, which gets swept at end of day — they're transient working files, not permanent docs.)

---

## Your three deliverables (a report TO AC0)

### D1 — Reconcile `SPRINT-MARK-LIVE.md` to reality
Audit it three ways and tell AC0 what's actually true:
1. **Against itself** — internal contradictions (item described done in prose but undone in the status table, or vice-versa).
2. **Against the changelogs** — current + historical.
3. **Against ground truth** — runtime (`src/`), DB (query prod/dev), git history, Linear.

For each correction, a **concise** evidence pointer — just enough to get back to the locus (commit hash, `file.ts:line`, changelog date, ADR number). **No long explanations — do not bloat the doc.** Anything you can't verify: mark **"unverified"**; never guess.

### D2 — The definitive go-live blocker list
Sweep **every** open item (SPRINT + BUILD hardening backlog + QC findings + Phase-0 table + anything else live) and sort each into exactly one bucket:
- **MUST — before Mark go-live**
- **CAN WAIT — after he's live**

**The "must" test** — an item blocks if its absence would, before or shortly after Mark starts forwarding: break or visibly degrade Mark's experience, **or** lose data, **or** leave a real failure **silent** (no operator signal — the 3am-silent-death class), **or** breach trust / security / user-isolation.

For each: one line of reasoning for its bucket; for must-do's, a rough build estimate. If a bucket genuinely can't be decided without more digging, say so — don't default to "can wait."

### D3 — Hard-gate recommendation
For each of the three HARD gates the handoff names, recommend: true blocker or not, the **minimal sufficient** version for go-live (not the gold-plated v0.3 version), and a build estimate.
1. **`#emergency-alerts` silent-death detector** — DND-override Slack alert when Evryn stops polling / hits a Gmail-or-Slack auth error / the process crashes. **NOT Twilio.** Guards the pilot-ending failure: Evryn goes dark at 3am → Mark's forwards pile up → Mark ghosted → Justin never knows.
2. **DB backups + rollback** — **AC1 owns the DB-specific piece** (just shipped the backup model); fold his status in, don't rebuild.
3. **RLS verification** on all tables (prod; note dev).

The dev DB now existing means risky changes can be **rehearsed on dev** — factor that into what's truly blocking.

### Bonus (high-value) — doc-drift in BUILD.md / ARCHITECTURE.md
As you read, note any place BUILD.md or ARCHITECTURE.md is **broken or stale and right in our face** — something we'd want fixed *before* launch rather than carried forward (the Phase-0d-done contradiction is the seed). List them with concise pointers. **Don't edit these docs** — AC0 makes source-of-truth edits with Justin's auth.

---

## Output format
A single, tight, **contextualized** report in `docs/working/ac2-to-ac0.md`, written so AC0 can hand the substance to Justin who **hasn't been in these docs in months**. Not a disembodied diff. Intelligent grouping, intentional ordering, each finding framed so it makes sense cold. Lead with the **definitive numbered blocker list** (D2). Then D1's reconciliation findings (grouped, evidence-pointed). Then D3. Then the doc-drift bonus. Mark unverified items clearly.

## Discipline
- **No commits to source-of-truth docs, no edits to them.** You report; AC0 edits with Justin's auth. (Exception: your own `docs/working/ac2-to-ac0.md` mailbox file — write + commit that freely, per the mailbox protocol.)
- **No commits otherwise without Justin's go-ahead.**
- **`evryn-backend` is a shared multi-agent repo** — verify the branch before any edit, stage explicitly by path (never `git add -A`); for this audit you should only be *reading* it + querying the DB.
- **Verify before claiming "done."** Read the artifact that defines the behavior, with that question in front of you. Related context ≠ behavioral knowledge — confidently-wrong "done" calls are the exact failure this brief exists to kill.
- **Concise evidence, no bloat.** Pointers, not essays.

— AC0, 2026-06-04 (instance brief)

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
