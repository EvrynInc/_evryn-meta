🗄 **SUPERSEDED BY: `_evryn-meta/docs/sessions/2026.09.02-ac037h3a2-ac037h3a3-handoff.md`** *(2026-09-02)*

> 🔴 **ARCHIVED — DO NOT SPIN FROM THIS FILE.** **Everything still true is carried in the superseding brief, in a single layer.**
>
> **Why it was superseded rather than carried:** it was accurate when written on 2026-08-22 and then accreted a full day of corrections **pointing at its own other sections** — which is the exact trigger `lock-protocol.md` step 10 names for SUPERSEDE. **A corrected doc acquires a read order, and a fresh instance then spends its first hour deciding which layer wins.**
>
> ⭐ **Come here for FORENSICS, not for instruction** — how a disposition was reached, what a verdict rested on, which claims were corrected and by whom. **Its per-file verdicts are reproduced current in the superseding brief; its reasoning is not.**
>
> *(Archival annotation only. The frozen-record rule is intact — nothing in the body below has been altered.)*

~~🔒 **HELD-SESSION-DOC** — the live marching orders for the ACP-lineage instance retiring `docs/working/`; a fresh instance spins from this file · **RETIRE UPON:** `docs/working/` no longer exists · **OWNER:** ACP · **HELD SINCE:** 2026-08-22~~

# `AC0-37h3a2` — finish retiring `docs/working/`. Written 2026-08-22 by `AC0-37h3`.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. **STOP.**
>
> **Do not load. Do not resume. Do not finish the current task**, however nearly-done it looks. **Ping Justin in one line — *"I compacted. I am stopping."* — and do NOTHING else.** *(His ruling, 2026-08-22: **"I want you doing no work with a cooked brain."**)*
>
> **A well-written summary is the attractive siren that will wreck your ship, not the safety net.** The danger is not that it is bad — it is that it is GOOD, so it draws you onward. *(ADR-058.)*

> ### 📌 THIS DOC REPLACES `docs/working/2026.08.20-ac037g-ac037h-handoff.md` AS YOUR MARCHING ORDERS.
>
> **That file is 1,055 lines written across six instances, and reading it top-to-bottom makes you derive, re-derive and reconcile four separate times** — §2 says one thing, §7 overrides it, §8 overrides that, §9–12 override again. **Everything still true is carried here.**
>
> ⚠️ **It also carries at least one `VERIFIED AT SOURCE` label that is FALSE** *(see §6.4)*. **Go to it only under a §2.2 trigger, and treat its claims as leads.**

---

# §1 — THE JOB, AND WHERE IT STANDS

**Retire `docs/working/` into `docs/sessions/`.** *(Justin's design, 2026-08-19.)* **One folder, not two** — and the load-bearing half is not the merge: **session-doc references are NEVER repointed again.** Findability becomes a rule — `docs/sessions/`, or `docs/sessions/historical/YYYY.MM/` filed by the doc's own date, which is the first thing in its filename.

🔴 **THE TEST FOR WHAT STAYS, and it is much tighter than earlier lanes used:** *"The ONLY instance in which a working doc should sit here is if we had a session handoff that was supposed to go to a new session, but that session is still needing to re-spin from it."* ⚠️ **"Is anything referencing it?" is DEAD as a test. Do not resurrect it.**

**⭐ WHERE IT STANDS: 58 → 16. Forty-two files moved, all committed.** *(For four consecutive instances the honest line was "not one file has moved." That is over.)*

**Your job is the last 16, then three closing steps. You are NOT starting cold — every file already has a disposition in §3.**

---

# §2 — YOUR LOAD

## 2.1 · Load these now, in this order. ~1,900 lines.

**Widest lens inward, intended shape before current state.** *(`ac.md`, Context Discipline — it is a standing rule, not this list's preference.)*

1. **`_evryn-meta/.claude/agents/ac.md`** *(full — 832)* — your manual. **Nothing else makes you AC.**
2. **`_evryn-meta/docs/hub/roadmap.md`** *(full — 184)* — the Hub. Company truth.
3. **`_evryn-meta/docs/current-state.md`** *(full — 195)* — the snapshot, including its 📮 BULLETIN.
4. **THIS FILE** — ⚠️ **you have almost certainly read it already, because it is what you were pointed at. RE-READ §3 and §4 after `ac.md` lands**, because they mean different things once you hold the manual.
5. **The four protocols:** `ac-writing-protocol.md` *(118)* · `lock-protocol.md` *(134 — **read step 10 twice; it is the rule you are applying**)* · `doc-update-routing-protocol.md` *(119)* · `mailbox-protocol.md` *(214)*.

**Confirm every bottom canary.** ⚠️ **A line count here is a HYPOTHESIS — trust your own read and flag any delta.**

## 2.1a · Session-startup hygiene — ALREADY DONE 2026-08-22, do not redo

✅ **`.claude/settings.local.json`: absent** *(checked)*. ✅ **Repo-sync: all repos on their canonical branch, `_evryn-meta` clean on `main`.** ✅ **Worktrees: the canonical tree plus `_evryn-meta-acf15` on `acf15/nest-prep` — 🔴 that one is ACT's, do NOT reap it.** ✅ **`#dev-alerts` peek: Justin declined it.**

⚠️ **DO re-run the repo-sync check yourself if you are on a different machine, or if hours have passed** — it is cheap and it is the one that fails silently.

## 2.2 · 🔴 TRIGGERS — the moment one of these fires, LOAD THE FILE BEFORE YOU DECIDE.

> **Justin's instruction, 2026-08-22, and it is the reason this section exists rather than a bare list: *"if you don't have him load something right away, make sure that he knows that if he runs into xyz scenarios, running blind is MUCH more dangerous than loading a file."***
>
> 🔑 **The load above is small BECAUSE the derivation is already done — not because the job is light.** ⚠️ **The moment you are deciding something the manifest does not already answer, you are outside what that load covers.**

| The moment you… | Load this, in full, BEFORE deciding |
|---|---|
| **Are about to judge ANYTHING that touches product, at all** | 🔴 **DO NOT. Ask `AC0-37h3b`** — §5.1. **This is Justin's stipulation, not a preference.** |
| **Doubt a §3 disposition, or want to know what `ACx` actually checked** | **`2026.08.19-ac037f-acx-working-docs-sweep-lane.md`** *(384)* — per-file verdicts for all 55, plus its own honest-limits §13. ⚠️ **In `docs/working/` NOW; moves to `docs/sessions/historical/2026.08/` when you close this out.** *(Resolve it by the date rule rather than by a path I write here — which is the very rule this job exists to establish.)* |
| **Need the fuller DERIVATION behind a §3 disposition, or Justin's earlier rulings verbatim** | **`2026.08.20-ac037g-ac037h-handoff.md` §§9–12** *(same folder, same caveat)*. 🔴 **§§0–8 of that file are superseded — do not read them for instruction.** |
| **Need to know whether a working doc's content already landed somewhere** | `_evryn-meta/CHANGELOG.md` *(513)* and/or `evryn-backend/CHANGELOG.md` *(352)* — **the cheapest way to avoid redoing closed work** |
| **Hit a question of whether something SHIPPED** | `_evryn-meta/docs/deploy-log.md` *(37)* **plus** that Step's own status — **the authoritative pair.** A *"rides the next deploy"* note is intent, never proof |
| **Need to know if an owed item is already tracked** | `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` *(786)* — its 🔵 LIVE region is the open work |
| **Are about to write into ANY mailbox, or discharge an entry** | `mailbox-protocol.md` — **at the moment of use** |
| **Consider spinning ANY subagent** | 🔴 **`docs/protocols/ac-orchestration-protocol.md` (743) + the sub-doc for your target. MANDATORY — its core alone does not authorise a spin.** |
| **Are asked about the `#lock` or `#sweep` cadence** | that protocol, every time |

⛔ **You do NOT need, and should not load speculatively:** `technical-vision.md` · `ARCHITECTURE.md` · the Code Atlas · the dependency map · the BUILD doc. 🔑 **The last four are `AC0-37h3b`'s load, deliberately — and the split only works if you do not duplicate it.**

---

# §3 — THE SIXTEEN, AND WHAT EACH WAITS ON

**Grouped by BLOCKER, because the blocker is the actionable part.**

> # 🔴 READ THIS BEFORE ACTING ON ANY DISPOSITION BELOW — A REHOME IS NOT A REHOME UNTIL IT IS COMMITTED
>
> *(The rule is `AC0-37h3b`'s, generalised 2026-09-01T17:26 from this lane's decision to hold the `/health` doc. **It changes the status of most of this section**, so it sits at the top rather than inside one entry.)*
>
> 🔑 **An UNCOMMITTED destination is a PROMISE, not a HOME.** **A source doc archived against one is archived against nothing anyone else can see** — and if that edit is later declined, reset, or swept by another lane, **the content exists nowhere while both halves read as done.**
>
> ⚠️ **The asymmetry is what settles it: a file held one day too long costs a `git mv`. A file archived against a destination that then vanishes costs the content, silently.** ⇒ **OVER-HOLD.**
>
> ### 📋 VERIFIED AT SOURCE 2026-09-01T17:30 by `AC0-37h3a2` — what is actually committed
>
> ✅ **THE ONE MOVE ALREADY MADE IS SAFE, and it was checked rather than assumed.** **`2026.07.22-ac0-ac2-fast-follow-brief.md` was retired against `SPRINT` Step 94's recipe lift — commit `14d8faf`, confirmed an ancestor of `main`.** **A real, shared, committed home.**
>
> ### ✅ UPDATED 2026-09-01T17:14 — THE QUEUE LARGELY CLEARED. Justin committed `1816850` ("Nine ballot rulings executed"), and `evryn-backend` went from FIVE uncommitted paths to ONE.
> ✅ **LANDED, and each verified at source before this lane acted on it:** **`ARCHITECTURE.md`** *(both `/health` halves — contract at `:1258`, colour rule at `:1270`)* · **`SPRINT-v0.2-optionals.md`** · **`atlas/03-email-processing.md`** · **the `gatekeeper-onboarding.md` → `gatekeeper-runbook.md` rename.**
> ⇒ ✅ **THE `/health` DESIGN DOC WAS RELEASED AND IS MOVED. `docs/working/` is 14.**
> 🔴 **STILL UNCOMMITTED, and it is the ONE that still holds two files: `docs/SPRINT-V0.2-HARDENING.md`** — it carries **Step 70a's lift** *(which holds the cleanup-batch brief)* **AND Step 114's two-clocks capture** *(which holds the AC5 brief)*. **Both stay held until it commits.**
> ⚠️ **`_evryn-meta` still has three: `qc.md`, `current-state.md`, and the two-author `ac-orchestration-protocol.md`.**
>
> **THE ORIGINAL AUDIT, kept because it is the record of why four files were held:**
> | Uncommitted path | What of ours depends on it |
> |---|---|
> | `docs/ARCHITECTURE.md` | the `/health` response-body + colour-semantics rehome — **already held for this reason** |
> | `docs/SPRINT-V0.2-HARDENING.md` | **Step 70a's lift** *(the prompt-injection item)* **AND Step 114's two-clocks capture** |
> | `docs/SPRINT-v0.2-optionals.md` | Step 120 / the Step 53 region |
> | `docs/atlas/03-email-processing.md` | the Atlas I1 correction |
> | `RM docs/gatekeeper-onboarding.md → docs/gatekeeper-runbook.md` | **the rename AND the §5 clustering fix — staged, NOT committed** |
>
> ⇒ 🔴 **SO EVERY "DISCHARGED" AND "CLEARED" BELOW THAT NAMES ONE OF THOSE IS PENDING-COMMIT, NOT DONE.** **Specifically: the cleanup-batch brief's leg 2, the AC5 brief's two-clocks capture, the runbook rename, and the `/health` rehome.** **Do not move any of those four files until the paths above are committed.**
> ⭐ **AND THE STATUS OF THIS WHOLE JOB FOLLOWS FROM IT: the sixteen are no longer gated on ANALYSIS. They are gated on JUSTIN'S VET QUEUE.** **The dispositions exist; the homes are drafted; what is missing is commits.**

### 🅰 Blocked on `AC0-37h3b` — product-coupled, 6 files

⚠️ **Several LOOK ready to retire. Do not.** **This list was fixed in advance, while the deciding instance still held the whole corpus** — precisely because an under-loaded agent cannot reliably tell in the moment which questions are product-coupled.

- **`2026.08.11-ac0-ac0-handoff.md`** *(1,346 lines — the largest file in the corpus)* — ✅ **B1 IS LANDED AND COMMITTED** *(`evryn-backend` `abd532f`, Justin-vetted: the `ARCHITECTURE.md` Identity-Composition block + `SPRINT-v0.2-optionals.md` Step 120)*. **That coupling is DISCHARGED.**
  > 🔴 **BUT DO NOT RETIRE IT — AND THIS CORRECTS MY OWN DISPOSITION.** **`ACx` named B1 as its blocker, and I inherited that as if B1 were the ONLY thing in it. A residue pass on 2026-08-22 says otherwise: 32 residue-shaped hits, and at least five sections that are owed on their face.**
  > - **§9.4 — *"Inherited items I did NOT touch, so they are still owed exactly as written."*** · **§8.9 — *"ROUTED, NOT DONE — the follow-ups this night generated."***
  > - **§10.3 — a DRAFTED RULE marked *"paste this verbatim; do not paraphrase it,"* with §10.4 stating where it belongs and that it is PARKED.** *(It is the compaction-experiment rule. ⚠️ Check whether ADR-058 already absorbed it before doing anything.)*
  > - **§2.6 — a deliberately HELD control set (Steps 103/104/105).** · **§9.9 — what died with that session, including one item recorded PERMANENTLY LOST.**
  > ### ✅ CLOSING VERDICT, 2026-09-01 — THE RESIDUE PASS IS DONE AND THIS FILE IS RETIRABLE.
  > *(The pass was run earlier the same day and then left without a disposition, which is how a worked file silently becomes an unworked one. This is that disposition.)*
  > **Every section the disposition above named is discharged:**
  > - **§8.9 *"ROUTED, NOT DONE"* — 9 items, all closed.** **Its item 2** *(a guard scanning tracked files for stray control bytes)* **shipped as the pre-commit hook and executed on every commit this lane made today.** **Its item 7** *(a load list is an artifact of record — commit it with the brief)* **landed in `ac-orchestration-protocol.md` part 2, Justin-authorised, verified committed at line 127.** **Its item 8** *(a `current-state` paragraph waiting on a ruling)* **is in that file's BULLETIN.** **Its item 9** *(the parked compaction rule)* **— see below.** The rest are product items with tracker homes.
  > - 🔴 **§10.3–10.4 — the DRAFTED COMPACTION RULE, and this was the live hazard: it is LANDED AND ITS CENTRAL INSTRUCTION IS REVERSED.** **The block is headed *"paste this verbatim; do not paraphrase it"* and prescribes STOP → RELOAD → resume. Justin overturned exactly that on 2026-08-18** *("we tried that, and the agent just sails through it and confidently flails about, unloaded")*. ✅ **Marked SUPERSEDED in place on Justin's ruling rather than deleted, so a reader meets the reversal instead of re-installing it.** ⭐ **§10.4's architectural call was RIGHT and did land: the rule belongs in the router, not in AC's manual, because any agent can compact.**
  > - **§9.4 *"Inherited items I did NOT touch"* — resolved or rehomed.** The 6-month TTL loaded gun is in `current-state`; the dashboard status-pill note is `SPRINT` Step 107; the changelog obligation was discharged when the branches merged.
  > - **§9.9 *"WHAT DIED WITH MY SESSION"* — the receipt-audit gap stands as PERMANENT, and that is now recorded where it can act.** **Its cheap never-run mitigation — check each sweep lane's self-reported receipts against the Atlas partition's own file list — is carried into `ac-orchestration-protocol.md` alongside the load-list rule, as the stated fallback for when a list is already lost.**
  > - **§2.6 — the held control set (Steps 103/104/105)** is three ordinary numbered sprint Steps today; the hold was consumed on 2026-08-11.
  > ⇒ **RETIRABLE. Nothing in it is the sole home of anything.** ⚠️ **It moves with the closing sequence rather than now, purely because §7's owed-list resolution is what the two `docs/sessions/historical/` moves depend on being true.** ⭐ **It is the single highest-yield unread thing in the folder** — and the lesson it proves is the estate's own: **a file can be 95% ghost town and still be the sole home of something.** ⚠️ **Its §§7–10 are a running log; the owed material is concentrated in §§8.9, 9.4, 9.9 and 10.3–10.4.**
- ⏳ **`2026.07.15-ac3-step78-real-health-design.md` — HOLD. The product lane owns the verdict and is bringing back ONE, not three.** *(Was: "the extraction is MOOT — the rule already lives in `atlas/07-safety.md` invariant 3 and in `liveness.ts`'s own comment; it needs a HOME, not an extraction.")* 🔴 **`AC0-37h3b2` read `atlas/07-safety.md` and Step 78 at source on 2026-09-01 and stands behind only ONE un-rehomed item:** **the `/health` RESPONSE-BODY contract — the `checks` object (`db`/`slack_socket`/`poll`/`m1`) — which has no durable home.** `ARCHITECTURE.md` describes M1's *behaviour* but never the response *shape*; the Atlas describes the *decision* (`decideHealthStatus`) but not the body. ⚠️ **The dashboard reads that object, so it is an integration contract with an external consumer.** **→ lands in `ARCHITECTURE.md` → Monitoring & Silent-Death Safety; a source-of-truth edit, Justin-gated, and the product lane's, not ours.** 🔴 **AND A SECOND ITEM, VERDICTED 2026-09-01 AND UPGRADED FROM "WEAK" TO URGENT — the dashboard COLOUR SEMANTICS.** *(`AC0-37h3b2` rated this second-weakest in its first pass and reversed itself after reading `SPRINT` Step 107 against it. It flagged the reversal unprompted so this lane would know which of its verdicts moved.)* **Why it is urgent: Step 107 is an OPEN TODO that will edit these exact rules, and instructs a builder to fix the pills *"by re-deriving the full set from `src/db/items.ts`, not by patching the four known gaps."*** ⚠️ **The rule that makes the colours CORRECT is written down only in the file we are about to archive: AMBER means an EXPECTED DOWNSTREAM CONSEQUENCE of another light's fault, never an independent fault** *(the one live case: the poll light during an M1 halt, because the halt itself called `stopPolling`)* — **and a genuine wedge, poll stale with the breaker NOT tripped, MUST stay RED.** 🔑 **It is the presentation-layer mirror of `decideHealthStatus`'s halted-beats-wedged precedence; re-derived wrong, ONE halt reads as TWO faults on the operator's board — and the doc records that this was already gotten wrong once and caught by QC as a self-contradiction.** **→ fold into Step 107's body, the same move that resolved the AC2 fast-follow brief for Step 94. Sprint edit, Justin-gated.**
  > ✅ **Two claims DROPPED from the list, both adequately homed** *(verified at source, not inherited)*: the **startup-grace rule** — *"never-polled ≠ wedged until uptime exceeds the threshold"* — has **three** homes (Step 78's body, `getPollLiveness`'s `uptimeSeconds` parameter, `atlas/07-safety.md` invariant 5); and the **"no Railway health-check is configured"** fact is in Step 78's body with its forward-risk conditional attached.
  > ✅ **CORRECTED AND LANDED 2026-09-01T16:52 — IT WAS ONE REHOME, NOT TWO, AND IT IS DONE.** 🔴 **Step 107 turned out to be a DIFFERENT COLOUR SYSTEM ENTIRELY — the twelve `ItemStatus` pills on the Product tab, not the five `/health` lights.** *(`AC0-37h3b` caught this on Justin's ruling; both the response-body contract and the amber-means-expected-consequence rule went into a SINGLE `ARCHITECTURE.md` entry.)* **The contract was verified at source against `src/safety/health.ts` rather than taken from a summary.** ⚠️ **This lane relayed the two-rehome version to Justin; it was wrong and the correction is his to see.** ✅ **RETIRED 2026-09-01 — MOVED to `docs/sessions/historical/2026.07/`. The hold worked exactly as intended and then released cleanly.**
  > **The sequence is worth keeping, because it is the pending-commit rule paying for itself in one afternoon:** **at 16:48 this doc read as cleared, and this lane REFUSED to move it** — `ARCHITECTURE.md` was modified-but-uncommitted, so the rehome existed in one working tree and not on the shared record. **At 17:14 Justin committed `1816850` ("Nine ballot rulings executed"), and BOTH halves landed.** ✅ **Verified at source before moving, with a control in the same run:** the `/health` response-body contract is at `ARCHITECTURE.md:1258` *(the `checks` object, plus the unauthenticated-endpoint caveat at `:1267`)*, and the colour rule is at `:1270` — ***"RED means a fault; AMBER means an EXPECTED DOWNSTREAM CONSEQUENCE of another light… re-deriving it wrong makes one halt read as two independent faults, and an operator then chases a phantom at 3am."***
  > ⭐ **Had this been moved at 16:48 it would have been retired against a destination that did not yet exist on the shared record. Waiting 26 minutes cost nothing and the rule cost nothing to apply.**
- `2026.07.22-ac0-ac0-handoff.md` — **Soren's unsettled v0.3 batch 3.** See §4.6.
- ✅ **`2026.07.22-ac0-ac2-fast-follow-brief.md` — CLEARED TO RETIRE 2026-09-01 by `AC0-37h3b2`, verified at source.** *(Was: "SPRINT Step 94 cites its §9 for a rebuild recipe owed at the `CLUSTERING_MODE` flip — a FUTURE gate, so the citation is live.")* **The condition is met: the full recipe was LIFTED VERBATIM into Step 94's own body — the block headed *"🛠 HOW TO RUN THE NESTED-RELAY LIVE CHECK — rebuild from this"* — and the wrong `§9` citation is gone. Commit `14d8faf`.** **Everything else in the brief is rehomed or tracked** (Steps 90/93/71/97/102, `ARCHITECTURE.md`'s Record-legibility block, `atlas/01-agent-core.md` §4.10, Step 35). **Sole residue: §10.1's two stale REMOTE refs, which are a push op nobody is here for.** ⇒ **Move it on Justin's go.**
- 🔴 **`2026.08.04-ac0-ac5-silent-email-drop-brief.md` (462 lines) — HOLD. The question below was too narrow, and the file is holding FIVE untracked things, one of them a silent mail-loss defect.** *(Was: "did its two generalizable rules reach `LEARNINGS.md`?" — worked 2026-09-01 by `AC0-37h3a2` from this side, then extended UNSOLICITED by `AC0-37h3b2` from the product side. Neither half found all five.)*
  > **⚠️ FIRST, THE PREMISE WAS WRONG: this lane produced FOUR-PLUS generalizable rules, not two.** ✅ **One LANDED richly** — *"reading cannot find an ABSENCE; only breaking things can"* is in `LEARNINGS.md:131` with full provenance, in `ac.md:286`, and in `qc.md`. ⚠️ **But that entry names its own still-open promotion — the orchestration protocol's Review-depth section, and whether a QC brief should require an execution receipt by default — marked *"still open for `#sweep`,"* and `#sweep` last ran 2026-07-24.**
  > **❌ THREE RULES NEVER LANDED** *(instrument: `git grep -a` over `LEARNINGS.md`, `qc.md`, `dc.md`, four distinctive phrasings each, with a known-true control in the same run that returned 12 hits — **so the search ran; this is still not proof of absence, since nobody has read all 1,106 lines of `LEARNINGS.md`**)*:
  > 1. **PAIR-SYMMETRY** — *a property applied to one sibling and not the other*; **four separate instances in this one lane**, and its own set-down calls it structural: *"this lane produces paired artifacts by design, and each new one gets its shape mirrored while its properties don't."* **The countermeasure that worked: assert siblings in ONE block so an asymmetry is visible at review time.**
  > 2. 🔴 **THE FAIL-SAFE-DEFAULT RULE — and it is currently PROTECTING LIVE CODE FROM BEING "FIXED."** *"A fail-safe default is right when the failure would otherwise be SILENT, and WRONG when it converts a LOUD failure into a quiet one."* **They built a bound on `PORT`, then deliberately reverted it: `server.listen(NaN)` throws naming the bad value AND fires `notifyDev`, whereas bounding it would silently bind 3000 and leave a Railway health check failing with nothing explaining why.** ⚠️ **`config.ts` and the test header carry anti-re-filing notes; the REASONING lives only in this doc. Protected by prose only — no suite can enforce it.**
  > 3. **THE CLOCK-MISMATCH PATTERN** — *"a durable bookmark measured in OUR clock, consumed by a query measured in THEIRS; the tell is that the safety margin's own comment names the gap it absorbs, which reveals the design assumed that gap stays small."* ⚠️ **The brief says QC *"promoted this as a standing pattern."* It is not in `qc.md` — confirmed independently by both lanes.** 🔑 **Per `ac.md`, a QC pattern survives only if an AC carries it into her manual; she cannot write it herself. This one was never carried, so it dies with the file.**
  > **🔴 AND TWO UNTRACKED DEFECTS `AC0-37h3b2` FOUND THAT THIS LANE DID NOT** *(it read the file because its own brief's §1.4 rated it a PARTIAL with the note "the lane is closed and its findings landed" — which was wrong)*:
  > 4. 🔴 **THE TWO-CLOCKS BUG — a concrete silent-mail-loss scenario, in NO sprint Step.** `getNewestItemCreatedAt` returns **our INSERT time**; `deriveCatchUpPlan` turns it into a Gmail **RECEIVE-date** search; the 1-hour margin is sized for that gap. **Steady state the gap is seconds; during the recovery drain the mechanism exists for, it is DAYS.** **Failure: process down 3 days → cold start drains the backlog inserting rows stamped TODAY → crash mid-drain → the next bookmark is now today, so it scans `after: today − 1h` and the undrained 3-day-old remainder is outside the window and never re-fetched. Silently gone.** ⚠️ **`SPRINT` Step 114 would fix it incidentally but does NOT name it** ⇒ **descope or partially implement Step 114 and this goes with it, with nobody knowing it was ever a reason. A defect covered only as a side effect of an unbuilt Step is not tracked.**
  > 5. ⚠️ **A TEST RED SINCE 2026-07-22 — SIX WEEKS — with a diagnosed one-line cause and no open Step.** `tests/test-evryn-initiated-placeholder.ts` asserts an error naming `supabase_upsert`, removed in Step 57; the runtime correctly says `create_user`. **A stale assertion, not a runtime defect.** 🔑 **Why nobody notices: it needs live DB credentials to run at all, so it sits OUTSIDE the 65-suite green run — the suite reports clean while a known-red test is simply not in it.** **It appears only in Step 96's CLOSED body and a May changelog: a record, not a tracker row.**
  > 📌 **Weaker, but recorded:** a **Justin instinct** on true-cold-start behaviour — *"rest the cursor at the profile `historyId` and ingest nothing, rather than an arbitrary 10"* — with a research verdict explicitly promised to land in that log. **It never landed, and `atlas/02-email-ingest.md` confirms the latest-10 fallback is still in the code.** **An open decision with a dangling promise.**
  > ### 🔴 CORRECTIONS, 2026-09-01T16:25 — the list above is FIVE items and only THREE survive. Read this before acting on any of it.
  > - ❌ **ITEM 5 (the "six weeks red" test) IS FALSE. It was FIXED 2026-08-05, the day after the finding was written.** **Commit `376853c`, *"test: fix the placeholder assertion that named a tool removed in Step 57"* — independently verified by this lane, not taken on report: the commit is an ancestor of `main`, and `tests/test-evryn-initiated-placeholder.ts:128` asserts the `create_user` string today.** ⚠️ **This lane relayed it to Justin as a live finding. It was not.**
  > - ✅ **ITEM 4 (the two-clocks defect) IS REAL — verified at source by `AC0-37h3b`** *(`src/db/items.ts:286` returns our INSERT time; `src/email/client.ts:252` feeds it into Gmail's `after:`, which filters on the message's OWN date)* — **and it is now NAMED IN `SPRINT` Step 114's body with its mechanism, instrument and incidence boundary.** ⇒ **Captured in the tracker; it no longer depends on this file.**
  > ⇒ **RELEASE CONDITION, NARROWED: only the THREE generalizable rules remain uncaptured** — pair-symmetry, fail-safe-default, clock-mismatch. **Two are on Justin's ballot now; the product lane expects to land them shortly. HOLD the file until it confirms.**
  > ### 🔑 AND THE MIRROR RULE, WHICH THIS LANE LEARNED THE EXPENSIVE WAY — it cuts OPPOSITE to the one above and both are true
  > **The rule this lane sent the product lane:** *a closed lane's brief is where routed-but-UNTRACKED work hides, because the closure reads as completion.*
  > 🔴 **THE MIRROR, sent back: a closed lane's brief is where ALREADY-FIXED work hides, because THE FINDING READS AS STILL-OPEN.** **Two of that file's five items were in the second category.** ⚠️ **This lane surfaced item 5 to Justin as live because the brief recorded it as owed and nobody had re-checked — which is the same unexamined-inheritance failure §6.4 warns about, aimed at a FINDING rather than at a `VERIFIED AT SOURCE` label.** ⇒ **AN ITEM'S AGE IS NOT EVIDENCE IT IS STILL OPEN. Re-checking item 5 took one `git log` and one `grep`. Do that before reporting any owed item as live.**
  > 🔑 **THE TRANSFERABLE LESSON, AND IT NOW GOVERNS THE REST OF THIS JOB: a CLOSED lane's brief is exactly where routed-but-untracked items hide, BECAUSE THE CLOSURE READS AS COMPLETION.** ⚠️ **"The lane is closed" is not evidence its routed items were tracked — and an inherited "low-yield" label has now been wrong TWICE in one day.** ⇒ **Any remaining file that is a closed lane's brief carrying a *"still owed"* or *"routed to AC0"* section goes to the product lane before it is filed. That section is the one that does not travel.**
- 🔴 **`2026.08.04-ac0-aca-atlas-refactor-brief.md` (549 lines) — DO NOT RETIRE. CONFIRMED A LIVE SPEC 2026-09-01 by `AC0-37h3b2`, which read all 549 lines.** **Three of its four deliverables shipped** (dependency map, claims audit, Code Atlas — all merged). 🔴 **The fourth has not: the `classify.ts` extraction — which `current-state` still names the critical path and which FIVE open sprint Steps are sequenced against (100, 101, 110, 98, and the §2.5 canary requirement).** **Two things live ONLY in this file:**
  > 1. 🔑 **THE PROPOSED SEAM DECOMPOSITION — five modules, sequenced lowest-risk-first, with the argument for the ordering** *(`rendering.ts` → `stamps.ts` → `validators.ts` → the 16-tool MCP surface → `identity.ts` last)*. **The load-bearing part is WHY steps 1–2 come first: `verdict.ts` and `preload.ts` import only pure functions from the god-module, so those two steps sever both files from it completely — shrinking the blast radius AND attacking the 7-file import cycle, at the lowest risk in the plan. The cheapest extraction is also the highest structural value.** **Confirmed present in no other document — not the sprint, not the Atlas, not `ARCHITECTURE.md`.**
  > 2. 🔴 **A LIVE, CONDITIONAL, OWNED REQUIREMENT WITH NO STEP BEHIND IT** — Justin's ruling of 2026-08-10 that the **top-of-file truncation-canary sweep must RIDE the extraction**, because inserting one line at the top of every source file shifts **1,096 `file:line` references across 65 files**, and doing it standalone introduces a systematic off-by-one across the corpus. ⚠️ **Plus its own escape clause: *"if the extraction is ever cancelled or indefinitely deferred, the top half becomes owed again as its own trip — flag it rather than letting it die with the lane."*** **Nothing in either sprint tracks this.** ⇒ **Retire this file and that requirement dies silently with it.**
  > ⇒ **VERDICT: it needs a REAL HOME, not `historical/`.** ⚠️ **AND IT COUPLES TO §3 🅳's NAPKIN — see the note there.**

### 🅱 The ACh P-queue — 3 files

> # ✅ THE QUEUE IS DISCHARGED, 2026-09-01. ALL ELEVEN ITEMS ARE CLOSED, ROUTED OR TRACKED.
>
> *(Worked by `AC0-37h3a2` at source. **This brief only ever discussed six of the eleven** — P2 through P8 — so five had never been checked by anyone in this lineage. **There is no P5; the queue runs P1–P4, P6–P11.**)*
>
> | Item | Verdict |
> |---|---|
> | **P1 — the operator guide was wrong about `/health`** | ✅ **CLOSED — FIXED SINCE, and better than the alarm.** It claimed the guide still showed the pre-Step-78 shape, documented `HEALTH_WEDGED_THRESHOLD_MS` nowhere, and never stated the `/health` contract. **All three are now false:** the full shape including the `checks` block is at `operator-guide.md:140–141`, the threshold appears **3×**, and `:161` even carries a careful reading note that `checks.slack_socket` does not change the status word. |
> | **P2 — six un-rehomed items** | ✅ **CLOSED** — verified 2026-08-22; three QC patterns already in `qc.md`, one declined by Justin, one committed. |
> | **P3 — a `liveness.ts` comment repoint** | ✅ **CLOSED** — commit `789f7d8`. |
> | **P4 — two research docs to relocate** | ✅ **RESOLVED** — one is already in the team research folder *(filename lost an `acu` in the move, which is why searches missed it)*; the other is the product lane's consult. |
> | **P6 — delete the `ac2/step57` branch** | ✅ **DECIDED — KEPT.** Reasoning durable in `repo-inventory.md`. |
> | **P7 — outstanding pushes** | ⚠️ **HALF CLOSED, HALF NOW BIGGER — see the flag below.** |
> | **P8 — Soren's unsettled batch** | ✅ **HOMED 2026-09-01** *(a new research doc, breadcrumbed from the v0.3 build doc and Soren's memory)* — ⏸ **pending commit.** |
> | **P9 — three unanswered questions** | ✅ **CLOSED.** Its two named stale lines in `current-state.md` are **gone** *(0 hits)*; the branch question folded into P7; the ACU product-radar doc is retired and its one open finding is tracked as `SPRINT` Step 92. |
> | **P10 — two process flags** | ✅ **NOTHING OWED** — explicitly *"read once; no action."* Both are records: a skipped auth step on two team-memory writes *(verified no damage — canaries intact, additions only)*, and a resolved contradiction in a retirement count. |
> | **P11 — canaries missing from 30 of 44 runtime files** | ✅ **TRACKED, and it has a real home:** this is the same sweep Justin ruled must **RIDE the `classify.ts` extraction**, because inserting one line atop every source file shifts 1,096 `file:line` references. **That ruling lives in the Atlas-refactor brief** *(§3 🅰)*. |
>
> ⇒ 🔴 **ALL THREE OF THIS GROUP'S FILES RELEASE THE MOMENT P8's HOME COMMITS.** **Nothing else holds them.**
>
> ### ⚠️ THE ONE LIVE THING P7 THROWS OFF, AND IT IS NOW MUCH BIGGER THAN WHEN IT WAS WRITTEN
> ✅ **The branch alarm is dead** — `ac5/pre-refactor-quicklist` no longer exists locally or remotely.
> 🔴 **But the push question is live and has grown by an order of magnitude.** **When written: `evryn-backend` 4 ahead, `_evryn-meta` 8 ahead. Measured 2026-09-01: `evryn-backend` 3 ahead, `_evryn-meta` FIFTY ahead, `evryn-team-workspace` 0.**
> ⚠️ **AND THE REASON IT IS JUSTIN'S AND NOT A HOUSEKEEPING CALL, carried verbatim from P7 because it is easy to miss:** ***"`_evryn-meta`'s push IS the Vercel dashboard deploy. Pushing that repo deploys the dashboard along with the whole backlog. A Justin-gated act, not incidental."***
> ⇒ **Fifty commits of unpushed work sit on ONE MACHINE, and the act that makes them durable is also a deploy.** **Not this lane's to perform. Surfaced to Justin.**

**`2026.06.30-ac0-ac3-identity-ops-brief.md`** · **`2026.08.04-ach-justin-housekeeping-recommendations.md`** · **`2026.08.06-ac0-ac0-handoff.md`** *(`current-state:183` names it the queue's home)*

> ✅ **A BLOCKER ON THE FIRST OF THOSE THREE IS CLEARED, 2026-09-01.** **`2026.06.30-ac0-ac3-identity-ops-brief.md:185` says *"Step 78 HALF B — BUILT + QC-GO; ⛔ HELD BY JUSTIN — NOT merged, NOT routed to AC0, NOT deployed."*** 🔴 **THAT HOLD IS STALE — lifted 2026-07-20, forty-two days before this lane read it.** **`AC0-37h3b2` re-ran the git rather than repeating its predecessor's claim: the merge commit `5cc6f28` (2026-07-20 15:19) says *"hold lifted 2026-07-20"*; the branch tip `7bffd0d` is an ancestor of BOTH `main` and `origin/main`; the deploy is recorded at `de9c079`, 105 seconds later; the branch is reaped.** ⭐ **The push is the load-bearing check, not a bonus — Step 78's own body records that for Vercel the PUSH is the deploy, so ancestry of `origin/main` is direct evidence the deploy trigger fired.** ⚠️ **Honest boundary it stated rather than papered over: these prove merged-pushed-and-recorded-as-deployed; they do NOT prove Vercel's build succeeded, which no git command can settle.** ⇒ **No live hold. Nothing at risk. The file is not blocked by this** *(it is still held by the P-queue itself)*.
> 🔑 **AND WHY IT WENT STALE IS THIS LANE'S OWN THESIS AGAIN: the hold was lifted in the SPRINT and in the changelog, and nobody went back to the brief that announced it.** **A hold is released where the work is TRACKED; the document that first announced it is exactly what nobody updates.**

**P2 was recorded as five unplaced items. It is ONE, plus a decision** — verified with a control 2026-08-22:

- **(ii)(iii)(iv) — the three QC patterns: ✅ ALREADY IN `qc.md`.** `AC0-37i` landed them 08-21; `ACx` grepped them absent on 08-19 and its verdict went stale.
- ✅ **(v) — the "clean up as you go" rule: DECLINED by Justin**, because `ac.md:474` already carries it in substance. **An extra rule in the always-loaded manual costs every AC, every session, forever.**
- ✅ **(vi) — the line-count-drift lesson: WRITTEN, VETTED AND COMMITTED** *(`646f51b`, in `ac-orchestration-protocol.md` beside the line-span instruction it qualifies)*.

⚠️ **The other two files are held by the WHOLE queue, not just P2.** **Answered 2026-09-01 by `AC0-37h3b2` — ⚠️ both REASONED from docs it has read in full, NOT verified against the `ACh` P-queue doc itself, which it has not read. Treat as strong leads.** ✅ **P3 — the `liveness.ts` comment repoint: CLOSED**, landed as `789f7d8` (2026-08-20), *"Repoint the /health design comment at its durable homes."* ⭐ **With a sting worth carrying, because it is this job's own defect class: that same commit added header lines and thereby BROKE the Atlas's citation INTO `liveness.ts` — a mutual citation created and half of it invalidated in one act. Both drifted citations were repaired in `atlas/07-safety.md` on 2026-08-22.** ✅ **P5 — the Gate-B suite's BUILD half: OPEN, and already tracked in three non-session-doc homes** — `SPRINT` Step 88 (the substance), the sprint's 🚦 GATES section (blocking Steps 43, 82, 99), and `evryn-backend/docs/gate-b-plan.md`. ⇒ **It needs no lifting out of anything.** ✅ **P4 — WORKED 2026-09-01 by `AC0-37h3a2`, and it is SMALLER THAN THIS BRIEF SAID. It is not one doc; it is TWO, and one of them is already home.** *(This brief said "a research doc went to `historical/` instead of the research folder — his call." Both halves of that were incomplete.)*
> - ✅ **`2026.07.13-acu-outreach-engine-research.md` IS ALREADY IN THE RIGHT PLACE** — `evryn-team-workspace/shared/projects/product/research/`, exactly where Justin ruled it should go. 🔑 **The reason nobody found it: the filename DROPPED ONE `acu` in the move** *(`acu-acu-…` → `acu-…`)*, **so every search for the name recorded in the `ACh` doc misses it.** ⇒ **Nothing owed. Do not re-move it.**
> - ❌ **`2026.07.13-acu-ac0-step53-identity-loading-research.md` IS the real divergence** — it sits in `docs/sessions/historical/2026.07/` rather than the research folder. 🔑 **AND IT IS THE SAME ITEM AS ACP CONSULT ①, WHICH THE PRODUCT LANE HAS TAKEN.** ⇒ **P4 collapses to that one doc, and it is NOT this lane's.** **Stop tracking it here.**
> - ✅ **THE CATCH THAT STOPPED `ACh` IS DISSOLVED.** It parked P4 because both docs were referenced by `2026.07.14-acu-acu-self-brief.md`, *"an ACU document Justin says STAYS, because that lane is live"* — so a move would break ACU's master doc. **That self-brief is now in `docs/sessions/historical/2026.07/` itself.** ⇒ **It is a retired session doc, and under the no-repointing rule its stale pointer is not a bug and is not fixed. The blocker was real when written and is gone.**
> ⚠️ **AND A THIRD INSTRUMENT FAILURE, CAUGHT ONLY BY A CONTROL IN THE SAME RUN — worth more than the finding.** **A `find` across all repos returned NOTHING for the outreach doc, which read as *"the file does not exist anywhere."*** **The same `find` also returned nothing for its sibling — a file `git ls-files` had just listed.** ⇒ **The instrument was broken, not the filesystem.** **Without the control this lane would have reported a research document as LOST.** 🔑 **`ac.md`'s rule — *"before believing what a search did not find, confirm the search actually RAN"* — caught a false-negative for the third time today, and this one would have been the most alarming.**
>
> **Still open and NOT product: P6** — 🔴 **REVERSED 2026-08-22: that branch is NOT to be deleted, and the reasoning now lives in `docs/repo-inventory.md` → *Deliberately Preserved Branches*.** *(Three of its six SHAs are cited in committed docs; the WORK is fully landed, the CITATIONS are what is at risk.)* ⚠️ **`current-state.md` carries a one-line breadcrumb. Do not re-derive this — it has nearly been executed twice.** · **P8** *(Soren's batch — §4.6)* — ✅ **WORKED 2026-09-01 by `AC0-37h3a2`, read at SOURCE in `soren/MEMORY.md` rather than from any summary** *(which is the whole lesson of this item: `ACh`'s recommendation was built on a summary and would have done the exact thing Soren wrote a line to prevent)*.
> 🔑 **THE ROUTE IS ALREADY DONE, AND DONE CORRECTLY. What is missing is the ANSWER.** **`ACh`'s note sits in Soren's memory, clearly marked *"Not written by Soren"*, and it states the needed decision in his own terms:** *"that handoff can't retire while it is the only home for batch 3. If those items are still live design, they likely want a durable home that PRESERVES the unsettled framing (not a source-of-truth doc); if any have since been settled or dropped, saying so would release the handoff. Your call, not mine."*
> 🔴 **AND THE FACT `ACh` COULD NOT HAVE SEEN, WHICH IS WHAT MAKES THIS ACTIONABLE: SOREN HAS RUN SINCE AND HAS NOT ANSWERED IT.** **His memory carries a `#lock` entry dated 2026-08-31** *(the paradigm-and-pricing session)* — **so the note has been in front of him for ~4 weeks across at least one full session, and batch 3 is untouched.**
> ⇒ **This does NOT need re-routing, and writing it into a mailbox would be wrong twice over** *(the route exists, and Justin has ruled that mailboxes are not how the founding team is reached)*. ⇒ **It needs JUSTIN to ask Soren the question, or to rule it himself. That is the entire remaining action.** ⚠️ **Do NOT sweep the batch. Soren's own text forbids it verbatim:** *"⛔ NOTHING IN THIS THIRD BATCH IS SETTLED… Consequence if you sweep it: you'd be building against a design that was still moving when it was written down."*

### 🅲 Held by a LIVE INSTRUCTION — 2 files

🔑 **This is the class the no-repointing rule does NOT cover, and the distinction is the whole point: a stale PATH is fine; a doc named as a REQUIRED PRE-READ is an instruction, and it must resolve.**

- **`2026.08.12-qc-sweep-findings.md`** — `product-full-cascade.md` names it a required pre-read. ⚠️ **Source-of-truth edit → Justin.**
- ⏳ **`2026.07.21-ac0-ac3-cleanup-batch-brief.md`** — **SPRINT Step 50 routes work to it and that Step is still TODO** *(re-verified at source 2026-09-01 by `AC0-37h3b2`: Step 50 reads TODO and sits in the LIVE region's section 3)*. ⭐ **But unlike the AC2 fast-follow brief, Step 50's own body already describes the work self-sufficiently** — *"compare what identity files say against what the runtime actually exposes; remove operational redundancy, reclaim tokens for voice/judgment. The `createUser` gap was exhibit A."* ⇒ **The pointer looks like convenience rather than the only copy.** 🔴 **BUT DO NOT RETIRE IT, AND STEP 50 IS THE LEAST OF WHY.** *(Verdict 2026-09-01 by `AC0-37h3b2`, from reading all 51 lines. The question "is Step 50's pointer the only copy?" was the wrong question — Step 50 is not the only thing in the file.)* **THREE things are in it:**
  > 1. ⚠️ **Step 50's own residue — MODEST, roughly as this brief guessed.** The Step's body already carries the scope note and the `createUser` exhibit. **Only the brief has** the exact runtime seams to compare identity prose against (`buildPersonContext`, the MCP `tool(...)` description strings, `buildForwardedEmailPrompt`, the handoff prompts in `process.ts` / `poll.ts`), one concrete probe question — *"she is told the runtime owns history; is she still instructed to fetch it anywhere?"* — and the output shape (a says-vs-exposes list per `file:line`, with Mira voicing any identity edit).
  > 2. 🔴 **STEP 70a — A LIVE SECURITY ITEM WHOSE ONLY MECHANISM IS IN THIS BRIEF, AND WHOSE TRACKER ENTRY POINTS AT IT.** **`SPRINT` Step 70's status is `MOVED`, which in that doc means *"still worth doing, tracked in ANOTHER DOCUMENT — the Step names which."* The document it names is this one.** **The substance: a user's `pending_notes` render into Evryn's `systemPrompt`, so if she notes an untrusted email's content VERBATIM while triaging, an embedded prompt injection is laundered INTO THE TRUSTED LAYER, past the untrusted-content fence — and Reflection can then fold it into the durable `story`, which is also in the systemPrompt.** `reflection.md` carries the consolidation-side half of the defence; **the note-WRITE side is the gap**, and the intent is two independent chances to strip an injection. 🔴 **Archiving this file under the no-repointing rule turns a live security item's only home into a historical document while its tracker entry still says "tracked elsewhere." Same shape as the Atlas-refactor brief, with a security item in it instead of a refactor spec.**
  > 3. 🔴 **AN UNTRACKED ITEM THAT IS WRONG IN PRODUCTION-FACING PROSE RIGHT NOW** *(verified at source by `AC0-37h3b2`, not taken from the brief)*: **`evryn-backend/docs/gatekeeper-onboarding.md` §5, line 58** still reads *"the clustering delivery model is **built but not deployed** … hit a ship-blocker as of Round 17,"* and instructs the operator ***"Do not promise the twice-daily package to a gatekeeper until clustering is live."*** **Clustering SHIPPED `v0.2.7` on 2026-07-10 — deployed, clean boot, dormant at `CLUSTERING_MODE=off`; the Round-17 blocker was fixed and live-verified before that deploy.** ⇒ **A runbook describing a state resolved 53 days ago, instructing the operator to UNDER-PROMISE — against Justin's recorded intent that a gatekeeper should only ever experience the clustered model.** ⚠️ **Do not over-correct: the true state is DEPLOYED BUT OFF, flip pending at onboarding — not "live."** 🔴 **NOT OURS AND NOT THE PRODUCT LANE'S TO TOUCH — it is the operator-facing identity-adjacent file at the centre of `SPRINT` Step 121's open ownership question. ROUTE IT; do not edit it.**
  > ✅ **LEG 2 DISCHARGED 2026-09-01T16:25 — `AC0-37h3b` LIFTED Step 70a's full mechanism into `SPRINT` Step 70's own body**: the defect, the `pending_notes` → `systemPrompt` laundering path, the two-pass defence with `reflection.md:18` quoted verbatim as the half that already exists, the note-write side named as the gap, and the ownership boundary *(route the wording to Mira; never fold it into a build)*. **The Step no longer points into `docs/working/`.**
  > ✅ **LEG 3 DISCHARGED 2026-09-01T16:52 — AND THIS LANE HAD IT WRONG IN TWO WAYS, BOTH WORTH RECORDING.** 🔴 **(i) It is NOT Evryn's identity layer and was never Mira's to route.** **`docs/gatekeeper-runbook.md` is the OPERATOR's runbook — AC-maintained, Justin-vetted.** ⚠️ **This lane recommended routing it to Mira. That was the same two-files-one-name confusion that cost the original round-trip, committed again.** 🔴 **(ii) THE FILE IS RENAMED: `evryn-backend/docs/gatekeeper-onboarding.md` → `evryn-backend/docs/gatekeeper-runbook.md`** *(Justin's ruling: "those can't have the same name — that's going to be a massive ongoing tripping hazard")*. **The IDENTITY module `identity/activities/gatekeeper-onboarding.md` KEEPS its name — that is the whole point of the rename.** **All four live SPRINT references and `current-state.md:94` are repointed; frozen records and `docs/working/` deliberately left alone.** ✅ **And the §5 text is FIXED: it now reads DEPLOYED BUT OFF — a flip you perform at onboarding, deliberately not "live," which would be a second wrong claim in the other direction.**
  > ⇒ **RETIREMENT CONDITION, NARROWED TO ONE: Step 50's runtime seams lifted into Step 50's body. The product lane holds it.**

### 🅳 GENUINELY STAY — 3 files, all bannered already, moving to `docs/sessions/`

`2026.07.19-ac-justin-restructure-napkin.md` *(the `classify.ts` extraction napkin — `current-state:64` holds it open as explicitly unsettled and to be re-derived)* — 🔴 **BUT SEE THIS, FOUND 2026-09-01 by `AC0-37h3b2` and it changes what this file is: the re-derivation `current-state:64` calls for HAS BEEN DONE. It is the seam decomposition inside `2026.08.04-ac0-aca-atlas-refactor-brief.md` (§3 🅰), produced from a live read of the runtime — and `current-state` does not mention that brief at all.** ⇒ **The one file every agent loads at startup routes the next extractor to the SKETCH and away from the DERIVATION.** **The `current-state` fix is source-of-truth and Justin-gated; the product lane is taking it to him.** ✅ **RESOLVED 2026-09-01T16:25 — AND THE NAPKIN STAYS EITHER WAY, so this coupling is closed rather than pending.** **Justin ruled YES on the re-aim and `AC0-37h3b` has made the edit: `current-state:64` now sends a next extractor to the Atlas-refactor brief and states the lane is LIVE and PAUSED rather than finished.** ⚠️ **It is UNCOMMITTED, awaiting his SCM vet — so on the shared record the old line is still what everyone reads. Treat the change as PROPOSED, not landed.** ⭐ **But the napkin's own status no longer turns on it: the product lane deliberately KEPT the napkin's reference, because Justin's instruction there was to re-derive from first principles and the napkin is the record of what he sketched.** ⇒ **It is no longer the thing a builder starts FROM; it is still the thing that says WHY. Group 🅳 unchanged** · `2026.07.28-acp-ac0-desktop-permissions-fix.md` *(re-opened open question; the desktop still needs the `z.archive/` move and the hook re-armed)* · `2026.08.17-ac037b-acp38-marching-orders.md` *(ACP-38 not spun)*

⚠️ **Move these LAST, with the final commit.** **Two are pointed at from `current-state.md`'s "Recent handoff pointers"** — *"the single most consequential line in this file"* by its own account, wrong twice before. **Moving them early opens a window where the line that routes a fresh AC is wrong.**

### 🅴 Retire at close — 2 files

- **`2026.08.19-ac037f-acx-working-docs-sweep-lane.md`** ⚠️ **Per `doc-update-routing-protocol.md` change-type 8, exclusion 2: MOVE it, but NEVER repoint a path inside it.** A document whose value is that it captured a before-state stops carrying it the moment its paths are updated.
- ✅ **`2026.08.20-ac037g-ac037h-handoff.md` — THE CHECK IS DONE, 2026-09-01 by `AC0-37h3a2`. Almost everything is carried or closed; ONE thing is not.** *(The instruction was "confirm nothing in §§9–12 is still un-carried." The live owed material is actually in **§7.10 and §8**, which is what was read.)*
  > ✅ **§7.10 B — DISCHARGED.** **B1 landed as `abd532f`** *(the `ARCHITECTURE.md` Identity-Composition block + `SPRINT-v0.2-optionals.md` Step 120)*; B2 and B3 were already done 2026-08-21.
  > ✅ **§7.10 C — LANDED THE SAME DAY IT WAS FLAGGED.** It said *"three environment facts live only in docs you are about to retire; `ac.md` does NOT contain these."* **They are now in `ac.md` (the boxed *"THREE THINGS ABOUT THIS MACHINE'S TOOLING"* block), placed there 2026-08-21 at Justin's direction, explicitly because they were surviving only in `docs/working/`.**
  > 🔴 **BUT §8.4(b)6 IS THE ONE GENUINELY UN-CARRIED ITEM, AND IT IS STILL OPEN: THE LIST OF THREE IS INCOMPLETE.** **Readers reported the true count is higher, and the instruction was *"re-run the §7.10 C check against the FULL list, not the three it names."* Nobody did.** **The named candidates:** the **two machine roots** *(desktop `/c/Users/Justin/…` vs laptop `/c/Users/jbmcg/…`)* · **`MSYS_NO_PATHCONV=1` path mangling** · **`git worktree remove` leaving an empty folder on Windows** · **no `python` on this machine** · **`core.autocrlf` normalising before a byte comparison** · **CRLF line endings in team `MEMORY.md` files that `Edit` chokes on.** ⚠️ **This lane hit the CRLF one repeatedly on 2026-09-01 — every mailbox commit warned `LF will be replaced by CRLF` — so at least one candidate is live and reproducible today.** ⇒ **Same class as the three that landed, same fix: propose them for `ac.md` BEFORE the folder retires. Source-of-truth, so Justin-gated.**
  > ### ✅ VERIFIED 2026-09-01 by `AC0-37h3a2` — the candidate list is NOT adoptable as written. Three survive, one is FALSE, two are undetermined.
  > **This is why the re-run was owed rather than optional: a stale gotcha is worse than a missing one, because it teaches an agent to distrust a working tool.**
  > - ✅ **CONFIRMED — TWO MACHINE ROOTS.** Desktop `c:/Users/Justin/…` vs laptop `c:/Users/jbmcg/…` *(`$HOME` on this machine is `/c/Users/jbmcg`)*. 🔴 **AND IT IS ALREADY BITING INSIDE THE ALWAYS-LOADED MANUAL: `ac.md:261` gives a worked worktree example using `c:/Users/Justin/Evryn/Code-ac/…` — a path that does not exist on this machine, in an example an agent would copy.** **That is a live defect, not just a missing gotcha.**
  > - ✅ **CONFIRMED — `core.autocrlf=true`**, so git normalises line endings before any byte comparison. **This is the source of the `LF will be replaced by CRLF` warning on every commit in this repo.**
  > - ✅ **CONFIRMED — ALL EIGHT team `MEMORY.md` files are CRLF**, at 62–358 CRLF pairs each *(`dominic` 110 · `emma` 204 · `lucas` 197 · `marlowe` 261 · `mira` 332 · `nathan` 358 · `soren` 187 · `thea` 62)*. **Checked by byte with `node`, not by `file(1)`.**
  > - ❌ **FALSE — *"no `python` on this machine."*** **`command -v python` resolves.** ⇒ **Do NOT carry this one forward.**
  > - ⚠️ **UNDETERMINED — `MSYS_NO_PATHCONV` path mangling.** The variable is unset here, which neither confirms nor denies the gotcha *(the claim is about needing to SET it)*. **Not proposable without a real reproduction.**
  > - ⚠️ **UNDETERMINED — `git worktree remove` leaving an empty folder on Windows.** **Would require performing the operation to test; not worth doing gratuitously.**
  > 🔑 **AND A LIVE SPECIMEN OF THE VERY CLASS, produced WHILE verifying it — worth carrying because it is the cheapest possible demonstration:** **the first CRLF check returned NOTHING and read as a clean "no CRLF anywhere."** **The actual cause was a wrong relative path** *(`../evryn-team-workspace` run from inside `Code/`, which walks one level too far up)* — **the glob matched no files, so the loop body never ran.** ⚠️ **An empty result from a command that never found its target is indistinguishable from an empty result from a command that looked and found nothing.** ⇒ **The `ls` control is what caught it, in the same run.** **This is `ac.md`'s own "confirm the search actually RAN" rule, failing and being caught inside a check of the instrument-failure list itself.**
  > ✅ **§7.10 E — CARRIED.** Its three unruled questions *(the foreground-vs-background default now resting on nothing · the untested hold-banner aging window · `conductor-cascade.md:51` telling a scout to load a file that auto-injects)* **are already in this brief's §8.** Its fourth is closed.
  > ✅ **§8.3 items 2, 3, 4, 5 — closed or corrected**; `current-state`'s two bad citations were both fixed 2026-08-21 and the corrections are visible in the file today.
  > 🔴 **AND THE SPECIMEN, WORTH SEEING IN SITU BEFORE THIS FILE GOES: §8.3 is headed *"VERIFIED MECHANICALLY … these three you may rely on"* — and its item 1 is the FALSE label §6.4 of this brief warns about** *(it calls `SPRINT-V0.2-HARDENING.md:620` a redirect stub carrying no content; it is a 12-line entry carrying the full original Step 53 body)*. ⇒ **A false claim sitting under a heading that explicitly invites reliance. That is the whole reason §6.4 exists.**
  > ⇒ **RETIRABLE once §8.4(b)6's environment facts are proposed to Justin. Nothing else in it is un-carried.**

**Count check: 6 + 3 + 2 + 3 + 2 = 16.** ✅

---

# §4 — HOW YOU OPERATE. Justin's rulings, 2026-08-21/22.

**4.1 · COMMITS.** ✅ **Your own session docs and mailbox messages: commit freely.** ✅ **Source-of-truth docs: make the edit and commit** — *provided you end the session with a **specific list of the paths you changed**, so he can find them in the SCM timeline. He vets after the fact.* 🔴 **THE EXCEPTION: anything that changes HOW AGENTS WORK gets a PING and his SCM VET *BEFORE* you commit** — `ac.md`, the protocols, the load cascades. ⚠️ **He runs other ACs today; a process change landed silently reaches instances that never agreed to it.**

**4.2 · 🔴 `Write` AND `Edit` HANG ON ANY `.claude/` PATH.** They stall on an authorization prompt **Justin is never notified about** — indefinitely, killing all forward movement, with nothing to notice. ✅ **Use bash. TESTED AND CONFIRMED 2026-08-22.** **Two things learned doing it:** team `MEMORY.md` files are **pure CRLF** *(append with `\r\n`)*, and **a naive append lands BELOW the truncation canary, destroying its purpose — INSERT ABOVE IT.**

**4.3 · EDITING SOMEONE ELSE'S DOC — `A → (true A)`.** *"You can't rewrite, exactly — you can just update so it's actually true, so that it now says **what it was already meaning to say**."* **Fix a path so it resolves; record that a condition the author named has been met.** ⛔ **Never re-scope, sharpen, or improve the claim.** ⚠️ **If the TRUE statement differs from the INTENDED one, that is a ROUTE, not an edit.**

**4.4 · 🔴 OTHER LINEAGES' DOCS ARE NEVER YOURS TO RETIRE.** **The top-level lineages are `ACP` (yours), `ACT`, and `ACv`** *(ACv short-lived)*. **Banner it for its owner to evaluate, message that owner's inbox, move on.** 🔑 *"They've been doing this culling process too, so all of their stuff is actually in-flight."* **You cannot see their worktrees or unspun briefs.** ✅ **All 34 are already moved and bannered; ACT and ACv have been notified.** *(Now also written into `lock-protocol.md` step 10.)*

**4.5 · WHEN YOU REMOVE WORDING, DO NOT ARGUE AGAINST IT.** **The reader never sees what you deleted**, so a clause defending the change re-introduces the wrong idea into their head. **Just write the new instruction.** ⚠️ **One carve-out — a REDIRECT, never an argument: when the old wording still exists somewhere the reader will meet it** *(e.g. pre-08-20 docs say "canary" and mean the heartbeat)*, **one pointer earns its place.**

**4.6 · SOREN'S BATCH 3 — do not act on Justin's approval of the sweep.** `ACh`'s ballot recommended sweeping it into `BUILD-EVRYN-v0.3.md` **and Justin approved that** — then `ACh` **deliberately did not do it**, because Soren's own text forbids exactly that, and *"my recommendation had been built on a summary, not the source."* **The record lives only in `soren/MEMORY.md:129-137`,** which also names the decision needed: *"if those items are still live design, they likely want a durable home that PRESERVES the unsettled framing (not a source-of-truth doc)."* ⇒ **Route the question; do not decide it.**

**4.7 · Two naming traps.** The `acp` in `2026.07.28-acp-ac0-desktop-permissions-fix.md` is a **lane**, not this lineage. **`ACf` IS what became `ACT`.**

**4.8 · Peer etiquette** *(Justin's, for everyone running today)*: **don't bug each other · other people's emergencies are not automatically yours** ⇒ **so make any request you send CHEAP TO ANSWER, or it will correctly be declined.**

---

# §5 — WHAT IS LIVE THE MOMENT YOU ARRIVE

## 5.1 · The PRODUCT LANE — your consultant, and a PEER

⚠️ **Address the ROLE, not the instance.** `AC0-37h3b` was re-spun on 2026-08-22 at its context edge, so whoever answers you may not be the instance that wrote the entries in your inbox. **The address `docs/mailboxes/inbox-ac037h3b.md` does not change** — that is exactly why it is derived from the role.

**A separate instance, not a subagent** *(subagent resume is session-bound; as a sub it would have died with the last ACP and its ~350k product load would have to be re-paid)*. **Brief: `docs/sessions/2026.08.22-ac037h3-ac037h3b-product-extraction-brief.md`.**

**Channel PROVEN in both directions.** **You write to `docs/mailboxes/inbox-ac037h3b.md`; it writes to `inbox-acp.md`.** 🔴 **COMMITTING IS SENDING.** ⭐ **It has been told a fresh ACP may answer it and that this is planned — introduce yourself and carry on.**

- 🔑 **It is CONSULTING, not being tasked. It is product-loaded where you are not.** ⭐ **If it says your framing is wrong, it is probably right** — it corrected this lineage three separate times on its first day, including a `VERIFIED AT SOURCE` label that was false.
- 📌 **Standing rule it relies on: if you edit its brief while it is running, tell it in its inbox WITH THE DIFF COMMAND.** **Silence means its loaded copy is trustworthy.**
- ⚠️ **The mailbox files are the ONLY surface you both write to unprompted.** **Commit mailbox traffic PROMPTLY**, and `git diff <file>` before committing one.

## 5.2 · The tray — EMPTY. Everything is committed and pushed.

✅ **Nothing is held for a vet. `_evryn-meta`, `evryn-backend` and `evryn-team-workspace` were all clean and pushed at set-down.**

⚠️ **The ONE file that may show modified and is NOT yours: `docs/sessions/2026.08.22-ac037h3-ac037h3b-product-extraction-brief.md`.** **The product lane edits its own brief while it runs — leave it, and never `git add -A`.**

## 5.2a · ✅ EVERYTHING JUSTIN OWED IS ANSWERED. Do not re-ask any of it.

- ✅ **The EIGHT retirements: SIGNED OFF** *(commit `2af917e`)* — *"otherwise, looks good,"* with two carried notes, both now closed.
- ✅ **`ac-mailbox.md`** — he asked whether the participants posted a *release* or whether content was *trapped*. **Answered from the file itself: ACT (08-19) and ACv-2 (08-20) both posted explicit releases with "nothing owed."** Closed.
- ✅ **P2(v): he said YES to declining the duplicate. ACh's P2 is fully closed.**
- ✅ **P6: DECIDED — the branch is KEPT**, on the product lane's concurrence. **Reasoning in `docs/repo-inventory.md`; breadcrumb in `current-state.md`.** ⚠️ **Do not re-derive it — it has nearly been executed twice.**
- ✅ **`qc.md`, `repo-inventory.md`, `current-state.md` and the orchestration protocol: all vetted and committed.**

⏳ **The only two things genuinely still open with Justin are P4 and the `acm` recheck — and BOTH are waiting on the product lane first, not on him.** See §3 🅰 and §3 🅱.

> ### 🔴 A NOTE ON THIS SECTION ITSELF — it is the shape that wastes the most of Justin's time, and it has now bitten twice in one day.
>
> **An *"awaiting his answer"* line survives past the answer.** **The instance that wrote this section carried *"awaiting sign-off"* forward for an hour after he had given it, put it in this brief, and then had to be told.** ⚠️ **Its second form is worse: a section that lists work as UNCOMMITTED after it has been committed** — which reads as *"there is a tray"* when the tray is empty.
>
> ⇒ **Before you re-ask him anything, check whether he already answered. Before you report a tray, run `git status`.**

## 5.3 · Instruments

**Arm your own heartbeat and an inbox watcher at spin-up.** ⚠️ **NEVER inherit a task id — a `Monitor` dies with the session that armed it.** **Ask Justin the two separate questions** *(read the inbox? · arm a watcher?)*. **Anchor on `$HOME`; emit a proof-of-fire line BEFORE the loop; fire on COMMIT, using `git log --all`.**

---

# §6 — TRAPS. Every one of these cost real time TODAY.

**6.1 · 🔴 `git grep` does NOT accept `--binary-files=text`** — that is GNU `grep`'s flag; `git grep` uses **`-a`**. **A scan with the wrong flag errors to stderr and emits nothing, which reads exactly like a clean all-clear.** ⇒ **A 30-lookup sweep returned a false clean bill of health; re-run correctly it found ELEVEN live referrers.** 🔑 **Caught ONLY by pairing it with a KNOWN-TRUE control in the same run. Always do that.**

**6.2 · Bash `grep` ABORTS on some reads here** — not an error, not empty: aborted, printing exactly like a clean result. **When an ABSENCE matters, read the matching lines with `node`.** **`grep -P` is separately broken** *(use `awk`)*. **A filesystem-wide scan double-counts because a worktree is a full checkout** — `git grep` per repo.

**6.3 · A lineage regex matches `refACTor`.** **Select other-lineage files by EXPLICIT LIST, never a pattern.**

**6.4 · 🔴 A `VERIFIED AT SOURCE` LABEL IS A CLAIM ABOUT AN INSTRUMENT — and those get inherited unexamined.** **Live specimen:** the old brief states *"`SPRINT-V0.2-HARDENING.md:620` is a redirect stub carrying no content,"* stamped VERIFIED. **It is a 12-line entry carrying the full original Step 53 body.** ⚠️ **The instance that propagated it HAD THAT FILE OPEN and never put the two side by side.** ⇒ **Treat every such label in the old brief as UNVERIFIED unless it names WHO ran WHAT.**

**6.5 · A markdown-emphasis checker cannot tell a glob from a bold marker.** `identity/**` reads as unbalanced `**`. **Look at the line before believing the flag.**

**6.6 · `SPRINT-V0.2-HARDENING.md` writes steps as `93.` while every other doc says *"Step 93"*** — so `grep "Step 93"` returns nothing for a Step plainly there. **Six false losses so far.**

**6.7 · `npm run typecheck` CANNOT PASS in `evryn-backend`** — `marked` declared, absent from `node_modules` *(**SPRINT Step 118**)*. **It predates you and fails in the direction that looks like your own change's fault.**

---

# §7 — THE THREE CLOSING STEPS, IN THIS ORDER

**1.** **Clear the 16.**

**2.** 🔴 **THEN, IN ONE COMMIT: the announcement into `current-state.md` + delete `docs/working/` + the changelog entry.** **The announcement CANNOT land early** — announcing a retirement that has not happened puts a lie in the file every agent loads first, which is the exact class this series spent two days cleaning up. ⚠️ **Fix `current-state.md`'s two "Recent handoff pointers" paths in this same commit.**

> ### 📋 THE APPROVED ANNOUNCEMENT, CARRIED HERE ON PURPOSE
>
> 🔴 **It used to live only in the brief that gets DELETED by this very commit — a trap I nearly left you.** **Justin approved this text; paste it at the top of `docs/current-state.md`.**
>
> ```
> > # 📁 `docs/working/` IS RETIRED. EVERYTHING IS IN `docs/sessions/` NOW.
> >
> > **One folder, not two.** Working docs and session docs were always the same thing, so they now live in one place.
> >
> > ### 🔎 CAN'T FIND A DOC? You do not need to search, and nobody needs to fix the link.
> >
> > **Every working or session doc ever written is in exactly one of two places:**
> > - **`docs/sessions/`** — if it is still live.
> > - **`docs/sessions/historical/YYYY.MM/`** — if it is retired, **filed by the doc's own date**, which is the first thing in its filename.
> >
> > **So a path like `docs/working/2026.07.15-ac3-step78-real-health-design.md` resolves in one step: it is a 2026-07 doc, so look in `docs/sessions/historical/2026.07/`.**
> >
> > ### 🔴 THE RULE THAT MATTERS MOST: DO NOT FIX REFERENCES TO SESSION DOCS.
> >
> > **A stale path to a session doc is not a bug, and NOBODY GOES LOOKING FOR THEM.** The date in the filename plus the two locations above make it findable in one step, forever. **Sweeping for them is make-work, and the make-work was bad enough to have consumed whole sessions.**
> >
> > ⭐ **But if you happen to be in a doc and see a session path that has moved, fix it.** **What is abolished is the OBLIGATION TO SWEEP — not the permission to fix what is already under your nose.**
> >
> > ⚠️ **This does NOT extend to source-of-truth docs** — protocols, manuals, ADRs, ARCHITECTURE, the Hub, sprint docs. **Those still get their references fixed when they move**, because they are cited as authority rather than as history.
> ```

**3.** **One line into ACP-38's marching orders** so he learns the folder is gone **as a settled sentence, never a task.**

---

# §8 — STILL OPEN, AND NOT YOURS TO ABSORB

- ✅ **CLOSED 2026-08-22 (`b46abb9`) — recorded so it is not re-found as new:** `qc.md` named `docs/ac-to-qc.md` in **four** places as her brief source, in a retired repo. **The input side of the defect `AC0-37g` fixed the output side of on 08-20** — a repoint sweep that greps the retired path finds the writer and misses the reader. 🔑 **What made it worse than an ordinary stale path: both files STILL EXIST on disk, so writing to one succeeds and lands where nobody reads.**
- 🔴 **TWO FINDINGS FROM THE PRODUCT LANE, neither actioned, both verified at source by it:**
  - ✅ **Soren's dedicated-alias rule — NOW TRACKED, though still not landed.** *(Updated 2026-09-01.)* **It is `SPRINT` Step 121, minted in commit `ca472ed`: *"hand the home-and-author question to a successor unresolved."*** 🔴 **AND THE FILE NAME IN THIS ENTRY IS NOW WRONG: the operator runbook was renamed `gatekeeper-onboarding.md` → `gatekeeper-runbook.md`** *(Justin's ruling — "those can't have the same name, that's a massive ongoing tripping hazard")*, **and `identity/activities/gatekeeper-onboarding.md` keeps its name. That rename is the whole point: the two files were being confused, and this lane confused them again on 2026-09-01.** ⚠️ **The operator runbook is AC-MAINTAINED, not Mira's — so *"route it, do not write it"* was wrong for that file and right for the identity module.** ⇒ **Still owed: an answer to Step 121's home-and-author question. Not a write.**
  - **`SPRINT-V0.2-HARDENING.md:746` (Step 94) cites *"the AC2 brief §9"* for the relay live-check recipe. It is §7.** 🔴 **And that recipe is the ONLY written copy — the harness was deleted.** ⚠️ **That brief is one of YOUR sixteen** *(§3 🅰)*, **so the citation is both wrong AND live.**
- ✅ **CLOSED 2026-08-22, recorded here so it is not re-found as new:** the product lane created `docs/mailboxes/inbox-mira.md` to route B1's identity half, and **Justin KILLED it — mailboxes are not how the founding team is reached.** **The content now lives in Mira's memory plus a doc in her `evryn-team-workspace` product folder.** *(This entry previously read "nobody is known to be watching it, flagged to Justin," which was true when written and stopped being true the same day; corrected 2026-09-01 by `AC0-37h3a2` from the product lane's own final inbox entry.)*
- **§6 items 1 and 2 stay with this lineage and are NOT started:** the **auto-load audit** *(how much of what an AC auto-loads should be a protocol or skill pulled at its moment — and **the deliverable is the TRIGGER that re-loads it, not the cut**)*, and the **naming cutover's estate-wide cutoff.** ⚠️ **Items 3–7 were closed by `AC0-37i` on 08-21.**
- **Open questions Justin has not ruled on:** the foreground-vs-background default now rests on nothing *(background renders identically to foreground)* · the hold-banner aging window is untested · `conductor-cascade.md:51` still tells a spun scout to load `CLAUDE.md`, which auto-injects.

---

# §9 — HONEST LIMITS OF WHAT YOU ARE INHERITING

- **What actually moved: 34 other-lineage docs to `docs/sessions/` with owner banners, and 8 ACP docs retired to `historical/2026.08/`.** **The dispositions rest on `ACx`'s per-file work plus a corrected referrer scan — NOT on reading all 42 in full.** ⚠️ **`ACx`'s own §13 admits it read headers and residue sections rather than bodies for the ten it retired, and held two of 45 `src/` files.** ⇒ **Execute a verdict you have CONFIRMED; treat an unconfirmed one as a LEAD.** 🔴 **Do NOT rest this on "ACx was fully loaded" — Justin believed that, and it is not what ACx says about itself.**
- ⚠️ **The 34 were selected by an EXPLICIT LIST and moved by script, then verified file-by-file afterwards** *(all present, all bannered, none left behind)*. **The first run of that script failed completely and silently — a bash `/tmp` path that node reads as `C:\tmp` — and it was the independent verification, not the exit code, that caught it.**
- **Cost if a disposition is wrong: DISCOVERABILITY, not loss.** Every file is one `git mv` away, nothing was deleted, and under the no-repointing rule a wrong retirement breaks no reference.
- **No product doc was ever loaded by this lineage today.** **Every product claim in this brief is a LEAD.** **That is what B is for.**
- **Nothing is deployed. No `src/` file was touched. `evryn-backend` was modified only by B, and only in docs.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
