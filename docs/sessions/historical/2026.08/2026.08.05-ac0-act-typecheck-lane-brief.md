# Brief: ACt — the typecheck lane (SPRINT Step 98)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this:** your complete marching orders and your lane's shared surface. Author: **AC0-30**, 2026-08-05. **You are a single-purpose lane: one defect class, one fix, one merge. When it lands, your lane closes.**
>
> **⚠️ You are a SUB-AC under AC0** — not a peer, not top-level. Report to AC0, never to Justin. Cap headings at `##`. You do not merge, push, or deploy.

---

## 🔴 §-1 — MEASURED CORRECTION, AC0-30, 2026-08-05. **READ THIS FIRST. IT REPLACES THE "~107 ERRORS" FRAMING EVERYWHERE BELOW.**

**AC0 measured this properly before spinning you, and the job is a different shape than §0/§2/§5 describe. Those sections' *reasoning* stands; their *numbers* do not.**

### The "~107 errors" figure is wrong. Here is what is actually true.

That number came from a lane report and was never re-measured against the project's own compiler settings. AC0 also produced a bogus "40" earlier using **mismatched flags** (`--module esnext --moduleResolution bundler` guessed by hand instead of read from `tsconfig.json`). ⚠️ **Both are artifacts of measuring with the wrong config — a live instance of "a check that comes back clean/alarming is worthless if it wasn't the check you thought you were running."**

**Measured correctly, two separate facts:**

**1. 🔴 Naively widening `include` DOES NOT WORK — and this, not the error count, is the real job.** Adding `tests/**/*` to the existing `tsconfig.json` produces **79 failures, and every single one is `TS6059: File … is not under rootDir 'src'`.** Zero of them are type errors. The cause is in the config itself:

```jsonc
"declaration": true,  "declarationMap": true,  "sourceMap": true,
"outDir": "./dist",   "rootDir": "./src",      "include": ["src/**/*"]
```

`rootDir: "./src"` combined with emit (`declaration`/`outDir`) means **the build structurally refuses to compile anything outside `src/`.** So *"add tests to include"* is not a small change that surfaces errors — **it breaks the build and surfaces nothing.**

**2. The actual type errors, once the config is done right: 10 errors across 7 files** *(of 80 test files)* — measured by type-checking `tests/**/*.ts` directly with the project's real `strict` settings. They are:
`test-resolve-inbound-sender.ts` (3) · `test-step61-process.ts` (2) · `test-sendemail-provenance.ts` · `test-send-transport.ts` · `test-send-allowlist.ts` · `test-reflection-consolidate.ts` · `test-find-user-by-name.ts` (1 each).
Categories: **3× TS2339** (property doesn't exist), **3× TS2307** (can't find module), **2× TS2345** + **1× TS2741** + **1× TS2322** (argument/shape mismatches).

### ⇒ What this changes about your lane

- **The mechanical half is SMALL** — 10 errors, 7 files. Do not plan a week around it.
- **🔴 The DESIGN half is the whole job, and it is genuinely yours.** You must give the tests a type-check path that does **not** fight `rootDir`. The obvious candidate is a separate **`tsconfig.tests.json`** that `extends` the base, sets **`noEmit: true`**, drops/relaxes `rootDir`, and includes both trees — plus **an npm script that actually runs it.** ⚠️ **A config that type-checks tests but isn't wired into the command people run has changed nothing** — that is the failure mode that created this gap in the first place. **Decide deliberately whether `npm run typecheck` runs both, or whether there's a second script, and make the default the safe one.**
- **⚠️ The four untracked `tests/_gate-a-*.ts` files are in that directory and are HELD Gate-A evidence.** They will appear in any `tests/**` glob. **Do not commit them, do not delete them, and decide deliberately whether your config includes or excludes them** — an `_`-prefix exclusion may be the right call, and the runtime's own identity loader already uses exactly that convention (`!f.startsWith("_")`).
- **§1's surface-don't-silence discipline is UNCHANGED and is still the most important thing in this brief.** With only 10 errors, the odds that *several* are real defects rather than noise go **up**, not down — three of them are "property does not exist," which is precisely the shape of a test asserting against something that moved.

**Re-measure everything yourself at spin time.** Two prior measurements of this exact thing were wrong in two different directions; do not make yours the third. **Read `tsconfig.json` before choosing any compiler flag.**

---

## §0 — WHO YOU ARE, AND WHY THIS IS URGENT

You are **ACt** (t = typecheck). Sign `ACt`.

**Your one job: make `tests/` actually type-checked, and fix every error that surfaces.**

**Why it's urgent, and it isn't about tidiness.** The next lane (**ACa**) is about to restructure `src/triage/classify.ts` — 3,186 lines, ~16% of the runtime — and split it into modules. **The test suite is that refactor's only safety net.** Right now that net has never been inspected: `tsconfig.json`'s `include` is `["src/**/*"]`, so `tsc --noEmit` **never sees `tests/` at all**, and `tsx` — how every test actually runs — **strips types without checking them.**

**This is not theoretical.** AC5 hand-checked five test files on one branch and it **caught four real defects.** Repo-wide the count is **~107 errors**, of which **~40 sit in the 20 test files that cover `classify.ts`** — precisely the ones ACa will depend on.

**⇒ Nothing else moves on the product until you land.** Justin's call, 2026-08-05: *"let's not limp along — let's get this fixed."* You are the only thing between here and the refactor.

---

## §1 — 🔴 THE ONE DISCIPLINE THAT MATTERS MOST

**A type error can be a nuisance OR a real defect wearing a nuisance's clothes. You must not treat them the same.**

Most of the 107 will be mechanical — an implicit `any`, a missing import, an unused binding. **Some will not be.** A test that doesn't compile can still *pass* under `tsx`, because `tsx` never checked it. So an error may be sitting on top of an assertion that has been **silently meaningless** — comparing `undefined` to `undefined`, asserting a property that no longer exists, checking a shape that changed months ago.

> ### The rule: when fixing an error would CHANGE WHAT A TEST ASSERTS, stop and surface it. Do not make the compiler quiet.
>
> **Silencing a real defect is worse than the original gap**, because afterwards the suite looks green *and* type-clean, and the lie is now load-bearing. **There is live precedent:** a test in this very repo asserted an error message naming a tool that had been deleted months earlier. It sat red — and unnoticed — because nothing ran it. That is exactly the shape you're hunting.
>
> **Keep a running list of every error whose fix changes an assertion's meaning**, with a one-line "what it was really testing vs. what it appears to test now." **That list is a deliverable, not a byproduct.** AC0 would far rather receive twelve questions than one silent repair.

**The counterpart discipline, from a lesson this repo just paid for:** *reading cannot find an absence.* Where you change an assertion, **prove the test can still fail** — break the thing it covers, confirm it goes red, restore. **Do that in a sandbox copy, never in the live tree** (`evryn-quality/CLAUDE.md` carries the full teaching; all three agent manuals now require it).

---

## §2 — SCOPE

**IN:**
1. **Make `tests/` type-checked.** The mechanism is your design call: widen `include`, add a separate `tsconfig.tests.json`, or a project-references split. **Weigh it against how tests are actually run (`tsx`) and how `npm run typecheck` is invoked** — a config that type-checks tests but isn't wired into the command anyone runs has changed nothing.
2. **Fix all ~107 errors** — re-count at spin time; do not trust that number.
3. **Surface, don't silence,** per §1.

**OUT — do not do these:**
- **Any change to `src/` behavior.** If a test error reveals a genuine runtime bug, **route it to AC0; do not fix it here.** This lane must not alter what ships.
- **Refactoring tests for style.** Type-correctness only.
- **`classify.ts` or anything ACa will move.** If your fix seems to require touching runtime source, **stop and flag it.**

**Done means:** `tests/` is genuinely covered by the project's typecheck command; that command exits 0; **all existing suites still pass**; your assertion-change list is written; QC has returned GO; and it is **merged before ACa spins**.

⚠️ **If this looks like it won't land quickly, say so IMMEDIATELY.** The whole week is queued behind it. AC0 would far rather hear "this is bigger than it looked" in hour one than on Thursday.

---

## §3 — HOW YOU WORK (non-negotiable — Justin named this explicitly)

**You design → DC builds → QC reviews. You do not write the code yourself** — that collapses the independent-review property that makes two passes meaningful.

> 🔴 **Justin's condition for running this lane as a sub-AC at all, verbatim: you *"have to be fastidious with the orchestration protocol."*** This lane is partly an experiment in AC-under-AC. **If you improvise a brief, you fail the experiment and probably the build.**

**That means, concretely — every one of these, every time:**
- **Read `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` in full before you spin anything.** It is on your load list.
- **Paste the six-part tagged brief VERBATIM** — `<identity>` / `<mandatory_load>` / `<pre_task>` / `<task>` / `<questions_first>` / `<receipts>` — with only the codified substitutions. **You may not change one word** of those blocks without Justin's explicit prior OK. Not to condense, not to "tailor it."
- **Assemble the load YOURSELF and name every file with its line span.** Do not tell a subagent to "load its cascade." Unnamed = unread.
- **Model-pin every spin.** An unpinned spawn silently inherits.
- **Verify the two-part receipts on return** — every `(full — N lines)` span must reach the end. **Re-run on a partial load; do not act on its output.**
- **NEVER tell a child where to report.** Delivery is automatic; a role-addressed instruction is the thing that misroutes it.
- **DC gets `evryn-dev-workspace/CLAUDE.md`; QC gets `evryn-quality/CLAUDE.md`.** A subagent called "DC" without DC's manual is a generic model guessing.

**Worktree:** `git -C evryn-backend worktree add ../evryn-backend-typecheck -b act/tests-typecheck main`. **Do not reap it** — worktree lifecycle is Justin's call now, not a lane's.

**Merge, push, deploy: NOT YOURS.** Hand back to AC0 with a set-down.

---

## §4 — REPORTING

- **Report to AC0, never to Justin.** No Slack pings from you.
- **Use `## ACt's read — <topic>`** for your own judgment. Never `#`.
- **Flag `NEEDS-JUSTIN`** on anything you think is genuinely his call, with your recommendation. **Asymmetric default: when unsure, flag it.** Over-flagging costs AC0 two seconds; under-flagging means the question is never asked.
- **Set-down discipline — the lesson this repo paid for twice this week:** when you hand back, **name a PROPERTY to grep, not just a commit SHA.** SHAs do not survive a rebase; behaviors do. A set-down whose identifiers have been rewritten reads as lost work and costs someone a panic.

---

## §5 — WHAT'S ALREADY KNOWN (don't re-derive)

- **~40 of the ~107 errors are in the 20 test files that reference `classify.ts`** — AC0 measured this by type-checking that subset directly. Those matter most, but you are fixing all of them.
- **`tsx` strips types without checking**, which is why every suite can be green while the files don't compile.
- **A real specimen, already fixed:** `tests/test-evryn-initiated-placeholder.ts` asserted an error naming `supabase_upsert`, a tool removed in Step 57 (`v0.2.8`). Red since 2026-07-22, invisible because nothing type-checked it and it needed a live DB to run. **That's the class.**
- **`main` is `4a8e271`, clean, and typechecks (`src` only) — verified 2026-08-05.** Re-verify at spin.

---

## §6 — RUNNING LOG (append yours)

*(ACt: append dated entries — decisions, the assertion-change list, what's open. This file must stay current enough that a fresh ACt could re-spin from it alone.)*

---

### 2026-08-05T18:11-07:00 — ACt. **BUILT + QC-GO.** *(Superseded by the CLOSED entry below — read that first.)*

**Two commits, since merged:** the build (`a8d31ec`) and a comment-only fix-trip (`4c9478d`) correcting the `?query` justification described below. ⚠️ **That fix-trip SHA already moved once** — it was `49051ff` when I verified it, and DC amended rather than appended, so the identifier evaporated within minutes while the tree stayed byte-identical (`git diff 49051ff 4c9478d` is empty). **Treat every SHA in this entry as disposable and use the grep properties below** — this lane produced a live specimen of exactly the failure §4's set-down rule warns about. The fix-trip **skipped QC deliberately and legitimately** — I verified mechanically that executable code is **byte-identical** across it (stripped comments/blanks from both sides of all 4 files and compared hashes), and a pure-comment change is the one category the "QC verifies every real code change" rule exempts. Post-fix-trip re-verification: typecheck exit 0, build exit 0, all 7 touched suites ALL PASSED.

**Where the work lives — grep for the PROPERTY, not the SHA.** ⚠️ **The branch `act/tests-typecheck` and both worktrees NO LONGER EXIST** — merged and reaped; see the CLOSED entry. **The work is on `main`.** Identify it by these, which survive any rebase:
- **`tsconfig.tests.json`** — the file exists nowhere else in the repo.
- **`moduleDetection`** — appears nowhere in the repo except that config (2 hits).
- **`importFresh`** — the helper `tests/import-fresh.ts` and its three call sites (8 hits).
- **`original_from_name stash (Step 57 F2`** — the coverage block in `tests/test-step61-process.ts` (2 hits).
If those greps come back empty on a tree that claims to carry Step 98, the work is not there.

#### §1's required deliverable — the assertion-change list

**NONE of the 10 errors required changing what a test asserts.** That is the headline. But two clusters had an *obvious* fix that would have silently gutted the test, and both were avoided:
- **The three `TS2307` `?query` errors** — the naive fix (delete the query so `tsc` resolves the specifier) was rejected. Fixed via a non-literal specifier through `importFresh`, so **zero suppressions**: no `@ts-expect-error`, no `@ts-ignore`, and the `typeof import(...)` type parameter keeps the bindings fully typed (QC proved they are *not* degraded to `any` by probing the real signature).
- **The three `TS2339`-on-`never` in `test-resolve-inbound-sender.ts`** — the naive fix (drop to a bare `!== null` check) was rejected. Fixed with a type-level cast; **all three field comparisons still execute**, mutation-proven RED individually (email / display_name / status).
- The remaining four (`TS2741`, 2× `TS2345`, `TS2322`) are inert or type-declaration-only.

#### Two real defects the typecheck exposed (this is the lane paying for itself)

1. **A coverage gap:** `buildForwardItemMetadata`'s `originalFromName` half — **Step 57 F2 / the verdict-time half of AC3's 54c real-name capture**, consumed by `readStashedFromName` in `verdict.ts` — had **no test coverage at all**. Unfixed, a regression would silently store every verdict-time-reified contact under its email local-part ("jsmith" instead of "Jane Smith") with nothing failing. Now covered in both directions incl. the omit-vs-null contract, mutation-proven.
2. **A test that could not run:** `tests/test-step61-process.ts` boot-failed on `EVRYN_ENV` in any tree without a `.env` — its static import hoisted above env seeding. It was not merely un-type-checked; it was **un-runnable**. Converted to the seeded-dynamic-import pattern its siblings already use. QC confirmed the base→HEAD failing-set diff is **exactly one line**: this file.

#### 🔴 Corrections to §-1's measurements — re-measured, and one is load-bearing

- **`rootDir` is necessary AND sufficient.** §-1's causal story ("`rootDir` combined with emit (`declaration`/`outDir`)") is **half right**. Measured: `noEmit: true` with `rootDir` kept still yields all 75 TS6059; toggling `declaration`/`declarationMap`/`sourceMap` off with `rootDir` relaxed is **byte-identical**. `noEmit` is correct for a checker but is *not* what fixes TS6059.
- **🔴 `moduleDetection: "force"` is REQUIRED and §-1 never names it.** Without it: **116 errors**, of which **106 are a measurement artifact** — test files using only `await import(...)` have no top-level import/export, so TS models them as *scripts sharing one global scope* and every file's `let failures = 0` collides. `package.json` declares `"type": "module"`, so Node and `tsx` genuinely treat them as ES modules; the setting makes the check model reality. **QC attacked this specifically and it held** — she traced all 4 of the non-obvious `TS2339`s in that 106 to source (a `const prompt` colliding with Node's global `prompt()`, and a `const empty` colliding across two files) and confirmed **zero real defects are hidden**.
- **"79 vs 75" is not a discrepancy.** Both are right, measured in different trees: 75 in a clean worktree, **exactly 79** with the 4 untracked `_gate-a-*.ts` files present. QC verified by copying them in. This is itself a live demonstration of why they are excluded — *an error count that varies with untracked local debris is not a check.*

#### 🔴 A wrong claim I authored, caught by QC, corrected before merge

My build brief asserted that deleting a `?query` suffix would leave those tests "passing while asserting nothing." **That is false.** QC measured it and I reproduced it independently: with the query stripped, flipping `SEND_ENABLED` to `"false"` still gives **5 FAILED** — the test remains fully discriminating. The real invariant is **load order** (nothing imports `config.ts` before the env seeding); the query's reach is narrower than the prose claimed, because the fresh `client.js?q` still binds the **cached** `config.ts` (that transitive import carries no query). DC had written my claim into four permanent shouted comments; a comment-only fix-trip is correcting them to state the true invariant. **The `?query` suffixes stay** — they are genuine defense-in-depth. *(Class: I asserted runtime behavior from related context instead of measuring it — the exact failure the "verify before claiming" rule names.)*

#### Open items for AC0 — two contradictions I am NOT resolving unilaterally ✅ **BOTH RESOLVED — Justin ruled 2026-08-06; do not re-raise**

> **(1) Justin ruled "before the extraction" was RIGHT** — §0's sequencing stood, and AC0 corrected SPRINT Step 98's contradictory closing line. **(2)** Stale SHA noted. Both items below are kept for the reasoning trail only.

1. **`NEEDS-JUSTIN` — SPRINT Step 98's own text contradicts this brief on sequencing.** Step 98 closes `[AC design → DC+QC; **after the extraction, not before**]` and says "Sequence it AFTER the `src/triage/classify.ts` extraction lands." §0 of this brief says the reverse ("Nothing else moves on the product until you land", "merged before ACa spins"). §-1's measured correction (10 errors, not ~107) materially weakens Step 98's stated rationale (that it would be "a large mechanical trip touching most test files at once" — it is 10 files), so the reversal looks justified — **but the sprint text was never updated and still reads authoritative.** *Recommendation: merge this first as §0 directs, and update Step 98's sequencing line so the sprint stops asserting the opposite.*
2. **Stale SHA:** §5 says `main` is `4a8e271`; actual `main` at spin was **`dda163c`** (`4a8e271` is its parent).

#### Carried forward — not mine to land

- **The boot-failure class is far wider than assumed.** QC measured **27 of 61** runnable suites boot-fail on `EVRYN_ENV` in a tree with no `.env` — pure config boot-fail, not DB failure, the same defect DC fixed in one file. Identical at base and HEAD (**not a regression**), and they run fine in `evryn-backend`, which has a `.env`. **This matters for ACa:** a refactor touching those files will hit it. `tests/test-reflection-consolidate.ts` has the identical defect and was deliberately left alone as out of scope — it is the obvious next candidate.
- **QC's pattern proposal** ✅ **PROMOTED** (Justin approved 2026-08-06) — **it now lives in the "Patterns This Role Watches For" section of `evryn-quality/CLAUDE.md`, which is its ONE home. Read it there.** I am deliberately NOT restating it here: the paraphrase I originally put in this slot was itself the fork-risk the pattern warns about, and AC0 caught it. This lane is its live specimen (the `?query` episode above).

#### Constraints held

`tsconfig.json` **byte-identical** to `dda163c` (verified `git diff --exit-code`); **zero `src/` changes**; the four untracked `tests/_gate-a-*.ts` files never committed, deleted, moved, or edited (QC md5-verified them unchanged after copying them for measurement); `main` untouched *by me*; nothing pushed or deployed by this lane. *(Worktree reaping — deferred at the time per §3 — was later rescinded and completed; see the CLOSED entry.)*

---

### 2026-08-06T15:09-07:00 — ACt. ✅ **LANE CLOSED. Merged, pushed, reaped. Nothing owed.**

**Final state.** Work merged to `main` (lane tip `4c9478d`; `main` then advanced to `c0f08fb` with AC0's CHANGELOG + SPRINT flip) and **pushed by AC0**. QC's pattern **promoted and signed off**. Both `NEEDS-JUSTIN` items **ruled** (see above). **Both worktrees reaped and the branch deleted** — `git worktree list` now shows only the canonical `evryn-backend` tree. **Nothing is owed to anyone.**

**Reap detail (it accepted `-d`, which is the proof).** `git branch -d act/tests-typecheck` → *"Deleted branch act/tests-typecheck (was 4c9478d)"*. Lowercase `-d` refuses an unmerged branch, so its acceptance is independent confirmation the work genuinely landed on `main` — no `-D` was used, nothing was forced. Both worktrees were `status --porcelain` **clean** before removal; nothing was stashed or discarded. Untouched as required: `main`, the four `tests/_gate-a-*.ts` held-evidence files, and `backups/full-public-2026-08-05.sql` (all still present and untracked).

#### 🔴 Things that outlive this lane and are written NOWHERE ELSE

1. **⚠️ REAPING A WORKTREE THAT HAS A JUNCTIONED `node_modules` IS A FOOT-GUN — unlink the junction FIRST.** Both worktrees had `node_modules` **junctioned** (Windows `mklink /J`) and **both junctions pointed at the CANONICAL repo's own install**, `evryn-backend/node_modules`. A recursive delete that follows a reparse point does not delete a *copy* — **it deletes the real repo's dependencies.** The safe order, which I used: (a) `cmd //c "rmdir <worktree>\node_modules"` — plain `rmdir` unlinks a junction **without following it**; (b) verify the target survived (I counted 161 top-level entries before and after); (c) *then* `git worktree remove`. **This hazard is not mentioned in CLAUDE.md's Worktree & Branch Discipline section, which describes `git worktree remove` with no junction caveat** — and the junction trick is the standard way we make `npx` work in a worktree, so every future reap hits it. **Strong candidate for a line in that section.**
2. **The naive `include` widening is worse than "it errors" — with a real emit it VANDALIZES `tests/`.** QC measured that widening `include` and running an actual build (not `--noEmit`) relocates the emit root and writes **77 compiled `.js` / `.d.ts` / `.map` files directly into `tests/`**, which then have to be cleaned up by hand. §-1 and the config header both say it "breaks the build"; that is **understated**. This is the concrete reason the separate-config design was right rather than merely tidy.
3. **My `<mandatory_load>` for QC was incomplete, and the brief template's cascade-reconcile clause is what caught it.** She loaded four **standing** files my list omitted — `_evryn-meta/docs/hub/technical-vision.md`, `evryn-backend/docs/operator-guide.md`, and both mailboxes (`ac-to-qc.md`, `dc-to-ac.md`) — and reported them. **Lesson for the next lane-AC: the reconcile clause is not boilerplate, it is load-bearing, and it recovered a real omission of mine.** Include those four in a QC load list from the start.
4. **I nearly published 116 as the real error count — the exact "third wrong number" trap §-1 warned about.** Probes B and C returned 116 errors dominated by TS1375/TS2451/TS2393. It *looked* like a finding. It was a script-vs-module artifact, and publishing it would have made mine the third wrong measurement in a row. **What saved it was refusing to report a number whose error *codes* I could not explain** — the codes (redeclared block-scoped variable, duplicate function implementation) describe a *scope collision*, which is nonsense for files that pass individually. **That is the generalizable move: if you cannot explain why each error code appears, you do not yet have a measurement.**
5. **Re-check property for the build config, if anyone touches this later:** `npx tsc` must still emit **exactly 45 `.d.ts` + 45 `.d.ts.map` + 45 `.js`** (the 45 `src/` files) and **zero test output into `dist/`**. QC verified this; it is the cheap check that the test config never leaked into the build.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
