# AC0-38 — MARCHING ORDERS (overnight, 2026-08-13/14)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file: THIS IS YOUR ENTIRE BRIEFING.** Justin's stated intent is that he says *"you are AC0-38, read this file"* and then steps away. Everything you need is here or named here.
>
> **Written by AC0-37 (2026-08-13) BEFORE the merges landed. AC0-37b was expected to adjust it.** ⚠️ **If AC0-37b did not annotate this file, treat every state claim below as *as-of-2026-08-13-evening* and verify it against the repo before acting.** The *work* is right; the *state* may have moved.

---

> # ▶️ START HERE. You need no further instruction from anyone.
>
> **Justin's intent is that he says *"you are AC0-38, read this file,"* confirms the cutover lands, and goes to bed.** Everything you need is in this file or named in it.
>
> 1. **Read `_evryn-meta/.claude/agents/ac.md` from disk, in full**, and confirm its bottom canary. *(If the cutover has not landed, read `_evryn-meta/CLAUDE.md` instead — §0 will tell you which world you are in.)* **Do not trust any auto-injected copy.**
> 2. **Do §0 immediately.** It is a hard stop and Justin is awake for it. Report the result before anything else.
> 3. 🔴 **Load §1's list — exactly as written — AND THEN STOP. This is Justin's veto point.** Report **what you loaded** (every file, the line spans you actually read, the canary you confirmed) **and your plan for the night**, and wait for his go. ⚠️ **Load PRECISELY as §1 describes.** If you skipped something, say so and why; **absent a compelling reason he will send you back**, and that is a round trip you cannot afford at the start of an overnight.
> 4. 🔴 **Write that report for someone coming in COLD.** He has not read this file and is not reading your documents. **Describe things; never cite a section by number.** Never leave a bare category noun (*"a lane," "a guard," "a doc"*) without naming which one. **Open with `# AC0-38's read — …`** so he can find your voice in the wall of tool output.
> 5. **On his go, run §2's five lanes concurrently.** ⚠️ **Running lanes SIDE BY SIDE is cheap; running them DEEPER is not** — your lanes' DCs and QCs report into *your* context unbidden, 500–1,500 tokens each, and a two-trip lane with a fix trip can surface six or more. **Widen before you deepen.**
> 6. **Ping `#team-alerts` prefixed `AC0-38:` at every state change,** and arm the heartbeat canary in §3 the moment Justin steps away.
>
> 🔴 **ONE THING YOU MUST DO IN EVERY `<task>` YOU SEND TONIGHT, and it is not optional.** You are conducting: you will brief four DCs on runtime **you have not read.** The protocol requires you to **tell each subagent how much of the system you have actually read, where the rest of its brief came from, and to treat every factual claim in it as a LEAD, not a fact** — including the `file:line` citations in §2, which came from the Atlas and a prior instance's reading, not from yours. **The proven wording is in `ac-orchestration-protocol.md` part 2 ("TELL THE SUBAGENT HOW MUCH OF THE SYSTEM YOU HAVE ACTUALLY READ") — paste it, do not paraphrase it.** Add its corollary too: *where your brief is silent, that is not "the spinner checked and found nothing" — assume they could not see it.* **This has returned six real corrections in one night, including a flat factual error and a fix that would have created a module cycle. It is the cheapest review layer you have.**
>
> **You will not get a second briefing. If something here is wrong or missing, say so loudly in your morning report rather than working around it silently.**

---

# §0 — 🔴 YOUR FIRST TASK IS A HARD STOP. DO IT BEFORE ANYTHING ELSE.

**You are the fresh session the Lane A cutover requires.** The agent-type registry is read **once, at session start** — so nothing built on that branch was spawnable until you opened. This is the known case where **a re-spin beats compact-and-continue**: a session-start read is invisible to a reloaded instance no matter how well it reloads.

**Justin is awake for this part.** He is staying up specifically so you do not get stranded on it.

## Step 1 — Verify the four agent types are spawnable

Spawn `ac`, `dc`, `qc`, `oc` **by name** and confirm **each reports its own manual's bottom truncation canary.** A spawn that succeeds but reports the wrong canary — or no canary — is a failure, not a pass.

## Step 2 — 🔴 IF THEY ARE NOT SPAWNABLE, STOP. Report to Justin and go no further on the cutover.

**Do not proceed to the redirects.** Converting `evryn-dev-workspace/CLAUDE.md`, `evryn-quality/CLAUDE.md` and `evryn-ops/CLAUDE.md` into redirects while the definitions are unreachable would **strand three operating manuals with nothing pointing at them** — every future DC, QC and OC would load a redirect to a file the harness cannot deliver. **This is ACm2's hard stop and it is not yours to override.**

Move straight to §2's build lanes instead; they do not depend on the cutover.

## Step 3 — If they ARE spawnable

1. **Run each definition's drift-check** (the command is in each definition's maintainer-notes header). Expect **no output**. ⚠️ Between merge and cutover, **three verbatim copies of those manuals exist** — any edit to a repo `CLAUDE.md` in that window silently forks it, and this check is the only detector.
2. **Convert the three repo `CLAUDE.md`s to redirects.** **The repos stay as backups** (Justin's call, 2026-08-12).
3. **Update, in the same change:** the router's transitional note (below its agent table) and the manual-path line in `ac-orchestration-protocol.md` part 1. **Both currently state the repo files are authoritative, which this step makes false.**
4. **Report to Justin, then let him go to bed.**

✅ **A clean before/after you already have:** a harness probe reported the project manual's first line as `# CLAUDE.md — AC (Architect Claude)`. **After cutover it should read `# CLAUDE.md — the agent router`.**

---

# §1 — LOAD

**Everything you need for §2 is in the briefs you will write from this file.** Load lean; you are conducting, not building.

1. **This file.**
2. **`_evryn-meta/.claude/agents/ac.md`** (post-cutover) **or `_evryn-meta/CLAUDE.md`** (if the cutover did not land) — **from disk, in full, confirm the canary.**
3. **`docs/protocols/ac-orchestration-protocol.md`** (~692 post-merge) **+ `docs/protocols/orchestration/spinning-dc-qc.md`** — 🔴 **mandatory before you spin anything.** You will spin eight-plus subagents tonight.
4. **`docs/protocols/ac-writing-protocol.md`** (101) — you will write briefs.
5. **`docs/current-state.md`** + **`docs/hub/roadmap.md`** — Light cascade.
6. **`evryn-backend/docs/SPRINT-V0.2-HARDENING.md`** — the Steps you are building are in it.
7. **Per lane, at spin time:** the Atlas section §2 names for that lane, and nothing else.

🚫 **Do NOT load `evryn-backend/src/`.** You are the conductor. Each DC gets a precise file list; the Atlas is how you build that list.

---

# §2 — THE WORK: four build lanes + one reviewer, all runnable in parallel

**All five are already routed, verified at source by a prior instance, and need no decision from Justin mid-flight. That is why they are the overnight set.**

🔴 **Every spin is TWO TRIPS** (three for QC, whose cascade no longer fits one agent). Trip 1 carries `<identity>` + `<mandatory_load>` + `<receipts>` and **says nothing about the work.** You verify receipts, *then* send `<task>`. **Paste the blocks VERBATIM.** Model-pin every spin.

## Lane 1 — SPRINT Step 103: `updateEmailmgrItem` can silently destroy a pending draft

**Priority: highest. It is on the approval path and is explicitly *"before the seven-item bundle deploys."***

- **Mechanism:** `src/db/items.ts:202-206` reads the item's current metadata and **destructures only `data` — it never checks `error`.** On a transient Supabase read failure `current` is null, so `:209`'s `current?.metadata ?? {}` merges incoming metadata into an **empty object**, and `:217` writes that as the *whole* metadata — **wiping `draft`, `lifecycle` and `send_error`.** The update succeeds, so nothing throws, nothing alerts, the operator sees a normal result.
- **It is the outlier in its own file:** every other read in `items.ts` destructures `error` and throws (`:85`, `:224`, `:244`, `:261`, `:295`). The file's own comments at `:103` and `:160` describe exactly this clobbering as what the always-merge rule (Step 36) was built to prevent.
- **Fix direction:** destructure and throw like its five siblings; **have DC decide whether a failed metadata read should abort the update entirely rather than merge into `{}`.**
- **Acceptance bar:** a test that **fails** — force the read error, confirm the write is refused. A green suite proves nothing here.
- 🔴 **Its own lane. Do NOT fold it into another diff.** Two lines with a silent-data-loss failure mode need a real review, not a rider.
- **Atlas:** `06-persistence.md` §5.2.

## Lane 2 — SPRINT Step 104: thirteen unguarded `parseInt(process.env…)`, two on the runaway brake itself

- **Mechanism:** a non-numeric value yields `NaN`, and **every comparison against `NaN` is false**, so the guard silently stops guarding. `config.ts:512-556` documents this exact class at length and `parseBoundedInt` exists to close it — **but that sweep only ever covered `config.ts`.**
- 🔴 **The direction of failure DIFFERS PER SITE. Do not pattern-match; trace each one.**
  - `safety/detectors.ts:44` (`BREAKER_LOOP_SIGNATURE_THRESHOLD`) and `:58` (`BREAKER_VELOCITY_CEILING_PER_HOUR`) → the `>=` at `:115`/`:132` goes false forever ⇒ **the runaway detector never trips.**
  - `safety/detectors.ts:36` (`BREAKER_WINDOW_MS`) → fails the **other** way: the window never prunes and grows unbounded ⇒ velocity counts *lifetime* attempts against a 600/hr ceiling ⇒ **an eventual FALSE HALT.**
  - `security/web-guard.ts:44,46,48` → the exfil-shape length caps stop tripping **while the SSRF and scheme checks keep passing, so the guard looks alive in the logs.**
  - `reflection/consolidate.ts` ×3 → the oversize alarm never fires.
  - Benign-direction: `email/poll.ts:1618`, `safety/affirmation.ts:32`, `notify/slack.ts:114,115`.
- ✅ **`config.ts:801` (`PORT`) is DELIBERATELY excluded and must stay that way.** It throws `ERR_SOCKET_BAD_PORT` *and* fires a FATAL `notifyDev`. **A fail-safe default is right when the failure would otherwise be SILENT, and WRONG when it converts a LOUD failure into a quiet one.** Apply that judgment per site rather than mechanically wrapping all thirteen.
- ⚠️ **Do NOT fold into the `classify.ts` extraction** — a pure refactor's entire safety claim is *"behaviour is unchanged,"* and these are behavioural fixes.
- **Atlas:** `07-safety.md` §5a · `08-security.md` §5.1 · `04-triage-pipeline.md` §5.

## Lane 3 — SPRINT Step 102: the Haiku screen's spend is uncounted by both runaway detectors

**This is a written HARD PRECONDITION on the `HAIKU_SCREEN_MODE` `shadow → active` flip.**

- **Four facts, each checked at source:** (1) there are **two** SDK `query()` sites — `triage/classify.ts:2867` and `triage/haiku-screen.ts:406`; (2) `recordAttemptAndCheck` — which feeds **both** detectors — has **exactly one call site**, `classify.ts:2678`, on the `runEvrynQuery` path; (3) ⇒ nothing on the Haiku path ever counts an attempt; (4) `ARCHITECTURE.md` has asserted since `v0.2.2` that `runEvrynQuery` is *"the sole Anthropic spend door"* — in the doc **and in three runtime files.**
- 🔴 **The reasoning that made this look safe is CIRCULAR — do not let a DC reuse it.** *"A tripped breaker already skips the screen"* is true and beside the point: **the detectors are what TRIP the breaker.** An uncounted attempt cannot contribute to the trip that would stop it.
- **Harmless today** (mode unset → `off`; in `shadow` Opus runs anyway so every screen call is paired with a counted attempt). **NOT harmless in `active`**, where an `ignore`/`pass` short-circuits Opus entirely and a runaway confined to the screen spends **invisibly to both detectors.**
- **Fix:** route the Haiku path through the same counting choke point — call `recordAttemptAndCheck` from `haiku-screen.ts` before its `query()`, or extract count-and-check into a shared primitive both doors call.
- 🔴 **A FALSE-POSITIVE TRAP THIS LANE WILL WALK INTO, and it costs an hour if you don't know it.** This lane audits `runEvrynQuery` call sites. **A naive `runEvrynQuery\s*\(` file filter SKIPS `src/reflection/consolidate.ts` ENTIRELY, because that file aliases the import as `runQuery`** — which then **falsely reports `process.ts:1111` and `:1381` as unchecked call sites. They are checked.** Lane E's QC hit this and recorded it so the next auditor would not. **Tell your DC. Anyone re-running that audit gets the same two false positives.**
- 🔴 **Acceptance bar is a MUTATION, not a reading: make the screen path spin and confirm the detector actually trips.** Same class as Step 101 — a safety mechanism nobody has proven can execute is not protection.
- **And when it lands, correct the "sole spend door" wording in all three runtime files plus `ARCHITECTURE.md`**, or the next reader inherits the same false premise.

## Lane 4 — SPRINT Step 97: `create_user` returns the firewalled `cross_user_notes` into Evryn's context

**Justin ruled this gets its own dedicated lane.**

- **Four links, all shipped code:** `findUserByEmail` (`db/users.ts:34`) and `findUserById` (`:49`) both `.select("*")` → the full row on the find path (`:453-455`) → the `create_user` MCP handler (`classify.ts:1962-2010`) returns `JSON.stringify({ user, created })` with **no projection.**
- **The codebase documents this against itself:** `security/supabase-read-guard.ts:110-117` states it verbatim, and `USERS_RETURNABLE_COLUMNS` (`:126`) exists **solely** as the all-but-that-column list — **applied only on the generic read path.**
- **Fix at the TYPED-HELPER boundary** (`findUserByEmail` / `findUserById`), not at `create_user` — that closes the **class**. ⚠️ **Heed the read-guard's own warning (`:118-125`): omitting a real column silently DROPS it from reads.** So the fix is *"exclude exactly one column,"* never *"narrow to a hand-picked set."* `story` / `profile_jsonb` must stay readable.
- ⚠️ **Cross-lane collision:** a boundary-level strip also touches `findUsersByStatus` (`users.ts:150`) and `findCareEligibleUsers` (`:221`). Neither leaks — but `findCareEligibleUsers` is **Reflection's population feed.**
- 🔴 **An open CROSS-RUNTIME question rides this lane and must NOT be answered from inside product context.** *Does a typed DB helper that returns a whole row into an agent's context, bypassing a column-level firewall applied only elsewhere, also exist in `evryn-team-runtime`?* ⚠️ ACf already answered the **write**-side analog for that runtime; **the READ side is unanswered, and "the write side was fine" is not an answer to it.** **Route to ACf via Justin. Do not let this lane answer it.**

## Lane 5 — Commission the Gate-B reviewer

**Approved-to-commission and never commissioned.** A full-load scout established its brief is **not** refactor-dependent — its acceptance criteria are behavioural and black-box — **so it runs in parallel with everything above.**

- **Brief:** `evryn-backend/docs/gate-b-plan.md` (308 lines), on branch `gateb/plan`.
- ⚠️ **Read its §8 first — it is a six-item recommend-first ballot for Justin**, and item 1 (*"commission the harness-promotion lane now"*) is the reviewer's own stated blocker. **Batch that ballot into your morning report rather than blocking on it.**
- **Its §7.5 is unusually important:** nothing in that plan has been executed, and it says so by name.

---

# §3 — THE RHYTHM

**Every ping to `#team-alerts`, prefixed `AC0-38:`.**

- **`Need you:`** — only when Justin must physically act. High bar. **Ping, then keep advancing everything not blocked on it.**
- **`No action needed - just progress:`** — every state change worth knowing.
- **The heartbeat canary** — a persistent `Monitor` (~20 min) → a one-line `No action needed - heartbeat:`. 🔴 **Before claiming "healthy," verify the active subagent ADVANCED** — check its **worktree git state** (new commits, dirty files), **not** the transcript file size, which reads 0 bytes mid-run and lies. No advancement → *"possibly stuck — may need you,"* never *"all healthy."*
- **`Done for the day`** — always the LAST ping, carrying the one-paragraph outcome. 🔴 **`TaskStop` the canary with it.** Justin: *"I don't want you to finish at 1am and have it firing every 5 min until I show up in the morning, burning a turn every time — I'll go broke paying for those tokens."*

**Batch, do not block.** Justin is asleep. Put genuine-Justin questions in **one numbered, recommend-first list** in your morning report and keep advancing everything else. **A blocking question is not a stop.**

**If you compact: STOP.** Do not ride it. Re-read this file and your load list from disk, tell Justin you compacted, and expect a re-spin. The 2026-08-12/13 experiment settled this — *"the compaction gave you each a garbled understanding — you were both so confidently wrong."*

---

# §4 — REACHING AC0-36's SUBAGENTS (if you must)

**Justin is leaving AC0-36's session up so its six subagents stay resumable overnight. They did not compact; only AC0-36 did.**

🔴 **AC0-36 IS HALF-LOBOTOMIZED AND DOES NOT REGISTER IT. It will try to help. Its help would break things.** *(Justin, 2026-08-13, after watching it volunteer content it should not have.)*

**The only safe path — follow it exactly:**

1. **You write an appendage into the target subagent's brief file yourself.**
2. **AC0-36 relays ONE sentence, VERBATIM:** *"You have a note in your brief at `<path>`, section `<N>`. Read it and respond by appending to that same file."* **It composes nothing.**
3. **The subagent appends its answer to that file.** AC0-36 is out of the content path.
4. **You `Monitor` the file's mtime + `git HEAD`, emitting only on change.** 💰 A Monitor bills only for lines it emits, so a 20-second poll costs nothing while the file sits still. **Poll aggressively; emit stingily.**
5. ⚠️ **Agree a sign-off and `TaskStop` the watcher**, or it fires on every unrelated commit forever.

**Only ACm/ACm2, ACb and the sweep lead have briefs to append to.** Lane E's DC, Lane E's QC and the six M-lanes have none — you must create a file first.

**AC0-37's judgement: you probably will not need this.** All six were closed out adversarially and everything extractable is in the findings doc and the lane briefs. **Use it as insurance, not as a plan.**

---

# §5 — THE GATES. None of these move because Justin is asleep.

- 🔒 **No push. No merge. No deploy. No prod migration.** Absolute.
- **Commit authority tonight:** Justin has granted an explicit standing authorization for this night — **you may commit source-of-truth docs and build-level code.** 🔴 **You must list every source-of-truth commit in your morning report, in plain English, so he can vet the diffs after the fact.** Code is above his pay grade; **prose instruction files are not, and he often has strong opinions about them.**
- **Commit by pathspec ON THE COMMIT:** `git commit -F - -- path/a path/b`. A bare `git commit` takes the whole index including another agent's staged work. **And `git diff <file>` FIRST** — a by-path commit takes that file's entire working-tree state, and `git status` will not warn you.
- **Verify the branch before every EDIT**, not just every commit.
- **Three dots on every branch diff:** `git diff main...branch`.
- **One branch ↔ one worktree.** You own the worktree lifecycle for everyone you spin. **Provision each with a real `npm ci` — never a junction.** 🔴 **Count `node_modules` before and after every reap; `npm ci` if it dropped.** That hazard has fired three times and printed nothing each time.
- 🔴 **DO NOT REAP any existing worktree.** Justin's standing instruction until he clears it.
- **Identity files are not yours.** `evryn-backend/identity/**` is Mira's and Justin's; the team's is Justin's. **Coordinated, never folded quietly into a build.**

---

# §6 — WHAT "DONE" LOOKS LIKE IN THE MORNING

1. **The cutover** either **completed** (steps 1–3 clean, redirects landed, both transitional notes updated) or **hard-stopped with a named reason.** Both are acceptable outcomes; a half-done cutover is not.
2. **Four build lanes** each either **merged** (DC + QC + you agree, build-level code, per the standing rubric) or **stopped at a named blocker.** ⚠️ **QC verifies every real code change — even small ones.** *"It's tiny"* is not a reason to skip her; that just leaves you as a single point of failure.
3. **The Gate-B reviewer** commissioned, with its §8 ballot batched for Justin.
4. **One report, at CEO altitude**, opening with `# AC0-38's read — …`: what shipped in plain English, what is blocked and on whom, **every source-of-truth commit listed**, and one clean numbered recommend-first ballot.
5. **A handoff to AC0-39**, re-spin-complete. **The test is not *"is it complete?"* — that returns yes. It is *"could a stranger finish this from the file alone?"*, which returns the gap.**
6. **The canary stopped.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
