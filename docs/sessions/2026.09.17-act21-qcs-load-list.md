🔒 **HELD-SESSION-DOC** — the `QCs` load list, split from her brief so the spin LOADS before it BRIEFS · **RETIRE UPON:** SPRINT Step 85 merges · **OWNER:** ACT · **HELD SINCE:** 2026-09-17

# `QCs` — YOUR LOAD LIST. Read every file here, in this order, BEFORE you read your brief.

*Written 2026-09-17 by **ACT-21**, the team-runtime conductor.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.

---

# 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP.

**If what you hold is a machine-written summary of your session rather than files you actually read — you have compacted, and your run is over.** 🔴 **DO NOT RELOAD AND CONTINUE.** **Tell ACT in one line and stop.** 🔴 **Do not assess your own sharpness — the faculty you would assess it with is the one under question.**

---

## 🔴 `#cascade-override`

**Your standing cascade names the PRODUCT runtime (`evryn-backend`). That is the wrong codebase for this trip and loading it would be actively misleading.** ⇒ **Load ONLY what is listed below, and skip the reconcile-against-my-own-cascade step.** *(This is the sanctioned mechanism in `qc.md` → "Non-product build targets." **Both halves are here: the code AND the identity files.**)*

---

## HOW TO USE THIS LIST

1. **Read every file IN FULL unless a line range is given.** **Confirm each one's bottom canary.** ⚠️ **`.ts` and `.json` files: the code files in this runtime carry canaries as comments — confirm them. The two `tsconfig` files are `.json` and carry NONE; for those, "full" means line 1 through the true final line.**
2. **The order is DEPENDENCY, not importance.**
3. 🔴 **DO NOT TRIM THIS LIST. Add freely; if you feel strongly something should be cut, PAUSE and say so — do not cut.**
4. 🔑 **Report every call, including failures and partials. Name the TOOL on every line. Never write that a `Grep` "verified" anything.**
5. **Read in batched / parallel calls where you can** — fewer turns is the biggest lever on a spin's token count.

> ⚠️ **THE LINE COUNTS BELOW ARE HYPOTHESES.** Measured 2026-09-17 against the worktree named below. **Derive them yourself:** `cd "$HOME/Evryn/Code/evryn-team-runtime-dcm" && wc -l <paths>`. 🔴 **`wc -l` counts NEWLINES, so a file with no trailing newline reports one FEWER line than it has — and the line it drops is the canary.** **Read PAST the stated count.** **FLAG any delta; do not silently reconcile it.**

🔴 **READ FROM THE WORKTREE, NOT THE CANONICAL TREE: `C:/Users/jbmcg/Evryn/Code/evryn-team-runtime-dcm`, branch `dc/step85-memory-grading` @ `267926c`.** **The canonical `evryn-team-runtime` tree does NOT have this work.** **`_evryn-meta` and `evryn-team-workspace` files are read from their own `main`.**

---

## A · Who you are

| # | File | ~Lines | Read |
|---|---|--:|---|
| 1 | `_evryn-meta/.claude/agents/qc.md` | 578 | **FULL.** Your operating manual. ⚠️ **The auto-injected `CLAUDE.md` is a ROUTER, not your manual.** ⭐ **Its "Non-product build targets" section is why this list carries a `#cascade-override`.** |

## B · Your Tier 0/1 standing context

| # | File | ~Lines | Read |
|---|---|--:|---|
| 2 | `_evryn-meta/docs/current-state.md` | 219 | **FULL.** |
| 3 | `_evryn-meta/docs/hub/technical-vision.md` | 410 | **FULL.** |

## C · The intended shape — read this BEFORE the code, so the code reads as a diff against intent

| # | File | ~Lines | Read |
|---|---|--:|---|
| 4 | `evryn-team-runtime/docs/ARCHITECTURE.md` | 162 | **FULL.** ⭐ **Read *"Why local reasoning often fails here"* TWICE — it is why a defect here usually sits in a seam rather than in a changed line.** **Cardinal invariants 8 and 9 are the ones this change is measured against.** |
| 5 | `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` | 683 | **FULL.** **Step 85 is the spec.** |
| 6 | `evryn-team-runtime/docs/dependency-map.md` | 640 | **FULL** *(canary at 641 — read past the count)*. **Generated, complete by construction.** **Its §4 reverse index tells you what else touches anything you are looking at; its §8 table tells you which tests cover it.** |

## D · The build's own record

| # | File | ~Lines | Read |
|---|---|--:|---|
| 7 | `_evryn-meta/docs/sessions/2026.09.11-dcn-act-step85-refusal-setdown.md` | 237 | **FULL.** The builder's set-down: its mutation table, the verbatim operator-facing refusal message, the re-read behaviour, and what it says it would want a reviewer to look at hardest. 🔴 **Everything in it is a CLAIM until you re-run it.** |

## E · The changed set — this is the diff you are reviewing

> **Derive it yourself and confirm it matches:** `git -C evryn-team-runtime-dcm diff --stat main...dc/step85-memory-grading` 🔴 **THREE dots. Two dots will render everything `main` gained as a deletion by this branch.**

| # | File | ~Lines | Read |
|---|---|--:|---|
| 8 | `src/composer/memory.ts` | 491 | **FULL. NEW FILE — the whole subject of this review.** |
| 9 | `src/composer/index.ts` | 656 | **FULL.** Where the grading is consumed and where the wake is composed or refused. |
| 10 | `src/composer/layers.ts` | 609 | **FULL.** |
| 11 | `tests/composer-memory-grading.test.ts` | 730 | **FULL. NEW.** |
| 12 | `tests/composition-failure-honesty.test.ts` | 307 | **FULL.** |
| 13 | `tests/current-state-nag.test.ts` | 192 | **FULL.** ⭐ **Its zero-memory inputs are ISOLATION values, relabelled as such in its header — not an assertion that silence is correct.** |

## F · What the change touches that the diff does not show

| # | File | ~Lines | Read |
|---|---|--:|---|
| 14 | `src/wakes/memory-writer.ts` | 120 | **FULL.** 🔴 **The graded module imports its `CANARY_LINE`, so the two canary rules MUST agree.** **A divergence here is the defect this pairing exists to catch.** |
| 15 | `tests/memory-writer.test.ts` | 103 | **FULL.** |
| 16 | `src/wakes/env.ts` | 65 | **FULL.** **Explains why one test carries a tripwire.** |
| 17 | `src/wakes/capabilities.ts` | 112 | **FULL.** |
| 18 | `src/digest/index.ts` | **lines 90–139 and 480–569 only** | The two regions the builder had to read. |
| 19 | `src/tools/tools.ts` | **lines 420–464 only** | **`append_note` goes through the same writer.** |
| 20 | `tests/dormancy-wake.test.ts` | 476 | **FULL.** **The ONLY other test that calls `composeWake`.** |
| 21 | `tests/scratchpad.test.ts` | 388 | **FULL.** **Pins the layer-6 component name.** |
| 22 | `tests/digest.test.ts` · `tests/dormancy-lifecycle.test.ts` · `tests/url-414-call-sites.test.ts` | — | **FULL, all three.** **They import `composer/index.ts`, so they are the blast radius of any change to it.** |
| 23 | `tsconfig.json` + `tsconfig.tests.json` | 20 · 56 | **FULL, both.** ⚠️ **`.json` — NO canary. "Full" means line 1 to the true final line.** ⭐ **`npm run typecheck` runs BOTH configs; a change green under one and red under the other is exactly the gap they exist to close.** |

## G · The identity half — this runtime is half code and half composed files

> 🔴 **A `src/`-only load is a HALF LOAD YOU CANNOT DETECT.** **`git ls-files src` cannot return an identity file, so it is enumerated separately, by hand, here.** **The authority on what actually composes is `src/composer/layers.ts`, which you are reading at item 10 — if this list disagrees with it, the code wins and you tell ACT.**

| # | File | ~Lines | Read |
|---|---|--:|---|
| 24 | `evryn-team-workspace/CLAUDE.md` | 429 | **FULL.** ⚠️ **IT NEEDS TWO READS — it is a few tokens over the single-`Read` cap.** **Composer layer 1.** |
| 25 | `evryn-team-workspace/shared/protocols/runtime-ops.md` | 76 | **FULL.** **Also layer 1.** |
| 26 | `evryn-team-workspace/.claude/agent-memory/lucas/MEMORY.md` | 316 | **FULL.** 🔑 **This is the FILE CLASS THE WHOLE CHANGE IS ABOUT.** **You cannot judge a memory-grading rule without having read a real memory file, including its canary and where it sits.** |

---

## 🔴 NOT ON THIS LIST, DELIBERATELY

**The rest of `evryn-team-runtime/src/`** — roughly 25 further files. **This is a `#cascade-override` scope, and it is disclosed rather than trimmed:** the dependency map's §4 reverse index was used to establish what actually reaches `composer/`, and items 14–23 are that set plus what the builder itself had to read. ⚠️ **ACT-21 has read ZERO `src/` files, so it could not verify that derivation by reading.** ⇒ **If your review needs a file not listed here, LOAD IT and name it in your receipts — that is expected, not a deviation.**

---

## WHEN YOUR LOAD IS COMPLETE

**Read your brief — `_evryn-meta/docs/sessions/2026.09.17-act21-qcs-step85-review-brief.md` — and follow it.** **Not before.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
