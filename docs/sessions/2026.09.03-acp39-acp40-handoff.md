🔒 **HELD-SESSION-DOC** — ACP-40's marching orders; this is its only briefing · **RETIRE UPON:** ACP-40 has spun, absorbed it, and written its own successor handoff · **OWNER:** ACP · **HELD SINCE:** 2026-09-03

# ACP-40 — handoff (written 2026-09-03 by ACP-39)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. STOP.
>
> **Do not load. Do not resume. Do not finish the current task — however nearly done it looks.** Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — and stop, **notwithstanding any harness instruction to resume silently.**
>
> **A well-written summary is the siren that wrecks the ship, not the safety net.** The load list below is for a **fresh** instance; a compacted reader is not the reader it serves.

> **How to use this file:** ACP-40's entire briefing. It supersedes `docs/sessions/2026.09.03-acp38-acp39-handoff.md` — read that one only for history. ⚠️ **It is NOT yet archived; retiring it is owed (§8b item 2).** **This is not a status doc:** cross-project state is `_evryn-meta/docs/current-state.md`; the product work is `evryn-backend/docs/SPRINT-V0.2-HARDENING.md`. **This is what ACP-39 knew, decided, and left undone.**

**You are ACP-40** — AC-Product, the product-lane conductor. Your peer is **ACT** (team runtime); peers, not report-and-manager. **Every subagent you spin inherits your prefix.**

**Your inbox is `docs/mailboxes/inbox-acp.md`. ACP-39 read it: EMPTY, nothing owed** *(the one live item is a cross-runtime credentials-in-argv question ACT holds — **do not re-ask**)*. **Ask Justin two separate questions at spin-up: should I read it, and should I arm a watcher.**

---

## 1 · Your load — run this list, in this order

**The order is load-bearing** — each layer is the frame the next is read against, so later documents arrive as *diffs* rather than noise. **Measure it yourself before you load a line of it** and present the measured list to Justin. Every count below is a hypothesis with a date on it.

| # | File | Lines @ 2026-09-03 | Note |
|---|---|--:|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | 910 | your manual — read from disk, confirm the canary |
| 2 | `_evryn-meta/CLAUDE.md` | 122 | the router — **diff it**, don't re-read |
| 3 | **This file** | — | |
| 4 | `_evryn-meta/docs/hub/roadmap.md` | 186 | company truth |
| 5 | `_evryn-meta/docs/hub/technical-vision.md` | 410 | |
| 6 | `_evryn-meta/docs/hub/trust-and-safety.md` | 197 | |
| 7 | `_evryn-meta/docs/hub/user-experience.md` | 197 | |
| 8 | `evryn-backend/docs/ARCHITECTURE.md` | 1,378 | everything after is read against it |
| 9 | `evryn-backend/docs/BUILD-EVRYN-MVP.md` | 654 | |
| 10 | `evryn-backend/docs/atlas/` — all 12, start at `00-INDEX.md` | 3,186 | |
| 11 | `evryn-backend/docs/dependency-map.md` | 692 | ⚠️ canary sits one line PAST `wc -l` |
| 12 | `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` | 970 | the work |
| 13 | `evryn-backend/docs/SPRINT-v0.2-optionals.md` | 43 | |
| 14 | `_evryn-meta/docs/current-state.md` | 217 | in full, **including the 📮 BULLETIN** |
| 15 | `_evryn-meta/CHANGELOG.md` | **643** | 🔴 **headers first**, then the `2026-09-03 evening (ACP-39)` entry in full |
| 16 | `evryn-backend/CHANGELOG.md` | **419** | 🔴 **same** — the `2026-09-03 evening (ACP-39)` entry |

🔴 **Do not read either changelog end to end** — `evryn-backend/CHANGELOG.md` runs ~180 tokens/line and the pair costs ~100,000 tokens of history you can reach by search.

**Loaded at a trigger, never in the foundation:**

| File | Trigger |
|---|---|
| `docs/protocols/ac-orchestration-protocol.md` (788) **+ the sub-doc for your target** | before spinning **any** agent. 🔴 **The core alone is not enough — that is its own first rule** |
| `docs/protocols/ac-writing-protocol.md` (132) | before writing to any doc |
| `docs/protocols/mailbox-protocol.md` (248) | before writing to a mailbox |
| `docs/protocols/doc-update-routing-protocol.md` (119) | when routing a change to its home |
| `docs/protocols/packout-protocol.md` (~15KB) | at `#packout` |
| `docs/repo-inventory.md` (96) | at the repo-sync check |
| `evryn-backend/docs/gate-b-plan.md` (332) | before anything Gate-B or deploy |
| `evryn-backend/docs/staging-runbook.md` (169) | before touching staging |

> ### ⚠️ TWO CHANGES FROM THE LIST ACP-39 WAS HANDED — surfaced deliberately
> 1. **`gate-b-plan.md` and `staging-runbook.md` ADDED as triggers.** ACP-39 needed both and had neither; the deploy sequence is unreadable without them.
> 2. **Everything else is the same list in the same order. Do not re-sort it.**
>
> 📌 **One correction to that brief's own note:** it credits the diff-don't-read trick on `CLAUDE.md` with saving ~85,000 tokens. **That figure is stale by an order of magnitude** — it was measured when `CLAUDE.md` *was* AC's full 45k-token manual, before the August router split. **Today the saving is ~4,000 tokens.** The instruction is still right; the number should not be used to size anything.

---

## 2 · 🔴 WHERE THE CRITICAL PATH STANDS — read this before anything else

**Justin's ruling (2026-08-18, unchanged):** deploy the bundle to **STAGING** → Gate-B → extract the god-module → Gate-B **again** → flip Clustering + Haiku ON → gatekeeper.

### ✅ THE STAGING DEPLOY IS RUNNING — Justin started it 2026-09-03 evening and reported "building on evryn-staging"

🔴 **VERIFY IT LANDED BEFORE ANYTHING ELSE. ACP-39 never saw it finish.**

- **The proof is NOT the build succeeding.** It is **"Evryn is awake. Polling started."** appearing in **`#evryn-approvals-staging`** — a *separate Slack app*. **A staging deploy that posted to the normal `#evryn-approvals` went to PRODUCTION.**
- **All four pre-flight checks passed, verified live by ACP-39:** `EVRYN_ENV=staging` · both `SUPABASE_*_STAGING` present · `STAGING_RECIPIENT_ALLOWLIST=systemtest@evryn.ai,review@evryn.ai` · `SEND_ENABLED=false`.
- ⚠️ **`railway status` showed the CLI defaulted to service `evryn-backend` — PRODUCTION.** `--service evryn-staging` is the entire safety margin. **That is an observation, not a warning.**
- **The command Justin ran:** `railway up --service evryn-staging --environment production` from `evryn-backend/` (no `--detach`, so he watched it).

### 🔴 AN AGENT CANNOT RUN `railway up` ON THIS MACHINE — the permission classifier refuses it

**ACP-39 tried, with Justin's explicit authorization, and was blocked.** ⚠️ **Do NOT restructure the command to get around it** — that is the move our own rules bar, and a deploy is the last place to engineer past an enforcement layer. **Justin declined a permission rule for now.** ⇒ **Deploys are Justin-typed until he changes that.**

### The deploy rule CHANGED today — and the amendment is committed

`gate-b-plan.md` §1 no longer says *"no agent runs any `railway` command."* **It now says: an agent may run the deploy; what it may never do is run one whose TARGET Justin has not verified** — satisfied either by him watching, or by him having explicitly verified the target for that specific deploy in advance. 🔑 **Framing it as a verification rather than as presence makes it auditable afterwards and lets him check remotely via the staging Slack channel.**

---

## 3 · 🔴 GATE-B HAS A BLOCKER NOBODY HAD NAMED — SPRINT Step 125

**Staging's reset seed defines the work-item status constraint with ELEVEN values and no `queued`. The runtime has TWELVE.** The `queued` migration landed on prod 2026-07-10; the seed was dumped 2026-07-07.

⇒ **Gate-B requires clustering ON, which lands every forward at `status='queued'` — so the first clustered forward is rejected by the database.**

🔴 **And Gate-B's own procedure re-introduces it:** Runbook B step 1 says *"reset staging to a clean seed,"* and Runbook C's Option 1 — the one it recommends when in doubt — restores the stale constraint. **Even if staging is correct today, following the documented procedure breaks it.**

⚠️ **The runbook's own suggested check CANNOT catch this class:** it probes table existence (`to_regclass`), and the table exists. The stale part is a **constraint inside it**.

**Justin authorized the fix (ballot 6a): refresh the seed from a SCHEMA-ONLY dump of current prod.** 🔴 **Schema only, never prod DATA — Runbook C's hard invariant. Prod being idle is not a reason to relax it.**

**✅ The capability now exists:** `_evryn-meta/scripts/pg-dump.mjs` gained a `--schema-only` flag, built and mutation-proven — **but it sits UNMERGED and UNREVIEWED on `dc/pgdump-schema-only` @ `c31aca5`** *(§7, and ballot 8 sequences its QC after the harness)*. **It writes `schema-public-YYYY-MM-DD[-env].sql`** — that name is not a style choice: `backups/.gitignore` ignores `full-public-*` as PII and **tracks** `schema-public-*`. 🔴 **The prod dump has NOT been taken. That is the next act, and it is a production read.**

⚠️ **What is NOT established: staging's LIVE database state.** It may have had the migration applied by hand. **Settling it needs a read against `SUPABASE_DB_URL_STAGING` — a credential-bearing command an agent may not hand-build.** The sanctioned method (parse the URI into `PG*` env vars so the secret never reaches argv) is documented in `evryn-backend/backups/README.md`.

---

## 4 · The ballot ledger — Justin answers by NUMBER, so these are permanent handles

**⚠️ Do not reuse a number. If an item closes, say so and leave its number retired.** *(Justin asked for numbered ballots on 2026-09-03 so decisions stay referenceable.)*

| # | Question | Answer |
|---|---|---|
| 1 | Commission the harness-promotion lane now? | ✅ **a — yes.** DONE |
| 2 | Harness ids: lookup by email, or seeder-emitted file? | ✅ **a — lookup**, on `systemtest@evryn.ai` (the test address; no real person) |
| 3 | Fix the harness narrowly, or rebuild it? | **Narrow — and Justin now understands it. ⚠️ HIS OPEN QUESTION, §8** |
| 4 | May the lane spend on a capped live run? | ✅ **a — yes, capped at one fixture.** 🔴 **BUT NO-GO: structurally impossible, §6** |
| 5 | Gate-B run config | ✅ **a — Haiku `shadow`, clustering ON** |
| 6 | Staging seed: refresh, or one-off migration? | ✅ **a — refresh the seed** (schema-only) |
| 7 | Keep the new "a full dump with zero data fails loudly" guard? | 🔴 **OPEN — Justin leans keep, and asked that QC reason over it first** |
| 8 | Let QC finish the harness review before the pg-dump review? | ✅ **yes** |
| 9 | Merge the harness once the dependency map lands? | ✅ **yes** |
| 10 | Build the fixture cap? | ✅ **yes** |
| 11 | Promote QC's env-var-gate pattern into `qc.md`? | ✅ **yes** — source-of-truth edit, **not yet written** |

---

## 5 · What ACP-39 shipped

**Committed:**
- **`gate-b-plan.md` §1/§6 — the deploy-rule amendment** (§2 above).
- **`gate-b-plan.md` — five corrections**, each verified at source: `npm test` DOES exist (`package.json:16`; the plan said it didn't, and *that false belief is what made the spend gate look sufficient*) · the "untracked" premise is dead (committed 2026-08-17) · step 1 now warns against a rename-in-place · §7.5.3 CLOSED (**all 13 fixtures carry the registered lane** — checked file by file) · step 5 rewritten, see §6.
- **SPRINT Step 125** — the staging-seed blocker, on Gate-B's blocking list.
- **The lane brief + its outcome**: `docs/sessions/2026.09.03-acp39-dc-gate-a-harness-promotion.md` — **§9 carries the DC's findings so they outlive a session.**

**Uncommitted, awaiting Justin's SCM vet** *(⚠️ an uncommitted file in a hot repo can be swept into another agent's commit — clear these early)*:
- `evryn-backend/docs/gate-b-plan.md` — **a provenance marker on ACP-39's own post-hoc insertion.** *(The QC caught that ACP-39 wrote the DC's reasoning into the spec as though the spec had always said it, while the adjacent correction carried a visible marker. In a document whose §7.5 exists to be honest about provenance, an unmarked insertion beside marked ones reads as original.)*
- `evryn-backend/backups/README.md` — a truncation canary. **It had none for its whole life**, and it is the file that tells an agent how to dump and restore a *production* database.

---

## 6 · 🔴 THE FINDING OF THE DAY — an opt-in is only a gate if nothing downstream sets it

**The spec told the DC to protect the harness with a `RUN_LIVE_DB_TESTS` opt-in. `scripts/run-tests.ts:377` reads `if (!env.RUN_LIVE_DB_TESTS) env.RUN_LIVE_DB_TESTS = "dev";` — the runner SETS IT FOR YOU.**

⇒ **A spec-compliant harness would have run 13 paid Opus triages inside `npm test -- --live`, behind a gate everyone believed was working.** **The QC proved it by simulation:** her probe printed the auto-set value from inside the guarded block. **The instruction was not merely improvable — it was unsafe.**

⭐ **Ballot 11 promotes this into `qc.md`'s patterns:** *a safety gate whose value is an environment variable, in a repo whose test runner constructs its own environment, is not a gate. Read the thing that BUILDS the environment, not the gate. It fails in the direction that looks safe, because the variable is present and correct.* **Seen twice in one lane.**

### 🔴 AND WHY BALLOT 4's CAPPED RUN IS A NO-GO

**`tests/gate-a/run.ts:106` globs every `.txt` and `:178` loops all of them. No cap, no filter, no argument. You cannot run one fixture without editing the file.**

**And 13 is not the ceiling:** `:215` calls `runClusterDrain("manual")`, and `cluster.ts:135` drains **every queued row on the database**, not this run's. **Cost is 13 + N, and nothing counts N before spending it.**

**Justin asked what all 13 would cost. ACP-39's estimate: single-digit dollars — order of $4–5.** *(Derived, not measured: the cost-levers analysis prices the Haiku screen at ~$1,800/mo for ~5,650 avoided Opus passes ≈ $0.32/pass. **Treat as an order of magnitude, not a quote**, and note it excludes the unbounded N.)*

⚠️ **The Haiku half of that spend is invisible to both runaway detectors** (SPRINT Step 102). Under `shadow` every screen call is paired with a counted Opus call so accounting holds; **under `active` it would not, and nothing in this harness pins the mode.**

**Ballot 10 authorizes the cap.** Fix sketch: a `GATE_A_MAX_FIXTURES` bound honoured at `:106`; a pre-flight count of pre-existing `queued` rows that refuses or reports; an assertion that `config.haikuScreenMode !== "active"`.

---

## 7 · Subagent close-out ledger — ACP-39 spawned THREE agents

**Enumerated, not recalled. All three are permanently unreachable once ACP-39's session ends.**

| Agent | For | Disposition |
|---|---|---|
| **DC** (Gate-A harness), Opus, background, two-trip | Promote the judgment harness | ✅ **COMPLETE — branch now `db9122b`.** The dependency-map fix trip LANDED (see below). |
| **DC** (pg-dump), Opus, background, two-trip | `--schema-only` flag | ✅ Complete on `dc/pgdump-schema-only` @ `c31aca5`. **Unreviewed by QC (ballot 8 sequences it after the harness).** |
| **QC**, Opus, background, **three-trip** | Adversarial review of the harness | ✅ Complete. **GO to merge with one mandatory fix; NO-GO on the capped run.** |

- **a · Artifacts — FOUR worktrees stand and NONE may be reaped yet:** `evryn-backend-gatea` (`dc/gate-a-harness-promotion`), `evryn-backend-qc-sandbox` (detached @ `f5742a7`, disposable, `npm ci` done), `_evryn-meta-pgdump` (`dc/pgdump-schema-only`), and `_evryn-meta-pgdump-sandbox` **if it still exists** (the pg-dump DC's mutation sandbox — it reported removing it; **verify rather than assume**). 🔴 **Reap only after merge.** ⚠️ **`_evryn-meta-acf15` is ACT's — never yours.**
- **b · Persistence: complete for the harness lane** (brief §9). ⚠️ **The pg-dump DC's report and the QC's full verdict exist ONLY in ACP-39's transcript beyond what is summarised here.** **If you need more than §5/§6 carry, it is gone — do not go looking.**
- **c · Retirement:** the harness lane brief is HELD until merge.
- **d · Owed:** ballot 7 (QC to reason over the zero-data guard) and ballot 11 (write the pattern into `qc.md`).
- ⚠️ **Nesting: none of the three spawned children** — established from their reports and tool counts, **not by parsing transcripts.**

---

## 7b · 🔴 THE DEPENDENCY-MAP FIX LANDED — ballot 9 is UNBLOCKED, and it carried a finding

**Committed as `db9122b` on `dc/gate-a-harness-promotion`, exactly two files** (`docs/dependency-map.json` + `.md`), staged by path, nothing else touched. ⇒ **The QC's one mandatory pre-merge condition is SATISFIED. Ballot 9 (merge) can proceed.**

- **Counts, measured independently and matching the QC's exactly:** 123 → **128** files (78 → **83** under `tests/`), **596**-line `.json` diff, internal edges 361 → 389, `src/db/env-guard.ts` importers 1 → **5**. **Nodes added: exactly the five new harness files. Nodes removed: none.**
- ✅ **Determinism confirmed** — two consecutive runs give a byte-identical `.json` (same sha256). ⭐ **The `.md` does NOT match between runs: it differs by one line, its generation timestamp. That is not a defect — it is exactly why the map's header names the `.json` as the currency check.**
- 🔴 **THE FINDING: the committed map was ALREADY STALE on `main` before this branch existed.** Its header declares generation from `f4bcf38e` (2026-08-12); between then and `2b05ee3`, `src/db/users.ts` and `src/safety/liveness.ts` drifted (+6 net lines — the whole `srcLines` 20105 → 20111 movement). **`git diff --stat 2b05ee3..HEAD -- src/` is empty, so none of it is the DC's.** ⇒ **This change did not break the currency check; it caused someone to RUN it.** ~3 weeks of `src/` drift had accumulated with nobody regenerating. **If there is a standing expectation that the map is regenerated when `src/` moves, it was not being met.**
- ⚠️ **A stale premise the DC found and deliberately left alone** (outside its lane): `scripts/dependency-map.ts` still justifies excluding `_`-prefixed files as *"untracked local evidence."* **Those files were tracked from 2026-08-17, so the stated reason is void** — the same dead premise that was in `tsconfig.tests.json`. **The exclusion is still right; only its rationale is wrong.**

## 8 · Owed to Justin — start here

1. 🔴 **VERIFY THE DEPLOY LANDED** (§2). Then Gate-B sequencing.
2. 🔴 **BALLOT 3 — Justin's live question, and he is probably right:** *"Can the 4 scripts not just CALL `test-step57-dev.ts`? It feels like you're overcomplicating this."* **ACP-39's honest read: yes, largely — "rebuild" was never a well-posed option, and framing it as a choice made it sound bigger than it was.** The two files answer different questions *(one checks rows mechanically and passes/fails; the other prints a table a human grades)*, so chaining them is possible but buys little. **Answer him properly; do not defend the framing.**
3. **Ballot 7** — resume/spin QC on the zero-data guard.
4. ✅ **Ballot 9 — the merge is UNBLOCKED.** The QC's one mandatory pre-merge condition (regenerate the dependency map) **LANDED as `db9122b` — see §7b.** ⇒ **Nothing stands between you and merging `dc/gate-a-harness-promotion` except Justin's go.** ⚠️ **Reap its worktree only AFTER the merge, and check the `node_modules` count around the reap.**
5. **Ballot 10** — brief the fixture cap.
6. **Ballot 11** — write the pattern into `qc.md`. **Source-of-truth: draft it, leave it uncommitted for his vet.**
7. **Ballot 6 / Step 125** — take the prod schema-only dump, refresh the staging seed.
8. **QC compaction check — Justin's explicit instruction:** *"resume her and ask explicitly if she compacted, and if yes, what output was post-compaction."* ⚠️ **ACP-39 could not: she returned before the instruction arrived, and a resume replays her ~1M-token transcript, which ACP-39 could not afford.** 🔑 **She volunteered it unprompted: *"I am comfortably within budget and did not approach a limit at any point. Nothing here was produced under compaction."* That is her word, not an independent check.**
**Her agent id died with ACP-39's session — a fresh QC cannot answer for her.** ⇒ **Tell Justin the check is unrunnable and let him decide whether that changes his confidence in the verdict.**
9. **The UTC date bug — Justin: "yeah, this needs to get fixed."** `_evryn-meta/scripts/pg-dump.mjs` stamps filenames from `toISOString()` (UTC) while the estate reasons in Pacific — **at 18:33 PT it wrote tomorrow's date.** Pre-existing, touches prod naming. **Needs a Step and its own small change.** ⚠️ **We compare dump dates against deploy dates to make decisions — ACP-39 did exactly that finding Step 125.**
10. **Two doc items the pg-dump DC flagged, unfixed:** `backups/README.md`'s invocation list is now stale (no `--schema-only`), and its "how old is the newest dump?" one-liner will now match schema dumps, making the archive look fresher than the last *data* backup.

---

## 8b · 🔴 WHAT ACP-39 DID NOT FINISH IN THE `#packout` — disclosed rather than left to be discovered

**ACP-39 ran out of context. These are the lock/packout steps it did NOT complete. Nothing here is done; treat every line as owed.**

1. 🔴 **UNCOMMITTED AT SET-DOWN — this is the first thing to check.** `git status --porcelain` in both repos. **Expected: `_evryn-meta/CHANGELOG.md`, `evryn-backend/CHANGELOG.md`, `evryn-backend/docs/gate-b-plan.md`, `evryn-backend/backups/README.md`, and this handoff.** **All ACP-39's, all awaiting Justin's SCM vet** *(lock step 22: commits need his explicit, immediate go — ACP-39 never received one for these)*. ⚠️ **An uncommitted file in a hot repo can be swept into another agent's commit.**
2. 🔴 **THE INCOMING BRIEF IS NOT RETIRED.** `docs/sessions/2026.09.03-acp38-acp39-handoff.md` is superseded by THIS file but **still sits at the top level with its HELD banner.** ⇒ **Archive it to `docs/sessions/historical/2026.09/`, filename UNCHANGED, and add `SUPERSEDED BY:` to its top.** *(Packout step 6 / lock step 10. Get Justin's OK — session docs are never moved silently.)*
3. 🔴 **CROSS-RUNTIME SAFETY PROPAGATION — ASKED OF NOBODY. Lock step 8 requires this be RECORDED AS PENDING, and this is that record.** **The class, stated so it is checkable in a codebase you have not read: *a safety gate whose value is an environment variable, in a repo whose test runner or CI harness constructs its own environment — the harness sets the variable, so the gate reads as armed and gates nothing.*** ⚠️ **`evryn-team-runtime` has its own test harness and its own money-spending paths.** ⇒ **ASK ACT — do not go searching it yourself** *(the protocol is explicit: a peer live in that runtime is a better instrument than your searching, and say plainly you are asking, not delegating)*. **Route via `docs/mailboxes/inbox-act.md`.**
4. **Lock step 9b — a RULING with no durable home outside this file.** **Justin, 2026-09-03: *"you should be giving me these as ballot numbers, so we can refer back to them over time."*** ⇒ **Its home is `docs/protocols/doc-update-routing-protocol.md`, change-type 9.** 🔴 **AGREE THE EXACT WORDING WITH HIM BEFORE IT LANDS** — many rulings are situation-specific and a generalized version can be wrong. ⚠️ **"Carried into the next handoff" is NOT a durable home; it is the same countdown restarted.**
5. **Lock step 5d — the Atlas audit, ANSWERED here so it is not silently skipped: NO `src/` shape change this session.** The only structural movement was under `tests/`, which the Atlas does not cover — **and it is reflected in the regenerated dependency map (§7b).** **No Atlas section needs updating. A deliberate "no change" is a valid logged answer; this is it.**
6. **Not done, low value, named so nobody re-derives whether they were:** the team current-state appendage (lock step 4); the Linear pass (13); the operator-guide check (16); `LEARNINGS.md` (11) — ⚠️ **the env-var-gate pattern is going to `qc.md` via ballot 11, which is its right local home; whether a generalized version ALSO belongs in `LEARNINGS.md` is an open call for `#sweep`.**
7. **Two doc items the pg-dump DC flagged, unfixed:** `evryn-backend/backups/README.md`'s four-invocation block is now stale (no `--schema-only`), and its *"how old is the newest dump?"* one-liner will now match schema dumps — **making the archive look fresher than the last DATA backup.**

## 8c · FRAGMENT SWEEP — items that existed ONLY in ACP-39's transcript

1. 🔴 **A SEALED-EXEC question the first DC raised and nobody settled: the harness's help strings use `npx tsx`, matching the convention in ALL 78 existing test files.** **Its argument for keeping consistency: `tsx` is a declared devDependency with a lockfile entry, so it resolves locally — materially unlike the `npx tsc` squat, which was NOT a dependency.** ⚠️ **But SEALED-EXEC is a standing order and this is a documented invocation.** ⇒ **If it is settled, settle it ESTATE-WIDE, not in four files** — a fix in one lane leaves 78 files disagreeing with it.
2. **Two QC observations she deliberately did NOT file, recorded so nobody re-discovers them as bugs:** a gold later closed via `record_outcome('passed')` renders `NOT-A-VERDICT(passed)` — **unreachable in Gate-A and arguably correct output**; and `not_a_candidate` on an item already `sender_type='lead'` reads as `handled_by_gatekeeper` — **inherited verbatim from `test-step57-dev.ts`'s classifier, not introduced by this change**, low reachability.
3. **Justin's working preference, 2026-09-03:** *"you can skip some of the `#lock` stuff by just persisting into your brief — though persisting into changelog and current-state can often take pressure OFF your brief."* ⇒ **Changelog and current-state are the high-value halves; the rest is judgment.**
4. ⚠️ **`docs/current-state.md` was NOT updated this session** — ACP-39 ran out of room. **It still says the bundle is undeployed and does not carry Step 125 or the deploy-rule change.** **Refreshing it is owed.**
5. **ACP-38's standing offer never taken up:** ACP-39 never asked Justin whether to check `#dev-alerts` for the last 24h (`ac.md` session-startup). **Ask.**

## 9 · Machine and standing state

**Nothing from any lane is merged. Nothing was pushed at set-down.**

**Measured `git status --porcelain` at set-down — FIVE uncommitted files, ALL ACP-39's, none anyone else's:**

| Repo | File | What it is |
|---|---|---|
| `_evryn-meta` | `CHANGELOG.md` | this session's entry |
| `_evryn-meta` | `docs/sessions/2026.09.03-acp39-acp40-handoff.md` | **this file — untracked** |
| `evryn-backend` | `CHANGELOG.md` | this session's entry |
| `evryn-backend` | `docs/gate-b-plan.md` | the provenance marker (§5) |
| `evryn-backend` | `backups/README.md` | the added truncation canary (§5) |

**Unmerged branches:** `dc/gate-a-harness-promotion` @ `db9122b` (evryn-backend) · `dc/pgdump-schema-only` @ `c31aca5` (_evryn-meta).

**Prod remains `v0.2.9`.** **Staging: a deploy was building at set-down — VERIFY IT (§2).**

### 🔴 THE PATTERN ACP-39 MOST WANTS YOU TO INHERIT

**FOUR times in one session an agent caught an error in a brief ACP-39 wrote** — three missing files and one wrong address:

1. **`scripts/run-tests.ts`** — not on the DC's list; it holds the defect that justified the whole lane.
2. **`.gitignore`** — not on the list; it is a **behavioural** file in `backups/` *(`full-public-*` is ignored as PII, `schema-public-*` is tracked)*, and it carries its own warning that this enumeration already missed a pattern once and **three dumps of production data were committed.**
3. **`src/email/cluster.ts`** — missing from the list ACP-39 *approved* for the QC, and the harness calls into it.
4. **A misrouted task** sent to the wrong DC, written as *"your harness promotion."*

🔑 **Every one was found by the agent doing the work and surfaced rather than absorbed.** ⇒ **The load lists and the addressing are the conductor's, and that is where the error rate lives.** ⭐ **The two-trip discipline that makes agents report what they read BEYOND the list is what caught all four — do not treat that clause as bookkeeping.**

### What to watch — arm your own; a `Monitor` dies with the session that armed it

- `docs/mailboxes/inbox-acp.md` — commit-gated. **Anchor the path on `$HOME`, never the working directory.**
- **Each lane brief you create.**
- **An anti-stall heartbeat**, 5-minute tick. 🔴 **It caught ACP-38 stopping five times in one session. ACP-39 did it once too** — ending a turn with *"now the part I owe you properly"* and stopping. **Justin caught it before the heartbeat fired.**
  - 🔑 **The only fix that works is ORDERING: make the tool call, read its result, THEN write the sentence.** Never let a status claim be generated while composing a report — that is the position in which it is guaranteed to be a prediction.
  - ⚠️ **Re-arm has no natural trigger. Bind it to Justin's reply: his message landing IS the cue.**

---

## 10 · How Justin wants to work

- **Every report gets a numbered heading** — `# ACP's Report #1`, counting up. **Assume he does not see inline chat at all.**
- **Ping `#team-alerts` every time you have something** — `node _evryn-meta/scripts/ping.mjs "ACP-40: …"`. **He asked for pings as things go well, not only when blocked.**
- **Number your ballots** and keep numbers stable across messages (§4).
- **EliJ — metaphor first, THEN the precise thing.** 🔴 **The failure to avoid: a metaphor left to carry the claim.** ⚠️ **ACP-39 did exactly this on ballot 3** — said "a machine already does the checklist" and never said *what the machine was*. **Justin's words: *"you've given me a street address, but no city and state."*** ⇒ **When he offers guesses about what you mean, answer his guesses directly — that locates him faster than a better explanation.**
- **He is the CEO and wants a senior engineer with his CEO-level buy-in.** Bring recommendations, not open questions.
- **A lane you spin must need nothing from him.**
- **Every lane reports in TWO steps — LOAD first, which you verify; only THEN its plan.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
