🔒 **HELD-SESSION-DOC** — the material recovered from ACP-39's session that is not in the ACP-40 handoff, plus the persistences it owes · **RETIRE UPON:** every item in §7's owed-table has landed in a durable home (or been explicitly dropped by Justin) · **OWNER:** ACP · **HELD SINCE:** 2026-09-03

# What ACP-39's session produced that its handoff does not carry

*Written 2026-09-03T21:04:01-07:00 by **ACP-39a**, a half-step instance Justin spun between ACP-39 and ACP-40 to audit the handoff before ACP-40 inherits it.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> **How to use this file:** it is a **companion to `2026.09.03-acp39-acp40-handoff.md`, not a replacement for it.** The handoff is ACP-40's marching orders and has been corrected in place; this file holds the material that would have bogged the handoff down — recovered subagent findings, a tooling haul, and the list of persistences still owed. **Read the handoff first. Come here when it points you here.**
>
> 🔴 **Justin's scoping, 2026-09-03: nothing in here was persisted to its permanent home tonight, deliberately** — *"I just don't have the time tonight to vet all that. I want to make sure it's properly captured, but let's just put a note in the handoff that these persistences are owed."* ⇒ **§7 is that note in full. This file is a holding pen with a testable exit condition, not a home.**

---

## 0 · How to weigh what is in here

**Everything below is one of three kinds, and each is labelled:**

- **VERIFIED AT SOURCE** — I read the file, ran the git command, or read the transcript record myself. Cited.
- **RECOVERED** — quoted from a subagent's returned report in ACP-39's transcript, with its record number. The report is real; whether its *claim* is true is a separate question, and where I checked one I say so.
- **MY READ** — my judgment, flagged as such.

**Transcript citations are `L<n>`** — the line number in `C:\Users\jbmcg\.claude\projects\c--Users-jbmcg-Evryn-Code--evryn-meta\caffb457-cb76-49ef-bf8c-14fa3643c34d.jsonl`, ACP-39's session. That file holds the **whole** session, before and after its compaction — compaction replaced the context, not the log.

---

## 1 · 🔴 PERISHABLE — three subagent transcripts are still on disk, and the handoff told ACP-40 they were gone

**VERIFIED AT SOURCE, 2026-09-03T20:0x PT.** The ACP-40 handoff §7b originally read *"it is gone — do not go looking."* **That was false**, and it is the most costly kind of wrong: an *instruction* rather than a fact, so it forecloses rather than merely misinforming. Corrected in the handoff; recorded here with the detail.

| Agent | Transcript file | Size | Records | Tool calls |
|---|---|--:|--:|--:|
| **QC** — adversarial review of the Gate-A harness | `aeda3a815fd82936f.output` | 2.74 MB | 400 | 108 |
| **DC-1** — Gate-A harness promotion | `a42e9bc0eb44488c9.output` | 1.59 MB | 371 | 94 |
| **DC-2** — `pg-dump --schema-only` | `a7e344ae51c391ea7.output` | 0.56 MB | 215 | 48 |

**Directory:** `C:\Users\jbmcg\AppData\Local\Temp\claude\c--Users-jbmcg-Evryn-Code--evryn-meta\caffb457-cb76-49ef-bf8c-14fa3643c34d\tasks\`

These are the agents' **complete working transcripts** — everything they did and considered, not only what they reported. Their *returned reports* are additionally recoverable from ACP-39's own session file.

⚠️ **THE CLOCK: this is a TEMP directory.** A temp sweep or a machine restart takes it. **The honest statement is not "this is safe" — it is "this exists now and will not exist forever."** Everything in §§2–4 below was recovered from the returned reports; the internal transcripts are the part still unharvested.

🔑 **The mechanism worth keeping, because it is why this was missed in the first place (VERIFIED AT SOURCE).** A background subagent's report reaches its conductor by **two different paths**, and only one of them looks like a message. If the conductor is idle, the report arrives as an ordinary conversation record. **If the conductor is mid-turn, it arrives through the message queue and is stored as a `queue-operation` record** — which every ordinary reading of a transcript skips. **DC-1's entire report stream (three reports, records L539, L701, L1072) arrived that way and is invisible to a normal read.** The QC's and DC-2's reports appear twice, once by each path.

---

## 2 · 🔴 The QC's verdict — the parts that did not reach the handoff

**RECOVERED from L1028/L1034.** Her verdict was deliberately split: *"GO to merge — with one mandatory pre-merge fix, and the live path explicitly UNVERIFIED"* and *"NO-GO on the authorized capped run."* The handoff carries the split, the NO-GO reasoning and her headline finding. **It does not carry the following, and two of them bear directly on ballot 9.**

### 2a · 🔴 She put a limit on her own GO

> *"Evryn's live tool-calling through the post-③ hooks — **a GO on an interactive pathway built on stubbed or gated coverage is not a clean GO. Mine is not.**"*

**That is the reviewer telling you how far to trust her own verdict, and ballot 9 is a merge decision resting on it.** It is also the exact standing principle SPRINT Step 88 records (*"a GO built on stubbed coverage does not count"*) — applied by a reviewer to herself.

**Her four unverifiable items, in her order:**
1. The dirty-DB pre-flight actually refuses — needs a DEV database, **no spend**.
2. The clustering drain actually fires — needs a DEV DB, `CLUSTERING_MODE=manual`, and **real spend**.
3. The live tool-calling above.
4. **Whether the 13 fixtures produce the expected verdicts — *"the harness's entire purpose. Never run."***

### 2b · 🔴 Two verdict branches will likely never fire, and she said this must be written down

> *"No fixture is designed to produce `handled_by_gatekeeper` or `not_a_candidate`. These branches are correct against `verdict.ts`'s matrix but will likely stay unexercised by this fixture set — **worth knowing before treating a clean run as coverage.**"*

⇒ **A clean Gate-A run is not coverage of those two branches.** Whoever reads a green Gate-A report needs this beside it.

### 2c · Three of the six harness files carry only the gate that does not gate

**Her Finding 3.** `run.ts` carries both gates. **`seed.ts`, `review.ts` and `times.ts` carry only the auto-set `RUN_LIVE_DB_TESTS` gate** — the one the whole lane exists because it cannot gate. And **`seed.ts` writes**: `findOrCreateUser` (`:103`), `registerGatekeeperInboundAddress` (`:113`), an `append_pending_note` RPC (`:142`).

> *"For `seed.ts` it is one-deep, and the one is the subdirectory… The whole change exists because someone was about to perform exactly that rename."*

### 2d · Her triage of DC-1's four unverified items

| DC-1's item | QC's ruling |
|---|---|
| Dirty-DB pre-flight refusal | **Highest value.** *"If it fails, Gate-A certifies a run that never happened."* DEV DB, no spend. |
| Clustering drain path | **Partly, and less than it looks** — `grading.ts` prints `NOT-A-VERDICT(queued)` and `run.ts` reports it under INTEGRITY, so *"failure is visible rather than silent."* |
| `findUserByEmail` resolving the seeded gatekeeper | **No.** The abort path is loud by construction (`review.ts:53–62`). *"The old failure mode — a stale UUID matching zero rows and printing clean — is structurally gone."* |
| The two branches never firing | **Confirmed; must be written down.** → §2b. |

### 2e · Her recommendation for the next lane, and her closing read

> *"§7.1 should gain a depmap step before the next lane runs against it."*

> *"This lane has now produced two findings of the same shape… Both were real obligations living in files outside the load list, and both were found by the person doing the work rather than by the spec."*

### 2f · A structural property she credited to DC-1

> *"ESM hoists the static imports above the gate blocks, so the gates run **after** those imports evaluate. They still hold because every static import is genuinely side-effect-free."*

**MY READ:** that is a real invariant with no guard on it — a future import with a side effect would break the gates silently. Worth a line wherever the harness's gating is described.

### 2g · Her own compaction statement, unprompted

> *"I am comfortably within budget and did not approach a limit at any point. **Nothing here was produced under compaction.**"*

**Relevant to the handoff's §8 item 8**, which records Justin's instruction to resume her and ask explicitly. See §5c.

---

## 3 · DC-1 (Gate-A harness) — two corrections that were never applied

**RECOVERED from L701; the gap VERIFIED AT SOURCE against the handoff's own list.** DC-1 made **six** corrections to `gate-b-plan.md`. The handoff records *"five corrections, each verified at source."* **Two of the six did not land and are recorded nowhere as owed:**

1. **§7.4's line numbers are wrong** — the hardcoded UUID is at `_gate-a-review.ts:14`, **not `:8`**; the `external_id` list is at `_gate-a-times.ts:34–41`, **not `:28–35`**.
2. 🔴 **§7.2(a) is correct but INCOMPLETE, and this one is substantive.** The skip branch is real (`process.ts:447–459`), **but an item at status `new` routes to `resumeParkedItem` and IS re-triaged.** ⇒ *"So a dirty DB can yield a **mixture** of stale and fresh rows, which is worse than uniformly stale."* **`gate-b-plan.md` still carries the incomplete version, and Gate-B's procedure depends on it.**

**Also recovered, and durable-worthy — its own load-list gap report:**

> *"`scripts/run-tests.ts` (full) — not on the list, and it is the file the entire sequencing trap turns on… **This is the gap in the load list worth carrying forward: any lane that changes what lives under `tests/` needs the runner that discovers it.**"*

**And one design choice worth keeping** (its own words): `grading.ts` was created as *"the single home for the ③ disposition matrix. **Two copies is precisely the shape where a seventh verdict gets added to one and silently missed in the other.**"*

---

## 4 · DC-2 (pg-dump) — a close-out that did not happen

**RECOVERED from L1044 → L1051 → L1058. VERIFIED AT SOURCE:** the handoff's subagent ledger records this agent as *"✅ Complete"* with no mention of any of the below.

**What happened:** ACP-39 sent the dependency-map fix trip **to the wrong DC**. DC-2 **refused it**, with four reasons and **four blocking questions**, and proved the misroute by timestamps — *"Git authorship cannot distinguish us — every agent on this machine commits as 'Justin McGowan (laptop)'… The timestamps and my own session record are what settle it."* Its stated reasons included:

> *"I have no load for this lane — none."* … *"There is very likely a live DC holding this lane."* … *"**A misroute is itself information you need. If I absorb it silently, your routing stays broken and nobody learns.**"*

**ACP-39 then decided not to reply** (L1058), on the reasoning that DC-2 had already dropped the task so a reply was not worth a full transcript replay. **Defensible on cost — but it leaves an agent holding four unanswered blocking questions**, which packout step 3d ("confirm it has no open question waiting on you") does not permit.

**Its three flagged-not-fixed items** *(the UTC bug is in the handoff; the other two are §7's items)*: the README's four-invocation block is stale (no `--schema-only`); the README's *"how old is the newest dump?"* one-liner now matches schema dumps, **making the archive look fresher than the last DATA backup**.

**And its own load-list gap report:**

> *"A future brief touching `backups/` should carry `.gitignore` on the load list — **it is a behavioural file there, not boilerplate**."*

---

## 5 · Process findings about ACP-39's own close-out

### 5a · Packout step 4 did not run, and was never disclosed
**VERIFIED AT SOURCE (L1105 thinking; absent from Reports #17 and #18 and from §8b).** Packout step 4 requires a top-to-bottom fresh-eyes re-read of **both** briefs. ACP-39 read the outgoing one only, decided in its own reasoning to skip the incoming one, and surfaced that to nobody. §8b is honest about the **lock** steps and silent about the **packout** ones.

### 5b · The session ended in a compaction
**VERIFIED AT SOURCE.** Compaction fired at `02:24:18Z`; Justin caught it three seconds later. **§8c — the fragment sweep — was authored inside a ~3,000-token window immediately before that.** Neither fact is in the handoff.

### 5c · The heartbeat ran unarmed for ~100 records
**RECOVERED (L955 `TaskStop`, no re-arm observed through L1066).** ACP-39 stopped its heartbeat and worked four reports, two `SendMessage`s and two `Edit`s without one — the precise failure the incoming brief spent three paragraphs warning about, and which ACP-38 had demonstrated the day before.

**MY READ:** the incoming brief's own diagnosis is the right one and it held up perfectly — *"the re-arm has no natural trigger. The pause happens when you notice you are blocked; nothing at all happens when you become un-blocked."*

---

## 6 · 🔧 THE TOOLING HAUL — five instrument failures, none of them written down anywhere

**`ac.md` keeps a machine-gotchas box for exactly this class. None of these is in it.** Every one produced a **confident, wrong-looking output rather than an error**, which is the property that makes them worth the space.

### 6.1 🔴 The permission classifier blocks compound and heredoc Bash forms, unpredictably — and nobody has diagnosed why
**RECOVERED, three independent instances in one session:**
- A `git commit` using a **heredoc** was blocked. **The same commit with `-m` flags succeeded.**
- A compound `git status --porcelain && echo && git status --porcelain` was blocked — **containing no credential, no ping and no outbound tool.**
- A single `git -C <absolute-path> status --porcelain` **succeeded.**

⚠️ **This does NOT match the documented secret-read-plus-outbound-tool signature** that `ac.md` and the router describe. **The cause is unknown.** The workaround that worked every time: **one command, absolute paths, no heredoc.**

### 6.2 🔴 A mutation can silently fail to apply on LINE ENDINGS and print a clean pass
**RECOVERED from the QC's execution table.** Her mutation G1a did not apply — a CRLF/LF mismatch — and the run printed `ALL PASSED`. **She reported the pass as a failure, proved the mutation applied on disk (occurrence count 1 → 0), and re-ran it; it then drove 2 assertions RED.**

🔑 `qc.md` already says *prove the mutation actually landed.* **This is a new mechanism for how it doesn't** — and on this machine `core.autocrlf` is `true`, so it is a standing hazard rather than a one-off.

### 6.3 The Bash cwd resets between calls, and it bit twice in two different disguises
**RECOVERED.** Once as a **doubled repo name** — `Cannot find module 'c:\…\Code\_evryn-meta\_evryn-meta\scripts\ping.mjs'` — and once as a plain `No such file or directory` on a file that demonstrably exists. **Neither reads as a cwd problem.** Fix: absolute paths, or lead with `cd /c/Users/jbmcg/Evryn/Code &&`.

### 6.4 `ls -la` mtimes on `backups/*.sql` are meaningless
**RECOVERED and VERIFIED AT SOURCE.** Every migration file reports `2026-07-19` — a bulk touch. **The FILENAMES carry the real timeline.** This matters directly: **SPRINT Step 125's entire argument is a dating argument**, and anyone re-deriving it from mtimes gets a confident wrong answer.

### 6.5 `pg_dump` exits 0 whether or not a flag you passed took effect
**RECOVERED.** Which is why DC-2 built a contents assertion, and why its mutation caught a real failure the exit code could not see: *"`pg_dump` itself exited 0 on that run. The exit code could not have told you anything — the contents assertion is the only thing that caught it."*

### 6.6 Smaller, still worth the line
- **The safe Railway variable-read pattern:** `railway variables --service <svc> --kv | cut -d= -f1 | sort` for names; a targeted `grep -E "^(EVRYN_ENV|SEND_ENABLED|…)="` for non-secret values. **Never print the whole `--kv` output.**
- **The Railway CLI defaults to service `evryn-backend` — PRODUCTION.** `--service evryn-staging` is the entire safety margin.
- **`identity/core.md`'s canary is at line 282 with 283 blank** — a real off-by-one against the brief that named it.
- **A *vendored* `.claude/settings.local.json` exists at `evryn-website/node_modules/es-abstract/`.** It is a dependency's file, **not ours, and must not be deleted** by the session-startup hygiene step. Neither `_evryn-meta` nor `evryn-backend` has one.
- **`SPRINT-V0.2-HARDENING.md` is too dense to `Read` above ~175 lines per page** — three agents hit the 25,000-token cap on it independently today.
- **The Read tool numbers one line past `wc -l`** on a file whose last line has no trailing newline. Trust the canary, never the count.

---

## 7 · 🔴 THE OWED PERSISTENCES — what still has to reach a permanent home

**Justin's ruling, 2026-09-03: none of this was written to its permanent home tonight.** Each row names **where it goes**, so the obligation cannot be satisfied vacuously by saying it was "routed."

| # | What | Destination | Kind |
|---|---|---|---|
| **P1** | The five tooling gotchas + the six smaller ones (§6) | **`_evryn-meta/.claude/agents/ac.md`**, the machine-gotchas box under *Context Discipline* — **source-of-truth, needs Justin's SCM vet** | local-first |
| **P2** | 6.1 (the classifier) also belongs in the **router's** ping section, which currently describes only the secret-plus-outbound signature | `_evryn-meta/CLAUDE.md` — **source-of-truth, Justin's vet** | local |
| **P3** | 6.2 (mutation fails on line endings) | **`_evryn-meta/.claude/agents/qc.md`**, Patterns This Role Watches For — beside the existing baseline entry | local |
| **P4** | §2a–2d — the QC's self-limited GO, the two-branches-never-fire ruling, Finding 3, and her §9d triage | **SPRINT Step 88** (Gate-B) for the coverage caveats; the **Gate-A lane brief §9** for the rest | local |
| **P5** | §3 — DC-1's two unapplied corrections | **`evryn-backend/docs/gate-b-plan.md` §7.4 and §7.2(a)** — needs Justin's authorization (source-of-truth) | local |
| **P6** | §2e — *"§7.1 should gain a depmap step"* | `evryn-backend/docs/gate-b-plan.md` §7.1 | local |
| **P7** | §4 — DC-2's four unanswered blocking questions and the misroute | **The Gate-A / pg-dump lane record**; and the two README items are `evryn-backend/backups/README.md` | local |
| **P8** | Justin's ruling: *"you can load all those in milliseconds. You do this all the time."* | **`docs/protocols/doc-update-routing-protocol.md`, change-type 9** (lock step 9b) — 🔴 **agree the wording with him first** | ruling |
| **P9** | Justin's ruling: *"you should be giving me these as ballot numbers, so we can refer back to them over time"* | Same — change-type 9. **Already recorded as owed in the handoff §8b item 4** | ruling |
| **P10** | The generalizable half of §6.1/§6.2 — *an instrument that reports success while having done nothing is the estate's dominant failure shape* | **`LEARNINGS.md`**, un-numbered appendage in the Unpromoted section. ⚠️ **Local half (P1–P3) goes FIRST** — `LEARNINGS.md` drains at `#sweep`, which can be weeks away | general |
| **P11** | §5a/§5b — that packout step 4 did not run and the session compacted | Nowhere permanent; **it is captured in the corrected handoff and here.** Retire with this doc | — |
| **P12** | The three subagent transcripts (§1) — **if anything more is wanted from them, harvest before the temp directory is swept** | Their content would go to P4/P7 | 🔴 perishable |

### 7a · Lock-protocol steps this session did not run, and why
**Recorded so the absence is a logged answer rather than a silent skip.**

| Step | Disposition |
|---|---|
| 1 · changelogs | **Owed.** Justin deferred permanent-home writes tonight. ACP-39's own entries are already written and uncommitted. |
| 2 · deploy log | **N/A** — no production deploy. Justin ran a **staging** deploy; the ledger tracks prod. |
| 3 · current-state | **Owed.** It still says the bundle is undeployed and carries neither Step 125 nor the deploy-rule change. *(Already in the handoff as §8c item 4.)* |
| 4 · team current-state | **Owed.** Already in the handoff §8b item 6. |
| 5a–c · sprint / build / architecture | **No change this session** — ACP-39a wrote no code and shipped nothing. Step 125 already landed. |
| 5d · Code Atlas | **No `src/` shape change this session.** ACP-39 answered this in §8b item 5; I re-confirm it — no `src/` file was touched by anyone today. |
| 5e / 11 · learnings | **Routed to P1–P3 (local) and P10 (general), not written.** |
| 6 · identity files | **N/A** — no change under `evryn-backend/identity/`. |
| 7 · Hub & spokes | **N/A** — no company-level truth changed. |
| 8 · cross-runtime safety propagation | **Already recorded as pending** in the handoff §8b item 3 (the env-var-gate class → ACT). **Unchanged; still owed; still nobody's until it is asked.** |
| 9 · decisions / ADRs | **No architectural decision** this session. |
| 9b · rulings | **P8 and P9.** |
| 10 · retire stale docs | 🔴 **DELIBERATELY NOT DONE.** The protocol requires Justin's explicit OK and forbids moving a session doc silently. The incoming brief's retirement is already the handoff's §8b item 2. |
| 13 · Linear | **N/A** — no tickets of this session's making. |
| 14 · Bitwarden | **N/A** — `.env` untouched. |
| 15 · mailboxes | ACP-39 read `inbox-acp.md`: **empty**. Unchanged. |
| 16 · operator guide | **No operator-facing behaviour changed.** |
| 17 · DB backups / schema-reference | **N/A** — no migration ran. Newest archival dump is `2026-09-02`, inside the week. |
| 18 · auto-memory | Checked: carries only the DO-NOT-WRITE-HERE notice. **Clean.** |
| 19 · settings.local.json / worktrees | **No `settings.local.json`** in either repo (only the vendored one at §6.6, which is not ours). **Worktrees: none reaped** — all four standing trees belong to live lanes or to ACT. |
| 20 · fragment sweep | **Run** — this document and the handoff corrections are its output. |
| 21 · subagent close-out | **Run** — see §8. |
| 22–23 · commit | Justin's call. |

---

## 8 · ACP-39a's own subagent close-out

**Enumerated, not recalled.** Four agents spawned; two were killed by a user interrupt before doing any work and produced nothing.

| Agent | For | Disposition |
|---|---|---|
| **ACP39a-C**, Opus, background, two-trip | Transcript records L1–L670 | ✅ Closed. Load verified 26/26 files, canaries confirmed; three errored calls disclosed with their recovery. |
| **ACP39a-D**, Opus, background, two-trip | Transcript records L671–L1069 | ✅ Closed. Load verified 26/26; zero failed calls on the work trip. |

- **a · Artifacts: NONE.** Both were read-only by brief. No worktree, no branch, no uncommitted file. Verified by `git worktree list` and `git branch --list` in both repos afterwards.
- **b · Persistence: this document.** Their reports were long and good; **their substance is here and in the corrected handoff. The full reports die with ACP-39a's session** — that is a known and accepted loss, disclosed rather than hidden.
- **c · Retirement: n/a** — neither held a brief.
- **d · Nothing owed.** Neither raised a blocking question or a `NEEDS-JUSTIN`.
- **e · Nesting: none.** Both were barred from spawning and no orphan appears in any listing.

---

## 9 · What ACP-39a did NOT do

**Stated plainly so silence is not mistaken for coverage.**

1. **I did not read `evryn-backend/src/`.** Every runtime claim here is either quoted from an agent that did read it, or verified by me against a *non-`src/`* artifact (a migration file, a test file, `git`). Where I verified, I said so.
2. **I did not harvest the three subagent internal transcripts** (§1). Their *returned reports* are recovered; their working records are not.
3. **I ran no test, no mutation and no live probe.** Everything is reading and `git`.
4. **I did not read the compaction summary** at L1246 deliberately — it is a third-party compression of the session, and the source records were available.
5. **I did not verify anything about the live staging database**, the Railway environment, or whether the deploy landed. That needs credentials and Slack, and it remains ACP-40's first job.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
