🔒 **HELD-SESSION-DOC** — ACT-A-P2's brief · **RETIRE UPON:** ACT-A-P2 posts `PACKOUT COMPLETE` in the exchange log and Justin has ruled on the finished handoff · **OWNER:** ACT (the ACT-A side-project line, not the main ACT line) · **HELD SINCE:** 2026-09-14

# ACT-A-P2 — BRIEF: take P1's audit, read the rest of ACT-A's session, and finish the `#packout` so the handoff is 100%

*Written 2026-09-14 by **ACT-A**. Justin launched you with three sentences; this file and your load list are everything you need.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you do not see it, re-read until you do.
>
> **How to use this file:** §1–§2 are who you are and what you are walking into; §3–§4 are your inputs and your channel; **§5 is trip 1 (report and stop); §6 is the work, which starts only when BOTH ACT-A's sign-off and P1's `P1 DONE` are in the exchange log**; §7 is a list of items ACT-A already knows about, for you to verify; §8 is the boundaries.

---

# 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP.

**If what you hold is a machine-written summary of your session rather than files you read, you have compacted.** **Do not reload and continue** *(Justin: "the agent just sails through it and confidently flails about, unloaded")*. **You may do exactly one thing:** append one line to the exchange log — `ACT-A-P2: I compacted at <timestamp>. Stopping.` — commit it, and stop. **Not a summary of where you got to.** ⚠️ **Justin's 2026-09-14 "re-spin in place" ruling was for ACT-A only; it does not extend to you.** **Do not assess your own sharpness.**

---

## §1 — Who you are, and the situation you are walking into

**You are `ACT-A-P2`, an AC instance in your own Claude Code session.** In `being-an-admiral.md`'s terms **you are a Captain, and your Admiral is ACT-A** — the instance that wrote this brief. **Justin launched you, but you do not report to him and you do not ping him:** ACT-A is his sole conduit, and your channel is the exchange log (§4). **Cap every heading you write at `##`.**

**Your manual is `_evryn-meta/.claude/agents/ac.md`,** which you have loaded. **The router's hard rule holds: if your identity did not load, stop and report the failure.**

**The situation, in plain terms:**
- **ACT-A is an alternate-branch ACT** — not the main ACT line, which is at ACT-21 — working a side project with Justin today. Its question, in his words: *"Should I keep building the autonomous agents in the Claude Agent SDK or is another direction better?"*
- **ACT-A worked a long session** (16:04 to 18:01 UTC before the break): loaded context, ran a whole-runtime scout (**Scout-F**), fixed `ARCHITECTURE.md`, fetched Anthropic's documentation, reported to Justin in three numbered reports.
- **Justin then told it to run a full `#packout`** — *"treat all of the pieces of that like an astronaut or surgeon's checklist … **don't skimp** on that."* ACT-A turned its session doc into a handoff.
- 🔴 **ACT-A COMPACTED at 18:00:56 UTC (11:00:56 Pacific) before the packout was finished.** Packout steps 4 and 5 — the fresh-eyes re-reads — never ran.
- **So the handoff has never been checked against the conversation it is supposed to carry, and ACT-A cannot do that check**, because a compacted instance cannot trust its own memory. **Justin's fix is two instances loaded identically to ACT-A, reading the actual transcript:**
  - **ACT-A-P1** reads the first two-thirds and appends an addendum to the handoff listing everything it found missing or wrong — **without changing the handoff itself.**
  - **You** read P1's addendum, read the rest of the transcript — the last third, then everything after the compaction — and **do the final `#packout` pass on the handoff, so that it is 100%.**

**Justin's words about the design, verbatim:**
> *"sub-1's output isn't for you - it's for sub-2, who will read sub-1's output and then continue reading the transcript, and then will take all of that and do the final #packout pass on the handoff and make sure it's 100%."*
>
> *"let's make them independent instances, so that they can communicate with eachother - so that sub-2 can bounce back to sub-1 and say 'hey, so you said xyz in your report, and I found out abc later, so I'm incorporating x way' or whatever"*

**And the authorization boundary, verbatim:** *"I don't authorize *any* work beyond this right now - the *only* authed work is to get the #packout that was *supposed* to be done before compaction done. Once the handoff is right, then I'll explicitly authorize next steps."*

**Who reads your finished handoff next:** most likely **ACT-A itself** — compacted and reloaded, continuing once Justin authorizes next steps — or, if Justin decides otherwise, a fresh ACT-A. **It has to work for both.**

## §2 — How much ACT-A has actually read, and what that means for you

**ACT-A is compacted and reloaded.** It holds the same load you hold, plus a mechanical profile of the transcript. **It has NOT re-read the conversation.** It *has* read everything after the compaction live — but it did that as a compacted instance.

⇒ **Everything this brief says about what happened came from ACT-A's compaction summary, a handful of targeted checks, and its post-compaction work. Treat every factual claim in it — including every item in §7 — as a LEAD, not a fact.** **Where the transcript disagrees, the transcript wins; say so loudly.** **Where this brief is silent, assume ACT-A could not see, not that it checked.**

**You are the Captain of this pass.** Bring your own judgment. **If ACT-A has scoped something wrongly or assumed something false, say so in the exchange log and settle it with ACT-A before you run on it** *(`being-an-admiral.md` → "Captains command their own ships")*.

## §3 — Your inputs

1. **The handoff** — `_evryn-meta/docs/sessions/2026.09.14-acta-acta-sdk-vs-claude-code-session.md`, item 12 of your load, read at `93eeba0`.
2. **P1's addendum** — appended to the bottom of that handoff by ACT-A-P1. **It does not exist until P1 posts `P1 DONE`.**
3. **The transcript extract** — `_evryn-meta/.cache/acta-packout/`:
   - **`transcript-part2.md` — raw transcript L480 through the compaction at L824. Read it in full, to its canary.** It opens with an **OVERLAP** entry: the last numbered report ACT-A sent before your part begins, so the first message from Justin in your part has its context.
   - **`transcript-part3-post-compaction.md` — L824 to the point it was extracted. Read it in full, to its canary.** ⚠️ **Everything ACT-A did in it was done by a compacted instance.** **Justin's words in it are rulings.** It includes the load list Justin approved and every instruction that set up your pass.
   - **`transcript-part1.md` is P1's — do not read it end to end.** You MAY open specific entries in it to verify a P1 finding before you incorporate it: search it for the `#### L<n>` heading P1 cites, and read around that entry.
   - **Both parts are mechanical, script-generated renderings — no model summarized them.** Each header explains what is elided (the CONTENTS of file reads) and the legend. **They are large — page with `offset` and `limit`, and report every window.**
   - ⚠️ **`_evryn-meta/.cache/` is gitignored and machine-local; nothing may cite the extract as a source. Cite the raw transcript line, `L<n>`.**
4. **Drill-down:** `node _evryn-meta/.cache/acta-packout/show-record.js <L> [<L> …]`, run from `C:/Users/Justin/Evryn/Code`, prints any raw transcript record in full — including a file read's contents as ACT-A received them. 🔴 **Never `Read` the raw transcript directly** (`C:/Users/Justin/.claude/projects/c--Users-Justin-Evryn-Code--evryn-meta/35277727-fd2f-4919-a979-60f7e91043c1.jsonl`) — it is several megabytes.
5. **Load at the moment you need it, and report that you did:** Scout-F's captured report, `evryn-team-runtime/docs/research/2026.09.14-acta-scoutf-runtime-capability-inventory.md` *(if a question turns on what Scout-F said)*; any file a passage turns on; the four uncommitted `#lock` files named in §6 step 7.

## §4 — The exchange log is your ONLY channel

**`_evryn-meta/docs/sessions/2026.09.14-acta-actap-exchange.md`.** **Read it top to bottom before you arm anything on it.** Its header carries the rules.

**When you start:**
- **Run `ListAgents`** and put your own session address — its first line — in your first entry.
- **Arm a watcher on the exchange log** — the `md5sum` loop in `being-an-admiral.md`, section *"A session address does not survive a restart"*. **It is what wakes you when `P1 DONE` lands.**
- **Arm a heartbeat** — `ac.md`, *"HOW TO ARM ONE"*, 5-minute cadence. **Keep both running while you wait: waiting on P1 is waiting on a process, not a hold.** On each heartbeat while you wait, check the exchange log's tail and nothing else.

**Talking to P1:** questions and answers go in the exchange log, addressed `P2 → P1`. A `SendMessage` to P1's published address is a nudge only. **Before a signal, confirm P1's address resolves in `ListAgents`; if it does not, the log and P1's watcher carry it.**

## §5 — TRIP 1: receipts, plan, concerns — then STOP

**Your first exchange entry carries exactly these three things. Then stop, and begin no work until BOTH `SIGN-OFF P2` (from ACT-A) and `P1 DONE` (from P1) are in the exchange log.**

- **Receipts for your load** — every file, the tool, the line ranges you actually read, and the bottom canary you saw (or the true last line). **Report every failed, errored, refused or partial call** and what you did about it. **Name anything deferred and its trigger.** **State the commit you read the handoff at.**
- **Your plan** — in what order you will work, and **what you will NOT do**.
- **Every concern**, including anything in this brief — or in §7's list — you already believe is wrong.

## §6 — TRIP 2: finish the packout. Only after `SIGN-OFF P2` AND `P1 DONE`.

**The job, in one sentence: make the handoff something a brand-new ACT-A could pick up and be fully competent from — carrying everything from the session that matters, stating nothing false, and saying plainly what is still open.** The steps below are the order that makes that tractable. **Where one needs judgment, it is yours.**

**Step 1 — Confirm what moved.** `git -C _evryn-meta diff 93eeba0 HEAD -- docs/sessions/2026.09.14-acta-acta-sdk-vs-claude-code-session.md` should show **only P1's addendum** added, and `git status --porcelain` on that path should be empty. **If anything else changed, stop and say so in the exchange log.**

**Step 2 — Read P1's addendum in full.**

**Step 3 — Read `transcript-part2.md`, then `transcript-part3-post-compaction.md`, in full.** Hunt with the same lenses P1 used on part 1 *(copied here so you use the same ones)*: **Justin's words, verbatim where wording carries meaning** · **what ACT-A told Justin** that the handoff omits or contradicts · **commitments and open loops** · **the reasons behind decisions** · **facts at the wrong resolution** · **errors** · **packout and lock steps skipped, partial or unrecorded** · **ACT-A's private reasoning (⬜ entries) that never reached a document.** **Answer every `P2: CHECK LATER PART` P1 left you.** **Keep your own findings as `P2-NN`**, in the same shape P1 used.

**Step 4 — Settle anything unclear with P1**, in the exchange log. *(Justin's example of the point of this: "you said xyz in your report, and I found out abc later, so I'm incorporating x way.")* **Verify a P1 finding against the transcript before you rely on it** — a drill-down into its citation costs a call; a wrong finding written into the handoff costs the next instance.

**Step 5 — Decide the handoff's form, and post the decision before you make large edits.** `packout-protocol.md` step 1 asks you to choose — **clean and append · clean and carry · supersede** — and `lock-protocol.md` step 10 decides it for you when its trigger fires: ***the moment you write a correction that points at another section of the same document, that document needs superseding, not another correction.*** **The handoff was already carried once, with corrections threaded through it.** **Decide, and post the decision and your reason in the exchange log; then proceed.** ACT-A objects there if it disagrees.
- **If you supersede:** write the fresh handoff as a new file in `_evryn-meta/docs/sessions/` named by the convention in `ac.md` (date, author `acta`, recipient `acta`, a short title); give it the `HELD-SESSION-DOC` banner, the truncation canary, and the top compaction block *(rewritten to current truth — see §7, item 2)*; and **add one line at the top of the old handoff: `SUPERSEDED BY: <new path>`.** 🔴 **Do NOT move or archive the old handoff** — `lock-protocol.md` step 10 requires Justin's explicit OK before any session doc moves. **Record the archive as pending, for ACT-A to put to him.** ⚠️ **Supersede means the new doc carries EVERYTHING the old one needed to** — the old one's content being on disk does not help a reader who only opens the new one *(`lock-protocol.md` step 10: "Skimping on this is a very common failure mode")*.
- **If you carry:** fix everything in place, so that no section tells a reader to distrust another.

**Step 6 — Make the handoff right.** Incorporate every valid finding — P1's, yours, and §7's items as you verify them — **into the body, where each belongs.**
- **Justin's rulings go in verbatim** where the wording carries the meaning. **Post-compaction rulings included** — they are in part 3.
- 🔴 **The load list.** `packout-protocol.md` step 4: *"Copy it VERBATIM from the incoming brief unless you have a compelling reason to change it — and if you change it, SURFACE THAT LOUDLY."* **The list of record is the one Justin approved after the compaction** (part 3; §7 item 1). **Any change to it gets flagged loudly in the handoff and in your `PACKOUT COMPLETE` entry.**
- **The ledgers (§8 subagents, §9 uncommitted work, §10 fragments) are brought fully current**, including what happened after the compaction and what your pass itself created.

**Step 7 — Verify the `#lock` that ran as packout step 0.** Walk `lock-protocol.md` steps 1–20 against what the transcript shows ACT-A did and against the handoff's "`#lock` logged answers". **Correct the logged answers where they are wrong or missing.** For the four files ACT-A edited in its lock and left uncommitted for Justin's review — `_evryn-meta/docs/current-state.md` · `_evryn-meta/CHANGELOG.md` · `_evryn-meta/LEARNINGS.md` · `evryn-team-runtime/CHANGELOG.md`:
- **First `git diff <file>`. If anything in it is not ACT-A's, do not touch that file — record the collision.**
- **You MAY correct an error inside ACT-A's own uncommitted hunks** (a false fact, a missing caveat, a wrong reference). **Leave every such edit uncommitted** — they are source-of-truth files awaiting Justin's review.
- **Anything larger — a new entry, a new section, a missed lock step's whole write-up — goes into the handoff as an exact proposed edit, flagged for Justin's review.** Do not write it into the file.

**Step 8 — Close out the agents.** `packout-protocol.md` step 3: enumerate every agent involved, and record each in the handoff's ledger with what a fresh instance should check if its close-out did not land — **Scout-F** (a subagent of ACT-A, resumable only from ACT-A's own session `35277727-fd2f-4919-a979-60f7e91043c1`), **ACT-A-P1**, and **yourself** (standalone instances; the exchange log is your record).

**Step 9 — The audit record.** **P1's addendum must not disappear, and it must not read as live instructions.** Either keep it at the end under a heading that says it is **already incorporated above and not to be acted on**, with a one-line disposition beneath each finding (`incorporated at §x` · `rejected because …` · `for Justin: …`) and your own `P2-NN` findings beside it — **or, if you superseded, leave it in the old handoff and give the new one a pointer to it.**

**Step 10 — Commit, then the fresh-eyes passes.** Commit the handoff (and a new handoff file if you superseded) by pathspec after `git diff` shows only this pass's work. **Then run packout steps 4 and 5 — a full top-to-bottom read of the finished handoff, and one more pass asking only: *could a brand-new instance, holding none of your context, understand this fully?*** Fix what those passes find and commit again.

**Step 11 — The cross-check with P1.** In the exchange log, ask P1 to confirm that each of its findings landed correctly or was rejected for a stated reason. **Settle anything it raises**, commit, and post the result.

**Step 12 — Post `PACKOUT COMPLETE`** in the exchange log, carrying:
- **The commit(s)** of the finished handoff, and **carry or supersede** with the reason.
- **What changed**, section by section, in a few lines each.
- **Any edits you made to the four lock files** — which files, which hunks — **uncommitted**.
- **Everything that needs Justin, as recommend-first ballot items for ACT-A to put to him.** **Check the ballot numbering against the transcript before you number anything** — numbers already shown to Justin are never reused.
- **Anything not done, and why.** **If everything is done, say so plainly.**

**Then signal P1 that the packout is complete, kill your heartbeat and your watcher, and stop.**

## §7 — Items ACT-A already knows about. VERIFY each before acting on it — every one is a lead.

1. **The handoff's reload list (§0.1) does not repeat the load Justin gave ACT-A at startup.** It moved the SPRINT doc, the memory design-of-record and the dependency map to "pull at their moment," dropped both changelog skims, and never names the cascade file Justin invoked. **After the compaction, ACT-A derived a corrected list — startup items 1–16 plus a table of triggers — and Justin approved it:** *"Okay, that load list looks good."* **Its full text is ACT-A's chat message in part 3.** ⇒ **It is the load list of record.** ⚠️ **Justin's same message added the constraint that governs its item 13 (the conversation record):** *"if you load *everything* from last time, you'll compact before you even get started. That's what the handoff is for."* **Reconcile item 13 with that — and flag loudly whatever you change.**
2. **That list's item 12 said a "fresh copy should skip" the handoff's top block, and Justin corrected ACT-A:** *"why skip the top block? You *are* a compacted ACT-A."* ACT-A had meant instances like you. **The block governs a compacted ACT-A; word it so no reader — ACT-A, a fresh ACT-A, or an auditor — can take it the wrong way.** ⚠️ **Its wording is also still an OPEN ballot (⓻) — correct its facts to current truth, but do not decide the ballot.**
3. **The handoff's §3 receipts overclaim three reads made before the compaction:** the dependency map's last window ended at line 639 of 641, so its canary was never seen; both changelog skims were 90 lines with no pass over the headings; and `doc-update-routing-protocol.md` was read only at lines 77–94. **ACT-A's post-compaction reload read all three in full — record both facts.**
4. **§0.1 row 11 says Scout-F's report is to be read "once captured."** It was captured before the compaction.
5. **Nothing after the compaction is recorded in the handoff.** From part 3: when ACT-A compacted; that ACT-A said it could NOT see the compaction hook's "🔴 YOUR CONTEXT WAS JUST COMPACTED" message in its context *(the hook log at `_evryn-meta/.claude/compaction-log.txt` has NOT been checked — the handoff's top-block step 6 — and checking it is not part of this packout)*; that it committed the handoff as `93eeba0` and read it before Justin told it to; that it pinged Justin and stopped; Justin's instructions and rulings after that, verbatim; that ACT-A established from `SendMessage`'s own documentation that subagents cannot converse with each other — a subagent's messages go out under its parent session's address and replies return to the parent — which is why you and P1 are separate instances; and the heartbeat being stopped and re-armed.
6. **Numbering.** Reports **#1–#3** went to Justin before the compaction. After it, ACT-A's messages to him were **not numbered** — the stop notice, the load list, and the progress notes. **Report #4 — the packout report — is still owed.** Ballots **⓹–⑫** are still unput to Justin in chat, and the next new ballot is **⑬**. **Check all of this against the transcript.**
7. **The ledger of uncommitted and unpushed work (§9)** needs: the post-compaction commit `93eeba0`; the commits of this pass's own documents (`docs/sessions/2026.09.14-acta-actap-*`); and the gitignored working folder `_evryn-meta/.cache/acta-packout/` — the transcript extracts and two scripts. **They are machine-local, and ACT-A owns deleting them once Justin accepts the handoff.**
8. **The subagent ledger (§8)** needs you and P1 (Step 8 above).
9. **Status rows in §2** — *"Full `#packout`, no skimping — In progress"* and the in-place re-spin row — **need their post-compaction truth.**
10. **The header's *"Handoff state as of 2026-09-14T10:48-07:00"*** needs updating.
11. **§0, "What to do first,"** was written for the pre-compaction ACT-A. ⚠️ **Justin has since ruled that next steps wait for his explicit authorization.** ⇒ **§0 must say that first**, and hold the earlier plan (spot-verify Scout-F → framing report → deep dive) as the proposal awaiting his go — **not as authorized work.**

## §8 — Boundaries

- **You may edit:** the handoff (its body, freely, within this packout); a new superseding handoff file, if Step 5 calls for one; one `SUPERSEDED BY` line at the top of the old handoff, if you supersede; the exchange log (append only); and **corrections inside ACT-A's own uncommitted hunks** of the four `#lock` files (Step 7), **left uncommitted**. **Nothing else.**
- **Never move, archive or delete any document.** That needs Justin's explicit OK.
- **No subagents.** Read the transcript yourself.
- **No pushes, no merges, no pings to Justin.** Commit only the handoff file(s) and your exchange entries, by pathspec, after `git diff` shows only your work.
- **Do not read or watch `_evryn-meta/docs/mailboxes/inbox-act.md`** — the main ACT line's *(Justin, 2026-09-14)*. **Do not edit `_evryn-meta/scripts/compaction-alarm.mjs`.** **Do not read the compaction log** — that experiment is not part of this packout.
- **Do not write to auto-memory. Do not use `AskUserQuestion`.**
- **If the harness refuses a commit or an action,** write your exchange entry anyway, say plainly that it was refused, and wait. **Never route around a refusal**, and never ask another session to do it for you.
- **Finish the whole task, not the tractable part.** If you genuinely cannot finish something, do the rest and say plainly what is missing and why.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
