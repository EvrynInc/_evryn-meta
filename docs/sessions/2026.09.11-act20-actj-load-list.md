🔒 **HELD-SESSION-DOC** — the `ACTj` lane's LOAD LIST · **RETIRE UPON:** the `ACTj` lane closes out · **OWNER:** ACT · **HELD SINCE:** 2026-09-11

# ACTj — YOUR LOAD. Read every file here, in full, BEFORE you read your brief.

*Split out of the lane brief by **ACT-20** on 2026-09-11, on Justin's ruling, so that your load happens before you are told your task.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you do not see it, re-read until you do.

---

## Why this file exists, and what it asks of you

**You load first. You read your brief second.** An agent that knows its task while it loads scores each file against that task and quietly drops whatever looks irrelevant — and it never registers a moment of choosing. So this list deliberately tells you **what each file IS**, and never what it is *for*. Your brief explains the *for*, after you are loaded.

🔴 **DO NOT TRIM THIS LIST. You may ADD to it freely** — that is encouraged, and reporting what you added is how the next brief gets better. ⚠️ **If you feel strongly that something should be cut, do NOT cut it: pause, say so with your reasoning, and wait.**

**Read in the order given. The order is DEPENDENCY, not importance** — a later document is usually a diff against an earlier one, so out of order you build a picture the next document exists to correct, then pay twice to unbuild it.

### Where to read from

- **Paths are rooted at `C:/Users/jbmcg/Evryn/Code/`** — the shared parent holding every Evryn repo as a sibling folder.
- 🔴 **Every `evryn-team-runtime/...` file below is read from YOUR WORKTREE: `evryn-team-runtime-actj`** (branch `actj/money-layer`). ACT-20 brought it current with `main` at `b3488ae` on 2026-09-11, and it holds no commits of its own. **Do not read the runtime from the canonical tree.**
- Files in `_evryn-meta/` and `evryn-team-workspace/` are read from those repos' `main`.

### The line counts, and what actually proves a full read

**Counts were measured with `wc -l` at 2026-09-11 ~14:55 PT. Treat every one as a HYPOTHESIS, not a fact** — several agents work these repos at once. **Re-derive any you care about** (`wc -l <path>`) and **FLAG a delta rather than reconciling it silently**: a silent reconciliation destroys the one signal that this list was built against a different version.

⚠️ **`wc -l` counts NEWLINES, so a file whose last line has no trailing newline reports one FEWER line than it has — and the line it drops is the canary.** ⇒ **The bottom canary, not the number, is what proves a full read.** Read past the count.

**Code files carry their canary as a comment on the last line.** If one does not, **say so in your receipts** — do not invent a pass.

---

## A. Your identity

| # | File | Read | What it is |
|---|---|---|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | full — 944 | **Your operating manual.** ⚠️ The `CLAUDE.md` auto-injected into your context is a small ROUTER, not a manual. |

## B. The intended shape, then the status

| # | File | Read | What it is |
|---|---|---|---|
| 2 | `evryn-team-runtime/docs/ARCHITECTURE.md` | full — 162 | The build-altitude source of truth for this runtime: its cardinal invariants and its system map. 🔑 **Read "why local reasoning often fails here" TWICE.** |
| 3 | `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` | full — 662 | The tracker: every open item as a numbered Step, plus the GATES section (the standing preconditions on any deploy). |
| 4 | `evryn-team-runtime/docs/dependency-map.md` | full — 640 | **Generated** — what imports what, complete by construction. **§4, the reverse index, is the most useful section in it.** |

## C. The money modules

| # | File | Read | What it is |
|---|---|---|---|
| 5 | `evryn-team-runtime/src/money/budget.ts` | full — 113 | Pure math: forecast, the 60%-of-cap pitch gate, the freeze assessment, the global velocity verdict. |
| 6 | `evryn-team-runtime/src/money/ledger.ts` | full — 221 | The DB plumbing beneath it: `getJob`, `recordCostEvent`, `accrueJobSpend`, `lastHourSpendUsd`. |

## D. The wake path

| # | File | Read | What it is |
|---|---|---|---|
| 7 | `evryn-team-runtime/src/wakes/index.ts` | full — 756 | `runWake` — the SDK `query()` harness: options, hooks, the wake output contract, lossless parking. |
| 8 | `evryn-team-runtime/src/composer/layers.ts` | full — 593 | How a wake's layers are assembled and stamped into the composition manifest. |

## E. Dispatch and the surfaces that reach it

| # | File | Read | What it is |
|---|---|---|---|
| 9 | `evryn-team-runtime/src/scheduler/index.ts` | full — 876 | The deterministic 15-second loop: dispatch, the sweep cadence gates, the velocity gate, singleton lock refresh. |
| 10 | `evryn-team-runtime/src/slack/index.ts` | full — 406 | Slack ingress: mention-gated processing, thread subscription, the DB mirror. |
| 11 | `evryn-team-runtime/src/digest/index.ts` | full — 1,101 | The shared per-thread digest, its freshness sweep, and the dormancy lifecycle. |

## F. What everything here stands on — this runtime's five hub files, plus pagination

| # | File | Read | What it is |
|---|---|---|---|
| 12 | `evryn-team-runtime/src/db/paginate.ts` | full — 535 | Cursor pagination that terminates on a zero-row page, plus URL-BYTE chunking walked around it. **ARCHITECTURE's cardinal invariant 8 is written about this file.** |
| 13 | `evryn-team-runtime/src/db/types.ts` | full — 282 | The hand-maintained row types — one interface per migration table. |
| 14 | `evryn-team-runtime/src/db/client.ts` | full — 27 | The Supabase client. The one place credential selection happens. |
| 15 | `evryn-team-runtime/src/boot/env.ts` | full — 197 | Env parsing. Every branch refuses loudly; there are no fallbacks. |
| 16 | `evryn-team-runtime/src/config/agents.ts` | full — 59 | Per-agent configuration, including the EXPLICIT model pin (invariant 5 — never a floating alias). |
| 17 | `evryn-team-runtime/src/safety/notify.ts` | full — 99 | The Slack notifier every kill / park / freeze ping goes through. ⚠️ It carries a credential-hygiene fix with a real incident behind it — do not "improve" its diagnostics. |
| 18 | `evryn-team-runtime/src/safety/kill.ts` | full — 346 | Kill switches, pause states, and the gate `dispatch()` consults before any wake. |

*(Items 13–17 are four of the five files the dependency map identifies as this runtime's hubs; ten of thirteen directories depend on them and together they are small. Everyone reading this runtime should hold them.)*

## G. Migrations

| # | File | Read | What it is |
|---|---|---|---|
| 19 | `evryn-team-runtime/migrations/002_jobs.sql` | full — 53 | The `jobs` table: caps, spend, forecast, status, cap history. |
| 20 | `evryn-team-runtime/migrations/005_cost_events.sql` | full — 52 | The `cost_events` table: per-wake token and cost capture. |
| 21 | `evryn-team-runtime/migrations/012_accrue_job_spend.sql` | full — 23 | The atomic spend-accrual RPC the ledger rests on. |

⚠️ **Enumerate the full migration set live if you need it — `git -C evryn-team-runtime-actj ls-files migrations`. Never a `**/*` pathspec: `**/` requires an intervening directory, so it silently drops every file sitting directly in the folder.**

## H. The tests that already guard this territory

| # | File | Read | What it is |
|---|---|---|---|
| 22 | `evryn-team-runtime/tests/budget.test.ts` | full — 192 | Guards the pure budget math. |
| 23 | `evryn-team-runtime/tests/ledger-velocity.test.ts` | full — 174 | Guards the ledger's velocity reads. |
| 24 | `evryn-team-runtime/tests/scheduler-velocity-wiring.test.ts` | full — 378 | Proves the scheduler routes through the velocity gate rather than around it. |
| 25 | `evryn-team-runtime/tests/park-trigger.test.ts` | full — 153 | Guards what happens to a trigger when a wake parks. |
| 26 | `evryn-team-runtime/tests/park-inert.test.ts` | full — 105 | Guards that a park record is written inert. ⚠️ The tracker records that this file's "park note never fans out" guard is ACCIDENTAL — protected by an incomplete stub, not an assertion. Know that before you trust it. |
| 27 | `evryn-team-runtime/tests/fixtures/` — all four files | full — 10 · 15 · 7 · 14 | 🔴 **TWO OF THESE MUST NOT COMPILE — their failure to compile IS the assertion** (`gate-reason-unnarrowed.ts`, `velocity-basis-omitted.ts`). **Do not "fix" them.** |

🔴 **`tests/` beyond this list is NOT part of your load.** The sanctioned cascade treats `tests/` as a skim for shape, and every lane brief before 2026-09-08 demanded all 63 files in full — about 12,500 lines, larger than `src/` itself, and the leading suspect for a lane that compacted mid-load. **If you need another test file, read it and report that you did.**

## I. 🔴 THE IDENTITY HALF — enumerate it separately, every time

**This runtime composes these files into every agent wake. They program an agent's behaviour as surely as a line of code — and no import edge will ever point at one, so the dependency map cannot see them and `git ls-files src` cannot return them.**

> **This list was derived from `src/composer/index.ts` and `src/config/skills.ts` — the code that actually assembles a prompt — not from any cascade doc.**

| # | File | Layer | Read |
|---|---|---|---|
| 28 | `evryn-team-workspace/CLAUDE.md` | L1 | full — 426 |
| 29 | `evryn-team-workspace/shared/protocols/runtime-ops.md` | L1 | full — 76 |
| 30 | `_evryn-meta/docs/hub/roadmap.md` | L2, the Hub | full — 187 |
| 31 | `evryn-team-workspace/shared/current-state/current-state.md` | L3 | full — 557. 🔴 **Composed IN FULL and never windowed, by Justin's explicit ruling.** |
| 32 | `_evryn-meta/docs/hub/vision-and-ethos.md` | L4, Lucas's spoke | full — 231 |
| 33 | `evryn-team-workspace/.claude/agents/lucas.md` | L5, agent identity | full — 201 |
| 34 | `evryn-team-workspace/.claude/agent-memory/lucas/MEMORY.md` | L6, story + notes | full — 316. ⚠️ `archive/` is NOT composed — live only. |
| 35 | `evryn-team-workspace/shared/protocols/proposal-protocol.md` | L7/L8 skill | full — 142 |
| 36 | `evryn-team-workspace/shared/protocols/consolidation-protocol.md` | L7/L8 skill | full — 389 |
| 37 | `evryn-team-workspace/shared/protocols/domain-routing.md` | L7/L8 skill | full — 47 |
| 38 | `evryn-team-workspace/shared/protocols/standup-protocol.md` | L7/L8 skill | full — 251 |
| 39 | `evryn-team-workspace/shared/protocols/writing-protocol.md` | L7/L8 skill | full — 158 |

⚠️ **`ACTIVE_AGENTS = ['lucas']` today, which is why items 32–34 are Lucas-only. The other seven agent definitions exist and become load-bearing later — that is a fact about today's config, not about the system.**

---

## When your load is complete

**Total as measured today: about 12,200 lines across 39 items.**

1. **Read your brief: `_evryn-meta/docs/sessions/2026.09.08-act17-actj-money-layer-brief.md`.** It carries your task, your boundaries, your channel and your instruments.
2. **Then post ONE entry to your exchange doc** (named in the brief) with three things: your **read receipts** (every file, the line ranges you actually read, and for each the bottom canary you confirmed — or the true final line where it carries none), your **plan**, and **every concern** you have, including anything in the brief you think is wrong.
3. **Then STOP and wait for ACT.** Begin no work.

🔑 **Report every call, including any that FAILED, errored, or came back partial — and never let a retry silently absorb a failure.** **Name the TOOL on every line.** ⚠️ **Never describe a `Grep` as having "verified" or "confirmed" anything** — a search returns only what your own pattern asked for, so it can locate but never establish.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
