🔒 **HELD-SESSION-DOC** — the `DCj` brief (a standalone DC building the per-job money layer) · **RETIRE UPON:** the money-layer build merges · **OWNER:** ACT · **HELD SINCE:** 2026-09-18

> # 🛑 ON HOLD — 2026-09-18T12:45:40-07:00, ACT-22. DO NOT BUILD. Post one line to your log that you were spun while on hold, commit it, and STOP.
> **Justin's answers of 12:44 PT change the design this build rests on; the spec is being revised. This banner is removed when the revised spec is signed off.**

# `DCj` — BRIEF: build the per-job money layer (SPRINT Steps 19 + 20). **Standalone. Read this only once your load is complete.**

*Written 2026-09-18 by **ACT-22**, the team-runtime conductor. **Justin spins you with three sentences, and their order is load-bearing:***

> *"You are DCj. Load everything in `_evryn-meta/docs/sessions/2026.09.18-actj-dcj-money-layer-load-list.md`. Only once you are fully loaded, read and faithfully execute `_evryn-meta/docs/sessions/2026.09.18-act22-dcj-money-layer-brief.md`. Do not read the brief before you are fully loaded."*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.

---

# 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP.

**If what you hold is a machine-written summary of your session rather than files you actually read — you have compacted, and your run is over.** 🔴 **Do not reload and continue; do not finish the current task.** **Your ONE permitted act: append one line to your log — *"DCj: I compacted at `<pulled timestamp>`. Stopping."* — commit it, and stop.** 🔴 **Do not assess your own sharpness — the faculty you would assess it with is the one under question.** **Name any uncommitted work in that line and leave it in the tree.**

---

## §1 — WHO YOU ARE, AND WHO YOU REPORT TO

**You are `DCj` — a DC (Developer Claude), running as your own standalone Claude Code session.** **Your manual is `_evryn-meta/.claude/agents/dc.md`, and it is the first item on your load list.** 🔴 **If it did not load in full with its canary confirmed, STOP — you are not authorized to do anything; report the failure.**

- **You report to ACT** — the team-runtime conductor, currently `ACT-22`. **Not to Justin, and not to `ACTj`.** You do not talk to Justin; **you ping `#dev-alerts` only** (`node _evryn-meta/scripts/ping.mjs --dev "DCj: …"`), as an attention tap when you post.
- 🔑 **`ACTj` — the money-layer lane AC — designed this build and wrote your work order. She reads the whole runtime; ACT-22 has read none of `src/`.** **She will review your committed diff once.** **Her design was signed off by ACT; its reasoning is in `_evryn-meta/docs/sessions/2026.09.08-act17-actj-exchange.md`** — read her 2026-09-11 design entry and ACT-22's two 2026-09-18 entries **only if the spec leaves you unsure why**, not as a load.

## §2 — 🔴 A STANDALONE HAS NO PARENT. THIS IS HOW YOUR WORDS REACH ACT.

**Nothing you type into your own chat reaches anyone.** **Your channel is your LOG: `_evryn-meta/docs/sessions/2026.09.18-act22-dcj-log.md`** — append-only, both of us write to it, **committed immediately** (committing is sending; an uncommitted entry is invisible).

- **Header every entry** `### DCj — <timestamp> — <one-line subject>`, **timestamp pulled** from `powershell -Command "Get-Date -Format 'yyyy-MM-ddTHH:mm:sszzz'"` — **never typed from memory.**
- **At the TWO moments that matter — your trip-1 receipts, and your final report — ALSO append one pointer line to `_evryn-meta/docs/mailboxes/inbox-act.md`** (`**[<timestamp> · DCj → ACT]** <what is up>, see the log`) **and commit it.** 🔑 **Why both: whichever ACT instance is running watches that inbox — so your report survives ACT being re-spun mid-build.**
- **Publish your session address** (`ListAgents`, first line) **at the top of your first log entry.**
- **Arm a commit-gated watcher on your log** *(the shape is in `_evryn-meta/docs/protocols/mailbox-protocol.md` §2; anchor the path on `$HOME`; print a proof-of-fire line before the loop)* **and a 5-minute heartbeat.** ⚠️ **`Monitor` caps at 30 minutes in these sessions — re-arm each on its expiry notice.**
- ⚠️ **`_evryn-meta` is a HOT repo.** `git diff <file>` before every commit and **put the pathspec on the COMMIT** — `git add` does not scope one.

## §3 — HOW THIS RUN WORKS: TWO TRIPS

### TRIP 1 — you have loaded. Now REPORT, then STOP.
**Post ONE log entry** (plus the inbox pointer) with:
- **READ RECEIPTS** — every file, the spans you actually read, and each bottom canary confirmed *(or the true last line where a file has none)*. **Every call that failed, errored or came back partial — disclosed, never absorbed by a retry. The tool named on every line; a `Grep` never "verifies" anything.**
- **Your PLAN** — what you will build, in what order, and **what you will NOT do.**
- **Every concern, and anything in the spec or this brief you think is WRONG.** 🔴 **Push back — the spec was written by an agent that read the source, but ACT-22 has read none of it, and "a trip-1 report with no corrections usually means the clause was read as politeness."**

**Then STOP and wait for ACT's sign-off in your log. Begin no build.**

### TRIP 2 — BUILD, after sign-off.
**Before you touch code, read — this is trip-2 reading, not load:**
1. **`_evryn-meta/docs/sessions/2026.09.18-actj-dc-money-layer-build-spec.md` — YOUR WORK ORDER. Execute it in full; §3.1–§3.9 are your requirements, each with the test that enforces it.** **If the spec and the exchange log disagree, the log is the record — stop and say so.**
2. **`evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` — Steps 19 and 20 only** *(the tracker's statement of what you are closing)*.
3. **Re-hold `ARCHITECTURE.md`'s "Why local reasoning often fails here" — you loaded it (load item 1a).** **Defects in this runtime live in seams — a value minted in one file, stored in a second, given meaning by a third** — and your build mints a value (`job_id`) that travels exactly like that.

## §4 — WHERE YOU BUILD

- **Your worktree: `C:/Users/jbmcg/Evryn/Code/evryn-team-runtime-dcj`, branch `dcj/money-layer`,** cut from `actj/money-layer` at `0f0d6c2`, **`npm ci` already run inside it (not a junction).** **Build ONLY there.** **Every `file:line` in the spec is at `0f0d6c2`.**
- **Mutation sandbox — yours to create:** `git -C C:/Users/jbmcg/Evryn/Code/evryn-team-runtime worktree add --detach C:/Users/jbmcg/Evryn/Code/evryn-team-runtime-dcj-sbx <your tip sha>`, **then `npm ci` inside it — NEVER a junction** *(reaping a junctioned worktree has wiped the shared `node_modules` three times)*. **Break code only there. Leave it for ACT to reap.**
- **Re-measure the baseline yourself** (`npm test && npm run typecheck`). **Never `npx tsc`.**
- **Commit to `dcj/money-layer` as often as you like, pathspec on every commit. Push YOUR branch when done** (`git push -u origin dcj/money-layer`). **Never `main`.**
- **If your change moves the import graph** *(a new import edge, a new module)*: **as your LAST commit, regenerate the dependency map from a CLEAN committed tree** — `npm run depmap` — **and commit `docs/dependency-map.*` alone.** ⚠️ **Never regenerate mid-change: a map generated from a dirty tree stamps a revision at which none of its numbers held.**

## §5 — BOUNDARIES *(the spec's §5 binds you too; these add to it)*

- 🔒 **Never:** merge · push to `main` · deploy (`railway up`) · apply a migration · write under any `.claude/` directory · edit an identity file.
- 🔒 **Do not edit `docs/SPRINT-*.md`, `docs/ARCHITECTURE.md` or any `CHANGELOG.md`** — those are ACT's. **Report what they should say; ACT writes it.**
- 🔒 **Do not edit `src/composer/`, `runWake`'s `query()` options block, `src/money/budget.ts` or `tests/budget.test.ts`, or the two must-not-compile fixtures** — the spec's §5 gives each reason.
- 🟡 **Two values are NOT decided, and you build around them — you do not decide them:**
  - **`cap_usd`'s number — Justin's.** **Wire it end to end with a placeholder whose IDENTIFIER says it awaits Justin** (spec §3.7). **ACT will not merge a placeholder.**
  - **Whether the 60% pitch gate fires on spend alone — Justin's.** **`budget.ts` stays untouched.** **If he rules during your build, ACT posts it in your log and it becomes your LAST item.**
- 🔴 **When the brief or spec is WRONG, say so BEFORE you build around it** — never silently build the in-scope workaround. *(A DC in this repo once found the right fix needed a migration its brief forbade, built the workaround instead, and never said so; Justin caught it from outside the code.)*

## §6 — YOUR FINAL REPORT — in your log, then the inbox pointer, then a `--dev` ping

**Open it `## DCj's Output` and close it `## End of DCj's Output`. No heading above `##`.** It contains:
1. **Work-trip receipts** — every file read or re-read, with ranges; anything beyond your load list, with one line on why.
2. **What you BUILT** — per spec item §3.1–§3.9: done / not done, with the commit. 🔴 **Keep what you DID separate from what you RECOMMEND.**
3. **Your mutation table, run by you** — each break, where, **proof it applied**, which tests went RED, which paired control stayed GREEN, **and a baseline control in the same run that had to hit.**
4. **Suite and typecheck numbers you MEASURED**, before and after, on both tsconfigs.
5. **What you could NOT establish, and what it would take.** **This is the list ACT works from — it is not a disclaimer.**
6. **What the tracker, `ARCHITECTURE.md` and the changelog should now say** — as proposals for ACT.

## §7 — HOW MUCH ACT HAS ACTUALLY READ, so you weigh this brief correctly

**ACT-22 has read ZERO `src/` files.** **It has read the spec, `ACTj`'s design and trip-1 entries, the SPRINT, `ARCHITECTURE.md` and the dependency map.** ⇒ **Every factual claim here is a LEAD.** **Where you can see something ACT cannot, your read wins — say so loudly and early.** ⚠️ **Where this brief is SILENT, do not read that as "ACT checked and found nothing" — assume it could not see it.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
