# AC0 Handoff — 2026-06-16 (finish the go-live deploy; M1 Android-DND is round-2)

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

> **UPDATE 2026-06-16 evening — DEPLOY COMPLETE.** The go-live bundle is **deployed** (`main` `49fb6ff`, `railway up` SUCCESS, healthy; both alerts confirmed) and post-deploy cleanup is done (worktree/branch reaps · `notify_queue` table DROPPED dev+prod · `evryn-backend`+`_evryn-meta` pushed). This handoff is now the **historical pre-deploy record** — live state + the round-2 list live in `docs/current-state.md`. **Round-2 (other machine):** M1 Stage 2 detectors + Android-DND pierce · determination-guardrail re-arrange (Mira brief) · cache-TTL flip · ARCH/schema/operator-guide doc absorptions · AC3/AC2 spins.

**From:** AC0 (this session — closed the go-live bundle to the deploy gate, ran the M1 emergency-channel phone setup with Justin, hit a long Android-DND debug). **To:** a **fresh** AC0 spun to do the deploy (Justin set this down at ~12% pre-compaction; he is re-spinning you rather than surviving compaction).

**Read first:** `docs/current-state.md` (the 2026-06-16 entry) + `CHANGELOG.md` (2026-06-16) + this handoff. **Load the Full Startup Context Cascade** — you're finishing a build/deploy. This handoff **supersedes** `docs/sessions/2026-06-15-ac0-handoff.md` for the resume.

**Standing rules (Justin):** ping `#team-alerts` **every** response (Node `fetch` to `SLACK_TEAM_WEBHOOK_URL`, prefix `AC0:`). Deploy is **manual `railway up`, never without Justin's explicit go.** All commits this session are **local + unpushed** (push is post-deploy per the machine plan).

---

## WHERE IT STANDS — one sentence
The v0.2 go-live bundle is **committed, QC-GO, deploy-ready** (local/unpushed); **the deploy is NOT done** (next AC0 + Justin); the determination-sharing guardrail was **pulled to round-2**; and the **M1 emergency channel + code ship in the bundle, but making it pierce Justin's Android DND is an unsolved round-2 troubleshooting item** (NOT deploy-blocking).

## GIT STATE (precise — all LOCAL, UNPUSHED)
- **Bundle: `9364bfe`** on branch `bundle/go-live-v0.2`, in worktree `c:/Users/Justin/Evryn/Code/evryn-backend-integration`. The bundle's final content. Supersedes the interim `f4083e6`. Contains the 4 merges (morning-sweep-c2[+quiet-hours] · m1-emergency-channel · cost-pass-stamp · mira-phase6) + my commit of the 3 held files: `src/triage/classify.ts` (record_pass pass-stamp, final), `tests/test-record-pass.ts`, `identity/activities/triage.md` (Mira Pass-line). **`identity/core.md` was reverted to committed state — the determination-sharing guardrail is PULLED** (see below). **QC re-confirm GO** on the Pass-line (real QC subagent). Typecheck/tests NOT re-run this session (no node_modules in the worktree; relied on prior verification + QC GO; Railway build is the deploy-time gate).
- **`66bbf78`** on `_evryn-meta` main — CLAUDE.md: two Context-Discipline additions (IDE-open-file ≠ relevance; ask *which* runtime at load) + Justin's enrichments. Justin authorized this commit.
- **`5ac097c`** on `_evryn-meta` main — the Mira guardrail brief (`docs/working/2026-06-15-mira-guardrail-placement-brief.md`): preserves the **pulled v0.2 guardrail wording verbatim** (it was held-uncommitted in core.md → gone from git once pulled; this is the only copy) + the decision + Mira's split target.
- **`ea16bce`** on `evryn-backend` main — sprint Steps 55 (determination-sharing round-2 re-arrange) + 56 (Nathan pass-stamp glance).
- **UNCOMMITTED + WRONG (must fix — see "@mention doc edits" below):** `evryn-backend/docs/operator-guide.md` (DND section) + `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (Step 3 phrase + the new Step 3.a @mention sub-item). These assert "@mention is the DND-pierce fix" — **the pierce test disproved that.**

## THE DEPLOY-CLOSE RESUME SEQUENCE (next AC0 + Justin)
1. **(Optional) review** the 4 local commits in SCM.
2. **Merge** `bundle/go-live-v0.2` → `main` (you do it, or Justin). Left on the branch so Justin could review the bundle diff cleanly.
3. **Railway vars** — Justin regenerated a working project token (in `evryn-backend/.env`); `railway status` confirms **project evryn-backend / production / Service: None**. **You still must pin the SERVICE to set vars** (status showed Service: None — find the service via `railway status --json` / `railway service`, then `railway variables --set "KEY=VALUE" -s <service> --skip-deploys`). Set: `SLACK_EMERGENCY_WEBHOOK_URL=<the emergency-alert-bot incoming-webhook URL — Justin has it, ask him; do NOT put it in a committed file>` and `EMERGENCY_SMOKE_TEST=true`.
4. **Justin's explicit deploy-go** + M1 phone smoke (the EMERGENCY_SMOKE_TEST fires one alert on boot → confirms the code→channel plumbing; the Android-DND *pierce* is round-2, NOT this gate).
5. **`railway up`** from `evryn-backend` → smoke-check healthy (Supabase/Slack/polling).
6. **Post-deploy:** `notify_queue` table-DROP (AFTER deploy — old code SELECTs it; dev-first + pg_dump, ADR-037) · reap the 5 go-live branches + the integration worktree · **doc absorptions** (delete the retired notify_queue machinery in ARCHITECTURE.md/schema-reference.md; operator-guide quiet-hours sections; current-state Slack line; **AND rewrite the operator-guide M1-DND section + sprint Step 3.a correctly per the M1 findings below**) · **push everything.**
7. **Switch machines → round-2.**

## M1 EMERGENCY CHANNEL — the saga + findings (the load-bearing new nuance)
**Setup done:** bot `emergency-alert-bot` created, VIP'd, joined to `#emergency-alerts`. Justin's Slack member ID = **`U09GKFYLFDE`**. Channel decision = **keep `#emergency-alerts` separate** (the VIP'd bot posts only there = zero-noise; Justin agreed).

**The hard-won findings (DO NOT lose these — Justin spent an hour isolating them):**
1. **@mention is NOT the fix.** Plain posts and @mention posts behaved identically in every test. (My doc edits claiming @mention-required are WRONG — revert them.)
2. **The "silent on mobile while testing" was Slack suppressing mobile-notification *sound* while Justin is *present on a running desktop Slack client*** (even **minimized** — not just "viewing the channel"; Justin corrected my channel-focus guess). Fully **kill** desktop Slack (Task-Manager end-task, not the W10 "X" which only minimizes to tray) → mobile **sounds**. Real emergencies = Justin asleep/away → desktop idle → Slack marks him away → mobile sounds. **This part is a testing artifact, not a real defect.**
3. **Android system DND is a SEPARATE wall from Slack VIP.** VIP is a *Slack* setting (tells Slack to deliver); Android DND is *OS-level*. **Slack-VIP does NOT make a notification pierce Android DND.** This is the crux.
4. **Pierce test (Android DND ON, desktop killed):** with Justin's Android DND set to allow only "VIP messages and mentions," **neither plain nor @mention pierced** — no sound, no vibrate, fully suppressed.
5. **When Justin widened the Android DND setting to "allow ALL Slack through DND," BOTH `#team-alerts` and the emergency bot PIERCED** (sound through DND). So the pierce works via the *Android DND allow-list*, but the **"VIP messages and mentions" scoping did NOT let the bot through** — Justin: "there must be something about the VIP setting." The blunt "allow all Slack" works but is **not emergency-scoped** (everything would wake him).

**ROUND-2 M1 work (NOT deploy-blocking):** make **only** `#emergency-alerts` pierce Android DND without waking Justin for all Slack. Candidates to try: per-Android-notification-channel **"Override Do Not Disturb"**; Android **Priority conversations** (does a Slack *channel* qualify? the Priority mark Justin set didn't pierce); or **revisit whether Slack is a robust-enough pierce at all** — this is the *original* M1 question ("is Slack enough, or do we need something louder like a call/SMS for the truly-critical alert"). Pairs with M1 **Stage 2** (the detector conditions, already round-2). All of it lands **before Mark forwards.**

## THE @MENTION DOC EDITS — WRONG, must revert/rewrite
This session I edited `operator-guide.md` (DND section: "VIP + @mention to sound") and added `SPRINT` **Step 3.a** ("@mention REQUIRED to pierce"). **Both are wrong** (finding #1 + #4 above). They're **uncommitted** in `evryn-backend` main. **Next AC0:** `git checkout -- docs/operator-guide.md docs/SPRINT-V0.2-HARDENING.md` to drop them (this keeps committed Steps 55/56), then write the **correct** M1-DND content (findings above) at the deploy #lock. (Reverting restores the operator-guide's *old* channel-theory, also imperfect — but the deploy #lock rewrites that section anyway; the durable truth is in this handoff.)

## RAILWAY
Old `RAILWAY_TOKEN` was expired (both machines had the same dead value). Justin **regenerated a project token → it's in `evryn-backend/.env` now**; `railway status` works (evryn-backend / production / **Service: None** — pin the service before `variables --set`). CLI is also account-logged-in as justin@evryn.ai. `railway variables --help`: subcommands `list/set/delete`, flags `-s/--service`, `-e/--environment`, `--skip-deploys`.

## GUARDRAIL (recap — fully captured in the Mira brief `5ac097c`)
Determination-sharing guardrail **pulled from this deploy → Mira re-arranges → round-2** (it fires near-term in v0.2 — the first "why doesn't [Mark] want to talk to me?" follow-up — not v0.3; Justin's correction, AC0 agreed). The personal-pass graceful decline **already ships** in `triage.md` (neutral "isn't available"). v0.2 wording + Mira's split target + Nathan input all in the brief. Sprint Step 55.

## OWED / DEFERRED (named, not lost)
- **Formal #lock not fully run** (compaction-proximity): I wrote handoff + current-state + CHANGELOG. Next AC0/Justin: verify no ADR is owed (none believed needed — session decisions are sprint/operational), and run the deploy #lock properly post-deploy.
- **Team current-state append** (coordination note) — still owed from 2026-06-15; carry forward.
- **The @mention doc revert** (above) — next AC0, first thing.

## SPIN AFTER DEPLOY
- **Round-2 (this machine's next AC0 or post-switch):** M1 Stage 2 detectors **+ the Android-DND-pierce fix** (above) · the determination-sharing re-arrange (Mira) · the **cache-TTL flip** (cheap cost win) · then M1 setup → tell Mark go.
- **AC3 / AC2** as in the 2026-06-15 handoff (forward-persistence findings; dashboard).

## DOC RETIREMENT (fragment sweep — verify + do at the deploy #lock, don't retire blind)
- **Retire (superseded/absorbed):** `docs/sessions/2026-06-15-ac0-handoff.md` (superseded by THIS handoff) → `docs/sessions/historical/` after the deploy · `docs/working/2026-06-09-phase6-{findings,mira-dispatch}.md` (content absorbed into `SPRINT-V0.2-HARDENING.md` Phase 3 + Step 6) · `docs/sessions/2026-06-11-mira-phase6-identity-brief.md` (the Mira batch ships in the bundle) → after deploy.
- **KEEP (still live):** `docs/working/2026-06-15-mira-guardrail-placement-brief.md` (round-2 Mira reads it for the re-arrange — reap after round-2) · `docs/sessions/2026-06-12-acf-cost-levers.md` (cost thrust, queued) · `docs/sessions/2026-06-12-dashboard-side-ac-brief.md` (AC2 dashboard, pending).
- Candidate list, not a confirmed delete order — verify each isn't still referenced before moving.

## LEARNING (this session — consider codifying)
**Don't write a fix into a doc as *confirmed* before you've empirically tested it.** I wrote the operator-guide + sprint "@mention is the DND fix" edits from research/inference *before* the pierce test — Justin made me test it, and it was wrong. Twice this session I got ahead of the evidence on a fix. Sibling of CLAUDE.md's "verify before claiming behavior," applied to *fixes you're about to persist*, not just runtime reads: confirm empirically, *then* write it down.

## Other-machine setup (Justin, on the laptop — added 2026-06-16)

Committed repos come down via `git pull` (or `git clone`). What git does **NOT** carry (do these on the laptop):
- **`.env` files** (gitignored secrets) — Justin restores from Bitwarden on the move. `evryn-backend/.env` is the load-bearing one (Gmail/Supabase/Anthropic/Slack creds, the 3 admin `SUPABASE_DB_URL_*`, and `RAILWAY_TOKEN`).
- **Railway CLI** — already logged in on the laptop (confirmed 2026-06-16).
- **`psql` / `pg_dump`** — NOT a file, an **install**: `winget install PostgreSQL.PostgreSQL.17` → puts them at `C:\Program Files\PostgreSQL\17\bin\` (the path `evryn-backend/backups/README.md` invokes by full path). Needed for DB ops — drops/migrations/dumps (the `notify_queue` DROP was run from the *desktop* this session precisely because the laptop lacked psql).
- **`node_modules`** (gitignored) — rebuild with `npm install` in `evryn-backend` (and any repo you run).

— AC0, 2026-06-16

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
