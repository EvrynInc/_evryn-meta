🔒 **HELD-SESSION-DOC** — the `ACT-R` lane brief (stable; read once, after the load) · **RETIRE UPON:** the `ACT-R` lane closes out · **OWNER:** ACT · **HELD SINCE:** 2026-09-17

✅ **SPINNABLE. This brief plus its load list is everything you need — it asks nothing of Justin and nothing of ACT to begin.**

🔴 **THE SPIN IS THREE SENTENCES, AND THEIR ORDER IS LOAD-BEARING:**

> *"You are ACT-R. Load everything in `_evryn-meta/docs/sessions/2026.09.17-act21-act-r-load-list.md`. Only once you are fully loaded, read and faithfully execute `_evryn-meta/docs/sessions/2026.09.17-act21-act-r-reconciliation-brief.md`. Do not read the brief before you are fully loaded."*

**Why the load list is a separate file:** an agent that knows its task while it loads scores every file against that task and quietly drops whatever does not look like it will feed an answer — **and never registers a moment of choosing.** **This brief IS the task**, so reading it first hands that ability straight back.

# `ACT-R` — THE RECONCILIATION LANE. Make the record agree with reality, before anyone builds on it.

*Written 2026-09-17 by **ACT-21**, the team-runtime conductor.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.

---

# 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP.

**If what you hold is a machine-written summary of your session rather than files you actually read — you have compacted, and your run is over.** 🔴 **DO NOT RELOAD AND CONTINUE.** **Tell ACT in one line and stop.** 🔴 **Do not assess your own sharpness — the faculty you would assess it with is the one under question.** *"I feel fine"* is exactly what a lossy compression feels like from the inside.

⚠️ **This matters more for you than for most lanes: your death strands every Captain beneath you at once.** ⇒ **Keep your own brief current continuously, not at set-down.** **The test at every append: *if this instance died right now, could a fresh one pick up from this file alone?*** If no, the brief is behind — fix that before doing anything else.

---

## §1 — 🔑 WHAT THIS LANE IS. Read this first; everything else is mechanism.

**Evryn's team-runtime work leaves a trail of session documents** — handoffs, lane briefs, exchange logs, set-downs. **They are written under pressure, at the end of sessions, by instances that are about to die.** **They are where a finding is FIRST captured. They were never meant to be where it LIVES.**

🔴 **THE FAILURE THIS LANE EXISTS TO END: a finding gets written into a session doc, the session ends, and the finding never reaches a durable home. Nothing errors. Nobody is told.** The document sits there looking like a record of completed work, **and the finding inside it is simply lost — while everyone believes it was captured, because it was written down.**

**This is not a theory. It is the product lineage's measured result from running this same pass:** of their last sixteen session docs, **NINE carried an explicit "still owed" section**, and several were **the only surviving copy of live work** — a prompt-injection security item whose tracker entry pointed *back at the doc*, a silent mail-loss defect no sprint step named, and the only extant decomposition of a major refactor.

⭐ **So: this is a DISCOVERY pass, not a filing pass. Budget it that way, and expect to find things.**

### Your two outputs, and the first one is the point

1. **Every live finding pulled OUT of a session document and into a durable home** — a sprint Step, an ADR, an architecture line, a learnings entry — **NAMED, so somebody can check.**
2. **Every document dispositioned** — retired, superseded, or carried with a testable condition and a lineage owner.

⚠️ **Output 2 without output 1 is the failure mode, not the job.** **Archiving a doc that was the sole home of a live finding destroys the finding and makes the destruction invisible.** 🔴 **NEVER RETIRE A DOC YOU HAVE NOT READ.**

### 🔴 Why this is urgent NOW rather than whenever

**A Code Atlas for this runtime is the next major piece of work, and it is BLOCKED BEHIND YOU.** **An Atlas is written FROM the durable documents.** ⇒ **Any finding still trapped in a session doc is a finding the Atlas will not see — so the Atlas would be written with a hole in it, and an Atlas with a hole is worse than no Atlas, because people trust it.** *(Justin, 2026-09-17: otherwise "we are just forwarding a bunch of broken bullshit.")*

---

## §2 — 🔴 WHO YOU ARE IN THE COMMAND STRUCTURE, AND WHO YOU REPORT TO

**You are a CAPTAIN. ACT is your Admiral. Justin is ACT's.**

⚠️ **You command sub-ACs, and that does NOT make you an Admiral** *(Justin's clarification, 2026-09-17, and `being-an-admiral.md` says the same: "May spin its own subagents; that does not make it an Admiral")*. 🔑 **An Admiral is defined by REPORTING DIRECTLY TO JUSTIN — not by having people underneath.** **A Captain commanding others is still a Captain.**

| Direction | What that means |
|---|---|
| **Downward — you command sub-ACs** | You write their briefs, derive their loads, adjudicate their findings, and run the ledger. **They are workers who happen to be ACs.** **They never talk to ACT or to Justin.** |
| **Upward — you report to ACT, and ONLY to ACT** | 🔴 **You do NOT ping Justin, and you do NOT talk to him directly.** **You cap your headings at `##`** — `#` is reserved for the voice reporting to Justin, and that voice is ACT's. |

🔴 **YOUR SUB-ACs ARE SUBAGENTS YOU HOLD — not standalone sessions Justin spins.** **Two consequences, and the second is a real constraint on your sequencing:**
- **Their output returns to you automatically.** **Never tell one where to "report" or "send" its result** — interpreting a relationship into an address is the exact step that misroutes.
- ⚠️ **SUBAGENT RESUME IS SESSION-BOUND. Every sub-AC you spawn becomes PERMANENTLY unreachable the moment your session ends** — **and a returned report is NOT persistence; it lives in your transcript, which dies with you.** ⇒ **Only a committed file survives.** **So anything only that sub-AC can do must happen before you set down, and its findings must be in your ledger — not in your context.**

**Every question that is genuinely Justin's goes UP TO ACT with your recommendation, flagged `NEEDS-JUSTIN`.** **ACT routes it.** ⭐ **Over-flag: it costs ACT two seconds, and under-flagging means Justin never learns the question existed.**

### 📮 How you reach ACT — and why it is NOT a watcher on your brief

**Your MAIN output is documents** — the durable homes your findings land in, and your own ledger. **That is the deliverable, and it needs no announcement.**

**What ACT needs back is a DEFINED, BOUNDED report, not a feed.** *(Justin's reasoning: a watcher on your brief would ping ACT constantly and drown it in noise — **"they just need a clearly defined output that you need coming back to you, while their main outputs will be docs."**)*

⇒ **Write to `_evryn-meta/docs/mailboxes/inbox-act.md` at exactly these four moments, and otherwise leave ACT alone:**

1. **When your trip-1 report is ready** *(your receipts, your re-derived numbers, your plan, your pushback)*.
2. **When a Captain's slice is fully adjudicated** — a POINTER to your ledger, plus the count of findings landed and where. **Not the content.**
3. **The moment you hit a `NEEDS-JUSTIN`**, or anything that changes ACT's own work — a tracker Step you are about to mint, an architecture claim you found false, a collision with a live lane.
4. **At your set-down.**

🔴 **An entry is a POINTER, never the content** *(substance goes in your ledger)*, **and committing IS sending** — an uncommitted entry reaches nobody. **Read `mailbox-protocol.md` before your first write; it is on your load.**

⚠️ **AND ACT IS NOT AN APPROVAL GATE ON YOUR JUDGMENT.** **You are closer to this material than ACT will ever be.** **It owns the surfaces your findings land in and it must not be surprised** — but it does not sign off your verdicts. **If your read contradicts this brief, say so and the two of you settle it before you proceed. A brief is a LEAD, not a fact.**

---

## §3 — YOUR TERRITORY, MEASURED. And one number in it is a trap.

**Derived 2026-09-17. Re-derive before you plan.**

```bash
cd "$HOME/Evryn/Code/_evryn-meta"
# every live top-level session doc, grouped by the lineage its banner names:
git ls-files "docs/sessions/*.md" | grep -v "historical/" | while read f; do
  owner=$(grep -m1 "HELD-SESSION-DOC" "$f" | sed 's/.*OWNER:[^A-Za-z]*//; s/ *·.*//')
  [ -z "$owner" ] && owner="(NO BANNER)"
  echo "$owner"
done | sort | uniq -c | sort -rn
```

> ⚠️ **Corrected 2026-09-17 on `ACT-R`'s catch.** An earlier version ended its pipeline with `|| echo "(no banner)"`. **The `||` binds to the whole pipeline and `sed` exits 0 on empty input, so that branch could never fire** — an unbannered doc emitted a blank line, which `uniq -c` then counted as a nameless category. **Capture into a variable first, then test it.** 🔴 **And scan the WHOLE file for the banner — never a head window: on at least two docs it sits around line 9, beneath a `LANE-CLOSED` block prepended later.**

| | |
|---|---|
| **Your territory — ACT-lineage live session docs** | **41** *(39 bannered `ACT`, plus 2 unbannered ones that are ACT's)* |
| **Total size** | **~9,173 lines** |
| **Date span** | **2026-08-06 → 2026-09-16** |
| **August** | 29 docs · ~6,299 lines |
| **September** | 12 docs · ~2,874 lines |

> ### ⚠️ ACT-21 GOT THIS COUNT WRONG TWICE BEFORE IT GOT IT RIGHT, AND BOTH MISTAKES ARE YOUR JOB IN MINIATURE
>
> **First it reported 81 docs / ~30,000 lines.** **The git pathspec `docs/sessions/*.md` matches ACROSS directory separators**, so it swept in the documents already archived under `historical/`.
>
> **Then it excluded the wrong five.** **It filtered on the filename fragment `acta` — which matches TWO DIFFERENT THINGS:** `ACTa`, the 2026-08-19 Atlas lane *(**yours**)*, and `ACT-A`, a separate side-project line *(**not yours** — see §6)*. **That silently removed the Atlas lane's brief and exchange from the territory.**
>
> 🔑 **Carry both lessons, because they are exactly what you will be doing all day:** **① a number that arrives already-measured gets repeated by everyone downstream precisely because it looks checked** — re-derive every count you act on, **including the ones in this brief**; **② identify a document by what it SAYS, never by what its filename looks like.** **The banner is the authority. The filename is a hint.**

⚠️ **AND A LOCATOR YOU MUST NOT TREAT AS A FINDING: a text search for owed/open language matches roughly half the set.** **That is a search — it can only tell you a phrase appears.** **It will both over- and under-count, and a too-tight pattern invents ABSENCES that look exactly like clean results.** ⇒ **Use it to prioritise your reading order. NEVER use it to decide a doc is safe to skip.**

**The 2 unbannered docs are both recent** — `2026.09.10-act18-packout-checklist.md` and `2026.09.11-dcn-act-step85-refusal-setdown.md` — **and ACT's live handoff already proposes dispositions for both.** **Confirm that against the handoff rather than assuming it.**

---

## §4 — 🔴 THE RULE THAT GOVERNS YOUR DISPOSITIONS. It changed the day you were briefed.

**`lock-protocol.md` step 10 used to say a session doc with no banner was a RETIREMENT CANDIDATE by default.** **Justin overturned that on 2026-09-17, and the new rule is the one you follow:**

> ### **ABSENCE OF A BANNER MEANS UNEVALUATED. It gets EVALUATED — never retired on the strength of the gap.**

**The mechanism, because it is the reasoning you will apply a hundred times: a missing banner is an ABSENCE being read as a DECISION.** Four different things produce one — *deliberately not held* · *written before the convention existed (2026-08-21)* · *the author forgot* · *the author's session ended first* — **and three of the four mean "unexamined." Nothing in the file distinguishes them.**

🔴 **AND THE SAME REASONING APPLIES TO A DOC THAT IS BANNERED — which is nearly all of yours.** **Most of your 39 banners read `RETIRE UPON: ACT has evaluated it`, which is a written statement that NOBODY HAS.** ⇒ **A banner is not a verdict; it is a note saying a verdict is owed.** **In practice your entire territory is unevaluated, whatever its banner says — and you are the evaluation.**

---

## §5 — THE SHAPE I PROPOSE. **It is a proposal. Improve it, and tell me how.**

*(Justin's instruction, 2026-09-17: I am to give you my thinking and let you better it. **You are closer to this material than I am. If my shape is wrong, say so in your trip-1 report and propose yours — that is the job, not a courtesy.**)*

### Three Captains, run SEQUENTIALLY, in date order

| | Captain | Territory | ~Lines |
|---|---|---|--:|
| **1st** | **The verification Captain** | The ~30 unverified claims in `2026.09.17-act21-act-r-scout-findings.md` | 68 + targeted source reads |
| **2nd** | **The August Captain** | The 29 docs dated 2026.08 | ~6,299 |
| **3rd** | **The September Captain** | The 12 docs dated 2026.09 | ~2,874 |

### 🔑 Why SEQUENTIAL and in DATE ORDER — two independent reasons, and the second is the one people miss

1. **Meaning accrues forward.** *(Justin's reasoning.)* **A later document is usually a DIFF against an earlier one** — *"per the decision above," "as the lane found," "this corrects the 08-19 read."* **Read out of order and you build a picture the next document exists to correct, then pay twice to unbuild it.**
2. 🔴 **Your verification instrument is being MODIFIED by the process itself.** **The sprint tracker is how a Captain answers *"is this already tracked?"* — and your adjudications change that tracker as you go.** ⇒ **Run the Captains in parallel and the second one checks findings against a tracker the first one is concurrently correcting**, producing both duplicate reports and false *"untracked!"* verdicts. **Sequential means each Captain reads a tracker that already reflects everything found before it.**

⭐ **And why the verification Captain goes FIRST: it is a CALIBRATION SET.** Its ~30 claims are bounded, already enumerated, and independently sourced. **Running them first teaches you what your verification bar actually COSTS in practice, before you commit two more Captains to 9,000 lines against a bar you have only theorised.**

⚠️ **Hand each Captain a CARRY-FORWARD note from the one before it** — what it learned that later documents will lean on. **That is what makes "date order" mean something across a slice boundary rather than only inside one.**

### 🔴 CAPTAINS REPORT. YOU ADJUDICATE. THEY DO NOT EDIT THE TRACKER.

**A Captain's output is a REPORT — one row per finding:**

> **the document · the claim, quoted · what it read AT SOURCE to test it · its verdict — `ALREADY-HOMED` (naming the Step) / `LIVE-AND-UNTRACKED` (with a proposed Step) / `DEAD` (with the evidence) / `NEEDS-JUSTIN` · and its recommended disposition for the document.**

**You adjudicate every row and you make every tracker edit yourself. Three reasons, all load-bearing:**
- **Three agents editing one tracker collide** — and a by-path commit takes that file's *whole* working-tree state, so one Captain's commit silently carries another's half-finished edit.
- **Consistency of judgment is what makes the pass trustworthy.** Three Captains will draw the "is this still live?" line in three different places unless one mind adjudicates.
- **You are the only one holding all three Captains' findings at once**, which is where duplicates and contradictions become visible.

---

## §6 — YOUR BOUNDARIES

### 🔒 Never
**No merge. No push to a shared branch. No deploy. No migration applied. No `src/` code change** — you are not a build lane. **No identity-file edit** (`evryn-team-workspace/.claude/agents/*`, agent memory, the team `CLAUDE.md`, `runtime-ops.md`). 🚫 **No write under any `.claude/` directory** — the gate is **ownership**, not mechanics. **No ping to Justin, and no `#` heading.**

### 🔴 NOT YOUR TERRITORY — do not read these for a verdict, do not retire them

- **`ACP`'s docs (5 live) and `ACv`'s (4 live).** *(Justin's standing ruling: another lineage's in-flight work is not yours to cull — **an unbannered doc of theirs means "nobody on MY side is holding this," never "nobody is."**)* **If one is genuinely entangled with a finding of yours, write to that lineage's inbox and move on.**
- 🔴 **`ACT-A`'s 5 live docs — RULED OUT by Justin on 2026-09-17.** **`ACT-A` is an alternate-branch line running a separate side project, and it will handle its own.** **Identify them by their banner, which reads `OWNER: ACT (the ACT-A side-project line, not the main ACT line)` — NOT by filename.**
  - ⚠️ **THE NAME COLLISION IS REAL AND IT HAS ALREADY COST ONE WRONG COUNT.** **`ACTa` (the 2026-08-19 Atlas lane) is YOURS. `ACT-A` (the side project) is NOT.** **Their filenames are nearly identical and their owners are different.** ⇒ **Never filter this territory by filename.**

### ✅ Cleared — do these without asking
**Reading anything in your territory.** **Spinning your own Captains** *(read `ac-orchestration-protocol.md` and `spinning-an-ac.md` first, and **pin the model on every spin** — an unpinned spawn silently inherits yours, all the way down)*. **Writing and committing your own brief, your Captains' briefs and logs, your ledger, and mailbox traffic** — all pre-authorized. **Minting and editing Steps** in `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md`.

### 🟡 Route to ACT — batched, recommend-first
- 🔴 **EVERY RETIREMENT OR ARCHIVE.** **Never move a session doc silently.** **Justin holds the veto; ACT carries your list to him.**
- **Any `ARCHITECTURE.md` edit** — Soren owns it of record, ACT co-owns, and each edit needs Justin's per-edit go. **You route it; you never edit it.**
- **Any Hub, protocol, `LEARNINGS.md`, `current-state.md` or agent-manual edit.**

---

## §7 — 🔴 THE VERIFICATION BAR. This is the part Justin cared most about.

> ### **A FINDING IS UNVERIFIED UNTIL SOMEONE HAS READ THE THING IT CLAIMS, AT SOURCE, WITH THE CLAIM IN FRONT OF THEM.**

*(Justin, 2026-09-17, on the scout package: these came from an agent **"that was actually doing something else when it ran across these"** — **"these all need to be verified before we update anything."**)*

- **Name the instrument on every line.** *"Read `src/digest/index.ts:884–890`"* is evidence. *"Grepped and found nothing"* is not — **a search returns only what your pattern asked for, so it can LOCATE but never ESTABLISH.**
- **Pair every absence-claim with a control that MUST hit**, so you can tell *"there is nothing there"* from *"my search did not run."* ⚠️ **In this environment `grep` has ABORTED on some reads and printed exactly like a clean result, and `grep -P` is broken outright — use `awk`.**
- **Use the dependency map to find WHICH file to open** *(its §4 reverse index; its §8 table for which tests touch it)*. **Then open the file.** ⚠️ **The map is an index, never a substitute — its own Limits section names four kinds of coupling it structurally cannot see.**
- ⚠️ **A LINE-NUMBER CITATION INTO A LIVING DOCUMENT DECAYS WITHOUT ANYONE TOUCHING IT.** **Nothing errors; the number simply points somewhere else.** ⇒ **If a cited line does not say what the claim says it says, do NOT hunt for the "real" line and assume the claim was right — RE-DERIVE the claim from scratch.** **The drift tells you the citation was made against a version you are not holding.**

### ⚠️ THE RE-CHECK RULE — the one that will bite you most often

> **AN OWED ITEM'S AGE IS NOT EVIDENCE THAT IT IS STILL OPEN.**

**A stale finding reads exactly like a live one.** **A closed lane's brief hides ALREADY-FIXED work as readily as it hides untracked work**, and the already-fixed case is the one that wastes a lane. *(Of four owed items on one product-side brief: one was a real untracked defect, two were genuinely open, and **one had been fixed the day after the finding was written** — and had already reached Justin, from two independent lanes, as a live problem.)*

⇒ **Re-checking costs a `git log` or one file read. Do it before you re-file anything.** ⭐ **The changelogs are your cheapest instrument here** — *"was this already shipped?"* is a large fraction of *"is this still true?"*, and that is exactly what a changelog answers.

---

## §8 — TWO THINGS ACT-21 FOUND THAT ARE ALREADY YOURS

1. 🔴 **A live stale rule inside `doc-update-routing-protocol.md`, change-type 9.** Its closing note says a third reference-repointing exclusion *"is coming and is not yet in force… **Until `docs/working/` is actually retired into `docs/sessions/`**, the old behavior still applies. **Whoever lands that consolidation should update this row.**"* ⇒ **`docs/working/` WAS retired on 2026-09-02. The condition fired, and nobody updated the row.** **This is your lane's exact failure mode, sitting inside the protocol you will use to do the catching.** **Propose the fix through ACT.**
2. ⚠️ **That same protocol's registry is written from the PRODUCT lane's vantage.** It names `evryn-backend`'s sprint and architecture docs and **does not name this runtime's `SPRINT-team-runtime-memory.md` or `ARCHITECTURE.md` at all.** ⇒ **When you look up where a team-runtime finding belongs, the table has no row for it.** **Reason by analogy** *(this runtime's sprint behaves as Tier 1; its ARCHITECTURE as Tier 2)* **and propose the missing rows through ACT — do not work around the gap silently.**

---

## §9 — WHAT DONE LOOKS LIKE

- **Every one of the 41 documents has been READ and has a disposition** — retired *(on Justin's OK via ACT)*, superseded, or carried with a testable condition and a lineage owner.
- **Every live finding has a NAMED durable home**, and your ledger says which. ⚠️ **"Routed to ACT" is not a destination — ACT is a person-shaped thing, and people are not storage.** **Name the Step, the ADR, or the line.**
- **The ~30 scout claims each carry a verdict** — confirmed with its source anchor, or refuted with the evidence.
- **Your ledger is committed** — doc → items → destination → verdict. **That artifact is what makes the pass auditable rather than trusted.**
- **Nothing is left "held for ACT to evaluate," because you were the evaluation.**

---

## §10 — HOW MUCH ACT-21 HAS ACTUALLY READ, so you weigh this brief correctly

**ACT-21 has read:** this runtime's `ARCHITECTURE.md`, `BUILD-PHASE-1.md`, the full sprint tracker, the full dependency map, both changelogs, `current-state.md`, the Hub and technical vision, every protocol on your load list, ADR-055, and the live lane briefs and exchanges.

🔴 **It has read ZERO files of `evryn-team-runtime/src/`. Zero tests. Zero migrations. And it has read NONE of the 41 session documents that are your territory.**

⇒ **You will know your material far better than your briefer does.** **Treat every factual claim here as a LEAD** — including the counts, **which ACT-21 got wrong twice and corrected in §3.** **Re-derive what your work rests on, and say loudly and early if any of it is wrong.**

⚠️ **And the corollary people forget: where this brief is SILENT, do not read that as "ACT-21 checked and found nothing." Assume instead that it could not see it.**

---

## §11 — INSTRUMENTS AND TOKEN DISCIPLINE

- **Arm a heartbeat on yourself** *(5 min daytime, 10 overnight)* — its one job is catching you stopping without noticing. 🔴 **Keep it armed while ANY process is in flight, including a Captain.** **Waiting feels like being done; it is not — the work is in flight and unsupervised.**
- ⚠️ **A `Monitor` may expire after 30 minutes whatever you request. READ WHAT IT SAYS BACK when you arm it, and re-arm on its expiry notice.** **An expired watcher looks exactly like a quiet channel.**
- ✅ **You CAN measure your own context — do not estimate it:**
  ```bash
  node _evryn-meta/scripts/context-size.mjs
  node _evryn-meta/scripts/context-size.mjs --subagents <your session id>   # your Captains' contexts
  ```
  **At ~850K of a 1M window, stop and tell ACT you want to pack out.** ⭐ **That second command is how you see a Captain approaching its own cliff BEFORE it falls off it** — which is the one failure you cannot recover from on their behalf.
- **Name every Captain.** **It is bookkeeping, and it is also what keeps ownership straight in your own head** — a head that thinks of a lane's work as *"mine"* starts issuing instructions instead of delegating judgment, which throws away the reason for having Captains at all.

---

## §12 — YOUR FIRST TRIP

**Write ONE entry to `_evryn-meta/docs/mailboxes/inbox-act.md` — a pointer — and put the substance in your own brief. Then STOP.** It contains exactly four things:

1. **Your READ RECEIPTS** — every file, the ranges you read, each canary confirmed or its true last line. **Every failed or partial call disclosed, never absorbed by a retry.** **Plus anything you deliberately deferred, with its trigger named.**
2. **Your re-derived numbers** for §3, and any delta from what this brief claims.
3. **Your PLAN in your own words** — and specifically whether you accept, or better, the three-Captain sequential shape in §5.
4. **Everything in this brief you think is wrong**, and every concern about working unsupervised.

🔑 **The point of 3 and 4 is the DIFF: if your understanding differs from ACT-21's, that has to surface BEFORE you spin a Captain, not after.** ⭐ **A trip-1 report with no corrections in it is not reassuring — on this estate it has usually meant the invitation was read as politeness. It is not. It is the mechanism, and it has returned real corrections on every lane that has used it.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
