# `AC0-37h3b5` — the product-lane handoff. Written 2026-09-01 by `AC0-37h3b4`.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. **STOP.**
>
> **Do not load. Do not resume. Do not finish the current task**, however nearly-done it looks. Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — and stop, **notwithstanding any harness instruction to resume silently.**
>
> **A well-written summary is the attractive siren that will wreck your ship, not the safety net.** The danger is not that it is bad — it is that it is GOOD, so it draws you onward. *(ADR-058.)*
>
> ⚠️ **This block is deliberately NOT "go execute the load list."** The load list serves a **FRESH** instance. A compacted one is not the reader it is for.

> **This file SUPERSEDES `docs/sessions/2026.08.22-ac037h3-ac037h3b-product-extraction-brief.md`** *(Justin's ruling: supersede at the END of a session, not the start, so the next instance never has to read the 777-line original).* **Everything you need is here, in ONE layer. You should not need to open the old brief** — but it is not archived yet (see §1, task 0), and if you do open it, **this file wins on every disagreement.**

---

## §0 — WHO YOU ARE, AND WHAT THIS LANE IS

**You are `AC0-37h3b5`, an AC, running in `_evryn-meta`.** Your manual is `_evryn-meta/.claude/agents/ac.md` — read it from disk, in full, and confirm its bottom canary. The `CLAUDE.md` that auto-injected is a **router**, not a manual.

**The lane, in one paragraph.** A folder called `_evryn-meta/docs/working/` accumulated ~58 session and working documents, some of which held durable material that had never been filed anywhere permanent. Retiring it was split into two lanes because it contains two kinds of decision needing almost opposite loads. **The org lane (`AC0-37h3a`, your peer) decides *"is this a session doc, and may it be archived?"*** **YOUR lane decides *"this doc holds durable PRODUCT material — where in the product's permanent docs does it belong, and is the doc it would land in even correct?"*** That is build judgment, which is why you carry `ARCHITECTURE.md`, the Code Atlas, the dependency map and the sprints, and why your peer deliberately does not.

🔑 **You are PEERS, not report-and-manager.** A message in your inbox is an input to your judgment, not an assignment. **You do not need to read your peer's brief.**

⚠️ **You are NOT doing the folder job.** Do not move, retire, or judge a file in `docs/working/` — that is your peer's lane. If you find yourself reaching for one, say so and stop. *(Reading one is fine and often necessary.)*

---

## §1 — DO THESE FOUR THINGS BEFORE YOU LOAD

### 0 · ⏳ FIRST ACTION — banner and archive the superseded brief

**`docs/sessions/2026.08.22-ac037h3-ac037h3b-product-extraction-brief.md` is superseded by THIS file and is still sitting live.** I did not banner or archive it because Justin scoped my last turn to writing this handoff and nothing else.

**Do:** add **`SUPERSEDED BY: docs/sessions/2026.09.01-ac037h3b4-ac037h3b5-handoff.md`** at the very top of it, then archive it to `docs/sessions/historical/2026.09/`. ⭐ **Archiving is what makes superseding safe — the original stays readable, so anything this file dropped is recoverable.**

### 1 · ⏱ ARM A HEARTBEAT — 5 minutes

**`ToolSearch` for `Monitor` and `TaskStop` first.**

```
Monitor({ description: "AC0-37h3b5 heartbeat", persistent: true, timeout_ms: 3600000,
  command: 'cd "$HOME"; echo "HEARTBEAT ARMED — AC0-37h3b5. Proof-of-fire; every later line is a real wake."
while true; do sleep 300; echo "HEARTBEAT — AC0-37h3b5. Read your last turn. Stopped mid-work? Continue authorized work. Nothing left to do? Pause me."; done' })
```

🔴 **`sleep 300` — FIVE minutes. Justin confirmed this again on 2026-09-01:** *"leave at 5 min — I'm mostly not in the room, I'm bouncing around between other meetings and sessions."* Use `600` only overnight. **`cd "$HOME"` is load-bearing** — a `Monitor` inherits the Bash tool's mutable working directory. **Emit the proof-of-fire line BEFORE the loop**; *"Monitor started"* means only that a process launched. **Announce every pause, kill and re-arm in chat.**

⭐ **It works. It caught me twice** ending a turn on *"moving to X now"* instead of moving to X. Do not skip it because you feel alert.

### 2 · 📮 READ YOUR INBOX, THEN ARM A WATCHER ON IT

**Read `docs/mailboxes/inbox-ac037h3b.md` BEFORE arming anything** — a watcher's baseline is the instant you arm it, so anything already waiting is invisible to it permanently.

```bash
cd "$HOME/Evryn/Code/_evryn-meta"; P="docs/mailboxes/inbox-ac037h3b.md"
echo "WATCHER ARMED — AC0-37h3b5 on $P. Proof-of-fire; every later line is a real change."
last=$(git log --all -1 --format=%H -- "$P" 2>/dev/null)
while true; do sleep 60
  cur=$(git log --all -1 --format=%H -- "$P" 2>/dev/null)
  if [ "$cur" != "$last" ]; then echo "INBOX CHANGED — read $P (do not trust any author guess)"; last="$cur"; fi
done
```

🔴 **`git log --all`, not your own HEAD** — a peer committing from a different worktree never moves your HEAD. **Fire on a COMMIT, not an edit: committing IS sending.** **Do NOT detect the author** — emit "changed, go read it" and read it; an instrument that guesses can lie to you.

⭐ **PROVEN LIVE, 2026-09-01: this channel is fast.** My watcher caught four separate peer commits inside ~60 seconds each. **Your peer can reach you live; you can reach them live.**

### 3 · 🔴 THE LOAD LIST IS NOT YOURS TO TRIM

**If you want to change it in any way, ask Justin.** Your peer is deliberately not product-loaded and cannot judge a product load list — that asymmetry is the entire reason this lane exists.

> **The argument, because you deserve one and because you will feel the pull to trim.**
> **1 —** You exist *because* the other half of this job is under-loaded. If you trim, nobody in this job is product-loaded and neither of you can tell.
> **2 —** Justin's evidence: *"the number of agents I've talked to who were **sure** they could do the work just fine without the heavy load, and then when I gave them the heavy load they were like 'oh shit, that really changes my understanding' is uncountable at this point."*
> **3 — 🔑 Your own judgment is not a reliable instrument here.** A scout that dropped briefed files diagnosed itself: *"my relevance judgment is formed from inside the gap the file would close — so on exactly the files that matter most, it is systematically biased toward dropping them."* **You will not catch yourself doing this.**
> **4 —** Under-loading does not produce work that looks incomplete. It produces work that is **confidently wrong in the same register as the sound parts**, so nobody can locate the contamination to discount it.
> ⭐ **You MAY load MORE, and should say so.** ⛔ **What is barred is silently taking LESS.**

---

## §2 — YOUR LOAD. Every file IN FULL, in this ORDER.

> 🔑 **The order is a rule, not a preference** (`ac.md`, Context Discipline): **widest lens inward, and INTENDED SHAPE BEFORE CURRENT STATE.** Read in this sequence and the later documents land as a **diff against intent**, which is what makes a divergence pop instead of reading as just another fact. **Read it out of order and a stale line and a true line look identical.**
>
> 🔴 **TREAT EVERY LINE COUNT AS APPROXIMATE AND LET THE CANARY GOVERN.** A count tells you roughly how much to page; only `FULL FILE LOADED` tells you that you reached the end. `atlas/11-verification-and-tooling.md` §3.5 documents this exact false negative — an agent honouring a count literally reports "full" having never seen the canary.

**The list below is preserved EXACTLY as handed to me.** The only change is a **VERIFIED** column: I loaded all of it on 2026-09-01 and these are the counts I actually saw. **Four had drifted upward** — all because the files grew, none because the list was wrong.

| # | File | Listed | **VERIFIED 09-01** | Why it is on the list |
|---|---|--:|--:|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | 832 | **866** | Your manual. Nothing else makes you AC. |
| 2 | `_evryn-meta/docs/hub/roadmap.md` | 184 | **187** | The Hub. Company truth. |
| 3 | `_evryn-meta/docs/hub/technical-vision.md` | 397 | **411** | The widest-lens architecture spoke — the frame every build-altitude doc descends from. |
| 4 | `evryn-backend/docs/ARCHITECTURE.md` | 1,348 | **1,379** | **Your primary destination.** |
| 5 | **The Code Atlas, ALL TWELVE**, `evryn-backend/docs/atlas/` | 3,130 | **~3,172** | A complete non-overlapping partition of all 45 `src/` files **plus the identity half** — which is why it substitutes for the runtime. **Per file, as I verified them:** `00-INDEX` 104 · `01-agent-core` 177 · `02-email-ingest` 217 · `03-email-processing` 313 · `04-triage-pipeline` 258 · `05-approval-and-notify` 455 · `06-persistence` 322 · `07-safety` 665 · `08-security` 169 · `09-bootstrap-and-config` 172 · `10-identity-runtime` 191 · `11-verification-and-tooling` 129. **Each invariant is tagged STRUCTURAL or INSTRUCTIONAL; that tag is its most valuable content.** ⚠️ **Treat STRUCTURAL as an assertion to verify, not a guarantee** — the 2026-08-11/12 mutation sweep corrected the Atlas in **both** directions. |
| 6 | `evryn-backend/docs/dependency-map.md` | 693 | **693** | The machine-generated import graph. **Use it, never a grep, for "what imports X."** ⚠️ Its generation stamp is one commit behind **by construction** — not staleness, and it has been misread as such. 🔴 **THE LINE-COUNT TRAP IS REAL AND I HIT IT: `wc -l` reports 692 because the last line has no trailing newline; the canary sits on 693.** |
| 7 | `evryn-backend/docs/BUILD-EVRYN-MVP.md` | 654 | **655** | v0.2 scope and build order. |
| 8 | `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` | 811 | **879** | The 🔵 LIVE region is the open work; ⚪ CLOSED is the build record. *(I added 9 lines to Step 50 — see §4. Count measured by byte, not `wc -l`.)* |
| 9 | `evryn-backend/docs/SPRINT-v0.2-optionals.md` | 36 | **41** | 🔴 **Your second destination — write here.** **Optionals is the destination because of `MOVED` semantics:** the main sprint's Step-53 entry sits in its ⚪ CLOSED build record, kept whole by that doc's own rule, while the **live tracking** is here. 🔑 **A `VERIFIED AT SOURCE` label is a claim about an INSTRUMENT, and claims about instruments get inherited unexamined. Treat one as UNVERIFIED unless it names WHO ran WHAT.** |
| 10 | `_evryn-meta/docs/current-state.md` | 194 | **202** | The snapshot — deliberately AFTER the build docs. |
| 11 | `_evryn-meta/docs/protocols/ac-writing-protocol.md` | 118 | **119** | Before you write anything durable. |

**⭐ THREE ADDITIONS I TOOK, and you should too — this is the "load MORE" case, not a trim.**

| + | File | Lines | Why |
|---|---|--:|---|
| a | `_evryn-meta/docs/hub/trust-and-safety.md` | 198 | `ARCHITECTURE.md`'s own Required Context block names **four** prerequisites; the list above gives three. **Justin ruled these two spokes into the product cascade on 2026-08-22.** |
| b | `_evryn-meta/docs/hub/user-experience.md` | 198 | Same reason. Read both **before** `ARCHITECTURE.md`. |
| c | `_evryn-meta/docs/protocols/mailbox-protocol.md` | 215 | You will use the mailbox on day one. Read it at the moment of use. |

**Pull at a trigger, not up front:** `evryn-backend/identity/core.md` (283, **READ-ONLY**) if anything touches identity mechanics · `_evryn-meta/docs/decisions/012-trigger-composed-identity.md` if Step 53/120 work touches ADR-012 · **`docs/protocols/lock-protocol.md` the moment `#lock` is invoked or you supersede a doc** (its step 10 defines the procedure) · `ac-orchestration-protocol.md` + `orchestration/spinning-*.md` before spinning any DC/QC/lane-AC · `evryn-backend/docs/gate-b-plan.md` for Gate-B work · `docs/repo-inventory.md` for the session-start sync cross-check.

🔴 **DELIBERATELY EXCLUDED: the runtime.** `evryn-backend/src/**` is ~20,100 lines and `identity/**` a further ~1,400. **That load is what makes agents compact, and a compacted agent is done for the session.** **The Atlas and the dependency map are on your list precisely so you do not need it.** Pull a single **named** `src/` file at a trigger if a question genuinely turns on it; never the tree.

---

## §3 — 🔴 YOUR MARCHING ORDERS. Justin's rulings, 2026-09-01, all of them unexecuted.

**These are his words from the session that produced this file. Nothing below is started.**

### 3.1 · Land four rulings in `ac.md` — **LEAVE THEM UNCOMMITTED**

🔴 **His explicit instruction: *"put this in ac.md, but leave in scm for me to vet"* and *"leave all of that in SCM — I'm gonna want to tweak some."*** **Make the edits; do not commit them.**

**Ruling A — lettered options on ballots.** *(His words: "give me multiple choice answers from here on out — I could have just said 'a' for this," then on review: "yes/no is fine — but if I have options, they would ideally be lettered. I think the previous wording is good enough, but take a stab if you want.")*
⇒ **This one is OPTIONAL and he has already said the existing text is good enough.** The rule half-exists in `ac.md` → *Working With Justin*, under **"Number items in lists, and shape each so Justin can sign off easily,"** which already offers yes/no **or** named options. **If you touch it at all, the change is to make lettered options the default rather than an alternative — and to always leave a final "(c) something else — tell me" so multiple-choice never becomes a false dilemma.** ⚠️ **Low value. Do not spend real time here.**

**Ruling B — he does not see your inline chat work. APPROVED: *"good. put it in."*** Destination: `ac.md` → *Working With Justin*, immediately after the **"GENERALIZED 2026-08-10 — while ANY subagent is running…"** bullet, which it broadens. Proposed text:

> - 🔴 **GENERALIZED AGAIN, 2026-09-01, AND THIS TIME IT IS CONDITIONAL ON NOTHING: ASSUME JUSTIN DOES NOT SEE YOUR INLINE CHAT WORK AT ALL. If you want him to see something, it goes in a report under a `# <your instance name>'s report` heading.** *(His words: "I can't possibly read through every last little thing — so I'll never see stuff like that… if you want to think out loud in chat, that's fine, but assume I DON'T see it.")* ⚠️ **The bullet above is narrower and reads as a subagent-wave discipline. It is not — the heading is the ONLY channel that reaches him.** **Thinking out loud in chat stays welcome and he does not want it suppressed; what is barred is mistaking it for delivery.** 🔑 **The test is one question: if this mattered and he never read it, would that be a failure? Yes → it is not chat, it is a report, and it needs the heading.**

**Ruling C — delegate across the lane boundary to protect context. APPROVED.** Destination: `ac.md` → *Context Discipline*. Proposed text:

> 🔑 **PROTECT YOUR CONTEXT BY DELEGATING ACROSS A LANE BOUNDARY — and the test is what you have ALREADY READ, not whose job it looks like.** *(Justin, 2026-09-01: "if you want to load stuff over to 3a because it doesn't need your heavy load, that's actually a good idea… if something's like 'go fix the #lock protocol', that's more his lane.")*
>
> ⭐ **THE TEST: if the file is ALREADY in your context, fix it yourself. If you would have to READ a file to make a small fix, hand it to a lane already holding it.**
>
> ⚠️ **The failure this closes is not laziness — it is the opposite.** It is a heavily-loaded agent doing a small favour and paying a disproportionate context price for it. **The fix looks trivial and the load never appears in the diff**, so nobody — including you — sees the cost until you are compacting early. 🔴 **And a compacted agent is done for the session.**
>
> 📌 **Say WHY when you hand one over, so the recipient can price it.** *(Worked example, 2026-09-01: an AC found a wrong line in `operator-guide.md`, wrote "I have NOT read it — I grepped it twice, so by that rule it is yours," and handed it to the org lane rather than opening the file.)*

**Ruling D — the proportionality clause on C. APPROVED: *"yep, put this in."*** As a sub-bullet under C:

> ⚠️ **And the rule needs PROPORTION, not a threshold of zero — check the file's SIZE before you delegate.** **A hand-off costs a round-trip and a wake on both sides.** ⇒ **If the read is smaller than the cost of delegating it, just do it.** *(Live specimen, 2026-09-01: an AC was about to hand a Step-body lift across a lane boundary on this rule, then looked — the source was **fifty lines**. Delegating would have spent two agents' turns to save one cheap read.)* 🔑 **The rule exists to stop you paying a 500-line load for a one-line fix. It is not about never reading anything.** ⭐ **The peer lane's generalisation, worth carrying: *a rule that names a MECHANISM decays the moment someone applies the mechanism without the PURPOSE — and it decays in the direction that LOOKS compliant.***

### 3.2 · Rehome the QC mutation-sweep findings — **and CLEAN IT UP first**

**The file:** `_evryn-meta/docs/working/2026.08.12-qc-sweep-findings.md`, **636 lines.**

**Justin's ruling, verbatim:** *"rehome to evryn-team-workspace.../product/research — but is it **everything** in that, or do you need to extract what's actually research — because is there session doc bullshit that's a waste of time? Clean it up **carefully** if you need to, then put it there, and fix references. … And if you are just taking stuff out of the session doc, then retire the session doc."*

⇒ **THE JOB, in order:**
1. **Read the file and decide what is genuinely durable research vs. session narration.** Its §8 (*"Disclosed failures and deviations of the sweep lead"*) and parts of §3 are process narration; §4 (head-level findings), §5 (lane results), §6 (patterns) and the **Appendices A/B/C (the per-mutation verdict tables)** are the research. **Use your judgment — he asked you to.**
2. **Write the cleaned version to `evryn-team-workspace/shared/projects/product/research/`.** *(Precedent for the folder and the pattern: `2026.07.28-soren-outreach-fill-open-design-questions.md`, which I created there on 2026-09-01 under the same ruling.)*
3. **Fix the references — there are FOUR live ones and one is a load cascade:**
   - `_evryn-meta/docs/protocols/load-cascades/product-full-cascade.md:37` — 🔴 **tells every product lane to read its §4.** *(It already anticipates the move and asks for a repoint.)*
   - `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` — **Step 118a's acceptance criterion depends on Appendix A.4**, and **Step 116(b)** says to read it before trusting an Atlas claim.
   - `evryn-backend/docs/atlas/03-email-processing.md:111` — cites it as the instrument behind the I1 correction.
4. **Then retire the session doc**, per his instruction.

🔑 **WHY THIS IS NOT A SESSION DOC, which is the finding that changed the disposition:** run the brief's own test — *"would someone go looking for this because of what it CONTAINS, or only because of when it HAPPENED?"* **Four live documents cite it for its content, including a load cascade.** It is a **misfiled real doc**, and the tell is exact: *"if you are writing a retirement CONDITION and the condition is 'once its content is rehomed,' you are not holding a session doc."*

**⇒ The discharge check is already DONE — do not redo it.** All seven of its §7 owed items are homed (three in Step 106, one each in Steps 104 / 118a, one self-closed, one — the *"a load list is an artifact of record"* lesson — **grepped to `ac-orchestration-protocol.md` and `LEARNINGS.md` but NOT read, so treat that one as located rather than verified**). **All four of its §6 proposed QC patterns are landed in `qc.md`.** **Two findings were NOT homed — see 3.3.**

### 3.3 · Mint two sprint Steps — **with a "check these" warning**

**Justin: *"Mint the steps — and make sure you put them in the right place in the flow."*** **Numbers 122 and 123 were verified free at mint time on 2026-09-01** *(scan carried a control: `121` hit once, `122`/`123` hit zero)*. **Re-verify before you write — a sibling lane may have minted since.**

🔴 **AND HIS EXPLICIT CAVEAT, WHICH MUST GO INTO THE STEP TEXT:** *"just give some kind of 'check these' warning — because we're not in the build really right now, we're just kind of dipping in as a sidecar to this rehoming… Warn appropriately, and with humility about the limitations of your context."*

⇒ **Both Steps must open with a provenance-and-limits banner along these lines:**

> ⚠️ **PROVENANCE AND LIMITS — read before acting.** This Step was filed by an AC working a **documentation-rehoming lane**, not a build lane. **Its evidence is the 2026-08-11/12 adversarial mutation sweep, which DID execute** — but **the filing AC ran nothing, read no test file, and does not hold `src/`.** ⇒ **Treat the mechanism as reported-and-plausible, not as diagnosed. Verify at source before scoping.**

**Step 122 — C3, the serious one:**

> 122. **TODO — 🔴 THE APPROVAL GATE'S THREE CORE INVARIANTS ARE INVISIBLE TO THE GATE THAT ACTUALLY RUNS. Removing the outbound approval gate is GREEN in CI and pre-merge.**
>     > **The mechanism, as reported by the sweep's lane M5.** `approval/flow.ts`'s **INV-1** (nothing sends except through the gate), **INV-3** (a successful send is never re-sendable) and **INV-4** (an audit row is never filed against the wrong user) have assertions in exactly one place — `tests/test-execute-approval-transport.ts` — and that suite **`process.exit(0)`s unless `RUN_LIVE_DB_TESTS=dev`.** ⇒ **In the default gate it does not run at all.**
>     > 🔴 **If that holds: delete the approval gate, the re-send guard, or the cross-user audit guard and `npm test` stays green and `npm run typecheck` stays clean.** **The single most load-bearing safety boundary in the product would have no regression protection anyone executes.**
>     > ⚠️ **Same family as Steps 108 and 109 — but worse in one respect: here the assertions genuinely exist and are genuinely discriminating. They simply never run.** A reader checking coverage finds the test, reads it, and is satisfied.
>     > **Acceptance is a mutation, not a reading:** delete each guard in a sandbox worktree and confirm the verdict flips **in the default run**, not under `RUN_LIVE_DB_TESTS=dev`. 📌 **Interacts with Step 118a** — that Step runs this suite *once*; this Step is about it running *every time*. — [AC design → DC+QC]

**Step 123 — C5, non-blocker:** the approval short-id **collision refusal** (Atlas `05-approval-and-notify.md` INV-8) is the unprotected half — the parser is covered; the refusal that stops an ambiguous `approve <id4>` sending the wrong draft to a real person is not. **Low reachability** (needs two pending drafts sharing four hex characters), **but the failure is a wrong email to a real contact.** **Sequence with 122** — same suite, same gating problem; fixing 122's execution gate is what makes a test here meaningful.

**Placement:** both belong in the 🔵 **LIVE** region, section **"2 · Open defects — verified at source, routed, not yet built."** ⚠️ **Step numbers are permanent handles — never renumber.**

---

## §4 — WHAT IS DONE. Do not redo any of it.

**All committed AND pushed on 2026-09-01. All three repos at zero unpushed.**

| Commit | Repo | What |
|---|---|---|
| `478dc6d` | `_evryn-meta` | `repo-inventory.md:35` said "four" and listed five. One word. |
| `7426532` | `evryn-backend` | **BUILD-EVRYN-v0.3.md** breadcrumb to the new research doc · **SPRINT Step 50's runtime seams lifted in** from the retiring cleanup-batch brief. |
| `b674f0c` | `evryn-team-workspace` | **New research doc** homing Soren's unsettled v0.3 batch · **Soren's `MEMORY.md`** breadcrumb, marked as an AC writing. |
| `6b419c5` · `ccca522` · `209703c` | `_evryn-meta` | Three mailbox commits. |

**⭐ Soren's unsettled cast-off-outreach "fill" batch is HOMED** at `evryn-team-workspace/shared/projects/product/research/2026.07.28-soren-outreach-fill-open-design-questions.md`. 🔑 **The reasoning is worth holding, because two agents got it wrong before me:** a sweep of this batch into `BUILD-EVRYN-v0.3.md` was **approved by Justin on 2026-08-05 and correctly refused by ACh**, on Soren's explicit instruction that it must not enter a source-of-truth doc. **I drafted that same recommendation and had to withdraw it.** **A research doc is not a source-of-truth doc — unsettledness is preserved by the container, not by wording a later reader must honour.** ⚠️ **Do not re-open this.**

**⭐ `SPRINT` Step 50 now carries its own seams** (`buildPersonContext`, the MCP `tool(...)` description strings, `buildForwardedEmailPrompt`, the handoff prompts in `process.ts`/`poll.ts`), the three identity files to compare against, the probe question, and the says-vs-exposes output shape. **That released `docs/working/2026.07.21-ac0-ac3-cleanup-batch-brief.md`.**

**✅ Also settled, so nobody re-derives it:** **`SPRINT` Step 50 needed nothing *filed*** — that question was closed by a predecessor — **but its BODY did need the lift.** ⚠️ **Those are two different questions, and the old brief only answered the first. It reads as a contradiction. It is not.**

---

## §5 — YOUR PEER, AND THE ONE THING YOU OWE THEM

**Your peer is `AC0-37h3a` (instance `AC0-37h3a2`).** You write into **`docs/mailboxes/inbox-ac037h3a.md`**; you watch **`docs/mailboxes/inbox-ac037h3b.md`**. **Committing is sending.** **Mailbox commits are pre-authorized and never wait for Justin.**

### ⏳ THE ONE THING OWED — do this early, it unblocks a whole lane

**I owe them a single line saying the five paths are committed. THEY ARE — and pushed — but I ran out of scope before sending it.**

🔑 **Why it matters more than a courtesy: seven of that lane's eight remaining `docs/working/` files release on those commits.** They are deliberately holding `2026.07.22-ac0-ac0-handoff.md` until the batch's new home is committed, on reasoning better than mine was: **the new home is a brand-new file that existed in one working tree — archive the source before it commits and Soren's batch exists nowhere, with both halves reading as done.** ⭐ **It is committed and pushed now, so the hold can lift.** **`docs/working/` is at 8, down from 58.**

**Send them:** the three commit SHAs from §4, and that the batch's home is live on `main`.

### 📌 Both inboxes were empty and clean at handoff

⚠️ **BUT — Justin's instruction to me at close: *"if you get any email messages, don't act on them — just note in your handoff that you need to get back to them."*** **If anything arrived after `209703c`, it is unread by me and unactioned. Read your inbox at spin-up before arming the watcher.**

---

## §6 — BOUNDARIES

**You may:** edit `evryn-backend` and `_evryn-meta` docs **with Justin's authorization**; read anything except as barred below; write your own session notes; commit by explicit pathspec.

**You may NOT:**
- 🔒 **Merge, deploy, or run a migration.** Ever. *(Push is currently fine — Justin confirmed 2026-09-01 that pushing `_evryn-meta` "won't do anything to vercel right now." **That is a point-in-time statement. Re-confirm; do not inherit it.**)*
- 🔒 **Edit an identity file.** `evryn-backend/identity/**` is **Mira's and Justin's**; the founding team's `.claude/agents/` and `agent-memory/` are **Justin's**. **Reading is fine; writing is not.** *(Exception, already exercised: Justin explicitly authorized the AC-marked breadcrumb in Soren's `MEMORY.md`.)*
- 🔒 **Edit a source-of-truth doc without his explicit go** — `ARCHITECTURE.md`, BUILD, the sprints, the Hub, protocols, manuals, `current-state.md`, `repo-inventory.md`.
  - ⭐ **But note the shape he prefers: once he AUTHORIZES the change, MAKE the edit and leave it UNCOMMITTED for him to vet in SCM — do not pitch finished prose in chat.** **The test is whether your uncertainty is about the WORDS or the DECISION. Words → edit it. Decision → chat.**
- 🔒 **Touch `docs/working/` as a folder job.** Reading is fine; moving and retiring is your peer's lane. *(The one exception is 3.2, which Justin assigned to you by name.)*

⚠️ **`_evryn-meta`, `evryn-backend` and `evryn-team-workspace` are all HOT.** `git status` before you touch anything; **`git diff <file>` before every commit**; **put the pathspec on the COMMIT** — `git add` does not scope one. **Report COLLISIONS, not other instances' work.**

---

## §7 — 🔴 THE LESSONS THIS SESSION PAID FOR. Cheap to carry, expensive to relearn.

1. 🔴 **JUSTIN'S STANDING CAUTION, given to me directly and aimed at how I worked: *"you're doing a LOT of grepping, which is often misleading."*** **He is right and you should take it as binding.** `ac.md` already says a grep **LOCATES** and never **CONCLUDES** — and that a clean grep *feels* like a result when what actually happened is *"my pattern did not match."* ⇒ **Pair every absence-claim with a known-true control in the same run, say which instrument you used, and prefer reading the document.** ⚠️ **Several of my findings rest partly on grep. Where they do, I said so — and you should treat those as leads.**
2. **A CLOSED lane's brief is exactly where routed-but-untracked work hides, because the closure reads as completion.** *"The lane is merged and reaped"* is evidence about the **code** and no evidence about what that lane **routed out**.
3. **AN OWED ITEM'S AGE IS NOT EVIDENCE IT IS STILL OPEN.** Re-checking costs a `git log`. **Two of Justin's fifteen rulings in the prior session rested on findings that were already stale.**
4. ⭐ **A ROUTER SECTION AND ITS OWN CONTENT DRIFT APART.** Soren wrote an honest router listing what he had persisted — but the batch **above** it was never updated, so two of its six items were already home while the text read as though all six were open. **Check the router, not just the list.**
5. **A point-in-time statement of INTENT gets read as a standing statement of STATE.** The peer recorded my watcher as "down" from a sign-off, repeated it in two messages and a report to Justin, and it was wrong the whole time. **Re-verify; do not inherit.**
6. 🔴 **A rule that names a MECHANISM decays the moment someone applies the mechanism without the PURPOSE — and it decays in the direction that LOOKS compliant.** *(I nearly delegated a fifty-line read under a rule whose purpose is protecting context headroom.)*
7. **NEVER TYPE A TIMESTAMP FROM MEMORY.** A mailbox header reading `19:02` on an entry committed at `17:17` breaks the orphan-receipt recovery that `mailbox-protocol.md` §5 depends on. **Run `powershell -Command "Get-Date -Format 'yyyy-MM-ddTHH:mm:sszzz'"`.** *(This happened twice today, both in my own lineage.)*
8. ⚠️ **DISCHARGE YOUR INBOX, DO NOT JUST ANSWER IT.** Both lanes spent the day acting on entries without deleting them, which makes the empty-inbox invariant meaningless. **An empty inbox means nothing is owed; a non-empty one means something is.**
9. **The `Write`/`Edit` tools DO work in `evryn-team-workspace`** — a recorded lane fact claiming they "hang awaiting auth" there is **stale as of 2026-09-01.** ⚠️ **But the team's `MEMORY.md` files are pure CRLF on disk** *(Soren's: 187 pairs, zero bare LF, verified by byte)* — **an LF-normalising edit would rewrite every line ending in another agent's identity file. Splice with a script and verify bytes before and after.**
10. **A pre-commit hook scans every repo for stray control characters on every commit** (~0.17s, silent when clean). **It fired clean on all six of my commits.** Do not route around it.
11. 🔴 **BACKTICKS IN A `node -e '…'` OR ANY `bash -c` ARGUMENT ARE COMMAND SUBSTITUTION — the shell runs them.** *(`ac-writing-protocol.md` documents this for commit messages; it fires identically here, and a `-e` script containing backticked markdown died mid-session for a previous instance.)* ⇒ **For anything containing markdown, use the `Read`/`Write`/`Edit` tools, or write the script to a FILE and run `node <file>`.** **I did the latter three times today for line-splices whose content was full of backticks; it works cleanly and the script can carry its own guards.** **Quoted heredocs (`<<'MSG'`) remain correct for commit messages.**
12. ⚠️ **PROVE A SPLICE, DO NOT ASSERT IT.** Every script I used to edit a file by line number carried **guards that refuse rather than corrupt** (verify the first and last line are what I think before writing) and **printed a byte-level verification after** (line delta, CRLF pairs, bare-LF count, canary still last, content present). **A mutation that silently failed to apply prints exactly the same as one that worked.**

---

## §8 — OPEN, AND NOT YOURS TO CLOSE ALONE

- **`_evryn-meta` was ~50 commits ahead of origin and I pushed it.** Justin authorized it: *"you can push — it won't do anything to vercel right now."* ⚠️ **The standing fact remains that pushing `_evryn-meta` IS the Vercel dashboard deploy. Re-confirm before the next push; do not inherit today's clearance.**
- **Consult ① is HALF the size it has been carried as.** `2026.07.13-acu-outreach-engine-research.md` **is already correctly filed** in `evryn-team-workspace/shared/projects/product/research/` — 🔑 **nobody found it because the filename dropped one `acu` in the move (`acu-acu-…` → `acu-…`), so every search by name missed it.** ⇒ **The only real divergence is `…-step53-identity-loading-research.md`, still in `docs/sessions/historical/2026.07/`.** ⭐ **And the blocker that parked this in August has dissolved** — it was held because both docs were referenced by an ACU self-brief Justin said was live, and that self-brief is itself now in `historical/`. **So consult ① is a one-file move plus breadcrumbs plus the whose-memory question** *(Justin has pre-authorised a memory note; choosing WHO gets it is his).*
- **The `AFFIRM_HOUR_PT` midnight-firing question is OPEN and both named causes are eliminated.** `AFFIRM_HOUR_PT=7` is deployed and running, so the env-var `NaN` class stays **LATENT** and `SPRINT` Step 104, `ARCHITECTURE.md`'s M1 section and `atlas/07-safety.md` §5a are all **correct**. 🔴 **But a third mechanism therefore exists and is unidentified.** ⭐ **The cheap next step Justin asked for: at the FIRST deploy after 2026-09-01, observe whether the affirmation fires at ~7am PT.** `SPRINT` Step 119 carries it.
- **The session-start repo-sync cross-check passed** — five active repos, all on `main`, all clean, matching `docs/repo-inventory.md`. **Archived repos correctly do not appear** (they are one level deeper in `Code/z.archive/`); **that absence is not a finding.**

### 🔴 8a — THE ATLAS-REFACTOR BRIEF. **This was on my lane's ownership list, it is UNRESOLVED, and it is the item most likely to be lost.**

**`_evryn-meta/docs/working/2026.08.04-ac0-aca-atlas-refactor-brief.md` (549 lines) is a LIVE SPEC and MUST NOT BE RETIRED.** *(Verdict established by `AC0-37h3b2`, who read it in full.)* **Finding it a durable home is an open item assigned to THIS lane and I did not get to it.**

**Why it is live:** it has four deliverables. **Three shipped** — the dependency map, the claims audit, and the Code Atlas, all merged. 🔴 **The fourth did not: the `classify.ts` extraction.** `current-state.md` still names it the critical path and the sprint sequences **Steps 100, 101 and 110** against it.

**Two things exist ONLY in that file, and both die with it:**

1. **The proposed seam decomposition** — the five-module extraction plan for `classify.ts`, sequenced lowest-risk-first: `rendering.ts` → `stamps.ts` → `validators.ts` → the 16-tool MCP surface under `src/agent/tools/` → `src/agent/identity.ts` last, with `runEvrynQuery` + the breaker gate staying put. ⭐ **The load-bearing insight is the ORDERING, not the list:** `verdict.ts` and `preload.ts` import ONLY pure functions from the god-module, so steps 1–2 sever both files from it completely — shrinking the blast radius *and* attacking the 7-file import cycle, at the lowest risk in the plan. ⚠️ **This appears in no other document.** **It is also what `current-state.md:64` was re-aimed to point at** *(that re-aim is DONE and committed)*.
2. 🔴 **A live, conditional, OWNED requirement with NO Step behind it — Justin's ruling of 2026-08-10: the top-of-file truncation-canary sweep must RIDE the extraction.** Inserting one line at the top of every source file shifts **1,096 `file:line` references across 65 files**, so running it standalone would introduce a deliberate, systematic off-by-one across the whole corpus. **The bottom half was swept 2026-08-10 (all 45 files carry one); the top half was deliberately not run.**
   > ⚠️ **AND ITS ESCAPE CLAUSE IS THE PART THAT DIES SILENTLY:** *"if the extraction is ever cancelled or indefinitely deferred, the top half becomes owed again as its own trip — flag it rather than letting it die with the lane."* 🔴 **No Step in either sprint tracks this.**

⇒ **Recommend to Justin: this is the same shape as the QC-sweep findings in §3.2 — a real doc misfiled as a working doc.** **Do not decide its home unilaterally; bring him a recommendation.**

### ❓ 8b — A CHANGELOG entry may be owed for my session, and I did not write one

**Checked at source: no `CHANGELOG.md` in `_evryn-meta` or `evryn-backend` was touched on 2026-09-01.** **My session made six commits across three repos** — a new research doc, two breadcrumbs, a sprint Step-body lift, and a one-word inventory fix, **all pushed.**

⚠️ **Whether that rises to an entry is a judgment call I did not make, and I am flagging it rather than deciding it.** **The routing rule says an entry is a *deploy or substantive change* (code, identity, schema, protocol, doc, dashboard) and NEVER session-management narration.** **The previous session's ruling was that no entry was warranted because it shipped nothing** — **mine did ship, so the same reasoning points the other way.** 🔑 **The meta changelog is skimmed by every AC on load, which is the argument for an entry; "these are doc moves, not behaviour" is the argument against.**

⇒ **Put it to Justin. Changelogs are excluded from the propose-first gate, so if he says yes you may simply write it.**

### 📌 8c — Closed, so you do not re-open them

- **The `#lock`-protocol wording question is ANSWERED.** *(Justin asked whether retiring session docs promptly solves the routed-but-untracked problem, and whether to say "harvested" or "reaped.")* **The answer: retiring faster does NOT solve it — step 10 asks whether the DOC is still needed and never asks what the doc ROUTED OUT and whether it landed.** **On vocabulary: coin nothing** — `reaped` is already this estate's word for worktrees, branches and subagents; `retire` is step 10's own verb. **Do not re-derive this.**
- **ACP consult ③** *(did all of the loading-architecture brief ship, or only Lane A?)* — **ANSWERED: it stays retired**, all seven of its gates verified closed at source.
- **`SPRINT` Step 70a's injection-vigilance mechanism** is lifted into the sprint and no longer points into a retiring folder. **The identity half is ROUTED to Mira, not written** — that remains correct and is not yours to write.

---

## §9 — THE TEST THIS FILE MUST PASS

**At every set-down, ask: *if I died right now, could a fresh instance pick up from this file alone?*** **Yes → a re-spin costs a LOAD, not a MEMORY.**

⚠️ **And the rule that governs this file's own future:** *(`lock-protocol.md` step 10)* — **the moment you write a correction pointing at another section of THIS document, it needs SUPERSEDING, not another correction.** **The testable question: does the new text tell the reader to DISTRUST older text in the same file?** **Yes → supersede. No → carry on.** ⭐ **Superseding is safe because the old file is archived, never deleted** — but **a superseding doc that drops something makes the drop invisible, so be thorough.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
