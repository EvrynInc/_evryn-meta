# Full Startup Context Cascade — Website (`evryn-website`)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file:** the **per-build full cascade** for work on the marketing website (`evryn-website`, Next.js on Vercel). Load the Light Startup Context Cascade (in `_evryn-meta/CLAUDE.md`) first, then this. One entry in CLAUDE.md's cascade router. **Owner: AC.**

---

> ⏳ **PLACEHOLDER — being authored 2026-07-21 by a dedicated AC subagent** (loaded on the product runtime, since the site ties into it). Until it lands, a session working on `evryn-website` should: load the Light cascade, read `evryn-website`'s own `README`/structure, and treat the product runtime as the system the site ties into. The authored version will replace this with the precise, *humble* (open-to-change) load list.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
