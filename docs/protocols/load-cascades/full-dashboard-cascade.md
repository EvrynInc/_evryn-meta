# Full Startup Context Cascade — Dashboard (`_evryn-meta/dashboard`)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file:** the **per-build full cascade** for work on the monitoring dashboard (`_evryn-meta/dashboard`). Load the Light Startup Context Cascade (in `_evryn-meta/CLAUDE.md`) first, then this. One entry in CLAUDE.md's cascade router. **Owner: AC.**

---

> ⏳ **PLACEHOLDER — being authored 2026-07-21 by a dedicated AC subagent** (loaded on the product runtime — the dashboard ties into BOTH the product AND the team runtime). Until it lands, a session working on the dashboard should: load the Light cascade, read the dashboard's own structure + its data/schema contracts (`dashboard/api/*`), and treat the product + team runtimes as the systems it reads from. The authored version will replace this with the precise, *humble* (open-to-change) load list.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
