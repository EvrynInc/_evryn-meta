# AC Writing Protocol

> **Truncation check:** The last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file:** Read this before writing to **any** document — source-of-truth docs (Hub, spokes, ARCHITECTURE, BUILD, ADRs), `current-state`, `CHANGELOG`, session docs, mailbox messages, protocol docs, `LEARNINGS` (`AGENT_PATTERNS` was collapsed into it 2026-08-12 and the file retired 2026-08-19), research files, briefs — anything that will be read later. This is the **craft layer: HOW to write** so a future instance (often a *you* with almost no context) can trust and use what you wrote. **WHERE each kind of content goes** — the Diátaxis routing table, the doc altitudes, the absorption/flow-up rule, the routing of decisions→ADRs / state→current-state / build-detail→repo-docs — stays in `_evryn-meta/CLAUDE.md` (Documentation Approach). That governs the *filing*; this governs the *writing*. When they touch (e.g. "one home per item"), both carry the pointer.
>
> **AC owns this file.** It is the AC-side counterpart to the team's `evryn-team-workspace/shared/protocols/writing-protocol.md`. The two are **deliberately kept separate** — see the #sweep note at the bottom.
>
> **Do not edit this file without Justin's approval.** Propose changes; don't make them directly.

---

## Writing for salience (the meta-principle that governs everything below)

If you're writing anything another instance must *follow* — a rule, an instruction, a protocol, a constraint in a doc — remember that **reading is not following.** An instance will read a rule, feel compliant, and then not apply it twenty tool calls later. Writing that doesn't stick is writing that doesn't exist. Three things make an instruction stick:

1. **Match the weight to the intent.** A command that reads as a suggestion gets skipped — and if it was actually a command, that's a disaster. A suggestion that reads as a command makes the system brittle (followed rigidly when judgment was wanted). Be explicit about which you mean: hard constraints get hard language ("you must," "never"); guidance gets guidance language ("consider," "prefer," "avoid"). The reader should never have to guess whether something is mandatory or discretionary.
2. **A concrete consequence** — "do X, *because if you don't, Y breaks.*" Without the consequence, even commands decay into suggestions once enough context passes.
3. **A specific action** — "run this command" / "add this line" / "read these 10 lines." Vague instructions get vaguely followed.

When writing any instruction, ask: does it have all three? If not, it will decay.

---

## Writing a rule the current models will actually follow

*(Justin's direction, 2026-09-08.)* **The problem this solves:** these manuals accrete. `ac.md` reached ~208,000 characters — about 74,000 tokens — and gained 3,300 more during the five days we spent discussing trimming it. **Nothing in it is careless; it grows because every rule in it was earned.** ⇒ **So the question is not what to cut. It is what SHAPE a rule takes, and when prose is the wrong instrument entirely.**

### The four parts of a rule

| Part | Job | Written for |
|---|---|---|
| **Rule** | What to do. Present tense, imperative, one sentence. | The agent, at the moment of action |
| **Mechanism** | Why the obvious move fails. | The agent — **this is the part that generalizes** |
| **Consequence** | What breaks when it is missed. | The agent |
| **Provenance** | Dated, parenthetical, short. | **A later auditor** |

🔑 **The mechanism must never be cut — and it is NOT the story of how we learned it.** A rule without its mechanism can only be applied to the cases someone thought to enumerate; a rule *with* it can be applied to the case nobody wrote down. **That is what "keep the why" means.**

⚖️ **Provenance does a different job, for a different reader, and it steers nothing.** Its entire function is to make the rule **falsifiable** — so a later reader can prove it dead. That is worth *some* tokens, and it is why these manuals carry dates at all. **What it is not is a narrative.** ⇒ **The size test: if the provenance runs longer than the mechanism, you wrote a war story. Cut it back to a dated clause.**

### Persuade. Emphasis is what you reach for when you have not.

*(Justin's reframe, 2026-09-08.)* **Emphasis works by CONTRAST, so it saturates.** At 64 red markers in one file, red carries almost no information — everything is shouting, so nothing is. **Persuasion works by COMPREHENSION, so it does not saturate.** A hundred well-argued rules each still argue. ⇒ **There is no emphasis budget to ration. There is a better instrument to use.**

🔑 **THE TEST, the moment you reach for a marker: is the ARGUMENT doing the work here, or am I shouting because I have not made it?**
- **The passage already persuades** → the marker is decoration. It is not the thing that made the rule land. Is it even needed? If it is, keep it. If not, lose it.
- **The passage does not persuade** → the marker is standing in for an argument you owe. ⇒ **Write (or strengthen) the argument.** ⚠️ **That usually makes the passage LONGER, and that's okay — *if it's earning its place at the table*: a rule that keeps being missed is far more often under-argued than under-emphasised.**

⭐ **What actually persuades a model — and the best writing in these manuals already does all five:** *(current to model 5 models (Opus 5, etc.) 2026.09.08)
1. **A mechanism that surprises.** *"A command that failed to EXECUTE prints exactly like a clean result."* The reader can feel the trap; nobody has to tell them to care.
2. **The consequence in the reader's own currency.** *"You ship someone else's work under your message"* — not *"attribution issues may result."*
3. **The TELL, so the failure is recognizable in the moment it happens.** *"An `Edit` that fails to match text plainly present is a NUL symptom."*
4. **The reader's counter-move, anticipated and answered.** *"You will want to write a better grep — the fix is a different instrument, not a sharper pattern."* **Meeting the objection is what separates persuading from asserting, and it is the half most often missing.**
5. **Honesty about cost and limits.** *"The entire cost is one extra commit per merge."* *"This is wrong HERE for a specific reason, not a universal one."* **Conceding what is true buys belief in everything around it.**

🔴 **BE VERY CAREFUL STRIPPING STRIDENCY FROM A RULE THAT IS WORKING.** A loud rule carrying no dates is usually one that kept failing *before anyone was recording dates* — **the volume is the fossil record**, and dating incidents is a recent practice here, so **the oldest and most battle-tested rules carry the fewest.** ⇒ **Never read absent provenance as "this happened once."** **Ask instead whether the passage carries its argument: if it does, leave it alone; if it does not, add the argument** — and only then ask whether the volume is still doing anything.

### Then ask who pays for a miss

**The question is not *"will anyone notice?"* — noticing and absorbing the cost are different things.**

| Who catches the miss | What it costs | What the rule needs |
|---|---|---|
| **Nobody** | It compounds silently, and the agent's own confidence is untouched | The fullest treatment: mechanism, tell, and consequence |
| **Justin** | **The scarcest resource in the estate** — one person across many parallel lanes | **Full persuasive weight. A rule he has to re-teach is not a cheap rule.** |
| **The agent itself** | A retry | A clear rule and its mechanism. Nothing further |

⚠️ *(This corrects an earlier draft of this section, which treated "visible" as "cheap." Ballot placement and reply structure are highly visible — Justin notices at once — and they are among the most expensive things to get wrong, precisely because he is the one who pays for every miss.)*

### And ask whether prose is the instrument at all

| | |
|---|---|
| **A machine can check it** | **Build the guard** (hook, script, tool restriction). Prose shrinks to one line naming it. |
| **No machine can check it** | **Prose is what you have — so make it persuasive**, by the five above. |

🔑 **A rule you must REMEMBER to obey is the weakest thing you can build, and the proof is these manuals themselves: most rules here exist BECAUSE they were violated by an agent that had already read them.** ⇒ **Adding volume to a rule that was read and skipped is doing more of the thing that already failed.** *(`scripts/scan-control-chars.js` was built in 2026-08 only after prevention-by-instruction had failed nine times, twice by authors mid-sentence about the hazard. The hook has held since.)*

⇒ **A rule you are tempted to state TWICE likely doesn't need repeating — it needs a guard.** State it once, and file the second copy as a hook candidate.

### Two conversions

- **A prohibition with no live provenance becomes a positive statement of what to do.** Describing success beats enumerating failure, and forbidding a mistake the model was not going to make can anchor it toward that mistake. **Prohibitions covering demonstrated failures stay** — the test is whether it still happens, not whether it sounds forbidding.
- **Keep an example only where the OUTPUT SHAPE is the lesson** — how to talk to Justin, how to render a ballot, what a handoff looks like. **Cut examples that merely illustrate judgment:** a concrete example is the strongest signal in a prompt, so the model matches its length, tone and structure — and an example of judgment freezes one instance of it into every later case.

### A worked example

*(The three-dot-diff rule in `ac.md` — 2,523 characters before.)*

> 🔴 **Read a branch's diff with three dots: `git diff main...branch`.**
> Two dots compares the two *tips*, so anything `main` gained that your branch lacks renders as a deletion **by your branch** — and `main` moves constantly in a hot repo, so the diff grows more alarming the longer the branch lives. Three dots compares against the merge base: what the branch actually changed.
> Get it wrong and a reviewer either rejects a clean branch, or "fixes" the phantom deletion and genuinely reverts someone's work.
> *(4 incidents: 2026-08-04 ×2, 2026-08-11 ×2. Note `git log main..branch -- <file>` genuinely wants two dots — it lists commits, not trees.)*

**~900 characters, and every load-bearing part is kept:** both halves of the mechanism, both consequences, dated provenance, and the one exception an agent actually needs. **What went: the flourish, the retelling, and a cross-reference that existed only because the rule is stated twice in the file.** ⚠️ **And note where the rubric sends this one next — the failure is silent AND machine-checkable, so its honest end-state is a `PreToolUse` guard plus a one-line pointer.**

---

## Core rules

**Write for a fresh instance.** When writing anything that will be read later, imagine waking up as a completely new instance with very limited context — none of this conversation, none of your current working picture. Will what you've written make sense? Include enough specific context that a cold reader understands what you mean without reconstructing your reasoning. Use explicit actors and concrete references — not "we decided to defer it" but "Justin and AC deferred the migration because the v0.2 deadline was more urgent." When helpful, include the *why* and ideally an example — not just the conclusion. **The sharpest version of this trap: you've just absorbed subagent reports the reader has never seen** — any reference that leans on them (a worker labeled "W1," a scout's shorthand, "the ladder") is invisible scaffolding to everyone but you. Re-ground every borrowed term in place. (Justin caught a batch of these in the 2026-07-10 design docs — even reading *in the flow*, the shorthands didn't parse.)

**Do not over-compress.** You have a strong tendency to re-compress language that was written a specific way for a reason — and doing so *often breaks things.* Before tightening prose, consider *why* it might have been verbose: the phrasing may carry nuance, emphasis, or context a future reader needs. Make sure any redundancy is *necessary* redundancy — but don't assume verbosity is waste. "Decided to use X" is almost useless; "Decided to use X because Y was too slow for Z, and the tradeoff we accepted is W" is worth keeping. Two AC-specific habits this depends on:
- **Active voice with explicit actors.** "AC will archive these files," not "the files will be archived." Passive voice creates genuine ambiguity across instances that can't clarify in real time.
- **This applies to code, too, not just prose.** Don't clean, refactor, or delete code without full context of *why* it exists — what looks redundant or messy may be intentional. Have the relevant context before you change anything.

**Keep context clear (scope + circumstances).** A note without context misleads. Make the *scope* and *circumstances* obvious. "Don't use sub-agents for doc reading" is unclear. "When precision matters, prefer reading docs directly over sub-agents, because sub-agents tend to compress nuance — but sub-agents are fine for low-lift tasks like finding a file path" is clear. A future reader should understand the *when* and *why*, not just the *what*.

**Cross-reference the most-recently-evolved thinking first.** When integrating older content into a newer structure, check the newest source first — it may have resolved ambiguities or superseded positions the older source still carries. Folding the stale version forward silently re-introduces a settled error.

**When you REMOVE wording, do not argue against the wording you just removed — just write the new instruction.** *(Justin, 2026-08-20, having watched it repeatedly: "when you remove wording from a doc, I notice you're arguing against the wording you just removed… the wording is *gone* — you don't have to argue against the old wording — just put in the new instruction.")* **The reader never sees what you deleted**, so a clause defending the change — *"this supersedes the previous X"*, *"NOT in Y"*, *"the old version said Z"* — is at best noise. **At worst it does damage in two directions:** it **re-introduces the wrong idea into the reader's head**, which is the same mechanism that makes an affirmative rule beat a denial list *(denial-by-enumeration invokes the frame it denies)*; and it reads as **defending an edit rather than stating a rule**, which quietly makes the rule feel negotiable. ⇒ **Delete cleanly, and write the new instruction as though it had always been the instruction.**
- ⚠️ **One carve-out, and it is a REDIRECT rather than an argument: when the old wording still exists somewhere the reader will meet it** — another doc, a live handoff, a frozen record, a peer's memory, a line someone quoted — **one short pointer earns its place.** *(e.g. "handoffs written before 2026-08-20 say 'canary' and mean the heartbeat.")* **State the mapping; do not justify the change.**
- **The test, and it is one question:** ***will the reader encounter the old wording anywhere else?*** **No → delete it clean and say nothing about it. Yes → one line of redirect, no defense.**

**When a rule is CONDITIONAL or TEMPORARY, write the condition AND ITS OWNER into the rule itself.** *(Adopted 2026-08-21 from the specimen that does it right — `doc-update-routing-protocol.md`: "a THIRD exclusion is coming and is not yet in force… whoever lands that consolidation should update this row." **Three things in one sentence: the rule, the condition that changes it, and who clears it.**)*
- 🔴 **A rule stated as an ABSOLUTE, when the real constraint is CONDITIONAL, gets either broken or obeyed past its usefulness — and both failures look like compliance from the outside.** *(Live specimen: "never rename a mailbox" was really "never while a watcher is armed on it." Inside 48 hours it was written as an absolute and then broken — by people who were each correct.)*
- ⇒ **Write all three parts — and take extra care with the OWNER, because it is the part writers most often leave out.** ⚠️ **Leaving it out is what turns a temporary note into a permanent one: a condition nobody owns is a condition nobody tests.**
- ⭐ **The payoff is that a stale rule announces itself.** A reader who meets *"not yet in force; whoever lands X updates this row"* can check X in seconds. **A reader who meets a bare absolute has no way to know it expired** — so it survives, unexamined, exactly as long as nobody questions it.
- 🔴 **AND THE OTHER HALF, WHICH THE ABOVE DOES NOT COVER: WHEN THE CONDITION ACTUALLY FLIPS, GO AND RE-DECIDE EVERY RULE THAT RESTED ON IT. Correcting the FACT is not finishing the job.** *(Added 2026-09-02 after two live specimens in one session.)*
  - ⚠️ **The failure is not a missing correction — it is a correction nobody converted into a DECISION.** **Both specimens had already been annotated with their own obsolescence and were still being obeyed.** **One carried a dated note reading, in effect, *"this no longer rests on visibility; choose on other grounds"* — and every agent kept choosing the old default for twelve more days.** **The other's premise was contradicted three sections above it, in the same file.**
  - 🔑 **Why annotation is not enough, and this is the mechanism: a rule is followed by people checking WHAT IT SAYS, never WHETHER ITS REASON STILL HOLDS.** ⇒ **A doc that records *"the reason for this is gone"* and leaves the rule standing has written down a contradiction and called it maintenance.** **The next reader sees a rule with a caveat, follows the rule, and is not wrong to.**
  - ⇒ **So when you correct an environmental fact, the same edit closes the loop: find what rested on it, and either re-state the rule on new grounds or change it.** ⭐ **If you genuinely cannot do that in the moment, say so explicitly and NAME AN OWNER** — *"this default now rests on nothing; ACP to re-pick"* — **because an un-owned contradiction is indistinguishable from a rule that is simply still true.**
  - 📌 **The tell, cheap to run when you are already editing: if you are writing the words *"no longer,"* *"this has changed,"* or *"as of [date] this is false"* — stop and ask what was built on the thing you just retired.**

**A CORRECTION IS LIKELY MORE USEFUL IN THE FILE IT CORRECTS THAN IT WOULD BE LIVING *ONLY* IN THE DOCUMENT THAT LOADS IT.** *(Justin's ruling, 2026-09-02.)* When you find a specific claim in a reference document wrong, the cheapest place to say so is the load list, cascade or onboarding doc that sends people there — **and a pointer there is fine; what fails is letting it be the correction's only home.** ⚠️ **Some readers reach a document by another route** — a search, a cross-reference, a link from somewhere else — **and a warning parked in the loader never reaches them**, so it protects mainly the readers who were already being walked through. **And a per-claim warning list becomes a second maintenance surface nobody owns**: it keeps naming claims that have since been repaired *(sending a reader to re-derive settled work, or to "fix" a correct claim back to the wrong one)* while missing ones that have since gone stale.
- ⇒ **Fix the claim at source — dated, with the instrument named and its provenance stated.** **If you genuinely cannot fix it, put the caveat INSIDE the document, beside the claim.**
- **What belongs in the loader is the durable reading instruction, not the per-claim list** — *"this document tags its claims as verified or asserted; treat an asserted one as a claim to check"* — because that never needs updating.
- ⭐ **Live specimen, and the part worth keeping: the warning list that prompted this ruling had already reasoned its way to the answer** — its own closing line read *"keep it current or delete it."* **It went stale anyway.** 🔑 **A maintenance instruction attached to a second copy does not stop the second copy rotting.** *(Of the four claims it named, two had since been repaired and two had been settled by execution — in opposite directions.)*

**WHEN YOU CARRY A PLAN FORWARD INTO A TRACKER, SAY WHEN IT WAS THOUGHT AND THAT THE GROUND HAS MOVED — it is a snapshot, not a spec.** *(Justin's ruling, 2026-09-02.)* Work often gets planned, deferred, and then rescued into a tracked item months later. **The rescue is right; the risk is that a tracker entry READS as current intent** — that is what trackers are for — **so a plan written against a system that has since changed gets handed to a builder as though it were still true.**
- **Say three things: that it is proposed, roughly when it was thought, and that the thing it plans has moved since.** ⚠️ **"Not built" is usually redundant in a tracker** — an open item is by definition not built — **so spend the words on the staleness instead, which is the part a reader cannot infer.**
- ⭐ **Frame it with humility rather than hedging:** *"this is our first draft and a snapshot of the thinking at the time"* is more useful than *"treat with caution,"* because it tells the reader **what kind** of thing they are holding. **Then say plainly what to do about it — re-derive against the current system before briefing anyone.**

**Use nuanced language.** Prefer language that reflects the *actual* degree of constraint. "Consider" rather than "do"; "avoid" rather than "don't"; "prefer" rather than "always." Reserve absolutes ("never," "must," "always") for genuine hard constraints — security invariants, privacy requirements, trust guarantees, things truly non-negotiable. Most guidance has exceptions or context-dependence; language that acknowledges this is more honest and more useful than a preference dressed as a rule. When something *is* an absolute, say so clearly — but earn it. (This is the same instinct as preferring structural invariants over instructional ones: a rule that can't be violated beats a rule that says "don't.")

---

## Writing for progressive depth

**One home per item; pointers thread a needle — the elevation discipline lives in CLAUDE.md.** The doc-*architecture* rule (each item lives in exactly ONE doc at its altitude; a pointer is descriptive enough to pull a reader to the real doc, but never so complete it substitutes for the read) is canonical in `_evryn-meta/CLAUDE.md` (Documentation Approach). It governs *where* content lives — read it there. This protocol's companion craft rule is the structural one below: *how* to write so an item at any altitude is scannable.

**Prefer structure over walls of text.** Any doc section carrying more than a couple of distinct points should be bullets or a numbered list, not a dense paragraph. A big undifferentiated block is hard to parse and *things get missed in it*; structure makes each point catchable. Compressing bullets into a block of prose to "save space" is strictly worse — same information, harder to recall. The constraint on an orienting doc is cognitive load, not file size; when a doc needs a size target, use character count, not line count (line count penalizes the whitespace that actually helps).

---

## Before you write

**Re-read before editing a shared file.** Your loaded version may be stale — other instances (parallel ACs, AC + Soren, AC + DC) write to the same files across sessions. Before appending to or editing a shared file (`current-state`, a sprint doc, anything multiple actors touch), run a line count and read the relevant section to confirm what's currently there. Edit on a stale version and you insert in the wrong place or overwrite someone's work — and because it's shared, the damage propagates to everyone who reads it next. (This is the writing-side companion to the "verify branch before every edit" discipline in CLAUDE.md.)

**Self-review before editing a source-of-truth document.** Ask three questions: (1) Would this mislead a future instance arriving with minimal context? (2) Am I stating something as fact that I haven't verified? (3) Am I closing a door that wasn't mine to close?

**Source-of-truth edits need Justin's approval — the rule lives in CLAUDE.md.** *Which* docs require proposing-before-editing (ARCHITECTURE, BUILD, Hub/spokes, LEARNINGS, protocols; CHANGELOG/ADRs/mailboxes excluded), and the ARCH/BUILD *edit-rights-vs-authorization* distinction, are an **authorization** rule — canonical in `_evryn-meta/CLAUDE.md` (Documentation Approach + the commit/authorization framework), because it belongs with the always-loaded gate, not gated behind reading this protocol. Honor it before editing any source-of-truth doc.

**Research without breadcrumbs is dead research.** When you create a research or analysis file, place breadcrumbs in the build/architecture docs where that research would change the quality of thinking — even preliminary ones. They ensure the research gets discovered at the right moment instead of sitting unread in a folder.

---

## Formatting conventions

**Path convention (in docs and config, not code).** Always use repo-root-relative paths with forward slashes. Within a repo: `docs/hub/roadmap.md`. Cross-repo: `_evryn-meta/docs/hub/roadmap.md`. **Never** use `../` (breaks when files move depth) or absolute paths like `C:\Users\...` (breaks across machines). This works from any clone on any machine. Code imports and programmatic references follow their language's conventions. *(The one exception is chat output to Justin, where `../`-prefixed sibling paths are correct so links open in his current VSCode window — that's a CLAUDE.md communication rule, not a doc rule.)*

**Cross-repo paths are rooted at the shared parent — write them so they're unambiguous.** Every Evryn repo is a **sibling folder under one shared parent directory** (e.g. `…/Code/_evryn-meta`, `…/Code/evryn-backend`). So a cross-repo path like `_evryn-meta/docs/hub/roadmap.md` resolves from that **parent** — from inside any repo it means *"go up out of this repo, into the sibling `_evryn-meta`,"* **not** a folder inside your current repo. Written this way a reader can **follow the reference without knowing the repo names** — the path is self-locating. This matters most in **agent files (CLAUDE.md, agent definitions)**, where an agent forms its map of where everything lives: a correct sibling-relative path is what lets it find another repo, so we don't lean on a memorized repo list to paper over ambiguous ones.

**Pull timestamps from the system — never type them from memory.** Run `powershell -Command "Get-Date -Format 'yyyy-MM-ddTHH:mm:sszzz'"` before writing any timestamp. Use the full ISO timestamp for anything where chronological precision matters — current-state appendages, session docs, archive filenames, memory-style notes. A wrong timestamp can corrupt the chronological record the interlocking systems depend on.

**🔴 Do not FORWARD line counts. Hand over the command that produces them.** *(Justin's ruling, 2026-09-08: "they're always stale when they get forwarded like this.")*

Applies to anything a later reader will act on — a load list, a handoff, a brief, a cascade. **Give the paths and give `wc -l`; the reader derives the number at the moment they need it.** When a count genuinely has to appear — a subagent load list needs spans so a partial read is visible — **say it is a hypothesis, name the command, and tell the agent to flag a delta rather than reconcile it silently.**

**Why forwarding fails, and it is not that counts drift — it is that forwarding LAUNDERS a stale number into a fact.** A count copied from one document to the next arrives with no provenance and no way to test it, so each reader inherits it as measured. **The record: `ac.md` has been forwarded as 749, then 911, and measured 940 in the same week; `current-state.md` as 220, then 212, then 217.**

⚠️ **And know the one way `wc -l` lies, because it lies precisely where the count is used as a check: it counts NEWLINES.** A file whose last line has no trailing newline reports **one fewer line than it has** — and the line it omits is the **last** one, which is where terminators live: a truncation canary, a closing marker, a checksum. ⇒ **A read bounded by that number stops one line short, finds no canary, and reports a truncation that did not happen.** **Read past the count, or `tail -1`.** *(Live: a generated map reports 634 and its canary is line 635.)*

**⇒ A count is fine for budgeting and useless for proving completeness. The bottom canary proves a full read; the number never did.**

**Every file you create or fully rewrite gets a truncation canary.** Add a note near the top saying the last line should read `FULL FILE LOADED`, and the line `Truncation canary — DO NOT REMOVE: FULL FILE LOADED` at the bottom. This lets a reading instance self-diagnose an incomplete load. The cost is two lines; a silent truncation is invisible critical-information loss.

**Every document gets a "how to use this" header** explaining what belongs in it, what doesn't, and how to write it. (For *where new content goes*, the header points at the routing table in CLAUDE.md — it doesn't re-litigate it.)

**🔴 WRITING A `\uXXXX` ESCAPE IN PROSE EMITS THE REAL CHARACTER — and a NUL makes the file BINARY to `grep` and `git diff`, so every text search silently skips it *and reports success*.** *(Six instances in one session, 2026-08-17, plus three earlier across eight repos.)*
- **The mechanism:** your file-writing instruction travels as **JSON**, and JSON interprets `\uXXXX` natively. **So prose that QUOTES an escape becomes the character.** ⇒ **The hazard concentrates precisely in documents ABOUT the hazard**, because those are the only places anyone types these escapes. **Every known instance occurred while documenting it** — including by authors who had read the full write-up that same day. ⚠️ **It is not reliably reproducible within one edit: on 2026-08-17 one escape in a single edit survived as text while another became a NUL. "It worked last time" is not evidence.**
- 🔑 **TWO TELLS, both learned the expensive way, because the damage hides itself:**
  1. **An `Edit` that FAILS TO MATCH text plainly present is a NUL symptom** — the file has gone binary. **Scan it; do not retry the edit.**
  2. ⚠️ **`JSON.stringify` renders a real NUL and the literal six-character text IDENTICALLY**, so the obvious inspection *cannot tell them apart*. **Only a byte-level check can** — `Buffer.indexOf(0)`, or the scanner.
- ✅ **The fix is two committed tools and they are a PAIR:** `node _evryn-meta/scripts/scan-control-chars.js <repo…>` to find, `node _evryn-meta/scripts/repair-control-chars.js <file>` to fix. The repair replaces the byte with its literal text and self-verifies — **then re-run the SCANNER independently, because a tool reporting its own success is not a check.**
- ⚠️ **You cannot TYPE the character to test with; every authoring path strips or interprets it. CONSTRUCT it** (`String.fromCharCode(0)`) — which is why both tools do, and why neither contains one.
- 🔴 **Prevention is the weak answer and we have proof: "remember to double the backslash" has now failed nine times, twice by authors mid-sentence about it. TREAT A SCAN AS THE MECHANISM AND THE INSTRUCTION AS A COURTESY.** *(A scan at `#lock` is what caught all six on 2026-08-17 — two of which had already been committed and pushed.)*

**🔴 Backticks in a shell commit message are COMMAND SUBSTITUTION — they will silently execute and blank your text.** *(Cost AC0 a mangled commit, 2026-08-05.)* We write in markdown, so `` `like this` `` is reflexive — but in `git commit -m "…"` under bash, a backticked span is **run as a command** and replaced by its output. Writing ``git commit -m "…a passing `npm run typecheck` is not evidence…"`` actually *ran* `npm run typecheck`, and the commit landed with that clause **deleted**. It fails silently: the commit succeeds, and you only notice if you read the message back.
- **The fix — use a heredoc for any commit message containing backticks:**
  ```bash
  git commit -F - <<'MSG'
  Subject line here
  
  Body with `backticked` identifiers, $variables, and "quotes" — all literal.
  MSG
  ```
  The quoted `<<'MSG'` delimiter is what disables *all* substitution. Unquoted `<<MSG` does not.
- **Same trap, same fix, for `$`** (variable expansion) and `!` (history expansion in interactive shells). **Any commit message quoting code — which is most of ours — should use the heredoc by default**, not as a special case.
- **Verify after writing one:** `git log -1 --format=%B` and confirm the text you meant is actually there. A commit message is a durable record other agents read; a silently truncated one misleads exactly as a truncated doc does.

**File naming.** Documents with a temporal quality — session docs, briefs, anything tied to a point in time — start the filename with the date: `YYYY.MM.DD-name.md` (sortable, immediately clear when it was created). Living documents that evolve continuously (a spoke, a build doc) skip the date prefix and carry an internal "last updated" line instead. Archive copies get a full timestamp name; the live file keeps its stable name.

---

## Before you submit

Do one final pass against every requirement above. These are requirements, not suggestions — override only with a compelling reason. If the file is missing anything specified here, fix it before you finish.

---

## [For #sweep only] cross-reference with the team's writing protocol

The Operations Team has a very similar writing protocol (`evryn-team-workspace/shared/protocols/writing-protocol.md`). These have very intentionally been kept **separate** — AC writes AC's docs in AC's ecosystem; the team writes the team's and the team's protocols are just a little different from AC's — but they benefit from the learnings of each other. So during **#sweep**, compare the two and see if there's anything they can share with each other (a sharper rule, a failure mode one caught that the other hasn't, a phrasing worth borrowing). Don't merge them; cross-pollinate them.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
