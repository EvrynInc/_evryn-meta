# Evryn — Deploy Log (production ship ledger)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file:** The single source of truth for **what shipped to production, when, and under which version.** One row per production deploy, newest first. When anyone references "round-2" or "the 6/29 ship," map it here.
>
> **The authoritative pair:** an item's STATUS (`TODO`/`DONE`) **+** this ledger. A *"riding round/wave-X"* tag on a sprint item is **intent to ship, never proof it shipped** — always check the item's status and this ledger. *(This is the exact gap that made the determination-sharing guardrail look shipped when it wasn't — Step 55, 2026-07-01.)*
>
> **Naming convention (adopted 2026-07-01):** each production deploy is **`v0.2.N`** — the Nth prod ship within the v0.2 phase (v0.3 ships become `v0.3.N`, etc.). **One bump per `railway up` to prod.** **No leading zero** (`v0.2.9` → `v0.2.10`), matching the semver / git-tag / npm convention — so we never have some systems padded and some not.
>   - **Sort-safety (why no-pad is fine):** the `v0.2.9 → v0.2.10` ordering is only "weird" under naive *lexicographic* sort. The systems we use sort **version-aware**: `git config tag.sort version:refname` is set on `evryn-backend` (so `git tag` lists `v0.2.10` after `v0.2.9` correctly), and this ledger table is hand-ordered newest-first. No auto-lexical-sorted surface exists.
>   - **Each deploy is git-tagged** on its commit (`v0.2.N`) in `evryn-backend`, so the number is anchored to the exact commit forever (`git describe`, `git log --tags`).
>
> **Legacy Rosetta (so existing references still decode — no retroactive rewrite of dated records):**
> - **"round-1"** = the discarded **2026-06-17** lobotomized hardening attempt — **NEVER shipped** (all code was junk, thrown away).
> - **"round-2"** = **v0.2.2** (the 2026-06-29 hardening deploy).
> - **"go-live bundle"** = **v0.2.1**.

---

| Version | Date | Commit (tag) | What shipped |
|---------|------|--------------|--------------|
| **v0.2.9** | 2026-07-23 | `6886417` (tag `v0.2.9`) | **AC2 fast-follow — SPRINT 91 halt-resume gatekeeper lane-resolution fix (Mark-blocker).** The post-M1-halt resume path (`resumeParkedItem`, `src/email/process.ts`) resolved the inbound sender via a blind `findOrCreateUser({status:"lead"})`, bypassing the gatekeeper-inbound-lane resolution (`resolveGatekeeperByInboundAddress` + `decideSenderResolution`) the ingest path uses. So on a resume, a sender arriving via a registered **non-primary** gatekeeper lane failed to be recognized as the gatekeeper → a phantom `lead` + triage against a **blank profile** (violating the Step-61 lane guarantee). Bit BOTH forwards (any clustering-off window) and direct messages (always). **Fix:** extract sender resolution into ONE shared `resolveInboundSender` helper both `processEmail` and `resumeParkedItem` call (a third path can't drift) + escalate-on-resume anomaly handling (a parked item whose lane registration was removed transitions the EXISTING item to `escalated`, never blind triage). **Independent QC-GO** (fresh full-load, zero blockers; QC ruled no live-path model exercise required — deterministic pre-triage change, interactive pathway untouched); 18 DB-free unit tests + typecheck clean + regressions green. **ZERO prod migration; code-only** (`process.ts` + a new test file). FF-merge `6886417`, `railway up` SUCCESS, **verified clean boot** (Supabase-prod / Slack / polling / health; catch-up deduped correctly). **#lock doc-flip (SPRINT 91→DONE) pending** — rides the broader ③-#lock doc batch (Justin-vetted). A pre-Mark DEV park→resume confidence-exercise is recommended (fold into Gate-B), NOT a ship gate. — AC2. |
| **v0.2.8** | 2026-07-22 | `2d14c2b` (tag `v0.2.8`) | **AC2 cost lane ③ — Runtime Bookkeeping + structured verdict (Step 57 / ADR-051), the keystone.** The runtime now PRE-LOADS what Evryn needs for triage (prior-triage history + contact context) and PERSISTS her disposition itself; Evryn returns a structured verdict via typed tools (`record_verdict` / `record_outcome`, or `submit_draft` for gold/edge — the draft IS the verdict) and does ONLY judgment (a pass drops ~15 model-turns → ~2–4). **Her raw `supabase_upsert` write-surface is REMOVED** (the write-side sibling of AC4's read-lock; ADR-033 amendment) — flexible correction survives only as the bounded, **operator-channel-only `correct_user_field`** (field-allowlist: display_name/full_name/email/outbound_address). **`triage_result` canonicalized for all 3 verdicts** (gold/edge/pass — ADR-018 amendment; the prediction→outcome audit is now a direct query). Folds in **54b forward-persistence** (contact↔Evryn forward re-attributes to the gatekeeper only at `record_outcome(matched)` — cross-user isolation preserved), **user-creation-at-verdict-time** (Q1, ADR-036 amendment; none for `ignore`), + new dispositions `handled_by_gatekeeper` / `not_a_candidate`. Assembled bundle = ③ code + **Mira's identity pass** (2 rounds; revoiced `triage.md`/`core.md`/`operator.md`/`gatekeeper.md` to the new tool surface — cleared the `record_pass`→`record_verdict` merge-blocker) + a note-ordering runtime fix + a cosmetic `updateUser` type-honesty fix. **Full-load Opus QC-GO** (whole 43-`src`+11-`identity` read, zero blockers) · Gate-A 13/13 (Megan suite on DEV) · DEV live-path green · rebase-verified clean FF. **ZERO prod migration; switch-less (no env flag).** **⭐ This `railway up` also carried the Anthropic key-split** (prod `ANTHROPIC_API_KEY` swapped to a dedicated **Evryn-Production Anthropic workspace** key with its own spend cap — clean per-product billing/blast-radius) **+ the dead unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` cleanup** (runtime reads only the `_PROD`-qualified pair, ADR-048). **Verified live:** clean boot on the prod DB (`EVRYN_ENV=production`) + a **real Anthropic call succeeded on the new key** (a test direct-email triage drafted a reply + recorded a `$0.40` llm_usage row — bad-key risk ruled out). *(Billing-separation — that usage lands in the Evryn-Production workspace, not `default` — pending a Justin console glance, V4.)* Commit `2d14c2b` (= the ③ runtime; the deploy tree also carried 4 inert docs commits at `050f690`, no runtime effect). Deploy via `railway up --detach`, deployment SUCCESS. **#lock source-of-truth doc updates (ARCHITECTURE/BUILD/operator-guide/current-state/sprint) + the CHANGELOG entry are PENDING** (Justin's SCM vet + coordination with AC0/ACf's in-flight changelog restructure). |
| **v0.2.7** | 2026-07-10 | `517014d` (tag `v0.2.7`) | **AC2 cost lane ② — Clustering (Step 58 / ADR-049), ships `CLUSTERING_MODE=off` + `declining.md` goes live.** Decouples ingest from triage: forwards land `queued` (lossless) instead of triaging on arrival; a batch drains twice daily (~10am + ~4:30pm PT, `CLUSTER_TIMES_PT`) → Haiku-screen → Opus back-to-back (cache-warm) → **one package per cluster** to the gatekeeper. Three pieces: **A** the `queued` pile + twice-daily drain + `cluster now` operator command + **restart-catch-up (primary recovery)** + a positive per-batch "cluster ran: …" heartbeat (`CLUSTER_HEALTHCHECK_URL`); **B** the personal-note real-time **note-turn** (a hand-forward WITH a note pulls Evryn in live — urgent/casual/wave-off) + **two structural collision guards** (`resolveNoteTurnSubmitItemId` / `isNoteTurnGuardedUpsert`) closing the Round-17 silent-drop; **C** reply-aware `detectForward` (a `Re:`/in-thread reply no longer mis-routes as a forward). New `queued` status; **1 prod migration** (`add-queued-status.sql` — CHECK-widen only, no data/RLS; pre/post `pg_dump` `backups/queued-status-{pre,post}-2026-07-10.sql`). Merged WITH **Mira PR #11** (note-pathway identity voice, `gatekeeper.md`+`triage.md`). **⭐ This `railway up` also carried `declining.md` LIVE** (Step 55, Mira's contact-facing decline playbook — merged `83bdc2c` but sat merged-not-deployed; takes effect at this restart). Verified: full DEV suite green incl the **live-Opus collision-fix proof** (PB4 — reply → separate placeholder, queued item never carries a reply draft). Deployment `1c5bdfaf`, clean boot (Supabase/Slack/polling; clustering **dormant** — no `CLUSTER_*` env set). **Flip path (Mark-gated, before Mark forwards):** `off`→`manual`→`scheduled` + set `CLUSTER_TIMES_PT`/`CLUSTER_HEALTHCHECK_URL`. Accepted residual: the note-turn `record_pass` third-vector (a real verdict, not a silent drop) closes in Step 57/③. |
| **v0.2.6** | 2026-07-06 | `8e11e9d` (tag `v0.2.6`) | **AC2 cost lane ① — Haiku pre-screen (Step 44), ships OFF** *(ADR-020 amendment; the #1 cost lever, ~$1,800/mo — banks only after a Mark-live shadow trial + Justin's flip)*. Three-verdict cheap `claude-haiku-4-5` screen (`src/triage/haiku-screen.ts`) placed IN FRONT of the Opus forward triage: `ignore` (obvious junk → `record_ignore` terminal-dead, **no stamp**), `pass` (real-person no-fit → `record_pass`, contact stamped), `escalate` (anything else → full Opus, unchanged). Gated by `HAIKU_SCREEN_MODE` (`off`|`shadow`|`active`); **shipped `off`** (env unset → screen never runs → ZERO prod behavior change). Fail-safe: every error/timeout/unparseable → escalate; false-FILTER is the only dangerous case (shadow store `haiku_screen_shadow` derives `false_filter`/`wrongly_junked` + fresh-false-filter `notifyDev`). **1 prod migration** (`add-haiku-screen-shadow-table.sql` — RLS on, service_role-only; pre/post `pg_dump` `backups/full-public-2026-07-06-{pre,post}-haiku.sql`). Merge `8e11e9d` (branch `dc/cost-haiku-r2`, QC-GO). Clean boot (Supabase/Slack/polling). **Flip path (Mark-gated):** `off`→`shadow` (~1wk real traffic, ~0 false-filter) → Justin sign-off → `active`. |
| **v0.2.5** | 2026-07-02 | `0c8c74b` (tag `v0.2.5`) | **AC4 Security Bundle 2 (constrained `supabase_read`) + Mira name-lookup beats** *(ADR-047 ②, the read-side inner-door — S1 v0.2 interim)*. Constrained `supabase_read` (`src/security/supabase-read-guard.ts`): table allow-list · no PostgREST resource-embed (deny-on-`(`) · **per-table scope-filters** (Rule 7, incl. the LOAD-BEARING `emailmgr_items.original_from_user_id`) · **`cross_user_notes` column-deny/rewrite** (Rule 8 — firewall-bypass fix) · row-cap 50 · fail-closed + log + `notifyDev`. New **`find_user_by_name`** typed tool (Rule 9 / Step-35(a) pulled forward) so name-lookups don't need the generic read. **Mira beats** (`feedback-guidance.md` + `operator.md` verify-and-lock/create-from-zero) repoint name-lookups to it. **Elevated per Justin's 1b ruling.** **Carried AC3's env-fix (ADR-048)** → the Railway `EVRYN_ENV=production` + qualified `SUPABASE_*_PROD` swap was a confirmed hard precondition (verified pre-deploy). **No migration.** Adversarial QC-GO + final converged QC after the re-rebase onto `main`; clean boot (Supabase-prod / Slack / polling). |
| **v0.2.4** | 2026-07-01 | `bca656b` (tag `v0.2.4`) | **AC4 Security Bundle 1 + Mira Step-41a** *(the security-bulkhead lane, ADR-047)* — (A) WebFetch/WebSearch **egress deny-hook** (`src/security/web-guard.ts`: SSRF/internal incl. `169.254.169.254` + IPv4-mapped/NAT64, non-http(s), exfil-shape; deny-by-shape + log + notifyDev; fail-closed), (B) `sendEmail` **provenance gate** (`client.ts`: required sanctioned provenance or refuse + notifyEmergency — the outbound-egress door), (C) untrusted-content **fence** (`src/security/fence.ts` in `buildForwardedEmailPrompt`+`processDirect`), + **Mira's Step-41a beats** (core/triage/onboarding — fetch-is-outbound / "theirs-to-protect" judgment). **Carried AC1's Step-23 pong fix** (was merged-not-deployed). **No migration.** Final converged adversarial QC = GO; clean boot (Supabase/Slack/polling; Step-21 cold-start handled wiped prod). |
| **v0.2.3** | 2026-07-01 | `83fa367` | **AC1 cleanup batch** — mechanical hardening (system-actor constant centralization, always-merge metadata, reference-only history wrapper across the 4 draft sites, per-tool ROLE guards on note/scope tools, M1 fast-follow tests, comment/rename fixes) **+ Step-72 `search_path` pin** on the 3 SECURITY DEFINER RPCs (prod migration, pre/post `pg_dump`). **LIVE** — deployed 2026-06-30 16:48 PT (Railway `d4563ce4`, latest SUCCESS); clean boot (Supabase/Slack/polling), proactive cron firing. Boot-health confirmed 2026-07-01. |
| **v0.2.2** | 2026-06-29 | `d198ef1` | **v0.2 Hardening** *(formerly "round-2")* — M1 silent-death layer, Step-61 gatekeeper-address resolution (ADR-046), lean Reflection (ADR-043), Lane-B operator/approval hardening. 4 prod migrations. |
| **v0.2.1** | 2026-06-16 | `49fb6ff` | **v0.2 Go-Live bundle** — quiet-hours redesign (ADR-040), morning sweep, M1 emergency Stage-1 (plumbing), cost pass-stamp (`record_pass`), Phase-6 Mira identity batch. First v0.2 production bundle. |
| *(pre-ledger)* | 2026-06-04 → 06-10 | `1610f3b` … `23f9858` | Earlier v0.2 dev-iteration deploys toward go-live (Oregon/us-west-2 cutover, context-architecture fixes, email/approval-flow polish, per-event cost-capture). Not individually renumbered — grouped here for completeness. |

**Upcoming (current wave — each deploys on Justin's go, then gets its number):** AC2 cost lane is now **ALL SHIPPED** — **① Haiku = v0.2.6 (OFF)** · **② Clustering = v0.2.7 (OFF; flip `off`→`manual`→`scheduled` before Mark forwards)** · **③ Runtime Bookkeeping = v0.2.8 (LIVE, above)**. **SPRINT 91** (phantom-lead / halt-resume Mark-blocker) shipped **v0.2.9** (above, 2026-07-23). Next prod deploys: the **WebFetch-timeout fast-follow** (built `dc/webfetch-timeout`, needs its live-path QC + Justin's per-turn-watchdog design call) + any **Gate-B** fixes. Numbers assigned **at deploy**, never reserved ahead.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
