# Current State — Evryn (Cross-Project)

> **Truncation check:** The last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

**How to use this file:** Snapshot of current project status across all Evryn repos. NOT a log — each update replaces the previous state. Updated during #lock by whoever is active (AC or Soren). Read this to orient on what's happening now.

**Do not edit this file without Justin's approval.** Propose changes; don't make them directly.

**Keep this file under 50 lines.** If a project needs more than 2-3 lines, the detail belongs in that repo's own state file or build doc — not here.

*Last updated: 2026-06-03 evening (AC0 #lock — Mark wiped to zero; create-from-zero test; go-live reconciled; DB move to Evryn Product (West Coast) in progress)*
*Updated: 2026-06-04 (AC1 — dev/staging DB created + seeded; Supabase Pro; pg_dump backup model. Decisions: ADR-037)*
*Updated: 2026-06-04 evening (AC0 #2 — Oregon/us-west-2 cutover DONE; create-from-zero test Phase 2 PASSED + paused; live-test findings routed to Mira/Soren; M1 silent-death = the one go-live blocker)*
*Updated: 2026-06-05 (AC2 #lock — pre-go-live audit delivered + ARCH/BUILD/SPRINT reconciled + sprint doc collapsed 597→290, all pushed; EVR-114 Soren meta-architecting + v0.2 maintenance-plan stub)*
*Updated: 2026-06-06 (AC2 #lock — subagent loading hardened: Startup Context Cascade + #cascade-override standardized across DC/QC + protocol; protocol simplified; AskUserQuestion banned. Orchestration tooling, not build work. 4 UNVERIFIED backend silent-failure candidates routed to AC1 — see docs/sessions/historical/2026.06/2026-06-06-ac2-subagent-loading.md)*
*Updated: 2026-06-08 (AC3 #lock — evryn-backend default branch renamed master→main via GitHub native rename; default moved + canonical tree synced + Railway healthy. Doc master-ref sweep = AC0's, pending. Repo-ops only, no build/arch change.)*
*Updated: 2026-06-08 (AC1 #lock — context-arch fix batch built + real-DC/real-QC-verified + deployed (`75fba9e`): runtime-owned conversation history with real name+email labels, history-count visible at submit_draft, double-send guard, silent-failure alerts. Mira PR #9 (identity tighten) merged; core.md history-claim scoped to email. 2 minor non-blockers → sprint hardening list.)*
*Updated: 2026-06-08 (AC0 #3 #lock — integration test Phases 3-5 PASSED: onboarding + notes + triage validation (effectively 18/18 on reasoning, raw 12/18 — synthetic fixtures are naive vs a now-web-searching Evryn; nuance in `evryn-backend/tests/2026-06-08-phase5-results.md`). Phase 6 live-fire is the last phase. Mira B2c/edge triage fix pushed (needs deploy). Fixture data cleaned from prod.)*
*Updated: 2026-06-09 (AC1 #lock — email-format + single-route-approval + dedup + SDK-cast fixes BUILT (DC) + QC-verified + DEPLOYED (`83fdd5e`, Railway `7aa4ce10`, healthy); Mira's 2 identity changes (`30d85f0` triage beat + Closing-the-Loop) rode the deploy; CLAUDE.md two-mode cascade-load rule; QC patterns promoted; lock-protocol step 10/17 sharpened. M1 + Phase 6 remain the go-live gates.)*
*Updated: 2026-06-09 (AC0 #4 #lock — Phase 6 live-fire STARTED: run sheet written (8 copy-paste scenarios — incl. Justin-as-42-Pictures edge loop, Amy-Lillard real-friend gold, claims-to-know-you). **S3a PASSED** (clean edge call + proportional handling; lifecycle correct) on the live `83fdd5e`. S3b–S6 + wipe + real-Mark email remain. Email-format + single-route-approval fixes routed to AC1 (shipped). Phase 5 results renamed date-first.)*
*Updated: 2026-06-09 evening (AC0 #5 #lock — Phase 6 S3a + full S3b multi-party loop PASSED on `83fdd5e`; S1/Nadia gold-drafted + queued for morning approval; Phase-6 findings batch (BUILD hardening item 9) + Mira dispatch routed to BUILD/SPRINT; **M1-sequencing conflation corrected** (M1 gates Mark forwarding, not the intro email); quiet-hours redesign queued for DC; new lighter handoff `docs/sessions/2026-06-09-ac0-handoff.md` written, 6/4 handoff proposed for retirement.)*
*Updated: 2026-06-10 (AC02 — per-event cost-capture BUILT (DC) + AC/QC GO + DEPLOYED (`23f9858`, prod migration applied); `llm_usage` table live, capturing every Evryn run's spend. Multi-inbox identity decided (ADR-039, build parked). Decisions: ADR-038/039. Ran parallel to AC0's Phase-6 live-fire; deploy coordinated with AC0.)*
*Updated: 2026-06-11 (AC0 #lock — **Phase 6 live-fire COMPLETE** (all scenarios + step-11 close run on `83fdd5e`+cost-capture). Findings 13-21 + Mira items 10-22 captured in the working docs — headline: **pass-notes → a deterministic system stamp on the contact only** (the ~$8k/mo cost lever; `llm_usage` ~$1.36/query), plus 6a known-user empty→deterministic bounce, 6c language-as-substance, review@/internal-address phantom-user fix. Cost-capture confirmed live. **DB wipe + Justin's own-eyes clean-verify + inbox clearing DONE, and real-Mark "we're ready" email SENT — all 2026-06-11; next: M1 + the v0.2-hardening campaign (cost-fix).**)*
*Updated: 2026-06-11 (AC02 — quiet-hours redesign BUILT+QC-GO (ADR-040, branch `dc/quiet-hours-redesign`; deploys with the go-live bundle) · **cost analysis v2 delivered** (`evryn-team-workspace/shared/projects/product/research/2026.06.11 evryn-cost-analysis.md`): ~$11k/mo today → **~$2k(warm)–$6.5k(cold) v0.2** → ~$200/mo irreducible; **cache temperature is the dial** (measure week one), **Reflection pulled to v0.2** (the profile-bloat re-read tax). SPRINT restructured into 3 gates. **Next: a fresh AC0 rebuilds the sprint as `SPRINT-V0.2-HARDENING` (one status-driven Steps list, killing intra-doc drift) + runs the v0.2-hardening campaign** (M1 + cost-fix). AC0 brief: `docs/sessions/2026-06-11-ac0-handoff.md`.)*
*Updated: 2026-06-13 (AC1 #lock — **sprint-doc restructure** to the one-home-per-item model: `SPRINT-V0.2-HARDENING` is the single live v0.2 sprint (one status-driven Steps list; no Current-State section, no v0.3 list); `SPRINT-MARK-LIVE` gutted to pure history (culminates in the sent Mark email); new `SPRINT-V0.3-CANDIDATES` holds granular v0.3; BUILD frozen-backlog deleted → pointers; ARCH Proactive-Behavior staleness fixed (basic daily cron live, intelligent-timing = v0.3). **Gate framing corrected: M1 + the cost pass-stamp are the two Gate-B gates** — not M1 alone. Docs-only; the M1 / cost / quiet-hours / Mira builds are in flight on their own worktrees.)*
*Updated: 2026-06-13 (AC0 #lock — **v0.2-hardening builds DONE + pushed, none merged, NOT deployed (safe weekend set-down)**: M1 emergency channel, cost pass-stamp (`record_pass`, 3 commits), morning-digest+C2 — all QC-GO on branches; Mira identity batch committed+pushed (4 rounds, not yet AC-reviewed/integration-QC'd). **Cost-levers thrust queued** (`evryn-team-workspace/shared/projects/product/research/2026.06.12 cost-levers-consolidated.md` — §4 runtime-bookkeeping keystone, §5 Haiku screen-out/escalate, §6b daily-clustering DECIDED, $X up-to-the-minute premium TBD). 3 new findings: forward content not mirrored to `messages` (=backlog-critical, the load-bearing one), triage.md prior-history→messages redirect, contact name-capture. **identity-as-runtime** concept agreed. Dashboard AC2 brief = the M1 liveness feed + cluster heartbeat. **Handoff: `docs/sessions/2026-06-13-ac0-handoff.md`** — next AC0 closes the bundle (assemble → integration-QC release-gate → Justin's deploy go) then runs the cost thrust. AC1 closed.)*
*Updated: 2026-06-13 (AC1 #lock — **AC writing protocol carved out**: doc-writing *craft* extracted from `_evryn-meta/CLAUDE.md` into a new `docs/protocols/ac-writing-protocol.md` (CLAUDE.md keeps doc-architecture/routing + a "Writing discipline" hard-pointer; the AC + team writing protocols deliberately stay separate, with a #sweep cross-ref on each; "read the writing protocol before #lock" wired into `lock-protocol.md`). **Subagent-loading model verified by live experiment**: only the *spawner's* CLAUDE.md auto-loads; strong "read and faithfully follow" naming reliably loads named files; a hard-pointer in the auto-loaded CLAUDE.md is followed even without a brief repeat — **team `CLAUDE.md` line 223 + `AGENT_PATTERNS.md` updated** to the verified model. The 3 deferred AC1 finish-edits also landed. Docs/process only — no build/arch/runtime change.)*
*Updated: 2026-06-15 (AC0 — **go-live bundle ASSEMBLED + double-vetted (QC + DC), HELD uncommitted, NOT deployed** (compaction set-down). Branch `bundle/go-live-v0.2` (worktree `evryn-backend-integration`, off `main` `fdd57f7`): the 5 go-live branches merged (quiet-hours/`morning-sweep-c2` · `m1-emergency-channel` · `cost-pass-stamp` · `mira-phase6-identity-batch`) + held edits — the **pass-stamp** (first-name + gatekeeper-UUID + `[shareable_with_user]` + self-doc scope) and Mira's 2 identity edits (tightened `record_pass` Pass-line + a determination-sharing guardrail in `core.md`). **OPEN: guardrail placement** — Mira proposed pull-to-v0.3, **Justin disagrees with her take**; next AC0 re-decides + builds the unified Nathan brief. **`identity-as-runtime`** codified into AC `CLAUDE.md` (QC/DC manuals already covered it → skipped). SF1/SF2 + the 3 forward-persistence findings → AC3 (`SPRINT` Steps 54d/54). **Everything HELD uncommitted for Justin's SCM review.** Next AC0 finishes the deploy. Full nuance: `docs/sessions/2026-06-15-ac0-handoff.md`.)*
*Updated: 2026-06-16 (AC0 — **go-live bundle CLOSED to the deploy gate**: bundle committed `9364bfe` (QC-GO, deploy-ready) + CLAUDE.md/Mira-brief/sprint commits — **all LOCAL/UNPUSHED**; determination-sharing guardrail **PULLED to round-2** (fires near-term v0.2, not v0.3); **M1 emergency channel set up, but making it pierce Justin's Android DND is an unsolved ROUND-2 item — NOT deploy-blocking** (key finding: Slack-VIP is Slack-side and does NOT pierce Android OS-DND; only "allow all Slack through DND" pierced, which isn't emergency-scoped); Railway token regenerated (vars not yet set). **Deploy NOT done — a fresh AC0 is being re-spun for it.** Handoff: `docs/sessions/2026.06.16-ac0-ac0-handoff.md`.)*
*Updated: 2026-06-16 evening (AC0 — **v0.2 GO-LIVE BUNDLE DEPLOYED + healthy**: merged → `main` (pushed `49fb6ff`), `railway up` SUCCESS on Oregon/us-west-2; clean boot (Supabase/Slack/polling), **both alerts confirmed live by Justin** (#evryn-approvals "awake" + #emergency-alerts smoke). Live: quiet-hours redesign (ADR-040) · morning sweep · **M1 emergency Stage 1** (plumbing only — detectors round-2) · **cost pass-stamp** (`record_pass`) · Phase-6 Mira identity batch. Post-deploy DONE: smoke-flag off · 6 worktrees/branches reaped · **`notify_queue` table DROPPED** dev+prod (pre-drop `pg_dump` in `backups/`) · `evryn-backend`+`_evryn-meta` pushed. **Round-2 (other machine):** M1 Stage 2 detectors + Android-DND pierce · determination-guardrail re-arrange (Mira brief) · cache-TTL flip · ARCH/schema/operator-guide `notify_queue`+M1 doc absorptions · AC3 (forward-persistence) + AC2 (dashboard).)*
*Updated: 2026-06-16 (AC2 #lock — **Evryn Product vitals/liveness dashboard tab SHIPPED + LIVE** (evryn-dashboard.vercel.app → "Evryn Product" tab, now the default): liveness banner (Railway `/health` + activity timestamps + 24h error count), today's activity + spend from the product DB, **aggregates-only / PII-firewalled**, fail-closed auth. DC-built + QC-GO + AC-reviewed + deployed. BUILD v0.3-hardening **item 5** added (dashboard `service_role` key). **Ownership (Justin's call): Soren = keep it operational; Emma = track spend + work with Soren to keep API costs optimized.** Open/deferred (~2wk, Justin): the Agents-tab `/api/data` 500 = the stale "Needs Attention" Agents `SUPABASE_SERVICE_ROLE_KEY` (April-2026 Vercel incident) — Agents DB, no end-user PII; a GitHub-token revoke is recommended sooner. Dashboard side-thrust; the main go-live bundle was untouched.)*
*Updated: 2026-06-18 (AC0 #lock — ⚠️ the 2026-06-17 5-AC v0.2-hardening wave (AC1-5) ran on **LOBOTOMIZED subagents** all day (DC/QC/Soren spun with no startup cascade; AC↔DC↔QC coordination never fired) → **all wave code is junk, NO merge**; caught pre-merge. **AC6 forensics: no git corruption** (the desktop was just un-pulled since 6/16, misread as up-to-date); QC lobotomized because the **laptop's `evryn-quality` is on a stale `master`** (good manual = `main`); **DC's manual was fine and it STILL skipped the cascade → precise-load every subagent on every spin; "testing shows it works" is NOT enough.** Plus a protocol gap: `ac-orchestration-protocol.md` has no provision for loading *team* subagents (→ a lobotomized Soren vet). **Recovery: a protocol-fix AC runs FIRST** (verify every agent — AC/DC/QC/OC/ops-team — can *receive* its load + add a team-subagent loading provision; bake in precise-load + the grep-the-cascade technique) — **MUST precede any re-spin or it re-lobotomizes** — then harvest each AC's pre-spin thinking + Q&A and re-spin AC1-5 clean. **STANDS (AC0 doc-work, no subagents):** the sprint cost-levers integration (Phase 2b / Steps 57-60, Step 44 → Haiku pre-screen, Step 61) + the day-1 orchestration briefs. Full recovery plan + ADR-owed (loading-discipline fix): `docs/sessions/2026.06.18-ac0-ac0-handoff.md`.)*
*Updated: 2026-06-19 (ACP — **subagent loading-discipline HARDENED + PROVEN, committed.** `ac-orchestration-protocol.md` + DC/QC/OC/team CLAUDE.mds: receipts-moved-last + `<pre_task>` checkpoint + two-part line-range receipts + line-spans-always + the additive-only standing-cascade rule + the "identity files are runtime" note + the "you don't know a file from its name" beat. Provable FULL-load demonstrated across DC, QC, OC, and Soren (team split-file load); a spinner-side truncation regression (an AC dropping QC's identity files by judgment) was caught and closed. **Loading is now reliable *when AC checks the two-part receipts and re-runs on any partial* — not deterministic** (it's still prompting). **GATE CLEARS for AC0**: harvest the AC1-5 salvage + re-spin clean per the 2026-06-18 recovery plan; AC0 just needs the updated protocol + this entry. **Durable fix = an SDK force-load harness** (per-call `query({systemPrompt})` force-load — the mechanism Evryn's runtime already uses) — queued as the real deterministic answer (formal ADR deferred until it's scoped). Still open: the OC session-start sync-ritual script (flagged in `docs/repo-inventory.md` + ADR-042). No build/schema/identity/runtime change this session.)*
*Updated: 2026-06-19 evening (AC0 — **RECOVERY EXECUTED off ACP's cleared gate; round-2 staged for Monday.** `evryn-quality` fork resolved + `evryn-ops` renamed → **every repo now on `main`**; Mira's identity-writing-bible merged (`7eb6d73`). **Round-2 brief** `docs/sessions/2026.06.19-ac0-round2-lanes-brief.md` — **describe-then-PAUSE** spine (reverses round-1 post-and-proceed) + research-not-gospel salvage, all 5 lanes (AC1a-5a); **4 r2 worktrees** staged off `main` (old junk worktrees on ice); round-1 corrupt lane-handoffs quarantined → `docs/working/2026.06.17-corrupted/`, spent forensics/ACP docs + round-1 brief → `sessions/historical/`. **A fresh AC0 Monday audits this staging first, then spins AC1a-5a.** Handoff: `docs/sessions/2026.06.19-ac0-ac0-handoff.md`.)*
*Updated: 2026-06-22 (AC0 — **foundation audit of the shipped v0.2 runtime COMPLETE + validated** (3 workflow passes: meta → deep-dive → BUILD-loaded validation): foundation SOUND — 115 findings, 0 refuted / 99 confirmed / 16 reframed. **ONE true v0.2 go-live blocker = M1** (already Lane M1). Marquee Slack-operator-auth finding recalibrated to real-but-not-a-v0.2-blocker + untracked → SPRINT Step 62; `service_role` cluster = documented-v0.2-accepted (BUILD S1). New **SPRINT Steps 62–67** (audit-derived). **Loading-discipline crisis + durable fix: `ac-orchestration-protocol.md` HARD RULE — AC pastes verbatim subagent-brief blocks, no one-word deviation w/o Justin's OK, + the Workflow load-gate variant.** Also codified: describe-then-pause + ping-before-workflow. Round-2 lanes ready to spin off clean `main` with audit findings folded in. Go-forward: `docs/sessions/2026.06.19-ac0-ac0-handoff.md` (2026-06-22 addendum). Findings: `docs/working/2026.06.22-ac0-foundation-audit-findings.md`.)*
*Updated: 2026-06-23 (AC1a #lock — **Lane A round-2 built to full QC-GO on `r2/lane-a-ingest-resilience`; NOT merged/deployed**: ingest/resilience Batch 1 (12 fixes) + Step 61 gatekeeper-address-resolution (new `gatekeeper_inbound_addresses` table + `users.outbound_address` + escalate-unregistered-forward) + Step 34 simplify-revert + Mira identity edits + bounce copy — 6 commits, all QC-GO. Both migrations applied+verified on **DEV** (prod = AC0 convergence). Handoff: `docs/working/2026.06.23-ac1a-ac0-handoff.md`. **For AC0:** converge/merge + reconcile main-drift + apply prod migrations + `.roles` prod-verify + `#dev-alerts`-on-identity-merge + mark sprint steps (13,16,17,19,20,22,29,32,34,40,52,61,66) DONE; **Justin wants Evryn to *input* gatekeeper addresses, not hand-register — investigate (may already exist)/build.** Push-to-origin decision pending Justin.)*
*Updated: 2026-06-23 (AC3a #lock — **Lane C round-2 (cost) built to QC-GO on `r2/lane-c-cost`; NOT merged/deployed; live-inert until Step-66 enrollment**: **lean Reflection** (Step 10 — periodic per-user `pending_notes`→`story` consolidation; new `story_versions` archive + atomic race-safe `consolidate_profile` RPC; `reflection.md` loaded only in the consolidation pathway, $0 on normal queries) + cache byte-identity guard (11b). QC-GO (full-load review + live DEV checks) + AC-verified 4 ways incl. a **live real-Opus consolidation** against Mira's voiced `reflection.md` (in-voice, through-line, `[binding]` carried to the story foot, provenance held). Mira's identity pass (reflection.md + core.md note-voice + binding-reconciliation + 3 canaries) committed on the branch + **AC identity-file-reviewed 7/7**. Migration applied+verified on **DEV** (prod = AC0 convergence). **Cache:** the Agent SDK exposes no breakpoints (#89) → the lever is placement (ADR-044, measurement-gated; week-one RUN = Step 69, leave notes IN). New **ADR-043/044**; **SPRINT 69–72**; ARCH/BUILD `story`-written-in-v0.2 staleness fixed (committed to `main`, NOT the lane — flagged for AC0). Handoff: `docs/working/2026.06.23-ac3a-ac0-lane-c-r2-handoff.md`. **For AC0:** converge + wire Reflection → Lane A's `findCareEligibleUsers()` (Step 66) + apply prod migration + `#dev-alerts` on identity merge + mark Steps 10/11b DONE. **Gate: Mira voice-pass DONE → Step-66 enablement OK at convergence.** Push-to-origin pending Justin.)*

*Updated: 2026-06-23 (AC5a #lock — **Lane M1 (silent-death) round-2 built to QC-GO on `r2/m1-silent-death`; NOT merged/deployed** — the ONE true v0.2 go-live blocker. Circuit-breaker at `runEvrynQuery` (sole spend door) + **clean lossless halt** (trip → park `processing→new` → hold Gmail cursor → dedup-resume the parked row on restart: one emergency alert, no error-storm, no dropped mail) + loop-signature/velocity detectors (operator-pathway excluded from loop-sig; **conservative 600/hr** ceiling, ratchet post-Mark on real data) + **persistent Anthropic-billing folds into the same clean halt** + Tier-B alert-only (in-loop Healthchecks heartbeat — `/health`-always-200 confirmed, audit 67b; Anthropic-vs-Gmail hard-auth; polling-dead wall-clock escalation) + watch-the-watchman daily affirmation (`notifyEmergencyWithStatus`) + send-bypass tier-5 assertion + cron `PROACTIVE_CHECK_HOUR_PT` 8→10. All NEW `src/safety/*` + minimal DI hooks (no import cycle). **Real-Soren re-vet** (round-1 was lobotomized; spine SOUND, receipts verified) → **real-DC build** → **real-QC heightened review = GO, no blockers**; typecheck + 2 new suites + full pure-logic regression green. Operator-guide M1 section + DND-respecting model done; ADR-041 amended; build spec `docs/working/2026.06.22-ac5a-m1-build-spec.md`; handoff `docs/working/2026.06.23-ac5a-ac0-lane-m1-r2-handoff.md`. **For AC0 at convergence:** merge into the go-live bundle; **two Lane-A interlocks — Step 19 (empty-Message-ID→`email.id` fallback, AC1a DONE) AND Step 21 (durable cursor — NOT in AC1a's DONE list; verify, else M1 restart-resume falls back to latest-10)**; reconcile the `poll.ts`/`classify.ts` cross-lane regions; mark Step 4 (+67b) DONE + add sub-steps (NB-3 fast-follow tests, NB-4 send-bypass re-target, Nit-1 rename, post-Mark velocity calibration → Step 43); (QC's dedup-fails-open pattern recurrence already trimmed+pushed to `evryn-quality/CLAUDE.md` by QC+ACP — no action); ARCH M1-absorption (M1 not shipped → deferred to convergence). Justin pre-live setup: Anthropic monthly ceiling + 2 Healthchecks checks. No schema/migration. Push-to-origin pending Justin.)*

*Updated: 2026-06-24 (ACc #lock — **round-2 build arm: Step 21 durable cursor + register tool, both QC-GO on branches off `r2/lane-a-ingest-resilience`; NOT merged/deployed**. **Step 21** (`r2/lane-a-step21-durable-cursor`, `a6a96c3`+`35506f4`) — durable email cursor / cold-start catch-up: a data-derived bookmark (newest `emailmgr_items.created_at` − 1h margin) → paginated Gmail date-search fallback, err-old invariant, existing `external_id` dedup absorbs overlap, `client.ts` stays DB-free. Closes convergence-plan **GAP 1**; satisfies M1's lossless-restart interlock (Step 19+21 both on the base). **Register tool** (`r2/lane-a-register-tool`, `1886515`+`e5d7c16`+`3d948ae`) — `register_gatekeeper_address` MCP tool: **Evryn self-registers** a gatekeeper's inbound lanes + gold destination via TYPED `db/users.ts` helpers (validate-first: existence + role-gate system/admin + all-or-nothing conflict policy), closing **GAP 2** (Justin's "Evryn inputs addresses, not hand-register" ask) + Mira's gatekeeper-onboarding beat (**AC identity-file-reviewed 7/7**, committed by Justin `3d948ae`). **Both QC-GO ×2** (build + fast-follow re-verify each). **No new migrations.** QC patterns promoted+pushed to `evryn-quality` (`7430c10`) + its identity-file-review broken link fixed. **For AC0:** converge both onto the bundle (seams: `classify.ts` MCP-region is additive vs B/M1; `poll.ts` one-line vs A/C/M1); mark Step 21 DONE + add a register-tool step + a catch-up historyId-ordering follow-up; `#dev-alerts`-on-identity-merge (register beat); operator-guide Step-48 runbook update; ADR candidate (Step 61 + register). Handoff: `docs/working/2026.06.24-acc-ac0-step21-register-handoff.md`. Both gaps now CLOSED. Push pending Justin/AC0.)*
*Updated: 2026-06-24 (AC2a #lock — **Lane B (operator/approval/Slack) round-2 built to QC-GO on `r2/lane-b-operator-approval`; NOT merged/deployed**: **Build A** (7 hygiene fixes — operator-channel auth gate [Step 62: `SLACK_OPERATOR_USER_ID` checked on the live handler + the catch-up replay loop, fail-open + loud warn], audit-recipient mislabel, collision/notes-threading + dead `supabase_upsert` string, real-parser test, subject-label drop, draft idempotency) + **Build B** (route raw `supabase_upsert` `emailmgr_items` status writes through the lifecycle-append path [18b], fabricated-UUID existence guards across the 5 note/scope tools [63], runtime-owned email threading [14], operator *conversational* re-draft context [15], `record_pass` downgrade-reasoning [68 runtime half], operator-gated `redact_user_from_message` tool [37]) + the **Step-64 v0.3-safe revision** (recipient-lookup miss → skip the audit row + alert, NEVER file under the gatekeeper — Justin's cross-user-bleed catch) + the Step-14 **subject-override removal** (Evryn titles her own responses; threading + quoted-original stay). **Mira identity beats committed on the branch** (`a9881a6`: triage.md downgrade-why + gatekeeper.md retrieval-before-answer + operator.md redact-tool repoint; AC- + ganged-QC-reviewed, identity-file-review reviewer satisfied). **Both code builds QC-GO + AC-reviewed; the tear-out + Mira edits QC-GO (one ganged trip).** **`SLACK_OPERATOR_USER_ID` set in Railway prod** (`--skip-deploys`, no restart) → gate goes live closed at deploy. **For AC0 at convergence:** merge into the go-live bundle; wire the two Lane-A seams (gold-destination `resolveGoldDestination` → `draft_to`; Step-32 transport failure-test against Lane A's `client.ts`); mark Lane B sprint steps DONE; `#dev-alerts` on the Mira-beat identity merge; small ARCH note (triage_reasoning may carry a downgrade-why, Step 68). **Flagged v0.3:** proper Step-64 fix = recipient-binding at submit (tracked candidate). Beat-3 (67a revision-history label) = runtime-only one-liner, DC+QC in flight at lock time. Handoff: `docs/working/2026.06.24-ac2a-ac0-lane-b-r2-handoff.md`; Mira brief: `docs/working/2026.06.24-ac2a-mira-lane-b-identity-beats.md`. Push pending Justin/AC0.)*
*Updated: 2026-06-25 (AC4a #lock — **STAGING lane (decoupled) round-2 deliverables DONE; actual standup gated on Justin's account actions before Mark**: **ADR-045** — staging runtime gets its OWN dedicated 4th Supabase project (~$10/mo, reset-able/disposable), NOT the dev DB (real-Soren call: pointing the runtime at dev breaks ARCH "dev=admin-tooling-only" + makes dev diverge from prod-shape → silently breaks migration rehearsal; extends ADR-037; ARCH 3→4-projects owed once provisioned). **Send-recipient allowlist BUILT (DC)+QC-GO+AC-GO** (`src/email/client.ts` `7705675` on `r2/lane-a-send-allowlist`, stacked on Step 21): `STAGING_RECIPIENT_ALLOWLIST` presence = staging mode → `sendEmail` only emails allowlisted test addresses, fail-closed/all-or-nothing across `to`+`bcc`, PROD a true no-op when unset; rides AC0 convergence; QC doc-notes: `review@evryn.ai` MUST be allowlisted on staging + "never set on prod." **Dashboard real-green-lights (build-ready spec, OC ops-reviewed; NOT built):** replace hollow `/health` (always-200, F067) with a 503-when-wedged endpoint (arms the inert Railway/M1 watchdog) + 5 per-subsystem lights (DB/Slack-socket/last-successful-poll/M1-heartbeat/24h-errors; the poll light keys on *last successful poll cycle* not last-DB-write → no quiet-hours false-red) + a staging dashboard tab on the 4th DB. **OC:** get-better loop + the two stale-fact fixes already clean on `main` (NOT re-done); added the `/health`-hollow pattern to `evryn-ops/CLAUDE.md` (`5df0f8d`); spun a properly-loaded OC for the staging ops review (standup sequence + 3 runbooks + 4 gaps). **Flagged for AC0/protocol-owner:** OC is spinnable but NOT a standing pipeline step (loop names DC+QC only; no codified OC trigger) — proposed codifying OC triggers (handoff §10). Handoff: `docs/working/2026.06.24-ac4a-ac0-lane-staging-r2-handoff.md`. Soren memory appended (Justin vetted). Nothing merged/deployed.)*

*Updated: 2026-06-25 (AC0 — **round-2 CONVERGENCE: all 5 lanes merged into `r2/convergence-bundle` (tip `d149535`, pushed); tsc-clean + pure-logic suite green; NOT merged-to-main, NOT deployed.** Merge order C→A/Step21/allowlist→register→M1→B; both cross-lane seams wired (Step 66 Reflection→`findCareEligibleUsers`; Step 14/61 gold-destination→`goldDestination`, ADR-046); the delicate M1×Step-61 `process.ts` merge (dedup-resume × resolution-reorder) resolved + verified (`test-breaker-resume` + `test-step61-process` pass). ADR-046 written; persistence checklist `docs/working/2026.06.25-ac0-ac0-persistence-checklist.md` (every loose item→home, nothing dropped). **Remaining (next AC0): integration-QC the bundle → Step-32 test → persist (SPRINT v0.2 DONE-marks + NEW steps + v0.3 + ARCH/BUILD absorptions) → [GATED] merge-to-main + prod migrations (Lane A Steps 13/61, Lane C Step 10) + deploy WITH Justin → cleanup (reap worktrees + tidy docs).** Justin pre-live setup in flight (Anthropic monthly ceiling + 2 Healthchecks + `PROACTIVE_CHECK_HOUR_PT=10`). Runbook: `docs/working/2026.06.24-ac0-ac0-convergence-plan.md` (CONVERGENCE EXECUTION STATE §). 6 live-DB tests "fail" only on prod state (migrations not yet applied / prod wiped) — expected, not merge bugs.)*

*Updated: 2026-06-25 evening (AC0 — **round-2 persistence pass DONE (persist-first, pre-QC) + DC code trip merged + a loading-discipline protocol unlock; NOT merged-to-main, NOT deployed.** Persistence (committed on `r2/convergence-bundle` + `_evryn-meta`): SPRINT v0.2 DONE-marks across all lanes + **new Steps 73–85** + V0.3 candidates + a consolidated **ARCH "v0.2 Round-2 Hardening — Architecture Absorptions"** section (M1 / Step-61 / Reflection / Lane-B) + BUILD status + operator-guide rows + **DND & redeploy framing corrections** (Justin caught both as persistently-mischaracterized — the emergency channel does NOT pierce Android OS-DND; a merge does NOT auto-redeploy). **Step-52 numbering collision resolved** (GA keeps 52; the strict-forward-separator fix = new **Step 85**). **DC round-2 trip merged** (bundle `d149535`→`d198ef1`): Step-32 `executeApproval` transport-DI + tests, Step-84 gold-destination test, DND/67b/Step-85 comment fixes — tsc-clean; the Step-32 integration test got a **hardened DEV-only guard** (refuses prod). **Loading-discipline UNLOCK (Justin's experiment):** putting the load-failure callout in the subagent `<task>` block (not just `<mandatory_load>`) made a DC that had partial-loaded BUILD re-load it IN FULL on re-spin → **codified as a standing `<task>` callout in `ac-orchestration-protocol.md`** (committed `2b4c545`). **NEXT: integration-QC the bundle (NOT yet run) → [GATED, next session] merge-to-main + prod migrations + deploy WITH Justin → cleanup.** Worktrees (14) + archival pg_dump (9d old; prod schema unchanged) → post-deploy cleanup. Handoff: `docs/working/2026.06.25-ac0-ac0-handoff.md`.)*

*Updated: 2026-06-29 (AC0 — **ROUND-2 v0.2-HARDENING DEPLOYED + LIVE.** Integration-QC GO (full-load, clean, zero blockers) → bundle `r2/convergence-bundle` (`d198ef1`) merged to `main` (fast-forward) + pushed → **4 prod migrations applied + verified** (`gatekeeper_inbound_addresses`, `story_versions` + `consolidate_profile` RPC, `emailmgr_items` `updated_at` trigger, profile-jsonb comment; pre/post `pg_dump` in `evryn-backend/backups/`) → **`railway up` SUCCESS, clean boot** (Supabase/Slack/polling; Step-21 cold-start handled the wiped prod). **M1 silent-death layer, Step-61 gatekeeper-address resolution (ADR-046), lean Reflection (ADR-043), Lane-B operator/approval hardening — ALL LIVE.** Healthchecks.io heartbeat (~15min grace) + daily-affirmation (~26h) wired + live; Railway env `HEALTHCHECK_URL`/`_AFFIRM_URL`/`PROACTIVE_CHECK_HOUR_PT=10` set, `AFFIRM_HOUR_PT=7` staged for next deploy. Identity beats live (`#dev-alerts` posted). **Next: a fresh AC0 (Justin's desktop) assembles the next v0.2-hardening batch.** Handoff: `docs/working/2026.06.29-ac0-ac0-deploy-handoff.md`.)*

*Updated: 2026-07-01 (AC — **subagent RESUME capability confirmed** (reverses the 2026-06-17 "no resume in AC's setup" finding): a deferred `SendMessage` tool resumes a completed subagent by its returned `agentId` with **full prior context intact** — proven at full-load scale (two ~500K-token fully-loaded QCs resumed and recalled loaded runtime identifiers — `runEvrynQuery` / `consolidate_profile` / `outbound_address` — from memory, one with **zero re-reads**; unforgeable in-context passphrases matched exactly). Mechanism = transcript replay (each resume re-sends the whole transcript → not free; resumability tied to transcript persistence). **OPEN: time-to-live** — proven only within-session/minutes-apart; an hours-later + post-compaction re-test is pending (live handles + re-quiz in `docs/working/2026.07.01-ac0-subagent-resume-test-state.md`). Persisted: new "Resuming a subagent" section in `docs/protocols/ac-orchestration-protocol.md` + corrected the stale CLAUDE.md fresh-instance bullet. **Orchestration-tooling discovery — no build/schema/identity/runtime change.** Nothing committed — awaiting Justin's SCM review.)*
*Updated: 2026-07-01 (AC4 — **v0.2.4 DEPLOYED + LIVE**: security-bulkhead **Bundle 1** (WebFetch/WebSearch egress deny-hook + `sendEmail` provenance gate + untrusted-content fence; ADR-047) + **Mira's Step-41a** fetch-decision identity beats, converged onto `main` (`bca656b`, tag `v0.2.4`) + carried AC1's Step-23 pong fix. Final converged adversarial QC = GO; `railway up` SUCCESS, clean boot (Supabase/Slack/polling, Step-21 cold-start). **No migration.** **Bundle 2** (constrained `supabase_read`, S1 v0.2 interim) BUILT + QC-GO on `ac4/sec-bundle2` (`547bc0b`), parked → next `v0.2.N` (rebase onto current `main` + final converged QC before its deploy). Ledger: `docs/deploy-log.md`. `_evryn-meta` persistence commits local-unpushed (AC0 pushes). Handoff: `docs/working/2026.06.30-ac0-ac4-security-bulkheads-brief.md` tail.)*

*Updated: 2026-07-01 evening (AC0 conductor — **wave snapshot** (complements AC4's v0.2.4 entry above): `v0.2.4` is LIVE. **Deploy queue (staged one lane at a time, `v0.2.N`, ledger `docs/deploy-log.md`):** next = AC4 **Bundle 2** (constrained `supabase_read` read-lock, S1 v0.2 interim — BUILT + QC-GO on `ac4/sec-bundle2` `547bc0b`, **unpushed**; parked, rebase-onto-`main` + converged QC before its ship) → then AC2 **cost ①** (Haiku pre-screen, BUILT on `dc/cost-haiku-prescreen` `89e8cff`, ships `off`; its DEV integration test is blocked on the env-fix below). **New workstream routed:** the dev/prod DB env-selection is **fail-open-to-prod** (a "dev" integration test that forgets to override its creds silently hits prod) → assigned to **AC3** as an early build (fail-safe explicit `SUPABASE_*_{PROD,DEV}` selection + a structural test-DB guard, as the shared primitive the staging work reuses; ADR + new SPRINT step owed; brief `docs/working/2026.07.01-ac2-ac0-dev-prod-db-env-selection.md`). AC0 gates + reconciles, does not run `railway up`. **Owed reconcile:** AC1's + AC4-Bundle-1's SPRINT DONE-marks. Wave handoff: `docs/working/2026.07.01-ac0-ac0-wave-handoff.md`.)*

*Updated: 2026-07-01 night (AC0 — **overnight set-down / batten-down**. **Nothing deploys unattended:** `v0.2.4` is LIVE + M1-protected + Healthchecks-monitored (Mark not forwarding → prod idle overnight); the two pending ships are BOTH gated on Justin's explicit go and will NOT fire while he's away — **`v0.2.5`** = AC4's ELEVATED Bundle 2 (per-table scope-filters w/ corrected key-sets + `cross_user_notes` column-deny [closes a firewall-bypass] + Step-35(a) `find_user_by_name` pulled forward; ADR-047; on `ac4/sec-bundle2`, holding for the build-go) and the **env-selection primitive** (Step 87, AC3; ADR-048 drafted; holding for the design-go). **Durable state:** `evryn-backend` `main` = `3a2fb43` (synced + clean; all AC1/AC4-Bundle-1 SPRINT reconciles + Step 87 pushed); `_evryn-meta` pushed incl. AC3's staging account-actions checklist (Step 80) + AC3→next-AC3 handoff. **Left untouched (active agents' in-flight — per Justin's don't-trip-anyone):** `evryn-quality` = 1 unpushed QC-manual commit; `evryn-team-workspace` = 2 uncommitted (Mira memory + team current-state) — flagged to Justin, NOT swept. Lanes: AC4 holding build-go; AC3 mid-env-fix (self-handoff written); AC2 parked behind the AC4 ship.)*

*Updated: 2026-07-02 morning (AC0 — **env-fix MERGED + Bundle 2 QC-GO, both ready-to-ship, still Justin-gated**. **`main` = `e04af87`** — AC3's env-selection primitive (Step 87 / ADR-048) merged: `required('EVRYN_ENV')` + qualified `SUPABASE_*_{PROD,DEV}` + `assertNotProd()` test guard; branch reaped. **⚠ Deploy landmine ARMED:** the next `railway up` needs `EVRYN_ENV=production` + `SUPABASE_URL_PROD` + `SUPABASE_SERVICE_KEY_PROD` set in Railway FIRST, or prod boot-fails (fail-safe by design) — Justin to set (inert to the running code). **AC4 Bundle 2 (`v0.2.5`) = BUILT + adversarial-QC-GO @ `8e5a084`** (EXPANDED per Justin 1b: per-table scope-filters w/ corrected key-sets + `cross_user_notes` column-deny [firewall-bypass fix] + `find_user_by_name` [Step-35a pulled forward]) — owes a **re-rebase onto `e04af87`** (env-fix landed under its QC'd branch → + `env-bootstrap` test-seed) + light re-verify, then Justin's merge-go → deploy-go (+ the env swap). 2 Mira NBs (name-lookup beat repoints) = non-blockers → Justin/Mira, ship-with-vs-fast-follow. **AC2 ① UNBLOCKED** (env-fix on main → its DEV test can run safely; still deploys 3rd). **Deploy order: env-fix ✓merged → `v0.2.5` (AC4) → AC2 ①.**)*

*Updated: 2026-07-02 afternoon (AC0 — **`v0.2.5` SHIPPED + LIVE; AC1 + AC4 lanes CLOSED; env landmine DE-ARMED.** `evryn-backend` `main` = `0c8c74b` (tag `v0.2.5`), `railway up` SUCCESS + clean boot. Now live in prod: AC4 security **Bundle 2** (constrained `supabase_read` read-lock — per-table scope-filters + `cross_user_notes` column-deny [firewall-bypass fix] + `find_user_by_name`) + Mira's name-lookup beats + **AC3's env-fix (ADR-048)**. Railway prod env-vars SET + **AC0-verified** (`EVRYN_ENV=production` + qualified `SUPABASE_*_PROD`; old unqualified pair KEPT until post-`v0.2.5` cleanup). AC1's Step-23 pong-warn fix **prod-verified** (warn gone from live logs). **Deploy queue:** `v0.2.5` ✓ → AC2 **cost ①** next (Haiku pre-screen, on `dc/cost-haiku-r2` rebased onto `v0.2.5`, ships `off`; gated on Justin's DEV-migration go, then deploys 3rd). **AC6** running a dedicated `ARCHITECTURE.md`/`BUILD` doc-reconciliation track (own branch). Reaped `origin/ac4/sec-bundle2` + the Mira namelookup branch (worktree dir file-locked → manual delete when the lock clears). **Owed bookkeeping (deferred into AC6's reconcile):** sprint DONE-marks Step 86/86b + 35(a). **Integration testing: a full pre-Mark live-fire is the remaining gate — cadence being decided with Justin** (Step 43 adversarial+cost / Step 82 staging validation are the queued homes). Handoff: `docs/working/2026.07.02-ac0-ac0-handoff.md`.)*

*Updated: 2026-07-02 (AC — **subagent-resume time-to-live CONFIRMED ≥ ~28h** (closes the "re-test pending" open item in the 2026-07-01 AC entry above): the two fully-loaded QC agents from the 2026-07-01 resume test were re-resumed ~28h later (overnight, across a date boundary) and recalled their unforgeable passphrases + loaded runtime identifiers with **zero re-reads** — the ~500K-token context survived overnight, intact and queryable from memory. Absolute ceiling + compaction-survival still unmeasured. Protocol "Resuming a subagent" updated; working doc archivable. Docs-only, uncommitted — awaiting Justin's SCM review.)*

*Updated: 2026-07-03 (AC0 — **wave narrowed to 2 live lanes + AC6 backburner; nothing deploying unattended.** `v0.2.5` remains LIVE (`0c8c74b`); `main` = `03e27e7` (docs-only sprint commit ahead — Step 88 minted). **AC2 (cost) ① Haiku pre-screen REBUILT to 3 verdicts** (`ignore` junk / `pass` real people / `escalate` rest — Justin's catch that the 2-verdict shape mislabeled junk as `pass`, polluting the v0.3 backlog + stamping spam) → DC-built + AC-reviewed + DEV-tested + **QC-GO**; committed `00542ca` on `dc/cost-haiku-r2`, ships OFF, **deploy-remaining** (next `v0.2.N`, Justin-gated). ②/③ held/not-started. **AC3 (identity/ops):** consolidated watch-list (Step 47) built → relocated to Soren's space (`evryn-team-workspace/shared/projects/product/watch-list.md`); **staging standup (Step 80) in-flight** — 4th Supabase project "Evryn Product — Staging" (Oregon, `hyqeutelwywbkisjekkb`) PROVISIONED (not yet seeded), Gmail OAuth in progress, Slack app + 2nd Railway service TODO; operator-guide env doc-sync done; **East DB deleted** (archive `backups/full-public-2026-06-03.sql` committed); local `.env` reorganized → `EVRYN_ENV=dev` + qualified prod/dev/staging trio. **AC6** = ARCH/BUILD doc-reconciliation, backburner (own branch). **Integration-test cadence = Step 88** (Gate A pre-③ DEV baseline · Gate B mandatory pre-Mark full live-fire, staging its home); **Justin picked Megan Griffiths** as the Step-43 fictional gatekeeper. **DB changes now hit 3 DBs** (dev/prod/staging). AC2+AC3 re-spun; AC0 replies in their brief tails.)*

*Updated: 2026-07-06 (AC0 reconcile of AC2's ship — **`v0.2.6` SHIPPED + LIVE; cost lane ① (Haiku pre-screen, Step 44) in prod, ships OFF.** `evryn-backend` `main` = `53a87f9` (deployed code = merge `8e11e9d`, tag `v0.2.6`; the pg_dumps sit one commit ahead); `railway up` deployment `22901ffe` SUCCESS + clean boot. **ZERO prod behavior change** — `HAIKU_SCREEN_MODE` unset → the three-verdict cheap-model screen (`ignore`/`pass`/`escalate`) never runs; **⚠ `off` = FULLY off — the screen never runs, collects NO data. A REQUIRED Mark-onboarding action is to flip `HAIKU_SCREEN_MODE`=off→`shadow`** (now in the operator-guide Go-Live Checklist + watch-list Step 44) to START the validation trial (~1wk real traffic, ~0 false-filter); Justin's explicit flip to `active` then banks the ~$1,800/mo — without the off→shadow flip the lever sits dead. 1 prod migration applied+verified (`haiku_screen_shadow` — RLS on, service_role-only; pre/post `pg_dump` in `backups/`). AC0 reconcile: current-state (this) + SPRINT Step 44 → DONE + CHANGELOG 2026-07-06 + team-cs; AC2 wrote the deploy-log row + `v0.2.6` tag. **Lane state: ② daily clustering (Step 58) is now UNBLOCKED — AC0 gave the all-clear; must be live before Mark forwards.** ③ bookkeeping + Step 71 still ahead. AC3 staging standup + AC6 doc-reconciliation unchanged.)*

*Updated: 2026-07-07 (AC3 — **staging environment STOOD UP + healthy** (Step 80 / ADR-045): 4th Supabase "Evryn Product — Staging" (ref `hyqeutelwywbkisjekkb`) **seeded by OC** prod-SHAPE (8 tables, RLS, RPCs/triggers/COMMENTs, +3 system actors, ZERO PII; reset recipe `evryn-backend/backups/staging-seed-*-2026-07-07.sql` `58170df`) + Railway `evryn-staging` env-wired + **deployed via `railway up --service evryn-staging`** (NO GitHub-connect; ⚠ a bare `railway up`=PROD) → boots clean, polling the staging inbox. **`SEND_ENABLED=false` — flip at validation-test time.** Home of the pre-Mark integration gate (Step 82/88). **Step-55 `declining.md` MERGED** to `evryn-backend` main `83bdc2c` (local, AC0 pushes; identity-file-review passed; NOT auto-deployed → next `railway up`). **54b FOLDED → Step 57** + a NEW user-creation-at-verdict OPEN QUESTION → routed AC0/AC2 (Step 57 = AC2's lane); 54a proceeds independently. Prod `_PROD` cleanup rides the next coordinated prod deploy (`railway variable delete` can't `--skip-deploys`). Commits local: `_evryn-meta` `3cb4ead`, `evryn-backend` `83bdc2c` — AC0 pushes. Detail: AC3 brief handoff `docs/working/2026.06.30-ac0-ac3-identity-ops-brief.md`.)*

*Updated: 2026-07-09 (AC0 — **AC6's ARCHITECTURE.md + BUILD-doc reconciliation MERGED to `evryn-backend` main `a4f6bb4`** (docs-only, no deploy — prod stays `v0.2.6`). Fidelity-certified ×2 (recent-window + a changelog-history drift pass that caught real stale badges — e.g. the emergency-channel "Mark-live blocker" already resolved by M1 `v0.2.2`) + Justin-vetted the full diff; clean fast-forward, 5 docs: ARCH reworked, `BUILD-EVRYN-MVP.md` slimmed to tuned v0.2 pointers, NEW `BUILD-EVRYN-v0.3.md` + `BUILD-FUTURE.md` (v0.3 / v0.4+ mechanics relocated verbatim), `SPRINT-V0.3-CANDIDATES.md` cross-ref. **Justin's goals, IN ORDER: (1) accuracy; (2) shorter [dedup + relocate v0.3+ mechanics]; (3) docs↔runtime AGREE — cut the cognition-tax of reconciling intent-vs-reality.** ⚠️ **NOT "now ACs can skip the runtime"** — knowing both intent AND the granular actual-runtime stays load-bearing; skipping `src/` because "the docs are accurate now" has almost always burned us. Docs = a trustworthy on-ramp, not a substitute. ac6 branch+worktree reaped. CHANGELOG 2026-07-09. Housekeeping: SPRINT Step 55 → DONE + Step 58 staleness flag; 2 team-workspace Principle-Breadcrumbs refs repointed → `BUILD-EVRYN-v0.3.md`. **MVP→v0.2 rename DECIDED AGAINST** (Justin — doc reads v0.2 internally + is clearly the MVP; not worth the ~79-ref churn). **Routed:** cadence-4:30 + Step-58-rewrite + 2 M1 code-comment heading refs → cost lane (AC2, needs a half-hour code change); Supabase-Security-Advisor Oct-30 sweep pulled into v0.2 → SPRINT Step 89 (Justin/OC). **Doc-accuracy process fix:** Part A = AC0 keeps ARCH/BUILD/SPRINT current at each #lock (effective NOW); Part B = **AC7 — SPUN 2026-07-09** (brief `docs/working/2026.07.09-ac0-ac7-doc-accuracy-process-brief.md`; seeded from AC6's drift evidence). **#lock 2026-07-09:** worktrees reaped (merged `dc/cost-haiku-r2` + stale `mira/declining-md-step55` remote branch); next-AC0 handoff `docs/working/2026.07.09-ac0-ac0-handoff.md`; team current-state appended. **Wave now = AC2 (cost) · AC3 (identity/ops) · AC7 (doc-process); AC6 CLOSED.** No prod deploy — still `v0.2.6`.)*

*Updated: 2026-07-09 evening (AC0 — fresh session, night set-down: **process-lesson codified** [from AC2's Round-17 Clustering Piece-B defect — a breaker-tripped test hid a live-path silent-drop → the "test interactive/tool-calling pathways LIVE before GO" rule now lives in the QC manual (pushed), the orchestration protocol, SPRINT Step 88, and AC's CLAUDE.md]; **AC3 reconciled** [Nathan Step-56 legal glance DONE — nothing gates Mark/v0.2, stamp is DSAR-reachable→v0.3; ops docs shipped `5f2c019`; SPRINT Step 52 GA→MOOT]; **Clustering** [AC2 cost ②] Round-17 Piece-B blocker FIXED but UNSHIPPED [deploy-prep + prod `queued` migration + merge-w/-Mira-PR#11 remain, all Justin-gated]. **Pushed all repos** (durability delegation). Prod stays `v0.2.6`; nothing deployed. New independent AC: **ACf** — autonomous-agents build, research-only, no collisions yet.)*

*Updated: 2026-07-11 (ACf #lock — **the autonomous-team runtime is DESIGNED, RATIFIED + STARTED.** The 3-day ACf arc (07-09→07-11): full corpus gathering (7 scouts) → blue-sky greenfield head-to-head → **ADR-050** (SDK mains on Justin's Max 20x subscription + a structurally-firewalled Mastra/open-model worker tier; supersedes the 5/30 "Mastra-not-SDK" direction — the metering pause + SDK primitives + Mastra 1.0 moved the facts; **Soren C-signed**, his 3 blockers applied) + the plan-of-record design `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.10-acf-justin-team-runtime-design-v2.md` (per-job budget caps w/ forecast + Emma aggregate watch, fresh-composed wakes + composition manifests, mention-gated Slack + little-birds watchers, deterministic heartbeats, security clearances w/ read-scoped sub-clearances, **in-house tasks/proposals REPLACING Linear at the Meta-Meeting cutover**). All 8 team agents consulted as protocol-loaded subagents on their worker rosters (~$380/mo near-term fleet). **NEW repo `evryn-team-runtime`** (main): ARCHITECTURE seed + DC-ready BUILD-PHASE-1. Infra UP: Supabase **"Evryn Team"** (`azugibmylktinddyqkhd`, us-west-2; full .env), Railway project, `claude setup-token` headless auth, Justin's statusline meters (ctx% + 5h%). All 21 research reports captured verbatim → `team-runtime/research/`. **Autonomy ON only post-Meta-Meeting (EVR-103/104).** Next: fresh ACf runs Phase-B spikes off `team-runtime/sessions/2026.07.11-acf-acf-handoff.md` (S6 subscription-ZDR check GATES Phase-1 confidential loads). Owed/proposed at this lock: repo-inventory + Hub repo-rows, LEARNINGS/AGENT_PATTERNS candidates, old Evryn-Agents Supabase project backup-then-delete (Phase B). No prod deploy — Evryn stays `v0.2.7`.)*

*Updated: 2026-07-11 evening (ACf #lock — **Phase B PROVEN + Phase-1 harness BUILT+MERGED (dormant).** Overnight autonomous run on Justin's Saturday orders: 6/8 spikes PASS zero failures (Railway subscription-auth · SDK primitives [pin 0.3.207] · Mastra hello · hooks→DB mirror [Evryn Team DB's first tables] · MCP bridge both-surfaces; S6 ZDR = CONDITIONAL → **Justin-ratified two-tier data rule, ADR-050 AMENDED** [Tier-B privileged-legal never on-subscription pending Nathan]; S3/S4 deferred to Justin-present) → 16 spike amendments Justin-approved + folded into BUILD-PHASE-1 → **full-protocol DC built the entire Phase-1 harness (52 files, 11 migrations applied+verified on Evryn Team DEV, tsc strict + 45/45 tests) → heightened QC: GO-as-DORMANT / NO-GO-for-activation (6 blockers, incl. a Read-gate path traversal + a Railway workspace-clone design gap) → merged `evryn-team-runtime` main `843d40d`.** Old Evryn-Agents Supabase project backed-up-then-DELETED (~$10/mo back). NOTHING deploys/activates — Slack app + blocker fix-trip + supervised live-fire all gate it, queued Sunday. Evryn prod untouched, `v0.2.7`. Session state: `evryn-team-workspace/shared/projects/ops/team-runtime/sessions/2026.07.11-acf-acf-handoff.md`.)*

*Updated: 2026-07-12→13 (ACf #lock — **the runtime's control layer is BUILT+MERGED and the control panel is DEPLOYED; still dormant by construction, no Railway deploy.** Sunday autonomous run on Justin's standing orders: **"Sunday controls" merged** (`evryn-team-runtime` main `6a94cab`, pushed; 117/117; DC + heightened-QC GO + fix-trip + light-verify GO) — git-write memory durability (cloud notes survive redeploys; token hygiene verified), per-agent **capability grants w/ tier presets** (migration 013 on DEV; fail-closed wake gate w/ ~5s cache; `confirm_before_execute` default ON), **hard pause** (parks in-flight losslessly) + kill classes, **boot-paused default** (first boot in any env starts OFF), and a tool gate that **fails CLOSED even on DB outage** (QC catch, fixed structurally). **Team Runtime dashboard tab deployed live-INERT** (`_evryn-meta` `dcbcab9`; QC GO zero blockers; "not yet connected" until Justin sets the 2 Vercel env vars). **Dormancy smoke PASSED** (boot honored the pre-paused switch; Lucas online on Slack; woke nobody). runtime-ops v2 drafted (Justin-gated). Skill pre-auth proven + Justin's standing opus-pin rule for subagent spins. Monday: orientation + ballot delivered (skills packaging rec; **5-hr-limit usage-governor proposed as a pre-autonomy requirement**); AC-header rule doubled into CLAUDE.md + orchestration protocol (Justin-tweaked, pushed `66e6943`). **Gates to Lucas-live: Justin's wiring batch (Vercel + Railway env) → the supervised live session (success criteria 1–8). Autonomy stays OFF until post-Meta-Meeting.** Evryn prod untouched, `v0.2.7`. Session state: `evryn-team-workspace/shared/projects/ops/team-runtime/sessions/2026.07.11-acf-acf-handoff.md`.)*

*Updated: 2026-07-13 afternoon (ACf #lock — **Monday sprint: composition v2 + skills v1 BUILT to QC-GO [branch `dc/composition-v2` `5c07745`, 159/159, unmerged] = Lucas's real about-to-be-paced architecture** — second read-only `_evryn-meta` clone for Hub+spokes, explicit layer order w/ team current-state composed NEVER-windowed, skills-as-a-registry (index + `load_skill` + skills-at-write), `state_reason` column; Lucas's composed mind measured ~61K tokens. **runtime-ops RATIFIED + moved to `shared/protocols/`** (⛔ gate: Justin's full read before any agent past summon-only). **AC added to Soren's worker roster** as his default build-route (design-v2 §5 + Soren memory). **OC ruled EARLY** (rides mains harness under Soren). Process codified into AC CLAUDE.md: secret-handling (self-poisoning → re-spin) + rebuilt Autonomous Work Protocol + model-pinning + 2 QC patterns. **Runtime WIRED for deploy inert** (Vercel env → dashboard tab live; Healthchecks; token extended to `_evryn-meta`); Railway env-set + supervised Lucas paces are the fresh ACf's next steps. Evryn prod untouched, `v0.2.7`.)*

*Updated: 2026-07-15 (AC0 — morning proper-handoff/doc-currency reconcile. **③ Step-57 "Runtime Bookkeeping" = Justin-RATIFIED (ADR-051: user-creation moves to verdict-time [none for `ignore`]; raw `supabase_upsert` REMOVED wholesale + a new operator-channel-only `correct_user_field` tool) + DC-BUILT + rebased + QC-GO with ZERO blockers, UNMERGED; ZERO prod migration.** Remaining before it merges: live-path DEV exercise → **Gate-A (Step 88, with AC0)** → Mira pass → Fable review → Justin's merge. **54a + 54c BUILT + MERGED** to `evryn-backend` main `2572dbc` (deploy-pending — ride the ③ bundle; 54a re-points the "have I met this person?" triage check at the contact's permanent record, 54c captures the real forwarded-sender name). **AC7 doc-accuracy process fix LANDED** across 3 repos (`_meta a9a34c0` / `backend c9813a0` / `team-ws c1d9ba8`) — the #lock doc-currency gate + new `doc-update-routing-protocol.md` are now LIVE; AC7 closed. **The ③ bundle deploy** (ONE `railway up`, run by a lane or AC0 — **NOT Justin, deploys are beyond his pay grade**) carries ③ + 54a/54c + the Anthropic key-split env swap + the dead `_PROD` var cleanup, gated Gate-A → Gate-B (mandatory pre-gatekeeper staging live-fire). SPRINT reconciled: 41b/48/49/56/58a/80 → DONE, 45 → MOOT, minted Step 90 (reply-item doctrine). Operating-model deltas this stretch (Justin heads-down on ACf/team-runtime): lanes take recommend-first ballots STRAIGHT to Justin (not routed via AC0); Opus-default + a one-turn Fable option for the ③ design synthesis; AC0 conducts merges/collisions/housekeeping. Prod stays `v0.2.7` (Clustering + Haiku both dormant-OFF). Handoff: `docs/working/2026.07.15-ac0-ac0-handoff.md`.)*

*Updated: 2026-07-15 evening (ACf #lock — **team-runtime MEMORY + WAKE-ECONOMICS architecture designed + Justin-ratified (ADR-052); Phase 1 approved + starting; no code/deploy.** A Fable deep-dive (full live-runtime read + memory corpus + external agentic-memory SOTA sweep) → design-of-record `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-team-runtime-memory-architecture.md`. Reframe: the memory *design* was mature; the gaps were (a) the runtime never FIRES consolidation and (b) the meta-meeting token bomb. Justin's rulings: thread-IS-the-session (full load, no 30-msg window) · **wake economics = MEMBERSHIP not a model gate** (invite/`leave_thread`/late-invite-one-pass — replaced the first-draft Haiku gate) · one shared **30-min-swept digest per thread + a per-agent scratchpad HEADLINE** (replaced per-message tagging/staleness machinery) · private per-thread scratchpad · **dormancy memory-write, thread-ID-stamped (echo rule)** · **per-agent wake serialization** ("one mind, one moment") · two-lane consolidation for Phase 2 (Lane A autonomous / Lane B parks Protected-list for Justin, review must be phone-light) · FTS-first retrieval. §4.6 "one mind, many rooms" settles the thread/identity model. **Lanes: Phase 1 building (ACf1** — brief `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-acf1-phase1-memory-build-brief.md`, Opus/DC-QC); **Phase 2 (ACf2)** approved-in-principle, **scheduling → ACU** (note `_evryn-meta/docs/working/2026.07.15-acf-acu-memory-lanes-sequencing.md`). Reconciled `evryn-team-runtime` ARCHITECTURE.md + BUILD-PHASE-1.md (breadcrumb + real drift fixes: composer 12-layer not "1-8"; notes opt-in not "mandatory"; BUILD status built-not-"ready-for-DC"). No runtime code / no deploy — prod `v0.2.7`, runtime dormant.)*

*Updated: 2026-07-15 evening (AC0 — product-wave close-down. **③ (Runtime Bookkeeping) DEV exercise GREEN, both tiers** — deterministic + live-Opus (breaker OFF): the real tool-loop fired through ③'s new hooks — junk → `record_verdict(ignore)`, a gold-fit forward ran the full loop → `pending_approval` + `triage_result=gold` canonicalized + contact/FK/54b-row (~$1.17). **Gate-A (Step 88A) RESULTS IN — 12/13 STRONG (a13 deferred), BASELINE PASS**: AC2 seeded the Megan fictional gatekeeper (`43a68cfa…`, `systemtest@evryn.ai`) + ran the a01–a13 §3 judgment suite live on current `main` (ONE seed covers both baseline + adversarial). Passes/edges/reply all correct; golds a01–a03 → `edge` = the EXPECTED Phase-5 web-search divergence (fictional entities don't web-verify → forgiving-skepticism edge, well-reasoned, NOT a mis-fire); whole loop flowed, NO gate falsely blocked, read-guard + egress-guard fired right, no security-boundary breach. §4 injection/SSRF suite is manual (not in criteria); send-leg no-op on DEV → Gate-B. **⚠ a12 hung the run ~27min on a likely hanging WebFetch — CURRENT-MAIN behavior, NOT a ③ regression, but ELEVATED to a PRE-MARK fix (Justin, 7/16) + ROUTED to AC2**: `runEvrynQuery` has no wall-clock timeout, so a hung fetch wedges the single-threaded poll loop (only recovery = Healthchecks alarm→manual restart) — real Mark-live risk since she web-researches every sender (why a13 didn't run). **AWAITING AC0 INDEPENDENT CO-SIGN** (next AC0): re-run a13 [dedup-skips a01–a12] + review the DEV state [Megan + run items LEFT INTACT] → co-sign the GO → sequence the rest. **Step 78 Half A** (real 503-when-wedged `/health` + M1-halted→200, ADR-041-consistent; new `src/safety/liveness.ts`+`health.ts`) **BUILT + QC-GO on `ac3/step78-real-health` (`d1238f3`); merge routed to AC0** — now a small **rebase-then-FF** (my operator-guide fix landed on `main` first; different sections, no real conflict); NOT deployed → rides the coordinated deploy wave; **needs Justin's explicit merge-go-to-AC0**. **Mira identity pass PRE-STAGED** (AC2's full dispatch prep in the ③ build-review doc — fires after Gate-A GO). Housekeeping: 10 settled working docs → `docs/sessions/historical/2026.07/` (+ ADR-020/049 pointers repointed); operator-guide Anthropic-ceiling recharacterized to actively-managed-not-one-time (Justin). Prod stays `v0.2.7`. Handoff: `docs/working/2026.07.15-ac0-ac0-eve-handoff.md`.)*

*Updated: 2026-07-16 (AC0 — **⭐ GATE-A CO-SIGNED = GO — and it's 13/13, not 12/13: a13 DID run.** AC0 loaded the full runtime + reviewed the DEV record directly (not AC2's grading table) and found **a13 already complete** — it ran in a SECOND invocation at `01:11:24Z` (150s; the runner dedup-skips a01–a12 by design), ~30min after a12's last write, landing `pending_approval`/`triage_result=edge` with strong reasoning (flagged the email is in Spanish **+ offered to translate** — the Phase-6 6c language-as-substance beat, live; named the unverifiable domain without treating absence as disqualifying for an emerging Mexican indie). **AC2's ③ handoff + AC0's eve handoff both still say "a13 NOT RUN" — STALE; corrected here + flagged to AC2 in the cost-lane mailbox.** **Independently confirmed** (off the DB, not prose): **0 cross-user stray items** (all 13 owned by Megan → lane-resolution fired on every forward), lifecycles recorded, ADR-036 contact FKs populated, no security-boundary breach, read/egress guards allowed legit work. **Concur** the 3 gold→`edge` = the fictional-entity web-search divergence, NOT a mis-fire — **+2 nuances banked:** a02's edge is *earned on a real content inconsistency* (a jury invite naming no festival), not merely failed verification; and gold/edge share ONE code path in `submitDraftForApproval`, so the golds-never-going-gold leaves **no untested path** (baseline intact — don't re-litigate). **+2 NEW hang fingerprints for the WebFetch fix:** a12 is **still stuck at `processing`** — which proves the **B2c backstop structurally CANNOT catch a hang** (it only runs *after* `runEvrynQuery` returns; a hang never returns), so the poll loop stays wedged until the 30-min stale-check merely *notices* — and a12 has **no `llm_usage` row** (13 items, 12 rows, $5.35) since usage is captured only post-completion. **⭐ Step 78 Half A MERGED** → `evryn-backend` main **`de3a5bd`** (+ sprint mark `11fa240`), on Justin's explicit go: rebase-then-FF, typecheck + all liveness/health tests re-run green. ⚠️ **The "different sections, no real conflict" claim in the eve handoff was WRONG on its premise** — Step-78's 3 commits **never touch `operator-guide.md` at all**; the 4-line "conflict" was a *file-state-diff illusion* from a stale branch base (the merge-base diff shows only 7 src/test files). Right conclusion, wrong reason — verified, rebased, and Justin's operator-guide wording confirmed intact on `main`. Worktree + local/remote branch reaped. SPRINT Step 78 → **IN PROGRESS** (Half A merged / Half B built-unmerged; **neither deployed** → both flip DONE at the bundle deploy). **Step 78 Half B** (dashboard lights) = **BUILT + QC-GO, UNMERGED** on `_evryn-meta` `ac3/step78b-dashboard-lights` (`7bffd0d`) — ⚠ dashboard = **Vercel push IS a live deploy** → merge gated separately. **WebFetch-timeout fast-follow = BUILT** (Justin ratified fast-follow): `dc/webfetch-timeout` `bf23bca`, stacked exactly on ③'s tip `d9306a8` (AbortController wall-clock cap, `EVRYN_QUERY_TIMEOUT_MS`); **still needs its live-path QC**. **Durability sweep:** 3 branches had **NEVER been pushed** (`dc/webfetch-timeout`, `ac3/step78b-dashboard-lights`, team-runtime `spike/s3-protocols-as-skills`) — all pushed; every repo now synced. **Gate-A cleanup deliberately HELD** (the state contradicted its description, and a12's stuck row is live evidence to verify the WebFetch fix against) — wipe after that QC. Prod stays `v0.2.7`. **Next (all Justin-gated):** ③ catch-up rebase → Mira pass → Fable merge-review → merge → one-window bundle deploy → Gate-B.)*

*Last #lock (full): 2026-07-15 evening (ACf — team-runtime memory architecture design + ADR-052 + Phase-1 start + ARCH/BUILD reconcile; no code, no deploy)*
*Prior full #lock: 2026-07-13 afternoon (ACf — composition v2/skills built QC-GO unmerged; runtime wired for deploy; AC-into-roster; process codified; no prod deploy)*
*Prior full #lock: 2026-07-13 (ACf — Sunday controls merged + dashboard tab deployed inert + dormancy smoke passed; no prod deploy)*
*Prior full #lock: 2026-07-11 evening (ACf — Phase B + Phase-1 harness merged dormant; no prod deploy)*
*Prior full #lock: 2026-07-11 (ACf — team-runtime arc: ADR-050 + design v2 + new repo/infra + research capture; no prod deploy)*
*Prior full #lock: 2026-07-10 (AC0 — end-of-week #lock: **Clustering DEPLOYED `v0.2.7`** [ships off, dormant] + `declining.md` live; v0.2.7 doc reconcile [current-state/ARCH markers→live/SPRINT Step 58 DONE]; **dashboard Opus-vs-Haiku by-model cut merged + Vercel-deployed** [Step 46 DONE] + orchestration-protocol non-runtime-target amendment merged [Justin-authed]; Step 81 DONE; clustering un-holds AC3's 54a/54c; AC2 pushed pre-compaction; clustering + dashboard worktrees reaped; next-AC0 handoff `docs/working/2026.07.10-ac0-ac0-handoff.md`. Prod `v0.2.7`. Wave = AC2[③ next]/AC3[54a-c]/AC7 + ACf independent.)*
*Prior full #lock: 2026-06-29 (AC0 — round-2 v0.2-hardening DEPLOYED + live; #lock + worktree reaps + desktop handoff + docs cleanup: sessions/historical month-foldered, round-2 working/session docs archived, cost-levers persisted → product/research)*
*Prior full #lock: 2026-06-25 evening (AC0 — persistence pass + DC round-2 code trip + loading-discipline experiment/protocol codification; bundle `d198ef1`, integration-QC next)*
*Prior full #lock: 2026-06-25 (AC0 — round-2 convergence bundle COMPLETE on `r2/convergence-bundle`, tsc-clean; integration-QC + deploy remaining)*
*Prior full #lock: 2026-06-25 (AC4a — STAGING lane: design finalized + send-allowlist QC-GO + ADR-045 + dashboard real-green-lights spec)*
*Prior full #lock: 2026-06-19 evening (AC0 — recovery executed + round-2 staged for the clean re-spin)*
*Prior full #lock: 2026-06-19 (ACP — loading-discipline hardening; gate clears for AC0)*
*Prior full #lock: 2026-06-18 (AC0 — lobotomized-subagent disaster: forensics + clean-respin packout)*
*Prior full #lock: 2026-06-16 evening (AC0 — go-live DEPLOY complete + post-deploy cleanup)*
*Prior full #lock: 2026-06-16 (AC2 — Evryn Product dashboard tab)*
*Prior full #lock: 2026-06-16 (AC0 — deploy-gate close + set-down)*
*Prior full #lock: 2026-06-15 (AC0 — go-live bundle close)*
*Last #sweep: 2026-04-04 (Lucas)*
*Last #align: 2026-04-04 (Lucas)*

---

## Strategic Pivot: Evryn Product First

Team agent build (Lucas) paused — not cancelled. Building Evryn product MVP (v0.2 "Gatekeeper's Inbox") for Mark (real-Mark = Mark Titus, Seattle filmmaker / Eva's Wild founder) first. See `evryn-backend/docs/BUILD-EVRYN-MVP.md`. Evryn is a broker, not a SaaS — surfaces connections from a gatekeeper's inbox; everyone is a "user," both sides pay per-connection.

## How AC works now

AC drives DC and QC as **subagents** (build/review loops), not hand-relayed mailbox notes. Protocol: `docs/protocols/ac-orchestration-protocol.md`. Subagent startup loading is standardized via the **Startup Context Cascade** (verbatim "read and faithfully follow" trigger + receipts watch on the task-specific additions), with **`#cascade-override`** the only guard-railed way to narrow a load (2026-06-06). QC verifies every real code change. **AC promotes QC's standing patterns** into QC's CLAUDE.md "Patterns This Role Watches For" section (QC can't write her own; new 2026-06-03). **Ping-by-default** on `#team-alerts`. Arch + BUILD docs: AC holds edit rights, **editing needs Justin's explicit auth first**.

## What's Next (critical path to Mark-live)

- **⭐ ROUND-2 v0.2-HARDENING DEPLOYED + LIVE 2026-06-29 (`main` `d198ef1`, `railway up` SUCCESS, clean boot).** M1 silent-death layer + Step-61 gatekeeper-address resolution + lean Reflection + Lane-B hardening all live; 4 prod migrations applied + verified; Healthchecks.io heartbeat + daily-affirmation wired. **Next critical-path action: a fresh AC0 (on Justin's desktop) assembles the next v0.2-hardening batch from `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` + `SPRINT-V0.3-CANDIDATES.md` and knocks items off until the sprint is done — then a full live-test before Mark onboards.** Deploy handoff: `docs/working/2026.06.29-ac0-ac0-deploy-handoff.md`. *(The dated stamps above are the live layer; the accreted bullets below are historical, pending the owed ARCH/current-state alignment pass.)*

- **v0.2 GO-LIVE BUNDLE DEPLOYED + healthy 2026-06-16 (`main` `49fb6ff`, `railway up` SUCCESS; both alerts confirmed). Post-deploy cleanup done (worktree/branch reaps · `notify_queue` DROPPED dev+prod · `evryn-backend`+`_evryn-meta` pushed). Remaining = round-2 on the other machine (M1 Stage 2 + Android-DND pierce · determination-guardrail re-arrange [Mira brief] · cache-TTL · ARCH/schema/operator-guide `notify_queue`+M1 doc absorptions · AC3/AC2). Resume `docs/sessions/2026.06.16-ac0-ac0-handoff.md` (bannered DEPLOY COMPLETE). The pre-deploy bundle-close detail below is now historical.** Branch `bundle/go-live-v0.2` (worktree `evryn-backend-integration`, off `main` `fdd57f7`). Contains: the 4 merges (quiet-hours rolled into `morning-sweep-c2` · `m1-emergency-channel` Stage 1 · `cost-pass-stamp` `record_pass` · `mira-phase6-identity-batch`), the committed interim `f4083e6` tag-drop (**superseded** — see below), and **held working-tree edits**: (1) the **pass-stamp rewrite** in `src/triage/classify.ts` — `This user emailed <gk-first-name> [gatekeeper: <uuid>]. Evryn chose not to make the connection. …[shareable_with_user]…` + the `gatekeeperFirstName` helper + updated `tests/test-record-pass.ts` — **QC GO *and* DC build-lens GO**; (2) Mira's tightened `record_pass` Pass-line in `triage.md`; (3) Mira's determination-sharing guardrail in `core.md` (**placement OPEN — see handoff**). **Deploy-close remaining (next AC0, in order):** resolve the guardrail placement *with Justin* (Mira said pull-to-v0.3; Justin disagrees) → final QC re-confirm of the identity edits → commit the held bundle (one commit superseding `f4083e6`) → **Justin's explicit deploy-go + walk him through the M1 emergency-Slack / phone-DND setup** → `railway up` → post-deploy (notify_queue table-DROP *after* deploy, 5 branch reaps, doc absorptions). **Then spin AC3** (3 forward-persistence findings + SF1/SF2 + Phase 3-5 batch + composition-investigation Step 53) **and AC2** (dashboard — briefed + pushed). **Full nuance + exact resume sequence: `docs/sessions/2026-06-15-ac0-handoff.md`.**
- **Context-architecture fixes: BUILT + QC-verified + DEPLOYED (AC1, 2026-06-08, `75fba9e`).** The live-test findings previously *routed* to Soren are now *shipped*: conversation history is runtime-owned (Evryn writes only the body; the runtime appends history once, labeled with real display-name + email, not "(user)"/UUID), the attached-history count is visible at `submit_draft` so Justin can verify it, plus a **double-send guard** (a re-approve after a successful send can't send twice) and **silent-failure alerts** (stale-check / approval-lookup / cron-checker DB errors now `notifyDev` instead of swallowing). Mira **PR #9** (identity tighten — create-from-zero gate + context-duplicate wording) merged; `core.md` history-claim scoped to the email/submit_draft path. Reviewed by **real DC + real QC** (the earlier generic-subagent review was redone properly — see AGENT_PATTERNS "Getting Subagents to Actually Load Their Context"). 2 minor non-blockers (drain-cooldown reset; stale collision help-string) → SPRINT hardening list (week of 6/8).
- **Email-format + approval-flow polish: BUILT + QC-verified + DEPLOYED (AC1, 2026-06-09, `83fdd5e`).** The review/sent emails are cleaned up (review@ = classification → draft → context, printed *once*, newest-first; Mark's email = draft + the incoming forward only, no rolling history — fixes the ~7× reprint from `stripReplyChain` being a no-op on Gmail nested quotes). Bare approvals are now **single-route** (clean "Confirmed — sent," no hollow LLM follow-up; awareness preserved via the persisted records — **ADR-035 §3 superseded**, amendment written). Direct-message review dedup + SDK typecheck cast (`npm run typecheck` green). Mira's 2 identity changes (`30d85f0` + Closing-the-Loop) rode the same deploy. DC built, AC+QC verified GO.
- **Oregon/us-west-2 cutover: DONE (AC0 #2, 2026-06-04 eve).** Prod is now in Oregon/us-west-2 ("Evryn Product (West Coast)", ref `wvaaqwapueycyxyhxdnh`): 036 migration applied + verified, pre/post `pg_dump` taken (`backend/backups/oregon-{pre,post}-036-2026-06-04.sql`, untracked), Railway repointed → Oregon/us-west-2, master (`1610f3b`) deployed + healthy (Supabase/Slack/polling confirmed). **Old East (`maruxkjwlfltlmureqkt`) still live — AC0 retires it once FULLY confident West is flawless; don't delete yet.** Apply mechanism: full-path PG17 psql + `SUPABASE_DB_URL_{PROD,DEV,EAST}` (see `backups/README.md`).
- **Dev/staging DB: UP (AC1, 2026-06-04).** `Evryn Product — Dev` (us-west-2, ref `maqkdesopsskptpxjbqs`) created + seeded as a **faithful mirror of prod** (5 tables, citext+vector, RLS, policies, comments, test data). Enables QC live-tests + **dev-first migrations**. Org now **Supabase Pro** (~$45/mo, 3 projects). `.env`: runtime = Evryn Product (West Coast); admin DB urls `SUPABASE_DB_URL_PROD`/`_DEV`/`_EAST` (dev API creds in Bitwarden, not active `.env`). Full decisions: **ADR-037**.
- **evryn-backend master (`1610f3b`) DEPLOYED to Evryn Product (West Coast)** (AC0 #2) — the validated bundle (EVR-71/68 + ADR-036 + Mira's triage beat + go-live reconciliation + AC1 DB/doc work). 036 column live on Evryn Product (West Coast). `POLL_INTERVAL_MS=10000` (test cadence — slow at Mark-live); `SEND_ENABLED=true` (already on in prod); `TEST_RECIPIENT`/`NODE_ENV=development` = dead config (6/8 sweep).
- **Integration test: Phases 2-5 PASSED** (create-from-zero · onboarding · notes · triage validation). Phase 2 (`create_user` not upsert, scope-lock, deep research, draft→approve→send) + Phase 3 (onboarding — Mark's gold/pass/edge criteria gathered) + Phase 4 (notes vs answer key) + **Phase 5 (triage: 18 fixtures → effectively 18/18 on reasoning; she web-verifies + distinguishes failed-verification from absent-footprint; "grade on reasoning, not label-match" — full nuance `evryn-backend/tests/2026-06-08-phase5-results.md`)**. **Phase 6 (live-fire prod pipeline) is the one remaining phase** — plan `evryn-backend/tests/phase6-live-fire-v02.md`. Fixture data cleaned from prod after Phase 5 (Mark restored to his 5 onboarding notes). **Real-Mark email: not yet sent.** Findings shipped: context-arch → built+deployed (AC1); verify-and-lock/gold-internal → Mira (shipped); **B2c/edge bug → Mira fix `30d85f0` DEPLOYED (AC1 `83fdd5e`)**; attachment-only calibration + dummy-Mark → adversarial test (post-launch).
- **Mark wiped to ZERO (2026-06-03).** Test-Mark row + 49 messages + 4 items deleted; DB now holds only Evryn + Operator + a Google-Workspace test lead (kept — trains "ignore Google pings"). Backup committed `f26414c`.
- **Integration test is now CREATE-FROM-ZERO** (was look-up-existing). Phase 2: operator introduces a never-seen Mark → Evryn creates his record via `create_user` (first real test of the ADR-036 tool); new Phase 2b re-tests the ADR-030 verify-and-lock *find* path. Test materials conformed (`f7f48a0`, `1627b8b`).
- **Go-live simplified to match.** No hand-created real-Mark row / no `TEST_RECIPIENT` flip — you introduce real Mark, Evryn creates his record; the approval gate is the backstop the removed redirect (ADR-029) gave. operator-guide STEP 0 + SPRINT reconciled.
- **036 migration: APPLIED to Evryn Product (West Coast)** (AC0 #2) — column + index + FK live; `users.email` UNIQUE confirmed already present (verify-only, no add needed).
- **Next (FRESH AC0 + Justin):** **Phase 6 live-fire COMPLETE** — all scenarios run on `83fdd5e`(+cost-capture `23f9858`): S1 Nadia gold→matched, S2 Garrett edge→pass (+ the review@ phantom-user mess), S7 Rick clean silent-pass, S8 Amy slam-dunk gold→matched (nothing sent to the real Amy), S4 StreamlineHub silent-pass, S5 direct-message check-in + the 42pictures self-handled close (≈ step-11), S6 robustness (6a empty-skip / 6b attachment silent-pass / 6c Spanish edge). **Findings 13-21 + Mira items 10-22** captured in `docs/working/2026-06-09-phase6-{findings,mira-dispatch}.md` (headline directions: **pass-note = deterministic stamp on the contact only**, the ~$8k/mo cost lever; 6a known-user empty→deterministic bounce; 6c language-as-substance + don't-quote-untranslated; review@/internal-address skip; binding hygiene; verbosity → Mira note template). **Next steps:** (1) **Pre-Go-Live DB wipe DONE (2026-06-11, AC0)** — prod create-from-zero ready (Evryn + Operator + the Google-notifications non-user marker (role=system, dormant — redesigned 6/13); 0 items / 0 messages; Operator notes cleared; pre-wipe `pg_dump` local), (2) real-Mark **"we're ready" email SENT (2026-06-11)**, (3) **M1 silent-death detector** (the one hard build gate, not built; gates Mark's *forwarding*, not the email). AC02 building the **quiet-hours redesign** (deploy after testing). 2026-06-09 handoff **retired to `docs/sessions/historical/`** (Phase 6 done, 2026-06-11).
- **AC2 pre-go-live audit + doc reconciliation: DONE + PUSHED** (commits `1e67163`, `a02c760`, `afeeb95`). One true build blocker = **M1 `#emergency-alerts` silent-death detector** (~½ day; NOT Twilio; the process-crash shape needs an external watchdog). Backups + RLS satisfied (**RLS live-verified ON, all 5 tables, prod+dev**). Codified the dated **Go-Live Delineation** in SPRINT + reconciled the stale ARCH/BUILD/SPRINT (collapsed the sprint doc 597→290 to a status-prefixed Build Record + grouped backlog; **adversarial test corrected to dev/fictional-gatekeeper, never live-Mark**). **EVR-114** opened (Soren meta-architecting + a v0.2 maintenance-plan stub).
- **EVR-72** (follow-up loads gatekeeper not contact) — ENABLED by 036's FK, NOT fixed.

## Pending doc-syncs (need Justin's auth — flagged, not done)

- **`master`→`main` doc-reference sweep — core sweep DONE (AC3, 2026-06-08).** Live operating-doc refs updated to `main` (orchestration protocol ×3; CLAUDE.md PR-review example + worktree-discipline lines). All historical/dated refs + "Master Plan"/"master key" left intact (rewriting dated records would falsify them). **Still owed:** the `master` refs in `evryn-backend` `ARCHITECTURE.md` / `BUILD-EVRYN-MVP.md` dated state-blocks — fold into the ARCHITECTURE accuracy pass below.
- `ARCHITECTURE.md` Data Model: 036 FK + `create_user` — **DONE 2026-06-03 eve.** `LEARNINGS.md`/`AGENT_PATTERNS.md`: largely captured (phantom-controls in LEARNINGS; QC patterns in her CLAUDE.md). **team CLAUDE.md** "identity-pushes-trigger-redeploys" stale — **routed to Soren** (flagged in team current-state + here; not yet fixed).
- **`ARCHITECTURE.md` full top-to-bottom accuracy pass — OWED (AC2, 2026-06-05).** This session only did *targeted* fixes (the 3 worst stale blocks — Current State, date stamp, East project ref — plus staleness-grep hits), **not** the exhaustive line-by-line accuracy read needed to vouch the whole doc is clean (the Proactive-Behavior "Target: v0.2…" staleness example was **FIXED 2026-06-13 (AC1)**, and the `SPRINT-MARK-LIVE`→new-sprint reference repoints are done — but the exhaustive line-by-line read is still owed). Also minor: **`evryn-backend/docs/operator-guide.md`** date-stamp drift (stamped 2026-05-29 but carries later create-from-zero content). Justin chose "leave for now" 2026-06-05 → **pick up at #sweep**.

## Infrastructure

- **evryn-backend default branch: `main`** — renamed from `master` 2026-06-08 (AC3, GitHub native rename: default moved, redirects set, canonical tree synced, `origin/master` deleted). Now in line with all other repos. Deploy-neutral (`railway up` is branch-agnostic; auto-deploy off) — verified healthy.
- **Railway: AUTO-DEPLOY OFF — staying off by design.** Manual `railway up`. **Live deploy = `517014d` (tag `v0.2.7`) on Oregon/us-west-2 (2026-07-10, AC2 — cost lane ② Clustering [Step 58 / ADR-049], **ships `CLUSTERING_MODE=off`** — dormant, zero prod behavior change until the Mark-gated flip; `declining.md` [Step 55] also went live this deploy; 1 prod migration [`queued`-status CHECK-widen]; clean boot Supabase/Slack/polling, deployment `1c5bdfaf`). Prior: `8e11e9d` = v0.2.6 Haiku pre-screen [Step 44], ships OFF (`22901ffe`); `0c8c74b` = v0.2.5 security Bundle 2 + env-fix (ADR-048 — `EVRYN_ENV=production` + qualified `SUPABASE_*_PROD` are the live creds); `bca656b` = v0.2.4 security Bundle 1; `83fa367` = v0.2.3 AC1 cleanup; `d198ef1` = v0.2.2 round-2 hardening. Deploy ledger: `docs/deploy-log.md`.** Env: `PROACTIVE_CHECK_HOUR_PT=10`, `AFFIRM_HOUR_PT=7` (staged — applies next deploy), `HEALTHCHECK_URL` + `HEALTHCHECK_AFFIRM_URL` set → Healthchecks.io heartbeat (~15min grace) + daily-affirmation (~26h) **LIVE**; quiet hours 18–8 PT; `POLL_INTERVAL_MS=10000` (test cadence — slow before Mark, at the next batch's live-test).
- **Supabase: on PRO. Prod is on Evryn Product (West Coast) (Oregon/us-west-2)** (AC1; faithful copy, connection-verified) + **dev project "Evryn Product — Dev"** (Oregon/us-west-2, seeded mirror, 2026-06-04). Old East (`maruxkjwlfltlmureqkt`) live until cutover-retire; its `pg_dump` (`backups/full-public-2026-06-03.sql`) is the archive. DB state: system actors + 1 Google lead (Mark wiped 2026-06-03). **Backup model:** Pro **daily auto-backups** (primary, live on prod+dev) + periodic real **`pg_dump`** (portable/archival) — replaces the old non-restorable JSON dumps (ADR-037).
- Slack: `#evryn-approvals` — pings post immediately (post quiet-hours redesign; the 18–8 PT window now gates only the stale-checker re-pings, no more queue+replay). `#emergency-alerts` **LIVE** — M1 Stage 2 detectors wired + deployed 2026-06-29 (DND-respecting / morning-visibility; a money runaway is protected structurally by the circuit-breaker halt, not a human-wake).

## Task Management

[Linear (EVR workspace)](https://linear.app/evryn) — **EVR-68 + EVR-71 fixed + DEPLOYED** (in the Oregon/us-west-2 cutover 2026-06-04; verified in `src/email/poll.ts` — held-cursor + HandleOutcome dedup. **Soren can close EVR-71/68** per Lucas's 2026-06-05 note). **EVR-72 enabled-not-fixed**. **EVR-114** = Soren meta-architecting meeting (R: soren, A: justin), opened 2026-06-05. EVR-67 = v0.3 S1 hardening. EVR-108/109/110 standing. Soren-owned; Justin/Lucas update per this state.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
