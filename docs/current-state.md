# Current State — Evryn (Cross-Project)

> **Truncation check:** The last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

**How to use this file:** Snapshot of current project status across all Evryn repos. NOT a log — each update *replaces* the previous state (shipped history lives in `CHANGELOG.md` + `docs/deploy-log.md`; per-session narrative lives in the handoff docs). Updated during #lock by whoever is active (AC or Soren). Read this to orient on what's happening *now*.

**Do not edit this file without Justin's approval.** Propose changes; don't make them directly.

**Keep this file lean** (target ~50 lines of body). If a project needs more than a few lines, the detail belongs in that repo's own state file, build doc, or the active handoff — not here. When something ships, it moves *out* of here into the changelog; only live + in-flight + next-action state stays.

*Last updated: 2026-07-20 (AC0-24 — ③ verified fully ASSEMBLED on `ac2/step57-runtime-bookkeeping` [Mira pass ×2 + note-ordering fix + rebase onto `main`, all in-branch — worktree-confirmed]; the Mira-pass merge-blocker is CLEARED, and a full-load **Opus merge-review returned GO** (zero blockers). Remaining ③ chain = Justin's merge-go → pre-deploy sign-off → bundle deploy → Gate-B. Step 78 Half B deployed to Vercel earlier today; prod `v0.2.7`). Handoff: `docs/working/2026.07.20-ac0-ac0-handoff.md`. [AC0-25, 2026-07-21: ③ has since MERGED to `main` (`2d14c2b`) + pushed; prod still `v0.2.7`, deploy PAUSED on Justin's Anthropic key-split — AC2 owns deploy → #lock. Added the "Watch-list & Mark-triggers" section below.] [ACf1, 2026-07-21T17:06-07:00: team-runtime lane — L9 boot-schema guard MERGED (`815f117`, undeployed); item (b) is the live next build; emergency-notify-the-boot-refusal finding flagged for ACf. Light #lock: current-state only (CHANGELOG rides the deploy; SPRINT-tracker/ARCHITECTURE "L9→DONE" flip left to ACf, who's active in those).] [ACf, 2026-07-22T19:04-07:00: team-runtime #lock — item (b) shared-digest re-key MERGED (`567056a`) + pushed, undeployed; migration 016 unapplied + item-(b) DB layer UNVERIFIED-LIVE; item (c) next; new finding L12 (dormant→live resurrection — ACf's design call → item (e)). Team-runtime CHANGELOG entry added; ARCHITECTURE memory-section item-(b) update routed to Justin/Soren. Handoff: `docs/working/2026.07.21-acf-acf-handoff.md`.] [AC0-26, 2026-07-22 night: **③ (Step 57) DEPLOYED to prod as `v0.2.8`** (AC2, verified clean boot + real Anthropic call on the new key; ADR-053 key-split live). Updated the product-half below (What's Live → v0.2.8; ③ out of In-Flight). **Still owed on the ③ #lock (AC2's / Justin-auth):** SPRINT Step-57→DONE flip · ARCHITECTURE ③-absorption · operator-guide `correct_user_field` + spend-ceiling · BUILD marker flips · cross-runtime raw-write-surface check (→ ACf/Soren). All repos pushed + synced tonight.] [AC0-27, 2026-07-24: **prod bumped to `v0.2.9`** — AC2 shipped SPRINT 91 (the halt-resume gatekeeper-recognition Mark-blocker) 2026-07-23. Sprint Steps 57 & 91 flipped → DONE; **Step 94 minted** (WebFetch wall-clock timeout, owner AC2, from Lucas's #sweep). ③ #lock ARCH/BUILD/operator-guide doc-flips DONE (AC2 `5a27c3f`, Justin-vetted). Working Lucas's #sweep residuals.] [ACp, 2026-07-28T12:37-07:00: **AC permissions root-caused + fixed on the LAPTOP** — a drive-letter case split in `~/.claude.json` made every repo's `.claude/settings.json` allow-list inert (see CHANGELOG 2026-07-28). ⭐ **DESKTOP still broken — AC0 owns, ASAP** (runbook below). Also: all repos pulled, `evryn-team-runtime` cloned (was missing), stale `dc/r2-step32-84-comments` reaped.] [AC2, 2026-08-04T13:17-07:00: **SPRINT Step 94 (the hung-call guard) MERGED to `evryn-backend` `main` `241c16c`, UNDEPLOYED.** Justin's per-turn-watchdog instinct won on measured SDK evidence — the flat cap is superseded by a two-layer budget (**ADR-054**); two independent full-load QC passes returned GO / zero blockers; live-exercised on DEV. **5 unpushed commits on backend `main` (AC0 pushes)** + a stale `origin/dc/webfetch-timeout` owed a delete. Worktree + branch reaped. One check is gated to the `CLUSTERING_MODE` flip — see the In-Flight entry.]*

---

## What's Live in Prod

- **Evryn product = `v0.2.9`** (`evryn-backend` tag `v0.2.9` / `main` `6886417`), on Railway Oregon/us-west-2, deployed 2026-07-23. Clean boot; M1-protected + Healthchecks-monitored. **`v0.2.9` shipped the SPRINT-91 halt-resume gatekeeper-recognition fix (AC2 — one shared `resolveInboundSender`, ZERO migration), on top of `v0.2.8` (2026-07-22), which shipped ③ Runtime Bookkeeping (Step 57 — verdict-time user-creation, raw `supabase_upsert` removed, operator-only `correct_user_field`) + Step-78 Half A real `/health` + 54a/54c + the Anthropic key-split (ADR-053, dedicated Evryn-Production workspace/key) + dead-var cleanup. ZERO prod migration.** Full ledger: `docs/deploy-log.md`.
- **Two big cost levers stay DORMANT on `v0.2.9`** (Clustering, Step 58/ADR-049 · Haiku pre-screen, Step 44). Both env-gated; `config.ts` treats *unset* identically to `off`. On the running `v0.2.9` deployment `CLUSTERING_MODE` and `HAIKU_SCREEN_MODE` are both **UNSET** → each runs as the safe no-op `off` (③ did not touch the lever code). Flipping them on is a deliberate pre-Mark go-live action; zero prod behavior change until then.
- **⚠️ `evryn-backend` `main` is now AHEAD of the deployed `v0.2.9`** — `main` = `241c16c`, **5 commits unpushed** (AC0 pushes). It carries **SPRINT Step 94** (the two-layer hung-call guard, ADR-054 — merged 2026-08-04, ZERO migration) plus Step 96's sprint entry. Prod still runs **`v0.2.9`**; Step 94 takes effect on the next `railway up`. The `dc/webfetch-timeout` branch + worktree are **reaped**; ⚠️ the **stale pre-rebase remote branch `origin/dc/webfetch-timeout` (`bf23bca`) still exists and should be deleted on the next push** — its commits are superseded and already on `main`.
- **Team runtime (`evryn-team-runtime`) is deployed to Railway but PAUSED / boot-paused by construction** — dormant, wakes nobody. Autonomy stays OFF until the Meta-Meeting. Evryn prod is untouched by all team-runtime work.

## In Flight — Product Wave (critical path to Mark-live)

*(③ Runtime Bookkeeping SHIPPED `v0.2.8`; SPRINT 91 halt-resume Mark-blocker SHIPPED `v0.2.9` (2026-07-23). ③ #lock doc-flips DONE: ARCHITECTURE ③-absorption + BUILD + operator-guide committed (AC2 `5a27c3f`, Justin-vetted); SPRINT Steps 57 & 91 → DONE (AC0-27, 2026-07-24).)*

- **Critical path now: ③ #lock finish → Gate-B → flip Clustering + Haiku ON → gatekeeper.** **Gate-A** stays co-signed GO (13/13, Megan fictional gatekeeper on `main`); **Gate-B** (SPRINT Step 88B — mandatory pre-Mark full create-from-zero staging live-fire, everything active together) is the remaining gate, fires after Justin flips `SEND_ENABLED` — a FRESH dedicated reviewer owns the verdict, AC3 supports staging mechanics, AC0 gates.
- **WebFetch-timeout fast-follow (SPRINT Step 94, AC2) — ✅ MERGED to `main` `241c16c` 2026-08-04, UNDEPLOYED (rides the next `railway up`; Justin: "ride next").** The ONE in-flight code delta. **Justin's design question is RESOLVED in favour of his instinct**, on measured evidence: the flat `EVRYN_QUERY_TIMEOUT_MS` cap is replaced by a **two-layer budget** — a per-turn inter-message **silence watchdog** (`EVRYN_QUERY_SILENCE_TIMEOUT_MS`, 120s, primary) plus a generous **absolute ceiling** (`EVRYN_QUERY_TIMEOUT_MS`, default raised 180s→600s, per-call overridable; Reflection passes `REFLECTION_QUERY_TIMEOUT_MS`=900s), enabled by `includePartialMessages: true`. Strictly better than the flat cap on both axes — catches a real hang in 2 min instead of 3, and no longer cuts off legitimate long work, which **dissolves NB-2** (`reflection.md`'s "never a hard cap"). **ADR-054** written + committed (`8f9a684`). Branch `dc/webfetch-timeout` **rebased onto `main`** (it was 38 behind, not the "~23" this file used to say) and is now **4 commits** ahead, tip `c513c03`; `main..branch` is clean (the old stale-base illusion is gone). Two independent full-load QC passes: **GO, zero blockers**; QC caught a real nested-note-turn defect (fixed) and a test that couldn't fail (fixed + mutation-checked). **Still owed before merge:** Justin's SCM-vet of the uncommitted `operator-guide.md` env/alert rows (they are NOT on the branch, so a merge alone won't carry them). **Owed before the `CLUSTERING_MODE` flip (NOT before merge):** a live exercise of the nested note-turn liveness relay — attempted twice on DEV, both inconclusive (verified from `llm_usage`: no inner query started), so it is gated to the supervised flip. Held Gate-A DEV state still **DO NOT WIPE** (+ new DEV debris listed in the handoff).
- **⭐ THE DEPLOY PLAN (AC0-28's call, Justin delegated deploy timing 2026-08-04) — ONE bundle, staging first, prod only after Gate-B passes.** Three things land on `main` first: the hung-call guard (✅ merged), the silent email-drop fix (AC5), the `classify.ts` extraction (ACa). Then **deploy that bundle to STAGING + flip `CLUSTERING_MODE` on there** → **run Gate-B against it** → fixes if any → **then ONE prod deploy of exactly what Gate-B validated.** Rationale: Gate-B's whole value is exercising the code a gatekeeper will actually run; three staggered prod deploys would leave it validating a version that no longer exists. **Prod stays `v0.2.9` until after Gate-B. ⚠️ DECISION POINT: if the extraction is not stable by Thursday AM, choose — slip Gate-B, or drop the extraction from the bundle.** Flag it Tuesday, not Thursday night.
- **SPRINT Step 96 (NEW 2026-08-04, AC5) — silent email drop in the ingest fan-out. Pre-gatekeeper P0.** An unbounded `Promise.all` at both Gmail fetch call sites swallows a failed fetch to `null` and filters it out, so it never reaches `planPollBatch`, `transientCount` stays 0, and the cursor advances past unread mail — no retry, no alert, no trace. Neither guard sees it. **Surfaced by Soren in passing; independently CONFIRMED at source by AC0-28**, who also corrected the severity: the ~1h cold-start-catchup margin is a partial, incidental backstop, so "permanently lost" was overstated — but it must not be leaned on.
- **The atlas/refactor lane (ACa, NEW 2026-08-04) — Justin's standing top priority, finally started.** Every AC pays a ~17,000-line runtime load before any work; `src/triage/classify.ts` is 2,606 lines of it. Four deliverables: regenerable dependency map → **claims audit** → Code Atlas → extraction. **⚠️ The audit is the load-bearing half.** Three proofs found in ONE session 2026-08-04 that this system asserts things about itself that are false: **(1) the 6-month `emailmgr_items` TTL does not exist** — not in `src/`, not as a DB job — **yet two shipped code comments reason FROM it**; (2) **"the sole Anthropic spend door"** is asserted in three runtime files + ARCHITECTURE and is false (`runHaikuScreen` → `defaultRunQuery` calls `query()` directly, so Haiku spend cannot trip the M1 breaker — not a runaway hole, a safety-claim accuracy defect); (3) `schema-reference.md` misdescribes the live DB. **Justin: "who the fuck knows what else is missing" — that question IS the audit.** Also his call: **the TTL should be 1 year, not 6 months** (currently a doc-only change; building it is a separate deliberate trip).
- **Step 78 (liveness/health) — NOW FULLY LIVE.** Half A (real 503-when-wedged `/health`) shipped with `v0.2.8`; Half B (dashboard's 5 per-subsystem lights, Vercel) went live-inert 2026-07-20. ⭐ **POST-DEPLOY CHECK OWED: verify the Half-B dashboard health lights came alive** (they rendered idle "—" until Half A deployed — Half A is now live, so they should read real status).
- **SPRINT 91 — ✅ SHIPPED `v0.2.9` (AC2, 2026-07-23).** The halt-resume gatekeeper-recognition Mark-blocker is FIXED (one shared `resolveInboundSender` helper both `processEmail` + `resumeParkedItem` call). · **SPRINT 92 (AC0, now unblocked)** — `schema-reference.md` full re-pull (3 live tables missing; interim DO-NOT-TRUST banner up) + close the protocol-gap (re-pull fires on memory, needs a structural migration-time trigger).
- **~~AC4 = Weekend architecture capture~~ → SUPERSEDED, now the ACa lane (see above), spun 2026-08-04.** Renamed deliberately: "AC4" already pointed at a prior, CLOSED security-bulkhead lane, so the name meant two things. The work is unchanged — the backend god-module (`triage/classify.ts`, 2,606 lines) makes every session expensive — and Justin's ruling now sequences it: **extract BEFORE Gate-B, so Gate-B tests the real runtime.** Napkin (explicitly *not* settled, re-derive from first principles): `docs/working/2026.07.19-ac-justin-restructure-napkin.md`.

## In Flight — Team Runtime Wave (ACf lanes)

- **Memory + wake-economics architecture designed + ratified (ADR-052). Phase 1 building (ACf1) — 3 of ~5 build items merged.** Item (a) scratchpad+headline MERGED; L9 boot-schema guard MERGED (`815f117`); **item (b) — the shared-digest re-key (§4.1 v3, the `thread_digests` registry) — MERGED (`567056a`) + pushed 2026-07-22** (fresh-QC-GO, zero blockers). **⚠ Migration 016 UNAPPLIED + the whole item-(b) DB layer UNVERIFIED-LIVE** (logic/unit-tested only; live-proven at the Justin-gated 016-apply + Lucas paces). **Item (c) — full-thread-load + composed-load advisory + per-agent serialization — is the live next build**; then membership wake-model (d) / dormancy-write (e). Phase 2 approved-in-principle; scheduling routed to ACU. **Deploy DEFERRED (ACf1's rec, Justin decides):** don't deploy item (b) alone — batch the Phase-1 migrations (015+016+…) into ONE migrate-then-deploy window at a checkpoint, then run the Lucas paces. **Open findings for ACf:** (1) **`L12`** — dormant→live digest resurrection gap (a re-activated dormant thread never re-appears in the ambient digest; nothing lost; design call → item (e)); (2) emergency-notify on boot refusals (product's `#emergency-alerts`) → its own trip. Design-of-record: `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-team-runtime-memory-architecture.md`; tracker: `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md`.
- **Harness is built + merged + supervised-live-proven** (composition v2, Sunday controls, dashboard control tab live-inert). Still dormant/boot-paused; Lucas's 8 supervised paces are 4/8 done. Gates to live: remaining paces + Meta-Meeting.

## Up Next

*Post-Mark backlog (dormant until Mark is live — not active):* EVR-108 (ARCHITECTURE.md compression pass — *Blocked*) · EVR-109 (force-load/caching as v0.3+ identity architecture, ADR-012 amendment — *Needs Authorization*) · EVR-110 (per-agent git worktrees — *Backlog*).

## Watch-list & Mark-triggers

**What we're actively watching:** `evryn-team-workspace/shared/projects/product/watch-list.md` (owner: Soren / EVR-114) — every live experiment + standing monitor with its trigger, exit-criterion, and owner. **Several are Mark-gated and auto-kick-in the moment Mark forwards** — the Haiku pre-screen `off→shadow` flip (→ ~$1,800/mo once ~0 false-filter is proven → Justin flips `active`), the cache-temperature measurement (Step 69 — cold-vs-warm readable in ~2-3 days), and the M1 velocity-ceiling ratchet (Step 76d). The onboarding flips themselves live in the operator-guide Go-Live Checklist + `evryn-backend/docs/gatekeeper-onboarding.md`.

**v0.2 maintenance plan (Step 47 / EVR-114, Soren):** the watch-list is live; the fuller plan (deviation-alerting, judgment-quality loop, regression cadence) is *scoped* (`docs/working/2026.07.13-acu-ac0-step47-maintenance-plan-scoping.md`, awaiting Justin's decisions), not yet assembled into its plan doc.

## Active Instances

- **Product:** AC0 (conductor — merges / collisions / housekeeping)
 - **AC2 (hardening fast-follows — the post-③ queue).** ③ shipped `v0.2.8` and Step 91 `v0.2.9`; the lane is now the fast-follow queue in `docs/working/2026.07.22-ac0-ac2-fast-follow-brief.md`. **Step 94 merged 2026-08-04**; next unstarted = SPRINT 90 (reply-item doctrine, design-call-first), then Step 71 (Reflection binding net — NB: it sits in `SPRINT-v0.2-optionals.md`, not the main sprint), Steps 35b/35d (gatekeeper-recognition), Step 93 (operator `discard <id4>`).
 - **AC5 (NEW 2026-08-04) — the silent email-drop lane.** Single-purpose: SPRINT Step 96 (unbounded `Promise.all` in the Gmail fetch fan-out drops mail AND advances the cursor). Deliberately STIPULATED load (9 files, not 43). Lane closes on merge. Brief: `docs/working/2026.08.04-ac0-ac5-silent-email-drop-brief.md`.
 - **ACa (NEW 2026-08-04) — the atlas/refactor lane.** Dependency map → claims audit → Code Atlas → `classify.ts` extraction. Full load (it maps everything). Brief: `docs/working/2026.08.04-ac0-aca-atlas-refactor-brief.md`.
 - **A binding-safety-net lane — AUTHORIZED, NOT YET BRIEFED OR SPUN (AC0 owes it).** Justin, 2026-08-04.
 - AC3 (identity / ops) — cleanup batch briefed, NOT spun.
- **Team runtime:** ACf lanes
 - ACf1 Phase-1 memory build
- **Uber-sequencing:** ACU owns the week's Fable allocation + Phase-2 sequencing
- **Closed:** AC1, AC4, AC6, AC7.

## Infrastructure

- ⭐ **AC PERMISSIONS — FIXED on the laptop, STILL BROKEN on the DESKTOP. Owner: AC0, ASAP.** A drive-letter **case split** in `~/.claude.json` (`c:/…` vs `C:/…`) meant the VS Code extension read a `hasTrustDialogAccepted: false` entry while the CLI wrote `true` to the other — and an untrusted folder makes Claude Code **refuse to load that repo's `.claude/settings.json` at all**, so the allow-list was inert and Justin hand-authorized every network/cross-repo Bash call. **This does NOT arrive via `git pull`** (`~/.claude.json` is home-dir user config; only the in-repo `additionalDirectories` half travels). Runbook, with the two diagnostic traps that make this easy to misdiagnose: `docs/working/2026.07.28-acp-ac0-desktop-permissions-fix.md`.
- **⚠️ `evryn-backend` `main` has 5 UNPUSHED commits (AC0 owns the push, 2026-08-04)** — SPRINT Step 94 + Step 96's sprint entry. Also push-time: **delete the stale `origin/dc/webfetch-timeout`** (pre-rebase, superseded). `_evryn-meta` also has unpushed doc/ADR commits.
- **Railway: AUTO-DEPLOY OFF by design** (manual `railway up`; branch-agnostic). Live = `v0.2.9`. Env: `ANTHROPIC_API_KEY` now the dedicated **Evryn-Production** workspace key (ADR-053 key-split, 2026-07-22; agents NEVER touch the value); the two dead `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` vars removed; `PROACTIVE_CHECK_HOUR_PT=10`, `AFFIRM_HOUR_PT=7`, `POLL_INTERVAL_MS=10000` (test cadence, slow before Mark). Healthchecks.io heartbeat (~15min) + daily-affirmation (~26h) LIVE.
- **Supabase: on PRO** (Oregon/us-west-2). Projects: 
 - **prod** ("Evryn Product — West Coast")
 - **dev** (seeded mirror)
 - **staging** ("Evryn Product — Staging", home of the Gate-B live-fire; `SEND_ENABLED=false`)
 - **"Evryn Team"** (team-runtime DB). 
 - **Old East project DELETED 2026-07-03** (archive `backups/full-public-2026-06-03.sql`). 
 - Backups: Pro daily auto-backups + periodic `pg_dump`. *(A fresh archival `pg_dump` is ~owed — the last one crosses a week ~07-17.)*
- **Slack:** `#evryn-approvals` (pings post immediately) · `#emergency-alerts` (M1 Stage 2 detectors LIVE; money-runaway is protected structurally by the circuit-breaker halt, not a human-wake).

## Task Management

[Linear (EVR workspace)](https://linear.app/evryn) — discrete tasks tracked there, live v0.2 build work in `evryn-backend/docs/SPRINT-V0.2-HARDENING.md`.
**Notable *active* tickets**:
- EVR-114 = Soren meta-architecting.
- EVR-67 = v0.3 S1 hardening.
- EVR-103/104 = Meta-Meeting (team-autonomy gate).

---

## Strategic Pivot: Evryn Product First

Team agent build was paused to focus on building the Evryn product MVP (v0.2 "Gatekeeper's Inbox") for Mark (real-Mark = Mark Titus, Seattle filmmaker / August Island Pictures / Eva's Wild) first. Real-Mark "we're ready" email SENT 2026-06-11; **Mark is not yet forwarding** (prod idle until he does). Team agent build has since resumed.

## How AC Works Now

AC drives DC (build) and QC (review) as **subagents** — protocol `docs/protocols/ac-orchestration-protocol.md`. QC verifies every real code change; AC promotes QC's standing patterns into her CLAUDE.md. Ping-by-default on `#team-alerts`. ARCH + BUILD docs: AC holds edit rights, editing needs Justin's explicit auth first. Lane-owning ACs own their lane's design; AC0 owns cross-lane orchestration + collisions + merge gates.

**Load-list home = the cascade** (as of 2026-07-22): for any build, its self-sufficient full-load list is `docs/protocols/load-cascades/<build>-full-cascade.md`; *this* file is the **freshness-authority** the cascade cross-checks (which build/sprint is active *now*). Changelogs are **per-repo** — each repo's own `CHANGELOG.md` plus the meta one (`_evryn-meta/CHANGELOG.md`), which every AC skims on load.

## Recent #lock / handoff pointers

- 2026-07-22 night (AC0-26 — ③ DEPLOYED `v0.2.8` + all-repo push batten-down + current-state product-half update [uncommitted] + AC2-brief-bloat recommendation) — `docs/working/2026.07.22-ac0-ac0-handoff.md`
- 2026-07-20 (AC0-23 → AC0-24 — the split: AC0-23 finishes the ③ merge gate, AC0-24 runs the AC3-under-AC0 experiment + is the merge-takeover fallback) — `docs/working/2026.07.20-ac0-ac0-handoff.md`
- 2026-07-16 night (AC0 — Gate-A co-sign + Step-78 Half A merge + ACU radar) — `docs/working/2026.07.16-ac0-ac0-handoff.md`
- 2026-07-15 evening (ACf — team-runtime memory architecture + ADR-052 + Phase-1 start)
- 2026-07-13 (ACf — Sunday controls merged + dashboard tab deployed inert + composition v2 built)
- 2026-07-11 (ACf — team-runtime arc: ADR-050 + design v2 + new repo/infra + Phase-1 harness merged dormant)
- 2026-07-10 (AC0 — Clustering DEPLOYED `v0.2.7` ships-off + `declining.md` live)
- 2026-06-29 (AC0 — round-2 v0.2-hardening DEPLOYED `v0.2.2`)

*Last #sweep: 2026-04-04 (Lucas) · Last #align: 2026-04-04 (Lucas) — both >7 days, overdue.*

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
