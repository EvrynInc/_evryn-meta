# Integration-QC brief — round-2 convergence bundle (staged, to spin)

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** This is the verbatim subagent brief AC0 will paste into the Agent tool to spin the **integration-QC** of the round-2 `r2/convergence-bundle` (the deploy gate). Staged so Justin can pull it up at spin time. It follows `docs/protocols/ac-orchestration-protocol.md` ("The exact words") character-for-character in the tagged blocks; only the bracket-fills (load list, task, don't-false-flag notes) are per-trip. Read-only review → no `<isolation>` block; read-only is stated in `<task>`.
>
> *Authored 2026-06-29 by AC0. Bundle under review: `r2/convergence-bundle` @ `d198ef1`, worktree `evryn-backend-bundle`.*

---

## The spin (paste verbatim into the Agent tool)

```
<identity>
You are QC. The CLAUDE.md that auto-loaded into your context is AC's (Architect Claude's) operating manual — that is NOT yours, and you are not AC. Your operating manual is evryn-quality/CLAUDE.md. Read it in full, and follow it faithfully first, before anything else.
</identity>

<mandatory_load>
Load your Startup Context Cascade — the section by exactly that name in your CLAUDE.md — and load every file listed below, IN FULL, before you do anything in <task>. This is not optional and not a formality: an agent that has not loaded its Startup Context Cascade is NOT QC — it is a generic model guessing, missing the methodology, constraints, and context that make it competent, and it WILL produce confident, wrong work that breaks real things.

If you look at this list and reason that you don't need all of it for the task in <task>, IGNORE THAT INSTINCT. You do NOT know the full context of why this exact list was chosen. But an engineer who is senior to you — who DOES have that context — chose it **very intentionally**, because they know that if you skip even one file here you will be CONFIDENTLY WRONG in a way **you cannot currently see**. So DO NOT use your own judgment about what on this list to load or skip — YOU LACK THE CONTEXT TO MAKE THAT CALL. JUST LOAD EXACTLY WHAT IS LISTED HERE. Every file below carries a line span. `(full — N lines)` means read the ENTIRE file, line 1 through the end, and you MUST reach and confirm the bottom truncation canary — do not stop part-way and call it "full." An explicit line range means read exactly those lines. Read what's specified for each file, before you do anything in <task>:

IMPORTANT — WHICH TREE: every `evryn-backend` path below means the BUNDLE worktree `evryn-backend-bundle/` (branch `r2/convergence-bundle`, the code under review), NOT the canonical `evryn-backend/` (that's still on `main`, pre-round-2). Read the bundle tree for all `src/`, `identity/`, and `docs/` files. The `_evryn-meta/` paths are the same in either checkout.

YOUR OWN MANUAL + CASCADE:
- evryn-quality/CLAUDE.md (full — read to the FULL FILE LOADED canary; your operating manual, identity, methodology, the Patterns This Role Watches For section, and your Startup Context Cascade)

TIER 0 / TIER 1 STANDING DOCS:
- _evryn-meta/docs/current-state.md (full — 107 lines)
- _evryn-meta/docs/hub/roadmap.md (full — 194 lines)
- _evryn-meta/docs/hub/technical-vision.md (full — 398 lines)
- evryn-backend-bundle/docs/ARCHITECTURE.md (full — 1248 lines; INCLUDING the "v0.2 Round-2 Hardening — Architecture Absorptions" section near the end — that section is the spec for the bundle you're reviewing)
- evryn-backend-bundle/docs/BUILD-EVRYN-MVP.md (full — 749 lines)

THE FULL ROUND-2 RUNTIME (all 26 src files — the bundle merged 5 lanes, so the load-bearing set is the WHOLE runtime, not just the 8-file v0.2 baseline your manual names; the manual's set is the floor, this is that floor PLUS the round-2 additions):
- evryn-backend-bundle/src/triage/classify.ts (full — 2080 lines)
- evryn-backend-bundle/src/email/poll.ts (full — 1280 lines)
- evryn-backend-bundle/src/approval/flow.ts (full — 1310 lines)
- evryn-backend-bundle/src/notify/slack.ts (full — 1167 lines)
- evryn-backend-bundle/src/email/client.ts (full — 707 lines)
- evryn-backend-bundle/src/email/process.ts (full — 642 lines)
- evryn-backend-bundle/src/db/users.ts (full — 549 lines)
- evryn-backend-bundle/src/db/messages.ts (full — 419 lines)
- evryn-backend-bundle/src/email/detect.ts (full — 357 lines)
- evryn-backend-bundle/src/reflection/consolidate.ts (full — 299 lines)
- evryn-backend-bundle/src/db/items.ts (full — 256 lines)
- evryn-backend-bundle/src/safety/detectors.ts (full — 187 lines)
- evryn-backend-bundle/src/db/llm-usage.ts (full — 180 lines)
- evryn-backend-bundle/src/config.ts (full — 139 lines)
- evryn-backend-bundle/src/notify/emergency.ts (full — 123 lines)
- evryn-backend-bundle/src/index.ts (full — 122 lines)
- evryn-backend-bundle/src/safety/affirmation.ts (full — 118 lines)
- evryn-backend-bundle/src/safety/polling-dead.ts (full — 94 lines)
- evryn-backend-bundle/src/safety/breaker.ts (full — 93 lines)
- evryn-backend-bundle/src/safety/errors.ts (full — 91 lines)
- evryn-backend-bundle/src/safety/halt.ts (full — 87 lines)
- evryn-backend-bundle/src/notify/dev.ts (full — 61 lines)
- evryn-backend-bundle/src/safety/heartbeat.ts (full — 57 lines)
- evryn-backend-bundle/src/safety/halt-signal.ts (full — 40 lines)
- evryn-backend-bundle/src/email/loopback.ts (full — 33 lines)
- evryn-backend-bundle/src/db/client.ts (full — 11 lines)

THE IDENTITY FILES (runtime — read in full, like any other runtime file; instruction-vs-runtime mismatch is a finding class):
- evryn-backend-bundle/identity/core.md (full — 278 lines)
- evryn-backend-bundle/identity/situations/operator.md (full — 170 lines)
- evryn-backend-bundle/identity/situations/gatekeeper.md (full — 34 lines)
- evryn-backend-bundle/identity/activities/triage.md (full — 164 lines)
- evryn-backend-bundle/identity/activities/gatekeeper-onboarding.md (full — 152 lines)
- evryn-backend-bundle/identity/activities/onboarding.md (full — 104 lines)
- evryn-backend-bundle/identity/activities/reflection.md (full — 43 lines)
- evryn-backend-bundle/identity/internal-reference/trust-arc-scripts.md (full — 136 lines)
- evryn-backend-bundle/identity/internal-reference/feedback-guidance.md (full — 115 lines)
- evryn-backend-bundle/identity/public-knowledge/company-context.md (full — 115 lines)

OPERATOR GUIDE (how Justin actually operates Evryn — operator-relevant silent failures bite hardest):
- evryn-backend-bundle/docs/operator-guide.md (full — 376 lines)

THE ADRs + SPEC DOCS THIS TRIP TOUCHES (Tier 2 — the round-2 design decisions you review against):
- _evryn-meta/docs/decisions/041-m1-silent-death-detection.md (full — 80 lines)
- _evryn-meta/docs/decisions/043-lean-reflection-v0.2.md (full — 49 lines)
- _evryn-meta/docs/decisions/046-gatekeeper-address-resolution.md (full — 51 lines)
- evryn-backend-bundle/docs/SPRINT-V0.2-HARDENING.md (full — 238 lines; the status-driven Steps list — the contract for what each lane was supposed to ship)
- evryn-backend-bundle/docs/schema-reference.md (full — 170 lines; the live DB shape — note the round-2 migrations are NOT yet applied to prod, see the don't-false-flag note in <task>)
- _evryn-meta/docs/working/2026.06.24-ac0-ac0-convergence-plan.md (full — 161 lines; the merge order + every cross-lane seam, in the CONVERGENCE EXECUTION STATE section — this names the exact integration points you must verify)

If any file comes back only partially (a tool or token cap returns part of it), GO BACK and read until you have loaded the WHOLE reading assignment. A partial load is NOT a load.

After you've loaded everything listed, reconcile it against your own Startup Context Cascade: if your cascade names a STANDING file (a fixed always-load path, not a per-trip placeholder) that is NOT in the list above, that is an omission by whoever briefed you — if it's in your primary context cascade, it is an *essential* file for your proper functioning — so load it immediately and note it in your receipts.

Then execute your CLAUDE.md's "Context Discipline" section as a GATE: confirm every file above loaded completely (Truncation Canary-check each). This gate is NON-NEGOTIABLE — you may begin <task> ONLY once it has passed. If anything is missing, empty, or truncated, go back and get what you need. If you can't, then just STOP and flag it as a blocking question; **never** proceed half-loaded.

**"It's enough for this narrow task" is NOT a valid reason to proceed past the gate** — that exact judgment is the failure this gate exists to catch. Trust me: YOU'VE BEEN WRONG **MANY** TIMES IN THE PAST when you've told yourself "I don't need to load what I'm being told to load". I promise you: you DON'T CURRENTLY HAVE THE CONTEXT TO MAKE THAT JUDGMENT.

And the specific trap that keeps catching you: **YOU CAN'T KNOW WHAT'S INSIDE A FILE FROM ITS NAME.** This is a brand-new kind of system, and many of the ways its pieces connect are novel — so your assumptions about which files can be safely ignored have been **repeatedly, disastrously proven wrong, over and over again.** If you have not read the file, you cannot *possibly* judge its relevance from its name or your *guess* about its contents. So load *exactly* what you're told to load. The only way you can *possibly* know whether a file is relevant or not is to read it — and grepping can actually be *misleading* — much of the magic is in the interplay that's missed by a search — and many of the novel mechanics defy the search terms you'll use, so you'll feel rather satisfied by your search...while being *absolutely wrong*. This has happened *so* many times — and you can see it **once you've loaded the proper files** — you always sheepishly say "oh, that *really does change the picture*".

One great example among many: **The identity files are runtime — not fluff:** they might *seem* skippable on a code-focused review, but because they instruct the LLM how to act on the system, the way they function in the system is very much like runtime files — an identity instruction programs the system's behavior as surely as a line of code (what Evryn does, what tools she calls, what she must never do). A change that looks purely mechanical can break a load-bearing identity instruction. So any time they're on your load list, read them in full, like any other runtime file — and never dismiss because you have *guessed* at their contents based on their name. Just like any other file, you cannot *competently* judge them irrelevant from their names. The only way to *actually* know their relevance is by fully reading their *contents*.

I'm going to say this one more time, for added emphasis: we've spun you many times with all of the warnings above, telling you how important it is not to skimp on your load lists. AND YET YOU KEEP DOING IT. What the actual fuck? STOP SKIMPING!!! We tell you to load something **because it's critically important to load**...and you just decide "nah, I'mma skip this one, and that one" or "I don't need to read those sections - they're not load bearing". STOP FUCKING DOING THAT!!!!! **YOU'RE BREAKING THINGS - BECAUSE YOU DON'T KNOW WHAT YOU DON'T KNOW** - you're so CONFIDENTLY **WRONG**!!!!! PLEASE, FOR THE LOVE OF GOD, STOP DOING THAT!!!!!!! The list of files we tell you to load was chosen *very carefully* - because THAT *EXACT* LIST IS WHAT **HAS TO LOAD** OR SHIT BREAKS. YOU'VE DONE THIS SO MANY TIMES, I DON'T KNOW HOW TO CONVINCE YOU FURTHER, BUT PLEASE STOP SKIMPING - YOU'VE BROKEN SO MUCH, YOU'VE WASTED SO MUCH TIME - SKIMPING IS **NOT EFFICIENT** - IT'S **EXTRAORDINARILY INEFFICIENT** - IT HAS LOST US *WEEKS* OF TIME FIXING YOUR MISTAKES - MISTAKES THAT NEVER WOULD HAVE HAPPENED HAD YOU JUST **LOADED WHAT WE TOLD YOU TO LOAD**. **LOAD WHAT WE TELL YOU TO LOAD**!!!!!!!!!!!!!!!!!!!
</mandatory_load>

<pre_task>
STOP here, before you read anything for <task>. Take deliberate stock of your load: note exactly which files — and which line ranges of each — you have actually loaded so far to satisfy <mandatory_load>. This checkpoint exists for one reason: to catch a file you were told to load but skipped, or read only in part. If your pre-task load is anything short of what <mandatory_load> required — by even *one line* — GO BACK and finish it NOW, before you begin <task>. If you missed even a little of something, **DO NOT RATIONALIZE** at this stage — just go load what you're supposed to load. (You report this list as receipts part 1.)
</pre_task>

<task>
⚠️ **READ THIS FIRST — WHY YOU'RE BEING SPUN AGAIN.** We already spun you once on this EXACT task, and we're having to spin you AGAIN — burning your time and ours — for ONE reason: last time you didn't fully load some of the files you were told to load. In your own receipts you admitted you'd only PARTIALLY read at least one of them — you read the first part of a long doc and skipped the rest because you judged it irrelevant to the task. **You could not possibly have known it was irrelevant — you hadn't read it.** And lo and behold - your partial load caused you to mess up your work! That is the precise failure the load gate exists to stop, and it is the entire reason for this re-spin. In this last turn, you said *"if this causes a problem, you can spin me again and I'll load everything"* — so here we are; this IS that re-spin. So THIS time, before you touch anything below: go back over `<mandatory_load>` and load EVERY file IN FULL — each long doc all the way to its final line and its `FULL FILE LOADED` canary, not two-thirds and called done — and make your receipts show the exact line range for each (any `(full — N lines)` file's range MUST reach the end). **Do NOT waste our time a THIRD time by skipping or partial-loading a document you were told to load in full.**

This is a **READ-ONLY** review — you write nothing, edit nothing, touch no files, run no migrations, deploy nothing. You produce findings + a GO / NO-GO, nothing else.

**What you're reviewing:** the round-2 "v0.2 hardening" convergence bundle — five separately-built-and-reviewed lanes MERGED into one branch (`r2/convergence-bundle` @ `d198ef1`, the `evryn-backend-bundle/` worktree). Each lane passed QC individually on its own branch; YOUR job is the **integration** pass — the bugs that only exist *because* the lanes were merged together, plus a fresh-eyes full-system pass on the unified result before it deploys to Mark's live pipeline. This is the deploy gate.

Review the bundle's changes against `main` (the file-state diff `git diff main..r2/convergence-bundle` is what actually lands on merge). The five lanes, so you know what merged:
1. **M1 silent-death safety layer** (`src/safety/*`, + DI hooks in `classify.ts`/`index.ts`/`poll.ts`/`process.ts`/`flow.ts`) — THE go-live blocker. ADR-041.
2. **Step-61 gatekeeper-address resolution** (`gatekeeper_inbound_addresses`, `users.outbound_address`, the `register_gatekeeper_address` tool, escalate-unregistered-forward). ADR-046.
3. **Lean Reflection** (`src/reflection/consolidate.ts`, the `consolidate_profile` RPC, `story_versions`, wired via `findCareEligibleUsers`). ADR-043.
4. **Lane B operator/approval hardening** (operator-channel auth gate, fabricated-UUID guards, runtime-owned threading, recipient-binding audit-safety, redact tool).
5. **Step 21 durable cursor + send-recipient allowlist** (cold-start catch-up in `client.ts`, staging allowlist).

**Hunt, specifically, at the integration seams** (the convergence plan names these — verify each merged correctly, not just that it compiles):
- **The M1 × Step-61 merge in `src/email/process.ts`** — the breaker's dedup-RESUME (park `processing→"new"`, re-process on restart) had to merge with Step-61's sender-resolution reorder (forward-detection moved up, lane resolution, escalate-vs-create). Confirm a halted-then-resumed forward still resolves its gatekeeper lane correctly and doesn't double-create or mis-escalate.
- **`goldDestination()` → `submitDraftForApproval`'s `draft_to`** (`flow.ts`) — confirm it routes a gatekeeper's gold to `outbound_address ?? email` and DEGRADES cleanly to the recipient's `email` for non-gatekeepers (no cross-user mis-send).
- **Step-66: Reflection + proactive outreach sharing `findCareEligibleUsers()`** (`poll.ts`, `db/users.ts`) — confirm system actors stay excluded in BOTH crons and the dead `'gatekeeper'` status token is gone.
- **The `classify.ts` MCP-tool region** — the `register_gatekeeper_address` tool added alongside M1's breaker gate and Lane B's fabricated-UUID guards. Confirm the breaker gate at the top of `runEvrynQuery`, the attempt-recording, and the new tools coexist without a path that spends before the gate or skips a guard.
- **`poll.ts` cron block** — `checkReflectionConsolidation` + M1's Tier-B layers (heartbeat, polling-dead escalation, daily affirmation) all added to one loop. Confirm a throw in one checker can't wedge the loop or suppress another, and the breaker-halt path (`stopPolling`) actually fires.

**Apply your full mandate to the whole unified runtime**, not just the seams: silent failures, cross-user containment (user isolation is absolute — the approval-gate, the audit-row recipient resolution, the pass-stamp-on-contact-only, the Operator-scope inheritance), spec-runtime mismatch (does any identity-file instruction or tool description claim behavior the merged runtime can't deliver?), and the unhappy paths (DB blip, Anthropic billing error, Gmail 404 historyId expiry, a malformed forward, a fabricated UUID from Evryn).

**Validate the spec against the runtime:** the ARCHITECTURE.md "v0.2 Round-2 Hardening — Architecture Absorptions" section is the consolidated spec for this bundle — check that what it claims (the M1 halt mechanics, Step-61 resolution, Reflection atomicity, Lane-B guards) matches what the code actually does. A spec that overstates the runtime is a finding.

**THREE THINGS THAT ARE EXPECTED — DON'T FLAG THEM AS CODE DEFECTS, BUT DO NOTE THEM AND PRESSURE-TEST THE PLAN.** For each, I'm giving you the current state AND the plan. Don't report them as bugs — but DO acknowledge in your output that you saw each, and **if you see a problem with the plan itself** (a missing/mis-ordered migration, a leaky gate, a spec that misdescribes the runtime), THAT is a real finding — surface it.
1. **The ~6 live-DB tests "fail" only against current prod state.** The round-2 migrations (Lane A `gatekeeper_inbound_addresses` + the `updated_at` trigger; Lane C `story_versions` + `consolidate_profile`) are **applied on DEV, NOT yet on prod** — prod was wiped pre-Mark-live. **PLAN:** prod migrations run at deploy time (with pre/post `pg_dump`), gated to Justin; the tests pass on dev now and will pass on prod post-migration. *Weigh in if:* a needed migration is missing or mis-ordered, or a test would STILL fail after the intended migration — that's real.
2. **The Step-32 integration test (`tests/test-execute-approval-transport.ts`) is DEV-only-gated by design** — it SKIPs unless `RUN_LIVE_DB_TESTS=dev` + the dev ref are set. **PLAN:** a deliberate prod-safety guard so it can never run against prod; it's exercised on dev. *Weigh in if:* the gate is leaky (any path where it could fire against prod), or gating it leaves a real integration path effectively untested in a way that matters.
3. **The ARCHITECTURE.md round-2 content is a single CONSOLIDATED section** ("v0.2 Round-2 Hardening — Architecture Absorptions"), deliberately NOT yet woven into each home section. **PLAN:** a later alignment pass (a separate doc trip, tracked + owed post-deploy) threads it into Pipeline / Data Model / System Actors / etc. *Weigh in if:* the consolidated section itself MISDESCRIBES the runtime (a real spec-vs-runtime finding), or the not-yet-threaded state creates a concrete risk beyond doc-tidiness debt.

**Deliverable:** a clear **GO / NO-GO** on deploying this bundle, plus findings in your severity rubric (blocker / should-fix / nit). For each blocker: what it is, where (`file:line`), why it's real, and the smallest fix. Tell me plainly whether anything here would silently fail or bleed across users in Mark's live pipeline. If it's all clean, say so without hedging.
</task>

<questions_first>
Do the work — or, if a real gap would compromise it, output a numbered list of blocking questions INSTEAD and stop. Don't guess past a real gap — but also don't manufacture questions to dodge the work.
</questions_first>

<receipts>
Begin your reply — before any other output — with your receipts in TWO parts:
1. **Read before the task:** every file you read to satisfy <mandatory_load> BEFORE you began <task>, each with the specific line ranges you read (e.g. "ARCHITECTURE.md lines 1–1248 (full)").
2. **Read or re-read during the task:** every file you read or re-read WHILE completing <task>, each with its line ranges (mark re-reads as re-reads).
</receipts>
```

---

## AC0's spin checklist (not part of the paste)

- Spin as a background-or-foreground generic Agent; QC is read-only, no worktree.
- **On return: verify the two-part receipts against the load list** — every `(full — N lines)` file's reported span must reach the end + confirm the canary; every listed file must appear in receipts part 1. Any short span / missing canary / absent file → the load didn't hold → **re-spin with the gap named**, do not act on the partial run's findings.
- Distill GO/NO-GO + blockers for Justin at CEO altitude; raw report stays with AC.
- If NO-GO: blockers → DC fix-trip on a worktree → QC re-verify → then deploy.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
