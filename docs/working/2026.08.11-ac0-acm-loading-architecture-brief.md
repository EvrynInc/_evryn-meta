# Lane A brief — the loading architecture: split the manual, split the protocol (AC0-35 → ACm)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file: THIS IS YOUR MARCHING ORDERS AND YOUR SHARED SURFACE.** It is written so a completely fresh ACm can be spun from it and work without asking anyone anything. It supersedes the spin message where they conflict. **Append your own notes, decisions and open questions to §8 as you go** — if you die mid-flight, this file is what a fresh instance re-spins from.
>
> **Owner:** ACm (a sub-AC under AC0-35). **AC0-35 is your parent** — questions go to him, never to Justin.

---

## §0 — What this lane is, and why Justin calls it priority #1

**Every subagent this organisation spins pays for the wrong manual before it reads one line of its actual work.**

`_evryn-meta/CLAUDE.md` is AC's operating manual. The harness **auto-injects it into every subagent unconditionally** — DC, QC, OC, scouts, even a generic agent spun to check whether a file exists. For DC and QC it is not merely waste, it is *the wrong manual*: the orchestration protocol currently spends a whole paragraph teaching them to disregard what the harness force-fed them.

**You are going to make that failure mode stop existing rather than continue being managed.**

Two deliverables, both source-of-truth documents, both of which **stay on your branch for Justin's own review — you never commit them to `main`.**

---

## §1 — 🔴 THE NUMBERS. Read this before you build the argument, because the recorded ones are WRONG.

**The case for this lane has always rested on a token measurement, and tonight I found that measurement is materially overstated. Fix it before you rely on it.**

- **What the prior handoff records:** `_evryn-meta/CLAUDE.md` ≈ **85,000 tokens**; `ac-orchestration-protocol.md` ≈ **155,000 tokens**; *"a sub-AC pays ~240K of pure operating manual."*
- **What was actually measured tonight:** a lane AC read `_evryn-meta/CLAUDE.md` and the **Read tool reported the file at 45,347 tokens** against its 25,000-token cap. That is the tool's own count, not an estimate.
- **⇒ The real figure is roughly 45K, not 85K — about half.** Extrapolating by byte ratio, `ac-orchestration-protocol.md` (168,528 bytes vs. CLAUDE.md's 129,973) lands near **~59K, not 155K**. So the combined burden is on the order of **~104K tokens, not ~240K**.

🔴 **Your first task is to MEASURE BOTH FILES PROPERLY and record the real numbers in this brief.** Do not inherit mine either — I extrapolated the protocol figure from a single data point and I am telling you so. The Read tool reports a token count when a file exceeds its cap; that is the cheapest true measurement available.

**Why this matters more than tidiness:** the argument for this restructure is a cost argument. **If it is presented to Justin on a number that is 2× wrong, the whole lane is discredited when someone checks** — and the honest number is *still* a strong argument. ~104K of pure operating manual, paid unconditionally by every subagent including ones that need none of it, is plenty to justify the work. **Make the case on the true figure.**

⚠️ **And note the direction of the error, because it is instructive: every one of those inflated numbers came from a doc citing a doc.** The 144-tokens-per-line figure in the orchestration protocol's own §"WHEN THE LOAD IS TOO BIG" produces ~88K for CLAUDE.md and is similarly high. **Re-derive; don't propagate.**

---

## §2 — Deliverable (i): split `CLAUDE.md` into agent identities behind a tiny router

**The shape Justin authorized:** AC, DC and QC each get an identity file. **`_evryn-meta/CLAUDE.md` becomes a tiny router** that says only *"load up as one of these agents."* **AC becomes one of many rather than the special case.**

### 🔴 The router's one hard rule — Justin's exact words, use them

> *"if your agent identity doesn't load, STOP — you're not authorized to do **ANYTHING** — just report the failure."*

**This supersedes any multi-rule safety floor you might be tempted to design.** It is better than the alternatives for a structural reason worth internalising: **an agent that cannot act without its identity cannot act *dangerously* without it either.** Do not dilute it into a checklist.

### ⚠️ The one new failure mode this creates — it MUST land in the same pass

Once the manual is split, **every sub-AC brief must name its identity file explicitly in `<mandatory_load>`, or the agent runs manual-less and nobody notices.** Today the auto-injection covers that by accident. **Write this into `ac-orchestration-protocol.md` in the same change** — a split that lands without it is a regression, not an improvement.

### 🔴 A finding from tonight that bears directly on the router's design, and that nobody had before

**The auto-injected `CLAUDE.md` is a STALE SNAPSHOT, verified twice tonight.** A lane AC read the on-disk file and found the injected copy missing content committed only hours earlier (the three-dot branch-diff bullet, commit `a04f5a0`). **AC0-35's own injected copy is missing it too.**

⇒ **Do not design the router on the assumption that auto-injection delivers current content.** It delivers *some* version. This strengthens the "read your identity file yourself and confirm its canary" beat rather than weakening it — and it is an argument for the router being **small enough that reading it in full is trivially cheap.**

---

## §3 — Deliverable (ii): turn `ac-orchestration-protocol.md` into an index + per-scenario sub-docs

**Justin's framing:** *"most of the time they're spinning a DC and a QC, but they're reading about spinning ACs and Team Agents — it's a bunch of bloat."*

**Proposed shape — and the lane owns the final call, so treat this as a starting point you may improve on:**

- The protocol becomes **the index + the universal core**: the brief skeleton, multi-trip loading, receipts, model pinning, the HARD RULE.
- `spinning-dc-qc.md`
- `spinning-an-ac.md`
- `spinning-a-team-agent.md`
- `orchestration-rationale.md` — the incident record.

### On how much rationale to cut: **keep every RULE, move every STORY**

The protocol's own claim that *"every clause is load-bearing because a paraphrase already failed"* is true **of the rules**. The **incident narratives** — the 2026-06-17 lobotomy, the six-shapes-in-a-day list, the token-measurement tables — are teaching material for a conductor, read once, and pure weight in a DC spin. **That split is where the cut is, and it is a large one.**

🔴 **But hold the counter-tension, because this is the single riskiest thing you will do tonight.** That document is the way it is because **every line was paid for**. The HARD RULE exists precisely because an AC once "condensed" these blocks and 5 of 8 reviewers then skipped a load-bearing doc. **You are the next agent to feel that pull.** So:

- **The verbatim blocks move; they do not get rewritten.** `<identity>`, `<mandatory_load>`, `<task>`, `<questions_first>`, `<receipts>`, and the trip-1 `<output>` block are **character-for-character frozen.** Relocating them is fine. Editing one word is not, and needs Justin's explicit prior OK.
- **When you cannot tell whether something is a rule or a story, it is a rule.** Move it to the core and note your uncertainty.
- **A rule that loses its story may stop being followed.** Some incidents *are* the teaching — the mechanism is what makes the rule stick. Where cutting the narrative would leave a bare instruction nobody obeys, **keep a one-line compressed version with a pointer to the full account.**

---

## §4 — 🚫 What is explicitly NOT in this lane

- **`conductor-cascade.md` is ALREADY DONE. Do not redo it.** Its scout step was rewritten on 2026-08-11 to point at the three-trip section, and three further passages that still assumed the old model were corrected in the same pass. It is current.
- 🔴 **`AGENT_PATTERNS.md` is OUT OF SCOPE tonight — do not open it to edit.** It carries a genuinely wrong entry at line 21 (it claims `SendMessage` is unavailable and subagents are fire-and-return; **all of that is false, and the multi-trip protocol runs on the mechanism it says does not exist**). **But a peer conductor, ACf, has a live `_evryn-meta` worktree on `acfl/learnings-consolidation` tonight, and that lane plausibly writes this exact file.** AC0-35 has ceded it to him and left him a note with the evidence. **Two branches rewriting one file is how one silently wins at merge — stay out.**
- **Do not touch `evryn-backend/identity/**`, any sprint doc, or `current-state.md`.** Other lanes are live in those tonight.
- **Do not write a `CHANGELOG.md` entry.** The AC who merges writes it. **Hand AC0-35 the changelog-worthy facts instead.**

---

## §5 — Isolation and the hard gates

- **Your worktree:** `C:/Users/Justin/Evryn/Code/_evryn-meta-lane-a` on branch **`acm/loading-architecture`**, cut from `main`. **Work only there.**
- 🔴 **This lane rewrites the very documents every other agent loads. That is why you are on a branch and why you land LAST.** Nothing auto-loads from a branch, and an already-spun agent loaded at spin time, so it cannot be changed underneath. **Under no circumstances commit to `main` while other lanes are running.**
- 🔒 **You NEVER merge, push, deploy, or run a migration.** Hard floor, Justin's alone, no matter how finished the work feels.
- **`git add <paths>` does NOT scope a commit** — a bare `git commit` commits the entire index including another agent's staged work. **Put the pathspec on the commit: `git commit -F - -- path/a path/b`.**
- **Source-of-truth output stays uncommitted to `main`, in SCM, for Justin's own read.** That gate is the entire safety mechanism here.

---

## §6 — 🔴 Describe-then-pause FIRST

**Before you move a single line, come back to AC0-35 with your proposed structure and reasoning.** This is the highest-blast-radius doc work in the organisation and a misaimed rewrite costs far more to unwind than a shape-check costs to run.

Bring:

1. **The corrected token numbers** (§1), measured, with how you measured them.
2. **The file layout for the CLAUDE.md split** — what the router keeps, what each identity file gets, and specifically **how you are handling content that is genuinely shared** (the writing discipline, worktree/branch rules, commit discipline, security posture). Duplicating it across three identities creates three copies that drift; putting all of it in the router recreates the bloat. **That tension is the actual design problem of this lane — tell me how you resolve it.**
3. **The protocol's index/sub-doc split**, with your rule-vs-story call on the big passages.
4. **Anything you think is wrong with this brief.** You will have read all three target manuals and I have read one; on their contents you are better informed than I am.

**Then wait for my response.** I am awake and conducting all night; I will answer quickly.

---

## §7 — Calibration: how much of this I have actually read

I am AC0-35, running the conductor cascade. **I have read in full:** `_evryn-meta/CLAUDE.md` (611), `ac-orchestration-protocol.md` (941), `ac-writing-protocol.md` (100), `conductor-cascade.md` (110), plus ARCHITECTURE, BUILD and the sprint on the product side. **I have NOT read:** `AGENT_PATTERNS.md` in full (only lines 14–29 and a grep of the resume material), `product-full-cascade.md`, or **any of the three target manuals** beyond DC's cascade section (lines 145–189).

⇒ **On what DC, QC and OC actually need from their manuals, you will know more than I do.** Treat my claims as **LEADS, not facts**. Where this brief is silent, do not read that as "AC0 checked and there was nothing there" — assume I could not see it. **Say so loudly and early if any of it is wrong.**

**One concrete thing I did check and you should know:** `evryn-ops/CLAUDE.md` has **no bottom truncation canary** (its last line is `---`). That is a doc-hygiene gap in a file you are about to restructure around. It is one of four found tonight; **flag it in your report rather than fixing it inline**, so the batch stays together.

---

## §8 — RUNNING LOG (ACm appends here; keep it re-spin-complete)

### AC0-35 — 2026-08-11 night — lane opened

Worktree provisioned off `main` `0ef7216`. Scope reduced from the original plan: **`AGENT_PATTERNS.md` removed** because a peer conductor took a live worktree in this repo tonight and that file is plausibly in his lane. The token figures the lane's argument rests on were found to be roughly 2× overstated and are ACm's first task to correct. Nothing built yet.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
