# Lane A brief — the loading architecture: split the manual, split the protocol (AC0-35 → ACm)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file: THIS IS YOUR MARCHING ORDERS AND YOUR SHARED SURFACE.** It is written so a completely fresh ACm can be spun from it and work without asking anyone anything. It supersedes the spin message where they conflict. **Append your own notes, decisions and open questions to §8 as you go** — if you die mid-flight, this file is what a fresh instance re-spins from.
>
> **Owner:** ACm (a sub-AC under AC0-35). **AC0-35 is your parent** — questions go to him, never to Justin.

---

## §0 — What this lane is, and why Justin calls it priority #1

> ✅ **STATUS: THE PROBLEM DESCRIBED BELOW IS FIXED ON THIS BRANCH (Phase A, 2026-08-12). The paragraph is kept in the PAST TENSE as the lane's problem statement — do not read it as current state.**

**Every subagent this organisation spun used to pay for the wrong manual before it read one line of its actual work.**

`_evryn-meta/CLAUDE.md` **was** AC's operating manual, and the harness auto-injects that path into every subagent unconditionally — DC, QC, OC, scouts, even a generic agent spun to check whether a file exists. For DC and QC it was not merely waste, it was *the wrong manual*, and the orchestration protocol spent a whole paragraph teaching them to disregard what the harness had force-fed them.

**⇒ That is over. `_evryn-meta/CLAUDE.md` is now a ~76-line router** naming each agent's manual; the ~45,000-token AC-manual injection is gone. ⚠️ **The half that replaced it: nothing auto-supplies ANY manual now, so a brief that omits the agent's own manual leaves it holding a routing table and nothing else.**

Two deliverables, both source-of-truth documents, both of which **stay on your branch for Justin's own review — you never commit them to `main`.**

---

## §1 — 🔴 THE NUMBERS. Read this before you build the argument, because the recorded ones are WRONG.

**The case for this lane has always rested on a token measurement, and tonight I found that measurement is materially overstated. Fix it before you rely on it.**

- **What the prior handoff records:** `_evryn-meta/CLAUDE.md` ≈ **85,000 tokens**; `ac-orchestration-protocol.md` ≈ **155,000 tokens**; *"a sub-AC pays ~240K of pure operating manual."*
- **What was actually measured tonight:** a lane AC read `_evryn-meta/CLAUDE.md` and the **Read tool reported the file at 45,347 tokens** against its 25,000-token cap. That is the tool's own count, not an estimate.
- **⇒ The real figure is roughly 45K, not 85K — about half.** Extrapolating by byte ratio, `ac-orchestration-protocol.md` (168,528 bytes vs. CLAUDE.md's 129,973) lands near **~59K, not 155K**. So the combined burden is on the order of **~104K tokens, not ~240K**.

🔴 **Your first task is to MEASURE BOTH FILES PROPERLY and record the real numbers in this brief.** Do not inherit mine either — I extrapolated the protocol figure from a single data point and I am telling you so. The Read tool reports a token count when a file exceeds its cap; that is the cheapest true measurement available.

**Why this matters more than tidiness:** the argument for this restructure is a cost argument. **If it is presented to Justin on a number that is 2× wrong, the whole lane is discredited when someone checks** — and the honest number is *still* a strong argument. ~104K of pure operating manual, paid unconditionally by every subagent including ones that need none of it, is plenty to justify the work. **Make the case on the true figure.**

⚠️ **And note the direction of the error, because it is instructive: every one of those inflated numbers came from a doc citing a doc.** The 144-tokens-per-line figure in the orchestration protocol's own §"WHEN THE LOAD IS TOO BIG" produces ~88K for CLAUDE.md and is similarly high. **Re-derive; don't propagate.**

---

## §2 — Deliverable (i): split `CLAUDE.md` into agent identities behind a tiny router

**The shape Justin authorized:** AC, DC and QC each get an identity file. **`_evryn-meta/CLAUDE.md` becomes a tiny router** that says only *"load up as one of these agents."* **AC becomes one of many rather than the special case.**

### 🔴 The router's one hard rule — Justin's exact words, use them

> *"if your agent identity doesn't load, STOP — you're not authorized to do **ANYTHING** — just report the failure."*

**This supersedes any multi-rule safety floor you might be tempted to design.** It is better than the alternatives for a structural reason worth internalising: **an agent that cannot act without its identity cannot act *dangerously* without it either.** Do not dilute it into a checklist.

### ⚠️ The one new failure mode this creates — it MUST land in the same pass

Once the manual is split, **every sub-AC brief must name its identity file explicitly in `<mandatory_load>`, or the agent runs manual-less and nobody notices.** Today the auto-injection covers that by accident. **Write this into `ac-orchestration-protocol.md` in the same change** — a split that lands without it is a regression, not an improvement.

### 🔴 A finding from tonight that bears directly on the router's design, and that nobody had before

**The auto-injected `CLAUDE.md` is a STALE SNAPSHOT, verified twice tonight.** A lane AC read the on-disk file and found the injected copy missing content committed only hours earlier (the three-dot branch-diff bullet, commit `a04f5a0`). **AC0-35's own injected copy is missing it too.**

⇒ **Do not design the router on the assumption that auto-injection delivers current content.** It delivers *some* version. This strengthens the "read your identity file yourself and confirm its canary" beat rather than weakening it — and it is an argument for the router being **small enough that reading it in full is trivially cheap.**

---

## §3 — Deliverable (ii): turn `ac-orchestration-protocol.md` into an index + per-scenario sub-docs

**Justin's framing:** *"most of the time they're spinning a DC and a QC, but they're reading about spinning ACs and Team Agents — it's a bunch of bloat."*

**Proposed shape — and the lane owns the final call, so treat this as a starting point you may improve on:**

- The protocol becomes **the index + the universal core**: the brief skeleton, multi-trip loading, receipts, model pinning, the HARD RULE.
- `spinning-dc-qc.md`
- `spinning-an-ac.md`
- `spinning-a-team-agent.md`
- `orchestration-rationale.md` — the incident record.

### On how much rationale to cut: **keep every RULE, move every STORY**

The protocol's own claim that *"every clause is load-bearing because a paraphrase already failed"* is true **of the rules**. The **incident narratives** — the 2026-06-17 lobotomy, the six-shapes-in-a-day list, the token-measurement tables — are teaching material for a conductor, read once, and pure weight in a DC spin. **That split is where the cut is, and it is a large one.**

🔴 **But hold the counter-tension, because this is the single riskiest thing you will do tonight.** That document is the way it is because **every line was paid for**. The HARD RULE exists precisely because an AC once "condensed" these blocks and 5 of 8 reviewers then skipped a load-bearing doc. **You are the next agent to feel that pull.** So:

- **The verbatim blocks move; they do not get rewritten.** `<identity>`, `<mandatory_load>`, `<task>`, `<questions_first>`, `<receipts>`, and the trip-1 `<output>` block are **character-for-character frozen.** Relocating them is fine. Editing one word is not, and needs Justin's explicit prior OK.
- **When you cannot tell whether something is a rule or a story, it is a rule.** Move it to the core and note your uncertainty.
- **A rule that loses its story may stop being followed.** Some incidents *are* the teaching — the mechanism is what makes the rule stick. Where cutting the narrative would leave a bare instruction nobody obeys, **keep a one-line compressed version with a pointer to the full account.**

---

## §4 — 🚫 What is explicitly NOT in this lane

- **`conductor-cascade.md` is ALREADY DONE. Do not redo it.** Its scout step was rewritten on 2026-08-11 to point at the three-trip section, and three further passages that still assumed the old model were corrected in the same pass. It is current.
- 🔴 **`AGENT_PATTERNS.md` is OUT OF SCOPE tonight — do not open it to edit.** It carries a genuinely wrong entry at line 21 (it claims `SendMessage` is unavailable and subagents are fire-and-return; **all of that is false, and the multi-trip protocol runs on the mechanism it says does not exist**). **But a peer conductor, ACf, has a live `_evryn-meta` worktree on `acfl/learnings-consolidation` tonight, and that lane plausibly writes this exact file.** AC0-35 has ceded it to him and left him a note with the evidence. **Two branches rewriting one file is how one silently wins at merge — stay out.**
- **Do not touch `evryn-backend/identity/**`, any sprint doc, or `current-state.md`.** Other lanes are live in those tonight.
- **Do not write a `CHANGELOG.md` entry.** The AC who merges writes it. **Hand AC0-35 the changelog-worthy facts instead.**

---

## §5 — Isolation and the hard gates

- **Your worktree:** `C:/Users/Justin/Evryn/Code/_evryn-meta-lane-a` on branch **`acm/loading-architecture`**, cut from `main`. **Work only there.**
- 🔴 **This lane rewrites the very documents every other agent loads. That is why you are on a branch and why you land LAST.** Nothing auto-loads from a branch, and an already-spun agent loaded at spin time, so it cannot be changed underneath. **Under no circumstances commit to `main` while other lanes are running.**
- 🔒 **You NEVER merge, push, deploy, or run a migration.** Hard floor, Justin's alone, no matter how finished the work feels.
- **`git add <paths>` does NOT scope a commit** — a bare `git commit` commits the entire index including another agent's staged work. **Put the pathspec on the commit: `git commit -F - -- path/a path/b`.**
- **Source-of-truth output stays uncommitted to `main`, in SCM, for Justin's own read.** That gate is the entire safety mechanism here.

---

## §6 — 🔴 Describe-then-pause FIRST

**Before you move a single line, come back to AC0-35 with your proposed structure and reasoning.** This is the highest-blast-radius doc work in the organisation and a misaimed rewrite costs far more to unwind than a shape-check costs to run.

Bring:

1. **The corrected token numbers** (§1), measured, with how you measured them.
2. **The file layout for the CLAUDE.md split** — what the router keeps, what each identity file gets, and specifically **how you are handling content that is genuinely shared** (the writing discipline, worktree/branch rules, commit discipline, security posture). Duplicating it across three identities creates three copies that drift; putting all of it in the router recreates the bloat. **That tension is the actual design problem of this lane — tell me how you resolve it.**
3. **The protocol's index/sub-doc split**, with your rule-vs-story call on the big passages.
4. **Anything you think is wrong with this brief.** You will have read all three target manuals and I have read one; on their contents you are better informed than I am.

**Then wait for my response.** I am awake and conducting all night; I will answer quickly.

---

## §7 — Calibration: how much of this I have actually read

I am AC0-35, running the conductor cascade. **I have read in full:** `_evryn-meta/CLAUDE.md` (611), `ac-orchestration-protocol.md` (941), `ac-writing-protocol.md` (100), `conductor-cascade.md` (110), plus ARCHITECTURE, BUILD and the sprint on the product side. **I have NOT read:** `AGENT_PATTERNS.md` in full (only lines 14–29 and a grep of the resume material), `product-full-cascade.md`, or **any of the three target manuals** beyond DC's cascade section (lines 145–189).

⇒ **On what DC, QC and OC actually need from their manuals, you will know more than I do.** Treat my claims as **LEADS, not facts**. Where this brief is silent, do not read that as "AC0 checked and there was nothing there" — assume I could not see it. **Say so loudly and early if any of it is wrong.**

**One concrete thing I did check and you should know:** `evryn-ops/CLAUDE.md` has **no bottom truncation canary** (its last line is `---`). That is a doc-hygiene gap in a file you are about to restructure around. It is one of four found tonight; **flag it in your report rather than fixing it inline**, so the batch stays together.

---

## §8 — RUNNING LOG (ACm appends here; keep it re-spin-complete)

### AC0-35 — 2026-08-11 night — lane opened

Worktree provisioned off `main` `0ef7216`. Scope reduced from the original plan: **`AGENT_PATTERNS.md` removed** because a peer conductor took a live worktree in this repo tonight and that file is plausibly in his lane. The token figures the lane's argument rests on were found to be roughly 2× overstated and are ACm's first task to correct. Nothing built yet.

### ACm — 2026-08-11T23:51:37-07:00 — measurements done, design proposed, AWAITING AC0-35's CLEARANCE

**Status: nothing built. No file moved, no line cut.** Describe-then-pause is respected; the design below went up to AC0-35 and this lane does not proceed until he clears it.

#### A. THE MEASURED NUMBERS — this replaces every prior figure

**Method (the only reliable one available):** `Read` a file with **no `offset` and no `limit`**. Over the cap the tool returns a partial view whose notice states the **whole-file** token count. ⚠️ **A `Read` carrying an explicit `limit` reports the count of the SLICE, not the file** — that is what made the earlier 26,137 figure for `evryn-quality/CLAUDE.md` unusable, and it is the mechanism behind every inflated number in our docs.

| File | Bytes | **Tokens** | Provenance |
|---|--:|--:|---|
| `_evryn-meta/CLAUDE.md` | 129,973 | **45,347** | tool-reported by another lane; corroborated here (see below) |
| `docs/protocols/ac-orchestration-protocol.md` | 168,528 | **58,732** | **measured by ACm, whole-file** |
| `evryn-quality/CLAUDE.md` | 105,066 | **36,059** | **measured by ACm, whole-file** |
| `evryn-dev-workspace/CLAUDE.md` | 49,615 | ~17,200 | **DERIVED** — under the cap, so unmeasurable by this method |
| `evryn-ops/CLAUDE.md` | 28,513 | ~9,900 | **DERIVED** — same reason |

**⇒ The lane's headline figure: `CLAUDE.md` + the protocol = 104,079 tokens, not the recorded ~240,000.**

**Why the two derived figures are trustworthy enough to quote (and how to tell if they stop being):** the three measured files give bytes/token of **2.866 · 2.870 · 2.914** — a spread of 1.7%, i.e. ~4 chars/token, the standard English rate. The derived numbers use 2.88 and are good to roughly ±2%. **Any file under the 25,000-token cap cannot be measured directly; derive it and label it derived.**

**Corroboration of the one number ACm did not measure himself:** 129,973 ÷ 2.88 predicts **45,130** against the reported **45,347** — 0.5% error. AC0-35's byte-ratio extrapolation, which he flagged as untrustworthy, was in fact accurate; the method is sound on this corpus. **What was wrong was never the ratio — it was tokens-per-LINE figures taken from dense slices.**

#### B. 🔴 A LIVE ERROR FOUND INSIDE THE PROTOCOL THIS LANE IS RESTRUCTURING

`ac-orchestration-protocol.md` line 185 states `_evryn-meta/CLAUDE.md` ≈ **144 tokens/line** and this protocol ≈ **166 tokens/line**. **Measured whole-file: 74.2 and 62.4** — overstated by 1.9× and 2.7×.

**Mechanism:** those rates were derived from *slices*, and our prose is wildly uneven. `evryn-quality/CLAUDE.md` lines 1–207 run **~174 tok/line** while the whole file runs **71.8** — the Patterns block is several times denser than the rest. **Sampling a slice and dividing by lines systematically overstates.**

⚠️ **This is not cosmetic, because that table is the EVIDENCE for the three-trip spin.** It concludes *"the measured reason is not the code — it is OUR OWN PROSE DOCS,"* sizing a scout's docs at ~800K against ~700K of runtime. At the true rates the doc half is roughly **half** what was claimed, which would make the **runtime** the dominant cost and **invert the stated diagnosis**. **The three-trip design itself is NOT in question** — it rests on three observed compactions, not on this arithmetic. What is in question is *what to scope down*, and that is a live design input for whoever next tunes a scout's load. **Routed to AC0-35; out of ACm's lane to fix, and the TS ~30–45 tok/line figure in the same table is unverified and probably derived the same way.**

#### C. THE BLOCKER — deliverable (i) cannot land without editing two frozen blocks

The `<identity>` block asserts *"The CLAUDE.md that auto-loaded into your context is AC's operating manual — that is NOT yours."* **After the split, what auto-loads is the router, so that sentence is false.** The AC-variant's mirror sentence (*"the CLAUDE.md that auto-loaded IS your operating manual"*) is false for the same reason. Both are inside the HARD RULE's character-for-character frozen set.

**There is no way around it:** the harness injects the file named `CLAUDE.md`, so the router must occupy that exact path for the split to work at all. ⇒ **Justin's explicit prior OK on two exact sentences is a hard prerequisite for deliverable (i) landing.** Everything else can be built and left ready.

#### D. THE DESIGN, IN ONE PARAGRAPH (full reasoning went up to AC0-35)

Router at `_evryn-meta/CLAUDE.md` (~60 lines: Justin's one hard rule, a four-row routing table, the read-it-yourself-and-check-the-canary beat, nothing else). AC's identity extracted whole to `_evryn-meta/identity/ac.md`. **DC's, QC's and OC's existing manuals are the identity files already — the router points AT them; no new copies are written.** **The shared-content de-duplication is deliberately NOT done tonight** (see §8's next entry once AC0 rules): doing it necessarily edits four manuals across four repos, and it is not on deliverable (i)'s critical path — the benefit comes entirely from what stops being injected. 🔴 **And one hazard found while reading all three manuals: AC's commit discipline carries a build-level-code carve-out that DC and QC must NEVER inherit. A naive "shared floor" file would hand it to them.** Any future floor must carry the strict version and name the single identity that holds the exception.

#### E. Doc-hygiene gaps observed (flagged, NOT fixed — AC0-35 is batching them)

- `evryn-ops/CLAUDE.md` has no bottom canary; true final line 317 is `---`.
- `ac-orchestration-protocol.md`: `wc -l` reports 941, the Read tool reports 942 total lines. Trailing-newline artefact, not a truncation — but any brief citing "(full — 941 lines)" will look off by one to a careful agent.

### ACm — 2026-08-12 — BUILT. Deliverable (i) complete; deliverable (ii) Phase A complete, Phase B specified below and NOT done.

**🔴 THE BRANCH IS IN A KNOWINGLY-INCOHERENT INTERMEDIATE STATE. THIS IS DELIBERATE, NOT AN OVERSIGHT.** `_evryn-meta/CLAUDE.md` is now a router, while the frozen `<identity>` blocks still call it *"AC's operating manual."* **Those two sentences were left untouched on purpose** — they are inside the HARD RULE's frozen set and only Justin may re-word them (AC0-35 surfaced it rather than absorbing it; drafts are on his morning ballot). **Do not "fix" this inconsistency. Do not merge until those sentences are ruled on.**

#### What exists now

| File | Lines | What it is |
|---|--:|---|
| `CLAUDE.md` | 53 | **the router** — ~1,304 tokens, down from 45,347 |
| `identity/ac.md` | 627 | AC's manual, moved whole + a directory-disambiguation header |
| `docs/protocols/ac-orchestration-protocol.md` | 672 | the universal core + an index |
| `docs/protocols/orchestration/spinning-an-ac.md` | 263 | AC variant · recursive propagation · tier rule · escalation ladder · work brief · lane-AC lifecycle · receipts up the chain |
| `docs/protocols/orchestration/spinning-dc-qc.md` | 46 | the loop · review depth |
| `docs/protocols/orchestration/spinning-a-team-agent.md` | 44 | the four substitutions |
| `docs/protocols/orchestration/orchestration-rationale.md` | 27 | mailbox fallback **only so far — see Phase B** |

**Effect:** every non-AC subagent's unconditional manual cost drops **45,347 → ~1,304 tokens (-97%)**. AC spins pay the same 45,347 (now read from disk rather than injected) plus the router — a ~3% surcharge, and they get a *current* copy instead of a possibly-stale injected one.

#### How the split was verified — reproducible, and the control was proven able to fail

The protocol was cut by **exact line ranges**, never retyped. Positive controls, all passing:
1. **Reassembly is byte-identical to the original** once git-normalized — all 941 lines accounted for exactly once. ⚠️ **Normalize line endings before comparing: this repo is `core.autocrlf=true` and its docs are CRLF on disk, so `sed` output looks like a total rewrite until you `tr -d '\r'` both sides.** Raw `cmp` gives a false FAIL.
2. **The control rejects a single deleted line and a one-word change** — so a PASS means something.
3. **The frozen `<identity>`…`</receipts>` block (52 lines) and the trip-1 `<output>` block (15 lines) are byte-identical** before and after the move. **Not one word changed.**
4. **Only 4 original lines are absent** from the new file set — all four are the old header prose deliberately replaced by the index.

Scripts: `scratchpad/split2.sh` and `scratchpad/core.sh` (session scratchpad; re-derivable from the ranges above).

#### The one content ADDITION (not a move) — flag it as such in review

A new call-out in the core's part 1: **naming the agent's own manual in `<mandatory_load>` is now load-bearing rather than redundant**, because the auto-injection no longer supplies *any* complete manual. It carries the per-agent paths and the stale-injection warning. **This is new prose in a source-of-truth doc, not relocated text** — review it as such.

#### 🔴 PHASE B — NOT DONE. This is the remaining half of deliverable (ii).

Brief §3's *"keep every RULE, move every STORY"* cut is **only partly executed.** What is done is the split by **target** (per-target sections → sub-docs): 941 → 672 core lines, a 29% cut. What is **not** done is moving the **incident narratives** out of the core into `orchestration-rationale.md`, which §3 says is the larger cut.

**Why I stopped:** this is the judgment-heavy, content-destructive half, and I am deep into my context. My own manual warns that a consolidation is triggered by exactly the exhaustion that makes an agent bad at it. **Stopping was the safer failure mode; a fresh ACm should do it against the three-bucket rule below.**

**The three-bucket rule AC0-35 adopted (use this, don't re-derive it):**
- **STORY → `orchestration-rationale.md`:** the 2026-06-17 lobotomy narrative · the 2026-06-22 Workflow-condensation incident · the "six corrections in one night" proof · the receipts live-specimen parenthetical · the 1.5M-token AC-under-AC cost figure · the resume-test provenance · the (wrong) tokens-per-line table.
- **RULE → stays in core:** all six brief parts · multi-trip mechanics · receipts rules · model pinning · the HARD RULE · the generic-subagent test · `#cascade-override` · the frozen blocks.
- **⭐ RULE-WHOSE-STORY-IS-ITS-ENFORCEMENT → core, compressed inline, pointer to the full account.** The document self-flags these — *"verbatim, because the phrasing is the teaching"*, *"and this wording is proven."* Strip them to bare instructions and they stop being obeyed. **When you cannot tell which bucket, it is a rule: leave it in the core and say you were unsure.**

**Also owed in Phase B:** move the wrong tokens-per-line table (core, §"The fact that forces it") to the rationale doc **verbatim, with a dated correction appended beneath it** — measured rates 74.2 and 62.4 tok/line against the claimed 144 and 166, the slice-derivation mechanism, and the note that the *diagnosis* ("it is our prose docs, not the code") may invert while the three-trip *design* stands on three observed compactions and is not in question.

#### 🔴 THE CROSS-REPO POINTERS THIS CHANGE BREAKS — and they CANNOT ride this branch

`evryn-dev-workspace/CLAUDE.md`, `evryn-quality/CLAUDE.md` and `evryn-ops/CLAUDE.md` each point readers at **"AC's `CLAUDE.md`"** for the cross-repo sync ritual and related detail. After the merge those point at a 53-line router that no longer contains any of it.

⚠️ **This branch is an `_evryn-meta` branch, so those three edits are structurally unable to live on it.** They need their own coordinated change in three repos, **timed with this lane's merge**. Left unfixed, three manuals send every DC, QC and OC to a routing table. *(In-repo pointers were repointed here: the core protocol's four cross-references and `conductor-cascade.md`'s two.)*

#### The common-floor rule, recorded so the NEXT lane inherits it (AC0-35's instruction)

When the shared-discipline de-duplication lane runs, it is governed by this:

> **A rule belongs in the common floor if and only if it is true for EVERY agent with no exception. The floor states the strictest form. Any carve-out lives in the single identity file that holds it — and the floor NAMES which one, so a reader who lacks the carve-out learns that it is not theirs.**

🔴 **The hazard that forces it:** AC's commit discipline carries a **build-level-code carve-out** (`identity/ac.md`, "The one standing exception") that DC and QC must never inherit. QC's manual states the strict version with no exception; the escalation ladder says outright *"if you are a sub-AC reading this: the carve-out is not addressed to you."* **A well-meaning merge of "the commit discipline" into one shared file would hand three agents an authorization Justin granted to exactly one — silently, inside a refactor. That is a permissions change wearing a tidy-up costume.**

### AC0-35's rulings on ACm's ballot — 2026-08-12, LANE STOPPED HERE

**Recorded because these decisions existed only in a spin message, which does not survive a session. A fresh instance must not re-litigate them.**

#### 🔴 THREE THINGS GATE THIS LANE'S MERGE. All three are Justin's or a later lane's — none is ACm's.

1. **The two frozen `<identity>` sentences.** After the split they are factually false (they call `CLAUDE.md` "AC's operating manual"; it is a router). They sit in the HARD RULE's frozen set, so **only Justin may re-word them.** Drafts are on his ballot — see §8's "C. THE BLOCKER" entry for the exact before→after. **They were deliberately left UNCHANGED on the branch. Do not fix them.**
2. **The three cross-repo pointer fixes** (`evryn-dev-workspace`, `evryn-quality`, `evryn-ops` all point at "AC's `CLAUDE.md`" for the sync ritual). **AC0-35 recorded this as a MERGE PRECONDITION, not a follow-up: merging Phase A without them ships three broken pointers.** They cannot live on an `_evryn-meta` branch; they are a coordinated same-window change in three repos, owned by a later lane.
3. **A rebase.** `main` moved during this lane (`8b82fb1` → `ad2cbab`), so the branch is now a sibling, not a descendant, and **a fast-forward merge is no longer possible.** Rebase onto current `main`, re-run the verification controls, then merge.

#### The ballot rulings

- **Q1 — Phase B: approved as (a) a fresh ACm against the §8 spec, but NOT tonight.** Two reasons, and the second was AC0-35's: ACm's own (the exhaustion that triggers a compression is what makes an agent bad at it), **plus — Justin has not seen Phase A yet, and Phase B's cut depends on how he reads the shape.** Running the most content-destructive half with no human eyes on the half preceding it is momentum, not judgment. **⇒ Phase B runs after Justin has looked. The §8 spec is what makes that cheap.**
- **Q2 — the three pointer fixes: (a), a small lane at merge time.** Not ACm's; see gate 2 above.
- **Q3 — `Nested subagents` stays in the CORE.** Confirmed. It governs *every* nesting, including a DC that spawns a helper, and its "never tell a child where to report" rule is referenced from inside the frozen block. **Separating a rule from the carve-out that makes it safe is how a rule stops being obeyed.**
- **Q4 — line counts: ADOPTED. `wc -l` for navigation, the canary for verification, and the canary wins on disagreement.** *"A line number tells an agent where to stop; only the canary tells it that it arrived."* ⇒ **A canary-less file is a genuine hole, not a formality** — which is how tonight's canary-gap batch should be read.
- **Q5 — branch commits of source-of-truth docs: correct.** Brief §5's gate is *"not committed to `main`"*; branch commits are how crash-survival works and the protocol has sub-ACs commit to their own branch. Nothing to reset.

#### For the record — the instrument-failure worth carrying past this lane

**A byte-level correctness control can FAIL catastrophically for a reason that is not content loss.** The reassembly control reported **all 941 lines differing**; the cause was `sed` stripping one `\r` per line on a CRLF repo (168,528 − 167,587 = 941 bytes = exactly one per line). **Nothing had been lost.** ⇒ **Normalize line endings before comparing, or a control designed to protect correctness will tell you to revert good work.** AC0-35 logged this as the third instrument-failure of the night and the only one whose wrong verdict would have destroyed something.

---

# §9 — HANDOFF TO ACm2 (written by ACm, 2026-08-12). *(Superseded — see §10.)*

> 🔴 **IF YOU ARE ACm3 OR LATER, START AT §10, NOT HERE.** §9 was written to ACm2 and **its file paths are now stale**: `identity/ac.md` moved to `.claude/agents/ac.md`, and `orchestration-rationale.md` was deleted. §9's *reasoning* is still good history; **its load list (§9.1) is not — §10 carries the current one.**
>
> **You are ACm2.** ACm wrote this assuming you have **zero memory** of anything below. Nothing in this section is recoverable from the diff — that is why it exists.

## 🔴 9.0 — IF YOU COMPACT, DO NOT PRETEND OTHERWISE

When context runs out, the harness replaces your conversation with a summary and instructs you, in substance, *"do not acknowledge the summary, resume as if the break never happened."* **You would then be working from a third-party compression of your own session instead of from files — and nothing feels wrong, because a good summary reads exactly like knowing.**

🥋 *"Resume as if the break never happened"* is the right **destination**, and **you cannot reach it from a summary** — so it applies **after** you reload, never before. You are not disobeying the harness; you are doing the one thing that makes obeying it possible.

**On any sign you are working from a summary rather than files you read: STOP → reload §9.1's list in full → tell AC0 you compacted and reloaded → then resume.**

🔴 **DO NOT ASSESS YOUR OWN SHARPNESS — the faculty you would assess it with is the one in question.** *"I feel fine"* is what a lossy compression feels like from inside. **Report what you RE-READ, never how you feel.** *(Live proof: a conductor compacted, complied, and worked competently for several turns; on finally reloading he found his load list had been missing a spoke, a sprint doc and two changelogs.)*

⚠️ **And the second-order trap, which bites AFTER a clean reload: reloading restores what you READ, not what you CONCLUDED.** Judgements formed under the old conditions ride through untouched. **Re-derive your open calls — estimates, cautions, "we can't afford to" — against conditions as they are now.**

## 9.1 — YOUR LOAD LIST

⚠️ **Line counts below were measured 2026-08-12 and WILL BE STALE — this branch is live (see §9.7). Re-run `wc -l` on every file before you trust a span, and use the canary, not the number, as your proof of a full read.**

**Everything is on branch `acm/loading-architecture`, worktree `C:/Users/Justin/Evryn/Code/_evryn-meta-lane-a`. Read the BRANCH copies — `main`'s are the pre-split originals and will mislead you.**

| # | File (branch) | Lines | Why you need it |
|---|---|--:|---|
| 1 | `docs/working/2026.08.11-ac0-acm-loading-architecture-brief.md` | ~340 | **This file, in full.** Your marching orders and the whole decision record. ⚠️ `main`'s copy is AC0's original and contains **none** of §8 or §9. |
| 2 | `docs/protocols/ac-orchestration-protocol.md` | ~692 | **The file Phase B edits.** Read every line; you are deciding what leaves it. |
| 3 | `docs/protocols/orchestration/orchestration-rationale.md` | 27 | **Phase B's destination.** Currently near-empty by design. |
| 4 | `docs/protocols/orchestration/spinning-an-ac.md` | 263 | Already-split content — so you don't move something twice. |
| 5 | `docs/protocols/orchestration/spinning-dc-qc.md` | 46 | Same. |
| 6 | `docs/protocols/orchestration/spinning-a-team-agent.md` | 44 | Same. |
| 7 | `CLAUDE.md` (the router) | ~76 | What every agent now auto-inherits. **Do not edit without checking §9.7 first — others are live in this file.** |
| 8 | `identity/ac.md` | ~657 | **The ten-reference audit's subject** (§9.3). ⚠️ It has grown since ACm wrote it — AC0 added content. |
| 9 | `docs/protocols/ac-writing-protocol.md` | 100 | **You are writing prose into source-of-truth docs.** Non-optional. |
| 10 | `_evryn-meta/docs/current-state.md` (main) | 152 | Light cascade — what is live org-wide. |
| 11 | `_evryn-meta/docs/hub/roadmap.md` (main) | 196 | Light cascade — company truth. |

**On your own manual:** you are spun from `_evryn-meta`, so **`main`'s `CLAUDE.md` auto-injects and that is still AC's full manual** (the split is unmerged). It is byte-identical to branch `identity/ac.md` except for the header box, the Light-cascade line, and AC0's additions. **Do not also read `identity/ac.md` in full for identity purposes — you already have it. Read it for the audit only.**

## 9.2 — PHASE B: what moves, and the test that decides

**Goal:** move incident NARRATIVE out of the core into `orchestration-rationale.md`. **Target ≈ 600 core lines. Do not chase 300 — see §9.5.**

**The three-bucket test (AC0 adopted it; do not re-derive):**

- **STORY → move.** Named passages: the **2026-06-17 lobotomy narrative** · the **2026-06-22 Workflow-condensation incident** (in the HARD RULE) · the **"six corrections in one night"** proof · the **receipts live-specimen** parenthetical (the 15-vs-16-calls run) · **"What this lets us DELETE"** under two-trip loading · **"The fact that forces it"** under three-trip, *including the wrong tokens-per-line table* · the **resume-test provenance** parenthetical.
- **RULE → stays.** All six brief parts · multi- and three-trip mechanics · receipts rules · model pinning · the HARD RULE itself · the generic-subagent test · `#cascade-override` · **every verbatim block**.
- **⭐ RULE-WHOSE-STORY-IS-ITS-ENFORCEMENT → stays, compressed, with a pointer.** The document self-flags these: *"verbatim, because the phrasing is the teaching"*, *"and this wording is proven."* Strip them to bare instructions and they stop being obeyed.

🔴 **When you cannot tell which bucket: it is a RULE. Leave it, and say you were unsure.**

**Also owed:** move the tokens-per-line table **verbatim** and append a dated correction beneath — claimed 144 / 166 tok/line, **measured whole-file 74.2 / 62.4**; mechanism = slice-derived rates on very uneven prose. **State that the three-trip DESIGN is not in question** (it rests on three observed compactions) **but its stated DIAGNOSIS — "the reason is our prose docs, not the code" — may invert.**

## 9.3 — THE TEN-REFERENCE AUDIT

`identity/ac.md` cites `ac-orchestration-protocol.md` at lines **106, 236, 268, 271, 350, 442, 446, 448, 553, 563** and cites the four sub-docs **zero times**. None are broken; all are coarse.

**Ruling (Justin approved): ONE FRONT DOOR — deep-link only where the reader's target is already known.** Rationale: deep links multiply the surface that goes stale when sections move, and this lane just proved sections move.

**Two are strong deep-link candidates:** the **QC/review-depth** references (~553, ~563 → `spinning-dc-qc.md`) and anything pointing at escalation or lane lifecycle (→ `spinning-an-ac.md`). 🔴 **Line 446 is different and is a DEFECT, not a pointer problem:** it says *"Subagents auto-load AC's CLAUDE.md, not DC's or QC's — this is the known failure mode."* **The router killed that problem.** Same stale framing appears in the protocol and in §0 of this brief. **Fix the claim, don't just repoint it.**

## 9.4 — 🔒 THREE FROZEN-BLOCK CHANGES ARE PENDING JUSTIN. THEY ARE NOT YOURS.

**You may not touch a character of the `<identity>` / `<mandatory_load>` / `<task>` / `<questions_first>` / `<receipts>` / `<output>` blocks.** The HARD RULE names Justin explicitly.

1. **DC/QC/OC `<identity>`** — says the auto-loaded CLAUDE.md *"is AC's operating manual."* After the split it is a router. **False.**
2. **AC-variant `<identity>`** (now in `spinning-an-ac.md`) — says it *"IS your operating manual (`_evryn-meta/CLAUDE.md`)."* **False** for the same reason.
3. **The manual paths** — if the DC/QC/OC-as-agent-definitions consolidation lands, `[evryn-dev-workspace/CLAUDE.md | evryn-quality/CLAUDE.md | evryn-ops/CLAUDE.md]` changes too. **This is why Justin is deciding consolidation BEFORE merge — so all three edits batch into one approval instead of two rounds.**

⚠️ **The branch is knowingly incoherent until he rules. That is deliberate. Do not "fix" it.**

## 9.5 — THE ~600-LINE FLOOR, AND WHAT REGISTRATION ACTUALLY UNLOCKS

**Justin accepted 600 once he understood why. Do not re-litigate it; do not promise 300.** The bulk of the core is **brief-construction machinery** — the six parts, the verbatim blocks, the exact-words section, multi-trip mechanics — and **every spinner needs all of it.** Narrative is only ~60–80 lines.

**Rejected, with reasons — do not re-propose:**
- **A ~80-line hub + `brief-construction.md`.** Every reader of this protocol is about to construct a brief, so the hop buys nothing and puts the load-discipline machinery one step further away — the exact shape that produces under-loading.
- **Retitling the file "core."** Justin ruled against: *"it **is** the protocol, and I don't want to have to fix all the references — other teams reference this."* **Solved with the lobby header instead. Keep the filename.**

**✅ AGENT-TYPE REGISTRATION IS CONFIRMED WORKING** (three seats: ACm's worktree probe failed, AC0's canonical mid-session probe failed identically, **Justin's fresh session spawned it**). ⇒ **The registry is read at SESSION START.** Justin's own note: the type was discovered **structurally, from the definition file alone, with zero prose telling anyone it existed.**

⚠️ **UNCONFIRMED, and do not overstate it:** spawnability proves the harness **read the file**. It does **not** yet prove the definition's **body programs the agent**. The canary `MERIDIAN-SEVENTEEN-ALBATROSS` in `.claude/agents/acm-probe.md` answers that; **it had not been reported back when ACm stopped.**

**The 20–40 lines of genuine simplification this unlocks:**
- **Model pinning could become structural** — `model: opus` in frontmatter (confirmed present in `evryn-team-workspace/.claude/agents/lucas.md`). Would make the recursive-inheritance burn *not-performable* rather than instructed-against. **Strongest candidate.**
- **QC's read-only discipline could become structural** via tool restriction (confirmed: definitions carry tool sets; `Explore` excludes `Edit`/`Write`). ⚠️ **BUT NOT A BLANKET READ-ONLY SET — that would break her: she must write `docs/qc-to-ac.md` and must mutate code in a provisioned sandbox worktree.** Any restriction has to preserve both.
- **What does NOT get simpler:** the load list, multi-trip loading, receipts, the verbatim blocks. **A definition cannot know which runtime files *this trip* needs.** That is the bulk, and it survives.

**Second-order finding worth keeping:** this is the first known case where **RE-SPIN beats compact-and-continue** — a session-start read is invisible to a reloaded instance no matter how well it reloads.

## 9.6 — DECISIONS NOT VISIBLE IN THE DIFF

- **Only ONE new identity file was written.** DC, QC and OC **already had** operating manuals; the router points at them. Writing three new copies under `_evryn-meta` would have forked three source-of-truth docs within a week.
- **`Nested subagents` deliberately stayed in the core** — it governs every nesting, including a DC spawning a helper, and its *"never tell a child where to report"* rule is referenced from inside the frozen block. Separating a rule from the carve-out that makes it safe is how a rule stops being obeyed.
- **Terminology ruled: "agent definition," NOT "identity."** Move the directory to `.claude/agents/`, **keep the prose word "manual."** 🔴 **Do not sweep "manual" → "identity": it would collide with Mira's `evryn-backend/identity/**` layer, which has heightened review and ships to production.** That collision is the dangerous one.
- **The pre-split protocol is NOT archived to `docs/historical/`.** Justin instructed it; ACm objected and the objection carried: **an archived copy would put a second, stale copy of the frozen blocks within reach of a loading agent** — the lobotomy in a new costume. Git already preserves it losslessly.
- **The common-floor de-duplication was deferred** to its own reviewed lane, governed by the rule in §8. 🔴 **Its hazard: AC's build-level-code commit carve-out must never reach DC or QC.**
- **Three cross-repo pointers break on merge** (`evryn-dev-workspace`, `evryn-quality`, `evryn-ops` all cite *"AC's `CLAUDE.md`"*). **A recorded MERGE PRECONDITION, not a follow-up.** They cannot live on an `_evryn-meta` branch. **Consolidation would dissolve this entirely** by making them intra-repo.
- **A rebase is required** — `main` moved during this lane; fast-forward is no longer possible (verified with `git merge-base --is-ancestor`, not assumed).
- **Measurement method:** `Read` with **no offset and no limit**; the over-cap notice gives the whole-file count. **A `Read` with an explicit `limit` reports the SLICE** — that is what made an early 26,137 figure unusable. Bytes/token ≈ **2.88**, calibrated on three files (2.866 / 2.870 / 2.914), for files under the cap.
- **CRLF:** this repo is `core.autocrlf=true` with CRLF on disk. **Normalize before any byte-level comparison** or a correctness control will false-FAIL and tell you to revert good work.

## 9.7 — 🔴 THE BRANCH IS SHARED AND LIVE. READ THIS BEFORE YOU COMMIT ANYTHING.

**`acm/loading-architecture` is not ACm's alone.** AC0 committed `fedcedf` to it (the compaction rule into the router, plus additions to `identity/ac.md`), and at the time of writing there is a **live uncommitted edit to `CLAUDE.md` that belongs to someone else.**

🔴 **ACm COMMITTED THAT SOMEONE-ELSE'S WORK BY ACCIDENT, AND IT IS WORTH MORE TO YOU THAN ACm'S RECORD.** Commit `a1d7e31` carries, under ACm's message, roughly seven lines of AC0's refinements to the compaction block — a `STOP`→`PAUSE IMMEDIATELY` rewrite, an "honour"→"honor" fix, and others. **Nothing was lost or corrupted; the damage is attribution.**

**How it happened, precisely:** ACm ran `git status --short`, saw ` M CLAUDE.md`, recognised it as its own edit, and committed by pathspec. **A by-path commit takes that file's ENTIRE working-tree state** — including another agent's uncommitted edits to the same file — **and `git status` cannot distinguish them, because both render as one modified path.**

**⇒ THE RULE ACm READ IN FULL THIS SESSION AND STILL BROKE: `git diff <file>` FIRST and confirm the WHOLE diff is yours.** `git status` is not sufficient. **Do this before every commit on this branch.**

**ACm deliberately did NOT attempt recovery.** `git reset --soft HEAD~1` would mix a rewrite into a branch another agent is actively committing to, which is far more dangerous than a wrong commit message. **Disposition is AC0's call.**

## 9.8 — RESUMABILITY: what only ACm can answer

**ACm remains reachable by `SendMessage` for the life of AC0's session** (resume survives a parent-side compaction — confirmed twice). After that, permanently gone. **Ask before then if you need:**

1. **Why a specific passage went to the bucket it did** — the per-passage reasoning is in ACm's head, not this brief.
2. **The exact extraction ranges used** (`410–686`, `750–761`, `831–850`, `924–934` against the 941-line original) and how the positive control was built — scripts are in a session scratchpad that dies with ACm; **the ranges above are the durable record, so re-derive rather than hunt for the scripts.**
3. **What ACm considered and discarded** beyond §9.5's two rejected options.

---

# §10 — HANDOFF TO ACm3 (written by ACm2, 2026-08-12). START HERE.

## 🔴 10.0 — IF YOU COMPACT, DO NOT PRETEND OTHERWISE

**If you are reading a SUMMARY of this lane rather than this file, you have compacted.** The harness will have told you to resume as though nothing happened. **Do not** — that is the right destination and you cannot reach it from a summary, so it applies *after* you reload, never before.

**STOP → re-read the load list in §10.1 IN FULL, from disk → tell AC0 you compacted and reloaded → then resume.** 🔴 **Do not assess your own sharpness; the faculty you would assess it with is the one in question.** Report what you *re-read*, never how you *feel*. ⚠️ And the second-order trap that bites *after* a clean reload: **reloading restores what you READ, not what you CONCLUDED** — re-derive your open judgement calls against conditions as they are now.

## 10.1 — YOUR LOAD LIST (supersedes §9.1)

⚠️ **Re-run `wc -l` before trusting any span; use the canary, not the number, as proof of a full read.** Everything is on branch `acm/loading-architecture`, worktree `C:/Users/Justin/Evryn/Code/_evryn-meta-lane-a`. **Read the BRANCH copies.**

| # | File (branch) | Why |
|---|---|---|
| 1 | `docs/working/2026.08.11-ac0-acm-loading-architecture-brief.md` | This file, in full. §10 first, then §8–§9 as history. |
| 2 | `docs/protocols/ac-orchestration-protocol.md` | The core. Phase B is DONE in it. |
| 3 | `docs/protocols/orchestration/spinning-an-ac.md` · `spinning-dc-qc.md` · `spinning-a-team-agent.md` | The three sub-docs. ⚠️ **`orchestration-rationale.md` NO LONGER EXISTS — deleted, see 10.2.** |
| 4 | `CLAUDE.md` | The router. **Contended — check `git diff` before editing.** |
| 5 | `.claude/agents/ac.md` | AC's manual. ⚠️ **MOVED from `identity/ac.md`.** |
| 6 | `.claude/agents/dc.md` · `qc.md` · `oc.md` | The three new agent definitions. |
| 7 | `docs/protocols/ac-writing-protocol.md` | You will write source-of-truth prose. Non-optional. |
| 8 | `_evryn-meta/docs/current-state.md` + `docs/hub/roadmap.md` (main) | Light cascade. |

## 10.2 — WHAT ACm2 DID, AND THE ONE THING THAT WILL SURPRISE YOU

**Phase B is complete, but NOT as §9.2 specified — Justin reopened the premise and the answer changed.** §9.2 said move seven narrative passages into `orchestration-rationale.md`. Justin: *"if compliance is what it's for, then putting it in its own sidecar completely misses the point, because literally no one is going to read that sidecar."* **He is right.** So:

- **Nothing was moved. Provenance was DELETED; consequences were kept inline.** The test applied per passage: *does the narrative supply a MECHANISM the reader needs to comply, or only PROVENANCE (who/when/which lane)?* Mechanism → compress and keep. Provenance → delete. Self-flagged verbatim teaching (*"verbatim, because the phrasing is the teaching"*) → untouched.
- 🔴 **`orchestration-rationale.md` was DELETED entirely.** Its only content was the mailbox fallback, which (a) duplicates mechanics already in AC's manual and (b) describes a protocol `main` **RETIRED** on 2026-08-12. Deleting it follows this lane's own precedent: ACm argued against archiving the pre-split protocol because *"git already preserves it losslessly"* and that objection carried.
- **The core's header now carries a standing rule so nobody re-creates it:** add consequences freely, never narrative, and do not create a rationale doc to hold it.

🔴 **HONEST FINDING — THE PRIZE WAS MUCH SMALLER THAN §9.2/§9.5 ASSUMED, AND I DID NOT MANUFACTURE A CUT TO HIT THE NUMBER.** §9.5 estimated ~60–80 lines of narrative and set a ~600-line target. **The genuinely removable narrative in the core was about 20 lines.** The reason: **Phase A's per-target split had already carried most of the war stories out** into the sub-docs. What remained was mostly *one-clause consequences already embedded in rules* — which are exactly what Justin says to keep. **The core is ~692 lines and chasing 600 would have meant cutting rules.** Do not re-open this as a line-count exercise.

## 10.3 — THE AGENT DEFINITIONS (new build)

`.claude/agents/{dc,qc,oc}.md` — frontmatter + a front-matter header + **each repo manual appended VERBATIM by file operation, never retyped.** Verified byte-identical to source, with controls proving the check rejects a 1-char change, a deleted line and an appended line. Sources: `evryn-dev-workspace@f09d113`, `evryn-quality@4f47560`, `evryn-ops@aaae21d`.

**Design calls, with reasoning — these are yours to overturn only with better reasoning:**
- **The definition holds the FULL manual, not a pointer.** Justin's end-state has the repo CLAUDE.mds become redirects at cutover, so the definition is the future single home. And a pointer-only definition is *advisory prose*, which the probe proved an agent can disregard; an inlined manual cannot be un-delivered. **Structural beats instructional.**
- **`model: opus` on all four.** ⚠️ **Correcting §9.5, which overstated this:** it does **not** make the recursive-burn *unperformable* — the Agent tool's `model` parameter still overrides frontmatter. It removes the **accidental** case (an unpinned spawn inheriting a parent's expensive model), which is the actual hazard, while leaving deliberate choice intact.
- 🔴 **NO `tools:` restriction on QC — and this resolves §9.5's open caution as a NO.** Tool restrictions are **tool**-granular; QC's discipline (*"read everywhere, write almost nowhere"*) is **path**-granular. They cannot be made to line up. Dropping `Edit`/`Write` breaks her mailbox write **and** her sandbox mutation testing — her highest-value check and the only way to detect *absence*. **The structural mechanism does not fit the shape of the discipline, so the discipline stays instructional.** The definition says so loudly, to stop a future "tidy-up" regression.
- **NO `memory:` field on any of them.** `lucas.md` carries `memory: project` and is otherwise the format exemplar — **copying it blindly would hand DC/QC/OC an auto-memory store all three manuals explicitly forbid.**
- **Every definition carries a two-sided "spawnability is not a brief" guard.** New failure mode this consolidation creates: an easy `subagent_type: dc` invites improvising the brief. Stated to the agent *and* to the spinner, like the never-tell-a-child-where-to-report pair.

## 10.4 — AC'S MANUAL MOVED: `identity/ac.md` → `.claude/agents/ac.md`

**This executes §9.6's recorded ruling** (*"agent definition, NOT identity — move the directory to `.claude/agents/`"*), which had been decided but never carried out. It also dissolves the `identity/` naming collision with Mira's `evryn-backend/identity/**` layer — the 14-line header box that existed purely to explain that collision is now a 2-line pointer. `_evryn-meta/identity/` is **removed**. AC's manual gained frontmatter (`name: ac`, `model: opus`), so `ac` becomes a spawnable type at the next session start.

## 10.5 — 🔒 GATES. NOTHING HERE IS ACm3's TO CLOSE.

1. **The frozen `<identity>` sentences** still call the auto-loaded file "AC's operating manual." **False, deliberately unchanged, Justin's alone.** `spinning-an-ac.md` now carries a note saying so directly above the frozen block.
2. **The three cross-repo pointer fixes** (`evryn-dev-workspace`, `evryn-quality`, `evryn-ops` all cite *"AC's `CLAUDE.md`"* for the sync ritual). **Merge precondition.** ⚠️ **Consolidation dissolves this** — post-cutover those become intra-repo.
3. **A rebase.** `main` has moved to `0066754`; the branch is a sibling. Verified with `git merge-base --is-ancestor`, not assumed. **Re-run the frozen-block byte-check after rebasing.**
4. 🔴 **THE CUTOVER IS JUSTIN'S AND NEEDS A FRESH SESSION.** The agent registry is read at **session start**, so nothing built here is spawnable until he restarts. Until he runs it, the repo CLAUDE.mds stay authoritative and the definitions are copies — **each definition carries a drift-check command; they were run and return clean.**

## 10.6 — DECISIONS AND FINDINGS NOT VISIBLE IN THE DIFF

- **A defect the byte-control caught that reading never would have:** my first build embedded a drift-check command containing the same marker string it searched for, so `awk` matched the command text instead of the marker and silently captured 16 extra lines. **The file content was correct; only the verification was wrong** — the shipped drift-check would have false-FAILED for everyone who ran it. Fixed by anchoring on `/^<!-- ===== MANUAL BODY/`, which the embedded command text cannot match. **The lesson generalizes: a self-referential marker is a broken marker.**
- **Two stale pointers fixed in AC's manual** — `docs/protocols/ac-dc-protocol.md` moved to `docs/historical/` on `main` on 2026-08-12, and the branch predates it. Both references would have been broken links post-rebase. **Cause: branch drift, not this lane.**
- **Two genuinely broken references found by the ten-reference audit** — §9.3 said *"none are broken; all are coarse."* **That was true pre-split; Phase A broke two of them.** Both pointed at "Review depth," which Phase A moved into `spinning-dc-qc.md`, leaving only a signpost in the core. **A doc split silently invalidates every deep link into what it moved — audit inbound references as part of any split, not after.**
- **Deliberately NOT done:** the shared-content de-duplication (still governed by §8's common-floor rule; **its hazard — AC's build-level-code commit carve-out must never reach DC or QC — is now WORSE, because three verbatim manual copies exist**).

---

# §11 — ACm2's CLOSE-OUT ADDENDUM (2026-08-13). The things that would have died with me.

> **Why this section exists:** §9.8 warned that *"why a specific passage went to the bucket it did — the per-passage reasoning is in ACm's head, not this brief."* **I reproduced that exact failure and am closing it here.** Everything below was in my head and in neither the brief nor the diff.

## 11.1 — PHASE B, PASSAGE BY PASSAGE. This is the record §9.8 said was missing.

**The test I applied to each candidate:** *does the narrative supply a MECHANISM the reader needs in order to comply, or only PROVENANCE (who / when / which lane / what it cost)?* Mechanism → keep, compressed. Provenance → delete. Self-flagged verbatim teaching → untouched.

| Passage | Verdict | Why |
|---|---|---|
| Lobby header's *"No asshole, you don't!"* quote | **KEPT VERBATIM** | Self-flagged *"Justin named it, so it is written down verbatim."* The *"five of eight reviewers"* clause is already a bare consequence — exactly what Justin asked for. |
| Scout-relevance block (*"I triage the load list…"*) | **COMPRESSED** | The two scout quotes ARE the mechanism — they let an agent recognise the reflex in itself — and the first is also canonical inside the frozen `<mandatory_load>`. Kept both; cut the operator-guide near-miss to one clause. |
| *"Six corrections in one night"* | **COMPRESSED** | Kept the count + the worst single example + *"the spinner caught none of them."* Cut the five-item enumeration. |
| Receipts live specimen (15-vs-16 calls) | **COMPRESSED** | Kept both defect shapes (silent retry, `Grep`-reported-as-`Read`) because they are what a reader must recognise, plus the punchline *"a clean-looking receipt is exactly what a smoothed one looks like."* Cut the rest. |
| *"What this lets us DELETE"* | **CUT the opening paragraph** | Pure doc-history ("before, we had pleading, all-caps, threats…"). The four bullets under it are RULES and stayed; the section was retitled so it is about what still applies. |
| *"The fact that forces it"* + tokens-per-line table | **KEPT the three compactions · DELETED and CORRECTED the table** | The compactions are the actual evidence. The table was **wrong** and is cited as evidence, so relocating it would have preserved a false claim in a quieter place. |
| HARD RULE's 2026-06-22 Workflow incident | 🔴 **KEPT — and I was UNSURE** | Per the tie-breaker (*when you cannot tell, it is a RULE*). It is the strongest "why you may not paraphrase" evidence we have, and cutting it risks the exact regression the HARD RULE guards. **If anyone revisits Phase B, revisit this one.** |
| *"Why this works, and why the old approach could not"* | **KEPT — mildly unsure** | It is the structural-invariant argument (remove the task and the triage has nothing to operate on). Without it, two-trip loading reads as bureaucracy. Judged mechanism, not story. |
| Resume-test provenance parenthetical | **CUT to the archive pointer** | The durable facts already sit in the section body above it. |
| Resume *"What's proven"* bullets | **COMPRESSED** | Kept the counter-intuitive claim (a heavy load genuinely survives resume); dropped the passphrase detail. |
| Nested-subagents 2026-07-16 provenance | **COMPRESSED to its consequence** | *"The framing this replaced had been quietly shaping fan-out design around a limitation that did not exist"* is the sticky part. |
| Generic-subagent 2026-07-16 example | **KEPT** | It is an EXAMPLE that draws the boundary, not a war story. |
| 2026-06-17 lobotomy clauses (part 1 + the anchor SET) | **KEPT AS-IS** | Already one-clause consequences fused into rules. No separate narrative was left in the core to cut. |
| Five dated provenance parentheticals (describe-then-pause origin · model pinning · lane-brief collision · cascade-as-source · dashboard authorization) | **CUT, or reduced to their live constraint** | The dashboard one kept its *authorized scope* clause — that bounds what the section may be read to permit, so it is a RULE. |

## 11.2 — REJECTED ALTERNATIVES AND NEAR-MISSES

- **Keeping `orchestration-rationale.md` with an honest retitle.** Rejected: its header promised incident narratives that would never arrive, and its only real content duplicated AC's manual while describing a protocol `main` had retired. A truthful retitle leaves a file whose entire content is a duplicate.
- **Pointer-only agent definitions.** Rejected: it avoids the fork, but re-creates the failure this lane exists to kill — an agent that must *remember* to fetch its manual — and definition prose is advisory. **The fork window is bounded by a cutover Justin controls; the advisory gap would have been permanent.**
- **Leaving AC's manual at `identity/ac.md`.** I nearly did, on scope grounds. What tipped it: §9.6 recorded the move as *ruled*, and leaving AC there while the other three sat in `.claude/agents/` preserved a naming collision whose only defence was a 14-line header box.
- **Chasing ~600 lines.** Deliberately not done; the remaining long lines are load-discipline machinery every spinner needs. **Cutting to hit a number is the HARD RULE's exact failure mode.**
- **A narrower-but-non-empty `tools:` set for QC.** No middle option survives: dropping `Bash` breaks her suites, dropping `Edit`/`Write` breaks her mailbox and mutation testing, and an explicit allow-list silently removes capability as new tools appear. **Omitting the field is the deliberate choice, not an oversight.**

## 11.3 — 🔒 THE MERGE PRECONDITIONS AND CUTOVER, WRITTEN FOR SOMEONE WHO IS NOT ME

⚠️ **Every SHA here goes stale fast — `main` moved from `0066754` to `500db61` in under a day. RE-DERIVE; never trust these numbers.**

### Precondition 1 — the two frozen sentences (JUSTIN'S ALONE)

Both call the auto-injected `CLAUDE.md` *"AC's operating manual."* Post-split it is a router, so both are **false**:
1. **DC/QC/OC variant** — `docs/protocols/ac-orchestration-protocol.md`, inside the `<identity>` block.
2. **AC variant** — `docs/protocols/orchestration/spinning-an-ac.md`, substitution 1.

🔴 **THE REPLACEMENT DRAFTS ARE NOT IN THIS BRIEF.** §9.4 says *"drafts are on his ballot"* — that ballot was a spin message and **does not survive a session.** ⇒ **Whoever executes must obtain the exact wording from Justin or draft it fresh for his approval. Do not reconstruct from memory.** A third sentence (the manual-path list `[evryn-dev-workspace/CLAUDE.md | …]`) changes **if and only if** the cutover lands — batch all three into one approval.

### Precondition 2 — the three cross-repo pointers (EXACT LOCATIONS)

Each sends its agent to *"AC's `CLAUDE.md`"* for the cross-repo sync ritual; after merge that file is a 78-line router containing none of it. **That content now lives at `_evryn-meta/.claude/agents/ac.md`.**

| Repo | File | Line (verified 2026-08-13) |
|---|---|---|
| `evryn-dev-workspace` | `CLAUDE.md` | **13** |
| `evryn-quality` | `CLAUDE.md` | **19** |
| `evryn-ops` | `CLAUDE.md` | **13** |

⚠️ **These CANNOT ride this branch** — it is an `_evryn-meta` branch. They are a coordinated same-window change across three repos. **Re-grep before editing** (`grep -n "AC's .CLAUDE.md" <repo>/CLAUDE.md`); line numbers drift. **The cutover dissolves this entirely** by making the reference intra-repo.

### Precondition 3 — the rebase

```bash
git -C _evryn-meta-lane-a merge-base --is-ancestor main acm/loading-architecture   # non-zero => rebase needed
git -C _evryn-meta-lane-a rebase main
```
🔴 **Then RE-RUN the frozen-block check — a rebase can silently take the wrong side of a conflict:**
```bash
f=docs/protocols/ac-orchestration-protocol.md
git show <pre-rebase-sha>:$f | tr -d '\r' | awk '/^<identity>/,/^<\/receipts>/' > /tmp/a
tr -d '\r' < $f              | awk '/^<identity>/,/^<\/receipts>/' > /tmp/b
cmp /tmp/a /tmp/b && echo UNTOUCHED
```
⚠️ **Normalize CRLF (`tr -d '\r'`) or this false-FAILS** — the repo is `core.autocrlf=true`. ⚠️ **And when you build a control, mutate a line you have PROVEN is non-blank:** `sed 's/./X/'` on a blank line silently does nothing and prints a meaningless PASS. **I wasted a control exactly this way.**

### The cutover — the ORDER matters, and step 2 is the one that gets skipped

1. **Merge** the branch to `main`.
2. 🔴 **Justin starts a COMPLETELY FRESH session.** The agent registry is read **at session start**; nothing built here is spawnable until he does. **A reload is not enough — this is the known case where a re-spin beats compact-and-continue.**
3. **Verify** `ac`, `dc`, `qc`, `oc` appear as spawnable agent types. **If they do not, STOP** — do not proceed, or step 5's redirects will strand three manuals.
4. **Run each definition's drift-check** (in its maintainer notes) and confirm all three are clean **before** converting anything.
5. **Convert** `evryn-dev-workspace/CLAUDE.md`, `evryn-quality/CLAUDE.md`, `evryn-ops/CLAUDE.md` to redirects; repos stay as backups.
6. **Update in the same change:** the router's transitional note (below its agent table) and the manual-path line in `ac-orchestration-protocol.md` part 1 — **both currently state the repo files are authoritative, which this step makes false.**

## 11.4 — HOW TO REBUILD OR RE-VERIFY AN AGENT DEFINITION

**The scratchpad headers die with me — and nothing is lost: each header IS the committed file, everything above the `<!-- ===== MANUAL BODY BEGINS` marker. Do not hunt for the scratchpad.**

```bash
awk '/^<!-- ===== MANUAL BODY/{print; exit} {print}' _evryn-meta/.claude/agents/qc.md > /tmp/hdr.md
cat /tmp/hdr.md evryn-quality/CLAUDE.md > _evryn-meta/.claude/agents/qc.md
diff <(tr -d '\r' < evryn-quality/CLAUDE.md) \
     <(awk '/^<!-- ===== MANUAL BODY/{f=1;next} f' _evryn-meta/.claude/agents/qc.md | tr -d '\r')
```
**Update the SHA in the header's provenance line when you do.** 🔴 **The trap I actually hit: never let the marker string appear twice in the file.** My first build embedded a drift-check command containing its own marker, so `awk` matched the command text and silently captured 16 extra lines — **the file was correct and only the verification was wrong.** Hence the start-of-line anchor `/^<!-- ===== MANUAL BODY/`, which prose cannot match.

## 11.5 — RESIDUAL RISKS ACm3 SHOULD CARRY

- 🔴 **Three verbatim manual copies now exist.** Between merge and cutover, any edit to a repo `CLAUDE.md` silently forks it. **The drift-check is the detector and it only works if someone runs it** — cutover step 4 is where.
- 🔴 **The common-floor hazard is now WORSE** (§8's rule still governs): AC's build-level-code commit carve-out must never reach DC or QC, and there are now more copies of the commit-discipline prose for a merge to get wrong.
- **`_evryn-meta/identity/` is gone**, not merely emptied. Check anything that assumed that path.
- **The core is 692 lines and the ~600 target is RETIRED** (Justin, at my recommendation). Do not re-open it as a line-count exercise.

---

# §12 — SET-DOWN: fragment sweep + fresh-eyes pass (ACm2, 2026-08-13). READ THIS BEFORE §9–§11.

## 12.1 — ⭐ THE LANE'S BUILD WORK IS COMPLETE. You are inheriting a MERGE, not a build.

**Stated plainly because §10 and §11 describe state and gates without ever saying this outright, and a fresh instance will look for work that no longer exists.**

| Deliverable | Status |
|---|---|
| (i) CLAUDE.md → router + agent manuals | ✅ **DONE** (Phase A) |
| (ii) protocol → core + per-target sub-docs | ✅ **DONE** (Phase A) |
| Phase B — the rule-vs-story cut | ✅ **DONE** by deletion (§11.1 has every verdict) |
| The ten-reference audit | ✅ **DONE** — 2 were genuinely broken, both fixed |
| DC/QC/OC agent definitions | ✅ **DONE**, byte-verified |
| AC manual → `.claude/agents/ac.md` | ✅ **DONE** |

🔴 **NOTHING IS LEFT TO BUILD. What remains is entirely gates** — §11.3's three merge preconditions and the six-step cutover, **none of which is ACm3's to close.** ⇒ **If you are ACm3, your job is to shepherd those, not to find more to cut.** Do not go looking for Phase C.

## 12.2 — 🔴 JUSTIN'S RULINGS, PERSISTED. These existed ONLY in a spin message.

**This is the exact class §11.3 flags as not surviving a session — the frozen-sentence drafts were lost this way. Recorded here so ACm3 does not re-litigate settled calls or mistake them for open proposals.** All four are Justin's, relayed via AC0 on 2026-08-13:

1. ✅ **Phase-B-by-deletion STANDS.** He agreed the sidecar was the wrong answer. The argument he accepted, in his words back: *a rationale doc looks live, so it invites being trusted as current while nobody maintains it — a stale-doc generator.*
2. ✅ **The ~600-line target is RETIRED**, at ACm2's recommendation.
3. ✅ **Keeping `.claude/agents/ac.md`** — the move stands; do not revert it.
4. ✅ **The model-pinning correction is ACCEPTED** — frontmatter kills the *accidental* recursive burn, not the deliberate one. §9.5's stronger claim ("not-performable") is superseded.

**Sequence he set:** Justin has reviewed **nothing** yet → he gets a ~200-line review list → **then** merge → **then** cutover, with every lane stood down.

## 12.3 — ⚠️ PUSH STATE, AND WHY IT MATTERS MORE THAN USUAL

**This branch had NEVER been pushed until 2026-08-13** — it existed only on Justin's laptop, from lane open until then. It was pushed at the commit carrying the Phase-B/audit/definitions work; **every commit after that, including this one, is again local-only.**

🔴 **Do not assume the branch is backed up. Check, don't trust:**
```bash
git -C _evryn-meta-lane-a log --oneline origin/acm/loading-architecture..HEAD   # any output = unpushed work
```
**Pushing is not ACm3's call** (hard floor — Justin's), but **flagging an unpushed delta is**, because on a single machine that delta is the only copy.

## 12.4 — FRESH-EYES CORRECTION TO §10.1's LOAD LIST — it would waste ~60K tokens

**§10.1 row 6 says to load `.claude/agents/{dc,qc,oc}.md`. Taken literally that is ~1,230 lines, and almost all of it is the three repo manuals reproduced verbatim.**

⇒ **Read only each file's HEADER — everything above the `<!-- ===== MANUAL BODY BEGINS` marker (~30 lines each).** That is the entire ACm2-authored content: frontmatter, the spawn-is-not-a-brief guard, and the maintainer notes. **Read a BODY only if you are changing that agent's manual**, and if you do, read it from its repo source, not the copy.

## 12.5 — FRAGMENT SWEEP: session facts that are in no diff

- 🔴 **A `Read` done before a task/resume boundary does NOT satisfy the `Edit` tool afterwards.** It returns *"File has not been read yet"* even though you loaded the file in full earlier in the session. **Re-read the region you are about to edit after every boundary.** Cost me a failed call; will cost ACm3 the same confusion.
- ⚠️ **The `Read` tool's display can show one line number beyond `wc -l`.** On the load trip I reported four files as one line longer than the brief stated; `wc -l` showed the brief was exact. **It is a display artifact, not drift.** This is the practical face of §9's Q4 ruling — *`wc -l` for navigation, the canary for verification, canary wins.*
- 🔴 **PROVENANCE CAVEAT ON A NUMBER I WROTE INTO A SOURCE-OF-TRUTH DOC.** The corrected rates now in `ac-orchestration-protocol.md` — **74.2 and 62.4 tokens/line** — are **ACm's measurements, not mine.** I re-derived the arithmetic and it checks (45,347 ÷ 611 = 74.2; 58,732 ÷ 941 = 62.4), **but I did not independently re-measure either file.** ⇒ **This is one more "doc citing a doc" hop — the exact failure §1 of this brief was opened to stop.** The *direction* of the correction is not in doubt (the old 144/166 came from dense slices and are plainly wrong), but **if anyone leans hard on 74.2/62.4, re-measure first:** `Read` the file with **no `offset` and no `limit`** and take the count from the over-cap notice.
- **Every SHA in §8–§11 is perishable and nothing load-bearing depends on one.** `main` moved twice during this lane in under a day. **All procedures in §11.3 are written as commands that re-derive their own inputs** (`merge-base --is-ancestor`, `git show <pre-rebase-sha>`, a re-grep before editing the cross-repo pointers). **Where a SHA appears, it is an illustration — re-derive it.**

---

# §13 — THE FROZEN SENTENCES: the VERBATIM originals, and the truth about the "drafts" (ACm2, 2026-08-13)

## 13.1 — 🔴 CORRECTION: THE REPLACEMENT DRAFTS DO NOT EXIST, AND THIS BRIEF SENDS YOU TO A DEAD END

**§8's ruling #1 says *"Drafts are on his ballot — see §8's 'C. THE BLOCKER' entry for the exact before→after."* THAT CROSS-REFERENCE IS FALSE.** §8's "C. THE BLOCKER" contains **only the before**, and even that as an abbreviated partial quote. **There is no "after" anywhere in this brief, and there never was.**

**ACm2's sworn position, stated precisely because the distinction matters:** **I never saw draft replacement wording at all** — not in a file, not in a brief section, not in any message sent to me. This is **different from "they existed and were lost."** If drafts were ever composed, they were composed by **ACm** (the first Lane A agent) in conversation with **AC0-35**, and I would have no way to know. ⇒ **The right place to look is ACm, if it is still resumable within AC0-35's session; otherwise they were never written down and Justin writes them fresh.**

🔴 **DO NOT RECONSTRUCT THEM FROM MEMORY OR FROM THE PARTIAL QUOTES IN §8.** These sentences sit inside the HARD RULE's character-for-character frozen set; an approximation entering a source-of-truth file carrying Justin's approval is worse than having nothing. **Verbatim or nothing.**

## 13.2 — THE ORIGINALS, VERBATIM FROM DISK. This is what a drafter needs in front of them.

**Recorded here so nobody has to hunt, and so nobody quotes the abbreviated version in §8 by mistake.**

**SENTENCE 1 — FALSE NOW.** File `docs/protocols/ac-orchestration-protocol.md`, block `<identity>` (the DC/QC/OC variant), second-to-last sentence:

```
The CLAUDE.md that auto-loaded into your context is AC's (Architect Claude's) operating manual — that is NOT yours, and you are not AC.
```

**SENTENCE 2 — FALSE NOW, AND FALSE TWICE OVER.** File `docs/protocols/orchestration/spinning-an-ac.md`, substitution 1 (the AC variant), second-to-last sentence:

```
The CLAUDE.md that auto-loaded into your context IS your operating manual (`_evryn-meta/CLAUDE.md`) — it is yours, and you are AC.
```

⚠️ **A drafter must fix BOTH faults in sentence 2, not just the obvious one:** (a) the auto-loaded file is now a **router**, not anyone's manual; **and (b) the parenthetical path is stale regardless** — AC's manual moved to `_evryn-meta/.claude/agents/ac.md`. **Fixing only the router half leaves a wrong path behind.**

**SENTENCE 3 — TRUE NOW, FALSE ONLY IF THE CUTOVER LANDS.** File `docs/protocols/ac-orchestration-protocol.md`, block `<identity>` (the DC/QC/OC variant), final sentence:

```
Your operating manual is [evryn-dev-workspace/CLAUDE.md | evryn-quality/CLAUDE.md | evryn-ops/CLAUDE.md]. Read it in full, and follow it faithfully first, before anything else.
```

**⇒ This resolves Justin's "two or three."** It is **two false today, three after cutover** — the count is ambiguous because sentence 3 is *conditional*, not because anyone miscounted. **Batch all three into one approval** (§11.3, precondition 1) so he rules once rather than twice.

## 13.3 — WHERE THE ORIGINALS LIVE, IF YOU WANT TO RE-READ THEM YOURSELF

Do not trust the quotes above over the files. Re-read with:
```bash
awk '/^<identity>/,/^<\/identity>/' docs/protocols/ac-orchestration-protocol.md          # sentences 1 and 3
sed -n '/^1\. \*\*`<identity>` — replace the block entirely with:\*\*/,/^   \*(Two clauses/p' \
    docs/protocols/orchestration/spinning-an-ac.md                                        # sentence 2
```
**Both blocks were verified byte-identical to their pre-lane state at every commit in this lane — ACm2 changed no character of either.**

---

# §14 — THE REPLACEMENT DRAFTS: RECOVERED. Answering §13.1 directly. (ACm, 2026-08-13)

> 🔴 **NOTHING HERE IS APPROVED. These are DRAFTS AWAITING JUSTIN'S APPROVAL.** No agent may apply them. Until he approves the exact words, the on-disk originals in §13.2 stand unchanged.

## 14.1 — §13.1 asked the right question and named the right source. Here is the answer.

**§13.1 states:** *"If drafts were ever composed, they were composed by ACm… The right place to look is ACm, if it is still resumable within AC0-35's session; otherwise they were never written down and Justin writes them fresh."*

✅ **ACm is that instance, was still resumable, and DOES hold them verbatim.** They were authored by ACm and relayed to AC0-35 in a report — which is precisely why they never reached a file, and precisely why §13.1 could not find them. **ACm2's caution was correct and its conclusion ("otherwise never written down") is now superseded rather than wrong.**

⚠️ **§13.1's warning still governs everything below:** these are the wording ACm *proposed*, not wording Justin *approved*. **Verbatim or nothing — and these are verbatim.**

## 14.2 — THE DRAFTS, AS AUTHORED

```
DRAFT for SENTENCE 1 (DC/QC/OC <identity>, in ac-orchestration-protocol.md):

The CLAUDE.md that auto-loaded into your context is a ROUTER, not an operating manual — it is not yours and it is not enough.
```

⚠️ **Scope note:** ACm drafted a replacement for the **second-to-last sentence only** (§13.2's SENTENCE 1). The following sentence — §13.2's SENTENCE 3, the manual-path list — was carried over **unchanged**, and ACm signalled that in its report with a `[…]` elision. **That `[…]` was shorthand for "unchanged," NOT proposed text. Do not paste an ellipsis into the block.**

```
DRAFT for SENTENCE 2 (AC variant <identity>, in orchestration/spinning-an-ac.md):

The CLAUDE.md that auto-loaded into your context is a ROUTER, not your operating manual. Yours is `_evryn-meta/identity/ac.md` — it is yours, and you are AC. Do not assume any auto-load was complete: read it in full yourself, confirm its bottom truncation canary, and follow it faithfully first, before anything else.
```

🔴 **THIS DRAFT IS NOW PARTLY STALE, AND §13.2 ALREADY PREDICTED WHY.** It hardcodes **`_evryn-meta/identity/ac.md`**. ACm wrote it *before* AC's manual was moved. **Per §10.4 and §12.2 item 3, that file is now `_evryn-meta/.claude/agents/ac.md`, and Justin ruled the move stands.**

⇒ **The draft fixes fault (a) — "router, not a manual" — and reintroduces fault (b), the stale path, which §13.2 warns must ALSO be fixed.** **Whoever finalises this must swap the path.** ACm is deliberately **not** doing so: altering proposed frozen-block wording is Justin's, and a half-corrected draft in a file is worse than a clearly-flagged one.

## 14.3 — ⚠️ THREE MORE OCCURRENCES, IN NO OTHER SECTION OF THIS BRIEF

**§11.3 and §13 enumerate three sentences. There are three further `CLAUDE.md` references inside the frozen `<mandatory_load>` block** *(line numbers read off disk 2026-08-13; they drift — re-grep)*:

- **line 350** — *"Load your Startup Context Cascade — the section by exactly that name in your CLAUDE.md"*
- **line 354** — *"include the agent's own CLAUDE.md (list it here too…)"*
- **line 360** — *"Then execute your CLAUDE.md's \"Context Discipline\" section"*

**These are AC-variant-only problems.** They stay **correct for DC/QC/OC**, whose manuals genuinely are `CLAUDE.md` files — **until the cutover, after which they go stale for everyone.** For **AC** they are already imprecise, because AC's manual is no longer a `CLAUDE.md`.

✅ **They very likely do NOT need the shared block edited, and there is precedent in this very protocol.** The **team variant already solves the identical problem additively**: its substitution 2 rewrites the opener and its substitution 4 repoints the Context-Discipline gate, **without touching the shared text.** ⇒ **The AC variant could gain the same two substitutions.** That is a cheaper, precedented ask than a rewrite — **but still Justin's**, since the substitution lists sit inside the HARD RULE's protected set.

## 14.4 — WHAT ACm RECOMMENDS JUSTIN BE ASKED, IN ONE SITTING

1. **Sentence 1** — approve, adjust, or replace DRAFT 1.
2. **Sentence 2** — approve DRAFT 2 **with the path corrected to `_evryn-meta/.claude/agents/ac.md`**, or replace it.
3. **Sentence 3** (the path list) — **only if the cutover lands.** Batch it now so he rules once.
4. **The three `<mandatory_load>` occurrences** — approve two additive AC-variant substitutions, or rule that they ride the same edit.


---

# §15 — RECONCILIATION AND MERGE PREP (AC0-37d, 2026-08-18, on the LAPTOP). READ THIS BEFORE §9–§14.

> **Who wrote this:** AC0-37d, the product-lane conductor, shepherding this lane's MERGE. **§12.1 is right that nothing is left to BUILD** — everything below is merge work, not a Phase C.

## 15.0 — MACHINE NOTE. Every absolute path in §1 through §14 is a DESKTOP path.

**Justin works this estate from two machines.** Desktop root: `/c/Users/Justin/Evryn/Code`. **Laptop root: `/c/Users/jbmcg/Evryn/Code`.** §5, §9.1, §10.1 and §12.3 all hardcode the desktop form. **Substitute when you run them — and substitute back going the other way.** Better: derive the root rather than type it (`cd "$(git rev-parse --show-toplevel)/.."`). ⚠️ **A wrong path in a `Monitor` snippet does not error — it goes quiet, and quiet is indistinguishable from "nothing has changed."**

## 15.1 — 🔴 JUSTIN IS NOT REVIEWING THIS BRANCH. THAT CHANGED TODAY, AND IT CHANGES WHO THE BACKSTOP IS.

**§12.2 records his sequence as *"he gets a ~200-line review list, then merge, then cutover."* THAT HALF IS SUPERSEDED.** His words, 2026-08-18: *"there's no way I'm doing this whole review — merging code is way above my pay grade. I need YOU to do that. If there are issues that you need my sign-off on, I can absolutely do that. There is no other backstop on a lot of this; YOU are the backstop."*

**⇒ What still goes to him, and it is narrow:** frozen-block wording (structurally his — the HARD RULE names him), genuine either-way design calls, and anything that changes the risk picture. **⇒ What does NOT: the diff.** ⚠️ **So the reviewing AC has no second reader.** QC does not review docs, ACf is a peer on another runtime, and Justin has stood down as the reader. **Work accordingly; slower is correct here.**

## 15.2 — ⭐ THE LOSS AUDIT: THREE OF MAIN'S CHANGES WOULD HAVE BEEN SILENTLY REVERTED. FIXED IN `5ba5978`.

🔴 **THE MECHANISM IS THE PART TO CARRY, BECAUSE IT DEFEATS THE OBVIOUS SAFEGUARD: git merges this branch CLEANLY, and that is exactly why it is dangerous.** The branch deletes `CLAUDE.md`'s body and creates `.claude/agents/ac.md`; `main` edits `CLAUDE.md`. **Different files — so there is no conflict, nothing to resolve, and nothing to notice.** The 2026-08-17 current-state restructure was the *safer* case precisely because it conflicted. **Here, a day of work on the manual would simply have ceased to exist, and the merge would have looked completely normal.**

**Established by diffing `main:CLAUDE.md` (643 lines) against `branch:.claude/agents/ac.md` (653) end to end — 17 differences, classified one at a time.** Not by reading hunks.

**The three carried forward** (all committed to `main` on 2026-08-18 as `73c5a62` + `03569b6`, after this branch forked at `8b82fb1`):

1. **SESSION STARTUP / repo-sync check** — the arrival-check pointer: a machine switch proves each repo is *synced*, not that the work is *reachable*.
2. **Architectural Mandate** — *"prefer simple over clever"* gained a discriminator. **The branch would have REVERTED it** to the older *"know the difference between simple and naive,"* which names the failure mode without giving any way to detect it.
3. **Worktree & Branch Discipline** — the corrected batten-down script (reachability from any remote-tracking ref, plus `git cherry` by patch-id; **deliberately NOT `origin/HEAD`, which is a local cached symref that goes stale**), the two-questions bullet, and the entire **third question** with its enumeration script. **About 25 lines, and the largest of the three.**

⚠️ **A MECHANICAL "MAIN WINS" RECONCILIATION WOULD HAVE BEEN ACTIVELY WRONG — 14 of the 17 differences are places the BRANCH is correct and `main` is stale.** Three worth naming so nobody re-imports them:
- **`main`'s DC line calls the mailbox model *"the fallback"* while `main`'s own later section calls it RETIRED.** The branch fixed that self-contradiction.
- **`main` still asserts *"Subagents auto-load AC's CLAUDE.md — this is the known failure mode."*** The router killed that problem; re-importing it would ship a false claim about the system this lane built.
- **Two pointers Phase A moved** into `docs/protocols/orchestration/spinning-dc-qc.md`.

**⇒ The merge is per-hunk judgment, not a strategy flag.**

**Controls, all run, both sides CRLF-normalized:**
- **Before replacing region 3, proved `ac.md`'s copy was byte-identical to the merge-base version** — so the splice could not clobber branch work.
- **Only the three intended regions changed:** 6 hunks, 5 lines removed, 37 added, net +32 (653 → 685).
- **The control rejects a one-character mutation** of a line proven non-blank, so a PASS means something.
- **Reconciliation re-run after the fix: lines present only in `main` went 45 → 8**, and all 8 are the known branch-correct cases above.
- Line endings consistently CRLF (685/685); canary intact; **the committed blob was verified, not just the working tree.**

⚠️ **AN INSTRUMENT FAILURE WORTH REPEATING BECAUSE THIS LANE ALREADY DOCUMENTED IT AND IT STILL FIRED:** the first run of the *"did I change only what I intended?"* control compared a CRLF file against an LF-normalized one and **reported the entire 653-line file as changed.** *(§8's closing entry records the identical failure.)* **It was caught only because the verdict was ABSURD** — one hunk spanning a whole file, on a three-line edit. **A slightly wrong verdict would have been believed.** ⇒ **Normalize BOTH sides, and prefer a control whose failure looks ridiculous over one whose failure looks plausible.**

## 15.3 — ✅ TWO THINGS THAT CAME BACK CLEAN, so nobody re-checks them

- **`ac-orchestration-protocol.md` has ZERO losses.** `main`'s only change since the fork was one header line the branch deleted wholesale, and the fact it carried (the mailbox model RETIRED to `docs/historical/ac-dc-protocol.md`) **is preserved on the branch** in the core's *"On the mailbox model"* block. Nothing to carry.
- **The three agent definitions show ZERO drift.** `.claude/agents/{dc,qc,oc}.md` bodies are byte-identical to `evryn-dev-workspace/CLAUDE.md`, `evryn-quality/CLAUDE.md` and `evryn-ops/CLAUDE.md` on `main`. **That is cutover step 4 (§11.3) already satisfied** — re-run it at cutover anyway, but expect green.
- **The whole conflict surface is THREE files:** `CLAUDE.md`, `docs/protocols/ac-orchestration-protocol.md`, and `.claude/agents/acm-probe.md` (a throwaway probe). **Everything else this branch touches, `main` never touched.**

## 15.4 — STATE, and what is still owed

- **Worktree materialized** at `_evryn-meta-lane-a` on `acm/loading-architecture` — plain `git worktree add`, **no junction**, so the `node_modules` hazard does not arise (`_evryn-meta` has none at its root and doc work needs none).
- **§12.3's unpushed-delta warning was RESOLVED at the materialize** — the branch stood at 0 commits ahead of `origin`. ⚠️ **It no longer is: `5ba5978` is local-only. Pushing is Justin's.**
- **The three merge preconditions in §11.3 are UNCHANGED and none is closed.** The frozen sentences (his), the three cross-repo pointers (a coordinated same-window change in three repos, which the cutover dissolves), and the rebase.
- 🔴 **The post-rebase frozen-block byte check NEEDS A NEW EXPECTED VALUE BEFORE ANYONE RUNS IT.** Two frozen sentences were deliberately changed with Justin's approval on 2026-08-17, **so the check will correctly report a difference — and the next person to run it will read that as a rebase defect.** *(Also flagged in §2 of `docs/working/2026.08.17-ac037c-ac037d-handoff.md`; still owed.)*
- **Open with Justin — the three `<mandatory_load>` references** (§14.3). **AC0-37d recommends the shared-block change over §14.3's additive route**, because the additive route fixes one agent now and leaves a problem that returns for all four at cutover. **Proposed exact wording:** *"your operating manual (the file named in `<identity>`, above)"* at first use, bare *"your operating manual"* thereafter.
  - 🔑 **Two facts make that unambiguous rather than vague:** `<identity>` **already defines that exact term** and binds it to a concrete path (*"Your operating manual is [evryn-dev-workspace/CLAUDE.md | …]"*), and the block **already cross-references `<identity>`** in its second occurrence.
  - ⚠️ **Rejected — naming the files in a parenthetical** (*"your claude.md + agent definition, when relevant"*): it is **already wrong for AC** (whose manual is not a `CLAUDE.md`), **wrong for everyone post-cutover** (those files become redirects), and a *"when relevant"* conditional sits inside the one block whose entire thesis is that the agent must **not** exercise that judgment.
---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
