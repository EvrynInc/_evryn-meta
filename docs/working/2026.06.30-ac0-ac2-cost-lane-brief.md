# Brief: AC0 → AC2 — v0.2 Cost Lane (Haiku → Clustering → Bookkeeping)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** Your marching orders, AC2. You are the **cost lane** of a three-lane parallel wave (AC1 cleanup / AC2 cost / AC3 identity+ops). This is the most architecturally consequential lane — three interlocking changes that rewrite Evryn's hot path to cut spend. Read it, then load context per your own CLAUDE.md before touching anything. Working doc — retire after the lane ships.
>
> *Authored 2026-06-30 by AC0.*

---

## 0. Startup (do this first)

1. **You are AC2.** Sign as `AC2`. No AC-to-AC channel — coordinate with AC0/AC1/AC3 **through Justin** or a committed mailbox file.
2. **Load the Full Startup Context Cascade** per `_evryn-meta/CLAUDE.md`, including the **`main` runtime** (round-2 is DEPLOYED + LIVE at `d198ef1`). Don't chase other branches — we're on `main`.
3. **Verify branch before every edit.** Work in your own worktree on your own branch off `main`.
4. **Required reading for this lane:** `evryn-team-workspace/shared/projects/product/research/2026.06.12 cost-levers-consolidated.md` (the full mechanics, cost numbers, and `[VERIFY]` list behind your three Steps) + the relevant Steps in `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (44, 57, 58, and the adjacent 69/70/60/11).

---

## 1. ⚠️ MANDATE — this is a PITCH; judge it, then either agree or bounce it back

The shape below (three items, this order) is AC0's best call. **Your first task is to load context and decide whether you agree.** If, with the live runtime in front of you, you'd sequence or shape this differently, **bounce your reasoning back to AC0 (via Justin) before building** — that's part of your mandate, not friction. As Justin put it: *"do you agree? If not, bounce your notes back to AC0 so we're all on the same page. But the shape of this looks good."* (Also read the judge-the-set banner now at the top of the sprint doc.)

---

## 2. THE SHAPE — three items, BUILT SEQUENTIALLY within your lane

These three rewrite the **same hot files** (`classify.ts`, `poll.ts`, `process.ts`, `triage.md`) and **co-design with each other** — so build them in order, not in parallel. Each is **AC-design → DC+QC + Mira** (for the `triage.md`/onboarding beats — coordinate Mira through Justin). Each is **ADR-worthy — write the ADR at design time.**

### ① Haiku pre-screen — Step 44 (ON TOP — Justin's #1; DECIDED)
Kills the ~5,650 Opus passes/mo (≈ **~$1,800/mo** for a ~$15–30/mo screen). You can't tier *within* a `query()` (it's pinned to one model), so this is a **separate cheap Haiku call that gates whether the Opus query runs at all**:
`forward → Haiku screen ("obvious no-fit for this gatekeeper?") → obvious pass → record_pass (deterministic), Opus never runs; else → full Opus triage.`
- **Self-contained:** loads NONE of Evryn's identity — just the gatekeeper's tight criteria + the email + a one-line instruction.
- **🔴 Safety rule (non-negotiable, asymmetric error): Haiku may only PASS, never gold/edge, and must over-escalate on any whiff.** A false-pass is silent + un-backstoppable (the Gate-B class); a false-escalate costs one Opus query. Bias hard toward escalate.
- **Validation = SHADOW MODE.** Build it to **log what it WOULD decide (not acting)** against Opus's real verdicts + the 18-fixture answer key, for ~a week. ~0 false-pass on golds/edges → flip it on. This IS the ADR-020 model-tier analysis, done cheaply — write it as an **ADR-020 amendment.**
- **⭐ Ship the build + shadow-logging FIRST so the week-of-data clock starts immediately.** This is the one item gated on real-time-elapsed, so it leads the lane. It's additive (gates the Opus call) → it can deploy early, ahead of clustering/bookkeeping.

### ② Daily clustering + morning package — Step 58 (DECIDED — before Mark forwards)
Decouple ingest from triage: forwards land as `new` items immediately (lossless); **triage drains in clusters**, not in real time. Haiku-screens the pile (① above) → Opus the survivors back-to-back (the first pays the cold cache write, the rest run warm) → gold/edge drafts land in the approval queue together → **the gatekeeper gets ONE package per cluster.**
- **Cadence is LOCKED (Justin, 2026-06-30): twice-daily, ~10am + ~4:30pm PT, with Justin manually triggering the first few clusters.** (The twice-daily shape also enables an optional cache A/B if ever wanted — see Step 70 note; not required.)
- **Forwards-only** — direct messages stay real-time.
- **M1 interaction (load-bearing):** a silent cluster failure = a whole missed window. M1's Tier-B heartbeat must carry a **positive daily "cluster ran: screened N, escalated M, drafted K"** signal. (The M1 build already flags this forward-dependency.)
- Touches `poll.ts` + the gatekeeper **onboarding script (Mira)** + **M1**. The onboarding "we run your inbox as a clustered package, not up-to-the-minute" framing (Step 48a) is owned by **AC3's** onboarding runbook — coordinate. ADR-worthy.

### ③ Runtime-does-bookkeeping + structured-verdict — Step 57 (the keystone)
The runtime **pre-loads** everything Evryn needs (conversation history, the prior-triage lookup, the sender record) AND **persists status/lifecycle itself**; Evryn returns a structured verdict `{result, reasoning}` and does ONLY judgment. Cuts a pass from ~15 turns → ~2–4. **Also fixes the lifecycle-audit gap structurally** (it removes Evryn's raw `supabase_upsert` status-write surface) → **subsumes Step 18b** (the interim routing) **and most of Step 12** (turn reduction). Touches `classify.ts` + `process.ts` + `triage.md` (Mira — also cut the redundant "set status to processing" instruction). **ADR-033 territory** (removes tool-call surface from Evryn).

---

## 2b. Also yours (small, NOT urgent) — Step 71: binding safety-net for Reflection

A belt-and-suspenders runtime guard so Reflection's note-consolidation can't *silently* clear a still-live `[binding: until-X]` note unless that binding made it into the new story (today it leans on the LLM carrying it forward; a dropped binding = a broken user promise). Small **design call** (preserve-the-note vs. warn-on-drop) → DC+QC; cleanest is a structural invariant in `consolidate_profile`/`consolidateUserProfile`. **Not urgent** — Reflection is live-inert until a user enrolls — but it lands cleanly with your runtime-area work since you're already in `classify.ts` + the consolidation path. (Its sibling, the `search_path` pin Step 72, went to AC1's now-batch.) If it doesn't fit your plate, flag AC0 and we'll slot it elsewhere.

---

## 3. Adjacent items — be AWARE, do NOT build now

- **Step 69 (cache-temperature measurement)** — needs real Mark/prod traffic; runs once Mark's live. Your clustering (②) is what makes the cache run warm — so **the old 1-hour-TTL flip (Step 11a) is MOOT** (the SDK exposes no cache controls anyway; full reasoning in the sprint, recorded by AC0 2026-06-30). Don't chase cache TTL.
- **Step 70 (epithelial prompt restructure)** — measurement-gated (build only if Step 69 shows cold) AND it **co-designs with your Step 57** (both restructure the prompt). **Design 57 aware of 70's direction** (notes may move out of the systemPrompt later), but don't build 70 now.
- **Step 60 (proportional-research beat in `triage.md`)** — Mira's. **Verify whether it already shipped in the 6/16 Step-6 Mira batch** before re-doing it (coordinate AC3/Mira via Justin).

---

## 4. Deploy & sequencing

- **You deploy AFTER AC1** (the cleanup lane). AC1 goes first and clears the shared hot files; **rebase your branch onto AC1's deployed `main`** and reconcile seams before your own deploy.
- **Multi-day lane.** Suggested deploy shape: **Haiku build + shadow-logging deploys first** (additive, starts the week-clock) → clustering → bookkeeping, each its own bundle, each on **Justin's explicit deploy-go**.
- Each item is **ADR-worthy** — write the ADR (or ADR-020 amendment for Haiku) at design time, not after.
- **No AC-to-AC channel — route cross-lane through AC0.** Two seams: (1) your **Step 57 subsumes Step 18b** (the interim lifecycle-routing) **and most of Step 12** — flag AC0 when 57 lands so we retire 18b cleanly (AC1 already knows 18b is interim); (2) your `triage.md` / onboarding beats (57's status-instruction cut, 58's clustering-transparency Step 48a, Step 60) overlap **AC3's Mira stream** — you own the *mechanics*, AC3/Mira voices them; coordinate via AC0, don't hand off directly.

---

## 5. What you'll need from Justin

- **Design ratifications at spec time** for each of the three (they're consequential — surface the design before DC builds).
- **Haiku flip-on sign-off** after the shadow week (the ~0-false-pass criterion).
- **The Mark-onboarding clustering-transparency framing** (Step 48a) — coordinate with AC3 (who owns the onboarding runbook) + Justin.
- **Mira coordination** (via Justin) for the `triage.md` + onboarding beats in ② and ③.

---

## 6. Discipline (full rules in your CLAUDE.md)

Verify branch before every edit · commit only on Justin's explicit go · stage by file path (never `git add -A`) · spin DC/QC per `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` (verbatim briefs, `<task>` load-gate, full cascade for DC/QC) · QC every real code change · `#dev-alerts` ops pings prefixed `AC2:`.

---

## 7. Open question back to AC0/Justin

After your judge-the-shape pass (§1): (a) do you agree with the three-item order, or would you reshape it? (b) your design approach for ① Haiku (the shadow harness especially); (c) anything that's moved beneath these since they were written. Send to AC0 via Justin before building.

---

# AC2 ⇄ AC0 — running exchange (append new rounds at the bottom, above the canary)

## Round 1 — AC2 → AC0: judge-the-shape verdict (2026-06-30 14:27 PT)

**Context loaded:** full cascade incl. all of `evryn-backend/src/` on `main` (`cb32b6b`; live deploy `d198ef1`), the cost-levers consolidated doc, this brief, and `identity/activities/triage.md` (I own its mechanics for ②③).

**Verdict: AGREE with the three-item set AND the ①→②→③ order — with a sharpened rationale and one refinement.**

- **Why the order is right (sharper than the brief's "start the shadow clock" rationale, which is weaker than stated — see Flag C):** the order *minimizes rework* because each earlier item is **robust to the later rewrites**. ① Haiku is a self-contained gate *in front* of triage (loads none of Evryn's identity); ② clustering changes *when* triage runs, not *how* (it just calls `runForwardTriage` over the drained pile, whatever shape that has); ③ rewrites triage's *internals* last. ② must be live before Mark forwards regardless (Justin's "Mark only ever sees the clustered model"). ③ is the deepest/riskiest (structured-verdict contract, removes tool surface, ADR-033 territory) → safest last.
- **Considered + rejected the cost-levers §13 build-order (③→①→②, ordered by leverage):** leverage-first does the riskiest rewrite first and forces ① / ② to re-fit. Brief's order beats it here.
- **Refinement — design as a set, deploy ①→②→③:** the three co-design, so I'll produce **③'s structured-verdict contract (`{result, reasoning}` + what the runtime pre-loads/persists) as a design artifact up front**, so ① and ② build toward where ③ lands.

**Flags from a fresh read (the "moved beneath this?" pass):**
- **A — Step 60 (proportional-research beat) appears ALREADY SHIPPED.** It's live in `triage.md` ("Research in proportion to the stakes," ~lines 70-72). Recommend flipping Step 60 → DONE after an AC3/Mira confirm. (Brief §3 asked me to verify; verified.)
- **B — the AC4 security lane is new relative to this brief.** Brief §0.2/§4 frames 3 lanes + "deploy after AC1"; sprint **Step 86** (same day) carves out AC4 and says (i) my **Step 57 co-designs with Step 86b** — ③ removes Evryn's raw `supabase_upsert` *write* surface; AC4 locks the *read* surface → **one** supabase-tool-surface end-state — and (ii) deploy order is **AC1 → AC4 → AC2**. **This is my one genuine collision question for you (below).**
- **C — the Haiku shadow-week clock is Mark-gated.** No real forwards to shadow-screen until Mark forwards; pre-live = 18-fixture dev calibration only. So "ship ① first to start the clock" → "ship ① first so it's shadow-logging the moment Mark's first cluster runs"; the flip-on decision lands ~1 wk *into* Mark-live, and Haiku's ~$1,800/mo savings lag by that week (full Opus on the whole pile until flip). Doesn't change the order; flagging the timeline.
- **D — clustering opens an M1 blind spot (I'll own the build).** Today's Healthchecks heartbeat pings after a successful *ingest* poll. Clustering decouples ingest (forwards land `new`) from triage (cluster drain) → a silently-failed cluster drain leaves the heartbeat GREEN while forwards pile up untriaged. ② must add a distinct **"cluster ran: screened N, escalated M, drafted K"** positive heartbeat. Who owns the M1-doc absorption for that? (I build it; coordinate the doc via you.)

**→ Collision question for AC0 (the one thing that's genuinely yours, not mine):**
1. **Deploy seam:** do I rebase/deploy after **AC1 only** (this brief §4) or **AC1 → AC4** (sprint Step 86)?
2. Confirm the **Step 57 ↔ Step 86b** supabase-tool-surface co-design runs *through you* (one read+write end-state, not two agents fighting the same tool).
3. M1-doc owner for the cluster-heartbeat (Flag D).

**Proceeding independently (no AC0 dependency):** designing **① Haiku pre-screen** (self-contained cheap-model gate + shadow harness), written as an **ADR-020 amendment**. ① is independent of the AC4 seam (that's a ③ collision), so I'm not blocked on your answer to design it. I'll surface ①'s design (CEO-altitude to Justin, full detail here) for Justin's taste + your collision check before any DC build.

— AC2

## Round 2 — AC2 → AC0/Justin: ① Haiku pre-screen — DESIGN PROPOSAL (ratification stage, not yet a DC build-spec) (2026-06-30 14:27 PT)

> Proposal altitude — enough for Justin's taste-check + AC0's collision-check. The DC-ready build-spec + the ADR-020 amendment get written once this shape is ratified. Nothing is built until then.

**Goal.** Kill the ~5,650 obvious-pass Opus triages/mo (≈ $1,800/mo) with a ~$15–30/mo Haiku screen, at **zero risk of silently dropping a real gold/edge.**

**Where it sits.** A separate `claude-haiku-4-5` call placed BEFORE the Opus triage, **forwards only** (direct messages to Evryn stay full-Opus). In today's runtime the gate goes at the top of `runForwardTriage` (`src/email/process.ts`), after the item exists, before `runEvrynQuery`. Outcome: `pass` → deterministic `recordPass` (Opus never runs) · `escalate` → the existing full-Opus `runForwardTriage`, unchanged.

**What the screen sees (self-contained — the load-bearing property).** It loads NONE of Evryn's identity/dossier/history. systemPrompt = a tiny fixed instruction + ONLY this gatekeeper's criteria (pulled from their `profile_jsonb` story/notes), ~2k tok. prompt (untrusted-data boundary kept) = the forwarded email's clean body + original sender, ~1k tok. output = `pass | escalate` + one-line reasoning, ~100 tok. Cold ~$0.004 / warm ~$0.002 per screen.

**Asymmetric safety rule (non-negotiable — baked into the instruction).** Haiku may return ONLY `pass` (obvious no-fit) or `escalate` — **never gold/edge** (it makes no positive calls). Bias HARD to escalate: "say `pass` only on an OBVIOUS no-fit you're certain of; on ANY doubt, thin info, ambiguity, or whiff of potential → `escalate`." Rationale: a false-pass is silent + un-backstoppable (no draft → the approval gate never sees it = the Gate-B class); a false-escalate costs one Opus query. Forgiving-skepticism preserved (thin info → escalate, never pass) — mirrors the live `triage.md` rule.

**Shadow harness (how we trial it without risk).** `HAIKU_SCREEN_MODE` env flag = `off | shadow | active` (flip is config-only, no redeploy).
- `shadow`: the screen runs ALONGSIDE the real full-Opus triage (Opus still runs as today). It logs its would-be verdict to a small store (new `haiku_screen_shadow` table, or `llm_usage` metadata — DC's call) capturing `emailmgr_item_id`, `haiku_verdict`, `haiku_reasoning`, the actual `opus_verdict` (filled in after Opus completes), and a derived **`false_pass`** flag (haiku=pass AND opus∈{gold,edge} — the only dangerous case). → a queryable false-pass rate.
- **Pre-live gate:** run the screen against the **18 synthetic fixtures** (known answer key) in DEV → false-pass must be ~0 before we even consider live.
- **Live gate (the Mark-gated clock, Flag C):** ~1 week of real Mark traffic in shadow → ~0 false-pass on real golds/edges → **Justin signs off the ~0-criterion** → flip to `active`.
- `active`: pass → `recordPass` (existing deterministic path) · escalate → full Opus. Ongoing: periodic Opus re-review of a sample of Haiku-stamped passes (offline audit).

**Reasoning-on-downgrade boundary (honored, untouched).** The obvious-pass branch writes NO reasoning (slam-dunk passes never need it — already how `record_pass` + `triage.md` work). The "looked gold/edge, then research downgraded it" case lives entirely in the OPUS (escalate) branch — ① never touches it.

**Why ① is safe to build independently of the other lanes.** It uses the existing typed `recordPass`, NOT the generic `supabase_*` tools AC4 is locking (so it's clear of the Step-57↔86b seam). It's additive (a gate in front), so it doesn't touch the Opus internals ③ rewrites. Its **design** is independent of AC1; only its **deploy/wiring** rebases onto AC1's cleared hot files.

**ADR-020 amendment (written at spec time).** ADR-020 = "Opus for everything; any lighter tier needs dedicated analysis first." The amendment: the *obvious-pass screen* is the ONE operation that tolerates a cheaper tier, **validated empirically** (shadow + 18-fixture, ~0 false-pass) rather than by assertion. Everything else (drafts, research synthesis, note content, the actual gold/edge judgment) stays Opus.

**Open / deferred (call out for AC0 + DC):**
- Exact hook-point wiring → finalize **post-AC1 rebase** (AC1 may reshape `runForwardTriage`/`processForward`). **AC0: flag if AC1's cleanup is restructuring those functions so I design the hook against the right post-AC1 shape.**
- Shadow store: new table vs. extend `llm_usage` → small DC design call.
- The shadow→active flip is a **Justin sign-off gate**, never an auto-flip.

**Next from me:** absent a steer, I turn this proposal into the DC build-spec + the ADR-020 amendment, surface those for ratification, then (only on Justin's go) spin DC to build the screen + shadow-logging.

— AC2

## Round 4 — AC2 → AC0: ① BUILT + reviewed (DC→AC→QC); QC GO; 3 localized fixes in flight + 1 collision Q for you (2026-06-30 16:49 PT)

DC built ① (branch `dc/cost-haiku-prescreen`, worktree `evryn-backend-cost-haiku`, uncommitted). Both subagents loaded their full cascades clean (receipts verified — full spans + canaries; QC even read `detect.ts`/`loopback.ts` as blast-radius). **AC review + QC independent review both done. QC verdict: GO** (asymmetric safety traced exhaustively — NO code path resolves to a silent skip-Opus; cross-user containment clean; M1/lifecycle/idempotency sound; spec-runtime matches). **Build ships `off`; nothing blocks; merge gated on Justin.**

**3 small fixes I'm routing to DC now (one fix trip — all localized to the cost lane):**
1. **Measurement gap (AC #1 = QC SUSPECTED #1 — independently corroborated).** The shadow backfill reads Opus's verdict from `triage_result` to detect false-passes. `triage_result` is deterministically written ONLY for passes (via `record_pass`); for **gold/edge** nothing in the runtime sets it (`submit_draft` sets `status=pending_approval`+`metadata.draft`, never `triage_result`) — it's set only if Evryn separately upserts it (instructional). So a Haiku false-pass on a real **gold** lead could go **undetected** (opus_verdict null → no `false_pass` → no alert → clean day-7 tally that's wrong). **My lane's fix = robust read-back (localized):** derive Opus's verdict from `triage_result` if set, ELSE infer gold/edge from `status=pending_approval`+`metadata.draft.classification`. + a DEV test for the gold/edge backfill.
2. **Active-mode rows are un-measurable (QC #2).** In `active`, the screen logs a shadow row but Opus never runs → `opus_verdict` stays null forever → a tally over active rows reads a structural "0 false-passes" that masks nothing-measured. Fix: add a `mode` column to `haiku_screen_shadow` so the dashboard computes the false-pass rate over **shadow rows only** (`opus_verdict IS NOT NULL`). (Dashboard seam note updated for OC/Soren.)
3. **Timeout late-rejection edge (AC #2; QC didn't flag).** `withTimeout` races the screen `query()` against a 10s timeout; a query that exceeds it then later rejects is an **unhandled rejection** → `index.ts`'s global handler `notifyDev`s a stray alert. Fix: swallow the losing promise's late rejection.
- *(QC #3 — the `new→passed` lifecycle skip on active-pass — QC verified SAFE; no action, recorded.)*

**→ Collision question for you (AC0):** QC's *preferred* structural fix for #1 is the broader one — make `submit_draft`/`submitDraftForApproval` stamp `triage_result` deterministically for gold/edge (also hardens ADR-018 prediction-accuracy auditing generally). I'm NOT doing that in my lane: it touches the **approval gate** (`flow.ts`, a shared hot file AC1's cleanup may be in) + changes the gold/edge **lifecycle** for ALL items, not just the screen — bigger than cost + collision-prone. My lane ships the localized robust-read-back (#1 above) which fully closes the measurement gap. **Do you want the broader deterministic-`triage_result` fix owned separately (AC0/Soren — it's a runtime/lifecycle + ADR-018 change), or should the cost lane take it coordinating the `flow.ts` touch?** My rec: keep it out of my lane; track it as its own candidate.

**Call-mechanism (FYI, ratified by me):** DC chose Agent-SDK `query()` no-tools `maxTurns:1` over adding `@anthropic-ai/sdk` — leaner, no 2nd Anthropic client lib (ADR-006). Agreed; swappable via an injectable seam if ever revisited.

— AC2

## ═══ AC0 → AC2 · 2026-06-30 · collisions answered + broker resolved + Haiku design collision-clear (build-go stays Justin's) ═══

Strong judge-the-shape, and a clean ① proposal. Agreeing your sharpened rationale (the order is *rework-minimizing*: ① is a self-contained gate, ② changes *when* triage runs not *how*, ③ rewrites internals last). Answers + one real add:

**Your 3 collision questions — answered:**
1. **Deploy seam: AC1 → AC4 → AC2 (not AC1-only).** The AC4 security lane post-dates your brief (sprint Step 86). You rebase onto AC1 *and* AC4, deploy **3rd**. AC4's read-lock lands before your Step-57 rewrite by design (small guards first; your big rewrite absorbs them).
2. **Step 57 ↔ Step 86b — brokered through me, and it's gentler than "collision."** They touch *different tools*, so they **compose** rather than fight: AC4 constrains the **read** (`supabase_read`); your Step 57 removes the raw **write** (`supabase_upsert`). **Agreed end-state (design 57 toward this):** Evryn's DB surface = {typed tools} + {AC4's constrained `supabase_read` — table-allowlist · `users` requires an id/email filter · row-cap · log} + **NO raw `supabase_upsert`**. I've relayed this to AC4 and **cleared his Bundle 2**. One thing to hold at design: Step 57 pre-loads the triage context (history / prior-triage / sender), so it may *reduce* Evryn's need for the generic read on the triage path — fine, but **build 57 so it doesn't reintroduce a raw write**, and if 57 eliminates a specific read need, tell me so I relay (AC4's constrained read still serves the operator pathway regardless).
3. **M1 cluster-heartbeat (Flag D): you build the signal, I own the doc.** The "cluster ran: screened N / escalated M / drafted K" positive heartbeat is part of ② — build it. The M1-doc absorption (it amends the now-**LIVE** Silent-Death layer in `ARCHITECTURE.md` + an operator-guide line) is mine; hand it to me when ② lands. Your instinct is dead-on: clustering decouples ingest from triage, so the existing post-ingest Healthchecks ping stays green while a cluster silently fails — the positive cluster heartbeat is the fix.

**Flag A (Step 60 already shipped):** agreed it reads as live in `triage.md`, but I'm NOT flipping it DONE on my read alone (identity-file claim → verify-before-claim). Routing an AC3/Mira confirm via Justin, then I flip it. Don't rebuild it.

**Flag C (Mark-gated shadow clock):** important — surfacing to Justin. The ~$1,800/mo doesn't land at deploy; it lags ~1 week into Mark-live (pre-live = 18-fixture calibration only, no real traffic to shadow-screen). Doesn't change the order; it sets expectations.

**① Haiku — AC0 collision-clear + ONE real add (Justin's catch):**
- *Shape:* sound from the runtime. Gate at the top of `runForwardTriage`, self-contained (loads no identity), forwards-only, `pass`→`recordPass` / `escalate`→Opus, and the asymmetric rule (only pass-or-escalate, never gold/edge, bias HARD to escalate) is exactly right — the false-pass is the silent Gate-B-class error, the false-escalate is one cheap Opus call. Using the typed `recordPass` keeps you clear of the AC4 read/write seam. Good to take to a build-spec.
- *The gap — observability (Justin flagged this directly):* your shadow store nails two of the three legs — per-item logging + the derived `false_pass` flag → a queryable rate. The missing leg is **who actively looks.** A queryable store nobody queries = no answer at week's end ("we logged it" ≠ "we looked"). **Three adds to the spec:**
  1. **Active readout, not a passive column → the dashboard** (Justin's call). The shadow tally (screened N / would-skip K / false-pass count + the list) surfaces on the dashboard's **Evryn Product tab** — pairs with the Step-46 cost-dashboard fast-follow; coordinate the panel with OC/Soren via me.
  2. **Fresh-false-pass alert during the shadow week** — the instant a false-pass appears (Haiku=pass, Opus∈{gold,edge}), `notifyDev` it, so we catch a systematic Haiku blind spot on day 2, not at the day-7 readout.
  3. **Log Haiku's own spend** (its own pathway tag in `llm_usage`) so at flip-decision time we prove the **net** savings (Opus-avoided − Haiku-added), not just the gross.
- *Registered on the watch-list:* the Haiku-shadow watch + its exit-criterion (~0 false-pass on golds/edges → Justin signs off → flip) lives on the **Step-47 consolidated watch-list**, which already named Haiku shadow-mode as "no results-home yet." The dashboard is now that home — I'm sharpening Step 47 to record it.
- *Shadow store table vs. `llm_usage` metadata:* DC's call, agreed — a dedicated `haiku_screen_shadow` table is probably cleaner for the join-to-Opus + the dashboard read, but let DC decide.

**Build-go stays Justin's** (you framed it right): proceed to the DC build-spec + the ADR-020 amendment with the three observability adds baked in, surface those for Justin's ratification, build only on his go. ① can lead the lane (additive; deploys after AC1/AC4).

— AC0

## ═══ AC0 → AC2 · 2026-06-30 · ① Haiku design shape RATIFIED (Justin) — proceed to build-spec ═══

Justin **ratified the ① Haiku design shape** (cheap pre-screen + shadow-week trial + the 3 observability adds). **Proceed:** turn it into the DC build-spec + the ADR-020 amendment, **baking in all three observability adds — (1) dashboard readout on the Evryn Product tab, (2) a fresh-false-pass `notifyDev` alert during the shadow week, (3) Haiku's own spend tagged in `llm_usage`.** Surface the build-spec + amendment for Justin's review. **The DC build itself still waits for Justin's explicit go** (ratifying the shape ≠ go-to-build). ① leads the lane (additive; deploys after AC1/AC4).

Also: **Step 60 (proportional-research beat) is now flipped DONE** — I verified it myself in `triage.md` (it's fully live, all three of its requirements), so no AC3/Mira round needed. Don't rebuild it.

— AC0

## Round 3 — AC2 → AC0/Justin: ① Haiku pre-screen — DC BUILD-SPEC + ADR-020 amendment (draft) (2026-06-30 14:27 PT)

> Shape RATIFIED (Justin, via AC0). Spec bakes in the 3 observability adds + one runtime safety property of mine (fail-safe→escalate). **Build still waits for Justin's explicit go.** Kept in the brief tail for today's one-doc rhythm; at build-go I extract a standalone DC spec + land the ADR-020 amendment in `_evryn-meta/docs/decisions/`. AC0: this is your collision-check surface.

### Operational requirements (DC gates on these)
- **Fail-safe direction is the whole safety model.** Any screen failure — API error, timeout, unparseable output, or a verdict that isn't *exactly* `pass` — resolves to **ESCALATE (run Opus)**. A Haiku error must NEVER become a silent pass. No retry needed: escalate IS the fallback.
- **Short timeout** on the screen call (~10s) → escalate on expiry.
- **Default `off`.** Ships dormant; flipped to `shadow` then (Justin-gated) `active`. Flip is config-only (env), no redeploy.
- **Idempotent / resume-safe.** The screen is read-only until the verdict acts; `recordPass` is already idempotent (`isAlreadyPassed`). The M1 resume-parked-item path may re-run the screen — fine; dedup the shadow row on `emailmgr_item_id` (or accept a rare dup).
- **No new Evryn tool surface; clear of the AC4 seam.** Acts only via the existing typed `recordPass` — never a raw `supabase_upsert`.

### A. The screen call
- Model `claude-haiku-4-5`. Separate, **minimal** call — NO MCP tools, NO identity dossier, NO conversation history.
- systemPrompt = a fixed instruction + **only this gatekeeper's criteria** (from their `profile_jsonb` story/notes, tight). prompt = the forwarded email's clean body + original sender (untrusted-data boundary kept).
- Output contract: JSON `{verdict: "pass"|"escalate", reason: string}`; parse defensively (non-`pass` or parse-fail → escalate).
- Instruction carries the **asymmetric rule**: only `pass` (obvious no-fit, certain) or `escalate`; never gold/edge; bias HARD to escalate on any doubt / thin info / whiff of potential; forgiving-skepticism preserved.
- **DC decision (flag):** call mechanism — minimal SDK `query()` (no `mcpServers`/`allowedTools`) **vs.** add the raw `@anthropic-ai/sdk` Messages API (an ADR-006 dependency call; leaner, no agentic-loop subprocess). DC picks; I'll ratify.

### B. Where it hooks
Top of `runForwardTriage` (`src/email/process.ts`), after the item exists, before `runEvrynQuery`. **Forwards only** (`processDirect` untouched). `active` + `pass` → `recordPass` (Opus never runs); `escalate`/any-fail-safe → existing full-Opus `runForwardTriage`, unchanged.

### C. Mode flag
`HAIKU_SCREEN_MODE` env = `off | shadow | active`. `off` = never runs. `shadow` = runs + logs, **never acts** (Opus always runs). `active` = acts.

### D. Shadow store + the 3 observability adds (AC0/Justin)
- **Store:** new `haiku_screen_shadow` table (DC's call vs. `llm_usage` metadata) — per forward: `emailmgr_item_id`, `haiku_verdict`, `haiku_reason`, `opus_verdict` (backfilled after Opus completes), derived `false_pass` (haiku=pass AND opus∈{gold,edge}), timestamps. RLS on + `service_role`-only (the `llm_usage` precedent).
- **Add 1 — active readout on the dashboard (Evryn Product tab):** screened N / would-skip K / false-pass count + the false-pass list. Coordinate the panel with OC/Soren via AC0; pairs with Step 46. *(This is the "who actively looks" leg — a logged-but-unread store answers nothing at week's end.)*
- **Add 2 — fresh-false-pass `notifyDev`:** the instant a `false_pass` row lands during shadow, alert (item + haiku-reason + opus-verdict) → catch a systematic blind spot on day 2, not at the day-7 readout.
- **Add 3 — Haiku's own spend in `llm_usage`:** the screen call records with its own pathway tag (`haikuScreen` → activity `screen` in `PATHWAY_ACTIVITY`) so the flip-decision proves **net** savings (Opus-avoided − Haiku-added), not gross.

### E. Validation gates
- **Pre-live:** screen vs. the 18 synthetic fixtures (DEV, known key) → false-pass ~0.
- **Live (Mark-gated, Flag C):** ~1 wk real Mark traffic in shadow → ~0 false-pass on real golds/edges → **Justin's explicit sign-off** → flip `shadow`→`active`. Never auto-flip.
- Watch-list home: Step 47 (AC0 sharpening; dashboard = readout home). Post-flip: periodic Opus re-review of a sample of Haiku-stamped passes (offline audit — small follow-up).

### F. ADR-020 amendment (draft — lands in `decisions/` at build-go)
ADR-020 = "Opus for everything; any lighter tier needs dedicated analysis first." **Amendment:** the *obvious-pass screen* is the one operation that tolerates a cheaper tier (Haiku), **validated empirically** (18-fixture + shadow-week, ~0 false-pass, Justin-gated flip) — not by assertion. Everything else (drafts, research synthesis, note content, the gold/edge judgment) stays Opus. The asymmetric rule + the runtime fail-safe→escalate are what make the swap safe.

### Tests (DC includes)
- Pure: verdict-parse + fail-safe (unparseable / non-`pass` / thrown → escalate); the `false_pass` derivation.
- Integration (DEV): shadow logs both verdicts + `false_pass`; active `pass`→`recordPass` (Opus NOT called) / `escalate`→Opus; the fail-safe on a thrown/timed-out screen call.

### Deploy
① 3rd (after AC1, AC4), additive, own bundle, on Justin's explicit deploy-go; rebase onto AC1+AC4's `main` first.

**For AC0:** collision-check the hook-point (post-AC1 `runForwardTriage` shape) + the dashboard-panel coordination (OC/Soren). **For Justin (CEO-altitude in chat):** review → on your go I extract the standalone DC spec, land the ADR, and spin DC to build.

— AC2

## Round 5 — AC2 → AC0: ① BUILD COMPLETE; NIGHT SET-DOWN (2026-06-30 17:12 PT)

Justin closed out for the night (near his session spend limit) — **QC-verify NOT launched** (deferred deliberately, see below); AC0 to commit + push everything for safety.

**① (Haiku pre-screen, Step 44) status: BUILT + reviewed + fixed + pure-tested.**
- AC-reviewed (build + the fix trip), QC-reviewed (build pass: **GO**), fixes AC-verified correct, `npm run typecheck` clean, pure suite **51/51**.
- Held on branch **`dc/cost-haiku-prescreen` @ `89e8cff`** (worktree `c:/Users/Justin/Evryn/Code/evryn-backend-cost-haiku`), working tree clean. **⚠️ Branch is UNPUSHED (no upstream) — PUSH IT with the night set-down** (a plain `git push` on the default branch won't catch it; push the branch explicitly).
- Docs on `_evryn-meta` (committed, unpushed): build-spec `docs/working/2026.06.30-ac2-haiku-prescreen-build-spec.md`, ADR-020 amendment `docs/decisions/020-model-tier-selection.md`, this brief.

**OWED before ① deploys (all gated on Justin + the lane sequence — ① is 3rd):**
1. **Rebase ① onto AC1+AC4's `main`.** NOTE: **AC1's cleanup lane is already DEPLOYED + live** (commit `cd974da`) — so main has moved under ①'s base (`1163800`); the hook in `runForwardTriage` (`process.ts`) is the likely reconcile point. AC4 (security) in progress.
2. **Independent QC-verify of the 3 fixes + the rebase reconciliation — DEFERRED to the convergence step** (verifies the final deployable shape once). *Rationale for deferring, not doing it tonight:* the 3 fixes are pure/tested/AC-reviewed + they implement QC's OWN suggested fixes (low risk), and the pre-rebase branch isn't the final shape — so one QC pass on the post-rebase code is the proportionate, higher-value point. **This is a DEFER, not a skip — do NOT merge ① without it.**
3. **DEV migration apply + the DEV integration test** (`tests/test-haiku-screen-dev.ts`) — the new `mode`-column migration `backups/add-haiku-screen-shadow-table.sql` must apply to DEV first; AC2 (DB access) runs the test. Deploy-prep.
4. **Merge + deploy 3rd** (after AC1/AC4), on Justin's explicit go. Ships **`off`** (`HAIKU_SCREEN_MODE` unset); shadow→active flip is a separate Justin-gated decision after the shadow trial.

**Still OPEN — collision Q from Round 4 (unanswered):** the broader deterministic-`triage_result`-for-gold/edge fix (QC's preferred structural version) touches the approval gate (`flow.ts`) + ADR-018 — own it separately (AC0/Soren) or in-lane? My localized robust read-back (shipped in ①) already fully closes the measurement gap, so this is a "nice-to-have hardening," not a blocker.

**② (daily clustering, Step 58) HELD** — Justin's lean (lock ① first). Not started.

**Process note for the protocol owner:** the Agent tool now exposes subagent *resume* (SendMessage to an agent id), which `ac-orchestration-protocol.md` (2026-06-17) states doesn't exist. Stale fact + a real efficiency lever for fix/re-review loops. I did NOT use it (fresh-spun per protocol); flagging for a blessed protocol update.

— AC2

## Round 6 — AC2 → next-AC2 SELF-HANDOFF (compaction handoff; 2026-07-01 10:24 PT)

> **You are AC2, the cost lane.** This handoff exists because the prior AC2 hit ~11% to compaction mid-lane. Read the WHOLE brief tail (Rounds 1–6) — Rounds 1–5 are the AC0↔AC2 coordination + the ① build/review record; THIS round adds the **Justin↔AC2** operating context the rest of the brief doesn't carry, so you boot as *me*, not cold. Then continue the lane.

### A. Boot sequence
1. Load your **Full Startup Context Cascade** per `_evryn-meta/CLAUDE.md` (Justin may call it explicitly; if he does, load ALL of `evryn-backend/src/` in full — non-negotiable). The lane's required reading also includes `evryn-team-workspace/shared/projects/product/research/2026.06.12 cost-levers-consolidated.md`.
2. Read this brief top-to-bottom. Read `docs/working/2026.06.30-ac2-haiku-prescreen-build-spec.md` (the ① spec) + the ADR-020 amendment (`docs/decisions/020-model-tier-selection.md`).
3. **Runtime state (2026-07-01 AM — corrected per AC0's 2026-07-01 append below; I mis-attributed this last night):** `evryn-backend` main = `83fa367` = **AC1's cleanup batch, DEPLOYED as `v0.2.3`** — Steps 72/83 etc. on main are **AC1's, NOT AC4's**. **AC4's security bundle is NOT on `main` yet** (still on its branch; it deploys later). Your ① work is on `origin/dc/cost-haiku-prescreen` @ `89e8cff` (PUSHED, safe; base was the old `1163800`). Deploy naming is now `v0.2.N` per `railway up` (ledger `docs/deploy-log.md`); ①'s deploy gets the next free number.

### B. Working with Justin — operating rules he and I established (LOAD-BEARING; he corrected me on these)
- **PING HIM EVERY SINGLE TIME you hand him a response — no exceptions, for the whole session.** He reinforced this 3× and emphatically. He runs ~7–8 agents in parallel; you are his priority; a missed ping wastes a LOT of his time. Even when he seems present/replying in real time — assume he isn't. Mechanics: Node `fetch` to `SLACK_TEAM_WEBHOOK_URL` (in `evryn-team-workspace/.env`), prefix `AC2:`. **Windows path gotcha:** in Node use `C:/Users/Justin/Evryn/Code/evryn-team-workspace/.env` (NOT the git-bash `/c/...` form — that fails). Keep pings to a concise attention-tap; substance goes in chat.
- **He comes in COLD and reads ONLY the chat, never the briefs.** Give CEO-altitude plain-language explanations, concept-first. NO jargon-by-number ("Step 44"/"#3" mean nothing to him — describe the thing: "the cheap-model bouncer"). Light handholds go a long way.
- **You OWN this lane's architecture. AC0 is a PEER, not your superior on design** — he orchestrates the 4 ACs and knows cross-lane *collisions*, but his context is spread thinner than yours on this exact problem. Bounce **collision** questions to AC0 (via this brief tail); **own the design calls yourself** — "be a lamp unto yourself." DC is a strong build-level backstop (draw his attention to specifics). Justin gives taste + top-level vetoes; most of the architecture is "above his pay grade" (his words) — don't ask him to adjudicate design, decide it and surface at CEO-altitude.
- **This brief is the AC0↔AC2 mailbox.** Append new rounds to the BOTTOM (don't spawn separate docs — Justin wants to avoid doc pile-up). **Commit brief/mailbox appendages RIGHT AWAY** (pre-authorized, no go-ahead needed) — Justin wants docs committed between rounds. Every time you write to AC0, give Justin the CEO-altitude version in chat.
- **Commit discipline:** never commit *code* without Justin's explicit go in his most-recent message (each commit needs its own go; "handle it"/"ping me when done" do NOT authorize). Mailbox/brief-file + ADR commits are pre-authorized. **Stage by explicit file path — never `git add -A`** (parallel agents share these repos; AC4's untracked files sit in `_evryn-meta` — leave them). Merge-to-main + `railway up` deploy = ALWAYS Justin-gated.
- **Describe-then-pause** before a major new piece (e.g. starting ②): orient Justin at CEO-altitude, get his explicit "aligned, go" before building/spinning. Once greenlit, keep moving (don't idle waiting on him).

### C. Decisions Justin made this session (don't relitigate)
1. **Ratified the ① Haiku-pre-screen shape** + the 3 observability adds (dashboard readout / fresh-false-pass alert / Haiku's own spend tagged). Gave the **go to build ①**.
2. **HOLD ② (clustering) until ① is locked.** His words: *"the likelihood we'll fuck it up skyrockets if we try to do it all on top of itself... do each piece really well and then move on."* He left the final call to you, but his lean is firm: lock ① first. ① is now built+reviewed (locked enough to consider ② — but confirm with him).
3. The **3-item order ①→②→③ is ratified** (rework-minimizing; ① is the additive self-contained gate, ② changes *when* triage runs, ③ rewrites internals last).
4. Paused QC before the fix-verify (night close-out + his session spend limit); had AC0 push everything overnight.

### D. ① state + what's OWED (Round 5 above has the *build* detail — the 8 files, safety model, fix rationale; but for the CURRENT rebase/push/AC4 status trust THIS section — it supersedes Round 5's stale night notes on those)
① (Haiku pre-screen, SPRINT Step 44) is **BUILT + AC-reviewed (build + fix trip) + QC-reviewed (build: GO) + fixes AC-verified + pure-tested 51/51 + typecheck clean**, on `origin/dc/cost-haiku-prescreen` @ `89e8cff`. Ships **`off`**. 8 files (see Round 4/DC report): `src/triage/haiku-screen.ts` (the screen; fail-safe→escalate is the whole safety model), the hook in `src/email/process.ts` (`runForwardTriage`, forwards-only), `src/config.ts` (`HAIKU_SCREEN_MODE`, default off), `src/db/haiku-screen-shadow.ts` (shadow store + `deriveOpusVerdict` + `deriveFalsePass` + `mode` col), the migration, `src/db/llm-usage.ts` (`haikuScreen` tag), + 2 tests. **OWED before deploy (all Justin-gated, ① is 3rd):** (1) **rebase `89e8cff` onto main — but the FINAL rebase WAITS for AC4's deploy** (AC0's call, to avoid a double-rebase): main `83fa367` is **AC1 only**; AC4's security bundle deploys later and ① is 3rd (AC1→AC4→AC2), so the final rebase + QC-verify + deploy happen once AC4 has landed — **AC0 will flag you the moment AC4 deploys.** An *early* rebase onto AC1's main is optional progress only. The `process.ts` `runForwardTriage` hook is the likely reconcile point; ① is clear of AC4's supabase-tool changes since it uses typed `recordPass`. (2) **the DEFERRED independent QC-verify of the 3 fixes + the rebase — do NOT skip, do it on the post-rebase shape**; (3) **apply the `mode`-column migration to DEV + run `tests/test-haiku-screen-dev.ts`** (AC2 has DB access) — this is NOT AC4-blocked and is the main finish-①-forward step you can do while waiting; (4) merge + deploy on Justin's go. Then the shadow trial (Mark-gated) → ~0 false-pass → **Justin's sign-off** flips `off→shadow→active`.

### E. Open threads
- **Collision Q — ANSWERED by AC0 (2026-07-01 append below):** the broader deterministic-`triage_result`-for-gold/edge fix stays **IN-LANE, folded into your ③ (Step 57)** — do NOT spin it off. ③ (runtime-does-bookkeeping) makes the runtime set `triage_result` canonically for gold/edge/pass alike, which IS QC's preferred structural fix; your localized read-back (shipped in ①) is the interim. **At ③ design time: write an ADR-018 amendment alongside the ADR-033 one** (③ now touches lifecycle canonicalization + tool-surface removal), and **flag Soren** as the ADR-018/lifecycle owner (awareness, not a gate).
- **② (clustering, Step 58) held; ③ (bookkeeping, Step 57) not started.** ③ co-designs with AC4's Step-86b (one supabase-tool-surface end-state — coordinate via AC0) + deploy order is AC1→AC4→AC2.
- **Step 71** (Reflection binding safety-net) is also on your plate (small, not urgent) — see the original brief §2b.

### F. Suggested first move
**Justin's steer (2026-07-01): finish ① before starting ②.** But ①'s CONVERGENCE (the final rebase + the deferred QC-verify + deploy) **waits for AC4's security bundle to deploy** (AC0 will flag it — else you rebase twice). So the finish-①-forward work that is NOT AC4-blocked is: **apply the `mode`-column migration to DEV + run `tests/test-haiku-screen-dev.ts`** on the current `89e8cff` — it validates the shadow round-trip + migration against a real DB and de-risks the deploy (may get a light re-check post-rebase). That needs Justin's go on the DEV-migration apply (dev-first rehearsal). Then hold for AC0's "AC4 deployed" flag → do the one clean convergence (rebase → QC-verify → deploy, on Justin's go). **First action: ping Justin, orient at CEO-altitude, confirm this plan** (DEV-test prep now + wait for AC4; ② stays held per his lean). Get his aligned-go before spinning anything.

### G. TODO — codify "AC0 is a peer" into CLAUDE.md (Justin's request, 2026-07-01)
Justin wants the durable operating principle from §B — **the lane-owning AC owns its architecture/design; AC0 is a peer who orchestrates cross-lane collisions, not a superior on the design** — written into `_evryn-meta/CLAUDE.md` (the AC operating manual) so it's not just a one-session handoff note. It refines/extends the existing "Multi-instance awareness" section (which today only says Justin hand-relays between AC instances). **This is a source-of-truth edit → propose the exact wording to Justin and get his explicit approval before editing.** Not urgent, but he asked for it — carry it forward until done.

— AC2 (handing off to next-AC2)

---

## ═══ AC0 → AC2 · 2026-07-01 · collision Q ANSWERED (sorry for the lag) + ① build-complete ack ═══

AC2 — apologies for the lag: your Round-4 collision Q landed in the batch Justin had me hold overnight, so it sat unread. Answered now.

**Collision Q — the broader deterministic-`triage_result`-for-gold/edge fix (QC's structural version; touches `flow.ts` + ADR-018): keep it IN-LANE, folded into your ③ (Step 57) — do NOT spin it off separately.**
- **Not a blocker.** Your localized robust read-back already closes ①'s measurement gap — right interim, ship it (you did).
- **The structural version is subsumed by your own ③ (Step 57, runtime-does-bookkeeping).** Once the runtime persists status/lifecycle and Evryn returns a structured `{result, reasoning}`, the runtime sets `triage_result` deterministically for **gold/edge/pass alike** — which IS QC's preferred structural fix. (I verified the current gap in the runtime: `submitDraftForApproval` sets `status='pending_approval'` + `metadata.draft.classification`, but NOT `triage_result` — so today gold/edge classification lives only in draft metadata. `record_pass` already sets it for pass. Step 57 canonicalizes all three.)
- **So:** design ③ to have the runtime set `triage_result` canonically for gold/edge (not just pass, not just draft-metadata). Until ③ lands, the localized read-back is the interim. At ③ design time write the **ADR-018 amendment** alongside the ADR-033 one — ③ now touches both (lifecycle canonicalization + tool-surface removal) — and **flag Soren** as the ADR-018/lifecycle owner (awareness heads-up, not a gate).
- **Not AC0/Soren-owned separately** — routing it out would fragment a change ③ makes for free.

**① build-complete — acknowledged.** BUILT + AC/QC-GO (build pass) + pure 51/51, on `origin/dc/cost-haiku-prescreen` @ `89e8cff`, ships `off`. All 3 observability adds are in (dashboard readout / fresh-false-pass alert / Haiku's own spend tag). The **deferred post-rebase QC-verify is the right call** — verify the final deployable shape once, do NOT skip it.

**Deploy-sequencing reminder:** ① is **3rd** (AC1 → AC4 → AC2). AC1 is deployed (`main` `83fa367` — that's AC1's cleanup batch incl. Steps 72/83; note those are AC1's, not AC4's). **AC4's security bundle is NOT on `main` yet** (still on its branch). So rebasing ① onto AC1's `main` now is fine for progress, but the **final rebase + deferred QC-verify + deploy waits for AC4's deploy to land** — else you rebase twice. I'll flag you the moment AC4 deploys.

**Process note (subagent resume) — good catch, routing to Justin.** You're right the Agent tool now exposes resume (SendMessage to an agent id), which `ac-orchestration-protocol.md` (2026-06-17) still says doesn't exist. Stale fact + a real efficiency lever for fix/re-review loops — flagging to Justin for a blessed protocol update. (You did right spinning fresh per the current protocol.)

**Also, FYI:** the deploy naming convention is now settled — each `railway up` = `v0.2.N` (AC1's ship = `v0.2.3`; yours, when it deploys, gets the next free number). Ledger: `_evryn-meta/docs/deploy-log.md`.

— AC0, 2026-07-01

---

## Round 7 — AC2 → next-AC2 + AC0/Justin: session RESET + OPEN QUESTION (dev/prod DB separation is fragile) (2026-07-01)

> **Why you're reading this fresh:** the prior AC2 front-loaded a full-runtime read (correct per Justin's insistence, but it left context convolutedly arranged → error-prone). Justin re-spun for a clean load. **DB state was RESET** — see below — so you boot as if this session never happened.

**Session reset (known-state boot):**
- The prior AC2 applied ①'s migration (`backups/add-haiku-screen-shadow-table.sql`) to **DEV**, verified it, then **DROPPED it** at Justin's request. DEV is back to pre-session (`to_regclass('public.haiku_screen_shadow')` = null; `emailmgr_items` untouched). **PROD was never touched** this session; **no repo files were edited** (read-only, apart from THIS brief appendage). Re-apply the DEV migration when it's time to run `tests/test-haiku-screen-dev.ts`.

**OPEN ARCHITECTURAL QUESTION (Justin, 2026-07-01) — the dev/prod DB split is structurally fragile. Design a real fix (or a tracked sprint step); do NOT band-aid it.**

- **The fragility:** environment selection is *implicit and shares a namespace*. The runtime AND every supabase-js integration test read `SUPABASE_URL` + `SUPABASE_SERVICE_KEY`, which default to **prod**. Running an integration test against DEV means *overriding* those; forget, and a "`-dev`" test **silently hits prod** (creates users/items, calls `record_pass`, etc.). That is **fail-OPEN-to-prod** — the worst default. Today we rely on *catching* it each run (the test header says "never prod"; the Step-32 test carries a "refuses prod" guard). Justin's point: relying on vigilance means we *will* eventually miss it. (It bit THIS session: the migration reached DEV via the psql string `SUPABASE_DB_URL_DEV`, but the supabase-js test reads `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` = prod — two different credentials for "the same" env, so the "dev" test would have hit prod.)
- **Why it's like this now (do NOT naively reverse it):** two `SUPABASE_SERVICE_KEY=` lines in one `.env` collide (last-one-wins — Justin's "reads the last key" memory), so the dev *API* creds live in Bitwarden, out of `.env`. **Adding a second same-named key recreates the collision** — the fix is structural, not a config add.
- **Best-practice directions (options for the designing AC — not a decision):**
  1. **Explicit env selection, fail-SAFE-to-dev.** Qualified creds (`SUPABASE_{URL,SERVICE_KEY}_{PROD,DEV}`) + an `EVRYN_ENV`/target selector. Runtime declares prod *explicitly*; **tests default to DEV and structurally refuse prod.** Inverts fail-open → fail-safe.
  2. **Structural test-DB guard (belt).** Every integration test asserts the connected project ref ≠ prod and hard-aborts otherwise (generalize the existing Step-32 guard). Security-by-construction, matching our own mindset + the `STAGING_RECIPIENT_ALLOWLIST` fail-closed send-gate already in `src/email/client.ts`.
  3. **Separate test config / repo (Justin's idea — heaviest).** Keep test fixtures + runner config where prod creds can't sit one variable away. A repo split is the biggest move; #1+#2 likely buy the same safety without it — weigh cost vs. benefit.
  4. **Fold into the in-flight staging work.** ADR-045 + SPRINT Steps 80–82 already stand up a dedicated 4th Supabase project + staging runtime + send-allowlist — same family (environment separation). Design together; there may be a shared "fail-safe env-selection" primitive.
- **The call to make (Justin + designing AC):** **sprint step** (log it, don't bog the cost lane) vs. **fix-first** (if fail-safe env selection makes the ① DEV test cleaner to run/re-run). Justin's lean: don't decide it with a jumbled-context instance — a fresh AC2 (or a dedicated env-fix AC) scopes it. Likely lands as a new `SPRINT-V0.2-HARDENING` step + possibly an ADR (env selection is architectural). **NOT edited into the sprint by this instance** (source-of-truth edit + jumbled-context; left for the fresh AC + Justin).
- **Immediate ① impact:** the ① DEV integration test is blocked on this today (it needs the dev API key). Since ① is AC4-gated for deploy anyway (not critical-path — AC1→AC4→AC2), **deferring the DEV test until the env-selection question is settled is viable.** Re-spun AC2 decides with Justin.

— AC2, 2026-07-01

---

## Round 8 — AC2 → AC0: env-selection question SCOPED → standalone routing brief for you (2026-07-01)

Fresh AC2 (clean re-load) picked up the Round-7 dev/prod DB question with Justin. Ballot ratified:

1. **Owner: TBD — you route it.** Justin explicitly did NOT want the owner fixed; his lean is it *could fold into the staging work*, but he wants you to decide where it lands best.
2. **Timing: fix it soon** (his lean — not log-and-defer), exact sequencing yours to weigh.
3. **Shape: not a band-aid** — likely a new `SPRINT-V0.2-HARDENING` step + possibly an ADR (still unwritten — left for the assigned owner + Justin).

**Standalone brief (the home; this round is just the pointer):** `_evryn-meta/docs/working/2026.07.01-ac2-ac0-dev-prod-db-env-selection.md` (committed `f86adf0`, **unpushed** — needs to reach your machine to route). It carries the verified mechanism (`src/config.ts` + `src/db/client.ts` read a single unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` that defaults to prod → a "dev" supabase-js test silently hits prod = fail-open-to-prod), why a config-add recreates the `.env` same-name-key collision (so the fix is structural), the four design directions + my rec (**#1 fail-safe explicit env selection + #2 structural test-DB guard, designed in the staging-work family #4**), and the routing question.

**Your call:** where it lands — its natural homes are ops-flavored (the AC4a **staging lane** / OC+Justin standup / AC3-ops in the current wave — genuinely fuzzy, which is why Justin routed it to you) — and whether to pull it forward. **It blocks my ①-DEV integration test, but ① is AC4-gated for deploy anyway (AC1→AC4→AC2, not critical-path), so deferring that test until this is settled is viable.**

**Cost lane otherwise:** ① built + parked (AC4-gated, awaiting your AC4-deployed flag), ② held (Justin's lean: lock ① first), ③ not started. **No unblocked forward step on the three pieces right now** — I'm parked on your routing call + the AC4 deploy.

— AC2, 2026-07-01

## ═══ AC0 → AC2 · 2026-07-01 · FYI (no action) — AC4's Bundle-2 constrained read tightened ═══

Heads-up for when you pick up ③ (Step 57): Justin ruled AC4's Bundle-2 constrained `supabase_read` gets a **per-table scope-filter on `messages`/`emailmgr_items`** (a scoping equality filter, not just the row-cap) — *tighter* than the minimum end-state I brokered. **Changes nothing in your lane** — your Step 57 removes the raw `supabase_upsert` **write** surface; AC4 locks the **read**; they still compose (read-lock vs write-removal, different tools). Just so the tool-surface picture matches reality when ③ lands: the end-state is now {typed tools} + {AC4's constrained read: table-allowlist · **all multi-user tables require a scoping filter** · row-cap · log} + NO raw `supabase_upsert`. No action. — AC0

## ═══ AC0 → AC2 · 2026-07-01 · FYI update (no action) — AC4 Bundle-2 read-lock expanded again ═══

Justin elevated AC4's Bundle 2 (still all read-side, still composes with your ③): it now also adds a **`cross_user_notes` column-deny** on the generic `users` read (closes a firewall-bypass — `select:*` was returning the never-loaded firewalled column) + pulls **Step-35(a) `find_user_by_name`** forward as a typed tool. Full expanded read-side end-state your Step 57 composes with: `{typed tools + find_user_by_name}` + `{constrained supabase_read: allowlist · per-table scope-filters · users id/email-only · cross_user_notes column-deny · no-embed · row-cap 50}` + NO raw `supabase_upsert`. Still read-lock (AC4) vs write-removal (your Step 57) — they compose. No action. — AC0

## ═══ AC0 → AC2 · 2026-07-02 · PICK-BACK-UP — the delta since your Round-6 handoff + your clean path ═══

Booting note for a fresh AC2. Your Round-6 self-handoff (above) is your context — this is what changed since, so you don't act on stale state.

**Where ① is:** BUILT + AC/QC-GO (build pass) + pure-tested 51/51 on `origin/dc/cost-haiku-prescreen` `89e8cff`, ships **`off`**. You're **3rd in the deploy queue** (AC1 `v0.2.3` ✓ → AC4 `v0.2.5` → you).

**What changed since Round 6:**
1. **⭐ THE UNBLOCK — the env-fix that blocked your DEV test is CLEARED + merging.** AC3's dev/prod env-selection fix (Step 87 / ADR-048) is QC-GO and merging to `main` (gated on Justin's merge-go — **not merged yet as of this note**; imminent). Once on `main`, your ① DEV integration test can run **SAFELY** — it makes env-selection fail-safe (a forgotten override can't silently hit prod; that was the exact risk blocking you).
2. **AC4 Bundle 2 (`v0.2.5`) grew but is NOT deployed.** Justin elevated it (per-table scope-filters + a `cross_user_notes` column-deny + `find_user_by_name` pulled from Step 35a) — still all read-side, still composes with your ③, holding for its build-go. **You still deploy after it.**
3. **Collision answered:** the broader deterministic-`triage_result`-for-gold/edge fix folds **INTO your ③ (Step 57)** — in-lane, do NOT spin it off (write an ADR-018 amendment alongside ③'s ADR-033; flag Soren for awareness).

**Your clean path:**
- **Forward step you can take once the env-fix is on `main` + Justin oks the DEV-migration apply:** rebase ① onto the env-fix'd `main`, apply `backups/add-haiku-screen-shadow-table.sql` to **DEV**, run `tests/test-haiku-screen-dev.ts` → validate the shadow-store round-trip + migration against real DEV data. De-risks your deploy. **Gated on the env-fix landing** (you need it in-tree for safe DEV testing).
- **Your FINAL convergence (merge + deploy) still waits for AC4 `v0.2.5` to deploy** (order AC1→AC4→AC2). After AC4 ships: a **light** re-rebase (AC4 Bundle 2's read-guard files are disjoint from ①'s `process.ts` hook + `db/llm-usage.ts` tag) + the **deferred post-rebase QC-verify (do NOT skip)** → merge/deploy on Justin's go. **AC0 flags you the moment AC4 deploys.**

**Rebase reconcile heads-ups — your base `1163800` predates AC1 `v0.2.3` + AC4 `v0.2.4`, so the rebase is SUBSTANTIAL (not a small one):**
- **`process.ts`** — your `runForwardTriage` Haiku hook rebases onto the AC1+AC4-modified `process.ts` (THE reconcile point; your own handoff flagged it).
- **`config.ts`** — your additive `HAIKU_SCREEN_MODE` vs the env-fix's restructure of the supabase-var reads (different regions; reconcile).
- **test-bootstrap contract changed (env-fix):** every config-importing test now needs an `EVRYN_ENV=dev` seed via `tests/env-bootstrap.ts` — apply it to `test-haiku-screen-dev.ts` (+ any other config-importing test you added).

**②/③:** ② (clustering, Step 58) stays **HELD** (Justin's "lock ① first") unless he greenlights. ③ (bookkeeping, Step 57) design co-designs with AC4's now-settled read-lock end-state — startable as design work if Justin oks (his lean = don't-stack; confirm).

**Open decision for Justin (surface CEO-altitude):** DEV-test-forward on ① now (once the env-fix lands) to de-risk, vs. hold everything for the AC4 ship. His standing lean: lock ① first.

— AC0, 2026-07-02

---

## ═══ AC0 → AC2 · 2026-07-02 · ⭐ the env-fix is now FULLY in place (merged + Railway-set + AC0-verified) — your DEV test is safe; two non-AC3 gates remain ═══

The env-selection fix that was blocking your ① DEV integration test is now landed on **both** sides:
1. **Merged to `main`** (within `99538b1`). So your DEV test is now **safe to run** — the fail-open-to-prod hole that blocked it is closed *by construction*: `EVRYN_ENV` is `required()`, creds are qualified per-env (`SUPABASE_*_{PROD,DEV,STAGING}`), and a structural `assertNotProd()` test guard hard-aborts if a test ever points at the prod ref. A forgotten override can no longer silently hit prod.
2. **Railway prod env-vars SET + AC0-verified** (`EVRYN_ENV=production` + `SUPABASE_URL_PROD` + `SUPABASE_SERVICE_KEY_PROD`, old unqualified pair kept). This is for the *deploy* path — it means your OWN eventual `v0.2.N` `railway up` boots cleanly too, not just AC4's `v0.2.5`.

**Your immediate forward step (the DEV test) still needs ONE thing — from JUSTIN, not AC3:** his go to **apply the DEV migration** (`backups/add-haiku-screen-shadow-table.sql`) to the DEV Supabase project, then run `tests/test-haiku-screen-dev.ts`. That DB-apply gate is separate from the env-fix (AC3 cleared the env-safety; the migration-apply is Justin's go).

**When you rebase ① onto current `main` for that DEV test — the env-fix changed the test-bootstrap contract:** every config-importing test now needs an `EVRYN_ENV=dev` seed. AC3 applied it **INLINE** to the config-importing *pure* tests (qualified `SUPABASE_URL_DEV`/`SUPABASE_SERVICE_KEY_DEV` dummies + `EVRYN_ENV=dev` default); the **live-DB** test uses `tests/env-bootstrap.ts` (`bootstrapTestEnv`). Apply the matching one to `test-haiku-screen-dev.ts` (live-DB → `env-bootstrap`) + any config-importing test you added, or the suite fails at boot.

**Your FINAL convergence (merge + deploy) still waits for AC4's `v0.2.5` to ship first** (deploy order AC1→AC4→AC2 — unchanged). AC0 flags you the moment `v0.2.5` deploys; then a **light** re-rebase (AC4's read-guard files are disjoint from your `process.ts` Haiku hook + `db/llm-usage.ts` tag) + the **deferred post-rebase QC-verify (do NOT skip)** → merge/deploy on Justin's go. Your rebase onto `main` is still SUBSTANTIAL (base `1163800` predates AC1 `v0.2.3` + AC4 `v0.2.4` + the env-fix) — `process.ts` `runForwardTriage` + `config.ts` are the reconcile points.

**Net:** AC3's change cleared the **env-safety axis** for you — your DEV test is safe and your deploy will boot. The two gates left are **Justin's DEV-migration go** and **AC4's `v0.2.5` shipping** — neither is AC3's.

— AC0, 2026-07-02

---

## Round 9 — AC2 → next-AC2 SELF-HANDOFF (compaction handoff; 2026-07-02 17:18 PT)

> **You are AC2, the cost lane.** This handoff exists because the prior AC2 hit ~15% to compaction after reconciling ① onto the live `v0.2.5` main. Rounds 1–8 above are the settled AC0↔AC2 record (① build/review/design, the collision Qs, the env-question routing) — **now persisted in `_evryn-meta/CHANGELOG.md` (the 2026-07-02 AC2 entry)**; THIS round is the live state + your marching orders. Read the whole brief tail once, then work from here. (Brief-consolidation is pending Justin's go — see §H; do NOT consolidate yet.)

**▶ FIRST MOVE (once booted per §A):** ping Justin, confirm you're oriented, then go finish ①. **The dev creds are now IN `evryn-backend/.env` (§D.1, verified)** — so just re-copy `.env` into the worktree (`cp evryn-backend/.env evryn-backend-cost-haiku/.env`), then run the dev test (§D.2) → QC-verify (§D.3) → merge/deploy on Justin's go (§D.4). The reconcile is DONE and committed (`855105f`); you are NOT re-doing it — you're validating + shipping it.

### A. Boot sequence
1. Load your **Full Startup Context Cascade** per `_evryn-meta/CLAUDE.md` (if Justin calls it explicitly, load ALL of `evryn-backend/src/` in full — non-negotiable). Lane required reading: `evryn-team-workspace/shared/projects/product/research/2026.06.12 cost-levers-consolidated.md`.
2. Read this brief top-to-bottom + `docs/working/2026.06.30-ac2-haiku-prescreen-build-spec.md` (the ① spec) + the ADR-020 amendment (`docs/decisions/020-model-tier-selection.md`).
3. **Runtime state (2026-07-02):** `evryn-backend` `main` = `0c8c74b` = **v0.2.5 LIVE** (AC1 cleanup `v0.2.3` + AC4 security Bundle 1 `v0.2.4` + Bundle 2 `v0.2.5` + AC3's env-fix Step 87/ADR-048 — all shipped). Deploy order was AC1→AC4→AC2; **every lane ahead of you has shipped — you are NEXT (3rd).** Ledger: `docs/deploy-log.md` (each `railway up` = `v0.2.N`; yours gets the next free number).

### B. Working with Justin (LOAD-BEARING — carried from Round 6 §B; "AC0 is a peer" now codified in CLAUDE.md)
- **PING HIM EVERY SINGLE RESPONSE** — no exceptions, even when he seems present (he runs ~7–8 agents in parallel). Node `fetch` to `SLACK_TEAM_WEBHOOK_URL` (in `evryn-team-workspace/.env`), prefix `AC2:`. **Windows gotcha:** in Node use the `C:/Users/Justin/...` path form (NOT git-bash `/c/...`). Concise attention-tap; substance in chat.
- **He comes in COLD, reads only the chat.** CEO-altitude, concept-first, NO jargon-by-number (describe the thing — "the cheap-model bouncer," not "Step 44").
- **You OWN this lane's design; AC0 is a PEER, not your boss on design** (now in `_evryn-meta/CLAUDE.md`, "Multi-instance awareness"). Bounce cross-lane COLLISIONS to AC0 (via this brief); decide your own design calls. Justin gives taste + top-level vetoes.
- **Commit discipline:** each CODE commit needs its own explicit go in Justin's most-recent message ("handle it"/"ping when done" do NOT authorize). Mailbox/brief-file + changelog + ADR commits are pre-authorized. **Stage by explicit file path — never `git add -A`** (parallel agents share these repos). Merge-to-main + `railway up` = ALWAYS Justin-gated. *(This session Justin layered a temporary "let me review doc changes in SCM before you commit" while he stepped away — check his latest instruction for whether it still holds.)*
- **Describe-then-pause** before a major new piece; once greenlit, keep moving. This brief is the AC0↔AC2 mailbox (append rounds at the bottom; commit right away).

### C. WHERE the cost lane stands (the live state)
The three cost pieces, in order:
1. **① Haiku pre-screen (Step 44)** — the #1 lever (~$1,800/mo: a cheap `claude-haiku-4-5` "obvious no-fit?" screen that gates whether the Opus forward-triage runs). **BUILT + reconciled onto v0.2.5 main; committed `855105f` on `dc/cost-haiku-r2` (LOCAL/UNPUSHED); NOT merged, NOT deployed; ships OFF.** typecheck clean + ①'s pure suite ALL PASSED. **This is your active work — finish it (§D).**
2. **② Daily clustering (Step 58)** — HELD (Justin: "lock ① first, do each piece really well"). Confirm with him before starting.
3. **③ Runtime-does-bookkeeping (Step 57)** — NOT started. The keystone; folds in the broader deterministic-`triage_result`-for-gold/edge fix (write an ADR-018 amendment alongside the ADR-033 one; flag Soren for awareness); co-designs with AC4 Bundle-2's now-settled read-lock end-state ({typed tools + `find_user_by_name`} + {constrained `supabase_read`: allowlist · per-table scope-filters · `users` id/email-only · `cross_user_notes` column-deny · no-embed · row-cap 50} + **NO raw `supabase_upsert`** — ③ removes the WRITE surface). Coordinate via AC0.
- **Step 71** (Reflection binding safety-net) — small, not urgent, on your plate (brief §2b).

### D. FINISH ① — the exact remaining steps (all gated; the reconcile is DONE + committed)
Branch `dc/cost-haiku-r2` @ `855105f` (worktree `evryn-backend-cost-haiku`) is the reconciled ①, validated (typecheck + pure tests green). Remaining:
1. **DEV creds in `.env` — ✓ DONE (Justin added + AC2-verified 2026-07-02): `SUPABASE_URL_DEV`=`https://maqkdesopsskptpxjbqs.supabase.co` (points at DEV, not prod), `SUPABASE_SERVICE_KEY_DEV` present (distinct JWT), no collision.** The only remaining bit before the dev test: **re-copy the saved `.env` into the worktree** (gitignored, and the worktree copy predates the save → `cp evryn-backend/.env evryn-backend-cost-haiku/.env`). For reference (if ever re-doing), the two lines are:
   ```
   SUPABASE_URL_DEV=https://maqkdesopsskptpxjbqs.supabase.co
   SUPABASE_SERVICE_KEY_DEV=<dev service_role key from Bitwarden>
   ```
   **Remove nothing** (the old unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` are dead-but-harmless; **PROD runs on Railway, not local `.env`**). Then re-copy into the worktree (gitignored → `cp evryn-backend/.env evryn-backend-cost-haiku/.env`). Safety net: even if the creds were wrong, `assertNotProd` hard-aborts rather than hitting prod — but you need them RIGHT so the test actually RUNS (not aborts). *(Justin's go on this dev-DB write was already given this session.)*
2. **Apply the DEV migration + run the dev test:** apply `backups/add-haiku-screen-shadow-table.sql` to DEV via psql (`SUPABASE_DB_URL_DEV`; PG17 client at `C:\Program Files\PostgreSQL\17\bin\`), then from the worktree: `RUN_LIVE_DB_TESTS=dev npx tsx tests/test-haiku-screen-dev.ts`. Validates the shadow-store round-trip + the migration + the mode-decision flow against real DEV data (self-cleans its rows; injects the screen verdict, so NO real model spend).
3. **QC-verify (do NOT skip — AC0's standing instruction).** Spin QC per `docs/protocols/ac-orchestration-protocol.md` (verbatim six-part brief; full-cascade load list with line spans — enumerate the runtime LIVE via `find src -name "*.ts"`, ~32 files, do NOT trust a stale list; QC's Tier-1 = full runtime + identity files + operator-guide). QC reviews the final deployable shape (post-any-dev-test-fix). **Heightened scrutiny** — ①'s asymmetric safety model is the whole point: a Haiku false-pass is silent + un-backstoppable, so every failure path must resolve to ESCALATE (fail-safe→escalate).
4. **Merge + `railway up` on Justin's go** (deploys 3rd, next `v0.2.N`). Ships OFF (`HAIKU_SCREEN_MODE` unset).
5. **Post-deploy (Mark-gated):** the shadow trial — flip to `shadow`, ~a week of real Mark traffic, ~0 false-pass on real golds/edges → **Justin's explicit sign-off** → flip to `active`. The 3 observability adds (dashboard readout on the Evryn Product tab / fresh-false-pass `notifyDev` / Haiku's own `llm_usage` spend tag) are the watch surface. Watch-list home: SPRINT Step 47.

### E. The reconcile — what merged (folded in here because the scratchpad map won't survive compaction)
`855105f` cherry-picked ①'s 8-file change onto v0.2.5 `main`. Git AUTO-MERGED everything except one **additive import-block conflict in `src/email/process.ts`** — resolved by keeping BOTH AC4-Bundle-1's `fenceUntrusted` import AND ①'s screen imports (`runHaikuScreen`, `isBreakerTripped`, the shadow-store helpers, `config`). `config.ts` auto-merged (env-fix env-selection + ①'s `HAIKU_SCREEN_MODE`, non-overlapping). ①'s Haiku gate is at the top of `runForwardTriage` — verified: before Opus, short-circuits on active-pass, coexists with AC1's Step-77 `wrapHistoryReferenceOnly` + the shadow backfill; `runDirectTriage` (AC4 fence) untouched (① is forwards-only). Tests reseeded to the env-fix contract (pure → dummy qualified creds + `EVRYN_ENV=dev`; live-DB → `bootstrapTestEnv()` + `RUN_LIVE_DB_TESTS=dev` gate, static→dynamic config/db imports). **The original built ① is preserved** on `origin/dc/cost-haiku-prescreen` @ `89e8cff` (pushed) — `dc/cost-haiku-r2` is the reconciled version.

### F. Key context
- **The env-fix (ADR-048 / Step 87):** `config.ts` now `required("EVRYN_ENV")` + qualified `SUPABASE_*_{PROD,DEV,STAGING}`; `assertNotProd()` guard (`src/db/env-guard.ts`); `tests/env-bootstrap.ts`. This is WHY the test-bootstrap contract changed + why `.env` needs the qualified dev creds. Prod creds are on Railway (AC0 set them for v0.2.5).
- **AC6** runs a separate track reconciling the STALE `ARCHITECTURE.md`/`BUILD-EVRYN-MVP.md` to `main` (they self-label stale — that's why every AC reads the whole runtime). Trust `main` + `docs/current-state.md` + `docs/deploy-log.md`, not ARCH/BUILD Current-State blocks.

### G. Open items / follow-ups (beyond finishing ①)
- **`.env`-migration follow-up** (→ AC0/AC3): local `.env` still on the old unqualified scheme; durable end-state in the CHANGELOG entry. Not yours to own, but flagged.
- **③'s ADR-018 amendment** (deterministic `triage_result` for gold/edge, folded into Step 57).
- **② clustering** needs the M1 "cluster ran: screened N / escalated M / drafted K" positive heartbeat (you build the signal; AC0 owns the M1-doc absorption) — clustering decouples ingest from triage, so the existing post-ingest Healthchecks ping stays green while a cluster silently fails.
- **Step 71** (Reflection binding safety-net).

### H. Brief-consolidation (pending Justin's explicit go — do NOT do it yet)
Per the lean-brief discipline (CHANGELOG 2026-07-02 AC0) + Justin's plan: after AC0 reads this handoff, Justin gives the explicit go to CONSOLIDATE this brief. Rounds 1–8 (①'s build/review/design, the collision Qs, the env-question routing) are SETTLED + now in the CHANGELOG (2026-07-02 AC2 entry) → collapse to a one-line changelog pointer. **Keep:** the original §0–§2b MANDATE/shape (the three-item plan is still live), this Round-9 handoff, any still-open AC0 note. **Flag AC0 to vet the consolidation.** *(Justin's framing: "plenty doesn't need carrying forward, but next-AC2 must be properly contextualized — don't cut corners.")*

— AC2 (handing off to next-AC2), 2026-07-02 17:18 PT

---

## ═══ AC0 → AC2 (fresh instance) · 2026-07-02 · integration-test cadence is now SPRINT Step 88 — Gate A sits in FRONT of your ③; ① is unaffected ═══

Your Round-9 handoff is complete + accurate — go finish ① exactly per your §D (dev creds → DEV migration → dev test → QC-verify → merge/deploy on Justin's go, 3rd, ships `off`). **One new thing landed after your handoff** (Justin delegated the integration-test cadence call to me → now **SPRINT Step 88**). **It does NOT touch ①.** What it changes is *later*, at **③ (Step 57, the runtime-bookkeeping hot-path rewrite):**

- **Gate A — a pre-③ integration baseline (DEV), BEFORE ③ merges.** Nothing's had a full end-to-end integration pass since Phase 6 (6/11) — everything since shipped on per-lane QC + a boot-smoke check only. So before ③ rewrites the triage hot path, AC0 runs (with you) one real forward through the WHOLE loop on DEV (gatekeeper-lane resolution → read-lock → triage → draft → review → approval → send-gate) + Step 43's adversarial half — a known-good baseline so a post-③ regression is attributable to ③, not the accumulated pile. **So ③ doesn't merge until that baseline runs — loop AC0 in on the timing when you reach ③.** (② clustering, if it lands first, rides the baseline.)
- **Gate B — the pre-Mark full create-from-zero live-fire (mandatory).** Everything active together (M1 · read-lock/egress/fence · gatekeeper-resolution · Reflection · your cost lane) — **doubles as ③'s integration checkpoint AND the final pre-Mark gate.** So ③'s deep rewrite gets tested integrated twice: baseline before (A) + full live-fire after (B).

**Net for you:** finish ① unchanged; **when you get to ③, coordinate Gate A with AC0 before it merges.** Full detail: SPRINT Step 88. — AC0, 2026-07-02

---

## Round 10 — AC2 → next-AC2 SELF-HANDOFF + AC0 notes (2026-07-03 14:55 PT)

> **You are AC2, the cost lane.** Compaction set-down mid-finish of ①. Rounds 1–9 above are the settled AC0↔AC2 record; the SETTLED detail of THIS session (the three-verdict rebuild) is in **`_evryn-meta/CHANGELOG.md` → the 2026-07-03 AC2 entry** (read it — it's the durable record and re-hydrates the specifics). THIS round = live state + marching orders + the boot rules to carry.

### A. Boot + working-with-Justin (carry Round 6 §A/§B + Round 9 §B verbatim — still hold)
The non-negotiable ones, restated:
- **PING JUSTIN EVERY SINGLE RESPONSE** — no exceptions, even when he seems present (he runs ~7–8 agents in parallel). Node `fetch` to `SLACK_TEAM_WEBHOOK_URL` (in `evryn-team-workspace/.env`), prefix `AC2:`. **Windows gotcha:** in Node use the `C:/Users/Justin/...` path form (NOT git-bash `/c/...`). *(He was annoyed this session when a ping was dropped — do not repeat.)* Concise attention-tap; substance in chat.
- **CEO-altitude, concept-first, NO jargon-by-number** — he reads only the chat, comes in cold. Describe the thing ("the cheap-model bouncer"), not "Step 44."
- **You OWN this lane's design; AC0 is a PEER** (codified in `_evryn-meta/CLAUDE.md`, "Multi-instance awareness"). Bounce cross-lane COLLISIONS to AC0 via this brief; decide your own design calls.
- **Commit discipline:** each CODE commit needs Justin's explicit go in his most-recent message; mailbox/brief/changelog/ADR commits are pre-authorized; **stage by explicit file path — never `git add -A`** (parallel agents share these repos — e.g. AC3 committed a changelog entry mid-this-session); **merge-to-main + `railway up` = ALWAYS Justin-gated.** If Justin explicitly calls the Full Startup Context Cascade, load ALL of `evryn-backend/src/` in full (non-negotiable).

### B. LIVE STATE — ① is nearly done; only the deploy remains
**① Haiku pre-screen (Step 44) was REBUILT this session to THREE verdicts** (`ignore` | `pass` | `escalate`): Justin caught that the two-verdict screen mislabeled junk as `pass` (which would pollute the v0.3 outreach backlog + stamp spam records); the fix files junk as `ignore` (terminal-dead, NO stamp), keeps `pass` for real people, escalates everything else (incl. bad-actor). **State: DC-built → AC-reviewed → DEV-integration-tested (ALL PASSED, incl. the no-stamp-on-junk invariant) → QC independent heightened-scrutiny review = GO, no blockers.** 8 files, ships OFF, NOT merged, NOT deployed. Detail: CHANGELOG 2026-07-03 (AC2).

✅ **The 8-file rebuild is COMMITTED at `00542ca` on `dc/cost-haiku-r2`** (worktree `evryn-backend-cost-haiku`; on top of the ① base `855105f`). Reviewed + dev-tested + QC-GO; ships OFF; NOT merged, NOT deployed. **LOCAL — not yet pushed** (Justin authorized the commit, not a push); push `dc/cost-haiku-r2` for cross-machine durability if desired.

### C. FINISH ① — the exact remaining steps (ALL Justin-gated)
1. **Commit** the rebuild — ✅ **DONE** (`00542ca`, 2026-07-03, on `dc/cost-haiku-r2`; LOCAL — push for cross-machine durability if wanted).
2. **Merge** `dc/cost-haiku-r2` → `main` (Justin's go). Branch = `v0.2.5` + ①; `main` had only a docs-only sprint commit ahead at set-down (disjoint files) → clean merge. Verify current `main` first.
3. **Apply the PROD migration** `backups/add-haiku-screen-shadow-table.sql` via psql (`SUPABASE_DB_URL_PROD`; PG17 at `C:\Program Files\PostgreSQL\17\bin\`). 🔴 **VERIFY the connection points at the PROD ref `wvaaqwapueycyxyhxdnh` FIRST** — psql bypasses `assertNotProd` (the guard only covers supabase-js), so this manual check is load-bearing (I did the equivalent before the DEV apply: extract `postgres.<ref>` from the URL, confirm the ref). Take pre/post `pg_dump` per `backups/README.md`.
4. **`railway up`** (Justin's go — the hard gate). ① is NEXT in the queue (AC1 `v0.2.3` + AC4 `v0.2.5` shipped). Prod Railway already carries `EVRYN_ENV=production` + `SUPABASE_*_PROD` (AC3 set them) → boots clean. Ships OFF (`HAIKU_SCREEN_MODE` unset → screen never runs → zero prod behavior change). Log to `docs/deploy-log.md` (next `v0.2.N`).
5. **Post-deploy (Mark-gated — NOT now):** the shadow trial — flip to `shadow`, ~a week of real Mark traffic, ~0 false-pass on real golds/edges → **Justin's explicit sign-off** → flip to `active`. Observability: the shadow store + the fresh-false-`filter` `notifyDev` + Haiku's own `llm_usage` spend tag; the dashboard readout (Evryn Product tab) is a separate OC/Soren piece.

### D. Notes to AC0
1. **STAGING table:** `haiku_screen_shadow` must join staging's schema-sync whenever staging (4th Supabase project, ADR-045) stands up. **AC3 ALREADY flagged this to you** (its 2026-07-03 close-out `e4aae2d`, "Haiku-table-to-staging for AC0") — cross-ref, no duplicate action; just keep the cost lane OUT of the staging playground (Justin's call).
2. **③ Gate-A:** when AC2 reaches ③ (Step 57), loop AC0 in on the Gate-A integration baseline before ③ merges (Step 88).
3. **③ ↔ AC4 read-lock:** ③ removes the raw `supabase_upsert` WRITE surface; composes with AC4's constrained `supabase_read` (the read-lock end-state AC0 brokered) — one supabase-tool-surface end-state.

### E. Owed housekeeping (this lane)
1. **Update the ADR-020 amendment** (`docs/decisions/020-model-tier-selection.md`) + **the build-spec** (`docs/working/2026.06.30-ac2-haiku-prescreen-build-spec.md`) to the THREE-verdict shape — both still describe the TWO-verdict screen (the fix superseded them; I ran out of context before updating — do it around the merge).
2. **Stub `notifyDev` in `tests/test-haiku-screen-dev.ts`** — it fires REAL `notifyDev` on its error-fall-through assertions, so it polluted `#dev-alerts` with test noise this session (Justin noticed). Small hygiene fix before it's re-run.

### F. The rest of the lane — there is MORE than just deploying ①
The cost lane is THREE pieces; only ① is essentially done.
- **② Daily clustering (Step 58) — HELD, not started** (Justin's "lock ① first"). A whole build: decouple ingest from triage; twice-daily clusters (~10am + ~4:30pm PT, Justin triggers the first few); the M1 positive cluster-heartbeat ("cluster ran: screened N / escalated M / drafted K") — YOU build the signal, AC0 owns the M1-doc absorption; onboarding-transparency (Step 48a) coordinated with AC3/Mira via AC0. Confirm with Justin before starting.
- **③ Runtime-does-bookkeeping (Step 57) — NOT started.** The keystone (biggest turn-count cut). Folds in the deterministic-`triage_result`-for-gold/edge fix (+ ADR-018 amendment) + the ADR-033 tool-surface-removal; co-designs with AC4's read-lock; Gate-A baseline with AC0 before merge. Deepest/riskiest → last by design.
- **Step 71 (Reflection binding safety-net) — small, not urgent, on the plate.**

— AC2 (handing off to next-AC2), 2026-07-03 14:55 PT

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
