# Lane B (Operator / Approval / Slack) — AC0 Convergence Handoff

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.

> **How to use this file:** AC2's high-resolution handoff to **AC0** for the 2026-06-17 v0.2-hardening wave. This is the doc AC0 converges Lane B from — every step touched, exact files/functions, design calls made + surfaced, shared seams, test status, and the Mira-batch + cross-lane notes AC0 needs to fold in. The CEO-facing summary lives in chat with Justin; this is the engineer-to-engineer record. **Living doc — appended as the build progresses.** Lane B = branch `lane-b/operator-approval`, worktree `c:/Users/jbmcg/Evryn/Code/evryn-backend-operator`.

*Started: 2026-06-17T14:18:44-07:00 (AC2). **Status: DONE — Lane B is QC-GO and fully committed; handed to AC0 for convergence (2026-06-17). See FINAL STATE below.***

---

## FINAL STATE — read this first (2026-06-17; governs current state, supersedes any "pending"/"uncommitted" markers in the chronological Build Log below)

**Lane B is DONE — QC-GO, everything committed (all local/unpushed; AC0 converges from local on this machine).**

**Committed:**
- **`lane-b/operator-approval` @ `4446ef0`** — the full lane: `bc7cc1b` (trip 1: Steps 18/26/27/28/31) · `ab6b915` (trip 2: 14/15/33/37/32) · `1a0f9ac` (identity repoints: `ignored`→`set_item_status`, operator redact→`redact_user_from_message`, `bad_actor` Phase-3 terminalize beat) · `4446ef0` (`bad_actor` user-record stamp + `matched` terminalization w/ item-lookup pointer). **B1 FULLY CLOSED** — every status Evryn sets is audited. **QC-GO** (proper re-run *with* ARCHITECTURE.md).
- **`evryn-quality` @ `06eaa9b`** — QC's promoted "guard-is-half-a-change" pattern (first entry in her new "Patterns This Role Watches For").
- **`_evryn-meta/CLAUDE.md` @ `83a3ecb`** + **`evryn-team-workspace/CLAUDE.md` @ `fffe372`** — subagent fresh-spawn / no-resume discipline.
- *(This handoff doc itself is on disk, currently uncommitted — readable from the working tree.)*

**What AC0 does at convergence:**
- **Merge `lane-b/operator-approval`** into the bundle. **Cross-lane seams:** `classify.ts` `allowedTools` (shared w/ Lane C — confirm no double-touch); `flow.ts`/`client.ts` Step-32 DI (coordinate w/ AC1, different files); Step-14 forward-curation *destination* rides AC1's Step 61.
- **DEPLOY GATE — verify before `railway up`:** AC1's `poll.ts:584` (`no_gk_response` → `set_item_status`) — **AC1 reports done verbatim**; AC0 confirms it's actually in the merged tree. If missing → hard NO-GO (`checkFollowUps` has no B2c backstop; the item would silently strand). AC1's `process.ts:283` comment is cosmetic.
- **Mira (coordinate, not a hard gate):** the new identity beats AC2 authored on `lane-b` (ignored repoints, operator redact, `bad_actor` terminalize + user-stamp, `matched`) are operational + QC-verified spec-accurate vs ARCHITECTURE.md — Mira vets/voices them in her batch.

**NOT gating — follow-ups:** the 3 deferred nits (redact-arg logging, zero-count redact stamp, injected-update at delivered-flip); 2 flags — (a) `users.status` typed-tool → fold into the v0.3 S1 "retire generic `supabase_upsert`" sweep; (b) the manual-Supabase double-send-recovery UX → make it a tool/AC action, never "Justin opens the DB."

**Provenance:** the chronological **Build Log** below records *how we got here* (the REJECT-vs-ROUTE deliberation that resolved to REJECT, the QC passes, the confabulation finding) — for *current* commit-state, this FINAL STATE block governs.

---

## Scope (Lane B steps)

Core: **14** (runtime-owned threading), **15** (operator re-draft context), **18** (status-write audit gap), **26** (operator subject), **27** (review-email handle position), **31** (parser test), **33** (draft-submit idempotency), **37** (`redact_user_from_message` tool). Plus **32 Lane-B half** (reassigned from AC1 2026-06-17): the `executeApproval` retry/rollback failure-path test. If time: **28** (collision help-string), **23** (pong-timeout), **24/38** (low-pri).

**Build sequencing:** Trip 1 = 18, 26, 27, 28, 31 (operator-surface + audit + parser test — cohesive, fully designed, no cross-lane dep). Trip 2 = 14, 15, 33, 37, 32 (threading + tooling + the rollback test). Single QC pass over the cumulative `lane-b/operator-approval` diff at the end (QC-batching).

---

## Design calls surfaced to Justin (decided)

### Step 18 — status-write audit gap → single-path `set_item_status` (Justin confirmed 2026-06-17)
- **Call:** every emailmgr_items status change must append `metadata.lifecycle`; today only `record_pass` (the pass path) does — all other transitions Evryn makes via the generic `supabase_upsert` tool skip the audit (bit live in S3b on an `escalated` transition).
- **Chosen (with Justin):** add a typed `set_item_status` MCP tool with a **strict enum** of the judgment statuses (`matched`, `no_gk_response`, `ignored`, `bad_actor`, `escalated`), `escalated` as the **golden-bridge** catch-all ("if none fit, escalate; never invent a status"), pass excluded (→ `record_pass`). It routes through `updateEmailmgrItem` (audit appended). **Single source of truth:** the generic `supabase_upsert` handler now **rejects** a status write to `emailmgr_items` with a helpful "use `set_item_status`" message (Evryn self-corrects in one turn if her identity instructions lag), and the status enumeration is removed from the `supabase_upsert` tool description. Non-status emailmgr_items writes (triage_result, sender_type, metadata) still go through `supabase_upsert`.
- **Why over alternatives:** (a) typed-tool-only relies on her always picking it — gap reopens; (b) a silent *router* (route the generic write through the audit path) was my first idea — Justin preferred the cleaner single-path/reject (one way to change status), which the helpful error de-risks. Less LLM work for her too (enum pick vs. free-form record) — Justin's cost lens.
- **Note:** `supabase_upsert` cannot be retired entirely — still load-bearing for non-status writes. Full retirement aligns with the v0.3 S1 security item (service_role + arbitrary-table surface) + Step 50 dedup review.

### Step 14 — runtime-owned threading (couples to Step 61, AC1)
- **Chosen:** derive `in_reply_to`/`thread_id` from the **item** (not Evryn's `submit_draft` args) at send time — direct-message replies thread for real (same mailbox; item carries Message-ID + Gmail thread_id); forward-curation gets the headers set harmlessly. **Cross-account threading is impossible** (a mailbox can only thread messages it holds) — so the forward-curation *destination* rides AC1's Step 61 outbound-address design; threading degrades gracefully to a clean standalone + match-by-quoted-original. Never pass a foreign Gmail `threadId` (would error). The HTML `gmail_quote` collapse structure lives in `client.ts` `renderMarkdownToHtml` (Lane A) — flagged optional, see seams.

---

## Mira batch notes (for AC0 to fold into the Mira batch — NOT needed for Lane B to proceed)

1. **Step 37 (needed at convergence):** new `redact_user_from_message` runtime tool. `operator.md` recovery patterns (mid-thread-bleed ~L58, wrong-scope-recovery ~L64) currently instruct redaction via raw `supabase_upsert` → repoint both beats to the new tool. One-line swap per pattern, no voice change.
2. **Step 18 (should ride the same deploy; self-correcting if it lags):** `triage.md` (and any `operator.md` beat) that instructs status changes via `supabase_upsert` → repoint to `set_item_status`. Not strictly blocking — the `supabase_upsert` reject returns a helpful "use set_item_status" error so Evryn self-corrects — but cleaner if it ships together (saves her a wasted turn).

## Cross-lane coordination (AC1 / AC0)

- **Step 61 ↔ Step 14 (AC1):** (1) don't engineer cross-account threading — it can't group across mailboxes; treat it as "nice if the destination is the same account Mark forwards from, graceful standalone otherwise." (2) If AC1's `process.ts` work is already capturing inbound identifiers, stashing the **designated outbound address** + (where extractable) the **original email's subject** on the item makes Lane B's threading + match-by-quote cleaner. Not a blocker; AC0 resolves at convergence.
- **Step 32 (AC1 ↔ AC2):** AC1 owns the `client.ts` `sendEmail` DI seam + its surfacing test; Lane B owns the `executeApproval` retry/rollback failure-path test (same DI seam). Coordinate the seam at convergence.
- **Step 14 `client.ts` touch (AC1):** if we want Gmail's native collapse arrows, `renderMarkdownToHtml` needs the `gmail_quote` div structure. Low priority — the `>`-blockquote render is acceptable per Step 14 ("or accept non-collapse"). AC0 resolves any `client.ts` merge.

---

## Build log

- **2026-06-17T14:27 — Trip 1 built (DC, commit `bc7cc1b` on `lane-b/operator-approval`):** Steps 18, 26, 27, 28, 31. AC-reviewed = GO. Tests green (test-set-item-status 35, test-slack-parsing 11, test-record-pass, test-review-send-format). Pure helpers `buildSetItemStatusUpdate` + `isEmailmgrStatusWrite` extracted (testable). `parseSlackMessage` now exported.
- **2026-06-17 — QC trip 1 = NO-GO (one blocker, B1 — VALID, AC agrees).** 26/27/28/31 all GO on their own; Step 18 *code* is correct (enum right, `escalated` true catch-all + surfaced via stale-check, sender_type derivation right, guard is status-present-only so combined writes pass, failure path notifyDevs). **B1: the reject-guard strands Evryn on the common path** because callers still steer her at the now-blocked `supabase_upsert` status path and `set_item_status` is referenced nowhere:
  - `identity/core.md:246` — "supabase_upsert to mark the item as ignored" → **Mira** repoint to `set_item_status`.
  - `identity/activities/triage.md:19/50` (+ 36/51 bad_actor) — "Update status to `ignored`" (names no tool) → **Mira** repoint.
  - **`src/email/poll.ts:584` (LANE A / AC1)** — the runtime-*constructed* `checkFollowUps` prompt literally says "Use the supabase_upsert tool to set status to no_gk_response" → **AC1** repoint to `set_item_status`. *(New cross-lane finding — not previously flagged.)*
  - Interaction: triage.md:108 — a non-terminal item at query-end errors, so a rejected status write doesn't just no-op, it can strand/​error on Mark's first noise wave.
- **OPEN DECISION (surfaced to Justin) — Step 18 guard: REJECT vs ROUTE.** B1 re-opens the reject-vs-route choice (Justin originally chose reject for the clean single-path).
  - **ROUTE (AC recommends):** soften the guard to *delegate* a `supabase_upsert` status write through `updateEmailmgrItem` (still audited) instead of rejecting. Eliminates B1 entirely — no caller-update deploy gate, Evryn can't wall, the audit invariant holds structurally regardless of instruction state. The clean end-state (capability removed) is still reachable later via the instruction repoint (Mira/AC1, no rush) + an optional final reject once callers are confirmed clean. Structural-over-instructional; safest for Mark's first traffic.
  - **REJECT + coordinate (Justin's original):** keep the reject; make the 3 caller repoints (core.md, triage.md, poll.ts:584) a HARD convergence deploy gate. Clean single-path immediately; fragile (miss any caller → common-path breakage).
  - **Resolution path:** if ROUTE → small DC fix-trip on the guard (classify.ts) + the Mira/AC1 repoints downgrade to no-rush. If REJECT → no code change; the 3 repoints become convergence gates AC0 must verify before deploy.
- **QC standing pattern (proposed to Justin for promotion into `evryn-quality/CLAUDE.md` "Patterns This Role Watches For"):** *"When a build adds a runtime guard/restriction on a tool Evryn uses, the identity files AND any runtime-constructed prompts that name that tool are part of the blast radius — a guard is only half a change; the other half is re-pointing every caller (identity-file AND runtime-string). The tell: a diff that removes/restricts a capability with no matching diff that redirects its callers."* (This caught B1.) Generalizes QC's spec-vs-runtime lens to the tool-surface seam.
- **2026-06-17T14:52 — Trip 2 built (DC, commit `ab6b915`):** Steps 14 (runtime-owned threading via pure `deriveThreadingFromItem`; `submit_draft` schema dropped `in_reply_to`/`thread_id`), 15 (`handleOperatorMessage` injects a "Current pending draft for this user" block on scoped threads; `findLatestPendingApprovalForUser`/`countPendingApprovalForUser` in items.ts; pure `formatPendingDraftBlock`), 33 (idempotent submit — pure `isDuplicateDraft`; up-front item fetch consolidated; true-duplicate early-returns with no re-send/re-ping), 37 (`redact_user_from_message` MCP tool gated behind `threadScopeContext` like rescope/set_thread_scope; pure `applyRedaction` + `redactInMessages` scrub content_raw+message_body and stamp metadata WITHOUT storing the term), 32 (`executeApproval` injectable `deps` for a hermetic retry/rollback test). 5 new tests green; typecheck clean; no regressions. **AC-reviewed = GO (pending QC):** the Step-33 fetch consolidation is sound (up-front item reused; user_id/content_raw unaffected by the status flip; duplicate/revision branching correct), Step-14 derivation correct per item type, Step-32 deps backward-compatible. **Convergence flags (DC):** (1) Step-32 DI seam broadened to also inject findItemById/updateEmailmgrItem (hermetic test; mirrors recordPass) — composes with AC1's separate `client.ts` sendEmail DI; (2) `classify.ts` `allowedTools` array is shared with Lane C — AC0 confirm no double-touch at merge; (3) no identity edits (Step-37 `operator.md` repoint is Mira's, in the batch above).
- **B1 DECISION (Justin, 2026-06-17): KEEP REJECT, fix the callers now (once, no rebuild).** Justin authorized mechanical tool-name swaps in identity files (do it + flag Mira; voice untouched; he reviews before commit). AC2 made the 4 mechanical identity repoints (UNCOMMITTED, pending Justin's review): `core.md:246` + `triage.md:19/50` (ignored→`set_item_status`), `operator.md:58` (redact→`redact_user_from_message`).
- **2026-06-17 — QC FINAL PASS = GO on trip-2 code; found B1-a (a NEW gap, valid, AC agrees).** All 5 trip-2 steps PASS (14/15/33/37/32 — threading pure + no foreign threadId; Step-15 block correctly scoped to inherited-scope user + fail-safe; Step-33 duplicate/revision branching correct; Step-37 redaction scrubs both fields, case-insensitive, regex-escaped, NEVER stores the term, operator-gated; Step-32 deps default to real). 7 tests green, typecheck clean.
  - **B1-a (convergence gate — Mira):** the guard rejects EVERY status write via supabase_upsert, not just `ignored`. `triage.md` flags `bad_actor` but NEVER explicitly instructs the item-status terminalization (the old pairing lived in the supabase_upsert tool *description*, which Step 18 trimmed) — so a `bad_actor` triage now can't terminalize → item stuck in `processing` → B2c errors it. `matched` is likewise un-instructed in identity. **This is a NEW Mira beat, not a mechanical swap** (no existing tool-instruction to swap — it's authoring the terminalization guidance) → routes to Mira, DEPLOY-GATING. `set_item_status` lists these statuses (discoverable), so she'd likely self-correct off the reject error, but we close it properly. (escalated: the tool names it as the golden bridge; a triage.md beat is nice-to-have.)
- **CONVERGENCE GATES — the Step-18 guard must NOT deploy without ALL of:** (1) AC2's 4 identity repoints [done, uncommitted]; (2) **B1-a Mira beat** — `bad_actor`/`matched` terminalize via `set_item_status` [Mira batch, gating]; (3) **AC1 `poll.ts:584`** — `no_gk_response` prompt → `set_item_status` [Lane A, gating]; (4) AC1 `process.ts:283` stale comment [Lane A, cosmetic].
- **QC nits (AC2 to fold into a small fix-pass, all Lane-B files):** (a) `logToolCall` logs `redact_text` to stdout/Railway (30-day) — for a containment tool, redact that arg in the log; (b) `redactInMessages` stamps `redacted:true` even when count==0 — only stamp when >0; (c) Step-32 success-path delivered-flip (flow.ts ~L537) uses the real `updateEmailmgrItem`, not the injected one — use the injected fn for full testability. All small.
- **QC pattern PROMOTED (uncommitted, pending Justin review):** new `## Patterns This Role Watches For` section in `evryn-quality/CLAUDE.md` — "a guard/restriction is only half a change; confirm every caller (identity + runtime prompts) was repointed."
- **Lane-B code state: QC-GO.** Remaining before AC0 can converge: Justin's commit-go on the identity edits + QC's CLAUDE.md; and the gating dependencies (B1-a + AC1 poll.ts) land in the same bundle.
- **2026-06-17 — B1-a CLOSED in Lane B (Justin authorized AC2 to do it + flag Mira).** Added a `bad_actor` terminalization beat to `triage.md` Phase 3 (UNCOMMITTED): *"call `set_item_status` with status `bad_actor`… Like a pass, this is silent."* Flag to AC0→Mira: AC2 authored this operational beat; Mira to vet/voice. **`matched` still un-instructed** — it's a *gatekeeper-feedback* outcome (not a triage.md terminal; lives in the feedback/`checkFollowUps` flow), NOT a day-1 path, and `set_item_status` lists `matched` (discoverable) → flag to Mira for a `feedback-guidance.md` beat, LOWER urgency (not deploy-gating for day 1).
- **AC1 note FINALIZED (exact find/replace, handed to Justin to pass):** `poll.ts:584` `Use the supabase_upsert tool to set status to no_gk_response` → `Use the set_item_status tool to set status to no_gk_response` (FUNCTIONAL, gating); `process.ts:283` comment `→ supabase_upsert to mark ignored/passed` → `→ set_item_status to mark ignored (or record_pass for a pass)` (cosmetic).
- **QC nits — DEFERRED (not worth a ~200k-token DC spin for 3 one-liners; fold into the next substantive DC pass or convergence):** (a) redact `redact_text` in the redact tool's `logToolCall` (containment — operator-only infra logs, 30-day, low sev); (b) only stamp `redacted:true` when count>0; (c) use injected `updateEmailmgrItem` at the delivered-flip (flow.ts ~L537) for testability. None block QC-GO.
- **CONFIRMED (Justin's question — claude-code-guide research, 2026-06-17):** each Agent-tool spawn is a **fresh, one-shot subagent with ZERO memory** of any prior subagent (docs: "the subagent does NOT receive the parent's conversation history or tool results"). The QC "final pass" was a fresh spawn, so its claim of "carrying ARCHITECTURE.md from trip-1" is a **confabulation** — primed by my brief saying "your second pass… you already reviewed trip-1" (a fresh subagent will accept/continue that false framing; a documented failure mode). Its *findings* were runtime-grounded (read `items.ts` + the runtime directly this pass), so **B1-a stands**; only the *architecture-doc* re-read was falsely skipped, and the runtime is the authoritative source for those behavioral claims anyway. **Process fix (PROPOSED protocol addition):** for a genuine second/follow-up pass with the prior agent's context, **continue the SAME agent via SendMessage to its returned `agentId`** (retains full transcript) — NOT a fresh Agent call; if spawning fresh, brief it as a *fresh* review with prior findings listed as inputs to **re-verify**, never implying it did the work. **CORRECTION (2026-06-17):** `SendMessage` is NOT exposed in this environment (ToolSearch by name + keyword returns nothing; the Agent tool has no resume param) — so continuation was never even possible here; every subagent AC2 spawns is *unavoidably fresh*. Justin's "run an experiment to test resume" can't be run (no mechanism). Practical rule for this env: ALWAYS brief subagents as fresh, never imply continuity.
- **2026-06-17 — QC RE-RUN (proper, with ARCHITECTURE.md required) = GO.** Justin's point: the runtime says what *is*, ARCHITECTURE.md says what *should be* — a review needs both. Fresh QC read all 1208 lines of ARCH + the full runtime + identity. **B1 is FULLY CLOSED within Lane B:** every status Evryn sets routes through an audited tool (`set_item_status`/`record_pass`); the guard keys on `table==="emailmgr_items"` only (confirmed — `users`-status writes for the bad_actor stamp are NOT blocked, test asserts it); the new `bad_actor` (item terminalize + user stamp) and `matched` beats are **spec-accurate vs ARCH** (User Model + delivered→matched lifecycle). Trip-2 code re-confirmed clean against ARCH. Tests green, typecheck clean.
  - **QC Finding 2 (addressed):** the `matched` beat didn't say how to obtain the prior delivered-gold item id on a fresh-reply confirmation → AC2 added the pointer (`triage.md` Phase-1 prior-history lookup / `supabase_read`). UNCOMMITTED.
  - **QC Findings 3-5 (low/informational):** F3 `process.ts:283` stale comment (in the AC1 note); F4 the `executeApproval` double-send alert tells the *human* to set delivered in Supabase — human path, not Evryn's tool, NOT a stranding risk (pre-existing); F5 `redactInMessages` stamps `redacted:true` on zero-occurrence (cosmetic nit — in the deferred-nits list).
- **CONVERGENCE CHECKPOINT (the one thing between this bundle + a clean deploy):** AC0 verifies Lane A actually repointed `poll.ts:584` (`no_gk_response` → `set_item_status`). **AC1 reports done verbatim** (Justin, 2026-06-17) — AC0 confirms at merge. If missed, it's a hard NO-GO on the bundle (the item would silently strand — `checkFollowUps` has no B2c backstop).
- **Lane-B FINAL STATE: QC-GO.** Then → AC0.
- **2026-06-17 — closeout (Justin's decisions, all COMMITTED):** (A) `bad_actor` + `matched` edits → **`4446ef0`** (lane-b). (B) Resume-learning capture → **`83a3ecb`** (`_evryn-meta/CLAUDE.md`, Orchestrating section) + **`fffe372`** (`evryn-team-workspace/CLAUDE.md`, Multi-agent subagents section). Core: fresh spawns = zero memory; a resume mechanism exists but is NOT exposed in AC's env (verified) so all AC subagents are unavoidably fresh; never imply continuity → confabulation.
- **TWO NEW FLAGS (Justin, 2026-06-17):**
  - **users.status typed-tool:** the bad_actor user-stamp uses `supabase_upsert` to set `users.status`. NOT an audit-gap (`users` has no lifecycle log; the Step-18 guard is emailmgr_items-only), so fine for go-live. Clean end-state = a typed tool for user status too → **fold into the broader "typed tools replace generic `supabase_upsert` everywhere" sweep** (v0.3 S1 security item — service_role / arbitrary-table surface — + Step 50 dedup). Not a one-off now (Justin's call). PROPOSE adding to the sprint there.
  - **manual-Supabase recovery UX:** `executeApproval`'s rare double-send guard (`flow.ts` ~L562 `notifyDev` to Justin, fires only when the post-send `delivered` flip fails 3×) tells the HUMAN to "Manually set status to delivered in Supabase." Pre-existing, NOT a stranding/security risk (human path, not Evryn's MCP tool), but a real UX rough-edge — **follow-up: make the recovery a runtime/tool action or "ping an AC," never "Justin opens the DB."** Low-urgency (rare path).
- **Deferred nits** (from earlier): redact `redact_text` in the redact tool's `logToolCall`; only stamp `redacted:true` when count>0; injected `updateEmailmgrItem` at the delivered-flip. Fold into the next substantive DC pass.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
