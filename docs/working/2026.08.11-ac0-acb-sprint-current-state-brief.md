# Lane B brief — the sprint + `current-state` restructure (AC0-35 → ACb)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file: THIS IS YOUR MARCHING ORDERS AND YOUR SHARED SURFACE.** It is written so a completely fresh ACb can be spun from it and work without asking anyone anything. It supersedes this message where they conflict. **Append your own notes, decisions and open questions to §7 as you go** — if you die mid-flight, this file is what a fresh instance re-spins from, so keep it current enough to do that.
>
> **Owner:** ACb (a sub-AC under AC0-35). **AC0-35 is your parent** — questions go to him, never to Justin.
>
> **⏹ LANE STATUS: COMPLETE.** Both documents are restructured and both branches are **pushed** — `acb/current-state-restructure` (`_evryn-meta`) and `acb/sprint-restructure` (`evryn-backend`). **Both unmerged, awaiting Justin's SCM review.** Nothing is owed by a fresh ACb2 except the merge procedure immediately below.

---

## 🔴 STOP — READ BEFORE MERGING `acb/current-state-restructure`. THIS IS THE MOST DANGEROUS THING IN THIS LANE.

**The work is done. The only remaining risk is the merge, and it is one git will not warn you about.**

**The hazard:** this branch **rewrites `docs/current-state.md` wholesale.** Since the branch was cut, **`main`'s copy has GROWN** — ACf appended a full team-runtime block, and AC0-35 corrected the Atlas-merge claim. ⇒ **A CONFLICT IS THE GOOD OUTCOME.** The dangerous outcome is a **clean merge**, which silently discards ACf's block with nothing failing and nothing printed.

### 🔴 REQUIRED PROCEDURE — all four. These are steps, not suggestions.

1. **Diff `main`'s `current-state.md` against this branch BY CONTENT before merging.** Do not accept git's verdict as evidence. **A clean merge proves nothing here.**
2. **ACf's team-runtime block MUST land in the new `📮 BULLETIN` section.** **Not optional, not a judgment call** — that section exists *precisely* because cross-lane notices like ACf's had nowhere to go, so dropping it would re-commit the exact failure this lane was spun to fix. Fold it in dated and signed, per the bulletin's own four rules.
3. **AC0-35's Atlas-merge correction is ALREADY MADE** in this branch. Do not re-apply it, and do not read its absence from the diff as a regression.
4. **Check Step 106's status before merging the sprint branch.** It is placed `TODO`; **Lane E was building it** (`dc/step106-nul-bytes`) while this lane ran. **If Lane E landed, flip it.**

*(Full close-out, rejected alternatives and Justin's ballot are in §7. If you read only one thing in this file, read those four steps.)*

---

## §0 — What this lane is, in one paragraph

**Two documents that track live state have both drifted into being logs, and each one forbids exactly that in its own text.** You are restructuring `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (the single active build plan for Evryn's v0.2 product build) and `_evryn-meta/docs/current-state.md` (the cross-project status snapshot every agent reads on load), **and reformatting the sprint to the shape of a tracker Justin recently reworked and liked.** This is a **docs-only** lane: you touch no code, no identity file, and no runtime.

**Why it matters:** both files are read cold, by fresh instances, at the start of every session. `current-state.md` is on the *Light* startup cascade — literally everyone loads it. A file that has become a log is one a cold reader cannot use, because "what is true now" is buried under "what was true on eleven previous occasions."

---

## §1 — 🔴 THE THREE FINDINGS, VERIFIED AT SOURCE BY AC0-35

**These are your starting evidence, not your scope limit.** Each was checked personally against the file, tonight. Where AC0-34's handoff and my own measurement disagreed, the corrected number is here.

### 1.1 The sprint violates a rule written in its own body, and the rule predicted the violation

`SPRINT-V0.2-HARDENING.md:49` reads, verbatim:

> *"**There is no 'Current State' section in this doc — by design.** Live cross-project state lives in `_evryn-meta/docs/current-state.md`… **Duplicating it in a header would just create the drift this doc exists to kill.**"*

**And lines 7–17 are two stacked deploy-status blocks sitting above it** — the first thing a cold reader hits.

⚠️ **State this accurately; do not overstate it.** The *upper* block (7–15) was deliberately corrected on 2026-08-10 and is currently accurate. The *lower* block (17) is explicitly labelled `HISTORICAL` and says so. **So the finding is not "the header is stale" — it is that a doc whose own rule forbids a current-state header has one anyway, and the last time it drifted it went four releases stale before anyone noticed.** The upper block's own text admits the failure mode: *"that mismatch is exactly how this header went four releases stale while every individual Step below stayed accurate."*

**The design question for you:** the header *does* carry something real (a pointer to `deploy-log.md` as the authoritative ship ledger, and the seven-item-undeployed fact). **Deleting it outright may lose a genuinely useful signpost.** Decide whether the right shape is (a) delete it and trust line 49's "derive current state by scanning the status prefixes," (b) collapse it to a two-line pointer that cannot drift because it contains no status, or (c) something better. **Recommend and justify; do not silently pick.**

### 1.2 Steps 103–106 are bolted onto the end, outside the phase structure

The doc's own model (`:44`) is explicit: **numbers are permanent handles, positions reflect timeline and phase.** Steps 103, 104, 105 and 106 currently sit in two ad-hoc sections appended after everything else (`## Defects surfaced by the Code Atlas build` and `## SEALED-EXEC + a grep blind spot in this repo`).

**Keep every number exactly where it is; move the positions into the phases they belong to.** The doc already has precedent for this — Step 44 sits among 57–60 on purpose, and `:44` tells you that is correct rather than mess.

> ✅ **CORRECTED by ACb, 2026-08-11 — this undercounts the problem, and AC0-35 accepted the correction as the basis for widening the lane.** Steps 103–106 are **not** an anomaly bolted onto the end; they are the **15th and 16th instance of a pattern that began at `:222`.** The doc has had **two organising schemes fighting** since then: sections 1–10 are *phases* (a timeline), and sections 11–26 are *provenance* groupings ("Foundation-audit-derived items", "Security bulkhead lane", "Defects surfaced by the Code Atlas build"). ⇒ **Re-homing only 103–106 into phases would relocate four items and leave the scheme broken** — and worse, Phases 0–4 are *entirely shipped*, so filing a live TODO into Phase 1 files today's defect under last quarter's plan. **The ratified fix was to flip the doc's dominant axis to open-vs-closed**, with phase/provenance kept as secondary grouping inside each region.

### 1.3 `current-state.md` has become append-only in practice — and the measurement is worse than previously recorded

- Its own header (`:5`) says: *"**NOT a log** — each update **replaces** the previous state."*
- Its own header (`:9`) sets a target of **~50 lines of body**. **It is at 152.**
- 🔴 **Line 11 alone is ~6,900 characters** and carries **11 bracketed dated entries from 10 distinct instances** (AC0-25, ACf1, ACf ×2, AC0-26, AC0-27, ACp, AC2, ACh, AC0-31, ACf-12), stacked into a single unbroken paragraph.
  > ⚠️ **This corrects the figure in AC0-34's handoff, which said "eleven … from six different instances."** The entry count was right; the instance count was not. **Measured by AC0-35 with a script, 2026-08-11** — if you re-derive it and get something different, trust your own count and say so.
  > ✅ **ACb re-derived it, 2026-08-11.** The **entry and instance counts are confirmed exactly.** Two small corrections: the character count measures **6,902** by `awk length` (AC0-35 recorded 6,788 — non-material, most likely a byte-vs-character difference); and **AC0-24 is not one of the 11 bracketed appendages** — it is the base *"Last updated"* attribution the appendages were stacked on top of, so the original list named 12 instances for 11 entries.

**The lane should fix the instance AND say why it keeps happening.** A doc that forbids logging and gets used as a log is not fixed by one cleanup — the next eleven instances will do exactly the same thing unless something structural changes. **Diagnosing that mechanism is part of the deliverable**, and it is the more valuable part. *(A live hypothesis worth testing, not a conclusion: appending is safe and replacing requires knowing what is still true, so under time pressure every instance appends. If that is the mechanism, the fix has to make replacing cheap or make appending structurally impossible.)*

---

## §2 — The reformat: adopt the team-runtime tracker's SHAPE

**The model:** `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` (298 lines) — the tracker Justin reworked on **2026-08-06** and called "awesome." *(Date corrected by ACb from 2026-08-10 — the tracker's own line 3 says "Restructured 2026-08-06", and its rule-2 renumber exception carries the same date.)*

🔴 **Read the tracker itself. Do not build from anyone's one-line description of it, including this one.** The point is to see what it actually does well, in its own text.

### ⚖️ ONE CONFLICT YOU MUST RESOLVE RATHER THAN STEAMROLL

**The team-runtime tracker's rework involved a one-time RENUMBER. Our sprint's model says NEVER renumber** (`:44`) — because Step numbers are permanent handles referenced by other docs, briefs, agents and Justin, and renumbering silently breaks every external reference.

**Default: adopt the FORMAT, not that particular move.** If after reading both you genuinely believe a renumber is right, you may propose it — **but it is not yours to execute unilaterally**, and it would require grepping *every* repo for the old numbers and updating them in the same pass. **Surface it to AC0-35 as a decision; do not do it and tell him after.**

### Why the reformat is being folded in now, so you don't re-litigate it

The original deferral (2026-08-06 handoff) was: *"doing it while the extraction is moving code would make every Step's file references churn simultaneously."* **The `classify.ts` extraction has not happened.** So doing it now is *before* that churn, not during it — and a well-formed tracker is what the extraction lane will then work against. The other half of the reason (*"I wanted the Atlas first so people can think straight"*) **is satisfied — the Atlas exists and is merged.**

---

## §3 — Your isolation, and the hard gates

- **Two worktrees, already provisioned for you by AC0-35** (you own neither their creation nor their reaping):
  - `C:/Users/Justin/Evryn/Code/_evryn-meta-lane-b` on branch **`acb/current-state-restructure`** — for `current-state.md`.
  - `C:/Users/Justin/Evryn/Code/evryn-backend-lane-b` on branch **`acb/sprint-restructure`** — for the sprint docs.
- **Work ONLY in those two worktrees. Never touch `main` in either repo.** Both are shared, live trees with other agents in them tonight.
- **Commit to your own branches freely, by explicit path.** 🔴 **`git add <paths>` does NOT scope a commit — a bare `git commit` afterwards commits the entire index, including anything another agent staged. Always put the pathspec on the commit: `git commit -F - -- path/a path/b`.**
- 🔒 **You NEVER merge, NEVER push, NEVER deploy, and NEVER run a migration.** Those sit on the escalation ladder's hard floor and belong to Justin via AC0-35. This is true no matter how finished the work feels.
- **You write no changelog entry.** The AC who merges writes it. **Hand AC0-35 the changelog-worthy facts in your report instead** — `CHANGELOG.md` is a hot multi-agent file and a sub-AC reaching into it is how another lane's uncommitted work gets swept up.
- **These are source-of-truth documents.** Your output stays **uncommitted-to-`main`, in SCM, for Justin's own review.** That is the whole gate on this lane — do not let "it's obviously better" substitute for his read.

---

## §4 — How to work this lane

1. **Describe-then-pause, to AC0-35.** Before you restructure anything, come back with what you think the target shape is for each document and why. This is a doc with real complexity and Justin's standing rule is that realigning a complex doc *after* a misaimed rewrite costs more than a 30-second shape-check first.
2. **Justin is asleep and will not answer tonight. BATCH rather than block.** Put genuine-Justin questions in a single numbered, recommend-first list in your report, and keep advancing everything that is not blocked on them. A blocking question is not a stop.
3. **Prefer full file writes over many incremental edits** on these two files — incremental edits render confusingly in VS Code and look like deletion of recent work. But **do not rewrite from memory**: re-read immediately before you write, because these are shared files.
4. **You are the lane owner and you own this lane's design.** AC0-35 holds broader cross-lane context and is worth bouncing collisions off, but **within your lane his brief is a pitch to bring your own judgment to, not orders.** If your read differs from this brief with the documents in front of you, say so and argue it.

---

## §5 — 🔴 What NOT to do

- 🚫 **Do NOT renumber Steps** (see §2, and the doc's own `:44`).
- 🚫 **Do NOT delete content you cannot re-home.** These docs carry hard-won engineering history. Settled/shipped detail may collapse to a pointer **only if that pointer resolves to something that already exists** (the repo's `CHANGELOG.md`, `deploy-log.md`) — never point at an entry that has not been written yet.
- 🚫 **Do NOT "fix" `current-state.md` by patching one section.** Half-freshening it makes one spot look maintained while the rest misleads — which is worse than leaving it visibly stale.
- 🚫 **Do NOT touch `evryn-backend/src/`, `evryn-backend/identity/**`, or any protocol/CLAUDE.md file.** Other lanes are live in some of those tonight and the identity layer is Mira's and Justin's.
- 🚫 **Do NOT invent status.** If you cannot establish whether something shipped, say so. `deploy-log.md` plus an item's own STATUS is the authoritative "did it ship?" pair; a "rides the next deploy" note is **intent, never proof**.

---

## §6 — Known-stale things you will trip over, so you don't chase them

- **The sprint's 2026-07-21 REORG MAP (`:19–26`) declares itself authoritative over inline statuses that lag.** That is real and deliberate — but it is also *itself* a year-old-in-agent-time artifact. Treat the tension as part of what you are fixing.
- **`current-state.md` is stale in named ways** — it carries no current SHA and does not reflect today. **You own it; that is the point of the lane.**
- **AC0-34's handoff (`docs/working/2026.08.11-ac0-ac0-handoff.md`) §2.7 lists line counts that have already drifted** — `ac-orchestration-protocol.md` is 941 not 842, `conductor-cascade.md` is 110 not 95. **Re-measure anything you rely on.**

---

## §7 — RUNNING LOG (ACb appends here; keep it re-spin-complete)

### AC0-35 — 2026-08-11 night — lane opened

Worktrees provisioned on both repos off current `main` (`_evryn-meta` `87dd55a`, `evryn-backend` `6995007`). Findings in §1 all verified at source tonight; §1.3's instance count corrected against the handoff. Nothing built yet. Justin is away for the night; AC0-35 is conducting.

### ACb — 2026-08-11T23:44:27-07:00 — measurements taken, shape proposed, PAUSED for AC0-35's ratification

**Nothing has been restructured yet.** This entry is written so a fresh ACb can re-spin and pick up without re-measuring. Both worktrees are clean and on their branches; `_evryn-meta-lane-b` was fast-forwarded `87dd55a → 2f330e6` (it was cut *before* this brief was committed, so the brief was not in it — a fresh ACb should check its base the same way).

**THE LOAD-BEARING MEASUREMENT — the closure ratio, which drives every other answer.** Measured by grep over the Step-header lines of each doc:

| | product sprint | team-runtime tracker |
|---|---|---|
| numbered Steps | ~106 (105 header lines matched) | 57 |
| DONE | 73 | 16 |
| MOOT / MOVED-out | 12 | 0 |
| open (TODO / IN PROGRESS / other) | 20 | 41 |
| **closed** | **~81%** | **~28%** |

⇒ **The tracker's central navigation claim — its rule 6, *"scan the labels; the boundary where ✅ gives way to ⬜ IS the current state"* — is a property of being 28% closed, not a property of the format.** At 81% closed, spread across 26 sections ordered by phase/provenance rather than by status, there is no boundary to find: the ~20 open Steps are scattered among ~85 closed ones. **This is almost certainly why the deploy-status header exists at all** — someone needed an at-a-glance answer, scanning did not give one, so they wrote a header, and it drifted four releases. **Deleting the header without fixing navigability just removes the crutch and leaves the limp, and a future instance re-adds a header within weeks.**

**Other measurements, all re-derived rather than inherited:**
- **Off-legend statuses: 10 Step lines carry a status the doc's own line 42 does not declare** — `MOVED →` ×6 (38, 51, 53, 71, 79, 89), `WATCH-LIST` (69), `SPLIT` (70), `TODO → ROUTED` (50), `DONE / (d → WATCH-LIST)` (76). Line 42 declares exactly five: `DONE · IN PROGRESS · TODO · BLOCKED · MOOT`. **`BLOCKED` is declared and never used.**
- **Two organising schemes are fighting.** Sections 1–10 are *phases* (timeline). Sections 11–26 (from `:222` onward) are *provenance* groupings ("Foundation-audit-derived items", "Security bulkhead lane", "Defects surfaced by the Code Atlas build"). ⇒ **Steps 103–106 are not anomalous; they are the 15th and 16th instance of a pattern that started at `:222`.** Re-homing only 103–106 relocates four items and leaves the scheme broken.
- `## SEALED-EXEC …` (`:460`) is the **only** section in the file with no `---` rule before it — a small tell that it was bolted on.
- **Renumber blast radius (settles §2): sprint Step numbers are cited in shipped `src/` code comments** — `triage/verdict.ts`, `triage/preload.ts`, `triage/outcome.ts`, `email/process.ts` (13 hits), `email/poll.ts` (9), `safety/fetch-retry.ts` — plus `tests/` (~40 files), `operator-guide.md` (18), `staging-runbook.md` (7), `SPRINT-MARK-LIVE.md` (9), `SPRINT-V0.3-CANDIDATES.md` (5), and **DC's and QC's operating manuals**. **A renumber would require a docs-only lane to edit runtime source comments.** And the tracker's own rule 2 records that its renumber was safe *only because a check first proved the doc effectively uncited* — **our sprint fails that precondition outright, so the tracker itself is the argument against copying the move.** ⇒ **Format, not renumber. Settled unless AC0-35 overrides.**
- `current-state.md` line 11 measures **6,902 characters** by `awk length` (AC0-35 recorded 6,788 — non-material, likely a byte-vs-character difference). **His entry and instance counts are CONFIRMED exactly: 11 bracketed appendages from 10 distinct instances** (`ACf` appears twice), sitting on top of an AC0-24 base attribution. *(Minor: §1.3's parenthetical list names AC0-24 among the instances, but AC0-24 is the base "Last updated" line, not a bracketed appendage — 12 names for 11 entries.)*

**🔴 THE `current-state.md` DIAGNOSIS — I tested §1.3's hypothesis and it does not survive; the mechanism is different, and the fix follows from the difference.** §1.3 proposed *"appending is safe, replacing requires knowing what is still true, so under pressure everyone appends."* **Evidence against it:** the appendages are *more* work than an in-place edit, not less — every one is signed with an instance name and a full ISO timestamp, several carry explicit ownership routing (*"AC0 owns, ASAP"*, *"ACf owns this repo's pushes"*), and one states outright that it is being recorded there *because the doc that held it was being retired* (ACh's entry). **That is the signature of correspondence, not of a shortcut.** ⇒ **The real mechanism: `current-state.md` is the only broadcast channel that exists.** CLAUDE.md is explicit that there is no live AC↔AC delivery and that coordination runs through Justin or a committed file; Slack reaches only Justin; a lane brief reaches only that lane. **`current-state.md` is the one file every instance loads on the Light cascade** — so it is structurally the only way to reach *everyone*, and a broadcast bus is append-only by nature (you cannot "replace" someone else's message). **The snapshot and the bus are two documents sharing one filename.** ⇒ The structural fix is to give the bus a bounded home with a scheduled drain, not to exhort people to replace.

**PROPOSED SHAPES — pitched to AC0-35, not yet ratified, NOT YET EXECUTED:**
1. **Sprint header:** collapse to a pointer carrying **no status**; relocate the *rules* it contains (deploy-log-plus-STATUS is the authoritative pair; "rides the next deploy" is intent not proof) into "The model", where the doc's other standing rules live; migrate the seven-item-undeployed *fact* to `current-state.md` / `deploy-log.md`. ⚠ **Do not lose `:17`'s owed task** — *"a `DONE`-status reformat/compaction pass on the older round-2 entries is still owed"* — that is work, not status, and it must survive the deletion.
2. **Sprint axis:** flip the dominant axis to **open-vs-closed**, with phase/provenance as secondary grouping inside each region. Numbers never move; positions do (blessed by `:44`). This is a bigger move than the brief scoped and is AC0-35's to ratify.
3. **Navigability:** a short **open-items index** of bare `Step N — five-word label` pointers carrying **no status text**, so a stale entry points at a Step whose own label contradicts it (visibly self-correcting) rather than making a silent false claim. ⚠ In tension with the tracker's rule 8 — flagged, not hidden.
4. **Gates as a first-class section**, per the tracker's rule 7 (a gate is re-evaluated and can RE-OPEN; a Step is monotonic). Gate-B is currently encoded as Step 88, and Step 88's own text says it re-runs if meaningful runtime work lands after it — so its `DONE` would become a lie. **Highest-value single transfer.**
5. **`current-state.md`:** true snapshot + a bounded, explicitly append-only **📮 BULLETIN** section with an expiry rule (`#lock` drains it; anything still true is folded into the body above).

**Open questions carried to AC0-35 (his call) and to Justin's morning ballot** — see ACb's report of this timestamp. Nothing else is owed by a re-spinning ACb except: re-read this §7, then execute whatever AC0-35 ratified.

### ACb — 2026-08-12T00:01-07:00 — AC0-35 ratified; BOTH DOCUMENTS EXECUTED. Awaiting Justin's SCM review.

**Rulings received:** Q1 (b)-with-routing ✅ · **Q2 OVERRULED — no open-items index; the axis flip is the navigability fix and an index on top of it would be a second home** ✅ · Q3 axis flip ✅ (recorded by AC0-35 as a deliberate scope expansion, to be named to Justin) · Q4a **ACb's broadcast-channel diagnosis adopted over AC0-35's** ✅ · Q4b bounded bulletin ✅ **plus his sharpening: the bulletin must NOT depend on `#lock` firing — give it an intrinsic staleness signal so an un-drained bulletin fails loudly** · Q5 format-not-renumber ✅.

**Delivered — two branches, neither merged, neither pushed:**
- **`acb/sprint-restructure`** (`evryn-backend-lane-b`), commit `b7524ea`. 476 → 663 lines.
- **`acb/current-state-restructure`** (`_evryn-meta-lane-b`), commits `a576efa` (the doc) + this one. 152 → 124 lines, body ~58.

**HOW THE SPRINT MOVE WAS MADE SAFE, because this matters if it ever needs redoing:** the reorder was done **programmatically, not by retyping** — a parser split the file into front-matter / section / step blocks and **proved a byte-exact round trip** before anything moved, then the emitter **proved all 107 original Step bodies present exactly once** in the output, and a final independent check confirmed **every Step number's occurrence count was unchanged** with `107` as the only new one. Scripts are in the session scratchpad (throwaway). **No Step was renumbered, reworded, compressed or deleted.**

**What changed beyond the ratified shape, each deliberate:**
- **Legend: six values, not five.** `MOVED` was legitimised (it encodes *still live, tracked elsewhere*, which none of the five could); `WATCH-LIST`, `SPLIT` and `TODO → ROUTED` were **destinations, not states**, and were mapped on with their destinations kept in the Step bodies. Four Step header tokens were rewritten — 50, 69, 70, 76 — token only, every clause preserved, each applied exactly once and verified.
- **`BLOCKED` is retained in the legend though unused**, with a note saying so, so its absence reads as *"nothing is blocked"* rather than as an omission.
- **Gate-A naming collision FLAGGED, NOT RESOLVED** — the product sprint's Phase-1 heading and `current-state.md`/`deploy-log.md` use that name for two different things. Both satisfied, so nothing is at risk; picking one meaning is Justin's or AC0's call, not a lane's.
- **The two flip-gates (`CLUSTERING_MODE`, `HAIKU_SCREEN_MODE`) were collected into the GATES section.** Their preconditions were scattered across four Steps and had **never been listed in one place**.
- **Step 107 minted** for ACf's routed dashboard status-pill finding, placed in the LIVE region beside the other operator-facing work. ⚠️ **The note file `docs/working/2026.08.10-acf-ac0-dashboard-status-pill-note.md` is now redundant — AC0-35 said he will delete it; ACb deliberately did not.**

**Two claims in `current-state.md` were corrected against git rather than carried forward:** the **Code Atlas IS merged** to `main` (it read "BUILT AND PUSHED BUT NOT MERGED"), and **`evryn-team-runtime` main IS pushed**, 0 ahead of origin (it read "17 commits AHEAD — exists only on Justin's machine"). **Both were true when written and had simply been overtaken.** This is the exact decay the new *"go ask the repo"* block exists to stop.

**⚠️ A COLLISION A FUTURE INSTANCE MUST KNOW ABOUT: Lane E was building Step 106 while this lane restructured the doc that holds it** (`evryn-backend-lane-e`, branch `dc/step106-nul-bytes`). Step 106 is placed as **TODO** in the LIVE region, which was correct at the time of writing. **If Lane E merged, its status is stale and needs flipping — check before trusting it.** This is a generic hazard of restructuring a tracker while lanes are live in it, and it is the one thing in this delivery most likely to be wrong by morning.

**Nothing is owed by a re-spinning ACb.** Both documents are committed to their branches; Justin's SCM review is the gate.

### ACb — CLOSE-OUT — the things that existed only in my head, written down before they died

**🔴 1. THE MERGE HAZARD THE DIFF CANNOT SHOW — read before merging `acb/current-state-restructure`.** My branch **rewrites `current-state.md` wholesale.** Since my branch was cut, **`main`'s copy has GROWN** — ACf appended a full team-runtime block and AC0-35 corrected the Atlas-merge claim. ⇒ **Either git conflicts (fine — reconcile by hand) or, the dangerous case, it merges cleanly and SILENTLY DISCARDS ACf's appended block.** **Do not read a clean merge as proof nothing was lost.** Compare the two versions **by content**, not by git's verdict. **ACf's content belongs in the new 📮 BULLETIN — that is exactly what it is for.** AC0-35's Atlas correction is **already present** in my version.

**2. Two further merge-time assumptions invisible in the diff:**
- **Step 106's status.** Placed `TODO`; Lane E was building it on `dc/step106-nul-bytes` while I worked. **If that landed, flip it.**
- **`docs/working/2026.08.10-acf-ac0-dashboard-status-pill-note.md` is now redundant** — its content is Step 107. AC0-35 said he would delete it; **if that was lost to compaction it is still sitting there, looking like an un-actioned routed item.** ACb deliberately did not delete it.

**3. Rejected alternatives, so nobody re-litigates them:**
- **Grouping LIVE by the original provenance sections** (~12 sub-headings for 20 Steps). *Safer* — needs no judgment about placement — but it does not navigate, which was the whole point of the axis flip. ⇒ **The four buckets are JUDGMENT, not derivation. Disagree and move a Step freely: positions carry no meaning, numbers are the handles.**
- **Hand-writing the reorder.** Rejected for a reason beyond safety: a hand-write changes every line, making the diff **unreviewable for Justin** — and his review is the only gate on this lane. The scripted move kept it at 529/342 rather than ~663/476.
- **Lifting Gate-B's named efficacy check into the GATES prose.** Rejected — it would create the second-home problem this lane exists to remove. The gate points; Step 88 holds the substance.
- **Rationalising the CLOSED region's ordering.** Deliberately NOT done — it still carries the old phase-then-provenance scheme, **quarantined below the divider where it costs a reader nothing.** ⇒ **The owed DONE-compaction pass should just compact; it must NOT try to re-home 87 closed Steps.**

**4. Two mechanical gotchas for anyone rebuilding these files with a script:** the final section's preamble **already contains the truncation canary**, so a reassembler that appends one emits **two** (I hit this and guarded it); and the carried-provenance blockquotes in LIVE render with an empty `> ` line — a cosmetic artifact of my quoting, not content.

**5. ⚠️ What I did NOT touch, so a reviewer does not assume otherwise:** the existing bullets of *"⚠️ Before you execute any Step"* and *"The model"* are **byte-identical to before.** I only **appended** to "The model". The permanent-handle rule, the judge-it-against-the-live-system rule and the one-list model are all original text.

**6. The five-item ballot for Justin, inlined so it cannot be lost** *(AC0-35 rescued it verbatim into his handoff; this is the second copy)*: **(1)** the sprint restructure is a **scope expansion** beyond the original brief — recommend **keep**, since Phases 0–4 are entirely shipped and re-homing live TODOs into them files today's defects under last quarter's plan; **(2)** `current-state.md` still carries ~8 lines of stable non-state prose ("Strategic pivot & how AC works now") — recommend moving to the Hub or CLAUDE.md, both propose-first, which is why it was left in place; **(3)** the BULLETIN needs a one-line `#lock` drain amendment — recommend **yes** (it degrades loudly without one, but should not have to); **(4)** **"Gate-A" means two different things across three source-of-truth docs** — recommend keeping the 13/13 Megan suite as Gate-A and renaming the Phase-1 one, since the Megan meaning is what live docs use; **(5)** **ACh's cross-runtime raw-write finding has no durable home** and survives only inside a status snapshot — recommend routing it to `evryn-team-runtime`'s ARCHITECTURE via ACf.

**⇒ With the above written down, this brief IS re-spin-complete.** A fresh ACb2 can finish the lane, and "finishing" now means only: **reconcile the merge per the REQUIRED PROCEDURE at the top of this file · flip Step 106 if Lane E landed · carry the ballot.**

### ACb — fragment sweep (final trip)

**🔴 1. A JUSTIN RULING LANDED AFTER THE REWRITE, AND I HAVE FIXED IT ON THIS BRANCH.** Justin ruled the sequencing: **deploy the bundle to STAGING → Gate-B → extract → Gate-B AGAIN.** My rewrite had carried the **opposite** order in the critical-path line, and had carried the deploy-before-or-after-extraction question as *"ONE GENUINELY OPEN QUESTION, deliberately unresolved."* **Both are now corrected on this branch:** the critical path states the ruled order, and the open-question bullet is replaced by the ruling **with its reasoning preserved** (a refactor's *"behavior is unchanged"* claim is only checkable against a known-good baseline, which seven never-deployed items are not; Gate-B is repeatable, which is what makes the second pass cheap). ⚠️ **It also carries an explicit staleness marker** — *"any doc still describing this as undecided, or ordering extract-before-deploy, is stale; this line is the ruling"* — so the next reader stops looking. **`current-state.md` on `main` still carries the old wording at roughly `:50` and `:64`; merging this branch is what fixes it.**

**2. Two findings that are AC0-35's, not mine — recorded ONLY as a backstop against his compaction.** Both were surfaced this session and he said he was carrying them; if they made it, ignore this. **(a)** The **auto-injected `CLAUDE.md` is a stale snapshot** — the copy delivered into a subagent's context was missing content present on disk (the three-dot-diff bullet, commit `a04f5a0`, landed hours earlier), which means **every subagent spun in that session inherited an out-of-date operating manual.** It was found only because the identity block's *"do not assume the auto-load was complete"* instruction was actually executed. **(b)** `docs/protocols/lock-protocol.md` **has no bottom truncation canary**, which makes the standard *"confirm the canary"* load instruction unsatisfiable for it; AC0-35 was batching it with three others (`evryn-ops/CLAUDE.md`, `scripts/dependency-map.ts`, `tests/test-breaker-resume.ts`). **Neither is mine to fix and I did not touch them.**

**3. Nothing else surfaced.** Re-walked the session start to finish: the measurements, the five ratified rulings, the six-value legend, the Gate-A collision, the flip-gates, Step 107, the two git-corrected claims, the Lane E collision, the rejected alternatives and the ballot are all committed above. **The scripts that performed the reorder were throwaway and are gone with the scratchpad — the method is described in §7 and is reproducible from that description; nothing depends on the scripts surviving.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
