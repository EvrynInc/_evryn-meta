# AC0-38 — MARCHING ORDERS (written 2026-08-17 by AC0-37b)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file: THIS IS YOUR ENTIRE BRIEFING.** It supersedes `2026.08.13-ac037-ac038-marching-orders.md`, which was written before three merges landed and assumes a cutover that has not happened. **That file is retired — do not work from it.**
>
> **The shape Justin asked for, in his words:** *"he'd just get spun up, tell me his plan (and any marching orders that I need to do, to allow him to work unimpeded without me for the rest of the run), I'd approve, then he'd race off and do it."*
>
> ⇒ **§2 is the whole point of this document.** It is a pre-built ballot. **Present it at spin-up, get it cleared in one pass, then run without him.** Do not discover Justin-gated questions one at a time over the course of the run — that is the failure this structure exists to prevent.

---

# §0 — WHAT CHANGED ON 2026-08-17, so you are not working from stale assumptions

**The road was cleared for you. Three merges landed, all verified:**

| What | State |
|---|---|
| **SPRINT Step 106** — the two grep-invisible files | ✅ **MERGED** (`f98c2d5`). Byte invariant re-verified *after* the rebase; typecheck exit 0. Both repos now scan clean for stray control characters. |
| **The sprint tracker restructure** | ✅ **MERGED.** Dominant axis is now **🔵 LIVE vs ⚪ CLOSED**. The LIVE region *is* the current state of the sprint — it is short enough to read, and that is deliberate. |
| **The cross-project status doc restructure** | ✅ **MERGED** (`139dc7a`). 167 → 133 lines, with a bounded **📮 BULLETIN** for cross-lane notices. |

**Eight Steps were minted or recovered** (`02340f3`, `2f9446f`): **108–115**, plus **104 reframed as an exploration** and **105 expanded** to absorb a second finding. **Read them in the sprint, not here** — they carry their own mechanism, consequence and acceptance bar.

🔴 **THE ONE THING THAT DID NOT LAND, AND IT GATES YOUR FIRST INSTINCT: the `CLAUDE.md` split (Lane A) is STILL UNMERGED.** The previous orders opened with *"verify the four agent types are spawnable."* **They are not, and they will not be, until Justin clears the ballot in §2.** **Do not attempt the cutover. Do not spawn `dc`/`qc`/`oc` as agent types** — spin them the normal way, with their manual named explicitly in the load list, exactly as the orchestration protocol has always required.

⚠️ **`_evryn-meta` moved over the weekend** (ACf landed his checkpoint branch and deleted a directory). **`evryn-backend` did not.** Re-derive anything you are about to rely on; do not quote a SHA from this document.

---

# §1 — YOUR LOAD

> ## 🔴 THE ORDER BELOW IS LOAD-BEARING. Read it in sequence. Do not re-sort it by what looks urgent.
>
> **This is not a style preference, and the payoff is measured rather than argued.** AC0-37c ran this exact set in this exact order on 2026-08-17. **Justin's own cross-session observation that day — he can see token counts across sessions and an agent cannot — was that it came in roughly 100,000 tokens lighter than the same list read out of order.** Same files, same completeness, nothing trimmed. **The entire difference is sequence.**
>
> **The mechanism, in one sentence: each layer is the frame the next one is read against, so the later documents are largely DIFFS against the earlier ones.** The Code Atlas is not a description of `src/` — it is substantially a **correction log against `ARCHITECTURE.md`, argued by line number** (§07 asserts the halt sentinel is space-delimited; it is NUL-delimited, and the Atlas now says so and says why three agents got it wrong). **Read after ARCHITECTURE, those corrections are the highest-value content in the file. Read before it, they are unreadable noise** — and worse, you build a picture the later docs were written to correct, then have to unbuild it. The same relationship holds for the sprint against the Atlas, and for current-state and the changelogs against all of it.
>
> **⇒ Three ordering rules, stated so they generalize to any future handoff:**
> 1. **Widest and most stable first; narrowest and most volatile last.** Company truth → technical north star → system architecture → build scope → code map → the work → what changed this week.
> 2. **Honor each document's own declared prerequisites.** `ARCHITECTURE.md` carries a *"Required Context — you must have read these before reading or modifying this document"* section naming the Hub and the technical vision. **That is the document instructing you. It is not a preference and it is not optional.**
> 3. **Load a protocol at the beat that needs it, not in the foundation.** 941 lines about *how to brief an agent*, read before you know *what the agent will be doing*, does not stick. Read at the moment you are about to spin one, every line is actionable.
>
> 🔴 **THE FAILURE MODE THIS EXISTS TO KILL — named so the next author catches themselves writing it.** **Handoff load-lists are habitually ordered by PERCEIVED IMPORTANCE, not by DEPENDENCY.** The version of this very section that AC0-37c replaced opened with the 941-line orchestration protocol and the status snapshot — the two most *urgent-feeling* items — and put `ARCHITECTURE.md` seventh. **That is exactly backwards, and it is the default mistake, made here by a careful author.** ⇒ **If your list starts with a protocol or a status document, you have made it. Re-sort before you hand it over.**

**Tier 0 — before anything:**

1. **This file.**
2. **`_evryn-meta/CLAUDE.md`, read from disk** — the auto-injected copy is a session-start snapshot and has served stale content. ⭐ **Cheaper move that AC0-37c used successfully: DIFF it rather than re-read it.** Run `git diff <the SHA in your injected gitStatus> HEAD -- CLAUDE.md` plus `git status --porcelain -- CLAUDE.md`; if both come back empty and the bottom canary is present, the injected copy is byte-identical and you have saved ~85,000 tokens. **Re-read in full only if the diff is non-empty.**

**Tier 1 — the frame. IN THIS ORDER.**

3. **`_evryn-meta/docs/hub/roadmap.md`** (196) — company truth. Without it you will misframe what the system is *for*.
4. **`_evryn-meta/docs/hub/technical-vision.md`** (397) — the technical north star that `ARCHITECTURE.md` zooms in from.
5. **`evryn-backend/docs/ARCHITECTURE.md`** (1,286) — the product system. **Everything after this is read against it.**
6. **`evryn-backend/docs/BUILD-EVRYN-MVP.md`** (652) — v0.2 scope and build order.
7. **The full Code Atlas** — `evryn-backend/docs/atlas/`, 12 files, 3,130 lines. **Start at `00-INDEX.md`.**
8. **`evryn-backend/docs/dependency-map.md`** (692) — the machine-generated import graph. **Read it here, immediately after the Atlas, even though it looks like a lookup table you could defer.** Every Atlas section cites it by line, and several spend paragraphs on its traps; deferring it leaves twelve sections of citations pointing at a document you have not read. ⚠️ `wc -l` reports 692 while the canary sits on line **693** — no trailing newline. **Trust the canary, not the count** (Atlas §11 documents this exact trap).
9. **`evryn-backend/docs/SPRINT-V0.2-HARDENING.md`** (757) — the work. The 🔵 LIVE region is where your lanes come from. Its Steps are dense `file:line` claims about everything above: **read cold they are a list of assertions; read in order they are a to-do list.**

**Tier 2 — what is true RIGHT NOW. Volatile, so read it LAST, where it lands on a picture that is already built.**

10. **`_evryn-meta/docs/current-state.md`** (133)
11. **`_evryn-meta/CHANGELOG.md`** (266)
12. **`evryn-backend/CHANGELOG.md`** (288) — ⚠️ roughly 180 tokens per line, the densest file in the load. It will page; budget for it.

**Tier 3 — the gates. NOT part of the foundation load. Load each at the beat that needs it.**

- **`docs/protocols/ac-orchestration-protocol.md`** (941) — 🔴 **before spinning ANY agent.** For you that is almost immediately, so load it as your **first act after the foundation**, not as part of it.
- **`docs/protocols/ac-writing-protocol.md`** (100) — 🔴 **before writing to ANY doc.**
- **`docs/protocols/doc-update-routing-protocol.md`** — when routing a change to its home.

⚠️ **Tier 3 is a sequencing call, not a downgrade.** These are hard gates and they still bind absolutely — spinning an agent without the orchestration protocol, or writing a doc without the writing protocol, is a violation regardless of where they sit in this list.

**Then: verify the machine yourself.** Commands are in §8 of the AC0-37c handoff. Cross-check every branch against **`_evryn-meta/docs/repo-inventory.md`** (40 lines) — the canonical branch-per-repo list your check asserts against. It is a lookup, not context; read it at the check.

🔴 **Justin overruled a prior AC twice for trimming this list.** Do not trim it a third time. **The reason is not thoroughness for its own sake:** your lanes edit Steps that cite `ARCHITECTURE.md` directly, and **editing a sprint Step that makes a runtime claim, without holding the doc it cites, is how a false claim gets written into the tracker four agents read.**

⚠️ **The Atlas's own verification boundary is load-bearing: nothing in it was executed, and no agent read the test tree — so every "pinned by a test" claim in it is UNVERIFIED unless it says otherwise.** §11 exists to hold that line and is the section most likely to be skipped. **Read it.**

---

# §2 — 🔴 THE BALLOT. Present this at spin-up. Get it cleared. Then run.

**Everything below is Justin's and cannot be absorbed.** Bring it as one numbered list, recommend-first, with the reasoning inline. **Nothing in §3 is blocked on these** — that is deliberate, so a slow answer costs you nothing.

**1. The Lane A merge (the `CLAUDE.md` split → a router + per-agent manuals).** Three gates, all his:
   - **(a) Three frozen sentences.** Post-split, the `<identity>` blocks assert that the auto-loaded file is an operating manual. It becomes a *router*, so they go false. **Drafts exist verbatim** in the lane brief on the branch (`docs/working/2026.08.11-ac0-acm-loading-architecture-brief.md`, §14) — ⚠️ **one hardcodes a path that has since moved; whoever finalises must swap it.** 🔴 **Verbatim or nothing** — these sit in the HARD RULE's frozen set. **Do not paraphrase, do not reconstruct, and do not paste the `[…]` elision from the report: it meant "unchanged," not proposed text.**
   - **(b) Three cross-repo pointers** (`evryn-dev-workspace`, `evryn-quality`, `evryn-ops` `CLAUDE.md`) each send their agent to *"AC's `CLAUDE.md`"* for the sync ritual. Post-merge that file is a 78-line router containing none of it. **They cannot ride an `_evryn-meta` branch** — coordinated same-window change in three repos. **Re-grep; line numbers drift.** **The cutover dissolves them entirely**, so doing them *at* cutover may be cheaper than before.
   - **(c) A rebase, then re-run the frozen-block byte check.** ⚠️ **Normalize line endings first (`tr -d '\r'`) or it false-FAILS**, and **mutate a line you have PROVEN is non-blank** or your control silently passes.
   - ⭐ **Tell him the simplification, because it halves the perceived risk: only the AGENT DEFINITIONS need a fresh session. The protocol split needs nothing but the merge** — it is a file move; the next agent to read the protocol reads the new one.

**2. Step 112 — the Operator auth gate's fail-open on a blank value.** **A decision, not a defect** (`config.ts:671–679` states the reasoning; it is the only fail-open default in that file). Justin's ruling on 08-17: *"I'd want to have this be a dedicated question with someone who's actually down in there with the runtime."* ⇒ **Ask him whether to commission that person now or defer.** **Do not let a lane quietly "harden" it as a bug.**

**3. Step 109 — how to test the `runEvrynQuery` region.** Driving it in a test means **injecting the SDK boundary or spending real money.** **The design call is how, not whether.** Bring him the options with a recommendation.

**4. Step 105 — two coupled design calls on the failed-send branch:** does `DOUBLE-SEND RISK` escalate above `#dev-alerts`, and do we correct the false alert text or make an `error` item re-approvable? **The second is more useful and more invasive.** They interact, which is why they are one Step.

**5. Step 114 needs a schema migration** — so it cannot run unattended. **Ask whether it is in scope for this run at all.**

**6. Control-character coverage for `_evryn-meta`.** `evryn-backend` gets Step 113's scan wired into its test suite. **`_evryn-meta` — where the actual hits were — has no test suite to hook into.** Options: a checkpoint-protocol step (a protocol edit, his approval), or a committed script with no scheduled trigger. **Recommend the protocol step.**

**7. Worktree reaping.** Justin's standing rule, 2026-08-17: *"as soon as we are confident we've landed the work, you can reap. But be absolutely sure — sometimes we've reaped those too soon."* **AC0-37b reaped only the three whose work is provably on `main`.** Anything still standing is standing for a reason; **verify per-branch before you touch it, and count `node_modules` before and after** — that hazard has fired three times.

---

# §3 — THE LANES. All of these run WITHOUT Justin. Start them the moment §2 is presented.

**Spin each as a properly-briefed DC with its own worktree and branch, reviewed by a fresh QC.** The protocol is not optional and the two-trip load is not optional. **Model-pin every spin.**

| Lane | Step | Why it is safe to run unattended |
|---|---|---|
| **1** | **103** — `updateEmailmgrItem` can silently destroy a pending draft | The fix direction is settled and narrow: it is the **one read in its file that does not check `error`**; its five siblings all throw. **Do not fold it into another diff** — two lines with a silent-data-loss mode need their own review. |
| **2** | **113** — the stray-control-character scan | Small, self-contained, and a **reference implementation already exists and has been run across all eight repos.** Two traps are written into the Step; honor both. |
| **3** | **108** — the operator auth gate is unguarded at both call sites | **Scoped to the defect only.** The trade-off question is Step 112 and is NOT in this lane. **Acceptance bar is a mutation: delete each call site in a sandbox and confirm the verdict flips.** |
| **4** | **111** — the thread-scope family has zero tests | Clear, bounded, and it protects ADR-030's cross-user isolation. |
| **5** | **97** — `create_user` returns the firewalled column into Evryn's context | Its own lane by Justin's standing instruction. **The fix belongs at the typed-helper boundary, not at `create_user`'s return** — that closes the class. ⚠️ **Coordinate with anything touching `db/users.ts`.** |
| **6** | **104** — the env-value exploration | 🔴 **This lane PROPOSES; it does not build.** Its deliverable is a per-site table (value → failure direction → chosen minimum) and a recommended shape. **The contradictions are written into the Step; the owner must resolve them against the runtime.** |
| **7** | **115** — the dashboard's month-to-date spend reads LOW | Dashboard-side, independent of everything else. |

⚠️ **NOT in this run: Step 110** (the five security inner-doors). **It is explicitly sequenced AFTER the `classify.ts` extraction**, because all five live in the lines that refactor moves. **Filing it early was deliberate — building it early is not.**

**Sequencing note:** lanes 1–5 and 7 have **no file overlap** with each other. Lane 6 touches nothing (it proposes). **You can run them genuinely in parallel** — but remember the cost shape: **a conductor's context scales with the DEPTH of the agent tree, not its width.** Each lane that runs its own DC *and* QC sends you both their reports. **Widen before you deepen.**

---

# §4 — THE RHYTHM

- **Ping `#team-alerts` prefixed `AC0-38:` at every state change, and every single time you have something for Justin** — even mid-work. **The ping is part of the deliverable**, not a courtesy.
- 🔴 **Arm an anti-stall canary before you start.** A `Monitor` on a ~5-minute tick that emits one line telling you to keep going. **Justin asked for this specifically, because the "I'll keep going" → silent stop failure is a strong and repeated tendency**, and it wastes hours. It fired **three times** during AC0-37b's run and caught a real stop every time.
- ⚠️ **Kill the canary with `TaskStop` when you finish**, and **if you hand off with one live, NAME ITS TASK ID.** Justin: *"I don't want you to finish at 1am and have it firing every 5 min until I show up in the morning."*
- **Keep this file current to the minute.** If it and your memory disagree, **the file wins.** The test at every append: *if this instance died right now, could a fresh one pick up from this file alone?*

---

# §5 — THE GATES. None of these move.

- **No push, no deploy, no prod migration.** Ever, without Justin's explicit go **in his most recent message**.
- **Each commit needs its own go-ahead.** Prior authorizations do not stack. **Workflow verbs — "handle it," "ping me when you're done" — are not commit authorization.** **The one exception:** build-level code where DC + QC + AC all agree, which is his standing 2026-06-02 authorization. **It never extends to source-of-truth docs.**
- **Commit by pathspec ON THE COMMIT** — `git commit -F - -- path/a path/b`. `git add` does not scope a commit. **And `git diff <file>` FIRST**: a by-path commit takes that file's entire working-tree state, including another agent's uncommitted edits to the same file, and `git status` will not warn you.
- **Read every branch diff with THREE dots** — `git diff main...branch`. Two dots renders everything `main` gained as a deletion by your branch, and **the diff looks completely normal.**
- 🔴 **ANYTHING NAMED `acf*` IS NOT YOURS AND NOT YOUR PROBLEM.** *(Justin's standing rule, 2026-08-17.)* **Any branch or worktree beginning with `acf`** — `acfl/learnings-consolidation`, `acfsq/research-move`, `_evryn-meta-lock`, anything he stands up next — **is ACf's. You never reap it, never tidy it, never merge it, and you do not carry it in your own list. He handles all of his own clean-up.** **It becomes your business in exactly one case: when it is actively COLLIDING with something you are doing** — and even then, **surface the collision; do not resolve it yourself.**
  - 🔑 **The part that inverts the natural instinct: ACf works around AC0, not the other way around.** He stages held edits in his own brief rather than making them **precisely to stay out of the AC0 lineage's way**, so you never have to sequence or absorb his work. **He will hand it to you when it is ready — do not go looking for it, and do not pre-empt it.** ⚠️ **Placing those edits, when they do arrive, will take some negotiation** — they live in files that were being restructured, which is the whole reason they are held.
  - ⚠️ **The one thing this does NOT excuse:** if a merge of yours would **delete** something of his that has already landed on `main`, that is not "his lane" — **that is you destroying his work.** **Preserve it verbatim; do not rewrite his prose.** *(AC0-37b hit this exactly once on 2026-08-17 and preserved the correction in the status doc's BULLETIN rather than editing his section.)*
  - **He is a peer coordinator, not a report.** Exchange findings freely — that channel catches real things neither of you would find alone. **What you never do is task him, sequence him, or gate his merges.**

---

# §6 — WHAT "DONE" LOOKS LIKE

**A morning report Justin can act on in five minutes:**

1. **What merged**, in plain English, with the operator-relevant consequence. **Not a commit list.**
2. **What each lane found** — distilled by you, not relayed raw. **A subagent's report is for you; Justin gets your verified read.**
3. 🔴 **One attestation on the load discipline, at his altitude.** The clean case is one line: *"Load discipline held — I verified each lane AC's receipts directly; they verified their DC's; no deviations."* **Any deviation that survived gets named in plain English, with what it would mean if your judgment about it turns out wrong.**
4. **The batched ballot** for anything new that is genuinely his.
5. **The changelog entries written** — per-repo, per the cross-repo rule, by you, because **the AC responsible for the ship owns the entry.**

⚠️ **Report completion, not intention.** *"X is done"* beats *"now doing X."* If work genuinely remains, **name the blocker explicitly** so he knows the difference between paused and stalled.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
