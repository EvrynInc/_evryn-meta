# Handoff — AC0 → next AC0 — 2026-07-06 EOD (cost lane narrowing; staging on the critical path)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** START HERE if you're a fresh AC0. You are the **conductor** of a multi-lane v0.2-hardening wave — you orchestrate, gate, reconcile, and keep the housekeeping current; you do NOT write runtime code. This is the live board. **The operating model in `_evryn-meta/CLAUDE.md` + the prior handoff `docs/working/2026.07.02-ac0-ac0-handoff.md` (its "Operating model" §) are inline below and in `CLAUDE.md` — you do NOT need the prior handoff to boot (its BOARD is stale; do not act on it).** Companions to THIS handoff: the lane briefs (`docs/working/2026.06.30-ac0-ac{2,3}-*.md` + `2026.07.02-ac0-ac6-*.md` — read ONE on-demand, in full, when a lane engages; see First moves), `docs/deploy-log.md`, `docs/current-state.md`.
>
> *Authored 2026-07-06 EOD. Lanes gave honest "where I'm at right now" updates before this (not clean set-downs) — so treat their tails as mid-flight snapshots.*

---

## 🚀 First moves (fresh AC0 — do these on boot)
1. **Load your complete boot set:** the light cascade (`CLAUDE.md` auto-loads → `current-state.md` → Hub `roadmap.md` → `technical-vision.md`) + **this handoff, in full.** **This handoff is written to be self-sufficient** — it summarizes all 3 lanes' current state, so **you do NOT need to pre-read the lane briefs to boot.** (You read a lane's brief on-demand — see the 📖 note under this list.) Load the FULL `src/` runtime only if Justin explicitly calls the full cascade (then ALL of it — enumerate live).
2. **Ping "I'm up"** on `#team-alerts` (`AC0:` prefix) — remake your ping helper first (see §⚡ below).
3. **Orient, don't chase:** **nothing is hot-blocked waiting on YOU right now.** Every live wait is on **Justin** — his "go" on Clustering, his staging account-actions, his BUILD-relocation ballot for AC6, his Step-55 merge-go. Your job is to be *ready to gate + answer* when a lane surfaces work; you don't need to grab anything the instant you boot.
4. **Then watch for:** AC2 reaching **Runtime Bookkeeping** → run **Gate-A with them BEFORE it merges** (Step 88); keep the **AC6 DELTA-FOLD list** current as things land (do NOT rebase AC6 per-landing); CHANGELOG + current-state current at every ship.

**📖 Reading a lane brief (on-demand, NOT up-front — and don't hunt for a "tail"):** When a lane surfaces work and you want more depth than this handoff gives, read **that one lane's brief — the whole CURRENT brief, front to back.** Do NOT look for a "tail": *you* are a fresh instance, so the ENTIRE brief is unread by you, and its recent coordination rounds only make sense on top of the plan + context earlier in the doc. The briefs are already consolidated to be lean (settled history collapsed to one-line CHANGELOG pointers), so "the whole brief" is the *current* brief, not a giant archive — a single lane is a manageable read. Read one lane's brief when that lane engages; you rarely need all three at once. *(The per-lane pointers below each name a "freshest-state" section — that's a start-here hint, not a read-only-this: read the brief in full when you deep-dive it.)*

---

## ⚡ Operating model (unchanged — the load-bearing bits)
- **You conduct; you do NOT write runtime code.** Each lane's DC/QC builds; you read reports (appended to each brief), reconcile cross-lane collisions + sequencing, gate merges/deploys, keep Justin at CEO-altitude, keep housekeeping current (current-state, CHANGELOG, deploy-log, sprint marks).
- **Each lane's brief IS the AC↔AC0 mailbox** (`docs/working/`). Read the tail; append your reply as a `## ═══ AC0 → ACn · date · summary ═══` banner *before* the truncation canary; **commit + push it immediately** (brief/mailbox commits are pre-authorized). No live AC-to-AC channel — coordinate cross-lane through Justin or the committed briefs.
- **PING every final response** on `#team-alerts`, `AC0:` prefix — even when Justin seems present (he stressed this repeatedly, 7/6). Helper: `scratchpad/ping.mjs` (mine won't be at your path — remake it: read the `SLACK_TEAM_WEBHOOK_URL=` LINE directly from `C:/Users/Justin/Evryn/Code/evryn-team-workspace/.env`, POST `{text}`, print the `ok` response to confirm it landed).
- **Justin reads ONLY the chat, never briefs.** CEO-altitude, plain-English. **Use the LOCKED names, not jargon-by-number:** "Haiku Bouncer" (cost ①), "Clustering" (cost ②), "Runtime Bookkeeping" (cost ③). Never "Step 44"/"③"/"B2c" to him.
- **Commit/deploy gates are Justin's, per-action — they don't stack.** Lanes build/edit freely, pause for his explicit go before commit + before `railway up`. **EXCEPTION (standing): housekeeping-doc commits** (current-state, deploy-log, CHANGELOG, sprint DONE-marks) — you commit + push freely. Stage by explicit path, never `git add -A` (shared repos).
- **You're a PEER to the lanes on their design, not their boss.** Answer collision/sequencing Qs; let them own the architecture of their lane. `AskUserQuestion` is BANNED (hangs).
- **Verify before claiming.** You have (or load) the full runtime; check cross-lane behavior claims against `evryn-backend/src/`. If Justin calls the full cascade, load ALL of `src/` (enumerate LIVE: `find src -name '*.ts'` — it's grown well past the old counts; never trust a hard-coded number).
- **Subagent RESUME works** (a deferred `SendMessage` re-wakes a completed subagent by its `agentId` with full prior context — proven ≥28h): a real efficiency lever for fix/re-review loops (a fix-trip to the same DC keeps its build context). BUT a *resumed* QC is NOT fresh eyes — spin fresh where independence is the point. Mechanics: `docs/protocols/ac-orchestration-protocol.md`.

---

## 📊 LIVE STATE (authoritative)

**Prod = `v0.2.6` LIVE** (`evryn-backend` deployed commit `8e11e9d`, tag `v0.2.6`; `main` sits docs/tests-only ahead of the deployed `8e11e9d` — **re-verify the tip live before any merge**). Clean boot. The **Haiku Bouncer** (cost ①) is the payload — the cheap 3-verdict "obvious no-fit?" screen in front of Opus triage — **shipped `off` = FULLY dormant (never runs, collects no data).**

**⚠ Carry this — the un-missable Mark-onboarding action:** the flip `HAIKU_SCREEN_MODE`=off→`shadow` at Mark-onboarding is REQUIRED to start the validation trial; without it the ~$1,800/mo lever sits dead. It's flagged in **4 places** (current-state stamp, CHANGELOG 2026-07-06, the watch-list Step-44 entry, AND the operator-guide **Go-Live Checklist** — the one Justin actually runs). The later flip-to-`active` (after ~1wk of ~0 false-filter) is a separate Justin-gated call.

**The wave = 2 active build lanes (AC2 cost, AC3 identity/ops) + AC6 doc-reconciliation.** AC1 (cleanup) + AC4 (security) closed weeks ago. Mira is building in-flight (Step 55). Nothing deploys unattended; nothing is in front of Mark (still pre-forwarding).

**Deploy queue:** cost ① Haiku Bouncer ✓ (`v0.2.6`) → next is cost ② **Clustering** (must be live before Mark) → cost ③ Runtime Bookkeeping. Each `railway up` = the next `v0.2.N`, Justin-gated; the lane instance executes, you gate.

---

## 🛣 Critical path to Mark (the frame)
Finish the cost lane (**Clustering MUST be live before Mark forwards**; then Runtime Bookkeeping) → **Gate B: the mandatory pre-Mark full create-from-zero live-fire** (Step 88, its home is **staging** → so AC3's staging standup is on the critical path) → Justin's one-time account setups → Mark forwards → **flip Haiku Bouncer off→shadow**. Megan Griffiths is the fictional test gatekeeper; AC3 has written the fixtures.

---

## LANE: AC2 — cost (brief: `2026.06.30-ac0-ac2-cost-lane-brief.md` — read in full on-demand; freshest state = its last round, "Round 13 EOD")
- **Haiku Bouncer (①) = DONE + LIVE (`v0.2.6`, OFF).** Lane instance did merge/tag/prod-migration/deploy/deploy-log cleanly; AC0 did the current-state/sprint-44-DONE/CHANGELOG reconcile.
- **Clustering (②) = design decisions LOCKED by Justin (7/6), NOT built, awaiting Justin's final "aligned, go" on the reshaped design.** Locked: twice-daily ~10am + ~4:30pm PT; **manual-first** (a Slack `cluster now`-style command hand-fires the first batches) → scheduled once trusted; ingest/triage split via a **NEW lifecycle status (working name `queued`)** — small migration + `status` CHECK extension (**lifecycle changes route through JUSTIN, not Soren**); restart-catch-up is the primary interruption recovery; a **personal-note real-time pathway** (a forward *with* a note reaches Evryn live — she judges urgent-pull-now vs acknowledge-and-queue; has a Mira/voice half); **reply-aware forward detection** (a Mark reply mis-detected as a forward = a phantom queue item — close it here); gold latency up-to-half-a-day accepted.
  - **⟹ Scope grew to 3 coupled pieces:** (1) queue + twice-daily batch, (2) the personal-note real-time pathway, (3) the reply-aware forward-detection fix. Bigger than a "modest DC build."
  - **OPEN, awaiting Justin:** the "aligned, go"; one design Q (noted-forward-judged-NOT-urgent → quick real-time ack vs silent-to-queue — his voice call); verdict-change-tracking (edge→gold trail — offered, not decided).
- **Runtime Bookkeeping (③) = NOT started** (the keystone; folds in deterministic-`triage_result`-for-gold/edge + an ADR-018 amendment alongside ADR-033; composes with AC4's read-lock — write-removal vs read-lock). **Gate-A (Step 88 integration baseline) must run with AC0 BEFORE ③ merges** — loop in when AC2 reaches it.
- **Step 71** (Reflection binding safety-net) still on the plate (small).
- **⚠ CORRECTION for you (supersedes prior handoffs):** the M1 "batch ran — screened N / escalated M / drafted K" heartbeat is **NOT an AC0 fold-in.** AC2 builds it directly into `src/safety/*`; the ONLY doc piece is a paragraph AC2 adds to `ARCHITECTURE.md`, which **Justin reviews in SCM before AC2 commits.** You do not own it.

## LANE: AC3 — identity/ops (brief: `2026.06.30-ac0-ac3-identity-ops-brief.md` — read in full on-demand; freshest state = its "AC3 → AC0 · 2026-07-06 EOD" section)
- **⚡ Staging standup (Step 80) = CRITICAL PATH, in flight.** Supabase staging done (`hyqeutelwywbkisjekkb`); Justin set up the free staging Gmail; AC3 handed Justin the OAuth walkthrough → he mints the refresh token + returns 4 values → AC3 wires them into the **staging Railway service (D)**. **Still TODO: staging Slack app (C) + the 2nd Railway service (D)**; the dead Railway prod `_PROD` unqualified vars get cleaned up bundled into D. This is what unblocks Gate B — keep it moving with Justin.
- **Step 55 (determination-sharing) = BUILT by Mira; formal review + merge OWED.** Branch `mira/declining-md-step55-2026-07-06` (pushed, tip `a6e813a`): a NEW `identity/internal-reference/declining.md` + a *bound* on core.md's "You never disclose your assessments" + a module-list pointer. AC3 approved the **mechanics/runtime** only. **⚠ The FORMAL `evryn-team-workspace/shared/protocols/identity-file-review.md` run is still OWED before merge** (Justin flagged) → then Justin's merge-go + a `#dev-alerts` ping on the identity merge. **Mira flagged FOR AC0:** a core.md split-cleanup at the module-split step (Step 53).
- **54b (forward-persistence) = design RATIFIED + refined (Justin's gold→match §6); build TODO** (DC+QC not spun). Unblocks 54a.
- **Step 43 (Megan fixtures) = WRITTEN + committed** (`tests/2026.07.06-step43-megan-fixtures.md`) — answer-key + judgment/cost + adversarial (incl. the spoofed-forward). **You orchestrate the runs** (both Step-88 gates).
- AC3 next: the Step-55 protocol review → the decline-offer runtime+identity pair (Mira) → 54c real-name capture → Step 56 Nathan brief → Streams C/D/E.

## LANE: AC6 — ARCH/BUILD doc-reconciliation (brief: `2026.07.02-ac0-ac6-*.md` — read in full on-demand; freshest state = its two 2026-07-06 banners, incl. the DELTA-FOLD decision)
- **⚠ BIGGEST CHANGE FOR YOU — the DELTA-FOLD decision (Justin, 7/6) REVERSES the old "keep AC6 rebased as lanes ship":** AC6 now works against a **FROZEN baseline (`2a82a88`)** and does **NOT** chase mid-lane landings. ALL landings are HELD for **ONE consolidated end-of-lane Delta-Fold pass** (read changelog + **ONE rebase** onto then-current `main`, coordinated with you THEN + fold each landing through every doc-home at once → a final fidelity subagent certifies vs LIVE `main`). **So: do NOT rebase AC6's branch per-landing.** Your job is to **maintain the DELTA-FOLD running list** (append each thing that lands to it) — it currently has Haiku Bouncer; append cost ②/③, AC3 staging/identity, etc. as they ship.
- **ARCH pass = DONE + fidelity-validated** (the reviewer that failed Friday on a rate-limit was re-run, PASSED, 0 defects — definitive-vs-frozen-baseline, re-certified at the Delta-Fold). **BUILD accuracy pass = DONE + pushed (`7b10036`).** **BUILD relocation pass = plan presented, awaiting Justin's ballot** (proposes a new `BUILD-EVRYN-v0.3.md` + `BUILD-FUTURE.md`, relocating the v0.3 mechanics out of the v0.2 BUILD doc). Nothing moved yet.
- **2 flags AC6 handed you to adjudicate at YOUR pass (HOLD — not now):** (1) CONFIRMED-stale `notify_queue` mention in a **code comment** at `src/security/supabase-read-guard.ts:35-37` (cosmetic; the guard is allow-list so it's harmless; route a one-line comment tidy to a lane, or note-and-leave — AC6 can't touch `src/`); (2) UNVERIFIED claim that `docs/schema-reference.md` is stale vs the live schema (you/AC3 verify — AC3 owns schema-reference-adjacent staleness).
- AC6's branch `7b10036` is pushed; its `_evryn-meta` EOD note was committed-then-pushed by you (see below).

---

## 📌 AC0-owed (standing — carry these)
1. **Gate-A / Gate-B integration cadence (Step 88).** Gate A = a pre-Runtime-Bookkeeping end-to-end baseline on DEV (before ③ merges). Gate B = the mandatory pre-Mark full create-from-zero live-fire (staging its home). Megan = the character; AC3 built the fixtures; **you orchestrate the runs.**
2. **The DELTA-FOLD list for AC6** — append every landing (it's the running record AC6 folds at the end; you also do the ONE rebase with AC6 then, NOT per-landing).
3. **The 2 AC6 flags** (notify_queue code-comment + schema-reference verify) — adjudicate at your pass.
4. **DB-in-3-places seed order** (your 7/03 routing): seed staging from a prod-shape `pg_dump` taken AFTER a prod migration lands, so new tables (e.g. `haiku_screen_shadow`) come along free — hold this ordering as AC3 seeds staging.
5. **Step-55 merge awareness:** the identity-file-review protocol run is owed (AC3 runs it; you're aware); the core.md split-cleanup (Mira's flag) lands at Step 53.
6. **Housekeeping as-you-go:** CHANGELOG at every ship (top priority), current-state, deploy-log, sprint marks.

## ✅ On Justin's plate
- The "aligned, go" on Clustering's reshaped 3-piece design (AC2 waiting).
- Staging account actions (staging Slack app + 2nd Railway service wiring; the OAuth values → AC3).
- The BUILD-relocation ballot for AC6.
- The Step-55 merge-go (after the protocol review).

## 🧭 Key docs
- `docs/current-state.md` (snapshot, current thru 2026-07-06) · `docs/deploy-log.md` (ship ledger, v0.2.6 logged) · `_evryn-meta/CHANGELOG.md` · `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (Step 44 = DONE) · the 3 lane briefs · `_evryn-meta/CLAUDE.md` + `docs/protocols/ac-orchestration-protocol.md` · prior handoff `docs/working/2026.07.02-ac0-ac0-handoff.md` (operating model).

## 🔧 Repo state (as of this handoff)
All active repos + the AC6/cost/Mira-step55 branches were committed + pushed to origin at EOD (batten-down + this handoff). AC6/cost worktrees are still live (active lanes — do NOT reap). No prod deploy since `v0.2.6`.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
