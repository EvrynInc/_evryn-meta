# Handoff — AC0-33 → AC0-34 — the overnight run of 2026-08-10/11

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** the LIVE state of AC0's overnight run. It is written so that a **completely fresh AC0** can be re-spun from it and continue without asking anyone anything. It is updated continuously *as work happens*, not at checkpoints — Justin's explicit instruction, because he may need to re-spin AC0 at any moment.
>
> **This file does NOT replace `docs/working/2026.08.10-ac0-ac0-handoff.md`** (AC0-32's pack-out, committed at `870b167`). That one is the *briefing* AC0-33 was given — its §0.1 pre-mortem, §3 authorizations, §5 findings and §6 rules are all still live and are NOT restated here in full. **Read that file first, then this one.**

---

## §0 — IF YOU ARE A FRESH AC0 READING THIS COLD, DO THIS

1. **Read `docs/working/2026.08.10-ac0-ac0-handoff.md` in full** (380 lines). That is the briefing. Everything in it still stands unless this file says otherwise.
2. **Load the Conductor Startup Context Cascade** — `docs/protocols/load-cascades/conductor-cascade.md` — and the non-runtime half of `docs/protocols/load-cascades/product-full-cascade.md`. **Do not load `evryn-backend/src/` yourself.**
3. **Read `docs/protocols/ac-orchestration-protocol.md` in full (796 lines).** Non-optional before spinning anything — every spin is two trips.
4. **Read §2 and §3 below** — the live lane state and what is already done.
5. 🔴 **Every subagent AC0-33 spun is UNREACHABLE to you.** Resume is session-bound; a new AC0 session cannot `SendMessage` any of them. If a lane was mid-flight, check its *worktree git state* (commits, dirty files) — that is ground truth — and re-spin fresh from its brief.

---

## §1 — THE FRAME (why tonight looks like this)

**Justin is asleep.** He handed AC0-33 the pack-out at ~20:30 PT on 2026-08-10 and said, in his own words:

- *"I'd rather have you stop than fuck things up — so if you aren't sure about something, don't do it. Just stop. Do the parts you can, and the parts you're unsure about we'll do in the morning."*
- *"Be very careful. Make sure you're using all of the protocols properly."*
- *"One strong failure mode is: you often say you're going to do something, but then you just stop."* ⇒ he asked for a **20-minute wake canary**, armed (see §5).
- *"If you compact, pause, don't continue — I'll need to re-spin you."* ⇒ **if you are reading this because AC0-33 compacted: do NOT continue that instance. Re-spin.**
- **ACf is running his own lanes overnight, in `evryn-team-runtime`, committing into `_evryn-meta` alongside AC0.** He is a **peer, not a report**. Do not task, sequence or gate him. See §2.1 of the pack-out for the full collision hazard.

---

## §2 — LIVE LANE STATE

> **Update this table every time anything moves. It is the first thing a fresh instance reads.**

| Lane | Who | State as of 2026-08-10T21:45-07:00 |
|---|---|---|
| **A — safety net** | fresh QC | ✅ **COMPLETE.** Load 126/126 exact. **Verdict: GO, zero blockers**, 16 RED / 2 GREEN on her own mutation set. **MERGED to `main` `2fc057f`, CHANGELOG `823060a`, PUSHED. Worktrees reaped, branches deleted.** See §9. |
| **B — test runner** | DC → fresh QC | **DC DONE:** `scripts/run-tests.ts` (681 lines) + `npm test`. **I rebased it onto the new `main`; tip is now `6530ba8`** (was `f4e826a`). **Fresh QC load VERIFIED (all 80 files, 45/45 src + 11/11 identity live-enumerated); review task sent 21:44; WORKING.** **NOT merged.** |
| **C — atlas / Code Atlas** | ACa2 + 6 scouts | **ACa2 has SET DOWN.** ✅ **9 of 12 sections landed** across 5 branches. ⏳ **`05-approval-notify` (s4) and `07-safety` (s6) never committed** — those two scouts are ACa2's children and are **unreachable to me**. **NOT merged — deliberately; see §9.3.** |
| **D — Gate-B reviewer** | not spun | **Still held.** Scope = the PLAN, not the run (§4.2). **Its real blocker turned out to be different from what I assumed — see §6.0b.** |
| **E — persistence writes** | AC0-33 | ✅ **DONE and pushed** — 6 commits across 4 repos on `ac0/overnight-review`. |

### 2.1 Worktrees alive right now

| Path | Branch | Purpose | Reap? |
|---|---|---|---|
| `evryn-backend` | `main` | canonical | **NEVER** |
| `evryn-backend-dc-step101` | `dc/acn-step101` | ACn's first commit; ancestor of the sweep branch | after the sweep merges |
| `evryn-backend-dc-sweep` | `dc/acn-boot-sweep` | the safety-net work under review | after merge |
| `evryn-backend-qc-sandbox` | detached at `f2f4ef0` | **mutation sandbox AC0-33 provisioned for QC** — she may wreck it freely | after QC lands |
| `evryn-backend-runner` | `dc/test-runner` | the test-runner build | after merge |

⚠️ **`node_modules` was installed with `npm ci` in the two new worktrees — NOT junctioned.** Junctioning is what caused two `node_modules` wipe incidents on 2026-08-10. **Baseline: the canonical tree had 161 entries in `node_modules` before any worktree work, and all three trees read 161 after.** Re-check that count after any `git worktree remove` (pack-out §6.1).

### 2.2 Repo state, verified at 20:40 PT

- `evryn-backend` `main` = `2a1bdab`, **0 ahead of origin**, working tree carries only the **five deliberate untracked files** (`backups/full-public-2026-08-05.sql` + four `tests/_gate-a-*.ts`). **Do not commit, delete or modify them.**
- `_evryn-meta` `main` = `870b167`, **0 ahead of origin**, working tree **clean** (ACf's `2026.08.06-acf-acfw-wake-outage-brief.md` was committed by him at `c1e61f9` — it is no longer uncommitted, contrary to the pack-out's §2.1 item 6).
- Every repo is on its canonical branch (`evryn-team-agents` on `master`, all others `main`; ACf holds three team-runtime worktrees).
- Prod is `v0.2.9`. **Nothing was deployed tonight and nothing may be.**

---

## §3 — WHAT AC0-33 HAS ACTUALLY DONE (past tense only; intentions live in §4)

*(Every line here is an action with an artifact behind it. If it isn't done, it is in §4.)*

1. **Loaded in.** Conductor cascade, orchestration protocol (full 796), writing protocol, current-state, Hub, the full `SPRINT-V0.2-HARDENING.md` (412), the ACn safety-net brief through its QC-handoff section, the ACa atlas brief §-1→§1, QC's and DC's cascade sections, and the pack-out.
2. **Verified the world** — branches, sync state, worktrees, `node_modules` counts, canary coverage (45/45 `src/`, 11/11 identity, 6/6 named docs; `tests/` deliberately has none).
3. **Created the review branch `ac0/overnight-review`** in `_evryn-meta`, off `main` `870b167`. **Every source-of-truth doc write tonight goes there, never to `main`.**
4. **Provisioned two worktrees + one mutation sandbox** and ran `npm ci` in both new trees (no junctions).
5. **Armed the 20-minute wake canary** (a persistent Monitor tick; see §5 for how to stop it).
6. **Launched the three-lane wave** — QC, DC, ACa2 — all trip-1 load-only briefs, pasted verbatim from the orchestration protocol with only the codified substitutions, every file line-spanned, model pinned to **Opus** on all three.
7. **Pinged Justin ALL CLEAR** on `#team-alerts` at 20:54 PT.
8. **Verified all three lanes' trip-1 receipts against the lists I gave them, file by file.** All three reconciled **exactly** on call count — QC 126/126, DC 44/44, ACa2 47/47 — every listed file read to its confirmed bottom canary (or true final line, for the `tests/` carve-out), every partial read disclosed and closed, no compaction reported by any of them, no `#cascade-override` used anywhere. **Then sent trip 2 to all three.**
   - **Three real catches came back up at me, and two were my errors:** my section-G header said *"29 files"* over a list of 27 (arithmetic slip, no gap — 27 is the correct number); several stated line spans were one short (`wc -l` under-reports a file whose last line has no trailing newline); and DC correctly **refused to resolve** its own manual's *"load the identity files if the task touches the runtime"* rule, because that rule needs the task and trip 1 withholds it. **I answered all three in trip 2** rather than letting them ride.
9. **✅ PERSISTED §5 AND §6 — 4 commits, 4 repos, all on `ac0/overnight-review`, all pushed.** These were the writes AC0-32 flagged as unable to survive in a working doc.
   - `_evryn-meta` `60be64a` — `CLAUDE.md` (the worktree/`node_modules` reap hazard, written as **detect-and-repair**, plus the reliable prevention proven tonight: `npm ci`, never a junction · the generalized `#`-heading rule) · `AGENT_PATTERNS.md` (a full-runtime load no longer fits one agent · the seven-instance *"a check that did not RUN, read as a check that PASSED"*) · `ac-orchestration-protocol.md` (**the lane brief is TRIP-2 reading** — the authorized write AC0-32 dropped).
   - `evryn-backend` `a41a66c` — `ARCHITECTURE.md` (the spend-door finding, now **diagnosed** not suspected · the serial drain as a safety brake · the undocumented 7-file import cycle + the dependency map's blind spots) · `BUILD-EVRYN-v0.3.md` (the extraction will **not** deliver concurrency; removing the serial loop is a *safety* change) · `SPRINT-V0.2-HARDENING.md` (header was 4 releases stale · Step 98's "27 of 61" was wrong in both terms *and* in its cause · **new Step 102**).
   - `evryn-quality` `cbe5b9c` — the promoted QC pattern: never record a mutation verdict without a baseline in the same run.
   - `evryn-team-workspace` `18d2392` — the watch-list's Haiku `active`-flip gate is now **two** conditions, not one.
10. 🔴 **I RE-VERIFIED THE SPEND-DOOR FINDING AT SOURCE MYSELF before writing it into ARCHITECTURE as fact** — rather than repeating AC0-32's report. Confirmed: exactly two SDK `query()` sites (`classify.ts:2867`, `haiku-screen.ts:406`); `recordAttemptAndCheck` defined at `detectors.ts:161` with **exactly one call site**, `classify.ts:2678`; `haiku-screen.ts` imports `query` directly at `:1` and never imports the counter. Also confirmed the serial drain independently: `poll.ts:416` is a plain `for (const email of args.emails)` and `:419` is `await args.handle(email)`, wired to `handleNewEmail` at `:1056`. **Both now carry their `file:line` citations in the docs**, so the next reader can re-check rather than trust.

---

## §4 — DECISIONS AC0-33 MADE, WITH REASONING (so they are auditable, not silent)

**These are the judgment calls. Each one is named here because Justin cannot audit what he was never shown.**

### 4.1 The lanes run in BACKGROUND, not foreground

The protocol's default is foreground, because background is invisible to Justin. **AC0-33 chose background deliberately**: Justin is asleep (nobody is watching the wall in real time), and a foreground block returns only when *every* child in it finishes — which would have frozen AC0 for the whole load and made the parallel wave pointless. **Trade named per the protocol: Justin will not see these agents' output; he gets AC0's distillation plus this file.**

### 4.2 The Gate-B lane will produce a PLAN, not a RUN — and that is a gate, not a scope cut

Step 88B's Gate-B is a **create-from-zero live-fire on staging with everything active together**. Staging exists (Step 80) but **the seven-item bundle has never been deployed to it**, and deploying is on the escalation ladder's hard floor — never AC0's, asleep-Justin or not. ⇒ **the run is physically blocked.** What is *not* blocked is everything up to the gate: the fixture set, the scenario suite, the acceptance criteria, the named thin-record efficacy check (run twice — once with a thin `active` gatekeeper, once with a `status: lead` record, because `buildRecordLegibilityBlock` renders different text for each), and Step 99's large-burst exercise. **That is what gets commissioned.**

### 4.3 🔴 ACa2 was NOT given the full `src/` runtime — and this needs Justin's eyes

**The tier rule** (`ac-orchestration-protocol.md`, "Choosing the sub-AC's tier") says an AC that makes any claim about runtime behavior gets the **Full** cascade and the entire runtime, non-negotiable. **The pack-out's §4.3** — which Justin approved specifically, on 2026-08-10, after asking how the atlas lane would be kept safe — says the opposite for *this* lane, because **three agents attempted the full product load on 2026-08-10 and two of them compacted during the LOAD.** Its answer is to restructure the work: a conductor plus a scout bench, one file per subsystem, one author per file, every claim citing `file:line`.

**AC0-33 followed §4.3**, on the protocol's own specific-governs-general reasoning (a dated, conditioned, specific grant is not silently revoked by a general rule). **ACa2's load is everything except the 45 `src/` files** — it has `classify.ts` in full, all 11 identity files, the dependency map, ARCHITECTURE, both BUILD docs, the sprint, both changelogs, the protocols, and DC's and QC's manuals. Its scouts carry the `src/` half. **It is being told explicitly that this is AC0's call and that it should push back if its own read differs.**

**⚠️ This is surfaced, not resolved.** If Justin disagrees, the fix is to re-spin ACa2 with the full runtime and accept the compaction risk.

### 4.4 QC and DC both got the *full* standing load their own manuals demand

No `#cascade-override` was used anywhere tonight. QC got all 45 `src/` files, all 11 identity files, ARCHITECTURE, BUILD, the sprint, the operator guide and the dependency map, plus the 29 test files under review. **AC0-33 explicitly declined to thin her load**, because the protocol's carve-out requires the spinner to have *recently read the full runtime himself* — and AC0-33 has not. **The known consequence: her load sits at roughly the size that compacted two of three agents earlier today.** If her trip-1 receipts show a compaction, the correct response is to **discard and re-spin**, not to discount — and if a second one compacts, that is a genuine blocker to surface to Justin rather than route around by thinning.

### 4.5 The test runner is scoped to the local runner; CI goes on the morning ballot

§4.2 of the pack-out names both *"no test runner and no CI."* A GitHub Actions workflow is repo automation with recurring external effects and a live secrets question (the 13 DB-touching suites must never run in CI with credentials). **AC0-33 is building the runner tonight and putting the CI design on Justin's ballot** rather than installing automation he has not seen.

---

## §5 — THE CANARY, AND HOW TO TURN IT OFF

A **persistent Monitor** is armed, emitting one wake line every 20 minutes. Its job is to catch AC0's known failure mode — announcing work at the end of a turn and then stopping. **Task id: `bs79hilwz`.** Stop it with `TaskStop(task_id: "bs79hilwz")` **as the last action of the night**, together with the `Done for the day` ping — so that post-Done silence reads as intended rather than as a stall. A fresh AC0 re-spun mid-night will need to arm its own; the old one dies with the session.

---

## §6 — THE OWED WORK, IN PRIORITY ORDER

**Nothing below is done. Anything that moves gets rewritten into §3 in the past tense.**

✅ **1. Safety-net merge — DONE.** Merged `2fc057f`, CHANGELOG `823060a`, pushed, three worktrees reaped (with the `node_modules` incident caught and repaired), branches deleted.
⏳ **2. The test runner — NO-GO from a fresh QC on one blocker; a one-line DC fix trip is IN FLIGHT.** **When it returns:** verify the fix **by executing it yourself** (a probe suite that genuinely fails an assertion *and* prints a `.invalid` host in its detail must go RED and exit non-zero), confirm the full-suite baseline is unchanged, and confirm the diff is still exactly two files. **Only merge if DC, QC and you all agree** — otherwise it waits for morning, and that is fine.
✅ **3. The Atlas — COMPLETE (12/12) and deliberately NOT merged.** All seven branches pushed. **The merge is Justin's call (§9.3); do not take it on his behalf.**
✅ **4. Gate-B reviewer — COMMISSIONED and delivered.** `docs/gate-b-plan.md`, 307 lines, `71393c6` on `gateb/plan`, pushed.
⏳ **5. The morning report is DRAFTED IN §8 of this file and is current.** Deliver it from there. Justin asked for *"what happened, and why should I trust it"* — §8.1 (load discipline), §8.4a (the verified zero-runtime-change claim) and §8.5 (the sixteen-item ballot) are the spine.
⏳ **6. Close-out, in this order:** reap the remaining worktrees **with the count-before / count-after `node_modules` check** (the Atlas branches are all pushed, so reaping the trees loses nothing) · leave every canonical tree on `main` and clean · **ping `Done for the day`** · **then `TaskStop` the canary (§5)** so post-Done silence reads as intended.

### 5.8 ✅ CONFIRMED AT SOURCE TONIGHT — `core.md` advertises FOUR identity modules to Evryn that do not exist on disk

**Verified by AC0-33 by enumerating every module path `core.md` names and testing each for existence.** Present: 9. **Missing: 4** — `situations/new-contact.md`, `internal-reference/canary-procedure.md`, `internal-reference/crisis-protocol.md`, `internal-reference/contact-capture.md`.

**The runtime degrades correctly, and the mechanism is a single `try/catch`** around the file read inside the `read_identity_module` tool handler (`src/triage/classify.ts`, ~`:1730–1736`): it returns *"Error: Module '<path>' not found."* rather than throwing. **Read at source; the sibling `readIdentityFile` (`:107–110`) is a bare `readFileSync` with no guard, so the catch is doing real work.**

🔴 **THE ONE THING THE EXTRACTION MUST NOT LOSE: that `try/catch` is the only reason four advertised modules are four graceful "not found" replies instead of four crashes.** Any refactor that moves the tool handler must carry the catch with it. **Also verified, and it is the reason this matters for the Atlas specifically:** `IDENTITY_DIR` is `join(import.meta.dirname, "../../identity")` (`:95`) — resolved **relative to the module's own location** — so moving prompt-composition code to a different directory depth silently re-points it, and the two halves then fail *differently*: `readIdentityFile` throws loudly on `core.md`, while `readIdentityDir` (`:186–191`) guards with `existsSync`, returns `""`, and `loadCommonPrefix` (`:242`) drops it via `parts.filter(p => p.length > 0)` — **no separator, no gap, no trace.**

### 5.9 ✅ SETTLED TONIGHT — the extraction's blast radius is **21 importers (8 `src` + 13 `tests`)**, and the durable answer is "stop using a grep for this"

**This number has been wrong three times** (the brief said 8, a scout said 16, the pack-out carried 21 with a warning that the prescribed grep was defective). **AC0-33 measured it four ways tonight and the disagreement is the useful part:**

| Method | Result | Verdict |
|---|--:|---|
| The prescribed `grep -rn "triage/classify" src tests` | **31 files** | ❌ **Over-counts.** 13 of them mention `classify.ts` only **in a comment** and import nothing. |
| A single-line import regex (`from`/`import(` + `classify.js`) | **20** | ❌ **Under-counts by one.** |
| The machine-generated `docs/dependency-map.md` (TypeScript AST) | **8 src + 13 tests = 21** | ✅ **Correct.** |
| ACa's independent earlier measurement | **8 + 13 = 21** | ✅ Agrees with the AST, by a different route. |

🔴 **The file every grep misses, and WHY — this is the transferable part.** `tests/test-record-pass.ts` imports it like this:

```
const { buildPassStamp, resolveGatekeeperName, isAlreadyPassed } = await import(
  "../src/triage/classify.js"
);
```

**The specifier sits on its own line (`:52`), so `import(` is on the previous one.** *Any* single-line grep — however carefully written — cannot see it. So a grep of import specifiers fails here in **both directions at once**: it over-counts comments and under-counts multi-line imports, and the two errors do not cancel; they just make the number arbitrary.

**⇒ THE RULE, and it should be handed to the atlas lane: for "what imports X," use `npm run depmap` and read `docs/dependency-map.md`. Do not grep.** An AST parser resolves what a regex can only approximate. **And note what this cost:** the pack-out prescribed a *corrected* grep as the durable fix, and that corrected grep is still wrong — the fix was the wrong *kind* of instrument, not a wrong pattern.

⚠️ **Two limits to state honestly:** the map covers **static and dynamic imports it can resolve statically**, so a non-literal dynamic import is still invisible to it (three exist, all in `tests/`); and **`identity/*.md` is outside the graph entirely.** The 21 is the *code* blast radius, not the whole one.

### 6.0 🔴 A QUESTION I WAS ABOUT TO PUT ON THE BALLOT IS ALREADY SETTLED — do not re-ask it

**The pack-out's §8 item 7 asks: *"does the seven-item undeployed bundle deploy BEFORE or AFTER the extraction?"* and calls it "genuinely open, and deferred twice."** It is not open. **`docs/working/2026.08.06-ac0-ac0-handoff.md` §9b settles it**, on 2026-08-10, in Justin's own words — *"I don't want to refactor while she's live, and I want Gate B to be meaningful… so the plan is to do two Gate B's, one before and one after the actual refactor."*

**The settled sequence, which every lane tonight is sitting inside:**

| # | Step | Where we are |
|---|---|---|
| 1 | **Read-only refactor** — dependency map · claims audit · **Code Atlas** | 1 and 2 done; **the Atlas is lane C tonight** |
| 2 | **Make Gate-B's suite runnable** | **the "one true blocker" — see below** |
| 3 | **Deploy the seven to STAGING** | 🔒 Justin's gate, never AC0's |
| 4 | **GATE-B #1** — validates the seven **and creates the known-good baseline** | the reviewer AC0-33 is commissioning |
| 5 | **The extraction** + its safety net | needs lanes A and B merged first |
| 6 | **GATE-B #2** — validates what Lacey actually runs | |
| 7 | **Prod deploy → flip Clustering ON + Haiku to shadow → register her lanes** | AC0-31 nearly let 7 fall off the list entirely |

**Why the answer is BEFORE, and it is a better argument than the one that was deferred twice:** Gate-B #1 *creates* the validated baseline that the extraction's entire safety claim — *"behavior is unchanged"* — has to be checkable against. Without it, a post-extraction misbehavior cannot be attributed between "a latent bug in the seven, which have never run in production" and "the extraction broke it." **⇒ Report this to Justin as RESOLVED with its reasoning, not as a question.**

⚠️ **And the cost he should re-consent to with open eyes, because it is a real one:** this puts the extraction **between Lacey saying yes and Lacey being served** — a *weeks*, not days, commitment. He has said repeatedly that meaningful beats fast and that she expects early-adopter roughness. **If she pushes for speed on Friday 2026-08-14, that is the trade being made.**

### 6.0b 🔴 THE GATE-B BLOCKER IS NOT THE ONE I ASSUMED — and it changes what the Gate-B lane is for

Step 2 above — *"make Gate-B's suite runnable"* — is called **"the one true blocker; nothing after it can start."** It is **not** the safety-net lane (that is the *extraction's* net). It is that **Gate-B's own harness is not runnable**: the four `tests/_gate-a-*.ts` files exist **only as untracked files in the canonical tree**, and `_gate-a-run.ts`'s grading logic is written against the **pre-③ world** and must be re-derived before it means anything.

⚠️ **A CONFLICT TO SURFACE, NOT RESOLVE:** `current-state.md` records that **Justin has approved committing those four files via a lane** — while the pack-out (§2) instructs AC0-33 plainly: *"Do not commit, delete, or modify them."* **Both are his.** AC0-33 honored the pack-out (the more specific, more recent instruction, and the safer branch of a genuine ambiguity) and **touched nothing.** ⇒ **Put it on the ballot; do not act on it unasked.** Committing them also means dropping the `tsconfig.tests.json` exclude, which is a real second-order change.

### 6.1 Carried to the morning ballot — do NOT act on these tonight

- **`current-state.md` is stale in three named ways** (no current SHA — it says `d95cf83`, `main` is `2a1bdab`; the "nothing is spun / no worktrees" line; and no mention of the dependency map). **AC0-32 said explicitly: do not fix tonight.** Honored. It is also the hottest shared doc in the repo and ACf is live in it.
- **`AGENT_PATTERNS.md` contradicts itself on subagent resume.** Its 2026-06-23 entry says `SendMessage` resume *"is not exposed in AC's default setup"* and that subagents are fire-and-return — while the file's own newest section describes two-trip loading, which **runs entirely on resume**, and resume has been proven at ~22 days same-session. The stale entry is still there and a cold reader hits it first. **Flagged, not fixed** — it is outside the writes Justin pre-approved.
- **The four stale safety-claim comments** (`breaker.ts:4`, `llm-usage.ts:7`, `ARCH:1162`, `ARCH:248`) that assert the "sole spend door." Two are `src/` comment changes, so they need DC, not me. The ARCHITECTURE half is now diagnosed at `:1171`; the wording elsewhere is not yet corrected.
- **CI for the test suite.** Deliberately not built (§4.5). DC is returning a design proposal instead.

---

## §7 — THE HARD BLOCKERS (unchanged from the pack-out §3.3 — repeated because they are the whole safety story)

Stop and leave things safe rather than improvising past any of these:

- **A deploy, a prod migration, or anything touching production — including a STAGING deploy.** Never AC0's.
- **A source-of-truth doc edit that cannot go on the review branch**, or any change to `evryn-backend/identity/**` (Mira's and Justin's layer).
- **A merge where the lane, the reviewer and AC0 do not all agree**, or where a reviewer returned NO-GO.
- **Anything irreversible, outward-facing, or involving money.**
- **A subagent reporting that its context compacted** — its output is junk. Discard and re-spin; do not discount it.
- **Two instructions in conflict, or a doc contradicting the runtime.** Surface it; never silently resolve.

**When you stop: leave the tree clean, commit and push what is safe, write what happened into THIS file, and ping `#team-alerts` with `Need you:`.**

---

---

# §9 — WHAT HAPPENED AFTER 21:20 (the part a fresh instance most needs)

## 9.1 ✅ The safety net is MERGED, and here is the audit trail

**`main` = `823060a`, pushed, 0 ahead of origin.** Merge commit `2fc057f` (`--no-ff`) + CHANGELOG `823060a`.

**Why I was authorized:** the pack-out §3.1 grants the merge *"ONLY after a fresh QC pass on `f2f4ef0`."* That happened. Lane + reviewer + me all agree; it is build-level code, not a source-of-truth doc, and not a push/deploy/migration — so the standing authorization covers it.

**What I verified MYSELF rather than taking on report:**
- `git merge-tree` produced tree **`90e46632…`** — **byte-identical to the tree QC had executed and tested.** The merge commit then produced exactly that tree.
- File sets fully disjoint: branch = `tests/` only; `main`'s three new commits = `src/`, `docs/`, `scripts/`, `package.json`. **That is why `--no-ff` rather than a rebase** — it ships the tree she ran, not an equivalent one she didn't.
- `npx tsc --noEmit -p tsconfig.tests.json` on the merged tree → **exit 0** (unpiped, real exit code).
- The five deliberate untracked files survived.

## 9.2 🔴 THE `node_modules` WIPE HAPPENED FOR REAL — and the check I had just written caught it

Reaping the three lane worktrees took the canonical `node_modules` from **161 → 0**. `npm ci` restored it to 161; I then **ran a real suite** (not `tsx --version`) to prove the toolchain rather than assume it. **Ninety seconds, start to finish.**

- **The mechanism is now well-evidenced:** the reaped set included a tree an earlier lane had explicitly junctioned. **The four worktrees I provisioned tonight with `npm ci` instead were unaffected.**
- ⚠️ **My own reap command included `--force` on its first attempt — minutes after I wrote "never `--force`" into CLAUDE.md.** Recorded honestly on the review branch, because it is the argument *for* detect-and-repair over prohibition: **the prohibition was violated by the person who wrote it, in the same hour, and the check worked anyway.**
- ⚠️ **And a second self-inflicted one: I reaped `evryn-backend-dc-sweep` while the incoming QC was mid-load from it.** She recovered via `git show f2f4ef0:…` and her load is complete — but I should have sequenced the reap after her load. **`f2f4ef0` is NOT dangling** (I verified `merge-base --is-ancestor f2f4ef0 main`); the `--no-ff` merge holds it permanently.

## 9.3 🔴 THE ATLAS IS BUILT — 9 of 12 sections — AND I AM DELIBERATELY NOT MERGING IT

**A 12-section Atlas at `evryn-backend/docs/atlas/`, designed as a complete non-overlapping partition of all 45 `src/` files (20,089 lines) plus the identity half.** Landed:

| Branch | Sections | SHA |
|---|---|---|
| `aca/atlas` | `00-INDEX`, `01-agent-core`, `08-security`, `09-bootstrap-and-config`, `10-identity-runtime`, `11-verification-and-tooling` | `b5f9d15`, `fc1e2e0` |
| `aca/atlas-s1` | `02-email-ingest` (216 lines) | `f90f109` |
| `aca/atlas-s2` | `03-email-processing` (294) | `5cbb165` |
| `aca/atlas-s3` | `04-triage-pipeline` (257) | `ae159f2` |
| `aca/atlas-s5` | `06-persistence` (321) | **`5ad9a7b`** *(re-verified against the migration SQL; `b3cde19` is its first draft)* |
| `aca/atlas-s4` | `05-approval-and-notify` (454) | `4dc5224` |
| `aca/atlas-s6` | `07-safety` (630) | `258f347` |

### ✅ I gated the Atlas against the bar I set for it, and it passes

**Checked mechanically across all twelve sections, on the committed blobs rather than the working copies:**
- **12 of 12 carry the mandated *"THIS IS AN INDEX. IT IS NEVER A SUBSTITUTE FOR READING THE RUNTIME"* block.** That was the one hard requirement, and it is the thing that stops the Atlas re-creating the blindness it was built to end.
- **12 of 12 carry a bottom truncation canary.**
- **Well over 500 `file:line` citations** across **3,108 lines** mapping a 20,089-line runtime.

⚠️ **My citation count is a floor, not a figure — my counting pattern was wrong (see §9.5's correction; that was the third time tonight).** `01-agent-core` uses the bare `:NNNN` form because it covers a single file and the filename is implied; my regex read that as **1 citation when it has 41**, and I briefly concluded the section was thin. It isn't. **Assume every per-section number I quote here is an undercount.**

✅ **ALL TWELVE SECTIONS ARE IN, across seven branches.** Both stragglers landed after ACa2 had already set down — which is the routing quirk (§9.4) doing exactly what it does. **I found them by inspecting branches, not by being told.**

🔴 **THE MERGE DECISION, AND THE REASONING, BECAUSE IT IS MINE AND JUSTIN SHOULD BE ABLE TO OVERRULE IT: I am NOT merging the Atlas.** The standing build-level authorization covers *code Justin cannot meaningfully evaluate* and **explicitly excludes source-of-truth docs.** The Atlas is a brand-new documentation layer that **every future agent will load and reason from**, and a wrong Atlas is worse than none. **That is squarely his call, not mine** — so it stays on its branches, complete and pushed, for his review.

**Two scouts never reported.** They are ACa2's children, and **ACa2 has set down — so they are unreachable to me** (only their spawner can resume them). If they have not landed by morning, sections 05 and 07 need a fresh spin.

## 9.4 🔴 THE ROUTING QUIRK WAS 3 FOR 3 — a real harness finding

**None of scouts s1, s2 or s3's work-trip reports reached ACa2.** All three surfaced to **me** instead. ACa2 discovered they had finished only by running `git log` in their worktrees. **⇒ Silence from a nested agent is not evidence it is still working.** I relayed s1's findings down explicitly; s5's report reached nobody at all, and I found its commit by inspecting the branch.

**The durable rule: check worktree git state, never infer from silence.**

## 9.5 🔴 A CENSUS I RAN MYSELF: the unguarded-`parseInt` class is ELEVEN sites, not three — and it reaches a security guard

Two scouts independently flagged instances. **I then ran the full census at source and traced each consequence, because the direction of failure differs per site and pattern-matching would have been wrong.**

**Eleven bare `parseInt(process.env.X ?? "…", 10)` sites live outside `config.ts`'s bounded set.** `config.ts:512–556` documents this exact class at length and `parseBoundedInt` exists to close it — **but that cleanup only ever covered knobs inside `config.ts`.**

| Site | If the value is garbage → `NaN` | Direction |
|---|---|---|
| 🔴 `security/web-guard.ts:44,46,48` | every `length > NaN` is false → **the exfil-shape length caps silently stop tripping**, while the SSRF and scheme checks keep passing, so the guard still looks alive in logs | **DANGEROUS — silently off** |
| 🔴 `reflection/consolidate.ts` ×3 | `:189` `length > target * multiplier` → false → **the oversize alarm silently never fires** | **DANGEROUS — silently off** |
| `email/poll.ts:1618` (`PROACTIVE_CHECK_HOUR_PT`) | `:1723` `if (ptHour !== NaN) return;` is always true → **the daily proactive check-in silently never runs** | **silently off** |
| `safety/detectors.ts:36` (`BREAKER_WINDOW_MS`) | `prune()`'s guard `windowAttempts[0].at < NaN` is false → **the window never prunes and grows without bound** → velocity counts *lifetime* attempts against a 600/hr ceiling | **fails toward a FALSE HALT, not blindness** — I corrected my own first instinct here by tracing it |
| `safety/affirmation.ts:32` | `:66` `if (hour < NaN) return false` never fires → **the hour gate is disabled**, so the affirmation fires at the wrong time rather than never | mis-timed, not lost |
| `notify/slack.ts:114,115` | quiet-hours comparisons go false → quiet hours effectively disabled | low — ADR-040 posts immediately anyway |
| `config.ts:801` (`PORT`) | **deliberately excluded, with documented reasoning** — it throws loudly, and a fail-safe default would make a loud failure quiet | ✅ correct as-is |

### 🔴🔴 CORRECTION TO MY OWN CENSUS — it is THIRTEEN sites, and the two I missed are the worst two in the codebase. I made the identical mistake I had documented two hours earlier.

**A scout found two sites my grep did not: `safety/detectors.ts:44` (`BREAKER_LOOP_SIGNATURE_THRESHOLD`) and `:58` (`BREAKER_VELOCITY_CEILING_PER_HOUR`).** I verified both at source.

**Why I missed them, and this is the part worth keeping: they are written as MULTI-LINE `parseInt(` calls** —

```
const LOOP_SIGNATURE_THRESHOLD = parseInt(
  process.env.BREAKER_LOOP_SIGNATURE_THRESHOLD ?? "8",
  10,
);
```

— and my census pattern was `parseInt\(\s*process\.env`, which requires them on **one line**. 🔴 **This is the SAME failure I wrote up two hours earlier in §5.9** (a single-line regex blind to `test-record-pass.ts`'s multi-line dynamic import), **committed by me, in the same session, on the same shape, after writing the pattern about it.** *Reading the rule is not following the rule* — and the instrument that caught it was a person reading the file, not a better pattern.

**And the consequence is the most serious latent finding of the night:**

| Site | Comparison | If `NaN` |
|---|---|---|
| `detectors.ts:44` → `:115` | `if (next >= threshold)` | **false forever → the LOOP-SIGNATURE runaway detector never trips** |
| `detectors.ts:58` → `:132` | `if (windowAttempts.length >= ceiling)` | **false forever → the VELOCITY runaway detector never trips** |

⇒ **A garbage value in either variable silently disables the runaway-cost brake** — no error, no log, no alert. **And it composes with §5.1:** the Haiku path is already uncounted by those same detectors, and the serial-drain rate limit is the thing v0.3 concurrency removes. **Three independent ways to weaken the same brake, none of which announces itself.**

⚠️ **Also from that scout, and unverified by me:** `safety/liveness.ts:96` leaves its threshold parameter optional, where the sibling `safety/polling-dead.ts:93` made the identical parameter **required** — after discovering that deleting the call-site argument compiled clean and left every test green. The `/health` path is incidentally safe because `health.ts:73` re-requires it; a *direct* call to `getPollLiveness` is not.

⚠️ **Severity, stated honestly: NOTHING IS BROKEN TODAY.** Every one of these defaults to a valid value, and the vars actually set in Railway are valid. **This is a latent class: one typo in one Railway variable silently disables a guard, with no error, no log and no alert.**

🔑 **And the detail that makes it worth Justin's attention rather than a tracker row: `safety/query-watchdog.ts:108–118` DOCUMENTS THIS EXACT HAZARD IN PROSE** — *"This codebase's dominant env idiom is `parseInt(process.env.X ?? "…", 10)`, which yields NaN on garbage — so the next caller to copy that idiom would hand us NaN and get a silent total outage."* **The codebase diagnosed the class, wrote it down twice, fixed it in one file, and the idiom persists in eleven others.**

## 9.6 🔴 WHAT THE ATLAS FOUND ON ITS WAY PAST — the audit dividend, and it is the real yield of the night

**The Atlas was commissioned as a map. Writing it forced six agents to read all 45 runtime files against their own documentation, and that is what produced the findings below.** None of it was hunted for. **Every one is PRE-EXISTING; nobody changed a line of `src/` tonight.**

**Ranked by what I would want Justin to look at first:**

0. 🔴🔴 **THE SHARPEST CODE FINDING OF THE NIGHT, VERIFIED BY ME AT SOURCE: `updateEmailmgrItem` can silently destroy a draft.** `src/db/items.ts:202–206` reads the item's current metadata and **destructures only `data` — it never checks `error`.** On a transient Supabase read failure `current` is `null`, so `:209`'s `current?.metadata ?? {}` merges the incoming metadata into an **empty object**, and the update at `:217` then writes that as the *whole* metadata — **destroying `draft`, `lifecycle` and `send_error`.** The update itself succeeds, so **nothing throws, nothing alerts, and the operator sees a normal result.**
   - **It is the outlier in its own file.** Every other read in `items.ts` destructures `error` and throws — `:85`, `:224`, `:244`, `:261`, `:295`. This one call does not.
   - ⚠️ **And the file's own comments at `:103` and `:160` describe exactly this clobbering as the thing the always-merge rule was built to prevent.** SPRINT Step 36 is the step that built it.
   - **Reachability:** needs a transient DB read error at that moment, so it is rare — but it sits on the **approval path**, and what it destroys is the draft awaiting send. **Pre-existing and shipped on `main`; nothing tonight caused it.**
   - *(Recommend: its own small DC+QC lane before the seven-item bundle deploys. **Explicitly do NOT fold it into another diff** — two lines with a silent-data-loss failure mode deserve a real review, not a rider. I did not touch it.)*
1. 🔴 **The approval/send path has no reachable emergency escalation — VERIFIED BY ME AT SOURCE.** `src/approval/flow.ts` contains **exactly one** `notifyEmergency` call (`:625`), and it sits **inside a block the file's own comment declares dead** (`:614–617`: *"as placed, the assertion is dead — the two top-of-function guards already early-return on exactly these conditions"*). ⇒ Every real failure on that path tops out at `notifyDev` (`#dev-alerts`, low urgency) — **including `DOUBLE-SEND RISK` (`:724–736`), the case where the email HAS GONE OUT to a real recipient but the status flip failed, so a re-approve sends a second copy.** ⚠️ **Accuracy note: the send path as a whole is not unguarded** — `email/client.ts:1280` has a reachable `notifyEmergency` (the Step-86e provenance gate). **It is specifically the double-send case that has no emergency channel.** *(Recommend: escalate it, as its own reviewed change, never folded into another lane. It changes operator alerting, so it is Justin's call.)*
2. 🔴 **`security/web-guard.ts:44,46,48` — the exfil-shape length caps use bare `parseInt`.** Part of the eleven-site census in §9.5. **A garbage value silently switches the exfil checks off while SSRF and scheme checks keep passing — so the guard still looks alive in the logs.** *(Recommend: route all eleven through `parseBoundedInt` in one config-hygiene trip. Not the extraction lane — a pure refactor's whole claim is "behaviour is unchanged," and these are behavioural fixes.)*
3. ⚠️ **`identity/public-knowledge/company-context.md` declares a 7-day shelf life, is dated 2026-04-21 — about 112 days ago — and is force-loaded into EVERY query.** So on every single call Evryn is told that a document she is holding is stale, with no way to ever clear it. **An always-failed instruction trains disregard for instructions**, which is the part that matters. **Evryn's identity layer — Mira's and Justin's. Never a build-lane edit.**
4. **The operator auth gate is only *conditionally* structural.** `slack.ts:311` fails open and `config.ts:680` makes `SLACK_OPERATOR_USER_ID` optional, mitigated only by a `console.warn`. ✅ **Partly answerable already: SPRINT Step 62 records the variable as set in Railway prod, so the gate is live-closed today** — but that record is from June. *(Recommend: a one-line ops check before Gate-B. If it is ever unset, any poster in the channel can approve outbound mail.)*
5. **Two cursor-safety invariants in the ingest layer are INSTRUCTIONAL, not structural** — protected by statement ordering plus a comment reading *"do not hoist this above the pagination."* Exactly the class this refactor exists to make visible, and now visible.
6. **A stale line-reference inside the most load-bearing comment in `flow.ts`** — `:615` locates its guards at "~L491–500"; they are at 578–587. **Trivial in itself, and a perfect worked example of the decay `file:line` citations exist to catch.**
7. **A third site reasoning from the never-firing 6-month TTL — and it is in the identity layer.** `identity/activities/triage.md:37` tells Evryn work-item detail *"ages out after a while… may simply be gone."* 🔑 **It was missed by every prior audit because it contains no TTL vocabulary** — a grep of `identity/` for `TTL` / `cleanup_emailmgr_items` / `6-month` returns nothing. **The audits found the sites that used the words; only reading found the one that paraphrased.**
8. **An Apple-Mail-shaped forward parses as a forward but extracts a null sender** (mechanism verified by that scout, real-world frequency unknown), and **`client.ts:172` is dead code shadowing a live function with different case-handling.**

**What NONE of it has:** the Atlas was written entirely by reading. **No test was run, no mutation executed, no probe fired against any of these** — every section says so in its own §6. **So each item above is a well-argued claim, not a verified defect**, except the two I re-derived at source myself (items 1 and 2).

---

# §8 — THE MORNING REPORT, DRAFTED AS IT HAPPENS

> **Why this is here and not composed at the end:** Justin asked for a re-spinnable instance. A report composed only at 6am dies with a compaction at 5am. **Everything below is written the moment it becomes true, so a fresh AC0 can deliver it verbatim.** Lane outcomes fill in as they land; everything else is already final.

## What Justin asked for, exactly

> *"What happened, and why should I trust it."* — not a list of commits. For each thing that moved: **what it was, what verified it, who checked whom, and what could NOT be verified.** Then one ballot, recommend-first, each item with the context a cold reader needs.

## 8.1 Load discipline — the one-line attestation he needs

**It held, all the way down, with zero deviations at my layer.** I personally verified all three lanes' trip-1 receipts against the lists I gave them. **All three reconciled *exactly* on call count** — QC 126 declared / 126 observed, DC 44/44, ACa2 47/47 — every file read to its confirmed bottom canary (or true final line, for the `tests/` files that carry none by design), every partial read disclosed rather than absorbed into a silent retry, **no compaction reported by any of them**, and no `#cascade-override` used anywhere.

**Three things came back up at me, and two were my errors** — worth telling him because it is evidence the checking runs in both directions:
1. My list's section header said *"29 files"* over a list of **27**. Arithmetic slip; 27 is correct and nothing was missed.
2. Several of my stated line counts were one short — `wc -l` under-reports a file whose last line has no trailing newline.
3. **DC refused to resolve a rule rather than guessing.** Its manual says *"if the task touches the runtime, load the identity files even if the brief omits them"* — but that rule needs the task, and trip 1 withholds the task by design. It reported the conflict instead of picking. **That is the loading discipline working exactly as intended**, and I answered it in trip 2.

**What I could NOT do:** I could not observe any lane's raw call-stream directly (they run in background), so the counts above are *reconciliations of their declarations against the harness's own tool-use totals*, not a per-tool audit of their transcripts. Where a lane spun children of its own, I am relying on **its** attestation for **its** children, and I will say so per-lane rather than letting my check read as covering theirs.

## 8.2 The judgment calls I made — flagged so he can overrule them

**All four are in §4 above with full reasoning.** The one that most needs his eyes: **§4.3 — I did not give the atlas lane the 45 `src/` files.** The orchestration protocol's tier rule says a sub-AC making runtime claims gets the whole runtime, no exceptions. His own §4.3 scout-team design says the opposite for this lane, *because* three agents tried the full load on 2026-08-10 and two compacted during it. **I followed his specific, dated design over the general rule** — and wrote it down rather than absorbing it.

The others: background rather than foreground (he loses visibility; he was asleep and a foreground block would have frozen me); Gate-B scoped to a plan rather than a run (the run needs a staging deploy, which is his gate); and the test runner built without CI (CI has a secrets question and recurring external effects).

## 8.3 Two questions I can now CLOSE rather than ask

**(a) His own question from 2026-08-05, asked twice and never answered: *"Tell me more about these 27 of 61 test suites crashing? Do we need to fix something?"***

**Answer: yes, something was genuinely wrong — and it was worse and differently-shaped than the number suggested.**
- **The count was wrong in both terms: it is 37 of 75, not 27 of 61.** Measured by *executing* every suite with the environment deliberately absent, rather than inferred.
- 🔴 **The diagnosis was wrong too, and this is the part that mattered.** Everyone believed the fix was to seed `EVRYN_ENV`. But `EVRYN_ENV` is only the **first of NINE** module-scope required variables in `src/config.ts` — so the existing helper *could not* make a suite runnable; it just moved the crash one line down.
- **Why nobody could see it:** a `.env` file exists on this machine, so all of these suites pass here. The failure is only visible if you deliberately run without it.
- **What it actually cost:** not 37 broken tests — **491 assertions that could not execute at all.**
- **Status: fixed, 37 → 13, pending the merge gate.** All 13 remaining are legitimate: 11 need a real database, and two make real *paid* model calls.
- ⚠️ **And the bigger finding underneath it: there is no test runner and no CI.** Those 491 assertions only run if a human types ~75 commands, which means nobody has ever run the suite as a suite. **A net nobody runs is not a net** — that is why I put a lane on it tonight.

**(b) *"Does the seven-item bundle deploy before or after the extraction?"*** — **already settled, on 2026-08-10, by him.** See §6.0. Two Gate-Bs bracketing the refactor; the staging deploy is step 3 and Gate-B #1 is step 4. **The answer is BEFORE**, and the reason is that Gate-B #1 *creates* the known-good baseline the extraction's "behavior is unchanged" claim must be checkable against. **I am reporting it as resolved, not re-asking it.**

## 8.4 What rode my pushes that is NOT mine

**ACf worked overnight in `_evryn-meta` alongside me, and my pushes carried his commits** — expected, and Justin's standing note is that AC0 handles pushes for everyone. **Named so nothing surprises him:** `ee3ab6c` (ACfw set-down 2), `c1e61f9`, and four further ACf/ACfm working-doc commits (his handoff, a lane set-down, and two DC build specs for team-runtime steps 4 and 5). **I touched none of his files.** ✅ **No `dashboard/` file was in any push range**, so the `_evryn-meta` pushes were no-op dashboard redeploys.

## 8.4a ✅ THE CENTRAL TRUST CLAIM, VERIFIED RATHER THAN ASSERTED: nothing tonight touched the runtime

**Justin's whole question is *"why should I trust it."* This is the check that answers it, and I ran it rather than assuming it:**

| What | Verified |
|---|---|
| Everything that landed on `evryn-backend` `main` tonight | **26 files under `tests/` + `CHANGELOG.md`. ZERO `src/`, ZERO `identity/`.** |
| All seven Atlas branches | **`docs/atlas/` only — zero touches to `src/`, `identity/` or `tests/` on any of them.** |
| The test-runner branch | **exactly two files** — `scripts/run-tests.ts` and `package.json`. |
| My doc-review branch in `evryn-backend` | **three docs**, nothing else. |
| Prod | **still `v0.2.9`. Nothing was deployed, and no migration exists anywhere.** |

⇒ **Every finding in this report is ROUTED, not fixed.** The defects named below are all pre-existing and all still in the code. **That is deliberate: they are `src/` changes, they need DC+QC, and folding any of them into tonight's diffs would have put an unreviewed rider into work that had just cleared review.**

## 8.4b ⚠️ SOMETHING OF ACf's IS AT RISK, AND IT IS NOT MINE TO FIX

I ran the all-branches batten-down enumeration (the one that catches what `origin/main..main` cannot see) and **pushed every branch of mine — the whole Atlas now exists off-machine, where before tonight it existed only on this laptop.**

**The same sweep found work of ACf's that is machine-only:**
- `evryn-team-runtime`: branches **`acfm/step4-membership-verbs`** and **`acfw/in-url-wall`** — **NEVER PUSHED**, plus `main` **3 commits ahead of origin**.
- `evryn-team-workspace`: `main` **3 commits ahead of origin** (three ACfm brief commits).

🔴 **I deliberately did NOT push any of it.** My push authorization names `_evryn-meta` and `evryn-backend`; `evryn-team-runtime` and `evryn-team-workspace` are ACf's domain, and **he is a peer, not a report — I do not touch, task or gate his lanes.** Exchanging the *finding* is exactly what peers should do; pushing his branches for him is not.

**⇒ For Justin: this is a real if low-probability exposure — a crash tonight loses that work — and ACf may simply not have run this particular check, because `git status` and `origin/main..main` are both blind to a branch that has never been pushed even once.** One message to him closes it.

## 8.5 🔴 THE BALLOT — consolidated. Framed so each can be answered in seconds.

> **Everything here is ROUTED, not done.** Nothing tonight changed a line of `src/`. Items 1–4 are the ones I would want him to actually think about; 5–11 are near-mechanical.

### Group A — code defects found tonight, all pre-existing, all needing a lane

**1. 🔴 `updateEmailmgrItem` can silently destroy a draft** (`src/db/items.ts:202–206` — verified by me at source). An unchecked read error makes the metadata merge run against an empty object, wiping `draft`/`lifecycle`/`send_error` while the update reports success. It is the only read in that file that doesn't check its error, and the file's own comments describe exactly this as what the always-merge rule was built to prevent.
> **Recommend: (a) its own small DC+QC lane, before the seven-item bundle deploys.** *Rejected: (b) fold it into an existing lane — two lines with a silent-data-loss failure mode deserve a real review, not a rider in someone else's reviewed diff.*

**2. 🔴 Thirteen unguarded `parseInt(process.env…)` sites, two of them on the runaway-cost brake itself.** `BREAKER_LOOP_SIGNATURE_THRESHOLD` and `BREAKER_VELOCITY_CEILING_PER_HOUR` (`detectors.ts:44,:58`) — a garbage value makes their `>=` comparisons false forever and **silently disables a runaway detector.** `web-guard`'s three exfil-length caps fail the same way. `config.ts` already documents this exact class at length and has `parseBoundedInt` to fix it; that sweep only ever covered `config.ts`. **Nothing is broken today — every site defaults to a valid value.**
> **Recommend: (a) one config-hygiene lane covering all thirteen, applying `PORT`'s deliberate-exclusion reasoning per site.** *Rejected: (b) fold into the extraction — a pure refactor's whole claim is "behaviour is unchanged," and these are behavioural fixes.*

**3. `DOUBLE-SEND RISK` escalates only to `#dev-alerts`.** The one case where a real recipient has already received the email and a re-approve would send a second copy routes to the low-urgency channel, because `flow.ts`'s only `notifyEmergency` sits inside a block its own comment declares dead.
> **Recommend: (a) escalate it to `notifyEmergency`, as its own reviewed change.** It changes operator alerting, so it is genuinely your call, not mine. *Alternative: (b) leave it — `#dev-alerts` is checked daily and this is rare.*

### Group B — the one real decision of the night

**4. 🔴 Do I merge the Atlas?** All twelve sections are built, pushed, and sitting on seven branches. **I deliberately did not merge them.** The standing build-level authorization covers code you cannot meaningfully evaluate and **explicitly excludes source-of-truth docs** — and this is a new documentation layer every future agent will load and reason from, where being *wrong* is worse than being absent.
> **Recommend: (a) skim `00-INDEX` + one section, then let me merge section-by-section** (the write-safety protocol says the Atlas never lands in one shot). *Alternatives: (b) merge all seven branches now on my word; (c) hold until a reviewer pass.* ⚠️ **Whichever you pick, the Atlas was written entirely by READING — no test, mutation or probe was run against any of its claims, and each section says so in its own §6.**

### Group C — near-mechanical, answer in a word

**5. The four untracked `_gate-a-*.ts`** — `current-state` says you approved committing them via a lane; the pack-out told me not to touch them. **I touched nothing.** They are the *"one true blocker"* for Gate-B. *Rec: **(a) authorize a lane** — and warn it that `_gate-a-review.ts` and `_gate-a-times.ts` hardcode seven fixture UUIDs, so a reseed silently breaks the reviewer.*
**6. CI for the test suite** — deliberately not built; DC returned a design proposal instead. *Rec: **(a) read the proposal, then decide** — the live question is secrets, since the 13 DB/paid suites must never get credentials.*
**7. `AGENT_PATTERNS.md` contradicts itself on subagent resume** — a 2026-06-23 entry says resume doesn't exist, while the file's newest section describes two-trip loading, which runs entirely on resume. *Rec: **(a) let me correct it** — it's outside the writes you pre-approved, so I flagged rather than fixed.*
**8. `current-state.md` is stale in three named ways** (no current SHA; a false "nothing is spun / no worktrees" line; no mention of the dependency map). The pack-out said not to fix it tonight and I didn't. *Rec: **(a) fix at the next `#lock`.***
**9. The "sole spend door" claim is asserted as fact in at least four more places** (`breaker.ts:4–5`, `detectors.ts:3–7`, `llm-usage.ts:7`, `ARCH:1162`). ARCHITECTURE's `:1171` is now diagnosed on the review branch. *Rec: **(a) correct all of them in one pass**, in whichever lane next touches those files.*
**10. `identity/public-knowledge/company-context.md` declares a 7-day shelf life and is 112 days old** — force-loaded into every query, so Evryn is told on every call that a document she holds is stale, with no way to clear it. **Evryn's identity layer — Mira's and yours.** *Rec: **(a) Mira refreshes it; if a 7-day cadence isn't sustainable, replace the mechanism with one that can actually be satisfied.***
### Group D — Gate-B, from the reviewer who just wrote its plan (full versions in `docs/gate-b-plan.md` §8)

**12. Commission the harness-promotion lane now?** It is the *"one true blocker"* for Gate-B, it is `tests/`-only, and **it does not depend on the staging deploy** — so it can run before you decide anything else. *Rec: **(a) now.*** ⚠️ **Blocked behind item 5** (the `_gate-a-*` disposition conflict), which is why that one matters more than it looks.
**13. How should the promoted harness find its fixture IDs?** *Rec: **(a) consumers look up by `systemtest@evryn.ai`**, not a seeder-emitted ID file — because a stale ID file returns zero rows and then **prints a clean report**, which is this week's exact failure shape.* *(b) emit a file.*
**14. Promote the harness narrowly, or rebuild it as a ③-matrix verifier?** *Rec: **(a) narrowly, as a judgment harness** — `tests/test-step57-dev.ts` already asserts ③'s full disposition matrix, so rebuilding would duplicate it.* *(b) rebuild.*
**15. Which Haiku mode does the judgment half run in?** *Rec: **(a) `shadow`** — in `active` you would be grading Haiku's screen, not Evryn's judgment.* *(b) `active`.*
**16. Run the Step-99 burst with clustering ON or OFF?** *Rec: **(a) ON** — it is the structural mitigation for the false-`wedged`-then-restart trap, where a long poll trips the health threshold and the documented operator response is a restart that strands in-flight items.* *(b) off.*

### Group E — housekeeping

**11. ⚠️ ACf has work that exists only on this machine** — two never-pushed `evryn-team-runtime` branches plus `main` 3 ahead in two repos. **Not mine to push, and I didn't.** *Rec: **(a) one message to him.***

✅ **1. The four untracked `_gate-a-*.ts` files — a genuine conflict between two of his own instructions.** `current-state.md` says he approved committing them via a lane; the pack-out tells me not to commit, delete or modify them. **I honored the pack-out and touched nothing.** **Recommendation: authorize a dedicated lane** — it is the *"one true blocker"* for Gate-B per the go-live sequence, and it carries two non-obvious traps that would cost a day at the worst moment: the promoted seeder must call `assertNotProd()`, and `_gate-a-review.ts` + `_gate-a-times.ts` **hardcode seven fixture UUIDs**, so a reseed silently breaks the reviewer unless the seeder emits its IDs or the consumers look them up.

✅ **2. `AGENT_PATTERNS.md` contradicts itself on subagent resume.** Its 2026-06-23 entry states resume *"is not exposed in AC's default setup"* and that subagents are fire-and-return — while the same file's newest section describes two-trip loading, which **runs entirely on resume.** A cold reader hits the false one first. **Recommendation: let me correct it** (a) now, or (b) at the next `#lock`. It is outside the writes he pre-approved, so I flagged rather than fixed.

✅ **3. `current-state.md` is stale in three named ways** — no current SHA (it says `d95cf83`; `main` is `2a1bdab`), the "nothing is spun / no worktrees" line, and no mention of the dependency map. **The pack-out said explicitly not to fix it tonight, and I didn't.** **Recommendation: fix it at the next `#lock`** — it is the hottest shared doc in the repo and ACf was live in it all night.

✅ **4. CI for the test suite.** Deliberately not built. DC is returning a design proposal — what to run, what to exclude, and what it needs. **Recommendation: read the proposal, then decide** — the live question is secrets, because the 13 DB-touching suites must never run in CI with credentials.

✅ **5. The four stale "sole spend door" comments** (`breaker.ts:4`, `llm-usage.ts:7`, `ARCH:1162`, `ARCH:248`). ARCHITECTURE's `:1171` is now diagnosed; these four still assert the false claim. Two are `src/` comment changes, so they need DC. **Recommendation: fold into the next DC trip that touches those files rather than spending a trip on comments.**

### 8.6b ✅ LANE D — the Gate-B plan is written, AND it answered the ③ question nobody had

**`docs/gate-b-plan.md`, 307 lines, commit `71393c6` on `gateb/plan` (SHA verified, branch pushed).** It carries the run — prod-vs-staging callout, hard pre-flight gate, the reset FK note, a scenario suite with an **observable** per item, the twice-run thin-record efficacy check, the Step-99 burst, the adversarial half, a live-path gate on its own verdict — plus a readiness assessment and its own "could not verify" section.

🔴 **The valuable half is the answer to a question that had been open and unasked: does the held Gate-A harness's grading logic still hold after ③? It does NOT, fully.** The reviewer held both the harness and the post-③ runtime, which nobody had:

- **`handled_by_gatekeeper` and `not_a_candidate` — two verdicts ③ ITSELF introduced — both collapse to `"ignored"` in the grader and are indistinguishable from each other.** The grader has no branch for two of six verdicts.
- **`original_from_user_id` changed meaning at ③**, so a correct `null` now reads as a defect in the reviewer's untyped column.
- 🔴 **`_gate-a-run.ts` calls `processEmail` directly, bypassing `handleNewEmail`** — so empty-body (which is fixture a12's entire point), truncation, the internal-sender denylist and the whole poll/cursor/burst layer are **outside its reach.** It is a *triage-layer* harness, not an ingest-layer one. **Not a defect — a boundary that has to be stated, because Gate-B needs both halves and only real staging forwarding covers the second.**
- 🔴 **Two silent-failure traps, both the "works the first time, lies the second" shape:** a re-run without a reset hits the `external_id` dedup skip and **re-prints the previous run's verdicts with no warning**; and with `CLUSTERING_MODE` on — which Gate-B mandates — every fixture sits at `queued` and the table reads meaningless, because the runner has no batch-drain trigger.

✅ **And a correction to a claim I had repeated from an earlier handoff, which I then verified myself:** only `_gate-a-review.ts` hardcodes a UUID (1). **`_gate-a-times.ts` hardcodes six `external_id` STRINGS, not UUIDs** — fixture-derived, so they **survive a reseed.** Its staleness risk is renames, not reseeds. **That materially reduces the promotion work**, and it is the third inherited claim tonight that turned out wrong on inspection.

### 8.6c ⚠️ LANE B — the test runner is NO-GO, and the blocker is this night's own lesson turned on the instrument

**A fresh full-load QC returned NO-GO on one blocker: `npm test` can print `1 failed` and `=== ALL GREEN ===` and exit 0, simultaneously.** A suite with genuine failing assertions gets relabelled NEEDS-LIVE, and NEEDS-LIVE is not in the fail list — because that branch is evaluated *before* `failed > 0`.

🔑 **The mechanism is the most elegant thing found tonight: the runner's own defence manufactures the string that defeats its other defence.** The trigger was **its own seeded dummy, `https://dummy-project.supabase.invalid`**, printed inside an ordinary failure detail — and `.invalid` is in its network-signature list. **Defence 3 (environment sanitisation) generates the exact text that trips defence 2 (the relabel heuristic).** Measured reachability: **17 suites import `config`, and 15 of those also print a JSON failure detail** — including `test-query-timeout.ts`, the very suite DC's earlier fix was written to protect.

**Why the earlier fix missed it, precisely:** both guards ask *"is this a real network failure or prose that looks like one?"* **Neither asks the question the gate depends on — "did this suite report any failures?"** The false-positive half was closed; the false-negative half was not.

✅ **She also established the credential gate holds BY EXECUTION** — 11/11 checks, `CLAUDE_CODE_OAUTH_TOKEN` stripped, and a real `.env` placed in the tree still could not reach a child. **No spend is possible in default mode.**

**I did NOT merge it.** A reviewer NO-GO is a hard blocker. **I resumed the original DC on a one-line fix trip** (QC's own validated line, scope strictly limited to the blocker) — the protocol's normal build→review→fix loop. Her five non-blockers are on the ballot, not folded in.

### 8.6 Lane outcomes (appends as they land)

**C — the Atlas, first section landed 21:20 PT.** `docs/atlas/02-email-ingest.md` (216 lines), commit **`f90f109`** on `aca/atlas-s1`. **I verified the SHA exists rather than believing the report** — a subagent reported a nonexistent commit in this repo earlier today. The section covers the Gmail wire layer (`client.ts`, `detect.ts`, `loopback.ts`), cites `file:line` on every claim, and — the part worth telling Justin — **splits its ten invariants into STRUCTURAL vs INSTRUCTIONAL**, which is the distinction this whole refactor exists to make visible. **Two of the cursor-safety invariants turn out to be INSTRUCTIONAL only** (protected by statement ordering plus a comment saying *"do not hoist this above the pagination"*), which is exactly the class that has bitten us.

**Three things that scout raised, and how I handled them:**
1. *"Does a test really pin the `labelId: "INBOX"` guard, or is that comment lying?"* — **I checked at source: it is real, and stronger than the comment claims.** `tests/test-history-pagination.ts` asserts it on the first page **and** on every page. ⇒ genuinely structural. **Caveat I gave back: a test existing is not a test that runs** — 37 of 75 suites boot-failed until tonight's fix, and I have not verified this one executes.
2. *"The dependency map looks stale — regenerate it."* — **It is not stale, and the confusion is worth recording.** Its stamp names the HEAD at *generation* time, which by construction can never be the commit that lands the output. **Every future reader will make the same inference**, so I asked the lane to write that into the Atlas.
3. *"Does any Atlas section own `tests/`?"* — **the sharpest question of the night, and I routed it to the lane as its call.** If nothing owns the test tree, the Atlas will confidently record instructional properties as structural — the exact failure it exists to prevent, in its most convincing costume. **Live evidence in both directions tonight:** the `labelId` claim checked out true; the cache byte-identity guard in this same repo turned out **unable to fail at all** while its name told every reader it was covered.

⚠️ **A harness quirk worth Justin knowing, because it can silently strand work:** that scout's report **surfaced to me rather than to its parent lane.** Nested-agent reports can land at the top instead of one level down, which means a lane can be missing a fact its own child produced. **I relayed it down explicitly rather than assuming it arrived**, and told the lane to check its scouts' worktree git state rather than trusting silence.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
