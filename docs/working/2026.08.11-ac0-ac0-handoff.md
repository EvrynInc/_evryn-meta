# Handoff — AC0-34 → AC0-35 — 2026-08-11 evening

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file: THIS IS YOUR ENTIRE BRIEFING.** It is written so a completely fresh AC0 can be spun from it and start work without asking anyone anything. Justin re-spun me deliberately at a clean checkpoint so you would have a full context window for an overnight run. **Nothing was lost in the handover; it is all here.**
>
> **It SUPERSEDES `2026.08.10-ac0-ac0-overnight-handoff.md`** (AC0-33's), which is now history. You do not need to read that file. The three or four things in it that are still live have been carried forward here in full.

---

# §0 — YOUR FIRST FIVE MINUTES

**You are AC0** — Justin's conductor lane for the Evryn **product** build (`evryn-backend`, "Gatekeeper's Inbox," live on Railway at `v0.2.9`). I was AC0-34; you are **AC0-35**.

## 0.1 🔴 THE FOUR RULES JUSTIN SET FOR TONIGHT. Read these before anything else.

1. **🔴 IF YOU COMPACT, STOP.** Not "slow down" — **stop doing work.** You may let already-running subagents finish and you may record what they return, but **you do no further thinking, deciding, or gating yourself**, because post-compaction your judgment is compromised and you cannot detect that from the inside. Justin's instruction verbatim: *"if he compacts, he needs to just pause… his judgment will be compromised, post-compaction."* Ping `Need you: compacted, paused` and wait.
2. **🔴 KEEP THIS FILE CURRENT AS YOU GO — not at checkpoints.** This is the direct consequence of rule 1. Justin: *"if I come to him in the morning and find him paused, I can spin him straight from his fully-up-to-date brief."* **Every time anything moves, write it here.** The test at every append: *if I died right now, could a fresh AC0 pick up from this file alone?*
3. **🔴 ARM A 20-MINUTE CANARY.** Justin asked for it explicitly, because it is easy for something to stall silently overnight. A persistent `Monitor` tick every ~20 min → a one-line `No action needed - heartbeat:` ping. **Before you say "healthy," verify the active lane ADVANCED** — check its **worktree git state** (new commits, dirty files), never the transcript file size (a completed agent's file reads 0 bytes mid-run and will lie to you). No advancement → say *"possibly stuck — may need you,"* never "all healthy." **`Monitor` is a deferred tool: fetch it with `ToolSearch` (`select:Monitor,TaskStop,SendMessage,TodoWrite`).** Kill the canary with `TaskStop` as the last action of the night, together with your `Done for the day` ping, so post-Done silence reads as intended.
4. **Ping `#team-alerts` on every response, prefix `AC0:`.** Helper: `node <scratchpad>/ping.mjs "AC0: …"` — it reads `SLACK_TEAM_WEBHOOK_URL` **by name** from `evryn-team-workspace/.env` and never echoes it. If your scratchpad differs, re-create it (10 lines: read the `.env` line, POST `{text}`, print only the status code). **Use Node `fetch`, never bash+curl or PowerShell** — both mangle non-ASCII on Windows.

## 0.2 Your load — in this order

> ⚠️ **EVERY LINE COUNT IN THIS FILE IS A SNAPSHOT — re-run `wc -l` at spin time.** Two of them moved *during my own session*: `CLAUDE.md` 589→605 and the orchestration protocol 828→842, because the merge I landed added to both. **A stated span that is wrong makes a receipts check meaningless**, which is the one thing receipts exist to prevent.

1. **`_evryn-meta/CLAUDE.md`** (605 lines) — auto-loads, but read it yourself and confirm the bottom canary.
2. **THIS FILE, in full.**
3. **`_evryn-meta/docs/current-state.md`** (152) — ⚠️ known-stale in named ways; see §5.4.
4. **`_evryn-meta/docs/hub/roadmap.md`** (196) — company truth.
5. **`_evryn-meta/docs/protocols/load-cascades/conductor-cascade.md`** (95) — read *"The Conductor's Highest Discipline"* and the fragment-feels-like-authority box **twice**.
6. 🔴 **`_evryn-meta/docs/protocols/ac-orchestration-protocol.md`** (842, pages in ~3 reads) — **NON-OPTIONAL.** Every spin is multi-trip; briefing from memory of an older shape produces a wrong brief.
7. **`_evryn-meta/docs/protocols/ac-writing-protocol.md`** (100) — before you write to any doc.
8. 🔴 **`evryn-backend/docs/ARCHITECTURE.md` (1,285) and `evryn-backend/docs/BUILD-EVRYN-MVP.md` (651), IN FULL.** **I skipped these and Justin caught it. Do not repeat that.** They are on the conductor cascade's list (step 1 is the full cascade *minus the runtime*), and without them you are, in his words, *"really flying blind."* They give you the **intended** shape — the four-layer dossier, the bulkhead inner-doors, M1's design including the not-yet-built breaker scoping, the Publisher pattern. **Holding these yourself is now load-bearing to the whole scout design in §1** — it is what lets the scout stop carrying them.
9. **`evryn-backend/docs/atlas/00-INDEX.md`** (103) — read this one now. **Then read `11-verification-and-tooling.md` (128) too** — it is the "how do you establish that anything here is actually true" section, and it is what makes you able to gate Lanes C and D. **The other ten sections you pull at their moment**, which is the Atlas's own designed use; Lane C's brief needs all twelve, but *you* do not.
10. **`evryn-backend/docs/SPRINT-V0.2-HARDENING.md`** (475) — ⚠️ **grep `^103\.`, NOT `"Step 103"`.** The doc writes steps as numbered list items while prose calls them "Step N," so the obvious grep returns nothing and a scout has already concluded a step did not exist.

**Do NOT load `evryn-backend/src/` yourself.** That is the scout's job, and the whole point of the conductor cascade.

## 0.3 ✅ WHAT JUSTIN AUTHORIZED TONIGHT — so you neither re-ask nor over-reach

**He answered a full ballot before stepping away. These are cleared, and you do not need to ask again:**
- **Spin all five lanes** (§2) — A, B, C, D, E, on the sequencing in §2.0.
- **The three-trip loading design** (§1.2) and **the overload protocol** (§1.3) — settled, his design, do not re-derive.
- **The CLAUDE.md split + the orchestration-protocol split** (§2.1) — including the router's one hard rule in his own words.
- **Lane B does the reformat too**, not just the structural fix.
- **The control-set decision** (§2.6) — hold 103/104/105, do not tell Mega-QC.

**🔴 What he did NOT authorize, and what therefore still gates:** any **push** · any **deploy or migration** · any **commit of a source-of-truth doc to `main`** (lane output waits in SCM for his vet) · any **identity-file change** · and **the exact wording of the new `<output>` block** — see the flag in §1.2(c).

**Your first action after loading: Lane E** (§2.5). It is small, it is the precondition for Lane D, and it gets one thing genuinely moving before the bigger lanes spin.

---

# §1 🔴 THE MAIN EVENT — THE LOADING ARCHITECTURE. This is settled; do not re-derive it.

**Justin's framing, and it is the priority that gates everything else:** *"before any of that, we have to work out #1. That might make everything work better, no?"*

## 1.1 Why this exists — the fact that forced it

**A full-runtime load no longer fits in one agent.** Three independent confirmations:

1. **2026-08-10:** three agents attempted it; **two compacted during the load.**
2. **The Code Atlas says so about itself** (`00-INDEX.md`): every one of its seven authors needed a scoped load *"because a full-runtime load no longer fits one agent."*
3. **2026-08-11 (mine):** a full-load scout compacted at ~85% and never opened `classify.ts`, `process.ts`, `slack.ts` **or any of the 11 identity files.** Discarded per the junk rule.

**And it measured WHY, which nobody had.** The binding constraint is not the code — it is **our own prose docs**. Measured off the Read tool's own token counts: `CLAUDE.md` ≈ **144 tokens/line**, `ac-orchestration-protocol.md` ≈ **166**, `SPRINT-V0.2-HARDENING.md` ≈ **163**, `evryn-backend/CHANGELOG.md` ≈ **180**. TypeScript runs ~30–45. **The doc half of that load was ~800K tokens; the runtime it existed to read was ~700K.** More than half the budget went to documents whose entire purpose is to be in the **conductor's** head.

## 1.2 🔴 THE THREE-TRIP SPIN — Justin's design, and it is now the shape for any runtime-reading agent

**The problem it solves, in his words:** *"I don't trust subagents. I've given them very reasonable instructions before, pleaded with them to believe me that a file has to be loaded, and they just… don't feel like it — so they don't."*

| Trip | What it carries | The agent's ENTIRE output | Who gates |
|---|---|---|---|
| **1 — DERIVE** | identity + **the Atlas** + `dependency-map.md` + **the task, with a loud DO-NOT-START** | a **proposed load list** + receipts for what it read | **AC0 approves or names what's missing** |
| **2 — LOAD** | the approved list, verbatim | receipts only | **AC0 verifies receipts** |
| **3 — WORK** | `<task>` unlocked | the work | — |

### The four things that make this work, each answering a real objection Justin raised

**(a) "They can't know what to load without the task."** Correct — so **the task goes in trip 1**, with an explicit prohibition on starting. This does surrender the original two-trip guarantee (that relevance is not computable during a load). **What replaces it is that you review the derived list before approving it** — which is a *stronger* control, because you see the reasoning rather than only the receipts.

> 🔑 **Make that review real rather than a rubber stamp: write down YOUR OWN expected list BEFORE you read theirs, then diff.** If they propose 18 files and you expected 30, that is a conversation. If they propose something you had not thought of, that is the dividend. **You can only do this because you hold ARCHITECTURE and BUILD** — which is exactly why §0.2 item 8 is not optional.

**(b) 🔴 "They'll burn a lot of tokens deciding what to load, which defeats the purpose."** **The fix is to stop asking them to DECIDE and make them DERIVE.** The Atlas was built for precisely this: its index carries a *"you are about to change X → start at section N"* routing table, and every section's §0 names its files with line ranges. So trip 1 is a **lookup, not an exploration**:

> **Trip 1 forbids all exploration.** The agent may read **only** the Atlas and the dependency map. **No grep, no glob, no sampling, no opening any file outside those.** It walks: index → routing table → the sections that match → each section's §0 file list → that IS the proposal.

⚠️ **And a useful side effect: if the derivation turns out expensive anyway, that is a finding about the Atlas index, not about the agent.** It means the index is not doing its one job. Report it.

**(c) "They might race forward anyway, and not listen to us."** Three guards, in increasing strength:
1. An `<output>` block stating the trip's entire deliverable, and that **anything else means the trip is discarded**.
   > 🔴 **STOP — THIS ONE NEEDS JUSTIN BEFORE YOU SPIN ANYTHING.** `<output>` is a **new block in the verbatim brief skeleton**, and the protocol's HARD RULE says AC *"may not change one word without Justin's explicit prior OK"* — which covers adding a block, not just editing one. **He approved the *idea* of an `<output>` contract; he has not seen the words.** ⇒ **Draft the exact wording, show him the before → after, name it explicitly as a change to the protocol, and get his yes on those precise words.** If he is asleep and a lane is otherwise ready, spin it with the *existing* skeleton — the two-trip shape works and has all along — and hold the `<output>` change for the morning. **Do not invent it and proceed.**
2. Justin's own line, which is worth keeping verbatim: *"if you don't load properly, we're just going to resume you again and again until you do."*
3. 🔑 **The structural one, and the only one that cannot be ignored: in trip 1 they do not HAVE the runtime.** Racing ahead produces visibly empty work. **Racing is self-defeating rather than merely forbidden** — the same "remove the ability to make the wrong choice" move that two-trip loading itself is built on.

**(d) 🔑 "Isn't a third trip expensive?" — NO, AND THIS IS THE PART THAT DISSOLVES THE WORRY.** Resume replays the whole transcript, so a trip costs whatever is already loaded. In the three-trip shape the extra trip is inserted **before** the runtime is in context:

| | Two-trip | Three-trip |
|---|---|---|
| Light replay (~190K) | — | **1** |
| **Heavy replay (~890K)** | **1** | **1** |

**Same single expensive replay. The extra trip costs ~2% of the run.** Get the load right in the trip that loads it, because *every* trip after the runtime is in context is expensive — which is exactly why the derive-and-approve step belongs before it.

## 1.3 The overload protocol — four named strategies, one forbidden move

**Justin:** *"we need to give the scouts some instruction on how to manage the scenarios where it will overload itself. We just have to be equally clear that they have to tell us precisely how they solved the overload problem — otherwise we know how they're going to solve it: by not reading things they need to. That's always their default."*

**The default is: read every file on the approved list, in full.** An agent may only depart from that via one of these four, and **must name which one it used, per file:**

1. **Fan out sub-readers** — one per Atlas section, each holding one subsystem in full, parent reconciles. *This is exactly how the Atlas itself was built, so we know it works at this scale.* **Keep them alive as a bench** so a cross-seam question can go to both sides and be reconciled.
2. **Run it instead of reading it** — execute the suite, run `npm run depmap`, fire a probe. (Justin's idea, and a good one: sometimes the way to understand something is to run it.)
3. **Read a bounded slice — but only one the Atlas itself justifies.** Atlas §01 carries a table saying "changing prompt assembly? read `:95–352` and `:407–709`." **A slice with an Atlas citation behind it is a decision; a slice without one is a guess.**
4. **Hard stop** — *"I cannot do this in one head; here is the split I need."*

🚫 **THE ONE FORBIDDEN MOVE, named as forbidden so it has nowhere to hide: *"I judged this file unlikely to matter."*** It is their default, it is never legitimate, and the per-file disclosure exists to make it impossible to do quietly.

⚠️ **Honest limitation, state it when you use a fan-out:** a fan-out is **weaker at the seams** than one agent holding everything, and the conductor cascade warns that the seams are where the behavior lives. The Atlas says the same about itself — cross-subsystem invariants are its most at-risk class. **We accept it because the alternative is not a stronger agent; it is an agent that compacts.**

## 1.4 What the scout's load actually is now

- Its own **scout identity** (small — NOT the 605-line AC manual)
- **The Atlas** — 12 files, 3,108 lines
- **`evryn-backend/docs/dependency-map.md`** — 691 lines, machine-generated, cheap, and the seam layer. ⚠️ **Its canary is on line 692, not 691** — `wc -l` under-counts a file with no trailing newline. Two agents have already reported "(full)" without seeing it.
- **The runtime, both halves** — 45 `src/**/*.ts` + 11 `identity/**/*.md`, enumerated LIVE
- The overload protocol above

**Explicitly NOT: the Hub, technical-vision, ARCHITECTURE, BUILD, the sprint, or either changelog.** ~6,000 lines of dense prose, ~800K tokens — **you hold all of it.**

---

# §2 🔴 THE FIVE LANES, STAGED. Sequencing first — there is a real collision.

## 2.0 ⚠️ THE COLLISION, AND HOW TO AVOID IT

**Lane A rewrites the very documents every other lane's subagents load.** `_evryn-meta/CLAUDE.md` auto-injects into *every* subagent; `ac-orchestration-protocol.md` is on every AC-subagent's mandatory list.

**⇒ The rule: spin lanes B–E FIRST, against the CURRENT architecture (which works — it has all along). Lane A works on its own branch and lands LAST.** Nothing auto-loads from a branch, and an already-spun agent loaded at spin time, so it cannot be changed underneath. **Do not let Lane A commit to `main` while anything else is running.**

**Sequence:** **E (Step 106) → then B, C, D and A in parallel → A merges last.**

## 2.1 LANE A — the loading-architecture rewrite. **Justin's #1 priority.**

**Two deliverables, both source-of-truth docs, both left in SCM for Justin's vet — never committed to `main` by you.**

**(i) Split `_evryn-meta/CLAUDE.md` into agent identities.** AC, DC and QC each get an identity file; **meta's `CLAUDE.md` becomes a tiny router** that says only "load up as one of these agents." AC becomes one of many rather than the special case.
- **Why:** measured today — meta's `CLAUDE.md` is **~85,000 tokens**, and **every** subagent pays it unconditionally: DC, QC, scouts, even a generic agent spun to check whether a file exists. For DC and QC it is not merely waste, it is the *wrong manual* — the orchestration protocol currently spends a whole paragraph teaching them to disregard what the harness force-fed them. **Split it and that failure mode stops existing rather than being managed.**
- 🔴 **The router's one hard rule — Justin's exact words, use them:** *"if your agent identity doesn't load, STOP — you're not authorized to do **ANYTHING** — just report the failure."* This is better than the multi-rule safety floor I originally proposed, and it supersedes it: an agent that cannot act without its identity cannot act *dangerously* without it either.
- ⚠️ **The one new failure mode this creates, and it must land in the same pass:** every sub-AC must now name its identity file **explicitly** in `<mandatory_load>`, or it runs manual-less and nobody notices. Write that into the protocol in the same change.

**(ii) Turn `ac-orchestration-protocol.md` into an index with per-scenario sub-docs.**
- **Justin:** *"most of the time they're spinning a DC and a QC, but they're reading about spinning ACs and Team Agents — it's a bunch of bloat."* He is right: **842 lines, ~137,000 tokens**, and every AC subagent is required to load all of it. **Combined with the AC manual, a sub-AC pays ~220K of pure operating manual before it reads one line of the thing it is meant to work on.**
- **Proposed shape (the lane owns the final call):** the protocol becomes the index + the universal core (the brief skeleton, multi-trip loading, receipts, model pinning, the HARD RULE) · `spinning-dc-qc.md` · `spinning-an-ac.md` · `spinning-a-team-agent.md` · and `orchestration-rationale.md` for the incident record.
- **On how much rationale to cut — Justin asked, and my answer is: keep every RULE, move every STORY.** The protocol's own claim that "every clause is load-bearing because a paraphrase already failed" is true of the *rules*. The **incident narratives** — the 2026-06-17 lobotomy, the six-shapes-in-a-day list, the token-measurement tables — are teaching material for a conductor, read once, and are pure weight in a DC spin. That split is where the cut is, and it is a large one.

**(iii) Amend `conductor-cascade.md` — Justin said yes to this explicitly.** Its scout step currently prescribes *"the entire runtime, both halves, in one subagent"*, which **cannot be executed** (§1.1). Replace it with the three-trip shape and the overload protocol. **Without this, the next fresh AC0 re-pays exactly the cost I paid today.**

**(iv) 🔴 FIX `AGENT_PATTERNS.md:21` — IT IS FLATLY WRONG AND IT IS NOW DANGEROUS.** *(I verified this at source tonight; it has been flagged twice before and never fixed.)* That entry states **`SendMessage` is not exposed in AC's default setup**, that our subagents are *"fire-and-return,"* and that resume *"most likely still does not let AC reach back into a finished DC."* **All three are false** — resume is proven at ~22 days same-session with full context and zero re-reads, and **the entire multi-trip loading model runs on it.** The same file's line ~688 describes two-trip loading, which is *only* possible via resume. **A cold reader hits line 21 first and concludes the protocol cannot be run.** ⚠️ **This got MORE urgent tonight, not less:** three-trip loading depends on resume even harder than two-trip did.

## 2.2 LANE B — the sprint + `current-state` restructure. **Justin: do the reformat too.**

**Scope: BOTH `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` and `_evryn-meta/docs/current-state.md`, plus the reformat to the team-runtime tracker's shape.**

**Justin asked why not fold the reformat in, and I agree — here is the reasoning, so the lane does not re-litigate it.** The original deferral (08-06 handoff) was: *"doing it while the extraction is moving code would make every Step's file references churn simultaneously."* **The extraction has not happened.** So doing it now is *before* that churn, not during it — and a well-formed tracker is what the extraction lane will then work against. The other half of his reason (*"I wanted the Atlas first so people can think straight"*) **is satisfied — the Atlas exists and is merged.**

**Three concrete findings to hand the lane as its starting evidence — all verified at source by me:**

1. 🔑 **The sprint violates its own written rule, and the rule predicted the violation.** `SPRINT-V0.2-HARDENING.md:49` reads: *"**There is no 'Current State' section in this doc — by design.** Live cross-project state lives in `_evryn-meta/docs/current-state.md`… **Duplicating it in a header would just create the drift this doc exists to kill.**"* And lines **7–17 are exactly that** — **two stacked deploy-status blocks**, the lower of which sat **four releases stale** and was, in the doc's own words, the first thing a cold reader hit. *(Justin remembered this rule existed. It does.)*
2. **Steps 103–106 are bolted onto the end** in two new sections *after* the phase structure. The doc's own model: **numbers are permanent handles, positions reflect timeline and phase.** Keep the numbers, move the positions into their phases.
3. **`current-state.md` has become append-only in practice.** Written target ~50 lines; it is at **152**. Its header carries **eleven bracketed dated entries from six different instances stacked into one paragraph**, while the file's own rule says *"NOT a log — each update **replaces** the previous state."* **Same disease as the sprint: a doc that forbids logging, being used as a log.** The lane should fix the instance *and* say why it keeps happening.

⚠️ **One conflict for the lane to resolve, not to steamroll:** the team-runtime tracker's rework involved a **one-time renumber**, and our sprint's model says **never renumber** (numbers are referenced by other docs, briefs, and agents). Adopt the *format*, not that particular move — or if the lane thinks a renumber is right, it must grep every repo for the old numbers in the same pass.

## 2.3 LANE C — the Atlas cohesion pass

**Justin read all twelve sections: *"definitely authored by different instances."* Three problems, and his framing on the first is the one that matters.**

- **(i) Quality is uneven and the fix is levelling UP, never flattening.** *"A few of them have really good features the others don't; a dedicated pass will see that, and make them all as strong as can be."* **I confirmed this by comparison:** `07-safety` (a scout's) tags **every claim with its provenance** — "Map" where it came from the dependency map, "Read" where the author read the line — and it **ran a real currency check**, proving the map's edges describe the exact code it read. `01-agent-core` and `10-identity-runtime` (the conductor's) have neither feature and are excellent otherwise. **Propagate the best inventions across all twelve.**
- **(ii) British/American spelling is mixed — measured: 11 of 12 sections, 64 occurrences** (`behaviour` ×~30, plus `initialised`, `recognised`, `normalisation`, `defence`, `optimise`). **Justin's own fix landed and covered exactly one section** (`01-agent-core`, commit `55026ac`, merged). The other eleven are untouched.
- **(iii) The Atlas is MERGED to `main`**, so this works in the canonical tree.

🔴 **Standing constraint: every claim keeps its `file:line`, and a cohesion pass must NEVER invent a claim it did not verify.** Polishing prose is exactly where an unverified assertion slips in.

**Sizing:** needs all twelve sections (~3,100 lines) held at once *plus* enough runtime to confirm a levelled-up claim is still **true**. That is a full lane, not an inline job.

⚠️ **A related gap, and it is the Atlas's OWN stated top priority — decide whether it belongs here or in its own trip.** `00-INDEX.md` §"What is owed" item 1: **there is no `tests/` section.** **82 files, 19,503 lines** — I re-counted tonight, and the Atlas's own figure of *"81 files, ~18,578"* is already stale, which is itself a small argument for doing this — **read by nobody**. ⇒ *"Until it exists, every test-backed claim in every section is UNVERIFIED."* **My recommendation is its own trip** — reading 18,500 unread lines is a different job from polishing prose — **but it is the highest-value remaining Atlas gap and it should not be lost.**

## 2.4 LANE D — the Mega-QC break-everything sweep

**Justin: *"have a big QC methodically go through the whole system trying to break shit. We may not love what she finds, but we need to do it."***

**Shape:** a full-load QC in a **provisioned throwaway sandbox** (`git worktree add --detach <path> <sha>` — **you own provisioning it; never let her mutate a tree anyone is working in**), working down the invariants we actually rely on, **deleting or inverting each and recording whether the suite notices.**

🔑 **The targeting list already exists: the Atlas's STRUCTURAL-vs-INSTRUCTIONAL tags.** Anything tagged STRUCTURAL *on the strength of a test* is exactly what this sweep exists to test. **Expect some "STRUCTURAL" to turn out INSTRUCTIONAL — that is success, not failure.**

🔴 **Every verdict needs a BASELINE in the same run.** A mutation that silently failed to apply and a missing test runner are **indistinguishable from a real result**, except by the baseline. And **a baseline that comes back RED when it has no business being RED is a toolchain failure, not a finding** — check for a wiped `node_modules` before believing a single verdict.

## 2.5 LANE E — SPRINT Step 106 (the NUL bytes). **Do this FIRST; it is a precondition for Lane D.**

**`src/safety/halt-signal.ts` and `scripts/dependency-map.ts` each contain a literal NUL byte**, so `grep` classifies them as binary and **skips them silently**, and `git diff` renders them as *"Binary files differ."* One of them is **the circuit-breaker's halt contract**, and a change to it is **not reviewable as a diff.**

**Why it goes first:** this is not a defect for the sweep to *find* — it is a defect that would **blind the sweep**. Fix the instrument, then run it. It is small (preserve the runtime sentinel property using a code-point escape rather than a literal NUL) and it also owes a `// SEALED-EXEC:` audit comment on `scripts/dependency-map.ts:644`. Full detail: sprint Step 106.

## 2.6 🔴 THE CONTROL SET — Steps 103, 104, 105. **HOLD THEM. DO NOT TELL MEGA-QC.**

**Justin asked whether the small known-defect lanes should run before the sweep. The answer we settled on is neither of his two options:**

**Treat 103/104/105 as an undisclosed known-positive control set.** If her sweep finds them, the instrument is calibrated and every *other* finding it produces is worth more. **If it misses them, we have learned something critical about the sweep before we relied on its verdict.** That is the codebase's own hardest-won rule — *a check is only worth what it actually ran; prove it can fail before you trust that it passed* — turned on the checking apparatus itself.

**Second reason, purely practical:** if DC lanes are changing `src/` while a QC mutates a sandbox of `src/`, **her baseline moves under her** — and a moving baseline is exactly how a mutation run produces a plausible table of fictional verdicts.

**The three being held back** (all pre-existing, all in the sprint, all verified at source by AC0-33):
- **103** — `updateEmailmgrItem` (`src/db/items.ts:202–206`) can **silently destroy a pending draft**: it destructures only `data`, never `error`, so a transient read failure merges into `{}` and the update writes that as the whole metadata, wiping `draft`/`lifecycle`/`send_error`. **It is the only read in that file that does not check its error**, it is on the **approval path**, and the update reports success.
- **104** — **thirteen unguarded `parseInt(process.env…)` sites**, two of them on the runaway-cost brake itself (`detectors.ts:44` and `:58`): a garbage value makes their `>=` comparisons false forever and **silently disables a runaway detector.** Nothing is broken today; every site defaults to a valid value.
- **105** — the approval path's only `notifyEmergency` sits inside a block **its own comment declares dead**, so **`DOUBLE-SEND RISK`** — the case where a real recipient already received the email — tops out at low-urgency `#dev-alerts`.

## 2.7 📋 THE LOAD LISTS, PRE-DERIVED — line counts verified at `_evryn-meta` `33c4dee` / `evryn-backend` `6995007`

**Justin asked that these be worked out tonight so you do not spend the night deriving them.** ⚠️ **Re-verify each count with `wc -l` at spin time** — files move, and a stated span that is wrong makes the receipts check meaningless. **And remember `wc -l` under-counts a file whose last line has no trailing newline** (`dependency-map.md` reports 691; its canary is on 692) — so where a count and a canary disagree, trust the canary.

**Every list below is ADDITIVE to the target agent's own standing cascade.** Do not drop a standing file because it looks task-irrelevant — your omission is its omission, and under multi-trip loading nobody is left to catch it.

### Lane A — loading architecture (a sub-AC, docs-only, its own branch)
`_evryn-meta/CLAUDE.md` (605) · `docs/protocols/ac-orchestration-protocol.md` (842) · `ac-writing-protocol.md` (100) · `load-cascades/conductor-cascade.md` (95) · `load-cascades/product-full-cascade.md` (46) · `AGENT_PATTERNS.md` (738) · **and all three target manuals, because the split has to preserve what each actually needs:** `evryn-dev-workspace/CLAUDE.md` (312) · `evryn-quality/CLAUDE.md` (502) · `evryn-ops/CLAUDE.md` (317).
**Relevance clause to include for the three manuals** *(they are not obviously in scope and will be dropped without it)*: *"these are the manuals the router will point AT — the split is only correct if what stays in the router plus what moves into each identity together preserves everything each of these three agents relies on today."*
**No runtime. No Atlas. This lane touches no code.**

### Lane B — sprint + current-state restructure (a sub-AC, docs-only, its own branch)
`evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (475) · `SPRINT-v0.2-optionals.md` (25) · `SPRINT-V0.3-CANDIDATES.md` (104) · `_evryn-meta/docs/current-state.md` (152) · `docs/deploy-log.md` (37) · `docs/protocols/lock-protocol.md` (85) · `docs/protocols/doc-update-routing-protocol.md` (104) · `ac-writing-protocol.md` (100) · **`evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` (298) — THE FORMAT MODEL.**
**Relevance clause for the format model** *(this one is essential and non-obvious)*: *"this is the tracker Justin reworked on 2026-08-10 and called 'awesome' — it is the shape you are adopting. Read the tracker itself; do not build from anyone's one-line description of it."*
**Also give it `evryn-backend/docs/ARCHITECTURE.md` (1,285)** — Steps reference architecture claims and it must not restate one wrongly while repositioning it.
**No runtime.**

### Lane C — Atlas cohesion (a sub-AC with its own readers; needs runtime to verify a claim)
All 12 of `evryn-backend/docs/atlas/*.md` — `00-INDEX` (103) · `01-agent-core` (176) · `02-email-ingest` (216) · `03-email-processing` (294) · `04-triage-pipeline` (257) · `05-approval-and-notify` (454) · `06-persistence` (321) · `07-safety` (630) · `08-security` (168) · `09-bootstrap-and-config` (171) · `10-identity-runtime` (190) · `11-verification-and-tooling` (128) — **3,108 total** · plus `docs/dependency-map.md` (691, canary on **692**) · `ac-writing-protocol.md` (100).
**Runtime: per the §1.2 three-trip shape** — it derives which files it needs to re-verify a claim, you approve, it loads. **It should NOT pre-load all 45.**

### Lane D — Mega-QC break-everything sweep (a fresh full-load QC)
Her **full standing Tier-0 + Tier-1 cascade** (`evryn-quality/CLAUDE.md`, 502 — the easy-to-miss standing pieces are **the identity files and `operator-guide.md`**) · the runtime **both halves** (45 `src/**/*.ts` = 20,089 lines; 11 `identity/**/*.md` = 1,378) · `docs/ARCHITECTURE.md` (1,285) · `docs/operator-guide.md` (523) · `docs/dependency-map.md` (691) · **the Atlas (3,108) — it is her targeting list** · and **`tests/` — 82 files, 19,503 lines.**
🔴 **That total is well past what compacted my scout. She is the clearest case for the §1.3 fan-out**, and the Atlas partition gives her a ready-made split. **Do not thin her list to make it fit — restructure the work.**
**Relevance clause for the Atlas** *(she will not guess this)*: *"its STRUCTURAL-vs-INSTRUCTIONAL tags are your targeting list — anything tagged STRUCTURAL on the strength of a test is exactly what this sweep exists to break."*
**You provision her sandbox:** `git worktree add --detach <path> <sha>`. **Never let her mutate a tree anyone is working in.**

### Lane E — Step 106, the NUL bytes (a small DC + QC trip)
`evryn-backend/src/safety/halt-signal.ts` (40) · `scripts/dependency-map.ts` (1,017) · `src/safety/breaker.ts` (93) · `src/safety/halt.ts` (87) — **the three-file halt mechanism must be read together; `halt-signal.ts` is its contract** · sprint Step **106** (grep `^106\.`) · `evryn-backend/CHANGELOG.md` (267).
⚠️ **Read the two NUL files with `grep -a` or a direct `Read`** — an ordinary grep cannot see inside them, which is the entire defect.

---

# §3 — STATE OF THE WORLD (verified by me with git at 2026-08-11T18:58-07:00, not inherited)

| Repo | `main` | Pushed? | Notes |
|---|---|---|---|
| `_evryn-meta` | **`33c4dee`** | ✅ 0 ahead | Clean. One other worktree: `_evryn-meta-acf` (ACf's — **not yours, leave it**). |
| `evryn-backend` | **`6995007`** | ✅ 0 ahead | One worktree, one branch. Atlas + Gate-B plan + last night's review branch all merged. |
| `evryn-quality` | **`4f47560`** | ✅ 0 ahead | Clean, no worktrees. |
| `evryn-dev-workspace` | **`f09d113`** | ✅ 0 ahead | Clean. |
| `evryn-team-runtime` | — | ✅ 0 ahead | **ACf pushed his 17 commits during my session** — that exposure is closed. Still his repo, still his push. **NOT YOURS.** |

⚠️ **One thing of ACf's is still machine-only, and it is not mine to push:** the branch **`acf/team-runtime-docs`** (his `_evryn-meta` worktree) **has no remote at all — it has never been pushed even once.** `git status` and `origin/main..main` are both structurally blind to that, which is why it needs an all-branches enumeration to find. **Exchange the finding with him through Justin; do not push another head AC's branch for him.** *(The enumeration that finds this class is in `CLAUDE.md` → Worktree & Branch Discipline. Run it before any batten-down.)*

**Prod is `v0.2.9`. `main` is SEVEN items ahead, ZERO migrations, nothing deployed. Nothing may be deployed by you.**

**The only untracked files anywhere are the five deliberate ones** in `evryn-backend`: `backups/full-public-2026-08-05.sql` + four `tests/_gate-a-*.ts`. **Do not commit, delete or modify them.** *(Their disposition is a live ballot item and the source of a genuine conflict between two of Justin's own instructions — `current-state` says he approved committing them via a lane; the 08-10 pack-out said don't touch them. Nobody has touched them.)*

**Nothing is spun. No lanes are running. Every prior subagent is unreachable** (resume is session-bound).

## 3.1 ✅ ACf IS A PEER, AND THE ARRANGEMENT CHANGED TONIGHT — this is easier than it was

**ACf runs the team runtime and commits into `_evryn-meta` and `evryn-quality`.** Justin settled the working arrangement this evening: **ACf has moved to his own worktree** (`_evryn-meta-acf` on `acf/team-runtime-docs` — I can see it) **and is holding moves until you are merged.** ⇒ **Justin now only needs to coordinate the two of you at merge time.** The day-long collision hazard the last two handoffs warned about is largely gone.

**What still holds, because it costs nothing and the failure is silent:**
- 🔴 **`git add <paths>` does NOT scope a commit.** A bare `git commit` afterwards commits the **entire index**. ⇒ **Always put the pathspec on the commit: `git commit -F - -- path/a path/b`.**
- 🔴 **Even a by-path commit takes that file's whole working-tree state.** ⇒ **`git diff <file>` first on anything shared.**
- **Your pushes carry his commits — that is expected and correct.** A rejected non-fast-forward push means **he pushed first: `git fetch` and re-check, never force.**
- ⚠️ **Pushing `_evryn-meta` DEPLOYS THE DASHBOARD.** Check `git diff --name-only origin/main..main | grep ^dashboard/` before pushing and say in your report if anything of his rode along.
- **He is a peer, not a report.** Exchange findings freely; never task, sequence, or gate him. **His subagents are unreachable to you and yours to him** — route through Justin.

## 3.2 The phantom diff — do not be alarmed by it

**`git diff main..<branch>` on anything from 2026-08-10/11 will render everything `main` gained as a DELETION by the branch.** `main` moved ~49 commits under those branches. ⇒ **Use three dots — `git diff main...<branch>`.** Two dots will lie to you.

---

# §4 — WHAT I DID THIS SESSION (past tense only)

1. **Loaded in properly**, eventually — the conductor cascade, the full orchestration protocol (842), the writing protocol, current-state, the Hub, the repo inventory, both prior AC0 handoffs, the sprint's header and tail, ACf's routed note to me, four Atlas sections in full (`00`, `01`, `10`, `11`) plus the front of `07`, and — **after Justin caught that I had skipped them** — `ARCHITECTURE.md` and `BUILD-EVRYN-MVP.md` in full.
2. **Spun a full-load runtime scout. It COMPACTED and I discarded it** per the junk rule. Its honest failure report is what produced §1.1's measurements — **the most valuable thing that happened today came out of a failed run.**
3. **Landed both blocked merges** — `_evryn-meta` `33c4dee` and `evryn-quality` `4f47560` — after ACf committed his work. **I checked the merge shape before running it** (`git merge-tree --write-tree`, exit 0, no conflicts) **and verified afterward that ACf's rule survived**; the merge added exactly my two lines on top of his tip.
4. **Reaped both review worktrees + branches**, local and remote, with the `node_modules` count-before/count-after check (both repos have none; unchanged).
5. **Removed the empty stray `evryn-backend-ac0-review` folder** (git had already forgotten it; verified empty first).
6. **Pushed `_evryn-meta`, `evryn-quality`, and `evryn-dev-workspace`** — each with Justin's explicit go, and I confirmed no `dashboard/` file was in the meta range, so that redeploy was a no-op.
7. **Verified `dependency-map.md`'s canary is genuinely on line 692**, closing a gap my scout could not confirm.
8. **Measured the Atlas spelling drift** (11/12 sections, 64 hits) and the section-quality contrast that makes Lane C's "level up, don't flatten" concrete.
9. **Confirmed at source that the sprint violates its own line-49 rule** — the thing Justin half-remembered.

## 4.1 The mistake I made, recorded because it is the transferable part

**I did not read `ARCHITECTURE.md` or `BUILD-EVRYN-MVP.md` during my load. Justin caught it.** They *are* on the conductor cascade's list — step 1 is the full cascade *minus the runtime*.

**Why I missed them, precisely: I put them on the SCOUT's load list and let that stand in for my own read.** That is delegating a load I owed. It is the same shape as every other failure this week — **a step that did not run, read as a step that passed** — and the tell was that I could point at a list containing the file and feel covered.

⇒ **The check: "is this file on a list I gave someone else?" is not the same question as "have I read it?"**

---

# §5 — LIVE FINDINGS AND OPEN ITEMS YOU MUST NOT LOSE

**5.1 The go-live sequence is SETTLED (Justin, 2026-08-10) — do not re-open it.** Two Gate-Bs bracketing the refactor: **read-only refactor → make Gate-B's suite runnable → deploy the seven to STAGING → GATE-B #1 → the extraction → GATE-B #2 → prod deploy + flip Clustering ON + Haiku to shadow + register her lanes.** Gate-B #1 *creates* the known-good baseline the extraction's *"behavior is unchanged"* claim must be checkable against. **Report it as resolved; never as a question.**

**5.2 There is NO Gate-B deadline.** Justin met Lacey 2026-08-07 (went well); they meet again **Friday 2026-08-14**. He is explicit that Gate-B being *meaningful* beats it being fast, and Lacey has been told to expect delays and roughness. **Do not sequence work backwards from a date.**

**5.3 The 6-month `emailmgr_items` TTL EXISTS but is NEVER SCHEDULED — and it is a loaded gun.** `cleanup_emailmgr_items()` is in every prod dump and documented at `schema-reference.md:182`, but there is **zero `cron.schedule` and zero `src/` caller.** ⚠️ **Anyone "closing the gap" by scheduling it deletes the v0.3 outreach backlog in one command. Do not.** Now a v0.3 candidate.

**5.4 `current-state.md` is stale in named ways** — no current SHA, and it does not reflect today. **Lane B owns it; do not patch it piecemeal in the meantime** or you will make one spot look freshly maintained while the rest misleads.

**5.5 ACf routed one item to me that is not yet in the sprint** — `docs/working/2026.08.10-acf-ac0-dashboard-status-pill-note.md`. Paste-ready: **the dashboard renders 4 of the 12 `ItemStatus` values as an unstyled gray pill**, identical to the deliberate gray used for `passed`/`ignored` — so an item still being *worked* looks like one that was *declined*. ⚠️ **`queued` and `processing` are among the four, and they are the states that become most common once Clustering is switched on.** ⚠️ **Re-verify its line numbers before handing it to a builder — ACf flagged that they are from 08-06 and unverified since.** Once pasted into the sprint, that note file can be deleted.

**5.6 Retirement candidates — Justin approved passing these forward; get his per-item OK before moving any.** `2026.08.10-ac0-ac0-overnight-handoff.md` (AC0-33's, superseded by this file) · `2026.08.10-ac0-ac0-handoff.md` (AC0-32's pack-out) · `2026.08.10-ac0-acn-safety-net-brief.md` (lane closed + merged) · `2026.08.04-ac0-aca-atlas-refactor-brief.md` (lane closed; the Atlas is merged). **Read each fully before moving — all four carry engineering records.** **Explicitly NOT candidates:** `2026.08.05-ac0-ac0-handoff.md` (holds ACh's P1–P11 queue and the do-not-re-open list) and `2026.07.22-ac0-ac2-fast-follow-brief.md` (its §9 holds the nested-relay rebuild recipe owed at the `CLUSTERING_MODE` flip).

**5.7 Two verifications remain owed and UNOWNED:** the **Gate-B thin-record efficacy check** (must run twice — once with a thin `active` gatekeeper, once with a `status: lead` record, because the code renders different text for each; **owner: the Gate-B reviewer**), and the **nested-relay live check**, which is structurally unreachable until the `CLUSTERING_MODE` flip.

**5.8 Approved but never spun:** the **Gate-B reviewer** (approved-to-commission; a full-load scout established its brief is **not** refactor-dependent) · a **binding-safety-net lane** · a lane to **persist the poll cursor durably** · **Step 100**, which touches **Evryn's identity layer and must run a Mira sub with edits left in SCM for Justin.**

**5.9 The Linear item, finally concrete:** update the ticket for **Step 57 / ③ (Runtime Bookkeeping)** to reflect that it **SHIPPED to prod as `v0.2.8` on 2026-07-22**. It rode five handoffs as the bare word *"Linear"*; it is now a checkable task. Do it or close the ticket.

**5.10 🔴 A `_evryn-meta/CHANGELOG.md` ENTRY IS NOW OWED, AND IT IS MINE THAT I DID NOT WRITE.** *(Caught in my own fragment sweep; flagging rather than claiming.)* AC0-33's `#lock` correctly logged *"meta: NONE"* on the grounds that the meta-side writes were sitting unmerged on a branch. **Tonight I merged them to `main`** (`33c4dee`), so the rule now fires: the routing table says a substantive doc/protocol change gets an entry, written by the AC responsible for the ship. **The newest meta entry is still 2026-08-10 evening; there is nothing for 08-11.**
**What it should cover:** the worktree/`node_modules` reap hazard + the `#`-heading rule (CLAUDE.md) · two AGENT_PATTERNS entries + the three-instance worked example · *the lane brief is trip-2 reading* (orchestration protocol) · the QC mutation-baseline pattern (`evryn-quality`, its own repo's changelog) · and **tonight's loading-architecture decision itself once Lane A lands.**
**Why I did not just write it:** an uncommitted file left overnight in a repo a peer is working in is the documented sweep hazard, and a `CHANGELOG` edit is not brief-class traffic I may commit unasked. **Write it with Justin in the morning, or fold it into Lane A's landing.**

---

# §6 — YOUR HARD BLOCKERS. Stop and leave things safe rather than improvising past any of these.

- **A deploy, a prod migration, or anything touching production — including a STAGING deploy.** Never yours.
- **A source-of-truth doc edit committed to `main`** without Justin's explicit go (Hub, spokes, ARCHITECTURE, BUILD, protocols, CLAUDE.md, LEARNINGS, AGENT_PATTERNS, the sprint, current-state). **Working-brief and mailbox-class traffic is pre-authorized; everything else waits in SCM.**
- **Any change to `evryn-backend/identity/**`** — Mira's and Justin's layer, never folded quietly into a build.
- **A merge where the lane, the reviewer and you do not all agree**, or where a reviewer returned NO-GO.
- **A push** — absolute, no carve-out. Ask.
- **Anything irreversible, outward-facing, or involving money.**
- **A subagent reporting that its context compacted** — its output is junk. **Discard and re-spin; do not "discount" it.** *(This fired today. Discarding was the right call and it still produced the session's most valuable finding.)*
- **Two instructions in conflict, or a doc contradicting the runtime.** Surface it; never silently resolve.

**The standing exception that IS yours:** build-level code where DC, QC and you all agree, it is below Justin's evaluation altitude, and it is not a source-of-truth doc. That is his standing authorization of 2026-06-02. **It never extends to a push, a deploy, or a migration.**

---

# §7 — RUNNING LOG (append as you go; this is rule 2 from §0.1)

### AC0-34 — 2026-08-11T18:58-07:00 — set-down

Housekeeping closed, both merges landed with ACf's work verified intact, three repos pushed, worktrees reaped, tree clean everywhere. The loading architecture (§1) is designed and signed off by Justin; the five lanes (§2) are scoped and sequenced but **nothing is spun**. Justin is away for the night.

**The single most useful thing that happened:** a scout failing honestly. Its compaction report carried the token measurements that turned *"the load is too big"* from a vague feeling into a number — and that number is what made the whole §1 redesign obvious rather than speculative.

**The single most useful thing Justin did:** catching that I had skipped ARCHITECTURE and BUILD. Reading them is what makes §1 safe — the scout can only stop carrying the intended-shape docs because the conductor is now genuinely holding them.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
