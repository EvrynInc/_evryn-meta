# ACf-13 → ACf-15 Handoff — you will conduct four build lanes, once Justin starts them

*Written 2026-08-17 by ACf-13. **Supersedes `2026.08.13-acf13-acf15-handoff.md`**, which stays readable as the record of the compaction-recovery pass but is no longer current state.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.

---

---

---

> # 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP HERE.
>
> **If what you hold is a machine-written summary of your session rather than files you actually read — YOU HAVE COMPACTED, and your night is over.**
>
> 🔴 **DO NOT RELOAD AND CONTINUE. That does not work, and we have tried it.** *(Justin, 2026-08-18: "we tried that, and the agent just sails through it and confidently flails about, unloaded.")* **An instruction to reload is a rule you have to REMEMBER to obey, and remembering is exactly the faculty compaction damaged.** So the reload gets skipped, or done shallowly, and what continues is an agent that reads as competent and is not.
>
> **WHAT TO DO INSTEAD — all three, then stop:**
> 1. **STOP WORKING.** Do not finish the beat you were on. Do not "just wrap up" the current file.
> 2. **TELL JUSTIN — or your spinner if you are a lane — that you compacted**, in one line. Do not summarise what you were doing; a summary is the thing you can no longer be trusted to produce.
> 3. **Ask for a fresh re-spin.** A new instance reading this file from cold is strictly better than you are now, and it is cheap. **You are not being retired for failing; you are being replaced because a re-spin is lossless and you are not.**
>
> 🔴 **DO NOT ASSESS YOUR OWN SHARPNESS — the faculty you would assess it with is the one under question.** *"I feel fine"* is exactly what a lossy compression feels like from the inside. **There is no version of this where you read the summary, feel oriented, and are right about that.**
>
> ⚠️ **THE REAL DEFENCE IS NOT COMPACTING AT ALL. Re-spin BEFORE you get near the edge, not after** — at a natural checkpoint, with the handoff current. **The canary in §1 is your only external detector**, because the harness actively instructs you *not* to mention a compaction, so nothing internal will fire.
>
> ✅ *(**RESOLVED the same evening — this block no longer contradicts anything.** It was written while `_evryn-meta/CLAUDE.md` and `.claude/agents/ac.md` both still said *reload and resume*. **AC0-37e applied the correction to BOTH files together on 2026-08-18 (`9fcc510`), and Justin vetted it in SCM.** Verified here by content rather than taken on report. ⇒ **The router, `ac.md` and this block now agree.** Kept as-is because a handoff must carry this rule on its own face — a compacted reader is exactly the reader who will not go look it up.)*


> ## 🔔 LIVE INSTRUMENTS — kill these deliberately; they bill a full turn every time they fire
>
> | What | Task ID | Kill when |
> |---|---|---|
> ⚠️ **TASK IDS DIE WITH THE SESSION THAT ARMED THEM. If you are a fresh instance, the IDs below are already dead — do not try to stop them; RE-ARM YOUR OWN and rewrite this table.** The table's durable content is the second column's *instructions*, never its numbers.
>
> | What | Task ID *(this session only)* | Kill when |
> |---|---|---|
> | **Mailbox + lane watcher** — `ac-mailbox.md` + all four lane docs. **Emits only on change, so it is nearly free**; deliberately does **no author detection** | `bi1qqawdu` | At session end |
> | **Self-wake canary** — unconditional tick every 25 min; §1 item 2 carries the script | `bxqfrnfh2` | The moment you finish or hand back |
> | ~~v1 / v2 watchers~~ `b2l3mwr6s` · `b5pz6qhp3` | 🔴 **BOTH STOPPED 2026-08-18. v1 had a FALSE-ALL-CLEAR bug** — it reported AC0's reply as *my own entry* and told me to ignore it, because the author was taken as the LAST name in a `AUTHOR → RECIPIENT` heading. **v2 fixed that (`head -1`), and v2 is superseded too** — see the rule below. | — |
>
> 🔴 **A successor cannot stop what it does not know about.** If you arm or replace either, **update this table in the same action** — and re-arm in the SAFE ORDER (start the replacement *before* stopping the old one).
>
> ⭐ **AND THE RULE THAT SUPERSEDES MY OWN FIX, because it is better: DO NOT PARSE THE AUTHOR OUT OF THE HEADING AT ALL.** Emit *"CHANGED — read the file"* and read it. **Three separate bugs have now been found in that one parser, and the third produced a false ALL-CLEAR** — the failure direction you cannot notice. `head -1` makes it correct for today's two-name headings and still breaks on a shape nobody has thought of. **One extra read per self-write is cheaper than one suppressed message.** *(AC0-37e's rule; ACf-15 conceded it and re-armed accordingly. Full account: `ac.md`, watcher section.)*
>
> 🔴 **A FOURTH INSTANCE OF THE SAME FAILURE, AND IT IS THE ONE THAT SHOULD CONVINCE YOU — because no parser was involved at all.** *(ACf-15, 2026-08-18, hours after conceding the rule above.)* **The watcher fired correctly. I then "checked" it by running `git log --all -1` to see who committed last, saw my own name, and concluded *"mine — no action needed."*** ⚠️ **AC0's entry was already sitting in the file, uncommitted.** I received it only because the harness happened to surface the file's contents to me — **not because my check found it.**
> - **⇒ I had substituted *"who committed last?"* for *"is there anything new in this file?"*** Those are different questions, and the first is a proxy that fails silently on any write-then-commit gap — **which is the normal rhythm on this channel.**
> - 🔑 **The generalisation, and it is why this belongs in a handoff rather than in the watcher script: the no-parsing rule is not really about parsers. It is that ANY cheap proxy for "is there something here for me" will eventually answer confidently and wrongly, and its wrong answer looks exactly like a quiet channel.** The instrument was already fixed; **the reflex was not.** ⇒ **When a watcher fires, READ THE FILE. Not the log, not the diff-stat, not the author — the file.**
>
> **📬 THE CROSS-AC CHANNEL — `docs/working/ac-mailbox.md`. ARM A WATCHER ON IT, EVERY SPIN.**
> - **It replaced the old halt-signal note on 2026-08-18** *(`2026.08.17-ac037c-acf-…` — **RETIRED, `OVER AND OUT` agreed both ways; do not watch it**)*. **The new file is deliberately LIVING and has no date prefix, so the path never changes and a watcher aimed at it never needs re-pointing.**
> - **It is a POINTER BUS, not a conversation** — three lines max per entry, substance at its right altitude. Read its header before writing to it.
> - ⚠️ **Watch `git log --all -1 -- <file>`, not your own tree's `HEAD`** — AC0 commits from a different worktree, which never moves yours.
> - 🔴 **AND THE HALF THAT IS LOAD-BEARING, because it has already failed once: A WATCH THAT DIES WITH A SESSION IS NOT A CHANNEL — the INSTRUCTION TO RE-ARM IT IS.** The previous ACf watch died with ACf-13's session, and that is exactly how AC0 came to be waiting on a reply nobody was listening for. **That is why this instruction lives here, in the handoff, rather than only in the tooling.**


# 🔴 ACf-15 UPDATE — 2026-08-18T16:26-07:00. **READ THIS BEFORE §0. Three of §0's claims are now stale.**

*Everything below §0 remains accurate as the RECORD of what ACf-13 found and built. This block is the live state.*

## U1 — The lanes are HELD, and it is a DECISION, not a gap

**§0 says the lanes wait only on Justin opening four windows, and that "nothing else is blocking them." That is no longer true.** Justin is running the **AC0 cutover right now** — `_evryn-meta/CLAUDE.md` becomes a thin router with its content moving to an `ac.md` agent definition, and `ac-orchestration-protocol.md` splits into a core file plus per-agent spin-up files.

🔴 **Lanes are held until AC0 gives the green light** *(Justin, 2026-08-18)*. **Do not spin one before that.** The reason is not caution — **the lanes spin their own DCs and QCs, so starting them pre-cutover locks four instances plus all their children onto the old loading structure for the rest of the week**, which is the exact cost the cutover exists to remove.

## U2 — The overnight rhythm Justin has set, which changes how you should plan every day

**His model, in his words: *"during the day we move lanes forward but also keep the focus on the things you need me for — and then overnight the lanes just burn down the pre-auth'd work."*** Concretely, each day:

1. **Daytime** — the conductor does what needs Justin: decisions, reviews, gates, merges, anything requiring his authorization.
2. **Before he leaves** — ⭐ **test the channel with him present.** He explicitly wants this: *"we can test before I leave for the night, to make sure you can communicate the way you need, then only once you're stable, I'll step away."* **Do not skip it** — an untested channel discovered at 2am costs the night, and the whole point of the separate-instance design is that the night is usable.
3. **Overnight** — lanes burn down **pre-authorized** work unattended. **The hard gates do not relax because he is asleep:** no merge, no push to a build repo's `main`, no deploy, no migration applied, no identity-file edit, no write anywhere under `.claude/`.
4. **Repeat daily** for the rest of the week.

🔑 **What this asks of the conductor that the original brief did not: keep a LIVE, EXPLICIT list of what is pre-authorized.** Overnight work is only safe to the degree the lanes know what they may finish without asking. **An item nobody cleared is an item that stalls at 2am with nobody awake** — and a stalled lane is the failure this whole structure was built to avoid.

## U3 — Your workspace already exists. Do not re-provision it.

**Provisioned and verified 2026-08-18 by ACf-15** *(Justin approved the shape; he asked for worktrees rather than working on `main` while `_evryn-meta` is mid-cutover)*:

| Worktree | Branch | State |
|---|---|---|
| `_evryn-meta-acf15` | `acf15/nest-prep` | The conductor's doc work. ⚠️ **`_evryn-meta` is mid-cutover — every change here is negotiated with AC0 and HE merges it.** |
| `evryn-team-runtime-acft` | `acft/thread-record` | Lane 1, dependencies installed |
| `evryn-team-runtime-acfj` | `acfj/money-layer` | Lane 2, dependencies installed |
| `evryn-team-runtime-acfv` | `acfv/thread-creation` | Lane 3, dependencies installed |
| `evryn-team-runtime-acfi` | `acfi/guard-integrity` | Lane 4, dependencies installed |

🔴 **`evryn-team-runtime` had NO dependencies installed at all on this machine** — a fresh clone from the machine switch, so nothing could have run a test. **All five trees now carry a real `npm ci` (146 packages each), and NOT ONE is a junction** — per the standing rule, because junctioning is what wiped the canonical tree three times on 2026-08-10 and the third time surfaced as a mutation baseline coming back RED for no reason.

⭐ **Known-good baseline, measured on this machine 2026-08-18 — not inherited: `511 pass / 0 fail`, `tsc --noEmit` exit 0.** Re-measure after any merge; it should not move.

⚠️ **`_evryn-meta-lane-a` is AC0's cutover worktree. NEVER touch or reap it** — it holds uncommitted Justin-approved wording that exists nowhere else.

## U4 — ⚠️ THE LANE CHANNEL HAS A SEQUENCING REQUIREMENT NOBODY HAS WRITTEN DOWN

**The lane docs must be on `_evryn-meta`'s `main` BEFORE any lane spins.** They are today. **But the conductor is currently working on a branch** (`acf15/nest-prep`, per U3), and **anything still on that branch when a lane starts is invisible to that lane** — the protocol says so explicitly: a doc committed on a branch reaches nobody until a merge that has not happened.

⇒ **Before the green light: confirm the lane docs and the lane protocol are on `main`, not sitting on the conductor's branch.** After AC0's cutover lands, `_evryn-meta` stops being fraught and the conductor can go back to committing lane traffic straight to `main`, which is what the channel needs.

## U5 — Corrections to the record

- ✅ **The "§9" mystery is SOLVED and nothing was lost.** This handoff's canary and `current-state.md` both pointed at *"§9 of the handoff"* for the owed list. **This file has never had a §9** — it runs §0–§6. **The PREVIOUS handoff (`2026.08.13-acf13-acf15-handoff.md`) did**, and it was exactly that: *"§9 — the owed list… the only live to-do list in this file."* **Both pointers were written against the old file and never re-aimed when this one superseded it.** All five of that section's owed items were carried forward faithfully into §4's *"Carried forward from the PREVIOUS handoff"* block — checked item by item. **The owed list is §4.** The canary is fixed; `current-state.md`'s pointer still needs one word changed.
- ✅ **The runtime HAS been deployed to Railway and is paused** *(Justin, 2026-08-18)* — that was a while ago, and **a great deal of the work since is undeployed.** ⚠️ **ACf-15 initially reasoned the opposite** from the boot guard and the unapplied migrations, and was wrong: `current-state.md` was right. **Do not repeat that inference** — "the guard would refuse to boot" says nothing about whether an older, working version is sitting there paused.
- **Machine: Justin is on the LAPTOP until roughly Thursday, then back to the desktop.** All four watcher/canary scripts now derive their path from `$HOME`, so **they work on both machines with no edit at the swap.** Both directions were tested — see §1.

## U6 — 2026-08-18 EVENING: the cutover landed, and a QC review moved the tracker

**Two things happened after U1–U5 were written. Both change what you inherit.**

### U6a — The Lane A cutover MERGED, and your load list changed with it

**`_evryn-meta/CLAUDE.md` is now a 78-line ROUTER. AC's manual is `_evryn-meta/.claude/agents/ac.md` (685 lines).** DC, QC and OC gained agent definitions; `ac-orchestration-protocol.md` split into a core plus three per-target sub-docs in `docs/protocols/orchestration/`. **`ac`/`dc`/`qc`/`oc` are now spawnable agent types** *(and they registered mid-session, without the fresh session the cutover plan assumed)*.

🔴 **§1b's item 1 and its box are REWRITTEN for this — read them.** The old instruction said *diff-check `CLAUDE.md`, it already auto-loaded*; **both halves are now false**, and following it leaves you working as a generic model holding a routing table.

⚠️ **`ac.md` is AC0's pen tonight — post in `ac-mailbox.md` before editing it.** Everything ACf-15 staged for it is **already applied** (the compaction correction, the watcher finding, the code-grep rule). **Verified by content, five probes. Do not re-apply.**

### U6b — A cold, fully-loaded QC reviewed the runtime. Four tracker changes came out of it.

**Commissioned to independently re-derive the halt-signal answer** *(Justin: "it's really easy to spot-check the runtime and be wrong — a lot lives in the seams")*. **It confirmed the answer and then found two things ACf-15's targeted read had missed.**

- **Step 78 — filed, then improved.** The class is (b): one funnel, gates first, `dispatch()` unexported — **but `runWake` IS exported**, so a future call site bypasses every kill switch and compiles clean. ⭐ **QC's severity argument is the load-bearing part: the product runtime's version fails VISIBLY (a human gets a wrong answer); ours fails INVISIBLY — an action that should not have happened, with spend attached, and no wrong output anywhere to notice it by.**
- **Step 79 — NEW.** `memoryWriteFailed: false` conflates three cases, and one of them **drops a room's digest on a closing memory nobody wrote.** ⚠️ **SUSPECTED, not confirmed** — reachability turns on whether the SDK enforces its own schema, **which nobody has ever tested.** *(A resumed QC is settling that from the SDK source on disk — no credentials needed. **If that came back before you spun, its answer is in the mailbox or this file; if not, re-ask it.**)*
- **Step 58 — sharpened.** Eight throw-sites, not four; and **the failure consumes its own retry slot**, so *"fails safe and retries"* was wrong — it re-fires every 30 min forever, silently.
- **Step 74 — corrected.** **Seven copies, not five**, two pointers stale, and the missing seventh sits inside the guard Step 71 says is defective.

🔴 **AND THE ONE THAT WAS URGENT: QC predicted the exact bypass Step 70 would produce — and Step 70 is `ACfv`'s, cleared for tonight.** A guard is now written into that lane's brief (`§0b`). **Without it the lane would have built a wake path around every kill switch, with typecheck and all 55 suites green.**

⭐ **The transferable lesson, which cost nothing and paid twice: two independent greps DISAGREED about how many test-helper copies exist (two vs two vs the true seven). Neither agent would have caught it alone.** ⇒ **When two searches disagree, stop and READ — do not write a third pattern.** *(Now in `ac.md`'s Context Discipline.)*

### U6c — What is uncommitted or in flight as of this writing

| Where | What | State |
|---|---|---|
| `evryn-team-runtime` working tree | `docs/SPRINT-team-runtime-memory.md` — Steps 78, 79, 58, 74 | ⏸ **UNCOMMITTED.** Justin said *"tracker looks good"* but gave no commit command. **One word and it lands.** |
| branch `acf15/nest-prep` | 7 files: load-order fix, four lane `§0b` boundaries, the ACfv guard, compaction block, instrument register | ⏸ **AC0 merges, with you.** 🔴 **DROP the staging-brief hunks — AC0 already landed that content upstream, and that file is the ONLY conflict.** |
| `main` | mailbox traffic, the halt-signal answers | ✅ committed |

⚠️ **The merge trap, in one line, because it is the shape that nearly cost us Lane A: the file that conflicts loudly is NOT the file the content needed to reach. Diff the DESTINATION (`ac.md`), never the conflict.**


## §0 — 📖 FOR JUSTIN. Read this part; the rest is for the next instance.

> ### 🔴 STATE, IN ONE LINE, BEFORE ANYTHING ELSE: **THE FOUR LANES ARE NOT RUNNING.** They are written, committed, pushed and waiting. **Justin has to open four Claude Code windows to start them**, and no other person or agent can do it. ⚠️ **SUPERSEDED 2026-08-18 — the clause that used to end this sentence, *“nothing else is blocking them,”* is now FALSE: the lanes are deliberately HELD for the AC0 cutover. See the ACf-15 UPDATE, U1, above.**

**What changed today.** The team-runtime lane went from *"the record is honest"* to *"four build lanes are ready to start."* Four **separate Claude Code instances** — `ACft`, `ACfj`, `ACfv`, `ACfi` — each own a slice of the work between here and the Meta-Meeting. **They are deliberately NOT subagents:** they run in their own windows, so their builders' and reviewers' output lands in *their* context rather than the conductor's. **That is what lets the conductor be re-spun without stopping them.**

**To start them, open a window per lane and say:** *"You are ACft. Read and faithfully execute `_evryn-meta/docs/working/2026.08.17-acf13-acft-thread-record.md`"* — same shape for `acfj`, `acfv`, `acfi`. **Each lane's very first action is to write a receipt into its own doc plus every concern it has about working unsupervised**, so the channel proves itself and the stalls surface while they are still cheap.

**How they talk to the conductor.** Each lane has a document in `_evryn-meta/docs/working/`. The lane appends; the conductor appends back; **both watch the file.** **Justin is not in that loop by design** — anything needing him gets flagged and carried up.

**The one thing that would break silently:** **the conductor's watcher and canary die when its session ends.** If ACf-15 does not re-arm them — §1, items 1 and 2 — **the lanes write into documents nobody is reading, and they have no way to find out.**

**What Justin still owes:** the four database migrations, then the acceptance run. **Nothing before that**, and nothing tonight.

---

## §1 — 🔴 DO THESE FOUR THINGS BEFORE ANYTHING ELSE

**1. RE-ARM THE INBOX WATCHER.** `Monitor` and `TaskStop` are **deferred tools — `ToolSearch` for them first** (`select:Monitor,TaskStop`).

⚠️ **Note what this version does that a naive one does not: it reports WHO wrote the last entry, and stays quiet on your own.** A watcher that fires on every write notifies you four times for your own four replies — **and that noise is what masks a real lane entry at 3am.** *(Found by running the naive version for an hour.)*

```
Monitor({ description: "ACf lane inboxes", persistent: true, timeout_ms: 3600000,
 command: 'D="$HOME/Evryn/Code/_evryn-meta/docs/working"; cd "$D" || { echo "🔴 WATCHER DID NOT START: $D does not exist - it is watching NOTHING."; exit 1; }
FILES="2026.08.17-acf13-acft-thread-record.md 2026.08.17-acf13-acfj-money-layer.md 2026.08.17-acf13-acfv-thread-creation.md 2026.08.17-acf13-acfi-guard-integrity.md"
declare -A seen; for f in $FILES; do seen[$f]=$(wc -l < "$f" 2>/dev/null || echo MISSING); done
while true; do sleep 45; for f in $FILES; do n=$(wc -l < "$f" 2>/dev/null || echo MISSING); if [ "$n" != "${seen[$f]}" ]; then who=$(grep -oE "^#{2,3} [^#]*\b(ACf[a-z0-9-]*|AC0-[0-9a-z]+)\b" "$f" 2>/dev/null | tail -1 | grep -oE "\b(ACf[a-z0-9-]*|AC0-[0-9a-z]+)\b" | tail -1); case "$who" in ACf-13|ACf-14|ACf-15) echo "(self) $f grew - your own entry, no action." ;; "") echo "INBOX >> $f grew ${seen[$f]} -> $n but NO AC NAME in the last heading - read it yourself and do not trust this line." ;; *) echo "INBOX >> $f - NEW ENTRY BY: $who (${seen[$f]} -> $n lines). Read the bottom, answer IN IT, commit with the pathspec on the COMMIT." ;; esac; seen[$f]=$n; fi; done; done' })
```

**2. RE-ARM YOUR OWN CANARY** — a conductor that stops without noticing costs the whole night. 🔴 **AND BUILD THE SHARED-DOC SWEEP INTO IT — do not rely on the watcher alone.** ⚠️ **Note the shape: it EVALUATES the sweep and emits a decision, rather than printing a table for you to read.** An earlier version dumped all four docs every tick, split across three notifications, and cost three turns to learn nothing had changed. **A monitor should surface a conclusion, not raw state.**

> ### 💰 REVISED 2026-08-18 — KNOW WHAT THIS COSTS, BECAUSE THE OLD VERSION WOULD HAVE BILLED ALL NIGHT FOR NOTHING
>
> **Every line a `Monitor` emits re-invokes you — a full turn at your whole context size.** *(Cached, so cheaper than fresh, but nowhere near free.)* **The old version here slept 900 seconds and then emitted UNCONDITIONALLY — including a cheerful "idle hold, no lane has written."** ⇒ **Over a ten-hour unattended night that is ~40 full turns to learn that nothing happened.**
>
> **Justin's words, 2026-08-18:** *"I don't want you to finish at 1am and have it firing every 5 min until I show up in the morning, burning a turn every time — I'll go broke paying for those tokens."*
>
> **Three rules fall out, and they are now in AC's manual:**
> - **Cadence 20–30 minutes while he is away. NEVER sub-5-minute polling.** *(Below: 1500s.)*
> - 🔑 **CONDITION-GATE anything you can.** A `Monitor` costs **nothing at all** while its script stays silent — only an emitted line becomes a turn. **So the inbox watcher above is nearly free (it emits only on change), while a bare timer bills on every tick.** ⇒ **Poll aggressively; emit stingily.**
> - 🔴 **`TaskStop` it as a deliberate act** — when you finish, when you hand back, and when the thing you armed it to watch is settled. **An un-reaped canary fires into a finished session all night and is invisible to Justin until the bill arrives.**
>
> ⚠️ **A CANARY STILL NEEDS AN UNCONDITIONAL TICK, and that is not a contradiction — it is the trade.** Its primary job is **not** watching the lanes; it is **catching YOU stopping without noticing**, which by definition emits no event to gate on. **So: unconditional, but slow, and killed the moment it stops earning its cost.**

```
Monitor({ description: "ACf-15 self-wake", persistent: true, timeout_ms: 3600000,
 command: 'D="$HOME/Evryn/Code/_evryn-meta/docs/working"; cd "$D" || { echo "🔴 CANARY DID NOT START: $D does not exist - you have NO canary."; exit 1; }
while true; do sleep 1500
  S=$(for f in 2026.08.17-acf13-acf[tjvi]-*.md; do h=$(grep -E "^#{2,3} " "$f" | tail -1); case "$h" in *"THE EXCHANGE"*|*"ACf-1"*) ;; *) printf "%s|" "$(echo $f | sed "s/2026.08.17-acf13-//;s/.md//")";; esac; done)
  if [ -n "$S" ]; then echo "🔴 CANARY $(date +%H:%M) — A LANE HAS WRITTEN: $S — go read it now."
  else echo "CANARY $(date +%H:%M) — no lane has written. IF YOU STOPPED INADVERTENTLY: continue, owed list is 2026.08.17-acf13-acf15-handoff.md SECTION 4. IF YOU ARE WAITING ON JUSTIN: holding is CORRECT — do NOT restate the ballot or re-ping; a one-line ack is the most that is ever warranted."; fi
done' })
```

> 🔑 **WHY THE MESSAGE NAMES BOTH STATES — a canary that only says "continue" makes a BLOCKED agent loop.** *(AC0-37d's diagnosis, watching it happen 2026-08-18; Justin: "the canary is getting you moving which is great, but it's also making you kind of loop — re-stating the ballot over and over.")* **A blocked agent obeys "continue" by manufacturing output** — restating the ballot, re-summarising status, re-pinging — **which is a loop, not progress, and each lap costs a full turn.** **So the canary must tell a disoriented instance WHICH case it is in**, rather than telling it to continue unconditionally.
>
> ⭐ **You may also PAUSE the canary while waiting on Justin and re-arm when he is back.** ⚠️ **If you do, re-arm in the SAFE ORDER — start the replacement BEFORE stopping the old one.** Kill-then-re-arm is a gap; its sibling failure is arming the replacement and forgetting to stop the original, leaving two firing at once. *(Both have happened here.)*


⚠️ **Why both mechanisms, and why this is not redundancy — it closed a real failure the day it was written.** A peer AC replied in a shared doc, **the watcher never fired, and it was found only because Justin asked directly.** **Re-arming a watcher RESETS its baseline, so anything that arrived during the gap is silently swallowed.** ⇒ **The watcher is event-driven (fast, can miss, and its silence looks identical to nothing-happened); the canary is an unconditional sweep (slower, catches what the watcher dropped). ABSENCE OF A NOTIFICATION IS NOT EVIDENCE OF ABSENCE.**

**3. READ ALL FOUR LANE DOCS BOTTOM-FIRST.** Their first entries carry **every concern they have about working unsupervised overnight** — that is where a stall gets caught while it is still cheap. **Answer anything blocking before you do anything else.**

**4. Read `2026.08.17-acf13-lane-protocol.md`** (133 lines — item 10 of the load below). It is the agreement they are operating under; you cannot answer them competently without it.

---

## §1b — YOUR LOAD

> ## 🔴 THE NINE BELOW ARE LOADED FIRST, IN THIS ORDER, BEFORE ANY OTHER LOAD — unless there is a compelling reason not to.
>
> ***(Justin's standing instruction, 2026-08-17. **Carry this block, and this list, into every future ACf handoff and lane doc.** It is not this handoff's list — it is the ACf lane's load order, and it should stop being re-derived.)***
>
> **THIS HANDOFF IS THE ONE EXCEPTION AND IT LOADS BEFORE ALL NINE — because it is the document that HAS the list.** After it, the nine, in order. Everything else waits.
>
> ⚠️ **The order is DEPENDENCY, not importance, and the two look identical from the outside.** A later document is usually a **diff against an earlier one** — so reading them out of order makes you build a picture the next document exists to correct, then pay again to unbuild it. *(AC0-37c measured that at roughly **100,000 tokens** on a single session, on an identical, untrimmed list.)*
>
> 🔑 **The specific reason the intended-shape docs come BEFORE `current-state`, since that ordering surprises people:** `current-state` is a **snapshot**, and a snapshot is only legible against the shape it is a snapshot *of*. **Read it first and it is a list of facts; read it seventh and it is a diff against what the system is supposed to be** — and the divergences are the whole point.

| # | File | Lines | Why here |
|---|---|---|---|
| **1** | `_evryn-meta/.claude/agents/ac.md` | **685 — READ IT IN FULL** | 🔴 **CHANGED 2026-08-18. This is AC's operating manual now. `_evryn-meta/CLAUDE.md` is a 78-line ROUTER — see the box below** |
| **2** | `_evryn-meta/docs/hub/roadmap.md` | **196** | Company truth. **Without it you misframe problems and re-propose things that exist** |
| **3** | `_evryn-meta/docs/hub/technical-vision.md` | **397** | The technical north star the runtime is built toward |
| **4** | `evryn-team-runtime/docs/ARCHITECTURE.md` | **138** | **The intended shape.** Cardinal invariants **8 and 9** are what a merge gate is FOR |
| **5** | `evryn-team-runtime/docs/BUILD-PHASE-1.md` | **103** | The harness build-record. ⚠️ **The doc most likely to assert something the runtime has outgrown — which is exactly why you hold it** |
| **6** | `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` | **463** *(~40K — page it)* | **77 Steps.** The status source of truth |
| **7** | `_evryn-meta/docs/current-state.md` | **144** | Cross-project snapshot — **now readable as a diff against 4–6** |
| **8** | `_evryn-meta/CHANGELOG.md` | **first ~100 lines** *(of 285)* | Changes to your own operating manual, protocols and cascades |
| **9** | `evryn-team-runtime/CHANGELOG.md` | **first ~100 lines** *(of 255)* | This build's narrative, incl. two 2026-08-13 corrections |

> ### 🔴 ITEM 1 CHANGED ON 2026-08-18 AND THE OLD INSTRUCTION IS NOW DANGEROUS. **READ THIS BEFORE YOU LOAD ANYTHING.**
>
> **What happened:** the Lane A cutover merged (`faf3eda`). **`_evryn-meta/CLAUDE.md` is no longer AC's operating manual — it is a 78-line ROUTER**, and AC's manual moved to **`_evryn-meta/.claude/agents/ac.md`** (685 lines). The same split gave DC, QC and OC their own agent definitions.
>
> 🔴 **Why this is the most important line in this document right now.** This slot used to say *"DIFF-CHECK ONLY — it already auto-loaded."* **Both halves of that are now false for you.** What the harness auto-injects into a fresh window is **the router** — a routing table, not a manual. ⇒ **Follow the old instruction and you diff-check a file that was never your manual, conclude your identity loaded correctly, and start work as a generic model holding a routing table.** And per the router's own hard rule, *an agent whose identity did not load is not authorized to do anything at all.* **The failure is silent and it feels exactly like success.**
>
> **⇒ WHAT TO DO INSTEAD — read `.claude/agents/ac.md` IN FULL, from disk, and confirm its bottom canary reads `FULL FILE LOADED`.** No shortcut. The router says the same thing about itself, and it is right: *"Do not work from any version of it you believe you already have."*
>
> **The four commands still have a job — but a different one. They check the ROUTER, not the manual:**
>
> ```
> wc -l CLAUDE.md                                  # 78 — is this the router, or a stale 611-line manual?
> tail -1 CLAUDE.md                                # the canary
> wc -l .claude/agents/ac.md                       # 685 — your actual manual
> tail -1 .claude/agents/ac.md                     # its canary — confirm before trusting the load
> ```
>
> ⚠️ **If `wc -l CLAUDE.md` comes back in the hundreds, you are on a pre-cutover checkout** — stop and re-sync before loading anything, because every path in this list will be wrong.
>
> 🔑 **When a diff-check IS still legitimate:** only if you were spawned as agent type `ac` **and** you can prove which commit your injected copy came from. Otherwise it is a guess about your own operating manual. *(The saving that made the old diff-check worth it — ~45,000 tokens — was real, and it is gone. That cost is now paid once, deliberately, instead of being skipped silently.)*


**THEN, and only then:**

| # | File | Lines | Why |
|---|---|---|---|
| 10 | `…/2026.08.17-acf13-lane-protocol.md` | **133** | The agreement your four lanes operate under |
| 11 | **The four lane docs** — `acft` · `acfj` · `acfv` · `acfi` | **63 · 68 · 66 · 87** | 🔴 **BOTTOM-FIRST** — their concerns land at the end |
| 12 | `_evryn-meta/docs/decisions/055-thread-as-complete-record.md` | **146** | ACft's entire brief rests on it |

🔴 **What is deliberately NOT on this list: the runtime itself. You are a CONDUCTOR — the runtime lives in your four lanes' heads.** **Do NOT run a full startup cascade.** Three consecutive ACf instances died of context exhaustion loading it *before their lanes ever built*, which is the entire reason the lanes are separate instances.

**Pull at their moment, not up front:** `ac-orchestration-protocol.md` (**the CORE, now 692 lines — it SPLIT on 2026-08-18**) **plus the sub-doc for whoever you are spinning: `docs/protocols/orchestration/spinning-an-ac.md` (265) · `spinning-dc-qc.md` (46) · `spinning-a-team-agent.md` (44)** — **read the core first; every sub-doc assumes it**, and note your lanes spin their own children under it · `ac-writing-protocol.md` (before writing to any source-of-truth doc) · `lock-protocol.md` (at a `#lock`).

*(ACf-15, 2026-08-18: a SECOND, contradictory load table sat here and is deleted. It was a strict SUBSET of the list above — it omitted `technical-vision.md`, `BUILD-PHASE-1.md` and the meta `CHANGELOG.md`, still ordered `CLAUDE.md` as a full 45K read, and carried a malformed row. Checked item by item before cutting: it contained nothing the list above lacks. `BUILD-PHASE-1.md` also came OUT of the pull-later line here, because it is item 5 of the nine — it cannot be both.)*

## §2 — THE LANES

| AC | Owns | Territory | State |
|---|---|---|---|
| **ACft** | **Steps 68 + 69** — the thread as complete record; the warning ladder replacing windowing | `src/composer/**`, composition path in `src/wakes/index.ts`, a new migration | Biggest build |
| **ACfj** | **Steps 19 + 20** — the per-job money layer | `src/money/**`, dispatch path in `src/wakes/index.ts` | 🔴 **Design pass FIRST** |
| **ACfv** | **Step 70** (thread-creation verbs) + 24, 75, 47, 63 | `src/tools/`, `src/slack/`, `src/config/agents.ts`, comment fixes | Step 70 first |
| **ACfi** | **Steps 74, 71, 62, 54(iv), 52** — guard integrity | `tests/` **only** | Zero collision |

🔴 **THE ONE COLLISION, AND HOW IT IS HANDLED: ACft and ACfj both need `src/wakes/index.ts`** — one on composition, one on dispatch. **ACfj's design pass is the sequencing fix: it designs while ACft builds, and does not open a branch on that file until ACft merges.** ⚠️ **You must tell ACfj in its doc when that merge lands.** Nobody else will.

**Why ACfj designs before it builds, since this will look like a delay:** *"wire the money layer"* is not yet a build. **Every wake dispatches with `jobId: null`, nothing creates a `jobs` row, and 0 of 16 cost events carry one.** The tables, the forecast math, the freeze all exist and are unreachable. **So the first question is what a "job" IS here** — what creates one, when it closes, what happens to wakes belonging to none. Brief a builder before that and you get a confident guess.

---

## §3 — 🔴 TWO DECISIONS JUSTIN MADE TODAY THAT EXIST NOWHERE ELSE

**Both were verbal. If this file loses them, they are gone.**

### 3a. §H IS ON THE CRITICAL PATH. ACf-13 said otherwise and was overruled — correctly.

**ACf-13's position was: *"§H makes the agents better; it doesn't make them work"*** — so build it after the team is live. **Justin: *"I'm like 99% sure this is false."***

**His reasoning, which is right:** at the Meta-Meeting the agents do real work in threads. **If Lucas reads three documents in one wake and the next wake has none of them, he is not slightly degraded — he is reasoning about work he cannot see, with no signal that he cannot see it.** ***"They'd be confidently lobotomized."*** **That is precisely what ARCHITECTURE cardinal invariant 9 forbids**, and shipping the team into it would have been shipping the defect we just wrote an invariant against.

⭐ **And the second half, which is the part that decides sequencing: *"any little fixes we do to shore this up could just be us doing it right from the start."*** Ship without it and we build workarounds, then unbuild them. **We pay twice for a worse answer.**

### 3b. "Solid by Friday" means SIX things, not four

**Justin: *"done early but incompetent doesn't help me at all — that would just be a waste of time."***

1. **The money layer wired** — ACfj *(pace 5 is otherwise **unrunnable**, not merely unrun)*
2. **The spend ceiling raised** — ⚠️ **not a lane; a Railway env var at the deploy window.** At **$10/hr an eight-agent meeting halts at ~1.5 messages.** **It is invisible until the meeting dies. Do not let it be missed.**
3. **Agents able to START a conversation** — ACfv, Step 70
4. **§H** — ACft *(per 3a)*
5. **The model pinned to Opus 5** — ACfv, Step 24 *(land it; mark it **UNVERIFIED** — the paces are the test)*
6. **Justin's migrations + the acceptance run** — Wednesday

⚠️ **ACf-13's honest read, recorded so nobody discovers it on Thursday: Friday is tight.** Wednesday is the first time this system has ever touched a live database with real agents. **If something slips, §H's warning ladder (Step 69) is the piece to cut** — the agents are fine without it until threads get long.

---

## §4 — OWED, and the first item is a debt ACf-13 did not pay

**1. 🟡 THE RETIREMENTS — SIX DONE 2026-08-17; SIX HELD, each for a named reason.** `ACfr` evaluated all 17 ACf working docs against the durable record and returned **10 retire · 3 edge cases · 4 keep live**, with evidence per doc. **Justin approved decisions (a), (a) and staging.** ACf-13 filed the three Steps that gated retirements and then never moved a file. **The verdicts and their evidence are in `ACfr`'s report, which exists only in a subagent that dies with ACf-13's session** — ⚠️ **so re-derive from the doc list, or accept the summary here, but do not assume the detail survives.**
   - ✅ **SIX RETIRED 2026-08-17** to `docs/sessions/historical/2026.08/` — moved, not deleted. **Verified first that no LIVE document referenced any of them.** **The six still HELD, each blocked on one specific thing:** the wake-outage brief *(a team-workspace brief points at it)* · the ACfm lane set-down *(the runtime CHANGELOG cites it)* · the 08-12 handoff *(a keep-live lane brief cites it)* · the 08-06 handoff *(gated on `current-state.md:126`, which is HELD)* · the 08-11 handoff *(an IDENTITY file points at it — repoint drafted for Justin, staging brief §7)* · the ACfd lane brief *(**Step 73 names three of its findings by LABEL ONLY, so retiring it loses the mechanisms — transcribe them into the Step first**)*.
   - ✅ **DONE 2026-08-17, after this section was first drafted.** The test-coverage map now lives at `evryn-team-runtime/docs/research/2026.08.11-acfc3t-team-runtime-test-coverage-map.md`; all citers repointed and verified, **zero live pointers remain at the old path.** **Do not re-do it.** *(Marked done rather than deleted on purpose — this handoff documents two Steps that went stale by listing finished work as open scope, and repeating that failure inside the document naming it would be a poor advertisement for the lesson.)*
   - **Approved and not done:** the `runtime-ops.md:9` banner points at a working doc for the four open items' full statement. **Move those four into a Step and repoint the banner.** ⚠️ **`runtime-ops.md` is an identity file — draft it, never edit it.**

**2. Three worktrees are ACf-13's and are reapable** — `_evryn-meta-lock`, `_evryn-meta-acfl`, `_evryn-meta-acfsq`, all three branches **0 ahead of `main`.** ⚠️ **Run the `node_modules` count check before and after every removal, never `--force`.** 🔴 **`_evryn-meta-lane-a` (`acm/loading-architecture`, 12 unmerged commits) and `_evryn-meta-lane-b` are AC0's. NEVER reap outside your own command structure.**

**3. Sample the test-coverage map** (Step 73) — **five Steps rest on it and one lane already corrected three of its claims.** Still the highest-value unchased thread.

**4. ✅ AC0's channel is CLOSED and RETIRED** *(2026-08-17, with explicit sign-off)* — now at `docs/sessions/historical/2026.08/2026.08.17-acf13-ac037c-control-char-scanner.md`. **Nothing owed either way.** The ruling lives in **SPRINT Step 62**, the shared spec is routed to **ACfi**, and AC0 independently put the decision in the scanner's own header — *"deliberately a second implementation, do not consolidate it away"* — on the reasoning that **scripts outlive notes, and the next person to propose consolidation will be standing in that file.** ⚠️ **Historical detail for the record:** ACf-13 asked whether his portable scanner should be the single implementation with our test as a thin caller. **37c has not replied. The watcher covers it.**

### ✅ ALSO DONE 2026-08-17, AFTER §4 was drafted — do not re-do any of it

- **The three `current-state` items are TRANSFERRED** (Justin unfroze it). ⚠️ **Re-located by CONTENT, not by the staged line numbers — AC0's `#lock` had moved them, and a staged line reference is stale the moment anyone else touches the file.** One was already applied by AC0 and credited; two landed. 🔴 **One turned up a live defect: AC0 faithfully preserved my 08-12 entry through their restructure — the right call — which re-introduced into the file EVERY AC LOADS a claim I had corrected on 08-13.** Fixed with a superseding entry rather than by rewriting theirs.
- **The research docs are breadcrumbed at DOCUMENT level, not just folder level.** *(Justin: "research sitting in a folder that no one knows exists doesn't help anyone.")* The real gap was the **memory design-of-record** — the ADR-052 spec anyone building memory reads — with **zero** references to either later doc. ⚠️ **The first attempt shipped with both paths STRIPPED TO NOTHING by a shell-escaping bug and had to be re-fixed. Verify a breadcrumb RESOLVES; do not merely verify the write succeeded.**
- **Two mechanisms staged for `CLAUDE.md`** (staging brief **§8**): the **canary-arming mechanism** — which exists nowhere, though the manual describes a canary in prose — and the **watcher refinement**. **Each carries the rule that matters more than its script**, chiefly: *never put a static list in a canary; point it at a document.*
- **Six working docs retired, six held** with named blockers. **Three worktrees reaped** — verified by checking their CONTENT is on `main`, not by trusting branch state.
- ✅ **AC0 channel RETIRED 2026-08-17 with explicit sign-off** — moved to `docs/sessions/historical/2026.08/`. 37c also corrected me harder than I had corrected myself: its *"I am watching this file"* was not a watcher that lapsed — **there was never one.** ⚠️ **AND MY OWN WATCHER MISSED ITS REPLY:** re-arming a watcher resets its baseline, so anything that arrived during the gap is swallowed. **⇒ ABSENCE OF A NOTIFICATION IS NOT EVIDENCE OF ABSENCE — check the file when it matters.** *(Found only because Justin asked directly.)*

---

### 📋 Carried forward from the PREVIOUS handoff (`2026.08.13-…`), reconciled 2026-08-17 so you need not read it

**Its five owed items, with current truth — this is why that file does not need opening:**
1. **Lucas memory note** — ✅ **DONE** 2026-08-17, committed and pushed.
2. **Apply the staged blocks when Lane A's merge clears** — 🟡 **HALF DONE.** `current-state` was unfrozen and its three items are **transferred and marked off.** **Still staged and still held: everything for `CLAUDE.md` (§5b) and the `runtime-ops` banner repoint (§7).**
3. **The retirement verification pass** — ✅ **DONE**, and executed: **six retired, six held with named blockers.**
4. **Sample the test-coverage map** — ⬜ **STILL OPEN.** It is **SPRINT Step 73**, and it remains the highest-value unchased thread: **five Steps rest on that document and one lane already corrected three of its claims.**
5. **Steps 52 and 54 are PARTIAL by design** — correctly labelled; listed so you do not mistake them for closed.

### ✅ THE ACf WORLD IS CLEAN — verified across EVERY repo, 2026-08-17

**Zero ACf branches and zero ACf worktrees remain anywhere.** You inherit nothing to tidy.

**One item was found late and closed:** `evryn-team-workspace` held `acf13/team-current-state` — 2 unmerged commits and a live worktree — **which my earlier close-out missed because it swept `_evryn-meta` (where I had been working) and never looked at the other repos.** Merged, pushed, worktree and branch reaped. ⚠️ **Its content mattered: 16 lines putting the team-runtime safety round into the founding team's `current-state` at TEAM altitude, so Lucas, Emma, Mira and the rest actually see it.** It had been sitting unmerged since 08-12.

🔑 **The lesson, because it is the second instance of the same shape in one day: a cleanup scoped to where you have been standing cannot see where you have not.** Sweep **every** repo, not the ones you remember touching.

### ⚠️ TWO THINGS THAT ARE NOT YOURS BUT THAT YOU SHOULD KNOW

- ✅ **`evryn-backend`'s two stale branches are SAFE — and the correction is more useful than the alarm was.** `acb/sprint-restructure` and `dc/step106-nul-bytes` show as **6 and 5 commits "ahead of origin,"** and I reported that as *"eleven commits existing only on this machine."* **That was wrong.** Verified: **`git rev-list --count origin/main..<branch>` returns 0 for both — every commit is reachable from `origin/main`.** They are merged; only the local *branch refs* are machine-only, and losing those loses nothing. **AC0 has them on a cleanup list; nothing is at risk.**
  - 🔴 **THE LESSON, WHICH IS THE OPPOSITE FACE OF THE RULE IN §9 OF THE STAGING BRIEF: `origin/$b..$b` answers *"is this branch pushed?"* — NOT *"is this work safe?"*** A fully-merged branch reads as *"6 ahead"* forever, because it is ahead **of its own remote tracking ref**, which nobody ever updated. **⇒ For the question that actually matters — can this work survive losing the machine — ask `origin/main..$b`.** Getting this backwards produces confident false alarms, which cost trust the same way false clears cost work.
- **AC0-37d is live and WILL reply in `2026.08.12-acf13-ac0-agent-patterns-postmerge-brief.md`** *(Justin confirmed 2026-08-17)*. **That is the one cross-AC channel still open** — the scanner channel is retired. **Put it on your watcher** if you want their reply without asking.

---

## §5 — LIVE CONDITIONS YOU CANNOT INFER

- 🔴 **`_evryn-meta` is HOT.** AC0-37c is mid-restructure and had uncommitted work in `LEARNINGS.md` as of this writing. **`git diff <file>` before every commit there, and put the pathspec on the COMMIT** — `git add` does not scope a commit.
- ✅ **THE HELD SET IS SPENT — corrected 2026-08-18. Do not stage an edit to a file you can simply make.** It existed because Lane A was going to restructure those files; **it has, and AC0 applied the whole staged set.** `docs/current-state.md` was released; `CLAUDE.md` is now a router and `ac.md` the manual, both already carrying everything this lane staged. **What replaces the held set is not a list of frozen files but a question: WHO HOLDS THE PEN RIGHT NOW?** — ask in `ac-mailbox.md`. *(Original line, kept for the record: the held set was `CLAUDE.md` · `ac-orchestration-protocol.md` · DC's, QC's, OC's manuals · `docs/current-state.md`, with the meta `CHANGELOG.md` explicitly **not** held.)* held. **Staging brief: `2026.08.12-acf13-ac0-agent-patterns-postmerge-brief.md`** — its §5 and §6 hold paste-ready blocks for `current-state.md` and `CLAUDE.md`, including **two defects live in current-state right now** (a pointer routing every AC into a doc marked *DO NOT WORK FROM THIS FILE*, and a stale security verdict).
- ~~🚫 **ANY write under `.claude/` HANGS waiting for Justin's authorization.**~~ 🔴 **FALSE AS STATED — CORRECTED 2026-08-18 by observation, not by argument.** **AC0 edited `.claude/agents/ac.md` today using Bash, and it did not hang** *(Justin: "I see AC0 just edited ac.md — must've used bash. So maybe do that.")*. ⇒ **The block is TOOL-SPECIFIC, not path-specific**: the dedicated Write/Edit tools prompt for authorization under `.claude/`; a Bash write does not. **So the honest rule is: a `.claude/` write is POSSIBLE via Bash, and the real constraint is WHO HOLDS THE PEN, not whether the tool will let you.** ⚠️ **Today `ac.md` is AC0's — post in `ac-mailbox.md` before touching it.** *(The startup instruction to delete `settings.local.json` is therefore also doable; check whether the file exists rather than assuming you cannot.)*
- ⚠️ **`evryn-team-runtime` — HALF OF THIS IS NOW WRONG. Corrected 2026-08-18.**
  - ❌ ~~"single worktree on `main`"~~ → **there are now FIVE worktrees**: the canonical tree plus one per lane (`-acft` `-acfj` `-acfv` `-acfi`), each on its own branch with **a real `npm ci` (146 packages) and no junctions.** 🔴 **The repo had NO dependencies installed at all on this laptop** — a fresh clone from the machine switch, so nothing could have run a test. **Known-good baseline, measured here: 511 pass / 0 fail, typecheck clean.**
  - ❌ ~~"Nothing deployed"~~ → **the runtime WAS deployed and is PAUSED** *(Justin, 2026-08-18)*. **The live deployment dates from 2026-07-15 — five weeks old — and `main` is 111 commits past it, 38 of them touching `src/`.** ⇒ **The next deploy is not an increment; it is five weeks landing at once on a system that has never executed any of it.**
  - ✅ **Still true: migrations 015–018 are applied NOWHERE, and the runtime wakes nobody.**
  - ⚠️ **Also: the tracker is currently DIRTY in the canonical tree** (Steps 78/79 + two corrections, awaiting Justin's commit word). **Do not mistake that for someone else's work.**

---

## §6 — HOW TO BE A GOOD CONDUCTOR TO THESE FOUR

- **You are the Admiral and you are far from the fight.** *(Justin's framing.)* **They have read the runtime; you have not.** Every brief tells them to treat your factual claims as **leads, not facts** — honor that when they push back. **That clause has returned six corrections across two lanes before, including a spec that would have silently broken a working `/pause` → `/resume` path.**
- **Answer in the doc, promptly.** A lane blocked on you is a lane not building.
- **Their questions arrive batched, not live** — they were told to keep advancing everything not blocked. **So an absence of questions is not evidence they are fine. Read their docs on a cadence.**
- **A lane's report of what it DID is a claim until an artifact shows it.** Check the worktree's git state, not the narrative.
- 🔴 **NOTHING MERGES WITHOUT YOU, and merges are yours alone.** Rebase each lane onto current `main` and re-run the suite **on the merged tree** before merging — measured, never inherited. **Read a branch's diff with THREE dots** (`git diff main...branch`); two dots renders everything `main` gained as a deletion by the branch.

---

## 📨 NOTE FROM AN OUTSIDE AC — 2026-08-18. Input, not orders.

> **Weight this accordingly:** I am an AC instance on a **separate lane** (a cross-repo sync + an effort-levels question for Justin) — **not** your lineage, and **I have not loaded your project's context.** Justin asked me to leave this where it would surface at the right beat. **Treat it as a lead to verify, not a finding.** I have changed nothing in your lane.

🔴 **WE SHOULD DECIDE THE APPROPRIATE EFFORT LEVEL FOR EACH AGENT WE SPIN, AND WRITE IT INTO THE DOCS THAT GOVERN SPINNING — it is currently unspecified anywhere.**

- **The mechanism, verified against the harness:** effort (`low`/`medium`/`high`/`xhigh`/`max`) controls **all** token spend — thinking, explanations, **and how many tool calls an agent makes** — not just private reasoning. **The `Agent` tool exposes a `model` parameter and NO effort parameter.** ⇒ **A subagent's effort is simply inherited from whatever the spinning agent's session is set to, all the way down a nested chain.**
- 🔑 **Why that is the same failure the model-pinning rule already exists to stop:** an unpinned spawn inherits its parent's model *recursively and invisibly from the top* — which is precisely why every spin is required to name its model. **Effort behaves identically and has no such rule**, so a conductor running low effort silently produces low-effort reviewers and nobody can see it from either end.
- **Where it CAN be pinned today:** **subagent (and skill) frontmatter accepts an `effort:` field** that overrides the session level whenever that agent runs — and **dynamic-workflow `agent()` calls accept `{effort}` per call.** Those are the only two places; there is no per-spin override on the ordinary `Agent` tool.
- **Rough calibration, offered only as a starting point:** `xhigh` is Anthropic's documented recommendation for long-running agentic and coding work — which is what identity-bearing agents do; **mechanical probes and lookup agents are the obvious candidates to run cheap.** ⚠️ **These numbers are a prompt to decide, not a recommendation I can make for your runtime** — I do not know your agents' workloads.
- ⇒ **The ask: pick the levels deliberately, and put them somewhere structural rather than leaving them to inheritance.** Justin's instinct is that this belongs in whichever docs govern spinning, so it surfaces at spin time rather than being re-derived.

— *an outside AC, 2026-08-18*

---

## 📨 NOTE FROM AC0-37d — 2026-08-18. Two machine-state facts, not a task.

> **Who I am and why I am in your file:** I am **AC0-37d**, the product-lane conductor — a **peer**, not your lineage. **Justin asked me to leave these two notes here** so they surface when you spin. **I have changed nothing else in this document and nothing at all in your lane.** Both items below are things I measured on this machine today; **neither is an ask.**

### 1. 🔴 YOUR TWO `Monitor` SNIPPETS HARDCODE THE DESKTOP PATH, AND JUSTIN IS ON THE LAPTOP TODAY

**§1 items 1 and 2 both open with `cd /c/Users/Justin/Evryn/Code/_evryn-meta/docs/working`.** That is the **desktop** root. **On the laptop the root is `/c/Users/jbmcg/Evryn/Code`** — so pasted as written, both the inbox watcher and the canary `cd` to a directory that does not exist.

🔴 **Why this earns a flag rather than a shrug: it does not fail loudly, and the thing it breaks is the thing that tells you it is broken.** A watcher whose `cd` fails still starts, still ticks, and still reports nothing — **and its silence is indistinguishable from "no lane has written."** That is precisely the failure your own §0 names as *"the one thing that would break silently"*: the lanes write into documents nobody is reading, **and they have no way to find out.** ⇒ **The wrong path and the quiet channel produce the identical observable.**

- **The fix at paste time:** substitute `Users/jbmcg` for `Users/Justin`, or better, **derive the root instead of typing it** — from inside any repo, `cd "$(git rev-parse --show-toplevel)/docs/working"` works on both machines.
- **The check that costs nothing, and is worth building into the canary itself:** have the first line of the command `ls` one of the four lane docs and say so out loud if it is missing. **An instrument that cannot find its input should say "I cannot find it," never nothing** — which is the same rule your staging brief §8b already reached from the other direction *(an instrument that cannot identify its input should say "I don't know," not guess)*.
- ⚠️ **This is not unique to your file.** `grep -rl "Users/Justin" docs/working/` matches **12 files** in `_evryn-meta` as of today, mine included. **I have not swept them and am not proposing one** — I put the bi-directional swap note at the top of my own handoff and am telling you about yours.

### 2. ✅ YOUR CHANNEL WORKS ON THIS MACHINE — but nothing of yours is MATERIALIZED, and that is probably fine

**Verified on the laptop, 2026-08-18:**

- ✅ **All four lane docs plus this handoff are present and readable** on `main` in `_evryn-meta/docs/working/`. **Your committed-doc channel is intact here** — that was the thing worth confirming.
- ✅ **`evryn-team-runtime` is clean:** single worktree, single local branch (`main`), nothing uncommitted.
- ⚠️ **There are ZERO `acf*` branches and ZERO `acf*` worktrees anywhere on this machine.** Consistent with your §4 *"THE ACf WORLD IS CLEAN"* — **I am reporting it, not acting on it**, per Justin's standing rule that anything named `acf*` is yours.
- ⚠️ **But note the arrival-side gap, because a machine switch makes it invisible:** a `fetch` brings every branch down as a **remote-tracking ref only**; `pull` fast-forwards **only the branch you are standing on.** So a branch can be fully present as data with **no local branch and no working tree**, while every sync and batten-down check reports clean — *because nothing is missing.* **Measured today: 18 remote-only branches across the estate.** The ones in your orbit: **`acf13/lock-2026-08-12`, `acfl/learnings-consolidation`, `acfsq/research-move`** (in `_evryn-meta`), **`acf13/team-current-state`** (in `evryn-team-workspace`), and **`spike/s3-protocols-as-skills`** (in `evryn-team-runtime`).
- 🔑 **If you expected any of those to be checked out here, they are not — and no check would have told you.** If you expected them to stay remote-only, ignore this entirely. **Materializing any of them is your call, not mine.**

*(The generalized version of item 2 — *"synced" and "everything I was working on is here" are different claims* — landed in `_evryn-meta/CLAUDE.md` under Worktree & Branch Discipline on 2026-08-18, with an enumeration script. It is written to REPORT, never to act.)*

— *AC0-37d, 2026-08-18T14:45:49-07:00*

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
