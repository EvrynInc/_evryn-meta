# Deploy Handoff — AC0 → next AC0 (2026-06-29, for the DESKTOP)

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** START-HERE for the AC0 who picks up after the round-2 deploy. Round-2 v0.2-hardening is **DEPLOYED + LIVE**; this hands off (1) the machine-sync you must do FIRST on the desktop, (2) the next real work (assemble the next batch), and (3) a checklist of low-priority cleanup — some of which the authoring AC0 is knocking off and checking off here as it goes. Read `docs/current-state.md` (the 2026-06-29 stamp = current truth) alongside this. Working/ephemeral; retire after the next batch ships.
>
> *Authored 2026-06-29 by AC0 (on the laptop, post-deploy).*

---

## ⚠️ STEP 0 — MACHINE SYNC FIRST (you're on the desktop; the deploy happened on the laptop)

The deploy was done on Justin's **laptop**. The **desktop is behind.** Before ANY work, sync every repo, and confirm each is on its canonical branch == origin, clean. **Do not skip — the 2026-06-17 lobotomy was half a stale-machine failure.**

```bash
cd /path/to/Evryn/Code
for d in _evryn-meta evryn-backend evryn-dev-workspace evryn-quality evryn-ops \
         evryn-team-workspace evryn-team-agents evryn-website \
         evryn-langgraph-archive evryn-prelaunch-landing; do
  [ -d "$d" ] && git -C "$d" fetch origin && echo "$d -> $(git -C "$d" branch --show-current)"
done
# Expected: all 'main' EXCEPT evryn-team-agents -> master (frozen).
```

Then `git pull` each onto its canonical branch and verify `local HEAD == origin`. **Specifically:**
- **`evryn-backend`** — pull `main` to **`d198ef1`** (the deployed round-2 code). This is the big one.
- **`evryn-ops`** — Justin flagged this explicitly: make sure it's properly on `main` and pulled on the desktop (it had a stale-fork history; confirm `branch --show-current` == `main` and `HEAD == origin/main`).
- **`_evryn-meta`** — pull `main` (carries this handoff + the #lock persistence).
- The desktop will NOT have the laptop's local junk branch refs or the locked `evryn-backend-bundle/` folder (those are laptop-only — ignore them; see cleanup list).

Anything that fails the check → surface to Justin, don't work around it.

---

## Where it stands (one paragraph)

**Round-2 v0.2-hardening is DEPLOYED + LIVE in prod** (`main` `d198ef1`, `railway up` SUCCESS 2026-06-29, clean boot). Integration-QC was GO (full-load, clean, zero blockers). Live now: **M1 silent-death safety layer** (circuit-breaker + clean halt + runaway detectors + Tier-B alert-only + daily watch-the-watchman), **Step-61 gatekeeper-address resolution** (ADR-046), **lean Reflection** (ADR-043, live-inert until a user enrolls), **Lane-B operator/approval hardening**. 4 prod migrations applied + verified (pre/post `pg_dump` in `evryn-backend/backups/oregon-{pre,post}-r2-deploy-2026-06-29.sql`). **Healthchecks.io heartbeat (~15min grace) + daily-affirmation (~26h) LIVE.** Railway env set: `HEALTHCHECK_URL`, `HEALTHCHECK_AFFIRM_URL`, `PROACTIVE_CHECK_HOUR_PT=10`; `AFFIRM_HOUR_PT=7` **staged** (self-applies at the next deploy — affirmation moved earlier so the channel-health check confirms before other alerts).

---

## 👉 THE NEXT REAL WORK — assemble the next batch

Justin's stated next move: **assemble a fresh batch of the next-most-important hardening items and knock them off until the v0.2 sprint is done — then a full live-test, then Mark.**

- Source the batch from **`evryn-backend/docs/SPRINT-V0.2-HARDENING.md`** (the `TODO` / `IN PROGRESS` Steps) + **`SPRINT-V0.3-CANDIDATES.md`**. Scan the status-prefix left edge — the `DONE`→`TODO` boundary is the current state.
- Note the **DONE-status reformat is owed** (the round-2 Steps are LIVE but still carry build-time detail) — see the cleanup list; you may want to do that reformat as you scope the batch so the sprint reads clean.
- Spin DC/QC per `docs/protocols/ac-orchestration-protocol.md` (paste the verbatim blocks — the standing `<task>` load callout is the proven fix; don't paraphrase). The integration-QC brief from this deploy (`docs/working/2026.06.29-ac0-qc-r2-integration-bundle-brief.md`) is a good shape reference.
- **Poll cadence:** leave `POLL_INTERVAL_MS=10000` (10s test cadence) as-is through the next batch; **slow it to ~30s at the next batch's live-test, before Mark** (Justin's call — the live-test is the moment).

---

## ✅ Cleanup checklist (low-priority — authoring AC0 is checking these off post-handoff; rest left for you / future ACs)

**Being done by the authoring AC0 right now (will be checked off + committed before set-down):**
- [ ] Commit the integration-QC brief into git history (rides the `_evryn-meta` #lock commit).
- [ ] `/sessions` + `/working` cleanup — verify everything is from this deploy thrust; retire the spent round-2 working docs (convergence-plan, persistence-checklist, lane handoffs, the 6/25 handoff, build specs) into `docs/sessions/historical/`; keep any still-useful working doc.
- [ ] Reorganize `docs/sessions/historical/` into month folders (`2026.06/`, etc.) — it's gotten deep.

**Left for the desktop AC0 / future ACs (Justin: doc-weaving is "another AC on another date"):**
- [ ] **Re-pull `evryn-backend/docs/schema-reference.md`** from prod (now migrated — it currently lacks the round-2 tables/columns).
- [ ] **SPRINT DONE-status reformat** — rewrite the round-2 Steps to `DONE — **bold title** — salient`, strip lane/branch refs (noise post-reap).
- [ ] **ARCH alignment pass** — weave the consolidated "v0.2 Round-2 Hardening — Architecture Absorptions" section into its home sections (Pipeline / Data Model / System Actors / Security / Memory Architecture). Big chunk currently sits consolidated; a dedicated AC does this.
- [ ] **operator-guide cleanup** — the `QUIET_START/END_HOUR_PT` rows still describe the retired notify_queue queue-and-replay model; the `HEALTHCHECK_*` env rows say "(set it)" but they're now SET; date-stamp drift. (QC flagged: worth landing before Mark relies on the guide.)
- [ ] **`emergency.ts` `bypassQuietHours` JSDoc comment drift** (SPRINT Step 67b) — a DC code-comment trip.
- [ ] **CHANGELOG May → `docs/historical/changelog-historical/`** monthly split.
- [ ] **EVR-72** (Linear) — reframed/resolved at v0.2; update the ticket (Soren-owned).
- [ ] **Mira voice-polish** owed on Lane A's surface-to-Operator clause (rides a follow-up).

**Laptop-only leftovers (moot on the desktop — for the record):**
- The `evryn-backend-bundle/` folder is still physically on the **laptop** disk (git de-registered it, but a Windows file-lock blocked the `rm`). Delete it manually when the lock clears, or ignore (you're on desktop now).
- Laptop-local **unmerged junk branch refs** kept after worktree removal (round-1 `lane-a/ingest-resilience`, `lane-b/operator-approval`, `lane-c/cost`, `m1/silent-death-stage2`, and the redundant `dc/r2-step32-84-comments`) — worktrees removed, refs kept for safety. Force-delete (`git branch -D`) once confirmed junk; they won't exist on the desktop.
- Origin still has the merged `r2/*` lane branches — can be pruned on origin (`git push origin --delete <branch>`) any time; harmless.

---

## Open notes / answered questions

- **"M1" naming (Justin asked):** it's a tracking label that stuck ("M1 = silent-death detection," the one go-live blocker the approval gate can't backstop). No documented expansion of the "M." **Recommendation: don't rename** — it's embedded everywhere (ADR-041, env discussion, the "M1 alive" affirmation string, operator-guide); keep "M1" as the internal code-name and lean on *"silent-death safety layer"* in human-facing docs (ARCH already does). Not worth the churn.
- **`AFFIRM_HOUR_PT=7`** is staged, not yet applied — self-applies on the next `railway up`. No action.
- **No `.env` change** this deploy (env went to Railway, not `.env`) — no Bitwarden re-upload needed.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
