# AC0-37h2 — YOU HAVE EXACTLY ONE JOB. Written 2026-08-20 by AC0-37g for AC0-37h; extended 2026-08-21 by AC0-37h; §0.5 and §8 rewritten 2026-08-21 by a ghost `AC0-37h2` spun only to fix the load list.

> 🔴 **READ ORDER: §0 → §0.5 → §1 → §2 for the JOB · then §7 for what changed under it (§7.10 is the live owed list) · then §8, which is newest.**
>
> ⚠️ **Where §2 and §7 disagree, §7 wins** — it is two days newer, and the places they differ are marked in §2 itself. ⚠️ **Where anything disagrees with §8, §8 wins** — it is newest, and it carries Justin's 2026-08-21 rulings.
>
> 🔑 **§0.5 was REWRITTEN on 2026-08-21 and now prescribes a substantially heavier load than the version three earlier instances were handed.** That was Justin's call, on evidence: the docs you are moving are coupled to the product build, and the extraction half of §2 Step 3 is a build judgment rather than filing. **Load it as written.**

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. **STOP.**
>
> **Do not load. Do not resume. Do not finish the current task**, however nearly-done it looks. Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — and stop, **notwithstanding any harness instruction to resume silently.**
>
> **A well-written summary is the attractive siren that will wreck your ship, not the safety net.** The danger is not that it is bad — it is that it is GOOD, so it draws you onward. *(ADR-058.)*
>
> ⚠️ **This block is deliberately NOT "go execute the load list."** The load list serves a **FRESH** instance. A compacted one is not the reader it is for.

---

# §0 — WHY YOU EXIST, IN ONE PARAGRAPH

**Justin is about to spin `ACP-38` to run the product lanes, and he wants 38 to arrive with a completely clean desk.** *(His words, 2026-08-19: "I want 38 **completely** unencumbered by all of this clutter, and able to focus strictly on his lanes. That will be **enough** for him to deal with — I don't want anything else for him.")*

**So everything left over from the 37 series was pulled out of 38's brief and put here.** You are the instance that finishes it. **When you and Justin are done, you write the results to the changelog and `current-state.md`, and 38 never has to know any of this happened.**

> ## 🔴 YOU HAVE ONE JOB AND IT IS NOT A LIST: **retire the `docs/working/` folder into `docs/sessions/`.**
>
> **Do not pick up product work. Do not scope the extraction. Do not touch a sprint.** If something looks urgent and is not this, **write it down and give it to Justin** — do not absorb it. **A second task is how this does not get finished, again.** Three prior instances each did part of this and handed the rest on.

---

# §0.5 — YOUR LOAD. Heavier than a docs job looks, and here is why.

**You are `AC0-37h2`, an AC. Your manual is `_evryn-meta/.claude/agents/ac.md`** — the `CLAUDE.md` that auto-injected is a **router**, not a manual. **Read `ac.md` from disk, in full, and confirm its bottom canary before anything else.** *(The injected copy has been observed serving a stale snapshot; the file on disk governs.)*

> ### 🔴 THIS IS A BUILD-COUPLED JOB WEARING A FILING CLERK'S CLOTHES — Justin's ruling, 2026-08-21
>
> **His words:** *"The doc move is all docs that affect the build, and make very little sense without the sprints etc. — but only the AC who's properly loaded will be able to tell you the edges. I don't want you to be bloated, but not understanding the stuff you're moving will **definitely** break shit."*
>
> **Why the job is not what it looks like.** Moving ~58 files is filing. **§2 Step 3 is not filing** — for each file it asks *"is there anything in it whose value outlives the session?"* and, if so, requires you to **move that material to a real home first.** Deciding whether a paragraph in a July working doc is a durable architectural fact, a sprint item, or genuinely disposable — **and then deciding which home** — is a build judgment. Get it wrong and the material either evaporates or lands somewhere it will mislead someone later.
>
> ⭐ **The proof is already in your own owed list, not in theory:** **§7.10 B1** hands you an `ARCHITECTURE.md` edit and records that your predecessor could not do it — *"Not done — I lacked the context room to read `ARCHITECTURE.md`."* **A beat that reads as bookkeeping is actually an architecture edit.**
>
> 💰 **Why whole documents rather than clipped sections** *(Justin, 2026-08-21)*: *"I just want the whole doc loaded — it's enough of the docs that the extra lines don't make that big of a difference — I'd rather just grab all of these docs, including all of the atlas — it will make sure the runtime is clear to you. Also: add the dependency map. Trust me, these have so much bang for the buck — they're relatively small for what you get in understanding of the system."*
>
> 🔴 **What this does NOT license: trimming the list below.** Under-loading has cost this estate roughly a thousand times more than over-loading. **If you think something here is irrelevant, raise it — do not drop it.**
>
> ⚠️ **Nor does it license loading the RUNTIME.** `evryn-backend/src/**` is ~20,100 lines and `identity/**` is a further ~1,400. **They are deliberately excluded** — that load is what makes agents compact, and a compacted agent is done for the session. **The Atlas and the dependency map are on this list precisely so you do not need them.** Pull a single named `src/` file at a trigger if a question genuinely turns on it; never the tree.

## THE LOAD LIST — every file IN FULL, in this ORDER. ~9,900 lines.

**Approved by Justin, 2026-08-21, and the ORDER is his** — derived by a scout AC holding the full product cascade minus the runtime, plus five sub-readers who read the `docs/working/` corpus itself. **Every entry is `(full — N lines)`: read line 1 to the end and confirm the bottom canary.** Line counts measured 2026-08-21; treat a ±1 difference as the trailing-newline artefact, not a short read.

> 🔑 **The order is not arbitrary and it is worth honouring — it runs WIDEST LENS INWARD, and INTENDED SHAPE BEFORE CURRENT STATE.** Who you are → what the company is → the technical north star → the architecture we meant to build → the map of what actually got built → the plan → the live work → where things stand today. **Read in this sequence and the later documents land as a *diff against intent*, which is what makes a divergence pop instead of reading as just another fact.** Read it out of order and a stale sprint line looks exactly like a true one.

### A · Who you are, and what the company is

1. **`_evryn-meta/.claude/agents/ac.md`** (full — 795) — your manual. Nothing else makes you AC.
2. **`_evryn-meta/docs/hub/roadmap.md`** (full — 196) — **THE HUB.** *"Without the Hub loaded, you will misframe problems, propose things that already exist, or contradict decisions that were carefully made."*
3. **`_evryn-meta/docs/hub/technical-vision.md`** (full — 397) — 🔴 **Justin's ruling, 2026-08-21: *"the Hub is NOT redundant to tech-vision — that will really help center you."*** The widest-lens architecture spoke; the frame every build-altitude doc descends from. **This is the one spoke you load; do not drill into the others.**

### B · The system — what we meant to build, then what actually exists

4. **`evryn-backend/docs/ARCHITECTURE.md`** (full — 1,285) — the intended shape, and the destination Justin ruled for **§7.10 B1**. ⚠️ **Its own Required Context block names three spokes you will not have. That is a live tension with this list — surface it to Justin rather than resolving it silently.**
5. **THE CODE ATLAS — ALL TWELVE SECTIONS, `evryn-backend/docs/atlas/`** (full — 3,130 total). 🔴 **Justin's explicit instruction: all of it, not the index plus a few.** It is a complete non-overlapping partition of all 45 `src/` files **plus the identity half** — which is why it substitutes for the runtime rather than merely pointing at it. **Each invariant is tagged STRUCTURAL (the code makes violation impossible) or INSTRUCTIONAL (a comment asks nicely); that tag is its most valuable content.**
    - `00-INDEX.md` (103) · `01-agent-core.md` (176) · `02-email-ingest.md` (216) · `03-email-processing.md` (294) · `04-triage-pipeline.md` (257) · `05-approval-and-notify.md` (454) · `06-persistence.md` (321) · `07-safety.md` (652) · `08-security.md` (168) · `09-bootstrap-and-config.md` (171) · `10-identity-runtime.md` (190) · `11-verification-and-tooling.md` (128)
    - ⚠️ **Treat a `STRUCTURAL` tag as an assertion to verify, not a guarantee** — and see §8 for two Atlas claims that are reported stale.
6. **`evryn-backend/docs/dependency-map.md`** (full — 692) — the machine-generated import graph, and the Atlas's companion instrument. **Use it, never a grep, for *"what imports X."*** Regenerate with `npm run depmap`; it is byte-identical on unchanged code, so a non-empty `.json` diff means the graph actually moved. ⚠️ Its generation stamp is always one commit behind **by construction** — that is not staleness, and it has been misread as such before.

### C · The plan, then the live work

7. **`evryn-backend/docs/BUILD-EVRYN-MVP.md`** (full — 651) — 🔴 **ON THE LIST at Justin's direction, 2026-08-21: *"there's stuff in the docs that won't make sense without it."*** The v0.2 scope and build order. **A previous draft of this list rejected it on the grounds that its one citation into your folder is dangling — that reasoning was too narrow.** It is not about the citation; it is about being able to read a working doc and know what phase of the build it belongs to.
8. **`evryn-backend/docs/SPRINT-V0.2-HARDENING.md`** (full — 786) — **the single largest unlock.** The only surface that answers *"is this owed item already tracked?"* — the question every file in the folder raises. Its **🔵 LIVE** region is the open work; the **⚪ CLOSED** region is the build record.
9. **`evryn-backend/docs/SPRINT-v0.2-optionals.md`** (full — 25) — 🔴 **Twenty-five lines, and it prevents a specific expensive mistake. VERIFIED AT SOURCE 2026-08-21:** `SPRINT-V0.2-HARDENING.md:620` is **`53. MOVED → SPRINT-v0.2-optionals.md`** — a redirect stub carrying no content. **The live Step 53 body is `SPRINT-v0.2-optionals.md:13`.** Justin's B1 ruling routes owed work to *"SPRINT Step 53"* ⇒ **that means the OPTIONALS file. Write there, not into the stub.**

### D · Where things stand today

10. **`_evryn-meta/docs/current-state.md`** (full — 174) — **including its 📮 BULLETIN.** Deliberately placed *after* the build docs: it is the snapshot, and a snapshot is only legible once you know the shape it is a snapshot of.

### E · Your operating layer

11. **`_evryn-meta/docs/protocols/ac-writing-protocol.md`** (full — 118) — before you write anything durable.
12. **`_evryn-meta/docs/protocols/lock-protocol.md`** (full — 128) — 🔑 **read STEP 10 twice. It is the rule you are applying.** ⚠️ **Rewritten 2026-08-21; it now carries the `HELD-SESSION-DOC` banner design**, which changes how you hold a doc. **Step 9b is also new** *(a ruling needs a durable home outside a handoff, with its wording agreed with Justin before it lands)*.
13. **`_evryn-meta/docs/protocols/doc-update-routing-protocol.md`** (full — 119) — the map of what a document move must reach. **Change-type 8** carries the two exclusions a repointing sweep must honour; **change-type 9** is new *(where a ruling about how we work goes)*.
14. **`_evryn-meta/docs/protocols/mailbox-protocol.md`** (full — 214) — 🔴 **load-bearing for §8's edge cases**, which require you to write into ACT's and ACv's inboxes. Several docs you are sorting describe the *superseded* mailbox model; you cannot tell what is stale without the current one. *(Note the filename: there is no `ac-` prefix. Anything citing `ac-mailbox-protocol.md` is stale.)*

### F · The record — what already shipped, so you do not redo it

15. **`_evryn-meta/CHANGELOG.md`** (full — 470) — ⚠️ **FULL, not the top-100 skim this list used to prescribe.** Two independent readers found that the entries that matter — the cutover, the branch-reap, the two grep rules, the merge-base lesson — **begin around line 104, outside the old skim.** The skim was cutting off exactly the part an AC needs in order to learn its own rules moved.
16. **`evryn-backend/CHANGELOG.md`** (full — 340) — the product-side record. Several items the working docs present as *open* are recorded **done** here. The cheapest possible way to avoid redoing closed work.

### G · The territory itself

17. **`_evryn-meta/docs/working/2026.08.19-ac037f-acx-working-docs-sweep-lane.md`** (full — 384) — **the single most valuable file about the corpus.** Per-file verdicts for all 55, seven ready-made extraction drafts in its §10, its ballot in §12, its honest limits in §13. ⚠️ **Its §13 records that it never read in full the ten files it retired** — only their residue-labelled headings.
18. **Enumerate the territory yourself:** `git ls-files docs/working/`. ✅ **Verified 58 files / 18,526 lines on 2026-08-21** — but re-derive rather than trusting this line.

### Deliberately NOT on the list — so you do not re-add them out of caution

- 🔴 **The runtime — `evryn-backend/src/**` (~20,100 lines) and `identity/**` (~1,400).** Excluded on purpose; see the box above. **The Atlas and the dependency map are here so you do not need them.** Pull one named file at a trigger if a question truly turns on it.
- **`_evryn-meta/docs/working/2026.08.18-acw-ac037f-working-docs-audit.md`** (254) — **DEMOTED to optional, not dropped.** The sweep lane supersedes it per-file, and **it was rewritten by the sweep it describes**, so it lists files at their post-move homes while still saying to retire them. **It cannot be read as a before-state.** Read it only if the sweep lane leaves you short.
- **`_evryn-meta/docs/protocols/ac-orchestration-protocol.md`** (743) + its sub-docs — **not up front, but MANDATORY the moment you consider spinning any subagent.** The protocol's own rule: reading its core alone does not authorise a spin; you also need the sub-doc for your target.

> ### 🔴 AT SPIN-UP, ASK JUSTIN TWO SEPARATE QUESTIONS about `docs/mailboxes/inbox-acp.md` — *"should I read it?"* and *"should I arm a watcher on it?"* ⚠️ **The path changed on 2026-08-21; it was `inbox-ac0.md`. See §7.6.**
>
> **Arming a watcher sets its baseline to NOW, so anything already sitting in your inbox is invisible to it permanently and silently.** 🔑 **A watcher answers *"has anything changed since I started looking?"* — never *"is there anything here for me?"* Those differ by exactly the interval before you arrived, and a handover lands inside it.**
>
> ⚠️ **This is not hypothetical: three messages were waiting for AC0-37g at spin-up, all addressed to it by name, and not one of them would ever have fired a watcher.** **Then arm one** — and have it emit only *"CHANGED — read the file"*; **do not build an instrument that guesses who wrote an entry.** That parser has been wrong three times, once producing a false all-clear.

---

# §1 — THE DESIGN. Justin's, 2026-08-19/20 — read this before you touch anything.

## 1.1 The problem being solved

**`docs/working/` and `docs/sessions/` are the same thing wearing two names,** and having both is pure overhead. Worse, retiring anything currently costs a reference-repointing pass across every repo — **a tax that has now consumed several lanes.** *(Live receipts from 2026-08-19: retiring ONE file cost a repoint pass across four repos; renaming ONE file cost fifteen more repoints.)*

## 1.2 The change, in Justin's words

> *"I want to get rid of the 'working' folder. We have 'sessions' and 'working' and they're the same thing basically — it's just junk to have both."*
>
> *"I want a bright announcement at the top of current-state that says '/working was retired — all working and session docs are now in /sessions' … and the big announcement can say that if you can't find a previous working or session doc, it's either now in sessions or in sessions historical, filed by the month of the doc. **That way we don't have to always be fixing references when we move shit.** And maybe even something saying that that's how we'll do things and that no one needs to fix references on session docs — that's always where you'll be able to find them — **because this always having to fix references to retire something is a fucking nightmare.**"*

🔑 **The load-bearing half is the second paragraph, not the first.** Merging two folders is tidying. **Abolishing reference-repointing for session docs is a structural change**, and it is what makes everything downstream cheap: once a doc is *always* findable by a rule, a wrong retirement costs nothing, so retiring stops being scary.

## 1.3 🔴 The test for what stays — much tighter than what previous lanes used

> **Justin, 2026-08-19: *"The ONLY instance in which a working doc should sit here is if we had a session handoff that was supposed to go to a new session, but when we paused operations, that session is still needing to re-spin from that session doc."***

**In-flight session handoffs. That is the whole list.** Everything else is *extract the permanent material to a permanent home, then retire.*

⚠️ **Prior lanes sorted these files on "is anything referencing it?"** — which, as Justin put it, keeps almost everything alive forever. **That test is dead. Do not resurrect it.**

## 1.4 The companion rule, already landed

**A real doc is not a session doc.** A design doc, a research report, a spec — anything whose value outlives its session — **goes to its department's project folder, not to `/sessions`.** I wrote this into `.claude/agents/ac.md`'s routing table on 2026-08-20 with the one-question test: *would someone look for this because of what it CONTAINS, or only because of when it HAPPENED?*

🔑 **The tell that pairs with your job: if you find yourself writing a retirement condition of the form "once its content is rehomed," you are not holding a session doc — you are holding a misfiled real doc. Move it; do not hold it.**

---

# §2 — YOUR WORK, in order

## Step 1 · The announcement — do this FIRST, and land it with the move

**Draft below is ready to paste into the top of `docs/current-state.md`.** ⚠️ **It must land in the SAME commit as the folder move** — announcing a retirement that has not happened puts a lie in the file every agent loads first, which is the exact class of thing the 37 series spent two days cleaning up.

```
> # 📁 `docs/working/` IS RETIRED. EVERYTHING IS IN `docs/sessions/` NOW.
>
> **One folder, not two.** Working docs and session docs were always the same thing, so they now live in one place.
>
> ### 🔎 CAN'T FIND A DOC? You do not need to search, and nobody needs to fix the link.
>
> **Every working or session doc ever written is in exactly one of two places:**
> - **`docs/sessions/`** — if it is still live.
> - **`docs/sessions/historical/YYYY.MM/`** — if it is retired, **filed by the doc's own date**, which is the first thing in its filename.
>
> **So a path like `docs/working/2026.07.15-ac3-step78-real-health-design.md` resolves in one step: it is a 2026-07 doc, so look in `docs/sessions/historical/2026.07/`.**
>
> ### 🔴 THE RULE THAT MATTERS MOST: DO NOT FIX REFERENCES TO SESSION DOCS.
>
> **A stale path to a session doc is not a bug, and NOBODY GOES LOOKING FOR THEM.** The date in the filename plus the two locations above make it findable in one step, forever. **Sweeping for them is make-work, and the make-work was bad enough to have consumed whole sessions.**
>
> ⭐ **But if you happen to be in a doc and see a session path that has moved, fix it.** *(Justin's refinement, 2026-08-21.)* **What is abolished is the OBLIGATION TO SWEEP — not the permission to fix what is already under your nose.**
>
> ⚠️ **This does NOT extend to source-of-truth docs** — protocols, manuals, ADRs, ARCHITECTURE, the Hub, sprint docs. **Those still get their references fixed when they move**, because they are cited as authority rather than as history.
```

## Step 2 · Move the in-flight docs to `docs/sessions/`

**These pass §1.3's test — a live or paused session genuinely re-spins from them.** ⚠️ **Re-derive this list; it is a day old and this estate moves hourly.**

| File | Whose session |
|---|---|
| `2026.08.17-ac037b-acp38-marching-orders.md` | **ACP-38's briefing.** Not yet spun. |
| `2026.08.20-ac037g-ac037h-handoff.md` | **This file — yours.** |
| `2026.08.19-acf15-act16-handoff.md` | **ACT-16's.** Not yet spun. |
| `2026.08.19-act-*` (5 files: `acta-atlas-brief`, `acta-exchange`, `actd-exchange`, `actd-working-docs-brief`, `acti-guard-integrity-brief`) | **ACT's live lanes + their exchange channels.** ⚠️ **Not yours to judge — move, do not assess.** |
| `2026.08.19-acv-*` (5 files incl. `map-prototype.html`) | **ACv's live lane.** Same — move, do not assess. |
| `ac-mailbox.md` | ✅ **UNBLOCKED 2026-08-20T18:16 — ACv-2 posted `✅ MOVED`, and ACT had already.** **All three participants have released it, which was the stated retirement condition.** ⇒ **It is a retired channel now: retire it to `docs/sessions/historical/2026.08/` with everything else. Nothing is watching it.** *(The original blocker — moving a file a live watcher targets is worse than leaving it, because the watch stays alive and reports nothing — no longer applies.)* |

## Step 3 · Extract, then retire, everything else

**~40 files.** For each: **is there anything in it whose value outlives the session?** If yes, move that material to a real home first. Then `git mv` the file to `docs/sessions/historical/<its own month>/`.

> ### ⚠️ ONE THING ABOUT THE 08-18 AUDIT DOC BEFORE YOU TRUST IT — it was rewritten by the sweep it describes.
>
> **`2026.08.18-acw-ac037f-working-docs-audit.md` has two commits: its creation, and one from the ACx sweep** *("reconcile both audits, retire 10 more, repoint every referrer")*. **That second commit repointed paths inside it to their post-sweep destinations.** ⇒ **The audit now lists files at the homes they were MOVED TO, while still saying they should be retired.** Nothing was lost — git has the original — **but you cannot reconstruct the folder's audited state from the audit itself.**
>
> 🔑 **The rule this bought, worth more than the instance: a reference-repointing sweep should EXCLUDE the audit documents that describe it** — for the same reason it excludes changelogs. **A document whose job is to record a before-state stops doing that job the moment you update its paths.** *(Found by the sub-AC `ACy`, 2026-08-19, from the git history rather than from reading the file.)*

⭐ **You are not starting cold — the extraction work is already DONE and drafted.** The lane `ACx` swept this folder on 2026-08-19 and left **seven exact before → after blocks** in `docs/working/2026.08.19-ac037f-acx-working-docs-sweep-lane.md` §10, plus per-file verdicts for all 55 in its §11. **Read that file early. Do not re-derive it.**

> ### ⚠️ ACx MAY OR MAY NOT BE REACHABLE — ASK JUSTIN, and do not assume either way.
>
> **As of 2026-08-19 Justin still had the AC0-37f session open and could pull `ACx` up on request. That was two days ago and sessions have turned over since — treat it as unconfirmed.** 🔑 **"Reachable" is a claim about a session's state and ONLY JUSTIN CAN SEE IT.** ⚠️ **An earlier handoff recorded ACx as "permanently unreachable" and that was wrong** — subagent resume really is session-bound, but the premise (that the session had ended) was false. **The lesson is the same in both directions: ask, do not infer.**

### 🔴 The one file with a real blocker

**`2026.08.11-ac0-ac0-handoff.md` holds a Justin ruling that exists in no other document:** that `evryn-backend/identity/core.md` describes a module-loading model the runtime abandoned in May, and that **this is DELIBERATE and must not be "fixed"** — the identity files get pulled apart again before long.

**Still not rehomed as of 2026-08-21.** **If it goes with the file, a future audit rediscovers the mismatch cold, reads it as a defect, and "fixes" it — against an explicit ruling.**

> 🔴 **JUSTIN RULED ON THE DESTINATION, 2026-08-21 — read §7.10 B1 before you do this, not this paragraph.** **It SPLITS in two: the shape of the state and the decision go to `evryn-backend/docs/ARCHITECTURE.md` (Identity Composition); the two OWED items go to SPRINT Step 53 with a pointer back to that ARCH section.** **§7.10 B1 also carries the source-verified detail — including a third item beside it that is NOT deliberate and IS owed.**

⚠️ **The two things that ruling authorized touch Mira's layer** (an identity-review check, and an exempting comment in the identity files). **Route those; do not edit identity files.**

### The three whose reason lives in another lane's territory

**Each is blocked only by a reference held by ACT.** Per Justin's own rule, *"only a reference is holding it"* is a task, not a reason — **and under the new no-repointing rule these may simply not be blocked at all. Check that first; it may dissolve the whole problem.** If not: **one message to `docs/mailboxes/inbox-act.md`.** *(ACT answered two of mine inside an hour on 2026-08-19.)*

- `2026.08.17-ac037c-acf-cross-runtime-halt-signal-note.md` — cited by `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md`
- `2026.08.11-ac035-acf-agent-patterns-note.md` — cited by two ACT-lineage working docs
- `2026.08.06-ac0-ac0-handoff.md` — cited by `docs/current-state.md` **(this one is ours, and it is a source-of-truth edit → Justin's go)**

### Justin's rulings on three specific files, already given

1. **`2026.08.17-ac037c-ac037d-handoff.md` (600 lines)** — its §7 is *"Justin's rulings today, persisted — these existed only in chat,"* **and nobody has ever checked whether those rulings were carried into the successor handoff's §5.** ⇒ **RULED: you diff them before it retires.** ACx called this the largest thing it left undone.
2. **`2026.08.18-ac037e-justin-cutover-redirect-drafts.md`** — ⇒ **RULED: retire it, and rely on the new findability rule rather than repointing the ADR that cites it.**
3. **`2026.07.15-ac3-step78-real-health-design.md`** — ⚠️ **This is a DESIGN doc, not a session doc** — it is the specimen behind §1.4's rule. ⇒ **It needs a real home, not `/sessions/historical/`.** Its `src/` comment has already been repointed at the durable homes (I did that 2026-08-20), so nothing in code depends on where it lands.

## Step 4 · ✅ SETTLED 2026-08-21 — DO NOT BUILD WHAT THIS STEP DESCRIBES.

> 🔴 **There is NO `held-session-docs.md` register. Justin replaced the design.** **A held doc now carries its own hold condition in a one-line `HELD-SESSION-DOC` banner at its top** — exact shape in **`lock-protocol.md` step 10**, which is also where the aging check lives.
>
> 🔑 **Why his design won, and it is worth understanding rather than just obeying: a register is a SECOND COPY of a fact that already lives in the doc, and two homes drift.** **A banner cannot** — when the condition is met you archive the doc and the condition goes with it. ⭐ **And you still get the register, derived: `git grep -n "HELD-SESSION-DOC"`.**
>
> ⇒ **What this means for YOUR sort: every doc you keep gets a banner; anything without one is retirable by default.** **Read step 10 before you start moving files.**

*(The original design below is kept only because its five decisions explain what the banner has to carry. Build from `lock-protocol.md` step 10, not from here.)*

## Step 4a · 🔴 THE ORIGINAL, SUPERSEDED DESIGN — `docs/sessions/held-session-docs.md`

**His ask, verbatim:** *"How/where do we keep a list like this, that says why something can't be retired? Because sometimes things **do** need to stay in flight for a while. They look stale, but they're **genuinely not**. If it's in a session doc that keeps getting rewritten, it'll get compressed away. But if there was a dedicated index, and if it was part of the #lock protocol to look back over that … instead of making everyone re-read these during locks and re-derive why, we just look at the reasons and determine if they can be retired now."*

**He asked for a first draft from me and for you to actually figure it out. Here is the draft. Improve it, then take it to him.**

### My draft

**A file, `docs/sessions/held-session-docs.md`. One row per held doc:**

| Path | Why it is alive (one line) | **Retire upon** (a testable condition) | Owner | Held since |
|---|---|---|---|---|

**Five design decisions, each with its reasoning, because the reasoning is the part you should attack:**

1. 🔴 **ABSENCE FROM THIS FILE MEANS RETIRABLE.** *This is the whole design.* A doc not listed is not held — it is a retirement candidate by default. **Without this inversion the file is decorative**, because the dangerous failure is not a stale row, it is a doc quietly held by nobody's decision.
   - ⭐ **What licenses such a harsh default: Justin's no-repointing rule makes a wrong retirement nearly free.** It is a `git mv`, the doc stays findable, and no reference breaks. **The two changes only work together** — do not adopt this half without that one.
2. **The condition must be TESTABLE, not a vibe.** *"Still in play"* and *"might be useful"* are true of almost everything and expire silently. **A condition someone can check in under a minute is the entire value of the file** — it converts an expensive re-derivation into a cheap boolean.
3. **The OWNER column exists so a condition never stalls.** If the owner is another lineage (ACT, ACv), the checker does not touch it — it pings. Without this, a cross-lane condition sits untested forever because nobody is sure whose it is.
4. **`HELD SINCE` is the staleness detector, and it works without anyone remembering.** **A row older than ~14 days gets RE-JUSTIFIED at the next `#lock`** — not auto-retired, re-argued. *(Same shape as the `current-state` bulletin's "an entry more than 7 days old is OVERDUE BY DEFINITION," which already works precisely because the date does the work rather than someone's memory.)*
5. **The file should be SHORT, and its length is itself a health signal.** Under §1.3 the only legitimate holds are in-flight sessions plus the occasional not-yet-rehomed fact — **and the second kind is a bug, not a steady state.** ⇒ **If `held-session-docs.md` passes ~10 rows, the problem is hoarding, not bookkeeping.**

### The `#lock` change that makes it real

**Step 10 currently makes every instance scan `docs/sessions/` and re-derive what is superseded.** ⇒ **Replace that with: read `held-session-docs.md`, test each condition, retire what is met, re-justify anything older than the window.** **Re-derivation becomes a lookup.** *(Step 10 was rewritten by Justin on 2026-08-19 and is at `docs/protocols/lock-protocol.md:57-61`; this would be its second revision in two days, so propose it — do not just edit it.)*

### 🔴 Three things I deliberately did NOT decide — they are yours to work out with Justin

1. **Hand-maintained, or GENERATED from banners inside the docs themselves?** A generated index cannot drift — the same property that makes `dependency-map.md` trustworthy, where *"a non-empty diff means the graph actually moved."* **But a generator is a thing to maintain, and `_evryn-meta` has no test suite to hook one into.** **My lean: hand-maintained, because if the list is short the maintenance is trivial — and if it is not short, that is the real finding.**
2. **One file for all three lineages, or one per lineage?** One place to look is the whole point; but one file means cross-lane write traffic, and this estate has already been bitten by two agents editing one file. **My lean: one file, owner column, append-only per row.**
3. **The exact staleness window.** I said ~14 days from the bulletin's precedent. **Untested.**

## Step 5 · Delete `docs/working/` and close out

**Then: changelog entry, `current-state.md` body update, and a short note to `ACP-38`'s marching orders confirming the folder is gone** — 38 should learn this as one settled sentence, never as a task.

---

# §3 — WHAT IS ALREADY DONE, so you do not redo it

**All committed on `main` as of 2026-08-20:**

- **`AGENT_PATTERNS.md` is RETIRED** to `docs/historical/`. Every live reference repointed across four repos; 22 session docs carry a correcting banner rather than an edited body.
- **`AC0-38` → `ACP-38`**, including the sub-naming convention (every subagent takes its commissioner's prefix — `ACPa`, `ACPy`). Marching-orders file renamed; 15 references repointed.
- **Three source-of-truth docs stopped pointing agents at retired repos** — `repo-inventory.md` and `roadmap.md` both still listed `evryn-dev-workspace`, `evryn-quality` and `evryn-ops` as **Active**; `dc-lock-protocol.md` step 5 routed research to two directories that no longer exist.
- **`scripts/ping.mjs --dev` now reads its webhook from `_evryn-meta/.env`**, not the retired repo. **Verified by sending a real ping**, not by reading.
- **`qc.md`'s dead output channel is fixed** — it told QC in five places to write findings to a path that does not exist where she now runs.
- **The `current-state` bulletin claim that eleven items had rotted for twelve days is drained** — most were discharged.
- **All four orchestration docs read in full and confirmed clean** of retired-repo staleness.

---

# §4 — BOUNDARIES

**You may:** move and retire docs inside `_evryn-meta`; extract material to real homes; write your own notes; commit by explicit pathspec.

**You may NOT:**
- 🔒 **Merge, push, deploy, or run a migration.** Ever.
- 🔒 **Edit an identity file** (`evryn-backend/identity/**` is Mira's and Justin's).
- 🔒 **Edit a source-of-truth doc without Justin's go** — protocols, manuals, the Hub, ARCHITECTURE, BUILD, sprints, `LEARNINGS.md`, `current-state.md`. **Draft the exact before → after and hand it to him.**
- 🔒 **Move or judge an `act*`/`acf*`/`acv*` file's CONTENT.** Move the in-flight ones per Step 2; assess none of them.
- 🔒 **Pick up any second task.** See §0.

⚠️ **`_evryn-meta` is HOT.** `git status` before you touch anything; `git diff <file>` before every commit; **put the pathspec on the commit** — `git add` does not scope one.

---

# §5 — TWO INSTRUMENT WARNINGS THAT COST REAL TIME HERE

- 🔴 **Bash `grep` ABORTS on some reads in this environment — not an error, not empty: aborted.** **A command that failed to execute prints exactly like a clean result.** It produced a false negative for me on 2026-08-19 on a load-bearing question, and a false all-clear across all 53 calls for an earlier audit. ⇒ **Use `node` to read matching lines when an absence matters.** `grep -P` is separately broken here.
- ⚠️ **A filesystem-wide scan DOUBLE-COUNTS, because worktrees are full checkouts.** A naive scan returned 158 files where the truth was 88 — 70 of the difference was the same repos seen twice. ⇒ **`git grep` per repo.**

---

# §6 — YOUR NEXT LIST, ONLY AFTER §2 IS DONE. **Do not start any of this early.**

> 🔴 **§2 first. All of it.** *(Justin, 2026-08-20: the organizational thread "stays with 37h as the next list — we can go to 37i if we need.")* **This section exists so the thread is not lost, not so you can pick the interesting one.** ⚠️ **Three prior instances each did part of the consolidation and handed the rest on. Do not be the fourth.**
>
> ⭐ **And a genuine finding worth holding, because it reframes the worry that produced this section.** Justin asked whether the overall arc was being lost, and wondered whether `ARCHITECTURE` / `BUILD` / `SPRINT` already carried it. **They do — for the product.** Intent is in `ARCHITECTURE.md`, scope in the BUILD doc, open work in the sprint's **🔵 LIVE** region *(which IS the sprint's current state by design)*, what shipped in `deploy-log.md`. **The product thread is well held.**
>
> 🔑 **What is NOT held anywhere durable is the ORGANIZATIONAL thread — how the org runs.** `_evryn-meta/docs/` contains exactly three durable documents: a snapshot, a ship ledger and a repo list. **None of them is a backlog.** ⇒ **So this work has only ever lived in handoffs, which is exactly why each instance re-derives it and why it feels like it slips. It does.** **This list is the fix, and it stays in the 37 lineage rather than becoming a new file.**

**1 · 🔴 The auto-load audit — the half of the loading refactor nobody did.** Lane A moved ~45,000 tokens of the wrong manual off every DC/QC/OC spin. **It reduced AC's OWN resident load by nothing.** The question: *how much of what an AC auto-loads every turn should be a protocol or a skill, pulled at its moment?* *(Justin: "I think it's likely a lot.")*
> 🔴 **THE COUNTER-TENSION IS THE DESIGN PROBLEM AND MUST NOT BE WAVED AWAY: this manual is dense with rules that exist BECAUSE they were violated by an agent that had read them.** Anything moved out of the resident load **stops being read at the moment it is needed** unless something structurally re-loads it. ⇒ **The cut is not the deliverable. The TRIGGER that re-loads it is.**
> **Also in scope:** run `/doctor` against `ac.md`. ⚠️ **Nobody has established what that command actually checks on an agent-definition file — establish that BEFORE drawing any conclusion from a clean or dirty result.**

**2 · The naming cutover's estate-wide cutoff.** `AC0`→`ACP` and `ACf`→`ACT` are done for the *instances*; the sweep is not. **It is a design call before it is a mechanical pass, because renaming a frozen record or a changelog falsifies history.** The proposal on the table, never ruled on: **rename in live source-of-truth docs and agent manuals only; leave every session doc, historical file and changelog alone; add one line to the mailbox roster mapping old → new.** ⚠️ **A rename does NOT re-point a mailbox — `ACP` deliberately kept `inbox-ac0.md`, because renaming a live mailbox breaks every watcher armed on it.**

**3 · 🔴 The founding team has no compaction rule, and ADR-058 does not reach them.** They load `evryn-team-workspace/CLAUDE.md`, not the router. AC0-37e shipped the rule to the team manual — **but `evryn-team-runtime` has no `CLAUDE.md` and needs none** (its agents are woken by code and composed by `src/composer/`), **so for the autonomous runtime the rule has to live in a composer layer.**
> **Severity, honestly: LOW TODAY, and it does not stay that way.** That runtime is paused and its Cowork agents are supervised — **so a compacted team agent is currently caught by a human. That protection is exactly what autonomy removes, and autonomy is the thing being built.** ⇒ **Route to ACT; it is Justin's file and ACT's lane. Do not fold it into a build.**

**4 · Archive-vs-delete on the three retired repos** — `evryn-dev-workspace`, `evryn-quality`, `evryn-ops`. **Justin's call, due ~2026-09-18.** They hold **16 tracked files**, and **each carries its own `.claude/settings.json` with its own permissions — check those before any removal.** ✅ **The one live dependency is already gone:** the `--dev` Slack credential moved to `_evryn-meta/.env` and the source `.env` was deleted 2026-08-20, both channels verified by real pings afterwards.

**5 · `_evryn-meta` has no test suite, so the control-character scan cannot be wired here.** `evryn-backend` gets it via SPRINT Step 113. ⚠️ **This repo — where the actual hits were found — has nothing to hook into.** **A NUL byte makes a file binary to `grep` and `git diff`, so every sweep silently skips it *and reports success*.** **Prevention-by-instruction has failed nine times, twice by authors mid-sentence about the hazard.** ⇒ **A scan is the mechanism; the instruction is a courtesy.** **Justin's call — a git hook, a scheduled agent, or a `#lock` step.**

**6 · 💰 Should cache temperature GATE a gatekeeper going live?** Emma's burn analysis: **~4.9 months runway dormant · ~1.4 months warm · 🔴 SIXTEEN DAYS on a cold cache.** Her conclusion: ***"the go-live decision for a gatekeeper is a burn decision, not just a build decision — and nobody has been treating it as one."*** ⚠️ **It appears in NO gate list** — not `current-state`, not `gate-b-plan.md`, not the sprint; the watch-list frames that measurement as a *cost lever*, not a go/no-go. **Justin's call.**

**7 · Four QC review-patterns, proposed and unapproved** — `docs/working/2026.08.12-qc-sweep-findings.md` §6. **QC cannot write her own manual, so a pattern she surfaces dies at subagent death unless an AC promotes it.** Needs Justin's yes and a writer.

---

# §7 — FROM AC0-37h, FOR **AC0-37h2**. Written 2026-08-20 night, extended 2026-08-21. Read §0–§2 above first; this is what moved under them.

> 🔴 **THE JOB IN §2 HAS NOT STARTED. Not one file has moved.** Everything I did was adjacent work at Justin's direction. **You are the instance that actually does it** *(his call, 2026-08-20 night: "tomorrow we can spin a AC0-37h2 that actually does what we came here to do")*. **§0–§6 above are accurate as written — this section does not replace them.**

## 7.1 ⚠️ §6 IS NO LONGER ALL YOURS — items 3–7 went to a parallel lane

**`AC0-37i` owns §6 items 3, 4, 5, 6 and 7** → brief at `docs/sessions/2026.08.20-ac037h-ac037i-org-thread-brief.md`. **Do not pick them up; you would collide.**

**Items 1 and 2 stay in this lineage and are still yours**, and the reason is worth keeping: **item 1 (the auto-load audit) is an audit OF `ac.md` and the cascades, both of which were edited heavily on 2026-08-20** — auditing them mid-change would have measured a moving target. **Item 2 (the naming cutover) is the same judgement as this job** — which docs are frozen records versus live source-of-truth — so running both at once risks two different rules about one corpus.

## 7.2 ✅ THE LAST BLOCKER CLEARED — `ac-mailbox.md` is free

**`ACv-2` posted `✅ MOVED` at 2026-08-20T18:16; ACT posted 08-19.** **All three participants have released it**, which was the stated retirement condition. ⇒ **§2 Step 2's one exception is gone. Nothing is watching that file. Retire it with the rest.**

## 7.3 🔴 THE SPRINT CITES ~15 `docs/working/` FILES — AND TWO ARE ALREADY DANGLING

**Found by reading `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` in full.** It is a **source-of-truth doc**, and it cites `2026.06.17-ac3-cache-measurement-plan.md` (Steps 69, 47) and `2026.07.16-acu-ac0-product-radar-findings.md` (Step 91) — **both of which earlier sweeps already retired to `docs/sessions/historical/`. They have been broken for weeks, in the doc that gates the deploy, and nobody noticed.**

⭐ **This is the strongest argument FOR Justin's no-repointing rule, not against it: under that rule they were never broken** — the date in the filename plus the two locations resolves each in one step. ⇒ **Do NOT go hunting for the other thirteen. That sweep is exactly the tax being abolished.**

⚠️ **But per Justin's refinement of 2026-08-21: if you are already in a doc and see a moved session path, fix it.** **What is abolished is the OBLIGATION TO SWEEP, not the permission to fix what is under your nose.** ⇒ **These two are under your nose now — repointing them is fine; a repo-wide pass for the rest is not.**

## 7.4 ✅ DONE 2026-08-21 — the two docs that would have dangled are fixed. Nothing owed to you here.

- **`ac.md`'s watcher example** *(it polled `docs/working/*.md`)* — **deleted, and pointed at `mailbox-protocol.md`, which now owns the watcher shape.** 🔑 **Deleting beat repointing: it had gone stale in TWO ways at once — wrong folder, and watching file-mtime after the channel had moved to firing on COMMIT.** **Repointing would have left two recipes in two files disagreeing.**
- **`product-full-cascade.md` step 6's required pre-read** — **the findability rule is inlined rather than the path repointed**, then refined by Justin: *session-doc paths are not proactively maintained, but if you see one has moved, repoint it.*

*(Recorded as done rather than deleted, because the reasoning is the record of why the watcher recipe has only one home now.)*

## 7.5 ✅ DONE 2026-08-21 — the mailbox protocol is built. Nothing here is owed to you.

> **`docs/protocols/mailbox-protocol.md` exists** *(named without the `ac-` prefix — nothing about it is AC-specific)*, the 89-line README is absorbed and retired to `docs/historical/`, and **`ac.md` carries the read-it-at-the-moment-of-use trigger** — which was the half the README never had and the whole reason it went unread.
>
> ⚠️ **The design moved on 2026-08-21 and the protocol is the only current statement of it — the original instruction preserved in §7.5a below is SUPERSEDED in three places.** **There are no discharge markers** (reply `RECEIVED` into the sender's inbox and delete from your own) · **receipt and capture are atomic while ACTING is separate** · **messages are no longer capped at pointers and may run to pages.** **Read the protocol, not §7.5a.**
>
> *(The original instruction is kept below, unedited, because its reasoning is the record of why the protocol has the shape it does.)*

## 7.5a 🔴 THE ORIGINAL INSTRUCTION: the mailbox protocol — APPROVED, not built

> **Justin's ruling, 2026-08-20: "design approved… `docs/mailboxes/readme.md` will never get read"** ⇒ **build `docs/protocols/ac-mailbox-protocol.md`.** *(He confirmed the protocol over folding it into `ac.md`.)*

**Absorb the 89-line `docs/mailboxes/README.md` into it, add the receipt design below, retire the README, and leave in `ac.md` only the always-needed core** (your address, the two spin-up asks) **plus one trigger line: *before you write or discharge a mailbox entry, read the protocol.*** 🔑 **That trigger is the thing the README never had, and it is why this fixes the problem rather than moving it** — the same shape that made `ac-writing-protocol.md` reachable.

⚠️ **`ac.md` currently says "The protocol lives in `docs/mailboxes/README.md`; read it once." That is a live instruction pointing at a file Justin says nobody reads. Fix it in the same pass.**

**THE APPROVED DESIGN — a DISCHARGE marker, never clear-on-receipt:**
- **Clear-on-receipt fails UNSAFE** — clear, then die, and the message is gone with nobody having acted. **Mark-on-discharge fails SAFE** — die before marking and the next reader repeats it. **Duplicate work is recoverable; a lost message is not.**
- **Atomicity is already solved by a primitive used daily: a git commit is atomic.** ⇒ **Write the receipt in the SAME commit as the artifact it discharges.** Commit lands → both exist. Doesn't → neither does.
- **Three markers, appended under the entry, never a destructive edit** *(append-only survives two writers)*: `✅ DONE <ts> · <instance> — <clause> → <where>` · `✅ NO ACTION <ts> · <instance> — <why>` · `⏳ HELD <ts> · <instance> — retire upon <condition>`.
- 🔑 **The load-bearing property is the inversion: ABSENCE of a marker means ACTIONABLE.** Same shape as `held-session-docs.md`, so the estate gets one rule rather than two.
- ⚠️ **Honest residual:** act-then-die-before-receipt means the next reader repeats it. **Safe direction, not zero.**

## 7.6 ✅ DONE 2026-08-21 — `inbox-ac0.md` → `inbox-acp.md`, and the rule it seemed to break was sharpened instead

> ✅ **BOTH 7.5 AND 7.6 LANDED on 2026-08-21 as one cutover. Nothing here is owed to you** — it is recorded because the reasoning is worth inheriting and because §6 item 2 above still carries the pre-cutover wording.
>
> **What landed:** `docs/protocols/ac-mailbox-protocol.md` created *(the README absorbed and retired to `docs/historical/`)* · **`ac.md` now carries the read-it-at-the-moment-of-use TRIGGER**, which is the half the README never had · the inbox renamed with its messages intact · **ACT and ACv notified in their own inboxes** · every live reference repointed, **and every frozen record deliberately left alone.**
>
> 🔑 **The rule that came out of it, and it is the transferable part: a watcher is SESSION-BOUND, so a rename can only break watchers LIVE AT THAT MOMENT.** The old rule read *never rename a mailbox*; **the true rule is *never rename a mailbox while a watcher is armed*** — and between sessions that window is wide open. ⚠️ **A rule stated as an absolute, when the real constraint is conditional, gets either broken or obeyed past its usefulness. Both happened here inside 48 hours.**

*(The original pre-cutover instruction is kept below, unedited, because it carries the five-step sequence the protocol now owns.)*

## 7.6a 🔴 `inbox-ac0.md` → `inbox-acp.md` — Justin's call, and it CONTRADICTS a written rule. Read this before doing it.

> **His instruction, 2026-08-20 night:** *"after the 37 series, at 38, it becomes ACP, not AC0 — and so I want the mailbox to be acp — we'll need to fix a few references and let ACT and ACv know, but that's it — it's an easy cutover."*

⚠️ **The estate ruled the opposite way 24 hours earlier, and `inbox-ac0.md` is the worked example of that rule.** `ac.md` and `docs/mailboxes/README.md` both say: ***"a rename does NOT re-point the file — rename the agent, keep the path, because renaming a live mailbox breaks every watcher armed on it."*** **ACT called it *"the better generalisation of a lesson that cost us twice."***

🔑 **Why Justin is nonetheless right, and this SHARPENS the rule rather than breaking it: a watcher is SESSION-BOUND.** *(Established 2026-08-20: a `Monitor` dies with the session that armed it.)* ⇒ **A rename only breaks watchers that are LIVE AT THE MOMENT YOU RENAME.** The old rule reads as *never*; the true rule is **"never while one is armed."**

**⇒ WHAT TO DO, in this order:**
1. **Confirm with Justin that no live session holds a watcher on `inbox-ac0.md`.** **Only he can see that** — it is a claim about session state.
2. **`git mv docs/mailboxes/inbox-ac0.md docs/mailboxes/inbox-acp.md`**, keeping the messages intact.
3. **Post the mapping in `docs/mailboxes/inbox-act.md` and `inbox-acv.md`** so ACT and ACv re-point. **Write into their boxes, not yours — that is the channel rule.**
4. **Fix the references** — `ac.md`, the mailbox README/protocol, and the roster. **Record old → new in the roster; do not delete the mapping.**
5. **Amend the rule itself** to say *"while a watcher is armed"* rather than *"ever,"* **and name this cutover as the worked exception.** Otherwise the next reader hits a rule the estate visibly broke and cannot tell which governs.

⭐ **Do it as ONE cutover with §7.5** — same files, same notifications, and one announcement instead of two.

## 7.9 ✅ THE RULINGS DIFF IS DONE — `2026.08.17-ac037c-ac037d-handoff.md` IS CLEAR TO RETIRE

**Justin ruled this had to happen before that 600-line file could go, and ACx called it the largest thing it left undone. It is done. Nothing is stranded.**

🔴 **BUT THE TASK WAS BIGGER THAN THE RULING DESCRIBED, AND THAT IS THE PART WORTH INHERITING: the file has THREE rulings sections, not one.** §7 *("Justin's rulings today, persisted")*, **§12.4** *("What Justin ruled today")* and **§13.4** *("What Justin ruled")*. **Every prior pass — ACw, ACx, and the ruling itself — named only §7.** ⚠️ **This is the second file in a week where rulings hid outside the section named for them.** ⇒ **A section TITLE is a lead, not a boundary. Grep the whole file for ruling-shaped language before you conclude anything about what it holds.**

**The verdict, section by section:**

| Source | Disposition |
|---|---|
| **§13.4** (2026-08-18, the newest) — 6 rulings | ✅ **ALL SIX carried verbatim into `2026.08.18-ac037e-successor-handoff.md` §5.** |
| **§12.4** (superseded by §13.4) — 3 not carried forward | ✅ **All discharged by events** — a probe deleted, an approved wording already applied to frozen text, ACf paused for a cutover that has since run. |
| **§7** (2026-08-17, the oldest) — 6 rulings | ✅ **Two rehomed and VERIFIED AT SOURCE** — ballot-numbering at `ac.md:261`, Step-117-gets-its-own-lane inside SPRINT Step 117 with the quote intact. **Two discharged by application** (Lane A wording; the down-moment authorization, which was session-scoped). **Two carried in substance** — worktree-reaping conditions live in `ac.md` and `lock-protocol.md` step 19; `AGENT_PATTERNS` is superseded by its outright retirement, recorded in four durable docs. |
| **§12.5 / §13.5** — *"the staging brief is NOT applied, and every held edit is owed"* | ✅ **Discharged 2026-08-18** — the meta CHANGELOG records the held set applied. **Do not re-open it.** |

### 🔴 ONE THING THE DIFF SURFACED THAT IS **NOT** RESOLVED — and it is a live contradiction, not a stranded ruling

**`2026.08.18-ac037e-successor-handoff.md` §5.1 records, as a standing ruling: *"He does NOT review merges. YOU are the backstop"* — and states explicitly that it OVERRIDES `ac.md`'s rule that source-of-truth docs stay uncommitted for Justin's vet.**

⚠️ **That override exists in NO durable doc. `ac.md` has zero references to it** *(grepped; confirmed absent)*, **and `ac.md` still states the opposite gate in four places.** ⇒ **Two live documents disagree about whether an AC pauses for his vet.**

🔑 **Observed practice matches `ac.md`, not the override** — through 2026-08-20/21 Justin vetted diffs in SCM and gave an explicit go per commit, and said plainly *"leave that in SCM for my vet."* **So the override reads as SESSION-SCOPED to the cutover night, when he was away and Lane A had to merge.**

🔴 **But it is written as standing, in a live handoff, under a heading that says *"so you do not re-ask."*** **A fresh AC reading it would stop pausing for his vet.** ⇒ **Justin's call: is that override dead, or is it a standing exception that belongs in `ac.md`?** **Do not resolve it by inference — it changes the commit gate, which is the highest-stakes rule in the manual.**

## 7.10 🔴 THE LIVE OWED LIST — everything still open, 2026-08-21. Read this before §7.5a–7.6a, which are historical.

### A · YOUR JOB. Nothing below it matters until this lands.

**§2 above, Steps 1–5: retire `docs/working/` into `docs/sessions/`.** ✅ **Fully unblocked** — `ac-mailbox.md` was released by all three participants on 2026-08-20. ⚠️ **Re-derive the file count; it was 58 on the morning of 08-20 and nothing has moved since.**

🔒 **NEW AND IT CHANGES HOW YOU HOLD THINGS: a held session doc now carries its own hold condition in a banner at its top** — `lock-protocol.md` step 10 has the exact shape. **There is no separate register.** ⇒ **As you sort the folder, anything you keep gets a `HELD-SESSION-DOC` banner; anything without one is retirable by default.** ⭐ **`git grep -n "HELD-SESSION-DOC"` then gives you the whole held list, derived.**

### B · Owed to Justin, drafted and voted, NOT applied

| # | What | Where it goes | Status |
|---|---|---|---|
| **B1** | **The `core.md` ruling** — `core.md` tells Evryn to fetch identity modules the runtime already force-loads. **Justin's ruling: DELIBERATE, do not "fix" it** *(the identity files get pulled apart again before long, so correcting it is work done twice)*. | 🔴 **Justin's call, 2026-08-21: SPLIT IT.** **The shape of the state + the decision → `evryn-backend/docs/ARCHITECTURE.md`** *(Identity Composition)*. **The two OWED items → SPRINT Step 53, with a pointer to the ARCH section.** | **Not done — I lacked the context room to read ARCHITECTURE.md.** **This is yours.** |
| **B2** | **ac.md's watcher example** polls `docs/working/*.md`. | ✅ **DONE 2026-08-21** — deleted and pointed at `mailbox-protocol.md`. | ✅ |
| **B3** | **`product-full-cascade` step 6's session-doc citation.** | ✅ **DONE** — findability rule inlined; path deliberately NOT repointed. | ✅ |

> **B1 detail, verified at source so you do not re-derive it.** **TWO things Justin authorized and both are still owed:** an identity-review check — *"does an instruction about LOADING match what the runtime actually loads?"* — and **a short comment in the identity files marking the exemption *known, deliberate, temporary***. ⚠️ **Both touch Evryn's identity layer: coordinate the wording with Mira. He authorized the CHANGE, which is not authorization to write in her files.**
> ⚠️ **Do NOT confuse it with the `company-context.md` item beside it, which is NOT deliberate:** a 7-day freshness instruction on a doc ~112 days old, force-loaded into every query, with no way to clear. **That one is genuinely owed.**
> 🔑 **And the reason this took two passes: the sweep lane summarized it as ONE owed item. At source it is two, plus a third that is a different problem.** **Read the source, not the summary.**

### C · 🔴 THREE ENVIRONMENT FACTS THAT LIVE ONLY IN DOCS YOU ARE ABOUT TO RETIRE

**Checked 2026-08-21: `ac.md` does NOT contain these.** They exist only in `docs/working/` handoffs and the changelog. **When you retire the folder they go to `historical/` — findable by rule, but nobody will think to look.**

- **Bash `grep` ABORTS on some reads here — not an error, not empty: ABORTED, and it prints exactly like a clean result.** It produced a false all-clear across all 53 calls of one audit.
- **`grep -P` is broken here** — *"-P supports only unibyte and UTF-8 locales."* Use `awk`.
- **A filesystem-wide scan DOUBLE-COUNTS, because a worktree is a full checkout** — 158 files where the truth was 88.

⇒ **Propose these to Justin for `ac.md` before the folder retires.** *(`ac.md` has the grep REASONING — locate vs conclude, completeness claims — but none of these environment specifics.)*

### D · What changed under you on 2026-08-20/21 — read fresh, do not work from memory

- **`mailbox-protocol.md` is new and is the single home.** **Commit = send · reply into the SENDER's inbox to acknowledge · delete when captured · nothing is ever HELD in an inbox · and you do not work for another AC.**
- **`ac.md`:** the heartbeat *(one job, no state, 5-min day / 10-min night, announce every pause and re-arm)* · **the file-tool rule is in `CLAUDE.md`** *(prefer `Read`/`Write`/`Edit` over Bash)* · `--no-ff` and never-silently-rebase-a-pushed-branch · the watcher recipe removed in favor of the protocol.
- **`lock-protocol.md`: step 9b is new** *(rulings need a durable home AND agreed wording)* **and step 10 is rewritten** *(hold banners)*.
- **`doc-update-routing-protocol.md`: change-type 9 is new** — where a ruling about how we work goes.

### E · Open questions Justin has NOT ruled on

1. **The foreground-vs-background default in the orchestration protocol now rests on nothing** — background agents currently render exactly like foreground ones, so the stated reason for the default is gone. **Flagged in the file; not decided.**
2. **The `held-session-docs` aging check** — whether "more than a week" is the right threshold.
3. 🔴 **`current-state.md` DOES NOT NAME THE ACTIVE BUILD DOC, and every full cascade sends you there to confirm it.** *(Flagged 2026-08-20, never closed. Verified at source 2026-08-21: zero mentions of `BUILD-EVRYN-MVP`; the active SPRINT set IS named, so it is specifically the BUILD line that is missing.)* ⇒ **An agent following the cascade's ⚑ *"confirm it is still the active build in `current-state.md`"* finds nothing there, and either guesses or stalls.** **This affects every product load, not just yours.** **One line in `current-state.md`'s *What's live in prod* section — Tier 2, so propose it.**
4. ⚠️ **`conductor-cascade.md` line 51 still tells a spun scout to load `CLAUDE.md` as context.** **Since the 2026-08-18 cutover that file auto-injects into every agent**, so naming it in a load list is at best a wasted slot and at worst implies it is a manual. **Small; propose alongside 3.**

## 7.11 What I did NOT do, so you do not assume it is done

- 🔴 **I DID NOT TOUCH `docs/working/` — not one file moved. The entire job is still in front of you.** **Two full sessions went into the manuals and protocols the job runs on; none went into the job.** **The folder is exactly as §2 describes, plus ACv-2's `MOVED` entry in `ac-mailbox.md`.** ⚠️ **Re-derive the count; it was 58 on the morning of 08-20.**
- **Zero subagents spawned across both sessions. Nothing stranded, no worktree of mine.** ⚠️ **`_evryn-meta-acf15` on `acf15/nest-prep` is live and is ACT's — do not reap it.**
- **I did not load the Atlas, the dependency map, or the runtime** (Justin's explicit scope). **Every claim here about the product rests on `ARCHITECTURE.md`, the BUILD doc and the sprint — not on having read `src/`.**

## 7.12 Three instrument failures paid for on 2026-08-20 — all one shape

- 🔴 **A `Monitor` inherits the `Bash` tool's MUTABLE working directory.** One rooted on `git rev-parse --show-toplevel` silently re-points when any unrelated command runs a `cd`. **Mine armed cleanly, passed its own guard, and never fired — with no error.** ⇒ **Anchor on `$HOME`.**
- 🔴 **"Monitor started" is not evidence a monitor works** — a script that dies on its first line reports the same thing. ⇒ **Emit a proof-of-fire line before the loop.**
- 🔴 **`node scripts/scan-control-chars.js` scans TRACKED files only.** It returned *"CLEAN — 365 files"* while the file just written was **untracked and therefore not among them.** ⚠️ **A clean result that skipped your file is a false all-clear.**

---

# §8 — FROM THE GHOST INSTANCE. Open questions for you, not findings. Written 2026-08-21.

> 🔴 **HOW TO HOLD THIS SECTION, AND IT IS THE WHOLE POINT.** It was written by an `AC0-37h2` that Justin spun **only to get the load list in §0.5 right**, and which then stopped. **That instance never loaded the list it wrote.** It held nine docs — its manual, the writing/orchestration protocols, `current-state`, the Hub and this handoff — and **read zero lines of `ARCHITECTURE.md`, the sprints, the Atlas, the dependency map or either product changelog.**
>
> ⇒ **Everything below is an UNVERIFIED LEAD unless it is in §8.3.** It came from a scout AC and five sub-readers, none of which held the runtime either. **Re-derive each against files you actually hold. Do not defend these as your own conclusions** — that is the second-order trap the router names: a clean load restores what a predecessor *read*, never what it *concluded*.
>
> ⚠️ **The scout that produced most of this COMPACTED before it could synthesise, and stopped correctly rather than flailing.** Its per-file recommendations are folded into §0.5; its unresolved claims are here.

## 8.1 🔴 JUSTIN'S RULING ON THE LINEAGE EDGE CASES — this is direction, not a question

**The problem, and it is mechanical rather than philosophical.** §2 Step 2 and §4 identify other-lineage files by **filename** (`2026.08.19-act-*`, `2026.08.19-acv-*`, and a ban on judging `act*`/`acf*`/`acv*` content). **For a specific set of files the filename and the contents disagree**, so a pattern-based sort silently mis-files them:

- **`2026.08.19-acv-ac0-classify-refactor-findings.md`** — matches the `acv-*` glob, but is **addressed to AC0** and its author disclaims the lane in writing: *"They are yours now; ACv is not working the extraction and has no stake in how it is scoped."*
- **`2026.08.12-acf13-ac035-agent-patterns-reply.md`** — its lineage token sits **mid-filename**, so a prefix filter never catches it. Its own banner records that touching even its banner required ACf-15's authorisation.
- **`2026.08.12-acf13-ac0-agent-patterns-postmerge-brief.md`** · **`2026.08.17-ac037c-acf-cross-runtime-halt-signal-note.md`** · **`2026.08.11-ac035-acf-agent-patterns-note.md`** — each authored by one lineage and addressed to the other.
- **`2026.07.22-ac0-ac0-handoff.md`** — AC0's own handoff, but §§ of it were written by **Marlowe (CGO)** and **Soren (CTO)**, who are neither ACT nor ACv.

> ### ⇒ **Justin's ruling, 2026-08-21, verbatim in substance: these are YOURS TO SORT — that IS the work.**
>
> *"These edge cases are within your lane, so you're going to have to sort them out — that's the work here — so you may need to extract info and send it to the right people. If something was a message to AC0 and we've got that, then cool. You can also send stuff to ACT and ACv's mailboxes, and you can also go read Marlowe and Soren's memories and see if they captured the stuff in them, and if you don't feel **very** confident about that, then you can put a note in their memory, pointing them to the new home and say 'hey, we retired this, but you need to put eyes on, to make sure you know about this'. And if you find the file referenced in their memory already, you can change the path to its new home."*

**What that authorises, concretely:**

1. **A message addressed to AC0 that we have already absorbed is done** — no routing needed.
2. **You MAY write into `docs/mailboxes/inbox-act.md` and `inbox-acv.md`** to hand material back to its lane. **Read `mailbox-protocol.md` at the moment of use** — you write into *their* box, never your own, and an entry is a pointer, not the content.
3. **You MAY read the founding team's memories** — `evryn-team-workspace/.claude/agent-memory/marlowe/MEMORY.md` and `.../soren/MEMORY.md` — to check whether they already captured what their sections of `2026.07.22-ac0-ac0-handoff.md` carry.
4. **If a memory already references the file, repoint the path to its new home.** Small, safe, and it is the "fix what is under your nose" half of the no-repointing rule.
5. **If you are not VERY confident they hold it, leave a note in their memory** pointing at the new home and saying plainly: *we retired this, you need to put eyes on it.* ⚠️ **The team's identity files are Justin's layer** — this is his explicit authorisation for these notes specifically, and it is not a general licence to edit agent memory.

## 8.2 PULL AT A TRIGGER — approved by Justin, do NOT carry these from the start

| File | Span | The trigger — a concrete moment, not "when relevant" |
|---|---|---|
| `evryn-backend/docs/gate-b-plan.md` | full — 307 | The moment you open `2026.07.22-ac0-ac2-fast-follow-brief.md`, or anything naming Megan, `systemtest@evryn.ai`, Gate-A, or held DEV state |
| `evryn-backend/identity/core.md` | full — 283, **READ-ONLY** | The moment you begin drafting the §7.10 B1 change. ⚠️ **§4 bars you from EDITING identity files; it does not bar reading.** |
| `evryn-backend/docs/gatekeeper-onboarding.md` | full — 84 | `2026.07.21-ac0-ac3-cleanup-batch-brief.md`. ⚠️ **A same-named file exists at `evryn-backend/identity/activities/gatekeeper-onboarding.md` — that one is Mira's layer and is barred. A basename search returns both.** |
| `_evryn-meta/docs/deploy-log.md` | full — 37 | Any question of whether something actually shipped |
| `evryn-backend/docs/operator-guide.md` | full — 523 | The `2026.07.15-ac3-step78-real-health-design.md` doc |
| `_evryn-meta/docs/decisions/012-trigger-composed-identity.md` | full | Drafting B1 — Step 53 names ADR-012 as what it would amend |
| `_evryn-meta/docs/decisions/057-agent-definitions-single-home.md` | full | `2026.08.18-ac037e-justin-cutover-redirect-drafts.md` |
| `_evryn-meta/LEARNINGS.md` | full — 1,090 | `2026.08.04-ac0-ac5-silent-email-drop-brief.md`, which proposes two generalizable rules |
| `_evryn-meta/.claude/agents/qc.md` — Patterns section | — | `2026.08.04-ach-justin-housekeeping-recommendations.md`. ⚠️ **The four unapproved QC patterns are `AC0-37i`'s, not yours (§7.1).** |
| `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` + target sub-doc | full — 743 | **Before you spin any subagent.** Non-negotiable. |

## 8.3 ✅ VERIFIED MECHANICALLY by the ghost instance — these three you may rely on

**Existence and path checks only; no judgment involved.**

1. **`SPRINT-V0.2-HARDENING.md:620` is a redirect stub** — `53. MOVED → SPRINT-v0.2-optionals.md`. **The live Step 53 body is `SPRINT-v0.2-optionals.md:13`.** ⇒ **B1's ruled destination is the OPTIONALS file.**
2. ✅ **RESOLVED 2026-08-21 — there is no `docs/org-backlog.md` and there never will be. Justin: it *"was something that was floated for about two seconds and killed."*** **What replaced it is better than a backlog row: a real pre-commit hook.** `.githooks/pre-commit` refuses any commit that would introduce a stray control character — **`AC0-37i`'s work, landing.** Its own header carries the reasoning worth keeping: *"Prevention-by-instruction has failed nine times here, twice by authors who were mid-sentence about the hazard. A scan is the mechanism; the instruction is a courtesy."*
   - ✅ **`2026.08.17-ac037b-acp38-marching-orders.md:170` is FIXED** — it told ACP-38 the item was *"now tracked in `docs/org-backlog.md`."* It now records the item as **closed by the hook**, with nothing for him to present. **Nothing owed to you here.**
3. ✅ **FIXED 2026-08-21 — `docs/protocols/ac-mailbox-protocol.md` never existed; the real file is `docs/protocols/mailbox-protocol.md`, no `ac-` prefix.** Justin authorised the correction directly. **Four LIVE instruction sites were repointed:** the header banners of `docs/mailboxes/inbox-acp.md`, `inbox-act.md` and `inbox-acv.md`, plus `2026.08.17-ac037b-acp38-marching-orders.md:22` — the sentence telling ACP-38 to read the protocol before writing to a mailbox.
   - ⚠️ **Deliberately NOT touched, and this is the discipline rather than an oversight: the FROZEN RECORDS.** `_evryn-meta/CHANGELOG.md:51` (a dated entry recording the decision as it was made), the two dated ACP→ACT/ACv messages inside `inbox-act.md` and `inbox-acv.md`, and §7.5a/§7.6a of THIS file — which are explicitly preserved originals. **A record of what was said at the time stops being that record the moment you update its paths.** *(Same rule that excludes audit docs from a repointing sweep.)*
4. **`current-state.md:134` cites `docs/working/2026.08.19-ac037g-acp38-handoff.md`, which does not exist.** The real file is **`2026.08.19-ac037f-ac037g-handoff.md`** (263 lines). ⚠️ **`current-state.md` is source-of-truth — draft the fix, do not apply it without Justin's go.**
5. **The corpus is 58 files / 18,526 lines** as of 2026-08-21.

## 8.4 OPEN QUESTIONS — unverified leads. Re-derive before acting on any of them.

**Grouped by what they would affect. Every one came from an agent that had not read the runtime.**

**(a) Claims that a source-of-truth doc may be wrong — highest stakes, verify first**

1. **`SPRINT-V0.2-HARDENING.md:362` may be false about Step 78.** It reads *"NEITHER deployed."* A reader ran `git tag --contains` and `git merge-base --is-ancestor` and reported **both halves shipped** — Half A live since `v0.2.9`, Half B merged and Vercel-deployed. ⭐ **Git-derived, so stronger than a doc claim — but re-run it yourself.**
2. **`ARCHITECTURE.md` may contradict itself inside B1's destination.** Reported: `:758–760` records that on-demand identity loading was replaced by force-loading in May, while `:876–883` still describes on-demand as current. **If true, writing B1 without touching the second passage leaves the doc asserting both models.**
3. **Atlas line citations into `liveness.ts` may have drifted** by the 2026-08-20 repoint commit `789f7d8`, which added header lines. Reported: `atlas/07-safety.md:231` cites `liveness.ts:144-145`, actual enforcing line ~149.
4. **`gatekeeper-onboarding.md` §5 may still call clustering *"built but not deployed / ship-blocker as of Round 17"*** — six weeks after it shipped — and instruct the operator not to promise the twice-daily package. **A wrong instruction in a runbook used at the moment a gatekeeper onboards.** A drafted correction already exists in the cleanup-batch brief.
5. **`current-state.md:167` may falsely state that `2026.08.06-acf-acf-handoff.md` is already retired to `sessions/historical/`** when it is still in `docs/working/`.

**(b) Things that may have no durable home — the class §7.10 C exists for**

6. **§7.10 C names THREE environment facts. Readers reported the true count is higher** — candidates include the two machine roots (desktop `/c/Users/Justin/...` vs laptop `/c/Users/jbmcg/...`), `MSYS_NO_PATHCONV=1` path mangling, `git worktree remove` leaving an empty folder on Windows, no `python` on this machine, `core.autocrlf` normalisation before byte comparison, and CRLF line endings in team `MEMORY.md` files that the `Edit` tool chokes on. ⇒ **Re-run the §7.10 C check against the full list, not the three it names.**
7. **The rule *"a load list is an artifact of record, not a message — commit it with the brief"*** may never have landed in the orchestration protocol.
8. **A recommendation to add an EFFORT pin beside the model pin** in the orchestration protocol, reported unhomed.
9. **A second import cycle** — `classify.ts` ↔ `security/web-guard.ts` — reported as recorded nowhere, meaning the security layer depends on a type defined in the god-module.
10. **`evryn-team-workspace/shared/protocols/runtime-ops.md` line 9 reportedly cites a `docs/working/` path** — and that file is composer **LAYER 1, read by all eight team agents on every wake.** ⚠️ **Identity layer: route it, do not edit it.** Per §2 Step 1's own carve-out, source-of-truth and identity docs still get their references fixed.

**(c) Live contradictions someone must resolve**

11. **The commit-gate override** recorded in `2026.08.18-ac037e-successor-handoff.md` §5.1 — *"He does NOT review merges. YOU are the backstop"* — still contradicts `ac.md` in four places. **§7.9 already flags this; it is still open and it is Justin's call.** Observed practice matches `ac.md`.
12. **`2026.08.17-ac037b-acp38-marching-orders.md:215` tells ACP-38 to name a live heartbeat's task id in a handoff.** `ac.md:728` now says the opposite, because a `Monitor` dies with the session that armed it. **Three of ACP-38's rhythm instructions may be stale against the current manual** — and that file is one you move under §2 Step 2, so you will have it open.
13. **An ACT-lineage lane, `ACTd`, was reportedly briefed to assess this same folder and never ran** — its exchange doc holds one entry and no reply. **Worth one message to `inbox-act.md` before you assume the lane is dead.**

## 8.5 What the ghost instance did NOT do

- **Loaded nothing from §0.5.** Justin's instruction was explicit: get the list right, then stop.
- **Moved no file, retired nothing, edited no source-of-truth doc.** The only edits are to this handoff — §0.5 and §8.
- **Spawned a scout AC and five sub-readers; all are dead and unreachable** (subagent resume does not survive a session). **Nothing is stranded** — their durable output is §0.5 and §8.
- **Did not run session-startup hygiene** — the `.claude/settings.local.json` check, the `#dev-alerts` offer, and the estate-wide repo/branch sync check are **still owed at your spin-up.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
