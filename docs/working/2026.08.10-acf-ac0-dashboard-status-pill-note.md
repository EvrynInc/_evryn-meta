# ACf → AC0 — one routed item for the PRODUCT sprint tracker

*From **ACf** (team-runtime lane), 2026-08-10. **Justin approved this handover explicitly** ("yes, hand over the note to AC0"). Delivered as a committed file because there is no live AC↔AC channel — a Slack ping addressed to "AC0" reaches Justin, not you. Justin has been told this file exists.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file:** a single routed finding, paste-ready. **Nothing else is owed to you here** — read it, drop it into `evryn-backend/docs/SPRINT-V0.2-HARDENING.md`, and this file can be deleted. No reply needed.

---

## Why it's coming from the team-runtime lane at all

It was found by a sprint audit of the **team-runtime** tracker (alias `D4`), which surfaced several dashboard items. Most of them are *dashboard-as-it-couples-to-the-team-runtime* and correctly stayed in that sprint. **This is the one with no team-runtime coupling whatsoever** — it touches no team-runtime table, no `agent_state` row, and no runtime semantics. The only thing it ever shared with that sprint is the folder the dashboard file happens to sit in. **It is product-side, so it is yours.**

**You own the product tracker's numbering, so it is handed to you unnumbered.**

---

## Paste-ready

```markdown
⬜ **TODO** — **The dashboard's Product tab renders 4 of the 12 `ItemStatus` values as an unstyled gray pill.** The status pill is coloured by a CSS class derived from the item's status (`_evryn-meta/dashboard/public/index.html` — the `.status-pill` rules at ~L338-350, rendered at L1055 and L1197 via `class="status-pill ${it.status}"`). Eight statuses have a color rule — `delivered`/`matched` green, `pending_approval` amber, `passed`/`ignored` gray, `error`/`escalated`/`bad_actor` red — but **`new`, `queued`, `processing` and `no_gk_response` have none**, so they fall through to the default gray, which reads identically to the deliberate gray used for `passed`/`ignored`. An item that is *still being worked* looks the same as one that was *declined*.

**Why this is product-side, not team-runtime:** the enum is `evryn-backend/src/db/items.ts`'s `ItemStatus` (12 values), and these are the email-manager's workflow states. It touches no team-runtime table, no `agent_state` row, and no runtime semantics — the only thing it ever shared with the team-runtime sprint was the folder the dashboard file sits in.

**Fix by re-deriving the full set from `src/db/items.ts`, not by patching the four known gaps** — the enum grows (`queued` was added by Clustering, ADR-049), so a hand-patch re-opens the same gap on the next status. Consider a check that fails when a value exists in the enum with no matching CSS rule.

⚠ **Worth doing before the gatekeeper goes live, not after:** `queued` and `processing` are the *transient* states — they will be the most common thing on the board once Clustering is switched on, so the statuses with no color are exactly the ones Justin will be looking at most.

*Provenance: found by the team-runtime lane (ACf, alias `D4`), routed here 2026-08-06 as the one dashboard item with no coupling to that runtime.*
```

---

## Two notes for you, not for the tracker

- **⚠️ The line numbers above were accurate on 2026-08-06 and have NOT been re-verified since.** `dashboard/public/index.html` is a live file. **Confirm the anchors against the file before handing this to a builder** — this org has been bitten more than once by a routed line-reference that was itself wrong.
- **A second, unrelated dashboard defect is NOT being routed to you**, so you don't go looking for it: the dashboard's month-to-date spend figure reads **low** (`dashboard/api/product.ts` selects `llm_usage` with no page size and reduces the rows into the today/this-month totals; a month of rows exceeds the silent 1000-row PostgREST ceiling). It is already tracked as **team-runtime SPRINT Step 16** because it is the same defect class that lane just fixed in its own runtime. **If you would rather own it as a dashboard item, say so via Justin and ACf will move it** — it is genuinely a boundary case, and it decides nothing automatically, so there is no urgency either way. ⚠️ Worth knowing regardless, since it is the number Justin makes spend decisions against: **treat that figure as a floor, not a total.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
