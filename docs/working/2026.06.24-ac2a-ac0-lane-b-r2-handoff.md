# Lane B (operator/approval/Slack) — round-2 handoff → AC0

> **How to use this file:** AC2a's convergence handoff for **Lane B** of the round-2 v0.2-hardening wave, written for **AC0** (who merges all five lanes) — and readable by Justin. It records exactly what shipped on `r2/lane-b-operator-approval`, the design calls made + surfaced, the **cross-lane seams AC0 must wire at merge**, the **Mira identity beats** (separate brief), and the **v0.3 + ops follow-ups**. Everything here is build-level detail; the CEO-altitude version is in chat.
>
> *Authored 2026-06-24T16:49-07:00 by AC2a.*

---

## Status at a glance

- **Branch:** `r2/lane-b-operator-approval` (worktree `evryn-backend-operator-r2`), 5 commits off `main`:
  - `504ead0` — Build A (7 fixes)
  - `0437149` — Build B (6 items + the Step-64 revision)
  - `76fe81c` — Step-14 subject-override removal (Justin's correction)
  - `a9881a6` — Mira identity beats (triage.md / gatekeeper.md / operator.md)
  - `0f874d9` — Beat 3 / Step 67a runtime label (revision-prompt history → reference-only)
  - **Uncommitted at #lock (for Justin's SCM review):** a 1-char typo fix in `identity/activities/triage.md` ("so"→"So"); plus the #lock doc edits in `_evryn-meta` (current-state, CHANGELOG) and `evryn-team-workspace` (team current-state).
- **NOT merged, NOT deployed.** AC0 converges.
- **QC:** Build A **QC-GO**; Build B **QC-GO**; the `76fe81c` tear-out + the `a9881a6` Mira edits **QC-GO in one ganged trip**; the `0f874d9` Beat-3 label **QC in flight at lock time** (a tiny contained change — AC-reviewed clean; the QC verdict lands shortly). All AC-reviewed.
- **Typecheck green** on the final tree; all new + adjacent pure-fn test suites green (incl. the new `test-revision-prompt.ts`).

---

## Per-step ledger (every Lane-B Step + its outcome)

**Build A (`504ead0`) — operator/approval/Slack hygiene:**
- **Step 64** (`flow.ts` executeApproval audit) — recipient mislabel fix. *(SUPERSEDED by the Build-B revision — see below.)*
- **Step 62** (`config.ts` + `slack.ts`) — operator-channel auth gate. New OPTIONAL `config.slackOperatorUserId`; pure `isAuthorizedOperator` gate on the live handler AND the catch-up replay loop (uses `continue`, not `return`); fails OPEN when unset (+ one loud startup warning); blocks + `notifyDev`s a non-operator. **Security boundary — got QC heightened scrutiny.**
- **Step 28/65** (`slack.ts`) — replaced the dead `"approve via full-UUID supabase_upsert"` collision string with an honest remediation; added `threadTs` to `handleNotesMessage` so its replies thread. **Note:** the audit's expected *second* copy of the dead string in the stale-checker (`poll.ts`) **does not exist** (grep-confirmed; the poll.ts `supabase_upsert` is a live tool instruction) — so Step 28 == the one slack.ts string, now fixed.
- **Step 31** (`slack.ts` + test) — exported `parseSlackMessage`; rewrote `test-slack-parsing.ts` to exercise the REAL parser incl. the strict-regex REJECT cases (ADR-035 silent-diversion guard).
- **Step 26** (`flow.ts`) — dropped the Gold/Edge/Reply label from the operator review subject; classification stays in the body. `findPendingByApprovalSubject` confirmed dead code.
- **Step 27** (`flow.ts` formatReviewEmail) — approval handle moved to just after `--- END DRAFT ---`.
- **Step 33** (`flow.ts` submitDraftForApproval) — content-idempotency early-return on an identical re-submit; a differing body proceeds. *(Justin: keep it — the dangerous double-submit was already neutralized by code-approval + the executeApproval status guard; this de-dups the review-email/ping only.)*

**Build B (`0437149`) — MCP guards / threading / operator context / redact:**
- **Step 64 REVISION** (`flow.ts` executeApproval audit) — **the live design call.** On a `findUserByEmail(draft.draft_to)` MISS, write NO audit row (do NOT fall back to `item.user_id` OR `EVRYN_USER_ID`); `notifyDev` + stamp `metadata.audit_gap` (`recipient_unresolved`). **Why:** in v0.3 the item belongs to the gatekeeper but the email goes to a *contact*, so `item.user_id` would file the contact's outbound under the gatekeeper — a cross-user history bleed (hard-constraint violation). Skip-and-alert is safe in both eras. *(Justin caught this; approved skip-and-alert.)* **Heightened QC scrutiny — confirmed correct.**
- **Step 18b** (`classify.ts` supabase_upsert handler + pure `decideUpsertRouting`) — `emailmgr_items` status writes (`table===emailmgr_items` + `update_id` + record has `status`) route through `updateEmailmgrItem` (lifecycle-appending) instead of the raw `.update()`; out-of-whitelist key → `notifyDev` + raw fallback (no silent drop). **Design call (option b):** transparent-route, zero caller-repoint — chosen over a new typed tool / REJECT-guard (those drag the round-1 caller-repoint sprawl). **Marked v0.2 INTERIM in code — removable when Step 57 (runtime-does-bookkeeping) strips the raw status-write surface.**
- **Step 63** (`classify.ts` pure `validateUserExists`, fail-CLOSED) — fabricated/wrong Evryn-supplied UUID now caught (findUserById existence check) before the write on `append_pending_note`, `append_cross_user_note`, `set_thread_scope`, `rescope_messages` (scope_user_id), `notify_slack` (about_user_id). Mirrors the record_pass B-1 hardening. **Cross-user containment — heightened QC scrutiny, confirmed fail-closed.**
- **Step 14** (`classify.ts` submit_draft + `flow.ts` deriveThreading) — runtime-owned threading: dropped submit_draft's `in_reply_to`/`thread_id` params; direct-message item → `inReplyTo = external_id`, `threadId = metadata.thread_id`; forward/evryn_initiated → no headers (cross-account can't thread). Quoted-original block (`buildIncomingInboundBlock`) already prepends the referenced email. **Subject-override REMOVED in `76fe81c`** (see below). **Gold-DESTINATION wiring = AC0 seam (below).**
- **Step 15** (`slack.ts` handleOperatorMessage) — injects the scoped user's current `pending_approval` draft as discrete context into the CONVERSATIONAL re-draft path (not the formal `notes <id>:` path); strictly scope-bound; fail-safe on lookup error.
- **Step 68 (runtime half)** (`classify.ts` record_pass description) — softened "always tight" → tight by default, capture the brief *why* on a non-obvious downgrade; slam-dunk passes still write nothing. **Description-only, no logic change.** Triage.md beat → Mira.
- **Step 37** (`classify.ts` + `db/messages.ts`) — new operator-gated `redact_user_from_message(message_id, term)` tool (registered only with `threadScopeContext`); replaces the term with `[redacted user]` in `content_raw` + `message_body` (case-insensitive, regex-escaped), records that a redaction happened WITHOUT storing the term. **Operator-gated, mutates content — heightened QC scrutiny, confirmed.** operator.md repoint → Mira.

**`76fe81c` — Step-14 subject-override removal (Justin's correction):** SPRINT Step 14 is threading + quoted-history, NOT the subject. The round-2 *brief's* salvage added a forward "Re: <original subject>" override; Justin removed it — **Evryn titles her own responses.** `deriveThreading` now returns only `{inReplyTo, threadId}`; `submitDraftForApproval` uses Evryn's drafted subject directly. `subjectOverride`/`original_subject` are zero-hits in Lane B. Threading + quoted-block kept.

---

## Cross-lane seams AC0 must wire at merge

1. **Step 14 gold-DESTINATION → Step 61 (Lane A).** `submitDraftForApproval` has a `// TODO(AC0 convergence, Step 61):` comment at the `draft_to` line. Lane A (`ingest-r2`) provides the `users.outbound_address` field + the pure `resolveGoldDestination(gatekeeper)` helper (returns `outbound_address ?? email`); the `register` worktree adds the Evryn/operator *setter* for the gold-destination. **Nobody has wired `resolveGoldDestination` into the send.** At convergence: route the gold notification's `draft_to` through `resolveGoldDestination(gatekeeper)` (a ~2-line change in `submitDraftForApproval`, once Lane A's `db/users.ts` is merged in). Graceful-degrades to the gatekeeper's primary email if `outbound_address` is unset.
2. **Step 32 transport failure-test → Lane A's `client.ts`.** Lane A (`ingest-r2`) added the injectable `transport` param to `sendEmail` (`SendTransport = (raw, threadId?) => Promise<void>`, 2nd positional, defaults to real Gmail). Lane B's half — an `executeApproval` retry/rollback failure-path test that injects a throwing transport — **can't be written in Lane B's isolated worktree** (`client.ts` is Lane A's file; the param isn't here). **Post-merge:** add a test that drives `executeApproval` with a throwing `transport` and asserts the 3-retry/backoff + the `error`-status + the "do NOT re-approve" notifyDev path. (Lane B did NOT modify `client.ts` — it's Lane A's.)

---

## Mira identity beats (paired, ON this branch)

Three runtime changes have a paired identity edit. **Brief: `_evryn-meta/docs/working/2026.06.24-ac2a-mira-lane-b-identity-beats.md`.**

**DONE — committed `a9881a6` on this branch** (the per-lane "Mira-in-the-AC's-worktree" model, so each beat rides with its runtime half; verified against the runtime that's already in this worktree). The beats:
- **Beat 1 — `identity/activities/triage.md`** (Step 68): two-tier pass reasoning (obvious → none; non-obvious downgrade → brief why) + a Phase-2 reconciliation Mira caught (dropped the now-false "re-derivable any day" claim).
- **Beat 1b — `identity/situations/gatekeeper.md`** (NEW, Mira-added, Justin-approved): "read the triage record before answering a gatekeeper question about someone already triaged" — the *retrieval* half of Beat 1's write (without it the captured why never gets used).
- **Beat 2 — `identity/situations/operator.md`** (Step 37): redact-recovery repointed (both mid-thread-bleed + wrong-scope) from raw `supabase_upsert` to `redact_user_from_message` (args/gating verified against the runtime).
- **Beat 3 — Step 67a: NO `core.md` edit** (Mira's call, and she's right — core.md ~line 240 already covers it; a core.md addition would bloat an always-loaded file for a single-pathway case). It became a **RUNTIME-only fix, committed `0f874d9`**: `handleRevisionNotes` now labels the revision-prompt history block reference-only so Evryn won't echo it into the revised draft. AC-reviewed clean; QC in flight at lock time.

**Cross-lane overlap to watch (Mira flagged):** Lane B's Mira beats touch `triage.md`, `gatekeeper.md`, `operator.md` (**not** core.md). **`triage.md` is touched by three lanes in different sections** — Beat 1 here, Lane A's sender-classification line (~line 48), Lane C's truncation canary (bottom) — different sections → clean merges, but flagged so the multi-lane file isn't a surprise at convergence.

The identity edits ride the convergence deploy (`identity-file-review.md`); they were **reviewed in the ganged QC trip (GO)** alongside the `76fe81c` tear-out, with the AC + QC reviewer requirement satisfied.

---

## Follow-ups (v0.3 + ops)

- **v0.3 — Step 64 proper fix (Justin's handoff requirement):** the skip-and-alert is the v0.2 INTERIM. The proper fix is **recipient-binding at submit time** — bind the recipient to an authoritative `user_id` on the draft so the lookup can't miss. **Track it as a real v0.3 candidate in `SPRINT-V0.3-CANDIDATES.md`** (not just a code comment); it pairs with the audit's "submit_draft does not bind the recipient to the item's user" finding.
- **Ops — `SLACK_OPERATOR_USER_ID` is ALREADY set in Railway prod** (`U09GKFYLFDE`, set 2026-06-24 with `--skip-deploys`, no restart). The Step-62 gate goes live *closed* the moment the bundle deploys — **no further env action needed.** (If a staging Railway service exists, set it there too.)
- **Step 18b removability for Step 57:** when Step 57 (runtime-does-bookkeeping) lands, the supabase_upsert→updateEmailmgrItem routing becomes dead and should be removed; Step 18b only closed the *audit-trail* symptom, not the cost/error one (the leftover non-pass statuses stay hand-written until Step 57). Flagged in the code comment.
- **v0.3 — batch redact (Mira flag):** `redact_user_from_message` is single-message; a multi-message bleed = N calls (operator.md's "one call per message" covers it today). A `message_ids[]` batch variant would make a multi-message recovery cleaner + remove the risk she redacts some-but-not-all. Track as a v0.3 enhancement; not a blocker.
- **Fast-follow — label the initial-draft history too (QC non-blocker from the `0f874d9` review):** Step 67a labeled the *revision*-prompt history reference-only; the *initial*-draft prompts (`processForward` / `processDirect` / `checkFollowUps` / `checkProactiveOutreach`) hand Evryn the same *unlabeled* `formatConversationHistory` block and also end in `submit_draft` → send — so the same echo / double-attach risk exists there, guarded only by core.md line 240. Clean fast-follow: factor a shared reference-only history wrapper and apply it at those four sites. **Pre-existing, non-blocking** (not a regression from any Lane B change); a v0.2/v0.3 hardening candidate.

---

## QC outcomes (all GO) + AC review

Every real code change got QC + AC review:
- **Build A (`504ead0`)** — QC-GO (heightened scrutiny on the Step-62 security boundary).
- **Build B (`0437149`)** — QC-GO (heightened scrutiny on 63/37/64 cross-user containment).
- **Ganged trip — the `76fe81c` tear-out + the `a9881a6` Mira identity beats** — QC-GO. Tear-out confirmed clean + dangle-free (the specific tear-out concern); Mira's beats confirmed to match the runtime (tool signatures/gating/retrieval path). Two cosmetic nits: a triage.md typo (now superseded by Justin's em-dash tweak) + a long `deriveThreading` doc-comment (post-convergence tidy).
- **Beat 3 (`0f874d9`)** — QC-GO, no blockers. AC-reviewed clean. **One non-blocker** → the "label the initial-draft history too" fast-follow above. **One transparency flag worth AC0's awareness:** that QC read `ARCHITECTURE.md` to line 821 of 1207 (it flagged this honestly, not silently). AC2a judged it immaterial for this one-line revision-prompt label — the governing sections (Identity Composition, Pipeline, Approval Gate, all in 1–821, + core.md read in full) were loaded; the unread tail is v0.3/security/diagram material. A full-ARCH re-run is available if anyone wants it before relying on the GO.

**`76fe81c` subject-override removal** is a 2-file revert to the pre-Build-B subject behavior (Evryn's drafted subject) — QC had already vetted that target state in Build A (Step 26 touched the *operator* approval_subject, never the outbound draft_subject). It was reviewed in the ganged trip above.

## Not attempted (optional)
Lane B's "if time" steps **23** (Slack pong-timeout warn-level), **24** (WebSocket heartbeat false-positive on restart), **38** (notify_slack webhook-fallback startup-window audit gap) were **not attempted** — out of scope against the core + audit-derived set. They remain TODO in `SPRINT-V0.2-HARDENING.md`.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
