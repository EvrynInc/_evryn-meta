# Handoff — AC0-37 → **AC0-37b** — written 2026-08-13T16:26-07:00

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file: THIS IS YOUR ENTIRE BRIEFING.** It supersedes `2026.08.13-ac0-ac0-handoff.md` (AC0-36 → AC0-37) operationally. That file is **not retired** — it holds the subagent ledger (§2), the fragment sweep (§11) and the merge-hazard detail (§3) — but everything in it that you need to *act* on has been re-derived, corrected and re-stated here against a full load. **Where the two disagree, this file wins.**
>
> **Why you exist:** AC0-37 spent its whole budget on the load (~12,000 lines, ~450K tokens) and had ~135K left when the plan was finished. Justin's call: hand the *execution* to a fresh instance with a pre-computed load list, so you start with ~500K of working room instead of ~135K.
>
> 🔴 **AC0-37 READ ALL OF IT AND WROTE THIS AGAINST THAT READ.** Every claim below is either something it read at source or something it measured with a command that is reproduced here so you can re-run it. Nothing is inherited from a prior handoff unchecked. Where AC0-37 could *not* verify something, it says so.

---

> # ▶️ START HERE. Do these in order, without being asked.
>
> 1. **Read `_evryn-meta/CLAUDE.md` from disk, in full** (612 lines), and confirm its bottom canary. **Do not trust the auto-injected copy** — it is a session-start snapshot and has been observed serving content committed hours earlier.
> 2. **Delete `.claude/settings.local.json` if it exists** (standing hygiene; flag to Justin first if it contains secrets), and run the repo-sync check in `CLAUDE.md` → SESSION STARTUP.
> 3. 🔴 **DO YOUR ENTIRE LOAD — §1, exactly as written — AND THEN STOP.** This is Justin's veto point and the main one he wants. **Do not begin the merges. Do not edit anything.**
> 4. **Then report two things, in one message:**
>    - **What you loaded** — every file, the line spans you actually read, and the canary you confirmed on each. ⚠️ **Load PRECISELY as §1 describes.** If you skipped something, say so and why; **absent a compelling reason he will simply send you back to load it**, and you will have spent a round trip. *(AC0-36 twice mis-stated its own load — once claiming a 941-line protocol read that was ~500. A load you report is auditable; a load you assume is not.)*
>    - **Your plan** — what you intend to do, in what order, and anything in this handoff you think is wrong.
> 5. 🔴 **WRITE THAT REPORT FOR SOMEONE COMING IN COLD.** Justin has not read this handoff, is not reading your documents, and is arriving from a different lane after hours on other work. **Never name a section by number** — describe the thing. Never write a bare category noun (*"a source-of-truth doc," "a lane," "a guard"*) without naming which one; the test is *"if he had to guess WHICH ONE I mean, could he guess wrong in a way that changes how he feels about it?"* **Open with a heading `# AC0-37b's read — …` so he can find your voice in the tool output.**
> 6. **WAIT for his go.** Then execute §3 in order. Ping `#team-alerts` prefixed `AC0-37b:` at every state change — and **every time you have a response for him, no exceptions.**
>
> **⚠️ IF JUSTIN IS NOT AVAILABLE after you report:** every merge in §3 needs his explicit go and you may not infer one. **Do not idle.** Work §4 (draft the six Steps into a scratch file, ready to paste), §5 (audit which ACf13 items now have a home), §7 (route the learnings), and §6.6's seven standalone wins — **then batch every question into ONE numbered recommend-first list and ping him.** A blocking question is not a stop.

---

# §1 — 🔴 YOUR LOAD LIST, AND WHY IT IS SHAPED THIS WAY

## 1.0 The insight that makes this list small: **DO THE MERGES FIRST, FROM THE RECIPES IN §3, WITHOUT READING BOTH SIDES.**

AC0-37 read both sides of every pending merge. **The reconciliations are pre-computed in §3 and they are tiny** — a two-line re-apply on one, a thirteen-line block move on another. You do not need to load the pre-merge originals to execute them.

⇒ **Merge first. Then load the merged docs once.** That is roughly **150K cheaper** than loading branch-and-main versions of the sprint and current-state and then merging.

⚠️ **This is only safe because §3 is executable.** If a recipe does not match what you find on disk — a diffstat that disagrees, a section that has moved — **STOP and read both sides yourself.** A recipe that has drifted is worth less than nothing.

## 1.1 TIER 0 — before you load anything else (~15K)

| # | File | Lines | Why |
|---|---|--:|---|
| 1 | **THIS FILE** | ~430 | Your briefing. |
| 2 | `_evryn-meta/CLAUDE.md` **from disk** | 612 | Your manual. **Read it from disk** — auto-injection is a session-start snapshot and has been observed serving stale content. |

**Then execute §3's merges.** Nothing else needs loading first.

## 1.2 TIER 1 — after the merges (~150K)

| # | File | Lines | Note |
|---|---|--:|---|
| 3 | `docs/protocols/ac-orchestration-protocol.md` | **~692 post-merge** (941 pre) | 🔴 Gate on spinning anything. Read all of it. Post-Lane-A it is the core + an index. |
| 4 | `docs/protocols/orchestration/spinning-an-ac.md` | 263 | Post-Lane-A. The AC variant, the tier rule, the escalation ladder, the work brief, the lane-AC lifecycle. **You will spin lane ACs; this is not optional.** |
| 5 | `docs/protocols/orchestration/spinning-dc-qc.md` · `spinning-a-team-agent.md` | 46 + 44 | Post-Lane-A. Cheap. |
| 6 | `docs/current-state.md` | ~124 post-merge | Post-Lane-B. |
| 7 | `docs/hub/roadmap.md` | 197 | Light cascade. |
| 8 | `docs/protocols/ac-writing-protocol.md` | 101 | 🔴 Gate on writing to any doc. |
| 9 | `docs/protocols/doc-update-routing-protocol.md` | 105 | **The single most on-point doc for your session** — it is the lookup for "which surface does this change touch, and may I edit it or must I route it." |
| 10 | `_evryn-meta/CHANGELOG.md` | 234 | You are writing an entry. |
| 11 | `docs/repo-inventory.md` · `docs/deploy-log.md` | 40 + 38 | Cheap; both load-bearing for merge/deploy claims. |
| 12 | `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` | **~663 post-merge** | Where the six Steps land. **Densest doc we own (~45K).** |
| 13 | `_evryn-meta-lane-a/docs/working/2026.08.11-ac0-acm-loading-architecture-brief.md` | **710** | ⚠️ **BRANCH copy.** Main's is 137 and has none of §8–§14. **§14 is new (2026-08-13) and holds the recovered frozen-sentence drafts.** Read §10–§14 first; §8–§9 are history. |
| 14 | `_evryn-meta-lock/docs/working/2026.08.12-acf13-ac0-agent-patterns-postmerge-brief.md` | 76+ | ⚠️ **ACf is still appending to it.** See §5. |

## 1.3 TIER 2 — load only when a specific beat needs it

- `_evryn-meta/LEARNINGS.md` (1,016) — **when you route the five drafted learnings** (§7). Not before.
- `evryn-backend/docs/gate-b-plan.md` (308) — **when you brief the Gate-B reviewer.** Its §8 is a six-item ballot for Justin.
- `docs/protocols/lock-protocol.md` (96) — **at the `#lock`, not before.** Justin: *"before that, it's just noise."*
- `evryn-backend/backups/README.md` — when you resolve the untracked files / take the `pg_dump`.
- `evryn-backend/docs/atlas/` (12 files, 3,108) — **only if you make a runtime claim.** Start at `00-INDEX.md`. The four build briefs in `2026.08.13-ac037-ac038-marching-orders.md` are already written and cite `file:line`; you should not need the Atlas to hand them off.

## 1.4 🚫 What you do NOT load, and why

- **`evryn-backend/src/`.** Conductor cascade. The Atlas + `docs/dependency-map.md` are the targeted-read mechanism.
- **`ARCHITECTURE.md` (1,285) · `BUILD-EVRYN-MVP.md` (652) · `technical-vision.md` (398).** AC0-37 read all three. **Nothing in your session requires them** — you are merging, minting Steps that are already drafted for you in §4, and handing off briefs that are already written. **Load `ARCHITECTURE.md` only if you find yourself about to assert how the runtime behaves.** That is ~73K you do not need to spend.
- **`evryn-backend/CHANGELOG.md`.** AC0-37 skimmed the top 107. You write the *meta* changelog; the backend entry belongs to whoever merges backend code, which is you — but a ~30-line entry does not require reading 266 lines of history.
- **The three mailboxes.** Justin: boilerplate only.
- **Main's pre-merge copies of the sprint and current-state.** §3 replaces them.

**Estimated total: ~165K, leaving you ~830K.**

---

# §2 — THE STATE OF PLAY, IN ONE PAGE

**Five branches are pending. Nothing is deployed. Prod is `v0.2.9`.**

| Branch | Repo | Holds | Merge state |
|---|---|---|---|
| `dc/step106-nul-bytes` | `evryn-backend` | Step 106 — de-NUL `halt-signal.ts` + Atlas corrections + regenerated dependency map | ✅ built, QC-GO at `f608d95` |
| `acb/sprint-restructure` | `evryn-backend` | Sprint doc: axis flip to open-vs-closed, deploy-header removed, Step 107 minted | ✅ built, awaiting Justin's SCM read |
| `acb/current-state-restructure` | `_evryn-meta` | current-state: 152 → 124 lines, new 📮 BULLETIN section | ✅ built, awaiting Justin's SCM read |
| `acm/loading-architecture` | `_evryn-meta` | 🔴 The router + the four agent definitions + the protocol split. **12 files, +2,861/−912.** | ✅ built; **three preconditions, all Justin's** |
| `acf13/lock-2026-08-12` + 2 more | `_evryn-meta` | **ACf's. NOT YOURS.** | 🚫 Route through ACf. |

**Anything whose branch name starts `acf` is ACf's and is not your problem at all** (Justin, 2026-08-13). ACf is a peer coordinator, not a report. You exchange findings; you do not sequence him.

**Six worktrees on `_evryn-meta`, plus `evryn-backend-lane-b`, `-lane-e`, `-m1…m6`, `-sweep`, `-qc-base`, `-qc-head`.** 🔴 **DO NOT REAP ANY OF THEM until Justin says so** — his explicit instruction, twice. And when you do: **count `node_modules` before and after, `npm ci` if it dropped.** That hazard has fired three times.

---

# §3 — 🔴 THE MERGE RECIPES. Executable. Run them in this order.

> **The order and its rationale — Justin asked that this be written down so nobody re-litigates it.**
>
> **Lane E first** because it is the only `src/`-touching branch, and its QC GO is *arithmetic against a specific `src/` tree* (45 files / 20,105 lines) — its whole contribution is *correcting* line-count claims in the Atlas, so any other `src/` change landing first falsifies exactly what it fixed. Nothing else pending touches `src/`, so landing it first costs nothing and protects its verdict.
>
> **Then the two Lane B branches**, because they are docs-only and cannot disturb Lane E — and because the sprint merge is the precondition for minting the six Steps (§4).
>
> **Lane A last**, because it rewrites the documents *every other agent loads*. Nothing should be spun against a half-landed router, and a merge conflict there is far more expensive to reason about than one in a tracker.

## 3.1 Lane E — `dc/step106-nul-bytes` → `evryn-backend` main

**What it does:** removes the literal NUL bytes from `src/safety/halt-signal.ts` (preserving the runtime sentinel property via an escape), corrects three Atlas sections, regenerates the dependency map.

🔴 **Why it goes first and why it matters more than "a tidy-up":** `evryn-backend/docs/atlas/07-safety.md:330-332` on `main` **right now** says the halt sentinel is **space**-delimited — *"note the leading and trailing space"* — twice, with emphasis. **They are NUL bytes.** AC0-37 read that line at source. Three separate agents made the identical misreading because every text-rendering path shows a NUL as a space. Until this merges, any agent reading the Atlas to understand the circuit-breaker's halt contract **learns a false byte-level fact**, and the "obvious tidy-up" it invites is the change that silently disables the lossless halt.

**Recipe:**
```bash
cd /c/Users/Justin/Evryn/Code/evryn-backend
git merge-base --is-ancestor main dc/step106-nul-bytes || echo "REBASE NEEDED"
git diff --stat main...dc/step106-nul-bytes     # THREE dots. Expect ~7 files.
```
⚠️ **Its QC GO covers `f608d95` against base `6995007` and does not extend past either.** If `main` has moved on `src/`, re-check the Atlas line counts before merging.

⭐ **A freebie: this branch also regenerates `docs/dependency-map.json`, which is stale on `main`** (the committed artifact is missing `tests/env-seed.ts` and 24 `value` edges — it landed 2026-08-10 without regeneration). **Merging Lane E closes findings-doc owed item 4 for free.** The staleness is entirely in the `tests/` half; the `src/` reverse index was never at risk.

### 🔴 SIX THINGS FROM LANE E's DC AND QC THAT EXIST NOWHERE ELSE. They die with those agents; they are written down here.

**From the DC — for any future editor of `src/safety/halt-signal.ts`:**

1. 🔴 **Reintroducing the defect HIDES ITS OWN REINTRODUCTION.** Put a literal NUL back — via a codemod, a find-and-replace, a "simplify" — and `git diff` renders *"Binary files differ"* again. **The review that would catch it is blinded by the very change it is reviewing.**
2. 🔴 **Never retype or copy the sentinel — always import the constant.** Anyone reconstructing it from a *rendered* view gets spaces and a value that fails `===`. **That is exactly how the Atlas came to assert space delimiters in the first place.**
3. **The invariant is "no literal NUL byte in the source," NOT "the escape is spelled a particular way."** The correct check is **byte-level** (`Buffer.indexOf(0) === -1`), not a string match. `\x00` is equally valid.
4. **Nothing guards any of it.** The repo-wide NUL scan exists only as a **scratchpad script — not in the repo, not in CI.** Step 106's own text says such a scan belongs in every future security sweep. **It still does not exist; that is the gap the DC would have closed next.** ⇒ **Consider whether Step 106's merge should carry it, or whether it earns its own small Step.**

**From the QC:**

5. **The CRLF scare, ruled out — and it WILL re-scare the next person.** The BASE worktree is all-LF and HEAD is all-CRLF, so a naive worktree byte-comparison shows a **phantom 56-byte delta that looks exactly like corruption.** **The committed blobs are both pure LF.** BASE escaped conversion only because git classified it binary. **Do not panic; do not "fix" it.**
6. 🔴 **The GO means *"this change does what it claims, safely."* It does NOT mean the halt contract is verified — the opposite is what it proved.** In its own words: *"It got **visible**, which is different from **guarded**."*

### ⚠️ And one flagged-but-unanswered risk BOTH Lane E agents raised independently

🔴 **Postgres `text` columns reject U+0000.** If the breaker trips during an operator Slack message, a string containing literal NUL code points appears to flow toward `createMessage(content_raw)` at **`src/notify/slack.ts:951`**. **Neither agent had DB access; both say it is pre-existing and unaffected by their change either way.**
⇒ **Ten minutes against the DEV database settles it. It should NOT gate this merge — and it must not get lost.** Either close it tonight (it is item 1 of §6.6's standalone wins) or file it as a Step.

### ⚠️ Step 106 conflates TWO files, and one of them is CORRECT CODE

`current-state.md:54` and Step 106 both lump `scripts/dependency-map.ts` together with `halt-signal.ts` as *"two files that contain a NUL byte."* **They are not one item.** `dependency-map.ts:443` uses `` `${rel}\0${target}` `` — a NUL as a **deliberate composite-key separator**, the idiomatic choice because it cannot collide with a filesystem path. **Routing them together sends someone to "fix" correct code.** **Split them when you flip Step 106** (findings §4.2, §7 item 6).

## 3.2 Lane B sprint — `acb/sprint-restructure` → `evryn-backend` main

**The reconciliation, measured exactly (do not re-derive it):**

```bash
git log --oneline 6995007..main -- docs/SPRINT-V0.2-HARDENING.md
#   f343eb8  Docs: repoint AGENT_PATTERNS citations at LEARNINGS.md
git diff --stat 6995007..main -- docs/SPRINT-V0.2-HARDENING.md
#   1 file changed, 2 insertions(+), 2 deletions(-)
```

🔴 **`main` gained exactly TWO lines the branch does not have, and the branch rewrites the file wholesale — so a clean merge silently drops them.** They are ACf-13's, and his own brief predicted this collision by name (*"this is the one to check first"*). Both are the same edit: a citation of `_evryn-meta/AGENT_PATTERNS.md` repointed to `LEARNINGS.md`.

**Both live inside Step-101 and Step-98 prose. Re-apply after merging** — find the two occurrences of `AGENT_PATTERNS.md` in the merged file and replace each with:

> ``_evryn-meta/LEARNINGS.md` (absorbed from `AGENT_PATTERNS.md`, collapsed 2026-08-12)`

**Then, in the same pass:** **flip Step 106 → DONE** if Lane E landed. ACb placed it `TODO` — correct when written, and she flagged it as *"the one thing in this delivery most likely to be wrong by morning."*

## 3.3 Lane B current-state — `acb/current-state-restructure` → `_evryn-meta` main

🔴 **A CLEAN MERGE IS THE DANGEROUS OUTCOME. The branch rewrites this file wholesale.**

**Measured:** `main` gained **17 lines across two commits** (`4d69593`, `0066754`) since the branch was cut. They are two distinct pieces:

1. ✅ **The Atlas block** (4 lines, `4d69593`, AC0-36's) — **ALREADY COVERED. AC0-37 verified this at source; do not spend a cycle on it.** ACb carries the correction at **her `:59`**, in her own words and *better* than main's — it states the Atlas **is merged**, cites her own at-source verification, and points at `npm run depmap`. ⚠️ **A grep for `00-INDEX.md` returns 0 on her file and is a FALSE ALARM** — the wrong needle. AC0-37 nearly wrote that grep into this handoff as a hazard; it would have sent you hunting a non-problem.
2. 🔴 **ACf-13's team-runtime blockquote** (11 lines, `0066754`) — **THIS IS THE ONE THAT DISAPPEARS SILENTLY.** It must land in the branch's new **📮 BULLETIN** section (her `:103`) — *"the section that exists precisely because that content had nowhere to go."* Fold it in dated and signed, per the bulletin's own four rules.

**Pull it verbatim rather than retyping it — this command is exact and cannot go stale:**
```bash
cd /c/Users/Justin/Evryn/Code/_evryn-meta
sed -n '/UPDATED 2026-08-12 (ACf-13)/,/Narrative: that repo/p' docs/current-state.md
```
⇒ **You do not need to read main's pre-merge `current-state.md` at all.** That command is the entire reconciliation. **Compare by CONTENT, never by git's verdict.**

## 3.4 Lane A — `acm/loading-architecture` → `_evryn-meta` main

### 🔑 First, the answer to Justin's question: *"we have the subagent cutover, but we also have the orchestration protocol cutover — is that all Lane A?"*

**Both are Lane A, both are already BUILT on the branch — but only ONE of them needs a cutover at all.**

| | What it is | How it lands |
|---|---|---|
| **The protocol restructure** | 941 → ~692-line core + an index + `orchestration/spinning-{an-ac,dc-qc,a-team-agent}.md` | **The merge IS the whole thing.** No cutover, no session restart, no verification step. It is a file move; the next agent to read the protocol reads the new one. |
| **The agent-definition cutover** | `.claude/agents/{ac,dc,qc,oc}.md` becoming the single home; the three repo `CLAUDE.md`s becoming redirects | 🔴 **Needs a FRESH SESSION**, because the agent-type registry is read **once, at session start.** |
| **The router** (`_evryn-meta/CLAUDE.md` → 78 lines) | What every agent auto-inherits | Lands with the merge; takes effect for **new sessions only**, which is exactly why merging mid-session is safe. |

⇒ **Say this plainly to Justin: only the agent definitions need the restart. The protocol split needs nothing but the merge.** That halves the perceived risk of the merge.

### The three preconditions — all Justin's, none yours

1. 🔴 **THE FROZEN SENTENCES — and there are THREE, not two. The drafts EXIST now.** ACm was resumed 2026-08-13 and **§14 of the lane-a brief holds them verbatim.** Read §13.2 (the originals) and §14.2 (the drafts) together.
   - **Sentence 1** — `ac-orchestration-protocol.md`, DC/QC/OC `<identity>`. Draft exists. Approve/adjust.
   - **Sentence 2** — `orchestration/spinning-an-ac.md`, substitution 1. Draft exists but 🔴 **it hardcodes `_evryn-meta/identity/ac.md`, which is STALE** — the file is now `_evryn-meta/.claude/agents/ac.md`. **Whoever finalises must swap the path.** ACm deliberately left it wrong rather than half-correct a frozen-block draft.
   - **Sentence 3** — the manual-path list. **TRUE today, false only after cutover.** Batch it so Justin rules once.
   - **Plus §14.3: three more `CLAUDE.md` references inside the frozen `<mandatory_load>` block.** ACm's recommendation — and AC0-37 agrees, it is cheaper and precedented — is **two additive AC-variant substitutions**, exactly as the team variant already solves the identical problem, rather than editing shared text. **Still Justin's call.**
   - 🔴 **VERBATIM OR NOTHING. Do not reconstruct, do not paraphrase, do not paste the `[…]` elision from ACm's report** — §14.2 warns it meant *"unchanged,"* not proposed text.
2. **Three cross-repo pointers** — `evryn-dev-workspace/CLAUDE.md:13`, `evryn-quality/CLAUDE.md:19`, `evryn-ops/CLAUDE.md:13`, each sending its agent to *"AC's `CLAUDE.md`"* for the sync ritual. ⚠️ **Re-grep; line numbers drift:** `grep -n "AC's .CLAUDE.md" <repo>/CLAUDE.md`. These **cannot ride an `_evryn-meta` branch** — they are a coordinated same-window change in three repos. **The cutover dissolves them entirely** by making the reference intra-repo, so consider doing them *at* cutover rather than before.
3. **A rebase**, then **re-run the frozen-block byte check.** ⚠️ **Normalize CRLF (`tr -d '\r'`) or it false-FAILS**, and **mutate a line you have PROVEN is non-blank** or your control silently passes. Command in §11.3 of the lane-a brief.

### Then: the cutover is AC0-38's first task. See §6.

---

# §4 — MINT THE SIX SWEEP FINDINGS (Justin approved: ballot 4 = yes, all six)

🔴 **AFTER `acb/sprint-restructure` merges, never before.** The branch rewrites the sprint wholesale; six Steps minted into `main`'s copy now would be silently discarded by the merge — the exact failure ACb wrote her whole close-out about.

## 🔑 THE SIX ARE PRE-WRITTEN BELOW, READY TO PASTE. ⇒ **You do NOT need to load `2026.08.12-qc-sweep-findings.md` (637 lines, ~17K) to mint them.**

AC0-37 read the findings doc in full and drafted these against it. **Each carries its evidence section so a reader can go check.** Place them in the **🔵 LIVE region**, under *"2 · Open defects — verified at source, routed, not yet built."* Numbers continue from 107. **Adjust the wording freely — you own it — but do not lose the mechanism, the consequence, or the provenance line.**

> ⚠️ **Load the findings doc only if you want to challenge one of these.** Its §7 also carries two Justin-gated items you should surface (§4.1 below).

---

**108. TODO — 🔴 THE OPERATOR AUTH GATE FAILS OPEN ON A BLANK VALUE, and it is unprotected at both call sites. This is the gate that decides who may approve outbound mail to real people.**
> **Two independent defects, both established by execution during the 2026-08-11/12 mutation sweep (findings §C2):**
> **(1) It fails OPEN on blank.** An executed 4-case probe through the real config and the real predicate found that **UNSET, `""` and `"   "` ALL AUTHORIZE A TOTAL STRANGER.** Only a properly-set value closes the gate. `config.ts:680` makes `SLACK_OPERATOR_USER_ID` optional — no `required()` — so nothing boot-fails. **This is the same empty-string class proven live on the runaway detectors (Step 113), now on the approval gate.**
> **(2) It is unprotected at both call sites.** Deleting the gate at *either* `notify/slack.ts:486-495` (the live handler) **or** `:1166-1175` (the disconnect catch-up replay) leaves **all 60 suites GREEN.** `tests/test-operator-gate.ts` exercises only the **pure predicate**, which is genuinely discriminating (the control mutation drove it RED with 3 assertions). ⇒ **The security boundary is well-tested and unenforced** — the classic predicate-tested / call-site-untested shape.
> ⚠️ **The sole mitigation is `warnIfOperatorGateOpen`, which is un-exported, has zero test references, is reachable only via a live Socket Mode connection, and only `console.warn`s. Nothing alerts.**
> **Note this REOPENS Step 62, which reads DONE.** 62 is the step that created this gate. Do not re-mark it; leave the history legible.
> **Acceptance bar: a mutation.** Delete each call site in a sandbox and confirm the verdict flips. — [AC design → DC+QC]

**109. TODO — 🔴 THE ENTIRE `runEvrynQuery` REGION (~500 lines) HAS NO EXECUTING TEST — the runaway-cost kill switch included.**
> **Established structurally, not inferred (findings §D1):** `test-query-timeout.ts` (112 assertions) **declares itself "PURE-LOGIC tests"** and says at `:529-530` that it *"simulates the relay rather than driving `runEvrynQuery`."* `test-breaker-resume.ts` drives `planPollBatch` through a **local mirror** and does not import `classify.ts` at all. `test-breaker-detectors.ts` imports the pure predicates from `src/safety/*`. **No suite that runs in SAFE mode drives the function.**
> 🔴 **Consequence: disabling the M1 breaker short-circuit at `classify.ts:2669` — the runaway-cost kill switch — passes 1,925 green assertions and a clean typecheck.** `ARCHITECTURE.md:1164` describes that check as *"checked at the TOP of `runEvrynQuery`."* **Deleting it is invisible.** Same for `includePartialMessages` (the property that makes the silence watchdog safe) and the explicit post-loop timer disarm.
> 🔑 **The transferable mechanism, and why nobody saw it: the pure decision helpers were extracted FOR TESTABILITY and are tested well. What the extraction left behind — the stateful shell that composes them — is unguarded. The high assertion count is exactly what stops anyone looking.** The test files are honest about it in their own headers; nothing downstream carries the caveat, and the Atlas records these as STRUCTURAL.
> ⚠️ **Scope this deliberately with Justin before building** — driving `runEvrynQuery` in a test means either injecting the SDK boundary or spending real money. **The design call is how, not whether.** — [AC design → DC+QC]

**110. TODO — 🔴 THE 3AM TRAP: on a send failure the system instructs the operator to do something STRUCTURALLY IMPOSSIBLE, and strands the draft.**
> **On a send failure after 3 retries, `approval/flow.ts:673-676` sets the item to `status: "error"`. The alert it then sends says:** *"Item still at pending_approval — Justin can retry `approve <short_id>`."*
> **`findPendingByShortId` filters `.eq("status","pending_approval")` (`flow.ts:911`). An `error` item can NEVER match.** ⇒ the retry returns *"I don't see a pending draft matching…"* — and **the repo's own live test independently confirms the alert is false**, asserting `status === "error"` at that exact point (`test-execute-approval-transport.ts:153`).
> ⇒ **After a Gmail outage the draft is UNREACHABLE from the operator surface and recovery requires a manual Supabase edit. This fires at the worst possible moment — a real outage, at night, with the operator following the instruction the system just gave him.** (findings §C1)
> **Fix direction is a design call, not just a string:** either correct the alert to name the real recovery, or make an `error` item re-approvable. **Justin should pick** — the second is more useful and more invasive. — [AC design → Justin → DC+QC]

**111. TODO — 🔴 FIVE SHIPPED SECURITY INNER-DOORS ARE UNPROTECTED AGAINST REGRESSION. All five are named as live bulkheads in `ARCHITECTURE.md` §"Bulkhead Layers (ADR-047)".**
> Each was mutated and the suite stayed green (findings §D3):
> - **`classify.ts:1724`** — `BLOCKED_PATHS`, **layer 2 of the documented THREE-layer operator security** (`ARCHITECTURE.md:748`, *"the tool blocks it"*). Disabling it makes `situations/operator.md` **readable by conversational Evryn.**
> - **`classify.ts:1727`** — the `..` path-traversal gate.
> - **`classify.ts:705`** — the untrusted-content fence on the forwarded email body. `test-fence.ts` covers `fenceUntrusted` **as a function**; **nothing asserts `buildForwardedEmailPrompt` still calls it.**
> - **`classify.ts:2914`** — the **web-egress deny-hook matcher**; breaking it un-gates `WebFetch`/`WebSearch` (the SSRF / cloud-metadata / exfil door). `makeWebToolGuardHook` is referenced by **no test.**
> - **`classify.ts:1701`** — the 50-row cap on `supabase_read`. The *policy* has 118 assertions; **the handler's use of `effectiveLimit` is unguarded.**
> 🔑 **The shape is identical in all five: a well-tested pure predicate, an untested call site.** *"The guard is tested and unenforced."*
> ⚠️ **Sequence AFTER the `classify.ts` extraction** — all five live in the lines that refactor moves. **But file the Step now**, because the extraction's safety claim is *"behaviour is unchanged,"* and these are the guards that would prove it. — [AC design → DC+QC]

**112. TODO — The thread-scope family has ZERO tests.** `getThreadScope` performs **no `.order()`** (`db/messages.ts:162-169`), so if a thread ever held two non-null scopes the answer is whichever row Postgres returns first — unspecified. `setMessageScope` (`:245-256`) and `rescopeMessages` (`:369-419`) can each **create** a two-scope thread. **The one-scope-per-thread property that ADR-030's entire cross-user isolation rests on is upheld by callers and convention, not by this layer** (findings §5.2; Atlas `06-persistence.md` §4.3).
> **An unrestricted `backfillThreadScope` also repoints an already-established thread's scope**, and the nondeterministic read then surfaces **the wrong user's profile in an operator turn** — in exactly the channel ADR-030 exists to isolate. — [AC design → DC+QC]

**113. TODO — 🔴 THE EMPTY-STRING ENV CLASS: `??` does not catch `""`, so a guard that is CREATED AND THEN CLEARED silently stops guarding.** ⚠️ **This is a DIFFERENT MECHANISM from Step 104's `parseInt`/NaN class — do not merge the two Steps.** 104 is about a *malformed* value; this is about an *empty* one, and `?? "default"` catches neither `""` nor `"   "`.
> **Demonstrated by executing the runtime, not by reading (findings §5.1):**
> - **5,000 identical loop signatures fail to trip the loop-signature detector.**
> - **5,000 attempts fail to trip the velocity ceiling.**
> - **A 40,000-character exfiltration URL is allowed** past the web guard's length caps.
> - 🔴 **And the guard keeps looking alive** — the SSRF and scheme checks still deny normally, so the logs show a functioning guard.
> ⇒ **The two automated defences against runaway Anthropic spend, and the exfil-shape half of the egress guard, can be silently disabled by an operator action that looks like housekeeping** (creating a Railway variable and clearing it).
> **`config.ts:512-556` was written to close this class; these sites were never routed through the shared bounded parser.** **Whether the fix is one change or a sweep is an architectural call — surface it.** — [AC design → DC+QC]

---

⚠️ **Carried forward from Appendix E.1, and it bears on 111 and 113: M6's and M4's GREENs were measured WITHOUT `npm run typecheck`.** Those two lanes were never instructed to run it, because the ruling was *derived from* wave 1. **Any invariant of theirs that is structural *because the compiler enforces it* may be a false GREEN** — `tsx` strips types unchecked. **Re-run the specific mutation under `typecheck` before treating an M6/M4 GREEN as unprotected.** *(Not theoretical: M2's I5-hoist and I8-compile came back `npm test` GREEN / typecheck RED — found only because wave 2 was told to look.)*

## 4.1 — Two Justin-gated items from the same findings doc that nobody has surfaced

Both belong in your morning ballot, not in a Step:

1. 🔴 **The INCONCLUSIVE set needs a `--live` decision.** **13 suites never ran** during the sweep, because `--live` spends real Anthropic money and writes to a real database — **which is Justin's call, and the sweep lead deliberately refused to make it or to wake him for it.** Every INCONCLUSIVE verdict in that document is a question **one authorised `--live` pass would close.** *(findings §7 item 3 + Appendix E.5 — the lead weighed running it and waking him, and rejected both on the escalation ladder's hard floor. It is a decision, not an oversight.)*
2. **Four standing QC review-patterns are proposed and unapproved** (findings §6). They need Justin's yes before an AC writes them into `evryn-quality/CLAUDE.md`'s *Patterns This Role Watches For*. ⚠️ **Per the 2026-08-11 routing ruling, that edit logs to the META changelog.** The sharpest of the four: *a documentation claim about a whitespace- or delimiter-sensitive literal, made without reading the bytes* — which is exactly the Atlas/NUL error Lane E is fixing.

⚠️ **Also carried forward, from Appendix E.1 and stated at the top of the findings doc: M6's and M4's GREENs were measured WITHOUT `npm run typecheck`.** Any of their invariants that is structural *because the compiler enforces it* may be a false GREEN. **Re-check under typecheck before writing a Step off an M6/M4 GREEN.** Two of the six above come from those lanes.

---

# §5 — ⚠️ THE ACf13 BRIEF: IT IS A LIVE, GROWING WIP. HANDLE ACCORDINGLY.

**AC0-37 got this wrong in its first read and Justin corrected it. The correction matters, so it is written here plainly:**

This is **not** a mailbox and **not** a finished punch-list. It is **ACf's running list of edits he cannot make yet**, because the files they belong in are being rewritten by our lanes. **He is still adding to it.** And the deeper point Justin made: *"the fact that stuff in there doesn't easily land in the right places is kind of the whole point of why he's writing to that instead of just putting it in the docs themselves — a lot of that is going to need to be in different places than it currently is."*

**⇒ Three rules when you work it:**

1. **Nothing may be written to `_evryn-meta/CLAUDE.md` right now.** It is being ported to the `ac.md` agent definition. **Same for DC's and QC's manuals.** Placement is a **judgment call per item, made by hand, post-merge** — not a find-and-replace.
2. **Every item you weave in gets a receipt note appended in the brief**, naming where it landed. He is still writing; without receipts neither of you can tell what remains.
3. **Do not delete the file when you think you are done.** It is his, and it is live.

**What AC0-37 verified is still owed as of 2026-08-13** (checked line-by-line against `main`, not inferred — Justin was ~98% sure AC0-36 had absorbed these; he had not, and none of it had landed):

| Location on `main` today | Still says |
|---|---|
| `CLAUDE.md:219` · `:352` · `:359` · `:391` | `AGENT_PATTERNS` in the source-of-truth list ×2, **in the routing table itself**, and in the persist-learnings order |
| `ac-orchestration-protocol.md:539` | the escalation ladder's hard-floor list ends *"…LEARNINGS, AGENT_PATTERNS"* |
| `evryn-dev-workspace/CLAUDE.md:232` · `evryn-quality/CLAUDE.md:425` | same stale reference — **both are yours**, and per Justin's 2026-08-11 ruling **both log to the META changelog** |

✅ **What ACf13 already landed and you must NOT redo:** the other nine files — `lock-protocol.md`, `doc-update-routing-protocol.md`, `ac-writing-protocol.md`, `identity-writing-bible.md`, `BUILD-EVRYN-MVP.md`, the two team-workspace protocols, the team-runtime tracker, **and the two sprint lines in §3.2 above.** AC0-37 confirmed all nine at source.

🔴 **The live hazard while this sits: the routing table in the manual every AC auto-loads currently tells fresh instances to file learnings into a 40-line pointer.** That is the *"recorded but never delivered"* shape, arriving through the door marked *"I wrote it down."*

---

# §6 — THE ORDERED WORK

## Tonight, with Justin (things AC0-38 structurally cannot do)

1. **Lane E merge** (§3.1) — closes a live Atlas lie.
2. **Lane B sprint merge** (§3.2) + re-apply ACf's two lines + flip Step 106.
3. **Mint the six Steps** (§4).
4. **Lane B current-state merge** (§3.3) — **by content.** ⭐ **Bonus: this closes an owed item for free** — ACb already fixed the extract-before-Gate-B contradiction on her branch (main's `:50`/`:64` contradict Justin's live ruling; her version states the ruled order *deploy → Gate-B → extract → Gate-B again* with a staleness marker). Do not fix it separately.
5. **Lane A**: Justin's review (~200 genuinely new lines — the router 78, three definition headers ~100, the protocol diff) → **the three frozen sentences + the two §14.3 substitutions, one approval** → the three cross-repo pointers → rebase + frozen-block re-check → **merge** → weave in whichever ACf13 items now have a home, with receipts.
6. **§6.6 — SEVEN STANDALONE WINS. None needs a merge first; four need no gate at all. 🔴 Justin explicitly handed these to you on 2026-08-13, so DO them — do not just carry them forward into another handoff.**
   - 🔴 **SETTLE THE POSTGRES NUL QUESTION** — query DEV and confirm whether `messages.content_raw` (a Postgres `text` column, which rejects U+0000) would reject a halt-sentinel-bearing string arriving via `createMessage` at `src/notify/slack.ts:951`. **Both Lane E agents flagged it; neither had DB access.** Read-only, ~10 minutes, needs no gate. Method + connection vars: `evryn-backend/backups/README.md`. **Close it or file it as a Step — do not leave it floating.**
   - **Draft the three ADRs** (the compaction rule · the agent-definition consolidation · the router split). **ADRs are excluded from the propose-first gate — write them at decision time.** A decision captured only in a session doc evaporates under compaction, and these three were all made this week.
   - `ac-writing-protocol.md` into the **Light** cascade. Justin approved the placement 2026-08-12. **It is mandatory before writing any doc and it is in ZERO cascades.** *(⚠️ post-Lane-A the Light cascade lives in `.claude/agents/ac.md`, not `CLAUDE.md`.)*
   - Delete `docs/working/2026.08.10-acf-ac0-dashboard-status-pill-note.md` — redundant; its content is Step 107. ACb deliberately left it.
   - Resolve the five untracked `evryn-backend` files (`tests/_gate-a-*.ts` ×4, `backups/full-public-2026-08-05.sql`). ⚠️ **Justin has approved committing the gate-a scripts VIA A LANE** — `_gate-a-run.ts`'s grading logic is written against the pre-③ world and must be re-derived first, and committing them means dropping the `tsconfig.tests.json` exclude. **The dump is a v0.3 candidate (encrypted off-machine).** So the honest answer tonight is probably *gitignore the dump, leave the scripts, and say so* — not "commit them."
   - Fresh `pg_dump` — newest is 8 days old. Method: `evryn-backend/backups/README.md`.
   - Fix the `CLAUDE.md` all-branches enumeration script: it prints every branch **once per worktree**, so with 6 meta worktrees a real hit drowns in a wall of six. Nobody has touched it; **it wants Justin's eyes.**
7. **Meta CHANGELOG entry** for the whole round *(the ADRs are in §6.6 above — write them early, not at the end)* · **current-state refresh** · **founding-team current-state append** (short, product-altitude).
8. **Write / adjust `2026.08.13-ac037-ac038-marching-orders.md`** (§7).

## Then: AC0-38 overnight

**His orders are already written** — `docs/working/2026.08.13-ac037-ac038-marching-orders.md`. **Expect to adjust them**; AC0-37 wrote them before the merges landed. **What you should not have to do is re-derive them.**

Justin will **stay up for the start of AC0-38's session** to confirm the cutover lands, so he is not stranded overnight.

---

# §7 — LEARNINGS DRAFTED, READY TO ROUTE

⚠️ **Route these by ALTITUDE, and read `LEARNINGS.md`'s own header first — Justin sharpened it 2026-08-13:** what *your* build needs goes **local and first**; only the *generalizable* half goes to `LEARNINGS.md`, because that file is drained at `#sweep` and `#sweep` can be weeks away. **The test: strip every name, path, SHA and date — is there still a rule another repo could act on?**

1. **Compaction is not a re-spin, and the difference decides what is recoverable.** A compacted parent keeps every subagent resumable; a re-spun one loses all of them. AC0-35 wrote *"permanently lost"* about six load lists a two-minute resume recovered verbatim. ⇒ **Establish which event occurred before declaring anything lost.**
2. **A reload restores what you READ, not what you CONCLUDED.** Judgements formed under the old conditions ride through wearing the costume of experience. **The tell: a conclusion you can state but cannot reconstruct the evidence for.**
3. **Methodology derived DURING a multi-wave run does not apply retroactively to earlier waves — and the asymmetry is invisible in the combined report.** Record which rulings bound which wave.
4. **A grep that returns zero feels exactly like proof of absence.** Three independent instances in one session.
5. **Compare by content, never by git's verdict, when one side rewrote a file wholesale.** A *clean* merge is the dangerous outcome.

**A sixth, from this session and not in any prior draft:**

6. **A load list stated in a handoff is a hypothesis until someone measures it.** The prior handoff cited *"Tier 1 + Tier 2 ≈ 250K tokens"*; measured from bytes it is **~330K**, ~40% higher — and it cited a branch tip (`0538ebf`) that had moved three commits by the time it was read. **Neither error was detectable by reading.** ⇒ **A handoff that hands you a number should hand you the command that produced it.** *(This one does — §3's diffstats are reproducible.)*

---

# §8 — FINDINGS THAT MUST NOT BE RE-DERIVED

1. **The protocol restructure needs no cutover; only the agent definitions do.** §3.4. This is the single most useful simplification AC0-37 found.
2. **Lane E's Atlas correction fixes a claim that is FALSE on `main` right now** (`07-safety.md:330-332`, "spaces" for NUL bytes). Read at source.
3. **The sprint's main-side drift is exactly 2 lines**, and the current-state's is exactly 17 across two commits. Both measured; both recipes in §3.
4. **The ACf13 brief is 0% absorbed, not ~98%.** §5, verified line-by-line.
5. **`acfl/learnings-consolidation` has a ZERO-line diff against main** — fully landed, nothing pending. So routing new learnings into `LEARNINGS.md` has **no collision**. AC0-37 checked this because it looked like a hazard and is not.
6. **`acfsq/research-move` adds 20 lines to one brief; `acf13/lock-2026-08-12` adds two working docs.** Neither collides with anything of ours.
7. **Every SHA in the prior handoff was perishable and several had already moved.** `main` moved twice on `_evryn-meta` during AC0-37's session alone. **Re-derive; never quote.**

---

# §9 — 🔴 THE AC0-36 RELAY, AND WHY IT IS DANGEROUS

**AC0-36's session is still open. Its six subagents did NOT compact — only it did.** So they are pristine and reachable. **But AC0-36 itself is half-lobotomized and does not know it.**

**Justin's direct observation, 2026-08-13:** *"I told him he's garbled, but when I just asked him for the 3 sentences, he wanted to help out — even though I told him not to — he doesn't seem to fully register that he's half-lobotomized. He thinks he can help. His 'help' would break shit."*

**⇒ THE ONLY SAFE PROTOCOL, and it must be followed exactly:**

1. **You write an appendage directly into the target subagent's brief file** — not a message asking AC0-36 to compose anything.
2. **AC0-36 relays ONE thing, VERBATIM:** *"You have a note in your brief at `<path>`, section `<N>`. Read it and respond by appending to that same file."*
3. **The subagent responds by appending to its brief.** AC0-36 is cut out of the content path entirely.
4. **You put a `Monitor` on the brief's mtime + `git HEAD`, emitting only on change.** 💰 A Monitor bills only for lines it emits — poll aggressively, emit stingily.
5. ⚠️ **Agree an explicit sign-off and `TaskStop` the watcher**, or it fires on every unrelated commit forever. Justin: *"I don't want you to finish at 1am and have it firing every 5 min until I show up in the morning."* **If you hand off with one live, NAME ITS TASK ID.**

**Which agents have briefs to append to — and which do not:**

| Agent | Brief | Reachable this way? |
|---|---|---|
| ACm / ACm2 (Lane A) | `2026.08.11-ac0-acm-loading-architecture-brief.md` **on the branch** | ✅ — this is exactly how §14's drafts were recovered today |
| ACb (Lane B) | `2026.08.11-ac0-acb-sprint-current-state-brief.md` **on the branch** | ✅ |
| Sweep lead | the findings doc (Appendix E is its work) | ✅ |
| **Lane E DC · Lane E QC · the six M-lanes** | 🔴 **NONE** | ❌ — you must create a file first and have AC0-36 point them at it |

**AC0-37's judgement, and Justin left it to you:** everything load-bearing is already extracted (AC0-36 closed all six out adversarially). The two questions worth the round-trip if you have room are (a) **the sweep lead, on re-checking M6/M4 GREENs under `typecheck`** — §4's caveat — and (b) **Lane E's DC, on whether the repo-wide NUL scan should become a CI check**, which its own set-down names as the gap it would close next. **Neither is blocking. If context is tight, skip both and say so.**

**Justin will leave AC0-36 up** so AC0-38 can reach the subs overnight if he must. **Set that up before you hand off** — a shared watched file plus the verbatim-relay instruction written into AC0-38's orders — or the capability exists in theory and not in practice.

---

# §10 — THE GATES THAT DO NOT MOVE

- **No push, no deploy, no prod migration.** Ever, without Justin's explicit go in his most recent message.
- **Each commit needs its own go-ahead.** Prior authorizations do not stack; workflow verbs (*"handle it," "ping me when you're done"*) are not commit authorization. **Exception:** build-level code where DC + QC + AC agree (his standing 2026-06-02 authorization), and mailbox/brief-class traffic.
- **Commit by pathspec on the COMMIT, never a bare `git commit`** — `git commit -F - -- path/a path/b`. And **`git diff <file>` first**: a by-path commit takes that file's entire working-tree state, including another agent's uncommitted edits to the same file, and `git status` will not warn you. **Repos are hot today; other agents are working.**
- **Read every branch diff with THREE dots** — `git diff main...branch`. Two dots renders everything `main` gained as a deletion by your branch, and the diff looks completely normal.
- **Justin handles pushes for everyone.** Coordinate; do not push.

---

# §11 — FRAGMENT SWEEP: small true things with no other home

**Each of these has already cost an agent time, or would.**

1. **`evryn-langgraph-archive` and `evryn-prelaunch-landing` return 403 on push.** ✅ Both are 0-ahead with clean trees — **nothing is lost.** They are the Sealed and Inactive repos and **the wall is expected** — but know it exists *before* a batten-down, not during one.
2. **The `claude.ai Google Calendar` MCP connector is unauthenticated** and cannot be authorised from a non-interactive session. **Four agents have now flagged it.** It is Justin's connector settings and it bears on nothing we are doing. **Do not be the fifth.**
3. **`src/notify/slack.ts:279` passes the RAW error object to `console.error`.** AC0-36 verified **by execution** that `evryn-backend` does *not* carry ACf's credential-bearing-URL leak class — a fetch rejection carries neither URL nor token in `.message`, `String()`, `.cause` or `.stack`; only the hostname surfaces, and the secret is the path. **This is not a hole today. It is the site that would leak first.** Routed as cheap hardening, not a defect.
4. **A script gotcha if anyone rebuilds the sprint programmatically:** the final section's preamble **already contains the truncation canary**, so a reassembler that appends one emits **two.** ACb hit this and guarded it.
5. **`AGENT_PATTERNS.md` is a 40-line pointer, not a destination.** Its long-term disposition — permanent redirect, or retire to `docs/historical/` once every inbound link is fixed — **is Justin's, and neither you nor ACf owns it.** ⚠️ **Retiring it is only safe AFTER every inbound link is fixed, and §5's edits are part of that set.**
6. **`_evryn-meta` `main` moved twice during AC0-37's session and `evryn-backend` `main` once.** Other agents are live in these repos today. **Re-derive every SHA; `git diff <file>` before every commit.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
