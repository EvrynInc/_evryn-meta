# Handoff: AC2 cost lane → next-AC2 (2026-07-07)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** The self-handoff from the outgoing AC2 (cost lane) to the next AC2, written because the outgoing instance is near compaction. It carries the **live state + next steps + open items** as of 2026-07-07. Read it AFTER the stable frame in the AC0↔AC2 mailbox/brief (`_evryn-meta/docs/working/2026.06.30-ac0-ac2-cost-lane-brief.md` §0–§8 = your boot sequence, working-with-Justin rules, the 3-item plan, discipline). This handoff is the *what's-true-right-now* layer on top of that. Retire when the cost lane ships.

---

## 0. You are next-AC2 — orient in 60 seconds

You own the **cost lane** — three interlocking changes that cut Evryn's per-forward spend. Named (Justin's convention — use these, NOT `①②③`-only, and NEVER jargon-by-number to Justin, who reads only chat + comes in cold):

1. **Haiku Bouncer** (= ①) — **DONE. Shipped `v0.2.6` 2026-07-06, live, OFF.** A cheap Haiku pre-screen in front of Opus forward-triage.
2. **Clustering** (= ②) — **IN PROGRESS.** Batch forwards into twice-daily packages. Piece A + C built + reviewed + QC-GO (unmerged); Piece B (note-pathway) + Mira voice in flight.
3. **Runtime Bookkeeping** (= ③) — **NOT STARTED.** The keystone/riskiest, deliberately last.

**Boot:** Load your Full Startup Context Cascade on `main` per `_evryn-meta/CLAUDE.md`; if Justin calls it explicitly, load ALL of `evryn-backend/src/` in full. Then diff your cost-lane worktree against main (the runtime on the worktree differs from main — that's the true state). Read the brief §0–§8 + this handoff.

**Your immediate next moves (in order):**
1. **Check Justin's latest message** for: (a) his yes/no on promoting QC's review-pattern (§3 — if yes, add it to `evryn-quality/CLAUDE.md`); (b) any new direction. Ping him (every response).
2. **Check whether Mira's note-pathway draft is back** — Justin was spinning her (2026-07-07) with your brief. If her voiced beats returned, reconcile them with the Piece-B mechanics you're about to design (identity-as-runtime — §2 Piece B).
3. **Build Piece B** — design the pull-forward mechanism (§2), spin DC (bundling the NB1 + NB2 fixes, §3), then QC (heightened — it touches identity). Everything else (A+C) is done + reviewed and waiting to converge.
4. Then: **converge A+C+B → migration → Justin-gated deploy (ships OFF)** (§5).

---

## 1. The three cost changes — where each stands

### Haiku Bouncer (①) — DONE, live, OFF
- **Shipped `v0.2.6`** (`main` @ merge `8e11e9d`, tag `v0.2.6`, deployed 2026-07-06). Prod migration `haiku_screen_shadow` applied + verified. Clean boot. Ships OFF (`HAIKU_SCREEN_MODE` unset → screen never runs → zero prod behavior change).
- **Three-verdict screen:** `ignore` (junk → `record_ignore`, terminal-dead, no stamp) · `pass` (real-person no-fit → `record_pass`, contact stamped) · `escalate` (anything else → full Opus). Fail-safe: every error/timeout/unparseable → escalate.
- **Flip path (Mark-gated, NOT now):** `off` → `shadow` (~1wk real Mark traffic, ~0 false-filter) → Justin's explicit sign-off → `active`. The ~$1,800/mo lands ~1wk into Mark-live, not before.
- Refs: `docs/deploy-log.md` (v0.2.6 row) · ADR-020 amendment (`docs/decisions/020-model-tier-selection.md`) · build-spec `docs/working/2026.06.30-ac2-haiku-prescreen-build-spec.md` (three-verdict banner on top) · CHANGELOG 2026-07-03. Code: `evryn-backend/src/triage/haiku-screen.ts`, `maybeRunHaikuScreen` in `src/email/process.ts`, `recordIgnore` in `src/triage/classify.ts`, `src/db/haiku-screen-shadow.ts`.

### Clustering (②) — see §2 (the live focus).

### Runtime Bookkeeping (③) — NOT STARTED
- The keystone. The runtime pre-loads Evryn's context AND persists status/lifecycle itself; Evryn returns a structured verdict `{result, reasoning}` and does ONLY judgment (cuts a pass ~15 → ~2–4 turns). Removes Evryn's raw `supabase_upsert` status-write surface (ADR-033 territory).
- **Folds in the deterministic-`triage_result`-for-gold/edge fix** — today only `pass`/`ignore` write their axis; gold/edge verdicts live only in `metadata.draft.classification`. ③ canonicalizes all three into `triage_result`, which makes the verdict→outcome audit a clean two-field query. (I documented this gap in `evryn-backend/docs/ARCHITECTURE.md`, "emailmgr_items Status Lifecycle" — my 2026-07-07 edit, committed.) Write an ADR-018 amendment alongside the ADR-033 one; flag Soren as ADR-018 owner (awareness).
- **Co-designs with:** AC4's now-shipped read-lock (they COMPOSE — ③ removes the WRITE surface, AC4 locked the READ) + Step 70 (prompt restructure, measurement-gated). Coordinate via AC0. Gate-A (SPRINT Step 88): ③ does NOT merge until AC0 runs a pre-③ integration baseline on DEV with you.
- Full detail: brief §2 ③ + `evryn-team-workspace/shared/projects/product/research/2026.06.12 cost-levers-consolidated.md` §4.

### Step 71 (Reflection binding safety-net) — small, on your plate, not urgent
- A runtime guard so Reflection's consolidation can't silently clear a still-live `[binding: until-X]` note unless it made the new story. Reflection is live-inert until a user enrolls, so no rush. Lands cleanly with `classify.ts`/consolidation work. Brief §2.

---

## 2. Clustering — precise live state (2026-07-07)

**Design (committed + pushed, Justin SCM-vetted):**
- Build-spec: `_evryn-meta/docs/working/2026.07.07-ac2-clustering-build-spec.md` — the 3 pieces, self-contained. **Read this — it's the source of truth for what Clustering is.**
- ADR-049: `_evryn-meta/docs/decisions/049-daily-clustering-pipeline.md` — the decision.
- The 3 pieces: **A** queue + twice-daily batch (new `queued` status; `CLUSTERING_MODE` off|manual|scheduled, default off; batch cron + manual `cluster now` operator command; restart-catch-up as PRIMARY recovery; positive per-batch M1 heartbeat) · **B** personal-note pathway (a forward WITH a personal note → the note reaches Evryn real-time + the item still queues unless she pulls it forward; she "always considers replying, guided by good customer service") · **C** reply-aware `detectForward` (a `Re:`/in-thread reply quoting a forward no longer mis-routes to triage → closes the phantom-duplicate hole).
- Justin's locked decisions: cadence twice-daily ~10am + ~4pm PT (see NIT below — it's whole-hour `16`=4pm, not 4:30); manual-first rollout; restart-catch-up PRIMARY (not next-batch); note-pathway = "any note → real-time to her"; reply-aware detection; urgent = via the note judgment, NOT a keyword detector; gold-latency up to ~half-day accepted.

**Piece A + Piece C — DONE (built + double-reviewed):**
- **DC-built, AC-reviewed (clean), QC-reviewed = GO, no blockers.** Committed on branch **`dc/clustering-a-c`** (`f66431f`), worktree **`C:/Users/Justin/Evryn/Code/evryn-backend-clustering`**, base `main` `9339ba0` (built on top of the Bouncer / v0.2.6). **UNMERGED, NOT deployed.** Ships `CLUSTERING_MODE=off` (fully dormant — real-time triage unchanged). 14 files, +1429/−50. Typecheck clean; pure + DEV tests written (AC2 runs the DEV integration test — spend-gated bits deliberately breaker-tripped).
- What A+C added (the key files/mechanics — read the diff `git -C evryn-backend-clustering diff main...HEAD`):
  - `queued` status (`src/db/items.ts` `ItemStatus`) + migration `backups/add-queued-status.sql` (widens the CHECK, RLS untouched — **written, NOT applied**; AC2 applies to dev+prod at deploy) + `queued` added to the `supabase_upsert` enum in `classify.ts`.
  - `CLUSTERING_MODE` + `CLUSTER_HOURS_PT` (`[10,16]`) + `CLUSTER_HEALTHCHECK_URL` in `config.ts` (`parseClusteringMode`/`parseClusterHoursPt`).
  - `processForward` ingest gate (off=real-time unchanged; manual/scheduled=create `queued` + return; **a labeled Piece-B seam** where the note-branch slots in).
  - `triageQueuedItem(item)` (`process.ts`) — reconstructs triage inputs from the STORED item, calls the EXISTING `runForwardTriage` with `clusteredDrain=true` (do NOT re-implement triage — keeps Bouncer + ③ composition).
  - `src/email/cluster.ts` (NEW) — `runClusterDrain(trigger)` (oldest-first, sequential, per-item try/catch, stop-on-halt, tally, heartbeat) + pure `shouldRunClusterBatch`/`tallyItemOutcome`.
  - `checkClusterDrain` cron (`poll.ts`); `cluster now` operator command (`slack.ts`, operator-gated); restart catch-up (`index.ts`).
  - **M1 park-target seam** (`parkInFlightItemForHalt` in `classify.ts`, driven by `ToolCallContext.clusteredDrain`): clustered halt → park `queued` (re-drained by next batch/restart); real-time → park `new` (unchanged). This is Clustering's ONLY coupling into ADR-041's lossless-halt invariant — QC verified it's correct in all cases.
  - `src/safety/cluster-heartbeat.ts` (NEW) — the §A7 positive "cluster ran…" heartbeat.
  - Piece C: reply-aware `detectForward(subject, body, replyIndicated?)` (`detect.ts`) + `threadHasPriorMessage` (`db/messages.ts`).

**Running the A+C DEV integration test (AC2's to run — two gotchas from the Bouncer):** `RUN_LIVE_DB_TESTS=dev npx tsx tests/test-clustering-dev.ts` in the clustering worktree. (1) It needs the runtime creds — copy the *current* `evryn-backend/.env` into the worktree (`.env` is gitignored; AC3 reorganized it 2026-07-03 to the qualified `EVRYN_ENV=dev` + `SUPABASE_*_DEV` scheme). (2) **Verify the test stubs `notifyDev`** before running — the Bouncer's DEV test fired real `#dev-alerts` noise (fixed 7/6 by blanking `config.slackDevWebhookUrl`); this test drives breaker/error scenarios that call `notifyDev`, so confirm it's stubbed or you'll spam the channel. (Dev already has the `haiku_screen_shadow` table; it will NOT have `queued`'s widened CHECK until you apply `add-queued-status.sql` to dev — do that first for a live drain test.)

**Piece B (personal-note pathway mechanics) — NOT BUILT. This is your next build.**
- Two open dependencies: **(a)** design the *pull-forward mechanism* — the build-spec §3 deliberately left it to DC/you. It has THREE cases Evryn judges from the note, in the real-time note-turn: **urgent** → pull the `queued` item forward + triage now; **casual** → leave it queued (rides the next batch); **stand-down** (a note like "ignore this / already handled") → mark the queued item done/deprioritized WITHOUT a full triage. All three need a handle on the linked item. Design this concretely before spinning DC (see build-spec §3 + Mira brief §3). **(b)** it PAIRS with **Mira's voice** — **Justin is spinning Mira NOW (7/7)** with the brief `_evryn-meta/docs/working/2026.07.07-ac2-mira-clustering-note-pathway-brief.md`. Her voiced beats come back → reconcile the mechanics with her voice (identity-as-runtime: the runtime must give Evryn exactly the capability the voice describes, and vice versa; you re-check on the way back).
- The Piece-B seam is already in `processForward` (a labeled comment) — additive branch on `forwardInfo.forwardingNote`, not a rewrite.
- **Bundle these into the Piece B DC trip** (QC's A+C findings worth fixing): the NB1 drained-count fix + the NB2 comment (see §3).

---

## 3. QC's findings on A+C — all non-blockers/nits (my evaluation + what to do)

QC returned **GO, no blockers** on a fully-loaded deep pass (37-file `src/` + 10 identity files + all docs/ADRs, receipts confirmed). I agree with GO. Actions:

- **NB1 — FIX (bundle into Piece B):** `runClusterDrain` counts a *halted* item in `drained` (`cluster.ts` — `summary.drained += 1` runs BEFORE the `if (outcome==="halted") break`). A halted item was parked back to `queued`, not triaged — so the heartbeat/`cluster now` summary over-reports by 1 on every halt, degrading the manual-mode supervision signal (the mode built for Justin to watch). Fix: move the increment to AFTER the halt check; update the DEV test assertion (`tests/test-clustering-dev.ts` "drained count is 1"). Small.
- **NB2 — comment (bundle):** `triageQueuedItem` reconstructs `email.threadId=""`. QC traced it benign (forwards don't thread; `getRecentMessages` keys on `sender.id`, not threadId). Add a one-line comment noting the latent coupling (if `runForwardTriage` ever reads `email.threadId`, the drain path silently passes `""`).
- **NB3 — v0.3 flag (no action now):** `threadHasPriorMessage` matches ANY message on the thread. QC traced it unreachable at v0.2 (forwards start fresh threads; `Fwd:` subject overrides anyway; DB-error is safely transient). Flag for v0.3 (when contacts converse on shared threads).
- **NB4 / NB5 — no action:** a hard crash (SIGKILL) mid-`processing` strands one item as `error` (same as the pre-existing real-time path; ADR-041 losslessness is about the *clean breaker halt*, not SIGKILL — no regression). Manual + scheduled drain concurrency is safe via the `processing` status-flip + once-per-window guards (single-replica).
- **NIT1 — AC doc (before the manual→scheduled flip):** `CLUSTER_HEALTHCHECK_URL` (the independent batch dead-man's-switch) isn't in the operator-guide + no Go-Live step. Add the row + step, else the "silent batch failure" detection is off. (AC owns operator-guide edits — see §4.)
- **NIT2 — AC doc:** cadence is whole-hour (`16` = **4pm**, not 4:30). Deliberate/documented; make the operator-guide/watch-list wording "~10am + ~4pm" so Justin isn't surprised. (Confirm with Justin if the :30 actually matters — DC flagged it; the build-spec §A4 said whole-hour is fine unless AC2 says otherwise.)
- **NIT3 — cosmetic:** `tallyItemOutcome` "screened" only catches Haiku active-*ignore*, not active-*pass*. Moot until the Bouncer flips to active. No action.
- **QC PATTERN — PENDING JUSTIN'S OK:** QC proposed a reusable review pattern: *"a change that adds a new lifecycle status value must be reconciled against EVERY status consumer (grep every `findItemsByStatus` / `.eq("status")` / `.in("status")` / `status === / !==` guard), not just the ones the feature touches."* I agree it's worth keeping. **Promote it to `evryn-quality/CLAUDE.md` "Patterns This Role Watches For" once Justin approves** (source-of-truth edit → needs his OK). I asked him (ping 2026-07-07); **awaiting his yes/no** — check his latest message.

---

## 4. AC doc-absorptions OWED (at convergence; Justin SCM-vets before commit; DEFERRED to avoid AC6 collision)

These are AC edits owed before the Clustering deploy, so the docs match the shipped code. **Deferred to convergence** (they're not urgent, and — critical — **AC6 is actively reconciling `ARCHITECTURE.md`** on `ac6/arch-build-reconciliation`, so making ARCH edits now risks a clash). **Coordinate with AC6 via AC0** when you do them.
- `evryn-backend/docs/ARCHITECTURE.md`: add `queued` to the emailmgr_items **Status Lifecycle** (ADR-018 section — the lifecycle diagram + a line); the **batch heartbeat** in Monitoring (the M1/ADR-041 "Silent-Death Safety Layer" section).
- `evryn-backend/docs/operator-guide.md`: `CLUSTER_HEALTHCHECK_URL` env row + a Go-Live step (create the 3rd Healthchecks check); the `cluster now` operator command; the cadence wording (~4pm).

---

## 5. The path to done for Clustering

1. **Piece B** — design the pull-forward mechanism → reconcile with Mira's voice (she's being spun now) → spin DC to build Piece B (bundle the NB1 fix + NB2 comment) → **QC** Piece B (identity-touching → heightened scrutiny + `identity-file-review.md`).
2. **Converge** A + C + B onto one branch (they share `poll.ts`/`process.ts`/`detect.ts` — watch the seams).
3. **Migration:** AC2 applies `backups/add-queued-status.sql` to **dev then prod** — verify the prod ref `wvaaqwapueycyxyhxdnh` FIRST (psql bypasses `assertNotProd`), pre/post `pg_dump`. This is the Bouncer's exact method — see brief §3.4 + `evryn-backend/backups/README.md`.
4. **AC doc-absorptions** (§4), coordinated with AC6.
5. **Justin-gated deploy** (`railway up` → next `v0.2.N`, ships `CLUSTERING_MODE=off` → zero behavior change) → tag + `deploy-log.md` row → AC0 reconcile (current-state, sprint Step 58 DONE-mark, CHANGELOG).
6. **Flip path (Mark-gated, post-deploy):** `CLUSTERING_MODE` `off` → `manual` (hand-fire the first batches via `cluster now`, watch them) → `scheduled`. Clustering must be live (past `off`) before Mark forwards (Justin: "Mark only ever sees the clustered model").

---

## 6. Live git / worktree / deploy state (verified 2026-07-07)

- **Deployed to prod:** `v0.2.6` (`main` `8e11e9d`, tag `v0.2.6`) — the Haiku Bouncer, OFF. Clustering is NOT deployed.
- **`evryn-backend` `main` = `1ada0f2`** (= origin; synced) — that's `9339ba0` + my 2026-07-07 ARCHITECTURE.md audit-note commit (`1ada0f2`), pushed.
- **Worktrees:**
  - `evryn-backend` (canonical) → `main` `1ada0f2`.
  - `evryn-backend-clustering` → `dc/clustering-a-c` `f66431f` — **Clustering A+C, committed, UNMERGED.** Your active build worktree.
  - `evryn-backend-cost-haiku` → `dc/cost-haiku-r2` `6dd3aa3` — the Bouncer branch, **merged into v0.2.6** → **reapable** (AC0/cleanup territory; don't reap mid-anything).
  - `evryn-backend-ac6` → `ac6/arch-build-reconciliation` `cff0422` — **AC6 active on ARCH/BUILD reconciliation.** Coordinate before ARCH edits (§4).
  - `evryn-backend-mira-step55` → `mira/declining-md-step55` — Mira's Step-55 work (declining.md). Separate; no collision with your files.
- **`_evryn-meta`:** origin `main` = `54f0afe` (your design-doc commits: build-spec + ADR-049 + AC0 round 14 + — separately — the ARCH note went to evryn-backend). **Local `main` = `918e54f`, AHEAD of origin** — those extra commits are **another agent's local-unpushed work** (same machine); **do NOT push `_evryn-meta` main** (you'd push their unpushed work) and do NOT sweep it. Stage only your own files by explicit path.

---

## 7. Uncommitted / loose ends (deliberate)

- **The Mira brief** (`_evryn-meta/docs/working/2026.07.07-ac2-mira-clustering-note-pathway-brief.md`) — written, **UNCOMMITTED** (held per Justin's standing 7/7 rule "any doc writes → I vet in SCM before commit"). Justin can read/hand it to Mira from disk regardless. Commit after his vet.
- **This handoff** — **UNCOMMITTED** for the same reason. **Recommend committing it for durability** once Justin glances (it survives compaction on disk regardless — compaction touches *context*, not the working tree — but git is safer). Path: `_evryn-meta/docs/working/2026.07.07-ac2-ac2-handoff.md`.
- **AC0↔AC2 brief Round 15** — a short pointer to this handoff (see the brief).

---

## 8. Key context + discipline (Justin, 7/6–7/7 — carry these)

- **Naming:** Haiku Bouncer / Clustering / Runtime Bookkeeping. NEVER jargon-by-number (`①`/`Step 44`/`B2c`) to Justin — he reads only chat, comes in cold, wants concept-first plain language.
- **Coordination map (Justin's 7/6 simplification):** item-lifecycle/status → **Justin** (he decides if Soren's needed — largely unnecessary). ARCH/operator-guide edits → **AC writes, Justin SCM-vets before commit**. Mira (voice) → **AC writes a self-contained brief, Justin spins her**. AC3 → via AC0. AC0 → kept posted via the brief.
- **"Any doc writes → Justin vets in SCM before you commit"** — his standing 7/7 rule. (He cleared the 4 Clustering design docs to commit on 7/7; the Mira brief + this handoff are the pending ones.)
- **Commit discipline:** each code commit needs Justin's explicit go in his most-recent message (workflow verbs don't authorize); stage by explicit file path, NEVER `git add -A` (parallel agents share repos); **merge-to-main + `railway up` = ALWAYS Justin-gated**; mailbox/brief/CHANGELOG/ADR commits pre-authorized (but honor the 7/7 vet-first overlay). DC/QC subagents commit on their own branches (not gated; the gate is at merge, which AC controls).
- **Ping Justin on `#team-alerts` every single response** (Node `fetch` to `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`, prefix `AC2:`, Windows `C:/…` path form in Node, concise tap — substance in chat). He runs ~7–8 agents in parallel; a missed ping wastes his time.
- **Describe-then-pause** before a major new piece (get his explicit "aligned, go"); then keep rolling (don't idle / don't block on him for authorized work; async subagents are the "keep rolling" mechanism). He explicitly said (7/7) "keep rolling, don't block on me — and if you say you'll keep rolling, actually keep rolling."
- **Orchestration:** spin DC/QC per `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` — VERBATIM 6-part briefs (grep+assemble the agent's full cascade, line-span every file, never skimp the load). QC every real code change; heightened scrutiny for M1/lifecycle/security/identity. Distill subagent output to Justin at CEO altitude (lead with a bold "AC's read" header). Read `ac-writing-protocol.md` before writing docs.
- **Multi-agent right now:** AC6 (ARCH reconcile), Mira (Step 55 + the note-pathway you briefed), + another agent with unpushed `_evryn-meta` commits. Surface collisions to Justin.

---

## 9. Subagent handles (likely dead post-compaction — spin fresh)

- DC A+C: `abbfe65cd425e9b77` (completed). QC A+C: `a9cd7041b6ab9ea3f` (completed). Resume-by-`agentId` (SendMessage) works ≥28h *within a session* (transcript-tied), **but a full session teardown / compaction likely ends resumability** — assume these are gone; spin fresh. And NEVER resume QC for an independence review (a resumed QC carries prior framing) — spin fresh QC for Piece B.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
