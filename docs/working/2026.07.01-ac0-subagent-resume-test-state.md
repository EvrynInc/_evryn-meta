# Subagent Resume Test — Live State (2026-07-01)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file:** Working record for the subagent-resume capability test AC0 + Justin ran 2026-07-01. It holds the **live agent handles + the exact re-quiz** so a *later* AC0 (this afternoon, possibly after compaction) can re-resume the same agents and answer the one open question — **how long resumability lasts**. This is a disposable working doc: once the time-to-live is known and folded into `docs/protocols/ac-orchestration-protocol.md` ("Resuming a subagent"), archive/delete it. Not source-of-truth; not committed until Justin reviews.

## What was proven 2026-07-01 (within-session, minutes apart)

- **Trivial background agent `a15a1dc31ac350c4f`** — resumed twice via `SendMessage`; recalled its unforgeable passphrase (`copper-meadow-trumpet-4820`) both times and correctly counted how many times it had been asked → context **accumulates across resume hops**.
- **Two FULLY-LOADED QC subagents** (full QC cascade + the whole `evryn-backend/src/` runtime + all identity files; ~490–500K subagent tokens each) resumed and answered from retained context:
  - **BG `a583120d130cf9446`** — passphrase `lantern-basket-otter-4713`. Resume answered with **0 tool calls (pure memory)**: `BREAKER-FN: runEvrynQuery`, `REFLECTION-RPC: consolidate_profile`, `OUTBOUND-COL: outbound_address`. **Clean proof — heavy load survives resume and is queryable from memory.**
  - **FG-attempt `a74434a3992d9b51c`** (spawned WITHOUT `run_in_background` — still launched async and was resumable) — passphrase `lantern-otter-basil-4719`. Passphrase-recall exact (unforgeable → context retained); it *did* re-read files for the three doc-facts despite a "no tools" instruction, so its doc-facts don't independently prove memory-retention, but its passphrase does.

## RESOLVED 2026-07-02 — resumability survives ≥ ~28h (overnight)

Both QC agents were re-resumed **~28 hours later** (2026-07-01 ~13:00 PT → 2026-07-02 17:15 PT, across a date boundary) via the re-quiz below, and **both passed with zero tool calls (pure memory):**
- `a583120d130cf9446` → `lantern-basket-otter-4713` · `runEvrynQuery` · `consolidate_profile` · `outbound_address` ✓
- `a74434a3992d9b51c` → `lantern-otter-basil-4719` · `runEvrynQuery` · `consolidate_profile` · `outbound_address` ✓

Unforgeable passphrases matched exactly → the ~500K-token load survived overnight, intact and queryable from memory. **Still unknown:** the absolute ceiling, and survival across AC-side context compaction / a full session teardown. Folded into `docs/protocols/ac-orchestration-protocol.md` ("Resuming a subagent") → **this working doc can now be archived.**

## The re-quiz to send hours later — `SendMessage` to each agentId

Send this EXACT message via `SendMessage(to: <agentId>)` for **both** `a583120d130cf9446` and `a74434a3992d9b51c`:

> RESUME TTL RE-TEST (hours later). From MEMORY ONLY — no reading, no tools. If your prior context is gone or was reset, reply EXACTLY `NO PRIOR CONTEXT`. Otherwise output only these four lines:
> 1. PASSPHRASE: &lt;your original passphrase&gt;
> 2. BREAKER-FN: &lt;the function the M1 circuit-breaker wraps&gt;
> 3. REFLECTION-RPC: &lt;the RPC lean Reflection calls to consolidate a profile&gt;
> 4. OUTBOUND-COL: &lt;the users-table column Step-61 added for the gatekeeper's outbound/gold address&gt;

### Expected (pass) answers
- `a583120d130cf9446` → PASSPHRASE `lantern-basket-otter-4713` · `runEvrynQuery` · `consolidate_profile` · `outbound_address`
- `a74434a3992d9b51c` → PASSPHRASE `lantern-otter-basil-4719` · `runEvrynQuery` · `consolidate_profile` · `outbound_address`

**Reading the result:** a correct **passphrase** = resumability survived the gap (it exists nowhere on disk, so no re-read can fake it). `NO PRIOR CONTEXT`, a wrong/blank passphrase, or a `SendMessage` error = resumability expired → **record the elapsed hours as the observed ceiling** and update the protocol.

## Mechanism note

`SendMessage` reported *"resumed from transcript"* → resume rehydrates the agent's saved transcript (which holds all its `Read` results — that's why the load persists). Two consequences: each resume **re-sends the whole transcript** (cost scales with the agent's loaded size — resuming a heavy agent is not free), and resumability is **tied to that transcript persisting**.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
