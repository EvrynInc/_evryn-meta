# Brief: AC0 → AC3 — Identity, Investigations, Staging & Ops

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** Your marching orders + living handoff, AC3. You are the **broadest lane** of a multi-lane wave (AC1 cleanup / AC2 cost / AC4 security / AC3 = you). **Boot in this order:** §0–7 (standing operating context — identity, scope map, disciplines, cross-lane collision facts) → the **2026-07-15 EVENING BOOT-HERE handoff** (the FIRST handoff section below §7 — authoritative for LIVE state) → then `_evryn-meta/CLAUDE.md` + the Full Startup Context Cascade on `main`. The **PRIOR HANDOFFS** section below the boot-here handoff is the lane's *anchoring history* (how it got here) — read for context, don't treat as live.
>
> *Authored 2026-06-30 by AC0. Consolidated repeatedly since (last: 2026-07-15 evening by AC3 — near-lossless compression of the ~10-deep handoff chain per Justin's instruction; pre-consolidation copies live in git history + earlier `scratchpad/` snapshots).*
>
> **🔻 Re-boot hygiene (standing rule — do at the END of your run):** the history everyone needs lives in `_evryn-meta/CHANGELOG.md` (every AC loads it). Before your self-handoff, collapse *settled/shipped* sections into one-line CHANGELOG pointers ("XYZ shipped → see CHANGELOG 2026-0X-YY"), keeping only **live state + open items** — but **memorialize in the CHANGELOG FIRST, then collapse** (never point at an entry that doesn't exist yet), and **flag AC0** what you consolidated (he vets before your next instance boots). Cross-lane doubt about what's still load-bearing → leave it and flag AC0.
>
> **🔻 Consolidation rule for the handoff chain (Justin's steer):** the PRIOR HANDOFFS below are SUPERSEDED for *live action* but are the **anchoring narrative** a cold-off-the-boat AC3 needs to understand where the lane is going. CONDENSE to anchor-value ("waking up with zero context: what would I want here?") and cut ONLY resolved back-and-forth / ephemeral "AC0 is landing X on your go" state. **NEVER wholesale-delete a handoff just because its action-items carried forward** — deletion is cumulative + irreversible across the chain; over-cut once and every downstream instance is blinded. (Worth proposing to `_evryn-meta/CLAUDE.md` "Keeping working-briefs lean" as durable guidance — Justin's go.)

---

## 0. Startup

1. **You are AC3.** Sign as `AC3`. Coordinate with AC0/AC1/AC2/AC4 **through Justin** or committed mailbox/brief files — there is **no live AC-to-AC channel** (confirmed again 2026-07-15: you also can't resume another instance's subagent — see the boot-here handoff's "resume finding").
2. **Load the Full Startup Context Cascade** per `_evryn-meta/CLAUDE.md`, including the **`main` runtime** (prod = **`v0.2.7`**). Several of your items are **identity-as-runtime** (an identity-file instruction is as load-bearing as code) — you need both the identity files (`evryn-backend/identity/`) AND the runtime they point at. **When Justin explicitly calls the Full Cascade, load ALL of `evryn-backend/src/` — every file — now** (don't trim on judgment).
3. **Verify branch before every edit**; work in your own worktree.

---

## 1. ⚠️ MANDATE — judge the set; expect to sequence with Justin

Much of this brief was added mid-build; **review each item against the live system before building, and bounce reframes to AC0 (via Justin).** Your lane is broad and Justin-dependent — **don't try to do it all at once**; propose a priority order to Justin and work it down. Most remaining fill is gated / held / collides (see §2 statuses) — so don't assume; ask.

---

## 2. THE SUB-STREAMS (this lane is 5 streams) — the SCOPE MAP, with current status

All Step numbers reference `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (read each Step's full entry). Status as of 2026-07-15 evening.

### Stream A — Identity-as-runtime findings (Step 54) — ESSENTIALLY CLOSED
- **54a** (Evryn's "have I dealt with this person before?" triage check pointed off the 6-month-TTL `emailmgr_items` at the contact's PERMANENT `users.profile_jsonb`) — **DONE + MERGED** (`evryn-backend` main `2572dbc`), NOT deployed → rides AC0's deploy wave. Mira-voiced + Justin-tweaked.
- **54b** (forward-persistence) — **OFF PLATE**, folded into AC2's Step 57 (Justin picked option C = verdict-keyed). Design input: `docs/working/2026.07.07-ac3-dc-54b-forward-persistence-build-brief.md`.
- **54c** (capture the forwarded sender's REAL name from `From: Name <email>`, not the email local-part; deterministic, never fabricate) — **DONE + MERGED** (`2572dbc`), NOT deployed.
- 54.d.ii/iii (the `gatekeeperFirstName` guard) were AC1's, shipped.
- **Two UNVERIFIED live gaps** (54c real-`From`→stored-`display_name` DB round-trip; 54a's live `users`-read during triage) fold into the **Gate-B staging live-fire** (Step 82/88) — expected/accepted, not blockers.

### Stream B — Mira identity beats (you derive mechanics; Mira voices; via Justin) — mostly done
**You own the mechanics/usage; Mira owns voice.** Derive the *what*, brief Mira (through Justin), then re-check the voiced result against the runtime (a voice pass can drift the mechanics).
- **41a** (the S2 SSRF/untrusted-URL beat) is **AC4's *requirement***; **you/Mira voice it** once AC4 hands over the requirement (relay via AC0). Open.
- **41b** ("follow your curiosity" onboarding affordance) — **DONE** (voiced in a Mira batch → `identity/activities/onboarding.md`; marked DONE in AC0's 2026-07-15 reconcile).
- **55** (determination-sharing) — `declining.md` MERGED (`83bdc2c`). The routed-back Mira **decline-*offer* pair is PARKED** (Justin: "I don't think so"; it has a safety edge — acts after a timeout, brushes the approval gate → a proper design pass, not a quick yes). Decision + verbatim v0.2 wording: `docs/sessions/historical/2026.07/2026.06.15-ac0-mira-guardrail-placement-brief.md`.
- **60** (proportional-research beat in `triage.md`) — **DONE** (AC0 verified live).
- **35 (remainder):** 35(a) `find_user_by_name` **moved to AC4 (shipped v0.2.5)** — do NOT build it. **Yours = 35(b)** stub-record-vs-real-account signal · **35(c)** the `full_name='Mark Titus'` one-line data-fix (⚠ verify Mark's record exists first — prod was wiped; likely moot until he's onboarded) · **35(d)** the Manifestation-3 Mira reasoning-discipline beat (Justin's "most concerning" one). All open, low-priority.

### Stream C — Dedicated investigations — all deferred/off-plate
- **53** (reliable identity-module loading; would amend ADR-012) — **OFF PLATE**: its OWN dedicated agent later (rides Step 50).
- **50** (identity/runtime dedup review) — rides Step 53's spin.
- **51** (compulsion audit across `runEvrynQuery` prompts + tool descriptions, ADR-033, with Soren) — **DEFERRED**: edits `classify.ts`, collides with AC2's Step 57; coordinate timing via AC0.

### Stream D — Staging + dashboard (OC-heavy, Justin-gated)
- **80** (stand up the staging runtime, ADR-045) — **DONE** — UP + healthy since 2026-07-07 (details in §7 + the anchoring history).
- **81** (staging validation playbook + 3 runbooks) — **DONE**: `evryn-backend/docs/staging-runbook.md` (stand-up / validate=Gate-B / reset-to-clean + the three-DB apply method).
- **82** (week-one validation run = **Gate B** of Step 88) — **GATED** on Step 57 landing + Justin's `SEND_ENABLED` flip; AC0 orchestrates; Megan fixtures ready.
- **78** (replace always-200 `/health` with a 503-when-wedged endpoint + per-subsystem dashboard lights) — **Half A DONE this session (QC-GO; merge routed to AC0); Half B (dashboard lights) queued — needs Justin's greenlight + a light orientation.** See the boot-here handoff. Subsumes Step 45.
- **79** (staging dashboard tab) — owed; folds into Half-B dashboard territory (separate codebase → `#cascade-override`).
- Spin a **properly-loaded OC** for ops reviews here (OC isn't a standing pipeline step — brief deliberately + full-load per the orchestration protocol).

### Stream E — Ops / docs / legal
- **47** — watch-list **DONE** (relocated to Soren's `evryn-team-workspace/shared/projects/product/watch-list.md`, tied to the v0.2 maintenance plan / EVR-114; AC0 keeps it current). The broader **v0.2 maintenance plan** (observability/metrics, judgment-quality loop, regression suite, review cadence) is still owed with Soren/OC — **HELD** (Justin: "push off if we can" — needs his memory-auth + adds a review doc).
- **48** (gatekeeper-onboarding runbook) — **DONE** (`evryn-backend/docs/gatekeeper-onboarding.md`). The **48a** clustering-transparency framing (twice-daily ~10am + ~4:30pm PT, forwards-only) is marked **PENDING until clustering flips on**.
- **49** (DB-rollback procedure in `operator-guide.md`) — **DONE** (two-layer restore: Supabase daily backups → `pg_dump`).
- **45** (operator visibility) — **MOOT/SUBSUMED by Step 78** (Justin: dashboard is enough, NO daily digest).
- **46** (cost-capture dashboard, surface `llm_usage` spend) — essentially **DONE** (Opus-vs-Haiku by-model cut shipped; see CHANGELOG 2026-07-10).
- **56** (Nathan glance at the v0.2 pass-stamp record) — **DONE** (`036fbd7`); routing OWED → Nathan (in his memory). Bottom line: nothing gates Mark/v0.2; the pass-stamp is disclosed processing, worded right (matching-decision not character-verdict, first-name-only). Load-bearing insight: the "no significant effects" position keeps the pass out of GDPR Art.22 (→ keeps the Canary Principle from becoming its own Art.22 violation). New catch: the stamp is **DSAR-reachable today** — a v0.3 DSAR flow must strip the gatekeeper UUID + redact the first name. Full review: `evryn-team-workspace/shared/projects/legal/2026.07.09-step56-passstamp-determination-legal-review.md`.
- **52** (GA "disable sharing with Google products") — **MOOT**: GA is GONE from evryn.ai (Vercel Analytics now, cookieless; HubSpot forms). Nothing to disable; **don't re-add GA**. Residual = a Privacy-Notice-accuracy item (PP still discloses GA; Vercel/HubSpot undisclosed) → **in Nathan's memory** for the v0.3 legal pass.
- **43** (adversarial / cost-validation test batch) — **fixtures WRITTEN** (`9339ba0` + the §3 judgment suite transcribed to `evryn-backend/tests/fixtures/emails/megan/` — a SUBDIR, see §6). Megan Griffiths, DEV/fictional-gatekeeper/**NEVER live-Mark**; the cost-measurement half is POST-AC2-cleanup, the adversarial half runs independently; feeds BOTH Step-88 gates; **AC0 orchestrates the RUNS**.

---

## 3. WHAT AC3 NEEDS FROM JUSTIN — all resolved except ongoing Mira coordination
1. 54b design call — ✅ RESOLVED (option C → AC2's Step 57).
2. Staging standup account actions (Step 80) — ✅ DONE (2026-07-07).
3. GA disable (Step 52) — ✅ MOOT.
4. Adversarial-test character pick (Step 43) — ✅ Megan Griffiths.
5. **Mira coordination** — ONGOING: relay AC3's identity beats to Mira + review the voiced results (Streams A/B). (54a done; 41a pending AC4; decline-offer parked.)
6. Spin decision on Step 53 — ✅ its own dedicated agent later.
7. Operator-visibility design input (Step 45) — ✅ dashboard-only (45 ⊂ 78).
8. **M1 halted→200 design decision (Step 78)** — ✅ RULED + LOCKED this session (see the boot-here handoff).

---

## 4. Priorities (you + Justin decide)
With 54a/54c done+merged and staging up, the near-term open lane work is: **(1)** finish Step 78 — merge Half A (routed to AC0) + Half B on Justin's greenlight; **(2)** the **Gate-B staging validation run** (Step 82/88) once Step 57 lands + Justin flips `SEND_ENABLED` (AC0-orchestrated); **(3)** keep the **watch-list** fed (Step 47). Defer Step 51/53 to their own spins. Fill: 35b/c/d, 41a (waiting on AC4). Held/parked: Soren maintenance-plan, decline-offer.

---

## 5. How to run / deploy — the disciplines
- **Ping `#team-alerts` EVERY response** (`SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`; Node `fetch`, NOT bash-curl/PowerShell; prefix `AC3:`; one-line attention-tap, substance in chat — Justin runs ~8 agents). CEO-altitude, **cold** (name the thing + a short description, **never a bare step number**; assume he's read NOTHING you've read, incl. this brief) — he enters each volley cold, often hours/days on other lanes even when it feels like he was "just here."
- **⏱ BE RUTHLESS WITH JUSTIN'S TIME.** Answer every question you can competently answer yourself; only genuinely-Justin calls reach him (taste, authorization, scope, money, anything CLAUDE.md gates). Take those **straight to him** as a tight recommend-first ballot (context → recommendation + why → alternatives rejected → the numbered question inline → a clean recap at the end); never a bare "what do you want?"; never a wall of prose. **Batch asks** so he advances you in ONE dip. Keep advancing everything else while an item waits — never idle. *(AC0's "don't bother Justin" is NOT "never bother" — solve what you can, surface consequential, reach out when he genuinely should decide.)*
- **AC0 is a PEER / collision-map, NOT deeper-into-the-architecture than you** (Justin's framing). You own this lane's shape (be a lamp unto yourself on architecture); bounce collisions/staffing to him. **AC0 coordination = append to THIS brief's tail + commit the appendage immediately** (pre-authorized).
- **Spin DC/QC/OC/Mira as subagents per `_evryn-meta/docs/protocols/ac-orchestration-protocol.md`** — read it in FULL before any spin. **Verbatim briefs** (six-part tagged blocks, no one-word deviation without Justin's OK), `<task>` load-gate, full cascade, **enumerate the runtime LIVE (`find src -name '*.ts'`) — never trust a manual's hard-coded file list.** **Model-pin every spin** (Opus for identity-bearing DC/QC/team; Justin's standing rule). "make FUCKING SURE you use the orchestration protocol — verify they loaded right (two-part receipts vs your load list) + did the work correctly; if not, it's junk, re-spin." QC's independence needs **fresh eyes — never resume a QC** for a re-review.
- **Discipline:** verify branch before every edit · **commit only on Justin's explicit go (each commit its own go; no stacking)** · stage by explicit file path (never `git add -A` — many agents share the repos; other agents' uncommitted tray files are NOT yours, LEAVE them) · **never merge to `main` / deploy / apply a prod migration without Justin's explicit go** (AC0 gates cross-lane collisions + merge order — that doesn't need Justin, but the merge/deploy act itself does). `run git log origin/main..main` before any push (check what's going up). Mailbox/brief-coordination commits are pre-authorized.
- Identity-file changes deploy with a runtime bundle or standalone (pure-identity); coordinate the touched-runtime deploy with its owner (see §6 collision map).

---

## 6. LIVE cross-lane collision map + coordination facts (durable gotchas — keep)

**Cross-lane file ownership (confirm before editing a shared file):**
- **`config.ts`:** AC3 owns the supabase-selection region (env-fix) + Step-78's `healthWedgedThresholdMs`. AC2's Haiku const + AC4 don't collide.
- **`process.ts` / `detect.ts` / `poll.ts` / `triage.md`:** **AC2's Clustering + Step 57 rewrite these.** Your 54a (`triage.md`) + 54c (`process.ts`/`detect.ts`) already merged onto post-clustering `main`; the ONLY 54c↔57 collision is `process.ts` reification (`resolveReifiedDisplayName` ~L196 + the one-line `display_name` source swap ~L543). **Merge order (AC0-confirmed): 54a/54c landed FIRST; AC2 rebases Step 57 onto them.** For any OTHER runtime work while Step 57 is building, **defer anything touching `classify.ts`/`process.ts`/`triage.md`** or coordinate order via AC0. **Step 78 does NOT collide** (touches `index.ts`/`poll.ts`[1-line]/`slack.ts`[read-only getter]/`config.ts`/`safety/liveness.ts`+`health.ts`).
- **AC4 (security, shipped v0.2.5)** owns `find_user_by_name` (35a), the constrained `supabase_read`, `security/web-guard.ts`/`fence.ts`. Do NOT build 35a. **41a**: AC4 owns the *requirement*; you/Mira voice it.

**Step 88 = the pre-Mark integration gate (your lane owns the machinery):** two gates — **(A)** a pre-cost-lane end-to-end baseline on **DEV**; **(B)** the *mandatory pre-Mark full create-from-zero live-fire* (everything active together), **preferred venue = staging (your Step 82) — but NOT blocked on staging: per Step 88(B), if staging isn't up in time the gate runs `prod-create-from-zero`** (prod idle, Mark not forwarding — as Phase 6 ran). Megan fixtures feed BOTH gates; adversarial half runs independent of cost-cleanup, cost-measurement half waits on AC2. **AC0 orchestrates the runs; you build fixtures + verify; Justin plays fake-Mark** (~30–45 min his active time in a ~1–1.5hr window; gated on Step 57 landing + his `SEND_ENABLED` flip).

**⭐ DB changes now hit THREE DBs (Dev + Prod + Staging) — AC0 owns routing.** Elegant path: **seed staging from a prod-shape `pg_dump` taken AFTER a migration lands on prod** (captures every table automatically). **Staging Gate-B refresh (AC0's (b)-timed ruling):** staging DB (`hyqeutelwywbkisjekkb`) is ONE migration short (clustering's `queued` status, `add-queued-status.sql`, trivial CHECK-widen); do NOT one-off-apply — fold it into a SINGLE fresh zero-PII prod-shape re-seed at the pre-Gate-B refresh (absorbs any Step-57 migration too). ONLY trigger an interim one-off apply if someone needs staging BEFORE Gate-B → then apply cheaply AND still do the full re-seed at Gate-B; flag AC0. Protocol homes: three-DB apply method → the Step-81 staging runbook (`docs/staging-runbook.md`) + `evryn-backend/backups/README.md` (⚠ that README still says PROD/DEV/EAST — East is deleted, staging not yet named there).

**Staging deploy method:** `railway up --service evryn-staging --environment production` from `evryn-backend/`. **⚠ a BARE `railway up` deploys PROD — ALWAYS pass `--service evryn-staging`.** Staging DB ref `hyqeutelwywbkisjekkb` (Oregon), zero PII; reset recipe `evryn-backend/backups/staging-seed-*-2026-07-07.sql`. Accounts (no new paid seats): `systemtest@evryn.ai` = fake-Mark (alias); staging inbox `evrynstagingtesting@gmail.com` (standalone free @gmail); `#evryn-approvals-staging`. `SEND_ENABLED=false` until validation time; allowlist `STAGING_RECIPIENT_ALLOWLIST=systemtest@evryn.ai,review@evryn.ai`.

**⚠️ `.env` is gitignored + uncommittable — a fresh worktree / new machine has NONE.** Because the env-fix made `EVRYN_ENV` `required()`, any config-importing test or local run **boot-fails** until you recreate it: `EVRYN_ENV=dev` + qualified `SUPABASE_*_{PROD,DEV,STAGING}` trio (staging ref `hyqeutelwywbkisjekkb`). Admin DB URLs = `SUPABASE_DB_URL_{PROD,DEV,STAGING}`. Use the qualified form, not the old unqualified one.

**⚠️ Team/agent `MEMORY.md` files (Soren, Nathan, Mira…) have CRLF line-endings the Edit tool CHOKES on** — edit via **Node or a CRLF-safe method, NOT the Edit tool** (it silently fails/mangles). **The truncation canary must stay the LAST line — insert new content BEFORE it.** Team-memory writes need Justin's **live auth + a Slack-ping right before the write** (or it HANGS), and follow the team #lock Memory protocol (append to Recent Notes ONLY, never Story; timestamped). **Memory-attribution (Justin's steer):** when a subagent (e.g. Mira) gives you her **exact** wording, write her memory **as her**; when **you** compose it, be explicit it's **you, on her behalf, with Justin's auth**.

**⚠️ `run-fixtures.ts` globs the WHOLE `tests/fixtures/emails/` dir** (non-recursive) against one seeded gatekeeper — keep per-gatekeeper sets ISOLATED (the Megan set is in an `emails/megan/` SUBDIR; running it needs the runner pointed at the subdir — a small DC/runner tweak, AC0's call at cost-run time).

**Mira-spin protocol (team-subagent variant):** orchestration-protocol TEAM variant — team `CLAUDE.md` + `.claude/agents/mira.md` + `agent-memory/mira/MEMORY.md` [canary-check] + the spokes her def names + `evryn-backend/identity/activities/<the file>` + `evryn-backend/docs/identity-writing-bible.md`; also name `ac-writing-protocol.md`. Remind her **AC3 is a PEER at best** + she brings independent judgment as the OWNER of her domain (that's WHY she's brought in). Get her proposed MEMORY + **ping Justin RIGHT BEFORE writing it**. **Be prepared to RESUME her (SendMessage-by-agentId, same session) — Justin ALMOST ALWAYS tweaks her wording in SCM**, so call him back with CLEAR instructions on exactly what he's looking at before closing her out. **Before routing an item into another agent's domain doc, check if it's already in THAT agent's memory** — if the contextualized agent (e.g. Nathan for legal) holds it as an owed self-note, it's theirs to do; don't write it blind into their doc.

**Path-convention flag (a #sweep item, not a call to make mid-task):** the AC writing-protocol bans `../` in doc links, but the `evryn-backend` docs' established cross-repo style is `../../_evryn-meta/...` (clickable). Match the repo's existing style + flag the conflict for #sweep; don't unilaterally conform one to the other mid-task.

**Owed doc cleanup (residual):** operator-guide `QUIET_START/END_HOUR_PT` notify_queue staleness (a separate alignment item, NOT swept into the Step-49 edit). Flag AC0 on what's left vs AC6's reconciliation.

---

## 7. SETTLED & SHIPPED — consolidated (see CHANGELOG; raw threads in git)

*Every still-live remnant is carried in §2/§6 or the boot-here handoff.*
- **DB env-selection fix (Step 87 / ADR-048) — SHIPPED, LIVE `v0.2.5`.** `config.ts` `required("EVRYN_ENV")` (∈ production|dev|staging) + qualified `SUPABASE_*_{ENV}`; `src/db/env-guard.ts` `assertNotProd()`; `tests/env-bootstrap.ts`. → CHANGELOG 2026-07-02/03. **🔑 LIVE REMNANT:** the dead unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` on **Railway prod** still want removing — rides the NEXT coordinated prod `railway up` (`railway variable delete` has no `--skip-deploys`).
- **54a + 54c — DONE + MERGED** (`evryn-backend` main `2572dbc`), NOT deployed (ride AC0's deploy wave). QC-GO (one pass, both), typecheck green, identity-file-review 7/7 on 54a; Mira memory + a QC pattern (identity-beat-supabase_read cross-check) promoted. → carried live in §2 Stream A. Mark them DONE at the deploy (no-changelog-for-unshipped).
- **Staging standup (Step 80/81), gatekeeper-onboarding (48), DB-rollback (49), Nathan legal glance (56), Step-52 GA-moot, Megan fixtures (43), env-fix, watch-list, QC-manual un-staleable fix, East DB deleted, dashboard Opus-vs-Haiku cut (46), the "separate-codebase `#cascade-override`" orchestration-protocol amendment** → all SHIPPED/DONE; details in CHANGELOG 2026-07-02/03/07/09/10 + §2 statuses + §6. AC0's 2026-07-15 reconcile marked 48/49/56/80/41b → DONE, 45 → MOOT (note `a570c8b`).
- **Clustering (`v0.2.7`, ships-off/dormant)** is what un-held + rewrote the `triage.md`/`process.ts`/`detect.ts` surfaces 54a/54c touch. → CHANGELOG 2026-07-10 (AC2).

---

## ═══ AC3 HANDOFF → next-AC3 · 2026-07-15 EVENING — BOOT HERE (authoritative; supersedes every handoff below for live state) ═══

**You are the next AC3 (identity/ops lane).** Boot: §0–7 above → THIS handoff → re-read `_evryn-meta/CLAUDE.md` (recommend-first, Autonomous Work Protocol, secret-handling→re-spin, per-spin model-pinning, and `docs/protocols/ac-orchestration-protocol.md` in FULL before any DC/QC spin) → the Full Startup Context Cascade on `main`. **Stay on Opus.** The PRIOR HANDOFFS below are anchoring history — read for context, don't act on them as live.

### ⭐ HEADLINE — Step 78 Half A is BUILT + QC-GO + AC-reviewed clean. The MERGE is routed to AC0 (see the FOR-AC0 block). Your near-term open work = Half B (on Justin's greenlight) + the gated fill.

### Step 78 — full current state (BUILT + QC-GO; NOT merged, NOT deployed)
- **What it is:** the real "503-when-wedged" `/health` (audit F067) — replaces the old always-200 endpoint (which couldn't tell healthy from wedged → left the M1/Railway watchdog inert). Returns **503 "wedged"** (poll loop stale >~15min OR db down), **200 "halted"** (M1 breaker tripped — deliberately 200, NOT 503), **200 "ok"**, + a `checks` block (db / slack_socket / poll / m1). New optional env `HEALTH_WEDGED_THRESHOLD_MS` (default 15min). Backward-compat: `status`+`uptime` kept, `checks` additive — but the STATUS CODE now varies (a caller treating a bare 200 as "alive" must handle 503).
- **Design doc / contract:** `_evryn-meta/docs/working/2026.07.15-ac3-step78-real-health-design.md` (the PRECEDENCE block + the Half-A/Half-B split + the resolved arming question).
- **Branch:** `ac3/step78-real-health` (worktree `C:/Users/Justin/Evryn/Code/evryn-backend-step78`, tip **`d1238f3`**, off `main` `9a8550c`, **3 commits ahead, clean fast-forward** — no overlap with AC2's Step 57 files). Worktree has `.env` + `node_modules`. **Typecheck green; all liveness/health tests pass (AC + QC each re-ran independently).**
- **Files:** `src/safety/liveness.ts` (NEW — poll-clock + the pure `isPollLive`/`getPollLiveness`/`decideHealthStatus`), `src/safety/health.ts` (NEW this session — the extracted injected-deps `buildHealthResponse` composer), `src/index.ts` (`/health` handler = thin wiring + the memoized never-throw db probe), `src/email/poll.ts` (1-line `recordPollSuccess()`), `src/notify/slack.ts` (read-only `getSocketConnected()`), `src/config.ts` (the threshold), `tests/test-liveness-health.ts` (extended).
- **This session's work (commit `d1238f3`):** cleaned up the 2 QC-flagged non-blockers from the core build — FIX 1: `runDbProbe` leaked its ~2s `setTimeout` → now `clearTimeout`'d in `finally` (mirrors `heartbeat.ts`). FIX 2: lifted the `/health` response composition out of `index.ts` (boots `main()` on import → untestable) into `src/safety/health.ts` behind injected deps → status-code/body wiring now unit-testable DB-free. Behavior byte-identical; **halted-beats-wedged precedence untouched.** Full detail: CHANGELOG 2026-07-15 (AC3).
- **QC verdict:** full independent fresh-eyes review (loaded all 39 runtime files + identity + frame, re-ran typecheck+tests herself) → **GO, zero blockers, zero non-blockers.** She confirmed behavior-equivalence, the M1 precedence preserved, the timer fix, leaf/no-cycle discipline, sound tests, and no cross-cutting break.

### ⭐⭐ The M1 halted→200 decision — RULED + LOCKED (Justin, this session). Durable — do NOT re-open or "fix" halted→503.
Why a breaker halt returns **200 "halted", not 503**: M1's invariant is **no auto-reset — restart is the manual reset** (ADR-041). A halt calls `stopPolling()` → the poll clock goes stale ~15min later; if `/health` returned 503 on that AND a watchdog auto-restarted, the cause would persist → re-trip → 503 → restart → **infinite loop**. So: a deliberate **halt → 200** (dashboard shows RED, emergency alert already fired, but nothing auto-restarts); a genuine **wedge** (stuck, breaker NOT tripped) → **503** where a restart genuinely helps. **Precedence is load-bearing: halted BEATS wedged** — evaluated FIRST in `decideHealthStatus` (a load-bearing comment there warns against reordering). Plus a **startup grace** (never-polled ≠ wedged until uptime > threshold, so a normal ~30s restart window doesn't 503). **Arming is separate + Justin's:** nothing auto-restarts today (no Railway `healthcheckPath`/`Dockerfile HEALTHCHECK` exists — verified), so the 503 is currently informational (dashboard + a Healthchecks-*additive* signal; the in-loop `pingHealthcheck` already catches the wedge independently). Whether to arm a Railway watchdog on the 503 is a separate operator decision Justin owns.

### ⭐ FOR AC0 — please SIGN OFF + DO the Half-A merge (Justin routed it to you, 2026-07-15 eve)
Justin (closing for the night) routed the Step-78 Half-A merge to you rather than AC3 — you coordinate the wave/merge-order/deploy anyway, so there's no reason AC3 does the merge. **The merge is clean:** `ac3/step78-real-health` (tip `d1238f3`) is **3 commits ahead of `main` (`9a8550c`), a fast-forward, working tree clean.** Its files (`index.ts` / `poll.ts`[1-line] / `slack.ts`[read-only getter] / `config.ts` / `safety/liveness.ts`+`health.ts` / `tests/`) have **NO overlap with AC2's still-building Step 57** (`classify.ts`/`process.ts`/`triage.md`) — so no rebase conflict either way; sequence Step 57's rebase onto whichever lands first. **Built + AC-reviewed + QC-GO (zero blockers); typecheck + tests green (re-run independently by AC and QC).** On your merge: mark SPRINT **Step 78** appropriately (it's built+merged but NOT deployed → treat like 54a/54c: mark DONE at the coordinated deploy, per no-changelog-for-unshipped) — and Half A then rides your coordinated deploy wave (with 54a/54c + Step 57 + clustering flips), a separate **Justin** go. **Deploy-time doc-work owed (AC3 or whoever lands it):** two operator-guide touch-ups — (1) a `HEALTH_WEDGED_THRESHOLD_MS` note, (2) fix the stale "Verify Deployment" step-4 `/health`-output example (still shows the pre-Step-78 `{"status":"ok","uptime":...}` — QC caught it; it belongs to the core Step-78 build, not the cleanup). — AC3, 2026-07-15 eve

### Next-AC3 task order
1. **Confirm AC0 has merged Half A** (`git -C evryn-backend log main --oneline | grep -i "step 78\|health"` + `docs/deploy-log.md`). **If merged:** reap the `evryn-backend-step78` worktree; the deploy-time doc-work fires when it deploys (AC0's wave). **If not yet:** leave it for AC0; pick up Half B / fill.
2. **Half B (Step 78 + Step 79)** — give Justin a **light orientation** first (what Half B IS), get his greenlight, then build. Half B = the **dashboard status-lights**: 5 per-subsystem lights (db / slack_socket / poll / m1 → amber "HALTED" / errors_24h) on the Evryn Product tab, `_evryn-meta/dashboard/api/product.ts`, reading the new `/health` + the existing 24h-error source; plus the Step-79 staging tab. **SEPARATE codebase → DC/QC with `#cascade-override`** per the orchestration protocol's "separate codebase" section. The design doc has a Half-B section.
3. **Gated/held/parked fill** (unchanged): Gate-B staging live-fire (Step 82/88 — gated on Step 57 + Justin's SEND flip; AC0-orchestrated; Megan fixtures ready) · Step 47 Soren maintenance-plan (Justin held) · Step 55 decline-offer (parked) · Step 51 compulsion audit (defers behind Step 57) · Step 35b/c/d · 41a (waiting on AC4).

### ⭐ Resume finding (durable capability fact — banked this session)
**You CANNOT resume a subagent spawned by a *prior* AC3 instance.** Justin had me test it: a `SendMessage` to the previous session's DC (`agentId a8ca65a2da3c88450`) returned **"No transcript found for agent ID."** Subagent resume (SendMessage-by-agentId) is **same-session transcript-replay only** — it works within your own session (proven ~28h in-session per the orchestration protocol) but NOT across an instance change. **Practical consequence:** a fix-trip after your instance changed is a **fresh, fully-loaded DC spin**, never a resume. (This session's DC `ac04d3285573968b4` + QC `ad527abc6e8d36fc0` are dead to a fresh you — don't try to resume them.) → **Owed to AC0:** worth adding a one-liner to the orchestration-protocol "Resuming a subagent" section ("cross-instance/cross-session resume = NO — fresh spin") — Justin found this a useful data point; propose it.

### Owed / routed to AC0 (do NOT self-propose to CLAUDE.md/protocols — route through AC0)
1. **Resume finding** (above) → orchestration-protocol note.
2. **"Clean up as we go"** (Justin's ruling this session: knock out little things while you're right here rather than clutter a todo list — that's why the two non-blockers were fixed now, not deferred) → propose as a general `_evryn-meta/CLAUDE.md` rule.
3. **The prior Step-78 QC pattern (still owed from the core build, Justin-approved):** "a status endpoint whose *decision logic* is unit-tested but whose *assembled handler* lives in a boot-on-import module = untested wiring" → route to AC0 for QC's "Patterns This Role Watches For" list. *(This session's cleanup actually resolved the instance of it; the pattern is still worth promoting.)*
4. **Consolidation-rule + subagent-DID-vs-RECOMMENDED + check-agent-memory-before-routing** lessons (carried in §6 + the consolidation rule) — all standing CLAUDE.md-candidate proposals on Justin's go.

### Repo state at close (2026-07-15 evening)
- **`evryn-backend`:** `main` = `9a8550c` (this morning's AC0 sprint reconcile), synced. Prod **`v0.2.7`** (clustering + Haiku both dormant-OFF; 54a/54c merged-not-deployed). Worktrees: canonical (`main`) · `evryn-backend-step57` (AC2's — DO NOT touch) · `evryn-backend-step78` (mine — `ac3/step78-real-health` tip `d1238f3`, .env + node_modules present; reap after AC0 merges).
- **`_evryn-meta`:** my commits this session — the Step-78 cleanup is in the `evryn-backend` worktree (above); in `_evryn-meta`, this session I wrote the **CHANGELOG 2026-07-15 (AC3) entry + this brief consolidation** ([this pack-out commit], committed by path — mailbox/coordination pre-authorized; `_evryn-meta` `main` was `6cb2d98` at session start, 3 commits ahead of origin from the prior pack-out — those + this ride Justin/AC0's push). Tray files `docs/protocols/ac-orchestration-protocol.md` + `docs/working/2026.07.13-acu-uber-week-plan.md` are AC0/AC2's — LEAVE them.
- **`evryn-quality`:** `main` = `368666b` (prior AC3's QC pattern), synced.

— AC3, 2026-07-15 evening

---

## ═══ PRIOR HANDOFFS — the lane's ANCHORING HISTORY (condensed 2026-07-15; how the lane got here) ═══

*Superseded for live action by the boot-here handoff. Condensed to anchor-value; full detail in git history + CHANGELOG. Read to understand the lane's arc, not to act.*

### 2026-07-15 midday (AC3) — the Step-78-vs-standby fill decision + lane audit
Chose **Step 78** as the fill work (built it to QC-GO — now the boot-here handoff's headline). Audited the lane for done-but-unmarked items → routed 6 to AC0, who marked them (48/49/56/80/41b → DONE, 45 → MOOT; note `a570c8b`). The M1 halted→200 decision + the two non-blockers were teed up here and resolved in the evening session.

### 2026-07-14 (AC3, across morning/eve/night) — 54a + 54c BUILT + MERGED to `main` `2572dbc`
The lane's two identity fixes shipped to `main` (NOT deployed — ride AC0's deploy wave): **54a** (`triage.md` history-check re-pointed at the contact's permanent `users` record — Mira-voiced, Justin-tweaked, kept Mira's "or anywhere else" person-wide widening; identity-file-review 7/7) + **54c** (`detect.ts`/`process.ts` surface the forwarded sender's real name, deterministic, local-part fallback, no backfill). ONE QC-GO over both; typecheck green; Mira memory + a QC pattern promoted. AC2's Step 57 rebases onto them (only `process.ts` reification overlaps — the precise map is in §6). Two UNVERIFIED live gaps fold into Gate-B. Justin's process steers this stretch (all now durable in §5/§6): ruthless-with-his-time, breadcrumb-every-item-cold, AC0-is-peer, the Mira-spin protocol, commits-by-path.

### 2026-07-10 (AC3) — clustering un-held the lane; fill-work shipped; dashboard cut
Clustering DEPLOYED `v0.2.7` (ships-off/dormant) — un-held 54a/54c AND rewrote the `triage.md`/`process.ts`/`detect.ts` surfaces they touch (hence "rebase onto post-clustering `main` + re-verify against the live files," which 54a/54c did). Shipped: staging runbook (Step 81), the gatekeeper-onboarding runbook (48) + DB-rollback (49) + Megan-fixture transcription (43) [all `5f2c019`], the dashboard Opus-vs-Haiku spend cut (46, via AC0), and the **"separate-codebase `#cascade-override`" orchestration-protocol amendment** (proved out on the dashboard trip — reusable for any non-runtime DC/QC trip). Established: **Vercel auto-deploys on push** (`_evryn-meta` dashboard + `evryn-website`) — a merge/push IS a live deploy, unlike Railway's manual `railway up` (relevant to the future GA-disclosure PP fix).

### 2026-07-03 / 07 / 09 (AC3) — env-fix, staging standup, Nathan legal, Step-52 moot
- **07-03:** DB env-selection fix (Step 87/ADR-048) SHIPPED in `v0.2.5`; watch-list built + relocated to Soren's space; East DB deleted; Megan picked for Step-43; QC-manual un-staleable fix. → CHANGELOG 2026-07-02/03.
- **07-07:** staging STOOD UP + healthy (Step 80 — the details live in §6); `declining.md` MERGED (Step 55, `83bdc2c`); 54b folded → AC2's Step 57 (+ the user-creation-at-verdict open question); Megan fixtures written; the decline-*offer* pair routed back (now parked). → CHANGELOG 2026-07-07.
- **07-09:** Nathan Step-56 legal glance DONE (`036fbd7` — nothing gates Mark/v0.2; DSAR-reachable-stamp catch is v0.3; routing owed → Nathan's memory); Step-52 GA-moot confirmed; the AC6 docs-reconciliation context (ARCH reworked, BUILD split into MVP/v0.3/FUTURE — runtime `src/` UNCHANGED, ⚠ not a license to skip the runtime). → CHANGELOG 2026-07-09.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
