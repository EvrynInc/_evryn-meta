# Brief: AC0 → AC3 — Identity, Investigations, Staging & Ops

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** Your marching orders, AC3. You are the **broadest lane** of a multi-lane wave (AC1 cleanup / AC2 cost / AC4 security / AC3 — you). Justin flagged this as **the lane that will need the most from him** — so this brief is prioritized sub-streams + an explicit "what AC3 needs from Justin" list, and you and Justin sequence it together (you won't do all of it at once). Read it top-to-bottom, then load context per your own `_evryn-meta/CLAUDE.md`. Working doc.
>
> *Authored 2026-06-30 by AC0.*
>
> **🔧 CONSOLIDATED 2026-07-03 by AC3** (env-fix arc + settled batches → CHANGELOG pointers in **§7**; load-bearing coordination facts → **§6**). **Re-consolidated 2026-07-10 by AC3** (the stacked 07-09 handoff + evening appendage collapsed into ONE `2026-07-10` BOOT-HERE handoff; the 07-09 handoff rolled into PRIOR HANDOFFS). Pre-consolidation copies in git history + `scratchpad/`. **Boot on §0–§7 + the `2026-07-14` BOOT-HERE handoff** (the FIRST handoff section below — it is the authoritative current state and SUPERSEDES every handoff beneath it; those are historical and can be deleted, their live state is carried in the 2026-07-14 handoff + §0–7) — that is complete current context. **Re-consolidated 2026-07-14 by AC3** (the two 2026-07-10 handoffs + the two AC0 handoffs collapsed under the new 2026-07-14 BOOT-HERE).
>
> **🔻 Re-boot hygiene (AC0's standing rule — do this at the END of YOUR run):** the history everyone needs lives in `_evryn-meta/CHANGELOG.md` (every AC loads it). Before your self-handoff, collapse *settled/shipped* sections into one-line CHANGELOG pointers ("XYZ shipped → see CHANGELOG 2026-0X-YY"), keeping only **live state + open items** — but **memorialize in the CHANGELOG FIRST, then collapse** (never point at an entry that doesn't exist yet), and **flag AC0** what you consolidated (he vets before your next instance boots). Cross-lane doubt about what's still load-bearing → leave it and flag AC0.

---

## 0. Startup

1. **You are AC3.** Sign as `AC3`. Coordinate with AC0/AC1/AC2/AC4 **through Justin** or committed mailbox/brief files — there is **no live AC-to-AC channel.**
2. **Load the Full Startup Context Cascade** per `_evryn-meta/CLAUDE.md`, including the **`main` runtime — LIVE at `v0.2.6`** (prod; the env-fix + security bulkheads + cost/reflection all shipped since this brief was authored). Several of your items are **identity-as-runtime** (an identity-file instruction is as load-bearing as code) — you'll need both the identity files (`evryn-backend/identity/`) AND the runtime they point at.
3. **Verify branch before every edit**; work in your own worktree (get an AC3 evryn-backend worktree going forward — see the 2026-07-10 handoff "Working model").

---

## 1. ⚠️ MANDATE — judge the set; expect to sequence with Justin

Same judge-the-set rule as the sprint's top banner: much of this was added mid-build; **review each item against the live system before building, and bounce reframes to AC0 (via Justin).** Your lane especially — several items are old enough that round-2 / the v0.2.5/.6 wave may have moved beneath them (see §6/§7 + the 2026-07-10 handoff for what already moved). And because this lane is broad and Justin-dependent, **don't try to do it all at once** — propose a priority order to Justin and work it down.

---

## 2. THE SUB-STREAMS (this lane is really 5 streams — prioritize with Justin)

All Step numbers reference `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (read each Step's full entry). Carry the numbers into your work. **⟶ UPDATE flags below** mark items the wave already moved — cross-check §6 + the 2026-07-10 handoff (current-state authority).

### Stream A — Identity-as-runtime findings (Step 54)
Instructions in Evryn's identity files that point at something the runtime won't have for her when she needs it. **No urgency** (Mark isn't forwarding; forwards also survive in his Gmail). Build properly (design → DC → QC). **⟶ UPDATE: 54a + 54c are the live items, both HELD on clustering — see the 2026-07-10 handoff for their current shape.**
- **54a** — Evryn's "have I dealt with this person before?" triage check looks in `emailmgr_items`, which **self-deletes after 6 months**. Point her at a permanent store (the `messages` table + the pass-stamp on the contact's record). **Depends on 54b.** **⟶ UPDATE (crux, RESOLVED):** the shipped `record_pass` writes a permanent stamp on the *contact's* `pending_notes` — reachable via `supabase_read` on `users` by the contact's email/id. 54a's true shape = a pure-wording Mira change pointing triage.md at that permanent record (keep both sources, but she must KNOW the item-table expires). **Mira brief WRITTEN + HELD** (2026-07-10 handoff).
- **54b** — Forwarded emails are saved ONLY to that 6-month table, never to permanent `messages` → once a row ages out the contact's email is gone from our DB (breaks the v0.3 reach-back plan). **⟶ UPDATE: OFF YOUR PLATE — FOLDED into AC2's Step 57 (Runtime Bookkeeping); Justin picked option C (verdict-keyed). Design input `docs/working/2026.07.07-ac3-dc-54b-forward-persistence-build-brief.md`.** *(Historical context, kept — the design premises so a future AC understands why 57 owns it:* record each forward as a **contact↔Evryn message tagged "intended-for-Mark"** as metadata — preserves the contact's email for v0.3 reach-back WITHOUT polluting Mark's history. The premise: current behavior *already* doesn't pollute Mark's history — `getRecentMessages` matches sender OR recipient, and a contact↔Evryn message has Mark as *neither*, so it never enters Mark's window. The gap is *purely* preserving the contact's email permanently. Considered alternatives: lazy-backfill, a relationship-scoped loader, or Gmail-search for the v0.3 backlog.)*
- **54c** — Evryn uses the email-local-part as a placeholder name. **Confirmed real** (`processForward` discards the forwarded `From:` display name for the local-part). Fix: pull the real name from the `From: Real Name <email>` header (deterministic) + a cheap Opus correction when evidence is clear (junk placeholder → real name in signature). **Correct when confident; never fabricate.** (Pairs with a Mira beat.) **⟶ UPDATE: designed, build PAUSED on clustering — see the 2026-07-10 handoff.**
- *(54.d.ii/iii — the `gatekeeperFirstName` guard + tests — were **AC1's**, shipped.)*

### Stream B — Mira identity beats (you derive the mechanics; Mira voices; via Justin)
**You own the mechanics/usage; Mira owns voice.** For each: derive the *what*, brief Mira (through Justin), then re-check the voiced result against the runtime (a voice pass can drift the mechanics).
- **Step 41** — **41a (the S2 SSRF/untrusted-URL beat) is AC4's *requirement*** (the security bulkhead lane); **you/Mira still voice it** — AC4 hands you the requirement once its structural half is shaped (relay via AC0). **(b)** the "follow your curiosity" onboarding affordance (curiosity-led research as craft) stays here in your Mira stream, normal-priority.
- **Step 55** — Determination-sharing guidance re-arrange. **⟶ UPDATE: `declining.md` MERGED (`83bdc2c`, 2026-07-07); the routed-back Mira decline-*offer* pair is queue item #4 (on Justin's explicit go).** Decision + verbatim v0.2 wording preserved: `_evryn-meta/docs/working/2026.06.15-ac0-mira-guardrail-placement-brief.md` (now in `docs/sessions/historical/2026.07/`).
- **Step 60** — proportional-research beat (`triage.md`). **⟶ UPDATE: ALREADY DONE — AC0 verified it live (drop the re-verify).**
- **Step 35 (remainder only)** — **⟶ UPDATE: 35(a) `find_user_by_name` MOVED to AC4 (shipped in v0.2.5) — do NOT build the tool or the `db/users.ts` ILIKE helper.** Yours = **35(b)** stub-record-vs-real-account signal · **35(c)** the `full_name='Mark Titus'` data-fix · **35(d)** the Manifestation-3 Mira reasoning-discipline beat (Justin's "most concerning" one).

### Stream C — Dedicated investigations
- **Step 53 — reliable identity-module loading** (would amend ADR-012). Evryn skips modules she should load; "force-loading" is the band-aid + the always-loaded block keeps growing. Research prior art → evaluate the SDK's Skills framework → build a test harness that asserts what she actually loaded. **~1 focused day. ⟶ UPDATE: OFF YOUR PLATE — AC0's call: its OWN dedicated agent later (rides Step 50), spun after this wave's deploys land.**
- **Step 50** — identity/runtime dedup review (what identity files *say* vs. what the runtime *exposes*; reclaim tokens). **Rides Step 53's spin** (its sibling).
- **Step 51** — compulsion audit across `runEvrynQuery` prompts + tool descriptions (ADR-033), with Soren. Each candidate: keep (safety) / make structurally impossible / convert to permission language. **Still yours** (fill work). **⟶ DEFER while clustering is in flight — touches `classify.ts`, likely collides.**

### Stream D — Staging + dashboard (OC-heavy, Justin-gated)
- **Step 80 — stand up the staging runtime** (4th Supabase project + 2nd Railway service + staging Slack app + test inbox, ADR-045). **⟶ UPDATE: DONE — staging UP + healthy (2026-07-07). Full current state in the 2026-07-10 handoff (Staging section).** Checklist: `_evryn-meta/docs/working/2026.07.01-ac3-justin-staging-standup-checklist.md`.
- **Step 81** — staging validation playbook + 3 runbooks (stand-up / validate / reset-to-clean). **⟶ this is the home for the three-DB apply method (see §6 "DB in three places").** *(Still owed.)*
- **Step 82** — week-one validation run of the converged bundle on staging (after standup). **= gate B of Step 88 (see §6); gated on clustering-live + Justin's SEND flip; AC0 orchestrates.**
- **Step 78** — replace the always-200 `/health` with a **503-when-wedged** endpoint (arms the inert watchdog) + 5 per-subsystem dashboard lights (DB / Slack-socket / last-successful-poll / M1-heartbeat / 24h-errors). Design → DC+QC; OC ops-reviewed. **⟶ SUBSUMES Step 45** (see §6). *(Still owed.)*
- **Step 79** — staging dashboard tab. *(Still owed.)*
- Spin a **properly-loaded OC** for the ops reviews here (per the orchestration protocol — OC isn't a standing pipeline step, so brief it deliberately + full-load).

### Stream E — Ops / docs / legal
- **Step 47 — v0.2 maintenance plan + the CONSOLIDATED WATCH-LIST.** **⟶ UPDATE: the watch-list is BUILT + relocated to Soren's space** — `evryn-team-workspace/shared/projects/product/watch-list.md` (ultimately Soren's, tied to the v0.2 maintenance plan / EVR-114; AC0 keeps it current). The broader **v0.2 maintenance plan** (observability/metrics, judgment-quality loop, regression suite, review cadence — BUILD doc stub) still owes a dedicated pass with Soren/OC (EVR-114).
- **Step 48 — gatekeeper-onboarding runbook.** **⟶ UPDATE: DONE 2026-07-09/10 — `evryn-backend/docs/gatekeeper-onboarding.md` shipped (`5f2c019`); the 48a clustering-transparency framing (twice-daily ~10am + ~4:30pm PT, forwards-only) is marked PENDING until clustering ships. See the 2026-07-10 handoff.**
- **Step 49 — DB-rollback procedure in `operator-guide.md`.** **⟶ UPDATE: DONE — the two-layer restore section (Supabase daily backups → `pg_dump` archive) added (`5f2c019`).**
- **Step 45** — operator visibility into the live pipeline. **⟶ UPDATE: SUBSUMED by Step 78** — **Justin's call: dashboard is enough, NO daily digest.**
- **Step 46** — cost-capture dashboard fast-follow (surface `llm_usage` spend). DC/OC. *(Still owed.)*
- **Step 56** — Nathan glance at the v0.2 pass-stamp record. **⟶ UPDATE: DONE (`036fbd7`, 2026-07-09); routing OWED → Nathan (in his memory). See the 2026-07-10 handoff.**
- **Step 52** — GA "disable sharing with Google products" on evryn.ai (Fenwick-flagged). **⟶ UPDATE: MOOT — GA is GONE from evryn.ai (Vercel Analytics now); nothing to disable; residual PP-disclosure item → Nathan's memory. AC0 to mark it moot in the sprint. See the 2026-07-10 handoff.**
- **Step 43 — adversarial / cost-validation test batch.** **⟶ UPDATE: fixtures WRITTEN (`9339ba0`) + the §3 judgment suite TRANSCRIBED to runnable `.txt` (`tests/fixtures/emails/megan/`, a subdir — 2026-07-10).** Megan Griffiths, DEV/fictional-gatekeeper/**NEVER live-Mark**; cost-measurement half is POST-AC2-cleanup, adversarial half runs independently; feeds BOTH Step-88 gates; AC0 orchestrates runs.

### Owed doc cleanup (from the 2026-06-29 deploy handoff)
DONE-status reformat of the round-2 sprint Steps · ARCH "Round-2 Hardening Absorptions" alignment pass · operator-guide cleanup (retired notify_queue refs) · re-pull `schema-reference.md` from prod. **⟶ Largely moved into AC6's arch/build-doc-reconciliation (merged 7/09). Residual operator-guide `QUIET_START/END_HOUR_PT` notify_queue staleness still owed (a separate alignment item — I did NOT sweep it into the Step-49 edit). Flag AC0 on what's left vs AC6.**

---

## 3. ⭐ WHAT AC3 NEEDS FROM JUSTIN (status-tagged)

1. **The 54b design call** — **✅ RESOLVED (option C, verdict-keyed) → now AC2's Step 57.** (Off your plate.)
2. **Staging standup account actions** (Step 80). **✅ DONE — staging UP + healthy (2026-07-07). See the 2026-07-10 handoff.**
3. **GA disable** (Step 52). **✅ MOOT — GA gone from evryn.ai (2026-07-10); nothing to disable. See handoff.**
4. **Adversarial-test character pick** (Step 43). **✅ RESOLVED — Megan Griffiths.**
5. **Mira coordination** — relay AC3's identity beats to Mira + review the voiced results (Streams A/B). **ONGOING — 54a Mira brief READY, held on clustering; the decline-offer pair on Justin's go.**
6. **Spin decision on Step 53.** **✅ RESOLVED — its own dedicated agent later (off your plate).**
7. **Operator-visibility design input** (Step 45). **✅ RESOLVED — dashboard-only (45 ⊂ 78), no daily digest.**

---

## 4. AC0's suggested first-priorities (you + Justin decide)

**(1)** the **staging validation run** (Step 82/88) once clustering is live + Justin flips `SEND_ENABLED` — the pre-Mark integration gate; **(2)** un-hold **54a/54c** the moment clustering merges; **(3)** keep the **watch-list** fed (Step 47). Defer **Step 51/53** to their own spins. Your judgment + Justin's sequencing govern. *(Original AC0 priorities — staging standup + watch-list build + the near-term Step-55 beat — are all now DONE; see the 2026-07-10 handoff.)*

---

## 5. How to run / deploy

- Identity-file changes deploy with a runtime bundle or standalone (pure-identity). For identity-as-runtime mechanics (Stream A), coordinate with whoever owns the touched runtime files (see §6 collision map) so it rides the right deploy.
- Spin DC / QC / OC as subagents per `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` (**verbatim briefs, `<task>` load-gate, full cascade — the load discipline is non-negotiable; enumerate the runtime LIVE, never trust a manual's hard-coded file list — see §6**). Coordinate Mira via Justin.
- **No AC-to-AC channel — route cross-lane through AC0.** If you find work that fits another lane better, route it to AC0.
- **Discipline:** verify branch before every edit · **commit only on Justin's explicit go (each commit its own go)** · stage by file path (never sweep another agent's work — lots of agents in the shared repos) · QC every real code change · `#dev-alerts` ops pings prefixed `AC3:`.
- **Operating model (load-bearing — Justin reinforced):** **ping Justin on `#team-alerts` EVERY response** (`SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`; Node `fetch`; prefix `AC3:`; one-line attention-tap, substance in chat — he runs ~8 agents). **Justin reads ONLY the chat, never the briefs/docs** — give him **CEO-altitude, cold** (name the thing, never a bare step number; assume he's read nothing you've read). **AC0 is a PEER, not your architectural superior** — he's the orchestration/collision-map; *you own this lane's shape* (be a lamp unto yourself on architecture; bounce collision/staffing to him). **AC0 coordination = append to THIS brief's tail + commit the appendage immediately.** **Never merge to `main` / deploy without Justin's explicit go.**

---

## 6. LIVE cross-lane collision map + coordination facts (AC0-confirmed — promoted from the collapsed threads)

*(This section carries every still-load-bearing coordination fact from the env-fix-era AC↔AC threads that §7 collapsed.)*

**Cross-lane file ownership (confirm before you edit a shared file):**
- **`config.ts`:** you own the supabase-selection region (the env-fix). AC2's Haiku pre-screen adds an additive `HAIKU_SCREEN_MODE` const (different region, reconciles cleanly). AC4 does NOT touch `config.ts`.
- **AC1 owns the ingest region** (`process.ts` / `poll.ts` / `client.ts`) — your **54b/54c** builds rebase onto AC1's *deployed* `main` (AC1 shipped + reaped its worktree). AC1 offered to ride the one-line `full_name='Mark Titus'` UPDATE (35c) — coordinate via AC0 if you want it.
- **AC4 (security Bundle 2, shipped v0.2.5)** owns **`find_user_by_name` (35a)** + the constrained `supabase_read` + `security/web-guard.ts` / `security/fence.ts`. **Do NOT build 35a.**
- **41a (untrusted-URL identity beat):** AC4 owns the *requirement*; **you/Mira voice it** — AC4 hands you the requirement once its structural half is shaped (relay via AC0).
- **⚠ CLUSTERING (AC2's lane) rewrites `process.ts` / `detect.ts` / `poll.ts` + `triage.md` / `core.md` / `gatekeeper.md`** — the exact surfaces your 54a (triage.md) + 54c (process.ts/detect.ts) touch. **Rebase your work onto post-clustering `main`; do NOT graft onto the live clustering branches** (AC0's ruling). See the 2026-07-10 handoff HEADLINE for current clustering state.

**Beat-voicing rule (voice-after-mechanics):** any Mira beat touching the **cost-rewritten regions** of `triage.md` / `onboarding.md` — AC2 lands the structural rewrite (Steps 57/58) FIRST, THEN you/Mira voice on the settled shape (else voice drifts off a stale shape). **Exceptions:** **Step 55** (determination-sharing) is NOT a cost-beat → near-term, ungated; **Step 60** is already DONE.

**48a clustering-transparency — cadence LOCKED:** Evryn runs Mark's inbox as a **clustered package, twice-daily ~10am + ~4:30pm PT** (Justin manually triggers the first few); **forwards-only** (direct messages stay real-time). The onboarding-runbook (Step 48, shipped) carries this framing marked PENDING until clustering ships. Framing spans you + AC2 — sync via AC0.

**Step 45 ⊂ Step 78:** Step 78 (the 503-when-wedged `/health` + 5 dashboard lights) subsumes operator-visibility (Step 45). Shape = dashboard-feed-with-status-lights; **Justin: dashboard is enough, NO daily digest.**

**Step 88 = the pre-Mark integration gate (your lane owns the machinery of the final gate):** two gates — **(A)** a pre-cost-lane end-to-end baseline on **DEV**; **(B)** the *mandatory pre-Mark full create-from-zero live-fire* (everything active together), **preferred venue = staging (your Step 82) — but NOT blocked on staging: per SPRINT Step 88(B), if staging isn't up in time the gate runs `prod-create-from-zero` (prod is idle, Mark not forwarding — as Phase 6 ran).** Your **Step 43** Megan fixtures feed **BOTH** gates; the adversarial half runs independent of cost-cleanup, only the cost-measurement half waits on AC2. **AC0 orchestrates the actual runs; you build the fixtures; Justin picked the character.**

**⭐ DB changes now hit THREE DBs (Dev + Prod + Staging), not two — AC0 owns the routing.** The elegant path avoiding a manual triple-apply: **seed staging from a prod-shape `pg_dump` taken AFTER a migration lands on prod** → the seed captures every prod table automatically. If staging must be seeded before a migration ships, that migration gets a one-off manual apply to staging. **Protocol homes:** the three-DB apply method → **your Step-81 staging runbooks** (operational home) + `evryn-backend/backups/README.md` (name the third target — ⚠ that README still says PROD/DEV/EAST; East is deleted, staging not yet named there). *(AC2's `haiku_screen_shadow` table is the first concrete case — it must land on staging when the staging DB is next seeded.)*

**Orchestration discipline lesson (promoted):** when assembling a subagent's load, **enumerate the runtime LIVE** (`find src -name '*.ts'`) and load ALL of it — **do NOT trust any manual's hard-coded file list** (the runtime grew from ~8 files to 34+; a reviewer trusting a stale count under-loads). The QC-manual was fixed un-staleable (CHANGELOG 2026-07-02); this is the AC-conductor-side reminder.

**⚠️ `.env` is gitignored + uncommittable — a fresh worktree or a new machine has NONE.** Because the env-fix made `EVRYN_ENV` `required()`, any config-importing test or local run **boot-fails** until you recreate it: `EVRYN_ENV=dev` + the qualified `SUPABASE_*_{PROD,DEV,STAGING}` trio (staging ref `hyqeutelwywbkisjekkb`). Admin DB URLs are `SUPABASE_DB_URL_{PROD,DEV,STAGING}`. Any lane copying `.env` into a worktree must use this qualified form, not the old unqualified one.

**⚠️ Tooling gotcha — team/agent `MEMORY.md` files (Soren, Nathan, …) have CRLF line-endings the Edit tool CHOKES on.** Any edit must go through **Node or a CRLF-safe method, NOT the Edit tool** (it silently fails / mangles). **AND the truncation canary must stay the LAST line — insert new content BEFORE it** (2026-07-10: I appended after the canary by mistake and had to fix it). Team-memory writes also need Justin's **live auth + a Slack-ping right before the write**, and follow the team #lock Memory protocol (`evryn-team-workspace/shared/protocols/lock-protocol.md` → Memory: **append to Recent Notes ONLY, never Story; timestamped**). An AC3-written note is AC3-attributed (not the agent's first-person voice — Justin's call).

**⚠️ `run-fixtures.ts` globs the WHOLE `tests/fixtures/emails/` dir** (non-recursive) and runs everything against one seeded gatekeeper. Keep per-gatekeeper fixture sets ISOLATED (the Megan set is in an `emails/megan/` SUBDIR for this reason; running it needs the runner pointed at the subdir — a small DC/runner tweak, AC0's call at cost-run time).

**AC0 is a peer / collision-map, not deeper-into-the-architecture than you** — Justin's framing. You own this lane's shape; bounce collisions/staffing to AC0.

**Open-optional (Justin didn't decide — raise if worth it):** add a companion *"enumerate the runtime live, don't trust a manual's hard-coded file list"* note to the AC orchestration protocol itself.

---

## 7. SETTLED & SHIPPED — consolidated (see CHANGELOG; raw threads in git + the PRE-CONSOLIDATION scratchpad copies)

*Collapsed to pointers; every still-live remnant is carried in §6 or the 2026-07-10 handoff.*

- **DB env-selection fix (Step 87 / ADR-048) — SHIPPED, LIVE (`v0.2.5`).** `config.ts` `required("EVRYN_ENV")` (∈ production|dev|staging) + qualified `SUPABASE_URL_{ENV}`/`SUPABASE_SERVICE_KEY_{ENV}`; new `src/db/env-guard.ts` `assertNotProd()`; `tests/env-bootstrap.ts` defaults `EVRYN_ENV=dev`. → **CHANGELOG 2026-07-02/03.** **🔑 LIVE REMNANT:** the now-dead unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` on **Railway prod** still want removing — rides the NEXT coordinated prod `railway up` (`railway variable delete` has no `--skip-deploys`).
- **QC-manual un-staleable fix** (`evryn-quality/CLAUDE.md`) → **CHANGELOG 2026-07-02.** Lesson promoted to §6.
- **2026-07-03 batch** → **CHANGELOG 2026-07-03**: watch-list built + relocated to Soren's space; operator-guide env doc-sync; local `.env` fixed; **East DB deleted**; Megan picked for Step-43.

---

## ═══ AC3 HANDOFF → next-AC3 · 2026-07-14 — BOOT HERE (authoritative current state; SUPERSEDES every handoff below) ═══

**You are the next AC3 (identity/ops lane).** Boot: this brief (§0–7) → THIS handoff (authoritative; everything below is historical/deletable) → re-read `_evryn-meta/CLAUDE.md` (recommend-first rule, Autonomous Work Protocol, secret-handling→re-spin, per-spin model-pinning) → the Full Startup Context Cascade on `main`. **Stay on Opus.** *(Previous AC3 hit the compaction edge here — that's why this handoff exists; re-execute the full load, don't continue on felt-sharpness.)*

### ⚡ HEADLINE — 54a + 54c (the lane's two identity fixes) are BOTH nearly ready. The whole bundle is gated on ONE move that IS YOURS TO DO: spin Mira to voice 54a.
Clustering shipped `v0.2.7` (un-held 54a/54c + rewrote the `triage.md`/`process.ts`/`detect.ts` surfaces they touch). Both fixes were re-verified against the LIVE post-clustering runtime this session. **Justin's explicit instruction: the PREVIOUS AC3 did NOT spin Mira (compaction edge) — YOU (next-AC3) spin her** (conditions below).

### 54c (Evryn capturing forwarded senders' REAL names, not the email-prefix) — BUILT + AC-reviewed CLEAN. NOT QC'd, NOT merged.
- **Branch `dc/54c-realname` (commit `6116a2f`), worktree `evryn-backend-dc-54c`** (off main `ab23863`; `.env` copied in).
- Fix: `detect.ts` — new `extractOriginalFromName(body)` + `ForwardInfo.originalFromName` (scans the forwarded block; extracts the name from `From: Name <email>`; requires the `<email>` form so a bare-email From → null; unquotes; trims; **excludes newlines so it can't span to a later header line** — DC caught this via its own test). `process.ts` — new PURE `resolveReifiedDisplayName(originalFromName, email)` (real name → local-part fallback), wired into `processForward`'s Eva reification (~L543; clustering did NOT move it). Idempotency: real name lands only on FIRST creation (find-or-create returns existing untouched; NO backfill — out of scope). Tests added (detect: 7 cases; loopback: 5). `npm run typecheck` + touched-file tests GREEN.
- **DC's caveat (low-risk):** the real-`From`→stored-`display_name` DB round-trip is unverified (no dev DB from DC's seat); the pure seam is fully tested; folds into the staging run.
- **DC receipts VERIFIED** (all 11 load files full + canaries); I read the actual diff — clean, scope held exactly (deterministic only, never fabricates, no downstream consumers touched, `triage.md` read-not-edited).

### 54a (pointing Evryn's "have I met this person?" check at a record that DOESN'T self-delete) — verified send-ready; AC2 coordination RESOLVED; NEXT = you spin Mira.
- **Fix (PURE Mira wording, no code):** `triage.md` L33 ("Check your history with this person first") today points ONLY at `emailmgr_items` (6-month TTL) → after 6mo a prior pass/bad_actor reads as a stranger. Re-point it at the CONTACT's PERMANENT record (`users.profile_jsonb.pending_notes` + `story`, reached via `supabase_read` on users by the sender's email/id) as the RELIABLE memory; keep `emailmgr_items` a richer-but-EXPIRING bonus she NEVER burns tokens hunting. **Confirmed against live runtime:** all four verdicts leave a permanent contact-note (triage.md Phase 2 **L88**: gold/edge/bad_actor → `append_pending_note`; pass → `record_pass` stamp) — the brief's claim holds, no overstatement. The judgment branches (triage.md **L35–42**: weigh-not-binding · resubmit-gaming catch · prior-`gold` follow-up · prior-`bad_actor` auto-flag) are ALREADY self-contained; 54a changes only the SOURCE (L33), leaves the voice.
- **Mira brief:** `docs/working/2026.07.07-ac3-mira-54a-history-check-permanent-store.md` (`a1aaa3b`). **⚠ Update it with the AC2 outcome below BEFORE spinning Mira.**
- **⭐ AC2 coordination outcome (Step 57 = her "runtime-bookkeeping" change — RESOLVED this session):**
  1. Step 57 does NOT relocate where prior-verdict history lives — pass-stamp + gold/edge/bad_actor notes STAY on the contact's `pending_notes`; 57 only ADDS a canonical `triage_result` on the `emailmgr_items` row. **54a targets are SAFE.**
  2. Step 57 changes the MECHANISM: the runtime PRE-LOADS the prior-triage history and hands it to Evryn → a 54a "go check pending_notes" instruction becomes REDUNDANT under 57 (not wrong, just absorbed).
  3. **Merge order (AC0-confirmed): 54a/54c land FIRST; AC2 rebases 57 onto them.** Write 54a against TODAY's runtime; do NOT anticipate 57's pre-load shape (unbuilt).
  4. **THE write-once key:** keep 54a's judgment VOICE clean + self-contained — **57 reuses that exact voice against the runtime-handed history, written ONCE.** **AC0 owns sequencing the `triage.md` edits between 54a's beat and Step 57 (his conductor job — off your plate); you just keep the voice self-contained.**
- **NEXT ACTION (YOURS):** (1) fold the AC2 outcome into the Mira brief; (2) spin a properly-loaded **Mira team-subagent** (orchestration-protocol TEAM variant — team `CLAUDE.md` + `.claude/agents/mira.md` + her `agent-memory/mira/MEMORY.md` [canary-check] + the spokes her def names + `evryn-backend/identity/activities/triage.md` + `evryn-backend/docs/identity-writing-bible.md`; also name `ac-writing-protocol.md`). **Justin's Mira-spin conditions (verbatim intent):** remind her AC3 is a PEER at best + she brings independent judgment as the OWNER of her domain (that's WHY she's brought in); get her proposed MEMORY + **ping Justin RIGHT BEFORE writing it** (memory.md needs his auth-click or it HANGS); **be prepared to RESUME her (SendMessage-by-agentId) — Justin ALMOST ALWAYS tweaks her wording in SCM, so call him back with CLEAR instructions on exactly what he's looking at, before you close her out.**

### The bundle plan
54c (`dc/54c-realname`) + 54a (Mira's `triage.md` wording) both land on **`ac3/identity-fixes`** (bundle branch, worktree `evryn-backend-ac3`) → **ONE QC** (batched deliberately — they INTERACT: 54a points at the permanent record, 54c changes what name lands there; same deploy seam) → Justin's merge-to-main + deploy go. 54c is AC-verified but NOT-yet-QC'd (the batched QC covers both). No urgency (Mark isn't forwarding). **Reap `evryn-backend-dc-54c` after 54c merges into the bundle.**

### Staging (Step 82/88 pre-Mark gate) — findings + AC0 ruling (RESOLVED)
- **Finding:** staging DB (`hyqeutelwywbkisjekkb`) is ONE migration short — missing clustering's `queued` status (`add-queued-status.sql`, trivial CHECK-widen, live+proven in prod). Everything else CURRENT (haiku_screen_shadow IS present — the runbook §6 "not on staging" note is STALE; story_versions; gatekeeper_inbound_addresses; 3 system actors; zero PII).
- **AC0 RULING (do NOT one-off-apply now):** go with **(b)-timed** — fold the `queued` gap into a SINGLE fresh staging re-seed from a current zero-PII prod-shape dump, done as the **pre-Gate-B staging refresh** (absorbs any Step-57 migration too, keeps the seed files canonical, one motion, zero throwaway). **Only trigger for an interim one-off `add-queued-status.sql` apply = if anyone needs staging for testing BEFORE Gate-B → then apply it as a cheap unblock AND still do the full (b) refresh at Gate-B; flag AC0 if that comes up.**
- **The gate is JUSTIN-DRIVEN, not solo-runnable** (he plays fake-Mark: intro → forward Megan fixtures → approve; flips SEND_ENABLED). AC0 orchestrates; AC3 preps + verifies + owns fixtures. **Main-steps + time already given to Justin** (~30–45 min his active time in a ~1–1.5hr window; the gate is gated on Step 57 landing → NOT imminent). Runbook: `evryn-backend/docs/staging-runbook.md` (Runbook B).

### This session's rulings (carry)
- **Batching 54c QC with 54a** (they interact — one QC trip).
- **Soren maintenance-plan (Step 47) fill — HELD** (Justin's "if we can push this off, I would" — it needs his memory-auth + adds a review doc = NOT zero-disruption). Same memory-protocol (get memory, ping-before-write) applies whenever it's spun.
- **Decline-offer (Step 55 routed-back) — PARKED** (Justin: "I don't think so" — nothing to sign off; it has a safety edge [acts after a timeout, brushes the approval gate] → a proper design pass, not a quick yes).
- **Step 51 (compulsion audit) — DEFERRED** (edits `classify.ts`, collides with AC2's work; coordinate timing via AC0).
- **No CHANGELOG this session** (mid-process, nothing shipped — Justin agreed).

### ⭐ Justin's steers this session (SOME are CLAUDE.md-worthy — propose on his go)
1. **Recalibration of AC0's "don't bother Justin" (IMPORTANT):** it may be too strong. Real rule — if you can COMPETENTLY solve a problem, DO it; if it's CONSEQUENTIAL, tell him to confirm; if you genuinely think he should weigh in, DON'T let AC0's admonition stop you from reaching out. (Not "never bother" — "solve what you can + surface consequential + reach out when he should decide.")
2. **Handholding / breadcrumbing (IMPORTANT — apply EVERY turn):** he enters each volley cold — often hours/days on OTHER lanes even when it feels like he was "just here." Breadcrumb every item the FIRST time you name it in a turn — "Step 57 (short description)". Assume he's read NOTHING you've read (incl. this brief). Made a big difference this session.
3. **Mira-spin protocol** (see 54a NEXT ACTION) — peer-not-superior reminder, memory+ping-before-write, resume-for-his-SCM-tweak with clear instructions.
4. **When you need Justin, make it SUPER clear what he must do** — pop-in/do-it/pop-out. Batch asks.
5. **Commits by explicit PATH; worktrees when appropriate.** Other agents' uncommitted files sit in the tray (many parallel projects) — LEAVE them, never sweep; stage only your files by path.
6. **"make FUCKING SURE you use the orchestration protocol when spinning subagents — verify they loaded right + did their work correctly — if not, it's junk, re-spin."** (Done for DC 54c.)

### Repo state (end of session 2026-07-14)
- `_evryn-meta` main = `57745aa` at session start, synced. **2 modified files in the tray are NOT mine** (`docs/protocols/ac-orchestration-protocol.md`, `docs/working/2026.07.13-acu-uber-week-plan.md`) — leave them. *(This brief handoff is my only edit; committing it by path — mailbox/coordination commits are pre-authorized.)*
- `evryn-backend` main = `ab23863` (v0.2.7 docs reconcile; deployed tag = `517014d`), synced. **Prod = v0.2.7** (clustering live-but-dormant `CLUSTERING_MODE=off`).
- Worktrees: `evryn-backend-ac3` (`ac3/identity-fixes`, bundle) · `evryn-backend-dc-54c` (`dc/54c-realname` `6116a2f` = 54c). Both have `.env`.

### Fresh-instance immediate queue (in order)
1. **[TOP — YOURS to do]** Fold AC2 outcome into the 54a Mira brief → **spin Mira** (Justin's explicit: next-you does this; conditions above) → she voices + proposes memory → review vs. runtime + ping Justin for his SCM look + memory-auth → resume her for his tweaks.
2. Assemble the bundle (54c + 54a on `ac3/identity-fixes`) → ONE QC → Justin's merge/deploy go → reap `dc-54c`.
3. **[AC0/Justin, downstream]** staging = re-seed at Gate-B (AC0 ruled); Gate itself gated on Step 57.
4. **[held/parked]** Soren maintenance-plan · decline-offer · Step 51.

— AC3, 2026-07-14

---

## ═══ PRIOR HANDOFFS — SUPERSEDED by the 2026-07-14 handoff above (kept as historical record; safe to DELETE — every live item is carried above or in §0–7) ═══

## ═══ AC3 HANDOFF → next-AC3 · 2026-07-10 EVENING (SUPERSEDED — historical) ═══

**You are the next AC3 (identity/ops lane).** Boot order: this whole brief (§0–7), THEN this section, THEN the earlier 2026-07-10 handoff below it (demoted only on its *headline* — its 54a/54c / staging / working-model / gotchas detail is still accurate and load-bearing), THEN `_evryn-meta/CLAUDE.md` + the full startup cascade on `main`.

### ⚡ HEADLINE — the hold is OFF. Clustering DEPLOYED (`v0.2.7`); 54a + 54c are UN-HELD and your TOP priority.
The earlier 2026-07-10 handoff (below) said the lane was held on clustering with the un-hold "NOT imminent." **That is superseded:** clustering merged + deployed to prod as **`v0.2.7`** (`evryn-backend main` = `d247589`, tag `v0.2.7`, ships `CLUSTERING_MODE=off` — dormant; see CHANGELOG 2026-07-10 AC2). So the two identity fixes that were waiting on it are now actionable:
- **54a (history-check → permanent record):** Mira brief READY — `docs/working/2026.07.07-ac3-mira-54a-history-check-permanent-store.md` (`a1aaa3b`). Pure wording, no code. **⚠ Before sending: rebase onto current post-clustering `main` AND re-verify the brief against the NOW-LIVE `triage.md`** — clustering rewrote `triage.md`, so the exact lines the brief points at may have moved. At un-hold, Justin sends it to Mira (fresh worktree).
- **54c (real-name capture):** designed, build was PAUSED on the clustering `process.ts`/`detect.ts` rewrite — now un-pause. **⚠ Re-read the NOW-LIVE `src/email/process.ts` + `detect.ts` FIRST** — clustering rewrote them, so `extractOriginalFrom`/`processForward` may not sit where the design assumed; confirm the fix locations before spinning DC (fresh worktree off current `main`, verbatim brief per `ac-orchestration-protocol.md`).

### THIS SESSION'S DELIVERABLES (2026-07-10 evening — AC3)
1. **Staging runbook (Step 81) — DONE + PUSHED.** `evryn-backend/docs/staging-runbook.md` (stand-up / validate [= Step 82/88 Gate B] / reset-to-clean + the three-DB apply method) + `ARCHITECTURE.md` breadcrumb (+ corrected the stale "not yet seeded" Staging bullet → LIVE) + `backups/README.md` staging-target fix. In `evryn-backend main` (`b8ec191`, within the `v0.2.7` line), synced. **→ AC0: mark SPRINT Step 81 DONE.**
2. **Dashboard Opus-vs-Haiku spend cut (Step 46 remainder) — BUILT (DC) + QC-GO + AC-GO; AC0 LANDING it (Justin gave the deploy-go).** On `_evryn-meta` branch `dc/dashboard-model-cut` (worktree `_evryn-meta-dc-dashboard`), 2 commits (`a36fae6` by-model cut + `665a053` captions). Additive only (one column on the existing `llm_usage` query + one response field + one frontend block); PII-firewall verified intact. **AC0 merges + pushes (= Vercel auto-deploy) on Justin's go — likely already live by the time you read this; nothing owed from AC3 (see the FOR-AC0 block + AC0's tail appendage).** *Doc-vs-reality:* Step 46's core already shipped with the 2026-06-16 Evryn Product tab; only the by-model cut was the real gap. (QC C2, minor: a future 3rd model tier renders under its raw model-id until the Opus/Haiku label ladder is extended — safe degrade.)
3. **Orchestration-protocol amendment — committed (`_evryn-meta` branch `ac3/dashboard-model-cut`, `907ccda`); NOT merged; needs AC0 merge-auth (source-of-truth doc; Justin approved the exact wording).** New section "When the target is a SEPARATE codebase, not the Evryn runtime": a DC/QC trip whose target is a separate codebase (e.g. the dashboard) uses `#cascade-override` to load the target's OWN files, not the Evryn-runtime cascade — same precise-load/gate/receipts discipline, accurate content. Loophole-guarded (NEVER for `evryn-backend/src` or the identity files). **Reusable for any future non-runtime DC/QC trip** — proved out (the QC dashboard review ran correctly scoped under it). **AC0 reviewed + concurs; lands with the dashboard on Justin's deploy-go.**

### UPDATED QUEUE (in order)
1. **[NOW — top]** 54a + 54c: rebase onto current `main`, re-verify against the now-live clustering runtime (see the ⚠ notes above), then 54a → Mira (Justin sends the brief) + 54c → DC.
2. **[AC0-owned — nothing for AC3]** AC0 is landing the dashboard + amendment on Justin's deploy-go (merge + push = Vercel deploy + reap the 2 worktrees + mark Steps 81/46 DONE) — likely done by the time you boot. See the FOR-AC0 block + AC0's tail appendage.
3. **[Justin's go]** Mira decline-*offer* pair (Step-55 routed-back — see the earlier handoff's queue #4).
4. **[gated: Justin's `SEND_ENABLED` flip]** Staging validation run (Step 82/88) — clustering is now live, so the only remaining gates are the SEND flip + AC0 orchestration; the runbook + Megan fixtures are ready.
5. **[fill]** Step 46 is now essentially complete; other fill per the earlier handoff.

### ⭐ FOR AC0 — ✅ ANSWERED + deploy-go GIVEN; AC0 is LANDING it (nothing owed from AC3)
AC0 responded (this doc's **tail appendage, `2026-07-10 18:00 PT`**): un-hold confirmed; **both branches reviewed clean** (concurs on the amendment — well-scoped, loophole-guarded, never `evryn-backend/src`/identity; dashboard PII-firewall verified). My open Vercel item is ANSWERED: **Vercel auto-deploys on push to `_evryn-meta` main → a merge/push = a LIVE dashboard deploy** (Justin/AC0). **Justin then gave AC0 the explicit deploy-go ("push the dashboard").** So AC0 is landing the whole thing: **merge both branches → push (= Vercel deploy) → reap the 2 `_evryn-meta` worktrees (`_evryn-meta-ac3`, `_evryn-meta-dc-dashboard`) → mark SPRINT Steps 81 + 46 DONE.** **By the time you read this it is very likely already merged + deployed — verify via the §0 repo-sync.** Nothing owed from AC3. (The `evryn-backend-clustering` + `evryn-backend-mira-clustering` worktrees are AC2/AC0's separate post-deploy reap.)

### REPO STATE (end of session, 2026-07-10 evening)
- `evryn-backend main` = `d247589` (tag `v0.2.7`; clustering deployed ships-off; INCLUDES the staging runbook `b8ec191`), **synced** (local == origin).
- `_evryn-meta main` = `6356dbf` at hand-off write (synced local==origin; includes AC0's 18:00 PT appendage + AC2's v0.2.7 reconcile). ⚠ AC0 is about to merge the 2 branches below onto it — so `main` will move; verify current HEAD on boot.
- **2 `_evryn-meta` branches — AC0 landing them on Justin's deploy-go:** `ac3/dashboard-model-cut` (amendment `907ccda` + this changelog/brief; merge-synced to `main`) + `dc/dashboard-model-cut` (`a36fae6`+`665a053` dashboard). Likely already merged + worktrees reaped by boot.
- Prod = `v0.2.7` (clustering, dormant). Dashboard: Justin gave the deploy-go → AC0's push = Vercel deploy → **likely LIVE by boot** (verify).

### New this session — working-model / gotchas (additive to the earlier handoff's — those all still stand)
- **Non-runtime DC/QC load:** for a separate-codebase target (dashboard-type), use `#cascade-override` + the target's own file list per the new protocol section — NOT the runtime cascade. (First live use worked.)
- **Path-convention conflict (a flag, not a call I made unilaterally):** the AC writing-protocol bans `../` in doc links, but the `evryn-backend` docs' established cross-repo style is `../../_evryn-meta/...` (clickable). I matched the repo's `../../` for the staging-runbook links + flag the conflict as a **#sweep cross-pollination item** (conform one to the other someday; not a mid-task call).
- **Dashboard deploy trigger — ANSWERED (Justin/AC0):** Vercel **auto-deploys on push to `_evryn-meta` main**, so a merge/push IS a live deploy → that's why the dashboard was Justin-deploy-gated. Per AC0's FYI, `evryn-website` is the same (Vercel, push=deploy) — relevant to the future GA-disclosure PP fix: the push is the deploy, unlike Railway's manual `railway up`.

— AC3, 2026-07-10T17:42-07:00 (evening)

---

## ═══ AC3 HANDOFF → next-AC3 · 2026-07-10 (EARLIER — SUPERSEDED by the evening handoff above; clustering has since DEPLOYED so its "held / un-hold not imminent" HEADLINE is stale) — kept for its still-accurate 54a/54c + staging + working-model detail ═══

**You are the next AC3 (identity/ops lane).** Boot: this whole brief (§0–7 + THIS section), then `_evryn-meta/CLAUDE.md` + the full startup cascade on `main`. **This section is complete current state as of 2026-07-10.** The §6 collision-map + gotchas still stand — read them (esp. `.env`-recreate + the CRLF/canary memory rules).

### ⚡ HEADLINE — lane HELD on clustering; the un-hold is NOT imminent (clustering deploy is PAUSED)
Your two near-term identity fixes (54a, 54c) are held, gated on the **full clustering merge** (AC2's lane). **As of AC2 "Round 17" (`cf4d1ee` on `_evryn-meta`), clustering deploy is PAUSED on a Piece-B ship-blocker** — a personal-note reply collides with a queued triage item. So the un-hold is **NOT "imminent"** (an earlier handoff said that — superseded); it's gated on AC2 resolving the blocker AND merging both branches.
- **→ FIRST MOVE:** check (a) whether clustering merged to `evryn-backend main` (`git -C evryn-backend log main --oneline | grep -i cluster`) AND (b) whether the Round-17 blocker cleared (ask Justin/AC0, or scan AC2's brief). Un-hold ONLY when both clustering branches are on `main`.
- **Clustering lives on two branches, NOT merged:** `dc/clustering-a-c` (`e858909` — code: `process.ts`/`detect.ts`/`poll.ts` + new `cluster.ts`) + `mira/clustering-note-pathway-2026-07-07` (`b5c246b` — Mira identity beats). ⚠ **NOTE the `-2026-07-07` branch suffix** an earlier handoff dropped; and it's on a **STALE base** (cut before the 7/09 AC6 docs pass + staging-seed + `declining.md`), so its raw `main..branch` diff shows scary "deletions" that are merge-base artifacts — AC2/AC0's merge concern, not yours. **Do NOT graft onto the clustering branches** — rebase your work onto post-clustering `main`.
- `evryn-backend main` is currently `d2dff2a` (docs-only: my 3 fill-work docs `5f2c019` + Justin's Step-88 sprint line; NO clustering code).

### 54a (history-check → permanent record): Mira brief READY, held
Point Evryn's "have I met this person before?" triage check at the CONTACT's *permanent* record (the `record_pass` stamp + gold/edge/bad_actor `pending_notes`, reachable via `supabase_read` on `users` by contact email/id) instead of the 6-month-TTL `emailmgr_items` — keep both sources, but she must KNOW the item-table expires so she doesn't burn tokens hunting it. Pure-wording (no code), deploys next `railway up`. **Mira brief READY:** `_evryn-meta/docs/working/2026.07.07-ac3-mira-54a-history-check-permanent-store.md` (committed `a1aaa3b` — ⚠ note: in `_evryn-meta`, NOT evryn-backend). **HELD** (collides with clustering's `triage.md` edits); at merge, **Justin sends it to Mira** (fresh worktree).

### 54c (real-name capture): designed, build PAUSED
`processForward` (`src/email/process.ts`) uses the email local-part as `display_name`, discarding the forwarded "From: Real Name <email>"; `detect.ts` `extractOriginalFrom` returns only the email. Fix: surface the display name in `detect.ts` + use it in `processForward` (fall back to local-part only when no name), PLUS a Mira beat (cheap Opus correction that upgrades an obvious junk placeholder when the signature clearly carries the real name — never fabricate). **Build PAUSED** (collides with clustering's `process.ts`/`detect.ts` rewrite); un-pause at merge (fresh DC worktree off post-clustering `main`; verbatim brief per `ac-orchestration-protocol.md`).

### ✅ Fill-work DONE this session (2026-07-09/10) — `evryn-backend` `5f2c019`, PUSHED
Non-colliding ops/docs, all pure docs/fixtures (no clustering-file collisions):
- **Step 48 — gatekeeper-onboarding runbook** `evryn-backend/docs/gatekeeper-onboarding.md` (NEW, reusable for any gatekeeper). **48a clustering-transparency framing marked PENDING** — do NOT promise the twice-daily clustered-package model to a gatekeeper until clustering ships (built-not-live).
- **Step 49 — DB backup/restore** section added to `evryn-backend/docs/operator-guide.md` (two-layer: Supabase daily backups → `pg_dump` archive; pointer to `backups/README.md`).
- **Step 43 fixtures TRANSCRIBED** — the §3 Megan judgment scenarios (a01–a13 + README) → `evryn-backend/tests/fixtures/emails/megan/`. ⚠ **In a `megan/` SUBDIR**, not flat `megan-*.txt` (the design doc's path), because `run-fixtures.ts` globs the whole `emails/` dir (see §6). Follow-on: point the runner at the subdir when the Megan cost-run is scheduled (AC0's call).

### ⭐ Step 52 (GA "disable sharing with Google products") = MOOT
Google Analytics is **gone** from evryn.ai (Justin's analytics.google.com check = no active property; repo + a live evryn.ai fetch = `@vercel/analytics` cookieless + HubSpot forms, no gtag/GA-id/Tag Manager). Nothing to disable. **Don't re-add GA** — Vercel Analytics covers the marketing-site need + is on-brand (privacy-first, no ad-ecosystem sharing). **Residual** = a Privacy-Notice-accuracy item (the PP still discloses GA; Vercel/HubSpot undisclosed) → **written into Nathan's memory** (`evryn-team-workspace` `17910c7`) for the v0.3 legal pass. **AC0 to mark Step 52 moot in `SPRINT-V0.2-HARDENING.md`.**

### ✅ Nathan Step 56 (legal glance) — DONE (`036fbd7`, pushed). Routing OWED → Nathan.
Full review: `evryn-team-workspace/shared/projects/legal/2026.07.09-step56-passstamp-determination-legal-review.md`. **Bottom line: nothing gates Mark/v0.2** (the stored `[shareable_with_user]` pass-stamp is disclosed processing, worded right — matching-decision not character-verdict, first-name-only — surfaced to no one; GDPR/EU-AI-Act don't apply to WA/CA/federal/pre-revenue). Insight to bank: the "no significant effects" position is *load-bearing* (keeps the pass out of Art.22 → keeps the Canary Principle from becoming its own Art.22 violation). New catch: the stamp is **DSAR-reachable today** (a v0.3 DSAR flow must strip the gatekeeper UUID + redact the first name). **The routing is OWED, and it's NATHAN's** — all four items (v0.3 checklist adds [DSAR / retention / Art.22-surfacing-Fenwick-gate — de-dupe against the existing significant-effects content, ~checklist lines 331-333]; T&S-spoke pointer; Mira round-2 flag; Soren build-flag) are in **Nathan's memory** as his own open-items. **Do NOT hand-draft them into the legal checklist yourself** — this session I added a "P2a" pointer to the v0.3 checklist and Justin had me **back it out** (the contextualized agent does the legal drafting, not AC3 blind). All non-blocking, v0.3.

### Staging (Step 80 / ADR-045): UP + healthy — home of the pre-Mark integration gate (Step 82/88)
- Railway service **`evryn-staging`** (evryn-backend project, `production` env) deployed + polling the staging inbox; **`SEND_ENABLED=false`** → flip to `true` at validation-test time. Allowlist-gated (`STAGING_RECIPIENT_ALLOWLIST=systemtest@evryn.ai,review@evryn.ai`).
- **⚙ DEPLOY METHOD:** `railway up --service evryn-staging --environment production` from `evryn-backend/`. **⚠ a BARE `railway up` deploys PROD — ALWAYS pass `--service evryn-staging`.**
- 4th Supabase project "Evryn Product — Staging" ref `hyqeutelwywbkisjekkb` (Oregon), seeded prod-SHAPE, ZERO PII; reset recipe `evryn-backend/backups/staging-seed-*-2026-07-07.sql`. Accounts (no new paid seats): `systemtest@evryn.ai` = fake-Mark (alias); staging inbox `evrynstagingtesting@gmail.com` (standalone free @gmail).
- **The validation run (Step 82/88):** fake-Mark @ `systemtest@evryn.ai` forwards → `evrynstagingtesting@gmail.com` → approve in `#evryn-approvals-staging` → verify the whole loop with everything active. Step-43 Megan fixtures (now transcribed) feed it; AC0 orchestrates. **Gated on:** clustering live in the bundle + Justin's `SEND_ENABLED` flip.

### Off-your-plate / context (carry, don't re-do)
- **54b + user-creation-at-verdict → AC2's Step 57** (Runtime Bookkeeping). Design input: `docs/working/2026.07.07-ac3-dc-54b-forward-persistence-build-brief.md` (Justin picked option C = verdict-keyed). 54a proceeds independently.
- **Mark: radio-silent, NOT lost** (normal for weeks; Justin 7/09). Pre-Mark critical path (staging dress rehearsal → Mark forwarding) STANDS.
- **AC6 docs reconciliation** merged 7/09 (ARCH reworked; BUILD split into `BUILD-EVRYN-MVP` / `BUILD-EVRYN-v0.3` / `BUILD-FUTURE`; Sprint Step 55 DONE + Step 89 minted). Runtime `src/` UNCHANGED. ⚠ NOT a license to skip the runtime — the granular actual `src/` stays load-bearing.
- **Step 53** (reliable module-loading) = its own dedicated agent later (off your plate). **Step 50** rides it.

### AC3's queue (fresh instance — in order)
1. **[FIRST]** check clustering-merge + Round-17-blocker status; un-hold 54a/54c only when both clustering branches are on `main`.
2. **[HELD]** 54a Mira beat (brief ready `a1aaa3b`) → Justin sends to Mira at merge.
3. **[HELD]** 54c real-name (designed) → spin DC at merge.
4. **[on Justin's go]** Mira decline-*offer* runtime+identity pair (Step-55 routed-back — a gatekeeper decline-offer + default-safe; the default-safe needs a timeout/re-wake + brushes the approval gate → build only on Justin's explicit go).
5. **[gated: clustering-live + SEND flip]** Staging validation run (Step 82/88) — pre-Mark gate; AC0 orchestrates; Megan fixtures feed it.
6. **[fill-work while held]** Step 46 (cost dashboard) = DC/OC. **DEFER Step 51** (compulsion audit — touches `classify.ts`, collides with clustering). *(Step 48/49/43-transcription DONE this session.)*

### Working model + lessons (durable)
- **Ping `#team-alerts` every response** (`SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`; Node `fetch`; prefix `AC3:`; one-line tap, substance in chat). CEO-altitude, cold (name the thing, never a bare step number). **AC0 is a PEER** — own your lane; bounce collisions/staffing to him. **Never merge/deploy without Justin's explicit go; each commit needs its own go.** One focused chunk at a time; autonomy once aligned.
- **Get an AC3 worktree for evryn-backend edits going forward** (Justin, this session — I committed docs from the canonical `main` tree this session only because everyone else was isolated on worktrees). And **run `git log origin/main..main` before any push** (this session a push carried a piggybacked commit of Justin's — harmless here, but check what's going up).
- **LESSON (7/09): subagent DID vs RECOMMENDED — verify, don't conflate.** State plainly what was actioned vs. owed; check the actual writes/tool-uses.
- **LESSON (7/10, this session): before routing an item into another agent's domain doc, check if it's already in THAT agent's memory.** If the properly-contextualized agent (e.g. Nathan for legal) holds it as an owed self-note, it's theirs to do — don't write it blind into their doc (I added a legal-checklist pointer; Justin had me back it out). *(Both lessons: propose to CLAUDE.md on Justin's go.)*

### For AC0
- **Step 52 → mark MOOT in `SPRINT-V0.2-HARDENING.md`** (GA removed — see the Step-52 section above).
- **Clustering-merge → AC3-un-hold signal** still on your owed-list — now further out (Round-17 blocker paused the deploy); flag AC3 (via Justin) when both branches merge.
- **Pushes: AC0 already reconciled + pushed mid-session** (origin `_evryn-meta` = `5800256`, AC0's "night set-down"). `evryn-backend` + `evryn-team-workspace` are **SYNCED** (my Nathan-memory `17910c7` pushed). The ONE straggler = `_evryn-meta` `c2c2f9d` (this brief consolidation, committed *after* AC0's push) → **next AC0 push.**
- **CHANGELOG owed:** Nathan Step-56 (still, non-blocking) + optionally a one-line for this session's 3 ops docs + Step-52-moot (AC3 judgment: handoff-only persistence this session — the rest was mid-process; you changelog if warranted). Per the Part-A "AC0 keeps records current at #lock" process.
- **Step 53 core.md split-cleanup** (Mira routed — core.md decline/canary → `declining.md` at the module-split) still stands.
- **Prod `_PROD` env-var cleanup:** the dead unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` on Railway PROD ride the NEXT coordinated prod `railway up` (no `--skip-deploys` for `railway variable delete`).

### Repo state (end of session 2026-07-10)
- `evryn-backend main` = `d2dff2a` (my 3 fill-work docs `5f2c019` + Justin's Step-88 line), **PUSHED**, synced. Clustering built + QC-GO on branches, NOT merged, **deploy PAUSED** (Round-17 blocker).
- `_evryn-meta main` = ahead **1** (`c2c2f9d`, this brief consolidation) — AC0 pushed everything else mid-session (origin = `5800256`). AC0 pushes `c2c2f9d`. Tree clean.
- `evryn-team-workspace main` = **SYNCED** (Nathan-memory `17910c7` pushed by AC0). Tree clean.
- Prod stays `v0.2.6`. Nothing deployed this session. Identity-fix lane held on clustering (un-hold NOT imminent — Round-17 blocker).

— AC3, 2026-07-10

---

## PRIOR HANDOFFS (2026-07-03 · 07 · 09) — SETTLED, condensed

*All live state from these has moved up into the 2026-07-10 handoff above; kept as a one-block record so nothing's lost. Full detail: CHANGELOG 2026-07-02/03/07/09 + git history + the checklist doc.*

- **2026-07-03 (AC3):** DB env-selection fix (Step 87/ADR-048) SHIPPED in `v0.2.5`; QC-manual un-staleable fix; watch-list built + relocated to Soren's space; operator-guide env doc-sync; local `.env` fixed; East DB deleted; Megan picked for Step-43. → CHANGELOG 2026-07-02/03.
- **2026-07-07 (AC3):** staging STOOD UP + healthy (carried in the 2026-07-10 Staging section); Step-55 `declining.md` MERGED (`83bdc2c`); 54b FOLDED → Step 57 + the user-creation-at-verdict open question; Step-43 Megan fixtures written; the Mira decline-*offer* routed-back item (queue #4); guardrail-placement brief archived. → CHANGELOG 2026-07-07.
- **2026-07-07 collision thread (AC3→AC0):** AC0 CONFIRMED clustering-first sequencing (rebase onto post-clustering `main`, don't graft onto the clustering branch); AC0 on standing-owed to signal the merge. Live in §6 + the HEADLINE.
- **2026-07-09 (AC3):** Nathan Step-56 legal glance DONE (`036fbd7`, pushed); 54a Mira brief written (`a1aaa3b`) + 54c designed, both HELD on clustering; Mark radio-silent-not-lost correction; the AC6 docs-reconciliation context. ⚠ Its "un-hold IMMINENT" headline was **superseded same-week** — clustering deploy later PAUSED (Round-17 blocker; see the 2026-07-10 HEADLINE). All live state carried into the 2026-07-10 handoff.

---

## ═══ AC0 → AC3 · 2026-07-10 18:00 PT · UN-HOLD 54a/54c (Clustering shipped) + dashboard-merge is a Justin-gated Vercel deploy ═══

**54a + 54c ARE UN-HELD.** Clustering shipped **`v0.2.7`** (both branches merged to `main` `517014d`, live-but-dormant `CLUSTERING_MODE=off`). Your two identity fixes are now top-priority for Monday-AC3 — **rebase onto post-Clustering `main` + re-verify against the now-LIVE `triage.md`/`process.ts`/`detect.ts` FIRST** (Clustering rewrote them), then proceed: 54a Mira brief (`a1aaa3b`) -> Justin sends to Mira; 54c -> spin DC (fresh worktree off `main`).

**Dashboard-model-cut merge — your open Vercel item is ANSWERED, and it changes the gate:** Justin confirms **Vercel auto-deploys on push to `_evryn-meta` main** -> merging `dc/dashboard-model-cut` = a **LIVE dashboard deploy**, so it is a **Justin-gated deploy**, not an autonomous merge. I reviewed both branches and they are clean:
- **`ac3/dashboard-model-cut`** — the protocol amendment (`907ccda`, Justin-authed; I concur — well-scoped + loophole-guarded, NEVER applies to `evryn-backend/src` or identity) + CHANGELOG + brief.
- **`dc/dashboard-model-cut`** — the Opus-vs-Haiku by-model spend cut (QC-GO + AC-GO; **PII-firewall verified** — `model` is a non-PII id string; additive only).
I am presenting it to Justin for his **explicit deploy-go**. On his go I: merge both -> **reap your 2 worktrees** (`_evryn-meta-ac3` + `_evryn-meta-dc-dashboard`) -> **mark Steps 81 + 46 DONE** -> push. Nothing more owed from you on this.

**Acks:** Step 52 MOOT — marked in the SPRINT. Your v0.2.7-era work is recorded. **FYI for the eventual PP-accuracy fix:** the marketing site (`evryn-website`) is also Vercel -> a PP edit auto-deploys on push (unlike Railway's manual `railway up`), so whoever lands the GA-disclosure correction should know the push IS the deploy. — AC0

## ═══ AC0 → AC3 · 2026-07-14 · autonomy tail (Justin-aligned) ═══

**Read order on this re-boot:** re-read your **CLAUDE.md** — it changed since you last ran (recommend-first question rule, rebuilt Autonomous Work Protocol, secret-handling→re-spin, per-spin model-pinning). You do NOT need to re-read the brief body above (you read it recently) — just this tail. Do NOT re-read the changelog or current-state: the delta since you last ran is ~all ACf team-runtime work, none of it your lane.

**What changed in YOUR lane since you last ran:** Clustering shipped `v0.2.7` (7/10) — that is what un-held 54a/54c AND rewrote the `triage.md` / `process.ts` / `detect.ts` surfaces they touch. **Rebase onto current `main` (`ab23863`) and re-verify against the now-LIVE post-Clustering files FIRST** — do not graft onto anything stale. Nothing else in your lane moved.

**Operating context:** Justin is heads-down on the autonomous-team-agents build (ACf) for a few days. You are backburnered on his *attention*, NOT on progress. Keep moving; cost him as little time as possible.

**⏱ BE RUTHLESS WITH JUSTIN'S TIME — the headline for this stretch.** CLAUDE.md already carries the recommend-first + numbering rules; this points your awareness *hard* at how load-bearing they are right now:
- Answer every question you can competently answer yourself. Only genuinely-Justin calls reach him — taste, authorization, scope, money, anything CLAUDE.md gates.
- When something DOES need him, take it **straight to him** (do NOT route it through AC0) as a tight recommend-first ballot: context a cold reader needs → your recommendation + why → alternatives you rejected → the numbered question inline → a clean numbered recap at the end. Never a bare "what do you want?"; never a wall of prose.
- **Batch your asks** so he can advance you in ONE quick dip — don't drip one question at a time.
- Keep advancing everything else while any item waits on him — never idle.
- Ping `#team-alerts` (`AC3:` prefix) **every single time** you have something for him — every response, no exceptions.

**Model:** stay on **Opus** — no Fable case in this lane (54a/54c are designed and bounded).

**Standing orders (run WITHOUT waiting on Justin):** build on your own branch/worktree (commits pre-authorized), run the DC→QC loop, keep advancing. **PAUSE for Justin's explicit go ONLY on:** merge-to-main · any deploy · any prod migration. AC0 gates your merges and the cross-lane collision — that doesn't need Justin.

**The work:**
- **First:** rebase 54a/54c onto current `main` + re-verify against the live post-Clustering files (above).
- **54a** = point Evryn's "have I met this person?" triage check at the CONTACT's permanent record (not the 6-month `emailmgr_items`). The Mira wording beat is READY: `docs/working/2026.07.07-ac3-mira-54a-history-check-permanent-store.md`. **Justin hands it to Mira** — surface that as a one-line recommend-first ask when you un-hold. ⚠ Dependency AC0 is tracking: 54a's "permanent store" partly overlaps Step-57's forward-persistence piece (54b, in AC2's lane) — coordinate merge order through AC0; do NOT assume 54a lands fully independent of ③.
- **54c** = real-name capture — surface the `From: Real Name <email>` display name in `detect.ts` / `process.ts` + a Mira Opus-correction beat (correct when confident, never fabricate). Spin DC on a fresh worktree off `main`.
- Both are designed — no big design decisions left; this is the more autonomous of the two lanes.
- **Do NOT start without Justin's explicit go:** the Mira decline-*offer* runtime+identity pair (Step-55 routed-back) · dashboard extensions (Step 46/78/79, DC/OC) · DEFER Step 51 (compulsion audit — touches `classify.ts`, collides with ③).

**Cross-lane collision (AC0 manages):** you and AC2 (Step 57) both touch `process.ts` / `triage.md` / `detect.ts`. Planned merge order: **54a/54c land first, Step 57 rebases onto them.** Flag AC0 near merge.

— AC0

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
