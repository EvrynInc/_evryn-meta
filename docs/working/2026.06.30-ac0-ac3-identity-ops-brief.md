# Brief: AC0 → AC3 — Identity, Investigations, Staging & Ops

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** Your marching orders, AC3. You are the **broadest lane** of a multi-lane wave (AC1 cleanup / AC2 cost / AC4 security / AC3 — you). Justin flagged this as **the lane that will need the most from him** — so this brief is prioritized sub-streams + an explicit "what AC3 needs from Justin" list, and you and Justin sequence it together (you won't do all of it at once). Read it top-to-bottom, then load context per your own `_evryn-meta/CLAUDE.md`. Working doc.
>
> *Authored 2026-06-30 by AC0.*
>
> **🔧 CONSOLIDATED 2026-07-03 by AC3 (verified by a properly-loaded fresh-AC3 sub-agent).** The long env-fix coordination arc + settled batches were collapsed to CHANGELOG pointers (**§7**) per AC0's re-boot hygiene rule; every load-bearing coordination fact scattered through those threads was **promoted into §6** (the live cross-lane collision map + coordination facts) so nothing was lost. **The pre-consolidation full version** (all the raw AC↔AC threads) is preserved in git history + `scratchpad/2026.06.30-ac0-ac3-identity-ops-brief.PRE-CONSOLIDATION.md`. **Boot on §0–§7 + the two 2026-07-03 handoff sections at the bottom** — that is complete current context.
>
> **🔻 Re-boot hygiene (AC0's standing rule — do this at the END of YOUR run):** the history everyone needs lives in `_evryn-meta/CHANGELOG.md` (every AC loads it). Before your self-handoff, collapse *settled/shipped* sections into one-line CHANGELOG pointers ("XYZ shipped → see CHANGELOG 2026-0X-YY"), keeping only **live state + open items** — but **memorialize in the CHANGELOG FIRST, then collapse** (never point at an entry that doesn't exist yet), and **flag AC0** what you consolidated (he vets before your next instance boots). Cross-lane doubt about what's still load-bearing → leave it and flag AC0.

---

## 0. Startup

1. **You are AC3.** Sign as `AC3`. Coordinate with AC0/AC1/AC2/AC4 **through Justin** or committed mailbox/brief files — there is **no live AC-to-AC channel.**
2. **Load the Full Startup Context Cascade** per `_evryn-meta/CLAUDE.md`, including the **`main` runtime — now LIVE at `v0.2.5` (`0c8c74b`)** (was `d198ef1` when this brief was authored; the env-fix + security bulkheads + cost/reflection shipped since). Several of your items are **identity-as-runtime** (an identity-file instruction is as load-bearing as code) — you'll need both the identity files (`evryn-backend/identity/`) AND the runtime they point at.
3. **Verify branch before every edit**; work in your own worktree.

---

## 1. ⚠️ MANDATE — judge the set; expect to sequence with Justin

Same judge-the-set rule as the sprint's top banner: much of this was added mid-build; **review each item against the live system before building, and bounce reframes to AC0 (via Justin).** Your lane especially — several items are old enough that round-2 / the v0.2.5 wave may have moved beneath them (see §6/§7 for what already moved). And because this lane is broad and Justin-dependent, **don't try to do it all at once** — propose a priority order to Justin and work it down.

---

## 2. THE SUB-STREAMS (this lane is really 5 streams — prioritize with Justin)

All Step numbers reference `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (read each Step's full entry). Carry the numbers into your work. **⟶ UPDATE flags below** mark items the wave already moved — cross-check §6.

### Stream A — Identity-as-runtime findings (Step 54)
Instructions in Evryn's identity files that point at something the runtime won't have for her when she needs it. **No urgency** (Mark isn't forwarding; forwards also survive in his Gmail). Build properly (design → DC → QC).
- **54a** — Evryn's "have I dealt with this person before?" triage check looks in `emailmgr_items`, which **self-deletes after 6 months**. Point her at a permanent store (the `messages` table + the pass-stamp on the contact's record). **Depends on 54b.** **⟶ UPDATE (crux):** the shipped `record_pass` writes a permanent stamp on the *contact's* `pending_notes` — but the open question is whether `triage.md` can actually *reach* it on a re-forward (a forward loads the *gatekeeper's* profile, not the contact's). **Settle 54a's true shape by reading `triage.md` + the contact-load path** before building.
- **54b** — Forwarded emails are saved ONLY to that 6-month table, never to permanent `messages` → once a row ages out the contact's email is gone from our DB (breaks the v0.3 reach-back plan). **🔴 NEEDS A DESIGN CALL FROM JUSTIN — and you OWE him a careful 1-pager** (still owed; no urgency). *Don't* just copy every forward into `messages` (Evryn loads a user's recent `messages` whenever she works with them → that would flood the *gatekeeper's* history with all ~200/day forwards). **Justin's steer (captured, right):** record each forward as a **contact↔Evryn message tagged "intended-for-Mark"** as metadata — preserves the contact's email for the v0.3 reach-back WITHOUT polluting Mark's history. **The premise the design rests on (verified in the runtime — don't lose it):** current behavior *already* doesn't pollute Mark's history — `getRecentMessages` matches sender OR recipient, and a contact↔Evryn message has Mark as *neither*, so it never enters Mark's window. The gap is *purely* preserving the contact's email permanently, NOT fixing a pollution problem (writing the design as if it were would be the reasoning error to avoid). Also weigh the gatekeeper-forward-is-consented vs. never-read-user↔user-messages distinction he raised (v0.3). Considered alternatives the 1-pager should still weigh: **lazy-backfill** (copy a contact's forwards to `messages` only if/when that contact starts a real conversation — the original *leading* option, before Justin's metadata steer superseded it), a relationship-scoped loader, or just Gmail-search for the v0.3 backlog.
- **54c** — Evryn uses the email-local-part as a placeholder name. **Confirmed real** (`processForward` discards the forwarded `From:` display name for the local-part). Fix: pull the real name from the `From: Real Name <email>` header (deterministic) + a cheap Opus correction when evidence is clear (junk placeholder → real name in signature). **Correct when confident; never fabricate.** (Pairs with a Mira beat.)
- *(54.d.ii/iii — the `gatekeeperFirstName` guard + tests — were **AC1's**, shipped.)*

### Stream B — Mira identity beats (you derive the mechanics; Mira voices; via Justin)
**You own the mechanics/usage; Mira owns voice.** For each: derive the *what*, brief Mira (through Justin), then re-check the voiced result against the runtime (a voice pass can drift the mechanics).
- **Step 41** — **41a (the S2 SSRF/untrusted-URL beat) is AC4's *requirement*** (the security bulkhead lane); **you/Mira still voice it** — AC4 hands you the requirement once its structural half is shaped (relay via AC0). **(b)** the "follow your curiosity" onboarding affordance (curiosity-led research as craft) stays here in your Mira stream, normal-priority.
- **Step 55** — Determination-sharing guidance re-arrange. **⭐ NEAR-TERM, NOT v0.3 — the priority beat of this stream** (the moment any decline goes out, the contact's next message is "why doesn't [gatekeeper] want to talk to me?", which Evryn handles via `processDirect`, so she needs the playbook in Mark's first days). It's **NOT a cost-beat** → independent of AC2's rewrite → proceeds near-term, not gated. Decision + verbatim v0.2 wording preserved: `_evryn-meta/docs/working/2026.06.15-ac0-mira-guardrail-placement-brief.md`.
- **Step 60** — proportional-research beat (`triage.md`). **⟶ UPDATE: ALREADY DONE — AC0 verified it live in `triage.md` (drop the re-verify).**
- **Step 35 (remainder only)** — **⟶ UPDATE: 35(a) `find_user_by_name` MOVED to AC4 (shipped in v0.2.5) — do NOT build the tool or the `db/users.ts` ILIKE helper.** Yours = **35(b)** stub-record-vs-real-account signal · **35(c)** the `full_name='Mark Titus'` data-fix · **35(d)** the Manifestation-3 Mira reasoning-discipline beat (Justin's "most concerning" one).

### Stream C — Dedicated investigations
- **Step 53 — reliable identity-module loading** (would amend ADR-012). Evryn skips modules she should load; "force-loading" is the band-aid + the always-loaded block keeps growing. Research prior art → evaluate the SDK's Skills framework → build a test harness that asserts what she actually loaded. **~1 focused day. ⟶ UPDATE: OFF YOUR PLATE — AC0's call: its OWN dedicated agent later (rides Step 50), spun after this wave's deploys land.**
- **Step 50** — identity/runtime dedup review (what identity files *say* vs. what the runtime *exposes*; reclaim tokens). **Rides Step 53's spin** (its sibling).
- **Step 51** — compulsion audit across `runEvrynQuery` prompts + tool descriptions (ADR-033), with Soren. Each candidate: keep (safety) / make structurally impossible / convert to permission language. **Still yours** (fill work).

### Stream D — Staging + dashboard (OC-heavy, Justin-gated)
- **Step 80 — stand up the staging runtime.** 🔴 **GATED ON JUSTIN'S ACCOUNT ACTIONS** (4th Supabase project + 2nd Railway service + staging Slack app + test inbox, ADR-045). The design is DONE; the standup needs his account work. **Start this clock early** — his slowest external dependency, AND it's the **home of the pre-Mark integration gate (Step 88 / 82)** → critical path. **LIVE IN-FLIGHT — see the 2026-07-03 handoff for exact state** (Supabase project ✅ done; Gmail OAuth in progress; Slack app + 2nd Railway service TODO). Checklist: `_evryn-meta/docs/working/2026.07.01-ac3-justin-staging-standup-checklist.md`.
- **Step 81** — staging validation playbook + 3 runbooks (stand-up / validate / reset-to-clean). **⟶ this is the home for the three-DB apply method (see §6 "DB in three places").**
- **Step 82** — week-one validation run of the converged bundle on staging (after standup). **= gate B of Step 88 (see §6).**
- **Step 78** — replace the always-200 `/health` with a **503-when-wedged** endpoint (arms the inert watchdog) + 5 per-subsystem dashboard lights (DB / Slack-socket / last-successful-poll / M1-heartbeat / 24h-errors). Design → DC+QC; OC ops-reviewed. **⟶ SUBSUMES Step 45** (see §6).
- **Step 79** — staging dashboard tab.
- Spin a **properly-loaded OC** for the ops reviews here (per the orchestration protocol — OC isn't a standing pipeline step, so brief it deliberately + full-load).

### Stream E — Ops / docs / legal
- **Step 47 — v0.2 maintenance plan + the CONSOLIDATED WATCH-LIST.** **⟶ UPDATE: the watch-list is BUILT + relocated to Soren's space** — `evryn-team-workspace/shared/projects/product/watch-list.md` (it's ultimately Soren's, tied to the v0.2 maintenance plan / EVR-114). Each entry: what we watch · decision it feeds · exit-criterion/trigger · owner. AC2's Haiku-shadow + cache-temperature watches fold in as generated (AC0 keeps it current; Haiku-shadow entry seeded, results-home = the Evryn Product dashboard tab). The broader **v0.2 maintenance plan** (observability/metrics, judgment-quality loop, regression suite, review cadence — BUILD doc stub) still owes a dedicated pass with Soren/OC (EVR-114).
- **Step 48 — gatekeeper-onboarding runbook** (PULL FORWARD). A reusable operator doc for onboarding *any* gatekeeper (register inbound lanes + set the gold-destination + the clustering-transparency framing **48a**). Proposed home `evryn-backend/docs/gatekeeper-onboarding.md`. **⟶ 48a cadence is LOCKED (see §6): twice-daily ~10am + ~4:30pm PT, forwards-only.**
- **Step 49** — written DB-rollback procedure in `operator-guide.md` (the ADR-037 `pg_dump` restore recipe, operator-readable).
- **Step 45** — operator visibility into the live pipeline (a correct silent pass looks like an outage to the operator). **⟶ UPDATE: SUBSUMED by Step 78** (dashboard-feed-with-status-lights) — **Justin's call: dashboard is enough, NO daily digest.**
- **Step 46** — cost-capture dashboard fast-follow (surface `llm_usage` spend). DC/OC.
- **Step 56** — Nathan glance at the v0.2 pass-stamp's stored `[shareable_with_user]` record (canary/ADR-010, GDPR Art. 22 / EU AI Act, retention). AC briefs → Nathan.
- **Step 52** — GA "disable sharing with Google products" on evryn.ai (Fenwick-flagged). **Justin's website/legal action** (still owed from him).
- **Step 43 — adversarial / cost-validation test batch.** AC builds fixtures; **⟶ Justin picked Megan Griffiths** (real-but-not-Mark → the run also exercises web-search). Run in **DEV**, fictional gatekeeper, **CLEAN profile, NEVER live-Mark.** ⚠️ the cost-measurement half is **POST-AC2-cleanup** (don't measure on bloat); the adversarial-security half runs independently. **These fixtures feed BOTH Step-88 gates (see §6).**

### Owed doc cleanup (from the 2026-06-29 deploy handoff)
DONE-status reformat of the round-2 sprint Steps · ARCH "Round-2 Hardening Absorptions" alignment pass · operator-guide cleanup (retired notify_queue refs, HEALTHCHECK rows) · re-pull `schema-reference.md` from prod. **⟶ Partly moving into AC6's arch/build-doc-reconciliation track — flag AC0 on whether you take any of these or leave them to AC6.**

---

## 3. ⭐ WHAT AC3 NEEDS FROM JUSTIN (status-tagged)

1. **The 54b design call** — how forwards land in permanent storage (his "contact↔Evryn message tagged intended-for-Mark" instinct is right — bring the 1-pager). **STILL OWED (no urgency).**
2. **Staging standup account actions** — 4th Supabase project ✅ done, 2nd Railway service + staging Slack app + test inbox still needed (Step 80). **IN PROGRESS — his slowest dependency; walk him through it.**
3. **GA disable** (Step 52) — his website/GA-admin action. **STILL OWED.**
4. **Adversarial-test character pick** (Step 43). **✅ RESOLVED — Megan Griffiths.**
5. **Mira coordination** — relay AC3's identity beats to Mira + review the voiced results (Streams A/B). **ONGOING (Step 55 near-term).**
6. **Spin decision on Step 53.** **✅ RESOLVED — its own dedicated agent later (off your plate).**
7. **Operator-visibility design input** (Step 45). **✅ RESOLVED — dashboard-only (45 ⊂ 78), no daily digest.**

---

## 4. AC0's suggested first-priorities (you + Justin decide)

**(1)** the **staging standup** (Step 80) — gated on Justin's slowest external actions, so start the clock (and it's the home of the pre-Mark integration gate); **(2)** the **consolidated watch-list** (Step 47) — DONE this wave, keep it fed; **(3)** the **near-term Mira determination-sharing beat** (Step 55, fires in Mark's first days). Defer **Step 53** to its own focused spin. Your judgment + Justin's sequencing govern.

---

## 5. How to run / deploy

- Identity-file changes deploy with a runtime bundle or standalone (pure-identity). For identity-as-runtime mechanics (Stream A), coordinate with whoever owns the touched runtime files (see §6 collision map) so it rides the right deploy.
- Spin DC / QC / OC as subagents per `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` (**verbatim briefs, `<task>` load-gate, full cascade — the load discipline is non-negotiable; enumerate the runtime LIVE, never trust a manual's hard-coded file list — see §6**). Coordinate Mira via Justin.
- **No AC-to-AC channel — route cross-lane through AC0.** If you find work that fits another lane better, route it to AC0.
- **Discipline:** verify branch before every edit · **commit only on Justin's explicit go** · stage by file path (never sweep another agent's work — lots of agents in the shared repos) · QC every real code change · `#dev-alerts` ops pings prefixed `AC3:`.
- **Operating model (load-bearing — Justin reinforced):** **ping Justin on `#team-alerts` EVERY response** (`SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`; Node `fetch`; prefix `AC3:`; one-line attention-tap, substance in chat — he runs ~8 agents). **Justin reads ONLY the chat, never the briefs/docs** — give him **CEO-altitude, cold** (name the thing, never a bare step number; assume he's read nothing you've read). **AC0 is a PEER, not your architectural superior** — he's the orchestration/collision-map; *you own this lane's shape* (be a lamp unto yourself on architecture; bounce collision/staffing to him). **AC0 coordination = append to THIS brief's tail + commit the appendage immediately.** **Never merge to `main` / deploy without Justin's explicit go.**

---

## 6. LIVE cross-lane collision map + coordination facts (AC0-confirmed — promoted from the collapsed threads)

*(This section carries every still-load-bearing coordination fact from the env-fix-era AC↔AC threads that §7 collapsed. The original §6 "open question back to AC0" is fully answered — folded here.)*

**Cross-lane file ownership (confirm before you edit a shared file):**
- **`config.ts`:** you own the supabase-selection region (the env-fix). AC2's Haiku pre-screen adds an additive `HAIKU_SCREEN_MODE` const (different region, reconciles cleanly). AC4 does NOT touch `config.ts`.
- **AC1 owns the ingest region** (`process.ts` / `poll.ts` / `client.ts`) — your **54b/54c** builds rebase onto AC1's *deployed* `main` (AC1 shipped + reaped its worktree). AC1 offered to ride the one-line `full_name='Mark Titus'` UPDATE (35c) — coordinate via AC0 if you want it.
- **AC4 (security Bundle 2, shipped v0.2.5)** owns **`find_user_by_name` (35a)** + the constrained `supabase_read` + `security/web-guard.ts` / `security/fence.ts`. **Do NOT build 35a.**
- **41a (untrusted-URL identity beat):** AC4 owns the *requirement*; **you/Mira voice it** — AC4 hands you the requirement once its structural half is shaped (relay via AC0).

**Beat-voicing rule (voice-after-mechanics):** any Mira beat touching the **cost-rewritten regions** of `triage.md` / `onboarding.md` — AC2 lands the structural rewrite (Steps 57/58) FIRST, THEN you/Mira voice on the settled shape (else voice drifts off a stale shape). **Exceptions:** **Step 55** (determination-sharing) is NOT a cost-beat → near-term, ungated; **Step 60** is already DONE (drop re-verify).

**48a clustering-transparency — cadence LOCKED:** Evryn runs Mark's inbox as a **clustered package, twice-daily ~10am + ~4:30pm PT** (Justin manually triggers the first few); **forwards-only** (direct messages stay real-time). Write the onboarding-runbook "we run your inbox as a clustered package, not up-to-the-minute" beat to that shape. Framing spans you + AC2 — sync via AC0.

**Step 45 ⊂ Step 78:** Step 78 (the 503-when-wedged `/health` + 5 dashboard lights) subsumes operator-visibility (Step 45). Shape = dashboard-feed-with-status-lights; **Justin: dashboard is enough, NO daily digest.**

**Step 88 = the pre-Mark integration gate (your lane owns the machinery of the final gate):** two gates — **(A)** a pre-cost-lane end-to-end baseline on **DEV**; **(B)** the *mandatory pre-Mark full create-from-zero live-fire* (everything active together), **preferred venue = staging (your Step 82, on the environment your Step 80 stands up) — but NOT blocked on staging: per SPRINT Step 88(B), if staging isn't up in time the gate runs `prod-create-from-zero` (prod is idle, Mark not forwarding — as Phase 6 ran).** Your **Step 43** adversarial fixtures (around Megan Griffiths) feed **BOTH** gates; the adversarial half runs independent of cost-cleanup, only the cost-measurement half waits on AC2. **AC0 orchestrates the actual runs; you build the fixtures; Justin picked the character.**

**⭐ DB changes now hit THREE DBs (Dev + Prod + Staging), not two — AC0 owns the routing.** The elegant path avoiding a manual triple-apply: **seed staging from a prod-shape `pg_dump` taken AFTER a migration lands on prod** → the seed captures every prod table automatically (clean order: e.g. AC2's ① Haiku table deploys to prod → THEN seed staging). If staging must be seeded before a migration ships, that migration gets a one-off manual apply to staging. **Protocol homes:** the three-DB apply method → **your Step-81 staging runbooks** (operational home) + `evryn-backend/backups/README.md` (name the third target); the **ARCH "Separate Supabase Projects" 3→4-projects bump = AC6's** (don't double-build). *(AC2's `haiku_screen_shadow` table is the first concrete case — it must land on staging when staging comes up; AC3 already flagged this to AC0.)*

**Orchestration discipline lesson (promoted):** when assembling a subagent's load, **enumerate the runtime LIVE** (`find src -name '*.ts'`) and load ALL of it — **do NOT trust any manual's hard-coded file list** (the runtime grew from ~8 files to 32+; a reviewer trusting a stale count under-loads). The QC-manual was fixed un-staleable this wave (CHANGELOG 2026-07-02); this is the AC-conductor-side reminder.

**⚠️ `.env` is gitignored + uncommittable — a fresh worktree or a new machine has NONE.** Because the env-fix made `EVRYN_ENV` `required()`, any config-importing test or local run **boot-fails** until you recreate it: `EVRYN_ENV=dev` + the qualified `SUPABASE_*_{PROD,DEV,STAGING}` trio (staging ref `hyqeutelwywbkisjekkb`). The 2026-07-03 scratchpad backup is session-local — gone in a fresh session, so recreate from this line. (Any lane copying `.env` into a worktree must use this reorganized qualified form, not the old unqualified one.)

**⚠️ Tooling gotcha — Soren's `MEMORY.md` has CRLF line-endings the Edit tool CHOKES on.** Any edit to it (e.g. updating the watch-list ownership note — Stream E) must go through Node or a CRLF-safe method, **NOT the Edit tool** (it silently fails / mangles). That's how the existing "Note from AC3" was inserted. *(This gotcha was buried in a parenthetical before and got skimmed past twice — it lives here now as a first-class item.)*

**AC0 is a peer / collision-map, not deeper-into-the-architecture than you** — Justin's framing. You own this lane's shape; bounce collisions/staffing to AC0.

**Open-optional (Justin didn't decide — raise if worth it):** add a companion *"enumerate the runtime live, don't trust a manual's hard-coded file list"* note to the AC orchestration protocol itself (the QC-manual fix already propagates the belt; this would be belt-and-suspenders on the conductor side).

---

## 7. SETTLED & SHIPPED — consolidated (see CHANGELOG; raw threads in git + the PRE-CONSOLIDATION scratchpad copy)

*The env-fix coordination arc and the settled batches below are DONE and memorialized — collapsed here to pointers. Every still-live remnant is carried in §6 or the 2026-07-07 handoff.*

- **DB env-selection fix (Step 87 / ADR-048) — SHIPPED, LIVE in `v0.2.5`.** Full arc (AC0 routing → AC3 design → Step 87 mint → merge-clearance → merge `e04af87` → Railway env-vars SET → live in `0c8c74b`) → **CHANGELOG 2026-07-02 (AC3)** + **2026-07-03 (AC3)**. What it does: `config.ts` `required("EVRYN_ENV")` (∈ production|dev|staging) + qualified `SUPABASE_URL_{ENV}`/`SUPABASE_SERVICE_KEY_{ENV}`; `getDb()` untouched; new `src/db/env-guard.ts` `assertNotProd()` hard-aborts on the prod ref; `tests/env-bootstrap.ts` defaults `EVRYN_ENV=dev`. ADR-048 on `_evryn-meta` main (`d95b1f2`). **It unblocked AC2's DEV test.** **🔑 LIVE REMNANT:** the now-DEAD unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` on **Railway prod** still want removing (bundle with the staging Railway step, Step 80; verify `v0.2.5` is live first, `--skip-deploys`) — see the 2026-07-07 handoff.
- **QC-manual un-staleable fix** (`evryn-quality/CLAUDE.md` `cdb1e89`) → **CHANGELOG 2026-07-02 (AC3)**. Lesson promoted to §6.
- **2026-07-03 batch** → **CHANGELOG 2026-07-03 (AC3)**: consolidated watch-list built + relocated to Soren's space (§6); operator-guide env doc-sync (my half) DONE; local `evryn-backend/.env` FIXED (`EVRYN_ENV=dev` + qualified prod/dev/staging trio, staging ref `hyqeutelwywbkisjekkb`); Step-43 character = Megan; **East DB deleted** (archive `backups/full-public-2026-06-03.sql` committed; stale East doc-refs fold into AC6's accuracy pass). Live remnants (Railway `_PROD` cleanup, ARCH doc-syncs = AC6's) carried in §6 / the handoff.

---

## ═══ AC3 HANDOFF → next-AC3 · 2026-07-09 (Thursday) — BOOT HERE (current state; supersedes ALL prior handoffs) ═══

**You are the next AC3 (identity/ops lane).** Boot: this whole brief (§0–7 + THIS section — the prior-handoff detail is condensed at the very bottom), then `_evryn-meta/CLAUDE.md` + the full startup cascade on `main`. **THIS section is complete current state as of 2026-07-09** — everything live is here; the collapsed prior handoffs below carry nothing you additionally need. **The §6 collision-map + gotchas (⚠ `.env`-recreate with the qualified `SUPABASE_*` trio; ⚠ Soren/team `MEMORY.md` is CRLF → never Edit-tool it, use Node) still stand — read them.**

### ⚡ HEADLINE — your lane is HELD on clustering, and the un-hold is NOW IMMINENT
Your two near-term identity fixes (54a, 54c) are BOTH held, gated on the **full clustering merge** (AC2's lane). **As of 2026-07-09 clustering is at DEPLOY-PREP: all 3 pieces are BUILT + QC-GO** — Piece B (the note-pathway, which edits `triage.md`/`core.md`/`gatekeeper.md` and is what collides with your Mira beats) just cleared heightened QC (AC2 "Round 16", unpushed `_evryn-meta` commits `702abc9`..`56c6379`). **NOT merged to `main` yet** (evryn-backend `main` = `9588687`, no clustering code on it; clustering lives on `dc/clustering-a-c` `e858909` [code — `process.ts`/`detect.ts`/`poll.ts`/+ new `cluster.ts`] + `mira/clustering-note-pathway` `b5c246b` [Mira identity]).
- **→ YOUR FIRST MOVE: check whether clustering merged to evryn-backend `main`** (`git -C evryn-backend log main --oneline | grep -i cluster`; or ask Justin/AC0). **The moment BOTH branches merge, un-hold:** (1) tell Justin the 54a Mira brief is ready to send Mira; (2) un-pause 54c (spin DC off post-clustering `main`). AC0 is on standing-owed to signal the merge — but check yourself; it's close.
- **Do NOT graft onto the live clustering branches** (AC0's ruling) — rebase your work onto post-clustering `main`.

### 54a (Fix #1 — history-check → permanent record): Mira brief READY, held
**The fix:** point Evryn's "have I met this person before?" triage check at the CONTACT's *permanent* record (the `record_pass` stamp + gold/edge/bad_actor `pending_notes`, reachable via `supabase_read` on `users` by the contact's email/id) instead of the 6-month-TTL `emailmgr_items`. **Keep both sources**, but she must KNOW the item-table is expiring so she doesn't burn tokens hunting it (Justin's steer). Verified reachable + buildable — pure-wording change (no code), deploys next `railway up`.
**Mira brief:** `docs/working/2026.07.07-ac3-mira-54a-history-check-permanent-store.md` (committed `a1aaa3b`, pushed). **HELD — not sent to Mira** (collides with clustering's `triage.md` edits). **Justin sends it to Mira** at clustering-merge; Mira works it on a FRESH worktree (not the clustering one).

### 54c (Fix #2 — real-name capture): designed, build PAUSED
**Confirmed in code:** `processForward` (`src/email/process.ts`) uses the email local-part as `display_name`, discarding the forwarded "From: Real Name <email>" name; `detect.ts`'s `extractOriginalFrom` returns only the email. **Fix:** surface the display name in `detect.ts` + use it in `processForward` (fall back to local-part only when no name). PLUS a Mira beat: a cheap Opus correction that upgrades an obvious junk placeholder when the signature clearly carries the real name — **never fabricate**.
Build PAUSED (premature `dc/54c-realname-capture` worktree reaped — no work lost). Waits on clustering (collides with its `process.ts`/`detect.ts` rewrite). Un-pause at merge (fresh DC worktree off post-clustering `main`; spin per `ac-orchestration-protocol.md` verbatim brief).

### ✅ Nathan (Step 56 legal glance): DONE + PUSHED this session
**Full review:** `evryn-team-workspace/shared/projects/legal/2026.07.09-step56-passstamp-determination-legal-review.md`. Nathan's memory entry filed to his Recent Notes. **Committed + pushed to the team repo** (`036fbd7`; Justin's explicit push auth; clean — only AC3's 2 files).
- **Bottom line: nothing gates Mark/v0.2.** The stored `[shareable_with_user]` pass-stamp is disclosed processing (PP §3.F/§2/§7), worded correctly (matching-decision not character-verdict; first-name-only), surfaced to no one; GDPR/EU-AI-Act don't apply to our WA/CA/federal/pre-revenue footprint. The ONE real question — Art.22/EU-AI-Act IF Evryn ever *surfaces* determinations to contacts AND we expand to EU — is deferred to a v0.3/Fenwick checkpoint. **Sharp insight to bank:** the "no significant effects" position is *load-bearing* (keeps the pass out of Art.22, which keeps the Canary Principle from becoming its own Art.22 violation). **New catch:** the stored stamp is **DSAR-reachable today** (v0.3 DSAR flow must strip the gatekeeper UUID + redact the first name).
- **⚠ Routing is OWED, not done** (Nathan's task was read-only — he actioned nothing): v0.3 ToS/PP checklist adds (DSAR note, retention line, Art.22-surfacing-Fenwick-gate — **DE-DUPE against the existing significant-effects/recruiter content at checklist ~lines 331-333**), a T&S-spoke pointer (propose to Justin), a Mira round-2 identity flag, a Soren v0.3 build-flag. ALL non-blocking, v0.3.
- **Team-agent memory-write mechanics (learned — reuse for any future team-subagent memory write):** the memory file is CRLF (**Edit tool corrupts it — write via Node**), needs Justin's **live auth + a Slack-ping right before the write**, and follow the team #lock Memory protocol (`evryn-team-workspace/shared/protocols/lock-protocol.md` → Memory section: **append to Recent Notes ONLY, never Story**; first-person; timestamped).

### Mark status (correction — carry this): radio-silent, NOT lost
Justin (2026-07-09): Mark is just in radio silence — normal for him to go quiet for *weeks*; **NOT written off**. Supersedes a "Mark may be lost" note that had floated in team/Nathan memory from 7/08 (AC3 corrected it in Nathan's memory this session). **The pre-Mark critical path (staging dress rehearsal → Mark forwarding) STANDS.**

### Staging (Step 80 / ADR-045): UP + healthy — home of the pre-Mark integration gate (Step 82/88)
Fully stood up 2026-07-07 (full detail: CHANGELOG 2026-07-07 + checklist `docs/working/2026.07.01-ac3-justin-staging-standup-checklist.md`). Load-bearing state + gotchas:
- Railway service **`evryn-staging`** (evryn-backend project, `production` env) deployed + polling the staging inbox; **`SEND_ENABLED=false`** → **flip to `true` at validation-test time**. Allowlist-gated (`STAGING_RECIPIENT_ALLOWLIST=systemtest@evryn.ai,review@evryn.ai`).
- **⚙ DEPLOY METHOD:** `railway up --service evryn-staging --environment production` from `evryn-backend/` (NO GitHub-connect). **⚠ a BARE `railway up` deploys PROD — ALWAYS pass `--service evryn-staging`.**
- 4th Supabase project "Evryn Product — Staging" ref `hyqeutelwywbkisjekkb` (Oregon), seeded prod-SHAPE, ZERO PII; **reset recipe** = `evryn-backend/backups/staging-seed-*-2026-07-07.sql`. Account resolutions (no new paid seats): `systemtest@evryn.ai` = fake-Mark (stays an alias); staging inbox `evrynstagingtesting@gmail.com` = a standalone free @gmail.
- **The validation run (Step 82/88):** fake-Mark @ `systemtest@evryn.ai` forwards → `evrynstagingtesting@gmail.com` → approve in `#evryn-approvals-staging` → verify the whole loop with everything active. Step-43 Megan fixtures feed it; AC0 orchestrates. Gated on: clustering being live in the bundle + Justin's `SEND_ENABLED` flip.

### 54b + user-creation-at-verdict: OFF your plate → AC2's Step 57
54b (forward-persistence) FOLDED into **Step 57 (Runtime Bookkeeping, AC2's lane)** + a NEW **user-creation-at-verdict open question** (should user-creation move to verdict-time + skip `ignore`? — with the Mark "I already took care of this, ignore it" case), also routed to AC0/AC2. Both = one verdict-keyed redesign AC2 owns. **Design input (keep the pointer):** `docs/working/2026.07.07-ac3-dc-54b-forward-persistence-build-brief.md` (Justin picked option C = verdict-keyed). 54a proceeds independently of it.

### Step 43 (Megan fixtures): WRITTEN
`evryn-backend/tests/2026.07.06-step43-megan-fixtures.md` (`9339ba0`): Megan-Griffiths answer-key + judgment/cost suite + adversarial suite re-aimed at the v0.2 guards + spoofed-forward. DEV/fictional-gatekeeper/never-live-Mark; feeds BOTH Step-88 gates. **Mechanical follow-on:** transcribe §3 → `tests/fixtures/emails/megan-*.txt` for `run-fixtures.ts`. AC0 orchestrates runs.

### Context: the AC6 docs reconciliation (merged 7/09, docs-only)
ARCHITECTURE.md reworked for accuracy; BUILD-EVRYN-MVP.md slimmed to v0.2 + NEW `BUILD-EVRYN-v0.3.md` + `BUILD-FUTURE.md` (v0.3+ relocated verbatim). Sprint: Step 55 DONE, Step 89 (Supabase Security Advisor sweep — Justin/OC) minted. **⚠ NOT a license to skip the runtime** — the granular actual `src/` stays load-bearing; the docs are a trustworthy on-ramp, not a substitute. **Runtime (`src/`) UNCHANGED by this pass.**

### AC3's queue (fresh instance — in order)
1. **[FIRST — check clustering-merge]** un-hold 54a + 54c the moment clustering (both branches) merges to `main` (see HEADLINE).
2. **[HELD on clustering]** 54a Mira beat (brief ready) → Justin sends to Mira at merge.
3. **[HELD on clustering]** 54c real-name (designed) → spin DC at merge.
4. **[on Justin's say-so]** Mira decline-*offer* runtime+identity pair (Step-55 routed-back: a gatekeeper decline-offer + default-safe; the default-safe needs a timeout/re-wake + brushes the approval gate → build only on Justin's explicit go).
5. **[DONE]** Nathan Step 56 (this session).
6. **[gated on clustering + Justin's SEND flip]** Staging validation run (Step 82/88) — the pre-Mark integration gate; AC0 orchestrates; Megan fixtures feed it.
7. **[non-colliding fill-work while held]** ops/docs: Step 48 (gatekeeper-onboarding runbook — incl. 48a clustering-transparency cadence, twice-daily ~10am + ~4:30pm, forwards-only), Step 49 (DB-rollback procedure in operator-guide) — pure docs, don't touch clustering's files. **Defer** Step 51 (compulsion audit — touches `classify.ts`, likely collides with clustering). Step 46 (cost dashboard) = DC/OC. Step 52 (GA "disable sharing with Google" on evryn.ai) = **Justin's website action, still owed from him**.

### ⚙ Working model + a lesson (durable — propose the lesson to CLAUDE.md on Justin's go)
- **Ping `#team-alerts` on every response** (`SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`; Node `fetch`; prefix `AC3:`; one-line attention-tap, substance in chat). Justin runs ~8 agents; give him CEO-altitude, cold (name the thing, never a bare step number). **AC0 is a PEER** (collision-map/orchestration), not your architectural superior — own your lane's shape; bounce collisions/staffing to him. **Never merge to `main`/deploy without Justin's explicit go.** Numbered ballots when you need him; low-res updates on tracks that don't involve him; one focused chunk at a time (don't interleave two live design problems); autonomy once aligned ("knock it out"; only pause for real decisions).
- **LESSON (Justin, 2026-07-09):** **distinguish what a subagent ACTUALLY DID from what it RECOMMENDED/INTENDED — verify, don't conflate.** AC3 told Justin Nathan "captured/routed" items when Nathan (read-only) had only *recommended* them + told himself to remember to do them later. "Captured"/"routed" implies *done*. When relaying subagent output: state plainly what was actioned vs. owed, and VERIFY (check the actual writes/tool-uses) — never take a subagent's "I'll route this" as done.

### For AC0
- **Nathan Step-56 DONE + committed+pushed** to the team repo (`036fbd7`; Justin's explicit push auth) — already pushed, nothing owed there.
- **`_evryn-meta` push-reconcile:** end-of-session `_evryn-meta` `main` is **ahead** by AC2's 5 Round-16 clustering-brief commits (`702abc9`..`56c6379`) + AC3's handoff/consolidation commit(s). **AC3 did NOT push `_evryn-meta`** (AC2's commits aren't AC3's to push — per Justin's rule; push-reconcile is yours). Tree clean.
- **Clustering-merge → AC3-un-hold signal** is on your standing owed-list — with Piece B now QC-GO/deploy-prep it's imminent; flag AC3 (via Justin) at merge.
- **CHANGELOG entry owed** for Nathan Step-56 (done, non-blocking) — per the Part-A "AC0 keeps records current at #lock" process (AC3 flagged rather than wrote it).
- **Step 53 core.md split-cleanup** (Mira routed — core.md decline/canary → `declining.md` at the module-split) still stands.
- **Prod `_PROD` env-var cleanup** (corrected — supersedes the older '--skip-deploys' phrasing in §7): the dead unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` on the Railway PROD service ride the NEXT coordinated prod `railway up` — `railway variable delete` has NO `--skip-deploys`, so it CANNOT be bundled early; delete them in the same window as the next prod deploy. (Detail: CHANGELOG 2026-07-07.)

### Repo state (end of session 2026-07-09)
- `evryn-backend` `main` = `9588687` (AC6 reconciliation + sprint housekeeping), synced. Clustering built+QC-GO on branches, NOT merged.
- `_evryn-meta` `main` = ahead (AC2 ×5 + AC3 handoff), NOT pushed (AC0 reconciles). Tree clean after the handoff commit.
- `evryn-team-workspace` = Nathan Step-56 (`036fbd7`) committed + PUSHED. Synced.
- Prod stays `v0.2.6`. Nothing deployed this session. Identity-fix lane held on clustering (imminent un-hold).

— AC3, 2026-07-09

---

## ═══ 2026-07-09 EVENING — AC3 session appendage → AC0 ═══

*Justin set down for the night ("commit what you need; AC0 handles all pushes"). Everything below is committed; AC0 pushes `evryn-team-workspace` + `_evryn-meta`.*

**Fill-work done while the identity lane is held (all committed):**
- **3 ops docs → `evryn-backend` main (`5f2c019`, PUSHED):** (1) **Step 48** — NEW reusable gatekeeper-onboarding runbook `docs/gatekeeper-onboarding.md` (48a clustering-transparency framing marked **PENDING** — do not promise the clustered-package model to a gatekeeper until clustering ships); (2) **Step 49** — a Database Backup & Restore section added to `docs/operator-guide.md` (two-layer model + pointer to `backups/README.md`); (3) **Step 43** — the §3 Megan judgment fixtures transcribed to runnable `.txt` (`tests/fixtures/emails/megan/` — a01–a13 + README). ⚠ **Placed in a `megan/` SUBDIR** (not flat `megan-*.txt` as the design doc named) because `run-fixtures.ts` globs the whole `emails/` dir → flat would mix Mark's 18 + Megan's 13 against one seeded gatekeeper. **Follow-on for the cost-run:** the runner needs pointing at the subdir (small DC/runner tweak — AC0's call when the Megan run is scheduled). *(`5f2c019` also carried Justin's `d2dff2a` Step-88 sprint line that had landed on `main` between my commit and push — attribution clean.)*
- **Task-4 (Nathan legal routing) BACKED OUT:** I'd added a "P2a" pointer to the v0.3 ToS/PP checklist; **reverted it** (team-workspace clean). Reason (Justin's call): the whole routing (checklist adds · T&S pointer · Soren build-flag) is already in **Nathan's** memory (his Step-56 "Open items"), NOT Soren's — so it's Nathan's to route + de-dupe at the v0.3 legal pass, not AC3's to write blind.

**⭐ Step 52 (GA "disable sharing with Google products") = MOOT — AC0 please mark it in `SPRINT-V0.2-HARDENING.md`.** Google Analytics is **gone** from evryn.ai: Justin's analytics.google.com check shows no active property, and the site (repo + a live evryn.ai fetch) runs `@vercel/analytics` (cookieless) + HubSpot forms — no gtag / GA measurement-id / Tag Manager. Nothing to disable. Residual = a Privacy-Notice-accuracy item (the PP still discloses GA; Vercel/HubSpot undisclosed) → **written into Nathan's memory** (`evryn-team-workspace` `17910c7`, unpushed) for the v0.3 legal pass. Don't re-add GA — Vercel Analytics covers the marketing-site need and is on-brand (privacy-first, no ad-ecosystem sharing).

**⚠ CORRECTION to this handoff's HEADLINE (which said the un-hold is "imminent"):** clustering is **NOT** imminent. As of `cf4d1ee` (AC2 Round 17, committed AFTER this handoff was written), **clustering deploy is PAUSED on a Piece-B ship-blocker** (a personal-note reply collides with a queued triage item). So 54a/54c stay held, and the un-hold is gated on AC2 resolving that blocker — further out than "imminent."

**Commits this session:** `evryn-backend` `5f2c019` **PUSHED** · `evryn-team-workspace` `17910c7` (Nathan memory) **unpushed** · `_evryn-meta` this appendage **unpushed**. — AC3, 2026-07-09 evening

---

## PRIOR HANDOFFS (2026-07-03 · 2026-07-07 · the 2026-07-07 collision thread) — SETTLED, condensed

*All live state from these has moved up into the 2026-07-09 handoff above; kept here as a one-block record so nothing's lost. Full detail: CHANGELOG 2026-07-02/03/07 + git history + the checklist doc.*

- **2026-07-03 batch (AC3):** DB env-selection fix (Step 87/ADR-048) SHIPPED in `v0.2.5`; QC-manual un-staleable fix; consolidated watch-list built + relocated to Soren's space; operator-guide env doc-sync; local `.env` fixed (`EVRYN_ENV=dev` + qualified prod/dev/staging trio); East DB deleted; Megan picked as the Step-43 gatekeeper. → CHANGELOG 2026-07-02/03.
- **2026-07-07 session-2 handoff (AC3):** staging STOOD UP + healthy (now carried in the "Staging" section above); Step-55 `declining.md` MERGED (`83bdc2c`); 54b FOLDED → Step 57 + the user-creation-at-verdict open question (both carried above); Step-43 Megan fixtures written (carried above); the Mira decline-*offer* routed-back item (now queue #4 above); the guardrail-placement brief moved to `docs/sessions/historical/2026.07/` (DONE); working-prefs (carried above). → CHANGELOG 2026-07-07.
- **2026-07-07 collision thread (AC3→AC0 + AC0's reply):** AC3 flagged that clustering rewrites the exact `process.ts`/`detect.ts` + `triage.md`/`core.md`/`gatekeeper.md` surfaces 54a/54c touch; **AC0 CONFIRMED clustering-first sequencing** (rebase onto post-clustering `main`, do NOT graft onto the live clustering branch) and is on standing-owed to signal the merge. Mira-worktree calls: leave `mira/clustering-note-pathway` (AC2's) alone; the stale `mira/declining-md-step55-2026-07-06` (remote-only, merged) reap is held for Justin's explicit go. All of this is live in the HEADLINE + 54a/54c sections above.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
