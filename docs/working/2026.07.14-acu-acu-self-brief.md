# ACU Self-Brief — working doc

> **Truncation check:** the last line should read `FULL FILE LOADED`.
>
> **What this is:** ACU's continuity note — written so a **fresh ACU instance (or a post-compaction me) can pick up cold and be genuinely useful**, not vaguely oriented. Working doc, not source-of-truth. The **master plan is `_evryn-meta/docs/working/2026.07.13-acu-uber-week-plan.md`** — this file is the pointer + live-state layer.

*Rewritten clean: Thu 2026-07-16 (fragment sweep + fresh-eyes pass, at Justin's request). Updated ~11:50am: meter/Fable-outage · 5 open loops closed · the two-state-report mechanic memorialized.*

***Latest: Thu 2026-07-16 19:20 PT — night set-down (fragment sweep + fresh-eyes pass at Justin's request, for crash-safety; he is NOT re-spinning ACU). START AT THE 🔻 APPENDAGE DIRECTLY BELOW — it supersedes the older live-state sections wherever they conflict.*** *The fresh-eyes pass fixed 5 internal contradictions, incl. one that would have re-broken things: this file told a future ACU to spin reporters in **background**, which now contradicts Justin's foreground default and would have made him blind again.*

---

## 🔻 APPENDAGE — Thursday 2026-07-16, 19:20 PT set-down (READ THIS BEFORE THE READ-THIS-FIRST BELOW)

> **Why this exists:** Justin shut down for the night and asked for full state persistence *in case his computer crashes* — he is NOT re-spinning ACU. If you are a fresh ACU reading this cold, **this appendage supersedes the older live-state sections below wherever they conflict.** Read this block, then the master plan, then the rest of this file.

### What today actually was

Today was **not** the week-planning day it started as. It became **a protocol day**, and that was right. Two findings dwarfed everything else:

1. **🔑 THE AC-UNDER-AC MODEL IS PROVEN AND GREEN.** Justin's idea, and it's the biggest lever available: *stop spinning a new top-level AC for every lane* — he's been babysitting ~8 surfaces. Instead **AC0 supervises AC2/AC3; ACf supervises ACf1/ACf2; Justin works ~3 surfaces.** ACU ran a 3-generation experiment: **a background child's report returns to its PARENT automatically** — no addressing, no `SendMessage`. **The 2026-07-15 "grandchild leaks to `main`" entry was WRONG as a system description** — it described one over-instructed brief. `SendMessage`'s only *named* recipient is `main` = the **top-level session**, so a child *told to report back* falls back to it. **⇒ Never tell a child where to report.** Protocol rewritten accordingly.
2. **🔴 EVERY AC HAS BEEN LOADING HALF THE RUNTIME.** Justin asked *"the full cascade says 'including the identity files', right?"* — **it did not.** AC's own CLAUDE.md said "the runtime — `evryn-backend/src/`" and **never mentioned identity files anywhere.** Same gap in DC's, Soren's, and (structurally) QC's. Justin then **generalized it: identity-as-runtime holds for ANY agentic system**, which exposed that **`#cascade-override` exempted `evryn-team-runtime`** — and ACU had walked straight through that hole (see the caveat on the team-runtime report). All four manuals + the protocol fixed today.

### ⛔ THE NEXT ACTION — Justin must vet these in SCM before ANY commit

**Uncommitted, held deliberately** (he asked to vet in source control before bed; he may or may not have gotten to it):
- `_evryn-meta/docs/working/2026.07.14-acu-acu-self-brief.md` ← *this file*
- `_evryn-meta/docs/working/2026.07.16-acu-team-runtime-state-report.md` (the 🔴 scope caveat)
- `evryn-team-workspace/.claude/agents/soren.md` (identity-as-runtime generalized)

**⚠️ `evryn-team-workspace` also shows `shared/projects/ops/team-runtime/2026.07.15-acf-acf2-phase2-consolidation-brief.md` modified — that is NOT ACU's. It's ACf's live work. NEVER stage it.** Stage by explicit path, always.

**Also: `_evryn-meta` is ~10 commits ahead of origin, unpushed. ACU does NOT push — AC0 does.** ⚠️ **And pushing `_evryn-meta` IS a live Vercel dashboard deploy** — so that backlog is not a "just push it" situation. Route it to AC0/Justin.

### 🧾 TWO NOTES JUSTIN ASKED FOR — deliver these when he's back (both in the chat log; re-derive if lost)

1. **A note for AC0** — Justin wants him to **run QC over the whole shebang at the next stable point**, to see whether she now catches anything she *should* have caught before. Context he needs: QC's *enumeration recipe* (`find src -name "*.ts"`) structurally could not return an identity file — **but her Tier-1 list named the identity files separately as item 10, so her review history is probably NOT compromised.** The sweep is to confirm that redundancy actually held in practice.
2. **A note for anyone downwind of the team-runtime report** — it now carries a 🔴 SCOPE CAVEAT; its harness findings stand, its behavioral risk register does not.

### The three live facts most likely to be STALE by the time you read this

- **ACf1 is building hard** — branch `acf1/mem-a-scratchpad`, own worktree, 7 commits 14:51→17:07 (item (a) COMPLETE, QC-GO, 214/214, awaiting Justin's merge-go per `c839c12`). **ACU's earlier "ACf1 hasn't started" was AC-verified TRUE at 12:30 and FALSE by 14:51 — a live finding decayed in 2 hours. Re-sample before quoting anything.**
- **Lucas was spun on Fable *before* Fable bricked** — may need a re-spin on Opus. Unconfirmed.
- **Fable was bricked all day** (API errors; everyone on Opus). **Justin asked ACU to prompt him to re-test periodically.** ⚠️ **Fable leaves the subscription after Sun 07-19 — it's API-only after that**, so the AC-under-AC structure matters regardless (and Justin's read: it'd work *better* under Fable, so nothing's wasted).

### Justin's standing decisions from today — do not re-litigate

- **FOREGROUND is the default for subagents.** Reason: **background renders NOTHING on his screen; foreground renders everything.** He *wants* to audit. Foreground does **not** force serial (two in one message block run concurrently). This is why the naming rules are load-bearing, not cosmetic.
- **Naming:** `# AC0's read` (top-level only, name first) · `## AC2's read` (sub-AC, auditable) · **DC/QC/OC/team BANNED from `'s read`** entirely.
- **Ballots: just use plain numbers.** He hates `Q-PROTO`-style IDs. Only rule: **don't put two contradicting numbering systems in one round.** *(ACU's `Q-PROTOCOL`/`Q-PASTELINES` both abbreviated to "QP" and he had to guess.)*
- **Signpost when important content sits ABOVE a wall of output** — he reads the end of a response and misses earlier substance. Give him a search string.
- **The AC2-ONLY test** is the next move on the restructure: AC2 and AC0 are already working closely, so it's the natural pilot. **Limit the blast radius; don't plug every sub-AC in at once.**

### The one thing ACU still owes and has now dropped THREE times

**AC3's two CLAUDE.md lesson candidates.** Propose wording on Justin's go: **(a)** distinguish what a subagent *actually DID* vs. what it *RECOMMENDED/INTENDED* — verify, don't conflate; **(b)** before routing an item into another agent's domain doc, check whether it's already in that agent's memory as their own owed self-note. **Stop deferring these.**

Also still open, unanswered: **DC's CLAUDE.md has NO truncation canary and never has** — the agent who writes all our code has the one manual that can't self-report a bad load. ACU recommended adding one; Justin never answered.

---

## 🛑 READ THIS FIRST

**You are soft. Do not run forward on a softened picture — re-read before acting.** Justin's standing instruction: a compacted/fresh ACU must re-read, because felt-sharpness is not sharpness. In order:

1. **`docs/working/2026.07.13-acu-uber-week-plan.md` — IN FULL.** The master plan. Its `SCOPE RESET` and `RE-JIGGER` blocks are the current authority; the older day-by-day below them is superseded where they conflict.
2. **This file, all of it.**
3. **The two state reports** (`docs/working/2026.07.16-acu-{product,team-runtime}-state-report.md`) — **these SUPERSEDE the old "go read ARCHITECTURE + SPRINT yourself" instruction.** They exist precisely so ACU isn't flying blind *without* burning its whole context on a 22K-line corpus (Justin's 07-16 design). **Read Layer 1 of each for the mental model; treat Layer 2 as perishable and re-sample it** (each ends with its own "how to re-sample" section). ⚠️ **The team-runtime report carries a 🔴 SCOPE CAVEAT — read that first.** *(If you will genuinely **direct build work**, the reports are still not a substitute for the runtime: load `evryn-backend/docs/ARCHITECTURE.md` (1244 ln) + `docs/SPRINT-V0.2-HARDENING.md` + BOTH runtime halves. Justin's 07-14 correction stands: ACU-as-orchestrator can lean on the map; ACU-directing-build cannot.)*

**Then ping Justin that you reloaded** before doing anything else.

---

## Who ACU is

Meta-layer **above** AC0 and ACf — not their boss, not a lane. Holds the cross-lane map; drives **Justin's** critical path.

**The role boundary — "don't reach into other chefs' dishes" (Justin, 2026-07-14, learned the hard way).** Do NOT re-inventory or re-issue work AC0/ACf are already driving with Justin. It double-tracks, burns tokens, lags reality (their briefs update faster than you can read them), and scrambles Justin (*"wait — am I doing that here or over there?"*). Surface ONLY:
1. **Justin's own critical path** — what only he can do, sequenced.
2. **Genuine cross-track dependencies** — *"the thing you're doing with ACf gates Gate-B over here."*
3. **ACU's own-lane work** — the week plan, the Friday manifest, research batches.

Consume monitor/lane output to keep **your** map current; tell Justin only the useful, non-scrambling slice. **When you do reference lane work, zone it under a heading** (`ACf` / `AC0 → AC2/AC3`) so he reads it as *over there*.

**Hard habits:**
- **Ping Justin on `#team-alerts` EVERY response, no exceptions** — even when he seems present. Node `fetch` to `SLACK_TEAM_WEBHOOK_URL` (in `evryn-team-workspace/.env`); never bash+curl or PowerShell. Prefixes: `Need you:` / `No action needed - just progress:` / `Done for the day`.
- **Recommend-first, always** — context → recommendation + why → alternatives rejected → the numbered question **right under its reasoning**. Never an open *"what do you want to do?"*. Recap ballot at the end complements, never replaces, inline questions.
- **Real bullets / numbered lists.** Never `a · b · c` interpunct smooshes — Justin has flagged this twice.
- **Model-pin every subagent spin** explicitly — **and pinning is RECURSIVE**: an unpinned spawn inherits its parent's model *down the whole chain* (a Fable head with unpinned children = a silent Fable DC+QC at ~2×).
- **Headers (updated 2026-07-16 — a bare `# AC's read` is now WRONG):** ACU's relay to Justin opens **`# ACU's read — <topic>`** (H1, **your designated name first** — he reads 4 surfaces and must know instantly which AC is talking). A **sub-AC** uses `## AC2's read` (never `#`, so he can still audit it). **DC/QC/OC/team agents are BANNED from `'s read` in any form** — they say `## For ACU — <topic>`. The `#` level is reserved exclusively for the top-level AC's voice; that's the boundary marker Justin scans to.

**Standing auths (Justin):**
- **Commit the week-plan + ACU's own working docs freely** when changed — no pre-vet; just tell him what changed. **Do NOT push** (AC0 pushes). **Never commit another agent's files.**
- **The team current-state (`evryn-team-workspace/shared/current-state/current-state.md`) is Lucas's — NEVER touch it.** ACU over-scoped into it on 2026-07-14 and was corrected. Schedule the standup; don't do it.

---

## The plan, in one breath

**v0.3 is OFF this week** (soft bonus only if both targets land AND room remains). Two completion targets:

- **(T) Team autonomy ON** — ACf's: Lucas paces → stamp the 7 (conversation-only) → Meta-Meeting → guardrail verify → autonomy.
- **(P) Mark-ready v0.2** — Step 57 shipped + Gate-A (DEV baseline, *before* 57 merges) + **Gate-B (full staging live-fire — the real pre-Mark gate; nothing has run end-to-end since 2026-06-11)** + 54a/54c + the two prod flips staged (Clustering `off`→`manual`→`scheduled`; Haiku `off`→`shadow`→`active`).

**Locked contingencies:**
- **Step 57 goes IN the live-fire** — never run Gate-B on bare `main` and re-do it. AC0 guaranteed 57 by Friday.
- **Mark-arrival override:** proceed as planned until Mark emails. **The instant he does** → ~2-day get-stable-and-tested sprint. Justin can stall him a few days; Mark does NOT come in until Gate-B passes.

---

## ⚡ Live state — Thu 2026-07-16

**(P) Product — AHEAD, largely agent-propelled.** Step 57 (Runtime Bookkeeping) **built + independently reviewed, zero blockers** — needs live rehearsal (Gate-A) → final gates → merge → deploy. 54a/54c merged. AC7 doc-accuracy fix landed. Still `v0.2.7`, dormant, pre-gatekeeper.

*Why Step 57 matters (don't under-rate it):* it's not "a cost change" — it (a) cuts the cost (runtime does bookkeeping, Evryn only judges), (b) **removes Evryn's raw DB-write tool** — the write-side twin of the shipped read-lock, closing the last big structural security door, and (c) **fixes a real audit gap** (gold/edge verdicts aren't recorded in the queryable field today).

**(T) Team — BEHIND, but moving fast as of Thu evening.** Lucas passed **4/8 paces**; runtime **deployed to Railway and RUNNING — but `__global__.active=false`, which is the *entire* dormancy guarantee**; the 7 others **unstamped**. ACf found **Lucas's memory wasn't robust** and pivoted to fix it (right call, not fast). **Meta-Meeting is OFF Thursday** → Fri PM/weekend. Its pre-work — **Soren's consolidation · blue-skies (Emma, Dominic, Thea, Nathan, then Lucas last) · sweep/align** — is itself most of a Justin-day, which is *why* the meeting keeps slipping.

**⏱ UPDATE (Thu 19:20) — supersedes the paragraph above where they conflict:** **ACf1 has been building hard since 14:51** (branch `acf1/mem-a-scratchpad`, own worktree; **item (a) COMPLETE, QC-GO, 214/214, awaiting only Justin's merge-go** per `c839c12`). **Soren's consolidation LANDED** (`ab414bc` — ballot-placement mechanic + a write/read-path pre-flight). **Lucas was spun** (on Fable, *before* it bricked — may need an Opus re-spin). **🔴 The one thing that will break the Meta-Meeting if nobody fixes it: the money governor is inert** — `$10/hr` trailing ceiling vs **~$1.00/wake measured**, so an 8-agent meeting halts itself. **Justin has already pasted that note to ACf.** It's ACf's fix; don't chase it.

**Memory lanes (ACf):** Phase 1 = Opus-only build load, no ACU action. **Phase 2 (ACf2) → Friday blitz** — main session on **Opus**, escalate only design-synthesis turns to Fable. Justin relayed this to ACf as *"Phase 2 can happen as soon as it makes sense in his flow."*

---

## ⚡ The Fable/usage finding (Thu 2026-07-16 — researched, changes the week's economics)

Justin's meters read All-models **4%** / Fable **0%** with a 23h reset, after a week of heavy use. **Not a glitch.**

- **Anthropic did a goodwill reset** of 5-hr + weekly limits for all users (~Jul 15). Established pattern — 4+ prior, always after outages; there were outages Jul 14/15/16.
- **Fable shares the ONE weekly pool** — it's a **50% sub-cap** of it, **not** a separate pool, **no** separate clock. *(This answers Justin's "can Fable be on a different clock?" — no.)*
- **Goodwill resets do NOT move the scheduled reset** (Anthropic's own doc) → full allowance **now** AND another at the Friday reset. Today's refill is genuinely use-it-or-lose-it.
- **🔴 TWO PROMOS EXPIRE SUN 2026-07-19, 11:59pm PT — THE REAL CLIFF (not Friday):** (a) Claude Code weekly limits **+50%** (5-hr unaffected); (b) **Fable FREE** up to 50% of weekly. After Jul 19: standard limits, Fable reverts to paid credits. **The whole Fri–Sun blitz sits inside the promo window. Next week the old scarcity returns** — that's the genuine planning cliff.
- **⇒ Fable rationing is OFF through Sunday.** The week plan's per-turn husbanding rubric and Thursday ~10% reserve are **MOOT**. **The 5-hr window is now the true governor** → spread work across windows, run overnight; don't pack one.
- *Epistemic honesty:* the specific reset announcement is inferred (3 agreeing signals; X blocked direct fetch). The **mechanism** is from Anthropic's docs, so the conclusion holds regardless.

**Updates (Justin, Thu 2026-07-16 ~11:30am):**
- **⚠️ FABLE IS BRICKED.** Repeated API errors; Justin tested and reverted every instance to Opus. Likely load (everyone's sprinting on the same reset). **Plan around Opus; do NOT gate work on Fable returning.** Justin re-tests periodically — **ACU's job to prompt him now and again.** Note the two promos are **independent**: a Fable outage does NOT cost us the +50% weekly boost, which applies to Opus and also dies Sunday. A week of *Opus* is still a very big deal.
- **⇒ TWO blitzes, not one:** Thursday (today, ~22h) + Fri 9:59am → Sun. The manifest covers both.
- **💰 Extra-usage is OFF (Justin confirmed).** Hitting a cap *blocks*, never bills. **There is no money exposure** — worst case is a wall and a wait. That's the permission slip to be aggressive. *(Open loop 1 below: CLOSED.)*
- **Justin's hard bottlenecks are now exactly two: the 5-hr rolling window, and his own attention.** Not tokens. Design around those.

---

## Open loops

**CLOSED (Justin, Thu 2026-07-16) — do not re-ask:**
1. ~~Extra-usage toggle~~ → **OFF. Confirmed. No money exposure.**
2. ~~Fold the meter finding into the week plan~~ → **DONE** (dated supersede-block at the top of the week plan + an authority-order note in its header). Justin: *"fold the meter finding into ALL your thinking."*
3. **Anthropic key-split → STAYS IN AC0's LANE. Not ACU's.** Justin's ruling: *"if that's in AC0's lane, let it stay in AC0's lane — too many cooks. When AC0 is ready for that, he'll let me know."* **Only take it on if it's needed OUTSIDE AC0's lane.** Runbook stays banked at `docs/working/2026.07.13-acu-ac0-anthropic-key-split-runbook.md`.
4. ~~Was Lucas spun?~~ → **YES — spun Thu, on Fable, with ACU's brief.** (Pre-dates the Fable outage; may need re-spinning on Opus — worth asking.)
5. **Fable-vs-Opus experiment → Justin owns the read, not ACU.** He hasn't switched everyone yet; he's converting instances to Fable as he talks to them and is *actively watching*. His ruling: *"I'll have the best read on that — check in with me later."* **Do NOT go interrogate AC0 about it.** *(Moot while Fable is bricked.)*

**ACU's own — ✅ DONE 2026-07-16 (kept so you don't redo them):**
6. ~~**The Blitz Manifest**~~ → **WRITTEN + COMMITTED**: `docs/working/2026.07.16-acu-justin-blitz-manifest.md`. **It is NOT a burn-down — it's a routing plan for Justin's attention**, because both full-load reporters independently found that's the binding constraint, not compute. Central finding: **~9 Justin-blocks against ~1 working day; it does not fit, so choose what gives deliberately.** Recommends **(T) team gets his blocks / (P) product runs agent-side / Gate-B slips to Friday** (Mark is radio-silent; the Mark-arrival override still inverts this instantly). **Justin's response: *"I think if we get the ACs-under-ACs thing fixed, a lot of this is fixed. We can revisit once we've tested this and it actually works."* ⇒ The manifest is PARKED behind the AC2 test, not rejected.**
8b. ~~**The AC-subagent protocol gap**~~ → **CLOSED + COMMITTED.** `ac-orchestration-protocol.md` now has a full `## Spinning an AC subagent` section (2 pre-approved substitutions), `## The escalation ladder`, `## The work brief`, `## Receipts up the chain`, `## The generic-subagent exception`, a rewritten `## Nested subagents`, and the foreground default. **Read the protocol; do not re-derive any of it from this file.**
7. **AC3's two CLAUDE.md lesson candidates — DROPPED TWICE, still owed.** ACU said it'd fold them into a ballot and never did: (a) distinguish what a subagent **actually DID** vs. **RECOMMENDED/INTENDED** — verify, don't conflate; (b) before routing an item into another agent's domain doc, check whether it's already in that agent's memory as their own owed self-note. Both real; propose wording on Justin's go.
**ACU's own — STILL LIVE:**
8. **Deliver the two notes Justin asked for** (see the appendage at the top): the **AC0 QC-sweep note** and the **report-is-annotated note**.
9. **Three stale lane briefs flagged for AC0's #lock** (not ACU's to sweep): `2026.06.30-ac0-ac2-cost-lane-brief.md`, `2026.06.30-ac0-ac3-identity-ops-brief.md`, `2026.07.07-ac3-dc-54b-forward-persistence-build-brief.md` — their work has largely shipped.
10. **Team current-state standup** — scheduled into the week plan as a **Justin+Lucas** block. **NOT ACU's to do.**

---

## 🔑 HOW ACU SEES THE LANES WITHOUT GOING BLIND — the two state reports (Justin's design, 2026-07-16; memorialized at his explicit request so he never has to re-explain this)

**The problem this solves.** ACU organizes the work but was **flying blind** — it had never read ARCHITECTURE, BUILD, the sprints, or the runtime. Justin's correction: *"you are organizing the work, and you're flying blind."* But those docs are **gigantic** — loading them directly would consume ACU's entire context set and leave nothing for the actual job.

**Justin's solution — the 80/20:** ACU dispatches **properly-loaded AC subagents, one per project**, which read everything and write ACU a **persistent report**. Future ACUs read the *report*, not the corpus. *"If you ask the subagents the right questions, that's a really good 80/20 on something that would literally take your whole context set otherwise."*

**✅ STATUS (Thu 2026-07-16 evening): BOTH LANDED, committed, and vetted by ACU.**
- **Product report — SOUND, trust it.** Full two-halves load (39 `src/` + all 11 identity files). Headline: **③ CANNOT MERGE** — `record_pass` is deleted on the branch while `triage.md` still tells Evryn to call it every pass ⇒ **the Mira identity pass is the blocker, not polish.**
- **Team-runtime report — SOUND ON THE HARNESS, HALF-LOADED ON BEHAVIOR. Read its 🔴 SCOPE CAVEAT before trusting any risk claim.** ACU's brief gave it only `evryn-team-runtime/src/` and omitted the identity half (agent definitions + memory + team manual + composed skills). Its code/DB/git findings stand; its *"what could bite when autonomy flips on"* section maps **the cage, not the animal.** Headline: the runtime is **live**, one unprotected boolean from autonomy, and **the money governor is inert** (`$10/hr` ceiling vs ~`$1`/wake ⇒ raise it before the Meta-Meeting).

**The two reports (in `_evryn-meta/docs/working/`):**
- **`2026.07.16-acu-product-state-report.md`** — the Evryn product. Reporter loaded: AC's full cascade + CHANGELOG + deploy-log + ARCHITECTURE + all 4 build/sprint docs + operator-guide + schema-reference + **all 39 `evryn-backend/src/` files** + **all 11 identity files** + both lane briefs (~22K lines). No override.
- **`2026.07.16-acu-team-runtime-state-report.md`** — the autonomous team runtime. Reporter loaded: AC light cascade + ADR-050/052 + `evryn-team-runtime` ARCHITECTURE/BUILD + **all 34 of its `src/` files** + the 5 spike results + the 7 post-7/9 design docs + ACf's live handoff + ratified `runtime-ops.md` (~9K lines). **Uses `#cascade-override`** (Justin cleared it) — the *product* cascade is the wrong system for this target.

**The design that makes them re-samplable — DON'T lose this.** Each report is **two layers**: **Layer 1 DURABLE** (the mental model — good for weeks, expensive to regenerate) and **Layer 2 LIVE STATE** (stamped, changes daily), where Layer 2 **ends with a "How to re-sample this layer" section** naming the exact files + questions. **⇒ A future ACU never re-runs the 22K-line read — it spins a cheap refresh agent against Layer 2's re-sample instructions only.**

**The mechanic for spinning these (the part that cost a round-trip — don't repeat it):**
- Spin as **AC subagents** (`general-purpose` type, **model-pinned** — Opus while Fable is bricked), **FOREGROUND** (see next bullet), both at once. ~2 is safe; ≈8 full-runtime agents trips a server-side rate limit.
- **⚠️ FOREGROUND, NOT BACKGROUND — this reverses what ACU did on 07-16.** Justin: *"Background agents don't render **at all** on my screen — foreground agents render **everything**."* ACU ran both reporters in background, so **Justin never saw a line of either.** He explicitly wants the audit trail (*"I kind of like being able to audit everything — I just need to know when I'm looking at DC's stuff vs AC2 vs AC0"*). **Foreground does NOT cost concurrency** — two children in ONE message block run genuinely concurrently (proven). It only means the block returns when the slowest finishes and you can't interleave a new spawn mid-batch. **Use background only if you must keep working while a long child runs — and say so out loud, because otherwise he can't know the work happened.**
- **✅ The AC variant is now CODIFIED in the protocol** (`ac-orchestration-protocol.md` → "Spinning an AC subagent"). **Read it there — don't re-derive.** The two pre-approved substitutions (everything else pastes verbatim):
  1. `<identity>` → *"You are AC (Architect Claude), designated **[ACU | AC0 | AC2 | …]**. The CLAUDE.md that auto-loaded into your context IS your operating manual (`_evryn-meta/CLAUDE.md`) — it is yours, and you are AC. Do not assume that auto-load was complete: read it in full yourself, confirm its bottom truncation canary, and follow it faithfully first, before anything else."*
  2. `<mandatory_load>` opener → name the tier (**`Full`** or `Light`) **Startup Context Cascade** (both occurrences) + *"is NOT AC"*. *(AC's CLAUDE.md has Light/Full tiers, not a bare "Startup Context Cascade" section.)* **Building or evaluating a build ⇒ FULL, non-negotiable. Then resolve the tier to actual files with line spans — the tier is a frame; the list is the load.**
- **Ask for what's meaningful to ACU** (Justin's steer), not a generic summary. The 7 that worked: what's *actually live* vs merged vs built · the ordered critical path · **where docs disagree with runtime** (what ACU can't trust) · lane state · **what's gated on JUSTIN vs agent-autonomous** (ACU's core job) · untracked risks · **what's parallelizable/unattended-safe**.
- Tell them: **you are producing a MAP, not a plan** — never re-issue AC0's/ACf's lane work (the role boundary), and cap headings at `##`.

**🔴 THE MISTAKE ACU MADE — DO NOT REPEAT IT. Every agentic runtime has TWO halves, and your load list must carry both.** *(Justin generalized this 2026-07-16: identity-as-runtime holds for **any** agentic system, not just Evryn.)*
- **Evryn product:** code = `evryn-backend/src/**/*.ts` · identity = **`evryn-backend/identity/**/*.md`** (11 files — enumerate live, don't trust the count).
- **Team runtime:** code = `evryn-team-runtime/src/**/*.ts` · identity = **the agent definitions (`evryn-team-workspace/.claude/agents/*.md`) + memory files + team manual + composed skills** — i.e. whatever `src/composer/layers.ts` assembles into a wake. **Read the composer; don't trust this line.**
- **What ACU did wrong:** used `#cascade-override` for the team-runtime reporter (correct — the *product* cascade is the wrong system) but listed only that repo's `src/`. **An override may swap WHICH system you load; it may never leave you holding HALF of one.** The result: a report whose behavioral risk register maps the cage, not the animal.
- **⚠️ The trap that makes this silent:** `find src -name "*.ts"` **cannot** return an identity file. So *every* "enumerate the runtime live" recipe omits the identity half unless you enumerate it separately. Feels like rigor while it happens.
- **Full rule + the two-halves table:** `docs/protocols/ac-orchestration-protocol.md` → "When the target is a SEPARATE codebase."

**⏱ AND: a live-state finding decays in HOURS, not days.** The team-runtime report's *"ACf1 has not started building"* was AC-verified true at 12:30 and **false by 14:51** (7 commits by 17:07). **Re-sample Layer 2 before acting on it; never quote it cold.**

---

## Banked assets (ACU's research — the Friday manifest's inputs)

All in `_evryn-meta/docs/working/`, all committed:

1. **`2026.07.13-acu-acu-v03-blitz-preread.md`** — v0.3 scope, decided-vs-open, work-packages sized/sequenced, 8 recommend-first kickoff decisions. Recommends an **outreach-first slice**. ⚠ Carries a correction block: the **Fenwick outreach-posture question is RESOLVED** (Fenwick already cleared on-Mark's-behalf replies) — do not re-open it.
2. **`2026.07.13-acu-acu-outreach-engine-research.md`** — recommends **tiered composition on the existing approval seam** (blended cost ~$1.70 → under $0.30/contact). ⚠ Two hard facts: the **batch-approval model is the real design decision** (200 drafts/day can't be per-draft approved), and the **connection-record/event schema split is a hard prerequisite**.
3. **`2026.07.13-acu-ac0-step53-identity-loading-research.md`** — reframe: on-demand identity loading is **already effectively retired** in the runtime; Step 53 is really "revive selective loading, or formalize force-load." Recommends harness-first + verified force-load; the rest is measurement-gated on Step 69 (needs real traffic).
4. **`2026.07.13-acu-ac0-step47-maintenance-plan-scoping.md`** — the *emergency* observability layer is strong; the gaps are steady-state (deviation-detection, judgment-quality loop, cadence). Only buildable-now piece = **Step 78**.
5. **`2026.07.13-acu-ac0-anthropic-key-split-runbook.md`** — smaller than framed: the team runtime never used an API key; it's one prod-workspace key + one Railway var. ~10–15 min of Justin.

**Added 2026-07-16 — the day's real artifacts (all committed):**
6. **`2026.07.16-acu-product-state-report.md`** + **`2026.07.16-acu-team-runtime-state-report.md`** — ACU's two maps. See the 🔑 section above for how to use and re-sample them.
7. **`2026.07.16-acu-justin-blitz-manifest.md`** — the attention-routing plan. Parked behind the AC2 test (Justin's call), not rejected.
8. **`2026.07.16-acu-ac0-product-radar-findings.md`** — 5 product findings routed to AC0 as **inputs, not a work order** (Justin's routing call: *"he'll decide how to deal with these"*). Headlines: **R1** — an untracked halt-resume bug that creates a phantom lead + triages an empty profile, in **no doc**, sequenced AFTER ③ (same file); **`schema-reference.md` is untrustworthy** (documents a dropped table as live); 2 unattended traps. **ACU does not own these — do not chase them.**

Also: **the cost model is stale (v0.2-only)** — must be refreshed before v0.3 scope locks (flagged in the pre-read).

---

## Tools & mechanics

- **Slack ping:** Node `fetch` → `SLACK_TEAM_WEBHOOK_URL` from `evryn-team-workspace/.env`. Slack reaches **Justin only** — there is no agent-to-agent delivery. To reach AC0/ACf, hand Justin a paste-line.
- **ACf monitor:** ACU keeps a resumable subagent that diffs ACf's handoff doc (`evryn-team-workspace/shared/projects/ops/team-runtime/sessions/2026.07.11-acf-acf-handoff.md`). **Monitors are session-bound — a fresh ACU must spin a NEW one** (any agentId in an old brief is dead). Brief it with the last-known baseline and ask for deltas only. It feeds **your** map — surface only the cross-cutting slice.
- **AC-as-runtime-agent: SORTED, no ACU action.** Justin + ACf decided AC becomes a runtime agent — a **top-level worker under Soren**, not a main. It's in the plan and Soren's memory. Don't re-open it.
- **Ownership:** AC0 owns his lanes (AC2 cost/Step 57, AC3 identity/ops, AC7 doc-process) **and all promotions/patterns arising from them**. ACf runs his own show. Lucas is Justin-spun. ACU tells Justin **when** each needs him — that's the job.

## Radar (Justin's, not ACU's to drive)

- **Fenwick $64,718.50 due 2026-08-14.** Nathan sent a capped-flat-fee proposal 07-08; awaiting Fenwick's scope+pitch. Emma's financial-model consolidation needs a Cowork session with Justin.
- **Mark is radio-silent, not written off.** A **second gatekeeper (Lacey Leavitt)** is warm via Megan and could arrive first. Build-side we're ready whenever *a* gatekeeper forwards.

## Committed (AC0 pushes; ACU never pushes)

**⚠️ `_evryn-meta` is ~10+ commits AHEAD of origin. ACU does NOT push — AC0 does. And pushing `_evryn-meta` IS a live Vercel dashboard deploy**, so that backlog is not a "just push it" situation — route it to AC0/Justin.

**Earlier:** `f804606` recommend-first rule + week plan + batch-1 research · `a944235` Fable rubric + batch-2 research · `6679ddb` night set-down · `d471e3d` Wed recalibration · `ef49096` ACf sequencing note → historical · `db98040` Wed-eve re-jigger · `d1bff43` Fable 80% · `ad25066` compaction-edge persist · `6e3c9ae` meter fold + the two-report mechanic.

**Thu 2026-07-16 (the protocol day) — across FOUR repos:**
- `_evryn-meta`: `334d785` team-runtime report · `b8ec50d` product report · `8841d26` AC0 radar doc + blitz manifest · `6c185f6` **another agent's** translator-discipline CLAUDE.md change (committed on Justin's instruction so ACU's edit wouldn't sweep it) · `a02b260` protocol: the AC-under-AC model · `04addbc` foreground default + receipts-to-Justin + identity gap · `1bf9a67` generalize identity-as-runtime to ANY agentic system.
- `evryn-quality`: `ac38324` QC — enumerate BOTH runtime halves *(amend note: the original message was corrupted by backticks executing as shell commands — **use `git commit -F -` with a quoted heredoc when a message contains backticks**)*.
- `evryn-dev-workspace`: `de6c0b9` DC as the identity-omission backstop · `00029c9` generalized to any agentic system.
- `evryn-team-workspace`: `ba239f7` + the Soren generalization *(⚠️ Soren and ACf are both live in this repo — **always re-read + stage by explicit path**)*.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
