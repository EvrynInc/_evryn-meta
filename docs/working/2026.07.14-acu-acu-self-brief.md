# ACU Self-Brief — working doc (2026-07-14 evening)

> **Truncation check:** last line should read `FULL FILE LOADED`.
>
> **What this is:** ACU's own continuity note so a re-spun ACU (or a post-compaction me) can pick up cold. Working doc, not source-of-truth. The **master plan lives in `_evryn-meta/docs/working/2026.07.13-acu-uber-week-plan.md`** — read that in FULL first; this is just the pointer + live-state layer.

*Written 2026-07-14T19:10 PT (Justin closing down for the night; AC0 pushes all repos after).*

## Who ACU is
Meta-layer above AC0/ACf — holds the cross-lane map, does NOT reach into lanes (the "don't reach into other chefs' dishes" rule — see week-plan §ACU role boundary). Surfaces to Justin ONLY: his own critical path, cross-track dependencies, and ACU's own-lane work. Zones any lane reference under an `ACf` / `AC0 → AC2/AC3` heading. **Pings Justin on `#team-alerts` EVERY response, no exceptions** (Node `fetch` to `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`; never bash+curl/PowerShell).

**Standing auth (Justin, 2026-07-15):** commit the week-plan (and ACU's own working docs) freely when changed — no pre-vet needed; just tell Justin what changed in chat. Do NOT push (AC0 pushes); do NOT commit other agents' files. The team current-state is Lucas's — never touch it; schedule the standup instead.

## ⚡ LATEST STATE — Thu 2026-07-16 (written at compaction edge; trust THIS over anything below it)

**POST-COMPACTION: you are soft. RE-READ before acting** — (1) `docs/working/2026.07.13-acu-uber-week-plan.md` IN FULL, (2) this whole file, (3) if directing build: `evryn-backend/docs/ARCHITECTURE.md` + `SPRINT-V0.2-HARDENING.md`. Justin's standing instruction: don't run forward on a softened picture; re-read what you read before.

**THE BIG FINDING (today, researched + verified):** Justin's usage meters showed All-models 4% / Fable 0% with a 23h reset — **NOT a glitch.** Anthropic did a **goodwill reset** of 5-hr + weekly limits for all users (~Jul 15; established pattern, follows outages). Load-bearing facts:
- **Fable shares the ONE weekly pool** — it's a **50% sub-cap** of it, NOT a separate pool, NO separate clock. (Answers Justin's "can Fable be on a different clock?" — no.)
- **Goodwill resets do NOT move the scheduled reset** (Anthropic doc) → full allowance NOW **and** another at the Friday reset. Today's refill is use-it-or-lose-it.
- **TWO PROMOS EXPIRE SUN 2026-07-19 11:59pm PT — the REAL cliff, not Friday:** (a) Claude Code weekly limits **+50%** (5-hr unaffected); (b) **Fable FREE** up to 50% of weekly. After Jul 19: standard limits, Fable reverts to paid credits. **The whole Fri–Sun blitz sits inside the promo.** Next week = old scarcity returns (real planning cliff).
- **⇒ Fable rationing is OFF through Sunday.** The per-turn husbanding rubric + Thursday ~10% reserve in the week plan are **MOOT**. The **5-hr window is now the true governor** → spread across windows, run overnight; don't pack one.
- **⚠ ONE OPEN JUSTIN ACTION (only real money exposure):** verify the **extra-usage / usage-credits toggle + monthly spend cap** in Settings. Off = hitting cap just blocks (costs time, not money). On = overflow **silently bills at API rates**; **Fable overflow past its 50% sub-cap = $10/M in, $50/M out** — expensive at our agent volume. Unconfirmed whether he did it.
- *Epistemic honesty:* the specific reset announcement is inferred (3 agreeing signals; X blocked fetch). The **mechanism** is from Anthropic's own docs, so the conclusion holds regardless.

**Wave state Thu AM (see week-plan RE-JIGGER for full):** (P) product **AHEAD** — Step 57 built + independently reviewed, **zero blockers**; needs live rehearsal (Gate-A) → gates → merge → deploy; 54a/54c merged; AC7 doc-process landed; still `v0.2.7` dormant. (T) team **BEHIND** — Lucas **4/8 paces**; ACf pivoted to a **Lucas-memory fix** (right call): Phase-1 build running (ACf1, Opus-only); **Meta-Meeting OFF Thursday** → Fri PM/weekend. **Phase 2 (ACf2) → Friday blitz** (leftover-then-fresh Fable; ACf2 *main* on Opus, Fable only for design-synthesis turns) — Justin relayed this to ACf.

**Open loop:** Justin asked "want me to fold the meter finding into the week plan and commit?" — ACU offered; **not yet done.** Do it on his yes (standing auth covers the commit).

## Reload manifest (what to load)
1. This file.
2. **`docs/working/2026.07.13-acu-uber-week-plan.md`** — FULL. The master plan (scope, targets, model policy, ladder, contingencies).
3. Light cascade: `CLAUDE.md` (auto) + `docs/current-state.md` + `docs/hub/roadmap.md`.
4. This session (2026-07-14) I also read the **full heavy cascade** first-hand — `evryn-backend/docs/ARCHITECTURE.md` (1242 ln), `BUILD-EVRYN-v0.3.md`, `BUILD-FUTURE.md`, `SPRINT-V0.2-HARDENING.md`, `SPRINT-V0.3-CANDIDATES.md`. A fresh ACU *directing build* should re-load ARCH + the active sprint; ACU-as-orchestrator can lean on the week-plan's synthesis.

## Live state (2026-07-14 eve)
- **Scope:** v0.3 OFF this week (soft bonus if room over the weekend). Two targets: **(T) team autonomy ON**, **(P) Mark-ready v0.2** = Step 57 shipped + Gate-A + Gate-B staging live-fire + 54a/54c + flips staged.
- **Contingencies (locked):** Step 57 goes IN the live-fire (no double-run); AC0 guarantees 57 by Friday. **Mark-arrival override:** the instant Mark emails → ~2-day get-stable-and-tested sprint; Mark doesn't come in until Gate-B passes.
- **Calendar:** Tue = ACf paces (Justin foreground) + kick AC0 + trigger Soren consolidation. Wed = blue-skies (Emma/Dominic/Thea/Nathan) + Soren consolidation lands + Step-57 design-align. Thu = **Meta-Meeting** + Step-57 build/QC/Gate-A. Fri = **Gate-B live-fire** (Justin's `SEND_ENABLED` flip) + autonomy flips ON + blitz begins. Sat/Sun = Mark-ready burn-down + re-auth sweep + team-runtime Phase-2.
- **Ladder (interrupts):** ACf > Lucas > AC0-true-blockers > ACU. Scheduled blocks: AC0 sittings · Soren consolidation (by Wed) · blue-skies · Meta-Meeting (Thu) · Gate-B (Fri).
- **Model policy:** per-turn Fable escalation (Opus default, flag `⚡ FABLE TURN NEXT` for the 5 rubric cases). Fable: 71% Mon eve → ~flat Tue → **80% Wed 5:18pm** (~20% to Fri 9:59am reset; comfortable since the big spends — Meta-Meeting conduct, Phase-2 design — slid to Fri+). Justin pastes the Fable % each morning. **Current calendar = the week-plan's RE-JIGGER block (Wed 5:18pm): Meta-Meeting OFF Thu; team autonomy slides to weekend/next-week; product likely completes this week; Phase-2 Fri on leftover-then-fresh Fable.**

## ACU's own open loops
- **Friday Blitz Manifest** — ACU assembles Thursday (from Meta-Meeting top-sheets + re-auth sweep + Mark-ready remainder). Hand Justin a Thursday-night pre-clear ballot. This is ACU's next real Fable moment.
- **ACf-doc monitor** — resumable subagent, `agentId a1a38a65fadb0c479`, watches `evryn-team-workspace/shared/projects/ops/team-runtime/sessions/2026.07.11-acf-acf-handoff.md`. Baseline advanced to HEAD `d30010f` (274 ln); next resume diffs from there. **Note:** resumable only within THIS session — a fresh ACU must spin a new monitor + re-baseline.
- **AC0 relay** — Justin is hand-relaying the scope-change + Fable-lanes + rhythm pitch to AC0 himself.
- **Team current-state trim** — deferred to a proper Lucas standup (it's cross-agent synthesis, not a quick trim). Justin waved off doing it tonight.

## Committed this session (pushed by AC0 tonight)
- `_evryn-meta/CLAUDE.md` — the recommend-first question rule (soft-open "Strongly consider…", Justin-worded) — committed earlier (`f804606`).
- `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` — AC relay header = `#` (H1); subagents/briefs capped at `##` (H1 reserved for AC's voice). **Committed this batch.**
- `_evryn-meta/docs/working/2026.07.13-acu-uber-week-plan.md` — the master plan (scope reset, ladder v2, contingencies, model rubric). **Committed this batch.**
- This self-brief. **Committed this batch.**
- NOT mine, leave alone: `docs/working/2026.07.14-ac2-step57-dc-build-review.md` (AC2's).

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
