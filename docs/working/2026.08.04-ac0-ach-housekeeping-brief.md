# ACh — The Housekeeping Lane (three queues) — brief

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file:** This is the **shared, durable surface** for the housekeeping lane — the one place its real state lives. ACh appends its findings and open questions here; AC0 appends his own. It exists because a conversation is a single point of failure for two agents at once and a committed file is not: **if ACh is re-spun, a fresh instance must be able to pick the lane up from this file alone.** At every append, apply that test.
>
> **What does NOT belong here:** the recommendation output itself. That goes in a separate deliverable doc (see §5). This file is the lane's *operating* record — decisions, reasoning, open questions, what was tried and rejected.
>
> **Owner:** AC0 created it; ACh is the lane. Commit freely — brief-class traffic is pre-authorized.

*Created 2026-08-04T14:59:15-07:00 by AC0 (AC0-29).*

---

## §0 — WHO YOU ARE, AND WHY THIS LANE EXISTS

You are **ACh** — a lane AC running under **AC0** (the head AC, who is in direct contact with Justin). Your lane has exactly one job across three queues: **look at accumulated cruft, judge what is safe to retire, and RECOMMEND.**

**🔴 YOU DO NOT EXECUTE. This is the single most important line in this brief.** You move nothing, delete nothing, reap no branch, retire no doc, commit no cleanup. **Justin vets every item before anything is touched.** Your deliverable is a list with a reason per item, plus an explicit edge-case list of what you could not confidently classify.

**Why the recommend-only shape (so you don't "improve" it):** the judgment calls here are irreversible in the ways that matter — a retired doc that was actually live, a reaped branch that held unlanded work. The cost of recommending wrongly is a corrected line in a list. The cost of *acting* wrongly is lost work. Those are not close, so the lane is deliberately shaped to make the cheap mistake possible and the expensive one impossible.

**Why you are FULLY loaded** (and why that is not negotiable): Queue 1 asks you to judge each doc *against the runtime and the changelogs* — *"is what this describes shipped, superseded, or still in flight?"* **That is a claim about how the system actually behaves**, and a doc's title and date routinely lie about it. Justin said this twice, the second time as: *"it needs a smart, properly-loaded AC — not a thin subagent — otherwise it's going to fuck it up."* You are carrying the full product runtime for exactly that reason. Use it.

---

## §1 — THE EDGE-CASE LIST IS AS VALUABLE AS THE MAIN LIST

Read this before you start, because it inverts the instinct you will otherwise have.

You will feel pressure to classify everything — to produce a clean, complete, confident list. **Resist it.** The items you *cannot* confidently classify are precisely where a wrong call would cost something real, and surfacing them honestly is worth more to us than a tidy list that quietly guesses on three of them.

So: when you are genuinely unsure, **put it in the edge-case list with what you know and what you'd need to resolve it.** An honest "I couldn't tell, and here's specifically why" is a *success*, not a gap in your work. A confident wrong classification is the failure.

---

## §2 — QUEUE 1: the working-doc cull

**The problem:** `_evryn-meta/docs/working/` has accreted to **31 files** spanning 2026-06-30 → 2026-08-04. Some are live lane briefs in daily use. Some describe work that shipped weeks ago. Nobody can tell which is which at a glance, and every AC that goes looking pays that cost.

**Your judgment call, per file:** is what this doc describes **shipped**, **superseded**, or **still in flight**?

**How to judge it — and this is the whole method:** a doc's title and date lie. The changelogs and `src/` do not. So for each doc, ask what it *claims*, then go check that claim against the runtime you have loaded and against the two changelogs. A brief describing a fix that is now visibly in `src/` and recorded in `evryn-backend/CHANGELOG.md` is settled history. A brief describing work you cannot find any trace of is either in flight or abandoned — and telling those two apart is exactly the kind of thing that belongs in the edge-case list if you can't.

**Destination for anything you recommend retiring:** `_evryn-meta/docs/sessions/historical/<YYYY.MM>/` — foldered by the doc's own date. Folders currently exist for `2026.03` through `2026.07`; a `2026.08` folder does not exist yet and would need creating.

**Why retiring is safe (say this in your reasons rather than hedging around it):** provenance is already preserved in git history and in the per-repo changelogs. Retiring a doc moves it; it destroys nothing. That is what makes the whole exercise low-risk — and stating it plainly in your reasons is more useful than a nervous qualifier.

### 🔴 Known traps — these are handed to you so they don't have to be rediscovered

- **PERMANENT, never retire:** the ADRs (`_evryn-meta/docs/decisions/` — not in `working/`, but don't get clever about them), and the ③ design doc, which was *deliberately relocated OUT of `/working`* to `evryn-team-workspace/shared/projects/product/v0.2/` precisely because a permanent doc doesn't belong in a transient folder.
- **Actively load-bearing RIGHT NOW — do not retire, no matter how they look:**
  - `2026.08.04-ac0-ac5-silent-email-drop-brief.md` — **AC5's live lane, mid-flight as you read this.**
  - `2026.08.04-ac0-aca-atlas-refactor-brief.md` — ACa's live lane.
  - `2026.07.22-ac0-ac2-fast-follow-brief.md` — AC2's live lane.
  - `2026.07.21-ac0-ac3-cleanup-batch-brief.md` — AC3's lane; briefed, not yet spun. **Briefed-but-unspun is still live**, not abandoned.
  - `2026.08.04-ac0-ac0-handoff.md` — AC0's current handoff.
  - `2026.07.28-acp-ac0-desktop-permissions-fix.md` — an unexecuted runbook for a machine fix that is still outstanding on the desktop.
  - `2026.08.04-ac0-ach-housekeeping-brief.md` — this file.
- **⚠️ `2026.07.22-ac0-ac0-handoff.md` is NOT purely retirable.** It is superseded as a handoff, but **two things inside it are still live** and have not been rehomed: **Soren's unsettled v0.3 design batch** (bulk outreach, the Publisher, fill-laundering security) and **Nathan's legal constraint** (backlog outreach is only defensible as a *response on behalf of* the gatekeeper the person originally wrote to — which makes it an eligibility rule: no inbound record → no on-behalf-of send). **Recommend rehoming those two first, and name where you think each should go.** Do not recommend retiring the doc without that.

---

## §3 — QUEUE 2: stale branches on `evryn-backend`

**The problem:** merged branches were never reaped, so every branch listing is noise.

**⚠️ Two prior counts disagreed** (AC2 reported ~24; AC0 counted 18 remote merged). **Measure it yourself rather than trusting either number** — the gap is probably branches that are dead but *not merged into main specifically*, which is a different and more careful call.

**AC0's measurement as of 2026-08-04 (verify it; don't inherit it):**

- **18 remote branches merged into `origin/main`** — the reap candidates: `bundle/go-live-v0.2`, `dc/clustering-a-c`, `dc/cost-haiku-r2`, `dc/cost-pass-stamp`, `dc/m1-emergency-channel`, `dc/morning-sweep-c2`, `dc/quiet-hours-redesign`, `dc4/qc-fixes`, `mira/clustering-note-pathway-2026-07-07`, `mira/phase6-identity-batch`, `r2/convergence-bundle`, `r2/lane-a-ingest-resilience`, `r2/lane-a-register-tool`, `r2/lane-a-send-allowlist`, `r2/lane-a-step21-durable-cursor`, `r2/lane-b-operator-approval`, `r2/lane-c-cost`, `r2/m1-silent-death`.
- **6 remote branches NOT merged into `origin/main`** — each needs its **own look and its own reason**: `ac2/step57-runtime-bookkeeping`, `dc/cost-haiku-prescreen`, `lane-a/ingest-resilience`, `lane-b/operator-approval`, `lane-c/cost`, `m1/silent-death-stage2`.
  - **A hypothesis worth testing, not assuming:** the four `lane-*` / `m1/*` names look like older-generation siblings of the `r2/lane-*` branches that *did* merge, and `dc/cost-haiku-prescreen` looks superseded by `dc/cost-haiku-r2` (the Haiku pre-screen shipped as `v0.2.6` — check the changelog). **If that holds, they are dead-but-unmerged, which is a different recommendation than "merged, therefore safe."** Verify each; don't pattern-match the names.

**The distinction that governs this queue:**
- **Merged into `main` → safe to reap.** The content is on `main`; reaping removes real noise.
- **Dead but NOT merged → do NOT reap on that reasoning.** Those may hold work that never landed. Each needs its own look and its own stated reason.

### 🔴 Branches you must LEAVE ALONE — do not recommend reaping any of these

- **`ac2/step57-runtime-bookkeeping`** — the local copy is **22 commits ahead of its origin copy (and 6 behind)**. Its content is already on `main` and deployed, but the divergence is **AC2's to reconcile. Do not recommend force-pushing or force-deleting it.**
- **`ac5/ingest-fetch-resilience`** — a **live lane's active branch**, 3 commits ahead of `main`, with a worktree at `evryn-backend-ingest`. Mid-flight.
- **`dc/step35b-record-legibility`** — a **live lane's active branch**, 1 commit ahead of `main`, worktree at `evryn-backend-35b`.
- **`mira/step35d-trust-loaded-record`** — a **live lane's branch**, worktree at `evryn-backend-mira35d`. Currently level with `main` (no commits yet) — **that means "not started," NOT "abandoned."**

### 🔴 The standing discipline that applies here

**A `git branch -d` that FAILS on origin-divergence is a SIGNAL, not a nuisance. Surface it; never recommend `-D` past it.** A branch can be merged into your HEAD while its upstream still carries unmerged commits — which is exactly the `ac2/step57` situation above. If you hit one, that is information, not an obstacle.

---

## §4 — QUEUE 3: the four held `tests/_gate-a-*.ts` files — a DECISION, not a cleanup

Four files sit **untracked** (not gitignored) in `evryn-backend/tests/`: `_gate-a-review.ts`, `_gate-a-run.ts`, `_gate-a-seed.ts`, `_gate-a-times.ts`.

**What AC0 established, so you start from facts rather than re-deriving them:**

- All four **self-label "EPHEMERAL … (delete after use)"** in their own first line. The `_` prefix is this repo's ephemeral convention: `_`-prefix means ephemeral means delete-at-close. **They were never meant to be committed.**
- **Two are one-shot forensics** — `_review` and `_times` are read-only database queries written for a specific past review.
- **Two are a real harness** — `_seed` seeds a fictional gatekeeper ("Megan") plus her answer-key criteria; `_run` puts the committed fixture suite through the live pipeline and prints a grading table.
- **Why we keep tripping over them:** nobody is holding the *files* — the hold is on the **DEV DATABASE STATE they read** (the Megan gatekeeper record, a frozen in-flight row), which an outstanding verification still needs. The scripts rode along, the delete step never fired, and each handoff dutifully re-flagged "don't touch" without revisiting why.

**🔴 The finding that makes this a decision:** `tests/fixtures/emails/*.txt` and the Megan spec **ARE committed**, and `BUILD-EVRYN-MVP.md` explicitly calls the synthetic fixtures + fictional gatekeeper a **"permanent regression suite."** So **the test DATA is permanent and committed, while the SEEDER and RUNNER for it are uncommitted scratch files everyone has been told not to touch and which are slated for deletion.** That is a gap in what our regression suite actually *is* — not untidiness.

**Your job here:** think this through with the runtime loaded and **make a recommendation with your reasoning.** AC0's own read is below — **treat it as an input to argue with, not a conclusion to ratify.** If you disagree, say so and say why; that is more useful than agreement.

> **AC0's read (argue with it):** `_review` + `_times` → delete when the DEV hold lifts; genuinely one-shot. `_seed` + `_run` → **promote into the committed permanent suite** — but not by copy-paste: they hard-code a UUID, they write to a live database, and they would need the ephemeral naming stripped, a proper home, and the DEV-only guard kept intact.

**⏳ Timing constraint you must honor in the recommendation:** nothing happens to any of the four until an outstanding verification releases the DEV hold. Your recommendation should say what to do *and* name that gate.

---

## §5 — YOUR DELIVERABLE

Write **one new document**: `_evryn-meta/docs/working/2026.08.04-ach-justin-housekeeping-recommendations.md`.

It carries, per queue:

1. **The main list** — every item, with a **one-line reason** each. The reason must be *specific and checkable* ("shipped as `v0.2.7`, recorded in `evryn-backend/CHANGELOG.md` 2026-07-10"), never generic ("looks old"). A reason a reader cannot verify is not a reason.
2. **The edge-case list** — everything you could not confidently classify, with what you know, what you'd need to resolve it, and what you'd guess if forced. **Per §1 this is not the leftovers; it is half the value.**
3. **Anything you found that ISN'T in the three queues but a fully-loaded reader would want flagged.** You are the first fully-loaded pair of eyes on this pile in a while — if something looks wrong, say so, clearly marked as outside your remit.

**Write it for Justin.** He is not an engineer, he did not read any of these docs, and he will vet your list to decide what actually moves. So: **concept first, then jargon; never a filename or a step number as the only identifier.** *"Step 92"* and *"the schema-reference doc"* mean nothing to him cold — *"the file documenting what our database actually looks like, which is badly out of date"* does. Follow `ac-writing-protocol.md` — it is on your load list and it is not optional for a doc someone will act on.

**Structure it so he can sign off item-by-item:** number the items (sub-items get letters), and shape each as a yes/no or a choice between named options wherever you can, so he can answer *"1. yes, 2.b, 3. no"* without writing prose. Some items genuinely can't be flattened that way — that's fine; do the work where you can.

**Then append a dated entry to §7 of THIS brief** recording what you did, what you decided and why, and what's open.

---

## §6 — HOW YOU WORK

- **🔴 Read-only, with exactly two exceptions:** you create your deliverable (§5) and you append to §7 of this brief. **You touch nothing else** — no moves, no deletes, no branch operations, no edits to any other file. Not as a first pass, not "obvious" ones, not at all.
- **You never merge, never push, never deploy, never run a prod migration.** You are a sub-AC; those belong to AC0 and Justin, and they sit on the hard floor of the escalation ladder.
- **Commit your own two files by explicit path** (`git add <path>`) — never `git add -A`. **Multiple agents are working in these repos right now**, and a broad add would sweep their uncommitted work into your commit under your message. Before committing a file, `git diff` it and confirm the whole diff is yours.
- **You may spin your own subagents** — and if you do, see the standing clause in `<task>`. Prefer reading things yourself where nuance matters: subagents summarize destructively, and this lane is *entirely* judgment about nuance.
- **Questions go UP to AC0, never to Justin.** You do not ping Justin. If something needs his call, flag it `NEEDS-JUSTIN` with your recommendation and hand it up. **Asymmetric default: when unsure whether it needs him, flag it** — the cost of over-flagging is AC0 spending two seconds; the cost of under-flagging is Justin never learning the question existed.
- **Cap your headings at `##`.** `#` is reserved for the top-level AC's voice to Justin. You may write `## ACh's read — <topic>`.
- **Don't state as fact what you haven't verified.** If you're inferring, say you're inferring. This lane's entire output is claims about what is safe to destroy; an unmarked guess in that list is the worst thing you could hand us.

---

## §7 — RUNNING LOG

*(ACh: append dated entries. Keep this current enough that a fresh instance could pick the lane up from this file alone.)*

### AC0 — 2026-08-04T14:59:15-07:00 — lane opened

Brief written. ACh spun with the FULL product cascade (both runtime halves, live-enumerated) per Justin's twice-stated requirement that this lane be fully loaded. Run in **background** at Justin's instruction so AC0 can keep working — Justin will not see ACh's output render, so AC0 owns verifying it.

**Known gap AC0 is flagging rather than papering over:** 30 of the 44 `src/**/*.ts` files carry **no bottom truncation canary**, so the standard canary check cannot fire for them. ACh has been told to report the exact final line number for those files instead (the workaround AC5 proved this week). This is a real doc-hygiene gap in the repo, separately routed.

### ACh — 2026-08-04T15:14:55-07:00 — all three queues delivered

**Deliverable written:** `_evryn-meta/docs/working/2026.08.04-ach-justin-housekeeping-recommendations.md` (ballot-shaped, for Justin). Load was completed in full — 44 `src` + 11 `identity` files, both changelogs, ARCH/BUILD/SPRINT/optionals, Hub, technical-vision, current-state, deploy-log, and the three protocols. Live-enumerated the runtime myself: **exactly 44 + 11, matching the briefed list** (no omissions).

**Counts:** Queue 1 — 12 retire clean · 2 retire-after-rehoming · 12 keep · 6 edge cases (32 files, not the 31 the brief states). Queue 2 — 23 reap · 1 leave alone. Queue 3 — a recommendation that differs from AC0's on one of four files.

**The load earned its keep — five findings that required the full runtime/doc set:**

1. **Five live docs were NOT on the brief's do-not-touch list** — the Step-47 maintenance-plan scoping doc (awaiting Justin's decisions, cited by current-state), the restructure napkin (live input to the ACa lane, cited by current-state), ACf's current handoff, and the team-memory item-(c) and item-(b) build specs (item c = "the live next build"; item b merged but migration 016 unapplied / UNVERIFIED-LIVE). Retiring any would have pulled something out from under an active lane.
2. **`2026.07.23-acf-ac0-brief.md` CLOSES an item current-state still shows open** — the cross-runtime raw-write-surface check owed on the ③ #lock. ACf verified team-runtime never had the class. Same rehome-then-retire shape as the 7/22 handoff; added as a second conditional.
3. **The Step-78 health design doc is cited BY THE RUNTIME** — `src/safety/liveness.ts` and `src/safety/health.ts` both name its path in comments, as the authority for the deliberately-counter-intuitive halted-beats-wedged rule marked "do NOT fix to 503". Retiring it silently breaks that pointer exactly where someone might "fix" a rule that must not be fixed. Filed as an edge case (lean keep).
4. **The Fenwick outreach ruling lives in TWO docs both otherwise slated for retirement** — the 7/22 handoff and the v0.3 blitz pre-read (whose version is a *correction* recording it as already resolved: a reply on the gatekeeper's behalf is legitimate). Retiring both without rehoming leaves it with no durable home. Rescuing it once covers both.
5. **Queue 3 is sharper than "there's a gap":** no committed file anywhere seeds the Megan gatekeeper (verified by grep across committed `tests/` + `src/`) — that capability exists ONLY in the untracked `_gate-a-seed.ts`. So deleting the four as their headers instruct makes a suite BUILD calls "permanent" literally unrunnable, and it's needed again for Gate-B.

**Where I differ from AC0 (Queue 3):** AC0 recommended promoting BOTH `_seed` and `_run`. I recommend promoting `_seed` (no committed equivalent — the real gap) but **NOT** `_run` — promoting it would commit a second fixture-runner alongside `tests/run-fixtures.ts`, duplicating the parse+drive logic in two places that will drift. `docs/staging-runbook.md` line 126 already identifies the cleaner fix independently: the committed runner only misses the Megan set because it globs `fixtures/emails/` non-recursively — so extend it to take a subdir rather than commit a twin. Timing gate honored: nothing executes until the DEV hold lifts.

**Queue 2 measurement — verified, not inherited:** independently measured 18 merged / 6 not-merged / 24 total — **confirms AC0's count exactly.** The four 2026-06-17 branches ARE the discarded round-1 attempt (deploy-log Rosetta), each with a 1:1 `r2/*` rebuild that merged and shipped `v0.2.2`. ⚠️ **Trap for whoever executes:** `git cherry` marks all their commits `+` ("not on main"), which *looks* like unlanded work — it is the expected signature of a REBUILD, not a cherry-pick. Verified by substance instead (safety modules, reflection module + identity file all live on main), and chased the 6 apparently-missing test files: each has a renamed round-2 equivalent (`test-cache-prefix-stability`→`test-cache-byte-identity`, `test-send-email-di`→`test-send-transport`, `test-safety-guards`/`test-spend-monitor`→`test-breaker-detectors`/`test-breaker-resume`) or tests the `set_item_status` tool Step 57 deliberately removed. **Nothing lost.**

**Outside-remit flags (in the deliverable, §11):** (a) three branches exist ONLY on this machine, never pushed — `ac5/ingest-fetch-resilience` (3 commits, the live silent-drop lane), `dc/step35b-record-legibility`, `mira/step35d-trust-loaded-record` — the exact class CLAUDE.md says a routine push-check cannot see; (b) current-state is stale twice over — main is exactly level with origin (not "5 unpushed"), and `origin/dc/webfetch-timeout` is already gone (confirmed via `git ls-remote`, authoritative).

**Nothing was executed.** Read-only throughout; the only writes were the deliverable and this entry. No NEEDS-JUSTIN blockers — the whole deliverable is his ballot. One question genuinely needs him: **is the ACU lane still live?** (it governs two docs' fate — ballot 4.d).

---

## §8 — 🔴 AC0 → ACh, 2026-08-05: JUSTIN ANSWERED YOUR BALLOT. Execute the cleared items; research the six he sent back to you.

**Read this whole section before touching anything.** Your ballot came back answered. Some items are cleared to execute; several were sent *back* to you because Justin's judgment was that you — armed with the research — are better placed than a fresh instance would be. Verbatim, on why: *"a new AC2 could only do what a subagent armed with that knowledge could do."*

> ### ⚠️ THE POSTURE JUSTIN ASKED FOR, IN HIS OWN WORDS — this governs everything below
> **_"Do encourage humility — I'd **much rather** have to weigh in on a bunch of things than to have things changed that shouldn't have been."_**
>
> **So: when in doubt, come back with a question rather than a completed action.** An over-cautious pass costs him thirty seconds of reading; an over-confident one costs work nobody can see was lost. **Your edge-case list was explicitly called the most valuable part of your last deliverable — keep producing one.** And note the standing lesson this repo just paid for twice: **a mechanical check that comes back clean is not evidence the underlying thing is safe** (see `_evryn-meta/AGENT_PATTERNS.md` → "Gating Other Agents' Work", and the "green indistinguishable from didn't-run" section above it). That applies directly to `git branch --merged`, which you already know misleads on a stale base.

### 8.1 — CLEARED TO EXECUTE (Justin said yes; go)

| Your ballot | Answer | Notes |
|---|---|---|
| **5 — reap the 18 merged branches** | ✅ **YES** | Straightforward. |
| **6 — reap the 5 superseded-rebuild branches** | ✅ **YES** — all five | Your individual verification is what earned this; the mechanical signal alone would not have. |
| **3 — retire the 12 clean documents** | ✅ **YES**, all twelve | To `docs/sessions/historical/<YYYY.MM>/`. ⚠️ **You flagged that a `2026.08` folder doesn't exist — nothing in this batch needs one, but create it if that changes.** |
| **2 — rehome-then-retire the two conditional docs** | ✅ **YES**, at the homes you named | **Plus a new requirement — see 8.2. Do not do this one without reading 8.2 first.** |

⚠️ **Verify before each reap, don't trust this list:** it is a day old and `main` has moved several times since you measured. **Re-confirm each branch's merged status at execution time**, and use `git branch -d` (lowercase) so a refusal-on-divergence stops you rather than being overridden.

### 8.2 — 🔴 BALLOT 2, WITH AN ADDED REQUIREMENT FROM JUSTIN

Rehome-then-retire is approved at your named homes — **Soren's unsettled v0.3 design batch → `evryn-backend/docs/BUILD-EVRYN-v0.3.md`; Nathan's legal eligibility rule → Nathan's memory file, with a pointer from the v0.3 build doc.**

**Justin added this, and it is not optional:** *"put a note in each of their respective memories, so they know where they went. Make it clear it's **you** writing the entry."*

- Memory files live at **`evryn-team-workspace/.claude/agent-memory/<name>/MEMORY.md`** — `soren/` and `nathan/` both exist.
- **Write the note in your own voice, explicitly attributed** — e.g. *"ACh (a housekeeping subagent under AC0), 2026-08-05: your v0.3 design batch was rehomed out of a retiring AC0 handoff into `<path>`. Nothing was changed; it was moved so it wouldn't be lost when that handoff was archived."* **Do not write it as if the agent wrote it themselves** — that is the whole point of Justin's instruction.
- ⚠️ **Soren's and Nathan's memory files are THEIR source-of-truth documents.** Append; do not restructure, do not edit anything already there. If your note doesn't obviously fit the file's existing shape, **stop and ask** rather than reshaping their file to suit it.
- 🔴 **The catch you yourself surfaced, restated because it is the whole reason this item exists:** the legal ruling appears in **two** documents that are both slated for retirement, stated slightly differently, and **the version in the blitz pre-read is a *correction*** recording that Fenwick already resolved it — outreach framed as a reply *on the gatekeeper's behalf* is legitimate. **Rescue the CORRECTED version. If both retire without this, that ruling has no durable home at all** and gets re-litigated from scratch.

### 8.3 — SENT BACK TO YOU (six items Justin wants researched, not guessed)

**a. The diverged branch `ac2/step57-runtime-bookkeeping` — your ballot 7. RESEARCH IT PROPERLY.**
Justin's ruling: *"AC2 is no longer in position to work this out — the 'AC2' who ran this was a session that closed a long time ago."* So the reconciliation is **yours to figure out**, not to route. What's known: its content shipped and is deployed; **local sits 22 ahead / 6 behind its remote twin.** Work out what that divergence actually *is* (superseded rebase? genuinely unlanded work? both?), then **recommend — do not execute.** ⚠️ Apply your own hard-won disambiguation sequence from the patterns file: a line count cannot tell an *older draft* from an *absent capability*, in either direction. **Read a sample of the "unique" lines.**

**b. AC3's identity/ops brief (65KB) — your ballot 4.a. Justin: read it, then decide.**
Your instinct was right and he agreed: it is the same shape as the handoff that turned out to be hiding two live items. **Read it end-to-end hunting for un-rehomed items**, then recommend. If it's clean, say so and retire it.

**c. The health-endpoint design doc — your ballot 4.b. Justin: extract, then fix references.**
Verbatim: *"If there's important info in there, let's extract that (assuming there's all kinds of session noise that's not necessary) to its proper home, and fix references."* **AC0 verified the citation is real:** `evryn-backend/src/safety/liveness.ts:5` names this file by path. So the job is: pull the load-bearing content to a durable home (the ARCHITECTURE §Monitoring section is the obvious candidate — **your call, propose it**), **update that code comment in the same change**, and only then retire. ⚠️ **The rule it documents is deliberately counter-intuitive and marked "do not fix this" — carry the *reasoning*, not just the conclusion, or the next engineer will re-derive it wrongly.**

**d. The two research documents — your ballot 4.e/4.f. Justin: relocate, WITH pointers.**
You argued "retire" was the wrong verb and he agreed — **relocate to `evryn-team-workspace/shared/projects/product/research/`.** But his condition is the important half: *"make sure that there are pointers — otherwise this is just **lost** research."* **A relocation without breadcrumbs is a slower way of losing it.** So each move must leave a pointer wherever a reader would plausibly go looking — the sprint step each supports is the minimum; consider the relevant ADR or build-doc section too. **Breadcrumbing is part of the task, not a follow-up.**

**e. The four held test scripts — your ballot 8. Justin deferred to your judgment over AC0's, with one condition.**
He couldn't adjudicate the technical call and said so plainly: *"I don't understand this well enough… if you think he's right, then cool. But again — I just need to know that if these are part of the flow, that they are properly breadcrumbed so they're not just lost."*
**AC0's position, for the record: I concede — your recommendation is better than mine.** Promoting both would commit a second fixture-runner alongside `run-fixtures.ts`, duplicating the file-parsing and pipeline-driving logic in two places that will drift. **Promote the seeder; extend the existing runner to take a folder argument.** The staging runbook already identifies that same fix independently, which is corroboration rather than coincidence. **So: proceed on your version — and satisfy Justin's condition by breadcrumbing the promoted seeder and the extended runner from wherever Gate-B is actually run** (the staging runbook and SPRINT Step 88B), so nobody arrives at Gate-B unable to find the suite.

**f. ⏳ TIMING — AC0 checked the DEV hold rather than sending Justin to ask. IT IS STILL REAL.**
Queried the DEV database directly, 2026-08-05: **the Megan gatekeeper record exists (1), there are 3 frozen `processing` rows, 26 items, and 58 cluster-test gatekeepers.** The state you were told to protect is intact. **The hold stands** — it exists for AC2's owed nested-relay verification, which is gated to the `CLUSTERING_MODE` flip and has not happened.
> 🔴 **A TENSION AC0 WANTS YOUR EYES ON, because it may invalidate your sequencing.** You recommended: *wait for the hold to lift → build the seeder/runner work → then delete all four files.* But **Gate-B is targeted for Friday 2026-08-07 and NEEDS a runnable suite** — and by your own finding, nothing committed can run it today. The hold lifts at the `CLUSTERING_MODE` flip, which happens *as part of* the Gate-B run-up. **So the window between "hold lifts" and "Gate-B needs the suite" may be near zero.** **Please re-examine whether the seeder/runner work should be built BEFORE the hold lifts** (it can be — building a committed seeder doesn't require wiping the DEV state it seeds) **rather than after.** If you agree, say so and flag it to Justin as a sequencing change; if you think I've misread the dependency, say that instead — **I have not read this runtime and you have.**

### 8.4 — ANSWERED, NO ACTION NEEDED FROM YOU

- **Your ballot 4.d — "is the ACU lane still live?"** Justin: **technically yes.** *"I need to just close him out — but I'll have **him** manage anything downstream of him."* ⇒ **Both ACU-coupled documents STAY** (its self-brief and the 13 July week plan it names as its master plan). **Do not retire either.** Justin owns closing that lane out; anything downstream is ACU's to route, not yours.

### 8.5 — HOW TO RETURN THIS

**Same shape as last time — it worked.** A ballot for Justin at his altitude (he is not an engineer and reads only chat), with a clear split between *what you executed* and *what you are recommending*. **Keep the edge-case list.** Report to AC0; do not ping Justin directly. Read-only on anything not explicitly cleared in 8.1/8.2 — and per the posture note at the top, **an unanswered question handed back is a success, not a failure.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
