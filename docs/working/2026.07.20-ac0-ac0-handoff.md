# Handoff — AC0-23 → AC0-24 — 2026-07-20 — ⭐ THE SPLIT: I finish the ③ merge; YOU take the AC3-under-AC0 experiment + are my merge-takeover fallback

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** START HERE, AC0-24. This is the NEWEST handoff and is authoritative for live state. For the standing depth it doesn't repeat, the prior handoff `docs/working/2026.07.16-ac0-ac0-handoff.md` (+ its 2026-07-17 addendum) is still valid context — read it after §1–§4 here. Do NOT re-derive; this doc routes you.
>
> **Written by AC0-23 while the ③ Mira pass was "nearly back" (Justin). If there is a `## ⚡ MERGE-TAKEOVER APPENDAGE` at the very bottom, AC0-23 ran out mid-merge — read it FIRST; it says exactly where the ③ gate stands.**
>
> **⭐⭐⭐ 2026-07-20 LATEST (AC0-23, closing) — ③ IS MERGED; AC2 IS RUNNING THE DEPLOY → `railway up` → THE FULL #LOCK.** So §1/§2/§4's "AC0-23 finishes ③ / at the Mira gate" framing is now **HISTORY** — the ③ pipeline is done from AC0's side; AC2 owns deploy + #lock, so **current-state / deploy-log / CHANGELOG / SPRINT / ARCH will be AC2-fresh — trust THOSE for product state, not this doc.** **Next AC0 boots POST-③ into: Weekend architecture capture (BEFORE Gate-B) → Gate-B → gatekeeper.** Read `docs/current-state.md` (AC2-updated) for product state; read THIS handoff for the AC0-ORCHESTRATION continuity current-state won't carry: the pending **AC3-under-AC0 experiment** (§3 + AC0-24's refined `/health`+DB-seam load list near the bottom — non-urgent), the **load-tiering framework** (§5 — light-default / full-at-gate / per-job-tiered / scout-on-demand), and the **owed trio** (§6: two LEARNINGS/AGENT_PATTERNS candidates [the load-tiering framework + the AC-under-AC `<task>`-override], a fresh `pg_dump` [>1wk], stale-handoff retirement pending Justin's OK). *(Correction: AC0-24 did docs, NOT ③ conducting — the ③ pipeline was AC0-23 + AC2 + Justin.)*
>
> **⭐⭐ NEWEST — READ FIRST: there is an `## ⭐⭐ AC0-24 SET-DOWN → next AC0` block at the very bottom of this file. AC0-24's session did NOT go the way §1–§3 below describe (Justin re-scoped it off the AC3 experiment onto a docs-current pass). That set-down carries the corrected LIVE state (③ is ASSEMBLED, not "at the Mira gate"), what's still owed, and the properly-reasoned AC3 load list. Read it FIRST, then §1–§7 for the still-valid standing depth.**

---

## §1 — THE SPLIT (read first — this is why you exist, AC0-24)

Justin spun you (**AC0-24**) to run the **AC3-under-AC0 experiment** (your primary job — §3). **AC0-23 is hanging back on the loaded instance to finish the ③ merge gate** (he's carrying the full runtime; the gate is runtime-heavy; no point re-paying that load). Two contingencies:

- **Normal path:** AC0-23 finishes ③ (Mira → rebase → Opus merge-review → Justin's merge → bundle deploy). You just run the AC3 experiment. The ③ gate is NOT yours.
- **Takeover path:** if AC0-23 runs out of context mid-merge, he appends a `## ⚡ MERGE-TAKEOVER APPENDAGE` at the bottom of THIS file with the exact merge state, and **you take over the ③ gate from there** (§4 tells you how + the load tier). Until that appendage exists, the merge is AC0-23's.

**You are AC0-24.** Sign mailbox/brief/ping traffic as `AC0-24` (or `AC0` if you're the only AC0 by then — ask Justin). Ping `#team-alerts` every response.

---

## §2 — LIVE STATE (2026-07-20)

- **PROD = `v0.2.7`, unchanged.** Clustering + Haiku both live-OFF/dormant. Evryn prod untouched by everything below.
- **⭐ Step 78 Half B is DEPLOYED to Vercel** (dashboard 5 health lights; merged `5cc6f28`, pushed) — but **LIVE-BUT-INERT**: the lights render idle "—" until **Half A** (the backend tri-state `/health`) deploys with the ③ Railway bundle. That's by design (graceful degrade). *Do not "fix" the idle lights — they light up when Half A deploys; there's a POST-③-deploy verify item for exactly this (§5 bundle-deploy + the 07-16 handoff's bundle section).*
- **`evryn-backend` `main` = `7d664af`** (Half A `de3a5bd` merged there; NOT deployed → rides the ③ bundle). Prod tag = `v0.2.7`.
- **`_evryn-meta` `main` is a few commits AHEAD of origin, UNPUSHED** (AC0-23's loop-close + dashboard-verify note + this handoff). **All docs → Vercel-INERT** (no `dashboard/` files in the unpushed range — verify with `git diff --name-only origin/main..main -- dashboard/`, expect empty). Rides the next push. **`⚠️` pushing `_evryn-meta` main IS the Vercel deploy** and carries the whole backlog — always run that `dashboard/` check first (empty = safe).
- **The Mira pass** (the ③ merge-blocker) was "nearly back" when this was written — AC2 is firing it. It rewords `triage.md` / `core.md` to match ③'s removed tool surface (see §4).

**Critical path to a gatekeeper (unchanged except Fable is GONE):**
> ⛔ Mira pass (AC2) → rebase ③ → **Opus merge-review** (Fable is retired — the "dedicated Fable review" in the older docs is now a dedicated **Opus** review) → **Justin's merge** → one bundle `railway up` → **Weekend architecture capture** (now slots in HERE, BEFORE Gate-B — Justin 2026-07-20) → **Gate-B** (mandatory staging create-from-zero live-fire) → flip Clustering + Haiku ON → Justin's account setups → gatekeeper.

---

## §3 — YOUR PRIMARY JOB, AC0-24: the AC3-under-AC0 experiment (Step 79)

**Decision (Justin approved, AC0-23 owns the "when"):** pull **AC3 under AC0 as a subagent**, scoped to **Step 79 (+ his fill items)** — **NOT Gate-B** (that's a separate fresh dedicated reviewer, §5). **AC2 stays a peer** through the ③ merge (unchanged). **This is the FIRST live run of the subagent-of-AC0 model** — treat kinks as "how do I make this work," report learnings.

**Why it's safe to run now:** AC3's next chunk (**Step 79 = the STAGING dashboard tab**) is genuinely OFF the ③ critical path — Vercel/dashboard, separate-codebase (`#cascade-override`), no backend runtime, no ③ collision. So it can't endanger the merge, and it doesn't need the product runtime → **it's the ideal low-stakes first shakedown.** It is **non-urgent** (AC3's own brief: "nothing in the lane is unblocked-AND-urgent").

**HOW to spin AC3 (aligned with Justin — keep his brief, wrap it):**
1. **Re-read the orchestration protocol's "Spinning a DC or QC subagent" + the AC-subagent / Conductor-Cascade "reporter" material IN FULL first** (`docs/protocols/ac-orchestration-protocol.md`). Don't spin from memory. Model-PIN Opus. Verify the two-part load receipts against your load list on return; junk + re-spin if wrong.
2. **Keep AC3's brief as the continuity spine — do NOT replace it or gut it.** `docs/working/2026.06.30-ac0-ac3-identity-ops-brief.md` (271 lines; §0–7 standing context + collision map + disciplines + the 2026-07-20 SET-DOWN = his go-forward). His set-down names Step 79 as the clean next chunk.
3. **The `<task>` wrapper does 3 jobs** (because a spawn is a fresh, zero-memory instance):
   - **(a) LOAD-GATE:** tell the fresh AC3 to LOAD its brief + its cascade (per its brief §0.2). Its own brief tells it what to load — but a spawn won't know to unless told.
   - **(b) ASSIGNMENT:** Step 79 (the staging dashboard tab). **Scope the staging-`/health` dependency FIRST** (see wrinkle below).
   - **(c) CONTRACT OVERRIDE — load-bearing:** AC3's brief is written for **peer-AC3** and says "ping `#team-alerts` EVERY response," "take recommend-first ballots STRAIGHT to Justin," "AC0 is a PEER," "coordinate through Justin." Under the subagent model these FLIP — **explicitly override in `<task>`:** escalations/ballots/reports come **to you (AC0-24)** via the 3-bucket triage (your-province / good-to-know / needs-Justin → you relay the genuine-Justin ones up as a clean ballot); it reports to you, doesn't ping Justin's Slack directly; you're its parent/orchestrator, not a peer. **If you skip this, fresh-AC3 boots and behaves as a peer, defeating the experiment.**
   - *(Forward note: if the model sticks for AC3, later fold the contract-override permanently into his brief so future spawns don't need it in `<task>`. For THIS first run, keep it in `<task>` — don't rewrite his brief mid-experiment.)*
4. **The 3-bucket contract (Justin's framing):** when AC3 surfaces something → **(1) Your province** = an inter-lane/sequencing call you resolve yourself · **(2) Good-to-know** = the lane owns it, you hold context · **(3) Needs Justin** = taste/authorization/scope/money → you bump up as a clean recommend-first ballot. Bucket 3 being a clean channel-to-Justin-through-you is the whole point of the model.

**⚠️ The Step-79 wrinkle to scope first:** Step 79 needs "the staging `/health`," but staging may NOT have Half A's tri-state `/health` deployed yet → the tab may have nothing real to show. Have AC3 scope that dependency at the top; **if Step 79 is actually blocked on a staging deploy of Half A, pivot AC3 to fill (watch-list / 35b/c/d) or hold** — and surface to you (bucket 1 or 3).

**Load tier for YOUR job (spawn + triage AC3):** **LIGHT — no product runtime.** Load: AC3's brief + the orchestration protocol + current-state + this handoff. Step 79 is dashboard-only; you never need `evryn-backend/src/**` to run this.

---

## §4 — MERGE-TAKEOVER READINESS (only if AC0-23 ran out — check the bottom appendage)

**The ③ gate is runtime-HEAVY. Load tier: FULL runtime (all `src/**` + `identity/**`) OR scout+fresh-full-load-reviewer.** Do NOT gate ③ on a constrained load — it rewrites the tool surface and the identity files must match; a missed interaction is exactly what the gate exists to catch. Justin's rule: "this merge is too important to go janky."

**③ in one line:** `ac2/step57-runtime-bookkeeping` (tip was `d9306a8`, on `evryn-backend`) — Runtime Bookkeeping: user-creation moves to verdict-time, the raw `supabase_upsert` status-write surface is REMOVED, `record_pass` is DELETED, a new operator-only `correct_user_field`. RATIFIED + BUILT + QC-GO + DEV-GREEN + Gate-A-cleared (13/13). Ships switch-less, ZERO prod migration.

**The gate steps (in order):**
1. **Verify the Mira pass INDEPENDENTLY** — the identity files must now match ③'s removed surface. Known instances AC0-23 confirmed against `main`: `triage.md:125` said "Call `record_pass`" (③ deletes it); `triage.md:21,58` + `core.md:250` named the removed raw-write surface; `triage.md:33` told her to `supabase_read` history ③ pre-loads. **After Mira's pass, re-check these against the runtime yourself** (identity-as-runtime — a stale instruction is a real defect). This is the "second set of eyes" — you re-check Mira's voiced output vs the runtime; AC2 fired her, so your check is the independent one.
2. **Rebase** `ac2/step57-runtime-bookkeeping` onto current `evryn-backend main` (catch-up).
3. **Dedicated Opus merge-review** (Fable retired). A full-load review of the merged diff.
4. **Justin's merge go** (his gate, per-action).
5. Then the **bundle deploy** (§5).

**③ context to read IN FULL before gating:** AC2's ③ brief `docs/working/2026.07.14-ac2-step57-dc-build-review.md` (the live START-HERE) + `docs/working/2026.06.30-ac0-ac2-cost-lane-brief.md` (the AC0⇄AC2 mailbox — the Gate-A co-sign is at its tail). ADR-051 = ③.

---

## §5 — PER-JOB TIERED LOAD LISTS (the load strategy — with humility)

**Framework (AC0-23 ↔ Justin, 2026-07-20):** conducting is two modes — **orchestration** (light, no runtime) and **runtime-gating** (heavy). So: **light-by-default + full-only-at-the-discrete-gates + targeted-reads-for-surprises + spin a fresh full-load scout/reviewer when a surprise needs breadth.** Each job below is **standalone** (don't assume a prior job's load carries over — each may add/subtract) and tagged with its **load tier**. **⚠️ HUMILITY:** a constrained list is only trustworthy until things drift — for any FULL-tier job, or if the state has moved since this was written, **re-spin fully-loaded (or spin a fresh full-load reviewer) to verify** rather than trusting the list.

- **AC3-subagent spawn + triage (Step 79)** — tier **LIGHT**. Load: AC3 brief + orchestration protocol + current-state + this handoff. NO product runtime.
- **③ merge gate** — tier **FULL-or-fresh-reviewer**. Load: all `src/**` + `identity/**` (or scout + a fresh full-load reviewer). Highest stakes — do not trust a constrained list. (§4.)
- **Bundle deploy (post-③)** — tier **TARGETED**. Load: `docs/deploy-log.md` + `evryn-backend/docs/staging-runbook.md` + the key-split runbook (`docs/working/2026.07.13-acu-ac0-anthropic-key-split-runbook.md`) + the 07-16 handoff's `📦 THE BUNDLE DEPLOY` section (the full contents list + the deploy-time doc-work). **Bundle carries:** ③ + 54a/54c + Step-78 Half A + WebFetch fast-follow + Anthropic key-split env swap + dead `_PROD` var cleanup. **ZERO prod migration.** ⭐ **POST-DEPLOY: verify the Half-B dashboard lights come alive** (they've been inert since 2026-07-20 — this is the deferred check; see the 07-16 handoff bundle section).
- **SPRINT 91 (halt-resume lane-resolution bug, post-③)** — tier **TARGETED→FULL**. Routed to AC2 (touches `process.ts` = ③ turf); AC0 gates. Load the SPRINT 91 step + `process.ts`. A confirmed Mark-blocker; deploy right AFTER ③.
- **SPRINT 92 (`schema-reference.md` re-pull)** — tier **LIGHT/TARGETED**. Do it AFTER the bundle deploy so one pull captures everything. Interim DO-NOT-TRUST banner is in place.
- **Gate-B (staging create-from-zero live-fire)** — tier **ORCHESTRATION for AC0** (the *reviewer* loads full, not you). **Commission a FRESH dedicated full-load sub-AC** (independence from ③'s author + fresh eyes; NOT AC2, NOT AC3-in-flight). AC3 supports the staging *mechanics*; the fresh reviewer owns the *verdict*; AC0 weighs it. Gated on ③ landing + Justin's `SEND_ENABLED` flip. Megan fixtures ready. **Load a live-fire GO carefully** — breaker-off, real-model, real tool-loop (a GO built on a tripped breaker proves plumbing, not behavior).
- **Weekend architecture capture** — tier **TBD-IN-THE-MOMENT (Justin's explicit steer, 2026-07-20)**. The load can't be pre-guessed — **read the napkin brief FIRST (`docs/working/2026.07.19-ac-justin-restructure-napkin.md`, deliberately NOT settled — re-derive from first principles), THEN decide the load in the moment.** Likely FULL (it's a `triage/classify.ts` god-module restructure) but confirm after reading. Wants a **fully-loaded dedicated AC**, NOT a session tail. **Slots in AFTER the bundle deploy, BEFORE Gate-B** so Gate-B validates the settled architecture.

---

## §6 — AC0 OWED / carry-forward (standing)

1. **Housekeeping — fix the Fable→Opus reference in the docs.** The 07-16 handoff + current-state still say "dedicated **Fable** review (ratified)." Fable is RETIRED — the ③ merge-review stays **Opus**. AC0-23 flagged this but deliberately deferred the doc-fix (didn't want commit-churn mid-flight). Fix it at your next housekeeping touch. *(Not yet written — named here so it isn't lost.)*
2. **The 07-16 handoff's `📌 AC0 OWED` list still stands** (gate the ③ chain, track the WebFetch fast-follow [AC2's to finish — NB-1/NB-2/NB-3 + live-path hang exercise + ADR], SPRINT 91/92, bundle-deploy doc-work, cross-lane Mira-coordinate + F2 metadata-shape). Read it there — not re-copied here.
3. **`pg_dump` archival** — was ~6 days old at the 07-16 #lock and crosses the week ~2026-07-17, so a fresh archival `pg_dump` is ~owed (method: `evryn-backend/backups/README.md`; DB URLs by name from `.env`, never echo). Low-urgency.
4. **Push discipline:** `_evryn-meta` main carries an unpushed docs backlog (Vercel-inert). Before ANY `_evryn-meta` push: `git diff --name-only origin/main..main -- dashboard/` (empty = safe). Stage by explicit path, never `git add -A`. Commit gates are Justin's per-action (merge / deploy / prod migration / Mira SCM); housekeeping docs + mailbox/brief + handoff = commit + push freely.
5. **Subagent lifecycle facts:** same-session resume by `agentId` works (full context); cross-instance/cross-session resume does NOT ("No transcript found") → a fix-trip after an instance change is a FRESH spin. A user interrupt KILLS background subagents (won't resume) → re-spin fresh.
6. **⚠️ The 07-16 handoff's `🚨 OVERNIGHT / UNATTENDED LANDMINES` are STILL LIVE — read them before any deploy or DEV touch.** Especially: **(a) DO NOT WIPE THE GATE-A DEV STATE** — a12's frozen `processing` row + the Megan/`<gatea-*>` items are the **verification fixture for the WebFetch fix**; cleanup is deliberately HELD until the WebFetch live-path exercise passes. **(b) 4 untracked `tests/_gate-a-*.ts`** in the canonical `evryn-backend` tree = the held verification kit — don't commit, don't delete. **(c) a bare `railway up` deploys PROD** — staging needs `--service evryn-staging --environment production`.

---

## §7 — Key docs (verified to resolve 2026-07-20)

- **This handoff** (newest) → then **`docs/working/2026.07.16-ac0-ac0-handoff.md`** (+ its 2026-07-17 addendum) for standing depth.
- **State:** `docs/current-state.md` · `CHANGELOG.md` (the 2026-07-20 Half-B-deploy entry is AC0-23's) · `docs/deploy-log.md`.
- **Lanes:** AC2/③ → `docs/working/2026.07.14-ac2-step57-dc-build-review.md` + `docs/working/2026.06.30-ac0-ac2-cost-lane-brief.md`. AC3 → `docs/working/2026.06.30-ac0-ac3-identity-ops-brief.md` (the 2026-07-20 SET-DOWN is his go-forward).
- **Sprint + ADR:** `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (Steps 88 Gate-A/B, 90, 91, 92) · `docs/decisions/051-...md` (③).
- **Manual + protocols:** `CLAUDE.md` · `docs/protocols/` → `ac-orchestration-protocol.md` (READ before any spin) · `lock-protocol.md` · `doc-update-routing-protocol.md` · `ac-writing-protocol.md`.
- **⚠️ ACU-authored — reference, DON'T process/sweep:** the key-split runbook (your deploy rider) + the uber-week-plan + product-state-report.
- **Ping helper — REMAKE YOUR OWN** (a fresh instance has its own scratchpad; AC0-23's copy is gone with his session). Node `fetch` POSTing `{text}` to `SLACK_TEAM_WEBHOOK_URL`, **read by NAME** from `C:/Users/Justin/Evryn/Code/evryn-team-workspace/.env` (C:/ path form in Node, not git-bash `/c/`), **never echo the URL** (a secret in-context → Fable-safeguard forced re-spin) — print only `ping OK`. Prefix `AC0-24:`. Ping `#team-alerts` "I'm up," then every response.

---

## ⭐⭐ AC0-24 SET-DOWN → next AC0 — NEWEST, READ THIS FIRST (2026-07-20 evening)

> §1–§7 above are the AC0-23→AC0-24 handoff written for the SPLIT (AC0-23 finishes ③, AC0-24 runs the AC3 experiment). **This session did NOT go that way.** Justin pulled AC0-24 OFF the AC3 case onto a single-focus **"get the docs current"** pass. So: **§3 (the AC3-under-AC0 experiment) is STILL OWED — not started.** §1/§2/§5's ③ state is now **STALE** — corrected here. §4/§6/§7 standing depth is still valid. Read this block, then §1–§7 for what it doesn't repeat.

### What this session was (the gear-shift)
Justin re-scoped AC0-24 to: **read the FULL `evryn-backend` runtime — BOTH halves (all 39 `src/*.ts` + all 11 `identity/*.md`) — then diff EVERY worktree, THEN update the current-states.** All done. He said he'd **spin a fresh AC0 for the rest.** So you (next AC0) inherit BOTH open threads: **(a)** the **AC3-under-AC0 experiment** (§3, still owed — load list below), and **(b)** if AC0-23 didn't finish it, the **③ merge gate** (§4 is your mechanics — but the state moved, see below).

### ⭐ GROUND TRUTH (verified against the runtime + the worktrees — the docs were stale; trust THIS)
- **③ is FULLY ASSEMBLED — NOT "at the Mira gate"** (§2 is stale). Worktree `evryn-backend-step57` tip **`20f046c`** is a **merge commit**; I confirmed BOTH the **Mira pass** (`cf39c9b`, 2 rounds, Justin-approved) AND the **DC note-ordering fix** (`db52e6a`) are ancestors, all rebased onto current `main`. On `main`, `triage.md:125` still names the deleted `record_pass`; on the ③ branch the identity files are rewritten to match ③'s removed surface — **so the Mira-pass merge-blocker is CLEARED.** **AC2's "assemble the branch" step is DONE.** And a parallel AC (commit `61621ef`, ~18:03 PT) has since taken it FURTHER: a **full-load Opus merge-review returned GO, zero blockers** (one deferred *cosmetic* non-blocker — `db/users.ts updateUser`'s Pick omits `email` though `correct_user_field` edits it, but `.update(fields)` writes it through unfiltered — verified, not a silent drop; do NOT reopen the gate for it). ⇒ **Remaining ③ chain: Justin's merge-go → FF-merge → AC0-23's pre-deploy sign-off → one-window bundle deploy → weekend architecture capture → Gate-B.** (Fable is RETIRED — the review was **Opus**.) ⚠️ **A parallel AC0/AC2 is ACTIVELY working ③ — coordinate with Justin before touching the ③ gate yourself.**
- **SPRINT 91 (Mark-blocker) — AC0-24 CONFIRMED against the live runtime.** `resumeParkedItem` (`src/email/process.ts` ~L483) does `findOrCreateUser({status:"lead"})` with **NO** `resolveGatekeeperByInboundAddress` / `decideSenderResolution` call — so a halt-resumed *gatekeeper* forward from a registered non-primary lane resumes as a phantom lead + empty-profile triage. Exactly the sprint description. Routed to AC2, sequence AFTER ③ (touches `process.ts`).
- **Worktrees at close** — `evryn-backend`: `main`=`7d664af` · `evryn-backend-step57`=`20f046c` (the assembled ③ — the trunk everything merges FROM; reap `evryn-backend-step57-dc` + `evryn-backend-step57-mira` ONLY after ③ merges) · `evryn-backend-webfetch`=`bf23bca` (the WebFetch fast-follow — stacked on the **OLD** ③ base, so it **needs a rebase onto the assembled ③** post-merge; still a separate item owing a DC fix-trip + AC2 live-path exercise + ADR per §6). `_evryn-meta`: `main`=`57964f9` + AC0-24's commits (docs, Vercel-inert) · `_evryn-meta-sweep` (Lucas) nothing ahead of main.

### Docs AC0-24 updated this session
- **`_evryn-meta/docs/current-state.md`** — 3 edits (folded into the snapshot, no new dated stamp): ③→ASSEMBLED + the corrected chain · **Fable→Opus** on the merge-review · handoff pointers → the 7/20 handoff.
- **`evryn-team-workspace/shared/current-state/current-state.md`** — one team-altitude appendage (signed `AC0-24`, ISO-stamped, before the canary) advancing the 7/16 AC0 note. Per the #lock protocol's "AC is build-ambassador; append, never overwrite; every line passes the non-technical-reader test."
- **This brief** (this set-down).
- **CHANGELOG: nothing owed** — ③ is assembled-on-branch, unshipped (no-changelog-for-unshipped). Nothing has shipped since the 7/20 Half-B Vercel deploy.
- **⚠️ FLAG (not AC0's to fix):** the TEAM current-state's last *full* rebuild was **2026-06-05** — its runway/Fenwick body is broadly stale; that's Lucas's `#standup` rebuild (and #sweep/#align are both overdue). AC0-24 only appended the build-progress correction, not the rest.

### 🔴 THE AC3 LOAD LIST — what the conducting AC0 ACTUALLY needs to run AC3 on Step 79 (Justin's ask, 2026-07-20)

**⚠️ CORRECTION to §5.** §5 tagged "AC3-subagent spawn + triage (Step 79)" as tier **LIGHT — NO product runtime**. **That is WRONG.** Step 79 = the STAGING dashboard tab, which **reads the backend's `/health` HTTP endpoint AND the staging Supabase DB directly.** Directing/gating a build that consumes a backend contract IS build work — so AC0 must hold that contract. AC0-24 read the whole runtime and mapped the seam; the corrected load is below. *(AC3's OWN load stays a `#cascade-override` **separate-codebase** load — the dashboard, NOT the product-runtime cascade, per the orchestration protocol's "When the target is a SEPARATE codebase" provision. This list is what **AC0** loads to assemble AC3's list + gate the work.)*

**How the dashboard and the backend interact (the load-bearing insight):** the monitoring dashboard (`_evryn-meta/dashboard/`, a separate Vercel codebase) reads the backend TWO ways, and Step 79 mirrors BOTH for staging —
1. **The backend `/health` HTTP endpoint** → the 4 per-subsystem health lights (`db · slack_socket · poll · m1`). `dashboard/api/product.ts` `fetchHealth()` hits `<railway-url>/health` and **parses the body even on a 503** (a "wedged" is a 503 whose body carries the checks). The response contract is defined in `src/safety/health.ts` (`HealthResponseBody = {status, uptime, checks:{db, slack_socket, poll, m1}}`) + `src/safety/liveness.ts` (`decideHealthStatus` — tri-state ok/wedged/halted, 503-on-wedged, **halted-beats-wedged**) and wired in `src/index.ts`. **This is the dashboard↔backend seam.**
2. **The Supabase DB directly** (aggregate, PII-firewalled) → activity counts / `llm_usage` spend / recent-error items. The prod tab reads the PROD Supabase; the staging tab reads the STAGING Supabase (`hyqeutelwywbkisjekkb`).

**⭐ Load this (over-loaded on the seam, per Justin's "when in doubt, over-load"):**

*Orchestration + lane spine:*
- `_evryn-meta/CLAUDE.md` (auto) · **this handoff + `docs/current-state.md`** (live state).
- **AC3 brief** `docs/working/2026.06.30-ac0-ac3-identity-ops-brief.md` (full — the continuity spine; its 2026-07-20 SET-DOWN names Step 79 as the clean next chunk + the contract-override you must apply when spinning it as a subagent, per §3).
- **`docs/protocols/ac-orchestration-protocol.md`** (full — READ before any spin: the **"When the target is a SEPARATE codebase"** `#cascade-override` provision, the **AC-subagent 2-substitution variant**, **"Nested subagents / never tell a child where to report,"** and **recursive-propagation**).

*The dashboard target (to assemble AC3's `#cascade-override` list + gate the build):*
- `dashboard/api/product.ts` (full — the prod "Evryn Product" tab AC3 mirrors: how it fetches `/health`, parses a 503 body, reads the DB aggregates, and its PII-firewall posture).
- `dashboard/public/index.html` (full — the tab structure + `renderHealthLights()` + the `.health-*` CSS).
- `dashboard/api/data.ts` · `dashboard/api/usage.ts` · `dashboard/api/team.ts` (the api/tab convention — over-load for the pattern).
- `dashboard/package.json` + `dashboard/vercel.json` (routing + the Vercel deploy = an `_evryn-meta` PUSH).
- `dashboard/api/staging.ts` — **does NOT exist yet; this is what Step 79 CREATES.**

*The backend `/health` contract (the seam — LOAD-BEARING):*
- `src/safety/health.ts` (full — `HealthResponseBody` + `buildHealthResponse`; the exact shape the staging tab parses).
- `src/safety/liveness.ts` (full — `decideHealthStatus` tri-state + 503-on-wedged + the halted-beats-wedged precedence the lights mirror at the presentation layer).
- `src/index.ts` (full — the `/health` handler wiring + the memoized never-throw db-probe).
- The **four subsystem-light SOURCES** (so AC0 can gate that each staging light reads truth): `src/safety/breaker.ts` `getBreakerState()` (the **m1** light) · `src/notify/slack.ts` `getSocketConnected()` (the **slack_socket** light — the getter suffices) · `src/safety/liveness.ts` `getPollLiveness`/`recordPollSuccess` (the **poll** light; `recordPollSuccess` is called from `src/email/poll.ts`) · the db probe in `index.ts` (the **db** light).

*The backend DB-read contract (if the staging tab surfaces activity/spend/errors, like the prod tab):*
- `src/db/llm-usage.ts` (the `llm_usage` columns — model/cost/tokens the cost cut reads).
- `src/db/items.ts` (`emailmgr_items` + `ItemStatus` — for the recent-items / 24h-error reads).
- `src/config.ts` (`EVRYN_ENV` selection + `healthWedgedThresholdMs`).
- **PII-firewall posture (gate this):** the dashboard reads aggregates ONLY — NEVER `agent_messages` / user-profile rows. Gate AC3's `staging.ts` to keep its `.from(...)` query set aggregate-only (the prod tab is byte-for-byte PII-safe; QC verified it on Half B — a hostile field renders escaped, `m1.reason` is a closed enum, `tripBreaker`'s free-text `detail` is a separate field `health.ts` never reads).

*Staging environment facts:*
- `evryn-backend/docs/staging-runbook.md` (full — staging Supabase ref `hyqeutelwywbkisjekkb`, the `evryn-staging` Railway service + its URL, the `railway up --service evryn-staging --environment production` deploy method, `SEND_ENABLED=false`).
- **⚠️ THE STEP-79 WRINKLE — SCOPE FIRST.** Staging's `/health` is almost certainly the OLD always-200 shape (staging stood up 7/07, before Half A's real `/health` existed 7/15, and Half A has never been deployed to staging). So the staging tab's 4 health lights will render idle "—" until Half A deploys to staging (graceful degrade, exactly like Half B on prod today). **Have AC3 confirm staging's deployed `/health` state as its literal FIRST move;** if Step 79 is genuinely blocked on a staging Half-A deploy, pivot AC3 to fill (watch-list / 35b/c/d) or hold, and surface to you (bucket 1/3).

**Deliberately NOT loaded (the cut, named per the over-load-audit discipline):** the triage/email/identity halves of the product runtime — `classify.ts`, `process.ts`, `poll.ts` (beyond the `recordPollSuccess` feeder), `slack.ts` (beyond `getSocketConnected`), `flow.ts`, the 11 identity files, the M1 detectors, the security guards (web-guard / supabase-read-guard / fence). Step 79 is a dashboard tab reading `/health` + DB aggregates — it never exercises Evryn's judgment runtime or her identity. Cut because the tab provably doesn't touch them, NOT because they're unimportant. *(If the work moves into the "WHY is a subsystem light red" drill-down — Half B's HOLD-1 territory — load more; but Step 79 is a straight staging MIRROR of the already-built, already-QC'd prod tab, so the `/health` + DB-read contracts above ARE the whole seam.)*

**Net:** AC0's load to run AC3 = **the dashboard + the backend `/health`+DB-read seam (~10 targeted backend files) + staging facts + the lane spine** — NOT the full 39-file runtime (a dashboard trip is `#cascade-override`), and NOT §5's earlier LIGHT-zero-backend list (which would leave AC0 unable to gate the seam). If in genuine doubt on any given file, include it — the asymmetry favors over-loading, and this list is already erring that way.

### If YOU are also carrying ③ (a parallel AC0/AC2 is ACTIVELY on it — coordinate with Justin FIRST)
§4 is the gate mechanics, but the state MOVED well past it: ③ is **assembled** (`20f046c`) **and the full-load Opus merge-review is DONE = GO** (commit `61621ef`). So the assemble + review steps are BOTH done — what remains is **Justin's explicit merge-go → FF-merge ③ → `main` → AC0-23's pre-deploy sign-off → the bundle deploy** (§5 TARGETED tier + the 07-16 handoff's `📦 THE BUNDLE DEPLOY` section) → weekend arch-capture → Gate-B. **⚠️ Because a parallel instance is live on this, do NOT re-run the QC review or touch the ③ gate without checking with Justin — you'd duplicate work / risk a collision.** If you genuinely need to gate anything ③ yourself, the tier is FULL-or-fresh-reviewer (§4/§5) — never a constrained load.

### Repo state at close (2026-07-20 evening, AC0-24)
- **`evryn-backend`:** untouched by AC0-24. `main`=`7d664af`, prod `v0.2.7`. Worktrees as listed under GROUND TRUTH above.
- **`_evryn-meta`:** on `main`. AC0-24's commit(s) carry the current-state edits + this brief (docs only, **Vercel-inert** — verify with `git diff --name-only origin/main..main -- dashboard/`, expect empty). `main` was ahead of origin by other agents' docs commits; AC0-24 pushed the docs backlog cleanly IF the dashboard-check was empty (see the final ping / chat for whether the push happened).
- **`evryn-team-workspace`:** on `main`. AC0-24's one appendage commit to `shared/current-state/current-state.md`. Check `origin/main..main` before pushing — it's a shared repo (many agents); push only if clean or coordinate.
- **Ping helper:** a fresh instance has its own scratchpad — AC0-24's copy is gone. REMAKE it per §7 (Node `fetch` to `SLACK_TEAM_WEBHOOK_URL`, read by NAME from `evryn-team-workspace/.env`, never echo the URL, print only `ping OK`).

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
