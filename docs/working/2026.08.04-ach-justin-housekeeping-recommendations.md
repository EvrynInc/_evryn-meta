# Housekeeping recommendations + pipeline queue for AC0-30

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

## 🔴 READ THIS FIRST — this file has TWO parts, and only ONE of them is live

**You are AC0-30, and you weren't in the conversation that produced this.** That's exactly why both parts are here.

| | What it is | What to do with it |
|---|---|---|
| **PART 1** (below, to the `═══` seam) | The original **ballot** ACh wrote for Justin on 2026-08-04. | **SETTLED RECORD — CONTEXT ONLY.** Justin has answered every question in it. **Do not re-open anything here.** Read it to understand *why* Part 2 says what it says. |
| **PART 2** (after the seam) | The **pipeline queue** — eleven items, each triageable as land-before vs park. | **THIS IS YOUR WORK.** Every item stands alone and carries a locator for its source material. |

**🔴 Every ballot question in Part 1 is ANSWERED.** The answers are consolidated in the settled-record table at the top of Part 2 — go there, not to the questions below. Where Part 1 states something that **later became false**, there is an inline `⛔ SUPERSEDED` marker at that exact spot routing you forward. If you hit a claim in Part 1 with no marker, it still holds.

**⏰ If you read only one item, read `P5` — Gate-B is Friday 2026-08-07 and no committed code can run the suite it requires.**

*Part 1 written 2026-08-04T15:14:55-07:00; Part 2 appended 2026-08-05T13:57-07:00. Both by ACh (a sub-AC under AC0), fully loaded with the Evryn product runtime.*

---

## PART 1 — THE ORIGINAL BALLOT (settled; context only)

> **How Part 1 was written:** a **ballot**. Three piles of accumulated cruft were reviewed; each item carries a specific, checkable reason, shaped so Justin could answer `1.a — yes`, `2 — no`. **At the time of writing, nothing here had been done** — ACh only looked and recommended. *(That is no longer true: see the settled-record table in Part 2 for what has since been executed.)*
>
> **What does NOT belong here:** the lane's working notes and reasoning — those live in `_evryn-meta/docs/sessions/historical/2026.08/2026.08.04-ac0-ach-housekeeping-brief.md` §7.

---

## The 10,000-foot version

Three piles of leftovers were reviewed:

1. **A folder of working documents** (`_evryn-meta/docs/working/`) — the scratch-pad briefs and handoffs the various AC instances write for each other. It's grown to 32 files and nobody can tell at a glance which are live and which describe work that shipped weeks ago.
2. **Old code branches** on the product repo — parallel copies of the codebase left behind after work merged. They're noise in every branch listing.
3. **Four test scripts** sitting outside version control, which turn out to be load-bearing for a test suite we've called permanent.

**The headline:** the branches are the clean, easy win. The working docs are messier than they look — **I found five documents that are actively live but weren't on the "don't touch" list I was given**, plus two that are safe to retire *only after* something inside them is moved somewhere durable first. And the four test scripts turn out to be a genuine gap worth fixing rather than a mess worth deleting.

**Retiring a document destroys nothing** — it moves the file into a dated archive folder, and its full history stays in version control and in the changelogs regardless. That's what makes this low-risk. Reaping a branch is the same: the code is already on the main line.

---

## Headline counts

| Pile | Retire / reap | Keep (live) | Couldn't confidently classify |
|---|---|---|---|
| **1. Working documents** (32) | 12 clean + 2 conditional | 12 | 6 |
| **2. Old code branches** (24) | 23 | 1 | 0 |
| **3. Test scripts** (4) | 2 delete, 2 promote | — | 0 |

---

# QUEUE 1 — The working-document folder

**What this is:** `_evryn-meta/docs/working/` holds the briefs and handoff notes your AC instances write to each other. Live ones are read daily; dead ones just cost every AC time when they go looking.

**How I judged each one:** a document's title and date lie about whether its work is finished. So for each, I checked what it *claims* against what's actually in the running code and recorded in the changelogs. Where I couldn't tell, I put it in the edge-case list rather than guessing.

**Where retired docs go:** `_evryn-meta/docs/sessions/historical/<YYYY.MM>/`, foldered by the document's own date. Folders exist for 2026.03 through 2026.07; **a 2026.08 folder does not exist yet** and would need creating (nothing in this batch needs one, but flagging it).

---

## 1. ⚠️ FIVE documents that are LIVE but were not on the do-not-touch list

**Read this section first — it's the most important thing I found.** I was handed a list of seven documents to protect. These five are *also* live, and retiring any of them would take something out from under an active lane. My recommendation on all five is **keep**.

- **1.a — The v0.2 maintenance-plan scoping document** (`2026.07.13-acu-ac0-step47-maintenance-plan-scoping.md`). This is a plan for keeping the product healthy after launch that is **explicitly waiting on decisions from you**. Both the cross-project status file and the sprint plan point at it as the live home of that unfinished work. → **Keep.**
- **1.b — The restructure napkin** (`2026.07.19-ac-justin-restructure-napkin.md`). This is the sketch behind the code-reorganisation lane you started today (the one breaking up the 2,600-line file every AC has to load). The status file names it directly as that lane's input, and flags it as *deliberately unsettled*. → **Keep.**
- **1.c — ACf's current handoff.** ACf is the instance building the AI team's memory system; its handoff is a live continuity note, not an old one. → **Keep.** ⚠️ **Path updated 2026-08-06:** this was `2026.07.21-acf-acf-handoff.md`, which had accreted appendages since July and was **replaced by a fresh standalone — `docs/working/2026.08.06-acf-acf-handoff.md`**. The July file is retired to `docs/sessions/historical/2026.08/`. Keep the *new* one; the recommendation stands, only the filename moved.
- **1.d — The team-memory "item c" build spec** (`2026.07.23-acf1-dc-item-c-build-spec.md`). The status file calls item (c) "the live next build". This document is the spec for a build that hasn't started. → **Keep.**
- **1.e — The team-memory "item b" build spec** (`2026.07.22-acf1-dc-item-b-shared-digest-build-spec.md`). Item (b) is merged, which makes it *look* finished — but a database migration behind it is still unapplied and that whole layer is recorded as never having been verified against a live database. The spec is what that verification will be run against. → **Keep until item (b) is verified live.**

> **Ballot 1:** Keep all five? **(a) yes, keep all five** / **(b) keep some — tell me which to release**

---

## 2. Two documents that are retirable ONLY after something inside them is rescued

Both of these carry a live fact that exists nowhere else durable. Retiring them as-is would lose it.

- **2.a — AC0's 22 July handoff** (`2026.07.22-ac0-ac0-handoff.md`). Superseded as a handoff, but two live things sit inside it: **Soren's unsettled v0.3 design batch** (bulk outreach, the Publisher safety-gate, fill-laundering security) and **Nathan's legal constraint** on backlog outreach. My recommended homes: the **design batch → the v0.3 build document** (`evryn-backend/docs/BUILD-EVRYN-v0.3.md`), which is already the home for v0.3 mechanics; the **legal constraint → Nathan's memory file**, where his other standing legal rulings live, with a pointer from the v0.3 build document.
- **2.b — ACf's 23 July brief to AC0** (`2026.07.23-acf-ac0-brief.md`). This is a short note whose main item **answers a question the status file still lists as open** — the check on whether the "remove the raw database-write tool" safety fix needed copying across to the AI-team runtime. ACf checked and the answer is **no, that runtime never had the problem**. Retiring this doc without recording the answer would leave the status file showing an open item that is actually closed. → Record the answer in `_evryn-meta/docs/current-state.md`, then retire.

> **Ballot 2:** Rehome-then-retire both, at the homes I've named? **(a) yes** / **(b) yes but different homes** / **(c) leave both in place for now**

### ⚠️ A related catch worth your eye

The **legal ruling on outreach appears in two documents I'd otherwise retire** — the 22 July handoff (2.a) and the v0.3 blitz pre-read (item 3.l below). They state it slightly differently, and the pre-read's version is a *correction* recording that Fenwick already resolved it: outreach framed as a reply **on the gatekeeper's behalf** is legitimate. **If both are retired without rehoming, that ruling has no durable home at all** — and it is exactly the kind of thing that would otherwise get re-litigated from scratch. Rescuing it once (per 2.a) covers both.

---

## 3. Recommended for retirement — clean

Each of these describes work that is verifiably finished and recorded elsewhere.

- **3.a — The forward-persistence build brief** (`2026.07.07-ac3-dc-54b-forward-persistence-build-brief.md`). The work was folded into the "runtime does the bookkeeping" change and shipped to production; I confirmed the function it describes is live in the running code.
- **3.b — The 13 July week plan** (`2026.07.13-acu-uber-week-plan.md`). A plan for a week that ended three weeks ago. *(See edge case 4.d — one other document points at this one.)*
  - ⛔ **SUPERSEDED — this one was NOT retired.** Justin ruled the ACU lane is still live, so this document **stays**. Eleven of the twelve in this section were retired, not twelve. **Do not "finish" the twelfth.** → Part 2, **P10.b**.
- **3.c — AC0's 15 July evening handoff** (`2026.07.15-ac0-ac0-eve-handoff.md`). Superseded by five later handoffs.
- **3.d — AC0's 16 July handoff** (`2026.07.16-ac0-ac0-handoff.md`). Superseded.
- **3.e — AC0's 20 July handoff** (`2026.07.20-ac0-ac0-handoff.md`). Superseded.
- **3.f — AC0's 21 July handoff** (`2026.07.21-ac0-ac0-handoff.md`). Superseded.
- **3.g — The hung-web-call brief** (`2026.07.16-ac2-dc-webfetch-timeout-brief.md`). This describes the *original* fix — a flat time limit on every query. That design was **replaced** by the two-layer approach that actually shipped, and the sprint plan explicitly says the original branch and commit are gone and not to go looking for them. Retiring it removes a description of a design we deliberately rejected.
- **3.h — The 16 July blitz manifest** (`2026.07.16-acu-justin-blitz-manifest.md`). A run-sheet for a session that happened.
- **3.i — The product state report** (`2026.07.16-acu-product-state-report.md`). A dated snapshot of the product, now three weeks and two production releases stale. `docs/current-state.md` is the living version of this.
- **3.j — The team-runtime state report** (`2026.07.16-acu-team-runtime-state-report.md`). Same — a dated snapshot, superseded by later merged work.
- **3.k — The changelog-split handoff** (`2026.07.22-acr-acr-changelog-historical-split-handoff.md`). The split it describes is done and recorded in the meta changelog; the one residual (two early months not yet split) is documented in the changelog's own footer, so this handoff isn't its only home.
- **3.l — The v0.3 blitz pre-read** (`2026.07.13-acu-acu-v03-blitz-preread.md`). **Its own header says to archive it once the session absorbed it.** ⚠️ But see the catch above — rescue the legal ruling first.

> **Ballot 3:** Retire all twelve to their dated archive folders? **(a) yes, all twelve** / **(b) all except ones I'll name**

---

## 4. Edge cases — where I could not confidently classify

Per the brief, this list is half the value. Each entry says what I know, what would settle it, and what I'd guess if forced.

- **4.a — AC3's identity/ops brief** (`2026.06.30-ac0-ac3-identity-ops-brief.md`) — **the largest file in the folder at 65KB.**
  - *What I know:* every lane item I could trace is recorded as finished (staging stood up, the watch-list built, the decline playbook shipped), and AC3 has since been given a *newer* brief which is on the protected list.
  - *Why I stopped:* it is the same shape as the 22 July handoff — a large accumulated brief that turned out to be carrying two un-rehomed live items. I did not read all 65KB, so I can't rule out the same thing here.
  - *To resolve:* someone reads it end-to-end hunting for un-rehomed items.
  - *If forced:* retire — but I'd want the read done first, and I would not do this one blind.
  - ⛔ **SUPERSEDED — the read was done, and "retire" was the WRONG guess.** I read all 271 lines. It is **not clean**: it carries **six un-rehomed items**, one of which is a live operator-facing defect. **Do not retire this document until those six are placed.** All six are enumerated in full in Part 2, **P2** — you will not need to open the 65KB.

- **4.b — The health-endpoint design document** (`2026.07.15-ac3-step78-real-health-design.md`) — **the running code cites this file by path.**
  - *What I know:* the work shipped. But two live source files (`evryn-backend/src/safety/liveness.ts` and `src/safety/health.ts`) name this document in their comments as the design authority for a rule that is deliberately counter-intuitive and marked "do not 'fix' this".
  - *Why it matters:* retiring the file silently breaks that pointer, so the next engineer who follows it hits nothing — exactly when they're about to change safety-critical code.
  - *To resolve:* either keep it, or retire it **and** update the two code comments in the same change.
  - *If forced:* keep — the cost of keeping is one file; the cost of a broken pointer is someone "fixing" a rule that must not be fixed.

- **4.c — ACU's product radar findings** (`2026.07.16-acu-ac0-product-radar-findings.md`).
  - *What I know:* the sprint plan cites this document as the provenance for two findings. One shipped; **the other (the database-schema reference being badly out of date) is still open.**
  - *To resolve:* retire once that open item is done.
  - *If forced:* keep until then.

- **4.d — ACU's self-brief** (`2026.07.14-acu-acu-self-brief.md`), coupled to **3.b**.
  - *What I know:* ACU is still listed as an active instance (owning sequencing), and this file is explicitly its cold-start continuity note. But it also records, as of 16 July, that you were *not* re-spinning ACU. It names the 13 July week plan (3.b) as its master plan — so retiring 3.b while keeping this leaves a pointer to a retired file.
  - *To resolve:* one question to you — **is the ACU lane still live?** If no, both retire together. If yes, both stay.
  - *If forced:* retire both — three weeks is long past for a week-plan — but this is genuinely your call, not mine.
  - ⛔ **SUPERSEDED — "retire both" was the WRONG guess.** Justin: the ACU lane is **technically still live** — *"I need to just close him out — but I'll have **him** manage anything downstream of him."* **Both ACU documents STAY.** Justin owns closing that lane; anything downstream is ACU's to route. This also constrains **P4** in Part 2 (moving ACU's research docs would break its self-brief).

- **4.e and 4.f — Two research documents:** the identity-loading research (`2026.07.13-acu-ac0-step53-identity-loading-research.md`) and the outreach-engine research (`2026.07.13-acu-acu-outreach-engine-research.md`).
  - *What I know:* neither is session state — they're research. The identity-loading one supports a sprint item that was deliberately **set aside, not closed**; the outreach one supports v0.3, which hasn't started.
  - *Why I stopped:* **I think "retire" is the wrong verb for both.** Your own documentation rules route research to a research folder (`evryn-team-workspace/shared/projects/product/research/`), not to a session archive — and an archive is precisely where research goes to be never found again. But relocating them is outside what I was asked to recommend.
  - *To resolve:* your call between three options.
  - *If forced:* **relocate to the research folder**, not retire.

> **Ballot 4:** For each: **4.a** (a) read-then-retire / (b) retire now / (c) keep · **4.b** (a) keep / (b) retire + fix the two code comments · **4.c** (a) keep until the schema item is done / (b) retire now · **4.d** — **is the ACU lane still live? yes / no** · **4.e+4.f** (a) relocate to the research folder / (b) retire to the archive / (c) leave in place

---

# QUEUE 2 — Old code branches on the product repo

> ⛔ **SUPERSEDED — ALL OF QUEUE 2 IS DONE. Justin executed the reap himself.** All **23** remote branches named below (18 merged + 5 superseded rebuilds) are **deleted**. `origin/ac2/step57-runtime-bookkeeping` was correctly preserved. Verified 2026-08-05: `evryn-backend`'s remote now carries only `main` + that one branch. **Nothing in Queue 2 is pending — read it for reasoning only.**

**What this is:** every time an AI developer builds something, it works on its own parallel copy of the codebase (a "branch"). When the work merges into the main line, the copy should be deleted. Many weren't, so every branch listing is cluttered.

**I measured this myself rather than trusting the number I was handed** — two earlier counts had disagreed. **My measurement confirms AC0's exactly: 18 merged, 6 not merged, 24 total.**

## 5. The 18 already-merged branches — safe to reap

Their content is already on the main line; deleting the copies removes pure noise and can't lose anything.

`bundle/go-live-v0.2` · `dc/clustering-a-c` · `dc/cost-haiku-r2` · `dc/cost-pass-stamp` · `dc/m1-emergency-channel` · `dc/morning-sweep-c2` · `dc/quiet-hours-redesign` · `dc4/qc-fixes` · `mira/clustering-note-pathway-2026-07-07` · `mira/phase6-identity-batch` · `r2/convergence-bundle` · `r2/lane-a-ingest-resilience` · `r2/lane-a-register-tool` · `r2/lane-a-send-allowlist` · `r2/lane-a-step21-durable-cursor` · `r2/lane-b-operator-approval` · `r2/lane-c-cost` · `r2/m1-silent-death`

> **Ballot 5:** Reap all 18? **(a) yes** / **(b) no**

## 6. The 5 dead-but-NOT-merged branches — safe to reap, on different reasoning

These were **never merged**, so "merged, therefore safe" does not apply. I verified each individually instead. **All five are superseded rebuilds — the work was redone from scratch and the rebuild shipped.**

- **6.a — Four branches from 17 June** (`lane-a/ingest-resilience`, `lane-b/operator-approval`, `lane-c/cost`, `m1/silent-death-stage2`). These are the **"round-1" attempt your deploy ledger records as discarded** — the 2026-06-17 work that was thrown away and rebuilt. I confirmed each has a one-to-one rebuilt counterpart (`r2/…`) that **did** merge and shipped as production release `v0.2.2`.
  - **The careful part:** a mechanical patch-comparison marks all their commits as "not on the main line" — which *looks* like unlanded work. It isn't. That's the expected signature of a **rebuild** (different commits, same substance) rather than a copy. So I checked the substance instead: the circuit-breaker, the detectors, the reflection module and its identity file are all present and running on the main line today.
  - **One genuine loose end I chased down:** six *test* files from these branches don't exist on the main line. I checked each — every one either has a rebuilt equivalent under a different name (for example the cache-stability test became `test-cache-byte-identity.ts`, the send-transport test became `test-send-transport.ts`), or tests a tool that was **deliberately deleted** later. **Nothing is lost.**
- **6.b — The first Haiku pre-screen branch** (`dc/cost-haiku-prescreen`). Superseded by `dc/cost-haiku-r2`, which merged and shipped as `v0.2.6`. Worth noting: this old branch holds the **two-verdict** version of the screen — the design you personally caught as wrong (it mislabelled junk as a real-person "pass"). It was rebuilt as three verdicts. So this branch preserves a design we knowingly rejected.

> **Ballot 6:** Reap all five on the "superseded rebuild" reasoning? **(a) yes** / **(b) yes for the four round-1 only** / **(c) no**

## 7. The one branch to LEAVE ALONE

- **`ac2/step57-runtime-bookkeeping`** — **do not reap, do not force-delete.** Its content is on the main line and deployed (I confirmed the release commit is an ancestor of the main line, and that all six of its commits have equivalents there). **But the local and remote copies have diverged** — the local copy sits 22 commits ahead and 6 behind its remote twin. That divergence is **AC2's to reconcile, not a housekeeping action.** A deletion that fails on divergence is a signal, not an obstacle.

> **Ballot 7:** Leave it alone and route the divergence to AC2? **(a) yes** / **(b) something else**

> ⛔ **SUPERSEDED — the divergence could NOT be routed to AC2, and it is now resolved.** Justin: *"AC2 is no longer in position to work this out — the 'AC2' who ran this was a session that closed a long time ago."* So I investigated it myself. **Verdict: a superseded rebuild, not unlanded work** — `main` carries the newer version. Full reasoning + the evidence in Part 2, **P6**. The branch is safe to delete; I recommended rather than executed.

---

# QUEUE 3 — The four held test scripts

**What this is:** four scripts sit in `evryn-backend/tests/` outside version control. Each self-labels "EPHEMERAL … delete after use". They keep getting re-flagged as "don't touch" at every handoff without anyone revisiting why.

**Why they've been stuck:** nobody is protecting the *files* — the hold is on the **dev-database state they read** (a fictional gatekeeper record, "Megan"), which an outstanding verification still needs.

## 8. What I found — this is a real gap, and it's sharper than it looked

The test **data is committed and permanent**: 13 fictional-gatekeeper email fixtures, their answer key, and a README. Your build document calls this a **"permanent regression suite."**

But **nothing committed can actually run it.** Specifically:

- **The seeder is uncommitted and has no committed equivalent.** I checked: **no committed file anywhere sets up the Megan gatekeeper** — her user record, her mail lane, or her criteria. That capability exists *only* in the untracked `_gate-a-seed.ts`.
- **The runner is uncommitted, but a committed runner already does 90% of the job.** The committed `tests/run-fixtures.ts` runs the *general* 18 fixtures. It doesn't reach the Megan set only because it looks in one folder and doesn't descend into the sub-folder they live in.
- **Your own staging runbook already documents this**, independently of me: it notes the committed runner "does NOT read the `megan/` subdir yet" and that an automated run needs it pointed at the sub-folder — **a small change to the existing runner.**

**So if these four files are deleted as their headers instruct, a suite we call permanent becomes unrunnable** — and it's needed again for **Gate-B**, the mandatory full rehearsal before a real gatekeeper goes live.

## 9. My recommendation — and where I disagree with AC0

AC0's read was: delete the two forensics scripts, and **promote both** the seeder and the runner into the committed suite. **I agree on three of the four and disagree on one.**

- **9.a — `_gate-a-review.ts` and `_gate-a-times.ts` → DELETE** (agreeing with AC0). Both are genuinely one-shot: read-only database queries written for one past review. `_review` hard-codes a dev-specific record ID; `_times` hard-codes a list of six specific fixtures. Neither generalises.
- **9.b — `_gate-a-seed.ts` → PROMOTE into the committed suite** (agreeing with AC0). This is the real gap. It has **no committed equivalent**, it's the only way to stand up the fictional gatekeeper, and it's already well-behaved: it looks up rather than hard-coding IDs, and it hard-aborts if pointed at production.
- **9.c — `_gate-a-run.ts` → do NOT promote it as-is. Instead, extend the runner we already have** (this is where I differ from AC0). Promoting it would commit a **second** fixture-runner alongside `run-fixtures.ts` — duplicating the same file-parsing and pipeline-driving logic in two places that will drift apart. Your staging runbook already identifies the cleaner fix: **teach the committed `run-fixtures.ts` to take a folder argument** so it can run either set. That's a smaller change, removes a duplicate, and leaves one runner to maintain.
  - *One detail to carry over when it's built:* the held script's grading logic was written before the "runtime does the bookkeeping" change shipped, and one of its comments still describes the older world. The logic still works, but whoever does this should re-check it against how verdicts are recorded today rather than porting the comment.

## 10. The timing gate — nothing happens yet

**None of Queue 3 should be executed until the outstanding verification releases the dev-database hold.** The four files can stay exactly where they are until then; they're untracked but not ignored, so they're visible and harmless.

**The sequencing I'd recommend:** wait for the hold to lift → build 9.b and 9.c as one small development trip → **only then** delete all four held files, including the two being promoted (their content will by then live in committed form).

> 🔴⛔ **SUPERSEDED — THIS SEQUENCING WAS WRONG, AND THE CORRECTION IS TIME-CRITICAL. Do not follow the paragraph above.**
>
> Waiting for the hold would have missed Gate-B. The hold lifts *as part of* the Gate-B run-up, so the gap between "hold lifts" and "Gate-B needs the suite" is **near zero**.
>
> **The correction: build NOW, in parallel with the hold.** The hold protects DEV *data*; promoting a seeder and extending a runner touches *files*. **Writing a seeder is not running one.** The only constraint is: don't *execute* it against DEV until the hold lifts.
>
> **Gate-B is Friday 2026-08-07 and nothing committed can run the suite today.** → Part 2, **P5**, which also carries two build requirements that would otherwise break the suite the first time it is used.

> **Ballot 8:** **(a)** Delete the two forensics scripts, promote the seeder, and extend the existing runner rather than promoting the second one *(my recommendation)* · **(b)** AC0's original: delete two, promote both · **(c)** delete all four and accept the suite stays manual-only · **(d)** something else

---

# OUTSIDE MY REMIT — things a fully-loaded reader would want flagged

These are not in the three queues. I'm surfacing them because I was the first fully-loaded pair of eyes on this pile in a while. **All are observations only; I changed nothing.**

- **11.a — Three branches exist only on this machine and have never been pushed.** `ac5/ingest-fetch-resilience` (the live silent-email-drop lane, 3 commits), `dc/step35b-record-legibility` (1 commit), and `mira/step35d-trust-loaded-record`. Your own operating manual flags this exact class as the one a routine "am I pushed?" check cannot see — and it cost you seven commits of work once before. **Two of these carry real, unbacked-up work on active lanes.** Recommend pushing them for durability. *(Not urgent-urgent, but a machine failure today would lose them.)*
  - ⛔ **SUPERSEDED, BUT NOT CLOSED — re-verify rather than dismissing this.** All three named branches are **resolved** (worktrees reaped, work landed, AC5's lane merged). **But the risk class is live again:** `ac5/pre-refactor-quicklist` (at `79644d3`) **has no remote at all.** → Part 2, **P7**.
- **11.b — The cross-project status file is stale in two places.** It says the product repo's main line has "5 unpushed commits" and that a stale remote branch `origin/dc/webfetch-timeout` "still exists and should be deleted." **Neither is true now:** I measured the main line as exactly level with its remote, and queried the remote directly — that branch is already gone. Worth correcting so the next AC doesn't chase them.
  - ⛔ **SUPERSEDED — these specific numbers are stale twice over.** `main` is now **4 ahead** of origin, not level and not 5. **Re-measure before correcting anything**; the underlying point (stale status lines send the next AC chasing ghosts) still stands. → Part 2, **P9**.
- **11.c — The working-doc folder has 32 files, not the 31 the brief states.** Trivial, but the brief's count is off by one.
  - ⛔ **SUPERSEDED — moot.** The folder is now **24 files** after the retirements. Ignore both counts.
- **11.d — A documentation-hygiene gap you may already know about:** 30 of the 44 runtime source files carry no truncation canary, so an AI instance reading them cannot self-diagnose an incomplete load. AC0 flagged this as separately routed; noting that I hit it directly (I had to report end-line numbers instead).

---

## The full ballot, in one place

> ⛔ **ALL ANSWERED — this ballot is closed.** Justin ruled on 2, 3, 5, 6 and sent six items back for research (their answers are Part 2's **P1–P6**). **Ballots 9 and 10 below were never answered and are still open** — see Part 2, **P9**, which also explains a numbering collision that could otherwise hide them. Do not re-ask 1–8.

1. **Five extra live docs** — keep all five? **(a) yes** / **(b) keep some**
2. **Two rehome-then-retire docs** — proceed at my named homes? **(a) yes** / **(b) different homes** / **(c) leave**
3. **Twelve clean retirements** — **(a) all twelve** / **(b) all except ones I name**
4. **Edge cases** — 4.a **(a)** read-then-retire / **(b)** retire / **(c)** keep · 4.b **(a)** keep / **(b)** retire + fix code comments · 4.c **(a)** keep / **(b)** retire · 4.d **is the ACU lane live? yes/no** · 4.e+4.f **(a)** relocate to research / **(b)** retire / **(c)** leave
5. **Reap the 18 merged branches?** **(a) yes** / **(b) no**
6. **Reap the 5 superseded branches?** **(a) yes** / **(b) four only** / **(c) no**
7. **Leave `ac2/step57` alone, route to AC2?** **(a) yes** / **(b) other**
8. **Test scripts** — **(a) my recommendation** / **(b) AC0's** / **(c) delete all four** / **(d) other**
9. **Push the three never-pushed branches for durability?** **(a) yes** / **(b) no**
10. **Correct the two stale lines in the status file?** **(a) yes** / **(b) no**

---
---

# ═══════════ END OF PART 1 · PART 2 BEGINS ═══════════

# PART 2 — PIPELINE QUEUE FOR AC0-30 (this is the live half)

*Appended 2026-08-05T13:57-07:00 by ACh. Everything above this seam is settled record.*

**Justin's framing, verbatim:** *"anything that needs to get moved forward should go to the recommendations doc — I'll have AC0-30 pipeline it all. I don't want you to handle any of it, because then AC0-30 won't know about it. We're trying to stabilize everything but one thing that needs to run right now, so AC0-30 can decide what needs to land before that, vs what needs to be parked."*

**Your triage question on every item: LAND-BEFORE the in-flight work, or PARK?** Each carries a suggested call — *suggested, not decided.* You hold the cross-lane picture; I don't.

**Every open question below carries a locator** — where its source material lives *right now*, with enough substance inline that you can judge it without opening anything. Where a source is slated for retirement or relocation, the item says so and points at the destination instead.

**State verified 13:57 PT, not assumed:** AC5's lane merged + closed (`src/safety/fetch-retry.ts` on `main`); AC0-30's `#lock` `37a32db` in `evryn-backend`; `main` = `07609fb`, 4 ahead of origin; `origin` = `main` + `ac2/step57` only; worktrees = canonical + `_qc-sandbox` (detached) + `evryn-backend-quicklist`.

---

## §A — SETTLED RECORD (Justin ruled; do not re-open)

| Ballot | Ruling | Status |
|---|---|---|
| 1 — keep the 5 extra live docs | ✅ keep | **DONE — all five still in `docs/working/`** |
| 2 — rehome-then-retire the 2 conditional docs | ✅ yes, at the named homes | **DONE in part — see P8 for the half deliberately not done** |
| 3 — retire the 12 clean documents | ✅ yes, all twelve | **11 retired** — the ACU week plan kept per ballot 4.d. See **P10.b** |
| 4.a — AC3's 65KB brief | read it, then decide | **READ. Not clean → P2** |
| 4.b — health design doc | extract, then fix references | **→ P3** |
| 4.c — ACU product-radar findings | *(never answered)* | **→ P9** |
| 4.d — is the ACU lane live? | ✅ technically yes | **Both ACU docs KEPT** |
| 4.e/4.f — the 2 research docs | relocate, WITH pointers | **→ P4** |
| 5 + 6 — reap 23 branches | ✅ yes | **DONE — Justin executed it** |
| 7 — `ac2/step57` divergence | AC2 is gone; you work it out | **RESOLVED → P6** |
| 8 — the four test scripts | ✅ ACh's version | **→ P5, with the sequencing REVERSED** |
| 9 + 10 | *(never answered)* | **→ P9** |

**Branch reap, confirmed against the remote:** all 23 deleted; `origin/ac2/step57-runtime-bookkeeping` preserved. Complete, not pending.

**What ACh committed — 4 commits, every one staged by explicit path:**

| Commit | Repo | Contents |
|---|---|---|
| `3a9caaf` | `_evryn-meta` | 9 settled docs → `docs/sessions/historical/2026.07/` |
| `241ed63` | `_evryn-meta` | current-state entry (ACf cross-runtime check CLOSED) + 2 more retired |
| `07609fb` | `evryn-backend` | Fenwick legal ruling → `BUILD-EVRYN-v0.3.md` |
| `a9e670b` | `evryn-team-workspace` | Attributed notes in Nathan's + Soren's memory files |

`docs/working/`: **32 → 24 files.** Nothing pushed, merged, or deployed.

**The legal rescue, because it was the highest-stakes item:** the Fenwick ruling sat in **two** documents both slated for retirement, stated differently, and the better version was a *correction*. I rescued it as a **binding eligibility rule, not a tone rule** — on-behalf-of framing must be true *per message*, so **no inbound record → no on-behalf-of send**. Recorded as a tone note it would have been re-litigated. **It now lives at `evryn-backend/docs/BUILD-EVRYN-v0.3.md`**, in the Sender Outreach + Phase I Backlog section.

---

## ⏰ P5 IS THE URGENT ONE — READ IT FIRST

**Gate-B is Friday 2026-08-07. No committed code can run the test suite it requires.** Everything else in this queue can wait; that cannot. It is listed as P5 only to keep numbering stable — **treat it as P0.**

---

## §B — THE QUEUE

### 🔴 P5 (READ FIRST). Gate-B has no runnable suite — and the work can start NOW

**Suggested: LAND-BEFORE. Most time-critical item in this document.**

**What it is.** Gate-B is the mandatory pre-Mark live-fire rehearsal, targeted **Friday 2026-08-07**. The suite that drives it exists as **four uncommitted files** in `evryn-backend/tests/` — `_gate-a-seed.ts`, `_gate-a-run.ts`, `_gate-a-review.ts`, `_gate-a-times.ts` — each self-labeled *"EPHEMERAL — delete after use."* The fixture *data* is committed and permanent (13 fictional-gatekeeper emails, an answer key, a README; the build doc calls it a *"permanent regression suite"*).

**🔴 The gap, verified by grep:** **no committed file anywhere seeds the Megan gatekeeper record.** That capability exists only in the untracked seeder. **If those files are deleted as their own headers instruct, a suite we call permanent becomes unrunnable — two days before it is needed.**

**The agreed shape** (AC0 conceded to ACh's version; Justin deferred to the technical call): **promote the seeder; extend the existing `run-fixtures.ts` to take a folder argument.** Promoting the held runner instead would commit a *second* fixture-runner beside the existing one, duplicating file-parsing and pipeline-driving logic in two places that then drift. The committed runner already does ~90% of the job — it misses the Megan set only because it doesn't descend into their sub-folder. **`evryn-backend/docs/staging-runbook.md` independently identifies the same fix** (it notes the runner "does NOT read the `megan/` subdir yet"), which is corroboration rather than coincidence. The two forensics scripts (`_review`, `_times`) are genuinely one-shot and can be deleted.

**🔴 The sequencing correction — this reverses what Part 1 §10 says.** The worry was that the DEV hold blocked this. **It doesn't.** The hold protects DEV *data* — the Megan record, 3 frozen `processing` rows, 26 items, 58 cluster-test gatekeepers. Promoting a seeder and extending a runner touches **files**. **Writing a seeder is not running one.** Build now; just don't *execute* it against DEV until the hold lifts. That collapses the near-zero window entirely.

**🔴 Two build requirements — both bite at Gate-B, not in review:**
1. **The seeder must call `assertNotProd()`** from `evryn-backend/src/db/env-guard.ts`. It is inherently destructive. *(The held version already hard-aborts on production — preserve that rather than re-deriving it.)*
2. **`_gate-a-review.ts` hardcodes `MEGAN_ID` (`43a68cfa-d6c6-4088-b86b-38a5d6583669`); `_gate-a-times.ts` hardcodes six fixture UUIDs.** A reseed generates **new** IDs, so promoting the seeder as-is silently breaks the reviewer the first time it is genuinely used. Either the seeder emits its IDs downstream, or lookup goes by a stable key (Megan's email) rather than a literal UUID. **This is the failure that surfaces at 9am Friday.**

*Also carry:* the held runner's grading logic predates the "runtime does the bookkeeping" change and one comment still describes the older world. Logic works; re-check it against how verdicts are recorded today rather than porting the stale comment.

**Breadcrumbing — Justin's explicit condition:** once promoted, the seeder and extended runner must be findable **from where Gate-B is actually run** — `evryn-backend/docs/staging-runbook.md` and the SPRINT Step 88B entry — *"so nobody arrives at Gate-B unable to find the suite."*

**Locator:** the four files are untracked (not ignored) in `evryn-backend/tests/` — visible via `git status`. Nothing about this item depends on a doc that is disappearing.

**Next action + owner: AC0-30 commissions a DC trip now.** ACh doesn't write deterministic code.

---

### P1. 🔴 The operator guide is wrong about `/health` — check AC5's work before commissioning a fix

**Suggested: LAND-BEFORE, after resolving the duplication question.**

**What it is.** Step 78 changed `/health` to return **503 when wedged** and **200 when M1 has halted it**, plus a `checks` block naming the unhappy subsystem. That shipped to production in `v0.2.8`. Two operator-guide touch-ups were tracked as owed *at deploy*. **Neither was made.**

**The gaps, verified by grep 2026-08-05 — locator is a live file, not a retiring one:**
- **`evryn-backend/docs/operator-guide.md:90`** — the "Verify Deployment" checklist still says to expect `{"status":"ok","uptime":...}`, the **pre-Step-78 shape**.
- **`HEALTH_WEDGED_THRESHOLD_MS` appears zero times** in that file, including its env-var table.
- Its three `wedged`/`halted` mentions (lines 42, 169, 181) are all about Healthchecks.io and M1 conceptually. **None documents the `/health` contract.**

**What breaks if ignored.** This is the sheet Justin reads *during a deploy*, under time pressure, and he is not an engineer. As written it inverts two readings: a legitimate **503 reads as "the deploy failed,"** and a **`halted` 200 reads as "all fine"** when polling is stopped and mail is not ingesting. Gate-B is Friday.

**⚠️ Justin's steer — verbatim, and the reason this routes to you and not to a DC:**
> *"I'm fairly certain AC5 is dealing with possibly this exact problem at the moment — but he'll know if this is something different or the same."*

**Next action + owner: AC0-30.** Resolve against **AC5's just-merged Step 96 work first.** Do **not** spec a standalone fix — it may duplicate what already shipped. Question to answer: *did AC5's close-out already correct this, or is it genuinely untouched?* Only if untouched does it become a DC job.

---

### P2. 🔴 AC3's 65KB brief is NOT clean — six un-rehomed items, reproduced here so you never open it

**Suggested: PARK the brief; item (i) is P1 and lands before.**

**Locator:** `_evryn-meta/docs/working/2026.06.30-ac0-ac3-identity-ops-brief.md`, 271 lines. Its "Owed / routed to AC0" list is **lines 232–240**. ⚠️ **This document is NOT being retired** — see the marker at Part 1 §4.a — so the source stays available. But the substance is reproduced below so you don't need it.

**Three items confirmed ALREADY rehomed** — listed so nobody redoes them: the subagent-resume finding (in AC's CLAUDE.md, twice), the three CLAUDE.md lessons (consolidation rule, DID-vs-RECOMMENDED, check-agent-memory-before-routing), and the three-dot-diff lesson (in `_evryn-meta/AGENT_PATTERNS.md`).

**The six NOT rehomed** — each verified absent by grep:

**(i) The operator-guide `/health` debt** → this is **P1**. Originated here. Highest stakes.

**(ii) 🔴 A QC pattern Justin ALREADY APPROVED that was simply never written.** *(Brief line 235. No approval needed — it needs a writer.)*
> **Destination:** `evryn-quality/CLAUDE.md` → "Patterns This Role Watches For" (the list runs ~lines 152–222; append in the same Trigger/Failure/Move shape).
> **Pattern:** *A status endpoint whose **decision logic** is unit-tested but whose **assembled handler** lives in a boot-on-import module.* **Failure:** the decision function is covered, but the wiring that turns it into an HTTP response — status code, body shape, header — is never exercised, because importing the module boots the app. Coverage looks good and the wiring is untested. **Move:** when a handler's logic is extracted for testability, check whether the *composition* is reachable by a test without booting; if not, the extraction only half-solved it. **Seen 1×:** Step 78's `/health` — resolved by lifting composition into `src/safety/health.ts` behind injected deps.

**(iii) QC pattern — PENDING JUSTIN'S APPROVAL.** *(Brief line 238.)*
> **Pattern:** *A client that treats a non-2xx response as "no data" when the error response's **body carries the payload**.* **Failure:** `if (!res.ok) return null` throws away exactly the diagnostic state the UI exists to surface — the endpoint signals the fault via status code *and* explains it in the body, and the client discards the explanation, so the display blanks in precisely the state it was built for. **Move:** for any fetch wrapper reading a status/health/diagnostic endpoint, ask "does this signal failure via HTTP status while carrying detail in the body?" If yes, the `!res.ok` bail is a bug — parse the body first, reserve `null` for genuine transport failure. **Not** a concern where error bodies are empty/opaque. **Seen 1×:** the dashboard `/health` client would have blanked all four subsystem lights on the 503 whose body held those very checks.

**(iv) QC pattern — PENDING JUSTIN'S APPROVAL.** *(Brief line 239.)*
> **Pattern:** *A UI surfacing several subsystem statuses where one subsystem's failure **deliberately causes** another's.* **Failure:** the derived failure renders as its own independent alarm, so one root cause displays as N faults and the operator chases a phantom — at 3am, *"M1 halted, as designed"* vs *"M1 halted **and** the poll is broken too."* **Move:** enumerate every pair of surfaced signals, ask "can one deliberately cause the other?", and for each such pair verify **both** directions — the derived signal renders as an expected consequence with causality named, **and** that same signal occurring *independently* still alarms at full severity. *(A rule that softens a genuine alarm is worse than no rule.)* Check that presentation precedence matches the backend's decision precedence. **Seen 1×:** an M1 trip calls `stopPolling()` → poll staleness is caused-and-expected → amber; a stale poll with the breaker untripped is a real wedge → stays red.

**⚠️ Do not confuse (iii)/(iv) with the pattern that DID land.** `evryn-quality/CLAUDE.md:198` holds *"a presentation/UI layer that re-derives a backend DECISION"* — a **different shape**, from a later trip (commit `743e757`). Three patterns total: one landed, two pending, plus (ii) approved-but-unwritten.

**(v) A CLAUDE.md rule proposal — PENDING JUSTIN'S APPROVAL.** *(Brief line 234.)* Justin's own "clean up as we go" ruling: knock out small things while you're already in the file rather than growing a todo list. **Destination if approved:** `_evryn-meta/CLAUDE.md`. Verified absent today.

**(vi) A minor orchestration lesson.** *(Brief line 240.)* Line counts drift mid-session in a multi-agent repo — a brief said `current-state.md` was 149 lines; by the time QC read it, 155. **Move:** count close to spin-time, and tell the subagent to trust its own read over the brief's number and flag the delta. **Destination:** `docs/protocols/ac-orchestration-protocol.md`.

**Next action + owner: AC0-30.** Route (ii) to be written — it has approval already. Put (iii), (iv), (v) to Justin as a yes/no; the text above is what he's approving. **Do not retire this brief until all six are placed** — retiring it is what makes them invisible.

---

### P3. The Step-78 health design doc — extract, fix a live code reference, then retire

**Suggested: PARK unless P1 forces it open. Small and self-contained.**

**Locator:** `_evryn-meta/docs/working/2026.07.15-ac3-step78-real-health-design.md`, 101 lines. ⚠️ **This one IS slated for retirement — but it cannot simply go, because shipped runtime code cites it by path:** `evryn-backend/src/safety/liveness.ts:5`. **The full load-bearing content is reproduced below, so the retirement is not blocked on re-reading it.**

**Justin's ruling, verbatim:** *"If there's important info in there, let's extract that (assuming there's all kinds of session noise that's not necessary) to its proper home, and fix references."*

**The rule is deliberately counter-intuitive and marked "do not fix this":** a breaker halt returns **200, not 503**.

🔴 **The reasoning must travel with the rule, or the next engineer re-derives it wrongly.** M1's invariant is that it **never auto-resets** — restart *is* the manual reset. A halt calls `stopPolling()`, so the poll clock goes stale ~15 minutes later. If `/health` returned 503 on that **and** a watchdog auto-restarted, the cause would still be present → re-trip → 503 → restart → **infinite restart loop**. So a deliberate halt returns **200** (dashboard shows red, the emergency alert already fired, nothing auto-restarts), while a genuine **wedge** — stuck with the breaker *not* tripped — returns **503**, where a restart genuinely helps. **Precedence is load-bearing: halted is evaluated BEFORE wedged in `decideHealthStatus`.** A startup grace prevents a normal ~30s restart window from 503-ing. *(Arming a Railway watchdog on the 503 remains a separate operator decision Justin owns; nothing auto-restarts today.)*

**Proposed home: `evryn-backend/docs/ARCHITECTURE.md` §"Monitoring & Silent-Death Safety (M1)" (line 1151)** — where a reader already goes for M1 semantics, putting the rule beside the invariant it depends on. **Your call; propose otherwise if you prefer.**

**Next action + owner: AC0-30 approves the home, then ONE change does all three** — (1) extract rule + reasoning into ARCHITECTURE, (2) repoint the `liveness.ts:5` comment, (3) retire the doc. **Doing (3) without (2) breaks a live code reference.**

---

### P4. Two research documents — relocate WITH breadcrumbs (has an ownership catch)

**Suggested: PARK — it reaches into a lane Justin says is his to close.**

**Locator — both currently in `_evryn-meta/docs/working/`:**
- `2026.07.13-acu-ac0-step53-identity-loading-research.md` — supports SPRINT Step 53, which was **set aside, not closed**.
- `2026.07.13-acu-acu-outreach-engine-research.md` — supports v0.3, which hasn't started.

**Justin's ruling:** relocate to `evryn-team-workspace/shared/projects/product/research/` (directory exists — verified). **His condition is the important half:** *"make sure that there are pointers — otherwise this is just **lost** research."* A relocation without breadcrumbs is a slower way of losing it.

**🔴 The catch that stopped ACh executing.** I grepped every inbound reference. Both are referenced by **`_evryn-meta/docs/working/2026.07.14-acu-acu-self-brief.md`** — an ACU document Justin says **stays**, because that lane is live. So:
- Moving the files **breaks ACU's own master doc** unless those two links are updated in the same commit.
- But Justin also said of that lane: *"I'll have **him** manage anything downstream of him."* Editing ACU's self-brief is plausibly ACU's call, not a housekeeping subagent's.

Outside ACU's self-brief and this document, the outreach-engine research has **no other inbound reference at all** — a move without breadcrumbs orphans it completely.

**Suggested breadcrumb targets** (minimum: the sprint step each supports): step53 research → the SPRINT Step 53 entry; outreach research → `evryn-backend/docs/BUILD-EVRYN-v0.3.md`, Sender Outreach section — where the Fenwick ruling now lives, so the two sit together.

**Next action + owner: AC0-30 decides who moves them** — you, ACU, or Justin — and ensures the self-brief links are fixed **in the same commit as the move**.

---

### P6. The diverged `ac2/step57` branch — superseded rebuild, safe to delete

**Suggested: PARK (harmless). Analysis is done; don't repeat it.**

**Locator:** `origin/ac2/step57-runtime-bookkeeping` — the one branch deliberately spared in the reap; still on the remote. Nothing about this depends on a doc.

**What it is.** It shows **22 ahead / 6 behind** its remote twin, which *looks* like unlanded work. Justin ruled reconciliation was ACh's because the original AC2 session is long closed: *"AC2 is no longer in position to work this out."*

**Verdict: a superseded rebuild, not lost work.** I didn't trust the counts — **a line count cannot distinguish an older draft from an absent capability, in either direction.** I compared substance: the key file `test-step57-dev.ts` is **larger on `main`** (47,504 bytes vs 44,309 on the branch), and the remaining "unique" files are round-2 renames of work already landed. **`main` carries the newer version.** The Step 57 content shipped and is deployed in `v0.2.8`.

**Next action + owner: AC0-30.** Deletion is safe on this analysis; ACh recommended rather than executed. Low urgency.

---

### P7. Outstanding pushes — three repos, deliberately left for you

**Suggested: your call — these carry other agents' commits, including your own.**

ACh pushed nothing. As of 13:57 PT:

| Repo | Unpushed |
|---|---|
| `evryn-backend` (`main`) | **4 ahead** of origin (tip `07609fb`) |
| `_evryn-meta` (`main`) | **8 ahead** of origin |
| `evryn-team-workspace` (`main`) | **2 ahead** of origin |

**The earlier hold no longer applies.** Justin's *"leave AC5's stuff, including his unpushed commits"* was issued while that lane was open. **AC5's material is now merged into `main`**, so it no longer blocks these. Still your call.

⚠️ **`_evryn-meta`'s push IS the Vercel dashboard deploy.** Pushing that repo deploys the dashboard along with the whole backlog. A Justin-gated act, not incidental.

**🔴 A correction to ACh's own earlier alarm, because a stale alarm is worse than none.** The three branches named in Part 1 §11.a are **resolved**. **But the class is live again:** `ac5/pre-refactor-quicklist` (at `79644d3`, in worktrees `evryn-backend-quicklist` and `_qc-sandbox`) **has no remote at all** — it exists only on this machine. If AC5's lane is closed, confirm that branch's content is landed or intentionally disposable. **A routine `git status` on `main` cannot see this class** — enumerate all local branches to catch it.

---

### P8. Soren's unsettled design batch — HELD, deliberately not swept

**Suggested: PARK until Soren or Justin rules.**

**Locator:** `_evryn-meta/docs/working/2026.07.22-ac0-ac0-handoff.md`, the batch at **lines 160–221**; Soren's prohibition at **line 166**. ⚠️ **This document is HELD, not retired** — the source is safe to read.

**What it is.** Part of the rehome-then-retire item was to move Soren's v0.3 design batch (bulk outreach, the Publisher safety-gate, fill-laundering security) into the v0.3 build doc. **ACh did not do it, and that was deliberate.**

**Why.** Reading the source rather than a summary, Soren's own text forbids exactly that move:
> *"⛔ NOTHING IN THIS THIRD BATCH IS SETTLED, AND NONE OF IT SHOULD BE SWEPT INTO A LANE... It is deliberately **NOT** written into `ARCHITECTURE.md` or the BUILD docs... **Consequence if you sweep it:** you'd be building against a design that was still moving when it was written down."*

He had already rehomed the durable parts himself, and explicitly noted **Nathan's eligibility rule was the one thing left intact** — which is precisely the piece ACh *did* rescue (§A).

**Consequence:** that handoff stays in `docs/working/`. ACh recorded the reason in Soren's memory file so he isn't surprised to find it.

**Next action + owner: AC0-30 routes to Soren (or Justin)** for a ruling on whether the batch is settled enough to land. Nothing breaks while it waits.

---

### P9. Three questions never answered — and a numbering collision that could hide them

**Suggested: needs Justin. Low urgency, easy to lose.**

⚠️ **The collision:** ACh's final *chat* report used different numbering from this *document*. "Ballot 10" in chat meant *"persist the research"* — **answered yes**, producing Part 2. **This document's** ballot 10 was a different question. Both originals remain open:

- **Doc ballot 9 — "Push the never-pushed branches for durability?"** Partly overtaken; the live instance is now `ac5/pre-refactor-quicklist`. **→ P7.** **Locator:** no doc needed — enumerate local branches in `evryn-backend`.
- **Doc ballot 10 — "Correct the two stale lines in the cross-project status file?"** **Locator: `_evryn-meta/docs/current-state.md`** (a live file, not retiring). It claimed the product repo had "5 unpushed commits" and that `origin/dc/webfetch-timeout` still existed. ⚠️ **Both numbers are now stale twice over** — `main` is 4 ahead, and that branch is gone. **Re-measure before editing.** The principle stands: stale status lines send the next AC chasing ghosts.
- **Ballot 4.c — ACU's product-radar findings** (`_evryn-meta/docs/working/2026.07.16-acu-ac0-product-radar-findings.md`, kept, not retired). The sprint plan cites it as provenance for two findings; one shipped, **the other — the database-schema reference being badly out of date — is still open.** Retire once that closes.

---

### P10. Process flags — two things you should know; neither is damage

**Suggested: read once; no action unless you disagree.**

**(a) 🔴 ACh wrote to two team-memory files without the protocol's auth step.** Justin's instruction required attributed notes in Nathan's and Soren's `MEMORY.md`, and I wrote them (commit `a9e670b`). **Afterwards** — while reading an unrelated doc — I found that team-memory writes are supposed to be preceded by Justin's live authorization and a Slack ping, and that those files have CRLF line endings the Edit tool reportedly mangles.

**I verified rather than assumed:** both canaries are still the last line, each file has exactly one `Note from ACh`, both remain fully CRLF (247/247 and 141/141 lines), and both diffs were additions-only. **No damage.** But these are Nathan's and Soren's source-of-truth documents and a step was skipped in their domain. **Locator:** `evryn-team-workspace/.claude/agent-memory/{nathan,soren}/MEMORY.md`, notes appended at end of file. If convention puts such notes in a specific section, correct it while it's fresh.

**(b) A contradiction inside Justin's own answers, and its resolution.** He cleared *"all twelve"* retirements, then said the ACU lane is live and **both ACU documents stay** — one of which was among the twelve. **ACh took the conservative reading: retired 11, kept the ACU week plan.** Recorded so nobody re-opens it as an error or "finishes" the twelfth.

---

### P11. Carried-forward observation

**Suggested: PARK. Recorded so it survives.**

**Truncation canaries are missing from 30 of the 44 runtime source files** in `evryn-backend/src/`, so an AI instance reading one cannot self-diagnose an incomplete load. AC0 flagged this as separately routed; ACh hit it directly (had to report end-line numbers instead of relying on a canary). **Still open as far as I can determine.**

---

## §C — HOW ACh WORKED, SO YOU CAN CALIBRATE TRUST

- **Read-only** outside two explicitly-cleared items. Every commit staged **by explicit path** with `git diff` run first — other agents were committing into `docs/working/` throughout.
- **Nothing pushed, merged, or deployed.**
- **Verification changed two answers.** The `ac2/step57` divergence (mechanical signals said "unlanded"; substance said "superseded") and the never-pushed alarm (I was told it was obsolete — the original three are, but a new one exists). Both would have been recorded wrongly had I accepted the summary I was handed.
- **A mechanical check that comes back clean is not evidence the underlying thing is safe.** `git branch --merged` misleads on a stale base; a passing grep proves a string is absent, not that a concept is handled. Where possible I checked substance — byte sizes, file contents, the remote itself.

**The risk this document closes:** everything in Part 2 existed **only in a subagent transcript**. Six research answers, six un-rehomed items, two process flags, and a live Gate-B blocker would all have died at compaction. If further items surface in this lane, they need a durable home the same day — a handoff is where a finding is captured under pressure, never where it lives.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
