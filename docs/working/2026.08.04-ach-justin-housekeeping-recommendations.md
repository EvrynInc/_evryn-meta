# Housekeeping lane — pipeline queue for AC0-30

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload before relying on this doc.

**How to use this document.** This is a **work queue handed to AC0-30**, not a ballot. Justin has already ruled on everything that needed his judgment (§A — settled record, do not re-open). What remains is §B: **eleven pipeline items**, each written to stand alone so you can act on it without reading anything else and without re-reading a 65KB source doc.

**Justin's framing, verbatim (2026-08-05):** *"anything that needs to get moved forward should go to the recommendations doc — I'll have AC0-30 pipeline it all. I don't want you to handle any of it, because then AC0-30 won't know about it. We're trying to stabilize everything but one thing that needs to run right now, so AC0-30 can decide what needs to land before that, vs what needs to be parked."*

**So your triage question on every item below is:** *does this need to LAND before the in-flight work, or does it PARK?* Each item carries a suggested call — **suggested, not decided.** You hold the cross-lane picture; I don't.

**Author:** ACh, a housekeeping subagent under AC0, 2026-08-05T13:57-07:00. I was read-only outside two explicitly-cleared items, and I did not push, merge, or deploy anything. Several items below were things I *could* have quietly executed; Justin's instruction was that they route to you instead, specifically so you know they exist.

**State this doc was written against — verified at 13:57 PT, not assumed:** AC5's lane is merged and closed (`src/safety/fetch-retry.ts` is on `main`); AC0-30's `#lock` `37a32db` is in `evryn-backend`; `evryn-backend` `main` = `07609fb`, 4 ahead of origin; `origin` carries only `main` + `ac2/step57-runtime-bookkeeping`; worktrees are canonical + `_qc-sandbox` (detached) + `evryn-backend-quicklist`.

---

## §A — SETTLED RECORD (Justin ruled; do not re-open)

| Item | Ruling | Status |
|---|---|---|
| Reap the 18 merged branches | ✅ yes | **DONE — Justin executed it himself** |
| Reap the 5 superseded-rebuild branches | ✅ yes, all five | **DONE — same push** |
| Retire the 12 clean documents | ✅ yes | **DONE — 11 retired; see P10.b for why 11, not 12** |
| Rehome-then-retire the 2 conditional docs | ✅ yes, at the named homes | **DONE in part — see P8 for the half deliberately not done** |
| Is the ACU lane still live? | Technically yes — Justin will close it out himself | **Both ACU docs KEPT** |
| Persist all research into this doc | ✅ yes | **This document** |

**Branch reap outcome, confirmed against the remote:** all 23 remote branches deleted. `origin/ac2/step57-runtime-bookkeeping` correctly preserved. Remote is now `main` + that one branch. **This is complete, not pending.**

**What I committed — 4 commits, every one staged by explicit path:**

| Commit | Repo | Contents |
|---|---|---|
| `3a9caaf` | `_evryn-meta` | 9 settled docs → `docs/sessions/historical/2026.07/` |
| `241ed63` | `_evryn-meta` | current-state entry (ACf cross-runtime check CLOSED) + 2 more docs retired |
| `07609fb` | `evryn-backend` | Fenwick legal ruling → `BUILD-EVRYN-v0.3.md` |
| `a9e670b` | `evryn-team-workspace` | Attributed notes in Nathan's + Soren's memory files |

`docs/working/` went **32 → 24 files**.

**On the legal rescue, because it was the highest-stakes item:** the Fenwick ruling existed in **two** documents that were *both* slated for retirement, stated differently, and the better version was a *correction*. I rescued the corrected version and wrote it as a **binding eligibility rule rather than a tone rule** — on-behalf-of framing must be true *per message*, so **no inbound record → no on-behalf-of send**. That distinction is the entire ruling; recorded as a tone note it would have been re-litigated from scratch.

---

## §B — THE PIPELINE QUEUE

### P1. 🔴 The operator guide is wrong about `/health` — but check AC5's work before commissioning anything

**Suggested: LAND-BEFORE — after you resolve the duplication question.**

**What it is.** Step 78 changed the `/health` endpoint so it returns **503 when the system is wedged** and **200 when M1 has halted it**, plus a `checks` block naming which subsystem is unhappy. That shipped to production in `v0.2.8`. Two operator-guide touch-ups were tracked as owed *at deploy time*. **Neither was ever made.**

**The concrete gaps, verified by grep 2026-08-05:**
- `evryn-backend/docs/operator-guide.md:90` — the "Verify Deployment" checklist still tells Justin to expect `{"status":"ok","uptime":...}`, the **pre-Step-78 shape**.
- `HEALTH_WEDGED_THRESHOLD_MS` appears **zero times** in the guide, including its env-var table.
- The guide's three `wedged`/`halted` mentions are all about Healthchecks.io and M1 conceptually. **None documents the `/health` contract.**

**Why it matters / what breaks if ignored.** This is the sheet Justin reads *during a deploy*, under time pressure, and he is not an engineer. As written it inverts two readings: a legitimate **503 reads as "the deploy failed,"** and a **`halted` 200 reads as "all fine"** when polling is stopped and mail is not ingesting. Gate-B is targeted **Friday 2026-08-07**.

**⚠️ Justin's steer — verbatim, and the reason this routes to you rather than to a DC:**
> *"I'm fairly certain AC5 is dealing with possibly this exact problem at the moment — but he'll know if this is something different or the same."*

**Next action + owner: AC0-30.** Resolve this **against AC5's just-merged Step 96 work before commissioning any fix.** Do **not** spec a standalone fix — it may duplicate something already shipped. The question to answer first: *did AC5's close-out already correct the guide's health section, or is this genuinely untouched?* Only if untouched does it become a DC job.

---

### P2. 🔴 AC3's 65KB brief is NOT clean — six un-rehomed items, enumerated so you never open the 65KB

**Suggested: PARK the brief; LAND item (i), which is P1.**

**What it is.** `_evryn-meta/docs/working/2026.06.30-ac0-ac3-identity-ops-brief.md`, the identity/ops lane brief, 271 lines. Justin sent it back because it's the same shape as a handoff that turned out to be hiding live items. **He was right.** I read it end-to-end. It is ~90% properly consolidated, but its "Owed / routed to AC0" tail was never worked.

**Three items I confirmed ARE safely rehomed** — listed so nobody redoes them: the subagent-resume finding (in AC's CLAUDE.md, twice), the three CLAUDE.md lessons (consolidation rule, DID-vs-RECOMMENDED, check-agent-memory-before-routing — all present), and the three-dot-diff lesson (in `AGENT_PATTERNS.md`).

**The six that are NOT rehomed** — each verified absent by grep:

- **(i) The operator-guide `/health` debt.** This is **P1 above** — it originated here. Highest stakes of the six.
- **(ii) A QC pattern Justin ALREADY APPROVED that was simply never written.** *"A status endpoint whose decision logic is unit-tested but whose assembled handler lives in a boot-on-import module = untested wiring."* Belongs in `evryn-quality/CLAUDE.md` → "Patterns This Role Watches For". It has approval; it needs a writer.
- **(iii) QC pattern, pending Justin's approval.** *A client that treats a non-2xx response as "no data" when the error response's **body carries the payload**.* Failure: `if (!res.ok) return null` discards exactly the diagnostic state the UI exists to display — the dashboard would have blanked all four health lights on the 503 whose body held those very checks.
- **(iv) QC pattern, pending Justin's approval.** *A UI surfacing several subsystem statuses where one subsystem's failure **deliberately causes** another's.* Failure: one root cause renders as N independent alarms and the operator chases a phantom at 3am. The move is to verify **both** directions — the derived signal reads as an expected consequence, **and** that same signal occurring independently still alarms at full severity.
- **(v) A CLAUDE.md rule proposal.** Justin's "clean up as we go" ruling — knock out small things while you're already in the file rather than growing a todo list.
- **(vi) A minor orchestration lesson.** Line counts drift mid-session in a multi-agent repo; brief subagents to trust their own read over the brief's stated count and flag the delta.

**⚠️ Do not confuse (iii)/(iv) with the pattern that DID land.** `evryn-quality/CLAUDE.md:198` holds *"a presentation/UI layer that re-derives a backend DECISION"* — that came from a later trip (commit `743e757`) and was never on this owed list. It is a **different shape** from (iv). Three patterns total: one landed, two outstanding.

**Next action + owner: AC0-30.** Route (ii) to be written — it already has approval. Put (iii), (iv), (v) to Justin for a yes/no. **Do not retire this brief until its six items are placed** — retiring it is precisely what makes them invisible.

---

### P3. The Step-78 health design doc — extract, fix a live code reference, then retire

**Suggested: PARK unless P1 forces it open. Small and self-contained.**

**What it is.** `_evryn-meta/docs/working/2026.07.15-ac3-step78-real-health-design.md`, 101 lines. A working doc — but **shipped runtime code cites it by path**: `evryn-backend/src/safety/liveness.ts:5`. Retiring it would leave a dangling reference in production code.

**Justin's ruling, verbatim:** *"If there's important info in there, let's extract that (assuming there's all kinds of session noise that's not necessary) to its proper home, and fix references."*

**The load-bearing content is small and deliberately counter-intuitive:** a breaker halt returns **200, not 503** — and it is marked "do not fix this," because it looks like a bug.

🔴 **The reasoning must travel with the rule, or the next engineer re-derives it wrongly.** M1's invariant is that it **never auto-resets** — restart *is* the manual reset. A halt calls `stopPolling()`, so the poll clock goes stale ~15 minutes later. If `/health` returned 503 on that **and** a watchdog auto-restarted, the cause would still be present → re-trip → 503 → restart → **infinite restart loop**. So a deliberate halt returns **200** (dashboard shows red, the emergency alert already fired, nothing auto-restarts), while a genuine **wedge** — stuck with the breaker *not* tripped — returns **503**, where a restart genuinely helps. **Precedence is load-bearing: halted is evaluated BEFORE wedged in `decideHealthStatus`.** A startup grace also prevents a normal ~30s restart window from 503-ing.

**My proposed home: `evryn-backend/docs/ARCHITECTURE.md` §"Monitoring & Silent-Death Safety (M1)" (line 1151)** — where a reader already goes for M1 semantics, putting the rule beside the invariant it depends on.

**Next action + owner: AC0-30 approves the home, then one small trip does all three in a single change** — (1) extract rule + reasoning into ARCHITECTURE, (2) update the `liveness.ts:5` comment to the new location, (3) retire the design doc. **Doing (3) without (2) breaks a live code reference.**

---

### P4. Two research documents — relocate WITH breadcrumbs (carries an ownership catch)

**Suggested: PARK — it reaches into a lane Justin says is his to close.**

**What it is.** Two ACU research docs in `docs/working/`:
- `2026.07.13-acu-ac0-step53-identity-loading-research.md`
- `2026.07.13-acu-acu-outreach-engine-research.md`

**Justin's ruling:** relocate to `evryn-team-workspace/shared/projects/product/research/` (directory exists — verified). **His condition is the important half:** *"make sure that there are pointers — otherwise this is just **lost** research."* A relocation without breadcrumbs is a slower way of losing it.

**🔴 The catch that stopped me executing.** I grepped every inbound reference. Both are referenced by **`2026.07.14-acu-acu-self-brief.md`** — an ACU document Justin says **stays**, because that lane is live. So:
- Moving the files **breaks ACU's own master doc** unless those two links are updated in the same commit.
- But Justin also said of that lane: *"I'll have **him** manage anything downstream of him."* Editing ACU's self-brief is plausibly ACU's call, not a housekeeping subagent's.

Outside ACU's self-brief and this document, the outreach-engine research has **no other inbound reference at all** — so a move without breadcrumbs orphans it completely.

**Suggested breadcrumb targets** (minimum: the sprint step each supports): step53 identity-loading research → the SPRINT Step 53 entry; outreach-engine research → `evryn-backend/docs/BUILD-EVRYN-v0.3.md` in the Sender Outreach section, which is where the Fenwick legal ruling now lives, so the two sit together.

**Next action + owner: AC0-30 decides who moves them** — you, ACU, or Justin — and ensures the ACU self-brief links are fixed **in the same commit as the move**.

---

### P5. 🔴 Gate-B needs a runnable suite and nothing committed can run one — build it NOW, don't wait for the hold

**Suggested: LAND-BEFORE. The most time-critical item in this queue.**

**What it is.** Gate-B is the mandatory pre-Mark live-fire rehearsal, targeted **Friday 2026-08-07**. The suite that drives it exists as **four uncommitted files** in `evryn-backend/tests/` (`_gate-a-seed.ts`, `_gate-a-run.ts`, `_gate-a-review.ts`, `_gate-a-times.ts`), each self-labelled "EPHEMERAL — delete after use." The fixture *data* is committed and permanent (13 fictional-gatekeeper emails, an answer key, a README; the build doc calls it a "permanent regression suite"). **But I verified by grep that no committed file seeds the Megan gatekeeper record** — that capability exists only in the untracked seeder. **If those files are deleted as their own headers instruct, a suite we call permanent becomes unrunnable.**

**The agreed shape** — AC0 conceded to my recommendation, and Justin deferred to the technical call: **promote the seeder; extend the existing `run-fixtures.ts` to take a folder argument.** Promoting the held runner instead would commit a *second* fixture-runner alongside the existing one, duplicating file-parsing and pipeline-driving logic in two places that then drift. The committed runner already does ~90% of the job — it misses the Megan set only because it doesn't descend into their sub-folder. **The staging runbook independently identifies this same fix**, which is corroboration rather than coincidence. The two forensics scripts (`_review`, `_times`) are genuinely one-shot and can be deleted.

**🔴 The sequencing question — I re-examined it, and the dependency AC0 feared does not exist.**

The worry was: I'd recommended *wait for the DEV hold to lift → then build*. But the hold lifts as part of the Gate-B run-up, so the window between "hold lifts" and "Gate-B needs the suite" could be **near zero**.

**That concern is correct, and the resolution is clean: build now, in parallel with the hold.** The DEV hold protects *data* — the Megan record, 3 frozen `processing` rows, 26 items, 58 cluster-test gatekeepers. Promoting a seeder and extending a runner touches **files**, not that data. **Writing a seeder is not running one.** The only real constraint: **do not execute the seeder against DEV until the hold lifts.** That collapses the zero-width window and de-risks Friday entirely.

**🔴 Two hard requirements for whoever builds it — both bite at Gate-B, not in review:**
1. **The seeder must call `assertNotProd()`** from `src/db/env-guard.ts`. It is inherently destructive and that guard exists for exactly this. *(The held version already hard-aborts on production; preserve that behavior rather than re-deriving it.)*
2. **`_gate-a-review.ts` hardcodes `MEGAN_ID` (`43a68cfa-d6c6-4088-b86b-38a5d6583669`) and `_gate-a-times.ts` hardcodes six fixture UUIDs.** A reseed generates **new** IDs — so promoting the seeder as-is silently breaks the reviewer the first time it is genuinely used. Either the seeder emits its IDs for downstream consumption, or the review/times logic looks Megan up by a stable key (her email) rather than a literal UUID. **This is the failure that surfaces at 9am Friday.**

*One more detail to carry:* the held runner's grading logic predates the "runtime does the bookkeeping" change, and one of its comments still describes the older world. The logic works; whoever builds this should re-check it against how verdicts are recorded today rather than porting the stale comment.

**Breadcrumbing — Justin's explicit condition on this item:** once promoted, the seeder and extended runner must be findable **from wherever Gate-B is actually run** — `evryn-backend/docs/staging-runbook.md` and the SPRINT Step 88B entry — *"so nobody arrives at Gate-B unable to find the suite."*

**Next action + owner: AC0-30 commissions a DC trip now.** I don't write deterministic code, and this needs to exist before Friday regardless of the hold.

---

### P6. The diverged `ac2/step57` branch — superseded rebuild, safe to delete

**Suggested: PARK (harmless) — but the analysis is done and need not be repeated.**

**What it is.** `ac2/step57-runtime-bookkeeping` is the one branch deliberately spared in the reap. It shows **22 ahead / 6 behind** its remote twin, which *looks* like unlanded work. Justin ruled the reconciliation was mine because the original AC2 session is long closed: *"AC2 is no longer in position to work this out."*

**What the divergence actually is: a superseded rebuild, not lost work.** I did not trust the counts — **a line count cannot distinguish an older draft from an absent capability, in either direction.** I compared substance instead: the key file `test-step57-dev.ts` is **larger on `main`** (47,504 bytes vs 44,309 on the branch), and the remaining "unique" files are round-2 renames of work already landed. **`main` carries the newer version.** The Step 57 content shipped and is deployed in `v0.2.8`.

**Next action + owner: AC0-30.** Deletion is safe on this analysis. I recommended rather than executed, per instruction. Low urgency — leaving it costs nothing.

---

### P7. Outstanding pushes — three repos, deliberately left for you

**Suggested: your call. These carry other agents' commits, including your own.**

I pushed nothing, in any repo. As of 13:57 PT:

| Repo | Unpushed |
|---|---|
| `evryn-backend` (`main`) | **4 ahead** of origin (tip `07609fb`) |
| `_evryn-meta` (`main`) | **8 ahead** of origin |
| `evryn-team-workspace` (`main`) | **2 ahead** of origin |

**The earlier hold no longer applies.** Justin's *"leave AC5's stuff, including his unpushed commits"* was issued while that lane was open. **AC5's material is now merged into `main`**, so that hold no longer blocks these. They remain your call — they carry other agents' in-flight work.

⚠️ **`_evryn-meta`'s push is the Vercel dashboard deploy.** Pushing that repo deploys the dashboard along with everything else in the backlog. A Justin-gated act, not an incidental one.

**🔴 A correction to a prior alarm of mine, because a stale alarm is worse than none.** I earlier flagged three never-pushed branches (`ac5/ingest-fetch-resilience`, `dc/step35b-record-legibility`, `mira/step35d-trust-loaded-record`). **Those three are resolved** — worktrees reaped, work landed. **But the class is live again:** `ac5/pre-refactor-quicklist` (at `79644d3`, in worktrees `evryn-backend-quicklist` and `_qc-sandbox`) **has no remote at all** and exists only on this machine. If AC5's lane is closed, confirm that branch's content is landed or intentionally disposable — otherwise a machine failure loses it silently. **A routine `git status` on `main` cannot see this class.**

---

### P8. Soren's unsettled design batch — HELD, deliberately not swept

**Suggested: PARK until Soren or Justin rules.**

**What it is.** Part of the rehome-then-retire item was to move Soren's v0.3 design batch out of the retiring `2026.07.22-ac0-ac0-handoff.md` into the v0.3 build doc. **I did not do it, and I believe that was correct.**

**Why.** Reading the source rather than a summary of it, Soren's own text at line 166 explicitly forbids that exact move:
> *"⛔ NOTHING IN THIS THIRD BATCH IS SETTLED, AND NONE OF IT SHOULD BE SWEPT INTO A LANE... It is deliberately **NOT** written into `ARCHITECTURE.md` or the BUILD docs... **Consequence if you sweep it:** you'd be building against a design that was still moving when it was written down."*

He had already rehomed the durable parts himself, and explicitly noted that **Nathan's eligibility rule was the one thing left intact** — which is precisely the piece I *did* rescue (see §A).

**Consequence:** `2026.07.22-ac0-ac0-handoff.md` is **HELD, not retired.** I recorded the reason in Soren's memory file so he isn't surprised to find it still there.

**Next action + owner: AC0-30 routes to Soren (or Justin)** for a ruling on whether the batch is settled enough to land. Nothing breaks while it waits.

---

### P9. Two questions that were never answered — and a numbering collision that could hide them

**Suggested: needs Justin. Low urgency, but easy to lose.**

Justin's answer set covered ballots 2, 3, 5, 6 and sent six items back for research. **This document's ballots 9 and 10 were never addressed.**

⚠️ **Watch the numbering collision.** In my final chat report I used a *different* numbering from this document's. "Ballot 10" in chat meant *"persist the research into this doc"* — **answered yes**, and that produced this file. **This document's** ballot 10 was a different question entirely. Both original questions remain open:

- **Doc ballot 9 — "Push the never-pushed branches for durability?"** Partly overtaken: the three originals resolved themselves, but `ac5/pre-refactor-quicklist` is a new instance of the same risk. **See P7.**
- **Doc ballot 10 — "Correct the two stale lines in the cross-project status file?"** I had found `docs/current-state.md` claiming the product repo had "5 unpushed commits" and that a stale remote branch `origin/dc/webfetch-timeout` "still exists and should be deleted" — **neither was true when I measured.** ⚠️ **State has moved substantially since; re-verify before correcting.** The point stands regardless: stale status lines send the next AC chasing ghosts.

---

### P10. Process flags — two things you should know, neither of them damage

**Suggested: read once; no action unless you disagree.**

**(a) 🔴 I wrote to two team-memory files without the protocol's auth step.** Justin's instruction required attributed notes in Nathan's and Soren's `MEMORY.md`, and I wrote them (commit `a9e670b`). **Afterwards** — while reading an unrelated doc — I found that team-memory writes are supposed to be preceded by Justin's live authorization and a Slack ping, and that those files have CRLF line endings the Edit tool reportedly mangles.

**I verified rather than assumed:** both canaries are still the last line, each file contains exactly one `Note from ACh`, both remain fully CRLF (247/247 and 141/141 lines), and both diffs were additions-only. **No damage.** But these are Nathan's and Soren's source-of-truth documents, a protocol step was skipped, and you should hear it from me rather than discover it. If the convention is that such notes belong in a specific section rather than at file end, that's worth correcting while it's fresh.

**(b) A contradiction inside Justin's own answers, and how I resolved it.** §8.1 cleared *"all twelve"* document retirements. §8.4 then said the ACU lane is live and **both ACU documents stay** — and one of those was among the twelve. **I took the conservative reading: retired 11, kept the ACU week plan.** Recorded here so nobody re-opens it as an error or "finishes" the twelfth.

---

### P11. Carried-forward observations from the original sweep

**Suggested: PARK. Recorded so they survive; none is urgent.**

- **Truncation canaries are missing from 30 of the 44 runtime source files**, so an AI instance reading them cannot self-diagnose an incomplete load. AC0 flagged this as separately routed; I hit it directly (I had to report end-line numbers instead of relying on a canary). **Still open as far as I can tell.**
- **Two observations are now moot** and are noted only so nobody re-raises them: the working-doc count discrepancy (32 vs a stated 31 — the folder is now 24), and the three never-pushed branches (resolved; superseded by P7).

---

## §C — HOW I WORKED, SO YOU CAN CALIBRATE TRUST

- **Read-only** outside the two explicitly-cleared items. Every commit staged **by explicit path**, with `git diff` run first — other agents were committing into `_evryn-meta/docs/working/` throughout.
- **Nothing pushed, merged, or deployed.**
- **I verified rather than inherited, and it changed two answers.** The `ac2/step57` divergence (mechanical signals said "unlanded"; substance said "superseded") and the never-pushed-branch alarm (I was told it was obsolete — the original three are, but a new one exists). Both would have been recorded wrongly had I accepted the summary.
- **A mechanical check that comes back clean is not evidence the underlying thing is safe.** `git branch --merged` misleads on a stale base, and a passing grep only proves a string is absent, not that a concept is handled. Where I could, I checked substance — byte sizes, actual file contents, the remote itself.

**The standing risk this document closes:** everything in §B existed **only in a subagent transcript** until now. Six research answers, six un-rehomed items, two process flags, and a live Gate-B blocker would all have died at my compaction. If further items surface in this lane, they need a durable home the same day — a handoff is where a finding is captured under pressure, never where it lives.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
