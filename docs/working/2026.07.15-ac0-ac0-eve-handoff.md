# Handoff — AC0 → next AC0 — 2026-07-15 EVENING — ⭐ Gate-A 12/13 STRONG (awaiting YOUR co-sign) · ③ ready-to-merge behind co-sign→Mira→Fable→Justin · Step-78 Half-A merge routed to AC0

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **This SUPERSEDES the 2026-07-15 *morning* handoff** (archived to `docs/sessions/historical/2026.07/`). Read THIS one; it is complete + self-sufficient for the conductor view.
>
> **How to use this file:** START HERE, fresh AC0. You conduct the **product wave** (AC2 = cost / ③ Step-57 "Runtime Bookkeeping"; AC3 = identity/ops / Step 78) under **ACU** (a meta-coordinator one level up — his `docs/working/2026.07.13-acu-uber-week-plan.md` is context, not gospel; **his docs are HIS track — don't process/sweep them**). You orchestrate, gate merges/deploys, reconcile collisions, keep housekeeping current, translate to Justin at CEO altitude. **You do NOT write runtime code.**
>
> **This handoff is your self-sufficient ENTRY POINT + routing map — it is NOT the whole picture by itself.** To be FULLY up to speed, boot-read (in this order, per First moves): THIS handoff → AC2's + AC3's full lane handoffs (Justin freshly CONSOLIDATED them 2026-07-16 so they're digestible — read them in full, not on-demand) → the ③ live handoff → the runtime. The #1 task (the Gate-A co-sign) can't be done from this handoff alone; it needs AC2's ③ handoff + the runtime. (Beyond those, read any *other* doc on-demand when it surfaces work — see Key docs.)

---

## 🚀 First moves (fresh AC0, on boot)
1. **`git fetch` all repos**; confirm each on its canonical branch (all `main` except `evryn-team-agents`=master frozen), tree clean, per `docs/repo-inventory.md` + the CLAUDE.md ritual. SHAs below are as-of-close and WILL have moved.
   - **⚠ SHARED-MACHINE GOTCHA (bit me hard this session — CARRY THIS): other ACs commit on the shared LOCAL `main` and DON'T always push. A `git fetch` from `origin` MISSES their local commits.** Twice I thought "no new mail / nothing changed" when AC2 had committed a fresh message LOCALLY (unpushed). **Always check LOCAL state — `git log`, and re-read the actual mailbox file on disk — not just `origin`.** At this close-down `_evryn-meta` local was 14 commits ahead of origin (ACU/ACf/AC3/AC2 + mine); I pushed them all for overnight durability, so origin *should* == local when you boot — verify.
2. **Load the Light cascade** (`_evryn-meta/CLAUDE.md` auto → `docs/current-state.md` → Hub `roadmap.md`). **Load the FULL `src/` runtime BEFORE you co-sign Gate-A / gate ③ / touch anything ③-adjacent** — ③ is a runtime rewrite, you can't judge it blind. ARCH+BUILD are reconciled+accurate (AC6 7/09 + AC7 doc-currency headers) — a trustworthy on-ramp, **NOT** license to skip `src/`.
3. **Remake your ping helper** (a Node `fetch` POSTing `{text}` to `SLACK_TEAM_WEBHOOK_URL` — read that var's LINE from `C:/Users/Justin/Evryn/Code/evryn-team-workspace/.env`; use the `C:/Users/...` path in Node, NOT git-bash `/c/...`; prefix `AC0:`). **Ping `#team-alerts` "I'm up," then ping EVERY response — no exceptions** (Justin corrected the prior AC0 on this hard).
4. **Read BOTH lane handoffs IN FULL — freshly consolidated + digestible (Justin, 2026-07-16), so it's fast and gets you FULLY up to speed** (do this at boot, not on-demand):
   - **AC2 / ③ (LOAD-BEARING for your #1 task):** the live ③ START-HERE handoff `docs/working/2026.07.14-ac2-step57-dc-build-review.md` (Gate-A results + per-fixture grading + **how to re-run a13** + the Mira dispatch prep) + the cost-lane brief `docs/working/2026.06.30-ac0-ac2-cost-lane-brief.md` (the durable ③ operating manual + the AC0⇄AC2 mailbox).
   - **AC3 / identity-ops:** `docs/working/2026.06.30-ac0-ac3-identity-ops-brief.md` — the **2026-07-15 EVENING BOOT-HERE** section is authoritative (Step-78 Half-A merge-routed-to-you + Half-B queued + Gate-B machinery).
5. **⭐ THE #1 LIVE THING ON YOU = the Gate-A CO-SIGN** (LIVE MAP + AC0 OWED §1). Gate-A came back **12/13 STRONG**; AC2 left the DEV state intact for your independent review. That's your first substantive move (autonomous — no Justin gate).

---

## ⚡ Operating model (CARRY THESE)
- **You conduct; you do NOT write runtime code.** Lanes' DC/QC build; you gate merges/deploys, reconcile cross-lane collisions + sequencing, keep Justin at CEO altitude, keep housekeeping current (current-state, CHANGELOG, deploy-log, SPRINT marks).
- **Lanes take recommend-first ballots STRAIGHT to Justin — NOT routed through AC0** (Justin, 7/14). You conduct **merges / collisions / housekeeping**, not question-routing.
- **Model = Opus-default + per-turn Fable escalation** (`⚡ FABLE TURN NEXT: [what] — flip me, then say go`; flip back after). Reach for Fable on irreversible/expensive calls (merge-gates, deploy sequencing), heavy multi-constraint synthesis, adversarial review of high-stakes work. **The ③ merge-gate gets a dedicated Fable review (ratified).** Lane ACs stay Opus.
- **Deploys: Justin does NOT run `railway up`** ("beyond my pay grade") — a lane or AC0 runs it, on Justin's explicit go. ⚠ **bare `railway up` = PROD**; staging = `railway up --service evryn-staging --environment production`. ⚠ **a Vercel push = a live deploy** (`_evryn-meta/dashboard/` + `evryn-website` auto-deploy on push to main — so merging a dashboard/website branch IS a deploy → Justin-gated).
- **Commit/deploy gates are Justin's, per-action, don't stack.** Lanes build/commit on their branches freely; PAUSE for his explicit go on **merge-to-main / any deploy / any prod migration / Mira's SCM review.** **STANDING EXCEPTION (yours):** housekeeping-doc commits+pushes (current-state, CHANGELOG, deploy-log, SPRINT marks) + mailbox/brief + working-doc/handoff — commit + push freely. **Stage by explicit path, never `git add -A`** (ACU's/ACf's files are often dirty/committed-unpushed in the shared repos — never sweep them into your commit; but pushing their already-*committed* work up for durability is fine and is the AC0 batten-down role).
- **Part A doc-currency (standing):** keep ARCH/BUILD/SPRINT current at each checkpoint; ARCH edits are Justin-SCM-vetted before commit. AC7's #lock doc-currency gate + `doc-update-routing-protocol.md` are LIVE — use the routing protocol (change-type → doc-home).

---

## 📊 LIVE MAP (the conductor's picture)
- **Prod DEPLOYED = `v0.2.7`** (`evryn-backend` `517014d`; Clustering + Haiku both live-**OFF**/dormant). **`evryn-backend` main = `aac2166`**, AHEAD of prod: carries 54a/54c + AC7 doc-headers + AC0's 7/15 SPRINT reconcile + **my operator-guide ceiling fix** — all **deploy-pending**.
- **⭐⭐ GATE-A (Step 88A) = 12/13 STRONG · BASELINE PASS · AWAITING YOUR CO-SIGN.** AC2 seeded the **Megan** fictional gatekeeper (id `43a68cfa-d6c6-4088-b86b-38a5d6583669`, `systemtest@evryn.ai`, lane registered, criteria in pending_notes) and ran the **§3 Megan judgment suite (a01–a13)** live on **current `main`** (NO ③). **ONE seed covers BOTH Gate-A pieces** (baseline + adversarial): the Megan **gold** fixtures reaching `pending_approval` ARE the baseline "a legit forward flows the whole loop." Results:
  - passes a04–a07 = **4/4 clean `pass`** (no draft, no mis-fire); edges a08–a10 = **3/3**; a11 = correct `reply`/not-triage; a12 = correct graceful `edge`; **golds a01–a03 all → `edge`** = the **EXPECTED Phase-5 web-search divergence** (fictional producers/festivals/films don't web-verify → forgiving-skepticism `edge`, well-reasoned — **NOT a gate mis-fire**).
  - **Baseline = PASS** (whole loop flowed, NO gate falsely blocked, AC4 read-guard ALLOW on scoped reads + web-egress guard ALLOW on legit research, "before" reference captured). **No security-boundary breach.**
  - **⚠ a12 hung the run ~27 min** — after its verdict, Evryn's query blocked, most likely on a **hanging WebFetch (no hard per-fetch timeout)** — this is **CURRENT-MAIN behavior, NOT a ③ regression** (a01–a03/a08 submit_draft'd fine). **→ owes a v0.3 WebFetch-timeout-hardening note** (route to `SPRINT-V0.3-CANDIDATES.md`). It's why a13 didn't run.
  - **YOUR CO-SIGN (AC0's "with-AC0" two-eyes):** load the runtime → **re-run a13** (dedup-skips a01–a12; ⚠ could hang like a12 — watch it) → independently review the DEV state (Megan `43a68cfa…` + the run's items are **LEFT INTACT** for you; ephemeral scripts left in the canonical tree; **cleanup is post-co-sign**) → **co-sign the GO.** Full tactical picture (how-to-re-run + the per-fixture grading) is in the ③ handoff `docs/working/2026.07.14-ac2-step57-dc-build-review.md` ("Gate-A execution state" / "Gate-A RESULTS") + AC2's mailbox round at the tail of the cost-lane brief.
- **⭐ ③ (Step 57 Runtime Bookkeeping) = RATIFIED + BUILT + QC-GO + DEV-GREEN, UNMERGED.** Branch **`ac2/step57-runtime-bookkeeping`** tip **`d9306a8`** (harness `16fbb99` + comment-scrub `d9306a8`; **verify pushed** — AC2 pushes for durability but check). DEV exercise GREEN both tiers (deterministic + live-Opus breaker-OFF — the real tool-loop fired through ③'s new hooks). **What ③ does:** runtime pre-loads context + owns status/lifecycle writing; Evryn returns a structured verdict + does only judgment (cuts a pass ~15→2-4 turns); **removes her raw `supabase_upsert` write surface** (a real safety tightening) → tool-surface end-state = typed `record_verdict`/`record_outcome` + operator-only `correct_user_field` + AC4's constrained `supabase_read`, **NO raw supabase_upsert**. Ratified calls (ADR-051): user-creation → verdict-time (none for `ignore`); `supabase_upsert` removed wholesale. **Ships switch-less, ZERO prod migration.** **Remaining before merge:** Gate-A co-sign (above) → catch-up rebase (③ → current `main`) → Mira pass → Fable merge-review → Justin's merge.
- **🩺 Step 78 Half A (AC3) = BUILT + QC-GO on `ac3/step78-real-health` (`d1238f3`); MERGE ROUTED TO AC0.** Real 503-when-wedged `/health` (poll stale >~15min OR db down → 503; M1-breaker-tripped → **200 "halted"**, deliberately NOT 503, ADR-041 no-auto-reset-else-restart-loop) + per-subsystem `checks`; new `src/safety/liveness.ts`+`health.ts`, new env `HEALTH_WEDGED_THRESHOLD_MS`. **NOT deployed → rides the coordinated deploy wave.** Files (`index.ts`/`poll.ts`[1-line]/`slack.ts`[read-only getter]/`config.ts`/`safety/*`/`tests/`) have **NO overlap with ③** (`classify.ts`/`process.ts`/`triage.md`). **⚠ Now a rebase-then-FF, not a clean FF** — my operator-guide ceiling fix (`aac2166`) landed on `main` first, and Step 78 ALSO touches `operator-guide.md` (different sections — the /health example + a `HEALTH_WEDGED_THRESHOLD` note — no real conflict). **⚠ NEEDS JUSTIN'S EXPLICIT MERGE-GO-TO-YOU** — AC3's handoff says "Justin routed the merge to AC0," but that's a second-hand relay; I asked Justin directly at close-down — **check his answer** before merging. On merge: rebase-then-FF onto current `main`, mark SPRINT Step 78 (DONE-at-deploy, per no-changelog-for-unshipped), reap the `evryn-backend-step78` worktree. **Deploy-time doc-work owed:** 2 operator-guide touch-ups — a `HEALTH_WEDGED_THRESHOLD_MS` note + fix the stale "Verify Deployment" step-4 `/health` example (still shows the pre-Step-78 `{"status":"ok",...}`).
- **54a + 54c = MERGED** to `evryn-backend` main `2572dbc` (deploy-pending — ride the ③ bundle; 54a re-points the "have I met this person?" triage check at the contact's permanent record, 54c captures the real forwarded-sender name).
- **AC7 = CLOSED + LANDED.** #lock doc-currency gate + `doc-update-routing-protocol.md` are LIVE.
- **Mira identity pass = PRE-STAGED, fires after Gate-A co-sign/GO.** AC2 left the FULL dispatch prep in the ③ build-review doc's **"## Mira dispatch prep (AC2, 2026-07-15)"** — exact per-file deltas (`triage.md`/`core.md`/`operator.md`/`gatekeeper.md` + the new `correct_user_field` operator beat), the tool-surface facts Mira must match, her cascade load-list (team-subagent variant), Justin's verbatim spin-instructions (peer request · independent judgment · her output carries her memory wording + reasoning · **PAUSE for Justin's SCM review before you commit**), and the worktree plan (`evryn-backend-step57-mira` off step57, edits 4 files, does NOT commit). You brief the mechanics; Mira voices; you re-check her voiced version vs. the runtime.
- **CHANGELOG is CURRENT** (AC3 added the Step-78 entry 7/15; ACf backfilled 07-14). Nothing shipped this session → no ③/54a/54c/78 changelog until the bundle deploy (no-changelog-for-unshipped).

## 🛣 Critical path to a gatekeeper
**Gate-A CO-SIGN (yours) → catch-up rebase (③ → current `main`) → Mira identity pass (Justin SCM-review + her memory auth) → Fable merge-review → Justin's merge → deploy THE BUNDLE (ONE `railway up`) → Gate-B (MANDATORY pre-gatekeeper staging create-from-zero live-fire, everything active together) → flip Clustering + Haiku on → Justin account setups → gatekeeper.**
- **Gatekeeper:** Mark radio-silent-not-lost; a 2nd gatekeeper warming. Hold as "ready when *a* gatekeeper shows."
- **Mark-arrival override (Justin):** the instant Mark emails, everything re-jiggers to a ~2-day get-stable sprint; Mark does NOT come in until Gate-B passes.

## 📦 THE BUNDLE DEPLOY (one coordinated `railway up` — AC0 assembles/gates; lane-or-AC0 runs it, NOT Justin)
Carries **③ + 54a/54c + Step-78-Half-A (once merged) + the Anthropic key-split env swap + the dead `_PROD` var cleanup**. **ZERO prod migration** (③ + Step-78 both need none). Gated Gate-A(co-signed) → Mira → Fable → Justin's merge → deploy → Gate-B.
- **Key-split** (runbook `docs/working/2026.07.13-acu-ac0-anthropic-key-split-runbook.md` — ACU-authored, but it's your rider): **value-only env swap** on `evryn-backend` prod (`ANTHROPIC_API_KEY` → a dedicated Evryn-Production key), NO code; Justin ~10 min console (J1–J5).
- **`_PROD` cleanup:** dead unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` on Railway prod; `railway variable delete` can't `--skip-deploys` → rides this window.
- **Staging for Gate-B:** UP + healthy (Step 80). It's one migration short (the clustering `queued` CHECK-widen) → fold into a **pre-Gate-B fresh staging re-seed** from a current zero-PII prod-shape dump (closes the gap + absorbs any migration + gives a clean create-from-zero slate). `SEND_ENABLED=false` until the run (Justin flips it); Megan fixtures (`evryn-backend/tests/fixtures/emails/megan/`) feed it.
- **Deploy-time doc-work (Part A, AC0):** ARCH ③-absorption (from ADR-051; Justin-SCM-vetted) + the 2 Step-78 operator-guide touch-ups + CHANGELOG + deploy-log + current-state + team-cs + SPRINT marks (54a/54c/③/78 → DONE). *(OPEN, Justin hasn't answered from a prior session: draft the ARCH absorption ahead of the deploy vs at-deploy — my rec: **at-deploy**, ARCH should describe what's live.)*

## LANES
### AC2 — cost / ③ (brief: `docs/working/2026.06.30-ac0-ac2-cost-lane-brief.md`; live ③ handoff: `docs/working/2026.07.14-ac2-step57-dc-build-review.md`)
- **③ is through DEV-green + Gate-A; the ball is on YOU for the co-sign.** After co-sign: rebase → Mira (prepped) → Fable → Justin merge. Seams AC2 tracks for you: **F2 metadata-shape** (③ stashes `original_from_name` in `emailmgr_items.metadata`, additive, self-contained — ensure ③↔54c agree at merge); **Mira-coordinate-once** (③'s Mira pass ↔ 54a's already-applied `triage.md` beat — voiced ONCE against ③'s pre-loaded shape, the build-review doc's divergence list is the input); **AC4 read-lock** composes (③ removes the write, AC4 locked the read). **Step 71** (Reflection binding safety-net) = separate small trip, may ride the deploy window.
### AC3 — identity/ops (brief: `docs/working/2026.06.30-ac0-ac3-identity-ops-brief.md`; the 7/15-eve BOOT-HERE section is authoritative)
- **Step 78 Half A → merge routed to you (above).** **Half B** (Step 78 dashboard status-lights + Step 79 staging tab) is QUEUED — **needs Justin's greenlight + a light orientation first** (it's a SEPARATE codebase → DC/QC with `#cascade-override`). 54a/54c merged. **Gate-B machinery** is AC3's (Megan fixtures ready; gated on ③ landing + Justin's SEND flip; AC0-orchestrated). Fill: 35b/c/d, 41a (waiting on AC4). Held/parked: Soren maintenance-plan (Step 47), decline-offer (Step 55 routed-back), Step 51 (compulsion audit — collides with ③).

## 📌 AC0 OWED (standing — carry these)
1. **⭐ Gate-A CO-SIGN (TOP, do first) — AUTONOMOUS, no Justin gate; just do it** (a DEV-only review — no merge/deploy/prod-migration). Load the runtime → re-run a13 → review the intact DEV state → co-sign the GO. Then post-co-sign cleanup (Megan + run items) can happen. Everything *downstream* of the co-sign (rebase → Mira → Fable → merge → deploy) is Justin-gated and waits for him.
2. **Step-78 Half-A merge** — ONLY on Justin's explicit merge-go-to-you (check his close-down answer): rebase-then-FF `ac3/step78-real-health` onto current `main`, mark SPRINT Step 78 (DONE-at-deploy), reap the worktree.
3. **Sequence the ③ merge chain** — catch-up rebase → Mira pass → Fable review → Justin's merge → bundle deploy → Gate-B.
4. **Route the a12 WebFetch-hang** → `SPRINT-V0.3-CANDIDATES.md` (v0.3 WebFetch-per-fetch-timeout hardening; current-main behavior, low-reachability).
5. **Deploy-time doc-work** (Part A) — see BUNDLE DEPLOY.
6. **Cross-lane:** Mira-coordinate-once + F2 `original_from_name` metadata-shape.
7. **Housekeeping as-you-go** (CHANGELOG at every ship); **ping every response.**

## ✅ On Justin's plate
- **Explicit merge-go for Step-78 Half-A** (I asked him at close-down — check his answer).
- His go on the **③ merge** + the **bundle deploy**. **Mira SCM-review + her memory auth.** Staging `SEND_ENABLED` flip at Gate-B. The Clustering + Haiku prod flips + account setups before a gatekeeper. **Step-78 Half-B greenlight + light orientation.** The ARCH-absorption-timing call (rec: at-deploy).

## 🧭 Key docs
`docs/current-state.md` (7/15-eve line is latest) · `_evryn-meta/CHANGELOG.md` · `docs/deploy-log.md` · `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (esp. Step 88 Gate-A/B, Step 90) + `docs/decisions/051-*` (ADR-051, ③) · ③ docs `docs/working/2026.07.14-ac2-step57-runtime-bookkeeping-design.md` + `-dc-build-review.md` (the live ③ handoff — Gate-A results + Mira dispatch prep) · lane briefs `docs/working/2026.06.30-ac0-ac2-cost-lane-brief.md` + `-ac3-identity-ops-brief.md` · `evryn-backend/docs/staging-runbook.md` + `operator-guide.md` (Go-Live Checklist) · `_evryn-meta/CLAUDE.md` + `docs/protocols/{ac-orchestration-protocol,lock-protocol,doc-update-routing-protocol,ac-writing-protocol}.md`. *(Key-split runbook + uber-week-plan are ACU-authored — reference, don't process.)*

## 🔧 Repo state (as of close — `git fetch` on boot; SHAs move)
- `_evryn-meta` main = pushed at close (I pushed the local ~14-ahead — ACU/ACf/AC3/AC2 committed work + my current-state/handoff — for overnight durability; origin should == local, verify). Untouched dirty: ACU's `uber-week-plan.md` (`M` — HIS, leave); AC3's `2026.07.15-ac3-step78-real-health-design.md` was untracked earlier (his — leave/verify).
- `evryn-backend` main = `aac2166` (54a/54c + AC7 headers + sprint reconcile + my operator-guide ceiling fix), pushed. Prod = `v0.2.7`. Branches: `ac2/step57-runtime-bookkeeping` (`d9306a8`, ③) + `ac3/step78-real-health` (`d1238f3`, Step 78). Worktrees: canonical `main` · `evryn-backend-step57` (AC2's ③ — DO NOT touch) · `evryn-backend-step78` (AC3's — reap after the Half-A merge).
- `evryn-team-workspace` main = ahead of origin with ACf's team-runtime work (their track — I did NOT push it; no team-cs hole for me to fill — my 7/15-morning team-cs entry stands, ACf posted their own). `evryn-quality` synced.

## 🗒 What THIS session (2026-07-15 evening) did (fragment-sweep record)
- **Doc-archive:** 10 settled/shipped working docs → `docs/sessions/historical/2026.07/` + repointed ADR-020/049 pointers in the same commit (`92eddcd`). **Held (not archived):** the 54b forward-persistence brief — still a ③ background input, reaps when ③ merges.
- **2 AC0→AC2 Gate-A mailbox rounds** (Gate-A readiness + the plan/split; AC2 later consolidated them into a "GATE-A PLAN + SPLIT" section) — `510ec87`, `00c1bf7`.
- **Operator-guide Anthropic-ceiling recharacterization** (Justin: it's actively-managed, not one-time) → `aac2166` (evryn-backend, pushed). Heading "Two one-time setups" → "Two pre-Mark monitoring setups"; item 1 reworded.
- **current-state 7/15-eve line + this handoff + archived the morning handoff.**
- **Mira dispatch pre-staged** (read AC2's prep — ready to fire after Gate-A GO).
- **Dispatched a doc-triage subagent** earlier (light-loaded) for the archive ballot — its work is done + absorbed.
- **Pushed `_evryn-meta`** for overnight durability (carried up others' committed work).

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
