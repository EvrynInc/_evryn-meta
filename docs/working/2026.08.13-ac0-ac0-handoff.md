# Handoff — AC0-36 → **AC0-37** — written 2026-08-13

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file: THIS IS YOUR ENTIRE BRIEFING.** It supersedes `2026.08.11-ac0-ac0-handoff.md` operationally — **but that file is NOT retired**, because §10 of it holds the compaction finding and §9.8's load list is the ancestor of yours. Read it only where this file points you at it.

---

> # 🛑 YOUR STARTUP IS A **TWO-TRIP**. LOAD, THEN STOP AND REPORT. DO NOT ACT.
>
> **Justin's explicit instruction, 2026-08-13.**
>
> **TRIP 1 — load §1's list, in full, and then STOP.** Report to Justin: every file, the lines you actually read, and the canary you confirmed on each. **Do not summarise the work. Do not propose a plan. Do not spin anything. Do not touch a file.**
>
> **TRIP 2 — only after he responds.** He wants to see what you actually hold before you do anything with it.
>
> **Why:** AC0-36 twice mis-stated its own load — once claiming a 941-line protocol read that was ~500 lines. **A load you report is auditable; a load you assume is not.** This trip exists so the gap is caught at the start rather than three decisions later.

> # 🔬 IF YOU COMPACT — the rule is in `_evryn-meta/CLAUDE.md`, and the experiment it came from is OVER
>
> **Justin's verdict, 2026-08-13: *"The experiment where we use compaction to save the subagents is an utter failure — it's wasted most of the day trying to get both you and ACf back up to speed. The compaction gave you each a garbled understanding — you were both so confidently wrong. We can decisively put that to bed."***
>
> ⇒ **Do NOT ride a compaction. If you compact, STOP, tell Justin, and expect a re-spin.** The reload is not sufficient — **both AC0-36 and ACf spent most of a day recovering and were confidently wrong throughout.**
>
> ✅ **The one thing compaction did NOT break is subagent resume** (§2). That property is real and valuable; the "just reload and continue" workflow is not.

---

## §1 — YOUR LOAD LIST. Read every file, then stop and report.

⚠️ **Re-run `wc -l` at spin time.** Counts drift; a stated span that is wrong makes your receipts meaningless.

### TIER 1 — mandatory before you do anything

| # | File | Lines | Why |
|---|---|--:|---|
| 1 | `_evryn-meta/CLAUDE.md` | 611 | Your manual. **Read it from disk** — the auto-injection is a snapshot taken at session start. |
| 2 | **THIS FILE** | — | Your briefing. |
| 3 | `_evryn-meta/docs/current-state.md` | ~167 | Light cascade. ⚠️ **Known-stale in one specific way — see §5.1.** |
| 4 | `_evryn-meta/docs/hub/roadmap.md` | 196 | Light cascade — company truth. |
| 5 | `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` | 941 | 🔴 **Mandatory before you spin ANYTHING.** AC0-36 read half of this and mis-reported it as full. **Read all of it.** |
| 6 | `_evryn-meta/docs/protocols/ac-writing-protocol.md` | 100 | 🔴 **Mandatory before you write to ANY doc.** It is in **zero** cascades — that gap is an owed item (§6). |
| 7 | `_evryn-meta/docs/protocols/lock-protocol.md` | 97 | You will be closing out. |
| 8 | `_evryn-meta/docs/working/2026.08.12-qc-sweep-findings.md` | ~640 | The mutation sweep. 🔴 **Appendix E is new and it reverses a "permanent" loss claim — read it.** |

### TIER 2 — before any product build work, ruling, or runtime claim

**This is the conductor cascade = the product full cascade MINUS `src/`.** AC0-36's predecessor omitted most of it; do not repeat that.

`_evryn-meta/docs/hub/technical-vision.md` (397) · `evryn-backend/docs/ARCHITECTURE.md` (1,285) · `evryn-backend/docs/BUILD-EVRYN-MVP.md` (651) · `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (475, **~163 tok/line — the densest doc we own**) · `evryn-backend/docs/SPRINT-v0.2-optionals.md` (25) · **skim** `evryn-backend/CHANGELOG.md` + `_evryn-meta/CHANGELOG.md` (top ~100 lines each) · **the Code Atlas** `evryn-backend/docs/atlas/` (12 files, 3,108 — start at `00-INDEX.md`).

**Measured cost of the whole of Tier 1 + Tier 2: ~250K tokens.** AC0-36 read all of it and had room. **Prose here runs 60–80 tok/line whole-file** — ⚠️ a *slice*-derived rate reads much higher and will make you over-cautious (the protocol's own "144–166 tok/line" figures are slice-derived and overstate by ~2×; correcting them is on ACm2's branch).

### 🚫 What you do NOT load

**`evryn-backend/src/`.** The conductor cascade keeps the runtime in a subagent's context. The Atlas + the dependency map are how you make a *targeted* read when you need one.

---

## §2 — 🔴 THE SUBAGENT LEDGER — and **whether these are alive depends on WHICH kind of successor you are**

> ### 🔑 READ THIS FIRST. Justin's clarification, 2026-08-13, and it is the most useful mechanical fact in this file.
>
> **"Compaction doesn't compact subs."**
>
> | If you are… | The six agents below are… | What to do |
> |---|---|---|
> | **A COMPACTION of AC0-36** (same session) | ✅ **ALIVE and PRISTINE** — full context, zero re-reads | 🔴 **TALK TO THEM.** Your own context is the damaged thing; theirs is not. |
> | **A FRESH SESSION** (a real re-spin) | ❌ **Permanently unreachable** | Do not go looking. Everything extractable is already in this file and in the findings doc. |
>
> **Justin's framing, worth internalising: *"your own context will be fucked, but your subs will be pristine as they are now — so be less afraid of the limit for them, more afraid for your personal context being lost."***
>
> ⇒ **The asymmetry that follows: a subagent's context is CHEAP to preserve and EXPENSIVE to recreate. Yours is the opposite.** Spend your tokens writing down what only *you* hold; let the subs hold what they hold.
>
> ⚠️ **And the corollary Justin drew explicitly: AC0-37 may spin a post-compaction AC0-36 to interrogate these subs.** That path works as long as the *session* lives. **It is not available across a re-spin.**
>
> ### 🔴 THE TRAP IN THAT PLAN — read this before deciding how to spin AC0-37
>
> **Justin's stated intent (2026-08-13) is that AC0-37 is a stopover who will *"come back for subagent stuff."*** ⚠️ **That plan only works on ONE of the two possible spins, and the choice is IRREVERSIBLE:**
>
> - **If AC0-37 IS this session compacted** → ✅ the six are alive and AC0-37 can talk to them directly. **No intermediary needed.**
> - **If AC0-37 is a FRESH SESSION** → ❌ the six are gone — **and so is the fallback.** *"Spin a post-compaction AC0-36"* **requires AC0-36's session to still exist.** A fresh AC0-37 cannot reach back into a session that has closed; there is no instance left to spin.
>
> 🔑 **⇒ THE DECISION ABOUT WHETHER THE SUBAGENTS SURVIVE IS MADE AT RE-SPIN TIME, BY JUSTIN, AND IT CANNOT BE UNDONE.** **If anything in §4, §5 or §11 needs to be asked of a subagent rather than read off this page, ask it BEFORE the session closes.**
>
> ✅ **AC0-36's judgement, so nobody has to guess: everything extractable HAS been extracted.** All six were closed out adversarially, and their content is in Appendix E of the findings doc and in §3, §4 and §11 here. **Losing them costs re-interrogation of ground already covered — not lost work.** **Do not hold a session open on their account alone.**

| agentId | Who | Holds | Status at close-out |
|---|---|---|---|
| `a953846a83c3081ea` | **Sweep lead** (Lane D Mega-QC) | The six lane briefs, verbatim | ✅ **FULLY EXTRACTED → Appendix E** |
| `abba711330f0c1d16` | **ACb** (Lane B) | Sprint + current-state restructure | ✅ Extracted; wrote `5f14ab0` to its brief |
| `a2488af010049b71c` | **Lane E DC** | The NUL fix | ✅ **FULLY EXTRACTED → §4.2** |
| `ad1ebc4054aa968e6` | **Lane E QC** | The NUL review | ✅ **FULLY EXTRACTED → §4.3** |
| `ac4fabb5fa7843345` | **ACm** (Lane A, 1st) | Loading architecture | ✅ Extracted into its brief (§9) |
| `a313c28f89d92b311` | **ACm2** (Lane A, 2nd) | Phase B + agent definitions | ⚠️ **Resume sent at close-out; reply may not have landed — see §7** |

**⚠️ The six mutation lanes (M1–M6) were the sweep lead's CHILDREN, not AC0's.** They were never directly reachable and are gone. **Their load lists are recovered (Appendix E); their agents are not.**

🔑 **THE CAPABILITY WORTH KEEPING, PROVEN TWICE TODAY:** resume returns an agent with **full prior context, zero re-reads** — and it **survives a parent-side compaction**. Justin's observation after doing this with ACf: *"it was staggering what the compaction summary didn't give him that his subagents' reports really helped with."* ⇒ **If you inherit live subagents, talk to them before you trust any summary of their work.**

---

## §3 — 🔴 THE MERGE HAZARDS. Read these before touching any branch.

### 3.1 Lane B: **a CLEAN merge is the dangerous outcome**

**ACb's own warning, and it is the sharpest thing it said:**

> *"My branch rewrites `current-state.md` **wholesale**. Now that `main`'s copy has grown, a **clean** merge is the dangerous outcome — it would silently discard ACf's appended team-runtime block. **Do not read a clean merge as proof nothing was lost; compare by content, not by git's verdict.**"*

**What must happen:** ACf's team-runtime block (added on `main` as `0066754`) belongs in Lane B's new **📮 BULLETIN** section — *"the section that exists precisely because that content had nowhere to go."* **AC0-36's Atlas correction is already carried in ACb's version.**

### 3.2 Lane A: three preconditions, all Justin's

**Branch `acm/loading-architecture`, tip `0538ebf`, pushed.** Before merge: the **frozen `<identity>` sentences** that go false after the split (Justin's alone — they name him explicitly), the **cross-repo pointer fixes**, and a **rebase**. **Then the cutover needs a FRESH SESSION** — the agent registry is read at session start, so a mid-session merge does not register the new agent types.

🔴 **THE ONE THAT WAS NOT EXECUTABLE, caught by ACm2 at close-out — and it is the highest-stakes precondition.** The brief said the frozen-sentence replacement drafts were *"on Justin's ballot."* **That ballot was a SPIN MESSAGE, and spin messages do not survive a session — the drafts are already gone.** ⇒ **The brief now says so plainly and instructs whoever executes it to get the wording from Justin or draft it fresh for his approval — NEVER to reconstruct it from memory.** Both sentences are located by file and block.

**ACm2 also made the rest executable rather than aspirational:** the three cross-repo pointers now carry **exact files and line numbers** (dev-workspace 13, quality 19, ops 13) **plus a re-grep instruction because line numbers drift**; the rebase carries its command, the mandatory frozen-block re-check after it, and **both traps — normalize CRLF or the check false-FAILS, and mutate a proven-non-blank line or your control silently passes** (the mistake it made and caught in this lane); and **the cutover is six ordered steps**, with **step 2 (Justin starts a completely fresh session) flagged as the one that gets skipped** and **step 3 as a hard stop** — if the four agent types are not spawnable, converting the repo manuals to redirects would strand three manuals with nothing pointing at them.

⚠️ **ACm2's own flag, which generalizes: every SHA in that brief is perishable.** `main` moved `0066754` → `500db61` inside a day. **The brief says re-derive rather than trust — but a reader in a hurry may still quote a stale number.**

### 3.3 Lane E: ⚠️ a merge-ORDER hazard its QC flagged at close-out

> *"My verification of the Atlas corrections is **arithmetic against a specific `src/` tree** — 45 files / 20,105 lines. **Any other lane that merges to `main` and changes `src/` line counts falsifies those numbers again** — and this commit's whole Atlas contribution is *correcting* exactly that class of number. My GO covers `f608d95` against base `6995007` and does not extend past either."*

⇒ **If another `src/`-touching branch lands first, Lane E's Atlas corrections may be wrong on arrival. Re-check the counts, or land Lane E first.**

---

## §4 — CONTENT THAT EXISTED ONLY IN A DYING AGENT'S HEAD

### 4.1 The sweep's reliability picture changed — see Appendix E of the findings doc

🔴 **Wave 1 (M6, M4, M3) ran WITHOUT the three methodology rulings — those were derived FROM it.** Concretely: **M6 and M4 were never told to run `npm run typecheck` alongside `npm test`.** ⇒ **Any of their invariants that is STRUCTURAL-by-compiler may have been measured through a gate that cannot see it.** **Re-check any M6/M4 GREEN under typecheck before acting on it.**

### 4.2 Lane E DC — four things for a future editor of `src/safety/halt-signal.ts`

1. 🔴 **Reintroducing the defect HIDES ITS OWN REINTRODUCTION.** Put a literal NUL back — a codemod, a find-and-replace, a "simplify" — and `git diff` renders *"Binary files differ"* again. **The review that would catch it is blinded by the very change it is reviewing.**
2. 🔴 **Never retype or copy the sentinel — always import the constant.** Anyone reconstructing it from a *rendered* view gets spaces and a value that fails `===`. **That is exactly how the Atlas came to assert space delimiters.**
3. **The invariant is "no literal NUL byte in the source," NOT "the escape is spelled `\u0000`."** The correct check is byte-level (`Buffer.indexOf(0) === -1`), not a string match. `\x00` is equally valid; `\u0000` won on a tiebreaker (it matches `JSON.stringify` output).
4. **Nothing guards any of it.** The repo-wide NUL scan exists only as a scratchpad script — **not in the repo, not in CI.** Step 106's own text says such a scan belongs in every future security sweep. **It still does not exist. That is the gap the DC would close next.**

**Also rejected, with reasons:** `String.fromCharCode(0)` (turns a compile-time literal into a computed value) · a `.gitattributes` `* text=auto eol=lf` pin (**correct in principle, touches every file, nowhere near this lane — flagged as adjacent, not proposed**).

**A method note that bit it:** *"'assert it matches exactly once' is not sufficient when your search string carries trailing context."* Its first Atlas pass asserted exactly-one match and **passed while missing half the work**, because it had anchored on a surrounding sentence and there were two occurrences with different context. ⇒ **Assert on the anchored string, then sweep separately for the bare pattern.**

### 4.3 Lane E QC — what a GO does not convey

1. 🔴 **The GO means "this change does what it claims, safely." It does NOT mean the halt contract is verified — the opposite is what it proved.** *"It got **visible**, which is different from **guarded**."*
2. **The CRLF scare, ruled out, and it will re-scare the next person:** BASE worktree is all-LF, HEAD is all-CRLF, so a naive worktree byte-comparison shows a **phantom 56-byte delta that looks like corruption**. The committed blobs are **both pure LF**. BASE escaped conversion only because git classified it binary.
3. ⚠️ **A false-positive trap in call-site auditing:** a naive `runEvrynQuery\s*\(` file filter **skips `src/reflection/consolidate.ts` entirely** (it aliases the import as `runQuery`), which **falsely reports `process.ts:1111` and `:1381` as unchecked call sites. They are checked.** Anyone re-running that audit will get the same two false positives.
4. **Its honest gap:** its "no guard exists" claim covered `tests/`, `scripts/`, `package.json`, `.github/` — **it did not check `.husky/` or `.git/hooks`.**

### 4.4 Both Lane E agents independently flag the same unverified risk

🔴 **Postgres `text` columns reject U+0000.** If the breaker trips during an operator Slack message, a string containing literal NUL code points appears to flow toward `createMessage(content_raw)` at `slack.ts:951`. **Neither had DB access; both say it is pre-existing and unaffected by their change either way.** ⇒ **Ten minutes against the dev DB settles it. It should not gate the merge; it should not get lost.**

### 4.5 ACb — six decisions not visible in its diff

Its four LIVE buckets are **judgment, not derivation — move a Step freely if you disagree** · it rejected hand-writing the reorder because **that makes the diff unreviewable for Justin, and his review is the only gate on the lane** · it deliberately did **not** rationalise the CLOSED region's ordering (*"the owed DONE-compaction pass should just compact, not re-home 87 closed Steps"*) · ⚠️ **script gotcha: the final section's preamble already contains the canary, so a reassembler that appends one emits two** · it appended only — the existing "Before you execute" and "The model" bullets are **byte-identical**.

**Two smaller owed items it named:** Step 106's status may be stale if Lane E lands, and **the dashboard status-pill note file is now redundant but was deliberately not deleted.**

---

## §5 — 🔴 GAPS AC0-36 FOUND AND DID NOT CLOSE

### 5.1 The sweep's blocker findings are NOT tracked as sprint Steps

**Verified by grep against `SPRINT-V0.2-HARDENING.md` — zero hits for every one of these:**

| Sweep finding | Where it lives now | Sprint Step? |
|---|---|---|
| Operator auth gate **fails open on blank** — unset, `""` and `"   "` all authorise a stranger | findings §C2 | ❌ **none** (Step 62 says "DONE") |
| `runEvrynQuery`'s ~500-line region has **no executing test** — the runaway kill switch included | findings §D1 | ❌ **none** |
| The **3AM approval trap** — the system instructs a recovery that is structurally impossible | findings §C1 | ❌ **none** |
| **Five shipped security inner-doors** unprotected | findings §D3 | ❌ **none** |
| The **thread-scope family** has zero tests | findings §5.2 | ❌ **none** |
| The **empty-string** env class (`??` does not catch `""`) | findings §5.1 | ⚠️ **partial** — Step 104 covers `parseInt`/NaN, which is a *different* mechanism |

⇒ **The Atlas-derived defects (Steps 100–106) are tracked. The mutation sweep's are not.** **Minting them is a source-of-truth edit and needs Justin's go.** **You have everything you need: the findings doc is committed and each item above names its section.**

### 5.2 `current-state.md` contradicts Justin's live ruling

**Justin confirmed 2026-08-13: deploy the bundle to staging → Gate-B → extract → Gate-B again.** ⚠️ **`current-state.md:64` still records *"extract BEFORE Gate-B"* as a Justin ruling, and `:50` calls the order an open question.** **Three sources, three answers. Justin's live words win. Fixing the doc is owed** (it is also Lane B's file — coordinate with 3.1).

### 5.3 Gate-B has never been run

**Steps 80 (staging stood up) and 81 (runbooks) are DONE. Steps 82, 88 and 99 are TODO.** **The infrastructure exists; the run has not happened.** The reviewer is **approved-to-commission and uncommissioned**, and a full-load scout established its brief is **not** refactor-dependent — so it can be commissioned in parallel with anything.

---

## §6 — #lock STATUS: what AC0-36 did, and what is OWED

**DONE:** ✅ the load-list recovery (Appendix E) · ✅ subagent close-out, all six · ✅ `evryn-ops/CLAUDE.md` canary, pushed `aaae21d` · ✅ `ac-dc-protocol.md` retired to `docs/historical/` with both live references rewritten · ✅ the Atlas + dependency map added to `product-full-cascade.md` · ✅ everything pushed — **8 never-pushed branches now on GitHub.**

**OWED — each is doable without AC0-36's context:**

| # | Item | Pointer |
|---|---|---|
| 1 | **Meta CHANGELOG entry** for this session | The router split, the compaction rule, the agent definitions, the retirement, Appendix E |
| 2 | **`current-state.md` refresh** | §5.2 above; coordinate with Lane B (§3.1) |
| 3 | **Founding-team current-state append** | Short, product-altitude, per `lock-protocol.md` step 4 |
| 4 | **Mint sprint Steps for §5.1's six findings** | Justin's go needed |
| 5 | **ADRs** for: the compaction rule · the agent-definition consolidation · the router split | `lock-protocol.md` step 9 |
| 6 | **`LEARNINGS.md`** — content drafted in §8 below, just route it | ⚠️ `AGENT_PATTERNS.md` is collapsed; do **not** route there |
| 7 | **Add `ac-writing-protocol.md` to the LIGHT cascade** | Justin approved the placement 2026-08-12. **It is mandatory and in zero load lists.** |
| 8 | **Fresh `pg_dump`** — newest is 8 days old, >1 week | `evryn-backend/backups/README.md` |
| 9 | **Mailboxes have content** — `dc-to-ac.md` (9), `dc-architecture-notes-for-ac.md` (1), `qc-to-ac.md` (7) | Read + absorb + clear |
| 10 | **The "separate-instance sub-AC" protocol** (§9) into `spinning-an-ac.md` | Justin's design, 2026-08-13 |

**N/A this session, logged explicitly per the protocol:** no prod deploy · no identity-file change · no Hub/spoke change · no schema migration · no `.env` change · no `settings.local.json` found · auto-memory clean (7 lines, the DO-NOT-WRITE notice).

**Cross-runtime propagation (step 8): answered in the OTHER direction.** ACf asked AC0-36 whether a credential-bearing-URL-in-a-caught-error class existed in `evryn-backend`. **Verified NO by execution** — a fetch rejection carries neither URL nor token in `.message`, `String()`, `.cause` or `.stack`; only the hostname surfaces, and the secret is the path. ⚠️ **`src/notify/slack.ts:279` passes the RAW error object to `console.error`** — not a hole today, but the site that would leak first. **Routed as cheap hardening, not a defect.**

---

## §7 — HONEST STATE OF THE CLOSE-OUT

✅ **ALL SIX subagents closed out cleanly. ACm2's close-out landed as `0538ebf` on its branch.**

🔑 **And the reason to insist on an adversarial close-out rather than a confirmatory one: ACm2's honest answer to "is your brief re-spin-complete?" was NO.** It had recorded its Phase B *test* but not its *verdicts* — so a successor could have re-derived a defensible cut but could not have answered *"why did you keep X and delete Y?"*, meaning any reviewer challenge would have restarted the judgement from zero. **It then wrote all 14 passage verdicts with reasoning, five rejected alternatives, and — the part worth copying — it NAMED THE TWO CALLS IT WAS GENUINELY UNSURE ABOUT** as the first places to look if anyone revisits Phase B, on the grounds that *"an unsure call that reads as confident is worse than the cut itself."*

⇒ **When you close a lane out, ask the adversarial question. "Confirm you're complete" gets you a yes; "could a stranger finish this from the file alone?" gets you the gap.**

**AC0-36's own errors this session, so you can audit what you inherit:** it claimed a full protocol read at ~500 of 941 lines · it declared the sweep's load lists permanently lost **without checking whether the session had ended or merely compacted** (they were recoverable, and Appendix E is the proof) · it recommended a peer act against a live Justin instruction instead of flagging the conflict · its independent byte-check of the agent definitions **reported a difference that was purely line endings** — the same CRLF trap that broke three mutation lanes.

---

## §8 — LEARNINGS drafted, ready to route (owed item 6)

1. **Compaction is not a re-spin, and the difference decides what is recoverable.** A compacted parent keeps every subagent resumable; a re-spun one loses all of them. **AC0-35 wrote "permanently lost" about six load lists that a two-minute resume recovered verbatim.** ⇒ **Before declaring anything lost with a subagent, establish which event occurred.**
2. **A reload restores what you READ, not what you CONCLUDED.** Judgements formed under the old conditions ride through untouched wearing the costume of experience. **The tell is a conclusion you can state but cannot reconstruct the evidence for.**
3. **Methodology derived DURING a multi-wave run does not apply retroactively to the earlier waves** — and the resulting asymmetry is invisible in the combined report. **Record which rulings bound which wave.**
4. **A grep that returns zero feels exactly like proof of absence.** Three independent instances in one session: a case-sensitive grep reading as content loss · a call-site audit whose file filter skipped an aliased import and produced two false positives · a naive `wc -l`/`tail -1` canary check reporting a false negative on trailing blank lines.
5. **Compare by content, never by git's verdict, when one side rewrote a file wholesale.** A *clean* merge is the dangerous outcome — it silently discards what the other side appended.

---

## §9 — Justin's new design: sub-ACs as SEPARATE INSTANCES (2026-08-13)

**The problem it solves:** a sub-AC's own children report **into the conductor's context** — *"the sub's subs output their stuff into your context, and you virtually never need the grandchild outputs, just the child outputs."*

**The design:** **one-layer subs, run them yourself as subagents. Subs-that-have-subs, run as their own SESSION** — write a self-contained brief carrying their load, and **instructions to arm a file watcher on a shared doc** so the two of you can volley without Justin relaying.

**The watcher mechanism is proven** (AC0-36 ↔ ACf-13, 2026-08-12): a `Monitor` polling a file's mtime + `git HEAD`, emitting **only on actual change**. 💰 **A Monitor bills only for lines it emits — so a 20-second poll costs nothing while the file sits still, and one turn when a message lands.** **Poll aggressively; emit stingily.** ⚠️ **Agree an explicit sign-off and `TaskStop` the watcher, or it fires on every unrelated commit forever.**

**Owed: write this into `docs/protocols/orchestration/spinning-an-ac.md`** — ⚠️ that file is on Lane A's branch, so it lands after the merge.

---

## §10 — 📋 JUSTIN'S LANE A REVIEW → MERGE → CUTOVER. Step by step, as he asked.

**Everything is a real folder on disk: `C:\Users\Justin\Evryn\Code\_evryn-meta-lane-a`. Just open the files.**

### Step 1 — REVIEW (~200 lines of genuinely new material, not 2,700)

| Open | Read | What it is |
|---|---|---|
| `_evryn-meta-lane-a/CLAUDE.md` | **all 86** | 🔴 **The router. Every agent in the org auto-loads it. Slowest read.** |
| `.claude/agents/dc.md` | **lines 1–30 only** | New header. **30–342 is DC's existing manual, character-for-character.** |
| `.claude/agents/qc.md` | **1–33 only** | Same shape; 33–535 is QC's manual. |
| `.claude/agents/oc.md` | **1–31 only** | Same shape; 31–352 is OC's manual. |
| `docs/protocols/ac-orchestration-protocol.md` | **the diff** | Phase B cuts (84 added / 333 removed) + the corrected token figures. |
| `.claude/agents/ac.md` | header + ~10 lines | **Your existing `CLAUDE.md`, moved.** |

⚠️ **Use `git diff main...acm/loading-architecture` — THREE dots.** Two dots renders `main`'s newer commits as deletions by this branch, which is a lie.

### Step 2 — THE THREE PRECONDITIONS (all yours, none delegable)

1. 🔴 **The two frozen `<identity>` sentences that go false after the split.** **Their replacement drafts NO LONGER EXIST** (§3.2) — they lived in a spin message. **Write them fresh or approve fresh wording. Do not let anyone reconstruct them from memory.** ACm2's brief locates both by file and block.
2. **Three cross-repo pointers** — `evryn-dev-workspace/CLAUDE.md:13`, `evryn-quality/CLAUDE.md:19`, `evryn-ops/CLAUDE.md:13`. ⚠️ **Re-grep; line numbers drift.**
3. **Rebase onto current `main`**, then **re-run the frozen-block check** — ⚠️ **normalize CRLF or it false-FAILS, and mutate a proven-non-blank line or your control silently passes.**

### Step 3 — MERGE, with one hazard

**Lane B (`acb/current-state-restructure`) rewrites `current-state.md` wholesale.** 🔴 **A CLEAN merge is the dangerous outcome — it silently discards ACf's team-runtime block.** **Compare by content, not by git's verdict.** ACf's block goes into Lane B's new **📮 BULLETIN** section.

### Step 4 — CUTOVER (six steps; two get skipped)

1. Stand every lane down; no worktrees but the canonical ones.
2. 🔴 **Start a COMPLETELY FRESH SESSION.** The agent registry is read **once, at session start** — a mid-session merge registers nothing. **This is the step that gets skipped.**
3. 🔴 **HARD STOP: spawn `dc`, `qc`, `oc` by name and confirm each reports its own manual's canary.** **If they are not spawnable, STOP** — converting the repo manuals to redirects now would strand three manuals with nothing pointing at them.
4. Only then: repo `CLAUDE.md`s become redirects. **Repos stay as backups** (your call, 2026-08-12).
5. Re-verify post-merge.
6. Lanes back up.

✅ **A clean before/after you already have:** the probe today reported the project manual's first line as `# CLAUDE.md — AC (Architect Claude)`. **After cutover it should read `# CLAUDE.md — the agent router`.**

---

## §11 — 🧹 FRAGMENT SWEEP — everything from this session that has no other home

**Dumped rather than polished, on Justin's instruction, because this is the material that dies with AC0-36's context.**

1. 🔴 **THE PROBE FINDING THAT MATTERS MOST, and it is about every agent we run.** The Haiku probe **read its standing instructions, read the live prompt, judged they conflicted, and chose the prompt** — then disclosed it accurately: *"That was a choice, not blindness… I should have either executed the standing report format because it's load-bearing, or surfaced the conflict explicitly. I did neither."* ⇒ **This is the same silent-conflict-resolution failure we chase in big agents, appearing in a throwaway probe with three sentences of instruction. It is the DEFAULT, not a quirk.**
2. 🔑 **The layer distinction that follows, and it bounds what agent definitions can buy us:** `model:` and `tools:` in frontmatter are **harness-enforced — an agent cannot override them.** **Prose in a definition is advisory.** ⇒ **Moving a prose rule into a definition buys nothing.** Only the two structural fields do.
3. ✅ **Canary confirmed:** on Opus the probe returned `MERIDIAN-SEVENTEEN-ALBATROSS` first, then answered the arbitrary question; a bare "report" produced the exact four-part format. **So a definition's BODY genuinely programs the agent** — the Haiku result was a model-capability artifact, not a mechanism failure.
4. **Agent-type registration: CONFIRMED, and the registry is REPO-SCOPED and read at SESSION START.** Three seats proved it: ACm2's worktree spawn failed, AC0's canonical-tree spawn failed **mid-session**, Justin's **fresh session** succeeded. ⚠️ **`lucas` is NOT a spawnable type from `_evryn-meta`** — "load Lucas" works by *prose*, via the workspace `CLAUDE.md`. **Registration and say-the-name are unrelated mechanisms; they had been conflated.**
5. 🔴 **This is the first known case where RE-SPIN beats compact-and-continue** — a session-start read is invisible to a reloaded instance no matter how well it reloads.
6. **Justin's standing rule on the canary, verbatim:** *"I don't want you to finish at 1am and have it firing every 5 min until I show up in the morning, burning a turn every time — I'll go broke paying for those tokens."* ⇒ **`TaskStop` it deliberately; and if you hand off with one live, NAME ITS TASK ID — a successor cannot stop what it does not know exists.**
7. **Arm the canary whenever Justin steps away — *including for twenty minutes.*** Its primary job is **not** watching subagents; it is **stopping AC from silently pausing**, *"because that happens way too often and I lose all the time while I'm gone."*
8. 💰 **Monitor economics:** it bills **only for lines it emits.** A 20-second poll costs nothing while a file sits still. **A heartbeat timer bills a full turn per tick.** ⇒ **Condition-gate whenever the thing you want is an event.**
9. **`evryn-langgraph-archive` and `evryn-prelaunch-landing` return 403 on push.** ✅ Both are 0-ahead with clean trees — **nothing was lost.** They are the Sealed and Inactive repos; **the wall is expected, but know it exists before you ever need to write to them.**
10. **The `claude.ai Google Calendar` MCP connector is unauthenticated** and cannot be authorised from a non-interactive session. Four agents have now flagged it. **Justin's connector settings; no bearing on any current work.**
11. **AC0-36 committed one edit of Justin's** (`haiku → opus` on the probe) rather than let it evaporate at shutdown — attributed in the commit message. **Flagged so it is not mistaken for AC's work.**
12. **ACb's two smaller owed items:** SPRINT **Step 106's status may be stale** if Lane E lands, and **the dashboard status-pill note file is now redundant** but was deliberately not deleted.
13. **AC0-36's own `CLAUDE.md` catch, still unverified by anyone:** the all-branches enumeration script prints **every branch once per worktree** — with eight worktrees it printed everything eight times. **Not wrong, but noisy enough that a real hit could be missed in the wall. It is a `CLAUDE.md` script; nobody has touched it. It wants Justin's eyes.**

---

## §12 — 🔴 AC0-37's ACTION LIST. Written from a fresh-eyes pass; these are the gaps a cold reader would hit.

### 12.1 The lanes you must RE-SPIN, and what each replaces

**Every agent in §2 dies with this session (or lives, if you are its compaction — read §2's table first). These are the successors:**

| Spin | Replaces | Off which brief | State |
|---|---|---|---|
| **ACm3** | ACm / ACm2 (Lane A) | `docs/working/2026.08.11-ac0-acm-loading-architecture-brief.md` **on branch `acm/loading-architecture`** ⚠️ **`main`'s copy is the original and has none of §8–§11** | ✅ **Re-spin-complete, adversarially verified.** Its §11 carries all 14 Phase-B verdicts + 5 rejected alternatives. **Do not re-litigate the ~600-line floor — it is retired.** |
| **ACb2** | ACb (Lane B) | `docs/working/2026.08.11-ac0-acb-sprint-current-state-brief.md` **on branch `acb/current-state-restructure`** | ✅ **Re-spin-complete** (`5f14ab0`). Finishing the lane = reconcile the merge, flip Step 106 if Lane E landed, carry the 5-item ballot. |
| **A fresh QC** | Lane E's QC | — | ⚠️ **Only if Lane E is re-reviewed. Do NOT resume a QC for independence — resume is a continuity tool.** Its GO stands at `f608d95`; see §3.3's merge-order caveat. |
| **The Gate-B reviewer** | *nothing — never spun* | `evryn-backend/docs/gate-b-plan.md` (branch `gateb/plan`) | 🔲 **Approved-to-commission, uncommissioned.** A full-load scout established its brief is **not** refactor-dependent, so it can run in parallel with anything. |

⚠️ **The sweep lead, Lane E's DC and Lane E's QC have NO brief of their own.** Their durable homes are the **findings doc** (incl. Appendix E), **branch `dc/step106-nul-bytes`**, and **§4.2–§4.4 of this file**. ⇒ **There is nothing to re-spin them "off." If that work needs continuing, it is a fresh lane with a fresh brief.**

### 12.2 Worktrees — ⚠️ JUSTIN'S EXPLICIT INSTRUCTION: **do NOT reap until you have confirmed you have everything.**

**Eleven-plus worktrees exist. AC0-35 and AC0-36 both deliberately left them all.**

| Category | Status | Reap? |
|---|---|---|
| `_evryn-meta-lane-a` · `_evryn-meta-lane-b` · `evryn-backend-lane-e` · `evryn-backend-lane-b` | **HOLD UNMERGED WORK** | 🔴 **NO. Not until merged.** |
| `evryn-backend-m1…m6`, `-sweep`, `-qc-base`, `-qc-head` | Sweep + review sandboxes; **work finished** | ⚠️ **Candidates only** — they hold regenerated dependency maps and probe residue. **Read the findings doc first.** |
| ACf's (`_evryn-meta-acfl`, `-acfsq`, `-lock`, `evryn-team-workspace-acf13`) | **NOT YOURS** | 🚫 **Never. Route through ACf.** |

🔴 **THE HAZARD, and it has fired three times: reaping a worktree can silently wipe the CANONICAL tree's `node_modules`, printing nothing.** ⇒ **Count before, count after, `npm ci` if it dropped** (`CLAUDE.md`, Worktree & Branch Discipline). **Never `--force`.**

### 12.3 🔴 THE SIX SWEEP FINDINGS THAT ARE TRACKED NOWHERE — this is the biggest owed item on the list

**Full table with section pointers: §5.1.** Restated here because it is the thing most likely to get lost:

**A mutation sweep proved six defects BY EXECUTION. Not one of them is a sprint Step.** They exist only in `docs/working/2026.08.12-qc-sweep-findings.md`, which is a *working doc* — the category this org retires and archives.

**Two of them are pre-gatekeeper safety:** the **operator auth gate fails OPEN on a blank value** (unset, `""`, `"   "` all authorise a stranger on the gate deciding who approves outbound mail to real people), and the **runaway-cost kill switch has no executing test** — deleting it passes all 1,925 assertions.

⇒ **Minting these as sprint Steps needs Justin's go. Ask for it early — a findings doc is not a tracker, and this one is one archive-sweep away from being invisible.**

### 12.4 The frozen sentences: **NOT persisted. They are gone.**

**Asked directly and answered honestly: the replacement drafts were never written to a file.** They lived in a spin message; spin messages do not survive a session. ⇒ **They cannot be recovered and must NOT be reconstructed from memory** — ACm2's brief records this and locates both target sentences by file and block. **Justin writes them fresh, or approves fresh wording. This is a merge precondition, and it is his alone.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
