# LANE 2 (ACfj) — The per-job money layer (SPRINT Steps 19 + 20)

**Lane AC: `ACfj`. Conductor: ACf-13 (or its successor).** Opened 2026-08-17.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file:** your brief is §1–§6. **Everything below the line is a running two-way exchange — append, never overwrite.** The mechanics (commit immediately, batch questions, hard limits, how to spin your own DC/QC) are in **`_evryn-meta/docs/working/2026.08.17-acf13-lane-protocol.md` — read it first; it is short and binding.**

---

## §0 — 🔴 YOUR FIRST ACTION, BEFORE ANY WORK

**Append an entry to this doc, and commit it, BEFORE you load anything.** It carries three things:

1. **You are alive** — name your worktree and branch.
2. 🔴 **Every question or concern you have about your ability to work through the NIGHT, unsupervised.** Not about the work — about the *conditions*. Anything you can foresee that would stall you at 2am with nobody awake: a gate you will hit, a permission you will need, a tool you are not sure you have, an instruction in this brief that contradicts another. **Say it now, while the conductor is still here to answer.**
3. **Anything in the brief you think is wrong.** You have not read the runtime yet, so this is about the brief's internal logic, not its facts.

**Why this comes first: the conductor and you have never run this channel before.** It proves the round trip works *before* either of us builds on the assumption that it does — **and it is the last cheap moment to catch a condition that would end your night silently.** *(The conductor already caught one this way: an early draft of the protocol forbade the very commit this channel depends on.)*

**Do not start the load until that commit lands.**

## §1 — 🔴 THIS IS A DESIGN PASS FIRST. DO NOT BRIEF A BUILDER YET.

**This is the difference between this lane and the others, and it is deliberate.** *"Wire the money layer"* sounds like a build and is not one yet.

**The state of the thing, verified: every wake dispatches with `jobId: null`, nothing anywhere creates a `jobs` row, and 0 of 16 recorded cost events carry a job id.** The tables exist. The forecast math exists. The 60%-pitch gate and the 100%-freeze exist. **None of it is reachable, because nothing upstream ever creates the object they all operate on.**

**⇒ So the first question is not "how do we wire it" but "what IS a job in this runtime?"** — what creates one, when it opens and closes, what happens to wakes that belong to no job, and whether a job spans threads, agents, or neither. **Brief a builder before that is settled and you will get a confident guess.**

**Your first deliverable is a design entry in this doc, not a branch.**

## §2 — Why it matters, and the specific thing it blocks

**It is on the critical path to the Meta-Meeting in a way the tracker does not make obvious.** Gate G3 is the eight acceptance paces on Lucas. **Pace 5 tests exactly this layer** — a $2 cap accrues, the 60% forecast beat fires, 100% freezes and parks and pings.

⇒ **Pace 5 is not merely unrun. It is UNRUNNABLE.** There is nothing for it to test. **Whoever picks up G3 walks into a test they cannot execute**, and that is the discovery this lane exists to prevent.

## §3 — Scope

- **Step 19** — the per-job money governor. The tracker carries the verified state.
- **Step 20** — ⛔ currently BLOCKED on 19: the **frozen-job park path** leaves no record, no ping and no resume, and the scheduler consumes the trigger. **Latent today; it ARMS the moment jobs are wired.** ⇒ **It bundles with your work rather than following it — do not ship 19 and leave 20 for later, because shipping 19 is what makes 20 dangerous.**
- **Step 23** (a dashboard spend view) is also blocked on you, but it is a different repo and a different lane. **Not yours. Note in this doc when 19 lands so it can unblock.**

⚠️ **Step 17 — raising the global spend ceiling from $10/hr — is NOT yours.** It is a Railway environment variable applied at the deploy window. **Named here only so you do not build around the current value:** at $10/hr an eight-agent meeting halts at roughly 1.5 messages. **And know what that ceiling actually measures** — it sums an *API-equivalent valuation*, so on the Max subscription real per-wake dollars are ≈$0. **It is a usage-intensity proxy, not a money cap.** Do not design as though it were one.

## §4 — Your territory, and the collision that sets your sequencing

**Yours:** `src/money/**` · the dispatch path in `src/wakes/index.ts` · `src/scheduler/` dispatch sites · your migration if the design needs one · tests.

🔴 **`src/wakes/index.ts` is SHARED with ACft (Lane 1), which is rewriting the COMPOSITION path in the same file, starting now.** **Your design pass is what keeps you out of each other's way: design while they build, and do not open a branch on that file until Lane 1 has merged.** The conductor will tell you in this doc when that happens. **If your design turns out to need dispatch changes before then, say so here rather than reaching in.**

## §5 — Two things to get right

- **It fails SAFE today, which is why nobody has fixed it in months.** Nothing runs away; the layer simply does not exist in practice. **That is not a reason to scope it small — it is the reason it stayed invisible.**
- **This is the money/safety domain, not memory.** The tracker says plainly it *"needs its own owner."* **You are it.** Where a design choice affects what Emma (the CFO agent) can see or decide, **flag it `NEEDS-JUSTIN` rather than choosing for her.**

## §6 — How much the conductor has actually read

**ACf-13 is running the conductor cascade. It has read the tracker, ARCHITECTURE and the changelog — it wrote much of them — but almost none of `src/`.** **You will be the better-informed party on every question of fact.** Treat every factual claim here as a **lead, not a fact**, including the `jobId: null` and 0-of-16 figures — **they came from the tracker, not from the conductor's own read.** Say so loudly if any of it is wrong. And where this brief is silent, assume the conductor could not see it rather than that it checked.

---

## THE EXCHANGE — append below

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
