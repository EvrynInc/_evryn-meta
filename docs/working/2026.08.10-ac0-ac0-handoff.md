# Handoff — AC0-32 → AC0-33 — 2026-08-10

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this: START HERE.** Written mid-session, deliberately, before any compaction risk — not at set-down. It **supersedes `docs/working/2026.08.06-ac0-ac0-handoff.md` operationally**, but that file is NOT retired: it remains the home of the reload manifest, the two-trip-loading explanation, the go-live sequence (§9b), and the do-not-re-open list. **Read it after this one.**
>
> 🔴 **The 2026-08-05 handoff also stays live** — it holds ACh's P1–P11 queue and the older `#lock` ledger.

---

## §1 — WHAT THIS SESSION ESTABLISHED THAT NOTHING ELSE RECORDS

### 1.1 🔴 A FULL-RUNTIME LOAD NO LONGER RELIABLY FITS IN ONE AGENT. This is the session's biggest finding.

**Three agents attempted the product's full cascade + both runtime halves (~20,047 `src/` lines + 1,378 identity + ~5,000 docs). Two compacted during the LOAD, before doing any work.** A third fit only after ~1,570 lines of docs were trimmed — and then **compacted during its work anyway**.

**What this means, stated plainly: every "full load" spin on `evryn-backend` for months has been running with near-zero headroom, and we only discovered it because two agents were honest enough to disclose.** The protocol's standing rule — *"when in doubt, OVER-LOAD; we have had a thousand times more failures from under-loading"* — is still right about the *direction*, but it silently assumes over-loading is always *possible*. **It isn't any more.**

⚠️ **Do NOT respond by trimming lists.** That is the intuitive fix and it re-creates the under-load failure. **The answer is to restructure the WORK** — see §1.2.

### 1.2 The shape Justin wants explored: every lane runs LIGHT and keeps a resumable SCOUT

**His steer, 2026-08-10:** rather than a lane holding the runtime itself, *"I probably would have had ACa running its own scout, so that it could 1) have a good list and 2) be able to resume the scout if it had questions."*

**Why this is better than what AC0-32 did, and the reasoning is worth carrying:** a scout's value was never *"it holds 20,000 lines."* It is *"it can answer questions against 20,000 lines, re-reading as needed."* **Resume + targeted re-read beats resident memory** — and it inverts the problem, because the lane stops needing to *be* the thing that holds the codebase.

**His open question, NOT yet answered — think about it carefully, it is becoming standing workflow:** *"ACa's scout is going to be at the edge of compaction too — so how do we do this? Fan out agents, but overlap them enough that we can see the seams? … we have to get extra room somehow. But we then need to get it into a usable form, so it's not just compressed to hell when we're reasoning over it."*

**AC0-32's partial thinking, offered as a starting point, NOT a decision:**
- **Naive partition fails**, because the defects live *at the seams* (Step 100: an identity file instructing a tool the code doesn't register on that pathway; Step 96: the `client.ts → poll.ts` return contract). Split by directory and nobody sees a seam.
- **The seam surface is far smaller than the runtime.** Exports, signatures and the import graph are perhaps 1,500–2,500 lines of the 20,000. **That is the layer that must be held whole; bodies can be paged in.** ⇒ *This is what the Atlas is*, which is why deliverable 1 unlocks everything after it.
- **Overlap the interface layer; partition the implementation layer.** Every specialist gets the full interface surface plus its own subsystem's bodies — so each sees every seam it touches from both sides.
- **Synthesize by WRITING, not by summarizing.** N agents → N reports → whoever merges compacts them, which is Justin's *"compressed to hell"* failure. Instead each agent writes its own section of a shared committed artifact; nobody holds all sections. **The artifact IS the synthesis mechanism.**
- **Keep the specialists alive as a bench.** Resume is indefinite within a session. A cross-seam question asked of *both* sides and reconciled is a **better** check than one agent's memory — adversarial by construction.
- ⚠️ **Hard limit to design around: resume dies at session end.** Everything durable must be written down.

---

## §2 — AGENT LEDGER (all session-bound; unreachable to a fresh instance)

| Agent | `agentId` | What it is | State |
|---|---|---|---|
| **ACs** — runtime scout | `abb7c6a77cea796c1` | Full-load map + READ-NOW list; answered 6 targeted questions | ✅ Done, read-only, nothing owed. **Resumable — use it for targeted runtime answers.** |
| **ACa** — atlas lane | `ac58a7a30863490c4` | Deliverables 1+2 | ✅ Landed. **Compacted during work** (disclosed, handled correctly). Worktree `evryn-backend-atlas` / `aca/atlas-refactor`. |
| **ACa's DC** — dep map | `a386294e8c65b0b36` | Built the map | ✅ Landed. Committed `e4f7110` on `dc/aca-depmap`, worktree `evryn-backend-aca-depmap`. **Unreaped.** |
| **ACn** — safety-net lane | `ae9c0178f6e7aa98f` | Step 101 + boot-failing suites | 🔄 **STILL OUT at time of writing.** Worktree `evryn-backend-safetynet` / `acn/safety-net`; sandbox `_acn-sandbox`. |
| **ACn's DC** — Step 101 | `a3949ab2233c79ca5` | Built the guard | ✅ Landed (reported through to AC0, not just ACn). Work **uncommitted** in `evryn-backend-dc-step101`. |
| **DC** — canary sweep | `a5e76d1b734f89dff` | Load verified, **idle** | ⏸ **Awaiting trip 2 — BLOCKED on Justin's top-vs-bottom ruling.** Worktree `evryn-backend-canary` / `dc/canary-sweep`. |

🔴 **Worktrees live right now (7):** canonical · `_acn-sandbox` · `evryn-backend-atlas` · `evryn-backend-safetynet` · `evryn-backend-canary` · `evryn-backend-aca-depmap` · `evryn-backend-dc-step101`. **None reaped. Nothing merged, nothing pushed, nothing deployed. Prod remains `v0.2.9`.**

---

## §3 — FINDINGS, WITH PROVENANCE ATTACHED

### 3.1 🔴 FINDING — "the sole Anthropic spend door" is FALSE, and it becomes a real money hole at the Haiku flip. **AC0 verified this at source personally.**

- **Two SDK `query()` sites exist:** `src/triage/classify.ts:2867` and `src/triage/haiku-screen.ts:406`.
- **`recordAttemptAndCheck` has exactly ONE call site** — `classify.ts:2678`. **Nothing on the Haiku path calls it**, so screen attempts are never counted by the runaway detectors.
- **`ARCHITECTURE.md:1171` already flagged this UNVERIFIED and reasoned it was safe because *"a tripped breaker already skips the screen."* That reasoning is circular** — the detectors are what *trip* the breaker.
- **Masked today** (`HAIKU_SCREEN_MODE` unset → `off`; in `shadow`, Opus still runs so every screen call is paired with a counted attempt). **In `active`, an `ignore`/`pass` short-circuits Opus entirely — a runaway confined to the screen spends money invisibly to both detectors.**
- ⇒ **This is a hard precondition on the `active` flip**, which is a *planned, money-motivated* action (~$1,800/mo).

### 3.2 The truncation-canary picture — measured three ways, worse than believed

**0 of 45 `src/` files have a TOP canary. 24 have bottom-only. 21 have neither.** The convention is **stratigraphic**: adopted at the 2026-06-22 M1 build and applied only to what was written after — `safety/`, `security/`, `constants/` are 100%; the original email/db/core files have none. **No sweep has ever run on this repo** (the 87-file sweep was ACf's, on `evryn-team-runtime` — a different repo).

🔴 **BLOCKING DECISION, unanswered:** adding a TOP canary shifts every line down by one, and **there are 1,096 `file:line` references across 65 files**, including the active sprint doc and both live lane briefs. AC0's recommendation: **bottom-only now (21 files, zero shift); the top note rides the extraction when lines churn anyway.**

### 3.3 The extraction's blast radius — corrected twice, and the fix is a grep that is ALSO wrong

Brief said 8 importers. Scout said 16 (8 src + 8 test). **ACa measured 21 (8 src + 13 test).** ⚠️ **And the grep AC0 prescribed as the durable fix — `grep -rn 'triage/classify' src tests` — is DEFECTIVE: it cannot match `./classify.js`, silently dropping the two same-directory importers (`verdict.ts:57`, `preload.ts:37`).** Any replacement must match both forms.

### 3.4 An undocumented 7-file import cycle

`approval/flow ↔ email/cluster ↔ email/process ↔ notify/slack ↔ triage/classify ↔ triage/preload ↔ triage/verdict`. **Does not contradict `ARCHITECTURE.md:1160`** — the specific `classify→safety→poll` cycle really is broken. The claim is true but *incomplete*, and nobody tracked it because nothing generated it. **Best single argument for the dependency map existing.**

### 3.5 Step 101 is done and MUTATION-PROVEN

Five deliberate breakages, all caught, all restores green. **The decisive proof:** the identity loader was gutted to `core.md` only (8 of 9 identity files gone) and the **original** test passed all four assertions. No `src/` change was needed.

⚠️ **AC0 was WRONG about this file and was corrected from below:** AC0 told the lane the `loadReflectionPrefix` checks were "genuinely discriminating." **They carry the identical memo defect** (`cachedReflectionPrefix`, `classify.ts:246`/`:261`). Corrected by DC.

---

## §4 — THE PROCESS CHANGE JUSTIN INSTITUTED TODAY (already written into the conductor cascade, UNCOMMITTED)

**LAND everything → ONE conversation with every question in a single ballot → sign-off → LAUNCH → SILENCE until the wave lands.** Reason: questions posted mid-wave compete with subagent output in his terminal and lose. **Foreground/background does not help — subagent output renders to him regardless.**

**Hard floor that still interrupts:** the escalation ladder's 🔒 items, a blocker on the thing he just said to do first, and **a material new fact that changes the shape of something already authorized.** *(Test: would he decide differently knowing it?)*

🔴 **Also codified: a scope change to an already-approved lane is JUSTIN'S call, not the conductor's.** Live example: AC0-32 trimmed ACa's load to beat the context ceiling and presented it as a completed fix. His response: *"not sure I would have signed off on that"* — followed by a materially better alternative (§1.2).

**Uncommitted in `docs/protocols/load-cascades/conductor-cascade.md`:** the cadence section above, plus a new **"depth on a FRAGMENT feels exactly like authority"** discipline — the tell is grammatical (*"the right fix is X"* → overstepped; *"I think X, check me"* → correct), and it binds what you tell Justin as hard as what you write in briefs, because he cannot evaluate the code and your confidence is his only signal.

---

## §5 — OPEN, AND OWED

1. 🔴 **The canary top-vs-bottom ruling** — DC is loaded and idle waiting on it (§3.2).
2. **The batch ACa handed up** (in its lane brief §6): count the Haiku attempt before any `active` flip · correct four stale safety claims + close `ARCH:1171` · the TTL doc-fix · add `scripts/**/*` to `tsconfig.tests.json` (**measured: 0 errors**) · sign off the decomposition sequence · commit-or-hold the dependency map.
3. **ACa's proposed decomposition, for sign-off before deliverable 4:** `rendering.ts` → `stamps.ts` → `validators.ts` → `agent/tools/` → `agent/identity.ts` (last, carries the `IDENTITY_DIR` landmine). **Its new argument: steps 1–2 sever `verdict.ts` and `preload.ts` entirely, so the cheapest extraction also attacks the 7-file cycle.**
4. **ACa disagreed with AC0 on the v0.3 seam question, with better reasoning:** `classify.ts` is *not* what is single-threaded — **the serialization is in the caller** (`planPollBatch` awaits `handle(email)` serially, `ARCH:1165`). ⇒ **The extraction will not deliver v0.3 concurrency and must not be sold as doing so.** And the serial drain is *currently doing safety work*.
5. **Subagent close-out is owed** — 7 worktrees, several unreaped, and two DCs whose sandboxes only their own parent can dispose of. **Resume is session-bound: when this session ends they are unreachable forever.**
6. **Inherited, untouched:** the two doc-staleness flags (`current-state.md` names no current SHA; `SPRINT-V0.2-HARDENING.md`'s header still says `LIVE = v0.2.4` when prod is `v0.2.9`).

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
