# Lane brief — `ACfsq`: should the team runtime's agents have SESSIONS?

*Written 2026-08-11 by **ACf-12** for a lane to be spun by **ACf-13**. **Read-only research and design.** No code, no edits, no merge.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file.** The lane's shared surface — `ACfsq` reads it in full, appends findings as it goes, and **keeps it current enough to re-spin from cold.** Commit freely; brief-class traffic is pre-authorized. The tagged `<identity>`/`<mandatory_load>`/`<receipts>` blocks are pasted verbatim by whoever spins the lane; the file list is in the ACf-13 handoff.

---

## §0 — 🔴 How much the author of this brief had read

**ACf-12 wrote this running the conductor cascade: ~450 lines of `src/`, zero migrations, zero tests.** Every claim here is a **LEAD, not a fact** — including line numbers. **You will read the system; that makes you better informed than this document.** Re-derive what the work rests on, say so loudly if any of it is wrong, and **where this brief is silent, assume the author could not see it rather than that they checked.**

---

## §1 — The question, and why Justin thinks it matters

`evryn-team-runtime` composes each agent **fresh, every wake**: identity + memory + the thread transcript + ambient context are assembled into a new prompt, the agent acts, the wake ends.

**Justin's observation, which is correct at source:** *"You've got the whole thread… but that's kind of it. You don't have access to your thinking during that turn, or any of the other deep layers — just the superficial layer."*

**Verified:** `src/wakes/index.ts` is the only place a main's model is called, and it passes **no `resume`, no `sessionId`, no `continue`.** So what survives a wake is: identity, `MEMORY.md`, the thread transcript (including the agent's own past replies), digest blurbs, the private scratchpad, and any note deliberately written. **What does NOT survive: the agent's reasoning, its tool calls and their results, anything it loaded mid-wake, and every intermediate turn** — up to 40 of them.

**⇒ The concrete cost, and it is real:** an agent that spends a wake investigating something and concludes *"nothing here yet, I'll watch it"* has, by the next wake, **no memory of having investigated at all. It cannot know what it already ruled out.**

Justin's word for it was *"a disaster,"* and **he is reconsidering the decision to avoid SDK sessions** — *"if we keep threads under control, it's not a terrible idea. And if we give the agents a standing order to announce if they compact, that even keeps that danger from being a problem."*

**This would reverse part of ADR-052 §3.2. It is the most important open question in this lane.**

---

## §2 — What has already been established, so you do not redo it

**ACfm's prior analysis** (`_evryn-meta/docs/working/2026.08.11-acfm-acf-runtime-questions.md`, Q1) reached these, and you should **treat them as leads to verify, not settled**:

- **The original objection was that SDK compaction summarises history opaquely and un-steerably** — a summariser whose output we do not control. Fresh composition keeps every byte enumerable, logged in the wake manifest, and diffable.
- **Justin's counter is genuinely strong:** compaction only fires when a context grows large, and Step 4's membership model plus Step 5's 7-day age-out exist precisely to keep rooms small — so under tight membership **most threads should never reach compaction at all.**
- **⚠️ But the counter-counter has not been reckoned with:** an announcement tells you compaction *happened*; **it cannot tell you what was lost** — that is the nature of the failure. And **a session's contents are not enumerable in the wake manifest**, which is one of this runtime's genuinely unusual strengths (ARCHITECTURE cardinal invariant 2).
- **The cheaper alternative that targets the actual pain:** the per-(agent, thread) **scratchpad already exists and already composes back into that room's wakes.** Nothing today instructs an agent to use it as a working record. *"Before you finish, put in your scratchpad what you looked at and what you ruled out"* recovers most of the lost value at **zero architectural cost**, and it is steerable and inspectable.

**🔴 The open UNKNOWN that gates everything:** *does an SDK session survive a process restart?* The whole runtime rests on ARCHITECTURE cardinal invariant 4 — **everything durable is in Postgres; crash → restart → re-arm losslessly.** **A session living in SDK-side state would be the first thing in this system that is not.**

---

## §3 — 🔴 What the runtime scout added, and it is the strongest argument on the table

**The identity half carries a cost invisible from `src/`.** `evryn-team-workspace/shared/protocols/runtime-ops.md` — **composer layer 1, composed into every wake of every agent** — tells each agent, in §1, that **there is no hidden longer session behind this; what you see is what you have.**

**⇒ Adopting sessions makes that sentence FALSE in the cache-stable prefix of all eight agents simultaneously.** So the change is not only architectural; **it silently invalidates a standing instruction every agent reads every time it wakes**, and correcting it is a Justin-gated identity edit that invalidates every agent's cached prefix fleet-wide.

**That is why the identity half is on your load list and is mandatory for this lane specifically.** No other lane needs it; you cannot answer this question without it.

---

## §4 — 🔴 Pre-decided constraints on your deliverable

1. **🔴 RETURN A RECOMMENDATION CONDITIONAL ON BOTH BRANCHES OF THE PERSISTENCE UNKNOWN. Do not resolve it by assumption.** Your output must read *"if sessions survive a restart, then X; if they do not, then Y"* — **not** a single confident answer resting on a guess. **This is the single most important instruction in this brief:** an unknown quietly resolved is exactly how a confident, wrong architecture decision gets made.
2. **On the unknown itself: go to the pinned SDK.** `@anthropic-ai/claude-agent-sdk` is pinned at **`0.3.207`**; read its typings in `node_modules`. ⚠️ **If it cannot be established from the typings, report UNKNOWN — do not infer it from documentation you half-remember or from how you imagine it works.**
3. **You write NO code and edit NO files** except this brief and your deliverable doc. **No merge, no push, no worktree needed.**
4. **You do NOT edit `runtime-ops.md` or any identity file** — you may *recommend* wording; landing it is Justin's.
5. **Do not re-litigate ADR-052 wholesale.** The question is narrow: *sessions or not, and if not, does the scratchpad instruction close enough of the gap?*
6. **Give the cheap option a fair hearing.** ACfm's recommendation is *"do not swap the model wholesale, and do not dismiss the problem either"* — instruct the scratchpad as a working record first, then trial sessions on **one agent in one long-running thread** and measure. **Say plainly whether you agree, and why.**
7. **Anything genuinely ambiguous: BATCH IT** into one numbered recommend-first list. Keep advancing everything not blocked.

---

## §5 — Deliverable

**A recommendation doc at `_evryn-meta/docs/working/2026.08.12-acfsq-acf-session-question.md`**, carrying:

1. **The plain-English statement of what is lost today**, written for Justin — who is not an engineer and will read this cold.
2. **The conditional recommendation** (§4.1), with the persistence finding stated as verified-or-UNKNOWN.
3. **The full cost ledger of adopting sessions** — including the wake manifest, invariant 4, and the `runtime-ops.md` §1 contradiction from §3.
4. **The cheap alternative, costed** — what the scratchpad instruction would and would not recover.
5. **If you recommend a trial: its exact shape** — which agent, which thread, what you would measure, what result would decide it.
6. **What you could NOT establish**, named honestly.
7. **Whether this warrants an ADR** (it would reverse part of ADR-052 §3.2). **Recommend; do not write one** — that is Justin's call.

**Write it per `_evryn-meta/docs/protocols/ac-writing-protocol.md`** — canary, how-to-use header, structure over walls, and no claim stated as verified that you did not verify.

---

## §6 — Notes appended by the lane

*(`ACfsq` appends here. Keep it current — this is the crash-survival surface.)*

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
