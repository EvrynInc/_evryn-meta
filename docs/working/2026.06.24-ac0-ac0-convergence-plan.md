# Round-2 Convergence Plan — AC0 (2026-06-24)

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

> **⭐ THIS IS THE NEXT-AC0 HANDOFF — START HERE. Read the `CONVERGENCE EXECUTION STATE` § first** (a few sections down) — it is the current truth and supersedes the older planning sections below it. **As of 2026-06-25 the convergence MERGE is COMPLETE:** all 5 lanes are merged into `r2/convergence-bundle` (`d149535`, pushed), tsc-clean + suite green + fragment-swept. This doc unlocks the rest: it points to the **one companion you also need — the persistence checklist (`docs/working/2026.06.25-ac0-ac0-persistence-checklist.md`)**, whose Sections A/B/C/E/K are the "what-lands-where" detail referenced below; and the **lane handoffs** (`docs/working/2026.06.23-ac{1a,3a,5a}-*`, `2026.06.24-ac2a-*`, `2026.06.24-acc-*`, `2026.06.24-ac4a-*`) as deeper per-lane reference. current-state carries the one-paragraph summary (auto-loads).
>
> **What's left (all in the EXECUTION STATE §):** integration-QC the bundle → Step-32 + gold-destination tests → persist (per the persistence checklist) → **[gated, with Justin]** merge-to-main + prod migrations + deploy → cleanup. No merge to `main`, no deploy without Justin's explicit go.
>
> Working/ephemeral; retire after the bundle ships. *Authored 2026-06-24 by AC0; EXECUTION STATE updated 2026-06-25 (merge complete).*

---

## Status snapshot (2026-06-24)

| Lane | AC | Branch | State | On origin |
|---|---|---|---|---|
| A — ingest/resilience | AC1a | `r2/lane-a-ingest-resilience` | **QC-GO, closed** | ✅ |
| C — cost/Reflection | AC3a | `r2/lane-c-cost` | **QC-GO, closed** (live-inert until Step 66) | ✅ |
| M1 — silent-death | AC5a | `r2/m1-silent-death` | **QC-GO, closed** (THE go-live blocker) | ✅ |
| B — operator/approval | AC2a | `r2/lane-b-operator-approval` | in flight | branch not yet pushed (no commits yet) |
| staging | AC4a | (no code worktree) | in flight | — |

`main` (evryn-backend) = `722a8d6`. `_evryn-meta` main = pushed. The 3 closed lanes' branches all pushed.

**Code convergence bundle = A + B + C + M1** (shared seams). **Staging (AC4a) is decoupled** — infra/playbook/OC track, lands independently, NOT in the code merge.

---

## UPDATE 2026-06-24 (post-ACc) — both GAPs CLOSED + the Lane-A branch stack

ACc (the build arm) shipped both net-new builds — **GAP 1 + GAP 2 are now CLOSED** (built, QC-GO ×2 each, pushed). The Lane-A line is now a **stack of branches, all off `r2/lane-a-ingest-resilience` (`6a6b38b`)**, to be layered at convergence (merge Lane A once, then layer each delta):
- `r2/lane-a-step21-durable-cursor` (ACc, `a6a96c3`+`35506f4`) — **Step 21 / GAP 1.** Durable cursor via a data-derived bookmark (newest `emailmgr_items.created_at` − 1h margin) + paginated Gmail date-search fallback; err-old invariant; existing `external_id` dedup absorbs overlap; `client.ts` stays DB-free. Files: `db/items.ts`, `email/client.ts` (FETCH-side: catch-up fns), `email/poll.ts` (1 line), 2 tests. **No migration.** M1 interlock now SATISFIED (Step 19+21 both on the base).
- `r2/lane-a-register-tool` (ACc, `1886515`+`e5d7c16`+`3d948ae`) — **register tool / GAP 2.** `register_gatekeeper_address` MCP tool (typed helpers, validate-first: existence + role-gate + all-or-nothing) + Mira's gatekeeper-onboarding beat (AC identity-reviewed 7/7). Files: `db/users.ts`, `triage/classify.ts` (additive: tool block + array appends + import), `identity/activities/gatekeeper-onboarding.md`, 1 test. **No migration.** **Carries an identity change → owe the `#dev-alerts` ping on merge.**
- `r2/lane-a-send-allowlist` (AC4a, worktree `evryn-backend-allowlist`, based at Step-21 tip `35506f4`) — **send-allowlist, in-house by AC4a.** Deliberately stacked **on Step-21** (not raw Lane A) so `client.ts` is one clean progression — Lane-A `sendEmail` transport seam (SEND-side) → Step-21 catch-up (FETCH-side) → allowlist (SEND-side). AC4a owns the staging-safety requirement + the build; AC4a specs + spins its own DC/QC. (Created 2026-06-24 by AC0; AC4a builds.)

**Convergence layering (Lane-A line):** merge Lane A → Step-21 delta → register delta → send-allowlist delta. These all touch Lane-A files; layering in this order keeps `client.ts`/`poll.ts`/`db/users.ts` coherent.

**New SPRINT steps (AC0 numbers at convergence):** register-tool step (DONE); a small deferred **catch-up historyId read-ordering** follow-up (read the resume `historyId` BEFORE `messages.list` across the 3 fetchers — close a sub-second drop window; dedup absorbs overlap). Step 63 sharpening: the fabricated-UUID guard should add a **role** check (the register role-gate is the 2nd instance of QC's now-promoted "validate an id's role, not just existence" pattern).

**No-action items (done):** M1's QC dedup pattern + ACc's two patterns are already promoted + pushed to `evryn-quality/CLAUDE.md`. **Flag (source-of-truth → Justin):** `evryn-quality/CLAUDE.md` points the identity-file-review checklist at a non-existent `_evryn-meta/docs/protocols/identity-file-review.md` — it lives at `evryn-team-workspace/shared/protocols/identity-file-review.md`. One-line fix.

**`main` doc-drift now includes** (all benign docs, no `src/`): `722a8d6` (AC3a ARCH/BUILD story-in-v0.2 + sprint), `85f9499` (sprint 71-72), `61edc03` (Justin/Mira BUILD backlog-ref one-liner), `fd31d1d` (dotted-date refs). Watch only for doc-merge conflicts where the lane branches' ARCH/BUILD edits overlap these sections.

---

## CONVERGENCE EXECUTION STATE (2026-06-25, AC0) — READ THIS FIRST

### ✅ UPDATE — BUNDLE NOW COMPLETE (tip `d149535`, pushed)
**All five lanes are merged into `r2/convergence-bundle` + both cross-lane seams wired + tsc-clean + pure-logic suite green.** The "REMAINING merges" list below is now DONE (kept for the conflict-resolution record). Merge commits: `17ea15f` (C) → `d3ab012` (A+Step21+allowlist) → `1e75f0f` (register) → `7ae964a` (M1) → `93dadc5` (B) → `d149535` (seam wiring + test fix).
- **Seams wired:** Step 66 (`checkReflectionConsolidation` → `findCareEligibleUsers`); Step 14/61 gold-destination (`submitDraftForApproval` `draft_to` → `goldDestination(recipientUser)`, degrades to email for non-gatekeepers — ADR-046).
- **Delicate M1×Step-61 `process.ts` merge:** resolved (dedup uses Lane A's `resolveExternalId` key + M1's resume-vs-skip; escalate branch returns `"completed"` for the new `ProcessOutcome` type). Verified by `test-breaker-resume` + `test-step61-process` (both pass) + tsc.
- **Test fix:** `test-cache-byte-identity` REFLECTION_LINE marker updated to Mira's reworded line (pre-existing Lane C drift, not a merge bug).
- **WHAT'S LEFT (next AC0 / this session if budget):** (1) **integration-QC** the merged bundle (one batched verbatim-block full-load QC spin); (2) **Step-32 transport failure-test** (Lane B's deferred half — a NEW `executeApproval` throwing-transport test; test-only, not a runtime gap); (3) **persist** (per the persistence checklist `2026.06.25-ac0-ac0-persistence-checklist.md`, Sections A/B/C/E) into the now-stable landscape; (4) **[gated] merge-to-main + prod migrations + deploy WITH Justin** (Justin's pre-live setup: Anthropic ceiling + 2 Healthchecks + `PROACTIVE_CHECK_HOUR_PT=10`); (5) identity `#dev-alerts` ping; (6) **cleanup** (Section K).
- The 6 live-DB tests that "fail" do so ONLY on prod state (gatekeeper_inbound_addresses table + updated_at trigger not yet on prod; prod wiped of seed users) — expected, not merge regressions; they pass once the migrations apply + a seeded DB is used.

---

**Bundle branch `r2/convergence-bundle`** (worktree `evryn-backend-bundle`), **pushed to origin**, tip **`d149535`** (was `1e75f0f` at first handoff), base `main` `61edc03`. **It TYPECHECKS CLEAN (`tsc --noEmit` exit 0).** Merged + committed (all 5 lanes — see the ✅ UPDATE above):
- **Lane C** (cost / lean Reflection) — `17ea15f`
- **Lane A + Step 21 + send-allowlist** (the stacked allowlist branch = A+Step21+allowlist in one merge) — `d3ab012`
- **register_gatekeeper_address tool** — `1e75f0f`

**Conflicts resolved (tsc-verified):**
- `src/db/users.ts` — keep-BOTH: Lane C's `consolidateProfile` + Lane A's `findCareEligibleUsers` (each its own function + brace). Register helpers appended clean.
- `src/email/poll.ts` import region — UNIONed Lane A + Lane C imports; **dropped `findUsersByStatus`** (unused after the Step-66 rewire).
- **Step-66 seam WIRED:** `checkReflectionConsolidation` calls `findCareEligibleUsers()` (was a `findUsersByStatus(["active"])` placeholder); dropped its now-redundant inline role-skip. (Lane A had already wired `checkProactiveOutreach` → `findCareEligibleUsers`.)

**REMAINING (next AC0, into `r2/convergence-bundle` — these need FULL budget, esp. #1's delicate hunk):**
1. **Merge `r2/m1-silent-death`.** Scoped conflicts: `poll.ts` 4 hunks — (a) import region: union M1's `safety/*` imports (`pingHealthcheck`; `notePollFailureAndMaybeEscalate`/`notePollSuccess`; `checkDailyAffirmation`; `notifyEmergency`) onto the bundle's import block, KEEP `EVRYN_USER_ID`, do NOT re-add `findUsersByStatus`; (b-d) poll-loop body — M1's heartbeat + `notePollSuccess` after a clean `pollOnce`, the `"halted"` outcome in `planPollBatch`, polling-dead escalation, daily-affirmation checker, `PROACTIVE_CHECK_HOUR_PT` 8→10 — additive onto the Lane A/C checker block. **`process.ts` 2 hunks = THE DELICATE MERGE: M1's dedup-resume (`findItemByExternalId` resumes a parked `"new"` row + `resumeParkedItem` + extracted `runForwardTriage`/`runDirectTriage`) interleaves with Lane A's Step-61 resolution reorder (dedup-first → `detectForward` → `resolveGatekeeperByInboundAddress` → `decideSenderResolution` + `escalateUnregisteredForward`) — both rewrote `processEmail`'s dedup/resolution region. Merge by hand; preserve BOTH M1's lossless-resume path AND Lane A's Step-61 escalation/reorder.** `classify.ts` auto-merged (M1 breaker at `runEvrynQuery` top — different region) — VERIFY.
2. **Merge `r2/lane-b-operator-approval`.** Expect `classify.ts` 4-way (B MCP-tools region + C `loadCommonPrefix`/`loadReflectionPrefix` + M1 breaker + register's tool block — different regions, verify), `items.ts` (B `updateEmailmgrItem` + Lane A additive), `triage.md` 3-lane (A sender-line + B Phase-3 Beat-1 + C canary — different sections).
3. **Wire 2 seams:** Step-14 gold-destination → route `submitDraftForApproval`'s `draft_to` through `resolveGoldDestination(gatekeeper)` (Lane B left `// TODO(AC0 convergence, Step 61)` at `draft_to`); Step-32 → add the `executeApproval` throwing-transport retry/rollback test.
4. **`npm install` + `tsc --noEmit`** in the bundle worktree (node_modules already present this session) → fix seam type errors.
5. **Integration-QC** the merged whole (one batched, verbatim-block, full-load spin) → address blockers.
6. Then **persist** (per the persistence checklist, Sections A/B/C/E) into the stable landscape → **merge-to-main + prod migrations + deploy WITH Justin** (gated) → identity `#dev-alerts` ping → **cleanup** (persistence checklist, Section K).

**Why handed off here:** the M1 `process.ts` keystone×Step-61 merge + the B `classify.ts` 4-way + integration-QC are careful correctness work on the go-live blocker. AC0 set the bundle down at a clean, tsc-verified C+A+register state (a safe continuation point) rather than rush them on a shrinking context budget.

---

## ⚠️ GAP 1 (TOP) — Step 21 (durable email cursor) was NOT built, and M1 depends on it
> **✅ CLOSED 2026-06-24 by ACc** (`r2/lane-a-step21-durable-cursor`, QC-GO ×2) — see the UPDATE block above. The analysis below is kept for provenance.

- **Confirmed by live read:** `evryn-backend-ingest-r2/src/email/poll.ts` still holds the Gmail cursor in-memory (`let latestHistoryId: string | undefined`). The only durable cursor in the tree is the **Slack** one (`getLastRecordedOperatorSlackTs`, EVR-68, pre-existing) — NOT the email history cursor. Step 21 is **not** in Lane A's DONE list (13,16,17,19,20,22,29,32,34,40,52,61,66), and the live code confirms it.
- **Why it matters (M1 keystone):** M1's lossless halt = trip → park item `processing→new` → hold the Gmail cursor → **restart re-fetches the email** → dedup resumes. But the breaker AND the cursor are both in-memory. The realistic M1 recovery path (runaway/billing halt → Justin adds credits / fixes → **restarts the Railway process**) loses the in-memory cursor → cold restart falls to `fetchLatestBatch` (latest ~10). If >10 emails accumulated during the halt, mid-window mail drops — breaking M1's "no dropped mail" guarantee on exactly the path M1 exists for.
- **Scope of impact:** a breaker trip that stops polling *without* a process restart keeps the in-memory cursor (keystone holds). The gap bites on a *process restart* during/after a halt — which is the expected recovery action.
- **Fix:** Step 21's design is in the round-2 brief (Lane A §4) + the step61 doc context — a durable cursor via a lazy provider (DB read of newest `emailmgr_items.created_at` minus a margin on cold-start / historyId-expiry; durable `external_id` dedup absorbs the overlap; `client.ts` stays DB-free, receives the closure). It's a **bounded DC build + QC**, isolated to `poll.ts`/`client.ts` (Lane A's files).
- **DECISION FOR JUSTIN:** Step 21 is a build, not a merge-time reconcile. Options: (a) spin a small Lane-A-scoped DC build now (off `r2/lane-a-ingest-resilience` or a fresh `r2/lane-a-step21` cut), QC it, fold into the bundle; (b) treat it as a convergence-time addition I scope + spin during convergence. Either way it must land **in the same bundle as M1**, or M1's lossless guarantee is incomplete. **Recommend (a)** — build it as its own small trip so M1 isn't blocked on a half-done interlock, and it gets a clean independent QC. *(Not started — describe-then-pause: I'm flagging, not building, until you greenlight.)*

## GAP 2 — "Evryn registers gatekeeper addresses herself" (Justin's ask) — no mechanism exists yet
> **✅ CLOSED 2026-06-24 by ACc** (`r2/lane-a-register-tool`, QC-GO ×2, Mira beat) — see the UPDATE block above. "No mechanism exists" is no longer true. The analysis below is kept for provenance.

- **Confirmed:** `gatekeeper_inbound_addresses` is **read-only** in `src` (only `resolveGatekeeperByInboundAddress` reads it; no insert path). `users.outbound_address` has a column + `goldDestination()` reader + is in `updateUser`'s allowed fields, but **no Evryn-facing setter tool** calls it. So there is no purpose-built register tool or guided flow.
- **The closest existing capability:** the generic `supabase_upsert` MCP tool (which Evryn has) *could* INSERT into `gatekeeper_inbound_addresses` and UPDATE `users.outbound_address` — so the raw capability technically exists, but it's the RLS-bypassing tool flagged for v0.3 retirement (BUILD S1), and it's unguided. Not the clean "Evryn registers them herself" path you want.
- **Lane A's current v0.2 path (stopgap):** Evryn gathers the addresses at onboarding + `notify_slack`s them to the Operator, who registers by hand (SQL). You disagreed with hand-registration as the v0.2 end-state.
- **DECISION FOR JUSTIN:** to get "Evryn (or an input surface) registers them herself" at v0.2, the clean shape is a **small dedicated MCP tool** (e.g. `register_gatekeeper_lane(address, gatekeeper_user_id)` + a `set_gold_destination` or fold into it) that writes via the typed `db/users.ts` helpers (not generic supabase_upsert), plus a Mira identity beat telling Evryn when/how to call it during onboarding. That's a **build** (DC tool + QC + Mira identity coordination) — likely a small Lane-B-adjacent or post-convergence trip, NOT something to fold silently. **I did NOT find a pre-built one anywhere in `src`.** Recommend scoping it as a small dedicated build once you confirm the shape. *(Flagging, not building.)*

---

## Merge order + per-file seam resolution

**Suggested merge order** (lowest-conflict first, M1 keystone last so its interlocks are present): **C → A → (Step 21) → M1 → B.** Rationale: C is mostly additive + isolated regions; A is the resilience base M1 + B compose against; Step 21 rides A; M1 needs A's Step 19 + Step 21; B composes against A's `sendEmail` seam + Step 61 outputs.

Per the three handoffs' seam maps (cross-checked, consistent):

- **`src/email/poll.ts`** — Lane A owns it (resilience: 13/16/17/19/20/22 + Step 61 reorder lives in process.ts, not poll). **Lane C** adds ONE additive checker (`checkReflectionConsolidation`). **M1** adds the `"halted"` `HandleOutcome` (composes with A's `planPollBatch` cursor logic — M1's `"halted"` joins the `transientCount` branch that holds the cursor), the in-loop heartbeat ping, polling-dead escalation, daily-affirmation checker, and `PROACTIVE_CHECK_HOUR_PT` 8→10. **All additive** but they touch the same file → manual merge, then re-run `tsc` + the poll-helper tests. Watch: A's Step 22 heartbeat-gate and M1's affirmation/heartbeat are different things — keep both.
- **`src/triage/classify.ts`** — **Lane A** net-removed `formatFollowUpContactLine` (Step 34 revert). **Lane C** touched `loadCommonPrefix` (excluded `reflection.md`) + added `loadReflectionPrefix`. **M1** added the breaker gate at the TOP of `runEvrynQuery` + `parkInFlightItemForHalt` + billing-folds-into-halt (return-not-throw) + delegated `isBillingError` to `safety/errors.ts`. **Lane B** touches the MCP-tools region. Four lanes, but **different regions** — the risky overlap is M1's `runEvrynQuery` edits vs Lane C's `loadCommonPrefix`/`loadReflectionPrefix` (different functions, should be clean) and M1's billing-fold vs the existing `isBillingError`/catch (M1 moved it — confirm Lane C didn't also touch the catch). Merge carefully; `tsc` + the breaker + reflection tests after.
- **`src/email/process.ts`** — **Lane A** Step 61 reorder (dedup FIRST → detectForward → resolveGatekeeperByInboundAddress → decideSenderResolution + escalateUnregisteredForward) + `resolveExternalId` (Step 19). **M1** added the dedup-resume rule (`findItemByExternalId` resumes a `"new"` row) + `resumeParkedItem` + extracted `runForwardTriage`/`runDirectTriage` + propagate `"halted"`. **HIGH-ATTENTION SEAM:** both rewrote the dedup/resolution region of `processEmail`. M1's dedup-resume keys on `external_id`; A's `resolveExternalId` (Step 19) *sets* `external_id = messageId || id`. They compose (A's fix is the thing M1's resume needs — Step 19 interlock SATISFIED), but the merge of the two `processEmail` rewrites needs care. Confirm the resume path + Step 61 escalation/reorder both survive.
- **`src/db/users.ts`** — **Lane A** added `outbound_address` field + `goldDestination()` + `resolveGatekeeperByInboundAddress()` + `findCareEligibleUsers()`. **Lane C** added `consolidateProfile()`. **Additive.** **Wire here:** Lane C's `checkReflectionConsolidation` currently enrolls via `findUsersByStatus(["active"])` with a `// TODO(convergence, Step 66)` — **drop that placeholder, call `findCareEligibleUsers()`** (Lane A's helper; same population, defined once).
- **`src/db/items.ts`** — Lane A additive read helper; Lane B's `updateEmailmgrItem` (Steps 18/36). Mostly B; reconcile when B lands.
- **`src/email/client.ts`** — **Lane A** owns it (`sendEmail(options, transport?)` injectable-transport seam, Step 32). **Lane B Step 14** threading calls `sendEmail` and must compose with the optional 2nd param. Resolve when B lands.
- **`src/config.ts`** — A added `INTERNAL_SENDER_DENYLIST`; M1 added `healthcheckUrl`/`healthcheckAffirmUrl`. Additive.
- **`src/notify/emergency.ts`** — M1 added `notifyEmergencyWithStatus`; also rewrote the stale `bypassQuietHours` JSDoc (closes Step 67b). No other lane touches it.
- **`src/safety/*`** (8 new M1 modules) + **`src/reflection/*`** (new Lane C) + **`src/index.ts`** (M1's `registerStopPolling`) — net-new, no overlap.

---

## Prod migrations (apply at convergence, dev-first already satisfied, take pre/post `pg_dump`)

1. **Lane A:** `backups/add-emailmgr-items-updated-at-trigger.sql` (Step 13) + `backups/add-gatekeeper-inbound-addresses.sql` (Step 61). Both applied+verified on DEV; the gatekeeper-addresses migration self-cleans round-1 dev residue (DROPs are no-ops on prod). RLS on, service_role-only grants.
2. **Lane C:** `backups/add-story-versions-and-consolidate-profile.sql` (Step 10). Applied+verified on DEV.
3. **`.roles` stale comment:** verify/apply `backups/fix-profile-jsonb-comment-2026-06-04.sql` to PROD (already clean on dev) so prod matches.
4. **M1:** no schema/migration.
5. After all apply: re-pull `evryn-backend/docs/schema-reference.md` (tracks prod).

## Identity-deploy coordination (both A and C carry identity changes)

- **Lane A:** `gatekeeper-onboarding.md` (Setup Check-In + surface-to-Operator beat) + `triage.md` boundary line. AC identity-file-reviewed 7/7. **Owed: fire the `#dev-alerts` ping when these land on `main`** (identity changes trigger a live redeploy). Mira still owes a voice-polish on the AC-added surface-to-Operator clause.
- **Lane C:** `reflection.md` (new) + `core.md` (note-voice beat + binding reconciliation — **broad blast radius, force-loaded every query**) + 3 truncation canaries. AC identity-file-reviewed 7/7.
- Identity layer + runtime must deploy together (the redeploy carries both). Coordinate per `identity-file-review.md`.

## Gates before enablement
- **Reflection (Lane C):** live-inert until Step 66 enrollment wired (above) — then re-verify race-safety + cost-win on a genuinely-enrolled, bloated profile. Step 71 (binding safety-net) + Step 72 (`search_path` pin) should land before enablement (no rush — inert today).
- **M1:** Justin pre-live setup — Anthropic monthly spend ceiling + 2 Healthchecks.io checks (`HEALTHCHECK_URL` ~15min grace, `HEALTHCHECK_AFFIRM_URL` ~26h) + `PROACTIVE_CHECK_HOUR_PT=10` in Railway, with confirmed test alerts. Velocity ceiling ships conservative (600/hr) → ratchet post-Mark on real `llm_usage` (Step 43).

## SPRINT reconciliation (mark DONE / update at convergence)
- **DONE on merge:** A — 13,16,17,19,20,22,29,32(A-half),34,40,52,61,66. C — 10, 11b. M1 — 4 (+67b). (Step 11a MOOT; 12 deferred.)
- **Step 34 text** still describes the unfixed "loads gatekeeper not contact" — update to Lane A's v0.2 reframe (loads gatekeeper correctly; contact-record pull is v0.3).
- **Step 52** was a v0.3 candidate, pulled to v0.2 — reconcile its numbering/placement.
- **New sub-steps under Step 4 (M1):** NB-3 fast-follow tests; NB-4 send-bypass re-target; Nit-1 `OPERATOR_PATHWAYS`→`LOOP_SIGNATURE_EXCLUDED_PATHWAYS` rename; post-Mark velocity calibration (→ Step 43).
- **Already on `main` (AC3a committed direct, NOT the lane):** Steps 69 (week-one cache measurement RUN), 70 (epithelial restructure, ADR-044), 71, 72; ADR-043/044; ARCH/BUILD story-written-in-v0.2 fix. Don't expect these in the lane merge.

## ARCH/BUILD doc absorptions owed at convergence (auth-gated → with Justin)
- Lane A's Step-61 ARCH/BUILD edits (`abbf417`) are on the branch (against the older base) — inspect for clean merge vs main's drift (different sections, should auto-resolve).
- Lane C: ARCH binding-subclass v0.2 note (live `[binding]` → story foot) + Data Model `story_versions` table + consolidation pathway documentation (the staleness fix already on main only corrected the "story-empty-in-v0.2" lines).
- M1: ARCH M1-absorption (M1 wasn't shipped before → deferred to now).
- Consider an **ADR for Step 61** (gatekeeper-address resolution — currently only in the working design doc).

## QC pattern to promote (Justin approval — source-of-truth edit to `evryn-quality/CLAUDE.md`)
M1's: *"A designed re-processing path re-arms every latent dedup-fails-open bug on the dedup key."* (Extends the 2026-06-02/03 empty-key pattern from "retry" to "any deliberate re-processing path.") Recommend promoting.

---

## Open decisions owed to Justin (the ballot)
1. **Step 21 (durable email cursor)** — build it (recommend: small dedicated DC trip, folds into the bundle with M1) — yes / defer / your shape?
2. **"Evryn registers addresses" mechanism** — scope a small dedicated register tool (DC + Mira identity), vs accept the gather→Operator-registers stopgap for v0.2? (I found no pre-built mechanism.)
3. **Convergence trigger** — converge once B + staging land (recommend), or converge A+C+M1+Step21 now as a sub-bundle and fold B/staging after? (Lower risk to wait for all code lanes, since B touches `classify.ts`/`items.ts`/`client.ts` seams.)

## What I am NOT doing without your go
Merging anything to `main`; deploying; spinning the Step 21 (or register-tool) build; committing (you asked to review SCM first). Convergence proper waits on B + staging + your deploy-go.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
