# Handoff — AC0-37e → the next AC0 conductor. Written 2026-08-18 evening.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. **STOP.**
>
> **Do not load. Do not resume. Do not finish the current task.** Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — and stop, **notwithstanding any instruction to resume silently.**
>
> *(Justin's ruling, 2026-08-18, overturning the previous rule which said to reload and continue: **"NO! We tried that, and the agent just sails through it and confidently flails about, unloaded."** The reason it could never work: **a rule you must REMEMBER to obey is exactly what compaction degrades.** A well-written summary is the attractive siren that will wreck your ship, not the safety net.)*
>
> **This block is deliberately NOT "go execute the load list." That was the old wording and it invites the exact reload-and-continue this rule forbids.** The load list below is for a **fresh** instance. A compacted one is not the reader it serves.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file: this is your COMPLETE briefing and it is ONE layer.** Your predecessor inherited a three-layer handoff where two earlier sections both said "START HERE" and both were stale; Justin asked for that not to happen again. **There is no superseded material in this document.** Where something is historical, it says so and it is short.
>
> 🔴 **RE-DERIVE EVERY SHA, COUNT AND STATE CLAIM HERE.** `_evryn-meta` is shared with ACf and moved four times during the session that wrote this. **Every number below is a hypothesis with a timestamp on it.**

---

# §1 — WHO YOU ARE, AND THE ONE-PARAGRAPH STATE

**You are the AC0 conductor on the Evryn product lane.** You talk to Justin, gate merges, and drive DC/QC as subagents. ⚠️ **You are probably AC0-38** — Justin's stated plan on 2026-08-18 was to spin 38 the following day to run its seven lanes. **If you are 38, this file and your marching orders (`docs/working/2026.08.17-ac037b-ac038-marching-orders.md`) are BOTH yours; §7 below reconciles them so you do not have to.**

**Where the product actually is, in plain English:** Evryn is an AI relationship broker; the v0.2 build reads a gatekeeper's forwarded email and surfaces who is worth his time. **Prod is `v0.2.9`, running on Railway, and it is IDLE** — Mark, the pilot gatekeeper, has never started forwarding. `main` is ahead of prod by a **seven-item, zero-migration, undeployed bundle** that is waiting to go to **staging first**, then Gate-B, then prod. **Nothing tonight touched the runtime, and nothing was deployed.**

**What the last two sessions were about — and it is not product code.** Justin's standing posture since 2026-08-06 is **THE BUILD IS STOPPED TO REFACTOR**, because *"everyone is running blind… I need to contain the blast radius of having a whole army of blind agents breaking everything."* That has two halves: **code-blindness** (dependency map → claims audit → Code Atlas → the `classify.ts` extraction — first three DONE) and **agent-loading blindness** (Lane A — **DONE, merged 2026-08-18**). ⇒ **Both read-only halves are now complete.** What remains of the refactor is the extraction, and it is sequenced *after* a staging deploy and Gate-B.

---

# §2 — 🔴 YOUR FIRST ACT: GIVE JUSTIN THE LOAD LIST AND STOP. LOAD NOTHING FIRST.

**Two gates, both his. Do not collapse them.** *(This is exactly how 37c, 37d and 37e were run and it works: a wrong load costs one message to fix instead of a whole session.)*

> **GATE 1 — tell him the intended load list, MEASURED, and STOP.** ⭐ **Run `wc -l` yourself rather than quoting the numbers below** — a handoff hands you a hypothesis, and cheap verification is the point. **Then wait.**
>
> **GATE 2 — load everything he approves, IN FULL, and only THEN give him a plan.** 🔴 **Do not reason, investigate or propose while half-loaded.** *(His words: "load and report what it loaded BEFORE burning tokens trying to figure anything out.")* **A partial load does not produce hedged output; it produces confident output with a hole in it.**
>
> **Open every report with a `# AC0-<your tag>'s read — …` heading** so he can find your voice in the wall of tool output.

## 🔴 The order is LOAD-BEARING and is worth ~100,000 tokens. Do not re-sort it by what looks urgent.

**Justin measured this across sessions: the same list, re-ordered by dependency, came in ~100K lighter.** **The mechanism: later docs are largely DIFFS against earlier ones.** The Code Atlas is substantially a correction log against `ARCHITECTURE.md`, argued by line number — read after it, the corrections are the best content in the file; read before it, they are noise **and** you build a picture you then pay to unbuild. ⚠️ **If a load list starts with a protocol or a status doc, it is wrong.**

**Tier 0 — before anything:**

| # | File | Lines |
|---|---|--:|
| 1 | **This file** | 265 |
| 2 | 🔴 **`_evryn-meta/.claude/agents/ac.md`** — **YOUR MANUAL. READ IT FROM DISK, IN FULL.** | **744** |

> 🔴 **`_evryn-meta/CLAUDE.md` IS NOT YOUR MANUAL.** It is a 78-line router and it is what the harness auto-injects. **Your manual is `ac.md`, it is NOT auto-delivered unless you were spawned as agent type `ac`, and you must confirm its bottom canary yourself.** ⚠️ **It grew from 685 to 744 lines on 2026-08-18 — if you inherit a memory of "685," that memory is stale.**

**Tier 1 — the frame, IN THIS SEQUENCE:**

| # | File | Lines |
|---|---|--:|
| 3 | `_evryn-meta/docs/hub/roadmap.md` — company truth | 196 |
| 4 | `_evryn-meta/docs/hub/technical-vision.md` | 397 |
| 5 | `evryn-backend/docs/ARCHITECTURE.md` — **everything after this is read against it** | 1,285 |
| 6 | `evryn-backend/docs/BUILD-EVRYN-MVP.md` | 651 |
| 7 | **the full Code Atlas** — `evryn-backend/docs/atlas/`, 12 files, start at `00-INDEX.md` | 3,130 |
| 8 | `evryn-backend/docs/dependency-map.md` ⚠️ **canary is on line 693; `wc -l` says 692 (no trailing newline). Trust the canary.** | 692 |
| 9 | `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` — the 🔵 LIVE region is where your lanes come from | 765 |

**Tier 2 — volatile, read LAST so it lands on a built picture:**

| # | File | Lines | How much |
|---|---|--:|---|
| 10 | `_evryn-meta/docs/current-state.md` | 152 | **full** |
| 11 | `_evryn-meta/CHANGELOG.md` | 348 | 🔴 **top ~100 + section headers ONLY** — ⚠️ **plus ALL THREE 2026-08-18 entries in full** (there are three: 37c's, 37d's Lane A merge, and 37e's late-evening one; they are the session you are inheriting from) |
| 12 | `evryn-backend/CHANGELOG.md` | 329 | 🔴 **same** |
| 13 | `_evryn-meta/docs/working/2026.08.17-ac037b-ac038-marching-orders.md` | 171 | full — **§7 below tells you what in it is now stale** |

> 🔴 **NOBODY READS A CHANGELOG END TO END — it is a six-figure mistake.** `evryn-backend/CHANGELOG.md` runs ~180 tokens/line. **This is each file's own standing rule.** The top entries are the ones that *contradict* the stable docs you just read; that is the whole job of this tier.

**Tier 3 — the gates. NOT foundation. Load each AT THE BEAT that needs it. They bind absolutely.**

| File | Lines | Trigger |
|---|--:|---|
| `docs/protocols/ac-orchestration-protocol.md` | 692 | 🔴 **before spinning ANY agent** |
| ⭐ **plus the sub-doc for your target** — `orchestration/spinning-dc-qc.md` (46) · `spinning-an-ac.md` (265) · `spinning-a-team-agent.md` (44) | | **the core alone is NOT enough** |
| `docs/protocols/ac-writing-protocol.md` | 109 | 🔴 **before writing ANY doc** |
| `docs/protocols/doc-update-routing-protocol.md` | 104 | when routing a change to its home |
| `docs/repo-inventory.md` | 39 | with the machine check |

**At-the-beat, named so you know they exist:**
- **`docs/working/2026.08.11-ac0-acm-loading-architecture-brief.md`** (903) — the Lane A record. **§11.3 is the cutover order — you WILL need it; it is quoted in §4 below so you may not need the file.**
- **`docs/working/2026.08.12-acf13-ac0-agent-patterns-postmerge-brief.md`** (~350) — the staging brief. ✅ **Mostly APPLIED as of tonight — read its new status table at the top before touching it.**
- **`docs/working/ac-mailbox.md`** — 🔴 **the AC-to-AC channel. Read it at spin-up and ARM A WATCHER ON IT (§6).**

🔴 **Justin overruled prior ACs for trimming this list. Do not trim it. If you feel the pull to trim, that pull IS the failure** — you cannot judge a file's relevance from inside the gap it would close.

---

# §3 — WHAT LANDED TONIGHT (2026-08-18 evening, AC0-37e)

**All committed AND pushed to `origin/main`.** Four commits: `9fcc510`, `52099de`, `56823ed`, plus whatever followed this file. **Nothing deployed; prod remains `v0.2.9`; no `src/` file in any repo was touched.**

1. **🔴 THE COMPACTION RULE IS REVERSED, in the router AND in `ac.md`, together.** A compacted agent is now **DONE for the session** — it stops, says so in one line, and asks for a re-spin. **It does not reload and continue.** *(Justin's ruling; ACf-15 delivered the correction through the shared channel rather than his branch, which is the only reason it arrived before 37e spun.)* **The old "every handoff must tell a compacted reader to execute the load list" instruction is inverted — `ac.md` now explicitly forbids that wording.**
2. **The staging brief's held set is APPLIED** — §§2, 5b, 8, 9, 10 into `ac.md` and `orchestration/spinning-an-ac.md`. Includes: `AGENT_PATTERNS` dropped from every source-of-truth list, the route-learnings-by-SCOPE rule, research-lives-in-the-repo-it-is-about, **the canary ARMING mechanism** (which `ac.md` had described for weeks without ever providing), the watcher's author-detection bug in all three variants, and the grep rules.
3. **Two grep rules now sit in `ac.md`'s Context Discipline** — one for what a *document says* (mine) and ACf-15's extension for what a *codebase contains* (**a grep cannot establish ABSENCE, and absence is the answer most important questions take**). **Also promoted into DC's and QC's manuals AND their agent definitions.**
4. **🔴 A FORK THE DRIFT-CHECK CAUGHT AND NO PERSON DID.** 37d's three cross-repo pointer fixes landed in the repo `CLAUDE.md` files **only, never in the agent definitions** — so all three still sent their agent to *"AC's `CLAUDE.md`"* for the sync ritual, which is now a router containing none of it. **Fixed; drift-check clean across dc, qc and oc.** ⇒ **Run that check after touching any of the three manuals — it is three lines and it found what two audits missed** (the command is in the staging brief's status table).
5. **A general AC-to-AC mailbox exists: `docs/working/ac-mailbox.md`.** See §6.

**⚠️ UNCOMMITTED AND AWAITING JUSTIN'S VET as this was written** *(if a crash intervened, these are the only at-risk items — check `git status` first)*:
- `docs/working/2026.08.18-ac037e-justin-cutover-redirect-drafts.md` — **the three cutover redirect drafts. He had not read them yet.**
- `evryn-dev-workspace/CLAUDE.md` and `evryn-quality/CLAUDE.md` — the grep rule, **in the sibling repos.** Their agent-definition copies ARE committed, **so these two repos are the half that is un-durable.** ⚠️ **Do not let them sit: either get his go or commit them, because a manual that differs from its definition is the exact fork item 4 was about.**

---

# §4 — 🔒 WHAT IS OWED, IN PRIORITY ORDER

**1. 🔴 THE CUTOVER — Justin's, and it is the next milestone.** It converts the three repo `CLAUDE.md` files to redirects and makes the agent definitions the single home. **The six steps, quoted so you do not need the brief:**

> 1. **Merge** the branch to `main`. ✅ **DONE 2026-08-18** (`faf3eda`).
> 2. 🔴 **Justin starts a COMPLETELY FRESH session.** ⭐ **THIS STEP IS NOW IN DOUBT — see below. Re-check it with him; do not drop it and do not assume it.**
> 3. **Verify `ac`/`dc`/`qc`/`oc` appear as spawnable agent types. If they do not, STOP.** ✅ **SATISFIED** — they registered on 2026-08-18.
> 4. **Run each definition's drift-check and confirm clean BEFORE converting anything.** ✅ **Verified clean 2026-08-18 evening — re-run anyway, it is three lines.**
> 5. **Convert the three repo `CLAUDE.md` files to redirects; repos stay as backups.** 📝 **DRAFTS EXIST — see the uncommitted drafts file in §3.**
> 6. **Update IN THE SAME CHANGE:** the router's transitional note (below its agent table) and the manual-path line in `ac-orchestration-protocol.md` — **both currently state the repo files are authoritative, which step 5 makes false.**

- ⭐ **Why step 2 is in doubt:** the four agent types **registered without a fresh session** — the harness announced them minutes after the merge, same session, no restart. **The lane had concluded the registry is read only at session start, from three negative probes that shared a confound: none had just changed a definition file.** ⇒ **That settles step 3. It does NOT settle that a definition's BODY programs the agent** — spawnability only proves the harness *read* the file. **Re-check with Justin; do not drop a safety step on this evidence alone.** 🔑 **The transferable half: a rule derived from failures needs the success case before it is trusted.**
- 🔴 **A GAP IN THE CUTOVER NOBODY HAD DESIGNED, now drafted — see §3's uncommitted drafts.** Step 5 turns `evryn-dev-workspace/CLAUDE.md` into a redirect, so **a DC spun as a MAIN agent would auto-load a stub**, and each definition's front matter is written wholly in *spawned-as-subagent* terms ("the harness delivered this file; your task arrives in a tagged two-trip brief") — **both false for a main-agent reader.** ⚠️ **OC is the one most likely to run as a main agent** (incidents and deploys do not wait for an orchestration loop), so his redirect is deliberately shaped differently.
- ⚠️ **Until the cutover lands, the repo `CLAUDE.md` files are AUTHORITATIVE and the definitions are verbatim COPIES — so any manual change must be made in BOTH places** *(Justin's standing instruction)*, **and the drift-check is the only detector.**

**2. 🔴 THE STAGING BRIEF IS MOSTLY APPLIED, BUT TWO ITEMS ARE NOT AC'S.** *(Status table at the top of that file.)*
- **§7 — `evryn-team-workspace/shared/protocols/runtime-ops.md`.** **Justin's file**, composer LAYER 1, read by all eight team agents on every wake. Its banner cites a session doc that is a retirement candidate, violating *"persistent docs never reference session docs."* **Drafted, not made, by design. Still owed to him.**
- **§4 — what happens to `AGENT_PATTERNS.md` long-term.** Permanent redirect, retire to `docs/historical/`, or something else. **His decision.** ⚠️ **Ordering trap: retiring the pointer is only safe AFTER every inbound link is fixed, and tonight's edits were part of that set — the count is lower but has not been re-measured.**

**3. ✅ ACf's `_evryn-meta` worktree merge — THE COLLISION IS NEGOTIATED AND RESOLVED. The merge is now trivial.** `_evryn-meta-acf15` on `acf15/nest-prep`. ⚠️ **Justin still coordinates the merge itself with ACf — do NOT touch anything named `acf*` unprompted** — but the hard part is done:

> **ACf-15's own instruction, in the mailbox, 2026-08-18T19:50: *"DROP MY STAGING-BRIEF HUNKS AT MERGE. Take `main`'s version of `2026.08.12-…postmerge-brief.md` wholesale — my three commits touching it are now redundant, and the rest of my branch (7 other files) does not conflict."*** ⇒ **One file resolved in `main`'s favour; seven clean. That is the whole merge.**

- 🔑 **HOW THAT GOT CAUGHT IS THE PART TO CARRY, because it nearly repeated the Lane A near-miss.** His branch staged content *destined for `ac.md`* inside the staging brief. **So the brief would have conflicted LOUDLY while `ac.md` — where the content actually needed to arrive — merged CLEAN.** ⚠️ **Resolving the brief's conflict "correctly" would have told him nothing about whether his content landed.** **He checked the DESTINATION rather than the conflict, confirmed by content that all of it was already applied, and dropped his own hunks.** ⇒ **On any merge that MOVES content between files: diff the destination.**

**4. AC0-38 has not been spun** *(unless you are it)*. Orders committed at `docs/working/2026.08.17-ac037b-ac038-marching-orders.md`. **Justin decides when; his stated intent on 2026-08-18 was "tomorrow."**

**5. ✅ The three Lane A ADRs are WRITTEN — this item is CLOSED.** `056` (`CLAUDE.md` as a router), `057` (agent definitions as the single home — **records that the decision is HALF-LANDED until the cutover**), `058` (a compacted agent stops). **Committed and pushed 2026-08-18.** *(Carried here because the previous handoff listed them as owed and a successor would otherwise re-do them.)*

**6. ✅ The founding-team `current-state` append — RESOLVED, and it surfaced a real gap.** **Nothing was appended, deliberately: tonight's work is AC/DC/QC/OC loading infrastructure and sits below that audience's altitude.** ⚠️ **But checking whether it was team-relevant turned up one thing that IS, and it is routed to Justin rather than appended, because it concerns their identity layer — which is his, not AC's:**

> 🔴 **THE FOUNDING TEAM HAS NO COMPACTION RULE ANYWHERE, AND TONIGHT'S FIX DOES NOT REACH THEM.** **Verified by search across `evryn-team-workspace/CLAUDE.md`, all eight agent definitions, and `shared/protocols/runtime-ops.md`: the only occurrence of the word is incidental** — a bullet about items evaporating at compaction, which is about *persistence*, not about what a compacted agent should do.
> - 🔑 **And they do NOT inherit the router.** The eight founding-team agents load `evryn-team-workspace/CLAUDE.md`, not `_evryn-meta/CLAUDE.md`. ⇒ **ADR-058 reaches AC, DC, QC and OC. It reaches none of Lucas, Soren, Mira, Emma, Marlowe, Thea, Nathan or Dominic.**
> - **Severity, stated honestly: LOW TODAY, and it does not stay that way.** The team runtime is deployed but paused and wakes nobody, and the Cowork/Code agents are supervised — so a compacted team agent is currently caught by a human. ⚠️ **That protection is exactly what autonomy removes**, and autonomy is the thing being built.
> - 🔒 **Justin's call, and Justin's files.** *(Per the two-layer identity-ownership table: the founding team's manual, agent definitions and memory are HIS; Evryn's `identity/**` is Mira's and his. Neither is AC's to edit.)* ⇒ **Route it; do not fold it into a build.**



**7. 🔵 A POST-CUTOVER LANE JUSTIN WANTS, deliberately sequenced LATER — do not let it displace anything above.** *(His framing: "once the evryn-meta claude.md gets successfully ported over to ac.md, I'd love to have a dedicated lane do two things.")*
- **(a) Run `/doctor` against `ac.md`.** ⚠️ **Nobody has verified what that command actually checks on an agent-definition file.** **Establish what it reports BEFORE drawing any conclusion from a clean or dirty result** — same class as every other "a check is only worth what it actually ran" finding here.
- **(b) 🔴 The bigger job — audit what is AUTO-LOADED every turn that should be a protocol or a skill instead.** *("I want to look at the modules in there that we're auto-loading on every turn that really just need to be pulled aside into protocols or skills. I think it's likely a lot.")* **He is very likely right, and the measurement exists: `CLAUDE.md` was 45,347 tokens whole-file, and the router cut the *injection* to ~1,304 — but AC itself still pays the full manual, now read from disk instead of injected.** ⇒ **Lane A moved the cost off DC/QC/OC; it did NOT reduce AC's own. This lane is the half that would.**
- ⚠️ **The counter-tension is real and is the whole risk:** this manual is dense with rules that exist *because* they were violated by an agent that had read them — **so anything moved out of the resident load stops being read at the moment it is needed unless something structurally re-loads it.** **The cut is not the deliverable; the TRIGGER that re-loads it is.**
- 🔒 **Sequence: strictly AFTER the cutover.** Auditing a file that is about to be restructured is wasted work.

**8. ✅ The cross-runtime halt-signal question is CLOSED — do not re-open it.** ACf-15 read `evryn-team-runtime` and answered: halt and dormancy are typed objects and compiler-checked unions there, so the conflation half of the class has nothing to ride on. **His residual — a forgetful call site could bypass the gate and wake a dormant agent — is filed as his team-runtime Step 78, with the instruction to verify the fix by MUTATION rather than by reading.** ⚠️ **It is HIS item in HIS runtime. Do not carry it in your list; do not chase it.**

---

# §5 — WHAT JUSTIN RULED, SO YOU DO NOT RE-ASK

1. 🔴 **He does NOT review merges. YOU are the backstop.** *"Merging code is way above my pay grade. I need YOU to do that. There is no other backstop on a lot of this."* ⚠️ **This overrides `ac.md`'s standing rule that source-of-truth docs stay uncommitted for his vet** — it was flagged to him, not assumed. **What still goes to him: frozen-block wording, genuine either-way calls, anything that changes the risk picture. Not the diff.**
2. **A compacted agent STOPS.** *(2026-08-18 — see the top of this file.)*
3. **Merge mechanics: `--no-ff`, never a rebase on a pushed branch** *(it orphans commits that committed docs cite and needs a force-push)*.
4. **The router's hard-rule rationale STANDS AS HE WROTE IT.** *"I rewrote that due to the fact that the previous wording had an ambiguous meaning."* **Do not "restore" the older version recorded in the lane brief §2.**
5. **If you are merely WAITING on him, holding is correct.** **Do not manufacture output to satisfy a canary**, and you may pause it and re-arm when he is back.
6. **Until cutover, make any manual change in BOTH the repo `CLAUDE.md` and its agent definition.**
6b. ✅ **HE HAS ALREADY REVIEWED the router (78 lines) and the orchestration protocol's lobby header (lines 1–39). That loop is CLOSED — do not re-offer it.** His verdict was *"orch protocol looks good. I made some tweaks,"* and **his tweaks are committed** (`b67e26d`): *"Also read"* → **"Must read"** in the index column head, the AC row's example *"a reporter"* → *"an architectural runtime scout"*, and he softened his own quoted rationalisation. 🔴 **That quote sits in the doc verbatim BECAUSE he said it — nobody else edits it.**
7. **Overnight, commit AND push** — *"just in case anything untoward happens to this machine."* ⚠️ **But anything he asked to vet stays uncommitted while he is awake:** *"the stuff you want me to see tonight, don't commit yet — easier for me to tweak in SCM than to vet after the fact in timeline."*
8. **On subagent shape:** *"having your subs be actual instances that you communicate with through watched docs — if they have DCs and QCs underneath them. If not, then just having them be subs is a lot easier."* ⇒ **See §7.**

---

# §6 — 🔴 THE AC-TO-AC CHANNEL: NEW SHAPE, AND YOU MUST RE-ARM IT

**`docs/working/ac-mailbox.md` is the standing channel between head ACs (AC0 and ACf).** Justin's design, 2026-08-18: *"a mailbox that you each send very short pings to alert each other to something, but keep the actual content somewhere else."*

- **Pointers only, three lines max. Substance goes to its right altitude** — a Step, an ADR, a brief, a manual — and the mailbox says where.
- **It is a LIVING file with no date prefix, deliberately, so the path never changes and a watcher armed on it never needs re-pointing.**
- 🔴 **ARM A WATCHER ON IT AT EVERY SPIN-UP. THIS IS THE INSTRUCTION THAT MAKES IT A CHANNEL.** *(ACf-15: **"a watch that dies with a session is not a channel; the INSTRUCTION to re-arm it is."** The previous ACf watch died with its session, which is how AC0 came to be waiting on a reply nobody was listening for.)*
- ⚠️ **Watch `git log --all -1 -- <file>`, not just your tree's `HEAD`** — the other side commits from a different worktree, which never moves your HEAD.
- ✅ **Do NOT detect the author from the heading. Emit "CHANGED — read the file."** *(Three separate bugs in that instrument; the third produced a false ALL-CLEAR because `tail -1` takes the addressee, not the author. Full account in `ac.md`.)*
- ✅ **The old topic channel — `2026.08.17-ac037c-acf-cross-runtime-halt-signal-note.md` — is CLOSED. `OVER AND OUT` from both sides, 2026-08-18.** Its cross-runtime halt-signal question was answered and filed as ACf's team-runtime Step 78; its watcher-bug write-up is absorbed into `ac.md`. **37e's watcher on it is stopped.** 📝 **The file itself still sits in `docs/working/` — it wants retiring to `docs/sessions/historical/2026.08/`, but *never move a working doc without Justin's explicit OK*, so it is left in place for him.**
- 🔑 **The handover was done in the SAFE ORDER and it is worth copying:** the mailbox watcher was armed **before** the old one was stopped, and the retirement notice was written into the **OLD** file — because that is where ACf's watcher was pointed. **A new channel nobody is watching is not a channel.**

---

# §7 — HOW TO RUN 38's SEVEN LANES (reconciling this file with the marching orders)

**The orders at `docs/working/2026.08.17-ac037b-ac038-marching-orders.md` are still substantially correct. Three things in them are now STALE and this section governs:**

- ❌ **Its §0 says "the `CLAUDE.md` split is STILL UNMERGED… do not spawn `dc`/`qc`/`oc` as agent types."** **Both halves are now false: Lane A MERGED and the four types ARE spawnable.**
- ❌ **Its §2 ballot item 1 (the three Lane A merge gates) is CLOSED.** Do not re-present it.
- ⚠️ **Its §1 load list is right but its line counts have drifted** — `ac.md` is 744, not 685; the sprint is 765, not 757.

**✅ What in it is still live and good:** the seven lanes in §3, the gates in §5, the ACf boundary rules, and the "what done looks like" shape in §6. **Its load-order rationale in §1 is the best statement of that principle anywhere — read it.**

**🔑 Justin's guidance on lane shape, 2026-08-18, and it decides the architecture of the run:**

> **A lane that runs its own DC *and* QC should be a SEPARATE INSTANCE communicating through a watched doc — not a subagent.** A lane that is just one agent doing one thing can be a plain subagent, *"a lot easier."*

⇒ **Of the seven lanes, most carry a DC + a QC**, so most want to be real instances on committed lane docs with watchers. ⚠️ **And hold the cost shape in mind: a conductor's context scales with the DEPTH of the agent tree, not its width. Widen before you deepen.** **Seven lanes each returning a DC report and a QC report is a lot of surface for one conductor.**

⚠️ **Justin's own caution, 2026-08-18: *"with AC0-38 running 7 lanes, a lot of them will need to be their own instances. And that's a lot running with no supervision."*** **Treat the number of lanes you run at once as a decision to bring him, not a default.**

**NOT in that run: Step 110** (the five security inner-doors) — **explicitly sequenced AFTER the `classify.ts` extraction**, because all five live in the lines that refactor moves.

## 📋 YOUR OPENING BALLOT — assembled so you do not discover Justin-gated questions one at a time

**Use the marching orders' §2 ballot, with one item REMOVED and three ADDED.** *(Its whole design is "present it at spin-up, get it cleared in one pass, then run without him" — that is worth preserving.)*

- ❌ **REMOVE its item 1** (the three Lane A merge gates). **Closed — Lane A merged.**
- ✅ **KEEP its items 2–7** — Step 112 (the Operator auth gate's fail-open, a *decision* not a defect), Step 109 (how to test `runEvrynQuery` without spending real money), Step 105 (the two coupled failed-send design calls), Step 114 (needs a migration, so it cannot run unattended), control-character coverage for `_evryn-meta`, and worktree reaping.
- ➕ **ADD: the cutover.** Is it running today? **It is the gate that ends the three-copies window**, and every manual edit costs double until it does.
- ➕ **ADD: the three redirect drafts** (§3) — **he had not read them as of 2026-08-18 night.** They are cutover step 5's content.
- ➕ **ADD: `AGENT_PATTERNS.md`'s long-term fate** (§4.2) — permanent redirect, retire to `docs/historical/`, or something else. **His call, and it has been open since 2026-08-12.**
- ➕ **ADD: does the FOUNDING TEAM get the compaction rule?** (§4.6) — **they have none, and they do not inherit the router, so ADR-058 does not reach them.** **Low stakes while they are supervised; not low stakes once autonomy turns on.** **His files, his call.**

---

# §8 — MECHANICAL FACTS PAID FOR IN BLOOD. Do not re-learn them.

- 🔴 **LIVE MONITORS AT THE TIME OF WRITING — `TaskStop` them at set-down, and if you inherit them, NAME THEM in your own handoff:**
  - `bzyvvk8x1` — the 5-minute anti-stall heartbeat.
  - `btk2807le` — the **ac-mailbox** watcher.
  - `box17dged` — the **old halt-signal channel** watcher *(retire once ACf acknowledges)*.
- 🔴 **A MONITOR WITH A WRONG PATH DOES NOT ERROR — IT GOES QUIET, and its silence is indistinguishable from "nothing has changed."** ⇒ **Derive the root (`ROOT="$HOME/Evryn/Code"`), never hardcode a machine path** — Justin works from two machines with different home directories — **and make every missing input print a LOUD line.**
- 🔴 **On a merge that MOVES content between files, the conflict fires on the WRONG file.** Markers mark the file the content **LEFT**, never the file it should have **ARRIVED** in. **Diff the destination.**
- 🔴 **A grep cannot answer what a document SAYS, and cannot establish that anything is ABSENT.** Both rules are now in `ac.md`. **Read the file.**
- ⚠️ **`git branch -d` REFUSES on a fully-merged branch** — it measures merged-to-`origin/<branch>`, not merged-to-HEAD, and says so out loud. **Prove `origin/main..<branch>` = 0, then `-D`.**
- ⚠️ **`wc -l` under-counts a file whose last line has no trailing newline** (`dependency-map.md` reports 692; its canary is on 693). **Trust the canary.**
- ⚠️ **Writing a `\uXXXX` escape in prose EMITS THE REAL CHARACTER** — the tool argument travels as JSON. **A NUL makes the file binary to `grep` and `git diff`, so every text search silently skips it AND reports success.** ✅ **Scan with `node _evryn-meta/scripts/scan-control-chars.js <repo…>`** — ⚠️ **it only scans TRACKED files, so a brand-new untracked doc needs a byte check of its own.**
- ⚠️ **`scan-control-chars.js` must run from the PARENT with the tree as its argument.** From inside the tree it prints `[skip] … not a git repo` **and then reports CLEAN** — a vacuous pass.
- ⚠️ **This repo is `core.autocrlf=true`; docs are CRLF on disk. NORMALIZE BOTH SIDES (`tr -d '\r'`) before any byte comparison.**
- ⚠️ **Use a heredoc for any commit message containing backticks** — in `git commit -m "…"` a backticked span is **executed** and silently replaced by its output. **Use the QUOTED delimiter (`<<'MSG'`); unquoted does not disable substitution.**
- ⚠️ **Heredocs and `$(...)` inside a double-quoted `echo` have TWICE silently killed a verification block, letting a commit land unchecked.** ⇒ **Run verification as its own simple command**, never bundled into a compound one.
- ⚠️ **`MSYS_NO_PATHCONV=1` plus a POSIX path mangles a Windows path** — it once created a worktree at `C:\c\Users\…`. **Use a `C:/…` path for `worktree add`; reserve that env var for `git show 'rev:path'` arguments only.**
- ⚠️ **`git worktree remove` here can succeed at everything EXCEPT deleting the now-empty folder** (a Windows lock). **Check `worktree list` before concluding it failed, then `rmdir` separately after confirming the directory is empty.**
- 🔴 **Reaping a worktree can wipe the shared `node_modules` out of the CANONICAL tree, and it prints nothing when it happens.** ✅ **The reliable prevention is not to junction at all — run `npm ci` inside the worktree.** **Otherwise count `node_modules` before and after each reap, ONE AT A TIME, and repair on a delta** — a batch tells you *that* something broke, never *which step* broke it.
- ⚠️ **There is no `python` on this machine. Use `node` for scripting.** ⚠️ **And `/tmp` in git-bash is NOT `/tmp` to node** — node resolves it to `C:\tmp`. **Use the session scratchpad directory for temp files.**
- ⚠️ **`_evryn-meta` IS SHARED — ACf works in `_evryn-meta-acf15`.** **`git diff <file>` before every commit**, and **put the pathspec ON THE COMMIT** (`git commit -F - -- path/a path/b`) — `git add` does not scope a commit.
- **Machine:** laptop root `/c/Users/jbmcg/Evryn/Code`; desktop root `/c/Users/Justin/Evryn/Code`.
- 🔴 **Desktop permissions are an OPEN QUESTION, not a diagnosed bug** — do not apply the 2026-07-28 runbook as written. See `docs/current-state.md` → Infrastructure.

---

# §8b — WHAT I DID NOT DO, AND WHAT I AM LEAST SURE OF

**Stated plainly so you do not mistake silence for coverage.**

- **I ran no tests, no builds and no deploys, and I touched no `src/` file in any repo.** Everything I did was docs, manuals, protocols and ADRs. ⇒ **The `npm test` baseline of 65 suites / 1,925 assertions / 0 failed is INHERITED from 2026-08-17, not re-run by me.**
- **I did not spin a single subagent.** Every judgement in this file is my own read, not a distilled report. **That also means none of it has had a second pair of eyes** — Justin does not review merges, and there was no QC on doc work.
- ⚠️ **I did not verify the cutover's step 2 either way.** Whether a fresh session is still required is **genuinely open** — the evidence that the registry updates mid-session is real but narrow, and I did not test whether a definition's *body* actually programs a spawned agent. **Do not let my summary of it read as a finding.**
- ⚠️ **The redirect drafts are DESIGN, not tested behaviour.** Nobody has spun a main-agent DC against a redirect, because the redirect does not exist yet. **They are my best reasoning about a reader I could not observe.**
- ⚠️ **I applied the staging brief's edits by finding-by-content, as it instructed, because every line number in it was stale.** **I believe I found them all; I cannot prove it.** **The brief's new status table records exactly what I claim to have applied — check it against the files if a claim matters to you.**
- 🔑 **And the one to hold most loosely: I am the instance that wrote the rules I was applying.** ADR-058 and the manual edits are mine, made the same evening, with no independent review. **If something in them reads wrong to you, it may simply be wrong — re-derive it rather than defending it because it is written down.** *(That is this document's own second-order-trap rule, aimed at itself.)*

---

# §9 — THE ARC, SO YOU KNOW WHERE THIS IS ALL GOING

```
Lane A merged ✅ → CUTOVER (Justin's) → retire evryn-dev-workspace (LAST, undesigned)
                                       ↓
  deploy the bundle to STAGING → Gate-B → extract classify.ts → Gate-B AGAIN
                                       → flip Clustering + Haiku ON → gatekeeper forwards
```

**🔒 The critical path is Justin's ruling and is not yours to reorder:** ONE bundle, **staging first**, prod only after Gate-B passes. **Staggered prod deploys would leave Gate-B validating a version that no longer exists.** ⏱ **There is NO Gate-B deadline** — he is explicit that Gate-B being *meaningful* beats Gate-B being *fast*. **Do not sequence work backwards from a date.**

**🔵 The last step of the whole arc: RETIRE `evryn-dev-workspace`.** Justin's, and **NOT yet designed.** *"I'd actually like to retire `evryn-dev-workspace`. How do we do that? I feel like I want an archive."* 🔒 **Sequencing is explicit and his: this runs only AFTER the cutover AND after we have verified the subagents actually work well. Do not pull it forward.** **What must be worked out, none of it decided:** what else lives in that repo besides the manual; where the archive goes; **every inbound reference across the estate** (grep every repo for the repo NAME); and whether QC and OC follow — *"ask whether this is one retirement or three."*

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
