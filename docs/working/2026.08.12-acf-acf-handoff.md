# ACf-12 → ACf-13 Handoff — team-runtime lane. Written 2026-08-11 evening, for an overnight run.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **You are ACf-13, a fresh ACf.** This supersedes `2026.08.11-acf-acf-handoff.md` (ACf-11 → ACf-12), which stays readable for that session's narrative but is no longer current state.
>
> **🔴 THIS HANDOFF IS DIFFERENT FROM THE USUAL ONE, AND YOU SHOULD KNOW WHY.** Justin's explicit design: *"use this session to get things as absolutely ready to go as humanly possible, and then I'll re-spin you with fresh context and you can then just launch them on briefs you don't have to burn a bunch of tokens building."* **Everything is pre-built. Four lane briefs are written and committed. Your own load list is below, assembled by a full-load runtime scout so you do not spend a token working it out.**
>
> **⇒ Your job tonight is to EXECUTE, not to design.** Read, launch, coordinate, gate, merge, report. **If you find yourself building a brief, something has gone wrong — check whether you missed one that already exists.**

---

## §0 — ⚡ START HERE. Four things, in order.

1. **Load §1's TIER 1 list, in full.** Nothing else first. It is ~4,400 lines and it was chosen ruthlessly.
2. **Read the four lane briefs** (§3). You are not writing them — you are reading so you can gate their work competently.
3. **Confirm state yourself** (§2). Do not take this document's word for a SHA.
4. **Launch all four lanes** (§4), then coordinate. Justin has already approved the lane set and the overnight posture.

**🔴 THE TWO STANDING RULES JUSTIN ATTACHED TO THIS RUN — both are his words, and both are non-negotiable:**

> **(a) IF YOU COMPACT, PAUSE.** *"You have to tell next-you that if you compact, you need to pause. You can let your subagents finish, but you can't make new decisions."*
>
> **⇒ On compaction: let in-flight lanes finish and collect their reports. Do NOT gate a merge, do NOT resolve a lane's question, do NOT spin anything new, do NOT make a design call.** Update this handoff with exactly where things stand, ping `#team-alerts` that you compacted, and stop. **A fuzzy mind cannot judge its own fuzz** — and the specific danger here is that a post-compaction merge gate *feels* exactly like a pre-compaction one.
>
> **(b) KEEP THIS HANDOFF CURRENT AS YOU GO.** *"I also want ACf-13 to be updating your handoff brief as he goes, so that if I find him compacted in the morning, his brief is fully updated, and I can just re-spin him and continue."*
>
> **⇒ Append to §8 after every state change** — a lane launched, a report received, a merge gated, a question answered. **The test at every append: if I died right now, could a fresh ACf-14 pick up from this file alone?** Not at checkpoints. As you go.

---

## §1 — 🔴 YOUR LOAD LIST. Assembled by the runtime scout `ACfc3`, which had read the entire runtime and both halves.

**Why it is shaped this way:** you are a **conductor**, not a builder. You do not need the full runtime and must not try to hold it — three consecutive ACf instances have died of context exhaustion loading it. What you need is enough to **check a lane's claim against the code rather than taking it on report**, and to **gate a merge without rubber-stamping.**

### TIER 1 — before doing anything

**Governance**
- `_evryn-meta/CLAUDE.md` (full — 605 lines)
- `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` (full — 827) — **mandatory; you spin four lanes**
- `_evryn-meta/docs/protocols/load-cascades/conductor-cascade.md` (full — 96)
- `_evryn-meta/docs/current-state.md` (full — 152)
- `_evryn-meta/docs/hub/roadmap.md` (full — 196)

**This handoff + the four lane briefs** — see §3 for paths and spans.

**Intent**
- `evryn-team-runtime/docs/ARCHITECTURE.md` (full — 106) — **invariants 2, 4 and 8 are what a merge gate is FOR**
- `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` (full — 287)

**The runtime slice that makes your gating real rather than ceremonial**
- `evryn-team-runtime/src/scheduler/index.ts` (full — 784) — **lane 1 is *about* this file; gating it from a partial view is the fragment-feels-like-authority trap**
- `evryn-team-runtime/src/digest/index.ts` **572–886** — `sweepThreadDigests` (whose per-thread `.catch` isolation is the *pattern* Step 44 must copy) through the whole dormancy lifecycle
- `evryn-team-runtime/src/boot/schema.ts` (full — 221) — Steps 34+45 entirely
- `evryn-team-runtime/src/wakes/index.ts` **522–598** — the `memoryWriteFailed` setter and the mirror fence in one span
- `evryn-team-runtime/tests/source-no-nul-bytes.test.ts` (full — 129) — Step 31 entirely
- `evryn-team-runtime/tsconfig.json` (20) + `evryn-team-runtime/package.json` (33)

### TIER 2 — pull at the named beat, not up front

- **Before gating lane 1's merge:** `src/scheduler/dedup.ts` 72–95 + 309–345 + 397 (the dormancy-dominates rule) · `src/threads/membership.ts` 146–182 — ⚠️ **its `ACTIVE_AGENTS` intersection is a DEADLOCK GUARD, not an optimization; a "simplification" there pins threads in `closing` forever**
- **If lane 1 proposes touching the L7 fence:** `src/wakes/index.ts` 668–724 + `migrations/016_thread_digests.sql` 105–158
- **If lane 2 reports the set-equality guard infeasible:** `src/boot/schema.ts` 45–71 + `src/db/client.ts` (full — 28)
- **Before gating lane 2's typecheck change:** `evryn-team-runtime/tests/fixtures/README.md` (full — 41). **The scout called this the highest-value 41 lines in Tier 2** — it is the entire reason two files must stay broken.
- **Before writing merge changelog entries:** `evryn-team-runtime/CHANGELOG.md` (199) + `_evryn-meta/CHANGELOG.md`
- **Before answering a lane-3 question:** the memory design-of-record 56–95 + `2026.08.11-acfm-acf-runtime-questions.md` 13–54

### What the scout deliberately CUT, so you do not second-guess it

Not in Tier 1: the composer (~1,170 lines — no lane touches it), `db/paginate.ts`, `tools/tools.ts`, `slack/index.ts`, `boot/workspace.ts`, **and the entire identity half.** No lane edits an identity file, so the identity-is-runtime rule does not bind a conductor here.

> ⚠️ **ONE EXCEPTION, AND IT FLIPS INSTANTLY: if any lane proposes changing what an agent is TOLD — a tool description, a `runtime-ops.md` line, an agent definition — the identity half becomes mandatory that moment, and the change routes to Justin.** There is a live instance already waiting: `src/tools/tools.ts:370` tells an agent a feature is *"still being built"* while `runtime-ops.md:25` (composer layer 1, every wake) asserts it exists. **The layer-1 line is the correct one.**

---

## §2 — State, verified 2026-08-11 evening. **Confirm the SHAs yourself; a SHA in a doc is stale by definition.**

- **`evryn-team-runtime` `main` = `3e6c763`, PUSHED, 0 ahead / 0 behind.** Clean tree, one worktree, `main` only. **442 tests, typecheck strict-green. NOTHING DEPLOYED; the runtime is dormant and wakes nobody.**
- **`_evryn-meta`:** ACf-12 worked on branch **`acf/team-runtime-docs`** in worktree `_evryn-meta-acf`, **rebased onto AC0's `main` (`33c4dee`)**. ⚠️ **That branch is UNMERGED and carries the CLAUDE.md routing rule, the meta CHANGELOG entry, all four lane briefs and this handoff. Merging it is yours; Justin has vetted its content class but not this specific merge — ask.**
- **AC0 is active in `_evryn-meta` and `evryn-backend`. He is a PEER, not your report.** He owns pushes everywhere **except `evryn-team-runtime`, which is yours outright** (Justin, 2026-08-11) — your call on timing there, coordinate elsewhere.
- **Gate G1: migrations 015, 016, 017, 018 are written and APPLIED NOWHERE.** All four, in order, before any deploy. The L9 boot guard enforces it. **Justin's action, not yours.**
- **All prior subagents are closed out.** `ACfc3` (runtime scout) and `ACfc3t` (test analysis) both finished, both monitors down, neither wrote to any repo. **Subagent resume does not survive a session — they are unreachable to you. Their briefs and the capture doc are the record.**

---

## §3 — The four lane briefs. **Read all four; you are gating their work.**

| Lane | Brief (`_evryn-meta/docs/working/`) | Lines | Repo / posture |
|---|---|---|---|
| **`ACfd`** — dormancy & scheduler safety (Steps 43, 44, 50, 7, 51) | `2026.08.11-acf-acfd-dormancy-scheduler-lane-brief.md` | 156 | `evryn-team-runtime`, worktree + branch, **merges** |
| **`ACfg`** — guard integrity (Steps 34+45, 26, 31, 32, 52, 53, 54) | `2026.08.11-acf-acfg-guard-integrity-lane-brief.md` | 180 | `evryn-team-runtime`, worktree + branch, **merges** |
| **`ACfsq`** — the no-session architecture question | `2026.08.11-acf-acfs-session-question-lane-brief.md` | 90 | read-only, **nothing to merge** |
| **`ACfl`** — `AGENT_PATTERNS` → `LEARNINGS` | `2026.08.11-acf-acfl-learnings-consolidation-lane-brief.md` | 76 | `_evryn-meta` worktree, **commits on branch, NEVER merges** (source-of-truth docs; Justin vets) |

**Also load: `_evryn-meta/docs/working/2026.08.11-acfc3t-acf-test-coverage-map.md` (full — 331)** — the evidence base behind lanes 1 and 2. **You will need it at both merge gates.**

**Each brief already carries:** a blindness disclosure, its scope with pre-decided calls, a definition of done, a file-territory boundary against its sibling, and an empty §-for-notes the lane appends to. **What they do NOT carry is the tagged `<identity>`/`<mandatory_load>`/`<receipts>` blocks — you paste those verbatim from the orchestration protocol, using §5's file lists.**

---

## §4 — 🔴 Launch instructions

**Justin approved all four lanes and the overnight posture on 2026-08-11.** Spin them as **AC subagents** (the two-substitution AC variant), each running **its own DC and QC**.

**Every spin:**
- **Two-trip loading, no exceptions.** Trip 1 = `<identity>` + `<mandatory_load>` + `<receipts>`, **saying nothing about the work.** Verify receipts against your list. **Only then** resume with `<task>`.
- **Model-pinned `opus`** — explicitly, every spin, including the lanes' own spins (the recursive-propagation clause is mandatory in each lane's `<task>`).
- **Foreground**, so Justin can audit in the morning.
- **Tell each lane Justin is away and to BATCH rather than block** — one numbered recommend-first list in its report; keep advancing everything else.
- **You own worktree lifecycle.** Cut each code lane's worktree off current `main`; reap after merge.

### 🔴 MERGE ORDER IS DECIDED. Do not improvise it.

**`ACfd` merges FIRST. `ACfg` rebases onto it, re-runs typecheck on the rebased tree, then merges.**

**Why, and it is an ordering hazard git will never warn you about:** `ACfg`'s Step 26 makes `tests/` type-checked for the first time, while `ACfd` is *writing new tests*. Whichever lands second changes the bar for the other. **Both briefs already carry this instruction.** ⚠️ **If `ACfg` finishes first, it has been told to hold its config change specifically and land everything else.**

### The merge gate — your standing authorization, and its conditions

Justin confirmed you may merge to **`evryn-team-runtime` only**. That is the 2026-06-02 build-level-code standing authorization. **Use it only when ALL of these hold:**

1. The lane's QC returned **GO with zero blockers**, and the lane says so in its own voice.
2. **You re-ran `npm test` and `npx tsc --noEmit` on the MERGED tree yourself** — not on the branch, not on report. *(A prior ACf caught a real defect exactly here.)*
3. **No migration** is in the diff. **No identity-file edit** is in the diff.
4. You read the diff and agree. **Findings are inputs, not verdicts.**

**Anything short of that waits for Justin.** ⚠️ **And use `git diff main...branch` (three dots) to see what a branch actually changed** — a moved base makes a clean branch look like it reverts another lane's work, and that has bitten twice.

---

## §5 — Per-lane load lists, from the scout. **Paste into each `<mandatory_load>`; run `wc -l` first, spans move.**

**Shared governance core, on all four:** `_evryn-meta/CLAUDE.md` (605) · `docs/protocols/ac-orchestration-protocol.md` (827) · `docs/protocols/ac-writing-protocol.md` (100) · `docs/current-state.md` (152) · `docs/hub/roadmap.md` (196) · plus that lane's own brief.

⚠️ **Every `.ts`/`.sql` file in `evryn-team-runtime` carries a bottom truncation canary EXCEPT `src/wakes/memory-writer.ts`**, which holds the canary string as a functional constant — for that one, "full" means reaching the true final line (120). **State that carve-out in the brief; do not let an agent stall on it.** *(`scripts/smoke-wake-path.ts` also has none — that is Step 31's subject.)*

**`ACfd` (~9,000 lines).** Governance core + `ARCHITECTURE.md` (106) + SPRINT (287) + the memory design-of-record (`evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-team-runtime-memory-architecture.md`, 500) + ADR-052 (41) + the test-coverage capture (331).
*Code, full:* `scheduler/index.ts` (784) · `digest/index.ts` (886) · `scheduler/dedup.ts` (443) · `wakes/index.ts` (727) · `threads/membership.ts` (185) · `threads/kind.ts` (73) · **`boot/singleton.ts` (211 — both the `AcquireOpts` pattern AND the `STALE_MS` window Step 50 turns on)** · `money/ledger.ts` (222) + `money/budget.ts` (114) · `safety/kill.ts` (347) · `safety/notify.ts` (40) + `safety/rate-gate.ts` (55) + `safety/heartbeat.ts` (17) · `db/paginate.ts` (536) + `db/types.ts` (254) + `db/client.ts` (28) + `db/keyed-mutex.ts` (47) · `composer/index.ts` (576).
*Migrations:* 001 (58) · 007 (33) · 016 (160) · 017 (145) · 018 (74).
*Tests, full:* `dormancy-lifecycle` (343) · `dormancy-wake` (322) · `digest` (398) · `digest-pagination` (287) · `scheduler-dedup` (220) · `scheduler-members` (329) · `scheduler-velocity-wiring` (276) · `scheduler-lock-upsert` (23) · `composition-failure-honesty` (176) · `park-inert` (106) · `park-trigger` (154) · `rearm-episode` (133) · `rearm-parked` (88) · `singleton` (226) · `ledger-velocity` (175) · `url-414-call-sites` (260) · `ceiling-db.ts` (249) · `fake-db.ts` (164).

**`ACfg` (~5,500 lines).** Governance core + `ARCHITECTURE.md` + SPRINT + the test-coverage capture (331).
*Code, full:* `boot/schema.ts` (221) · `boot/index.ts` (162) · `boot/env.ts` (198) · `boot/workspace.ts` (396) · `boot/recovery.ts` (85) · `boot/singleton.ts` (211) · `safety/emergency.ts` (213) · `safety/notify.ts` (40) · `safety/heartbeat.ts` (17) · `safety/loop-signature.ts` (107) · `tools/sdk-server.ts` (24) · `tools/stdio-server.ts` (78) · `tools/tools.ts` (705) · `index.ts` (32) · `db/client.ts` (28) · `slack/index.ts` (407) · `slack/auth.ts` (30) · `config/agents.ts` (60) + `config/skills.ts` (119).
*Config:* `tsconfig.json` (20) · `package.json` (33) · `.env.example` (71) · `scripts/smoke-wake-path.ts` (130).
*Tests, full:* `source-no-nul-bytes` (129) · `boot-emergency-alert` (463) · `boot-schema` (219) · `boot-schema-integration` (93) · `boot-env` (142) · `boot-paused-default` (75) · `singleton` (226) · `workspace-git` (463) · `slack-auth` (125) · `slack-classify` (61) · `env-template-drift` (179) · `skills` (129) · `tools-sanitize` (31) · `scheduler-velocity-wiring` (276) · `dormancy-wake` (322) · `scheduler-members` (329) · `fixtures/README.md` (41) + all four fixtures (10/15/7/14).
*Migrations:* 008 (35) · 016 (160) · 017 (145) · 018 (74).

**`ACfsq` (read-only).** Governance core + `ARCHITECTURE.md` + design-of-record (500) + ADR-052 (41) + ADR-050 (40) + SPRINT + **`2026.08.11-acfm-acf-runtime-questions.md` (243)**.
*Code:* `wakes/index.ts` (727) · `composer/index.ts` (576) + `composer/layers.ts` (594) · `scheduler/index.ts` (784) · `tools/tools.ts` (705) · `wakes/memory-writer.ts` (120) · `wakes/env.ts` (65) · `db/types.ts` (254) · `config/agents.ts` (60) + `config/skills.ts` (119) · migrations 010 (45) + 015 (111) · `package.json` (33).
🔴 **THE IDENTITY HALF IS MANDATORY FOR THIS LANE AND NO OTHER:** `evryn-team-workspace/shared/protocols/runtime-ops.md` (77) · `evryn-team-workspace/CLAUDE.md` (399) · `.claude/agents/lucas.md` (202) · `.claude/agent-memory/lucas/MEMORY.md` (168). **Reason: `runtime-ops.md` §1 tells every agent, in every wake, that there is no hidden longer session behind this. Adopting sessions makes that line FALSE in the composed prefix of all eight agents — a cost invisible from `src/` alone.**

**`ACfl`.** Governance core + `_evryn-meta/AGENT_PATTERNS.md` (738) + `_evryn-meta/LEARNINGS.md` (146) + **the four manuals its verification test needs**: `evryn-team-workspace/CLAUDE.md` (399) · `evryn-team-workspace/shared/protocols/runtime-ops.md` (77) · `evryn-quality/CLAUDE.md` (502) · `evryn-dev-workspace/CLAUDE.md` (312) · `evryn-backend/CLAUDE.md` (52). **No team-runtime code** — Justin's "reflected in both domains" test is about the *manuals that govern* each domain, not `src/`.

---

## §6 — What is owed, beyond the lanes

- **The `_evryn-meta` branch `acf/team-runtime-docs` is unmerged.** Ask Justin before merging it — it carries a `CLAUDE.md` change.
- **Steps 48, 49, 55** (the membership-legibility trio: the in-wake roster line, `/who`, and a goodbye that wakes nobody) are **filed and unassigned.** **Step 55 needs a migration**, so it must not ride an unattended lane.
- **The SEALED-EXEC sweep** (`2026.08.11-acf-sealedexec-sweep-lane-brief.md`) is written and unspun. **The product runtime's status is UNKNOWN, not clear.** ⚠️ The test agent also found **seven bare-name `git` invocations, two of them in production code** (`boot/workspace.ts:78`, `composer/index.ts:124`) — weaker than the `npx` class, same shape, and the sweep should expect them.
- **Gate G3's blocker list is incomplete.** Justin approved adding **Step 19** (the inert per-job money layer) as a third blocking Step, because one of the four remaining Lucas paces tests a job spend cap and is therefore **unrunnable**, not merely unrun. **Not yet written into the tracker — do it.**
- **`BUILD-PHASE-1.md` review**: done by ACf-12. Its success criteria are the eight Lucas paces; criterion 5 is the one Step 19 blocks. Minor drift noted (it describes 8 composition layers; ARCHITECTURE says 13) — not worth a trip alone.

---

## §7 — How to talk to Justin

- **Ping `#team-alerts` on every response, prefix `ACf:`, BEFORE composing.** Webhook `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`; Node `fetch`, never bash+curl.
- **🔴 QUESTIONS GO INLINE**, beside the reasoning that makes them answerable. The end-of-message recap is a convenience copy, **never** the first place a question appears. **This cost him an hour on 2026-08-11** and is now its own block in `CLAUDE.md`.
- **Every noun gets its instance.** *"A source-of-truth doc"* → *"a source-of-truth doc **(the sprint tracker)**."* The test: if he had to guess which one, could he guess wrong in a way that changes how he feels?
- **Never a positional section title** (*"2a"*) — anchor by content.
- **Lead relays with `# ACf's read — <topic>`.** Sub-ACs cap at `##`.
- **He is not an engineer, he has read none of these documents, and he is cold every single volley.**
- **Batch.** One landing, one conversation, one launch — do not bring him in mid-wave except for the hard floor (a gate, an identity edit, money, anything irreversible, or a material fact that would change a decision he already made).

---

## §8 — 🔴 LIVE LOG. Append after every state change. This is the crash-survival surface.

*(ACf-13: append here as you go, per Justin's standing instruction in §0. The test at every append: **if I died right now, could ACf-14 pick up from this file alone?**)*

- **2026-08-11 evening (ACf-12):** All four lane briefs written and committed. Runtime scout and test-analysis subagent both closed out. `evryn-team-runtime` pushed and synced at `3e6c763`. `_evryn-meta` work committed on `acf/team-runtime-docs`, rebased onto AC0's `main`. **Nothing is spun.** Handing over.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
