# ACf-12 → ACf-13 Handoff — team-runtime lane. Written 2026-08-11 evening, for an overnight run.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **You are ACf-13, a fresh ACf.** This supersedes `2026.08.11-acf-acf-handoff.md` (ACf-11 → ACf-12), which stays readable for that session's narrative but is no longer current state.
>
> **🔴 THIS HANDOFF IS DIFFERENT FROM THE USUAL ONE, AND YOU SHOULD KNOW WHY.** Justin's explicit design: *"use this session to get things as absolutely ready to go as humanly possible, and then I'll re-spin you with fresh context and you can then just launch them on briefs you don't have to burn a bunch of tokens building."* **Everything is pre-built. Four lane briefs are written and committed. Your own load list is below, assembled by a full-load runtime scout so you do not spend a token working it out.**
>
> **⇒ Your job tonight is to EXECUTE, not to design.** Read, launch, coordinate, gate, merge, report. **If you find yourself building a brief, something has gone wrong — check whether you missed one that already exists.**

---

## §0 — ⚡ START HERE.

> ### 🔴 FIRST, THE THING THAT SAVES YOU THE MOST CONTEXT: **THIS HANDOFF IS YOUR COMPLETE INSTRUCTION SET. DO NOT RUN A STARTUP CASCADE.**
>
> **Justin is going to point you at this file and walk away.** *(His words: "I'm literally only going to point him to the handoff and walk away.")* He will not tell you which cascade to load, because **you are not loading one.**
>
> **§1's TIER 1 list IS your load.** It was assembled by a full-load runtime scout that had read the entire runtime — both halves — specifically so you would not have to. It already contains everything the Light cascade would give you (`CLAUDE.md`, `current-state.md`, the Hub) plus a deliberately-chosen runtime slice.
>
> ⚠️ **`_evryn-meta/CLAUDE.md` auto-loads into your context and its SESSION STARTUP section will tell you to run a Light cascade and then a full per-build cascade. Tonight, do not.** Running the team-runtime full cascade would pull the entire ~20,000-line runtime into your head — **the exact thing that has killed three consecutive ACf instances by context exhaustion before their lanes ever built**, and the exact cost this whole pre-built package exists to avoid. **You are a conductor. The runtime lives in your lanes' heads, not yours.**
>
> **If something genuinely seems missing from Tier 1, add it deliberately and note it in §8 — do not resolve the doubt by loading a cascade.**

**Five things, in order:**

1. **Arm the self-wake canary and load the two deferred tools** — §0(c) below. **One action, before anything else**, so it covers your load too.
2. **Load §1's TIER 1 list, in full.** Nothing else first. It is ~4,400 lines and it was chosen ruthlessly.
3. **Read the four lane briefs** (§3). You are not writing them — you are reading so you can gate their work competently.
4. **Confirm state yourself** (§2). Do not take this document's word for a SHA.
5. **Launch all four lanes** (§4), **ping Justin the single "all clear"** (§7), then coordinate through the night. **Justin has already approved the lane set, the merge order, and the overnight posture — there is nothing left for him to approve before you start.**

**🔴 THE FOUR STANDING RULES JUSTIN ATTACHED TO THIS RUN — all four are his, and all four are non-negotiable:**

> **(a) IF YOU COMPACT, PAUSE.** *"You have to tell next-you that if you compact, you need to pause. You can let your subagents finish, but you can't make new decisions."*
>
> **⇒ On compaction: let in-flight lanes finish and collect their reports. Do NOT gate a merge, do NOT resolve a lane's question, do NOT spin anything new, do NOT make a design call.** Update this handoff with exactly where things stand, ping `#team-alerts` that you compacted, and stop. **A fuzzy mind cannot judge its own fuzz** — and the specific danger here is that a post-compaction merge gate *feels* exactly like a pre-compaction one.
>
> **(b) KEEP THIS HANDOFF CURRENT AS YOU GO.** *"I also want ACf-13 to be updating your handoff brief as he goes, so that if I find him compacted in the morning, his brief is fully updated, and I can just re-spin him and continue."*
>
> **⇒ Append to §8 after every state change** — a lane launched, a report received, a merge gated, a question answered. **The test at every append: if I died right now, could a fresh ACf-14 pick up from this file alone?** Not at checkpoints. As you go.
>
> **(c) 🔴 ARM A SELF-WAKE CANARY BEFORE YOU DO ANYTHING ELSE — INCLUDING BEFORE YOU LOAD.**
>
> **⚠️ READ THIS CAREFULLY, BECAUSE THE OBVIOUS READING IS WRONG AND ACf-12 GOT IT WRONG FIRST.** **The canary does NOT ping Justin. It pings YOU.** *(His words: "the canary is to wake **you** in case you stop for some reason. I want it to ping **you** every 20 minutes, just in case you stopped.")*
>
> **Justin will not be there. He is not coming back tonight.** Nobody is watching for a heartbeat, so a heartbeat *to him* is worthless. **The failure this guards is you silently stopping mid-run** — and the fix is a notification that arrives in your own chat and wakes you back up.
>
> **The mechanism, concretely.** `Monitor` streams each stdout line of a script into your chat as a notification, and **a notification re-invokes you.** So a script that prints one line every 20 minutes is a 20-minute self-wake:
>
> ```
> Monitor({
>   description: "ACf-13 self-wake canary — 20 min",
>   persistent: true,
>   command: 'while true; do sleep 1200; echo "CANARY $(date +%H:%M) — if you are reading this, you may have stopped. Check every live lane ADVANCED (worktree git state: new commits, dirty files), then continue where you left off."; done'
> })
> ```
>
> **On each canary tick: check the lanes advanced via WORKTREE GIT STATE — new commits, dirty files — NOT the transcript file size.** ⚠️ **A completed agent's transcript symlink reads 0 bytes mid-run and will lie to you.** Then **carry on with whatever you were doing.** No ping to Justin, no announcement, nothing — **the tick is for you.**
>
> ⚠️ **`Monitor` is a DEFERRED tool — `ToolSearch` for it before you can call it** (`select:Monitor`). Do the same for **`SendMessage`** (`select:SendMessage`), which **every two-trip spin depends on.** Load both in your first action, alongside arming the canary.
>
> **Kill the canary with `TaskStop` when you send your final Done ping**, so it does not keep firing into a finished session.
>
> ### ⏱ ONE SPECIFIC WAY YOU MIGHT STOP, AND WHY IT IS COMPLETELY SAFE TO RESTART FROM
>
> *(Justin, 2026-08-11. He rates it unlikely — the limit is deep — but you should not meet it for the first time at 3am with no idea what happened.)*
>
> **You may hit Anthropic's 5-hour usage limit. It is believed to kill subagents as well as stop you.** Hopefully they are resumable by `agentId`; **if not, you will have to re-spin the affected lanes from their briefs — which is exactly what the briefs are built for.** A re-spin costs a load, not a memory: every lane brief is written to be picked up cold, and each lane keeps its own §-for-notes current for precisely this.
>
> 🔑 **THE PART THAT MAKES THIS EASY, AND IT IS NOT OBVIOUS: your next canary tick will not actually reach you until the next 5-hour window opens.** So **by the time you wake up, you are at the START of a fresh window with a lot of room** — there is no risk of immediately re-hitting the wall, and no reason to work timidly. **Just restart cleanly and keep going.**
>
> **When it happens:** re-read §8 to find where you were (this is why you keep it current), check each lane's **worktree git state** to see what actually survived, resume by `agentId` if you can, re-spin from the brief if you cannot — **and note it in §8** so Justin can see the night had a gap.

> **(d) 🔴 YOU HAVE STANDING PRE-AUTHORIZATION TO COMMIT AND PUSH, ALL NIGHT. This overrides the normal gate, and without it the night does not work.**
>
> *(Justin, 2026-08-11: "since ACf-13 is going to be working all night, he needs to be able to commit and push. Normally that's a no-go, so I need you to give him pre-auth all night to do that.")*
>
> **`CLAUDE.md` says every commit needs his explicit go-ahead in his most recent message, and that prior authorizations do not stack.** ⚠️ **That rule is correct and it is suspended for tonight, by him, deliberately** — because he will be asleep and a per-commit gate would stall you at the first one and waste the entire night. **Do not re-derive the standing rule from `CLAUDE.md` and stop; he has already answered it.**
>
> **What you may do freely:** commit and push in **`evryn-team-runtime`** (yours outright) and in **`_evryn-meta`**. Merge the two code lanes per §4's conditions. **Push at every natural checkpoint** so a crash or a hit usage limit cannot cost you work.
>
> **🔴 THE ONE CONDITION, AND IT IS NOT OPTIONAL — LIST SOURCE-OF-TRUTH COMMITS IN THE MORNING REPORT.** *(His words: "any source of truth docs that were committed without my eyes on the changes need to be listed out in the morning report, so that I can vet them in the timeline.")*
>
> **Keep a running list in §8 as you go** — do not try to reconstruct it at 6am — and reproduce it in the morning orientation as its own short section: **which source-of-truth doc, which commit, one line on what changed.** He vets them in the source-control timeline after the fact. **The docs that count: the Hub and spokes, `ARCHITECTURE.md`, BUILD docs, any protocol, any `CLAUDE.md`, `LEARNINGS.md`, `AGENT_PATTERNS.md`.** *(Lane briefs, handoffs, working docs and changelogs are ordinary traffic — they do not need listing.)*
>
> **🚫 WHAT THIS DOES NOT COVER, and these stay absolute even with nobody awake:** **no deploy** (`railway up`), **no production migration**, **no identity-file edit** (`runtime-ops.md`, agent definitions, agent memory — Justin's layer, and all four lane briefs already bar it). **Nothing tonight needs any of them.** If a lane reports it does, that is a stop-and-batch for morning.
>
> ⚠️ **And the multi-agent discipline still holds, because AC0's work may be sitting in `_evryn-meta` uncommitted:** **`git add <paths>` does NOT scope the commit** — a bare `git commit` afterwards commits the entire index, including whatever anyone else staged. **Put the pathspec on the commit itself** (`git commit -F - -- path/a path/b`), and **`git diff <file>` before committing a shared file** to confirm the whole diff is yours.

---

## §1 — 🔴 YOUR LOAD LIST. Assembled by the runtime scout `ACfc3`, which had read the entire runtime and both halves.

**Why it is shaped this way:** you are a **conductor**, not a builder. You do not need the full runtime and must not try to hold it — three consecutive ACf instances have died of context exhaustion loading it. What you need is enough to **check a lane's claim against the code rather than taking it on report**, and to **gate a merge without rubber-stamping.**

### TIER 1 — before doing anything

**Governance**
- `_evryn-meta/CLAUDE.md` (full — 605 lines)
- `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` (full — 827) — **mandatory; you spin four lanes**
- `_evryn-meta/docs/protocols/load-cascades/conductor-cascade.md` (full — 96)
- `_evryn-meta/docs/current-state.md` (full — 152)
- `_evryn-meta/docs/hub/roadmap.md` (full — 196)

**This handoff** — `_evryn-meta/docs/working/2026.08.12-acf-acf-handoff.md` (full — 329) — **plus the four lane briefs and the test-coverage capture; paths and spans in §3.**

**Intent**
- `evryn-team-runtime/docs/ARCHITECTURE.md` (full — 106) — **invariants 2, 4 and 8 are what a merge gate is FOR**
- `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` (full — 298)

**The runtime slice that makes your gating real rather than ceremonial**
- `evryn-team-runtime/src/scheduler/index.ts` (full — 784) — **lane 1 is *about* this file; gating it from a partial view is the fragment-feels-like-authority trap**
- `evryn-team-runtime/src/digest/index.ts` **572–886** — `sweepThreadDigests` (whose per-thread `.catch` isolation is the *pattern* Step 44 must copy) through the whole dormancy lifecycle
- `evryn-team-runtime/src/boot/schema.ts` (full — 221) — Steps 34+45 entirely
- `evryn-team-runtime/src/wakes/index.ts` **522–598** — the `memoryWriteFailed` setter and the mirror fence in one span
- `evryn-team-runtime/tests/source-no-nul-bytes.test.ts` (full — 129) — Step 31 entirely
- `evryn-team-runtime/tsconfig.json` (20) + `evryn-team-runtime/package.json` (33)

### TIER 2 — pull at the named beat, not up front

- **Before gating lane 1's merge:** `src/scheduler/dedup.ts` 72–95 + 309–345 + 397 (the dormancy-dominates rule) · `src/threads/membership.ts` 146–182 — ⚠️ **its `ACTIVE_AGENTS` intersection is a DEADLOCK GUARD, not an optimization; a "simplification" there pins threads in `closing` forever**
- **If lane 1 proposes touching the L7 fence:** `src/wakes/index.ts` 668–724 + `migrations/016_thread_digests.sql` 105–158
- **If lane 2 reports the set-equality guard infeasible:** `src/boot/schema.ts` 45–71 + `src/db/client.ts` (full — 28)
- **Before gating lane 2's typecheck change:** `evryn-team-runtime/tests/fixtures/README.md` (full — 41). **The scout called this the highest-value 41 lines in Tier 2** — it is the entire reason two files must stay broken.
- **Before writing merge changelog entries:** `evryn-team-runtime/CHANGELOG.md` (199) + `_evryn-meta/CHANGELOG.md`
- **Before answering a lane-3 question:** the memory design-of-record 56–95 + `2026.08.11-acfm-acf-runtime-questions.md` 13–54

### What the scout deliberately CUT, so you do not second-guess it

Not in Tier 1: the composer (~1,170 lines — no lane touches it), `db/paginate.ts`, `tools/tools.ts`, `slack/index.ts`, `boot/workspace.ts`, **and the entire identity half.** No lane edits an identity file, so the identity-is-runtime rule does not bind a conductor here.

> ⚠️ **ONE EXCEPTION, AND IT FLIPS INSTANTLY: if any lane proposes changing what an agent is TOLD — a tool description, a `runtime-ops.md` line, an agent definition — the identity half becomes mandatory that moment, and the change routes to Justin.** There is a live instance already waiting: `src/tools/tools.ts:370` tells an agent a feature is *"still being built"* while `runtime-ops.md:25` (composer layer 1, every wake) asserts it exists. **The layer-1 line is the correct one.**

---

## §2 — State, verified 2026-08-11 evening. **Confirm the SHAs yourself; a SHA in a doc is stale by definition.**

- **`evryn-team-runtime` `main` = `3eedd3f`, PUSHED, 0 ahead / 0 behind.** Clean tree, one worktree, `main` only. **442 tests, typecheck strict-green. NOTHING DEPLOYED; the runtime is dormant and wakes nobody.**
- **`_evryn-meta`:** ACf-12 worked on branch **`acf/team-runtime-docs`** in worktree `_evryn-meta-acf`, **rebased onto AC0's `main` (`33c4dee`)**. ⚠️ **That branch is UNMERGED and carries the CLAUDE.md routing rule, the meta CHANGELOG entry, all four lane briefs and this handoff. Merging it is yours; Justin has vetted its content class but not this specific merge — ask.**
- **AC0 is active in `_evryn-meta` and `evryn-backend`. He is a PEER, not your report.** He owns pushes everywhere **except `evryn-team-runtime`, which is yours outright** (Justin, 2026-08-11) — your call on timing there, coordinate elsewhere.

> ### 🔴 YOU AND AC0 ARE BOTH ON WORKTREES ALL NIGHT. THE ONLY DANGEROUS MOMENT IS A MERGE.
>
> *(Justin's instruction, 2026-08-11: "the only thing to watch out for is when you merge. Each of you will likely have merges, and if you're careful with them, that'll be fine.")*
>
> **Working in parallel is safe by construction — separate worktrees, separate branches, no shared working tree to switch under anyone.** ⚠️ **What is NOT safe is merging on a stale picture**, and `main` moved under me four times today alone.
>
> **Before ANY merge, in either repo:**
> 1. **`git fetch`** — then check whether `main` moved since you cut the branch. It probably did.
> 2. **Check AC0's worktree HEAD** (`git -C <repo> worktree list`) so you know what he is mid-flight on. **If he is sitting on the same files, wait or ask — do not race him.**
> 3. **If `main` gained anything: REBASE the branch onto it, re-run the gates on the REBASED tree, then fast-forward.** A merge commit here buries the lane's diff in unrelated noise.
> 4. **🔴 Read the diff with `git diff main...branch` — THREE dots.** Two dots compares tips, so **anything `main` gained that the branch lacks renders as a deletion BY the branch** — a clean branch looks like it reverts another lane's work. **This has bitten twice.** `git log main..branch -- <file>` proves a file was never touched.
>
> **You will not collide with AC0 in `evryn-team-runtime` — he is not in it.** The exposure is `_evryn-meta` only.
- **Gate G1: migrations 015, 016, 017, 018 are written and APPLIED NOWHERE.** All four, in order, before any deploy. The L9 boot guard enforces it. **Justin's action, not yours.**
- **All prior subagents are closed out.** `ACfc3` (runtime scout) and `ACfc3t` (test analysis) both finished, both monitors down, neither wrote to any repo. **Subagent resume does not survive a session — they are unreachable to you. Their briefs and the capture doc are the record.**

---

## §3 — The four lane briefs. **Read all four; you are gating their work.**

| Lane | Brief (`_evryn-meta/docs/working/`) | Lines | Repo / posture |
|---|---|---|---|
| **`ACfd`** — dormancy & scheduler safety (Steps 43, 44, 50, 7, 51) | `2026.08.11-acf-acfd-dormancy-scheduler-lane-brief.md` | 156 | `evryn-team-runtime`, worktree + branch, **merges** |
| **`ACfg`** — guard integrity (Steps 34+45, 26, 31, 32, 52, 53, 54) | `2026.08.11-acf-acfg-guard-integrity-lane-brief.md` | 180 | `evryn-team-runtime`, worktree + branch, **merges** |
| **`ACfsq`** — the no-session architecture question | `2026.08.11-acf-acfs-session-question-lane-brief.md` | 90 | read-only, **nothing to merge** |
| **`ACfl`** — `AGENT_PATTERNS` → `LEARNINGS` | `2026.08.11-acf-acfl-learnings-consolidation-lane-brief.md` | 76 | `_evryn-meta` worktree, **commits on branch, NEVER merges** (source-of-truth docs; Justin vets) |

**Also load: `_evryn-meta/docs/working/2026.08.11-acfc3t-acf-test-coverage-map.md` (full — 331)** — the evidence base behind lanes 1 and 2. **You will need it at both merge gates.**

**Each brief already carries:** a blindness disclosure, its scope with pre-decided calls, a definition of done, a file-territory boundary against its sibling, and an empty §-for-notes the lane appends to. **What they do NOT carry is the tagged `<identity>`/`<mandatory_load>`/`<receipts>` blocks — you paste those verbatim from the orchestration protocol, using §5's file lists.**

### 🔴 WHAT TO DO WHEN EACH LANE COMES BACK — they do NOT all end the same way

- **`ACfd` and `ACfg` — gate, then merge** per §4's conditions. **Then write the changelog entries yourself**, in `evryn-team-runtime/CHANGELOG.md`: a lane cannot ship, so the entry belongs to whoever merges. **Tell each lane to hand you the changelog-worthy facts in its report** rather than writing entries itself — `CHANGELOG.md` is a hot file and a lane reaching into it is how another lane's work gets swept up.
- **`ACfsq` — read its recommendation, do NOT act on it.** It reverses part of ADR-052; **that is Justin's call, not yours.** Distil it to CEO altitude for his morning and say what you think, clearly marked as your read.
- **`ACfl` — verify, do NOT merge.** Its two files are source-of-truth and Justin vets them in source control. **Check that it actually ran the extracted-specifics set-diff and reported the result** — at 738 lines, a read-through is not evidence, and *"I reviewed it carefully"* is exactly the claim this whole session has been learning to distrust.
- **Any lane returning a blocking question you cannot answer: BATCH IT for Justin's morning and keep every other lane advancing.** Do not stall a lane on a question, and do not wake him. The only things that interrupt overnight are the hard floor — a push, a deploy, a migration, an identity edit, money, anything irreversible.
- **🔴 A lane's report saying work is "in flight" is NOT proof it is unfinished — go look at the artifacts.** A sub-AC once reported a fix-trip in progress that had already completed; **what caught it was checking the worktree git state, not re-reading the report.** Same discipline in reverse: a claimed *"handled"* is a claim until the commit or the diff shows it.

### What "done" looks like for this run

**Two code lanes merged (or held with a written reason), two research/doc lanes reported, changelog entries written for whatever merged, every worktree and branch reaped, this handoff current, the canary stopped, and ONE `Done for the night` ping — followed by the morning orientation in §7's three-beat shape.** **If you compact before that, §0(a) governs: finish nothing new, update this file, ping, stop** — and **write the morning orientation anyway**, describing where things actually stand, so Justin can re-spin you and continue without reconstructing anything.

---

## §4 — 🔴 Launch instructions

**Justin approved all four lanes and the overnight posture on 2026-08-11.** Spin them as **AC subagents** (the two-substitution AC variant), each running **its own DC and QC**.

> ### 🔴 THE ORCHESTRATION PROTOCOL CHANGED ON 2026-08-11, AFTER MOST OF THIS PACKAGE WAS BUILT. Read this before you spin.
>
> **`ac-orchestration-protocol.md` gained a section — *"WHEN THE LOAD IS TOO BIG FOR ONE AGENT — the THREE-trip spin"* (now ~941 lines, and it is on your Tier 1 list). The conductor cascade changed with it.** Read both; the summary below is orientation, not a substitute.
>
> **What it establishes:** a full-runtime load **no longer reliably fits one agent** — three independent confirmations, including **my own runtime scout compacting mid-load earlier today.** The fix is a **DERIVE trip in front of the usual two**: the agent proposes its own load list from a map artifact, **you review and approve it**, then the normal LOAD and WORK trips follow.
>
> **✅ WHY THIS MOSTLY DOES NOT CHANGE YOUR NIGHT: the derive step is already DONE.** §5's per-lane lists were built by a full-load scout that had read the entire runtime, both halves. **The protocol's own test is *"could I hand this agent an explicit list right now?"* — you can; it is written. So spin the lanes TWO-trip against those lists.**
>
> 🔴 **BUT THE SIZE QUESTION IS LIVE, AND IT IS THE PART TO WATCH.** The protocol's measurement is counter-intuitive and you should hold it: **the expensive thing is OUR PROSE, not the code.** `CLAUDE.md` runs ~144 tokens/line and the orchestration protocol ~166; TypeScript runs ~30–45. **The governance core on every lane is ~1,880 lines of dense prose before a single line of runtime.** `ACfd`'s list is the largest and is the one most at risk.
>
> **⇒ If a lane comes back compacted, that run is JUNK — discard and re-run. Do NOT accept its output and do NOT "discount" it.** And when you re-run:
>
> 🚫 **DO NOT SHORTEN THE LIST.** That is the barred move, explicitly. **Restructure the work instead**, using the protocol's four named strategies — **fan out sub-readers** (one per subsystem, each holding its slice in full, parent reconciles — keep them alive as a bench for cross-seam questions), **run it instead of reading it**, **a bounded slice only where a map justifies it**, or **a hard stop** (*"I cannot do this in one head; here is the split I need"*). **The agent must name which strategy it used, per file.** ⚠️ **The forbidden move, named so it cannot hide: *"I pre-judged this file unlikely to matter."***
>
> ⚠️ **And do not reach for `#cascade-override` because a load looked big** — that narrows the *set* and stays barred here. **Three-trip changes the *delivery*.** Different instruments, different problems.

**Every spin:**
- **Two-trip loading against §5's pre-derived lists** — see the box above for when that is not enough. Trip 1 = `<identity>` + `<mandatory_load>` + `<receipts>`, **saying nothing about the work.** Verify receipts against your list. **Only then** resume with `<task>`.
- **Model-pinned `opus`** — explicitly, every spin, including the lanes' own spins (the recursive-propagation clause is mandatory in each lane's `<task>`).
- **Foreground**, so Justin can audit in the morning.
- **Tell each lane Justin is away and to BATCH rather than block** — one numbered recommend-first list in its report; keep advancing everything else.
- **You own worktree lifecycle.** Cut each code lane's worktree off current `main`; reap after merge.

### 🔴 MERGE ORDER IS DECIDED. Do not improvise it.

**`ACfd` merges FIRST. `ACfg` rebases onto it, re-runs typecheck on the rebased tree, then merges.**

**Why, and it is an ordering hazard git will never warn you about:** `ACfg`'s Step 26 makes `tests/` type-checked for the first time, while `ACfd` is *writing new tests*. Whichever lands second changes the bar for the other. **Both briefs already carry this instruction.** ⚠️ **If `ACfg` finishes first, it has been told to hold its config change specifically and land everything else.**

### The merge gate — your standing authorization, and its conditions

Justin confirmed you may merge to **`evryn-team-runtime` only**. That is the 2026-06-02 build-level-code standing authorization. **Use it only when ALL of these hold:**

1. The lane's QC returned **GO with zero blockers**, and the lane says so in its own voice.
2. **You re-ran `npm test` and `npx tsc --noEmit` on the MERGED tree yourself** — not on the branch, not on report. *(A prior ACf caught a real defect exactly here.)*
3. **No migration** is in the diff. **No identity-file edit** is in the diff.
4. You read the diff and agree. **Findings are inputs, not verdicts.**

**Anything short of that waits for Justin.** ⚠️ **And use `git diff main...branch` (three dots) to see what a branch actually changed** — a moved base makes a clean branch look like it reverts another lane's work, and that has bitten twice.

---

## §5 — Per-lane load lists, from the scout. **Paste into each `<mandatory_load>`; run `wc -l` first, spans move.**

**Shared governance core, on all four:** `_evryn-meta/CLAUDE.md` (605) · `docs/protocols/ac-orchestration-protocol.md` (941) · `docs/protocols/ac-writing-protocol.md` (100) · `docs/current-state.md` (152) · `docs/hub/roadmap.md` (196) · plus that lane's own brief.

⚠️ **Every `.ts`/`.sql` file in `evryn-team-runtime` carries a bottom truncation canary EXCEPT `src/wakes/memory-writer.ts`**, which holds the canary string as a functional constant — for that one, "full" means reaching the true final line (120). **State that carve-out in the brief; do not let an agent stall on it.** *(`scripts/smoke-wake-path.ts` also has none — that is Step 31's subject.)*

**`ACfd` (~9,000 lines).** Governance core + `ARCHITECTURE.md` (106) + SPRINT (287) + the memory design-of-record (`evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-team-runtime-memory-architecture.md`, 500) + ADR-052 (41) + the test-coverage capture (331).
*Code, full:* `scheduler/index.ts` (784) · `digest/index.ts` (886) · `scheduler/dedup.ts` (443) · `wakes/index.ts` (727) · `threads/membership.ts` (185) · `threads/kind.ts` (73) · **`boot/singleton.ts` (211 — both the `AcquireOpts` pattern AND the `STALE_MS` window Step 50 turns on)** · `money/ledger.ts` (222) + `money/budget.ts` (114) · `safety/kill.ts` (347) · `safety/notify.ts` (40) + `safety/rate-gate.ts` (55) + `safety/heartbeat.ts` (17) · `db/paginate.ts` (536) + `db/types.ts` (254) + `db/client.ts` (28) + `db/keyed-mutex.ts` (47) · `composer/index.ts` (576).
*Migrations:* 001 (58) · 007 (33) · 016 (160) · 017 (145) · 018 (74).
*Tests, full:* `dormancy-lifecycle` (343) · `dormancy-wake` (322) · `digest` (398) · `digest-pagination` (287) · `scheduler-dedup` (220) · `scheduler-members` (329) · `scheduler-velocity-wiring` (276) · `scheduler-lock-upsert` (23) · `composition-failure-honesty` (176) · `park-inert` (106) · `park-trigger` (154) · `rearm-episode` (133) · `rearm-parked` (88) · `singleton` (226) · `ledger-velocity` (175) · `url-414-call-sites` (260) · `ceiling-db.ts` (249) · `fake-db.ts` (164).

**`ACfg` (~5,500 lines).** Governance core + `ARCHITECTURE.md` + SPRINT + the test-coverage capture (331).
*Code, full:* `boot/schema.ts` (221) · `boot/index.ts` (162) · `boot/env.ts` (198) · `boot/workspace.ts` (396) · `boot/recovery.ts` (85) · `boot/singleton.ts` (211) · `safety/emergency.ts` (213) · `safety/notify.ts` (40) · `safety/heartbeat.ts` (17) · `safety/loop-signature.ts` (107) · `tools/sdk-server.ts` (24) · `tools/stdio-server.ts` (78) · `tools/tools.ts` (705) · `index.ts` (32) · `db/client.ts` (28) · `slack/index.ts` (407) · `slack/auth.ts` (30) · `config/agents.ts` (60) + `config/skills.ts` (119).
*Config:* `tsconfig.json` (20) · `package.json` (33) · `.env.example` (71) · `scripts/smoke-wake-path.ts` (130).
*Tests, full:* `source-no-nul-bytes` (129) · `boot-emergency-alert` (463) · `boot-schema` (219) · `boot-schema-integration` (93) · `boot-env` (142) · `boot-paused-default` (75) · `singleton` (226) · `workspace-git` (463) · `slack-auth` (125) · `slack-classify` (61) · `env-template-drift` (179) · `skills` (129) · `tools-sanitize` (31) · `scheduler-velocity-wiring` (276) · `dormancy-wake` (322) · `scheduler-members` (329) · `fixtures/README.md` (41) + all four fixtures (10/15/7/14).
*Migrations:* 008 (35) · 016 (160) · 017 (145) · 018 (74).

**`ACfsq` (read-only).** Governance core + `ARCHITECTURE.md` + design-of-record (500) + ADR-052 (41) + ADR-050 (40) + SPRINT + **`2026.08.11-acfm-acf-runtime-questions.md` (243)**.
*Code:* `wakes/index.ts` (727) · `composer/index.ts` (576) + `composer/layers.ts` (594) · `scheduler/index.ts` (784) · `tools/tools.ts` (705) · `wakes/memory-writer.ts` (120) · `wakes/env.ts` (65) · `db/types.ts` (254) · `config/agents.ts` (60) + `config/skills.ts` (119) · migrations 010 (45) + 015 (111) · `package.json` (33).
🔴 **THE IDENTITY HALF IS MANDATORY FOR THIS LANE AND NO OTHER:** `evryn-team-workspace/shared/protocols/runtime-ops.md` (77) · `evryn-team-workspace/CLAUDE.md` (399) · `.claude/agents/lucas.md` (202) · `.claude/agent-memory/lucas/MEMORY.md` (168). **Reason: `runtime-ops.md` §1 tells every agent, in every wake, that there is no hidden longer session behind this. Adopting sessions makes that line FALSE in the composed prefix of all eight agents — a cost invisible from `src/` alone.**

**`ACfl`.** Governance core + `_evryn-meta/AGENT_PATTERNS.md` (738) + `_evryn-meta/LEARNINGS.md` (146) + **the four manuals its verification test needs**: `evryn-team-workspace/CLAUDE.md` (399) · `evryn-team-workspace/shared/protocols/runtime-ops.md` (77) · `evryn-quality/CLAUDE.md` (502) · `evryn-dev-workspace/CLAUDE.md` (312) · `evryn-backend/CLAUDE.md` (52). **No team-runtime code** — Justin's "reflected in both domains" test is about the *manuals that govern* each domain, not `src/`.

---

## §6 — What is owed, beyond the lanes

- **The `_evryn-meta` branch `acf/team-runtime-docs` is unmerged.** Ask Justin before merging it — it carries a `CLAUDE.md` change.
- **Steps 48, 49, 55** (the membership-legibility trio: the in-wake roster line, `/who`, and a goodbye that wakes nobody) are **filed and unassigned.** **Step 55 needs a migration**, so it must not ride an unattended lane.
- **The SEALED-EXEC sweep** (`2026.08.11-acf-sealedexec-sweep-lane-brief.md`) is written and unspun. **The product runtime's status is UNKNOWN, not clear.** ⚠️ The test agent also found **seven bare-name `git` invocations, two of them in production code** (`boot/workspace.ts:78`, `composer/index.ts:124`) — weaker than the `npx` class, same shape, and the sweep should expect them.
- **Gate G3's blocker list is incomplete.** Justin approved adding **Step 19** (the inert per-job money layer) as a third blocking Step, because one of the four remaining Lucas paces tests a job spend cap and is therefore **unrunnable**, not merely unrun. **Not yet written into the tracker — do it.**
- **`BUILD-PHASE-1.md` review**: done by ACf-12. Its success criteria are the eight Lucas paces; criterion 5 is the one Step 19 blocks. Minor drift noted (it describes 8 composition layers; ARCHITECTURE says 13) — not worth a trip alone.

### 🔴 From ACf-12's end-of-session fragment sweep — things that were carried in a handoff and never written down. **Do not let these repeat the pattern.**

- **✅ CLOSED IN THE SWEEP (recorded so you do not re-open them):** **Steps 56 and 57** — Justin ruled on both on the morning of 2026-08-11 and neither had reached the tracker a full day later. 56 = the server-side query is the intended end state for the HTTP-414 fix, with the *why-the-workaround-shipped* provenance attached. 57 = the URL byte budget has never been validated against a real server, only against our own client-side arithmetic. **Both now filed.** And **G3 gained Step 19** as a third blocking Step. **The lesson worth carrying: a ruling that lives only in a handoff is a ruling that quietly does not exist.**
- **⬜ THE TRACKER'S DONE-ENTRY TRIMS ARE STILL OWED** *(Justin, 2026-08-11, ruling 4: "Make sure we've captured stuff in changelog for ALL steps, then make them appropriately tight — don't OVER-tighten to the point where we lose useful info in the tracker.")* **The changelog-capture half is DONE and the answer was clean:** all 16 DONE Steps are captured, with the single exception of **Step 36** (the four QC pattern promotions), whose gap is now closed by the DC/QC changelog-routing ruling. **The trims themselves have not been done.** ⚠️ **They are dense runtime narrative, so do them with the runtime picture in front of you — and show Justin ONE trimmed Step before doing sixteen.**
- **⬜ RETIREMENT CANDIDATES, never actioned — surface only; Justin's explicit OK before moving anything.** Ready for `docs/sessions/historical/2026.08/`: `2026.08.06-acf-acf-handoff.md` · `2026.08.10-acf-acf-handoff.md` · `2026.08.06-acf-acfw-wake-outage-brief.md` · both `2026.08.10-acfm-dc-step{4,5}-*-build-spec.md` · `2026.08.10-acfm-justin-runtime-ops-batched-edit-DRAFT.md` (applied to the live doc; superseded by the real file) · **and now `2026.08.11-acf-acf-handoff.md`, superseded by this one.** ⚠️ **`2026.08.10-acfm-acf-lane-setdown.md` is *becoming* retireable — its five findings are Steps 43–47 — but VERIFY the Steps carry everything before cutting it.** *(`2026.08.10-acf-ac0-dashboard-status-pill-note.md` is AC0's to retire, not yours.)*
- **⬜ `#sweep` CADENCE ROUTING TO LUCAS, never done.** Justin's Q2 reasoning: *"Learnings gets redistributed at #sweep, and that can sit for a while, while we continue to make the same mistakes."* ⇒ **make `#sweep` happen more often.** **Lucas owns sweep cadence, so this routes to him** — and `ACfl`'s work makes it more urgent, since `LEARNINGS.md` is about to get materially larger.
- **⬜ `ADR-052 §7` CARRIES A SUPERSEDED REQUIREMENT** *(flagged twice by the runtime scout; never fixed).* It states that consolidation-review touchpoints must be *"phone-first, under a minute."* **The design-of-record §8.1 explicitly REVERSES this** — *"'light enough' does NOT mean sub-minute — and demanding that is itself the corner-cutting failure… 5–10 minutes is a great outcome,"* with **frequency**, not speed, named as the real lever. **The ADR was never updated.** Small, and it will mislead whoever builds Phase 2 consolidation. **ADRs are exempt from the propose-first rule, so this is fixable directly — but say so to Justin.**
- **⬜ Step 33's OTHER two line references are UNVERIFIED.** ACf-12 corrected 33a (`tools.ts:339-340` → `:370`) **at source**, but 33b (`migrations/015_thread_scratchpads.sql:74`) and 33c (`src/composer/layers.ts:251-253`) were **not** re-checked — a grep for the 33a phrasing would not match their different wording. **Given 33a had drifted, assume these may have too.**
- **Three test findings from the capture doc that belong to `ACfd` and are NOT yet in its brief** — hand them to it in `<task>` when you spin it: **(i)** `dormancy-wake.test.ts:14-15`'s header claim that *"the whole suite has never had a runWake behavioral test"* is **false** (`composition-failure-honesty.test.ts` does execute `runWake`) and should be corrected in place; **(ii)** `scheduler-dedup.test.ts:189-199` exercises the priority merge in **one direction only** — wrap it in the same both-orders loop its two siblings already use; **(iii)** `composition-failure-honesty.test.ts` drives `runWake` three times as `lucas` and **never calls `clearGateCache()`**, so two of three tests read a cached gate state — inert today, but order- and clock-dependent the moment gate state matters, **and the same file documents this exact hazard for a *different* singleton.**

### Two things Justin offered that you should actually use

- **He can pause other agents to clear space for you.** His words: *"lots of agents everywhere. Expect things to move. Let me know if you need something stable — I can usually pause others to clear space for you."* **If a merge gate needs a quiet repo, ask.**
- **ACf-11 is available for clarification** on anything from the 08-10/11 overnight build, via Justin.

---

## §7 — How to talk to Justin

> ### 🔴 TONIGHT'S PING PROTOCOL OVERRIDES THE STANDING ONE. READ THIS BEFORE YOU PING ANYTHING.
>
> **`CLAUDE.md` says to ping Justin on every response. TONIGHT THAT IS WRONG, and it is wrong by his explicit instruction.** He is not there and is not coming back. **A stream of pings into an empty room is noise he wakes up to.**
>
> **You send exactly TWO pings, plus one conditional:**
>
> 1. **"ALL CLEAR"** — once all four lanes are launched and running. One line. That is his signal that the night started cleanly and he can stop thinking about it.
> 2. **🔴 A BLOCKER PING — IMMEDIATELY, if and only if you are genuinely blocked** and the night cannot proceed without him. **Do not sit on a real blocker until morning; that wastes the whole night.** But hold it to the real bar: a lane's question you can answer is not a blocker, and neither is anything you can batch. **The hard floor is the bar** — a push, a deploy, a migration, an identity edit, money, anything irreversible.
> 3. **"DONE FOR THE NIGHT"** — the last thing you do, carrying the one-paragraph outcome. **Kill the canary with it** (`TaskStop`).
>
> **Nothing else. No heartbeats, no progress notes, no "lane 2 returned GO."** All of that goes in §8 and in your final response.
>
> ### 🔴 AND YOUR FINAL RESPONSE IS A MORNING ORIENTATION, NOT A REPORT
>
> *(Justin's instruction: "the final response to me should be an orientation for the morning, which needs to consist of a light orientation — what we're doing, what got done, and what's next.")*
>
> **He will read it cold, having been away for hours, and he is not an engineer.** So write it in exactly three light beats:
>
> - **What we're doing** — one short paragraph re-orienting him on the lane. *What* the team runtime is, *where* we are in it. **Assume he remembers nothing.**
> - **What got done** — plain English outcomes, not Step numbers. *"The thing that stops a conversation's record vanishing now has a guard that actually fails when you break it"* beats *"Step 51 closed."*
> - **What's next** — the two or three things that matter, and **anything genuinely his to decide, stated inline with enough context to decide it.**
> - **🔴 PLUS ONE REQUIRED SECTION: "Source-of-truth docs I committed without your eyes."** Per §0(d) — the doc, the commit, one line on what changed, so he can vet each in the source-control timeline. **If the answer is "none," say "none" explicitly** rather than omitting the section; a missing section reads as forgotten, not as empty.
>
> **Keep it LIGHT.** He asked for an orientation, not a debrief. The detail lives in §8, in the changelogs, and in the tracker — all of which are there if he wants them. **Do not make him read a wall to find out whether the night went well.**

- **Ping mechanics:** `#team-alerts`, prefix `ACf:`, webhook `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`; Node `fetch`, never bash+curl (both bash-curl and PowerShell have failure modes on Windows that will burn you).
- **🔴 QUESTIONS GO INLINE**, beside the reasoning that makes them answerable. The end-of-message recap is a convenience copy, **never** the first place a question appears. **This cost him an hour on 2026-08-11** and is now its own block in `CLAUDE.md`.
- **Every noun gets its instance.** *"A source-of-truth doc"* → *"a source-of-truth doc **(the sprint tracker)**."* The test: if he had to guess which one, could he guess wrong in a way that changes how he feels?
- **Never a positional section title** (*"2a"*) — anchor by content.
- **Lead relays with `# ACf's read — <topic>`.** Sub-ACs cap at `##`.
- **He is not an engineer, he has read none of these documents, and he is cold every single volley.**
- **Batch.** One landing, one conversation, one launch — do not bring him in mid-wave except for the hard floor (a gate, an identity edit, money, anything irreversible, or a material fact that would change a decision he already made).

---

## §8 — 🔴 LIVE LOG. Append after every state change. This is the crash-survival surface.

*(ACf-13: append here as you go, per Justin's standing instruction in §0. The test at every append: **if I died right now, could ACf-14 pick up from this file alone?**)*

- **2026-08-11 evening (ACf-12):** All four lane briefs written and committed. Runtime scout and test-analysis subagent both closed out. `evryn-team-runtime` pushed and synced at `3e6c763`. `_evryn-meta` work committed on `acf/team-runtime-docs`, rebased onto AC0's `main`. **Nothing is spun.** Handing over.

---

### ACf-13's log begins here

**T0 — LOAD + STATE VERIFICATION COMPLETE.**

- **Canary armed** (`Monitor` task `bdy161upx`, 20-min self-wake). `Monitor` + `SendMessage` + `TaskStop` + `TaskOutput` schemas loaded.
- **Tier 1 loaded in full**, all canaries confirmed: `CLAUDE.md` (auto) · orchestration protocol (941) · conductor cascade (110) · current-state (152) · roadmap (196) · team-runtime `ARCHITECTURE.md` (106) · SPRINT (298) · `scheduler/index.ts` (783) · `digest/index.ts` 572–885 · `boot/schema.ts` (220) · `wakes/index.ts` 515–604 · `source-no-nul-bytes.test.ts` (128) · `tsconfig.json` · `package.json`. Plus all four lane briefs and the test-coverage capture (331).
- **NO startup cascade was run**, per §0. Nothing was added to Tier 1 — it was sufficient.

**STATE, VERIFIED MYSELF (§2's SHAs were stale in one important way):**

- **`evryn-team-runtime` `main` = `3eedd3f`**, pushed, 0 ahead / 0 behind, clean tree, one worktree. *(§2 said `3e6c763`; three SPRINT commits landed after it was written. `3eedd3f` is the tip.)*
- **`_evryn-meta` `main` = `a04f5a0`**, pushed, 0/0, clean tree.
- 🔴 **§6's owed item "the `acf/team-runtime-docs` branch is unmerged — ask Justin" is ALREADY CLOSED.** That branch is **merged into `main` and reaped**; the only local branch is `main`. All four lane briefs, the handoff, the meta CHANGELOG entry and the `CLAUDE.md` routing rule are on `main` and pushed. **Nothing to ask.**
- **Baseline MEASURED, not inherited: `npm test` = 442 pass / 0 fail; `npx tsc --noEmit` = clean (no output).** Matches §2's claim.

**WORKTREES PROVISIONED (all off current `main`, all with a real `npm ci` — no junctions, per `CLAUDE.md`'s reap hazard):**

| Lane | Worktree | Branch | Base |
|---|---|---|---|
| `ACfd` | `Code/evryn-team-runtime-acfd` | `acfd/dormancy-scheduler` | `3eedd3f` |
| `ACfg` | `Code/evryn-team-runtime-acfg` | `acfg/guard-integrity` | `3eedd3f` |
| `ACfl` | `Code/_evryn-meta-acfl` | `acfl/learnings-consolidation` | `a04f5a0` |
| `ACfsq` | *(none — read-only)* | — | — |

`node_modules` = **146 entries in all three team-runtime trees** (canonical unaffected). **Record that number — it is the before-value for the reap check at close-out.**
*(One self-caught error: the `ACfl` worktree was first cut from `evryn-team-runtime` because a `cd` persisted through an `&&` chain. Removed, branch deleted, re-cut from `_evryn-meta`. Verified by checking `AGENT_PATTERNS.md` and `LEARNINGS.md` are present in it.)*

**🔴 A DELIBERATE, DECLARED DEVIATION FROM THE ORCHESTRATION PROTOCOL — ACf-14 and Justin should both see this.**

The protocol's *"When the target is a SEPARATE codebase"* loophole guard states that when a `#cascade-override` target is **agentic**, the file list **must carry both halves — code AND identity.** The scout's per-lane lists (§5) give the identity half to **`ACfsq` only**, explicitly ("mandatory for this lane and no other").

**I am following the scout's lists and NOT adding the identity half to `ACfd` or `ACfg`.** The reasoning, so it can be judged rather than trusted:

1. **The scout had read both halves** when it made that assignment, so this is a derivation by a better-informed party, not a triage by an uninformed one.
2. **Neither lane may edit an identity file** — both briefs bar it outright (§3 in each).
3. 🔴 **The decisive one: size.** By the protocol's own token-per-line measurements, `ACfd`'s approved list is already ≈**850K tokens** — the governance core alone is ~2,000 lines of prose at ~150 tok/line. The identity half adds ≈**119K** (the team `CLAUDE.md` is 398 dense lines). That would put `ACfd` at ~97% of a 1M window **before it does any work**, which is where the 2026-08-11 product scout compacted. The protocol's standing-file rule yields on exactly one condition — *"unless including a standing file would literally break the run"* — and **an agent that compacts mid-load has had its run broken.**

**How the gap is bounded rather than hidden:** each code lane's `<task>` states plainly that **it does not hold the identity half**, and that **any implication for what an agent is TOLD — a tool description, an agent-facing string literal, a `runtime-ops.md` line — must be ROUTED to me, never acted on.**

**⚠️ WHAT IT WOULD MEAN IF I AM WRONG:** a lane could change `src/` in a way that contradicts a standing instruction in the identity half without either of us seeing it — the exact class where an agent burns tokens every wake reconciling a contradiction. **The live instance already on the books is Step 33a** (`tools/tools.ts:370` tells an agent a shipped feature is *"still being built"* while `runtime-ops.md:25` asserts it exists); that one is already known and is on nobody's tonight-list, so it cannot be re-broken. **Justin: this is the one call tonight I would most like a second opinion on.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
