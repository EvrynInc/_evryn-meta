# Handoff — AC0 → next AC0 — 2026-07-10 (end of week: Clustering `v0.2.7` LIVE + dashboard deployed; wave = AC2/AC3/AC7 + ACf independent)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** START HERE if you're a fresh AC0. You are the **conductor** of a multi-lane v0.2-hardening wave — you orchestrate, gate merges/deploys, reconcile cross-lane collisions, keep housekeeping current, and translate to Justin at CEO altitude. You do NOT write runtime code. This handoff is **self-sufficient** — it summarizes every lane's live state; you do NOT need to pre-read the lane briefs to boot (read a lane's brief in full, on-demand, when that lane surfaces work). Companions: `docs/current-state.md`, `_evryn-meta/CHANGELOG.md`, `docs/deploy-log.md`, the lane briefs in `docs/working/`.

---

## 🚀 First moves (fresh AC0 — do these on boot)
1. **Load the Light cascade** (`_evryn-meta/CLAUDE.md` auto-loads → `docs/current-state.md` → Hub `roadmap.md`). Load the **full `src/` runtime only if Justin explicitly calls the full cascade** (then ALL of it — enumerate live: `find evryn-backend/src -name '*.ts'`; never trust a hard-coded count). ⚠️ **ARCH + BUILD are reconciled + accurate vs the runtime** (AC6's lane, merged 2026-07-09) — a trustworthy on-ramp, **but NOT license to skip `src/`.** Justin was emphatic: knowing both intent AND the granular runtime stays load-bearing; skipping the runtime "because the docs are accurate now" has almost always burned us.
2. **Remake your ping helper** (⚡ below) and **ping `#team-alerts` "I'm up"** (`AC0:` prefix).
3. **Orient, don't chase:** nothing is hot-blocked on YOU at handoff. Live waits are on the lanes' Monday work + Justin's gates. Be ready to gate + answer + keep records current.
4. **Then watch for** (your standing owed-list, §"AC0 OWED"): AC2 reaching **③ Runtime Bookkeeping** → run **Gate-A** with them BEFORE it merges (SPRINT Step 88); orchestrate **Gate-B** (pre-gatekeeper full live-fire on staging).

---

## ⚡ Operating model (the load-bearing bits — carry these)
- **You conduct; you do NOT write runtime code.** Each lane's DC/QC builds; you gate merges/deploys, reconcile cross-lane collisions + sequencing, keep Justin at CEO-altitude, keep housekeeping current (current-state, CHANGELOG, deploy-log, sprint DONE-marks).
- **PING `#team-alerts` EVERY response**, `AC0:` prefix — even when Justin seems present (he reinforced this repeatedly). Helper (remake it — the path won't persist): a Node script that POSTs `{text}` to `SLACK_TEAM_WEBHOOK_URL` — read that var's LINE from `C:/Users/Justin/Evryn/Code/evryn-team-workspace/.env`. In Node use the `C:/Users/...` path form (NOT git-bash `/c/...`).
- **Each lane's brief IS the AC↔AC0 mailbox** (`docs/working/`). Append replies as a `## ═══ AC0 → ACn · date ═══` banner *before* the canary; **commit + push immediately** (brief/mailbox commits are pre-authorized). No live AC-to-AC channel — coordinate through Justin or committed briefs. ⚠️ **The briefs are CRLF and fight the Edit tool** — append via a small Node `fs.readFileSync`/`replace-before-canary`/`writeFileSync` script (write it to your scratchpad dir; `node -e` with markdown backticks breaks bash quoting — use a file). Slack pings via Node too (Windows).
- **Justin reads ONLY the chat, comes in cold each volley** (many lanes; he runs ~8 agents). CEO-altitude, plain English, **never jargon-by-number.** Use the LOCKED cost-lane names: **"Haiku Bouncer"** (cost ①, shipped `v0.2.6` OFF), **"Clustering"** (cost ②, shipped `v0.2.7` OFF), **"Runtime Bookkeeping"** (cost ③, NOT started — next).
- **Commit/deploy gates are Justin's, per-action — they don't stack.** Lanes build/edit freely, pause for his explicit go before a **code** commit + before any **deploy** + before **merge-to-main**. **⚠️ NEW this week — a *merge* can BE a deploy:** the monitoring **dashboard lives in `_evryn-meta/dashboard/` and Vercel auto-deploys on push to `_evryn-meta` main** — so merging a dashboard branch to main = a LIVE dashboard deploy → **Justin-gated.** (Same for `evryn-website` — a PP/marketing edit auto-deploys on push. Only `evryn-backend` prod is manual `railway up`.) **STANDING EXCEPTION (yours):** housekeeping-doc commits (current-state, CHANGELOG, deploy-log, sprint DONE-marks) + mailbox/brief + working-doc (handoffs) — you commit + push freely. **Stage by explicit path, never `git add -A`** (shared repos, many agents).
- **Part A doc-currency (standing AC0 responsibility):** keep `ARCHITECTURE.md` / BUILD / SPRINT current at each `#lock` — if a session changed what those docs claim, update the doc that same #lock (or route it, naming section + owner). ARCH is source-of-truth → Justin SCM-vets the diff before you commit it (he can do this fast; don't skip it).
- **You're a PEER to the lanes on their design, not their boss.** Answer collision/sequencing Qs; let them own their lane's architecture. **`AskUserQuestion` is BANNED (hangs) — ballot in chat + ping instead.**

---

## 📊 LIVE STATE (authoritative)
- **Prod = `v0.2.7` LIVE** (`evryn-backend` main `517014d`, tag `v0.2.7`, Railway deployment `1c5bdfaf`, clean boot). Shipped this week: **Clustering** (cost ②, Step 58 / ADR-049) — **ships `CLUSTERING_MODE=off` → DORMANT, zero prod behavior change until the Mark-gated flip.** The same deploy carried **`declining.md`** (Step 55, Mira's contact-facing decline playbook) LIVE. **Haiku Bouncer** (cost ①, `v0.2.6`) is also live but **OFF** (`HAIKU_SCREEN_MODE` unset).
- **Two prod flips are REQUIRED before a gatekeeper forwards (Mark-onboarding actions, both currently OFF):** (1) Clustering `off`→`manual`→`scheduled` + set `CLUSTER_TIMES_PT` (~10am + ~4:30pm PT) + `CLUSTER_HEALTHCHECK_URL`; (2) Haiku Bouncer `off`→`shadow`→(sign-off)→`active` (banks ~$1,800/mo). Both in the operator-guide Go-Live Checklist + watch-list.
- **Dashboard: the Opus-vs-Haiku by-model spend cut is LIVE** (deployed via Vercel on this #lock's push) on `evryn-dashboard.vercel.app` → "Evryn Product" tab (completes SPRINT Step 46). PII-firewall verified.
- **`_evryn-meta` main** carries this #lock (v0.2.7 reconcile + dashboard merge + protocol amendment + all lane briefs + this handoff — `git fetch` and read the tip at boot; the exact SHA advances as this #lock's final commits land). All repos pushed this #lock (see Repo state).
- **The wave = 3 active AC lanes + 1 independent AC:** **AC2** (cost — ③ next) · **AC3** (identity/ops — 54a/54c UN-HELD, top priority) · **AC7** (doc-accuracy process). **ACf** = a SEPARATE independent AC building Justin's autonomous team-agents (research phase) — works in `evryn-team-workspace` (see ⚠ below). **AC6/AC4/AC1 lanes CLOSED.**

---

## 🛣 Critical path to a gatekeeper (the frame)
Finish **③ Runtime Bookkeeping** (Gate-A DEV baseline runs WITH AC0 before it merges) → **Gate-B — the mandatory pre-gatekeeper full create-from-zero live-fire on staging** (staging is UP; needs `SEND_ENABLED`→true + the run; Megan-Griffiths fixtures feed it; AC0 orchestrates) → flip Clustering + Haiku on → Justin's account setups → gatekeeper forwards.

> **On "Mark":** Mark is **radio-silent, NOT lost** (Justin, 2026-07-09 — normal for him to go quiet for weeks; the earlier "may have checked out" flag is superseded). The pre-Mark path STANDS. **Also: Justin has been meeting a *second* gatekeeper** — someone could come in *before* Mark. So hold the frame as "ready when a gatekeeper shows" (Mark's return OR the new one), not "waiting on Mark specifically." The build work (③ / staging / the flips) is worth finishing regardless.

---

## LANES

### AC2 — cost (brief: `docs/working/2026.06.30-ac0-ac2-cost-lane-brief.md`)
- **① Haiku Bouncer = DONE + LIVE** (`v0.2.6`, OFF). **② Clustering = DONE + LIVE** (`v0.2.7`, OFF) — shipped this week; the Round-17 note-turn/queued-item silent-drop was caught + fixed + live-Opus-verified (PB4). **New tracked `Step 58a`** = the `cluster now`-not-in-Slack-catch-up-replay gap (low harm, deferred to the next clustering/③ trip).
- **③ Runtime Bookkeeping (Step 57) = NOT STARTED — the keystone, AC2's next + last cost change.** The runtime pre-loads context + owns status/lifecycle writing; Evryn returns a structured verdict `{result, reasoning}` and does only judgment (cuts a pass ~15→~2-4 turns; removes her raw `supabase_upsert` write surface — ADR-033 territory). **In scope (folded in):** **54b** (forward-persistence, verdict-keyed — Justin picked option C; brief `docs/working/2026.07.07-ac3-dc-54b-forward-persistence-build-brief.md`) + the **user-creation-at-verdict OPEN QUESTION** (move user-creation to verdict-time + create no user for `ignore`?) + the note-turn **`record_pass` residual** + Step 71 (Reflection binding safety-net).
- **⭐ YOUR standing gate:** **③ is an interactive/tool-calling-pathway rewrite → the process lesson applies** (a breaker-tripped/stubbed test proves the plumbing, not the behavior — it needs a live-path DEV exercise, breaker off, before GO). **Run Gate-A (Step 88) WITH AC2 — a DEV end-to-end baseline — BEFORE ③ merges.** ③ also co-designs with AC4's read-lock end-state (one tool-surface: ③ removes the write, AC4 locked the read).
- **AC2 was at ~3% compaction at handoff** → Justin will spin a fresh Monday-AC2; his work is all pushed.

### AC3 — identity/ops (brief: `docs/working/2026.06.30-ac0-ac3-identity-ops-brief.md`)
- **⭐ 54a + 54c are UN-HELD** (Clustering shipped, which rewrote the `triage.md`/`process.ts`/`detect.ts` surfaces they touch). **Monday-AC3's top priority.** **They MUST rebase onto post-Clustering `main` + re-verify against the now-LIVE files first** (don't graft onto anything stale). **54a** = point Evryn's "have I met this person?" triage check at the CONTACT's *permanent* record (not the 6-month `emailmgr_items`) — a Mira wording beat, brief READY (`docs/working/2026.07.07-ac3-mira-54a-history-check-permanent-store.md`, `a1aaa3b`), **Justin sends it to Mira** at un-hold. **54c** = real-name capture (surface the "From: Real Name" display name in `detect.ts`/`process.ts` + a Mira Opus-correction beat) — designed, spin DC on a fresh worktree off `main`.
- **Staging = UP + healthy** (Step 80). Remaining: flip `SEND_ENABLED`→true + the fake-Mark validation loop (= Gate-B, Step 82/88). Deploy method: `railway up --service evryn-staging --environment production` — ⚠ a **bare `railway up` deploys PROD**.
- **Shipped this week:** Nathan Step-56 legal glance (nothing gates a gatekeeper/v0.2; the stored pass-stamp is DSAR-reachable → v0.3 DSAR must strip the gk-UUID + redact the first name); Step 48 gatekeeper-onboarding runbook; Step 49 operator-guide DB-restore; Step 81 staging runbook; Step 43 Megan fixtures transcribed; Step 52 (GA) → MOOT. Dashboard by-model cut (Step 46) + the orchestration-protocol non-runtime-target amendment MERGED this #lock.
- **On AC3's queue:** the Mira decline-*offer* runtime+identity pair (Step-55 routed-back — build only on Justin's explicit go); Step 46/78/79 dashboard extensions (DC/OC); DEFER Step 51 (compulsion audit — touches `classify.ts`).

### AC7 — doc-accuracy process fix (brief: `docs/working/2026.07.09-ac0-ac7-doc-accuracy-process-brief.md`)
- **Part B** of the doc-accuracy fix: a hard `#lock` doc-currency GATE + the "update-routing protocol" (change-type → doc-section map) + doc-currency headers on ARCH+BUILD + standing beats in AC/Soren/Mira manuals. Cross-repo SOT + manual edits → **each needs Justin's explicit approval; Soren = ARCH/BUILD owner-of-record.** Coordinate via you. (No update from AC7 reached me this session — check its brief when it surfaces.)

### ACf — autonomous team-agents build (INDEPENDENT — not your lane) ⚠
- ACf is a separate AC Justin runs, building the autonomous founding-team agents — **research phase, no collisions yet.** He works in **`evryn-team-workspace`** (`shared/projects/ops/team-runtime/` + team helm/memory docs). **At handoff `evryn-team-workspace` is DIRTY with ACf's uncommitted in-flight work — DO NOT commit, sweep, or push it.** When you push `evryn-team-workspace` (e.g. your team-current-state append), **stage ONLY your own file by explicit path.** A worktree-coordination item may surface later; for now just don't trip him.

---

## 📌 AC0 OWED (standing — carry these)
1. **Gate-A (Step 88) WITH AC2 before ③ Runtime Bookkeeping merges** — a DEV live-path baseline (③ is an interactive-pathway rewrite; stubbed coverage isn't enough). **Gate-B** = the mandatory pre-gatekeeper full live-fire on **staging** (needs AC3's `SEND_ENABLED` flip; Megan fixtures; you orchestrate).
2. **Part A doc-currency** at each `#lock` (ARCH/BUILD/SPRINT) — standing.
3. **Prod `_PROD` env cleanup** — the dead unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` on the Railway **prod** service ride the NEXT coordinated prod `railway up` (the ③ deploy). `railway variable delete` can't `--skip-deploys`, so it can't go standalone — do it in the same window as the next prod deploy. (Was owed at the v0.2.7 deploy but AC2 didn't action it — still owed.)
4. **Housekeeping as-you-go:** CHANGELOG at every ship (top priority), current-state, deploy-log, sprint DONE-marks.
5. **Two CLAUDE.md-lesson candidates AC3 flagged (propose to Justin on his go):** (a) *distinguish what a subagent ACTUALLY DID from what it RECOMMENDED/INTENDED — verify, don't conflate* (AC3 relayed Nathan "captured/routed" items Nathan had only recommended); (b) *before routing an item into another agent's domain doc, check if it's already in THAT agent's memory as their owed self-note* (AC3 added a legal-checklist pointer Justin had him back out). Both are real; propose the wording to Justin.
6. **The process lesson is codified** (QC manual + orchestration protocol + SPRINT Step 88 + AC CLAUDE.md): *a test that trips the breaker / stubs the model / mocks the tool loop to save spend proves the plumbing, not the behavior — a new interactive/tool-calling pathway needs a live-path DEV exercise before GO; staging Gate-B is the backstop.* **Apply it to ③.**

---

## 🧹 Housekeeping leftovers (small)
- **Orphan dir `C:/Users/Justin/Evryn/Code/evryn-backend-clustering`** — git no longer tracks it as a worktree (deregistered + branch deleted), but the DIRECTORY is still on disk (it was file-locked during the reap; my `rm -rf` hit a permission prompt). **Safe to delete manually** (`rmdir /s /q` or delete the folder) — nothing references it. Not a git concern.
- **Megan fixtures runner-subdir:** the Megan Step-43 fixtures live in `evryn-backend/tests/fixtures/emails/megan/` (a subdir, NOT flat `megan-*.txt`) so `run-fixtures.ts` doesn't mix them with Mark's 18. When the Megan cost-run is scheduled (part of Gate-A/Gate-B), point the runner at the subdir (small DC/runner tweak).

---

## ✅ On Justin's plate
- Monday: spin fresh AC2 (③) + AC3 (54a/54c) + AC7. The Clustering + Haiku flips before a gatekeeper forwards. Staging `SEND_ENABLED` flip + validation run. Account setups. The go on each deploy/merge.

## 🧭 Key docs
`docs/current-state.md` (live deploy = `v0.2.7`) · `docs/deploy-log.md` (v0.2.7 latest) · `_evryn-meta/CHANGELOG.md` · `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` + `SPRINT-V0.3-CANDIDATES.md` · the lane briefs in `docs/working/` · `_evryn-meta/CLAUDE.md` + `docs/protocols/ac-orchestration-protocol.md` (now carries the non-runtime-target load provision) + `lock-protocol.md`.

## 🔧 Repo state (as of this #lock — all pushed)
- `evryn-backend` main = `ab23863` (v0.2.7 reconcile: ARCH markers→live, Steps 58/46/81 DONE), tag `v0.2.7` on `517014d`, synced.
- `_evryn-meta` main = the tip of this #lock (v0.2.7 current-state reconcile + dashboard merge + protocol amendment + all lane briefs + this handoff), synced — read `git -C _evryn-meta log` at boot for the exact SHA.
- `evryn-quality` synced. `evryn-team-workspace` = your team-cs append pushed (⚠ ACf's uncommitted work left untouched — do NOT sweep). `evryn-ops`/`evryn-website`/`evryn-dev-workspace` synced.
- **Dashboard Vercel-deployed** on this push (the by-model cut is live).
- Worktrees: only the 2 canonical trees (`evryn-backend` main, `_evryn-meta` main) + no stragglers (clustering + dashboard worktrees all reaped; orphan dir noted above).

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
