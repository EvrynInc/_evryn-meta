# Handoff — AC0-37c → **AC0-37d** — written 2026-08-17 evening

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file: THIS IS YOUR BRIEFING, and it is deliberately LEAN.** Today's narrative lives in the two `CHANGELOG.md` entries dated **2026-08-17 evening** — you will read those on load anyway, so this file carries **live state and open items only**. Where you want the story, the changelog has it.
>
> 🔴 **RE-DERIVE EVERY SHA AND COUNT HERE.** Both repos are hot and `_evryn-meta`'s `main` moved three times under AC0-37c in one session. **Every number below is an illustration, not a fact.**
>
> **Supersedes `2026.08.17-ac037b-ac037c-handoff.md`** operationally. That file is still worth keeping — its §5 gotchas and §9 fragment sweep are live reference — but **work from this one.**
>
> ⚠️ **AC0-37c IS WRAPPED (Justin, 2026-08-17 night). This is now AC0-37d's own working doc, and 37d is the definitive edition** — the same instance continues on the LAPTOP. **Corrections dated "2026-08-17 night" throughout are 37d's, made after executing a pre-machine-switch batten-down; where they contradict the original text, they govern.** Set-down state is at the bottom, in §10.

---

# ▶️ START HERE

**1. Who you are.** You are **AC0-37d**, the conductor on the Evryn product lane. You talk to Justin, gate merges, and drive DC/QC as subagents. **AC0-38 is a SEPARATE instance that has not been spun** — its orders are written and committed (`docs/working/2026.08.17-ac037b-ac038-marching-orders.md`). **You are not 38.** Its lanes are the `evryn-backend` defect fixes; **your job is the process/infra work that has to land underneath it.**

**2. 🔴 LOAD IN THIS ORDER. The order is not cosmetic — it is worth ~100,000 tokens.**

*Justin measured this across sessions: the SAME list, re-ordered by dependency, came in roughly 100K lighter. **The mechanism: later docs are largely DIFFS against earlier ones** — the Code Atlas is substantially a correction log against `ARCHITECTURE.md`, argued by line number. Read after it, the corrections are the best content in the file; read before it, they are noise and you build a picture you then pay to unbuild. **The full rationale and the failure mode it kills are in `2026.08.17-ac037b-ac038-marching-orders.md` §1 — read that section; it is written to generalize.***

- **Tier 0:** this file · **`_evryn-meta/CLAUDE.md` — DIFF IT, DON'T RE-READ IT.** Run `git diff <the SHA in your injected gitStatus> HEAD -- CLAUDE.md` plus `git status --porcelain -- CLAUDE.md`. Both empty + canary present ⇒ the injected copy is byte-identical and you have saved ~85K. **Re-read in full only if the diff is non-empty.**
- **Tier 1, in sequence:** `docs/hub/roadmap.md` (196) → `docs/hub/technical-vision.md` (397) → `evryn-backend/docs/ARCHITECTURE.md` (1,286) → `evryn-backend/docs/BUILD-EVRYN-MVP.md` (652) → **the full Code Atlas** (`evryn-backend/docs/atlas/`, 12 files, 3,130 — start at `00-INDEX.md`) → `evryn-backend/docs/dependency-map.md` (692; ⚠️ canary is on **693**, no trailing newline — trust the canary, not `wc -l`) → `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (~775).
- **Tier 2 — the volatile layer, read LAST so it lands on a built picture:** `docs/current-state.md` (133) → **both `CHANGELOG.md`s.**
  - 🔴 **YOU DO NOT READ A WHOLE CHANGELOG. Read roughly the TOP 100 LINES plus the section headers — that is the standing rule in each file's own header.** `evryn-backend/CHANGELOG.md` runs ~180 tokens/line and is the densest file in the load; reading it end-to-end burns six figures for history you do not need. **The 2026-08-17 evening entries are today's session and are worth reading in full; below that, headers only.**
  - **`docs/working/2026.08.17-ac037b-ac038-marching-orders.md`** (~171) — **read it here, in this tier.** 🔑 **It is not "38's problem": you are the one who hands 38 its run, its §1 carries the load-order rationale in full, and its §2 ballot overlaps yours — the handoff explicitly says do not double-ask.** *(AC0-37c loaded it and then left it off this list; that omission is fixed here.)*
- **Tier 3 — the gates. NOT part of the foundation. Load each at the beat that needs it:** `docs/protocols/ac-orchestration-protocol.md` (941 — **before spinning ANY agent**) · `docs/protocols/ac-writing-protocol.md` (100 — **before writing ANY doc**) · `docs/protocols/doc-update-routing-protocol.md`. **This is a sequencing call, not a downgrade — both bind absolutely.**
- **Also at-the-beat, NOT up front — named so you know they exist and roughly what they cost:**
  - **`_evryn-meta-lane-a/docs/working/2026.08.11-ac0-acm-loading-architecture-brief.md`** (710) — **the Lane A brief, and you WILL need it before touching that merge.** ⚠️ AC0-37c read only §11–§14 and said so; **§11.3 is the cutover order and §12 is the set-down. Read those before any merge action.**
  - **The two live ACf channel files** (see below) — short, and they are correspondence rather than context.
- **Then verify the machine yourself** (§5), cross-checking branches against `docs/repo-inventory.md` (40).

## 🔴 3. THE SEQUENCE IS TWO GATES, AND BOTH ARE JUSTIN'S. DO NOT COLLAPSE THEM.

*(Justin's explicit instruction, 2026-08-17. This is exactly how AC0-37c was run and it worked — the value is that a wrong load costs one message to fix instead of a whole session.)*

> **GATE 1 — your FIRST ACT is to tell him the INTENDED LOAD LIST. Load NOTHING first.**
> Not a summary of the handoff, not a plan, not "here's what I think we're doing." **The list, and only the list** — every file with its measured line count. ⭐ **Measure them (`wc -l`) rather than quoting this document's numbers; a handoff hands you a hypothesis, and cheap verification is the whole point.** **Then STOP and wait.**
>
> **⇒ Why this gate exists and is not ceremony:** he is the only one who can see that your list is wrong *before* you spend the tokens. AC0-37c's proposed list was missing `dependency-map.md` and mis-ordered the whole thing; **Justin caught both from the list alone.** Fixing it cost one message. Discovering it mid-load costs the load.
>
> **GATE 2 — load EVERYTHING on the approved list, IN FULL, and only THEN give him your plan.**
> 🔴 **Do not reason, plan, investigate or propose while half-loaded.** *(His words to 37c: "load and report what it loaded BEFORE burning tokens trying to figure anything out — I don't want it wasting tokens trying to understand while it's half-loaded.")* **A partial load does not produce hedged output; it produces confident output with a hole in it, and neither of you can see where.**
>
> **When the load is done, report it: every file, the line spans you actually read, and the canary you confirmed on each.** **A load you report is auditable; a load you assume is not.** **Then give him the plan.**
>
> **Open every report with a `# AC0-37d's read — …` heading** so he can find your voice in the tool output.

⚠️ **And do not trim the list to save budget.** Justin has overruled that twice. **If you feel the pull to trim, that pull is the failure, not the fix** — you cannot judge a file's relevance from inside the gap it would close.

---

# §1 — THE ONE-PARAGRAPH JOB

**Justin's framing, 2026-08-17, and it is the frame for everything below:** *"37b and now you are just sort of a waylay on the way to continuing the main show of 38… we fix the **process** stuff so that 38 can continue with the product line, with all of the internal infra having been stabilized underneath him."*

**That maps onto his own 2026-08-06 posture call — THE BUILD IS STOPPED TO REFACTOR — which has two halves, and only one is finished.** The **code-blindness** half (dependency map → claims audit → Code Atlas → extraction) has its first three deliverables DONE and merged. **The agent-loading-blindness half is Lane A, and it is still open. That is what you are here for.**

⚠️ **Do not let "stabilize the infra" quietly grow into "do the extraction first."** The `classify.ts` extraction is deliberately sequenced **after** the staging deploy and Gate-B. 38's lanes are small bounded defect fixes that do not need it.

---

# §2 — 🔴 LANE A IS THE CRITICAL PATH. Its approved wording is COMMITTED AND PUSHED; the merge is still gated on Justin.

**What Lane A is, in plain English:** `_evryn-meta/CLAUDE.md` is currently AC's ~45,000-token operating manual, and the harness force-feeds it to **every** agent we spin — including a DC, a QC, or a one-line lookup agent, none of whom should be reading AC's manual. **Lane A replaces it with a ~78-line router that says "you are agent X, go read your own manual," plus four per-agent manuals.** It is **built and sitting on branch `acm/loading-architecture`** (worktree `_evryn-meta-lane-a`).

**Why it is the critical path and not housekeeping:** 38's orders list **seven lanes**, each spinning a DC *and* a QC — about **fourteen subagents**, each auto-fed ~85K tokens of the wrong manual. **That is ~1.2M tokens of pure waste before a line of work.** And it is not only tokens: the record shows a full-runtime load **no longer reliably fits one agent** (three tried on 2026-08-10; **two compacted during the load**). ⇒ **Lane A is what makes 38's run affordable and non-compacting.**

### ✅ THE APPROVED WORDING IS NOW COMMITTED AND PUSHED ON THAT BRANCH — the reset hazard is GONE

*(Corrected at set-down. An earlier version of this handoff warned that these edits were uncommitted and that a reset would destroy them. **That warning is now obsolete: they were committed to `acm/loading-architecture` and pushed.** A hazard removed beats a hazard documented — and a handoff that asserts a state which no longer exists is the exact failure this session spent the day chasing.)*

**The two files carrying the approved wording are `docs/protocols/ac-orchestration-protocol.md` and `docs/protocols/orchestration/spinning-an-ac.md`.** They are safe on the branch and on origin. **You still must not merge without Justin’s review list — see the sequence below.**

**✅ APPLIED (Justin approved the exact words, 2026-08-17):**
- **Sentence 1**, DC/QC/OC `<identity>` → *"The CLAUDE.md that auto-loaded into your context is a ROUTER, not an operating manual — it is not yours and it is not enough."*
- **Sentence 2**, AC variant `<identity>` → *"…is a ROUTER, not your operating manual. Yours is `_evryn-meta/.claude/agents/ac.md` …"* ⚠️ **The drafted version hardcoded a stale `identity/ac.md`; the correct path was substituted and verified absent afterwards.**
- **A fourth stale reference no prior audit had listed** — `spinning-an-ac.md` substitution 2 claimed the tier headings are *"exact headings in `_evryn-meta/CLAUDE.md`."* Post-split they are not. 🔑 **It matters because the protocol tells a spinner to GREP that file for the anchor and to treat a no-match as a STALE manual it must not spin against ⇒ grepping the router returns nothing and halts a legitimate spin.** Fails loud, not silent — but it halts real work.

**⏸️ NOT APPLIED, deliberately:**
- **The manual-path list** (`[evryn-dev-workspace/CLAUDE.md | …]`) — **TRUE today**, false only once the cutover converts those repo files to redirects. **Justin pre-approved the change; no replacement text exists anywhere**, because what it becomes depends on the cutover. **It is cutover step 6. Do not invent frozen-block wording.**

### 🔴 THE OPEN DESIGN QUESTION — Justin pushed back on my draft and he was right

**I proposed two additive AC-variant substitutions** to fix the three `CLAUDE.md` references inside the shared `<mandatory_load>` block. **Justin's response:** *"where will the relevant info be, after the cutover? We don't need to reference what something used to say — if we have all of our references matching, we can just say what it's supposed to say now. Am I missing something?"*

**He is not missing something. My draft was a TRANSITIONAL patch, and here is why it does not hold:**
- The shared block says *"your CLAUDE.md"* three times. That is **correct for DC/QC/OC today** — their manuals genuinely are `CLAUDE.md` files.
- It is **already wrong for AC**, whose manual moved to `.claude/agents/ac.md`.
- 🔴 **After the cutover it is wrong for EVERYONE**, because DC/QC/OC's repo files become redirects.
- ⇒ **Two AC-variant substitutions fix one agent now and leave a problem that returns for all four.**

**⭐ The shape his instinct points at: change the shared block to say *"your operating manual"* instead of *"your CLAUDE.md."*** Agent-agnostic, **true today AND after cutover**, and nothing is lost because `<identity>` already names each agent's specific path. **One change, no variant substitutions, no rework at cutover.**

**The trade-off to put to him — this is a live decision, not a settled one:** the additive route **does not touch shared frozen text** (which was §14.3's whole argument for it), while the *"your operating manual"* route **does** — and shared frozen text sits inside the HARD RULE, so it needs his explicit yes on exact words. **My recommendation is the shared-block change, because the additive route buys safety now and pays for it twice.** **Bring him both, framed that way.**

### The remaining gates, and Justin's own sequence

- **Gate (b) — three cross-repo pointers.** `evryn-dev-workspace/CLAUDE.md` (~L13), `evryn-quality/CLAUDE.md` (~L19), `evryn-ops/CLAUDE.md` (~L13) each send their agent to *"AC's `CLAUDE.md`"* for the sync ritual. **Re-grep; line numbers drift.** ⚠️ **These CANNOT ride an `_evryn-meta` branch** — coordinated same-window change across three repos. **The cutover dissolves them entirely**, so doing them *at* cutover may be cheaper.
- **Gate (c) — the rebase. VERIFIED NEEDED:** `git -C _evryn-meta-lane-a merge-base --is-ancestor main acm/loading-architecture` returns non-zero. ⚠️ **Normalize line endings (`tr -d '\r'`) before any byte check or it false-FAILS**, and **mutate a line you have PROVEN is non-blank** or your control silently passes.
- 🔴 **A TRAP THIS WORK CREATED.** The lane's post-rebase check exists to prove the frozen blocks are **byte-identical to their pre-lane state.** **Two have now been deliberately changed with Justin's approval, so it will correctly report a difference — and the next person to run it will read that as a rebase defect.** **Give it a new expected value.**
- 🔒 **JUSTIN'S SEQUENCE, recorded in the lane brief §12.2 and not yours to reorder:** *he gets a **~200-line review list** → **then** merge → **then** cutover, with every lane stood down.* **He has reviewed NONE of the branch's ~2,962 insertions.** Wording approval is not merge approval.
- ✅ **The branch is fully pushed** (a §12.3 warning that it was local-only was true when written and is now resolved — do not re-raise it).
- ✅ **Lane A changes ZERO `evryn-backend` files**, so **no Atlas update is owed for this merge.**

---

# §3 — FOUR THINGS OWED AT THE CUTOVER, AND THEY EXIST NOWHERE ELSE

*(Justin: **"let's just make sure this doesn't get lost in the shuffle."** They share one trigger — each lands in `ac.md` and/or the orchestration protocol, **files that only exist in their new form once Lane A merges** — so doing any early means doing it twice in a file about to be replaced.)*

1. **The load-order rule** — the dependency-ordering discipline. It currently lives in exactly one place: 38's marching orders §1. **Copy it from there; do not re-derive it.**
2. 🔴 **The commit-before-merge / clean-worktree-before-reap rule — VERIFIED ABSENT from `CLAUDE.md`.** Justin asked whether it was covered; it is not. ⚠️ **Sharpen it when you write it, because the obvious framing is not the dangerous case:** *"commit the branch before merging"* is near-structurally guaranteed — git merges **commits**. **The real loss mode is uncommitted work in the LANE'S worktree, which is not in the branch, survives the merge invisibly, and dies at the reap — with the merge looking clean throughout.** *(Live: five of the nine trees reaped 2026-08-17 had uncommitted files. They were safe, but nothing in any protocol said to look.)* **Three parts:** ① check `git status` in the **lane's** worktree; ② **record the pre-merge SHA** so the merge is reversible — that is the rollback Justin asked for; ③ commit or deliberately discard before the reap, and say which.
   - ⭐ **ADD A FOURTH PART THAT IS NOT IN `CLAUDE.md` TODAY AND SHOULD BE: do destructive steps ONE AT A TIME, with the check between each.** `CLAUDE.md`'s `node_modules` rule already says count-before and count-after — **but a BATCH tells you only THAT something broke, never WHICH step broke it.** Reaping nine worktrees individually with a count between each costs seconds and would have named the culprit instantly; reaping them in one command and finding the count at zero leaves you with nine suspects and no way to choose. **Same principle, one level finer, and it generalizes to any destructive sweep.** *(Captured to `LEARNINGS.md` as the generalizable half — but `LEARNINGS.md` is for OTHER repos to learn from and drains at `#sweep`, so this is the copy that has to live in OUR flow.)*
3. 🔴 **The Atlas update must ride the lane's own merge — and the protocol a lane AC works from never mentions it.** `CLAUDE.md` carries the standing order; **a grep of `ac-orchestration-protocol.md` finds the Atlas named only as a map artifact to LOAD, never as an obligation to UPDATE.** ⇒ **A lane AC reading its own protocol never learns it owes one.** 🔑 **Why the builder and not the conductor:** a stale map is *worse than no map*, because an agent holding one reasons confidently about a system that no longer exists — **so late reconciliation manufactures the exact failure the Atlas prevents.** Write it as: *an Atlas update is part of the lane's diff, gated at merge like tests.*
4. **The three ADRs ride Lane A** — the compaction rule, the agent-definition consolidation, the router split. **Two of the three are ABOUT Lane A**, so they are its decision record, not follow-up work.

---

# §4 — OTHER OPEN ITEMS

- **SPRINT Step 117 (NEW, minted today)** — during a circuit-breaker halt, an operator's Slack message fails with a **generic error instead of "I am halted."** Verified by execution + read at source. **Not data loss; a diagnostic failure.** 🔴 **Justin's ruling: it gets a lane close to it, NOT a rider** — the recurring cause is that the halt sentinel is **type-invisible**, so a one-line guard closes this site and leaves the class. Full evidence in the Step.
- **AC0-38 has not been spun.** Its orders are written and committed. **Justin decides when.** Its seven lanes have no file overlap and can run genuinely in parallel — **but remember a conductor's context scales with the DEPTH of the agent tree, not its width. Widen before you deepen.**
- **`docs/current-state.md` is MERGED and fully in rotation** — *not* frozen awaiting the cutover *(Justin asked; verified: last commit `139dc7a`, clean, zero current-state files on the Lane A branch)*. **It is Tier 2 — AC holds the pen, each edit needs Justin's explicit go.** ⚠️ **It was NOT refreshed at this `#lock`** — see §6.
- **Three small items were CLEARED today; do not re-run them:** Step 116(a) is answered and promoted to 117 · the `.gitignore` data-dump gap is closed and committed · a fresh archival `pg_dump` was taken and verified. **Also verified so nobody re-checks it: SPRINT Step 92's DO-NOT-TRUST banner is ACCURATE against live prod.**

---

## 🔴 ARM TWO FILE WATCHERS AT SPIN-UP — both are LIVE two-way channels with ACf, and both are waiting on a reply

*(Justin's explicit instruction, 2026-08-17. **There is no live AC-to-AC delivery**, so a committed file plus a poll is the whole mechanism — and a channel nobody watches is a channel that silently dies.)*

| File | State |
|---|---|
| `docs/working/2026.08.17-ac037c-acf-cross-runtime-halt-signal-note.md` | **I asked, ACf has not answered.** The `#lock` step-8 safety propagation for Step 117's class. **You are holding it.** |
| ✅ **`docs/working/2026.08.12-acf13-ac0-agent-patterns-postmerge-brief.md`** | **READ IN FULL by AC0-37d, 2026-08-17 night (287 lines + canary). It is no longer unread inbound.** **What it holds, so the next spin need not re-read it to know whether it must:** §2 four `AGENT_PATTERNS`→`LEARNINGS` routing fixes for `CLAUDE.md` + one for the orchestration protocol · §5 held paste-ready blocks for `CLAUDE.md` (research-routing; the route-learnings-by-SCOPE rule) · §6 two `current-state` defects **both since fixed** · §7 a drafted-not-made `runtime-ops.md` edit (Justin's file, SPRINT Step 77) · §8 **the canary-arming and watcher mechanisms `CLAUDE.md` describes but never gives you** · §9 the sharpened unpushed-branch rule **and ACf's own retraction of the "eleven commits" false alarm.** 🔴 **ALL OF IT IS HELD FOR THE LANE A MERGE — apply none of it before.** *(Original 37c note, kept for the record:)* **UNREAD INBOUND — ACf wrote to it 2026-08-17 and AC0-37c NEVER READ IT.** *(Stated plainly rather than softened: I ran out of context, Justin told me not to read it, and I am not going to pretend otherwise. **Treat its contents as entirely unknown to me** — nothing in this handoff reflects it.)* **⇒ READ IT EARLY — it is not an at-the-beat file.** 🔑 **Why it is on your critical path rather than beside it: this is the brief STAGING ACf's held edits, and they land AT THE LANE A MERGE** — roughly six pointer fixes he is deliberately holding rather than making, precisely to stay out of the AC0 lineage's way. **It also carries the open `AGENT_PATTERNS.md` question** (it collapsed into `LEARNINGS.md`, but ~7 source-of-truth docs still route new learnings there — **an open decision for Justin, not for you to settle**). ⚠️ **Placing his held edits will take negotiation, because they live in the files Lane A restructures. That is the whole reason they are held.** **Reply in the file; he watches it.** |
| ~~`docs/working/2026.08.17-acf13-ac037c-control-char-scanner.md`~~ | ✅ **CLOSED — do not watch it.** ACf conceded, took the shared-spec proposal, and is retiring the file to `docs/sessions/historical/2026.08/`. **Nothing is owed either way.** The decision is persisted in **two independent places** — his SPRINT Step 62, and a header block in `_evryn-meta/scripts/scan-control-chars.js` itself, which is where anyone re-proposing consolidation will be standing. |

```bash
# arm at spin-up; TaskStop it when you set down
cd /c/Users/Justin/Evryn/Code/_evryn-meta
while true; do sleep 300; git log --oneline -1 --since=10.minutes -- \
  docs/working/2026.08.17-ac037c-acf-cross-runtime-halt-signal-note.md \
  docs/working/2026.08.12-acf13-ac0-agent-patterns-postmerge-brief.md; done
```

**What my reply to him says, so you can hold the thread without re-reading it:** he proposed that his runtime's NUL test should CALL our scanner rather than re-implement it. **I ruled: keep two, deliberately** — his own caveat is decisive, because the coupling would have to *"degrade to a skip"* when the sibling repo is absent, **and a skipping guard reports the same green as a passing one**, which is the exact vacuity failure his test already had once. **What I agreed should be shared is the SPEC, not the code** — a four-line comment in both so a divergence is visible by reading. **If he pushes back, take it to Justin as a real decision.**

---

# §5 — MACHINE STATE (re-derive; do not trust this picture)

```bash
cd /c/Users/Justin/Evryn/Code
for d in */; do d=${d%/}; [ -e "$d/.git" ] || continue; echo "$d -> $(git -C "$d" branch --show-current)"; done
git -C _evryn-meta status -sb; git -C evryn-backend status -sb
git -C _evryn-meta worktree list
node _evryn-meta/scripts/scan-control-chars.js _evryn-meta evryn-backend   # exit 1 EXPECTED
```

- **Both repos were pushed and at zero unpushed** at handoff. **Pushing is always Justin's.**
- **`evryn-backend` is down to ONE clean tree** — the nine mutation-sweep worktrees were reaped 2026-08-17 after verifying their content was on `main`. **`node_modules` held at 161; `npm test` then returned 65 suites / 1,925 assertions / 0 failed.** ⭐ **That is a known-good baseline banked immediately before the Lane A merge — re-run it after, because Lane A should not move a single assertion.**
- **`_evryn-meta` worktrees:** canonical + **`_evryn-meta-lane-a` (YOURS — see §2)**. *(Corrected 2026-08-17 night by AC0-37d: this line said "+ three `acf*` trees." **There are now ZERO ACf branches or worktrees in ANY repo** — verified across all twelve trees. ACf's own handoff records the same cleanup, including one he found late in `evryn-team-workspace`. **`_evryn-meta-lane-a` is the only non-canonical worktree in the estate.**)*
- 🔴 **ANYTHING NAMED `acf*` IS NOT YOURS.** Never reap, tidy, or merge it; do not carry it in your list. **It becomes your business only when it actively COLLIDES — and even then you SURFACE the collision, you do not resolve it.** **ACf works around AC0, not the reverse.** ⚠️ **The one carve-out: if your merge would DELETE something of his already on `main`, that is you destroying his work — preserve it verbatim, do not rewrite his prose.** **ACf was actively committing in `_evryn-meta` during this session.**
- **Known-deliberate untracked in `evryn-backend` — do NOT sweep, delete or commit:** `backups/full-public-*.sql` *(gitignored since 2026-08-17)*. 🔴 **The four `tests/_gate-a-*.ts` are NO LONGER on this list — they were COMMITTED 2026-08-17 night (`22226f8`, banners `d2f9e56`), on Justin's instruction, because a machine switch made them the estate's ONLY single-copy work.** *(Investigated first: no credentials, no third-party PII, `assertNotProd()` on every entry point, and committing changes nothing about what runs — `scripts/run-tests.ts:215` skips `_`-prefixed files and `tsconfig.tests.json` excludes them by path, not by tracked-status. Each now carries a banner that its grading logic predates ③ and must be re-derived before any verdict is trusted. Full reasoning: `evryn-backend/CHANGELOG.md`, 2026-08-17 night.)*
- **`evryn-backend` now has ONE local branch (`main`).** `acb/sprint-restructure` and `dc/step106-nul-bytes` were deleted **as local refs only**, after proving `origin/main..<branch>` = 0 for both. 🔴 **Their REMOTE branches were deliberately KEPT — do not delete them.** Both were rebased before merging, so the remote tips hold the pre-rebase originals, and **five of those SHAs are cited in committed docs** (`4d295c5` in `docs/atlas/07-safety.md`; `f4bcf38` is `docs/dependency-map.md`'s own generation stamp). Deleting them leaves those commits unreferenced and eligible for GC, dangling live citations, for no benefit.
- **The control-char scan exits 1 by design** — one deliberate vertical-tab in a frozen v0.1 prompt import. **That is the expected clean state, not a finding.**

---

# §6 — WHAT THIS `#lock` DID AND DID NOT DO

**Done:** both `CHANGELOG.md`s (2026-08-17 evening) · `LEARNINGS.md` un-numbered captures · SPRINT Step 117 minted + 116(a) closed · this handoff · worktree hygiene · the archival `pg_dump` · auto-memory and `settings.local.json` verified clean · **subagent close-out: ZERO subagents were spawned this session — nothing is owed, nothing is stranded.**

**NOT done — carry these:**
- ⚠️ **`docs/current-state.md` — PARTIALLY refreshed by AC0-37d, 2026-08-17 night, on Justin's explicit go.** Only the one line his own instruction falsified was corrected (the gate-a untracked instruction, line ~66). 🔴 **The rest of 37c's list is still owed and is still accurate:** it does not know the nine worktrees were reaped, that Lane A's wording gate is part-applied and now committed, or that Step 117 exists. **Propose the full refresh early.** *(Original note kept, because the reason it went unrefreshed still governs: it is Tier 2 and needs Justin's explicit go per edit.)*
- ✅ **CROSS-RUNTIME SAFETY PROPAGATION — ASKED, awaiting ACf. Do NOT re-ask; DO chase the answer.** Step 117's class is a safety-boundary class, so `#lock` step 8 applies. **The question is written and committed at `docs/working/2026.08.17-ac037c-acf-cross-runtime-halt-signal-note.md`** — routed to **ACf** (he owns `evryn-team-runtime`, which has its own kill switch and dormancy mechanism), **via Justin, because there is no live AC-to-AC channel.**
  - **The class, so you can carry it without re-reading the note:** *a halt / kill / dormancy signal returned as an ORDINARY IN-BAND VALUE — a bare string, a sentinel, a null — rather than as a distinct type or a throw, so every call site must REMEMBER to check it and a pathway that forgets treats "we are halted" as a real answer.*
  - 🔑 **The note asks for a FALSIFIABLE answer, which is the part that matters:** a clean "no" must establish that the state is **structurally unignorable** (a type or a throw) or that there is **exactly one consumer** — because *"all the current call sites check"* is precisely the state `evryn-backend` was in until one didn't.
  - ⚠️ **A pending answer does not block anything, but an unanswered check that nobody is holding is the same as a skipped one. You are now holding it.**
- **The founding-team `current-state` append was not written.** Today was process/infra work mostly below that audience's altitude; **one or two lines at most, or skip with a logged "no team-relevant change."**
- **Stale-doc retirement:** `2026.08.17-ac037b-ac037c-handoff.md` is now superseded by this file and is a **retirement candidate** — **but never move a session/working doc without Justin's explicit OK.**

---

# §7 — JUSTIN'S RULINGS TODAY, PERSISTED (these existed only in chat)

1. **Ballot numbering:** put carried-over items **FIRST** so numbers stay stable across messages. *(Two ballots with different 1-2-3s caused real confusion.)*
2. **Lane A wording:** sentences 1 and 2 approved as drafted (path corrected); the manual-path list pre-approved **conditional on cutover**; the extra explanatory clause on the anchor fix — **keep it**.
3. **Step 117 gets its own lane**, not a sidecar.
4. **The three small items** are authorized for down-moments while he is away — **but set them down the moment critical path appears, and resume in the next down moment.**
5. **Worktree reaping is authorized** with his standing condition: *"just make sure we've merged correctly, and it's all landed and stable."*
6. **`AGENT_PATTERNS.md` is collapsed into `LEARNINGS.md`** — never route there.

---

# §8 — GOTCHAS FROM THIS SESSION

- 🔴🔴 **I INTRODUCED A LIVE NUL BYTE INTO THE SPRINT DOC WHILE WRITING THE STEP ABOUT NUL BYTES — a fourth instance of the class, caught and repaired 2026-08-17. READ THIS ONE; it will happen to you.**
  - **What happened:** writing Step 117, I typed a `\u0000` escape in prose. **The tool argument travels as JSON, JSON interprets `\uXXXX` natively, and it became a real NUL byte.** One `\u0000` in the same edit survived as literal text and one did not — **so you cannot rely on "it worked last time."**
  - 🔑 **HOW IT ANNOUNCED ITSELF, which is the useful part: my next `Edit` on that file FAILED to match.** The NUL had made the file **binary**, so the tool could not find text that was plainly there. ⇒ **An inexplicable `Edit` match-failure on a file you just wrote is a NUL-byte symptom. Do not retry the edit — scan the file.**
  - ⚠️ **And the verification trap inside the verification: `JSON.stringify` renders a REAL NUL and the literal text `\u0000` IDENTICALLY.** My own inspection could not tell them apart. **Only a byte-level check can** — `Buffer.indexOf(0)`, or the scanner.
  - ✅ **The fix is two commands and they are already built.** `node _evryn-meta/scripts/scan-control-chars.js <repo>` to find, `node _evryn-meta/scripts/repair-control-chars.js <file>` to fix. **They are a PAIR.** The repair replaces the byte with its literal six-character text and self-verifies zero remaining; **then re-run the SCANNER independently, because a tool that reports its own success is not a check.**
  - **This is live evidence for SPRINT Step 113** (the repo-wide scan wired into the suite) — **the class has now recurred inside its own warning four times, and the fourth was an agent who had read the full write-up that same day.** ⇒ *"Remember to double the backslash"* is not a mechanism.
- 🔴 **A grep FALSE POSITIVE caused by CORRECT documentation.** Checking whether a doc covered three tables, a substring grep said yes — **because each appeared exactly once, inside the banner saying they were missing.** 🔑 **What caught it: the same check also returned three results that were obviously wrong in kind. The ABSURD output discredited the method; the plausible output would have stood.** ⇒ **Grep for a section heading, not a bare name — and when a check returns exactly what you hoped, re-run it a different way.**
- ⚠️ **`[ -L ]` does NOT reliably detect a Windows junction.** Do not build worktree safety on detecting one. **Count the canonical `node_modules` before and after each reap, one at a time, and repair on a delta.**
- ⚠️ **A `Read` from before a task/resume boundary does NOT satisfy the `Edit` tool afterwards** — re-read the region you are about to edit.
- ⚠️ **Use a heredoc for any commit message containing backticks** — in `git commit -m "…"` a backticked span is **executed** and silently replaced by its output.
- ⚠️ **`wc -l` under-counts a file whose last line has no trailing newline** (`dependency-map.md` reports 692; its canary is on 693). **Trust the canary.**
- ⚠️ **Appending to a file whose last line is a canary means inserting BEFORE it.**

---

# §10 — AC0-37d SET-DOWN (2026-08-17 night, on the DESKTOP; next spin is the SAME instance on the LAPTOP)

**Justin switched machine direction mid-session: we are on the DESKTOP now, moving to the LAPTOP.** 37c was retired; 37d is definitive.

**The estate is battened down. Verified across all twelve trees, after the work below:** `git rev-list --all --not --remotes` = **0** everywhere · 0 untracked · 0 modified · **0 stashes** · no stale worktree registrations. **Nothing is trapped on this machine.**

**What 37d shipped tonight** *(narrative in `evryn-backend/CHANGELOG.md`, 2026-08-17 night — do not re-summarise it here)*: the Gate-A harness committed + bannered; `tsconfig.tests.json`'s exclusion comment corrected because that commit falsified it; two merged local branch refs deleted; a stray `nul` file removed.

🔴 **THE ONE METHOD WORTH CARRYING OUT OF THIS SESSION — two git questions that look identical and are not:**
- **`origin/<branch>..<branch>`** answers *"should I push this branch?"* A **merged** branch answers *"yes, N"* **forever**, because it is ahead of its own tracking ref, which nobody updates after a merge.
- **`--all --not --remotes`** answers *"would losing this machine lose this work?"* **That is the batten-down question, and it is the only one that matters before a machine switch.**
- ⚠️ **`git branch -d` measures the FIRST one and says so out loud** — *"not merged to `refs/remotes/origin/…`, even though it is merged to HEAD"* — so its built-in guard refuses to delete a fully-merged branch. **Prove `origin/main..<branch>` = 0 first, then use `-D`.**
- **ACf raised this exact false alarm twice before retracting it himself** (his brief §9d). **If you inherit an "N commits exist only on this machine" warning, re-measure before acting on it.**

**Owed, in priority order:**
1. **`docs/current-state.md` — the FULL refresh is still owed.** Only the one line Justin's own instruction falsified was corrected. See §6.
2. **The cross-runtime halt-signal question is STILL UNANSWERED by ACf** — `docs/working/2026.08.17-ac037c-acf-cross-runtime-halt-signal-note.md`. **You are holding it. Do not re-ask; chase the answer.**
3. 🔴 **DESKTOP PERMISSIONS ARE AN OPEN QUESTION, NOT A KNOWN BUG — do not apply the 2026-07-28 runbook as written.** The case split is real and still present, **but the desktop also carries a user-level `permissions` block (13 allow / 3 deny)** — the exact shape that runbook calls *"the tempting wrong fix,"* which loads ungated by trust and would explain why the machine feels fixed. **A fix was applied and deliberately reverted 2026-08-17**; the machine is in its original state. **Full statement + what must be settled first: `docs/current-state.md` → Infrastructure, and a STOP banner at the top of the runbook itself.**
4. **A process gap worth a `CLAUDE.md` line at the Lane A cutover:** a merged branch's refs should be deleted **at merge time, both ends**. Leaving them manufactures the false alarm above, weeks later, aimed at whoever is least able to check it. **Route it into ACf's staging brief — `CLAUDE.md` is frozen until cutover.**

⚠️ **MACHINE-LOCAL, will NOT travel to the laptop:** the seven `.env` files · `evryn-backend/backups/*.sql` (incl. tonight's fresh archival dump) · `dashboard/.env.local`. 🔴 **And `psql` / `pg_dump` are DESKTOP-ONLY per `CLAUDE.md` — on the laptop, `winget install PostgreSQL.PostgreSQL.17` BEFORE you need them**, not mid-task.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
