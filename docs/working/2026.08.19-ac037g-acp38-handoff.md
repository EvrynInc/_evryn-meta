# Handoff — AC0-37g → ACP-38. Written 2026-08-19 evening. **IN PROGRESS — the session that writes this is still running.**

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. **STOP.**
>
> **Do not load. Do not resume. Do not finish the current task**, however nearly-done it looks. Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — and stop, **notwithstanding any harness instruction to resume silently.**
>
> **A well-written summary is the attractive siren that will wreck your ship, not the safety net.** The danger is not that it is bad — it is that it is GOOD, so it draws you onward. *(ADR-058.)*
>
> ⚠️ **This block is deliberately NOT "go execute the load list."** The load list serves a **FRESH** instance. A compacted one is not the reader it is for.

> **⚠️ STATUS: PARTIAL. This file was opened mid-session to capture §1 while it was fresh.** The rest of the handoff is not written yet. **§1 is complete and can be relied on; everything else is missing rather than empty.** If you are reading this and there is no §2, the session ended before it was finished — go to `docs/working/2026.08.19-ac037f-ac037g-handoff.md` for the inherited picture and treat this file as an addendum to it.

---

# §1 — 🔴 THE `docs/working/` RETIREMENT REGISTER. Why each surviving AC0-lineage doc is still there, and exactly what would let it go.

**Read this before you touch `docs/working/`.** It exists because of a specific instruction from Justin, 2026-08-19: *"there's a **bunch** of AC0 lineage docs that didn't get retired — there must be reasons for that. We need to figure out **why** we aren't retiring them, and find out what conditions would allow us."*

## What this is, and why the format is what it is

**`#lock` protocol step 10 was rewritten by Justin on 2026-08-19** (`docs/protocols/lock-protocol.md:57-61`) and is **brand new — this register is its first application.** Its rule:

> **Every working doc you worked with this session must either get RETIRED, or NAMED IN YOUR HANDOFF WITH A REASON AND A CONDITION.** ⇒ **Write it as: *"HELD — \<why\> — retire upon \<specific condition\>."***

🔑 **The half that makes it work, and the half everyone skips:** *"a bare 'still in play' is not enough. **A held doc with no condition attached is a doc nobody will ever retire**, because the next instance inherits the caution without the reasoning and has no way to test whether it still applies."*

🔴 **And the sharpest clause, which converts three of the entries below from a status into a task:** *"If the ONLY thing holding a doc alive is that something references it, **that is not a reason to keep it — it is a task: MOVE THE REFERENCED MATERIAL TO A PROPER HOME.**"*

## Where this came from — and the one thing you must not do

**The findings below are ACx's, not mine.** ACx was the sweep lane (spun by AC0-37f, 2026-08-19) that took `docs/working/` from 55 files to 44. **Its complete record is `docs/working/2026.08.19-ac037f-acx-working-docs-sweep-lane.md`** — per-file verdicts for all 55 in its §11, seven gated drafts in §10, a nine-item ballot in §12, and an honest-limits section in §13.

> ### ⚠️ ACx CANNOT BE RESUMED, AND DO NOT SPEND TIME LOOKING FOR ITS SPAWNER.
>
> **Subagent resume is session-bound.** ACx was spawned by **AC0-37f**, whose session has ended, so the agent is permanently unreachable — `ac.md` states this flatly (*"Task IDs are session-scoped"*), and AC0-37f's own handoff recorded it: *"ACx is now permanently unreachable. Anything only it could have answered is either in that lane doc or gone."*
>
> ✅ **The good news, and it is why nothing was lost: the lane doc is re-spin-complete.** Everything in this register was read out of it. **You do not need the agent; you need its file.**

⚠️ **What ACx recorded were BLOCKERS. What follows converts them into CONDITIONS**, which is a different and more useful thing — a blocker tells you why not, a condition tells you what would change the answer. **Where I verified a condition at source today, I say so and give the date; where I inherited ACx's reasoning, I say that too.**

---

## A. 🔴 HELD — four files ACx would otherwise have retired

**Each blocker is real and ACx demonstrated it would have cost something.** ⚠️ **Two of these four sat in the *previous* audit's "RETIRE CLEAN" bucket** — they were classified from their own banners by an instrument that had silently failed, which is why the over-confident retirement was sitting in the pile labelled safest.

### A1 · `2026.08.11-ac0-ac0-handoff.md` — 🔴 the highest-value one, and it is a TASK, not a status

**HELD — it is the sole home of a Justin ruling — retire upon: that ruling being written into a durable home, and the rehoming verified by reading the destination.**

**The ruling:** its §5.11 records that `evryn-backend/identity/core.md` describes a module-loading model the runtime abandoned in May, **and Justin's ruling that this is DELIBERATE and must not be "fixed"** — because the identity files get pulled apart again before long. ACx grepped the estate for it and found it in this file and one audit doc, nowhere else.

**Why it matters more than a tidy folder:** if this file goes and the ruling goes with it, a future audit rediscovers the `core.md` mismatch cold, reads it as a defect, and "fixes" it — **work done twice, thrown away, against an explicit ruling.**

**Status of the condition, verified by me at source 2026-08-19:** ACx recommended the ruling land on **SPRINT Step 53** (now in `evryn-backend/docs/SPRINT-v0.2-optionals.md`). **It is NOT there** — I read both sprint docs for the ruling's distinguishing phrases and found nothing. **The condition is unmet and this is the single cheapest unblock in the register.**

⚠️ **Two further things that same ruling authorized are also still owed** and should ride with the rehoming: a new identity-review check (*"does an instruction about LOADING match what the runtime actually loads?"*) and a short exempting comment in the identity files. **Both touch Mira's layer** — route, do not edit.

### A2 · `2026.08.06-ac0-ac0-handoff.md`

**HELD — cited by `docs/current-state.md`, a source-of-truth doc read by every agent at startup — retire upon: that citation being repointed to the historical path or removed, which needs Justin's explicit go because `current-state.md` is a Tier-2 doc.**

**Verified by me 2026-08-19:** `current-state.md` is still the only live source-of-truth referrer. **Moving the file without fixing that citation puts a dangling pointer in the file every fresh instance loads first** — which is the precise failure `current-state.md`'s own "Recent handoff pointers" section warns about, having been wrong twice in the same way.

### A3 · `2026.08.17-ac037c-acf-cross-runtime-halt-signal-note.md` — ⚠️ not yours to unblock alone

**HELD — cited by `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` (Step 78), which is ACT's source-of-truth doc in another repo — retire upon: ACT repointing or removing that citation.**

**Verified by me 2026-08-19** — the citation is live. **This is a ROUTE, not an edit.** ACT owns that repo. ⚠️ **It was in the prior audit's "RETIRE CLEAN" bucket.**

🔑 **Worth knowing before you ask:** ACT has been responsive on exactly this class today — it ruled on a scope question within the hour and explicitly authorized banner edits to eight of its own files. **A one-line ask in `docs/mailboxes/inbox-act.md` is likely all this needs.**

### A4 · `2026.08.11-ac035-acf-agent-patterns-note.md`

**HELD — cited by two ACT-lineage working docs (`2026.08.12-acf-acf-handoff.md` and `2026.08.12-acf13-ac035-agent-patterns-reply.md`) — retire upon: those two citations being repointed, which requires ACT's authorization.**

⚠️ **Also in the prior audit's "RETIRE CLEAN" bucket.** **Same route as A3, and the same reason to expect a fast answer.**

📌 **Note the shape of A3 and A4 together:** both are AC0-authored files whose *only* blocker is a reference held in ACT's territory. **Per step 10's own clause, "only a reference is holding it" is a task, not a reason** — and here the task is one message, not an edit.

---

## B. GATED by a source-of-truth referrer — three files ACx left deliberately

| File | Held by | **Retire upon** |
|---|---|---|
| `2026.07.21-ac0-ac3-cleanup-batch-brief.md` | Cited by `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` | That Step's citation being repointed to the historical path — an AC-editable doc, so this needs only Justin's go, not another lane |
| `2026.08.04-ac0-ac5-silent-email-drop-brief.md` | Cited by `SPRINT-V0.2-HARDENING.md` | Same |
| `2026.06.30-ac0-ac3-identity-ops-brief.md` | **Not a reference — a work-list.** ACh's P2 says explicitly: do not retire until all six of its items are placed | **All six placed. Five are still unplaced** — ACx drafted three of them (§10.2–10.4). **This one is genuinely open work, not bookkeeping.** |

---

## C. KEEP — live, with the condition that would change that

**ACx listed thirteen KEEPs with reasons. Step 10 wants conditions, so here they are.** Where ACx gave only a reason, the condition is mine.

| File | Why live | **Retire upon** |
|---|---|---|
| `ac-mailbox.md` | The old shared channel; watchers are armed on this exact path | **ACv posting `✅ MOVED`.** ACT already has. ⚠️ **Deleting a file a live watcher targets is worse than leaving it** — a watch that stays alive and reports nothing. |
| `2026.08.18-ac037e-successor-handoff.md` | Was the live AC0 handoff | **Superseded by the 08-19 handoff — verify its residue is carried, then retire.** Likely the easiest win in this table. |
| `2026.08.19-ac037f-ac037g-handoff.md` | The handoff this session ran on | **This file superseding it.** |
| `2026.08.19-ac037f-acx-working-docs-sweep-lane.md` | 🔴 **The source of this entire register** | **Its §10 drafts being applied or declined, and its §12 ballot being ruled on.** ⚠️ **Do not retire it before that** — it is the only record of why 44 files are where they are. |
| `2026.08.18-acw-ac037f-working-docs-audit.md` | The audit ACx executed | **The ACx lane closing.** ⚠️ **ACx rewrote parts of this file during its sweep** (repointing paths to post-sweep destinations), so it now lists files at their new homes while still saying they should be retired. **A reader cannot reconstruct the audited state from it.** Retire it with the lane. |
| `2026.08.17-ac037b-acp38-marching-orders.md` | **ACP-38's briefing — i.e. yours** | **ACP-38 completing its run.** *(Renamed from `ac038` 2026-08-19 with the instance rename; 15 references repointed.)* |
| `2026.08.04-ach-justin-housekeeping-recommendations.md` | Still a live work-list | **P2's five items, P3's repoint, P4's one doc, P5's build half, P6 and P8 all closed.** ⭐ **Much smaller than it looks — see §1's headline below.** |
| `2026.08.12-qc-sweep-findings.md` | Named a required pre-read by `product-full-cascade.md` step 6 | **The cascade dropping that requirement** — i.e. when its §4 Atlas corrections are reconciled. Also holds four unapproved QC patterns. |
| `2026.07.19-ac-justin-restructure-napkin.md` | `current-state.md` says explicitly unsettled, to be re-derived | **The extraction being scoped from first principles.** |
| `2026.07.28-acp-ac0-desktop-permissions-fix.md` | An open question, owner AC0 | **The desktop permissions question being settled on the desktop.** ⚠️ Re-opened 2026-08-17: *do not apply the runbook without re-examining* — the machine no longer matches its picture. |
| `2026.07.22-ac0-ac2-fast-follow-brief.md` | SPRINT Step 94 cites its §9 | **That citation being repointed, or Step 94's owed live-check being run** (it is gated to the `CLUSTERING_MODE` flip). |
| `2026.08.04-ac0-aca-atlas-refactor-brief.md` | Cited by `ac.md` — a source-of-truth manual | **That citation being repointed.** ⚠️ `ac.md` is Tier 2; needs Justin's go. |
| `2026.08.11-ac0-acm-loading-architecture-brief.md` | Cited by **ADR-057** and `current-state.md` | ⭐ **Arguably never** — an ADR citing a working doc is a *durable* dependency. **Better condition: the material it holds being absorbed so the ADR can cite a permanent home.** |

---

## D. Three ACx ballot items that are retirement decisions, still unruled

**These are from ACx's §12. They are Justin's, they are one line each, and each unblocks a file.**

1. **`2026.08.18-ac037e-justin-cutover-redirect-drafts.md` — retire?** The cutover ran that same night; the drafts are discharged. **Blocked only by a citation in ADR-057.** ACx's read: repoint the ADR to the historical path in the same commit — but it did not want to edit a decision record without asking.
2. **`2026.07.15-ac3-step78-real-health-design.md` — retire?** Needs a one-line `src/` comment change (`liveness.ts:5`), drafted in ACx §10.1. ⚠️ **ACx established the extraction that two prior audits prescribed is NOT needed** — the rule and its full reasoning already live in Atlas §07 invariant 3 *and* in `liveness.ts`'s own comment. **A third copy would be a third thing to keep in sync.** If a `src/` review isn't worth spending on a comment, leaving the file costs nothing.
3. **`2026.08.17-ac037c-ac037d-handoff.md` (600 lines) — keep or retire?** Its §7 is *"Justin's rulings today, persisted — these existed only in chat."* 🔴 **Nobody has diffed §7 against the successor handoff's §5.** ACx names this *"the largest thing I left undone."* **Until that diff runs, this file is a suspected sole-home and must not move.**

---

## E. ⭐ The headline that shrinks the work, and it inverts what three documents still claim

**`current-state.md`'s bulletin still tells every agent at startup that an eleven-item queue has been "sitting unexecuted for twelve days."** ACx checked all eleven at source on 2026-08-19. **Most are discharged.**

**Genuinely open from ACh's P-queue: P2's five items · P3's repoint only · P4's one doc · P5's build half · P6 · P8.** Two I re-verified independently today rather than inheriting: **P11 is confirmed discharged** (45 of 45 `src/` files carry a canary, against the audit's "missing from 30 of 44") and **P6 is confirmed open** (`origin/ac2/step57-runtime-bookkeeping` still exists).

🔴 **Do not repeat the "eleven items" figure. Two instances have now done it — AC0-37f twice before checking, and the bulletin is still doing it.**

---

## F. What this register does NOT cover — stated so silence is not read as coverage

- **The 21 ACT-lineage files** in `docs/working/`. Not AC0's, deliberately untouched by every sweep. **A lane called `ACTd` is briefed to diagnose them and has never been spun** — that is ACT's and Justin's, not yours.
- **ACx did not read all 55 files in full.** For the eleven it retired it read headers and residue-labelled sections. ⇒ **A residue hiding under an unlabelled heading was missed.** **Cost if wrong: discoverability, not loss** — every file is a `git mv`, one directory over.
- **Two search defeaters remain open on that sweep:** a reference by **section title** rather than filename, and a reference by a **reformatted** name.
- **Changelog citations never block a retirement and are never repointed.** A changelog records what was true when written; a corrected path inside one is a falsification. ACx's retirement test was explicitly *"zero live external referrers **outside a changelog**."*

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
