# Handoff — AC0-37b → **AC0-37c** — written 2026-08-17

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file: THIS IS YOUR BRIEFING.** This is a **planned set-down at a clean checkpoint**, not an emergency — nothing is half-finished, no branch is mid-merge, and both working trees are clean of tracked modifications.
>
> 🔴 **RE-DERIVE EVERY SHA IN THIS FILE.** Both repos are hot and `_evryn-meta` moved twice under AC0-37b in one session. **Every number here is an illustration, not a fact.**

---

> # ▶️ START HERE. In this order.
>
> **1. Know who you are and who 38 is.** You are **AC0-37c**, the *conductor* — you hold the product lane, talk to Justin, and gate merges. **AC0-38 is a separate instance that has not been spun yet.** Its job is to run several long build lanes semi-unattended, and **its orders are already written and committed** (`docs/working/2026.08.17-ac037b-ac038-marching-orders.md`). ⇒ **You are not 38, and you should not start 38's lanes yourself.** Your job is to close the items in §3–§4 and then hand 38 its run when Justin says go.
>
> **2. Load, in this order.** ⚠️ Read `_evryn-meta/CLAUDE.md` **from disk** — the auto-injected copy is a session-start snapshot and has served stale content.
> - **Tier 0:** this file · `_evryn-meta/CLAUDE.md` (**611** lines by `wc -l`) — ⚠️ **the Read tool will display 612.** That is a trailing-newline display artifact, **not** drift, and it has confused agents before. **Settled rule: `wc -l` for navigation, the CANARY for verification, and the canary wins on disagreement** — *"a line number tells you where to stop; only the canary tells you that you arrived."*
> - **Tier 1:** `docs/protocols/ac-orchestration-protocol.md` (🔴 the gate on spinning anything) · `docs/protocols/ac-writing-protocol.md` (🔴 the gate on writing to any doc) · `docs/current-state.md` (133 — it was restructured today) · `docs/hub/roadmap.md` · **both `CHANGELOG.md`s** (each carries a 2026-08-17 entry that is the narrative of this session) · `docs/working/2026.08.17-ac037b-ac038-marching-orders.md`
> - **Tier 1, product side, ONLY IF you will direct build work** — which carries the same load obligation as doing it: `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` (756) · `ARCHITECTURE.md` (1,285) · `BUILD-EVRYN-MVP.md` (651) · `docs/hub/technical-vision.md` (397) · the full Code Atlas (12 files, ~3,100 — start at `00-INDEX.md`). **Justin overruled a prior AC twice for trimming this; do not trim it a third time.**
> - **Do NOT load** `evryn-backend/src/` (use the Atlas + `docs/dependency-map.md` for a targeted read) or the QC sweep-findings doc unless a specific beat needs it.
>
> **3. Verify the machine state yourself** — the commands are in §8. **Do not trust this file's picture of it.**
>
> **4. 🔴 DO THE LOAD, THEN STOP AND REPORT IT — BEFORE you try to work anything out.** *(Justin's explicit instruction, 2026-08-17: **"load and report what it loaded BEFORE burning tokens trying to figure anything out… I don't want it wasting tokens trying to understand while it's half-loaded."**)*
> - **Report every file and the line spans you actually read**, plus the canary you confirmed on each. **A load you report is auditable; a load you assume is not.**
> - **Then STOP.** Justin verifies the load and only then tells you to proceed with your plan. **Do not start reasoning, planning lanes, or investigating while half-loaded** — that is how an instance burns its budget producing confident work with a hole in it.
> - ⚠️ **Do NOT trim the list in step 2.** The 2026-08-13 handoff shipped a shorter list; **Justin added to it and AC0-37b agreed the additions were necessary** — the sprint Steps you may edit cite `ARCHITECTURE.md` directly, and editing one without holding the other is how a false runtime claim gets written into the tracker four agents read. **If you find yourself tempted to trim for budget, that temptation is the failure, not the fix.**
> - **Open your report with a `# AC0-37c's read — …` heading** so Justin can find your voice in the tool output. He is arriving cold from other lanes; re-anchor him in a sentence or two before any detail.
>
> **5. The things he owes an answer on are in §3.** Batched deliberately. **Nothing in §4 is blocked on them** — so if he is away, work §4 and ping.

---

# §0 — 🔴 ANYTHING NAMED `acf*` IS NOT YOURS AND NOT YOUR PROBLEM

*(Justin's standing rule, stated explicitly 2026-08-17. It governs everything below, so read it before the worktree list in §8 or you will misread what you see there.)*

**Any branch or worktree whose name begins with `acf` belongs to ACf.** `acfl/learnings-consolidation` · `acfsq/research-move` · `_evryn-meta-lock` · anything else he stands up. **You never reap it, never tidy it, never merge it, and you do not carry it in your own list.** **ACf handles all of his own clean-up.**

**It becomes your business in exactly one case: when it is actively COLLIDING with something you are doing** — the same file, the same merge seam. **And even then the move is to SURFACE the collision, not to resolve it yourself.**

🔑 **The part that should change how you feel about it, because it inverts the natural instinct:** **ACf works around AC0 — not the other way around.** The reason he stages held edits in his own brief rather than making them is **precisely to stay out of the AC0 lineage's way**, so that you never have to sequence, absorb, or reason about his work. **He will hand it to you when it is ready.** ⇒ **Treating his material as something you must manage defeats the arrangement and costs you context you need elsewhere.**

⚠️ **The one thing this rule does NOT excuse:** if your own merge would **delete** something of his that has already landed on `main`, that is not "his lane" — **that is you destroying his work, and preserving it is yours.** AC0-37b hit this exactly once today (§2 item 5) and preserved his correction verbatim in the BULLETIN **without rewriting his prose.** That is the shape: **preserve, do not edit.**

---

# §1 — THE JOB, IN ONE PARAGRAPH

Justin's instruction, 2026-08-17: **(a) clear the road** so AC0-38 can run long autonomous lanes without tripping on half-finished work, and **(b) hand 38 a crisp plan** so he spins up, presents one ballot, gets it cleared, and runs unattended. **The road is now substantially clear** and **38's orders are written** (`docs/working/2026.08.17-ac037b-ac038-marching-orders.md`). What is left is listed in §4.

---

# §2 — WHAT LANDED TODAY. All verified, none deployed.

**`evryn-backend`:**

1. **SPRINT Step 106 MERGED** (`f98c2d5`) — `src/safety/halt-signal.ts` and `scripts/dependency-map.ts` are no longer invisible to `grep`/`git diff`. **Rebased onto current `main` and the byte invariant re-verified AFTER the rebase.** Typecheck exit 0. **Three false Atlas claims corrected** (`07-safety.md` asserted the halt sentinel was space-delimited; it is NUL-delimited).
2. **The sprint restructure MERGED** — dominant axis is now 🔵 LIVE vs ⚪ CLOSED. **It conflicted, which was the SAFE outcome**; a clean merge would have silently dropped two lines `main` gained. Both re-applied and verified **in the committed blob**.
3. **Eight Steps minted/recovered** (`02340f3`, `2f9446f`): **108–115**, plus **104 reframed as an exploration** and **105 expanded**. Step 106 flipped DONE and relocated to CLOSED.
4. **CHANGELOG entry written** (`55f8d61`).

**`_evryn-meta`:**

5. **The current-state restructure MERGED** (`139dc7a`) — 167 → 133 lines, with a bounded 📮 BULLETIN. 🔴 **ACf-13's team-runtime correction would have been DELETED by this merge** (the branch predates it, and still calls the fast-follow *upcoming* and Step 43 *"the top open item"*, both false). **His correction is preserved verbatim in the BULLETIN with a staleness marker — and his prose was deliberately NOT rewritten, because that lane is his.**
6. **Two AC0 handoffs repaired** (`2fade3d`) — they contained real control characters and were therefore **binary to `grep`**.
7. **A portable scanner committed** — `scripts/scan-control-chars.js`. Takes any number of repo paths, exits 1 on a finding.
8. **CHANGELOG entry written** (`6ff7b26`); **38's marching orders written** (`99313ea`).

**Worktrees reaped** (only the three whose work is provably on `main`): `evryn-backend-lane-e`, `evryn-backend-lane-b`, `_evryn-meta-lane-b`. **`node_modules` counted before and after — 161 → 161, intact.**

---

# §3 — THE OPEN BALLOT FOR JUSTIN

**These are batched deliberately. None of them blocks §4.**

1. **Lane A (the `CLAUDE.md` split) — three gates, all his:** the three frozen sentences (drafts exist verbatim in the lane brief on the branch, §14; **one hardcodes a stale path that must be swapped**), the three cross-repo pointer fixes, and a rebase + frozen-block byte re-check. ⭐ **Tell him the simplification: only the AGENT DEFINITIONS need a fresh session; the protocol split needs nothing but the merge.**
2. **A checkpoint-protocol step for the control-char scan in `_evryn-meta`** — he approved *drafting* it; the draft is in §6 below, **not applied**, because it is a protocol edit and ACf touched that file last week.
3. **Step 112** (the Operator gate's fail-open) · **Step 109** (how to test `runEvrynQuery` without spending real money) · **Step 105** (two coupled design calls) · **Step 114** (needs a migration). **All four are in 38's ballot too** — do not double-ask; coordinate.

---

# §4 — WHAT IS LEFT, IN PRIORITY ORDER

1. **Delete `.claude/agents/acm-probe.md`.** Justin approved it (*"it's safe to delete"*). ⚠️ **NOT YET DONE, deliberately: Lane A re-adds this file**, so deleting it now guarantees an add/add conflict at that merge. **Do it as part of the Lane A merge, keeping the deletion.** It is a throwaway harness probe that self-describes as *"Not a real agent. Delete after use."*
2. **Give ACf the scanner note** (§7 below — a ready-to-paste block for Justin to relay). **Justin asked for this to come AFTER the protocol is set up**, so ACf can be pointed at something rather than deriving it.
3. **Two stale remote branches.** `origin/dc/step106-nul-bytes` and `origin/acb/sprint-restructure` are the **pre-rebase** tips; their content is on `main`, but git correctly refused to delete the local branches because the remotes are ahead. **Deleting the remote refs is a push operation = Justin's.** Local `acb/current-state-restructure` was deleted cleanly.
4. **Push.** Both repos have unpushed commits from today. **Justin handles pushes** — ask.
5. ✅ **DONE 2026-08-17 by AC0-37c — the nine detached backend worktrees are REAPED, and `evryn-backend` is now a single clean tree.** *(Was: AC0-36's mutation-sweep trees, held un-reaped because their findings' capture had only been verified indirectly.)* **Justin authorized the reap with his standing condition — *"just make sure we've merged correctly, and it's all landed and stable"* — so the verification is recorded here rather than the outcome alone, because the verification is the reusable part.**
    > **What was checked, in order, before anything was removed:**
    > - **Eight of nine sat on a commit already on `main`.** The ninth, `-qc-head` at `f608d95`, held **four commits that were NOT** — the **pre-rebase Step 106 originals**, whose content landed on `main` under different SHAs. ⇒ **Verified by CONTENT, never by git's verdict** (the rule that saved the restructure merges the day before): `src/safety/halt-signal.ts` on `main` carries **no literal NUL in source** while its runtime value is still **exactly 24 characters delimited by code point 0 at both ends**; `docs/atlas/07-safety.md` carries the corrected NUL claim with **zero** occurrences of the old false *"leading and trailing space"*; the committed `dependency-map.json` is the regenerated one (`tests/env-seed.ts` present, 123 files).
    > - **The only uncommitted files across all nine were regenerated `docs/dependency-map.{json,md}`** — generated artifacts strictly older than `main`'s committed copy. **No untracked work in any tree.**
    > - **The sweep's findings are captured independently of the trees:** `docs/working/2026.08.12-qc-sweep-findings.md` is tracked on `main` (636 lines), and **all six sweep-derived Steps — 104, 108, 109, 110, 111, 116 — are present** in the sprint.
    >
    > 🔴 **HOW THE `node_modules` HAZARD WAS HANDLED, because the method matters more than the result.** All nine trees reported **exactly 161 entries, identical to canonical** — which is consistent with *either* nine real installs **or** nine junctions, and **`[ -L ]` does not reliably detect a Windows junction.** ⇒ **No attempt was made to prove they were safe.** Instead, per `CLAUDE.md`'s detect-and-repair rule, they were reaped **one at a time with a count check between each**, so a drop would name the culprit rather than being discovered later. **Never `--force`;** the five dirty trees had their regenerated artifacts `git restore`d first, so the discard was deliberate rather than a side effect of a flag. **Canonical held at 161 throughout; no repair needed.**
    > - **Then the toolchain was proven by EXECUTION, not by `tsx --version`:** `npm test` → **65 suites, 1,925 assertions, 0 failed, ALL GREEN**, matching the recorded `main` baseline exactly. ⭐ **That doubles as a known-good green baseline immediately before the Lane A merge** — Lane A is docs and identity and should not move a single assertion, so a post-merge re-run is a cheap, meaningful regression check. **Use it.**
    > - ⚠️ **Still deliberately untouched in the canonical tree:** the four `tests/_gate-a-*.ts` and `backups/full-public-2026-08-05.sql` (see §8). **Do not sweep them.**

---

# §5 — GOTCHAS LEARNED TODAY. Each one cost real time.

- 🔴 **`grep "Step 93"` returns NOTHING in the sprint doc** — it writes steps as numbered list items (`93.`). **This produced SIX false "lost content" findings** during the loss audit before it was caught. Grep `^93\.` instead. *(The Code Atlas §11 already documents this and it still caught a fresh instance.)*
- 🔴 **You cannot TYPE a control character through these tools.** The Write tool strips it; a JSON tool argument interprets `\uXXXX` into the real character. **Construct it — `String.fromCharCode(0)`.** AC0-37b introduced a NUL into the sprint doc while writing the note about NUL bytes, then could not write a repair script until it stopped trying to type one.
- ⚠️ **A verification grep with a backslash-escaped backtick inside single quotes matches a LITERAL BACKSLASH** and silently returns 0. AC0-37b's first verification of the re-applied sprint lines "passed" this way and meant nothing. **If a check returns exactly the number you hoped for, re-run it a different way.**
- ⚠️ **Node on Windows does not resolve git-bash `/c/...` paths.** Use the scratchpad dir or a relative path.
- ⚠️ **`git commit` during a merge cannot take a pathspec** — it commits the whole index. **If you have unrelated uncommitted work, commit it FIRST or it gets swept into the merge commit.** AC0-37b aborted a merge mid-flight to avoid exactly this.
- ⚠️ **A five-commit rebase means five conflict passes.** For a doc branch where you want the branch's content wholesale, **a single `--no-ff` merge is one controlled resolution instead.**

---

# §6 — THE DRAFTED PROTOCOL STEP (Justin approved DRAFTING it; it is NOT applied)

**Proposed addition to `docs/protocols/lock-protocol.md`.** ⚠️ **Do not apply without his explicit go — it is a protocol edit, and ACf edited that file on 2026-08-13.**

```markdown
### N. Stray-control-character scan (all repos)

Run: `node _evryn-meta/scripts/scan-control-chars.js <repo> [<repo> ...]`
Exit 1 = findings. Expected state: CLEAN everywhere except one deliberate
vertical-tab in `evryn-backend/docs/historical/Evryn_0.1_.../
The_Beautiful_Language_of_Evryn_v0.9.md` — a frozen Word-paste artifact,
left alone on purpose.

**Why this is a checkpoint step and not a test:** `evryn-backend` wires the
same check into its suite (SPRINT Step 113), but `_evryn-meta` — where the
actual instances were found — has no test suite to hook into. This is its
only trigger.

**What it catches:** an agent writing prose that QUOTES an escape sequence
emits the real control character, because a JSON tool argument interprets
\uXXXX natively. A NUL additionally makes the file binary to grep and git
diff, so the damage hides itself and every text search silently skips it.
Every known instance occurred while documenting this exact problem.

**On a finding:** replace the character with its literal six-character text.
Never retype it — construct it (`String.fromCharCode(0)`).
```

---

# §7 — THE ACf NOTE (ready to paste; Justin relays, since there is no agent-to-agent channel)

```
ACf — a small cross-runtime note from AC0-37b, no action required and not a
request to change anything in your lane.

We found a defect class in evryn-backend that also bit evryn-team-runtime
earlier (src/safety/loop-signature.ts, 2026-08-05/06): a literal NUL byte in
a source file makes it BINARY to grep and to git diff, so every security and
hygiene sweep silently skips it AND reports success.

The mechanism, which is the transferable part: an agent's file-writing
instruction travels as JSON, and JSON interprets \uXXXX as a real character
escape. So an agent writing prose that QUOTES an escape emits the actual
control character. Every instance we found -- three across eight repos, two
in AC handoffs -- happened while documenting this exact problem.

There is now a portable scanner committed at:
    _evryn-meta/scripts/scan-control-chars.js
Run it against any repo:  node _evryn-meta/scripts/scan-control-chars.js .
Exit 1 on a finding. It excludes real binaries and constructs the character
it searches for rather than containing one.

evryn-team-runtime scanned CLEAN on 2026-08-17. Nothing to fix today -- this
is only so the check exists and you do not have to derive it if it recurs.
Whether it earns a place in your tracker is entirely your call.
```

---

# §8 — MACHINE STATE (re-derive before trusting)

```bash
cd /c/Users/Justin/Evryn/Code
for d in */; do d=${d%/}; [ -e "$d/.git" ] || continue; echo "$d -> $(git -C "$d" branch --show-current)"; done
git -C _evryn-meta status -sb; git -C evryn-backend status -sb
node _evryn-meta/scripts/scan-control-chars.js _evryn-meta evryn-backend
```

**Known-deliberate untracked files in `evryn-backend` — do NOT sweep, delete or commit:** four `tests/_gate-a-*.ts` (the held Gate-A harness) and `backups/full-public-2026-08-05.sql` (a data-bearing prod dump). **Justin has approved committing the gate-a scripts via a lane**, but their grading logic is written against the pre-③ world and must be re-derived first.

**An anti-stall canary was armed this session** (`Monitor`, ~5-minute tick) at Justin's explicit request, because the *"I'll keep going"* → silent-stop failure is a strong repeated tendency. **It fired three times and caught a real stop every time.** ⚠️ **AC0-37b stopped it at close-out. If you run a long stretch, arm your own — and `TaskStop` it when you finish.**

---

# §9 — 🔴 FRAGMENT SWEEP (added at set-down). WHAT AC0-37b WAS HANDED AND DID NOT DO.

> **Read this section before you plan anything.** It is the honest accounting, and it exists because the sweep caught a failure worth naming: **the 2026-08-13 handoff said, in bold — *"Justin explicitly handed these to you, so DO them — do not just carry them forward into another handoff."*** **AC0-37b did six of the seven neither.** The day went to merges, the sweep findings and the loss audit — all real work Justin directed live — but **that is an explanation, not a discharge.**
>
> ⇒ **The generalizable lesson, because it will happen to you too: a standing list handed to you at spin-up is invisible by mid-session.** Live direction from Justin naturally displaces it, and nothing signals the displacement. **Re-read your own inherited list at the halfway point, not only at set-down.**

## 9.1 — The seven standalone wins (Justin's, 2026-08-13). Status, honestly.

| # | Item | Status |
|---|---|---|
| 1 | **Settle the Postgres NUL question** — does `content_raw` reject a halt-sentinel-bearing string? | ✅ **Now tracked** as SPRINT Step 116(a). Still **not answered** — ~10 min, read-only, against DEV. |
| 2 | **Draft three ADRs** — the compaction rule · the agent-definition consolidation · the router split | ❌ **NOT DONE.** ⚠️ **ADRs are excluded from the propose-first gate — they are written at decision time**, and all three decisions were made this month. **Under compaction a decision captured only in a session doc evaporates.** This is the highest-value item in this table. |
| 3 | **`ac-writing-protocol.md` into the Light cascade** (Justin approved the placement 2026-08-12) | ❌ **NOT DONE.** It is **mandatory before writing any doc and it is in ZERO cascades.** ⚠️ Post-Lane-A the Light cascade lives in `.claude/agents/ac.md`, not `CLAUDE.md` — so doing it **before** that merge means doing it twice. |
| 4 | **Delete `docs/working/2026.08.10-acf-ac0-dashboard-status-pill-note.md`** — redundant; its content is Step 107 | ❌ **NOT DONE.** ⚠️ **Check with Justin first** — the filename says `acf-ac0`, and the standing instruction this session was to steer clear of ACf's material. |
| 5 | **Resolve the five untracked `evryn-backend` files** | ❌ **NOT DONE** — but the do-not-sweep warning is in §8 above. Justin's steer: the honest answer is probably *gitignore the dump, leave the scripts, and say so* — **not** "commit them." |
| 6 | **Fresh `pg_dump`** | ❌ **NOT DONE.** It was 8 days old on 08-13; it is now **12**. Method: `evryn-backend/backups/README.md`. |
| 7 | **Fix the all-branches enumeration script in `CLAUDE.md`** | ❌ **NOT DONE — and AC0-37b hit the bug personally today.** It prints every branch **once per worktree**, so with six meta worktrees a real hit drowns in six copies; AC0-37b had to pipe it through `sort -u` to read it. **It wants Justin's eyes** (it is in the always-loaded manual). |

## 9.2 — Two Justin-gated items from the QC sweep-findings doc that nobody has surfaced

1. 🔴 **The INCONCLUSIVE verdicts need a `--live` decision.** **13 suites never ran** during the mutation sweep because `--live` **spends real Anthropic money and writes to a real database** — which is Justin's call, and the sweep lead **deliberately refused to make it or to wake him for it.** **Every INCONCLUSIVE verdict in that document is a question one authorised `--live` pass would close.** *(It is a decision, not an oversight — do not re-file it as a gap.)*
2. **Four standing QC review-patterns are proposed and unapproved** (sweep findings §6). They need Justin's yes before an AC writes them into `evryn-quality/CLAUDE.md`'s *Patterns This Role Watches For*. ⚠️ **Per the 2026-08-11 routing ruling, that edit logs to the META changelog.** The sharpest of the four: *a documentation claim about a whitespace- or delimiter-sensitive literal, made without reading the bytes* — **which is precisely the Atlas/NUL error Step 106 just fixed**, so it now has a live specimen.

## 9.3 — Five drafted learnings still needing routing by altitude

⚠️ **Route by ALTITUDE and read `LEARNINGS.md`'s own header first** — what *your* build needs goes **local and first**; only the *generalizable* half goes to `LEARNINGS.md`, because that file is drained at `#sweep` and `#sweep` can be weeks away. **The test: strip every name, path, SHA and date — is there still a rule another repo could act on?**

1. **Compaction is not a re-spin, and the difference decides what is recoverable.** A compacted parent keeps every subagent resumable; a re-spun one loses all of them. ⇒ **Establish which event occurred before declaring anything lost.**
2. **A reload restores what you READ, not what you CONCLUDED.** Judgements formed under the old conditions ride through wearing the costume of experience. **The tell: a conclusion you can state but cannot reconstruct the evidence for.**
3. **Methodology derived DURING a multi-wave run does not apply retroactively to earlier waves — and the asymmetry is invisible in the combined report.** Record which rulings bound which wave.
4. **A grep that returns zero feels exactly like proof of absence.** *(Three instances in one session then; **two more today** — a backslash-escaped backtick inside single quotes, and `grep "Step 93"` against a doc that writes `93.`)*
5. **Compare by content, never by git's verdict, when one side rewrote a file wholesale.** **A clean merge is the dangerous outcome.** *(Proven twice today: both restructure merges conflicted, and that was what saved them.)*
6. **A load list stated in a handoff is a hypothesis until someone measures it.** ⇒ **A handoff that hands you a number should hand you the command that produced it.**

## 9.4 — Lane A specifics found this session and recorded nowhere else

- ⭐ **GOOD NEWS, verified at source: the Lane A branch ALREADY carries `main`'s newest text** on the retired mailbox protocol (both `ac.md` occurrences and the trimmed protocol). **So that merge will NOT silently drop those three edits** — checked by content, not by git's verdict. **Do not spend a cycle re-deriving this.**
- 🔴 **There are more than three frozen references.** Beyond the three sentences on the ballot, **§14.3 of the lane brief names three further `CLAUDE.md` references inside the frozen `<mandatory_load>` block.** ACm's recommendation — **and it is cheaper and precedented** — is **two additive AC-variant substitutions**, exactly as the team variant already solves the identical problem, **rather than editing shared text.** **Still Justin's call. Put it on the same ballot so he rules once.**
- **`_evryn-meta/identity/` is REMOVED by Lane A**, not merely emptied. Check anything that assumed that path.
- ⚠️ **Between merge and cutover, THREE verbatim copies of each manual exist.** Any edit to a repo `CLAUDE.md` in that window **silently forks it.** **The drift-check is the only detector and it only works if someone runs it** — that is cutover step 4, and it is the step most likely to be skipped.
- 🔴 **The common-floor hazard, if anyone later de-duplicates the shared discipline:** **AC's build-level-code commit carve-out must NEVER reach DC or QC.** A well-meaning merge of "the commit discipline" into one shared file would hand three agents an authorization Justin granted to exactly one — **a permissions change wearing a tidy-up costume.**

## 9.5 — Smaller loose ends

- **`evryn-ops/CLAUDE.md` has no bottom truncation canary** (its last line is `---`). Flagged 2026-08-11, still open. **It is a real hole, not a formality** — a full-read file with no canary leaves an agent unable to verify its own load.
- **AC0-36's session is presumably dead** (it was live on 08-13). **Subagent resume does not survive a session**, so the relay protocol in the 08-13 handoff §9 is almost certainly moot. **Treat those subagents' briefs, not their reports, as the record.**
- **The `44.` "duplicate" in the sprint is EXPECTED** — Step 44 was relocated and leaves a pointer entry at its old position. **Documented in the doc's own numbering note. Do not "fix" it.**
- **Two committed tools, and they are a PAIR:** `scripts/scan-control-chars.js` finds stray control characters; **`scripts/repair-control-chars.js` fixes one.** Run the scan, then the repair on any file it names. ⚠️ **The repair is the half you cannot improvise under pressure** — you cannot *type* the character you are replacing (every authoring path strips or interprets it), so it must be **constructed** (`String.fromCharCode(0)`). That is the trap that cost AC0-37b two failed attempts.
- 🔑 **The loss-audit TECHNIQUE, since the one-off script is not worth committing but the method is.** When a doc is rewritten wholesale and you need to know what was lost: **extract distinctive tokens from the OLD version** (ADR numbers, Step numbers, backticked paths, env-var names, ticket ids), **check each against the NEW version, and for every miss check whether it has a home anywhere else** (changelog, sprint, ARCHITECTURE, the other repos). **What is left with no home is your real finding.** ⚠️ **Two failure modes, both hit today:** the "does it live elsewhere" check can silently fail and make everything look homeless — **verify the checker itself ran** — and **a token that is written differently in its home will read as lost** (`Step 93` vs `93.`). **Do the mechanical pass first so nothing is missed by skimming, then judge every hit by hand.**

## 9.6 — 🔴 THE CURRENT-STATE HIJACK GUARD — drafted for Justin, NOT applied

**Justin's question, 2026-08-17:** *"agents want to know that the next builders will see the log of changes… that's what the changelog is for… now that we have ACs read the most recent changes, they shouldn't need to hijack the current-state. How can we guard against that?"*

**His diagnosis is right, and there is a sharper version.** Of the eleven appended notes, **not all were "what changed."** At least two were **messages to another agent** — *"here is a collision you will hit,"* *"here is a claim I corrected."* **A changelog records history; it is not a place to hand someone a warning.** So two different needs were wearing one costume, and only one of them is served by "read the changelog."

**Proposed header block for `docs/current-state.md`** — it makes the three-way routing explicit so nobody re-derives it:

```markdown
> ### 🔴 BEFORE YOU ADD ANYTHING HERE — three destinations, and only one of them is this file
>
> | What you have | Where it goes | Why |
> |---|---|---|
> | **"Here is what the system looks like NOW"** | **This file's body** — and you REPLACE, never append | It is a snapshot. Two versions of one fact is the drift this doc exists to kill. |
> | **"Here is what CHANGED and why"** | **The repo's `CHANGELOG.md`** | Every AC skims it on load, so a change recorded there IS seen by the next builder. **That is the guarantee people were reaching for when they appended here.** |
> | **"Here is something you need to know BEFORE you touch this"** | **The 📮 BULLETIN below** | A warning is not history. It needs an owner and an expiry, which the body cannot give it. |
>
> 🔴 **If you are appending to the body, stop.** The urge means you have one of the other two, and the reason the urge is strong is real: **there is no agent-to-agent channel, so this was once the only way to reach everyone.** It is not any more.
```

**Why this and not a rule saying "don't append":** the appending was **rational** — it was correspondence, not laziness, and a prohibition would not have changed the incentive. **Naming the two legitimate destinations is what removes the reason.**

⚠️ **NOT APPLIED.** `current-state.md` is Tier 2 — AC holds the pen, each edit needs Justin's explicit go.

---

# §10 — PULLED FORWARD from the 2026-08-13 handoff (which is now RETIRED)

> **Justin asked for a diff of the two handoffs at set-down.** Everything below was true in the inbound handoff, is **still true**, and appears **nowhere else**. The 08-13 handoff (`docs/working/2026.08.13-ac037-ac037b-handoff.md`) is superseded — **this file replaces it operationally.** Do not work from it.

## 10.1 — Things you would otherwise waste a cycle re-deriving

- ✅ **`acfl/learnings-consolidation` has a ZERO-line diff against `main`** — fully landed, nothing pending. **You will see its worktree standing and assume there is work in it. There is not.** *(AC0-37 checked this precisely because it looked like a hazard.)*
- ✅ **`acfsq/research-move` adds ~20 lines to one brief** and collides with nothing of ours. **`acf13/lock-2026-08-12` has since MERGED** to `main` (that is part of what moved over the weekend).
- ⚠️ **`evryn-langgraph-archive` and `evryn-prelaunch-landing` return 403 on push.** **Both are 0-ahead with clean trees — nothing is lost.** They are the Sealed and Inactive repos and **the wall is expected.** Know this *before* a batten-down, not during one.
- 🚫 **The `claude.ai Google Calendar` MCP connector is unauthenticated** and cannot be authorised from a non-interactive session. **FOUR agents have now flagged it.** It is Justin's connector settings and **it bears on nothing we are doing. Do not be the fifth.**

## 10.2 — Small true things with no other home

- **`src/notify/slack.ts:279` passes the RAW error object to `console.error`.** AC0-36 verified **by execution** that `evryn-backend` does **not** carry the credential-bearing-URL leak class ACf fixed in the team runtime — a fetch rejection carries neither URL nor token in `.message`, `String()`, `.cause` or `.stack`; only the hostname surfaces, and the secret is the path. **This is NOT a hole today. It is the site that would leak first.** Routed as cheap hardening, not a defect — **do not re-file it as a vulnerability.**
- ⚠️ **A script gotcha if anyone ever rebuilds the sprint programmatically:** its final section's preamble **already contains the truncation canary**, so a reassembler that appends one emits **two.** ACb hit this and guarded against it.
- 🔴 **`AGENT_PATTERNS.md`'s long-term disposition is an OPEN JUSTIN DECISION** — permanent redirect, or retire to `docs/historical/` once every inbound link is fixed. It is a 40-line pointer today. **Neither AC0 nor ACf owns this call.** ⚠️ **Ordering trap: retiring it is only safe AFTER every inbound link is repointed** — and **six of those edits are still held in ACf's post-merge brief**, waiting on the Lane A restructure. **Retiring it first re-creates the dangling-link problem the pointer exists to prevent.**

## 10.3 — What §3's "Lane A ballot" actually IS, since Justin asked

**Plain English:** `_evryn-meta/CLAUDE.md` is currently AC's full ~45,000-token operating manual, and the harness force-feeds it to **every** subagent we spin — including a DC, a QC, or a one-line file-lookup agent, none of whom should be reading AC's manual. **Lane A replaces it with a 78-line router that just says "you are agent X, go read your own manual."** The work is **built and sitting on a branch**; it has not merged.

**Three things gate that merge, all Justin's:**
1. **Three sentences inside a frozen block go FALSE the moment it merges** — they say the auto-loaded file *is* an operating manual, and after the split it is a router. **Those blocks are character-for-character frozen; only Justin may re-word them.** Drafts exist verbatim on the branch (lane brief §14); **one hardcodes a path that has since moved and must be swapped.** ⚠️ **§9.4 above adds a fourth item to this same ballot** — three further references inside the frozen load block, which ACm recommends fixing **additively** rather than by editing shared text. **Put all of it on one ballot so he rules once.**
2. **Three other repos point their agents at "AC's `CLAUDE.md`"** for the sync ritual. After the merge that file is a router containing none of it. **These cannot ride an `_evryn-meta` branch** — they are a coordinated same-window change across three repos.
3. **A rebase**, then re-run the frozen-block byte check. ⚠️ **Normalize line endings first or it false-FAILS.**

⭐ **The simplification worth leading with, because it halves the apparent risk: ONLY the agent definitions need Justin to start a fresh session** (the agent registry is read once, at session start). **The protocol split needs nothing but the merge** — it is a file move, and the next agent to read the protocol reads the new one.

**Justin's 2026-08-17 ruling: he will work this ballot with AC0-37c directly.**

## 10.4 — Push state

✅ **Both repos PUSHED and at zero ahead as of this handoff** (`_evryn-meta` → `f5216b1`+, `evryn-backend` → `e73505e`). ⚠️ **Two stale REMOTE branches remain** — `origin/dc/step106-nul-bytes` and `origin/acb/sprint-restructure` are pre-rebase tips whose content is on `main`. Git correctly refused to delete the local branches because the remotes are ahead. **Deleting remote refs is a push operation = Justin's.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
