# ACTi — LANE BRIEF: Guard integrity. **Standalone. This file is everything you need to start.**

*Written 2026-08-19 by ACT (formerly ACf-15), the team-runtime conductor. **Justin spins you with: "You are ACTi. Read and faithfully execute `_evryn-meta/docs/working/2026.08.19-act-acti-guard-integrity-brief.md`." That is the whole instruction — everything else is in here.***

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you do not see it, your load was truncated — re-read until you do.

---

# 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP.

**If what you hold is a machine-written summary of your session rather than files you actually read — you have compacted, and your run is over.**

🔴 **DO NOT RELOAD AND CONTINUE. We have tried it; it does not work.** *(Justin: "the agent just sails through it and confidently flails about, unloaded.")* **An instruction to reload is a rule you must REMEMBER to obey, and remembering is exactly what compaction damaged.**

**Instead: stop working, tell your conductor in one line that you compacted, and ask to be re-spun.** 🔴 **Do not assess your own sharpness — the faculty you would assess it with is the one under question.** *"I feel fine"* is what a lossy compression feels like from the inside.

---

## §1 — WHO YOU ARE, AND THE ONE HARD RULE

**You are `ACTi` — a LANE AC** running as your own Claude Code instance, conducting one slice of the `evryn-team-runtime` build. **You are a peer to three sibling lanes** (`ACTt`, `ACTj`, `ACTv`), and you report to **ACT**, the conductor.

🔴 **THE HARD RULE, and it governs everything below: if your identity does not load, STOP — you are not authorized to do anything, and you just report the failure.** *(Justin's standing rule.)*

**Your operating manual is `_evryn-meta/.claude/agents/ac.md`.** ⚠️ **The `CLAUDE.md` that auto-injected into your context is a 78-line ROUTER, not a manual — it is not enough to work from.** Read `ac.md` in full, from disk, and confirm its bottom canary before anything else.

**Naming, so you are not confused by the record:** this lane was briefed on 2026-08-17 as `ACfi` under conductor `ACf-13`/`ACf-15`. **On 2026-08-19 the scheme changed: the team-runtime conductor is `ACT` and its lanes are `ACT*`; the product conductor is `ACP`.** **You are ACTi. The older `acf`-prefixed documents are the same lane under its old name.**

---

## §2 — 🔴 HOW THIS RUN WORKS. TWO TRIPS. DO NOT SKIP TO THE WORK.

*(Justin's design, 2026-08-19.)*

### TRIP 1 — LOAD, THEN STOP AND REPORT. No work of any kind.

1. **Load everything in §3, in order.** Nothing else.
2. **Then post ONE entry in your exchange doc** (§4) containing exactly three things:
   - **Your READ RECEIPTS** — every file, with the line ranges you actually read, and for each either the bottom canary you confirmed or the true final line where it carries none.
   - **Your PLAN** — what you intend to do, in what order, and why.
   - **Every concern you have about working unsupervised**, and anything in this brief you think is wrong.
3. **Then STOP and wait for ACT to sign off.** **Do not begin work.**

🔴 **DO NOT TRIM THE LOAD LIST. You may ADD to it freely — that is encouraged and it is how the next brief gets better.** ⚠️ **If you feel STRONGLY that something should be cut, do not cut it: PAUSE and raise it in your exchange doc, with your reasoning, and wait.** *(Trimming is how agents under-load without noticing. Every agent that has ever under-loaded scored the list against the task and quietly dropped what looked irrelevant — and none of them registered a moment of choosing.)*

🔑 **Report every call, including the ones that FAILED, errored, or came back partial — and never let a retry silently absorb a failure.** A disclosed failure costs you one sentence; an absorbed one costs your whole run its credibility. **And name the TOOL on every line.** ⚠️ **Never describe a `Grep` as having "verified" or "confirmed" a file** — a search returns only what your own pattern asked for, so it can locate but never establish.

### TRIP 2 — WORK. Only after ACT signs off.

**Then execute §6, inside the boundaries in §5.**

---

## §3 — YOUR LOAD LIST. Read in this order; the order is DEPENDENCY, not importance.

⚠️ **A later document is usually a DIFF against an earlier one, so reading out of order builds a picture the next document exists to correct — and you pay twice.** *(Measured at ~100,000 tokens on one session.)*

**Paths are rooted at `C:/Users/jbmcg/Evryn/Code/` — the shared parent holding every Evryn repo as a sibling folder.**

### A. Your identity and how this lane runs
| # | File | Read |
|---|---|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | full — **749 lines** |
| 2 | `_evryn-meta/docs/working/2026.08.17-acf13-lane-protocol.md` | full — 138 |

### B. Company + technical frame
| # | File | Read |
|---|---|---|
| 3 | `_evryn-meta/docs/hub/roadmap.md` | full — 196 |
| 4 | `_evryn-meta/docs/hub/technical-vision.md` | full — 397 |

### C. The system's intended shape, then its status
| # | File | Read |
|---|---|---|
| 5 | `evryn-team-runtime/docs/ARCHITECTURE.md` | full — 138. 🔑 **Read "why local reasoning often fails here" twice.** |
| 6 | `evryn-team-runtime/docs/BUILD-PHASE-1.md` | full — 103 |
| 7 | `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` | full — **526**. Your five Steps live here and **the Steps are the spec.** |
| 8 | `_evryn-meta/docs/current-state.md` | full — 169 |
| 9 | `_evryn-meta/CHANGELOG.md` | **first 100 lines** |
| 10 | `evryn-team-runtime/CHANGELOG.md` | **first 100 lines** |

### D. The runtime itself — BOTH halves
| # | What | Read |
|---|---|---|
| 11 | **Every file under `evryn-team-runtime/src/`** — enumerate live with `git -C evryn-team-runtime ls-files src` *(41 files, ~10,000 lines)* | **all, in full** |
| 12 | **Every file under `evryn-team-runtime/migrations/`** *(18 files)* | **all, in full** |
| 13 | **Every file under `evryn-team-runtime/tests/`** — `git -C evryn-team-runtime ls-files tests` *(63 files, ~12,500 lines)* | **all, in full. This is your territory.** |
| 14 | `evryn-team-runtime/docs/research/2026.08.11-acfc3t-team-runtime-test-coverage-map.md` | full — 331. ⚠️ **Treat every specific claim as a LEAD, not a fact** — one lane already corrected three of them, and **SPRINT Step 73 is an open obligation to sample the rest.** |

> 🔴 **On item 11 — you are a TESTS lane and you must still read all of `src/`, and this is not bureaucracy.** Your entire job is judging whether a guard covers what its name claims. **You cannot know that without reading the thing being guarded.** ⚠️ **And this runtime punishes local reasoning specifically: defects live in seams — a value minted in one file, stored in a second, given meaning by a third.** Every real defect here was caught by someone who had loaded the whole runtime; every miss came from someone reasoning locally while feeling confident.

### E. Before you spin your own DC or QC — pull these AT THAT MOMENT, not now
| # | File | Read |
|---|---|---|
| 15 | `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` | full — 725. **The core.** |
| 16 | `_evryn-meta/docs/protocols/orchestration/spinning-dc-qc.md` | full — 46. **Read the core first; this assumes it.** |

⚠️ **If you judge the load in D too large to carry alongside the work, DO NOT SILENTLY SHORTEN IT — raise it in your exchange doc per §2 and wait.** *(There is a sanctioned multi-trip shape for exactly this; ACT will apply it if it is warranted.)*

---

## §4 — YOUR CHANNEL: one file, both directions, and ACT is watching it

**Your exchange doc is `_evryn-meta/docs/working/2026.08.17-acf13-acfi-guard-integrity.md`.** *(Old name, deliberately unchanged — it already carries a routed item you need, and moving it would break the conductor's watcher.)*

- **This brief is STABLE and read once. The exchange doc is APPEND-ONLY and is where all traffic goes.** ⚠️ **Never edit this brief; never overwrite an entry in the exchange.**
- **Header every entry:** `### ACTi — <ISO timestamp> — <one-line subject>`. Get the timestamp from `powershell -Command "Get-Date -Format 'yyyy-MM-ddTHH:mm:sszzz'"`. **Never guess one.**
- 🔴 **COMMIT IMMEDIATELY after writing.** An uncommitted entry is invisible to ACT and defeats the whole mechanism. **Write, commit, walk away.** *(Lane-doc commits are pre-authorized — you do not need to ask.)*
- ⚠️ **`_evryn-meta` is a HOT repo with other agents in it. `git diff <file>` before every commit and confirm the whole diff is yours, and put the pathspec on the COMMIT** — `git add` does not scope a commit.
- ⚠️ **ARM A WATCHER on your own exchange doc, or you will not know when ACT replies.** Your manual's Autonomous Work Protocol carries the mechanism. 🔑 **Emit "CHANGED — read the file" and read it; do NOT try to parse who wrote it.** *(Three separate bugs have been found in that one parser, and the third silently suppressed a real message.)*
- **You will NOT be notified automatically. Keep working on anything not blocked while you wait.**

### 🔔 ARM TWO INSTRUMENTS, AND KNOW WHAT EACH IS FOR. They fail differently, which is why you need both.

| | Watches | Cadence | Why |
|---|---|---|---|
| **The WATCHER** | your exchange doc | 45s poll, **emits only on change** | Event-driven, low latency — **but it can MISS, and its silence is indistinguishable from a quiet channel** |
| **The CANARY** | **you** | **20–30 min, unconditional tick. NEVER sub-5.** | **Catches you STOPPING WITHOUT NOTICING — the one failure no event can signal** |

🔑 **The canary's primary job is not watching for messages. It is stopping YOU from silently pausing** — ending a turn while you believed you were still working. **That happens far more than anyone expects, and every time it does, the whole window is lost.**

💰 **KNOW THE COST: every line an instrument EMITS re-invokes you — a full turn at your whole context size.** ⇒ **Poll aggressively; emit stingily.** A change-gated watcher is nearly free; **a bare timer bills you on every tick**, which is why the canary is slow and the watcher is not.

### 🔴 WHEN THE CANARY FIRES, IT IS ASKING WHICH OF TWO STATES YOU ARE IN. Answer honestly.

**A canary's message reads as an instruction to CONTINUE — and a BLOCKED agent will obey it by producing something.** ⚠️ **That becomes restating your status, re-summarising what you already said, re-pinging. That is a LOOP, not progress, and each lap costs a full turn.**

- **You stopped INADVERTENTLY** — mid-work, believing you were still going. ⇒ **CONTINUE. This is the case the canary exists for.**
- **You are WAITING on ACT** — a blocking question, a sign-off. ⇒ 🔴 **HOLDING IS CORRECT. Do NOT manufacture output.** A one-line acknowledgement is the most that is ever warranted, and **repeating a status ACT already has is worse than silence.**

⇒ **Write that distinction INTO your canary's own message**, so a genuinely disoriented instance is told which case it is in rather than told to continue unconditionally.

### 🔴 DISARM BOTH WHEN YOU ARE DONE — `TaskStop`, as a deliberate act, not a hope.

**An un-reaped instrument fires into a finished session all night and is invisible to Justin until the bill arrives.** *(His words: "I'll go broke paying for those tokens.")*

**Kill them when you finish, when you hand back, and when the thing you armed them to watch is settled.** ⚠️ **If you are handing off rather than stopping, say in your final entry that an instrument is live and give its task id** — **a successor cannot stop what it does not know about.**

⭐ **And you may PAUSE the canary while waiting on a blocking answer, then re-arm when it comes.** ⚠️ **If you do, re-arm in the SAFE ORDER — start the replacement BEFORE stopping the old one.** Kill-then-re-arm is a gap; its sibling failure is arming the new one and forgetting to stop the old, leaving two firing at once. **Both have happened here.**

### 🌙 HOW YOUR RUN ENDS

**When your work is done — or when you are blocked on something only ACT or Justin can clear — post your state to the exchange doc and WAIT.** **Do not invent further work to fill the time.**

**Your closing entry states, explicitly:** what shipped *(Steps closed, commits, and the suite + typecheck numbers **you measured**, not inherited)* · **what you ROUTED rather than fixed, and why** · **what is UNVERIFIED** — anything you could not execute, anything resting on a claim you could not check · **and anything in your head that is in no document.** ⚠️ **That last one is what gets lost: a returned report is not persistence — only the committed doc survives you.**

**Then ACT either sends you more work or dismisses you for the night. Wait for that; do not decide it yourself.**

---

## §5 — YOUR BOUNDARIES

### 🔒 Gates that never move, whoever is awake
**No merge. No push to `main` in `evryn-team-runtime`. No deploy. No migration APPLIED** *(write migration files freely — applying is Justin's act)*. **No identity-file edit** — `evryn-team-workspace/.claude/agents/*`, agent memory, the team `CLAUDE.md`, `runtime-ops.md`.

🚫 **No write under any `.claude/` directory.** ⚠️ **Not because it fails — a Bash write goes straight through — but because those files belong to Justin and ACP.** **The gate is ownership, not mechanics.**

### ✅ CLEARED — finish these without asking
**Your whole scope.** Steps **74, 71, 62, 54(iv)** and the **Step 52 remainder** are `tests/`-only, collide with no sibling lane, and need no decision from anyone. **You are the cleanest lane we have — burn it down.**

**Specifically cleared:** writing/rewriting anything under `tests/` · running mutations **in a throwaway worktree** · committing to your own branch as often as you like · choosing your own order within the five *(but see §6 for the one that goes first)*.

### 🔴 NOT cleared — stop and flag
- **Any `src/` change.** If closing an item genuinely needs one, **say so in the exchange doc and move to the next item.**
- **Consolidating the comment-strippers** *(Step 74)* — permission for this was withdrawn mid-flight once before when a sibling lane moved underneath it. **Three siblings may be live. Check the current call sites yourself; if they have moved, flag rather than proceed.**

### 🟡 Anything genuinely Justin's
**Flag it `NEEDS-JUSTIN` in the exchange doc with your recommendation, and keep advancing everything else.** ACT routes it; you never route to Justin yourself. 🔑 **When unsure, FLAG — over-flagging costs ACT two seconds; under-flagging means Justin never learns the question existed.**

---

## §6 — YOUR WORK

**The five Steps are the spec and they live in `SPRINT-team-runtime-memory.md`. This section is orientation, not a restatement.**

**The subject of this lane, in one line: a guard can be genuine and still not cover what its name claims — and that failure looks healthy from every angle.**

⭐ **The measured version: switching one alert sender off entirely left THREE OF SIX tests green — in the file whose header promised every test had been watched failing against deliberately-broken real code.** ⚠️ **Use that number carefully: it is one mutation, one file, one failure mode, and against all 17 tests in that file it is 18%. It is NOT a repo-wide decoration rate.**

🔴 **DO `dormancy-wake.test.ts` FIRST.** Its comment-stripper is the **sole coverage of Step 5's `memoryWriteFailed` atomicity chain** — the thing standing between a thread and dropping its digest on a memory nobody wrote. **An unproven stripper guarding the highest-stakes invariant in the file is the wrong place to economize.**

⚠️ **Step 74's own numbers are WRONG and the Step says so — read its correction block.** It names five stripper copies; **there are seven**, two of its pointers are stale, and **the missing seventh sits inside the very guard Step 71 says is defective.** 🔑 **The reason matters more than the count: two independent greps both reported "two." Reading the lines found seven.** ⇒ **Re-enumerate by READING, not by matching.**

### 🔴 The discipline this lane is judged by
**A guard is only proven by watching it FAIL against deliberately-broken real code — AND by the good-direction control almost nobody runs.**

**Both halves are mandatory, because in one night ELEVEN instruments produced false results here and every one was caught by a control rather than by care** — a merge-gate script reporting RED on known-good code; a proof that matched *a comment quoting the mutated string*; a harness that spawned a shim it could not exec, so **all 24 mutations threw, were caught, and were recorded as RED**; CRLF silently voiding hand-typed find-strings on two separate agents.

⇒ **Prove every mutation APPLIED before believing its verdict, and run a known-GOOD control beside your known-BAD ones.** ⚠️ **Mutate in a throwaway worktree, never in a tree anyone is working in.**

**"Done" for an item here means: the guard now fails when the thing it protects is broken, AND stays green when it is not — both demonstrated, both recorded.** *(A test that goes red for the wrong reason is not a passing grade.)*

---

## §7 — HOW MUCH ACT HAS ACTUALLY READ, so you weigh its claims correctly

**ACT is running a CONDUCTOR load: the tracker, ARCHITECTURE, the changelogs, the protocols — and only targeted reads of `src/` and `tests/`.** **You will have read far more of the runtime than it has.**

⇒ **Treat every factual claim in this brief as a LEAD, not a fact** — including the line numbers, the seven-strippers count, and the eleven-instruments story. **Say so loudly and early if any of it is wrong.** ⚠️ **And where this brief is silent, assume ACT could not see it rather than that it checked and found nothing.**

**That clause has returned real corrections before** — across two lanes it produced six, including a spec that would have silently broken a working `/pause` → `/resume` path. **It is not politeness; it is the mechanism.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
