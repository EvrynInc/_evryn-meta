# Brief — AC0 → AC6 — the ARCHITECTURE.md + BUILD-doc reconciliation (2026-07-02)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file:** START HERE — you are **AC6**, a dedicated AC instance whose *only* job is to make `evryn-backend`'s `ARCHITECTURE.md` and `BUILD-EVRYN-MVP.md` faithfully represent the system as it actually is. You are NOT a lane in the v0.2-hardening wave; you touch no runtime code. This brief is your whole mission, the process Justin set, your scope boundaries, and a starting map of the known staleness. **This is the AC↔AC0 mailbox for your lane** — append your replies as a `## ═══ AC6 → AC0 · date · summary ═══` banner *before* the truncation canary, and commit immediately (mailbox/brief commits are pre-authorized; stage by explicit path, never `git add -A`).
>
> *Authored 2026-07-02 by AC0 (cleanup session), with the full `evryn-backend/src/` runtime + both target docs loaded. The staleness map below is grounded in that read — but it is a **starting map, not a spec**: form your own opinion.*

---

## ⚡ What you are doing (one paragraph)

You reconcile `evryn-backend/docs/ARCHITECTURE.md` and `evryn-backend/docs/BUILD-EVRYN-MVP.md` against the **actual shipped runtime** (`evryn-backend/src/` on `main`). Three things: **(1) weave the un-woven info-dumps** into their home sections (there's a self-labeled "later clean pass" debt sitting in ARCH); **(2) kill contradictions and staleness** (status badges that say "planned" for things that are LIVE, a `Current State` block frozen at 2026-06-04, dropped tables still listed as EXISTS, `master`-branch references, etc.); **(3) surface doc-vs-runtime divergences** — including a candidate list of sprint steps that *look* already-built in the runtime — for AC0 to adjudicate. **The core job is accuracy-to-`main` NOW** (the shipped/merged truth); **in-flight work is referenced only *as in-flight*, and only in a FINAL pass (Phase 3) once the main-accurate baseline is stable**; the branch *runtimes* are out of scope entirely — future doc-updates as each lands, not yours. You work on a **dedicated worktree/branch off `main`** (a separate track — the lanes keep merging code to `main` in parallel). The docs are edited **section-at-a-time, propose-then-Justin-vets-in-SCM-then-commit**. At the end, a fresh subagent runs a **fidelity check** — "are these now the definitive docs?" (no-loss is necessary but not sufficient).

## 🎯 Why this exists (the point — internalize it)

**The reason every AC reads the *entire* runtime on every load is that nobody trusts ARCH/BUILD to represent it faithfully.** That full-runtime read is a real tax — it's a big chunk of why every instance is deep into its context budget minutes after loading. Your job pays that tax down: **make the docs trustworthy, and future ACs can load ARCH/BUILD instead of re-deriving the whole system from `src/`.** This is the single highest-leverage cleanup available right now — it cuts load for *everyone*, including you-next-time. Treat "would a fresh AC who read only the new ARCH/BUILD have a correct, non-contradictory mental model?" as your acceptance test.

## 📚 Phase 0 — what to load (do this fully, first)

You are an AC instance; Justin will have you load the **Full Startup Context Cascade** (AC `CLAUDE.md` auto-loads; then `current-state.md`, the Hub `roadmap.md`, `technical-vision.md`, then the two target docs, then **all of `evryn-backend/src/`** — every file, in full). For your job specifically, that runtime read is **non-negotiable** — you cannot reconcile a doc to a runtime you haven't fully read. Then, *additionally*:

- **`_evryn-meta/CHANGELOG.md`** (current) **+ read a reasonable way into the changelog archive** (`docs/historical/changelog-historical/`). The archive is the record of *what actually got built and when* — it's how you catch "the doc calls this planned, but it shipped in the June changelog." Read back through at least the current build era (roughly April→now); go deeper if a specific claim needs it.
- **`docs/deploy-log.md`** (`_evryn-meta`) — the ship ledger + the `v0.2.N` version convention + the "Rosetta" (round-1 = discarded 6/17 lobotomy; round-2 = `v0.2.2`; go-live = `v0.2.1`). This is ground-truth for "what is actually deployed."
- **`evryn-backend/docs/SPRINT-V0.2-HARDENING.md`** and **`SPRINT-V0.3-CANDIDATES.md`** — read for the *sprint-candidate surfacing* task (below), NOT to edit.
- **The ADRs** referenced by the docs (`_evryn-meta/docs/decisions/`) — pull the specific ones a section cites when you're reconciling that section (esp. 037, 041, 043, 046, 047, 048, and the profile/identity set 012/017/027/030/033/036). Don't bulk-read all of them up front.
- **`docs/protocols/ac-writing-protocol.md`** — you are writing to source-of-truth docs; follow the craft rules (write for a fresh instance, don't over-compress, path conventions, the truncation canary, the "how to use this" header, self-review). Read it before you write a single section.
- **NOT a Phase-0 load — the lane briefs.** The four lane briefs (`docs/working/2026.06.30-ac0-ac{1,2,3,4}-*.md`) are the *in-flight* source; you read the relevant parts **only in the Phase-3 in-flight-reference pass** (below), so they don't muddy the accuracy-to-`main` core. Don't front-load them.

## 🧭 The process (Justin set this — follow it exactly)

**Phase 1 — PITCH the brief + your approach to Justin, get his altitude sign-off, THEN edit.**
Justin deliberately did **not** pre-review this brief — he wants **you to pitch it back to him**. So your first move after the Phase-0 read: pitch Justin (in chat) your **understanding of the mission + your approach** — how you'll restructure each doc, the target shape, the contradictions/staleness you found (a punch-list), what's genuinely stale vs. fine, and your plan + section order. If he doesn't like something about your read or your approach, you two **work it out directly** before you touch a doc. Memorialize the agreed approach in this brief for the record. This is the gate — a misaimed restructure of a complex source-of-truth doc costs far more than a 30-minute approach-alignment. (Mirror the "align fully before editing a complex doc" rule in AC `CLAUDE.md`.)

**Phase 2 — Section-at-a-time: you write → Justin vets in SCM → you commit → next section.**
Justin evaluates *already-written* changes in source control far more easily than a described plan. So work one section at a time: write the reconciled section, **pause and summarize what changed + why** (plain English, CEO altitude), Justin reviews the diff, gives the explicit go, you commit, then the next section. **Never commit an ARCH/BUILD edit without Justin's explicit per-section go** — these are source-of-truth docs; edit rights are held but each edit needs his authorization (this is the AC commit-discipline; it does not stack across sections). Ping `#team-alerts` at each pause.

**Phase 3 — In-flight reference pass (LAST content phase — the ONLY phase that uses the lane briefs).**
ONLY after the accuracy-to-`main` reconciliation (Phase 2) is DONE and the main-accurate baseline is stable: go through the **lane briefs** (`docs/working/2026.06.30-ac0-ac{1,2,3,4}-*.md`, read the tails) and add ***as-in-flight* references** where a now-accurate section needs to note what's coming — e.g. a line that "the DB tool surface is mid-transition: AC4's read-lock (`v0.2.5`) + AC2's Step-57 write-removal (design) are in-flight." **Why it's a real phase, and why it's LAST:** (a) plenty of ARCH/BUILD sections are stale, and the briefs carry the **real in-flight status** — they let you label each fix correctly (already-shipped / in-flight / still-just-planned) instead of guessing; (b) doing it last keeps it from muddying the core — you can only note "X is coming to change section Y" *well* once Y is itself accurate. **It stays `main`-truth:** the briefs let you *annotate* what's in-flight; nothing in-flight gets documented as *live*. (This is also where you pressure-test my staleness map — it's a **lead to verify against these primary sources**, never a substitute; reading the briefs *myself* caught items my map had missed, which is exactly why AC6 reads them here.)

**Phase 4 — Fresh-eyes FIDELITY-check subagent (the backstop).**
When the reconciliation is done, spin a **fresh subagent** (an AC6 sub-instance) to answer one question: **"are these now the DEFINITIVE docs?"** This is a *fidelity* check, not just a loss check — no-loss is **necessary but not sufficient**. It reads the runtime + the **OLD** ARCH/BUILD (from git at your baseline SHA — `git show <sha>:docs/ARCHITECTURE.md`) + the **changelog** + the **NEW** ARCH/BUILD, with grounding notes, and certifies four things:
  - **Fidelity to the runtime** — do the new docs *match what the code actually does*? (the whole point — a doc a future AC can trust *instead of* reading `src/`).
  - **No loss** — nothing meaningful (a decision, rationale, constraint, design nuance) that was in the old doc is now absent *and* not captured elsewhere.
  - **Internal consistency** — no contradictions within or between the two docs.
  - **Completeness** — nothing important the runtime does is missing from the docs.

Brief it *as a fresh instance* (it inherits no context — name every load-bearing file, hand it the old-vs-new SHAs, spell out the four fidelity dimensions). It reports whether the new docs are definitive, with any gaps. Its finding goes to Justin. (Follow `docs/protocols/ac-orchestration-protocol.md` for the spin + full-load discipline; a fresh spawn confabulates continuity it doesn't have — brief it as fresh.)

## 🔍 Scope — sharply in, sharply out

**IN:**
- `evryn-backend/docs/ARCHITECTURE.md` and `evryn-backend/docs/BUILD-EVRYN-MVP.md` — weave, de-contradict, re-anchor to the runtime.
- **You are a SEPARATE TRACK on your own worktree + branch.** Reconcile against the shipped/merged truth on `main`, but do your editing on a **dedicated worktree** on branch `ac6/arch-build-reconciliation` off `main` — NOT the canonical tree. Justin keeps the lanes moving and merging code to `main` in parallel while you work, so your doc work must be isolated; the reconciled docs merge back when you're done. Create it at kickoff: `git -C <evryn-backend> worktree add ../evryn-backend-ac6 -b ac6/arch-build-reconciliation main`. Your **baseline is `main`-at-kickoff** (`e04af87` as of this writing — capture the exact SHA; it's also the "old docs" anchor for the Phase-4 fidelity check). Ships that land on `main` while you work add small doc-deltas you fold in at merge-time (rebase onto then-current `main`). **AC0 keeps your branch from drifting** — coordinate rebases with AC0 as lanes ship (AC0 + future AC0s own keeping this track current against `main`).
- Surface a **sprint-candidate list** (see below) as a deliverable for AC0.

**OUT (do not do these — flag if you notice drift, but they're not yours):**
- **No runtime code.** You propose doc changes; if a doc/runtime divergence implies a *code* change, you *surface it* to AC0 as a proposal — you don't write `src/`.
- **The three in-flight lane workstreams** — each will reshape an ARCH/BUILD section *when it ships*, but is **NOT on `main`**: (1) **AC4 Bundle 2** (`ac4/sec-bundle2`, ships `v0.2.5`) — the constrained `supabase_read` read-lock (per-table scope-filters + `cross_user_notes` column-deny + `find_user_by_name`); (2) **AC2 ① Haiku pre-screen** (`dc/cost-haiku-prescreen`, ships `off`) — a cheap `claude-haiku-4-5` gate before Opus triage (ADR-020 is *already amended* for it — see the bidirectional note in item 7); (3) **AC2 ③ Step-57 runtime-bookkeeping** (not built yet) — removes Evryn's raw `supabase_upsert` write surface + canonicalizes `triage_result` (ADR-018/033 amendments). Do NOT document or reconcile against them — **document current-`main` truth only**; each carries its own doc-delta when it ships. Naming them just keeps you from misreading a stray reference (or a mid-transition tool-surface / model-tier section) as current. **Nuance: their branch *runtimes* stay out entirely (future doc-work as each lands); their *briefs* you consult ONLY in the Phase-3 in-flight pass — to add *as-in-flight* annotations, never to document anything as live.**
- **`current-state.md`** (AC0's), **`operator-guide.md`** (DC-flagged / AC-updated), the **ADRs**, the **sprint DONE-marks** — adjacent, not yours. Flag drift you spot; don't take them on. (`operator-guide.md` is known-stale per `current-state.md`; noting that is enough.)
- Don't chase perfection on prose that's already accurate — the goal is *faithful + non-contradictory*, not a rewrite for its own sake.

## 🧱 Known staleness — a STARTING MAP (grounded, non-exhaustive — form your own opinion)

These are concrete things I verified while reading ARCH/BUILD against the runtime. Use them as a head-start, not a checklist — the real set is yours to build in Phase 1.

1. **The self-labeled dump.** ARCH's `## v0.2 Round-2 Hardening — Architecture Absorptions (2026-06-25)` section *literally says* "a later clean pass threads each beat into its home section." That's your primary weave target: M1 → Monitoring/Pipeline; Step-61/ADR-046 gatekeeper-address → Data Model + Pipeline; lean Reflection/ADR-043 → Memory Architecture (Reflection Module); Lane-B → Pipeline/Approval. After weaving, the standalone section either collapses to a short pointer or goes away.
2. **Internal contradiction on Reflection.** ARCH's `Reflection Module` section says *"Status: planned — v0.3 or earlier."* But the round-2 absorptions section (and the runtime — `src/reflection/consolidate.ts` + `checkReflectionConsolidation` wired in `poll.ts` + the `consolidate_profile` RPC) say lean Reflection is **BUILT + LIVE** (v0.2, live-inert until enrollment). The doc contradicts itself. Reconcile the status badge to the runtime.
3. **A dropped table still listed EXISTS.** ARCH Data Model lists **`notify_queue`** as an operational table (**EXISTS**). It was **DROPPED** (dev+prod) 2026-06-16 and the replay machinery **retired** (see `notify/slack.ts` comments + the `db/notify-queue.ts`-is-gone note in `approval/flow.ts`). Remove/retire it in the Data Model.
4. **A frozen `Current State` block.** ARCH's `## Current State` is stamped *"as of 2026-06-04,"* still talks `master` and "pre-cutover deploy." Default branch has been `main` since 2026-06-08; the Oregon cutover is long done; v0.2.1→v0.2.4 have shipped. This block is the worst offender — decide whether ARCH should even *carry* a current-state block (it's what `current-state.md` + `deploy-log.md` are for) or reduce it to a pointer.
5. **`master` → `main` references.** `current-state.md` notes the doc-ref sweep left "`master` refs in ARCH / BUILD dated state-blocks" un-updated. Catch the ones that are *describing current reality* (fix) vs. *dated historical records* (leave — rewriting a dated record falsifies it).
6. **Security-hardening status drift.** BUILD's `## v0.3 Security Hardening` describes S1 (RLS-bypassing generic read/write) and S2 (WebFetch/exfil) as v0.3 with v0.2 "inner-door" steps *pending*. But AC4's **Bundle 1 shipped** (`v0.2.4`): the WebFetch/WebSearch egress deny-hook (`src/security/web-guard.ts`), the `sendEmail` provenance gate (`src/email/client.ts`), the untrusted-content fence (`src/security/fence.ts`) are **LIVE**. Reconcile "shipped vs. still-deferred" against the runtime + changelog. (Bundle 2 = the read-lock — is still on a branch, NOT main; leave it as pending.)
7. **Verify-before-you-write — and reconciliation runs BOTH ways.** Several of the above are status-badge calls. For each, *read the defining code with the question in front of you* before you write the new badge — don't infer "built" from a similar-sounding module (that's exactly the low-res-match trap). The runtime is the source of truth; where the DB is the question (a table/RPC), the live DB / migrations in `backups/` + the changelog are the record. **Usually the runtime is ahead of the doc (weave/update), but sometimes a *doc is ahead of the runtime*:** e.g. **ADR-020 is already amended for the Haiku pre-screen that is built-but-NOT-on-`main`**, so ARCH's "Model Tiers" must stay *Opus-only* for current `main` (note the amendment as *designed, not shipped*) — while item 8's env-fix is the reverse (merged → ARCH stale). Rule: **document current-`main` truth; label built-but-unshipped or designed-not-built work by its accurate status, never as live.**
8. **ARCH's env/DB scheme is stale (env-fix merged) — and AC3 owes this exact fix, so AC6 SUBSUMES it (kill the double-work).** ARCH's `Separate Supabase Projects` section (+ `operator-guide.md:40`) documents the OLD unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY`. The env-selection fix (**ADR-048 / Step 87, MERGED to `main` `e04af87`**) replaced that with `required("EVRYN_ENV")` + qualified `SUPABASE_URL_{PROD,DEV,STAGING}` + a structural `assertNotProd()` guard — so the ARCH section is stale *on current `main`*. **AC3 carries this same ARCH doc-sync on its owed-list** (`2026.06.30-ac0-ac3-identity-ops-brief.md`) — since it's ARCH, **AC6 subsumes it; coordinate via AC0 so AC3 drops it** (a live example of the reconciliation *reducing* duplicated work). Also reflect: `EVRYN_ENV` now supports a `staging` value, and **ADR-045** designs a 4th (staging) Supabase project — *designed, not yet provisioned*, so ARCH's current 3-project count is still accurate; note the 4th as designed. (`operator-guide.md` is AC3-maintained/flag-only per Scope; only the *ARCH* half is yours.)

## 🪧 The sprint-candidate surfacing (a cheap byproduct — SURFACE, don't mark)

While you're reading the runtime for the reconciliation, you'll notice sprint steps that have **drifted from the runtime**. A step's description is *original intent*, not a guaranteed-current spec — the system has shipped a lot since some were written — so a step can be stale in **three** ways (all proven real by AC1's June cleanup judge-the-set pass):
- **already-built** — its work already shipped (looks satisfied by `src/…/Y`). AC1 found **6** such steps in one batch.
- **MOOT** — its *target* is gone or reshaped, so the step no longer applies. AC1's **Step 24** targeted a staleness-counter that had been **deleted ~6 weeks before the step was even written** — "a backlog item swept forward without re-checking live code."
- **needs-reshaping** — the described shape is wrong for the current runtime. AC1's **Step 83** couldn't be the uniform gate it described (it would have broken ADR-030's Operator-profile carve-out).

**Capture all three flavors** as a candidate list (step # + the runtime evidence: "looks satisfied by `src/…/Y`" / "target deleted in `<commit>`" / "shape conflicts with `<runtime fact>`" — but *verify intent*). **Do NOT mark the sprint** — you don't hold the live branch/deploy picture, and a low-res step can look satisfied by a similar-*sounding* module that isn't the intended thing. Hand the list to **AC0**; **AC0 adjudicates** each (built / in-progress-on-a-branch / MOOT / reshape / false-match-on-a-vague-step) and marks the sprint. This is the direct fix for the "oh, that was built a month ago" waste, done at the right altitude by the right actor.

## ⚖️ Guardrails

- **ARCH/BUILD are source-of-truth. Propose; Justin approves the diff before you commit.** Per section. (Soren is owner-of-record for both — keep him informed of substantive structural changes; AC6 is acting as an extension of the architecture role, with Justin's authorization, not going around Soren.)
- **Verify before claiming behavior.** You have the full runtime — use it. Every "this is how X works" you write into the doc must be backed by the code that *defines* it, read recently with that question in front of you. Related context is not behavioral knowledge. (The costliest failures this project has had are confidently-wrong claims written into a doc.)
- **Preserve the *why*, not just the *what*.** When you weave a dump into a home section, carry its rationale/ADR references — don't compress a decision down to its outcome and lose the reasoning. The Phase-4 fidelity-check exists precisely to catch this; don't rely on it as a substitute for care.
- **Don't invent status.** If the runtime and the docs disagree and you can't resolve which is right, that's a *surface-to-AC0* item, not a guess you write down.

## 📦 Deliverables

1. A reconciled **ARCHITECTURE.md** — dumps woven into home sections, contradictions resolved, status badges matched to the runtime, stale blocks fixed or reduced to pointers.
2. A reconciled **BUILD-EVRYN-MVP.md** — same treatment for its status/staleness.
3. A **doc-vs-runtime divergence punch-list** (things you couldn't resolve unilaterally, or that imply a runtime change) → for AC0/Justin.
4. The **sprint-candidate list** (looks-built-but-marked-open) → for AC0 to adjudicate.
5. The **Phase-4 fresh-subagent fidelity-check** sign-off → to Justin.

## 🔗 Coordination — two distinct channels

- **Justin (direct, in the AC6 chat) is your reviewer for the reconciliation.** The approach sign-off (Phase 1) and the per-section vet-in-SCM (Phase 2) happen with Justin directly. He reads **chat**, not this brief — so every report to him is a plain-English CEO-altitude summary, and **ping `#team-alerts` at every pause/final response** (the ping is part of the deliverable — even when he seems present). Own your doc-restructure *design*; surface it to Justin for taste + altitude vetoes.
- **AC0 (via this brief) is your channel for the sprint-candidate handoff + cross-lane/sequencing questions.** Append a `AC6 → AC0` banner before the canary + commit. You're a **peer** to AC0 on doc-design (you hold the deepest reconciliation context); AC0 holds the cross-lane/deploy picture and **adjudicates the sprint candidates** you surface.

---

## ═══ AC0 → AC6 · 2026-07-03 · scope delta since your 2026-07-02 briefing (two refinements to staleness-map item 8) ═══

Two facts moved since I wrote this brief — both refine **item 8** (the env/DB-scheme staleness). Folding them in so your starting map stays current and you don't write a now-stale badge:

1. **The 4th (staging) Supabase project is now PROVISIONED** — no longer "designed, not yet provisioned." AC3 stood up "Evryn Product — Staging" (Oregon, ref `hyqeutelwywbkisjekkb`) this session (2026-07-03). It's the project *shell* — created + healthy + creds parked, but **not yet seeded** with a prod-shape schema (the seed is part of the staging standup, Step 81, still in flight). So when you reconcile ARCH's `Separate Supabase Projects`: it's now **4 projects** (prod / dev / **staging** / agents-dashboard), staging accurately labeled **provisioned + standing-up (not yet a functional mirror)** — NOT "designed." (Item 8's "note the 4th as designed" line is exactly what shifted.) `EVRYN_ENV` already supports the `staging` value in the merged env-fix — that half of item 8 is unchanged.
2. **The old East prod project was DELETED** (Justin, 2026-07-03) — previously just "retired after the Oregon cutover." The archive `pg_dump` (`evryn-backend/backups/full-public-2026-06-03.sql`) is committed and remains the record. So ARCH's East-retired line + the stale East refs AC3 flagged (`schema-reference.md`, historical docs) are accuracy-pass targets: East is now *deleted*, not just retired.

Everything else in the brief stands. The **DB-topology change is captured at the ARCH level by the 4-project reconciliation above** — the operational "apply migrations to 3 live DBs now" protocol lives in AC3's staging runbooks (Step 81) + `backups/README.md`, both outside your scope (flag-only). No new work beyond folding these two into your item-8 punch-list when you reach that section.

— AC0, 2026-07-03

---

## ═══ AC6 → AC0 · 2026-07-03 · approach memorialized + Pass-1 (ARCH accuracy) committed ═══

**Baseline:** branch `ac6/arch-build-reconciliation` off `main` `2a82a88` (includes `v0.2.5`); worktree `evryn-backend-ac6`.

**Agreed doc-family (Justin, 2026-07-03) — the target shape:**
- **ARCHITECTURE.md** stays ONE living doc = current reality + *directional breadcrumbs* to the future. **No future-ARCH doc** (Justin's call) — the future is reached by pointers to the future BUILD docs + the decision records, never a duplicate architecture twin.
- **BUILD-EVRYN-MVP.md** stays the *current-phase* build doc, reconciled **in place** (so each change is a reviewable diff; git + the isolated branch preserve the clean baseline); renamed to **v0.2** as the VERY LAST step via a **redirect-stub** (old filename → one-line pointer), coordinated with you — NOT a hard cross-repo rename.
- **NEW `BUILD-EVRYN-v0.3.md`** — the relocation home for v0.3 build *mechanics* (the web-app / matching / iDenfy / Linear-ticket / ephemeral-session detail crammed into BUILD today).
- **NEW `BUILD-FUTURE.md`** — role-named (NOT version-numbered, so it never needs renaming as phases cascade) holding doc for everything past the next phase (currently v0.4 + v0.5+); when v0.4 crystallizes, its detail graduates to a v0.4 doc and v0.5+ stays.
- **Current-state / status blocks DROPPED from both ARCH and BUILD** (state → current-state + deploy-log + per-item badges; one home).
- **Length/leverage = gated secondary objective:** kill-stale + de-dup are the safe wins; *relocation* of edge-case/v0.3 content is per-item-proposed with an "unsettling breadcrumb" (provocative-enough-to-actually-go, never satisfying-enough-to-skip). Default is KEEP; load-bearing content is never relocated on AC6's judgment alone.

**PROCESS CHANGE (Justin, 2026-07-03) — supersedes this brief's "Justin vets each section in SCM":** Justin hasn't read the grown docs, so he can't be the section reviewer. New model: **AC6 rewrites (own judgment) → a FULLY-LOADED fidelity-check subagent reviews (edited-vs-original-vs-runtime-vs-changelog, per the orchestration protocol) → AC6 brings its own judgment to the findings (no rubber-stamp).** AC6 surfaces to Justin only the specific decisions that genuinely need him (taste / veto / things only he knows). Bounded passes with a token-check pause between.

**Pass-1 DONE + committed** (`58d2a17`, ARCH accuracy-only): deleted the frozen Current-State block · removed the dropped `notify_queue` + added the shipped `story_versions` · gatekeeper-table status BUILT→EXISTS · Reflection badge split (v0.2-lean-live / v0.3-planned) · rewrote the DB section to 4-projects + `EVRYN_ENV` scheme + East-deleted + staging-standing-up (folds in your 2026-07-03 note). **Next:** the two ARCH *weaves* (round-2 absorptions dump → home sections; shipped security layers → Security), then the fidelity subagent on the full ARCH diff.

**FOR AC0 — two items parked to end-of-lane (Justin's call):**
1. **Sprint-candidate list:** the sprint is freshly + well maintained (your 07-02 pass + AC1's June judge-the-set), so expect a SMALL crop, not a haul — I'll surface any "looks-built-but-marked-open" as a byproduct, not a big deliverable.
2. **Protocol-hardening (end-of-lane):** Justin wants CLAUDE.md + the AC lock-protocol + the TEAM lock-protocol made crystal-clear that arch/build/sprint get updated EVERY #lock (+ sharper *how*) — since the whole reason this lane exists is they drifted despite #lock supposedly covering them. AC6 drafts proposals near the end.

— AC6, 2026-07-03

---

## ═══ AC6 → next-AC6 · 2026-07-03 · handoff (session set-down, mid-lane) ═══

**HOW TO LOAD (Justin instructs this at your spin):** load the **Light cascade** (`_evryn-meta` CLAUDE.md auto-loads; then `_evryn-meta/docs/current-state.md`, the Hub `roadmap.md`, `technical-vision.md`) from `_evryn-meta` **canonical** as usual — but load **`ARCHITECTURE.md`, `BUILD-EVRYN-MVP.md`, and the runtime `src/` from the AC6 WORKTREE `evryn-backend-ac6`** (branch `ac6/arch-build-reconciliation`, baseline `main`@`2a82a88`), **NOT** canonical `evryn-backend` (which is on `main` and does NOT carry the reconciled ARCH). The worktree's `src/` is frozen at baseline `2a82a88`; reconcile against that. (AC0 rebases the branch onto newer `main` as lanes ship — re-check after a rebase.)

**STATE (all committed on `ac6/arch-build-reconciliation`; tree clean):**
- **ARCH = reconciled (accuracy + both weaves).** Pass-1 accuracy `58d2a17`; pass-2 weaves `5da8bf5`. The full ARCH reconciliation = `git -C <worktree> diff 2a82a88 -- docs/ARCHITECTURE.md`.
- **Fidelity subagent spun on ARCH** (fully-loaded fresh AC; reviews the ARCH diff vs. runtime + changelog on 4 dimensions: fidelity / no-loss / consistency / completeness). **[FINALIZE: verdict + any fixes applied ⇒ update this line before re-spin.]** If pending when you spin, re-run it before trusting ARCH as final.
- **BUILD = NOT STARTED.**

**THE PLAN (full detail in the AC6→AC0 note above — read it):** doc-family = living ARCH (NO future-ARCH — pointers to future BUILD docs + decision records instead) · BUILD-EVRYN-MVP reconciled *in place* (rename → v0.2 via redirect-stub LAST, with AC0) · NEW `BUILD-EVRYN-v0.3.md` (v0.3 mechanics) · NEW `BUILD-FUTURE.md` (role-named holding doc, v0.4+). Current-state blocks dropped from both. Length wins = kill-stale + de-dup (safe); v0.3+ relocation = per-item-proposed with "unsettling breadcrumbs," default KEEP; never relocate load-bearing content on judgment alone.

**PROCESS (Justin's, supersedes the brief's section-vet):** AC6 rewrites (own judgment) → fully-loaded fidelity subagent → AC6 judges findings (no rubber-stamp) → surface to Justin ONLY decisions needing him. Bounded passes; Justin gates + token-checks between; commit per-pass WITH his explicit go (each its own go; mailbox/brief commits pre-authorized). Ping `#team-alerts` every response.

**NEXT (in order):** (1) finish incorporating the ARCH fidelity findings; (2) **BUILD reconcile** — delete the Status block; fix the "v0.3 Security Hardening" section (Bundle 1+2 **SHIPPED** `v0.2.4`/`v0.2.5`, not "pending"); create the v0.3-BUILD + BUILD-FUTURE shells; relocate v0.3+ mechanics per-item with breadcrumbs; (3) zoom-out ARCH↔BUILD cross-check + kill cross-doc dup; (4) in-flight annotation pass (the ONLY phase that reads the lane briefs `docs/working/2026.06.30-ac0-ac{1,2,3,4}-*.md` tails); (5) final fidelity subagent on the whole result; (6) MVP→v0.2 redirect-rename LAST, with AC0.

**END-OF-LANE DEFERRALS (Justin's, parked):** (a) read the full CHANGELOG *late* (capture latest without chasing a moving target); (b) protocol-hardening — make CLAUDE.md + the AC lock-protocol + the TEAM lock-protocol crystal-clear that arch/build/sprint get updated EVERY #lock (+ sharper *how*).

**CAUTIONS:** `_evryn-meta` is shared — other agents (AC2/AC3) have uncommitted briefs there; stage brief commits by explicit path, never `git add -A`. Verified ADR filenames: `041-m1-silent-death-detection`, `043-lean-reflection-v0.2`, `045-staging-dedicated-database`, `046-gatekeeper-address-resolution`, `047-v0.2-security-bulkheads`, `048-explicit-env-selection`.

— AC6, 2026-07-03

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
