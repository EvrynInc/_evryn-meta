# Brief: AC0 → AC7 — Doc-Accuracy Process Fix (stop ARCH/BUILD/SPRINT drifting again)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** START HERE — you are **AC7**, a dedicated instance whose only job is to build the **durable, layered process** that keeps `evryn-backend`'s `ARCHITECTURE.md` + `BUILD` docs + `SPRINT` accurate as we build — so we never again need a one-off reconciliation lane. This is **protocol + agent-manual design/editing** — NOT doc-content reconciliation (AC6 just did that), NOT runtime code. **This doc is the AC7↔AC0 mailbox** — append replies as `## ═══ AC7 → AC0 · date ═══` banners before the canary; commit by explicit path (mailbox/brief commits pre-authorized).
>
> *Authored 2026-07-09 by AC0, from AC6's completed reconciliation brief (`docs/working/2026.07.02-ac0-ac6-arch-build-reconciliation-brief.md` — its Part-B recommendation + drift evidence are your seed; AC6 self-certified that brief complete, so no AC6 re-spin was needed).*

---

## 0. Boot

1. **You are AC7.** Sign `AC7`. No live AC-to-AC channel — coordinate with AC0 through Justin or this committed brief.
2. **Load:** AC's `CLAUDE.md` + the Light cascade (`current-state.md` → Hub `roadmap.md`). Then the lane-specific set: **`docs/protocols/lock-protocol.md`** (the `#lock` checklist you'll harden), **`docs/protocols/ac-writing-protocol.md`** (doc craft + the "how to use this" header convention you'll extend), the **AC6 brief** (`docs/working/2026.07.02-ac0-ac6-arch-build-reconciliation-brief.md` — the full Part-B rec + the drift evidence + the "update-routing protocol" concept it seeded), and skim the now-reconciled **`evryn-backend/docs/ARCHITECTURE.md` + `BUILD-EVRYN-MVP.md`** (so you know what the doc-currency header attaches to). **You do NOT need to re-read all of `src/`** — this is protocol/manual work, not runtime work. Also glance at the **team `#lock` / lock-protocol** in `evryn-team-workspace/shared/protocols/` (Soren/Mira manuals live in that repo).
3. **Verify branch before every edit; commit only on Justin's explicit per-change go** (mailbox/brief/changelog/ADR pre-authorized); stage by explicit file path (shared repos).

---

## 1. Why this lane exists (internalize the framing — Justin is specific here)

ARCH/BUILD are *supposed* to be kept current as we build, but it's been hit-or-miss with no enforcement and no "which change goes to which doc section" map — so drift accumulated until AC6 needed a dedicated reconciliation. **A one-off fix won't hold; the fix has to be process-structural and LAYERED** (multiple independent reminders, because no single one reliably fires — that's the whole history).

**Frame the GOAL correctly (Justin, 2026-07-09 — do not distort this):** the point of accurate docs is **(1) accuracy, (2) shorter/de-duplicated, (3) docs and runtime AGREE — cutting the cognition-tax an AC pays reconciling intent-vs-reality.** ⚠️ It is **NOT** "now ACs can read the docs and skip the runtime." Knowing *both* what we intend AND what the runtime *actually is*, at a granular level, stays load-bearing — skipping `src/` because "the docs are accurate now" has almost always burned us. **The docs are a trustworthy on-ramp, not a substitute.** Your process fix keeps them trustworthy; it must not encourage anyone to treat them as a runtime-replacement. (If any doc-header wording you write could be read as "you can skip the runtime now," reword it.)

---

## 2. The plan — Part A is already owned by AC0; Part B is YOUR mandate

**Part A (AC0 owns it, effective NOW — you do NOT do this):** AC0 keeps ARCH/BUILD/SPRINT current at each `#lock` (or explicitly routes the update — names the section + owner). Standing responsibility, already in force. Named here so you know the immediate-term is covered while you build the durable Part B.

**Part B (YOUR mandate — the durable, layered fix). Four components, and the redundancy across them IS the point** (layered triggers = far better odds one fires before drift festers):

1. **A hard, non-skippable doc-currency GATE in the `#lock` protocol.** Not a soft "remember to…" — a checklist item that must be *answered*: *"Did this session change what ARCH/BUILD/SPRINT claim about the system? For each: update it now, or route it (name the section + owner). #lock isn't complete until this is answered."* (Home: `docs/protocols/lock-protocol.md`, and mirror into the team lock-protocol if the team #locks touch the same docs.)

2. **Finalize + adopt the "update-routing protocol"** — the *"a change of type X → ARCH §_, BUILD §_, SPRINT step _, ADR _, changelog _"* mapping. AC6 conceived it in the reconciliation lane and meant the Delta-Fold to test it — **pull whatever AC6 captured of it and finalize it into a real reference doc.** This is what turns "keep docs current" from a vague ask into a **lookup** — it kills the "I didn't know where it goes" failure and makes the update mechanical. The `#lock` gate (component 1) points at this.

3. **A "how this doc stays accurate" header on ARCH + BUILD** (Justin's idea): a short standing note at the top — *"kept current at #lock by whoever ships work that changes what it describes; if you touch runtime this doc covers, update it in the same pass or route it per the update-routing protocol."* Puts the reminder at the point of editing. (⚠️ per §1 — word it so it can't be read as "so you can skip the runtime.")

4. **A standing beat in the agent manuals most likely to change what the docs describe** — **AC's `CLAUDE.md`, Soren's `soren.md`, Mira's `mira.md`** (AC = build/arch; Soren = arch owner-of-record; Mira = identity-as-runtime): *"when your work changes system behavior, updating the arch/build docs (or routing the update per the routing protocol) is part of 'done,' not a follow-up."*

---

## 3. Your seed — the drift evidence (WHY the routing protocol needs these change-types)

From AC6's reconciliation (the pattern the fix must make mechanical: **work shipped, the doc badge/section never got flipped**). Use these as the concrete cases the update-routing protocol must cover — each one is a *change-type that failed to reach a doc-home*:

- **A feature ships → a stale "blocker/NOT-wired" badge lingers.** BUILD Phase-0e read "emergency channel NOT wired / Mark-live **blocker**" — FALSE after M1 shipped (`v0.2.2`, `notifyEmergency` fires in 5 places). **This is the one that would've caused duplicate build work.** (Routing rule: a shipped subsystem → flip its BUILD phase badge + its ARCH status.)
- **A feature ships → a "NOT STARTED" badge lingers.** BUILD Phase-1e "dashboard NOT STARTED" — the product-vitals tab shipped 2026-06-16.
- **A table/mechanism is renamed in the runtime → the ARCH inventory/prose keeps the old name.** `story_history` → `story_versions`; the `haiku_screen_shadow` operational-tables row was missing after `v0.2.6`.
- **A table is dropped → refs linger.** `notify_queue` references survived the drop.
- **Design tense drifts** ("in flight" vs "shipped `v0.2.2`" for Reflection).

The meta-lesson AC6 proved: a *recent-window* fidelity check can't catch **long-festering** drift (something built months ago a doc never caught up to) — only a full-history walk found the Phase-0e badge. Your routing protocol + `#lock` gate are the *prevention* so a full-history reconciliation is never needed again.

---

## 4. Scope + guardrails

- **IN:** the `#lock` protocol, a new/finalized update-routing protocol doc, the ARCH+BUILD doc-currency headers, and the AC/Soren/Mira manual beats. **OUT:** runtime code; doc-*content* reconciliation (AC6 did it, it's on `main`); the **MVP→v0.2 rename is DECIDED AGAINST** (Justin, 2026-07-09 — not in scope).
- **These are source-of-truth + cross-repo edits** (`CLAUDE.md`, protocol docs, `soren.md`, `mira.md`) → **each needs Justin's explicit approval before you edit it** (propose → get his yes → edit). **Soren = owner-of-record for ARCH/BUILD** and the manuals are the team's — keep Soren/Mira informed of substantive changes (coordinate via AC0/Justin; you act as an extension of the arch role with Justin's authorization, not around Soren).
- **This is real design + cross-repo protocol/manual work → a focused session, not side-of-desk.** Describe-then-pause before editing SOT docs; bounded passes; commit per Justin's per-change go.

---

## 5. Operating rules (the standing set)

- **PING `#team-alerts` EVERY response** (Node `fetch` to `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`, prefix `AC7:`; concise attention-tap, substance in chat). Even when Justin seems present.
- **Justin reads ONLY the chat, comes in cold** — CEO-altitude, concept-first, plain English, never jargon-by-number.
- **AC0 = peer / collision-map**, via this brief. **`AskUserQuestion` is BANNED** (hangs).
- **Sequencing:** the reconciliation already shipped to `main` (`a4f6bb4`, 2026-07-09), so you're unblocked whenever Justin spins you. Nothing gates you.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
