# `docs/working/` audit — the execution plan for AC0-37f

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file: this is an EXECUTION PLAN, not a report.** You are AC0-37f. You were spun to close out one job: get `_evryn-meta/docs/working/` down to what is actually live, without losing anything. Work through the steps in order — they are sequenced by dependency, not by what was found first. **Steps 1 and 2 gate everything else; do not start moving files before both are done.**
>
> **What belongs here:** the audit's findings, the sequenced work, and the open questions. **What does NOT belong here:** live session state (that is `docs/current-state.md`) or the narrative of how the audit ran (that is the meta `CHANGELOG.md`, 2026-08-18).
>
> **Authored 2026-08-18 by ACw**, a read-only sub-AC under AC0-37e, from a full product-cascade load plus the opening banner of every file in `docs/working/`. **ACw wrote nothing but this file and moved nothing.**
>
> **Every count and SHA below is a hypothesis with a timestamp on it.** `_evryn-meta` moved three commits during the session that produced this. **Re-derive before you act.**

---

## Section 0 — THE FRAMING: this audit has been run before. Reconcile the two; do not pick one.

**`docs/working/2026.08.04-ach-justin-housekeeping-recommendations.md` is a prior sweep of this same folder**, run 2026-08-04 by ACh (a fully-loaded sub-AC) with **Justin's ballot answers already incorporated**. Its Part 2 is an eleven-item queue, P1 through P11. The 2026-08-06 handoff records **"P1 is now DONE. The rest stand."** That was twelve days before this file was written.

**P2 through P11 appear in no sprint Step, no ADR, and no current-state body entry.** *(Instrument: `grep -c` for `P1` / `P5` / `P11` against `evryn-backend/docs/SPRINT-V0.2-HARDENING.md`, `_evryn-meta/docs/current-state.md` and `_evryn-meta/CHANGELOG.md` — no substantive hits. A grep establishes presence, not absence; a P-item tracked under different wording would not show up.)*

> ### Justin's framing, and it is reassuring rather than alarming
>
> **The two reports are COMPLEMENTARY, not competing.** A great many files have been created since ACh ran. **ACw's audit covers the roughly 25 newer files; ACh's covers the older set in more depth and already has Justin's rulings attached.**
>
> **So your first move is to COMPARE the two, then execute the reconciled list.** Do not re-derive what ACh already derived, and do not discard ACw's coverage of the newer files.

**Two of ACh's items are this task's hardest bucket, already worked out in full:**

- **P2** reproduces the six un-rehomed items from the AC3 identity-ops brief *"so you never open it,"* and ends with: **"Do not retire this brief until all six are placed — retiring it is what makes them invisible."**
- **P3** covers the Step-78 health design doc, records that **shipped runtime code cites it by path**, reproduces the load-bearing rule so the retirement is not blocked on re-reading it, and prescribes one change that does extract, repoint and retire together.

---

## STEP 1 — The three things that must be true before ANY file moves

**All three are about inbound references. Skip them and this sweep manufactures dangling pointers instead of removing clutter.**

### 1a. Shipped runtime source cites a file in `docs/working/`

- **What to do:** before retiring `2026.07.15-ac3-step78-real-health-design.md`, extract its content to its durable home and repoint the code comment.
- **What to check first:** open `evryn-backend/src/safety/liveness.ts` and confirm the citation is still there and still by path. *(ACw located it with `grep -rl` on the basename across all repos and did NOT open the file. Confirm at source before acting.)*
- **What is lost if skipped:** a live reference in production source points at a file that is no longer where it says. ACh's P3 states the same rule: **doing the retirement without the repoint breaks shipped code.**

### 1b. A previous retirement already created a dangling pointer, and nobody caught it

- **What to do:** treat this as the worked example of the failure mode, and fix it while you are here.
- **The instance:** the **live** watch-list — `evryn-team-workspace/shared/projects/product/watch-list.md`, section 2 (Cache-temperature measurement), owner Soren — cites `_evryn-meta/docs/working/2026.06.17-ac3-cache-measurement-plan.md`. **That file is not in `docs/working/`.** It is in `docs/sessions/historical/2026.06/`. *(Instrument: `ls` on the working path returned no such file; `find` located it in historical. Both direct.)*
- **What is lost if skipped:** nothing new — but it proves the risk is real rather than theoretical, and the watch-list is a doc Soren owns, so **the repoint is a route-to-owner, not an AC edit.**

### 1c. Repoint before you move, for every file you retire

- **What to do:** for each candidate, run the reference map (method in Step 6) and repoint every **live** referrer.
- **What to check first:** whether each referrer is live or historical. The dividing line is `docs/sessions/historical/**`, `docs/historical/**`, and the archived changelogs. **Do not repoint those** — they are frozen records, and a corrected path inside one is a falsification.
- **What is lost if skipped:** roughly fifty new dangling pointers, each of which reads to a future agent exactly like the watch-list one — a confident citation of a file that is not there.

---

## STEP 2 — Reconcile the two audits before touching the buckets

- **What to do:** read `2026.08.04-ach-justin-housekeeping-recommendations.md` Part 2 in full (P1 through P11), then read Steps 3 and 4 of this file. Produce one merged work-list.
- **What to check first:** **P-item status.** ACw verified only that P1 was marked done as of 2026-08-06 and that P2 through P11 are untracked elsewhere. **ACw did NOT verify whether any of P2 through P11 have been discharged in the twelve days since.** Several may be closed. Check each against the sprint and the changelog before re-executing it.
- **What is lost if skipped:** you either redo ACh's work, or you retire the AC3 brief and the Step-78 design doc without the extractions ACh already specified — which is the exact failure ACh's own P2 warns about.

---

## STEP 3 — The four buckets: all 53 files

**Counts at time of writing: 53 files, 16,736 lines.** *(This file makes 54.)*

### Bucket 4 — NOT AC0's to retire (ACf / team-runtime lineage). 21 files.

**Identify and stop.** ACf actively retires its own work — **19 files already sit in `docs/sessions/historical/2026.08/`** — so what remains here is presumptively deliberate.

| File | Note |
|---|---|
| `2026.08.06-acf-acf-handoff.md` | Self-declares SUPERSEDED. Its pointer to the 08-10 ACf handoff is dangling — that file is now in `docs/sessions/historical/2026.08/`. |
| `2026.08.06-acf-acfw-wake-outage-brief.md` | |
| `2026.08.10-acf-ac0-dashboard-status-pill-note.md` | Its own text hands it to AC0 and says it can be deleted; its content shipped as SPRINT Step 107. See ballot item 2. |
| `2026.08.10-acfm-acf-lane-setdown.md` | |
| `2026.08.11-acf-acf-handoff.md` | Cited by `evryn-team-workspace/shared/protocols/runtime-ops.md` — Justin's file. |
| `2026.08.11-acf-acfd-dormancy-scheduler-lane-brief.md` | |
| `2026.08.11-acf-acfl-learnings-consolidation-lane-brief.md` | |
| `2026.08.11-acf-acfs-session-question-lane-brief.md` | |
| `2026.08.11-acf-sealedexec-sweep-lane-brief.md` | Lane is briefed and unspun per current-state. |
| `2026.08.11-acfm-acf-runtime-questions.md` | Cited by current-state. |
| `2026.08.12-acf-acf-handoff.md` | |
| `2026.08.12-acf13-ac0-agent-patterns-postmerge-brief.md` | Addressed to AC0; status table maintained by AC0-37e; two of its items are Justin's. |
| `2026.08.12-acf13-ac035-agent-patterns-reply.md` | Both notes say delete once served. See ballot item 2. |
| `2026.08.12-acf13-ac035-merge-coordination.md` | Closed channel. |
| `2026.08.13-acf13-acf15-handoff.md` | Self-declares SUPERSEDED. |
| `2026.08.17-acf13-acf15-handoff.md` | The live ACf handoff. |
| `2026.08.17-acf13-acfi-guard-integrity.md` | Live lane doc, briefed and unspun. |
| `2026.08.17-acf13-acfj-money-layer.md` | Live lane doc, briefed and unspun. |
| `2026.08.17-acf13-acft-thread-record.md` | Live lane doc, briefed and unspun. |
| `2026.08.17-acf13-acfv-thread-creation.md` | Live lane doc, briefed and unspun. |
| `2026.08.17-acf13-lane-protocol.md` | Binding on the four lane docs above. |

### Bucket 3 — KEEP, still live. 12 files.

| File | Why it is live |
|---|---|
| `ac-mailbox.md` | Standing AC-to-AC channel; watchers are armed against this exact path. Living doc, no date prefix by design. |
| `2026.08.18-ac037e-successor-handoff.md` | The live AC0 handoff. |
| `2026.08.18-ac037e-justin-cutover-redirect-drafts.md` | Untracked; was awaiting Justin's vet. **Re-check: the cutover ran on 2026-08-18, so this may now be discharged.** |
| `2026.08.17-ac037b-ac038-marching-orders.md` | AC0-38 not yet spun; its section 3 seven lanes are the live build queue. |
| `2026.07.28-acp-ac0-desktop-permissions-fix.md` | current-state names it an **open question, owner AC0**, with a do-not-apply-as-written banner. |
| `2026.08.12-qc-sweep-findings.md` | `docs/protocols/load-cascades/product-full-cascade.md` step 6 makes it a **required pre-read before trusting any Code Atlas claim.** |
| `2026.07.19-ac-justin-restructure-napkin.md` | current-state: not settled, to be re-derived from first principles for the `classify.ts` extraction, which has not run. |
| `2026.08.04-ach-justin-housekeeping-recommendations.md` | **P2 through P11 unexecuted. This is a live work-list and it is your Step 2 input.** |
| `2026.08.11-ac0-acm-loading-architecture-brief.md` | Section 11.3 is the cutover order; cited by ADR-057. **Re-check now that the cutover has run.** |
| `2026.07.22-ac0-ac2-fast-follow-brief.md` | Its section 9 holds the nested note-turn relay rebuild recipe, gated to the `CLUSTERING_MODE` flip. SPRINT Step 94 cites it. |
| `2026.08.04-ac0-aca-atlas-refactor-brief.md` | Cited by `_evryn-meta/.claude/agents/ac.md` (the Atlas standing order). The extraction deliverable is unbuilt. |
| `2026.08.17-ac037c-ac037d-handoff.md` | Provisional. See ballot item 1. |

### Bucket 1 — RETIRE clean to `docs/sessions/historical/2026.08/`. 3 files.

| File | Why it is safe |
|---|---|
| `2026.08.11-ac035-acf-agent-patterns-note.md` | AC0-authored. ACf's reply confirms the collision never happened; both notes say delete once served. |
| `2026.08.17-ac037c-acf-cross-runtime-halt-signal-note.md` | `OVER AND OUT` from both sides. The live successor handoff section 6 already states it wants retiring here, and it was held only for lack of Justin's explicit OK. |
| `docs/sessions/historical/2026.08/2026.08.13-ac037-ac038-marching-orders.md` | Wholly superseded by `2026.08.17-ac037b-ac038-marching-orders.md`, which reconciles it explicitly in its section 0. |

### Bucket 2 — RETIRE, BUT IT HOLDS SOMETHING FIRST. 17 files.

`2026.06.30-ac0-ac3-identity-ops-brief.md` · `2026.07.15-ac3-step78-real-health-design.md` · `2026.07.21-ac0-ac3-cleanup-batch-brief.md` · `2026.07.22-ac0-ac0-handoff.md` · `docs/sessions/historical/2026.08/2026.08.04-ac0-ac0-handoff.md` · `2026.08.04-ac0-ac5-silent-email-drop-brief.md` · `docs/sessions/historical/2026.08/2026.08.04-ac0-ach-housekeeping-brief.md` · `docs/sessions/historical/2026.08/2026.08.05-ac0-ac0-handoff.md` · `2026.08.06-ac0-ac0-handoff.md` · `docs/sessions/historical/2026.08/2026.08.10-ac0-ac0-handoff.md` · `docs/sessions/historical/2026.08/2026.08.10-ac0-ac0-overnight-handoff.md` · `docs/sessions/historical/2026.08/2026.08.10-ac0-acn-safety-net-brief.md` · `2026.08.11-ac0-ac0-handoff.md` · `docs/sessions/historical/2026.08/2026.08.11-ac0-acb-sprint-current-state-brief.md` · `docs/sessions/historical/2026.08/2026.08.13-ac0-ac0-handoff.md` · `docs/sessions/historical/2026.08/2026.08.13-ac037-ac037b-handoff.md` · `docs/sessions/historical/2026.08/2026.08.17-ac037b-ac037c-handoff.md`

**Five are expanded in Step 4 and were verified at source. The other twelve are the AC0 handoff chain and closed lane briefs** — each carries a fragment sweep or a "do not retire, it still holds X" claim naming its own residue. **ACw did NOT verify those twelve individually.** They sit in bucket 2 rather than bucket 1 deliberately: an over-confident RETIRE is the expensive error here, because the file is the thing that would have told you.

---

## STEP 4 — Bucket 2 expanded: what is lost, where it goes, what breadcrumb

### 4a. `2026.07.22-ac0-ac0-handoff.md` — Emma's go-live burn gate

- **What is lost:** Marlowe's 2026-07-29 appendage carries Emma's runway table — dormant about 4.9 months; warm cache about 1.4 months; **cold cache about 16 days** — and her conclusion, verbatim: *"the go-live decision for a gatekeeper is a burn decision, not just a build decision — and nobody has been treating it as one,"* with the corollary that **the cache-temperature measurement should GATE go-live.**
- **Status as of 2026-08-18 night: PARTIALLY discharged.** AC0-37e recorded it in `docs/current-state.md`'s **BULLETIN**. ⚠️ **That is a queue entry, not a gate** — the bulletin is drained at every `#lock`, and its own rules make an entry more than seven days old overdue by definition. **The finding is now safe; the gate is not yet standing.**
- **Where it should go:** current-state's **Gates and cross-project blockers** section as a standing gate, and/or the watch-list section 2's *"Decision it feeds"* line — which today frames that measurement as feeding SPRINT Step 70 (the epithelial restructure) and the cost spread, **not** as a go/no-go on turning the gatekeeper on.
- **Breadcrumb:** SPRINT Step 69 and watch-list section 2 point at the gate entry.
- **A correction to the record, worth carrying:** the 2026-08-05 handoff says Soren's v0.3 design batch *"has NOT been rehomed."* **Soren's own section in the 07-22 handoff says he DID persist it** — into ARCHITECTURE, BUILD-EVRYN-v0.3, SPRINT Step 95, the operator guide and ADR footnotes — with a **deliberate** exclusion list (outreach economics, Publisher-as-approver as a decision, the bulk-import project, anything in the identity files). What is genuinely unhomed is his six-item residual, of which **items 1, 2, 3 and 6 are now closed** (SPRINT Steps 96, 95, 102; Nathan's memory file). **Item 4 is open:** the dedicated-alias-is-not-a-working-address rule is **absent from `evryn-backend/docs/gatekeeper-onboarding.md`** *(instrument: `grep -in "alias"`, no hits)*.

### 4b. `2026.07.15-ac3-step78-real-health-design.md` — a live code reference

- **What is lost:** the rule that a breaker halt returns **200, not 503** — **and its reasoning.** A halt calls `stopPolling()`, so the poll clock goes stale about 15 minutes later; a 503 there plus any watchdog produces an infinite restart loop against M1's no-auto-reset invariant. **Precedence is load-bearing: halted is evaluated before wedged.**
- **Where it goes:** `evryn-backend/docs/ARCHITECTURE.md`, section "Monitoring and Silent-Death Safety (M1)" — ACh's proposal in P3, and ACw agrees. The Code Atlas section 07 documents the behaviour but not this reasoning at that location.
- **Breadcrumb:** repoint the comment in `evryn-backend/src/safety/liveness.ts` at the ARCHITECTURE section.
- **Sequencing:** this is Step 1a. **One change does all three: extract, repoint, retire.**

### 4c. `2026.06.30-ac0-ac3-identity-ops-brief.md` — six un-rehomed items

- **What is lost:** ACh's P2 enumerates them — one **already-Justin-approved-but-never-written** QC pattern, two QC patterns pending Justin's approval, a `CLAUDE.md` rule proposal (Justin's own clean-up-as-we-go ruling), and an orchestration lesson (line counts drift mid-session; count close to spin time, and tell the subagent to trust its own read over the brief's number and flag the delta).
- **Where they go — RESOLVED, and the answer is good news.** ACw flagged that the stated destination looked gone. **The cutover ran on 2026-08-18: `evryn-dev-workspace`, `evryn-quality` and `evryn-ops` are retired to 50-line redirect stubs, and `_evryn-meta/.claude/agents/dc.md`, `qc.md` and `oc.md` are now the single home.** The **"Patterns This Role Watches For"** section still exists — **in `_evryn-meta/.claude/agents/qc.md`, at line 186** *(verified by reading it)*. **Send the three QC patterns there.**
- **Match the house shape.** That section states it explicitly: each entry is **Trigger** (the change-shape, scoped to the context it lives in), **Failure** (one line — what silently goes wrong), **Move** (a concrete review action a fresh QC can *do*), **Not** (only where the pattern is easy to over-apply), and **Seen Nx** (a recurrence count plus one concrete example in a clause). **No build-state breadcrumbs** — no step numbers, dates or ticket IDs; they go stale within weeks and a fresh QC cannot act on them. **Justin approves before an AC writes them in.** ⚠️ **Per the 2026-08-11 routing ruling, a `qc.md` change logs to the META changelog.**
- **Breadcrumb:** none needed once placed. **ACh's P2 already reproduces all six**, so the brief itself can retire as soon as they land.

### 4d. `docs/sessions/historical/2026.08/2026.08.17-ac037b-ac037c-handoff.md` — four items verified stranded

ACw checked each against the live successor handoff and the durable docs:

- **`docs/protocols/ac-writing-protocol.md` is in ZERO cascades.** Justin approved its placement in the Light cascade on 2026-08-12. *(Instrument: `grep -rn "ac-writing-protocol"` across `.claude/agents/ac.md` and all `docs/protocols/load-cascades/*.md` — every hit is a prose reference; **no cascade step.**)* It is mandatory before writing any doc, and **no cascade loads it.** → **Where it goes: the Light Startup Context Cascade in `_evryn-meta/.claude/agents/ac.md`.**
- **The `--live` decision on 13 never-run suites.** Every INCONCLUSIVE verdict in `2026.08.12-qc-sweep-findings.md` turns on it. It spends real Anthropic money and writes to a real database, so it is Justin's call, and the sweep lead deliberately refused to make it or to wake him for it. **It is a decision, not an oversight — do not re-file it as a gap.** → **Ballot.**
- **Four standing QC review-patterns, proposed and unapproved** (sweep findings section 6). The sharpest of the four: *a documentation claim about a whitespace- or delimiter-sensitive literal, made without reading the bytes.* → **Same destination as 4c: `qc.md`, after Justin's yes.**
- **The current-state hijack-guard header block — drafted, NOT applied.** *(Instrument: `grep -c "three destinations"` against `docs/current-state.md` returned **0**.)* It makes the three-way routing explicit: a snapshot goes in the body, history goes to the CHANGELOG, a warning goes to the BULLETIN. → **`docs/current-state.md`. Tier 2 — needs Justin's explicit go.**
- **Confirmed carried and safe, so you do not re-do them:** the three ADRs are written (`docs/decisions/058-compacted-agent-stops.md` exists); three of the drafted learnings are in `LEARNINGS.md`.

### 4e. `docs/sessions/historical/2026.08/2026.08.04-ac0-ach-housekeeping-brief.md`, `docs/sessions/historical/2026.08/2026.08.05-ac0-ac0-handoff.md`, `2026.08.06-ac0-ac0-handoff.md` — the P-queue's only pointers

- **What is lost:** the routing that says the P1 through P11 queue lives in the recommendations doc's Part 2, plus the 2026-08-06 status stamp *"P1 DONE, the rest stand."*
- **Where it goes:** fold the surviving P-items into `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` as Steps — **they already have ACh's write-ups, so this is a paste, not a re-derivation** — or record the queue's existence in current-state's BULLETIN.
- **What is lost if skipped:** eleven items reachable only by knowing to open a superseded handoff.

---

## STEP 5 — Retiring `AGENT_PATTERNS.md`: what must be true first

**Justin's decision:** it gets **RETIRED**, not left as a permanent redirect. His words: *"bite the bullet and make sure there are zero unfixed references and retire it."*

> ### The ordering trap, and it is real
>
> **Retiring the pointer is only safe AFTER every inbound reference is fixed.** The staging brief states this. **Tonight's edits cleared several, so the count must be RE-MEASURED, not assumed.**

**Measured 2026-08-18 by ACw: 82 files across seven repos contain the string `AGENT_PATTERNS`.** *(Instrument: `grep -rl --binary-files=text "AGENT_PATTERNS"` over `_evryn-meta`, `evryn-backend`, `evryn-dev-workspace`, `evryn-quality`, `evryn-ops`, `evryn-team-workspace` and `evryn-team-runtime`, filtered to `*.md` and `*.ts`.)*

**That 82 is NOT the work.** Most are historical or archived and **must not be touched**. Triage as follows:

- **Do not touch:** anything under `docs/sessions/historical/**`, `docs/historical/**`, the archived changelogs, and `evryn-team-workspace/shared/current-state/archive/**`. A corrected path inside a frozen record is a falsification.
- **Do not touch:** the live changelogs. A changelog entry records what was true when it was written.
- **Must be repointed — the live set ACw identified:** `_evryn-meta/.claude/agents/ac.md` · `docs/current-state.md` · `docs/protocols/ac-writing-protocol.md` · `docs/protocols/doc-update-routing-protocol.md` · `docs/protocols/lock-protocol.md` · `LEARNINGS.md` · `evryn-backend/docs/BUILD-EVRYN-MVP.md` · `evryn-backend/docs/identity-writing-bible.md` · `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` · `evryn-team-runtime/CHANGELOG.md` · `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md`.
- **Route, do not edit — these are the team's or Justin's:** `evryn-team-workspace/shared/current-state/current-state.md` · `evryn-team-workspace/shared/protocols/identity-file-review.md` · `evryn-team-workspace/shared/protocols/sweep-protocol.md`.
- 🔴 **A SECOND piece of shipped source cites it: `evryn-backend/src/db/users.ts`.** Same class as Step 1a. **Treat a source-comment repoint as a code change with a real review, not a doc tidy.**

> ### The instrument warning, and it is not a formality
>
> **A `grep` establishes PRESENCE. It cannot establish ABSENCE.** *"Zero unfixed references"* is a **completeness claim**, and that is exactly what a pattern-match cannot support. Before declaring it clean, close the known defeaters and **say that you did**:
>
> - a reference by a partial, hyphenated or reformatted form of the name;
> - a reference by section title rather than by filename;
> - file types outside the scan — ACw scanned `*.md` and `*.ts` only, **not** `*.json`, `*.sql` or `*.html`;
> - a file `grep` refuses to read at all. **This estate has already been bitten by that**: two files carried a stray control character that made them binary to `grep`, so every sweep silently skipped them *and reported success*.
>
> **When two searches disagree, stop and read.** Do not reconcile them by writing a third pattern.

---

## STEP 6 — The method, so you can re-run the reference map cheaply

**Build the map in one pass, then look up against it.** Do not loop `grep -r` once per filename — ACw tried that and it **timed out at two minutes** with no usable output.

- **One pass:** `grep -roHE` over the seven repos, restricted to `*.md` and `*.ts`, matching a working-doc name pattern; emit source-and-target pairs; drop self-references; sort unique. That produced **890 distinct pairs** in about a minute.
- **Then look up per file with `awk`, not `grep -P`.** See Step 8 — `grep -P` is broken in this environment and it fails in the most dangerous possible way.
- **Separate EXTERNAL referrers from INTERNAL ones.** A `docs/working/` file citing another `docs/working/` file usually retires along with it; **a referrer outside that folder is what gates the move.**

---

## STEP 7 — THE BALLOT. Everything here needs Justin. Put it to him in one pass.

**Gathered deliberately so you do not discover them one at a time over the course of the run.**

1. **`2026.08.17-ac037c-ac037d-handoff.md` (600 lines) — keep or retire?** Its section 13 is AC0-37e's briefing, which 37e consumed and replaced. But its **section 7 is "Justin's rulings today, persisted — these existed only in chat."** **Question: has section 7 been carried into the live successor handoff's rulings list, or is it the sole home of the 2026-08-17 rulings?** ACw did not diff them line by line.
2. **The bucket-4 boundary on three ACf-to-AC0 files.** `2026.08.10-acf-ac0-dashboard-status-pill-note.md`, `2026.08.12-acf13-ac035-agent-patterns-reply.md` and `2026.08.12-acf13-ac035-merge-coordination.md` are `acf`-named but addressed to AC0, and their own text authorises deletion. **Question: does "not AC0's to retire" turn on the FILENAME or on the LINEAGE that owns the content?** ACh's list hit this same question and deferred to Justin.
3. **The all-branches enumeration script.** The 037c handoff flagged that it prints every branch once per worktree, so a real hit drowns in copies. The script in `ac.md` was rewritten on 2026-08-18. **Question: does the per-worktree duplication still occur?** ACw did not run it. It lives in the always-loaded manual, so it wants Justin's eyes.
4. **The 2026-08-11 handoff's identity finding and Justin's ruling on it** — that `core.md` describes a loading model the runtime abandoned in May, and that **this is DELIBERATE, do not fix.** **Question: is that ruling recorded anywhere but that handoff?** A future reader who "fixes" it would be reversing a Justin ruling.
5. **Which 2026-08-13 handoff is retired?** The 037c handoff says *"pulled forward from the 2026-08-13 handoff (which is now RETIRED)"* — but **both** `docs/sessions/historical/2026.08/2026.08.13-ac0-ac0-handoff.md` and `docs/sessions/historical/2026.08/2026.08.13-ac037-ac037b-handoff.md` are still present. **Question: which one did it mean?**
6. **The `--live` decision** (Step 4d). Thirteen suites never ran during the mutation sweep; every INCONCLUSIVE verdict turns on one authorised pass. **It spends real money and writes to a real database.**
7. **The seven QC patterns** — three from ACh's P2, four from the sweep findings (Steps 4c and 4d). **One of the three is already approved and merely unwritten; it needs a writer, not a decision.** The rest need a yes.
8. **The current-state hijack-guard block** (Step 4d) — drafted, unapplied, Tier 2.
9. **Is `docs/sessions/historical/2026.08/` the right target for AC0-lineage docs?** Its 19 current occupants are almost entirely ACf-lineage. **ACw did not confirm the convention covers both lineages.**

---

## STEP 8 — ACw's honest limits. Read these before trusting any bucket.

- **ACw did not read all 53 files in full.** Every opening banner was read, plus full sections of ten files. **Buckets 1 and 4 rest on banners, filenames and the reference map — not on complete reads.** Bucket 2's five expanded entries are verified at source; **the other twelve are classified from their own do-not-retire claims, which were not individually re-verified.**
- **The reference map establishes presence, not absence.** It scanned `*.md` and `*.ts` only. A reference by partial name, by section title, or in `*.json` / `*.sql` / `*.html` would be invisible to it.
- **ACw did not verify the current status of P2 through P11** beyond the 2026-08-06 stamp. Some may have been discharged since.
- **ACw ran no tests and no builds, and touched no `src/` file.** The runtime code half was deliberately not loaded — roughly 334K tokens against a load already near its ceiling — and that was disclosed at the time and ratified. **Nothing in this plan rests on having read `src/`.**
- **ACw wrote exactly one file: this one.** No file was moved, deleted or edited, and no filesystem command is proposed anywhere in this plan.

> ### The instrument failure that must not be repeated, recorded because it nearly produced a catastrophe
>
> **ACw's first reference lookup used `grep -P`, which errors in this environment with *"-P supports only unibyte and UTF-8 locales."*** It failed on every one of 53 calls and returned **"EXTERNAL refs: 0" for all 53 files.**
>
> 🔴 **That is a FALSE ALL-CLEAR, not a finding.** Had it been trusted, the conclusion would have been that nothing anywhere references any file in this folder, and the recommendation would have been mass retirement — **including the file cited by shipped runtime source.**
>
> **It was caught only because all-zeros was absurd enough to notice.** A *slightly* wrong result would have been believed. The re-run with `awk` produced the real map.
>
> ⇒ **Two rules this paid for again, both already in `ac.md`: a command that failed to execute reads exactly like a fact about the filesystem, and an empty result is the most dangerous output a search can produce. Verify that the instrument RAN before believing what it did not find.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
