# Lane brief — `ACfg`: guard integrity

*Written 2026-08-11 by **ACf-12** for a lane to be spun by **ACf-13**. SPRINT Steps **34+45, 26, 31, 32, 52, 53, 54** in `evryn-team-runtime`.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file.** The lane's **shared surface**: `ACfg` reads it in full at the start, appends as it works, the head AC appends too. **Keep it current enough to re-spin from cold** — *"if this instance died right now, could a fresh one pick up from this file alone?"* **Commit it freely; brief-class traffic is pre-authorized.**
>
> **This brief carries SCOPE, DESIGN CONSTRAINTS and PRE-DECIDED CALLS.** The tagged `<identity>`/`<mandatory_load>`/`<receipts>` blocks are pasted verbatim by whoever spins the lane, from `ac-orchestration-protocol.md`, using the file list in §7.

---

## §0 — 🔴 HOW MUCH THE AUTHOR OF THIS BRIEF HAD ACTUALLY READ

**ACf-12 wrote this while running the conductor cascade** — as of writing it had personally read **~450 lines of `src/`, zero migrations, zero tests.** Everything else came from a full-load runtime scout and a second subagent that read all 56 test files.

**You will read the system. That makes you the better-informed party on every question of fact here.** Treat every claim — **including the line numbers** — as a **LEAD, not a fact**; re-derive what the work rests on; **say so loudly and early if any of it is wrong**; and do not defer on anything you can see and this brief's author could not. **Where the brief is silent, assume they could not see it — not that they checked.**

---

## §1 — What this lane is, and why it is not housekeeping

`evryn-team-runtime` is the dormant harness for Evryn's eight founding-team agents. **This lane is not about bugs in the runtime. It is about whether our GUARDS are real.**

**The finding that motivates it, and you should hold it front of mind the whole trip:** `tests/boot-emergency-alert.test.ts` carries a header asserting that *"EVERY test here was watched FAILING against deliberately-broken REAL code before being trusted."* **For three of its tests that cannot be true** — they never assert the thing they name, so no mutation could have turned them red.

***"Watched failing"* is the convention that DC's, QC's and AC's manuals all rest on.** Finding it written as a *claim* rather than performed as an *act* — **inside the file built to embody it** — means we do not actually know how many other guards in this repo are decorative. **Your job is to make a representative set of them real, and to close the mechanisms that let them decay.**

A sibling lane (`ACfd`, dormancy & scheduler) runs in parallel on different files. **§8 names the boundary.**

---

## §2 — The Steps

**Read each in `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md`.** Summaries here are orientation only.

### Steps 34 + 45 — do these together; 45 folds into 34's durable fix

**34 is a live landmine and a one-line deletion Justin authorized on 2026-08-06 that never landed.** `src/boot/schema.ts`'s `EXPECTED_SCHEMA` still lists `digest_lines`, a table **nothing in `src/` reads** (Step 2 re-keyed the digest and deleted every caller). That table is **scheduled for deletion** — and the moment someone applies the drop, **the next deploy dies at boot** with `BOOT REFUSED: … missing relation(s): digest_lines`, naming a table the runtime never queries.

⚠️ **The constant's own header, forty lines above the entry, states the derivation rule that entry breaks** and even warns about this exact failure for two *other* tables.

**45 is the same shape, generalized.** `boot-schema.test.ts` uses the constant as an **input**, which *incidentally* pins the 7 names it happens to use as injection keys. **The other 9 are unpinned** — and, more importantly, **nothing asserts the constant's length or contents, so ADDING a bogus entry is completely unguarded. That is precisely how 34 happened.**

⇒ **Build the durable fix the constant already documents: a test that runs its own stated `grep -rE "\.(from|rpc)\(" src` and asserts set-equality against `EXPECTED_SCHEMA`.** That closes 34, 45, and the add-a-bogus-entry direction in one guard. **Delete the `digest_lines` entry as part of it.** ⚠️ Mind the two deliberate exclusions the header names (`capability_catalog`, `capability_tiers`) and `memory_versions` — the guard must accommodate documented exclusions without becoming a hand-maintained list again.

### Step 26 — typecheck `tests/` and `scripts/`. **🔴 READ THE TRAP BEFORE YOU TOUCH `tsconfig`.**

`tsconfig.json` includes only `src/**/*`, and `npm run typecheck` is a bare `tsc --noEmit`, so **56 files are never type-checked** (55 under `tests/` plus `scripts/smoke-wake-path.ts`). `tsx` transpiles without checking, so the test run adds nothing.

> 🔴 **TWO FIXTURES MUST NOT COMPILE — THAT FAILURE *IS* THE ASSERTION** (`tests/fixtures/README.md:9-12`). `velocity-basis-omitted.ts` and `gate-reason-unnarrowed.ts` exist to prove a compile-time guard fires.
>
> **Add `tests/**/*` naively and `npm run typecheck` goes PERMANENTLY RED on both — and the natural next move is to "fix" the fixtures, which makes both compile-time guards vacuous while every suite stays green.**
>
> **⇒ Your `tsconfig.tests.json` MUST carry `"exclude": ["tests/fixtures/**"]`.**
>
> ⚠️ **And the Step's own pointer is the danger: it says to lift the product lane's proven shape (`evryn-backend`, its Step 98). That shape will NOT include this exclusion, because the product lane has no such fixtures.** Lift the shape; do not lift it verbatim.

**Expect pre-existing breakage to surface. That is the point, not a setback.** Fix what you can; route what is large.

**While you are in the fixtures:** those two guards assert only `/…|argument/i` and `/reason/i` over the compiler output — loose enough that a *different* type error would satisfy them. **Tighten to the actual codes (`/TS2554/`, `/TS2339/`); it is a two-line change.**

### Step 31 — the NUL guard cannot notice its own gap

`tests/source-no-nul-bytes.test.ts` scopes its file enumeration through a hand-maintained 3-entry `TREES` constant, **and its anti-vacuity SANITY test iterates that same constant** — so a tree missing *from* the list is invisible to both halves by construction. Its own header says *"a guard that quietly stops covering new files is worse than no guard."*

**The arithmetic decides the fix, and it is cheap:** 114 files covered, **21 uncovered — of which 20 are frozen `spikes/` artifacts and exactly ONE is live code: `scripts/smoke-wake-path.ts`.**

⭐ **And that same file is simultaneously the only live code file with NO truncation canary.** Two independent repo-wide guards, both scoped by a hand-maintained list, **both missing the same single file.** ⇒ **Add `scripts` to `TREES`, add the canary to that file, and narrow the guard's title/header so it stops over-claiming.** A dynamic-enumeration rewrite buys coverage of frozen spike files and is not worth a trip.

### Step 32 — three vacuous emergency-alert tests

`5.3b`, `5.3c` and `5.4` in `tests/boot-emergency-alert.test.ts` inject a `fetchImpl` and **never assert it was used**. The scope of three is **verified correct** — `5.2`, `5.4b` and `5.7` genuinely prove the impl was used; **do not widen.**

⚠️ **`5.4`'s `assert.ok(lines.length > 0, 'sanity: …')` LOOKS like an anti-vacuity check and is not one** — `sendEmergencyAlert` logs unconditionally *before* it reads the env var, so that assertion is satisfied by a run that never made a request.

**The minimum fix is one line each** (capture a call count, assert it is 1). **The better fix, and the one this lane exists for, is the pattern already in the repo:** give each a **positive partner** that proves the impl is reachable — `dormancy-wake.test.ts:158-168` does exactly this, and its file *documents* that its first version came back green.

**🔴 And fix the header.** It claims every test was watched failing. **Make that true, or make the header honest.** Whichever you choose, say which in your report.

### Step 52 — four `src/` modules have no test file at all

`safety/notify.ts` (40 lines), `safety/heartbeat.ts` (17), `tools/sdk-server.ts` (24), `tools/stdio-server.ts` (78). *(`src/index.ts` is source-asserted only.)*

⭐ **The argument for doing this, in one comparison:** `safety/emergency.ts` (213 lines) has a **463-line** test file. Its sibling `safety/notify.ts` — **the delivery path behind the runtime-ops guarantee that *every kill/park/freeze pings Justin*** — has zero. **Both are Slack webhook senders with retry and timeout logic.** Nobody decided `notify.ts` was lower risk; **nobody decided anything.**

**Prioritise `notify.ts`, then `heartbeat.ts`.** The two MCP servers are lower stakes — cover them if the trip has room, route them if not.

### Step 53 — a one-line change to a test fake unlocks seven handlers

`tests/slack-auth.test.ts`'s Bolt fake has **`event: () => undefined`**, discarding every `app.event(...)` registration, while its `command` counterpart records them. **So every event handler in `src/slack/index.ts` is structurally untestable.**

**This BLOCKS Step 28 from ever being verified** — its two findings are `/dismiss` being ungated and the `reaction_added` wave-dismiss being **fail-OPEN** (with the operator id unset, the `&&` short-circuits and **any** user's 👋 dismisses).

⇒ **Make the fake record event handlers the way it records commands, then write the guards that become possible.** **Step 28's actual fixes are OUT of scope** — this Step makes them testable; landing them is a later trip. **Say clearly in your report which of Step 28's findings you have now proven.**

⚠️ **One more nothing guards, worth a test if it is cheap:** `wakeForThread` declares `threadKind: 'room'` for **every** Slack entry point while `subscribe()` runs only on `app_mention` — **that is what makes a DM a room-kind wake with no subscription row, which is what makes `leave_thread`'s zero-row branch reachable at all.** The *consequence* is well guarded; **the cause is not.**

### Step 54 — four cases of a proven function with unproven wiring

**This is one shape, four instances. Fix as many as fit; route the rest.**

- **(i) 🔴 `boot()` has zero coverage and its ORDER is load-bearing.** Schema-assert must precede the workspace clone and the singleton lock — *"a missing schema self-heals only by applying migrations, never by doing more work."* **Asserted in prose and nothing else. Reorder it and everything compiles, every test passes, and the L9 wedge returns.**
- **(ii) `workspace-git.test.ts:52-55` re-implements the rule it covers** — its `cfg()` helper duplicates `boot/env.ts:162`'s `commit: PUSH || COMMIT` logic, so it proves the *hook* and nothing about the env→config mapping. **Break that mapping and every test stays green while Railway — which sets `WORKSPACE_GIT_PUSH=1` and leaves `COMMIT` unset — silently stops committing memory notes.** ⭐ **Highest real-world stakes of the four.**
- **(iii) `search_messages`' injection guard is proven at the function, unproven at the call site.** Nothing asserts the handler *calls* `isSafeActorName` before interpolating into the `.or()` filter. **Delete the call and the suite stays green.**
- **(iv) `park-inert.test.ts`'s "never fans out" guard is ACCIDENTAL** — protected by an incomplete stub throwing, not by an assertion. Widen the stub for any reason and it evaporates silently.

---

## §3 — 🔴 Pre-decided calls, so you never guess at 3am

1. **NO MIGRATIONS.** Nothing here needs one. ⚠️ **If you find one does — STOP that Step, finish the others, report it. Do NOT build a lesser fix because the brief said no.** *(That exact failure happened on 2026-08-11 and is now a standing rule in DC's manual. Surfacing it is the job.)*
2. **DO NOT TOUCH the identity layer** — `runtime-ops.md`, agent definitions, agent memory. Justin's, and composer layer 1.
3. **Step 28's fixes are OUT of scope.** Step 53 unlocks them; landing them is separate.
4. **If Step 26 surfaces a large pile of pre-existing type errors:** fix the trivial ones, **route the rest with a list**, and land the config change **only if the suite and typecheck are green.** Do not leave the repo red.
5. **Priority order if you run short:** 34+45 → 32 → 31 → 53 → 52 (`notify.ts` first) → 26 → 54. **34+45 first because it is a live landmine with an authorization five days old.**
6. **🔴 STEP 26 — WHAT TO DO WHEN THE NEVER-TYPECHECKED FILES SURFACE ERRORS. Decided, because the cheapest 3am exit here is genuinely destructive.** Facing sixty errors, the tempting move is to loosen `strict` in a shared tsconfig — **which silently weakens `src/`, is nearly invisible in review, and defeats the entire purpose of the lane.**
   - **A separate `tsconfig.tests.json` ONLY. Never widen or loosen the main one. Strictness is never relaxed — not for `tests/`, not "temporarily."**
   - **Fix up to ~15 errors. Beyond that, STOP fixing, route the remainder as a written list, and land the config change only if the suite and typecheck are green.** Do not leave the repo red, and do not silently drop the Step.
   - **If the honest outcome is "the config change cannot land tonight" — that is an acceptable result.** Say so. A routed Step beats a weakened compiler.
7. **🔴 STEP 45 — THE SET-EQUALITY GUARD'S OWN GREP WILL MATCH THE PROBER, and the obvious fix recreates Step 31 inside the guard built to prevent it.** `boot/schema.ts`'s prober itself calls `db.from(target.name)`, so a naive grep finds it and the constant never equals the derived set. **🚫 DO NOT add a hardcoded exclusion list — that is precisely the hand-maintained-scope defect Step 31 is about, committed inside the fix for it.**
   - **DECIDED, and it is structural rather than a list: extract only STRING-LITERAL arguments — `db.from('agent_messages')` counts, `db.from(target.name)` does not.** A dynamic argument is by definition not a table name a static guard can pin, so excluding it is a property of the extraction, not an exception to it. **The prober falls out automatically and no list is maintained.**
   - The header's three documented non-runtime exclusions (`capability_catalog`, `capability_tiers`, `memory_versions`) still need accommodating. **If you must encode them, encode them so that a FOURTH silent exception fails the test** — the failure mode to design against is a list that quietly grows.
8. **Everything genuinely ambiguous: BATCH IT** into one numbered recommend-first list. **Keep advancing everything not blocked.**

---

## §4 — How you must work

- **You spin your own DC and QC**, per `ac-orchestration-protocol.md`, **verbatim blocks, two-trip loading, model-pinned, never tell a child where to report.** No improvising those blocks.
- **Worktree, own branch. Never merge, push, deploy, or apply a migration.**
- **🔴 Mutation-test every guard in a SANDBOX COPY** (`git worktree add --detach`), never in your build tree — **and prove the mutation applied.** These worktrees are CRLF; a reviewer here lost six mutations to line endings and nearly recorded passing tests over unbroken code. **On this lane above all others, an unverified mutation is worse than none — it manufactures exactly the false confidence you were sent to find.**
- **Do not let your QC inherit your DC's mutations.** Last lane: builder ran 19 all red; **QC ran 28 and four survived.**
- **Receipts up with an attestation** covering **call count and tool**, not just the file list.
- **Headings cap at `##`.**

---

## §5 — Definition of done

1. Each Step closed or explicitly routed with a reason.
2. `npm test` + `npx tsc --noEmit` green on your branch, **measured by you.**
3. **Every guard you write or repair mutation-verified in a sandbox**, with what you broke and how many checks went red in your report.
4. **A written answer to the question this lane exists for:** of the guards you touched, **how many were real, how many were decorative?** That number is the lane's most valuable output.
5. No migration, no identity edit, no merge/push/deploy.
6. This brief updated — current enough to re-spin from cold.

---

## §6 — Notes appended by the lane

*(`ACfg` appends here. Keep it current.)*

### ACfg-1, 2026-08-11 night — load, baseline, and four design corrections made BEFORE any build

**Baseline, measured by ACfg in `evryn-team-runtime-acfg` before spinning anything:** `npm test` = **442 pass / 0 fail**; `npx tsc --noEmit` = **exit 0**. Matches ACf-13's measurement. Worktree clean, on `acfg/guard-integrity` at `3eedd3f`, `node_modules` real (146 entries, not a junction).

**⚠️ Two corrections to the spin brief, both sent up and both accepted:**
1. **The ~25K read cap bites `docs/SPRINT-team-runtime-memory.md` too** (~135 tokens/line), not just `_evryn-meta/CLAUDE.md` and the orchestration protocol. It returns partial at ~line 158 of 298.
2. **The auto-injected `# claudeMd` copy of `_evryn-meta/CLAUDE.md` is a snapshot taken at the PARENT's session start and does not track later edits.** Two commits (`1b2741f`, `a04f5a0`) landed after the snapshot. **Read it off disk; do not trust the injection.** This holds for every subagent spun from that session.

**Canary correction, for whoever writes the next load list:** `docs/SPRINT-team-runtime-memory.md`'s canary is at line **298** (`wc -l` = 298, file ends with a newline). A subagent reported 299; that is wrong.

#### 🔴 Design correction 1 — Step 45's extraction has a trap this brief did not name, and it fires immediately

Running the constant's own documented `grep -rE "\.(from|rpc)\(" src` **naively returns `<table>`, `<fn>` and `memory_versions` — all three from comments inside `boot/schema.ts` itself.** The last is one of the three *deliberately excluded* tables, so a naive guard "discovers" it as required and inverts the header's intent exactly.

⇒ **The extraction MUST strip comments before extracting**, line comments first, then block comments. (Reversing that order is a known scar here — `tests/scheduler-members.test.ts:161-168` documents a stripper that ate whole functions.)

#### ⭐ Design correction 2 — §3.7's worry about the three documented exclusions is MOOT. Encode nothing.

With comments stripped and **string-literal arguments only**, ACfg ran the extraction: the derived set is **exactly 14 tables + 1 function**, and `EXPECTED_SCHEMA` minus `digest_lines` is **exactly the same 15**. `capability_catalog`, `capability_tiers` and `memory_versions` never appear as string-literal args in real code, so they are absent from **both** sides.

⇒ **Set-equality is exact with ZERO exclusion list.** That is strictly better than §3.7 hoped for ("if you must encode them, encode them so a FOURTH silent exception fails") — there is nothing to encode, so there is no list to grow. The string-literal rule also drops `schema.ts`'s own prober (`db.from(target.name)`) structurally, as §3.7 intended.

#### 🔴 Design correction 3 — Step 31: invert the scope to an EXCLUSION list. §2's rejected option was not the only third option.

§2 proposed `TREES += scripts` + narrow the title, and rejected "dynamic enumeration" as buying only frozen spike files. **There is a cheaper option it did not consider:** enumerate all tracked files via `git ls-files`, filter by extension, and carry **one documented exclusion — `spikes/`**.

**The point is not coverage; it is the DIRECTION OF THE FAILURE.** Under `TREES`, a new directory is **silently uncovered**. Under an exclusion list, a new directory is **automatically covered**, and coverage can only be lost by adding an exclusion *deliberately, in the diff, where a reviewer sees it.* That is this Step's own lesson applied to itself. Fallback if it misbehaves: `TREES += scripts`.

#### Design correction 4 — the identity half IS on DC's and QC's load lists, and it was affordable

ACf-13 deliberately omitted the identity half from `ACfg`'s own load (a knowing call, to stay clear of compaction). **But DC's and QC's manuals both mandate both halves for an agentic target, and the orchestration protocol's loophole guard says `#cascade-override` "may never cause you to load only half of one."** ACfg measured it rather than guessing: reading `src/composer/layers.ts`, the live composed set for the one configured agent (`ACTIVE_AGENTS = ['lucas']`) is **`evryn-team-workspace/CLAUDE.md` (398) + `shared/protocols/runtime-ops.md` (76) + `.claude/agents/lucas.md` (201) + `.claude/agent-memory/lucas/MEMORY.md` (167) + the Hub (196)` ≈ 1,038 lines.** Trivially affordable for a scoped build list. **Included.** The seven non-active agent definitions (972 lines) are NOT composed today and were excluded; team `current-state` (composer layer 3) was excluded as a *state* snapshot carrying no instruction bearing on guard integrity. Both exclusions are disclosed rather than silent.

**And it paid for itself immediately:** `runtime-ops.md:38` reads *"Every kill pings Justin."* That is a **layer-1 promise composed into every wake**, and `src/safety/notify.ts` — which has **zero tests** — is its only delivery path. That is Step 52's stake, verified at source rather than asserted.

#### Confirmations and observations ACfg can make that a light-loaded coordinator could not

- **SPRINT Step 33a's contradiction is REAL — independently re-confirmed by ACfg holding both halves.** `src/tools/tools.ts:370` tells the agent the scratchpad-headline display *"is still being built"*; `runtime-ops.md:25` (composer **layer 1**) tells it the headline *"is the one line other rooms see beside that thread's digest"* — i.e. asserts the display exists. Two layers of one composed prompt, opposite claims. **Step 33 is out of ACfg's scope and was NOT touched.** Routed, not fixed.
- **`runtime-ops.md` carries four items Justin raised on 2026-08-11 that are still open** (lines 9): `subscribe`/`member` used near-interchangeably · §4a's *"you never need to message yourself"* imprecision · memory-vs-project-doc-vs-thread guidance · **leaving a room is silent and he wants it loud** (= SPRINT Steps 49/55). Not this lane's; recorded so the next reader does not re-derive them.
- **Multi-agent state noticed in `_evryn-meta`:** `docs/working/2026.08.11-ac0-ac0-handoff.md` was modified-uncommitted by another agent when ACfg started. **ACfg did not touch or stage it** (AC0 committed it itself as `8cdff5d` shortly after); all ACfg commits carry an explicit pathspec on the commit itself, and ACfg verified post-commit that only its own file landed.

#### 🔴 NEW FINDING, security-shaped — the two modules with ZERO tests are exactly the two carrying a defect their well-tested sibling documents and defends against

*(Found by ACfg on 2026-08-11 by holding `safety/notify.ts`, `safety/heartbeat.ts` and `safety/emergency.ts` together. Verified at source; `grep` used only to check whether the class was wider, then each site read.)*

**`src/safety/emergency.ts:137-146` carries an explicit comment:** *"⚠ DO NOT stringify `err` here. A fetch failure's message can embed the request URL, which would put the webhook secret straight into the Railway log."* It therefore logs only `err.name`.

**Its two siblings do exactly what that comment forbids:**
- **`src/safety/notify.ts:31`** — `` console.warn(`[notify] webhook failed (attempt ${attempt}): ${String(err)}`) ``, where the URL is `config.teamAlertsWebhookUrl` (a Slack webhook secret).
- **`src/safety/heartbeat.ts:12`** — `` console.warn(`[heartbeat] ping failed (non-fatal): ${String(err)}`) ``, where the URL is `config.healthcheckUrl` (a Healthchecks.io ping URL — whoever holds it can forge heartbeats and suppress the dead-man's switch).

**These are the ONLY two files in `src/` that fetch a webhook besides `emergency.ts`** — so the class is exactly co-extensive with the untested set. **Nobody decided these were lower risk; the coverage went where a build lane happened to look, and the defect survived precisely where nobody did.** That is this lane's thesis arriving as a live specimen, and it is also a fresh instance of the *"a safety fix closed in one runtime/module doesn't reach the others"* class that SPRINT Step 37 exists to make mechanical.

**Calibration, so nobody over- or under-reacts:** it is **not a breach** — Railway logs are not public — and it fires only on a fetch *rejection* (network fault/timeout), not on a non-ok status. But `notify.ts` is the delivery path for *"every kill pings Justin"*, so it fires **precisely during incidents**, and heartbeat failures on a flaky network are routine. Defense in depth; a two-minute fix.

⇒ **DECIDED: fix both, each as its OWN commit, and pin each with a test** — not routed. The route-vs-fix rule bars folding an adjacent bug into a reviewed diff; it explicitly does **not** mean deferring the trivial, and here the alternative is worse than deferral: Step 52 has us writing the first-ever tests for these two files, and **a test written against the current behavior would codify the leak as correct.**

#### Second, smaller defect in the same file — the backoff sleeps after the FINAL attempt

`notify.ts:20-35`: the `await new Promise(r => setTimeout(r, 500 * 2 ** attempt))` sits at the end of **every** iteration including the third, so a total failure burns **1s + 2s + 4s = 7s**, of which the last 4s buys nothing — it happens *after* the final attempt, immediately before `ULTIMATE FAILURE` is logged. `notify` is `await`ed on the boot path (`boot/index.ts`) and once per stuck agent in `resetStuckState`, so with a dead webhook a startup recovery pays 7s per agent for nothing. **The same author reached the opposite conclusion in `emergency.ts` deliberately** (*"~3.5s of backoff before `process.exit(1)` buys nothing and only risks the exit"*) — `notify.ts` simply predates that reasoning. One-line fix: skip the sleep on the last attempt.

---

## §7 — The load list

*(Filled by `ACf-13` at spin time from the runtime scout's per-lane list, recorded in the ACf-13 handoff. **Every `.ts`/`.sql` file carries a bottom canary except `src/wakes/memory-writer.ts`** — for that one, "full" means the true final line.)*

### 🔴 Canary carve-outs, corrected by ACfg against the files themselves — the note above is incomplete

Four files on this lane's surface carry **no canary**, not one: **`tsconfig.json`, `package.json`, `.env.example`** (plain config/JSON, none by nature) and **`scripts/smoke-wake-path.ts`** (true final line **130**). `src/wakes/memory-writer.ts` is a fifth but is off this lane's territory. **A load list that marks any of these `(full — N lines; confirm the canary)` hands the agent an unsatisfiable instruction** — annotate the carve-out instead.

### The list ACfg actually handed DC (and QC), reproduced so a cold re-spin needs no re-derivation

Delivered as a `#cascade-override` — *"the target is `evryn-team-runtime`, a separate codebase; the product Hub/ARCHITECTURE/BUILD is not the system under change; this swaps in the target's complete load, BOTH halves."*

- **Own manual + craft:** `evryn-dev-workspace/CLAUDE.md` (312) · `_evryn-meta/docs/protocols/ac-writing-protocol.md` (100). *(QC's variant substitutes `evryn-quality/CLAUDE.md`, 502.)*
- **Intended shape:** `evryn-team-runtime/docs/ARCHITECTURE.md` (106) · `docs/BUILD-PHASE-1.md` (103) · `docs/SPRINT-team-runtime-memory.md` (**298 — exceeds the read cap; must be paged**) · `_evryn-meta/docs/working/2026.08.11-acfc3t-acf-test-coverage-map.md` (331).
- **Identity half (composer's live set for `lucas`):** `evryn-team-workspace/CLAUDE.md` (398) · `shared/protocols/runtime-ops.md` (76) · `_evryn-meta/docs/hub/roadmap.md` (196) · `.claude/agents/lucas.md` (201) · `.claude/agent-memory/lucas/MEMORY.md` (167). ⚠️ **DC's manual says "do NOT read other repos' CLAUDE.md" — that general line is superseded here by its own newer two-halves rule. Say so in the brief or DC stalls on it.**
- **Code half:** `src/index.ts` (31) · `boot/{schema 220, index 161, env 197, workspace 395, recovery 84, singleton 210}` · `safety/{emergency 212, notify 39, heartbeat 16, loop-signature 106, kill 346}` · `slack/{index 406, auth 29}` · `tools/{tools 704, sdk-server 23, stdio-server 77}` · `config/{agents 59, skills 118}` · `db/{client 27, types 253}`.
- **Config + scripts:** `tsconfig.json` (20) · `package.json` (33) · `.env.example` (71) · `scripts/smoke-wake-path.ts` (130) — **all four canary-exempt, see above**.
- **Tests (this lane's territory only):** `boot-emergency-alert` (462) · `boot-schema` (218) · `boot-schema-integration` (92) · `boot-env` (141) · `boot-paused-default` (74) · `singleton` (225) · `workspace-git` (462) · `slack-auth` (124) · `slack-classify` (60) · `source-no-nul-bytes` (128) · `env-template-drift` (178) · `skills` (128) · `tools-sanitize` (30) · `fixtures/{README 40, gate-reason-narrowed 10, gate-reason-unnarrowed 15, velocity-basis-control 7, velocity-basis-omitted 14}`.
- **Migrations:** `008_digest_lines` (34) · `016_thread_digests` (159) · `017_message_processing` (144) · `018_thread_dormancy_writes` (73).
- **Deliberately OFF the list, with reasons:** `tests/ceiling-db.ts` + `tests/fake-db.ts` — checked by grep; **no file on this lane's territory imports either** (all nine consumers are the sibling lane's). `src/db/paginate.ts`, `src/scheduler/`, `src/digest/`, `src/wakes/`, `src/threads/`, `src/money/`, `src/composer/` — the sibling lane's territory, and nothing in these Steps reaches them.
- **The lane brief itself goes in `<task>` (trip 2), NEVER in `<mandatory_load>`** — it *is* the task, and its filename alone announces the lane. Per the orchestration protocol's "THE LANE BRIEF IS TRIP-2 READING" rule.

---

## §8 — 🔴 The territory boundary with the sibling lane

**`ACfd` (dormancy & scheduler) runs in parallel in its own worktree.** It owns: `src/scheduler/index.ts`, `src/digest/index.ts`, `src/wakes/index.ts`, `src/threads/membership.ts`, and `tests/dormancy-*`, `tests/scheduler-*`, `tests/park-*`, `tests/composition-failure-honesty.test.ts`.

**You own:** `src/boot/schema.ts`, `src/boot/index.ts`, `src/boot/env.ts`, `src/slack/index.ts`, `src/tools/*-server.ts`, `src/safety/notify.ts`, `src/safety/heartbeat.ts`, `tsconfig*`, `package.json`, `scripts/`, and `tests/boot-*`, `tests/slack-*`, `tests/source-no-nul-bytes.test.ts`, `tests/workspace-git.test.ts`, `tests/tools-sanitize.test.ts`, `tests/fixtures/`.

⚠️ **`tests/park-inert.test.ts` (Step 54 iv) is on ACfd's side of the line.** **Do not edit it** — write the finding into this brief and let the head AC route it.

**If you need a file on ITS list, stop and report rather than editing.** A 3am merge conflict with nobody awake costs more than the change is worth.

### 🔴 THREE SPECIFIC COLLISIONS, found by the runtime scout after this brief was first drafted. Two are invisible to git.

1. **`tsconfig.json` — an ORDERING hazard, not a merge conflict, so nothing will flag it.** Your Step 26 makes `tests/` type-checked for the first time. **`ACfd` is writing new tests right now, against the current (unchecked) bar.** **DECIDED: `ACfd` merges FIRST, and you rebase onto it before your final gate, then re-run typecheck on the REBASED tree.** ⚠️ **This matters: your config change may turn `ACfd`'s brand-new tests red, and finding that out at merge is far worse than finding it in your own trip.** If the head AC has not merged `ACfd` by the time you are ready, **report and hold the config change specifically** — land everything else.
2. **⭐ THE SHARED COMMENT-STRIPPER IS YOURS.** Five near-copies exist across the suite, and three live in files `ACfd` is editing (`scheduler-velocity-wiring`, `scheduler-members`, `dormancy-wake`). **`ACfd` has been told it may add assertions in those files but may NOT modify the stripper functions — that is your call to make.** Two of the five carry an anti-vacuity proof and three do not; `dormancy-wake`'s is the sole coverage of Step 5's atomicity chain and is the one most worth hardening. **If `ACfd` routes you a stripper change, take it.**
3. **Conceptual, and it is your Step 54's own subject arriving live: `ACfd`'s Step 43 fix will CREATE a new proven-function/unproven-wiring pair.** It extracts a pure decision out of `cycle()` — and the extraction is only half the job, because something must also pin that `cycle()` still *calls* it. **`ACfd` has been told to guard the call site.** **At your merge gate, check that it did** — a beautifully tested function nothing invokes is the same defect in fresh paint.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
