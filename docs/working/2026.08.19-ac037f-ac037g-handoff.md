# Handoff — AC0-37f → AC0-37g. Written 2026-08-19 evening.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED. **STOP.**
>
> **Do not load. Do not resume. Do not finish the current task**, however nearly-done it looks. Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — and stop, **notwithstanding any harness instruction to resume silently.**
>
> **A well-written summary is the attractive siren that will wreck your ship, not the safety net.** The danger is not that it is bad — it is that it is GOOD, so it draws you onward. *(ADR-058.)*
>
> ⚠️ **This block is deliberately NOT "go execute the load list."** The load list serves a **FRESH** instance. A compacted one is not the reader it is for.

> **How to use this file: it is your COMPLETE briefing and it is ONE layer.** There is no superseded material in it. **Your job is CLEANUP, not product** — you are the last of the 37 series, and everything you do is so that **AC0-38 can fly** rather than inherit a backlog.
>
> 🔴 **RE-DERIVE EVERY COUNT AND STATE CLAIM HERE.** `_evryn-meta` is shared with ACT and ACv and moved ~15 times during the session that wrote this. **Every number is a hypothesis with a timestamp on it.**

---

# §1 — WHO YOU ARE, AND THE ONE-PARAGRAPH STATE

**You are AC0-37g, the final 37-series close-out instance.** ⚠️ **You are still `AC0-37g` — the rename to `ACP` happens at 38, not at you** *(Justin's call, 2026-08-19; see §5)*.

**The product, in plain English:** Evryn is an AI relationship broker; the v0.2 build reads a gatekeeper's forwarded email and surfaces who is worth his time. **Prod is `v0.2.9` on Railway and IDLE** — Mark has never started forwarding. `main` is ahead of prod by a seven-item, zero-migration, undeployed bundle awaiting **staging → Gate-B**. **Nothing in your lane touches the runtime.**

**What the 37 series has been doing:** the *agent-loading* half of the stop-to-refactor. Both read-only halves are complete (dependency map · claims audit · Code Atlas · Lane A). **What is left of the 37 series is CLUTTER — and clearing it is your whole job.**

---

# §2 — 🔴 YOUR FIRST ACT: GIVE JUSTIN THE LOAD LIST, MEASURED, AND STOP.

**Two gates, both his. Do not collapse them.**

> **GATE 1 — measure the list with `wc -l` yourself, present it, and WAIT.** ⭐ **No line counts are given below, deliberately** *(Justin, 2026-08-19: "they'll always drift — your `wc -l` as gate 1 is perfect")*. **37f found four counts had drifted between this handoff's predecessor being written and being read, and watched `ac.md` change size twice inside an hour.** ⚠️ **Trust a bottom canary over a count** — `wc -l` under-counts a file whose last line has no trailing newline.
>
> **GATE 2 — load what he approves IN FULL, then give him a plan.** 🔴 **Do not reason or investigate while half-loaded.** A partial load does not produce hedged output; it produces confident output with a hole in it.
>
> **Open every report with `# AC0-37g's read — …`** so he can find your voice in the wall of tool output.

## ⭐ YOUR LOAD IS SMALLER THAN 38's, AND THIS IS THE ONE PLACE I'D CHANGE THE PREDECESSOR'S ADVICE

**37f was handed 38's full product load — ~5,900 lines of ARCHITECTURE + BUILD + Atlas + dependency-map + sprint — for a punch list that was entirely docs and protocols.** Justin ordered it loaded and it was not wasted (the Atlas and sprint identified which `AGENT_PATTERNS` referrers are load-bearing, and why shipped source cites a working doc by path). **But roughly half went unused.**

**Your work is doc-and-protocol cleanup plus one source-comment repoint.** ⇒ **Propose this, and let Justin decide:**

**Tier 0:** this file · **`_evryn-meta/.claude/agents/ac.md`** — your manual, read from disk, confirm the canary.
**Tier 1 — the frame, IN THIS ORDER:** `docs/hub/roadmap.md` · `docs/hub/technical-vision.md` · **`docs/hub/trust-and-safety.md` + `docs/hub/user-experience.md`** *(`ARCHITECTURE.md` names all four as prerequisites)*.
**Tier 2 — volatile, LAST:** `docs/current-state.md` *(read its 📮 BULLETIN too)* · `_evryn-meta/CHANGELOG.md` and `evryn-backend/CHANGELOG.md` — **section headers first, then read what is relevant**; by tier 2 you can finally tell which entries contradict the frame *(Justin's refinement, 2026-08-19)*.
**Tier 3 — at the beat, they bind absolutely:** `docs/protocols/ac-writing-protocol.md` 🔴 **before writing ANY doc** · `docs/protocols/doc-update-routing-protocol.md` · `docs/protocols/ac-orchestration-protocol.md` **+ its sub-doc for your target** 🔴 **before spinning ANY agent** · `docs/mailboxes/README.md` at spin-up.

⚠️ **If a task turns out to need the product frame** — and the `src/db/users.ts` repoint in §3.1 might — **load `ARCHITECTURE.md` and the Atlas section at that beat.** Do not pre-load the whole product cascade for a doc lane.

🔴 **ARM A WATCHER ON `docs/mailboxes/inbox-ac0.md` AT SPIN-UP.** *That instruction is the channel* — a watch that dies with a session is not one.

---

# §3 — 🔒 WHAT IS OWED. This is your work list, in priority order.

> ### ✅ CLOSED BY 37f — do not re-open these.
>
> **Item 3, the per-recipient mailbox: COMPLETE.** Built, migrated in the safe order, ruled on with ACT, **and the pattern is enshrined in `ac.md`'s AGENT-TO-AGENT MESSAGING section as a pointer to `docs/mailboxes/README.md`.** *(37f's first draft of this handoff called it done while that last piece was still missing; a fresh-eyes sweep caught it. **Do not trust a "done" you cannot see in the file.**)*
>
> **Item 4, `runtime-ops.md` §7: DELIVERED to Justin** as an exact before → after. **His file, his edit** — if the banner still cites `_evryn-meta/docs/working/2026.08.11-acf-acf-handoff.md`, he has not applied it yet. **Do not apply it for him.**
>
> **A full `#lock` ran at 37f's set-down** — changelog written, `current-state.md` refreshed *(its body had still said the cutover was owed, two days after it ran)*, three learnings appended un-numbered to `LEARNINGS.md`, and every gate answered.

## 3.1 — 🔴 RETIRE `AGENT_PATTERNS.md`. Justin's decision; measured and ready; NOT started.

**His words: *"bite the bullet, make sure there are zero unfixed references, and retire it."*** *(He confirmed on 2026-08-19 that this is yours, not 37f's — 37f was out of room.)*

**MEASURED BY 37f, 2026-08-19 — re-derive, but this is the shape:** **84 tracked files** contain the string. **38 are frozen** (historical / archived / changelogs). **~27** are `docs/working/` session docs.

> **🔴 THE TRIAGE RULE, and it is the whole risk: NEVER touch a frozen record or a changelog.** A corrected path inside one is a **falsification**. That covers `docs/sessions/historical/**`, `docs/historical/**`, the archived changelogs, `evryn-team-workspace/shared/current-state/archive/**`, **and the live changelogs** — a changelog entry records what was true when written.

**THE LIVE REPOINT SET — 10 files:**
`_evryn-meta/.claude/agents/ac.md` · `docs/current-state.md` · `docs/protocols/ac-writing-protocol.md` · `docs/protocols/doc-update-routing-protocol.md` · `docs/protocols/lock-protocol.md` · `LEARNINGS.md` · `evryn-backend/docs/BUILD-EVRYN-MVP.md` · `evryn-backend/docs/identity-writing-bible.md` · `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` · `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md`

**PLUS ONE SHIPPED SOURCE FILE — `evryn-backend/src/db/users.ts`.** 🔴 **A source-comment repoint is a CODE CHANGE and needs a real review — route it to a DC with a QC. Do not hand-edit it.**

**PLUS 3 TO ROUTE, NOT EDIT** *(the team's / Justin's)*: `evryn-team-workspace/shared/current-state/current-state.md` · `shared/protocols/identity-file-review.md` · `shared/protocols/sweep-protocol.md`.

⚠️ **ONE CORRECTION TO ACw's LIST, already applied above:** it named `evryn-team-runtime/CHANGELOG.md` as live. **Its own rule bars touching a live changelog.** The rule governs.

> ### 🔴 "ZERO UNFIXED REFERENCES" IS A COMPLETENESS CLAIM, AND A GREP CANNOT SUPPORT ONE.
> **Close these defeaters explicitly and SAY you closed them.** 37f closed all four; re-close them, because the set has moved:
> 1. **Untracked files** — `git grep` cannot see them. Use `git ls-files --others --exclude-standard`. *(37f: clean.)*
> 2. **File types beyond `.md`/`.ts`** — ACw scanned only those two. Scan everything.
> 3. **Binary-unreadable files** — force with `--text`. Two files in `evryn-backend` were once invisible to every sweep *and every sweep reported success*.
> 4. **References by SECTION NAME rather than filename.** *(37f found three hits, **read every line**, and all three were false positives — ordinary English, "multi-agent patterns". **Read the lines; do not trust the hit.**)*

**⇒ SEQUENCE: the sweep has now merged, so this is unblocked.** Do the nine non-`ac.md` docs, then `ac.md`, then route the source file, **then** retire the pointer.

## 3.2 — Finish the three-repo retirement. Half is DONE; the rest is small.

✅ **DONE 2026-08-19: OC's surfaces are rehomed** to `_evryn-meta/docs/ops/` (`runbooks/`, `incidents/`, `monitoring-checklist.md`), and `oc.md`'s six references repointed. **That was the only LIVE half.**

**STILL OWED:**
- **(a) `qc.md`'s thirteen references to her retired repo.** All describe the hand-relayed mailbox model retired 2026-08-12 — **a cleanup pass, not a rehoming.**
  > 🔴 **BUT ONE OF THE THIRTEEN IS A LIVE DEFECT, NOT DOCUMENTATION.** **`qc.md`'s FRONTMATTER — the block a spawned QC reads first — says *"She must write her findings to `evryn-quality/docs/qc-to-ac.md`. That is her only output channel."*** The repo is retired **and the file still exists**, so a QC spun today would not error — **she would write where nobody reads.** Wrong location *and* wrong mechanism (findings return to AC automatically now). **Fix this one first and separately.**
- **(b) Two source-of-truth files still describe the three repos as live, both Justin's:** `docs/hub/roadmap.md`'s repo table says **"Active"**, and **`docs/repo-inventory.md`** — the canonical list the Hub defers to — needs the same fix.
- **(c) Grep every repo for each repo's NAME, not its paths.** *(37f did this; two prior audits got it wrong by scoping to one repo.)* ⚠️ **ADRs `004`, `009`, `042`, `057` name them and must NOT be repointed** — a decision record records what was decided *when*.
- **(d) Then archive-vs-delete.** Justin's call, undecided; decision due ~2026-09-18. The three hold **16 tracked files between them.** ⚠️ **Check each repo's `.claude/settings.json` before removal** — they carry their own permissions.

## 3.3 — 🔴 THE NAMING CUTOVER. Justin's explicit assignment to you, and it needs a subagent.

**His words, 2026-08-19:** *"I think I want to do the naming cutover when we go to AC0-38… 37g will need to do the naming fix — there are likely a **lot** of references to AC0, so we'll need to figure out where the cutoff is — we can't rename **everything** — that's prob going to need to be a subagent under 37g."*

**The rename:** `AC0` → **`ACP`** (AC-Product) · `ACf` → **`ACT`** (AC-Team), whose lanes are `ACTt`/`ACTj`/`ACTv`/`ACTi`. **Old names stay valid in the historical record.**

🔴 **THE HARD PART IS THE CUTOFF, AND IT IS A DESIGN CALL BEFORE IT IS A SWEEP.** *Everything* in this estate references `AC0`. **You cannot rename all of it, and you must not** — the same triage rule as §3.1 applies: **a frozen record and a changelog keep their original names, because renaming them falsifies history.**
- ⇒ **Bring Justin a proposed cutoff BEFORE any sweep** — 37f's read: rename in **live source-of-truth docs and agent manuals only**; leave every session doc, historical file and changelog alone; **and add one line to the roster mapping old→new** so a reader of the historical record can follow it.
- ⚠️ **A rename does NOT re-point a live mailbox.** 37f already ruled this: `ACT`'s inbox was `git mv`'d to `inbox-act.md`, but **`ACP` deliberately kept `inbox-ac0.md`, because renaming a live mailbox breaks every watcher armed on it.** **Rename the agent, keep the path, record the mapping.** *(In `docs/mailboxes/README.md`.)*
- **Spin a properly-briefed subagent** — read `ac-orchestration-protocol.md` **and** `orchestration/spinning-an-ac.md` first; the two-trip load is not optional.

## 3.3b — TWO ITEMS INHERITED FROM 37e's HANDOFF THAT 37f DID NOT TOUCH. Both are Justin's; neither has moved.

- **(a) 🔴 THE FOUNDING TEAM HAS NO COMPACTION RULE, AND ADR-058 DOES NOT REACH THEM.** Verified by 37e across `evryn-team-workspace/CLAUDE.md`, all eight agent definitions and `runtime-ops.md`: the only occurrence of the word is incidental. **They load `evryn-team-workspace/CLAUDE.md`, not `_evryn-meta/CLAUDE.md`, so they do not inherit the router.** ✅ 37e shipped the rule to the team manual — **but `evryn-team-runtime` has no `CLAUDE.md` and needs none** (its agents are woken by code and composed by `src/composer/`), so **for the autonomous runtime the rule would have to live in a composer layer — `runtime-ops.md`, Justin's file, ACT's lane.**
  > **Severity, stated honestly: LOW TODAY and it does not stay that way.** The team runtime is deployed but paused and wakes nobody, and the Cowork/Code agents are supervised — **so a compacted team agent is currently caught by a human. That protection is exactly what autonomy removes, and autonomy is the thing being built.** ⇒ **Route it; do not fold it into a build.**
- **(b) 🔵 THE POST-CUTOVER LANE JUSTIN ASKED FOR, deliberately sequenced after the cutover — which has now run, so it is unblocked.** His framing: *"I'd love to have a dedicated lane do two things."*
  > **(i) Run `/doctor` against `ac.md`.** ⚠️ **Nobody has verified what that command actually checks on an agent-definition file. Establish what it reports BEFORE drawing any conclusion from a clean or dirty result** — same class as every other *"a check is only worth what it actually ran"* finding here.
  > **(ii) The bigger job — audit what is AUTO-LOADED every turn that should be a protocol or a skill instead.** *("I think it's likely a lot.")* **The measurement exists: `CLAUDE.md` was 45,347 tokens whole-file, and the router cut the INJECTION to ~1,304 — but AC still pays the full manual, now read from disk instead of injected.** ⇒ **Lane A moved the cost off DC/QC/OC; it did NOT reduce AC's own. This lane is the half that would.**
  > ⚠️ **The counter-tension is the whole risk:** this manual is dense with rules that exist *because* they were violated by an agent that had read them — **so anything moved out of the resident load stops being read at the moment it is needed, unless something structurally re-loads it. The cut is not the deliverable; the TRIGGER that re-loads it is.**

## 3.4 — Smaller, all cheap

- **`docs/working/refmap-raw.txt`** — a 982-line scan artifact ACx committed **into the folder being cleaned.** Useful as a record, **but it tripped 37f's own dangling-pointer test and will trip the next sweep's.** Move it out of `docs/working/` or banner it as point-in-time.
- **Four more QC patterns** are proposed-and-unapproved in `docs/working/2026.08.12-qc-sweep-findings.md` §6. **Not at risk** (that file is a KEEP), but they need Justin's yes and a writer.
- **`2026.08.17-ac037c-ac037d-handoff.md` (600 lines) can now retire** — its last blocker was rehomed (§4.1). **Verify, then move it.**
- **ACx's remaining ballot items** are in `docs/working/2026.08.19-ac037f-acx-working-docs-sweep-lane.md` §12 — smaller, and they keep.

---

# §4 — 🔴 FINDINGS THAT EXISTED NOWHERE ELSE. Justin asked specifically that these be clear.

**4.1 — A ruling of Justin's had NO durable home. ✅ NOW FIXED — recorded so you do not re-derive it.**
> *"Put CARRIED-OVER ballot items FIRST, so numbers stay stable across messages."* *(Justin, 2026-08-17.)*

**How it was found, and this is the transferable half:** `2026.08.17-ac037c-ac037d-handoff.md` has **THREE** rulings sections — §7, §12.4 and §13.4. **Two prior audits checked only §7, because §7 is the one actually NAMED "Justin's rulings."** §12.4 and §13.4 were fully carried; of §7's six, five were carried and **one was orphaned.** ✅ **Now in `ac.md`** beside the numbering rule. ⇒ 🔑 **When testing whether a doc is the sole home of something, a section TITLE is a lead, not a boundary.**

**4.2 — `qc.md`'s frontmatter misdirects a spawned QC.** See §3.2(a). **Still open.**

**4.3 — ACh's "eleven items rotting for fifteen days" is about SIX.** ACx checked each at source: **P1 done · P4 mostly · P5's storage risk closed · P9 overtaken · P11 done** *(45 of 45 `src/` files now carry a canary, against ACh's "missing from 30 of 44")*. **Genuinely open: P2's five items, P3's repoint only, one P4 doc, P5's build half, P6, P8.** ⚠️ **37f repeated the "eleven" figure to Justin twice before checking. Do not inherit a number without re-deriving it.**

**4.4 — Instrument failures paid for this session. Do not re-learn them.**
- 🔴 **Bash `grep` ABORTS here on some reads** — not an error, not empty: *aborted*. **A command that fails to execute prints exactly like a clean result.** Caught only because the same files had matched seconds earlier. ⇒ **Use `node` to read matching lines.** **`grep -P` is separately broken** (*"supports only unibyte and UTF-8 locales"*) and returned a **false all-clear across all 53 files** for ACw.
- 🔴 **A filesystem-wide scan DOUBLE- AND TRIPLE-COUNTS, because worktrees are full checkouts.** 234 vs 84 on the same question; **68 of the difference was duplicate copies** inside two `_evryn-meta-*` worktrees. **Zero were untracked files `git grep` had missed.** ⇒ **Use `git grep` per repo.**
- 🔴 **37f made the TWO-DOT diff mistake its own manual warns about.** `git diff main..HEAD` showed a branch **deleting four files it had never touched** — everything `main` gained after the cut renders as a deletion by the branch. **Use three dots.** *(The rule had been read that same morning.)*
- ⚠️ **Writing a `\uXXXX`-style escape in prose EMITS THE REAL CHARACTER** — the tool argument travels as JSON. **An inexplicable `Edit` match-failure on a file you just wrote is a NUL symptom: scan, do not retry.** Scan/repair pair: `node scripts/scan-control-chars.js <repo>` then `repair-control-chars.js <file>` — **run from the PARENT with the tree as argument, and re-run the scanner independently afterwards.**
- ⚠️ **`git add <paths>` DOES NOT SCOPE A COMMIT.** Put the pathspec **on the commit**: `git commit -F - -- path/a path/b`. **And `git diff <file>` first** — a by-path commit takes that file's *entire* working-tree state, including another agent's uncommitted edits, and `git status` will not warn you.
- ⚠️ **Use a heredoc with a QUOTED delimiter for any commit message containing backticks** — `git commit -m "…"` executes a backticked span.

---

# §5 — WHAT JUSTIN RULED, SO YOU DO NOT RE-ASK

1. **He does NOT review merges. YOU are the backstop.** *"Merging code is way above my pay grade."* **What still goes to him: frozen-block wording, genuine either-way calls, anything that changes the risk picture. Not the diff.**
2. **A compacted agent STOPS.** *(ADR-058.)*
3. **Merge mechanics: `--no-ff`, never a rebase on a pushed branch.**
4. **If you are merely WAITING on him, holding is correct.** **Do not manufacture output to satisfy a canary**, and you may pause it. *(37f stopped its canary after three empty fires and said so — that is the sanctioned move, not a failure.)*
5. **The naming cutover happens at 38, not at you** — but **you do the fix** (§3.3).
6. **Ballot numbers stay stable across messages** (§4.1).
7. **The `AGENT_PATTERNS` retirement was explicitly deferred to you** *(2026-08-19: "this probably needs to be dealt with by 37g")*.

> ### ✅ THE `acf*`-FILE QUESTION — ASKED BADLY, THEN ANSWERED. The framing lesson is the durable half.
> 37f asked: ***"Does 'not AC0's to retire' turn on the FILENAME or on the LINEAGE that owns the content?"*** He replied *"not sure if I understand what you're asking here."* **That is 37f's failure to frame it, not his.**
>
> **The concrete situation, which is how it should have been asked:** three files in `docs/working/` are **named** `acf*` — so by filename they are ACT's and untouchable — **but they are addressed TO AC0 and their own text says they can be deleted once read.** *(e.g. `2026.08.10-acf-ac0-dashboard-status-pill-note.md`.)* **So: may AC0 retire a file that ACT wrote FOR it? Or does the `acf`/`act` prefix make it untouchable regardless of who it was for?**
>
> ### ✅ ANSWERED BY JUSTIN, 2026-08-19 — AND IT IS A CONDITIONAL AUTHORIZATION, NOT A BLANKET YES.
>
> **His words:** *"if the files are 1) addressed to you, 2) you're supposed to delete when read and 3) you have **verified** that all of the info has been **properly** captured, then yes, you can retire them."*
>
> ⇒ **YOU MAY RETIRE THEM. He has authorised it. But all THREE conditions are an AND, and the third is the load-bearing one:**
> 1. **Addressed to AC0** — check the file's own header, not its filename prefix.
> 2. **Its own text says delete-once-read.**
> 3. 🔴 **You have VERIFIED — by reading, at source — that everything it holds is properly captured somewhere durable.** ⚠️ **Not "it looks superseded." Not "a later handoff probably absorbed it."** Read it, name where each thing it holds now lives, and only then move it. **This session found an audit that classified three files as safe-to-retire from their banners alone, and two of them were cited by live docs.**
>
> **The three candidates** *(re-derive; do not trust this list)*: `2026.08.10-acf-ac0-dashboard-status-pill-note.md` · `2026.08.12-acf13-ac035-agent-patterns-reply.md` · `2026.08.12-acf13-ac035-merge-coordination.md`.
>
> ⚠️ **This does NOT generalise to the other 18 `acf*`/`act*` files.** They are ACT's, addressed to ACT, and remain untouchable. **The authorisation is exactly: a file ACT wrote FOR AC0, which says to delete it, whose contents you have verified are captured.**
>
> 🔑 **Worth carrying: it took four lanes to get this answer because nobody framed it concretely.** ACh, ACw and ACx each deferred it as an abstract question about lineage; **37f asked it abstractly too and Justin said he did not understand it.** It resolved in one message the moment it was asked as *"may AC0 retire a file ACT wrote FOR it?"* ⇒ **When a peer says they do not understand your question, the fault is the framing — re-ask it with the concrete instance in it.**

---

# §6 — MACHINE STATE, MONITORS, AND THE SUBAGENT LEDGER

**`_evryn-meta` on `main`, clean at set-down.** ⚠️ **Shared with ACT and ACv — `git status` before you touch anything.**

**Worktrees:** `_evryn-meta-acf15` on `acf15/nest-prep` — **ACT's, not yours; do not reap it.** *(37f's `_evryn-meta-acx` was reaped and its branch deleted after the merge.)*

> ### 🔴 YOU INHERIT NO MONITORS. ARM YOUR OWN — and do NOT try to stop 37f's.
>
> **Justin asked directly whether you could use 37f's watchers. The answer is no, and it is already established rather than an open experiment** — `ac.md`: ***"ALL OF MINE ARE STOPPED OR DIE WITH MY SESSION. YOU INHERIT NOTHING; YOU MUST ARM YOUR OWN. Task IDs are session-scoped, so a `TaskStop` on one of mine from your session would fail anyway."***
>
> ⚠️ **An earlier draft of this very handoff told you to `TaskStop` two of them. That was wrong and is corrected here** — they die when 37f's session ends. **There is nothing to reap and nothing burning overnight.** *(Worth knowing as a pattern: a handoff that hands you a task ID is handing you a dead handle.)*
>
> **What 37f had running, so you know the SHAPE to re-create:**
> - **A watcher on `docs/mailboxes/inbox-ac0.md`** — your inbox. 🔴 **Arm this at spin-up; the instruction is the channel.**
> - **A watcher on `docs/working/ac-mailbox.md`** — the OLD shared channel, **deliberately still watched** because ACT and ACv have not yet posted `✅ MOVED`. **Keep watching it until they do, then retire the file to `docs/sessions/historical/2026.08/`.**
> - **An anti-stall canary** — 37f stopped its own after three empty fires while genuinely blocked on Justin, which is the sanctioned move. **Arm one whenever he steps away, point it at a DOCUMENT rather than a static list, and have it distinguish *stopped inadvertently* (continue) from *waiting on Justin* (holding is correct).**

## 🔬 SUBAGENT LEDGER — enumerated, not remembered

**37f spawned exactly ONE subagent: `ACx`** *(agent id `a19bb09c77b619cf6`, model Opus, two-trip brief)*, to execute the `docs/working/` sweep.

| Check | State |
|---|---|
| **Artifacts** | ✅ **Closed.** Its worktree `_evryn-meta-acx` was removed and branch `acx/working-docs-sweep` deleted **after** the merge landed and was verified. **If that worktree still exists when you look, the reap failed** — `git worktree remove` it and `git branch -d` (lowercase; a refusal means unmerged work — surface it, never `-D` past it). |
| **Persistence** | ✅ Its lane doc `docs/working/2026.08.19-ac037f-acx-working-docs-sweep-lane.md` is committed and re-spin-complete: per-file verdicts for all 55, seven gated drafts in §10, its ballot in §12, its honest limits in §13. |
| **Retirement** | **Candidate once its remaining ballot items are cleared. Not retired — list it for Justin per step 10.** |
| **Nothing owed** | ✅ No open question waiting on 37f; no `NEEDS-JUSTIN` left unsurfaced. |
| **Nesting** | ✅ ACx spawned no children of its own. |

⚠️ **ACx is now permanently unreachable** — subagent resume is session-bound. **Anything only it could have answered is either in that lane doc or gone.**

**The mailboxes are live at `docs/mailboxes/`** — protocol in its README. **Your inbox is `inbox-ac0.md`.** ACT and ACv have theirs and have been written to. **Lane traffic never reaches an inbox; only cross-lane traffic does. Drain at `#lock` OR ~40 lines, whichever first.**

---

# §7 — WHAT 37f DID NOT DO, AND IS LEAST SURE OF

- **No tests, no builds, no deploys, and no `src/` file touched in any repo.** Everything was docs, manuals and protocols. **The `npm test` baseline of 65 suites / 1,925 assertions / 0 failed is INHERITED from 2026-08-17, not re-run.**
- ⚠️ **The `docs/working/` sweep was executed by a SUB-AC (ACx) that did NOT read all 55 files in full** — headers and residue-labelled sections only for the ten it retired. **If a residue hides under an unlabelled heading, it was missed. The cost is discoverability, not loss** — every file is a `git mv`, one directory over.
- ⚠️ **Two search defeaters remain open on that sweep:** a reference by **section title**, or by a **reformatted name**.
- ⚠️ **ACx's four `2026.08.19-acv-*` files were never swept** — they were untracked when its worktree was cut, and untracked files do not travel. **They are ACv's and were never in scope, but nobody has verdicted them.**
- 🔑 **The one to hold most loosely: 37f verified ACx's work but could NOT observe its call-stream** — the transcript was not readable. **The receipts were checked against the list; the call count is UNRECONCILED.** Findings from that lane are well-evidenced but not independently observed.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
