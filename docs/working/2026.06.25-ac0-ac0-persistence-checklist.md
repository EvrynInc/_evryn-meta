# Round-2 Persistence Checklist — nothing drops (AC0, 2026-06-25)

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

> **How to use this file:** AC0's exhaustive map of EVERY loose item from the round-2 wave (all 5 lane handoffs + ACc + staging) → its permanent home → status → owner. The point (Justin's, 2026-06-25): make sure nothing the ACs surfaced gets dropped — that it lands in SPRINT / SPRINT-V0.3-CANDIDATES / an ADR / the ARCH-BUILD absorption list, so we can hit the ground running. This is a **transient orchestration tracker** (retired after convergence), NOT a new home — it points INTO the permanent homes (one-home-per-item). Companion to `2026.06.24-ac0-ac0-convergence-plan.md` (that = merge mechanics; this = what-lands-where). **Anything marked `PROPOSED` is a source-of-truth edit awaiting Justin's go — not yet made.**
>
> *Authored 2026-06-25T11:17-07:00 by AC0. Sources: the five lane handoffs + ACc + staging handoff (all in `docs/working/2026.06.2{3,4}-*`).*

---

## Legend
- **DONE** — already persisted/built + (where relevant) pushed.
- **AT-MERGE** — flips/lands when the bundle merges (the DONE-markings + ARCH absorptions); not lost — tracked here + in the source handoff.
- **PROPOSED** — a source-of-truth edit (SPRINT/ARCH/BUILD/protocol) I will make only on Justin's go.
- **GATED** — waits on Justin (account action / deploy / decision).

---

## A. SPRINT-V0.2-HARDENING — mark DONE at merge (the build is done; the status flip rides convergence)
`PROPOSED` (source-of-truth edit at convergence):
- **Lane A:** 13, 16, 17, 19, 20, 22, 29, 32(A-half), 34, 40, 52, 61, 66
- **Lane B:** 62, 64, 65, 28, 31, 26, 27, 33, 18b, 63, 14, 15, 68(runtime half), 37
- **Lane C:** 10, 11b  *(11a MOOT/drop; 12 deferred → subsumed by Step 57)*
- **M1:** 4, 67b
- **ACc:** 21
- **Text fix:** Step 34's SPRINT text still says "loads gatekeeper not contact" — update to Lane A's v0.2 reframe (loads gatekeeper *correctly*; contact-record enrich is v0.3). Step 52 was a v0.3 candidate pulled to v0.2 — reconcile its placement/number.

## B. SPRINT-V0.2-HARDENING — NEW steps to ADD (so they're tracked, not floating) — `PROPOSED`
1. **register_gatekeeper_address tool** — DONE (ACc, `r2/lane-a-register-tool`). Number + mark DONE. (ADR-046 written.)
2. **Send-recipient allowlist** (`src/email/client.ts`) — DONE (AC4a, `7705675`). Number + mark DONE.
3. **Catch-up historyId read-ordering** follow-up (ACc deferred) — read the resume `historyId` BEFORE `messages.list` across the 3 fetchers (close a sub-second drop window; dedup absorbs the overlap, err-old). Small `[DC+QC]`. NOT done.
4. **M1 sub-steps under Step 4** (AC5a): (a) NB-3 fast-follow tests (billing-fold path, `alertPersistentAuthError` gate, `notifyEmergencyWithStatus`); (b) NB-4 send-bypass assertion re-target (dead-as-placed); (c) Nit-1 rename `OPERATOR_PATHWAYS`→`LOOP_SIGNATURE_EXCLUDED_PATHWAYS`; (d) post-Mark velocity-ceiling calibration (→ Step 43). NOT done.
5. **"Label the initial-draft history" fast-follow** (Lane B QC non-blocker): the 4 initial-draft sites (`processForward`/`processDirect`/`checkFollowUps`/`checkProactiveOutreach`) hand Evryn an *unlabeled* history block → same echo/double-attach risk Step 67a fixed for the revision path. Factor a shared reference-only history wrapper + apply at the 4 sites. Pre-existing, non-blocking. `[DC+QC]`. NOT done.
6. **Real `/health` (503-when-wedged) + 5 per-subsystem dashboard lights** (AC4a §4b, OC-reviewed; closes audit F067 + Step 45). `[AC design → DC+QC; OC ops-reviewed]`. NOT built.
7. **Staging dashboard tab** (`dashboard/api/staging.ts` → the 4th DB) (AC4a). `[DC/dashboard]`. NOT built.
8. **Stand up the staging runtime** (2nd Railway service + 4th Supabase project per ADR-045 + test inbox + staging Slack app + allowlist). `[AC4a design DONE; OC + Justin standup]` — **GATED** on Justin's account actions; pre-Mark must-have, not a hard gate on this bundle.
9. **Staging validation playbook + 3 runbooks** (stand-up / validate / reset-to-clean). `[AC4a + OC]`. NOT done.
10. **Week-one validation run** of the converged bundle on staging (once staging exists). `[AC4a + OC + Justin]`.
11. **Step-63 role-check generalization** — Lane B's Step 63 added *existence* checks to the 5 note/scope tools; ACc's register added a *role* gate. Generalize the role-check to those 5 tools too (the now-promoted QC pattern "validate an id's role, not just existence"). NOT done.
12. **Confirm 69/70/71/72 present** (AC3a committed to `main`): 69 week-one cache-measurement RUN, 70 epithelial restructure (ADR-044, measurement-gated), 71 binding safety-net, 72 `search_path` pin on the 3 SECURITY DEFINER RPCs. Verify they're in the live SPRINT (they were committed direct to main, not a lane).

## C. SPRINT-V0.3-CANDIDATES — NEW v0.3 items — `PROPOSED`
- **Step-64 proper fix** — recipient-binding at submit time (bind the draft's recipient to an authoritative `user_id` so the audit lookup can't miss). Lane B's skip-and-alert is the v0.2 INTERIM. Pairs with the audit "submit_draft doesn't bind recipient to the item's user" finding.
- **Batch redact** — `message_ids[]` variant of `redact_user_from_message` (multi-message bleed = N calls today; batch removes the redact-some-but-not-all risk). Lane B / Mira.
- **Step-34 contact-record enrich** — pull the contact record into `checkFollowUps` when contacts activate/enrich (the v0.2 revert removed it as premature). Lane A.
- **Step-66 leads into care population** — when v0.3 builds contact outreach, bring `lead`s into `findCareEligibleUsers` (today deliberately excluded — proactive care = gatekeeper only). Lane A.
- **Unregistered-direct-sender triage gap** (Lane A low-pri): a gatekeeper emailing Evryn *directly* from an unregistered address → `processDirect`, where `triage.md` doesn't load → the triage lead/gatekeeper boundary beat won't fire. Address-gathering at onboarding largely closes it; a belt-and-suspenders core.md/gatekeeper.md beat is the v0.3 option (confirm the runtime mechanism first).
- **Reflection validation-guard soft-edge** (Lane C): the guard doesn't catch a plausible-length-but-wrong-content consolidation output (recoverable via `story_versions` archive at v0.2). Harden at v0.3.
- **L6 "cluster ran today" dashboard light** (AC4a) — fed by the "cluster ran: screened N, escalated M, drafted K" heartbeat; `product.ts` already stubs `cluster_heartbeat: null`.
- **Step-18b removal** — when Step 57 (runtime-does-bookkeeping) lands, the `supabase_upsert`→`updateEmailmgrItem` routing becomes dead → remove it. Flagged in the code comment; track so it isn't orphaned.

## D. ADRs
- **ADR-046 (Step 61 + register tool) — DONE** (written this session, uncommitted; `docs/decisions/046-gatekeeper-address-resolution.md`).
- ADR-041 (amended), 043, 044, 045 — already written. No action.

## E. ARCH / BUILD absorptions — `PROPOSED` (auth-gated, at convergence, with Justin)
- **ARCHITECTURE.md "Separate Supabase Projects" 3 → 4** (once the staging project is provisioned) — ADR-045.
- **ARCH binding-subclass v0.2 note** — a live `[binding]` is carried to the story foot during lean Reflection consolidation, honored from notes OR the story foot (Mira's reconciliation). Lane C.
- **ARCH Data Model + Memory Architecture** — the `story_versions` table + the consolidation pathway; Reflection now writes `story` in v0.2 (the on-main staleness fix only corrected the "story-empty-in-v0.2" lines — the table/pathway doc is the convergence-time addition). Lane C / ADR-043.
- **ARCH M1 absorption** — document the silent-death safety layer (circuit-breaker, clean halt, detectors, Healthchecks dead-man's-switch) now that it ships. M1.
- **ARCH Step-61 absorption** — `gatekeeper_inbound_addresses` + `outbound_address` + escalate-unregistered-forward into Data Model / Pipeline / System Actors. Lane A's `abbf417` carries draft edits — reconcile vs main-drift. ADR-046.
- **ARCH `triage_reasoning` may carry a downgrade-why** (Step 68) — small note. Lane B.
- **`.roles` stale comment** — verify/apply `backups/fix-profile-jsonb-comment-2026-06-04.sql` to PROD (already clean on dev). Lane A.
- **operator-guide.md** — Step-48 register-runbook supersede (register tool, not hand-SQL) · the M1 section (done on the Lane-M1 branch) · staging env warnings (`STAGING_RECIPIENT_ALLOWLIST` never on prod; `review@evryn.ai` must be allowlisted on staging) · `SLACK_OPERATOR_USER_ID`.
- Re-pull `evryn-backend/docs/schema-reference.md` after prod migrations apply.

## F. Protocol / ops — `PROPOSED` / GATED (Justin's call)
- **Codify OC triggers** into `ac-orchestration-protocol.md` (AC4a §10): ops-lens review on infra/deploy/cron/health builds · OC consult before deploy · post-deploy health pass · the on-demand→autonomous-OC handoff breadcrumb. **Justin's call whether AC0 or the protocol-owner drafts it.** (Flagged, not done.)
- **EVR-72** (Linear) — reframed/resolved at v0.2 by Lane A's Step 34 (loads gatekeeper correctly; the contact-enrich remainder is v0.3). Update the ticket.

## G. Identity-deploy coordination (rides the convergence deploy)
All identity edits ride the bundle deploy via `shared/protocols/identity-file-review.md` + a **`#dev-alerts` ping on the identity merge** (identity changes trigger a live redeploy):
- Lane A: `gatekeeper-onboarding.md`, `triage.md` (+ ACc's register beat in `gatekeeper-onboarding.md`).
- Lane B: `triage.md`, `gatekeeper.md`, `operator.md` (committed `a9881a6`).
- Lane C: `reflection.md` (new), `core.md` (broad — note-voice + binding reconciliation), 3 canaries.
- **`triage.md` is a 3-lane file** (Lane A sender-class line ~48, Lane B Beat 1 Phase-3, Lane C canary bottom) — different sections, clean merge, but watch it.
- Mira's voice-polish owed on Lane A's AC-added surface-to-Operator clause.

## H. Cross-lane seams to WIRE at merge (convergence work — in the convergence plan; listed so it isn't lost)
- **Step 14 gold-destination** → route `submitDraftForApproval`'s `draft_to` through `resolveGoldDestination(gatekeeper)` (Lane B TODO + Lane A helper). ~2 lines.
- **Step 32 transport failure-test** → post-merge, test `executeApproval` with a throwing `transport` (Lane B's half can't live in its worktree — `client.ts` is Lane A's).
- **Step 66 wire** → Reflection's `checkReflectionConsolidation` calls `findCareEligibleUsers()` (drop Lane C's `findUsersByStatus(["active"])` placeholder).
- **Merge-watch files:** `classify.ts` (Lane B MCP region + Lane C loadCommonPrefix + M1 breaker + ACc register — additive, different regions) · `poll.ts` (Lane A + Lane C checker + M1 hooks + Step-21 one line) · `process.ts` (Lane A Step-61 reorder + Step-19 + M1 dedup-resume — highest-attention) · `db/users.ts` (Lane A + Lane C + ACc additive) · `triage.md` (3-lane, above).

## I. Prod migrations (apply at convergence, dev-first satisfied, pre/post `pg_dump`) — GATED
- Lane A: `add-emailmgr-items-updated-at-trigger.sql` (13) + `add-gatekeeper-inbound-addresses.sql` (61).
- Lane C: `add-story-versions-and-consolidate-profile.sql` (10).
- `.roles`: `fix-profile-jsonb-comment-2026-06-04.sql` (verify/apply).
- M1: none. ACc/allowlist: none.

## J. Deploy-time (GATED — Justin's go)
Integration-QC the merged bundle → prod migrations → `railway up` → `#dev-alerts` identity-merge ping → confirm boot health. Justin pre-live: Anthropic monthly ceiling + 2 Healthchecks checks + `PROACTIVE_CHECK_HOUR_PT=10` (M1). `SLACK_OPERATOR_USER_ID` already set (Lane B).

## K. Cleanup (end — GATED, Justin's go)
- Reap r2 worktrees/branches after merge (`git worktree remove` + `git branch -d`) + the round-1 junk worktrees on ice (cost/ingest/m1/operator).
- Tidy `docs/working` + `docs/sessions`: the lane handoffs, briefs, design docs, this checklist + the convergence plan → `historical/` or retire once absorbed. (All this-project.)

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
