# Lane A (AC1) — Session Capture for the Re-Run (2026-06-18)

> **Truncation check:** the last line should read `FULL FILE LOADED`. If not, reload.

> **How to use this file:** AC1's capture of the 2026-06-17 Lane A wave, written at Justin's request after the subagent-load protocol was **confirmed misfiring** → all Lane A subagent work is junk and the worktree is being shelved. **For AC0 to rebuild the Lane A brief correctly from scratch.** It lives in a *dedicated file* (not appended to the shared `docs/sessions/2026-06-17-parallel-lanes-brief.md`) **because that brief was being concurrently edited by the other lanes' ACs when I tried to append — appending would have clobbered a sibling's addendum.** A one-line pointer to this file goes in the brief. AC0 should collect the per-lane capture files.

*Written 2026-06-18 (AC1).*

---

## Context

The subagent-load protocol was **CONFIRMED misfiring** this wave (identity/context files weren't synced to this machine — QC's for sure, DC's too), so **ALL Lane A subagent work is junk** and the worktree `evryn-backend-ingest` is being shelved. Lane A spun **7 subagents — 4 "DC" builds** (commits `0430a6f`, `30db607`, `3f8d3d4`, `d2d98af`) **+ 3 "QC" reviews** (all returned a now-meaningless GO).

**Provenance tags on every item:** `(Justin)` = his call · `(AC1-clean)` = my analysis from reading the **original, pre-subagent** runtime during my full-cascade load · `(AC2/AC0)` = cross-lane input · `(⚠️ SUBAGENT)` = surfaced or shaped by a suspect subagent → **re-derive with a clean DC/QC.**

**Headline opinion for AC0 (Justin asked for it):** the *design decisions* below are **materially un-corrupted** — they came from Justin's calls + my reads of the **original clean runtime** (my full-cascade load happened BEFORE any subagent ran) + AC2/AC0 inputs. The subagents *built to* these designs; they did not author them. What IS junk: **every implementation** (all `src/` + the migration SQL — shelved with the worktree) and **every QC "GO"/"verified-safe" stamp** (suspect QC → nothing is actually verified, even where my own AC-review agreed). A few items are subagent-*surfaced* (tagged ⚠️) and need clean re-derivation. **Net: the *thinking* is reusable; the *building* and *verifying* must be redone.**

## Part 1 — Decisions / changes / findings (roll into the re-brief)

1. **Step 34 / EVR-72 — the sprint's literal phrasing is WRONG (fix it in the re-brief).** The runtime *already* loads the gatekeeper (`checkFollowUps` → `findUserById(item.user_id)`; `user_id` = the forwarder). The fix is NOT to switch scope to the contact (that would leak the contact's private notes into a gatekeeper-facing follow-up) — it's to **add the contact's name + status to the prompt text** via the ADR-036 FK. The "enabled by the FK" note is the tell: needing the new FK proves the fix *adds* contact info. *(AC1-clean — I verified `checkFollowUps` against the original runtime; Justin's "I bet it's loading the contact" prompted the verify, and neither his bet nor the sprint text matched the code.)*
2. **Step 17 empty-body bounce = a DIRECT send that bypasses the approval gate — allowed ONLY because it's deterministic / fixed-template / no-LLM / no-PII (so it's *pre-authorized* content), respects `SEND_ENABLED`, records to `messages` for audit.** The bounce *copy* is a placeholder → **Mira owns the wording, and it must stay deterministic** (that's the whole basis for the gate-bypass). *(Justin's call, ballot A. The approval-gate tension was AC1-clean — raised from ARCHITECTURE's Outbound-Approval-Gate invariant. The "`sendEmail` isn't an MCP tool, so Evryn-the-LLM can't trigger it" reassurance was ⚠️ SUBAGENT (Step-17 QC) — independently checkable, but re-confirm clean.)*
3. **Step 21 cursor resilience = time-bounded catch-up, NO new table.** On cold-start / `historyId`-expiry, fetch "everything since the last processed email" (derived from existing data — newest `emailmgr_items.created_at`), larger page, dedup absorbs overlap — replacing the old "last 10 inbox" fallback that silently dropped mail during restart windows. *(AC1-clean — refined from reading `client.ts`/`poll.ts` in my original load; the no-new-table simplification was my call. Justin approved the approach, ballot B.)*
4. **Step 32 = SPLIT.** Lane A owns only the `client.ts` `sendEmail` injectable-transport seam (+ its own failure-surfacing test); the `executeApproval` retry/rollback test → **Lane B** (their file). *(AC1 noticed the cross-lane seam; AC0 formalized it in the brief; Justin confirmed ballot D.)*
5. **Step 13 must be a DB TRIGGER, not app-code** — and should **reuse the existing `public.touch_updated_at()`** (already wired to `users` via `trg_users_updated_at`), NOT introduce `moddatetime`; a trigger is required because Evryn's raw status writes bypass `updateEmailmgrItem`. *(Trigger-not-app-code is AC1-clean. The "reuse `touch_updated_at` + add `DROP TRIGGER IF EXISTS`" refinement was ⚠️ SUBAGENT (trip-1 QC) — but I independently verified `touch_updated_at`/`trg_users_updated_at` exist by grepping the real schema dump, so it's sound, just surfaced by a suspect agent.)*
6. **Step 61 (gatekeeper-address resolution) — full design exists + is the cleanest surviving artifact.** Inbound lanes (many→one via a `gatekeeper_inbound_addresses` table) + a settable `users.outbound_address` (gold destination, decoupled from the inbound lane) + **escalate unregistered-lane forwards** (don't triage blind against a blank lead — the bug Justin reacted to). Email-only v0.2 floor; multi-channel identity stays v0.3. *(Design = AC1-clean (runtime analysis done BEFORE the Step-61 subagent) + Justin's all-(a) ballot + AC2/AC0's outbound model. Full doc: `docs/working/2026-06-17-step61-gatekeeper-address-resolution.md` — Part 3. The build was a ⚠️ SUBAGENT DC.)*
7. **Step 61 outbound/threading model.** Gold → the gatekeeper's *designated* destination; threads into the original ONLY if that destination is the same account holding the original (AC2's headers), else graceful standalone with the original subject quoted for matching. Do NOT engineer cross-account threading (can't group across mailboxes). Lane A = SET the destination + STASH the original subject on the item; **AC2's Step 14 = USE them.** *(AC2 via Justin + AC0 synthesis.)*
8. **Minor builds (all AC1-clean designs; implementations junk):** Step 16 internal-address sender-skip (mirror the self-skip; kills the phantom-user cascade); Step 19 external-id dedup fallback (`messageId || gmailId`, single helper); Step 20 rate-limit the transient alert + remove the double-`notifyDev` (one alert per outage, not two-per-email — noticed from reading the code); Step 22 hour-gate the proactive heartbeat; Step 29 comment accuracy; Step 40 the stale `client.ts` `sendEmail` comment.
9. **Orchestration:** Lane A ran as two batches (mechanical-now vs. design-pending-on-ballot). AC0 added **"post-and-proceed"** to the brief (post the day-plan, don't wait for sign-off). The **migration dev-rehearsal is the Lane-A AC's job** ("AC applies"), which Justin confirmed ("it's yours, do it"). *(AC1 + AC0 + Justin.)*

## Part 2 — What Lane A pitched to OTHER lanes (AC0: roll into THOSE briefs)

- **→ Lane B:** (a) the Step 32 `executeApproval` retry/rollback test (the split); (b) `client.ts`'s `sendEmail` gained a `transport` param — Step 14's threading edits must compose with that signature; (c) **AC2's Step 18 `set_item_status`** — Lane A's `checkFollowUps` prompt must reference `set_item_status` (not `supabase_upsert`) for the `no_gk_response` close, a **convergence dependency** (prompt + tool must land in the same bundle). *(AC2 specified the swap; AC1 flagged the dependency. The swap commit `d2d98af` is junk now — the clean re-run re-adds this once Lane B's clean Step 18 exists.)*
- **→ AC0 / sprint:** **fix the Step 34 framing** (Part 1 #1); **Step 58 clustering** touches `poll.ts` — the seam is the synchronous per-email triage trigger (`handleNewEmail`→`processEmail`→`processForward`/`processDirect`→`runEvrynQuery`); a daily-cluster cron joins the existing crons. *(AC0's Step 58 + my seam analysis, AC1-clean.)*
- **→ Step 48 onboarding (⚠️ SUBAGENT-surfaced, but sound — re-derive):** the runbook MUST register **Mark's PRIMARY email as an inbound lane** — once Step 61 ships, an unregistered-lane forward escalates instead of triaging, so if his own lanes aren't registered, all his forwards escalate and triage is dead. *(Surfaced by the Step-61 QC subagent; a sound deduction from the Step-61 design, but flag it as subagent-surfaced.)*
- **→ Ops (⚠️ SUBAGENT-surfaced — verify independently):** a DC subagent's test failed with `original_from_user_id ... not found in schema cache` → maybe prod's PostgREST schema cache lacks ADR-036's column. Could be a real silent-failure (ADR-036 reification would fail live) or a test-env artifact. Verify on a clean pass.

## Part 3 — Outputs that SURVIVE the worktree scrap + corruption assessment

1. **🔴 DEV DATABASE — both migrations were APPLIED to `Evryn Product — Dev` (us-west-2) and PERSIST (the one that survives + needs action).** I ran the applies myself via psql (execution sound — not a subagent), but the migration *SQL was DC-authored* (suspect) + reviewed by suspect QC. Live in dev now: the `trg_emailmgr_items_updated_at` trigger; the `gatekeeper_inbound_addresses` table; the `users.outbound_address` column + its grant/revoke. **PROD was NOT touched.** **Recommendation: roll the dev changes back** (drop trigger, drop table, drop column) so the clean re-run starts fresh — the migrations' `IF NOT EXISTS` guards would otherwise SKIP recreating, leaving the suspect structure in place (and possibly conflicting if a clean DC designs a different schema). AC1 can roll it back now (has psql + the dev URL) on Justin's go. *(SQL is simple/additive and AC-reviewed + I independently verified the `touch_updated_at` reuse — but treat as suspect per principle; rolling back is the clean reset.)*
2. **`_evryn-meta/docs/working/2026-06-17-step61-gatekeeper-address-resolution.md`** (committed in evryn-meta). **Corruption: LOW.** Authored BEFORE the Step-61 subagent ran (it built *from* this). The design is clean analysis + Justin's ballot + AC2/AC0 inputs. **Reusable for the re-run.** Caveats: the one ⚠️ SUBAGENT element is the Step-48 primary-lane note (sound, re-derive); its "proposed ARCH/BUILD edits" should be re-reviewed by a clean AC before applying — do NOT auto-apply.
3. **`_evryn-meta/docs/working/2026-06-17-lane-a-handoff.md`** (committed). **Corruption: HIGH as a trustworthy record** — authored by me, but it *describes and leans on* the suspect subagent work (the 4 commits, the 3 GOs, the "verified-safe" claims). Recommend shelve/supersede — a record of the corrupted run, not a basis for the re-run.
4. **evryn-meta git commits** `393228d`, `7952ed3`, `44ab67e` on `main` (+ the commit landing this capture file) — they added/updated docs #2 and #3. Survive the scrap (different repo from the worktree). Cleanup follows whatever's decided for #2/#3.
5. **Slack pings** (`#team-alerts` + `#dev-alerts`) — several announced "Lane A QC-GO" and "dev rehearsal done." **The QC-GO claims are now false.** Not files to fix, but the `#dev-alerts` record is misleading; AC1 can post a one-line retraction on Justin's go.
6. **NO `CLAUDE.md` / identity-file changes, NO Linear changes** — Lane A was pure-runtime by design and touched none. *(Clean — nothing to undo there.)*
7. **Housekeeping:** the `.env` Justin copied into the worktree (for the dev rehearsal) holds live DB creds — it vanishes if the worktree is deleted, but if you *shelve* rather than delete, remove that `.env`.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
