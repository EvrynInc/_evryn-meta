# Lane A (AC1) — Session Capture for the Re-Run (2026-06-18)

> **Truncation check:** the last line should read `FULL FILE LOADED`. If not, reload.

> **How to use this file:** **THE canonical, complete capture** of the 2026-06-17 Lane A wave, written at Justin's request after the subagent-load protocol was **confirmed misfiring** → all Lane A subagent work is junk and the worktree is being shelved. **For AC0 to rebuild the Lane A brief correctly from scratch.** A shorter copy is inlined in `docs/sessions/2026-06-17-parallel-lanes-brief.md` (where AC0 collects per-lane addenda) and points here for the full set. **This file is the one to trust** (it's collision-safe to maintain; the brief was being concurrently appended by sibling lanes). Justin's directive: capture-only, no fixing — a specialty team does the repairs.

*Written 2026-06-18 (AC1); completed after a full start-to-finish re-read of the session transcript.*

---

## Context

The subagent-load protocol was **CONFIRMED misfiring** this wave (identity/context files weren't synced to this machine — QC's for sure, DC's too), so **ALL Lane A subagent work is junk** and the worktree `evryn-backend-ingest` is being shelved. Lane A spun **7 subagents — 4 "DC" builds** (commits `0430a6f`, `30db607`, `3f8d3d4`, `d2d98af`) **+ 3 "QC" reviews** (all returned a now-meaningless GO).

**Provenance tags on every item:** `(Justin)` = his call · `(AC1-clean)` = my analysis from reading the **original, pre-subagent** runtime during my full-cascade load · `(AC2/AC0)` = cross-lane input · `(⚠️ SUBAGENT)` = surfaced or shaped by a suspect subagent → **re-derive with a clean DC/QC.**

**Headline opinion for AC0 (Justin asked for it):** the *design decisions* below are **materially un-corrupted** — they came from Justin's calls + my reads of the **original clean runtime** (my full-cascade load happened BEFORE any subagent ran) + AC2/AC0 inputs. The subagents *built to* these designs; they did not author them. What IS junk: **every implementation** (all `src/` + the migration SQL — shelved with the worktree) and **every QC "GO"/"verified-safe" stamp** (suspect QC → nothing is actually verified, even where my own AC-review agreed). ⚠️-tagged items need clean re-derivation. **Net: the *thinking* is reusable; the *building* and *verifying* must be redone.**

## Part 1 — Decisions / changes / findings (roll into the re-brief)

1. **Step 34 / EVR-72 — the sprint's literal phrasing is WRONG (fix it in the re-brief).** The runtime *already* loads the gatekeeper (`checkFollowUps` → `findUserById(item.user_id)`; `user_id` = the forwarder). The fix is NOT to switch scope to the contact (that would leak the contact's private notes into a gatekeeper-facing follow-up) — it's to **add the contact's name + status to the prompt text** via the ADR-036 FK. The "enabled by the FK" note is the tell: needing the new FK proves the fix *adds* contact info. *(AC1-clean — I verified `checkFollowUps` against the original runtime; Justin's "I bet it's loading the contact" prompted the verify, and neither his bet nor the sprint text matched the code.)*
2. **Step 17 empty-body bounce = a DIRECT send that bypasses the approval gate — allowed ONLY because it's deterministic / fixed-template / no-LLM / no-PII (so it's *pre-authorized* content), respects `SEND_ENABLED`, and records to `messages` for audit.** The bounce *copy* is a placeholder → **Mira owns the wording, and it must stay deterministic** (that's the whole basis for the gate-bypass). *(Justin's call, ballot A. The approval-gate tension was AC1-clean — raised from ARCHITECTURE's Outbound-Approval-Gate invariant. The "`sendEmail` isn't an MCP tool, so Evryn-the-LLM can't trigger it" reassurance was ⚠️ SUBAGENT (Step-17 QC) — independently checkable, but re-confirm clean.)*
   - **Known edges of this design the clean build must handle deliberately (don't lose these):** (a) empty-body emails create **no `emailmgr_items` row → no durable dedup** (only in-memory, lost on restart), so a process restart can **re-bounce** an empty email still in the inbox; (b) a sender who repeatedly emails empty bodies gets a bounce each time (**no dedup-guard** was built — accepted at v0.2 as low-risk); (c) the bounce-vs-skip decision must happen **after a cheap no-LLM sender lookup** (known sender → bounce; unknown → silent skip + dev-alert), and lookup/send failures must degrade to a **transient retry**, not a silent drop or a blind bounce. *(Edges (a)/(b) ⚠️ SUBAGENT-surfaced (Step-17 QC) + my review; the after-lookup structure + transient-degrade is AC1-clean / Justin's design.)*
3. **Step 21 cursor resilience = time-bounded catch-up, NO new table.** On cold-start / `historyId`-expiry, fetch "everything since the last processed email" (derived from existing data — newest `emailmgr_items.created_at`, minus a small safety margin), via a **lazy provider** so the DB read only fires on a fallback (not every poll); larger page; existing dedup absorbs the over-inclusive overlap — replacing the old "last 10 inbox" fallback that silently dropped mail during restart windows. The fetch layer must stay DB-free (it receives the provider closure). *(AC1-clean — refined from reading `client.ts`/`poll.ts` in my original load; the no-new-table simplification was my call. Justin approved the approach, ballot B.)*
   - **Known limitation to decide deliberately:** the fallback **page size bounds how long an outage it covers** (the junk build used `maxResults: 100` ≈ ~12h at Mark's ~200/day, no pagination). The clean build should pick a sensible page size or paginate on `nextPageToken`. *(⚠️ SUBAGENT-surfaced (Step-21 QC) but a real property of the design.)*
4. **Step 32 = SPLIT.** Lane A owns only the `client.ts` `sendEmail` injectable-transport seam (default = real Gmail send; a throwing transport must propagate so a caller's retry has a real failure surface) + its own failure-surfacing test; the `executeApproval` retry/rollback test → **Lane B** (their file). *(AC1 noticed the cross-lane seam; AC0 formalized it in the brief; Justin confirmed ballot D.)*
5. **Step 13 must be a DB TRIGGER, not app-code** — and should **reuse the existing `public.touch_updated_at()`** (already wired to `users` via `trg_users_updated_at`), NOT introduce `moddatetime`; a trigger is required because Evryn's raw status writes bypass `updateEmailmgrItem`, and app-side would miss them. Make it idempotent (`DROP TRIGGER IF EXISTS`). *(Trigger-not-app-code is AC1-clean. The "reuse `touch_updated_at` + `DROP TRIGGER IF EXISTS`" refinement was ⚠️ SUBAGENT (trip-1 QC) — but I independently verified `touch_updated_at`/`trg_users_updated_at` exist by grepping the real schema dump, so it's sound, just surfaced by a suspect agent.)*
6. **Step 61 (gatekeeper-address resolution) — full design exists + is the cleanest surviving artifact.** Inbound lanes (many→one via a `gatekeeper_inbound_addresses` table, `address citext PK` → `gatekeeper_user_id`) + a settable `users.outbound_address` (gold destination, decoupled from the inbound lane; falls back to `users.email`) + **escalate unregistered-lane forwards** (don't triage blind against a blank lead — the bug Justin reacted to). Email-only v0.2 floor; multi-channel identity stays v0.3. *(Design = AC1-clean (runtime analysis done BEFORE the Step-61 subagent) + Justin's all-(a) ballot + AC2/AC0's outbound model. Full doc: `docs/working/2026-06-17-step61-gatekeeper-address-resolution.md` — Part 3. The build was a ⚠️ SUBAGENT DC.)*
   - **Load-bearing design principles inside Step 61 (do NOT lose — these prevent silent failures):**
     - **The lane resolver must THROW on a DB error, NOT swallow-to-null.** A swallowed error looks identical to a clean "no lane registered" miss, which routes a forward to escalate-or-blank-lead — so a transient Supabase blip would **misroute a real gatekeeper's forward**. Throw → `processEmail`'s caller treats it transient → hold cursor + retry. *(AC1-clean — I specified this in the DC brief.)*
     - **Resolution branch** = a pure decision: lane-hit → use the canonical gatekeeper record (no blank lead); forward + no-lane → **escalate** (short-circuit, no triage); not-a-forward + no-lane → existing find-or-create-as-lead (direct message from a new person). Keep dedup FIRST, before resolution. *(AC1-clean.)*
     - **Escalation mechanics:** create a tracked `emailmgr_items` row stamped `escalated`, **anchored to the Operator system actor** for the NOT-NULL `user_id` FK (there's no valid gatekeeper; the real sender goes in `metadata.unresolved_from`), with `external_id` stamped so a re-fetch dedups; + an operator Slack ping. The existing 4h stale-`escalated` checker is the backstop. *(Operator-anchor was a ⚠️ SUBAGENT (DC) implementation choice within my "least-bad FK, DC decides" design note — sound, but re-confirm.)*
7. **Step 61 outbound/threading model.** Gold → the gatekeeper's *designated* destination (`users.outbound_address`); threads into the original ONLY if that destination is the same account holding the original (AC2's headers), else graceful standalone with the original subject quoted for matching. Do NOT engineer cross-account threading (can't group across mailboxes). **Lane A = SET the destination + STASH the original subject on the item; AC2's Step 14 = USE them** (reads `outbound_address` off the gatekeeper record, `original_subject` off the item metadata). *(AC2 via Justin + AC0 synthesis.)*
8. **Minor builds (all AC1-clean designs; implementations junk):** Step 16 internal-address sender-skip (skip inbound from `review@evryn.ai`-type internal addresses by sender, mirroring the self-skip; kills the phantom-user cascade); Step 19 external-id dedup fallback (`messageId || gmailId` via a single shared helper, used by the dedup-check AND both item-creates); Step 20 rate-limit the transient-failure alert (generalize the existing checker-cooldown) + **remove the double-`notifyDev`** (the inner one in `process.ts`'s `findOrCreateUser` catch — one alert per outage, not two-per-email); Step 22 hour-gate the `[cron:proactive] woke` heartbeat; Step 29 comment accuracy (re-processing is safe by *dedup*, not by post-write throws being impossible); Step 40 drop the stale "test recipient override" clause from `client.ts`'s `sendEmail` comment (`TEST_RECIPIENT` is fully dead in `src/`; `NODE_ENV`/`nodeEnv` survives only in a startup log = cosmetic — any `.env`/Railway cleanup is ops, not code).
9. **Orchestration / process:** Lane A ran as two batches (mechanical-now vs. design-pending-on-ballot). AC0 added **"post-and-proceed"** to the brief (post the day-plan, don't wait for sign-off). The **migration dev-rehearsal is the Lane-A AC's job** ("AC applies"), which Justin confirmed ("it's yours, do it"). My **full-cascade load happened BEFORE any subagent ran** — that's why my runtime reads are clean. I **personally read every diff** (real AC reviews) — but per our model an AC review is **not** a substitute for independent QC, and the QC here was suspect. *(AC1 + AC0 + Justin.)*

## Part 2 — What Lane A pitched to OTHER lanes (AC0: roll into THOSE briefs)

- **→ Lane B:** (a) the Step 32 `executeApproval` retry/rollback test (the split); (b) `client.ts`'s `sendEmail` gained a `transport` param — Step 14's threading edits must compose with that signature; (c) **AC2's Step 18 `set_item_status`** — Lane A's `checkFollowUps` prompt must reference `set_item_status` (not `supabase_upsert`) for the `no_gk_response` close, a **convergence dependency** (the prompt + the tool must land in the same bundle); also a `process.ts` comment was swapped to match. *(AC2 specified the swap; AC1 flagged the dependency. The swap commit `d2d98af` is junk now — the clean re-run re-adds this once Lane B's clean Step 18 exists.)*
- **→ AC0 / sprint:** **fix the Step 34 framing** (Part 1 #1). **Step 58 clustering** touches `poll.ts` — the seam is the synchronous per-email triage trigger (`handleNewEmail`→`processEmail`→`processForward`/`processDirect`→`runEvrynQuery`); a daily-cluster cron will join the existing crons (`checkStaleItems`/`checkFollowUps`/`checkProactiveOutreach`/`checkMorningSweep`). **Step 57 (runtime-does-bookkeeping, the Phase-2b cost keystone) will ALSO rework `process.ts`** (a Lane A file) — a future cross-lane seam the re-spin's Lane A should coordinate with the cost thrust. *(AC0's Steps 57/58 + my seam analysis, AC1-clean.)*
- **→ ARCH/BUILD + v0.3 (auth-gated — AC0/Justin apply):** Step 61 implies edits to ARCHITECTURE (Identity resolution / Gatekeeper users / Data Model / Pipeline sender-resolution) + BUILD (gatekeeper model / workflow / onboarding) — **drafted verbatim in the Step 61 design doc's "Proposed doc edits," NOT applied; re-review with a clean AC before applying.** That doc also carries a **v0.3-scaling note Justin pre-authorized me to write**: the escalate-unregistered-forward floor *doesn't scale* (manual operator adjudication is untenable at multi-gatekeeper / v0.3 self-signup / cast-off volume) → v0.3 needs a real trust-based design (sibling to "Gaming-Resubmit Detection at Scale"). It's drafted in the design doc, not yet in ARCH/BUILD/SPRINT-v0.3. *(Justin pre-authorized; AC1 drafted.)*
- **→ Step 48 onboarding (⚠️ SUBAGENT-surfaced, but sound — re-derive):** the runbook MUST register **Mark's PRIMARY email as an inbound lane** — once Step 61 ships, an unregistered-lane forward escalates instead of triaging, so if his own lanes (incl. primary) aren't registered, all his forwards escalate and triage is dead. *(Surfaced by the Step-61 QC subagent; a sound deduction from the Step-61 design, but flag it.)*
- **→ Ops (⚠️ SUBAGENT-surfaced — verify independently):** a DC subagent's test (`test-processing-stuck`) failed with `original_from_user_id ... not found in schema cache` → maybe prod's PostgREST schema cache lacks ADR-036's column. Could be a real silent-failure (ADR-036 reification would fail live) or a test-env artifact. Verify on a clean pass.

## Part 3 — Outputs that SURVIVE the worktree scrap + corruption assessment

1. **🔴 DEV DATABASE — both migrations were APPLIED to `Evryn Product — Dev` (us-west-2) and PERSIST (the one that survives + needs action).** I ran the applies myself via psql (execution is sound — not a subagent), but the migration *SQL was DC-authored* (suspect) + reviewed by suspect QC. Live in dev now: the `trg_emailmgr_items_updated_at` trigger; the `gatekeeper_inbound_addresses` table; the `users.outbound_address` column + its grant/revoke. **PROD was NOT touched.** **The specialty team should roll the dev changes back** (drop trigger, drop table, drop column) so the clean re-run starts fresh — the migrations' `IF NOT EXISTS` guards would otherwise SKIP recreating, leaving the suspect structure in place (and possibly conflicting if a clean DC designs a different schema). *(SQL is simple/additive and I AC-reviewed it + independently verified the `touch_updated_at` reuse — but treat as suspect per principle.)*
2. **`_evryn-meta/docs/working/2026-06-17-step61-gatekeeper-address-resolution.md`** (committed). **Corruption: LOW.** Authored BEFORE the Step-61 subagent ran (it built *from* this). The design is my clean analysis + Justin's ballot + AC2/AC0 inputs. **Reusable for the re-run.** Caveats: the one ⚠️ SUBAGENT element is the Step-48 primary-lane note (sound, re-derive); its "proposed ARCH/BUILD edits" + the v0.3-scaling note should be re-reviewed by a clean AC before applying — do NOT auto-apply.
3. **`_evryn-meta/docs/working/2026-06-17-lane-a-handoff.md`** (committed). **Corruption: HIGH as a trustworthy record** — I authored it, but it *describes and vouches for* the suspect subagent work (the 4 commits, the 3 GOs, the "verified-safe"/"QC-GO" claims, the dev-applied "verification"). Recommend shelve/supersede — it's a record of the corrupted run, not a basis for the re-run.
4. **This file** (`2026-06-17-lane-a-RERUN-CAPTURE.md`, committed) + its shorter inline twin in the parallel-lanes brief. **Corruption: LOW** — it's my recordkeeping written clean for this purpose; it *describes* the corruption but isn't itself subagent-derived. This file is the canonical/complete one.
5. **evryn-meta git commits** on `main`: `393228d`/`7952ed3`/`44ab67e` (handoff + Step 61 design + dev-applied note), `6c4319b` (this capture), `5cdf9a1` (the brief inline addendum) + whatever lands this update. They survive the scrap (different repo from the worktree). Cleanup follows whatever's decided for the docs above.
6. **Slack pings** (`#team-alerts` + `#dev-alerts`) — several announced "Lane A QC-GO" and "dev rehearsal done." **The QC-GO claims are now false.** Not files to fix, but the `#dev-alerts` record is misleading — a one-line retraction would set it straight (specialty team / operator's call).
7. **NO `CLAUDE.md` / identity-file changes, NO Linear changes** — Lane A was pure-runtime by design and I touched none. *(Clean — nothing to undo there.)*
8. **Build-detail seam for the record:** the Step-21 design needs a "newest item timestamp" DB read; the junk build put a new additive helper (`getNewestEmailmgrItemCreatedAt`) in `src/db/items.ts`, which is **Lane B's region** (additive function, not `updateEmailmgrItem`) — a minor cross-lane placement the clean re-run should resolve. Also: `EVRYN_USER_ID`/`OPERATOR_USER_ID` were added as **local constants** in `poll.ts`/`process.ts` (matching the existing pattern; centralizing them is the separate deferred Step 42).
9. **The suspect QC also raised minor nits** beyond the items above (e.g. the bounce echoes the sender's own subject back; the `escalated` item has no `lifecycle[0]` entry; the escalation `notifySlack` writes no `messages`-audit row). **Minor — drop them; let a clean QC re-derive whatever's real.**
10. **Housekeeping:** the `.env` Justin copied into the worktree (for the dev rehearsal) holds live DB creds — it vanishes if the worktree is deleted, but if it's *shelved* rather than deleted, remove that `.env`.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
