# Handoff — AC0 → next AC0 — 2026-07-14 (ACf-focus stretch: AC7 CLOSED · ③ built-green/unmerged · 54a/54c merged · deploy bundle staged behind Gate-A/B)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** START HERE if you're a fresh AC0. You conduct the **product wave** (AC2 = cost/③, AC3 = identity/ops) under **ACU** (the meta-coordinator one level up — his `docs/working/2026.07.13-acu-uber-week-plan.md` shows the whole week; it is context, not gospel). You orchestrate, gate merges/deploys, reconcile cross-lane collisions, keep housekeeping current, translate to Justin at CEO altitude. **You do NOT write runtime code.** This handoff is **self-sufficient for the conductor view** — read a lane's brief in full on-demand when that lane surfaces work.

---

## 🌙 Context (why this stretch looks the way it does)
- **Justin backburnered the product wave for a few days to foreground ACf** (the autonomous team-agents build). You keep AC2/AC3 moving with **minimal Justin input**. This doc was written at a **night batten-down**: Justin commits it, and pushes everyone's everything later on his batched go-ahead (so at persist time, `_evryn-meta` local main sits a few commits ahead of origin — other agents' unpushed work, not a problem).

## ⚡ Operating model (this session's deltas — CARRY THESE)
- **PING `#team-alerts` EVERY response** (`AC0:` prefix). Non-negotiable, even when Justin seems present. *(I dropped this twice this session and got corrected — hold it hard.)*
- **Model = Opus-default + per-turn Fable escalation** (end the prior turn with `⚡ FABLE TURN NEXT: [what] — flip me, then say go`; flip back after). Reach for Fable only on: irreversible/expensive calls, many-interlocking-constraint decisions, heterogeneous synthesis, adversarial review of high-stakes work, framing-ambiguity. **Lane ACs (AC2/AC3) stay Opus**; AC2 has a **one-turn Fable option for the ③ design-synthesis turn**; the **③ merge-gate gets a dedicated Fable review** (ratified in the uber-week-plan model policy).
- **Lanes present recommend-first ballots STRAIGHT to Justin — NOT routed through AC0** (Justin's steer this session: routing through you adds cognitive-switching + "you have mail" overhead). You are conductor for **merges / collisions / housekeeping**, not a question-router. The time-discipline is drilled into the AC2/AC3 brief tails (2026-07-14).
- **Deploys: Justin does NOT run `railway up`** ("beyond my pay grade", 7/14) — **a lane or AC0 runs it, on Justin's explicit go**. You assemble + gate the bundle.
- **Commit/deploy gates are Justin's, per-action, don't stack.** Housekeeping (current-state / CHANGELOG / deploy-log / sprint marks) + mailbox/brief + working-doc/handoff commits are **pre-authorized** for you. **Stage by explicit path** — shared repos, and `_evryn-meta` has ACU's `ac-orchestration-protocol.md` + `uber-week-plan.md` **dirty (not yours — do NOT sweep)**.

## 📊 LIVE MAP (the conductor's picture — the important part)
- **Prod = `v0.2.7`** (`evryn-backend`), cost features dormant (Clustering + Haiku both live-OFF). Unchanged this stretch.
- **⭐ Step 57 / ③ (Runtime Bookkeeping) = DESIGNED + RATIFIED + DC-BUILT-GREEN, UNMERGED.** Justin ratified the design → **ADR-051** (verdict-time user-creation + an operator-only bounded correction tool; amends ADR-018/033/036). Built green on branch **`ac2/step57-runtime-bookkeeping`** (worktree `evryn-backend-step57`, tip ~`3266325`, 3 commits). It removes Evryn's raw `supabase_upsert` write surface, canonicalizes `triage_result` (all 3 verdicts), cuts a pass ~15→2-4 turns; **folds in** 54b forward-persistence (Justin picked option C) + user-creation-at-verdict + the note-turn `record_pass` residual + Step 71 (Reflection binding safety-net). Co-designs the tool-surface end-state with AC4's read-lock (③ removes the *write*, AC4 locked the *read*). Design doc + ADR-051 lead AC2's brief §0.
- **54a + 54c = MERGED to `evryn-backend` main `2572dbc` (now inside main `c9813a0`), NOT deployed.** QC-GO, identity-file-review 7/7 on 54a. **54a** = triage "have I met this person?" → the CONTACT's permanent record (Mira wording beat READY: `docs/working/2026.07.07-ac3-mira-54a-history-check-permanent-store.md`; **Justin hands it to Mira**). **54c** = real-name capture (`detect.ts`/`process.ts` `From: Real Name` + a Mira Opus-correction beat). Their two live behaviors **verify at Gate-B**. ③ rebases onto them; the ONLY collision is `src/email/process.ts`'s reification region (54c's `resolveReifiedDisplayName` ~L196 + one line ~L543); `triage.md`/`detect.ts` are clean.
- **AC7 = CLOSED + LANDED** across 3 repos (`_meta a9a34c0` / `backend c9813a0` / `team-ws c1d9ba8`), reaped, changelogged (`4dae58a`). **The doc-currency `#lock` gate + the new `docs/protocols/doc-update-routing-protocol.md` are now LIVE** — so the next `#lock` (the ③ deploy) runs under them.

## 🛣 Critical path to a gatekeeper (frame unchanged)
**③ → Gate-A (DEV live-path baseline, run WITH AC0, breaker OFF) → merge (+ Fable review + Justin go) → deploy THE BUNDLE → Gate-B (staging create-from-zero live-fire, everything active together) → flip Clustering + Haiku on → Justin account setups → gatekeeper.**
- **⚠ ③ is an interactive/tool-calling + money-path rewrite → the process lesson applies:** a breaker-tripped/stubbed/mocked test proves the *plumbing*, not the behavior. **Gate-A must be a live-path DEV exercise (breaker off, real model) BEFORE ③ merges** (SPRINT Step 88).
- **Gatekeeper:** Mark radio-silent-not-lost; a 2nd gatekeeper (Lacey) warming. Hold as "ready when a gatekeeper shows," not "waiting on Mark."

## 📦 THE DEPLOY BUNDLE (one coordinated `railway up` — AC0 assembles/gates; lane-or-AC0 runs it on Justin's go)
Carries **③ + 54a/54c + the Anthropic key-split env swap + the dead `_PROD` var cleanup**. Gated Gate-A → merge → Gate-B. At this deploy AC0 also runs the consolidated doc-currency pass (below).
- **Key-split** (runbook `docs/working/2026.07.13-acu-ac0-anthropic-key-split-runbook.md`, ACU-written): AC0-owned rider; **value-only env swap** on `evryn-backend` prod (`ANTHROPIC_API_KEY` → a dedicated Evryn-Production-workspace key), **no code**. Justin ~10 min console (J1–J5: verify ceiling, create workspace + limit, mint key, set on Railway). **No ACU questions** — the runbook is self-sufficient; team-runtime is subscription-auth and untouched. Rides the ③ deploy.
- **`_PROD` cleanup:** dead unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` on Railway prod; `railway variable delete` can't `--skip-deploys` → rides the same window.
- **Staging** for Gate-B: UP but **one migration short** (the clustering `queued` CHECK-widen). **DECIDED (AC0):** fold it into a single **pre-Gate-B fresh staging re-seed** from a current zero-PII prod-shape dump (keeps the seed files canonical + gives the live-fire its clean slate + absorbs any ③ migration) — **skip a one-off apply now** (nothing needs staging before Gate-B). Only trigger for the one-off: if someone needs staging for interim testing.

## 📌 AC0 OWED (standing — carry these)
1. **Gate-A WITH AC2 before ③ merges** — DEV live-path (breaker off). ③ pings you when built + rebased.
2. **Gate-B orchestration** — staging create-from-zero live-fire (needs AC3's `SEND_ENABLED` flip + the pre-Gate-B reseed + Megan-Griffiths fixtures; runner points at `evryn-backend/tests/fixtures/emails/megan/`).
3. **Part A doc-currency at the ③ deploy `#lock`:** **ARCH Step-57 absorption** (from ADR-051 — verdict-time creation, removed raw-write tool, canonicalized `triage_result`, operator-only correction tool; **needs Justin's auth** on the diff), plus the consolidated **CHANGELOG + deploy-log + current-state + team-cs**. Runs under AC7's new `#lock` gate + routing protocol. **OPEN (Justin hasn't answered):** draft the ARCH absorption ahead of the deploy vs at-deploy — **my rec: at-deploy** (ARCH should describe what's live).
4. **Housekeeping as-you-go** (CHANGELOG top priority); ping every response.
5. **Cross-lane merge order:** 54a/54c first (done) → ③ rebases (in progress). Hold the **"Mira-coordinate-once"** point (sequence the `triage.md` edits between 54a's beat and ③ so they don't collide).

## ✅ On Justin's plate
- Spin/keep the fresh AC2 (③) + AC3 (54a-c). Hand the 54a beat to Mira. Flip staging `SEND_ENABLED` at Gate-B. The Clustering + Haiku prod flips + account setups before a gatekeeper forwards. His go on the ③ merge + the bundle deploy. Answer the ARCH-timing ballot (rec: at-deploy).

## 🧭 Key docs
`docs/current-state.md` · `_evryn-meta/CHANGELOG.md` (tip of the July section is this AC7 entry) · `docs/deploy-log.md` · `evryn-backend/docs/SPRINT-V0.2-HARDENING.md` + `docs/decisions/051-*` (ADR-051) · lane briefs `docs/working/2026.06.30-ac0-ac2-cost-lane-brief.md` + `-ac3-identity-ops-brief.md` (both carry a 2026-07-14 autonomy tail) · `docs/working/2026.07.13-acu-uber-week-plan.md` · `docs/working/2026.07.13-acu-ac0-anthropic-key-split-runbook.md` · `_evryn-meta/CLAUDE.md` + `docs/protocols/{ac-orchestration-protocol,lock-protocol,doc-update-routing-protocol}.md`.

## 🔧 Repo state (as of this night persist — PUSH pending Justin's batched all-repos go)
- **All my session commits are already on origin:** autonomy tails `57745aa`, AC2 rebase relay `bc6afcf`, AC7 (`a9a34c0` / `c9813a0` / `c1d9ba8`), CHANGELOG `4dae58a`.
- **This handoff = the only new uncommitted work → committed now, push HELD** for Justin's batched go. `_evryn-meta` local main is a few commits ahead of origin (other agents' unpushed work + this handoff) — Justin pushes it all tonight.
- **Not mine — do NOT sweep:** `_evryn-meta` `ac-orchestration-protocol.md` + `uber-week-plan.md` dirty (ACU). `evryn-backend` has the active `evryn-backend-step57` worktree (AC2's ③). AC7 worktrees reaped; its orphan dir deleted by Justin.
- **Worktrees:** canonical trees + `evryn-backend-step57` (AC2, active). No stragglers.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
