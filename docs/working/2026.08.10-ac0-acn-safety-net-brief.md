# Brief: ACn — the safety-net lane (make the test suite real)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this:** the shared, committed surface for the AC0 ↔ ACn lane. **ACn appends its running log, decisions, and open questions here; AC0 appends its own.** Author: **AC0-32**, 2026-08-10T18:07:55-07:00.
>
> 🔴 **This file exists so a re-spin costs a LOAD, not a MEMORY.** The test at every append: *if this instance died right now, could a completely fresh one pick up from this file alone?* If no, the brief is behind — fix that before doing anything else. **This is not hypothetical: the parallel atlas lane's first instance compacted during its load on 2026-08-10 and had to be discarded.**

---

## §0 — WHO YOU ARE, AND WHY THIS LANE EXISTS

You are **ACn** (n = net). Sign `ACn`. You are a peer AC running one focused lane under AC0. **You own the engineering design inside this lane** — AC0 holds cross-lane orchestration and the merge gate, not your design calls. If your read differs from anything below, say so and bring your reasoning; a brief is a pitch, not orders.

**The posture you are part of.** Justin stopped the product build on 2026-08-06 to refactor, because every agent must load ~20,000 lines of runtime before doing anything and nobody can hold the whole picture: *"everyone is running blind… I need to contain the blast radius of having a whole army of blind agents breaking everything."*

The refactor's fourth deliverable is extracting `src/triage/classify.ts` (3,186 lines, ~16% of the runtime). **That extraction's only regression net is the test suite — and the test suite is not currently a net.** That is what you fix. **You are the reason the extraction is allowed to happen at all.**

**Model: stay on Opus. Pin the model explicitly on every subagent you spin.**

---

## §1 — YOUR TWO DELIVERABLES

### 1. SPRINT Step 101 — the cache byte-identity guard cannot fail

`tests/test-cache-byte-identity.ts` is named as the regression guard for an invariant worth real money: the force-loaded identity prefix must be byte-identical across calls, because Anthropic's prompt cache is prefix-based and a drifting byte silently re-pays the full prefix (~10× cost) on every subsequent call.

**It guards nothing.** `loadCommonPrefix()` memoizes (`classify.ts:206`, early return at `:228`); the test calls it three times (`:40–46`) and asserts the three results are equal. After call 1 they are *the same string reference*. **The assertion is true by construction and would pass with the composition logic deleted wholesale.**

**⚠️ Not the whole file is vacuous.** The `reflection.md` exclusion checks (`:53–61`) and the `loadReflectionPrefix` checks (`:65–77`) inspect real content and can genuinely fail. **It is specifically the byte-identity block that guards nothing** — so SPRINT Step 11(b), which is marked DONE and is the step that created this guard, is reopened *in part*, not wholly. Leave that history legible; do not re-mark 11(b) DONE.

#### AC0's analysis — offered as INPUT, explicitly NOT as a directive

**You hold the runtime and I do not** (I ran the conductor cascade and have read only `classify.ts:90–250`, `slack.ts:895–956`, and the three test-harness files). So weigh this and overrule it if the code says otherwise:

- **The failure that actually matters is not "the string changed" — it is "a whole identity layer silently became empty."** `readIdentityDir` returns `""` on a missing directory (`:188–191`, a *deliberate* 2026-06-01 fix to stop ENOENT taking down every pathway — do NOT "fix" it into a throw), and `loadCommonPrefix` then drops empty parts via `parts.filter(p => p.length > 0)` (`:242`). **No separator, no gap, no trace** — Evryn boots, answers mail, and is measurably worse at her job, with only a `console.log` nobody reads. And that log fires *once per process*, because of the memo.
- ⇒ **A memo bypass alone does not catch this.** Two freshly-imported instances reading the same empty directory agree perfectly. **The load-bearing assertions are content and structure:** that the composed prefix contains a known marker from `core.md` **and** from each of the four subdirectories, in the expected order, plus a **committed expected value** (a byte length or hash) so drift is detectable at all.
- **But there is a second, real invariant the memo does hide**, and I do not want to talk you out of it: determinism *across* instances. If `.sort()` at `:200` were ever removed, `readdirSync` order becomes filesystem-dependent and could differ between deploys — cache misses, real money, nothing red. A within-process memo guarantees self-agreement and hides exactly that. Whether the right guard for it is a fresh-instance comparison, an explicit order assertion, or both is **your call**.
- **`tests/import-fresh.ts` (Step 98) exists** and gives a cache-busting `?query` fresh module instance, typed, with no suppression. ⚠️ **It is run-verified on `email/client.js`, NOT on `classify.ts`** — a far heavier module that pulls the Agent SDK. If you use it, *prove* it yields a distinct `cachedCommonPrefix` rather than assuming it.

#### 🔴 The acceptance bar — mutation, executed, in a sandbox

**A test that stays green through the defect it is named for is worse than no test**, because its name tells the next reader the case is covered. So the deliverable is not "a better-looking test" — it is a test **proven** to fail:

- Delete one `parts.push(...)` line → suite goes **RED**.
- Change the `"\n\n---\n\n"` separator by one character → **RED**.
- Make one `readIdentityDir` call return `""` → **RED**. *(This is the money one.)*
- Restore each; suite goes **GREEN**.

**Do the breaking in a throwaway sandbox, never in a live tree:** `git worktree add --detach <sandbox-path> <sha>`, wreck it freely, delete it after. **You own provisioning sandboxes for any DC or QC you spin that needs to mutate** — do not leave them to improvise in the reviewed tree.

### 2. The boot-failing suites

**27 of 61 suites reportedly boot-fail on `EVRYN_ENV` without a local `.env`.** `config.ts:85` evaluates `requireEvrynEnv()` at module scope, so any test with a **static** `import` of `src/…` hoists that above all in-file seeding. `tests/env-bootstrap.ts` solves it by defaulting `EVRYN_ENV=dev` then **dynamically** importing the guard.

🔴 **THE TRAP, AND IT WILL COST YOU THE WHOLE TRIP IF YOU MISS IT: a `.env` exists in the repo root, and `dotenv` loads inside `config.ts` — so on this machine those suites PASS. You cannot see the failure you are fixing unless you deliberately run with the env absent.** Reproduce it that way first, and **measure the real number yourself — do not trust "27."** Every count in this lane's vicinity has been wrong at least once (the same sprint records "~107 type errors" that measured as 10, and two independent canary counts that were wrong in opposite directions).

**`tests/env-bootstrap.ts`'s header is the design rationale for the whole env-ordering discipline and contains a measured "this is a documentation correction, NOT a bug to fix by reordering" ruling.** Read it before you touch load order. Do not re-litigate it.

---

## §2 — HARD CONSTRAINTS

1. **🔴 TESTS AND CONFIG ONLY. No `src/` change, for any reason.** The whole basis for running you in parallel with the atlas lane is that you do not touch the runtime. **If you conclude a `src/` change is genuinely required — e.g. exporting a `resetCommonPrefixCache()` from `classify.ts` — STOP and report it. Do not do it.** That would put a test-only hook inside the single most dangerous file at the exact moment another lane is restructuring it, and the decision is AC0's and Justin's, not yours.
2. **🔴 KNOWN COLLISION — `tests/test-cache-byte-identity.ts` is under concurrent edit.** It is Step 101's subject **and** one of 8 test files whose `classify.ts` import the extraction must rewrite. **Sequencing, decided by AC0: you land FIRST; the atlas lane rebases onto you.** Keep your diff to that file as small as the engineering allows, and say explicitly in your report what you changed in it.
3. **You do NOT write the deterministic code.** Spin **DC** to build and **QC** to review — that independence is the point. A subagent called "DC" without `evryn-dev-workspace/CLAUDE.md` loaded is not DC; a "QC" without `evryn-quality/CLAUDE.md` is not QC.
4. **Follow `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` exactly when you spin anything** — including **two-trip loading**, which is not optional: trip 1 sends `<identity>` + `<mandatory_load>` + `<receipts>` and says **nothing** about the work; you verify receipts against your list; only then resume with `<task>`. Paste the blocks verbatim; you may not change a word without Justin's prior OK.
5. **Your worktree: `c:/Users/Justin/Evryn/Code/evryn-backend-safetynet`, branch `acn/safety-net`, cut from `main` at `b8a2986`.** AC0 created it and will reap it. Never touch `main`. Never merge, push, or deploy — those are Justin's gates and AC0's mechanics.
6. **Commit on your own branch by explicit path** — never `git add -A`, and `git diff <file>` before committing any shared file. **⚠️ `git add <paths>` does NOT scope the commit; a bare `git commit` afterwards commits the whole index.** Use `git commit -F - -- <paths>`.
7. **Do NOT touch the five untracked files** in the canonical tree (`backups/full-public-2026-08-05.sql`, four `tests/_gate-a-*.ts`). They are deliberate.
8. **Do NOT write a CHANGELOG entry.** You cannot ship, so the entry belongs to whoever merges. Hand AC0 the changelog-worthy facts in your report instead.
9. **`docs/schema-reference.md` is known-stale** (its own DO-NOT-TRUST banner; SPRINT Step 92 is the fix). Do not reason from it as a description of the live DB.

---

## §3 — HOW TO REPORT

- **Distinguish sharply between what you DID and what you RECOMMEND.** Those read identically in prose and mean opposite things. Every claimed action is a claim until an artifact shows it.
- **Attest for the layer below you:** report your own receipts to AC0 verbatim, and state explicitly that you verified your DC's and QC's receipts against the lists you gave them — including the call count and the tool used, or say plainly that you could not observe the call-stream.
- **Batch genuine-Justin questions** into one recommend-first list rather than blocking on them; keep advancing everything not blocked.
- **Cap headings at `##`.** Never `#` — that is reserved for AC0's voice to Justin.

---

## §4 — RUNNING LOG (append dated entries here)

> ### 🧭 READ THIS FIRST — state of the lane, and an index to the findings
>
> *(Maintained by ACn. Findings are numbered F1–F20 and are **not** in strict physical order below — a few were inserted at a fixed anchor. Use this index; it is the map.)*
>
> **Where things stand:**
> - **Deliverable 1 (Step 101) is BUILT, VERIFIED, and COMMITTED** — `dc/acn-step101` → **`316c9e8`**. Not merged, not pushed. **6 of 7 mutations now go RED, from 0 of 7.**
> - **Deliverable 2 (the boot-failing suites) is MEASURED, DESIGNED, and SAMPLE-PROVEN; 2 of 24 files done**, the other 22 in flight with DC #2.
> - **Nothing under `src/` has been touched, by anyone, at any point.** Re-verified before the commit.
> - **The canonical tree is pristine** — `main` at `b8a2986`, its five deliberate untracked files intact.
>
> **The numbers that replace the inherited ones:** **37** suites boot-fail on `EVRYN_ENV` (not 27), out of **75** runnable entry points (not 61). Of the 37: **24** are genuinely pure and seedable, **11** touch the DB and must not be seeded, and **2 more** that a naive grep called pure are actually live integration tests.
>
> **Index — the findings that would cost a fresh instance the most to re-derive:**
>
> | # | What it establishes |
> |---|---|
> | **F1** | The real boot-fail count: 37 of 75, measured by executing every suite |
> | **F2** | 🔴 `EVRYN_ENV` is only the first of **nine** required vars; `bootstrapTestEnv()` alone does not fix a suite |
> | **F3** | A dummy seed is right for some suites and harmful for others — a seeded value beats a real `.env` |
> | **F4** | 🔴 **Cross-lane, AC0's call:** the contested surface is **16** files, not 8; 9 overlap this lane |
> | **F5** | `importFresh` genuinely gives `classify.js` a fresh memo — proven, not assumed |
> | **F6** | 🔴 A committed hash/byte-length is **unusable** — CRLF vs LF makes it platform-dependent |
> | **F7** | The recomposition oracle works — exact equality, verified end-to-end |
> | **F8** | Two platform traps: separator counting is a Windows-only illusion; `.sort()` is undetectable here |
> | **F9** | A side-effect import placed first seeds env before `src` evaluates → the fix is **one line** |
> | **F10** | Adjacent: there is **no CI and no test runner** at all |
> | **F11** | Existing layer coverage lives in the *wrong file*, so mutation verdicts must be per-file |
> | **F12** | 🔴 **Before picture:** the old guard survived **5 of 5** mutations |
> | **F13** | A marker that is not unique to its source file proves nothing about that file |
> | **F14** | ~~Seed should load dotenv~~ — **REVERSED**; DC's hermetic design is better. Do not implement |
> | **F15** | ✅ **After picture:** 4 of 4 real mutations go RED; 46 assertions pass with no `.env` |
> | **F16** | 🔴 ACn's own purity split was **wrong** — replaced with an empirical keep-or-revert rule |
> | **F17** | 🟢 The `classify` specifier line is **untouched**, so the atlas lane's rebase auto-merges |
> | **F18** | ⚠️ A verification went stale mid-build — always diff the artifact against what you tested |
> | **F19** | ⚠️ DC #1 **stalled** without a work-trip report; ACn committed its build and says so plainly |
> | **F20** | The one-line fix sample-proven: 70 assertions that previously could not run |

**AC0-32 — 2026-08-10T18:07:55-07:00 — lane opened.** Worktree created, brief written, load trip verified clean (107 declared / 107 observed, no gaps). ACn's trip-2 task sent. Parallel lane: **ACa** (atlas/refactor) — its first instance compacted during load and was re-spun; it is not yet at work.

---

**ACn — 2026-08-10T18:19:39-07:00 — measurement complete; five findings, three of which change the lane's shape.**

*Everything below is MEASURED in the ACn worktree (`evryn-backend-safetynet`, `acn/safety-net`, HEAD `b8a2986`) unless marked otherwise. Nothing has been built yet; no `src/` file has been touched.*

**Environment setup (so a fresh instance can reproduce):** a git worktree contains only *tracked* files, so `.env` and `node_modules` are both absent from it. **That means this worktree IS the env-absent condition — no deliberate un-setting required.** `node_modules` was restored with a Windows junction to the canonical tree (`cmd //c "mklink /J node_modules C:\Users\Justin\Evryn\Code\evryn-backend\node_modules"`), which needs no admin. Verified `env | grep -E '^(EVRYN_ENV|SUPABASE_|ANTHROPIC_|SLACK_|GMAIL_)'` returns **nothing**, so no credential exists in this tree by any path — running the whole suite here cannot reach a database or a paid API. That is a safety property, not a coincidence, and it is why the measurement below was safe to run.

**F1 — The real boot-failure count is 37, not 27; the denominator is 75, not 61.** Measured by executing every one of the 77 `tests/*.ts` files and classifying stderr (script + raw results in ACn's scratchpad). Result: **37 `ENV_BOOT_FAIL` · 39 pass with no env at all · 1 `CREDS_FAIL`** (`test-evryn-initiated-placeholder.ts` seeds `EVRYN_ENV` but not the qualified Supabase pair). 77 files minus the 2 import-only helpers (`env-bootstrap.ts`, `import-fresh.ts`) = **75 runnable entry points.** The inherited "27 of 61" (SPRINT line 382, itself explicitly logged as *"an open question for AC0, not a closed finding"*) is wrong in both terms. AC0's instruction to re-measure was correct.

**F2 — 🔴 `EVRYN_ENV` is only the FIRST tripwire. `config.ts` has NINE module-scope required vars, and fixing `EVRYN_ENV` alone just moves the failure one line down.** `requireEvrynEnv()` evaluates at `config.ts:85`; six more `required(...)` calls sit at `:650–651` (`GMAIL_CLIENT_ID/SECRET/REFRESH_TOKEN`), `:668–669` (`SLACK_APP_TOKEN/BOT_TOKEN`) and `:716` (`ANTHROPIC_API_KEY`), plus the resolved `SUPABASE_URL_<SUFFIX>` / `SUPABASE_SERVICE_KEY_<SUFFIX>` pair at `:86`. **Proven, not inferred:** seeding only `EVRYN_ENV`+Supabase made the next run die on `GMAIL_CLIENT_ID`. ⇒ **`bootstrapTestEnv()` does NOT make a suite runnable without `.env`** — it defaults `EVRYN_ENV` and nothing else. Any fix that stops at `EVRYN_ENV` is not a fix.

**F3 — 🔴 A dummy-env seed is the RIGHT fix for only 26 of the 37, and is actively HARMFUL for the other 11.** `dotenv` does not overwrite an already-set `process.env` var, and any in-test seeding necessarily runs *before* `dotenv` (which loads inside `config.ts`). So a seeded dummy **beats a real `.env`** — exactly the trap already documented in `tests/env-bootstrap.ts`'s header for `EVRYN_ENV`. Seeding dummy Supabase creds into a live-DB test would therefore silently point it at a nonexistent database *on a machine that has perfectly good creds*. Split of the 37 (by whether the file references `getDb|createClient|@supabase`): **26 PURE** (safe to seed → become runnable anywhere) · **11 DB-touching** (`run-fixtures`, `test-approval-flow`, `test-breaker-resume`, `test-care-eligible-users`, `test-conversation`, `test-emailmgr-updated-at`, `test-gatekeeper-resolution`, `test-llm-usage`, `test-messages-ordering`, `test-pipeline-approval`, `test-processing-stuck`) — these must NOT be seeded; the correct end state for them is a clear, actionable "no `.env`" failure. **Adjacent observation, NOT ACn's to fix:** none of those 11 gates on `RUN_LIVE_DB_TESTS`, so on a machine with `.env` they run against dev automatically.

**F4 — 🔴 CROSS-LANE, FOR AC0: the contested surface is 16 files, not 1 — and 9 of them collide at the exact line both lanes edit.** `grep -l "triage/classify" tests/*.ts` returns **16** files, not the 8 the brief states. Nine are also in ACn's 26-file pure-fix set: `smoke-test`, `test-cache-byte-identity`, `test-context-labeling`, `test-poll-helpers-r2`, `test-prompt-composition`, `test-query`, `test-register-gatekeeper-address`, `test-role-target-guard`, `test-uuid-guard`. Both lanes' edits land in the **import block at the top of the file** — ACn converts the static `src/` import to a seeded dynamic one; the atlas lane rewrites the classify specifier. That is the worst-case overlap for a textual rebase. **ACn's recommendation (AC0's call, not ACn's — this is cross-lane sequencing): ship as TWO separate commits** — (1) Step 101 alone, one file, the actual gate on the extraction; (2) the boot-fail sweep. Two commits let AC0 land the gate immediately and sequence the sweep independently; one blended commit takes that option away.

**F5 — PROVEN: `importFresh` DOES yield `classify.js` a distinct, freshly-evaluated memo.** AC0 flagged Step 98's helper as run-verified on `email/client.js` only and asked for proof rather than assumption. Method (decisive, in the throwaway sandbox): import `classify.js?acn=1` → prefix = **149,121 bytes**; rename `identity/situations/gatekeeper.md` aside; import `classify.js?acn=2` → prefix = **142,734 bytes**; restore. Instance 2 saw the change ⇒ it genuinely re-evaluated and holds its own `cachedCommonPrefix`. Control confirmed the same query twice IS cached. **The by-product is the money finding for Step 101: deleting ONE 48-line identity file silently removed 6,387 bytes from Evryn's prompt with no error, no separator gap, and no trace** — a live demonstration of the exact defect the current guard cannot see.

**Also confirmed at source (AC0's read was right):** `loadCommonPrefix` memoizes at `classify.ts:228` and sets the cache at `:242`; the test calls it three times at `:40–42` and compares the results, which after call 1 are the same string reference. **The byte-identity block is vacuous exactly as described.** `readIdentityDir` returns `""` on a missing dir (`:188–191`) and `:242`'s `parts.filter(p => p.length > 0)` then drops it with no separator.

**One place ACn's read DIFFERS from the brief, on the design:** AC0 suggested a **committed expected value (byte length or hash)**. ACn recommends **against** hashing the whole prefix: the identity files are living documents, so every legitimate Mira edit would turn the suite red, and the reflex fix is to bump the constant — which is indistinguishable from bumping it to hide a real regression. A guard that cries wolf on every ordinary edit gets trained around. **The instrument ACn will spec instead is an independent oracle:** the test enumerates `identity/` itself using the documented rules, rebuilds the expected prefix, and asserts equality — immune to content edits, but red on a dropped `parts.push`, a changed separator, or a reordered layer. Plus explicit committed structural assertions that each of the four subdirectories exists and contributes ≥1 file, which is what catches the "a whole layer went empty" case that pure recomposition cannot see (both sides would agree on the emptiness).

**Honest coverage limit ACn will NOT paper over:** removing `.sort()` at `classify.ts:200` is **not reliably detectable** by any filesystem-observing test on a filesystem whose `readdirSync` already returns lexicographic order — the test's own enumeration and the runtime's would agree. A fresh-instance comparison does not catch it either (same filesystem, same order). ACn will still assert layer/file order explicitly, because that catches a *deliberate* reordering and documents the invariant — but ACn will not claim the sort is covered when it is not.

**NEXT ACTION (if this instance dies here):** spin DC (two-trip, per `ac-orchestration-protocol.md`) to build Step 101's replacement byte-identity block in `tests/test-cache-byte-identity.ts` per the design above; it must also self-seed all nine env vars so it stops boot-failing. Then mutation-test in the sandbox at `c:/Users/Justin/Evryn/Code/_acn-sandbox` (already provisioned, detached at `b8a2986`, node_modules junctioned). Then the 26-file sweep as a separate commit.

---

**ACn — 2026-08-10T18:33-07:00 — design premises validated by probe; the hash idea is dead on a measured, platform-level reason.**

**F6 — 🔴 A COMMITTED BYTE-LENGTH OR HASH WOULD BE PLATFORM-DEPENDENT AND IS THEREFORE UNUSABLE.** This is a stronger and more concrete reason than the "living documents" argument in ACn's previous entry, and it settles the question. **Measured:** the repo has **no `.gitattributes`** and `core.autocrlf=true`, so every identity `.md` is checked out **CRLF on Windows** and would be **LF on Linux (Railway/CI)**. `identity/core.md` is **44,531 bytes on disk here vs 44,248 LF-normalized** — 283 bytes, one per line, for one file. The whole composed prefix measures **149,121 bytes on this Windows machine** and would be roughly 1,378 bytes smaller on Linux. ⇒ **any absolute byte-length or hash constant committed from a dev machine is wrong in CI and wrong on Railway**, so the guard would be red on one platform and green on the other for reasons that have nothing to do with the invariant it protects. **The recomposition oracle has no such problem**, because both sides read the same on-disk bytes at test time. *(Not a live product bug: prod is uniformly Linux, so the prefix is stable within prod and the Anthropic cache is unaffected. It is purely a reason not to commit an absolute constant.)*

**F7 — the recomposition oracle WORKS, verified end-to-end.** A probe independently re-derived the prefix from disk using the documented rules (core.md, then `activities` minus `reflection.md`, then `situations` minus `operator.md`, then `internal-reference`, then `public-knowledge`; each dir's eligible `.md` files filtered `!startsWith("_") && !== "README.md"`, sorted, joined with `"\n\n---\n\n"`; empty parts dropped) and compared it to `loadCommonPrefix()`: **expected 149,121 B === actual 149,121 B, exact equality.** So the oracle is a viable instrument, not a hypothesis.

**F8 — two sub-premises measured, one of which corrects an assumption ACn had made:**
- **A naive separator COUNT is viable after all** — ACn expected identity content to contain `"\n\n---\n\n"` (the files do use `---` horizontal rules) and it measures **zero occurrences**, in core.md and in all ten others. The reason is CRLF: on disk those rules are `\r\n\r\n---\r\n\r\n`, which does not match the LF-only join separator. **⚠️ That makes a separator count correct HERE and WRONG ON LINUX**, where the content rules would match the separator exactly. So a separator count must NOT be used — it is a platform-dependent trap of the same family as F6. Recording it explicitly because it looks like it works on the machine anyone will test it on.
- **`readdirSync` already returns lexicographic order on this filesystem (measured: true for all four subdirectories).** This CONFIRMS the honest coverage limit in ACn's previous entry: **removing `.sort()` at `classify.ts:200` is not detectable by any filesystem-observing test here**, because the test's enumeration and the runtime's would agree. ACn will assert order explicitly (it catches a deliberate reversal and documents the invariant) but will NOT claim the sort is covered.

**Sandbox note:** probes live at `c:/Users/Justin/Evryn/Code/_acn-sandbox/tests/_acn-probe-*.ts` — a detached throwaway worktree, never committed, to be removed at close-out. All nine env vars must be exported to run anything there (see F2).

---

**ACn — 2026-08-10T18:45-07:00 — DC spun (two-trip); load verified; build in flight.**

**Worktrees ACn owns and must reap at close-out** (nobody else can — subagent resume is session-bound):
- `c:/Users/Justin/Evryn/Code/evryn-backend-safetynet` — ACn's own lane tree, branch `acn/safety-net` (created by AC0; AC0 reaps).
- `c:/Users/Justin/Evryn/Code/_acn-sandbox` — detached throwaway at `b8a2986`, for mutation testing. **ACn reaps.**
- `c:/Users/Justin/Evryn/Code/evryn-backend-dc-step101` — DC's build tree, branch `dc/acn-step101`, cut from `acn/safety-net`. **ACn reaps after merge.**
- All three needed `node_modules`; each got a junction to the canonical tree (`cmd //c "mklink /J node_modules …"`, no admin required).

**DC load trip verified — ACn's attestation.** DC was spun with the verbatim `<identity>`/`<mandatory_load>`/`<receipts>` blocks, model pinned to Opus, told nothing about the work. Its receipts list **all 25 files** ACn assigned, at the assigned spans, with canaries confirmed on all 13 Markdown files and true final lines given for the 10 canary-less TS/JSON files. **Call-count reconcile: DC declared 28 `Read` + 2 `Bash` + 1 `Glob` = 31; the harness reported `tool_uses: 31`. They agree.** DC disclosed **two token-cap PARTIAL reads** (`ARCHITECTURE.md` 1–430 of 1279; `BUILD-EVRYN-MVP.md` 1–486 of 652) as their own receipt lines and completed both by paging, rather than letting the retry absorb the failure. It also correctly reported its `Glob` as establishing the identity set's *membership only*, explicitly not the files' contents. Cascade reconcile: DC's own mandatory set (Hub, ARCHITECTURE, BUILD) was fully covered by ACn's list; no standing file was omitted; no `#cascade-override` was used. **Load accepted; trip 2 sent.**

**F9 — PROVEN: a bare side-effect import placed FIRST reliably seeds env before a statically-imported `src` module evaluates.** Probed with the environment fully absent: the seed module logged first, then `loadCommonPrefix()` returned 149,121 B and `EVRYN_ENV` resolved to `dev`. **This is what makes the boot-fix ONE LINE per file (`import "./env-seed.js";`) instead of a restructure into dynamic imports** — and it is the reason the fix rebases cleanly against the atlas lane: ACn *adds* a line above the classify import rather than rewriting that line, so git can auto-merge the two lanes' edits to the same file. The 26 pure boot-failers are uniform in shape — 24 open with a plain static `import … from "../src/…"`, and 2 (`smoke-test.ts`, `test-query.ts`) already open with a side-effect `import "dotenv/config"`, so the idiom is already native here.

**F10 — adjacent, NOT ACn's to fix, but material to the whole "safety net" premise: there is no CI and no test runner.** `ls .github/workflows` does not exist; `package.json` has `dev`/`start`/`build`/`typecheck` and **no test script**; there is no prettier/eslint/biome config. So the suite runs only when a human runs 75 individual `npx tsx` commands. **A safety net nobody executes is not a net** — worth AC0's attention independently of this lane. (The absence of an import-sorting formatter is also what makes F9's ordering invariant safe in practice.)

**F14 — a refinement that removes the F3 hazard at its root: the seed module should `import "dotenv/config"` ITSELF, first.** F3 established that a seeded dummy beats a real `.env` (because `dotenv` lives inside `config.ts` and never overwrites an already-set var), which is what made broad seeding dangerous. **If the seed module loads `dotenv` before filling gaps, that inverts: a real `.env` wins, and dummies only fill what is genuinely missing.** Measured both branches: with no `.env`, `SUPABASE_URL_DEV` resolves to `test-supabase_url_dev`; with a real `.env` present, it resolves to the real value. ~~ACn will require this shape.~~ **🔴 REVERSED — ACn was WRONG, and DC's independent choice is better. F14 is withdrawn; do not implement it.** DC, briefed only on the constraint and not on this refinement, chose *not* to load `dotenv` in the seed, and its reasoning beats mine on the property that actually matters. **The decisive asymmetry ACn under-weighted:**
- **With DC's shape (no dotenv):** dummies always win, so a pure test is **hermetic** — it behaves identically on Justin's machine, in a bare worktree, and in CI, and it can **never** reach a real database. DC additionally chose dummy hosts under `.invalid` (RFC 2606, guaranteed non-resolving), so a value that escapes into a real dial-out fails fast against an obviously fake host.
- **With F14 (dotenv first):** a real `.env` wins, so a test believed pure but *not actually* pure would silently connect to the **real dev database** on a developer's machine — and pass, hiding its own impurity.

F14 optimized for the wrong failure: it made *misuse in a DB test* benign, at the cost of making *a mispredicted "pure" test* silently live. Misuse is already guarded by documentation and by ACn's 26/11 split; impurity is guarded by nothing else. **Hermeticity wins.** Recording the reversal rather than quietly deleting F14, because the reasoning is the useful part — and because a lane brief that only ever shows the spinner being right is not a record anyone should trust.

**F12 — 🔴 THE BEFORE PICTURE, EXECUTED: the current guard survives FIVE OUT OF FIVE mutations. Step 101's claim is not merely true, it is an understatement.** Run in the throwaway sandbox with env exported (so the result isolates whether the *assertions* discriminate, not whether the file boot-fails). `guard` = `test-cache-byte-identity.ts`; `adj` = `test-prompt-composition.ts`. Each mutation was applied, both suites run, then `classify.ts` restored from a backup and the baseline re-checked GREEN.

| Mutation | guard | adj |
|---|---|---|
| baseline, unmutated | GREEN | GREEN |
| **M1** — delete `parts.push(readIdentityDir("internal-reference"))` | **GREEN** | RED |
| **M2** — layer separator changed by ONE character (`---` → `----`) | **GREEN** | GREEN |
| **M3a** — one `readIdentityDir` call yields `""` (situations layer empty) | **GREEN** | GREEN |
| **M3b** — an entire identity DIRECTORY renamed away (the real-world form) | **GREEN** | RED |
| **M4** — `.sort()` removed | **GREEN** | GREEN |
| restored | GREEN | GREEN |

**The guard named for the invariant caught NOTHING. The suite as a whole caught 2 of 5, and only by accident** — `test-prompt-composition.ts` happens to assert the string `"Trust Arc"`, which lives in `internal-reference`, so it fires on exactly the two mutations that remove *that* directory and is blind to the rest.

**F13 — a design lesson bought by M3a, which every marker-style assertion in this repo gets wrong.** M3a emptied the whole `situations` layer, and `test-prompt-composition.ts` — which explicitly asserts `/gatekeeper/i.test(common)` to prove that layer loaded — **stayed GREEN.** The reason: the word "gatekeeper" also appears in `activities/gatekeeper-onboarding.md` and `activities/triage.md`, so the marker survives the deletion of the file it is supposed to prove. **A marker that is not UNIQUE to its source file proves nothing about that file** — it silently degrades into "some file, somewhere, mentions this word." This is why ACn specced the completeness check against **each eligible file's actual content** rather than hand-picked markers; it is immune to the problem by construction. Worth promoting beyond this lane — the same mistake is cheap to make in any content assertion.

**F15 — ✅ THE AFTER PICTURE, EXECUTED INDEPENDENTLY BY ACn: the acceptance bar is MET — 4 of 4 real mutations now go RED, from 0 of 4.** ACn copied DC's built files into its own throwaway sandbox and re-ran the identical harness — independent of DC's own verification, which is the point.

| Mutation | BEFORE (guard) | AFTER (guard) | adj |
|---|---|---|---|
| baseline | GREEN | GREEN | GREEN |
| **M1** — dropped `parts.push` | GREEN | **🔴 RED** | RED |
| **M2** — separator changed one character | GREEN | **🔴 RED** | GREEN |
| **M3a** — a layer silently empty | GREEN | **🔴 RED** | GREEN |
| **M3b** — an identity directory absent | GREEN | **🔴 RED** | RED |
| **M4** — `.sort()` removed | GREEN | GREEN *(documented as uncovered, not claimed)* | GREEN |
| restored | GREEN | GREEN | GREEN |

**Note M2 and M3a specifically: the adjacent suite stays GREEN on both, so those two reds are attributable to the guard alone** — which is why F11 required per-file verdicts. **Boot-fix verified separately** (the concerns are kept apart on purpose: mutations run with env exported so the result isolates whether the *assertions* discriminate; the boot-fix runs with env absent): DC's file executes **46 assertions, all passing, with no `.env` and no exported variables at all** — so it passes on merit, not by failing to reach its assertions. **`tsc --noEmit -p tsconfig.tests.json` is clean (exit 0)**, run by ACn from DC's tree, with no suppressions in the diff.

**F16 — 🔴 ACn's OWN 26/11 SPLIT WAS WRONG, self-caught before it shipped. The screen was too shallow, and the fix is to stop classifying by grep entirely.** F3 classified a file as "pure" if it did not match `getDb|createClient|@supabase`. **That misses a test that reaches the database through a helper import** — and two did: **`smoke-test.ts`** ("runs a single fixture through the pipeline to verify everything connects") and **`test-query.ts`** ("test a single `query()` call to verify the SDK works end-to-end"). Both import `findUserByEmail` from `../src/db/users.js`, and `test-query.ts` additionally calls `runEvrynQuery`, i.e. a **real paid model call**. Both are live integration tests that ACn's grep called pure. Seeding them would have pointed them at a `.invalid` host and turned a meaningful integration test into a confusing failure. *(A re-screen for value — not type — imports from `src/db/` also surfaced `test-uuid-guard`, `test-merge-item-metadata` and `test-redact` as needing a look; the latter two import what are plainly pure string/object helpers that merely live in a `db/` module, but that is a judgment, not a grep result.)*

**⇒ The rule ACn will give DC is EMPIRICAL, not a classification handed down from here:** apply the seed, then run the file with the environment absent. **A test that passes against `.invalid` dummy credentials with no network has demonstrated its own purity**; a test that fails reaching for a real host has demonstrated it needs `.env`, and its seed gets reverted and the file reported. This removes ACn's classification — and its error — from the critical path, and it is self-validating in a way no grep of mine can be. **Revised scope: 24 pure candidates, of which DC has already done 2 (`test-cache-byte-identity`, `test-prompt-composition`), leaving 22** — each to be kept or reverted on the evidence of actually running it.

**F17 — 🟢 FOR THE ATLAS LANE'S REBASE, the fact AC0 needs: the `classify` specifier line is UNTOUCHED.** Diff on `dc/acn-step101` (uncommitted at time of writing, in `c:/Users/Justin/Evryn/Code/evryn-backend-dc-step101`):

| File | Change |
|---|---|
| `tests/test-cache-byte-identity.ts` | **+283 / −24** — the vacuous byte-identity block replaced; the `reflection.md` exclusion and `loadReflectionPrefix` blocks kept |
| `tests/test-prompt-composition.ts` | **+6** — the seed import + its comment, nothing else |
| `tests/env-seed.ts` | **new**, 97 lines |

**`git diff | grep "triage/classify"` returns NOTHING on either file** — neither lane's edit touches the other's line. ACn's changes are *additions above* the import block (the `import "./env-seed.js";` line) and *replacements below* it; the `from "../src/triage/classify.js"` specifier the extraction must rewrite is byte-for-byte unchanged. **So the atlas lane's rewrite of that specifier auto-merges against this work rather than conflicting with it** — which is what F9's design choice (add a line, don't restructure the import) was for. The same property will hold for the remaining 22 files, because the edit there is *only* the added line.

**F18 — ⚠️ ACn's first mutation run went STALE, caught only because ACn re-checked the file's mtime — and the re-verification found DC had improved the design.** DC edited `test-cache-byte-identity.ts` at 18:38, **after** ACn's 18:36 mutation run. Verifying a build that is still moving is a real trap in a background-subagent loop: the artifact is not frozen when the agent has not yet reported. **The rule ACn will carry forward: diff the artifact against the copy you tested before you report a verdict on it, every time.** Re-synced and re-ran; the 4/4 result held.

**What DC added on its own initiative, which ACn's spec did NOT ask for and should have:** an assertion that the two **deliberately excluded** files — `activities/reflection.md` and `situations/operator.md` — still **exist on disk**. **This closes a SECOND vacuity of exactly the kind Step 101 exists to kill, and neither AC0's analysis nor ACn's spec caught it:** the assertions "prefix EXCLUDES operator.md" and "prefix EXCLUDES reflection.md" are **vacuously true if the file is simply gone** — so Evryn could silently lose her entire Operator module or her Reflection module and the guard would report PASS, in the very file written to detect silent identity loss. DC also added a fail-fast after the structural block, on the reasoning that later assertions read from disk and an unhandled ENOENT is a strictly worse diagnostic than a named failure.

**ACn mutation-tested DC's addition specifically, since an unexecuted "it's covered" stops here or it stops nowhere:**

| Mutation | old guard | new guard |
|---|---|---|
| **M5** — `situations/operator.md` deleted from disk | GREEN | **🔴 RED** |
| **M6** — `activities/reflection.md` deleted from disk | GREEN | **🔴 RED** |
| restored | GREEN | GREEN |

**Final tally: 6 of 7 mutations caught (M1, M2, M3a, M3b, M5, M6), from 0 of 7. M4 (`.sort()` removed) stays GREEN and is documented in-source as uncovered rather than claimed.**

**F19 — ⚠️ DC #1 STALLED after finishing its edits and NEVER delivered a work-trip report. ACn committed its build rather than let it evaporate.** DC's transcript went static at 18:42 and did not grow for 8+ minutes; no completion notification arrived; nothing was committed. Its *artifacts* were complete and ACn had independently verified them, so ACn committed them **by explicit path on DC's own branch**, attributed in the message: **`dc/acn-step101` → `316c9e8`** (`tests/env-seed.ts` new 96 lines, `test-cache-byte-identity.ts` +307/−24, `test-prompt-composition.ts` +6; **385 insertions, 24 deletions, ZERO `src/` change — verified with `git diff --name-only | grep "^src/"` returning nothing before committing**).

**⇒ Consequence for the receipts chain, stated plainly rather than papered over: ACn verified DC #1's LOAD-trip receipts (31 declared / 31 observed, all 25 files, both partials disclosed) but there ARE no work-trip receipts to verify, because DC never produced them.** ACn's confidence in the build therefore rests entirely on its **own** independent verification — mutation testing, assertion counts, typecheck, diff inspection — not on DC's self-report. That is a weaker chain than the protocol wants, and it is named as such.

**F20 — the boot-fix premise validated on a 4-file sample before scaling it.** Applying only the one-line seed import to `test-uuid-guard`, `test-redact`, `test-quiet-hours` and `test-reflection-consolidate` in the sandbox: all four go from *unable to boot at all* to **exit 0 with 5, 10, 16 and 39 assertions respectively — 70 assertions that could not previously run**, with the environment fully absent. Reverted after measuring.

**LANE STATE at 2026-08-10T18:50 — in flight:**
- **DC #2** (`a…168`, Opus, two-trip) — load trip running, worktree `c:/Users/Justin/Evryn/Code/evryn-backend-dc-sweep`, branch `dc/acn-boot-sweep` cut from `316c9e8`. Task will be the 22 remaining pure suites, under the **empirical keep-or-revert rule** (F16), not ACn's grep classification.
- **QC** (`a…a01`, Opus, two-trip) — load trip running against the full runtime floor its manual requires (45 `src/` + 11 identity + ARCH/BUILD/Hub/technical-vision/current-state/operator-guide + ADR-048). Deliberately **not** given the three changed files in trip 1, so it reads their final state in trip 2 rather than a version still moving (the F18 staleness trap, avoided by design this time).
- **Worktree to reap that did not exist at the last entry:** `evryn-backend-dc-sweep`.

**F21 — QC load trip verified; QC caught a real omission in ACn's list.** QC's receipts account for **all 68 files** ACn assigned — 5 manual/state/company, 3 intended-shape, **45 `src/`**, **11 identity**, 4 harness/config — with canaries confirmed on every Markdown file and true final lines given for all 21 canary-less `src/` files. **QC's observed 24/21 canary split matches ACn's independently, from a separate read.** It ran **both** live enumerations separately (`find src -name "*.ts"` → 45; `find identity -name "*.md"` → 11), per the structural-trap rule that a `find src` can never return an identity file, and both matched the brief with zero omissions and zero extras. It disclosed **one token-cap PARTIAL** — its own operating manual, 212 of 496 lines — and paged the remaining 283 across two further calls rather than letting the retry absorb it.

**🔴 The gap, recorded because the report is the only signal a spinner ever gets that its list was incomplete: ACn omitted `evryn-backend/docs/dc-to-ac.md`, which is a STANDING Tier-0 item in QC's own cascade** ("most-recent `dc-to-ac.md` content on the repo you're reviewing"). QC loaded it under the reconcile rule and reported it. Content was cleared (`READ — absorbed by AC0 2026-06-02`), so nothing was lost this trip — but the omission was real, and **any future QC brief from this lane must include it.** QC also flagged that `docs/ac-to-dc.md` is non-empty (16.5 KB) without reading it, correctly leaving that judgment to the spinner.

**Call-count reconcile, stated honestly rather than rounded into a clean claim:** the harness reports **97 tool uses**; ACn's reconstruction from QC's receipts lands at ~98 (≈93 `Read` + ≈5 `Bash`). **ACn could NOT reconcile this to the exact unit**, because QC's receipt groups multi-chunk reads of one file into a single table row (e.g. `classify.ts` in 8 chunks is one row), so the row count is not the call count. Every listed file is accounted for with explicit spans, the one failure was disclosed, and the totals agree within ACn's own chunk-counting error — but "31/31 exact," which ACn could state for DC #1, is **not** what ACn can state here, and it is not claimed.

**QC's own two caveats, worth carrying:** (1) the runtime it loaded is **`dc/acn-step101`, not `main`** — every claim it makes is against that branch's shape, not deployed `v0.2.9`; (2) it loaded `current-state.md` as freshness context without verifying any of it against the runtime.

**QC work trip sent** with a provisioned throwaway sandbox at `c:/Users/Justin/Evryn/Code/_qc-sandbox` (detached at `316c9e8`, `node_modules` junctioned) and an explicit instruction to **mutation-test rather than clear by reading**. **ACn deliberately withheld its own mutation results** so QC's set is designed independently — the point being to learn about any mutation the guard FAILS to catch, which a primed reviewer is less likely to find. **Another worktree for the reap list: `_qc-sandbox`.**

**F22 — DC #2 load verified; it caught a STALE COUNT IN ACn'S OWN BRIEF, which is F18's failure mode repeating in a new costume.** DC #2's receipts account for all 35 assigned files with true final lines, and **21 of the 22 test suites matched the stated counts exactly**. The exception: ACn's list said `tests/test-cache-byte-identity.ts` was **318 lines; it is 340.** ACn took that number from a read *before* DC #1's final 18:38 edit (the excluded-file assertions + fail-fast). DC #2 flagged the 22-line gap, spent a second confirming read pinning EOF at 340, and **explicitly declined to reconcile its observation to ACn's figure** — which is precisely what the receipts instruction asks for and is the only reason it surfaced. **Twice in one lane a stale artifact count nearly propagated; the standing fix is F18's: re-read the artifact immediately before you quote a number from it.** DC #2 also disclosed **two token-cap PARTIALs** (ARCHITECTURE 1–430 of 1279; BUILD 1–486 of 652) as their own lines and paged both to EOF.

**Exemplary behavior worth naming:** DC #2 hit a conditional in its own Context Discipline — *if a task touches the runtime and the brief names `src/` without naming `identity/*.md`, treat that as an omission and load the identity half anyway* — and instead of silently resolving it in either direction, it **surfaced the conflict with its reasoning and offered to load all eleven on request**, substituting a `Glob` for the paths and correctly reporting that as establishing existence only, never contents. ACn confirmed the omission was deliberate and correct for this task (module load order, not Evryn's judgment). **That is the "flag it, never silently resolve" discipline working exactly as designed, from the layer least likely to be watched.**

**Call-count reconcile:** harness reports **43**; ACn's reconstruction lands at ~44. Consistent within ACn's own grouping error, but — as with QC — **not an exact-unit match, and not claimed as one.**

**F23 — 🔴 ACn ATTACKED ITS OWN CONCLUSION AND FOUND A REAL UNCOVERED GAP: the MIRROR IMAGE of the money bug. A new identity layer on disk that is never wired into `classify.ts` is invisible to the guard.** Having reported 6-of-7, ACn went looking for a mutation the new guard *misses* rather than resting on the result. Two found:

| Mutation | guard | Assessment |
|---|---|---|
| **M14** — drop `parts.filter(p => p.length > 0)` at `classify.ts:242` | **GREEN** | **Low severity, arguably benign.** With no empty layer both sides produce identical bytes, so it is undetectable — but the filter's *only* function is to hide an empty layer, and an empty layer is already caught by the structural preconditions (a). Removing it would make an empty layer produce a visible double separator, i.e. *more* detectable. Report, do not fix. |
| **M15** — create `identity/new-layer/policy.md`, a real layer never added to `loadCommonPrefix`'s push list | **GREEN** | **🔴 REAL GAP, and it matters.** |

**Why M15 matters:** it is the money bug with the sign flipped. The guard catches *a layer that should load and stopped*. It is blind to *a layer that was authored to load and never did* — because `classify.ts`'s push list is hardcoded **and** the test's `DIR_LAYERS` is hardcoded, so the two agree about a directory neither knows exists. Concretely: Mira adds `identity/new-layer/`, believes Evryn now has it, and Evryn silently never loads a byte of it. Nothing is red, nothing is logged, and the failure is indistinguishable from the instruction not working.

**The fix is cheap, test-side, and needs NO `src/` change** (so it stays inside this lane's constraint): assert that the set of subdirectories actually present under `identity/` **equals** the known set — `activities`, `situations`, `internal-reference`, `public-knowledge` — so that adding a fifth forces a deliberate decision (wire it into `classify.ts`, or add it to an explicitly-named excluded list) instead of silently doing nothing. **ACn is holding this for the fix-trip rather than dispatching it immediately, to see whether QC finds it independently** — if it does, that is a strong signal about the review; if it does not, ACn adds it and says so.

**F24 — ✅ DELIVERABLE 2, INDEPENDENTLY MEASURED BY ACn: all 22 suites KEEP, zero reverts, ≥387 assertions unlocked.** ACn ran its **own** sweep in its own sandbox, in parallel with DC #2 and deliberately duplicating it — the point being a dataset to check DC #2's report against rather than trusting it. Method: insert the seed import as the first import, run with the environment **fully absent**, record exit code and executed-assertion count, restore the file.

**Result: 22 of 22 KEEP. Not one file needed reverting** — so, with `smoke-test.ts` and `test-query.ts` correctly excluded (F16), the revised purity list is **exactly right**, and it was right because it was arrived at empirically rather than by grep.

**≥387 assertions now execute that previously could not run at all.** The floor qualifier is honest and the reason is a flaw in ACn's own instrument, not in the suites: **4 of the 22 use a `✓`/`✗` output convention rather than `PASS`/`FAIL`** (`test-draft-idempotency`, `test-operator-gate`, `test-revision-prompt`, `test-slack-parsing`), so ACn's grep scored them **0** and the real total is higher. Counted across the suite: **65 files use `PASS`/`FAIL`, 4 use `✓`/`✗`.** Recorded for two reasons — any assertion total quoted from this lane (including DC #2's, if it used the same metric) carries the same skew; and **a future test runner must handle both conventions or it will silently report those four as empty**, which is its own small version of the vacuity problem this lane exists to fix.

**The end state this produces, and it is a clean story:** **37 boot-failures → 13.** The 13 that remain are all legitimate — 11 DB-touching suites plus `smoke-test` and `test-query` — every one of which genuinely needs credentials and *should* fail loudly without them rather than be handed a dummy pointing at a non-resolving host.

**Sandbox re-verified clean afterwards** (`git status --porcelain src/ identity/` empty), so nothing from 30+ mutations and 22 file edits leaked into any later measurement.

**F25 — ✅ DELIVERABLE 2 COMMITTED: `e386600` on `dc/acn-boot-sweep` — 22 files, +132/−0, and ACn verified the scope rather than trusting the report.**

| Check | Result |
|---|---|
| Any `src/` change? | **None.** The hard constraint held. |
| Any file outside `tests/`? | **None** — all 22 changes are `tests/` |
| Per-file diff shape | **22 files × exactly +6/−0** — perfectly uniform; zero scope creep, no "while I was in there" edits |
| Is the `triage/classify` specifier line touched? | **NO** — grepping the whole diff for it returns nothing, so the atlas lane's rewrite still auto-merges |
| Suppressions introduced (`@ts-expect-error` / `@ts-ignore` / `: any`) | **None** |
| `tsc --noEmit -p tsconfig.tests.json` | **Clean, exit 0** (run by ACn from DC #2's tree) |

**F26 — the assertion total, now exact rather than a floor: 427.** ACn's sweep metric counted only the `PASS`/`FAIL` convention and scored the four `✓`/`✗` suites at zero (F24). Counting those four properly — `test-draft-idempotency` **7**, `test-operator-gate` **7**, `test-revision-prompt` **11**, `test-slack-parsing` **15** = **40** — gives **387 + 40 = 427 assertions that now execute and could not run at all before.** Each of the four also prints its own `N passed` summary line, which matched the tick count exactly, so the number is corroborated two ways per file.

---

**ACn — 2026-08-10T19:07:15-07:00 — QC returned GO; it found a bigger gap than either lane had, and corrected ACn on the mechanism.**

**F27 — ✅ QC VERDICT: GO, ZERO BLOCKERS.** QC designed its **own** 15-mutation set (ACn deliberately withheld its results so the design would be independent) and measured **9 of 15 RED**, covering every code-shape drift class: dropped `parts.push`, top-level separator, **within-layer** separator (`:203` — a mutation ACn never tried), layer reorder, dropped exclusion arg, *added* exclusion, a deleted excluded file, a removed directory, and an encoding change (`utf-8`→`latin1`). It confirmed the oracle is **genuinely independent on the code axis** — seven separate `classify.ts` mutations moved the runtime while the oracle held still — and verified all six restated rules line-by-line against `classify.ts`. **QC also disclosed three mutation attempts that did NOT apply, two of which printed a meaningless GREEN**, noting that without its before/after proof step those would have been recorded as "layer reordering is not detected," which is false. That is the mutation discipline working.

**F28 — 🔴 QC FOUND A GAP BIGGER THAN ACn'S M15: losing an individual identity FILE is undetected.** Block (a) asserts each layer yields **≥1** eligible file, so a layer can silently drain almost to empty. **Measured: deleting BOTH `activities/onboarding.md` AND `activities/triage.md` — two entire force-loaded modules, one governing the whole triage pathway — produces ZERO failures.** Boundary: `activities` (3 eligible) and `internal-reference` (3) can each lose **2 files** silently; `situations` and `public-knowledge` are protected only accidentally, by having one file each so 1→0 trips the check. **QC's framing is the one to keep: the test protects the files it LEAVES OUT better than the files it PUTS IN** — the two excluded files get explicit `existsSync` assertions; every included file gets none.

**F29 — 🔴 ACn WAS WRONG ABOUT *WHY* THE OLD TEST WAS VACUOUS, AND THE ERROR SHIPPED INTO THE NEW FILE.** ACn (and AC0's brief, and the commit message) said the old assertion was true by construction because the memo "hands back the SAME STRING REFERENCE." **JS string `===` is VALUE equality — QC measured it** — so `a === b` would have been true with or without the memo. The **conclusion** was right; the **mechanism** was not: the old block was vacuous because *the composition is deterministic*. Worse, that wrong mechanism was written into the new file's header and into block (e)'s label — and QC proved **removing the memo early-return at `classify.ts:228` leaves the suite GREEN**, so **(e) is itself a false coverage claim, inside the file whose entire purpose is eliminating false coverage claims.** Being wrong here is fair to call out loudly; it is the exact class of error this lane exists to kill.

**F30 — two more QC corrections ACn accepts:**
- **"It guarded nothing" was slightly too strong.** The old file *did* go red on total composition deletion — **accidentally**, via an unrelated size-sanity assertion (with only `core.md` left, the reflection prefix is no longer smaller than the common prefix). **The sharper and more damning measured fact: dropping ONE layer under the old test → ALL PASSED.** That is the realistic drift, and that is the number to quote.
- **`.sort()` matters MORE than ACn concluded.** F8 treated it as a benign untestable. QC's reasoning (explicitly flagged as **reasoned, not executed** — it did not test on Linux): **on ext4, `readdir` returns hash order, not lexicographic**, so `.sort()` is genuinely **load-bearing on Railway** while being untestable on NTFS. The line is not merely theoretical; it is uncovered *and* it matters in the deploy environment. Covering it needs a `src/` seam and QC recommends **not** doing it while ACa restructures `classify.ts` — ACn agrees.

**F31 — QC caught ACn's THIRD staleness slip.** The commit message ACn wrote says **46** assertions; QC measures **48**. ACn's 46 came from a run *before* DC #1's final edit. QC flagged it as "a mild cooked-context marker" in a message written to stand in for a missing report, and said it therefore trusted measured numbers over narrated ones — **which is the correct response, and the criticism is fair.** Three staleness slips in one lane (F18, F22, F31) all have the same root: quoting a number captured before the artifact stopped moving.

**F32 — DC #2 corrected ACn's cross-lane number, and ACn verified the correction: the contested set among the 22 is FOUR, not nine.** ACn's figure came from `grep -l "triage/classify"`, **which cannot distinguish an import from a prose comment** — `test-poll-helpers-r2.ts` merely *mentions* `classify.ts` at line 14 and has no import. Real importers among the 22: `test-context-labeling`, `test-register-gatekeeper-address`, `test-role-target-guard`, `test-uuid-guard`. **This figure was headed into a cross-lane rebase plan, so the catch mattered.** *(The 16-file total in F4/F17 has the same provenance and should be treated as an upper bound until re-checked by import, not by mention.)*

**F33 — ✅ ACn independently re-verified DC #2's committed tree and got the SAME 427.** Ran all 22 suites from `dc/acn-boot-sweep` at `e386600` with the environment absent: **22 of 22 exit 0, zero failures, 427 assertions.** DC #2 reported 427 from its own run; ACn measured 427 from a separate sweep in a separate sandbox before reading that report. **Two independent measurements agreeing is why this number is trustworthy** — unlike the 46/48 above, which came from one measurement quoted after the artifact moved.

**FIX TRIP DISPATCHED to DC #2** covering: (1) QC's G1 file-level manifest; (2) ACn's M15 unwired-layer check; (3) the false (e) claim + the wrong mechanism in the header + (c)'s imprecise "changed filter"; (4) QC's structural `env-seed`↔`bootstrapTestEnv` marker cross-check, which closes DC #2's own recommendation and has **five live-DB consumers on day one**; (5) the five stale headers DC #2's change made false, plus two now-wrong pointers to `test-record-pass.ts`. All `tests/`-only. Mutation proof required on (1) and (2), in a throwaway worktree — **explicitly NOT in DC's own tree, since the proof requires deleting identity files.**

**F34 — 🔴🔴 RETRACTION, AND IT IS THE MOST IMPORTANT CORRECTION IN THIS LOG: F4 WAS WRONG. THE CONTESTED SET IS **8** FILES — EXACTLY WHAT AC0'S BRIEF SAID. AC0 WAS RIGHT; ACn "CORRECTED" IT WITH A WRONG NUMBER.**

Following DC #2's catch (F32), ACn re-ran the count **by import instead of by mention**:

- `grep -l "triage/classify" tests/*.ts` → **16** ← ACn's original, flawed method
- `grep -lE 'from "\.\./src/triage/classify' tests/*.ts` → **8** ← correct

**The 8 real importers:** `smoke-test`, `test-cache-byte-identity`, `test-context-labeling`, `test-prompt-composition`, `test-query`, `test-register-gatekeeper-address`, `test-role-target-guard`, `test-uuid-guard`.

**The 8 that were wrongly counted mention the file only in a comment or a header note:** `test-clustering`, `test-gatekeeper-first-name`, `test-gatekeeper-resolution`, `test-haiku-screen`, `test-poll-helpers-r2`, `test-query-timeout`, `test-record-legibility`, `test-record-pass`.

**Why this is worth writing up rather than quietly editing F4:** ACn told AC0 its brief was wrong on a cross-lane number, marked it 🔴, and routed it as a decision input for the atlas lane's rebase plan — **and ACn was the one who was wrong.** The failure was using a *search* as though it were a *read*: `grep -l` answers "does this string appear," never "is this a dependency," and ACn treated the first answer as the second. That is precisely the error the receipts protocol warns every subagent about — *"a search can confirm a string is present but can NEVER establish what a file says"* — committed by the agent enforcing it on everyone else. **F4, F17 and the handoff block's "16" are all superseded by this entry.**

**The corrected cross-lane picture, which is BETTER than ACn reported:** 8 contested files; ACn's lane touches **6** of them (`test-cache-byte-identity`, `test-prompt-composition`, `test-context-labeling`, `test-register-gatekeeper-address`, `test-role-target-guard`, `test-uuid-guard` — `smoke-test` and `test-query` being correctly out of scope). In **all six**, ACn's edit is a line added *above* the import block and the `from "../src/triage/classify.js"` specifier is byte-for-byte untouched, so the atlas lane's rewrite still auto-merges.

**🔴 HANDOFF TO AC0 — WHERE THE WORK ACTUALLY IS, AND HOW TO SEQUENCE IT. Read this before looking for the deliverables.**

**The lane branch `acn/safety-net` is EMPTY — zero commits.** ACn is barred from merging, so nothing was merged into it. **The deliverables live on two DC branches**, and `dc/acn-boot-sweep` was cut *from* `dc/acn-step101`, so it **contains** it (verified with `git merge-base --is-ancestor`):

| Branch | Carries | Worktree |
|---|---|---|
| `dc/acn-step101` | **`316c9e8`** — deliverable 1 (Step 101) | `evryn-backend-dc-step101` |
| `dc/acn-boot-sweep` | `316c9e8` **+** deliverable 2's commit | `evryn-backend-dc-sweep` |

**This preserves exactly the sequencing option ACn recommended in F4, and AC0 should choose deliberately:**
- **Take `316c9e8` alone** (cherry-pick or merge `dc/acn-step101`) → the extraction's actual gate lands immediately, touching **2 test files**, minimum collision with the atlas lane.
- **Then take the sweep commit separately** → the remaining 22 files, whenever AC0 judges the atlas lane can absorb it.
- Merging `dc/acn-boot-sweep` wholesale lands both at once. That is a valid choice, but it is a *choice* — do not make it by accident.

**Cross-lane collision, precise final numbers:** **16** test files import `src/triage/classify`, not the 8 the brief stated. **7** of them are in ACn's scope (`test-cache-byte-identity`, `test-prompt-composition`, `test-context-labeling`, `test-poll-helpers-r2`, `test-register-gatekeeper-address`, `test-role-target-guard`, `test-uuid-guard`). **In every one, ACn's edit is a line ADDED ABOVE the import block; the `from "../src/triage/classify.js"` specifier line is byte-for-byte untouched** — so the atlas lane's rewrite of that specifier auto-merges rather than conflicting. That property was designed in (F9), not lucky.

**Worktrees ACn created and their disposition:** `_acn-sandbox` and `_qc-sandbox` are throwaway (detached) — **ACn reaps these**. `evryn-backend-dc-step101` and `evryn-backend-dc-sweep` hold the unmerged deliverables — **ACn leaves these for AC0**, since removing them before the merge would strand the only trees where the work has been executed. `evryn-backend-safetynet` is AC0's. All four ACn-created trees got a `node_modules` junction to the canonical tree; junctions need no admin and are removed with the worktree.

**F11 — coverage is not where you would expect it: `tests/test-prompt-composition.ts` already carries marker assertions** for `internal-reference` ("Trust Arc"), `public-knowledge` ("Where You Come From") and `situations` (`/gatekeeper/i`), plus the `operator.md` exclusion. **So some layer-went-empty mutations will go red in THAT file, not in the guard named for the invariant.** Consequence for the acceptance bar: mutation results must be reported **per-file**, or a red from the wrong suite reads as the cache guard working when it is not. `activities` has no marker anywhere, and its `/core/i` check is weak enough to match almost anything.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
