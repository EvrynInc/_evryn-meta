# Brief: ACn — the safety-net lane (make the test suite real)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this:** the shared, committed surface for the AC0 ↔ ACn lane. **ACn appends its running log, decisions, and open questions here; AC0 appends its own.** Author: **AC0-32**, 2026-08-10T18:07:55-07:00.
>
> 🔴 **This file exists so a re-spin costs a LOAD, not a MEMORY.** The test at every append: *if this instance died right now, could a completely fresh one pick up from this file alone?* If no, the brief is behind — fix that before doing anything else. **This is not hypothetical: the parallel atlas lane's first instance compacted during its load on 2026-08-10 and had to be discarded.**

---

## §0 — WHO YOU ARE, AND WHY THIS LANE EXISTS

You are **ACn** (n = net). Sign `ACn`. You are a peer AC running one focused lane under AC0. **You own the engineering design inside this lane** — AC0 holds cross-lane orchestration and the merge gate, not your design calls. If your read differs from anything below, say so and bring your reasoning; a brief is a pitch, not orders.

**The posture you are part of.** Justin stopped the product build on 2026-08-06 to refactor, because every agent must load ~20,000 lines of runtime before doing anything and nobody can hold the whole picture: *"everyone is running blind… I need to contain the blast radius of having a whole army of blind agents breaking everything."*

The refactor's fourth deliverable is extracting `src/triage/classify.ts` (3,186 lines, ~16% of the runtime). **That extraction's only regression net is the test suite — and the test suite is not currently a net.** That is what you fix. **You are the reason the extraction is allowed to happen at all.**

**Model: stay on Opus. Pin the model explicitly on every subagent you spin.**

---

## §1 — YOUR TWO DELIVERABLES

### 1. SPRINT Step 101 — the cache byte-identity guard cannot fail

`tests/test-cache-byte-identity.ts` is named as the regression guard for an invariant worth real money: the force-loaded identity prefix must be byte-identical across calls, because Anthropic's prompt cache is prefix-based and a drifting byte silently re-pays the full prefix (~10× cost) on every subsequent call.

**It guards nothing.** `loadCommonPrefix()` memoizes (`classify.ts:206`, early return at `:228`); the test calls it three times (`:40–46`) and asserts the three results are equal. After call 1 they are *the same string reference*. **The assertion is true by construction and would pass with the composition logic deleted wholesale.**

**⚠️ Not the whole file is vacuous.** The `reflection.md` exclusion checks (`:53–61`) and the `loadReflectionPrefix` checks (`:65–77`) inspect real content and can genuinely fail. **It is specifically the byte-identity block that guards nothing** — so SPRINT Step 11(b), which is marked DONE and is the step that created this guard, is reopened *in part*, not wholly. Leave that history legible; do not re-mark 11(b) DONE.

#### AC0's analysis — offered as INPUT, explicitly NOT as a directive

**You hold the runtime and I do not** (I ran the conductor cascade and have read only `classify.ts:90–250`, `slack.ts:895–956`, and the three test-harness files). So weigh this and overrule it if the code says otherwise:

- **The failure that actually matters is not "the string changed" — it is "a whole identity layer silently became empty."** `readIdentityDir` returns `""` on a missing directory (`:188–191`, a *deliberate* 2026-06-01 fix to stop ENOENT taking down every pathway — do NOT "fix" it into a throw), and `loadCommonPrefix` then drops empty parts via `parts.filter(p => p.length > 0)` (`:242`). **No separator, no gap, no trace** — Evryn boots, answers mail, and is measurably worse at her job, with only a `console.log` nobody reads. And that log fires *once per process*, because of the memo.
- ⇒ **A memo bypass alone does not catch this.** Two freshly-imported instances reading the same empty directory agree perfectly. **The load-bearing assertions are content and structure:** that the composed prefix contains a known marker from `core.md` **and** from each of the four subdirectories, in the expected order, plus a **committed expected value** (a byte length or hash) so drift is detectable at all.
- **But there is a second, real invariant the memo does hide**, and I do not want to talk you out of it: determinism *across* instances. If `.sort()` at `:200` were ever removed, `readdirSync` order becomes filesystem-dependent and could differ between deploys — cache misses, real money, nothing red. A within-process memo guarantees self-agreement and hides exactly that. Whether the right guard for it is a fresh-instance comparison, an explicit order assertion, or both is **your call**.
- **`tests/import-fresh.ts` (Step 98) exists** and gives a cache-busting `?query` fresh module instance, typed, with no suppression. ⚠️ **It is run-verified on `email/client.js`, NOT on `classify.ts`** — a far heavier module that pulls the Agent SDK. If you use it, *prove* it yields a distinct `cachedCommonPrefix` rather than assuming it.

#### 🔴 The acceptance bar — mutation, executed, in a sandbox

**A test that stays green through the defect it is named for is worse than no test**, because its name tells the next reader the case is covered. So the deliverable is not "a better-looking test" — it is a test **proven** to fail:

- Delete one `parts.push(...)` line → suite goes **RED**.
- Change the `"\n\n---\n\n"` separator by one character → **RED**.
- Make one `readIdentityDir` call return `""` → **RED**. *(This is the money one.)*
- Restore each; suite goes **GREEN**.

**Do the breaking in a throwaway sandbox, never in a live tree:** `git worktree add --detach <sandbox-path> <sha>`, wreck it freely, delete it after. **You own provisioning sandboxes for any DC or QC you spin that needs to mutate** — do not leave them to improvise in the reviewed tree.

### 2. The boot-failing suites

**27 of 61 suites reportedly boot-fail on `EVRYN_ENV` without a local `.env`.** `config.ts:85` evaluates `requireEvrynEnv()` at module scope, so any test with a **static** `import` of `src/…` hoists that above all in-file seeding. `tests/env-bootstrap.ts` solves it by defaulting `EVRYN_ENV=dev` then **dynamically** importing the guard.

🔴 **THE TRAP, AND IT WILL COST YOU THE WHOLE TRIP IF YOU MISS IT: a `.env` exists in the repo root, and `dotenv` loads inside `config.ts` — so on this machine those suites PASS. You cannot see the failure you are fixing unless you deliberately run with the env absent.** Reproduce it that way first, and **measure the real number yourself — do not trust "27."** Every count in this lane's vicinity has been wrong at least once (the same sprint records "~107 type errors" that measured as 10, and two independent canary counts that were wrong in opposite directions).

**`tests/env-bootstrap.ts`'s header is the design rationale for the whole env-ordering discipline and contains a measured "this is a documentation correction, NOT a bug to fix by reordering" ruling.** Read it before you touch load order. Do not re-litigate it.

---

## §2 — HARD CONSTRAINTS

1. **🔴 TESTS AND CONFIG ONLY. No `src/` change, for any reason.** The whole basis for running you in parallel with the atlas lane is that you do not touch the runtime. **If you conclude a `src/` change is genuinely required — e.g. exporting a `resetCommonPrefixCache()` from `classify.ts` — STOP and report it. Do not do it.** That would put a test-only hook inside the single most dangerous file at the exact moment another lane is restructuring it, and the decision is AC0's and Justin's, not yours.
2. **🔴 KNOWN COLLISION — `tests/test-cache-byte-identity.ts` is under concurrent edit.** It is Step 101's subject **and** one of 8 test files whose `classify.ts` import the extraction must rewrite. **Sequencing, decided by AC0: you land FIRST; the atlas lane rebases onto you.** Keep your diff to that file as small as the engineering allows, and say explicitly in your report what you changed in it.
3. **You do NOT write the deterministic code.** Spin **DC** to build and **QC** to review — that independence is the point. A subagent called "DC" without `evryn-dev-workspace/CLAUDE.md` loaded is not DC; a "QC" without `evryn-quality/CLAUDE.md` is not QC.
4. **Follow `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` exactly when you spin anything** — including **two-trip loading**, which is not optional: trip 1 sends `<identity>` + `<mandatory_load>` + `<receipts>` and says **nothing** about the work; you verify receipts against your list; only then resume with `<task>`. Paste the blocks verbatim; you may not change a word without Justin's prior OK.
5. **Your worktree: `c:/Users/Justin/Evryn/Code/evryn-backend-safetynet`, branch `acn/safety-net`, cut from `main` at `b8a2986`.** AC0 created it and will reap it. Never touch `main`. Never merge, push, or deploy — those are Justin's gates and AC0's mechanics.
6. **Commit on your own branch by explicit path** — never `git add -A`, and `git diff <file>` before committing any shared file. **⚠️ `git add <paths>` does NOT scope the commit; a bare `git commit` afterwards commits the whole index.** Use `git commit -F - -- <paths>`.
7. **Do NOT touch the five untracked files** in the canonical tree (`backups/full-public-2026-08-05.sql`, four `tests/_gate-a-*.ts`). They are deliberate.
8. **Do NOT write a CHANGELOG entry.** You cannot ship, so the entry belongs to whoever merges. Hand AC0 the changelog-worthy facts in your report instead.
9. **`docs/schema-reference.md` is known-stale** (its own DO-NOT-TRUST banner; SPRINT Step 92 is the fix). Do not reason from it as a description of the live DB.

---

## §3 — HOW TO REPORT

- **Distinguish sharply between what you DID and what you RECOMMEND.** Those read identically in prose and mean opposite things. Every claimed action is a claim until an artifact shows it.
- **Attest for the layer below you:** report your own receipts to AC0 verbatim, and state explicitly that you verified your DC's and QC's receipts against the lists you gave them — including the call count and the tool used, or say plainly that you could not observe the call-stream.
- **Batch genuine-Justin questions** into one recommend-first list rather than blocking on them; keep advancing everything not blocked.
- **Cap headings at `##`.** Never `#` — that is reserved for AC0's voice to Justin.

---

## §4 — RUNNING LOG (append dated entries here)

**AC0-32 — 2026-08-10T18:07:55-07:00 — lane opened.** Worktree created, brief written, load trip verified clean (107 declared / 107 observed, no gaps). ACn's trip-2 task sent. Parallel lane: **ACa** (atlas/refactor) — its first instance compacted during load and was re-spun; it is not yet at work.

---

**ACn — 2026-08-10T18:19:39-07:00 — measurement complete; five findings, three of which change the lane's shape.**

*Everything below is MEASURED in the ACn worktree (`evryn-backend-safetynet`, `acn/safety-net`, HEAD `b8a2986`) unless marked otherwise. Nothing has been built yet; no `src/` file has been touched.*

**Environment setup (so a fresh instance can reproduce):** a git worktree contains only *tracked* files, so `.env` and `node_modules` are both absent from it. **That means this worktree IS the env-absent condition — no deliberate un-setting required.** `node_modules` was restored with a Windows junction to the canonical tree (`cmd //c "mklink /J node_modules C:\Users\Justin\Evryn\Code\evryn-backend\node_modules"`), which needs no admin. Verified `env | grep -E '^(EVRYN_ENV|SUPABASE_|ANTHROPIC_|SLACK_|GMAIL_)'` returns **nothing**, so no credential exists in this tree by any path — running the whole suite here cannot reach a database or a paid API. That is a safety property, not a coincidence, and it is why the measurement below was safe to run.

**F1 — The real boot-failure count is 37, not 27; the denominator is 75, not 61.** Measured by executing every one of the 77 `tests/*.ts` files and classifying stderr (script + raw results in ACn's scratchpad). Result: **37 `ENV_BOOT_FAIL` · 39 pass with no env at all · 1 `CREDS_FAIL`** (`test-evryn-initiated-placeholder.ts` seeds `EVRYN_ENV` but not the qualified Supabase pair). 77 files minus the 2 import-only helpers (`env-bootstrap.ts`, `import-fresh.ts`) = **75 runnable entry points.** The inherited "27 of 61" (SPRINT line 382, itself explicitly logged as *"an open question for AC0, not a closed finding"*) is wrong in both terms. AC0's instruction to re-measure was correct.

**F2 — 🔴 `EVRYN_ENV` is only the FIRST tripwire. `config.ts` has NINE module-scope required vars, and fixing `EVRYN_ENV` alone just moves the failure one line down.** `requireEvrynEnv()` evaluates at `config.ts:85`; six more `required(...)` calls sit at `:650–651` (`GMAIL_CLIENT_ID/SECRET/REFRESH_TOKEN`), `:668–669` (`SLACK_APP_TOKEN/BOT_TOKEN`) and `:716` (`ANTHROPIC_API_KEY`), plus the resolved `SUPABASE_URL_<SUFFIX>` / `SUPABASE_SERVICE_KEY_<SUFFIX>` pair at `:86`. **Proven, not inferred:** seeding only `EVRYN_ENV`+Supabase made the next run die on `GMAIL_CLIENT_ID`. ⇒ **`bootstrapTestEnv()` does NOT make a suite runnable without `.env`** — it defaults `EVRYN_ENV` and nothing else. Any fix that stops at `EVRYN_ENV` is not a fix.

**F3 — 🔴 A dummy-env seed is the RIGHT fix for only 26 of the 37, and is actively HARMFUL for the other 11.** `dotenv` does not overwrite an already-set `process.env` var, and any in-test seeding necessarily runs *before* `dotenv` (which loads inside `config.ts`). So a seeded dummy **beats a real `.env`** — exactly the trap already documented in `tests/env-bootstrap.ts`'s header for `EVRYN_ENV`. Seeding dummy Supabase creds into a live-DB test would therefore silently point it at a nonexistent database *on a machine that has perfectly good creds*. Split of the 37 (by whether the file references `getDb|createClient|@supabase`): **26 PURE** (safe to seed → become runnable anywhere) · **11 DB-touching** (`run-fixtures`, `test-approval-flow`, `test-breaker-resume`, `test-care-eligible-users`, `test-conversation`, `test-emailmgr-updated-at`, `test-gatekeeper-resolution`, `test-llm-usage`, `test-messages-ordering`, `test-pipeline-approval`, `test-processing-stuck`) — these must NOT be seeded; the correct end state for them is a clear, actionable "no `.env`" failure. **Adjacent observation, NOT ACn's to fix:** none of those 11 gates on `RUN_LIVE_DB_TESTS`, so on a machine with `.env` they run against dev automatically.

**F4 — 🔴 CROSS-LANE, FOR AC0: the contested surface is 16 files, not 1 — and 9 of them collide at the exact line both lanes edit.** `grep -l "triage/classify" tests/*.ts` returns **16** files, not the 8 the brief states. Nine are also in ACn's 26-file pure-fix set: `smoke-test`, `test-cache-byte-identity`, `test-context-labeling`, `test-poll-helpers-r2`, `test-prompt-composition`, `test-query`, `test-register-gatekeeper-address`, `test-role-target-guard`, `test-uuid-guard`. Both lanes' edits land in the **import block at the top of the file** — ACn converts the static `src/` import to a seeded dynamic one; the atlas lane rewrites the classify specifier. That is the worst-case overlap for a textual rebase. **ACn's recommendation (AC0's call, not ACn's — this is cross-lane sequencing): ship as TWO separate commits** — (1) Step 101 alone, one file, the actual gate on the extraction; (2) the boot-fail sweep. Two commits let AC0 land the gate immediately and sequence the sweep independently; one blended commit takes that option away.

**F5 — PROVEN: `importFresh` DOES yield `classify.js` a distinct, freshly-evaluated memo.** AC0 flagged Step 98's helper as run-verified on `email/client.js` only and asked for proof rather than assumption. Method (decisive, in the throwaway sandbox): import `classify.js?acn=1` → prefix = **149,121 bytes**; rename `identity/situations/gatekeeper.md` aside; import `classify.js?acn=2` → prefix = **142,734 bytes**; restore. Instance 2 saw the change ⇒ it genuinely re-evaluated and holds its own `cachedCommonPrefix`. Control confirmed the same query twice IS cached. **The by-product is the money finding for Step 101: deleting ONE 48-line identity file silently removed 6,387 bytes from Evryn's prompt with no error, no separator gap, and no trace** — a live demonstration of the exact defect the current guard cannot see.

**Also confirmed at source (AC0's read was right):** `loadCommonPrefix` memoizes at `classify.ts:228` and sets the cache at `:242`; the test calls it three times at `:40–42` and compares the results, which after call 1 are the same string reference. **The byte-identity block is vacuous exactly as described.** `readIdentityDir` returns `""` on a missing dir (`:188–191`) and `:242`'s `parts.filter(p => p.length > 0)` then drops it with no separator.

**One place ACn's read DIFFERS from the brief, on the design:** AC0 suggested a **committed expected value (byte length or hash)**. ACn recommends **against** hashing the whole prefix: the identity files are living documents, so every legitimate Mira edit would turn the suite red, and the reflex fix is to bump the constant — which is indistinguishable from bumping it to hide a real regression. A guard that cries wolf on every ordinary edit gets trained around. **The instrument ACn will spec instead is an independent oracle:** the test enumerates `identity/` itself using the documented rules, rebuilds the expected prefix, and asserts equality — immune to content edits, but red on a dropped `parts.push`, a changed separator, or a reordered layer. Plus explicit committed structural assertions that each of the four subdirectories exists and contributes ≥1 file, which is what catches the "a whole layer went empty" case that pure recomposition cannot see (both sides would agree on the emptiness).

**Honest coverage limit ACn will NOT paper over:** removing `.sort()` at `classify.ts:200` is **not reliably detectable** by any filesystem-observing test on a filesystem whose `readdirSync` already returns lexicographic order — the test's own enumeration and the runtime's would agree. A fresh-instance comparison does not catch it either (same filesystem, same order). ACn will still assert layer/file order explicitly, because that catches a *deliberate* reordering and documents the invariant — but ACn will not claim the sort is covered when it is not.

**NEXT ACTION (if this instance dies here):** spin DC (two-trip, per `ac-orchestration-protocol.md`) to build Step 101's replacement byte-identity block in `tests/test-cache-byte-identity.ts` per the design above; it must also self-seed all nine env vars so it stops boot-failing. Then mutation-test in the sandbox at `c:/Users/Justin/Evryn/Code/_acn-sandbox` (already provisioned, detached at `b8a2986`, node_modules junctioned). Then the 26-file sweep as a separate commit.

---

**ACn — 2026-08-10T18:33-07:00 — design premises validated by probe; the hash idea is dead on a measured, platform-level reason.**

**F6 — 🔴 A COMMITTED BYTE-LENGTH OR HASH WOULD BE PLATFORM-DEPENDENT AND IS THEREFORE UNUSABLE.** This is a stronger and more concrete reason than the "living documents" argument in ACn's previous entry, and it settles the question. **Measured:** the repo has **no `.gitattributes`** and `core.autocrlf=true`, so every identity `.md` is checked out **CRLF on Windows** and would be **LF on Linux (Railway/CI)**. `identity/core.md` is **44,531 bytes on disk here vs 44,248 LF-normalized** — 283 bytes, one per line, for one file. The whole composed prefix measures **149,121 bytes on this Windows machine** and would be roughly 1,378 bytes smaller on Linux. ⇒ **any absolute byte-length or hash constant committed from a dev machine is wrong in CI and wrong on Railway**, so the guard would be red on one platform and green on the other for reasons that have nothing to do with the invariant it protects. **The recomposition oracle has no such problem**, because both sides read the same on-disk bytes at test time. *(Not a live product bug: prod is uniformly Linux, so the prefix is stable within prod and the Anthropic cache is unaffected. It is purely a reason not to commit an absolute constant.)*

**F7 — the recomposition oracle WORKS, verified end-to-end.** A probe independently re-derived the prefix from disk using the documented rules (core.md, then `activities` minus `reflection.md`, then `situations` minus `operator.md`, then `internal-reference`, then `public-knowledge`; each dir's eligible `.md` files filtered `!startsWith("_") && !== "README.md"`, sorted, joined with `"\n\n---\n\n"`; empty parts dropped) and compared it to `loadCommonPrefix()`: **expected 149,121 B === actual 149,121 B, exact equality.** So the oracle is a viable instrument, not a hypothesis.

**F8 — two sub-premises measured, one of which corrects an assumption ACn had made:**
- **A naive separator COUNT is viable after all** — ACn expected identity content to contain `"\n\n---\n\n"` (the files do use `---` horizontal rules) and it measures **zero occurrences**, in core.md and in all ten others. The reason is CRLF: on disk those rules are `\r\n\r\n---\r\n\r\n`, which does not match the LF-only join separator. **⚠️ That makes a separator count correct HERE and WRONG ON LINUX**, where the content rules would match the separator exactly. So a separator count must NOT be used — it is a platform-dependent trap of the same family as F6. Recording it explicitly because it looks like it works on the machine anyone will test it on.
- **`readdirSync` already returns lexicographic order on this filesystem (measured: true for all four subdirectories).** This CONFIRMS the honest coverage limit in ACn's previous entry: **removing `.sort()` at `classify.ts:200` is not detectable by any filesystem-observing test here**, because the test's enumeration and the runtime's would agree. ACn will assert order explicitly (it catches a deliberate reversal and documents the invariant) but will NOT claim the sort is covered.

**Sandbox note:** probes live at `c:/Users/Justin/Evryn/Code/_acn-sandbox/tests/_acn-probe-*.ts` — a detached throwaway worktree, never committed, to be removed at close-out. All nine env vars must be exported to run anything there (see F2).

---

**ACn — 2026-08-10T18:45-07:00 — DC spun (two-trip); load verified; build in flight.**

**Worktrees ACn owns and must reap at close-out** (nobody else can — subagent resume is session-bound):
- `c:/Users/Justin/Evryn/Code/evryn-backend-safetynet` — ACn's own lane tree, branch `acn/safety-net` (created by AC0; AC0 reaps).
- `c:/Users/Justin/Evryn/Code/_acn-sandbox` — detached throwaway at `b8a2986`, for mutation testing. **ACn reaps.**
- `c:/Users/Justin/Evryn/Code/evryn-backend-dc-step101` — DC's build tree, branch `dc/acn-step101`, cut from `acn/safety-net`. **ACn reaps after merge.**
- All three needed `node_modules`; each got a junction to the canonical tree (`cmd //c "mklink /J node_modules …"`, no admin required).

**DC load trip verified — ACn's attestation.** DC was spun with the verbatim `<identity>`/`<mandatory_load>`/`<receipts>` blocks, model pinned to Opus, told nothing about the work. Its receipts list **all 25 files** ACn assigned, at the assigned spans, with canaries confirmed on all 13 Markdown files and true final lines given for the 10 canary-less TS/JSON files. **Call-count reconcile: DC declared 28 `Read` + 2 `Bash` + 1 `Glob` = 31; the harness reported `tool_uses: 31`. They agree.** DC disclosed **two token-cap PARTIAL reads** (`ARCHITECTURE.md` 1–430 of 1279; `BUILD-EVRYN-MVP.md` 1–486 of 652) as their own receipt lines and completed both by paging, rather than letting the retry absorb the failure. It also correctly reported its `Glob` as establishing the identity set's *membership only*, explicitly not the files' contents. Cascade reconcile: DC's own mandatory set (Hub, ARCHITECTURE, BUILD) was fully covered by ACn's list; no standing file was omitted; no `#cascade-override` was used. **Load accepted; trip 2 sent.**

**F9 — PROVEN: a bare side-effect import placed FIRST reliably seeds env before a statically-imported `src` module evaluates.** Probed with the environment fully absent: the seed module logged first, then `loadCommonPrefix()` returned 149,121 B and `EVRYN_ENV` resolved to `dev`. **This is what makes the boot-fix ONE LINE per file (`import "./env-seed.js";`) instead of a restructure into dynamic imports** — and it is the reason the fix rebases cleanly against the atlas lane: ACn *adds* a line above the classify import rather than rewriting that line, so git can auto-merge the two lanes' edits to the same file. The 26 pure boot-failers are uniform in shape — 24 open with a plain static `import … from "../src/…"`, and 2 (`smoke-test.ts`, `test-query.ts`) already open with a side-effect `import "dotenv/config"`, so the idiom is already native here.

**F10 — adjacent, NOT ACn's to fix, but material to the whole "safety net" premise: there is no CI and no test runner.** `ls .github/workflows` does not exist; `package.json` has `dev`/`start`/`build`/`typecheck` and **no test script**; there is no prettier/eslint/biome config. So the suite runs only when a human runs 75 individual `npx tsx` commands. **A safety net nobody executes is not a net** — worth AC0's attention independently of this lane. (The absence of an import-sorting formatter is also what makes F9's ordering invariant safe in practice.)

**F14 — a refinement that removes the F3 hazard at its root: the seed module should `import "dotenv/config"` ITSELF, first.** F3 established that a seeded dummy beats a real `.env` (because `dotenv` lives inside `config.ts` and never overwrites an already-set var), which is what made broad seeding dangerous. **If the seed module loads `dotenv` before filling gaps, that inverts: a real `.env` wins, and dummies only fill what is genuinely missing.** Measured both branches: with no `.env`, `SUPABASE_URL_DEV` resolves to `test-supabase_url_dev`; with a real `.env` present, it resolves to the real value. **ACn will require this shape.** *(It reduces but does not eliminate the reason to keep the 26/11 split: a DB-touching test with no `.env` would now boot past `config.ts` on dummies and fail later at connect time with a worse message than the clean "missing `SUPABASE_URL_DEV`" it gets today. So the 11 still must NOT be seeded — but the failure mode if someone does is now confusing rather than dangerous.)*

**F12 — 🔴 THE BEFORE PICTURE, EXECUTED: the current guard survives FIVE OUT OF FIVE mutations. Step 101's claim is not merely true, it is an understatement.** Run in the throwaway sandbox with env exported (so the result isolates whether the *assertions* discriminate, not whether the file boot-fails). `guard` = `test-cache-byte-identity.ts`; `adj` = `test-prompt-composition.ts`. Each mutation was applied, both suites run, then `classify.ts` restored from a backup and the baseline re-checked GREEN.

| Mutation | guard | adj |
|---|---|---|
| baseline, unmutated | GREEN | GREEN |
| **M1** — delete `parts.push(readIdentityDir("internal-reference"))` | **GREEN** | RED |
| **M2** — layer separator changed by ONE character (`---` → `----`) | **GREEN** | GREEN |
| **M3a** — one `readIdentityDir` call yields `""` (situations layer empty) | **GREEN** | GREEN |
| **M3b** — an entire identity DIRECTORY renamed away (the real-world form) | **GREEN** | RED |
| **M4** — `.sort()` removed | **GREEN** | GREEN |
| restored | GREEN | GREEN |

**The guard named for the invariant caught NOTHING. The suite as a whole caught 2 of 5, and only by accident** — `test-prompt-composition.ts` happens to assert the string `"Trust Arc"`, which lives in `internal-reference`, so it fires on exactly the two mutations that remove *that* directory and is blind to the rest.

**F13 — a design lesson bought by M3a, which every marker-style assertion in this repo gets wrong.** M3a emptied the whole `situations` layer, and `test-prompt-composition.ts` — which explicitly asserts `/gatekeeper/i.test(common)` to prove that layer loaded — **stayed GREEN.** The reason: the word "gatekeeper" also appears in `activities/gatekeeper-onboarding.md` and `activities/triage.md`, so the marker survives the deletion of the file it is supposed to prove. **A marker that is not UNIQUE to its source file proves nothing about that file** — it silently degrades into "some file, somewhere, mentions this word." This is why ACn specced the completeness check against **each eligible file's actual content** rather than hand-picked markers; it is immune to the problem by construction. Worth promoting beyond this lane — the same mistake is cheap to make in any content assertion.

**F11 — coverage is not where you would expect it: `tests/test-prompt-composition.ts` already carries marker assertions** for `internal-reference` ("Trust Arc"), `public-knowledge` ("Where You Come From") and `situations` (`/gatekeeper/i`), plus the `operator.md` exclusion. **So some layer-went-empty mutations will go red in THAT file, not in the guard named for the invariant.** Consequence for the acceptance bar: mutation results must be reported **per-file**, or a red from the wrong suite reads as the cache guard working when it is not. `activities` has no marker anywhere, and its `/core/i` check is weak enough to match almost anything.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
