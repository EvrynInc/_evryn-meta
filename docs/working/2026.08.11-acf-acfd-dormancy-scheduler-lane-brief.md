# Lane brief — `ACfd`: the dormancy & scheduler safety cluster

*Written 2026-08-11 by **ACf-12** for a lane to be spun by **ACf-13**. SPRINT Steps **43, 44, 50, 7, 51** in `evryn-team-runtime`.*

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file.** It is the lane's **shared surface**, per the orchestration protocol: `ACfd` reads it in full at the start, appends its own notes as it works, and the head AC appends too. **It must always be current enough to re-spin from cold** — the test at every append is *"if this instance died right now, could a fresh one pick up from this file alone?"* **Commit it freely; brief-class traffic is pre-authorized.**
>
> **This brief carries SCOPE, DESIGN CONSTRAINTS and PRE-DECIDED CALLS. It does not carry the tagged `<identity>`/`<mandatory_load>`/`<receipts>` blocks** — those are pasted verbatim by whoever spins the lane, from `_evryn-meta/docs/protocols/ac-orchestration-protocol.md`, using the file list in §7 below.

---

## §0 — 🔴 HOW MUCH THE AUTHOR OF THIS BRIEF HAD ACTUALLY READ. Read this before you trust a word of it.

**ACf-12 wrote this while running the conductor cascade.** As of writing it had personally read, of `evryn-team-runtime`: **roughly 450 lines of `src/`, zero migrations, zero tests.** Everything else here came from (a) a full-load runtime scout, (b) a second full-load subagent that read all 56 test files, and (c) a handful of `file:line` anchors ACf-12 checked itself.

**You are about to read the system. That makes you the better-informed party on every question of fact in this document.**

- **Treat every factual claim here as a LEAD, not a fact — including the line numbers.** Re-derive anything the work rests on, and **say so loudly and early if any of it is wrong.**
- **Do not defer to this brief on anything you can see and it could not.**
- **And the half people forget: where this brief is SILENT, do not read that as "they checked and there was nothing there." Assume instead that they could not see it.**

**This is not humility theatre.** On 2026-08-10/11 two lanes briefed this way sent **six corrections back up**, including a flatly false claim and a spec that would have silently broken a working `/pause` → `/resume` path. **The spinner caught none of them.** Do that here.

---

## §1 — What this lane is, in one paragraph

`evryn-team-runtime` is the harness that will wake Evryn's eight founding-team agents. It is **built, dormant, and wakes nobody.** Phase 1 of its memory architecture (ADR-052) just completed — five merged Steps, nothing deployed. **This lane closes the safety gaps that Phase 1's own reviews surfaced in the code Phase 1 shipped**, because Justin ruled that Phase 2 (memory consolidation) must not be built on a dormancy mechanism that can silently stop or spend in a loop.

**Your five Steps all live in the same three files.** That is deliberate — a sibling lane (`ACfg`, guard integrity) is running in parallel on different territory, and the split is by file so you do not collide. **§8 names the boundary. Respect it.**

---

## §2 — The Steps, and what is already decided

**Read all five in `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md`. The summaries below are orientation, not a substitute.**

### Step 43 — `cycle()`'s time-and-latch layer has no guards. **The design is PRE-DECIDED; do not re-open it.**

Seven pieces of mutable state in `src/scheduler/index.ts` have **zero executed coverage**, because `cycle()` is not exported and cannot be called: its **prologue** opens with `refreshRuntimeLock`, whose failure path ends in `process.exit(1)`, and its **epilogue** re-arms itself in a `finally`.

> 🔴 **DO NOT make `cycle()` callable.** That means owning both the exit path and the self-rescheduling loop, which is a far larger and more dangerous change than these defects warrant. **Extract the DECISION instead** — a pure, exported, `shouldSweep(lastAt, now, intervalMs)`-shaped function the closure calls.
>
> **The precedent is inside the file you are editing.** `scheduler/index.ts` already exports five pure helpers, and **`velocityGate` was extracted for exactly this reason** — a reviewer proved a caller could pass a wrong-but-valid literal, so the decision moved out of the closure to where a test could reach it. Its own comment says so. **Match that idiom. Do not invent a sixth.** The house pattern for the clock half is `AcquireOpts` in `src/boot/singleton.ts:44-60`.

**🔴 And the split that decides whether you actually close this — the seven are TWO problems:**
- **Clock-driven** — `lastDigestSweepAt`, `lastDormancySweepAt`, `CYCLE_MS` rescheduling. These fall to an injected `now()`.
- **Latch / lifecycle** — `velocityWarned`, `inFlight`, `stopping`, `cycleTimer`, `pendingWakes`. **No clock injection touches any of these.**

**A fix that only injects a clock closes two and leaves five.** The latch half needs its own extraction — a pure decision over `(verdict, alreadyWarned)` returning **both** whether to fire **and** the next latch value, **so the RESET is testable, not just the fire.** ⚠️ **`velocityWarned`'s reset is the one that matters most: break it and Justin gets exactly ONE spend-velocity warning for the entire life of the process, then silence — while the gate keeps working perfectly and every test stays green.**

⚠️ **The digest gate is in a WORSE position than the dormancy gate**, which inverts the Step's original emphasis: `DIGEST_SWEEP_INTERVAL_MS` is imported by **no test at all**, so neither its wiring nor its value is pinned. **Cover both gates.**

### Step 44 — one failing thread halts dormancy fleet-wide, silently

`sweepDormantThreads`' loop has **no try/catch**, so one throwing thread exits the loop *and the function*; the scheduler's caller **swallows the throw** with a `console.warn` and substitutes an empty list, taking the outstanding set with it; and the function takes **no `Notifier`**, so it cannot alert even if it wanted to.

**It fails SAFE** — digests stay up, nothing vanishes — **which is exactly why it would sit undetected forever.** The visible symptom is that threads simply stop closing.

**Its sibling `sweepThreadDigests` already does this right**, ending with per-thread `.catch()` isolation. **Match it, and route a real alert.**

### Step 50 — the serial closing-walk vs the 90-second singleton-lock window

The same loop walks closing threads **one at a time with two awaited round-trips each**, inside the cycle that refreshes the singleton lock — so a long enough sweep lets the lock go stale and another boot reclaims it, at which point this process exits.

**📌 Severity was corrected DOWN on 2026-08-11 and you should honour that:** at one active agent and a few dozen threads this is nowhere near firing. **It is in your scope only because it lives in the loop Step 44 is already opening** — fix it while you are there, do not let it expand the lane. Bound the per-sweep work (process at most N and let the next sweep continue — every phase is already idempotent) and/or batch the two per-thread reads.

### Step 7 — dormant→live revival

Nothing transitions a thread's state back to `live`, so a re-activated dormant room never returns to anyone's ambient digest. **Do it in the sweep, not in the database trigger** — the sweep already runs on a cadence and already owns this lifecycle; deterministic, testable, one place, no migration.

> 🔴 **REVIVAL MUST ALSO CLEAR THAT ROOM'S `thread_dormancy_writes` COMPLETION ROWS.** Those rows mean *"this agent has written its closing memory for this room."* **If a room half-closes, revives, and later closes again, every member already marked complete is SKIPPED — they get no memory of the room's second life, and the room can never fully close again.** A seam comment marks the spot.

✅ **Justin's requirement — *"if they were in the room when it went dormant, they are in the room when it comes out"* — needs NO work.** Membership rows are untouched by dormancy; the roster never left. **Recorded so you do not build restoration for something already true by construction.**

⚠️ **The echo-rule marking** (a returning digest line noting *"re-activated after dormancy"*, so an agent holding both a closing memory and a fresh digest line reads them as one story) is **flagged, not settled.** **Build it if it is genuinely small where you are standing; if it is not, leave it and say so.**

### Step 51 — `memoryWriteFailed` has no executed coverage, and its attempt gate has none at all

This is **Step 5's own atomicity invariant** — the thing that stops the runtime recording a closing memory nobody wrote and then dropping the room's digest on it.

- All three ends (producer ×2 in `wakes/index.ts`, consumer in `scheduler/index.ts`) are **source-asserted by regex only**, and structurally cannot be executed without a live SDK.
- 🔴 **The actual hole: nothing guards the ATTEMPT GATE at `wakes/index.ts:528`.** Change it and the write silently never attempts → the flag stays `false` → the completion is recorded → **the digest drops on a memory that does not exist.** Step 5's exact failure, through a line no guard touches.
- ⚠️ **One existing guard is nearly tautological — do not count it as coverage.** `dormancy-wake.test.ts:275-278` asserts an ordering over a region that **ends at the thing it is ordering, by construction.**
- `dormancy-wake.test.ts`'s comment-stripper is the **sole** coverage of this chain and has **no anti-vacuity proof**, while two sibling strippers in the repo do. **Give it theirs.**

---

## §3 — 🔴 Pre-decided calls, so you never have to guess at 3am

**Justin is asleep. These are decided. Do not re-open them, and do not stop on them.**

1. **NO MIGRATIONS. None of these five needs one.** ⚠️ **If you discover one does — STOP that Step, finish the others, and report it.** Do **not** build a lesser fix because the brief said no migrations. *(That exact failure happened on 2026-08-11: a brief said "no migrations," the correct fix needed one, and the builder silently shipped the worse version. It is now a standing rule in DC's manual. **Surfacing it is the job.**)*
2. **DO NOT TOUCH `evryn-team-workspace/shared/protocols/runtime-ops.md`** or any agent definition or memory file. That is the identity layer, it is **Justin's**, and it is composer layer 1 — every edit invalidates every agent's cached prompt fleet-wide. **If your work implies an agent-facing instruction change, write it into this brief as a proposal.**
3. **Scope discipline: Step 50 rides Step 44's loop.** It does not justify widening into a performance project.
4. **If Step 43's latch half turns out to be much larger than the clock half** — plausible, since `inFlight`/`stopping`/`cycleTimer`/`pendingWakes` are lifecycle rather than decisions — **land the clock half plus `velocityWarned`, and route the remaining three with a written recommendation.** `velocityWarned` is not optional; it is the one with a live consequence.
5. **The echo-rule marking (Step 7) is discretionary — see §2.**
6. **Everything else that is genuinely ambiguous: BATCH IT.** One numbered, recommend-first list at the end of your report. **Keep advancing everything not blocked by it.** Do not stop the lane on a question.

---

## §4 — How you must work

- **You are a lane AC.** You spin your **own DC and your own QC**, following `ac-orchestration-protocol.md` **exactly** — the tagged brief pasted verbatim, **two-trip loading** (load trip says nothing about the work; verify receipts; only then send the task), **every spin model-pinned**, and **never tell a child where to report.** You may not improvise or condense those blocks.
- **You work in a worktree on your own branch.** You **never** merge, never push, never deploy, never apply a migration. Those are the head AC's, and the head's are Justin's.
- **🔴 Mutation-test every guard you write, in a SANDBOX COPY** (`git worktree add --detach <path> <sha>`), never in the tree you are building in. **And prove the mutation actually applied** — print the changed line or confirm zero occurrences of what you removed. **A mutation that silently fails to apply prints exactly the same green as a passing suite.** These worktrees are CRLF; a reviewer on this repo lost six mutations to line endings and would have recorded passing tests over unbroken code.
- **Do not let your QC inherit your DC's test inputs.** On the last lane the builder ran 19 mutations all red; **QC ran 28 and four survived.**
- **Report receipts up with an attestation** covering the **call count and the tool used**, not just the file list. If you could not observe a child's call stream, say exactly that.
- **Headings cap at `##`.** `#` is reserved for the top-level AC's voice to Justin.

---

## §5 — Definition of done

1. All five Steps closed, or explicitly routed with a written reason.
2. `npm test` and `npx tsc --noEmit` **green on your branch, measured by you, not inherited.**
3. Every new guard **mutation-verified in a sandbox**, with what you broke and how many checks went red stated in your report.
4. **No migration.** No identity-file edit. No merge, push or deploy.
5. This brief updated with what you did, what you found, and what you routed — **current enough to re-spin from cold.**
6. Your report hands the head AC the **changelog-worthy facts** (you do not write changelog entries; the AC who merges does).

---

## §6 — Notes appended by the lane

*(`ACfd` appends here as it works. Keep it current — this is the crash-survival surface.)*

---

## §7 — The load list

*(Filled by `ACf-13` at spin time from the runtime scout's per-lane list, recorded in the ACf-13 handoff. Every entry carries `(full — N lines)` or an exact span. **Every `.ts` and `.sql` file in this repo carries a bottom truncation canary except `src/wakes/memory-writer.ts`, which holds the canary string as a functional constant** — for that one, "full" means reaching the true final line.)*

---

## §8 — 🔴 The territory boundary with the sibling lane

**`ACfg` (guard integrity) is running in parallel on the same repo, in its own worktree.** It owns: `src/boot/schema.ts`, `tests/boot-*`, `tests/source-no-nul-bytes.test.ts`, `tsconfig*`, `package.json`, the Bolt test fake in `tests/slack-auth.test.ts`, `src/slack/index.ts`, and new test files for `safety/notify.ts` and `safety/heartbeat.ts`.

**You own:** `src/scheduler/index.ts`, `src/digest/index.ts`, `src/wakes/index.ts`, `src/threads/membership.ts`, and `tests/dormancy-*`, `tests/scheduler-*`, `tests/park-*`, `tests/composition-failure-honesty.test.ts`.

**If you need to change a file on ITS list — stop and report it rather than editing.** A merge conflict at 3am with nobody awake costs more than the change is worth. **The head AC merges both and will resolve it.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
