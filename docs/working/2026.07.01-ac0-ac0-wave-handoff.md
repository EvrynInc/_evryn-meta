# Handoff — AC0 → next AC0 — the 4-lane v0.2-hardening WAVE (2026-07-01)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** START HERE if you're a fresh AC0 picking up the v0.2-hardening wave. You are the **conductor** of a 4-lane parallel build. This gives you the operating model, every lane's live state, the deploy pipeline, and the open decisions. **Supersedes** `2026.06.30-ac0-ac0-wave-orchestration-handoff.md` (retire that). Working doc.
>
> *Authored 2026-07-01 ~14:15 PT by the outgoing AC0 (~12% compaction headroom). Companions: `docs/current-state.md` (snapshot, stale — owed an update) + `docs/deploy-log.md` (ship ledger).*

---

## ⚡ What you are doing (one paragraph)

You (AC0) conduct a **4-lane parallel v0.2-hardening wave** on `evryn-backend`, built on the round-2 hardening that's already live (`v0.2.2`, 6/29). Justin spins each lane as its own AC instance (AC1/AC2/AC3/AC4); **you do NOT write code** — each lane's DC/QC builds. Your job: read each lane's report (appended to its brief), reconcile/decide, reply (append to the brief + commit immediately), keep the lanes coordinated, keep Justin at CEO-altitude. Deploys are **staged, one lane at a time** (NOT a convergence — deliberately, to dodge round-2's merge tsunami), each a numbered ship (`v0.2.N`). **The endpoint:** finish this hardening batch so Mark can onboard (a full live-test precedes his first real forward).

## 🛠 Operating model — Justin set this; follow it EXACTLY

1. **Each lane's brief IS the AC↔AC0 mailbox** (in `docs/working/`). Read the tail, append your reply as a `## ═══ AC0 → ACn · date · summary ═══` banner **before the truncation canary**, and **COMMIT + PUSH immediately** — mailbox commits are **pre-authorized** (no Justin go needed). **Stage by explicit path** (never `git add -A` — lanes + AC4's untracked build-specs share `_evryn-meta`).
2. **Justin does NOT read the briefs.** Every reply to a lane → a **CEO-altitude plain-English chat summary.** NEVER a bare "Step 83" — describe the thing ("the cheap-model bouncer," "the web-fetch exfil door"). He comes in cold from other instances; re-anchor lightly each time.
3. **PING every final response** on `#team-alerts` (he stressed this most, repeatedly): Node `fetch` to `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`, prefix `AC0:`. **In Node read the env at `C:/Users/Justin/Evryn/Code/evryn-team-workspace/.env`** (the git-bash `/c/...` form fails). Ping even when he seems present — he runs ~7-8 agents. Keep the ping a one-line tap; substance goes in chat.
4. **Commit/deploy gates are Justin's — per-action, they don't stack.** Lanes build/edit freely but pause for his explicit go before commit + before `railway up`. A workflow verb ("handle it") is not a go. Only mailbox-brief commits (+ Justin-requested docs like this handoff) are pre-authorized.
5. **You're a PEER to the lanes on their architecture, not their boss.** They hold deeper context on their exact problem; you hold cross-lane collisions + sequencing. Answer collision/sequencing Qs; let them own design. `AskUserQuestion` is BANNED (hangs) — ask in chat + ping.

## 📊 The 4 lanes — LIVE STATE

### AC1 (cleanup) — DEPLOYED `v0.2.3` ✅
- Deployed 6/30 16:48 PT (`83fa367`) — live + healthy (verified Railway logs 7/1: Supabase/Slack/polling green, cron firing). PLUS a follow-up: the **Step-23 pong-warn fix** (`8e80d01`) is **merged to `main` but NOT deployed** — it rides AC4's Bundle 1 (`v0.2.4`). **So `main` is AHEAD of what's deployed.**
- **⭐ OWED TO AC0 (his close-out asked, NOT yet done):** mark these SPRINT steps DONE (genuinely deployed): **42, 75, 76c, 54.d.ii, 54.d.iii, 77, 76b, 72, 36, 23, 76a, 83, 39** (39 = audit-clean, no change). Step 38 stays TODO/deferred; the `validateUserExists` dead-code nit → `SPRINT-V0.3-CANDIDATES.md`; add a current-state line.
- **AC1 is STANDING BY for the next deploy.** When Bundle 1 ships (`v0.2.4`), **re-spin a deploy-AC1 to confirm the pong warn is GONE from live Railway logs** (test-green ≠ prod-verified) + land the pong-fix CHANGELOG entry with the Bundle-1 record.

### AC4 (security bulkheads) — Bundle 1 ready = `v0.2.4` (NEXT DEPLOY)
- **Bundle 1 GO + committed** on `ac4/sec-bundle1` (`0ef889f` + SSRF fix `d6f0cb2`): 3 locks — WebFetch/WebSearch egress deny-hook, `sendEmail` provenance gate, untrusted-email fence. Adversarial QC caught + AC4 fixed a real SSRF bypass (IPv4-mapped-IPv6 → cloud-metadata endpoint). ADR-047 + build-spec committed.
- **UNBLOCKED to deploy** (AC1 landed). Path: **rebase `ac4/sec-bundle1` onto `main` (`8e80d01`) → merge → `railway up`** — all Justin-gated. Becomes `v0.2.4` (carries the pong fix too).
- **Bundle 2** (the `service_role` constrained-read lock) = **cleared + spec'd** (`2026.07.01-ac4-bundle2-security-build-spec.md`), not built. Ships as a later number (likely `v0.2.5`; numbers are assigned at deploy, not reserved).
- **41a Mira beat** (untrusted-URL fetch decision): AC4 owns the requirement, Mira voices. Brief: `2026.07.01-ac4-mira-41a-fetch-decision-beat.md`.
- **AC4 is at a compaction self-handoff** — needs a fresh AC4 re-spin to deploy Bundle 1 + build Bundle 2.

### AC2 (cost) — ① built; ②③ ahead
- **① Haiku pre-screen (Step 44) BUILT + AC/QC-GO (build pass) + pure-tested 51/51**, on `origin/dc/cost-haiku-prescreen` @ `89e8cff`, **ships `off`** (`HAIKU_SCREEN_MODE` unset). The 3 observability adds are in (dashboard readout / fresh-false-pass `notifyDev` / Haiku's own `llm_usage` spend tag). Deploys **after AC4's two bundles** (its number assigned at deploy — likely `v0.2.6`).
- **Owed before ① deploys (Justin-gated):** rebase onto current `main` → **deferred post-rebase QC-verify (do NOT skip)** → DEV migration (`add-haiku-screen-shadow-table.sql`) + `tests/test-haiku-screen-dev.ts` → merge/deploy. Then a **~1-week Mark-gated shadow trial → ~0 false-pass → Justin's sign-off flips `off→shadow→active`.**
- **② clustering (Step 58) HELD** (Justin's "lock ① first, don't stack"). **③ bookkeeping (Step 57) not started** — co-designs with AC4's Bundle-2 read-lock (the broker below). The **broader deterministic-`triage_result`-for-gold/edge fix folds into ③** (verified: `submitDraftForApproval` doesn't set `triage_result` for gold/edge today; Step 57 canonicalizes it) — in-lane, not a separate spin.
- **AC2's next decision (wants Justin's read):** converge ① now vs. start ②. **AC2 at a compaction self-handoff** (Round 6).

### AC3 (identity/ops) — broadest, Justin-heavy
- Judge-the-set done; endorsed priority: **staging standup (Step 80 — GATED on Justin's account actions: a 4th Supabase project, a 2nd Railway service, a staging Slack app, a test inbox)** + **consolidated watch-list (Step 47 — fold in the Haiku-shadow watch; dashboard = results-home)** + **near-term determination-sharing Mira beat (Step 55).**
- **Step 53** (reliable identity-module loading) → its OWN dedicated post-wave spin (with Step 50); NOT AC3's plate, NOT a 5th lane now.
- Cross-lane ownership confirmed: AC3/Mira voice AFTER AC2's structural rewrites land; AC1 owns the ingest region; AC4 hands 41a to Mira. AC3 sequences with Justin (most of his lane is deferred until he can contextualize each item for Justin cold).

## 🚀 Deploy pipeline + versioning (SETTLED this session)
- **Naming: `v0.2.N`** — one per prod `railway up`, linear, don't conserve numbers, 3-part, **no leading zero** (git/semver convention; sort-safety via `git config tag.sort version:refname` on `evryn-backend` + the hand-ordered ledger). Source of truth: **`docs/deploy-log.md`**. Git-tags exist for `v0.2.1/2/3`.
- **Live: `v0.2.3`** (`83fa367`, 6/30 16:48). **`main` = `8e80d01`** (pong fix, undeployed — rides `v0.2.4`).
- **Order (staged, NOT convergence): AC1 (done) → AC4 Bundle 1 (`v0.2.4`, NEXT) → AC4 Bundle 2 (likely `v0.2.5`) → AC2 cost (likely `v0.2.6`).** Each Justin-gated.
- **AC0 gates + coordinates + reconciles; the fresh LANE instance EXECUTES the deploy** (rebase → merge → `railway up`, per that lane's brief runbook) — **you do NOT run `railway up` yourself.** Your part: confirm the lane is ready, relay Justin's explicit go, then reconcile after (mark sprint steps, update the ledger + current-state).
- **Rosetta:** "round-1" = discarded 6/17 lobotomy (NEVER shipped); "round-2" = `v0.2.2` (6/29); "go-live" = `v0.2.1` (6/16). **Discipline:** a step's STATUS + the ledger = truth; a "riding round/wave-X" tag = intent, NOT proof of ship. (This gap made Mira's determination-sharing guardrail look shipped when it wasn't.)

## 🔴 The AC4↔AC2 broker (RESOLVED — hold to it)
After AC2's Step 57 (③) + AC4's Bundle 2 (②), Evryn's DB surface = **{typed tools}** + **{AC4's constrained `supabase_read`: table-allowlist · `users` requires an id/email filter · row-cap · log}** + **NO raw `supabase_upsert`** (all writes via typed tools + Step-57 runtime bookkeeping). AC4 locks the READ; AC2 removes the WRITE — different tools, they compose. Full text in both briefs.

## ✅ On Justin's plate to decide (surface these)
1. **Deploy AC4 Bundle 1** (`v0.2.4`, the security bulkheads) — ready; re-spin a fresh AC4 for the rebase→merge→`railway up`.
2. **AC2** — converge ① now vs. start ② (clustering)?
3. **Mira** — determination-sharing (Step 55, AC3) + the 41a beat (AC4/Mira), both routed through Justin.
4. **Subagent-resume protocol update** — Justin is running an independent AC to define what works + write it into CLAUDE.md / `ac-orchestration-protocol.md` / current-states. (Confirmed: resume via `SendMessage` DOES work now; the protocol's "doesn't exist" claim is stale.)

## 📌 AC0-owed (do these)
- **Mark AC1's 13 DONE steps** in `SPRINT-V0.2-HARDENING.md` (AC1's close-out asked; not yet done).
- **Update `docs/current-state.md`** — stale (last full entry 6/29); needs the wave progress (v0.2.3 deployed, v0.2.4 pending, lane states). *(Justin had it open.)*
- **CHANGELOG** for the wave deploys (the pong fix's entry rides the v0.2.4 record per AC1).
- **#lock: propagate the versioning convention** — AC CLAUDE.md (short "ledger = truth; tag = intent, not ship"), current-state + CHANGELOG pointers to `deploy-log.md`, and **team current-state** (the Rosetta — the highest-value one; it's where "round-2" refs live for the crew). Justin: "do it at #lock."
- **When AC4 Bundle 1 ships:** re-spin a deploy-AC1 to verify the pong warn is gone from live logs.
- **ARCHITECTURE.md truncation canary** — added back this session (Justin authorized; AC4 had flagged it missing).

## 🧭 Key docs (don't re-derive)
- **The 4 lane briefs** (`docs/working/2026.06.30-ac0-ac{1,2,4}-*.md`, `...-ac3-*.md`) — each is the AC↔AC0 mailbox; **read the tails** for live state.
- **`docs/deploy-log.md`** — ship ledger + `v0.2.N` convention + Rosetta.
- **`evryn-backend/docs/SPRINT-V0.2-HARDENING.md`** — the Step list (needs AC1's DONE-marks; has the judge-the-set banner + Step-86 security lane + Step-47 watch-list).
- **`docs/current-state.md`** — snapshot (owed your update). **`_evryn-meta/CLAUDE.md`** — operating manual + `docs/protocols/ac-orchestration-protocol.md` (DC/QC spins).
- **`2026.06.15-ac0-mira-guardrail-placement-brief.md`** — Step 55 spec (back in `docs/working/`, do-not-reap).

## 🔧 Repo / commit state (as of handoff)
- All 10 repos on `main` (except frozen `evryn-team-agents` → `master`), synced with origin, clean.
- **`evryn-backend` `main` = `8e80d01`** (v0.2.3 deployed + pong fix undeployed). Origin lane branches: `ac4/sec-bundle1` (Bundle 1), `dc/cost-haiku-prescreen` (① Haiku). AC1's build branches are reaped.
- **`_evryn-meta` `main`** — tip advances as you commit briefs. Untracked in `_evryn-meta`: AC4's build-specs + ADR-047 (his work — leave them; stage by path).
- **You stay on `main` everywhere — no worktrees for AC0** (the lanes use their own; you do NOT read the worktrees — the briefs report their state).

## First moves for the new AC0
1. Load context per the **load-in note Justin will hand you** (light Cascade + this handoff + the 4 brief tails; stay on `main`; the full runtime is optional — see the note).
2. Send Justin "I'm up, here's the board" + ping; ask which lane needs you next.
3. **Highest-likelihood next actions:** deploy AC4 Bundle 1 (`v0.2.4`) on Justin's go; process AC1's owed sprint-marks; update `current-state.md`.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
