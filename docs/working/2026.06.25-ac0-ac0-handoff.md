# Round-2 Handoff — AC0 → next AC0 (2026-06-25 evening)

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

> **How to use this file:** The next AC0's START-HERE for the round-2 v0.2-hardening close-out. This is the "what's done / do this next / open flags" layer; the *detail* lives in the docs this points to (the #lock just refreshed them). Read, in order: `docs/current-state.md` (the 2026-06-25 evening AC0 stamp = current truth), then this, then `docs/working/2026.06.24-ac0-ac0-convergence-plan.md` (CONVERGENCE EXECUTION STATE §) + `docs/working/2026.06.25-ac0-ac0-persistence-checklist.md` for deeper reference. Working/ephemeral; retire after the bundle ships.
>
> *Authored 2026-06-25T17:39-07:00 by AC0.*

---

## Where it stands (one paragraph)
Round-2 v0.2 hardening is **converged + persistence-complete + DC-code-trip-merged**, sitting on the bundle branch **`r2/convergence-bundle`** (worktree `evryn-backend-bundle`, tip **`d198ef1`**, tsc-clean, pure-logic suite green). **NOT merged to `main`, NOT deployed.** The one thing between here and the deploy is **integration-QC** (not yet run). Deploy is **gated to the next session** (Justin's call).

## Commit / push state (READ — nothing pushed)
- **Bundle `d198ef1`** (local, unpushed): all 5 lanes + DC round-2 trip + hardened DEV-only test guard.
- **`_evryn-meta`** COMMITTED (local, unpushed): persistence-pass baselines + the orchestration-protocol experiment codification (latest `2b4c545`).
- **UNCOMMITTED (this #lock's edits, awaiting Justin's explicit commit/push go — step 18):** `_evryn-meta/docs/current-state.md`, `CHANGELOG.md`, `AGENT_PATTERNS.md`, this handoff; `evryn-team-workspace/.../current-state.md`. **Get Justin's "commit + push" before landing these.**

## DO THIS NEXT — integration-QC (the #1 action)
One batched, full-load **QC** spin on the unified bundle (code + the round-2 docs). Per the orchestration protocol:
- **Use the NEW standing `<task>` load-compliance callout** (just codified — `ac-orchestration-protocol.md`, "The exact words"). This QC spin is the first real use of it; it's the experiment's payoff. Paste it verbatim.
- QC's cascade is **tiered** — Tier 1 = the **full runtime set + the identity files + operator-guide**. Grep-assemble her load list with line spans; do NOT drop the identity files (her standing set).
- The bundle worktree is `evryn-backend-bundle` (`d198ef1`). QC is **read-only** (no worktree, no edits).
- **Tell QC up front (so she doesn't false-flag):** (1) the **6 live-DB tests "fail" only on prod state** (migrations not yet applied / prod wiped) — expected, not bugs; (2) the **Step-32 integration test (`test-execute-approval-transport.ts`) is DEV-only-gated** and SKIPs without `RUN_LIVE_DB_TESTS=dev` + the dev ref — by design (prod-safety); (3) the ARCH round-2 content is a **consolidated section**, deliberately not yet woven into each home section (alignment pass, below).
- Fold any QC blockers: DC builds the fix on a worktree → QC re-verifies → merge into the bundle. Then the bundle is deploy-ready.

## THEN — deploy (GATED, next session, WITH Justin)
1. Merge `r2/convergence-bundle` → `main` (Justin's explicit go).
2. **Prod migrations** (dev-first already done; take pre/post `pg_dump`): Lane A `add-emailmgr-items-updated-at-trigger.sql` (13) + `add-gatekeeper-inbound-addresses.sql` (61); Lane C `add-story-versions-and-consolidate-profile.sql` (10); verify/apply `.roles` `fix-profile-jsonb-comment-2026-06-04.sql`.
3. **`railway up`** + boot-health confirm.
4. **Pre-live setup — the division (Justin clarified: AC does Railway):** *AC* wires the Railway env vars via the Railway CLI — `HEALTHCHECK_URL`, `HEALTHCHECK_AFFIRM_URL`, `PROACTIVE_CHECK_HOUR_PT=10` (`SLACK_OPERATOR_USER_ID` already set). *Justin* (only he can): creates the **2 Healthchecks.io checks** (poll-heartbeat grace ~15 min; daily-affirmation grace ~26 h — see operator-guide) and hands AC the 2 ping URLs, AND sets the **Anthropic monthly spend ceiling** at console.anthropic.com. (Justin's Healthchecks setup was in flight this session.)
5. **`#dev-alerts` identity-merge ping** (the bundle carries identity changes — Lane A/B/C beats; reframed: identity changes take effect on the *manual* `railway up`, not an auto-redeploy).
6. Re-pull `evryn-backend/docs/schema-reference.md` after migrations.

## THEN — cleanup (post-deploy)
- **Reap the 14 round-2 worktrees + the `evryn-backend-dc-r2` experiment worktree** (`dc/r2-step32-84-comments` @ `b8e405d` — the experiment artifact; its test file is forward-committed into the bundle, the rest is a comment-wording variant). None are reapable until merged-to-main.
- **SPRINT DONE-entry reformat** (Justin's ask, deferred to here): rewrite the round-2 DONE entries to `DONE — **bold title** — salient`, and **strip the lane/branch refs** (noise post-reap). This pairs with the reap = one pass.
- **arch/build/sprint/runtime ALIGNMENT pass (a sub-AC):** weave the consolidated ARCH "v0.2 Round-2 Hardening — Architecture Absorptions" section (+ the BUILD round-2 note) **into their home sections** (Pipeline / Data Model / System Actors / Security / Memory Architecture); also clean the stale `current-state.md` "What's Next" body (accreted/historical — the dated stamps are the live layer) and the operator-guide `QUIET_START/END_HOUR_PT` rows (still describe the retired `notify_queue` queue-and-replay model). Justin: "probably an arch/build/sprint/runtime total alignment pass."
- **CHANGELOG May→`docs/historical/changelog-historical/`** monthly split.
- **Archival `pg_dump`** is 9 days old (prod schema unchanged this session) → the deploy's pre-migration dump covers it.

## Open flags / loose ends
- **EVR-72 (Linear):** reframed/resolved at v0.2 (Lane A loads the gatekeeper correctly; contact-enrich is v0.3) — update the ticket (Soren-owned).
- **Mira voice-polish owed** on Lane A's AC-added surface-to-Operator clause (rides the deploy / identity coordination).
- **SPRINT Step 85** text still says the `detect.ts` "Step 52"→"Step 85" code-comment renumber is "owed to DC" — it's **DONE** (in the bundle via the DC trip); clean at the reformat.
- **Step-52 collision** is resolved (GA keeps 52; strict-forward-separator = Step 85) — see the SPRINT flag at Step 52 + Step 85.

## The session's process unlock (so the next AC uses it)
Putting the load-failure callout in the subagent **`<task>`** block (not just `<mandatory_load>`) reliably fixes the chronic partial-load failure — proven live this session (a DC that partial-loaded BUILD re-loaded it in full on re-spin). Codified as a **standing verbatim `<task>` callout** in `ac-orchestration-protocol.md`; pattern written to `AGENT_PATTERNS.md`. **Use it on every DC/QC spin.**

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
