# ACf → ACf Handoff — 2026-07-21 session (cascade refactor + protocol sharpening + team-runtime conducting)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file:** you are (probably) a fresh ACf continuing this session's work, or a compacted ACf re-sharpening. This session's thrust was **org-level architecture** (the load-cascade refactor + protocol sharpening) plus **conducting the team-runtime memory lane** (ACf1). Read this WHOLE file, execute the RELOAD MANIFEST, then pick up the OPEN items. **This is the current continuity doc; it points to the team-runtime memory-lane handoff (below) for that lane's conducting context.** If chat memory and this doc disagree, trust this doc.

---

## Who you are
**ACf** — Justin's architect for the team-runtime work AND related org-level architecture. This session you did two things: (1) refactored the load-cascade system + started a protocol-sharpening pass (org-level, `_evryn-meta`); (2) conducted the team-runtime memory lane — ACf1 is a SEPARATE top-level AC Justin runs; you prep his brief + gate merges. You are NOT the builder; DC builds, QC reviews, ACf1 conducts his own lane, you conduct at the org/team-runtime-architecture altitude.

## RELOAD MANIFEST (execute in full on spin/compact)
1. Light Startup Cascade: `_evryn-meta/CLAUDE.md` (auto) · `_evryn-meta/docs/current-state.md` · Hub `_evryn-meta/docs/hub/roadmap.md`.
2. **This session's artifacts (read to know what shipped):** the new `_evryn-meta/docs/protocols/load-cascades/` dir (README-less; router lives in CLAUDE.md) — `full-product-cascade.md`, `full-team-runtime-cascade.md`, `conductor-cascade.md`, `full-website-cascade.md`, `full-dashboard-cascade.md`; and CLAUDE.md's collapsed "Full Startup Context Cascade" router + the new "persist-learnings" standing order (in Documentation Approach).
3. **The team-runtime memory-lane conducting context:** the still-live handoff `evryn-team-workspace/shared/projects/ops/team-runtime/sessions/2026.07.11-acf-acf-handoff.md` (its 2026-07-21 top block is current) + the tracker `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` (status source of truth; note the new "Dashboard ↔ team-runtime control surface" D1–D3 section).
4. `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` (full, 634 lines) — **before spinning ANY subagent.** This session's hard lesson: an under-specified brief self-lobotomizes the subagent (see LESSONS).
5. `_evryn-meta/docs/protocols/ac-writing-protocol.md` (full) — before writing any doc.
6. `_evryn-meta/docs/protocols/lock-protocol.md` + `evryn-team-workspace/shared/protocols/sweep-protocol.md` + `align-protocol.md` — the queued protocol-sharpening pass edits these.

## ✅ WHAT THIS SESSION SHIPPED (committed + pushed)
- **Load-cascade refactor.** CLAUDE.md's Full + Conductor cascade sections collapsed to a **router table** → per-build cascade files in `_evryn-meta/docs/protocols/load-cascades/`. Light cascade stays inline; the universal principles (two-halves, enumeration trap, two-modes, which-runtime, "when Justin calls the full cascade, don't trim it") stay in CLAUDE.md. Per-build FILE LISTS moved to the cascade files. (`_evryn-meta` commits through `d441395`→`4cf2c49`.)
- **`full-team-runtime-cascade.md`** restructured into **MUST-LOAD** (whole `src`+migrations both halves, the live agent = Lucas, ARCH/tracker/ADRs) vs **LOAD-DEPENDING-ON-WORK** (other 7 agents, skills, design v2, BUILD-PHASE-1, the memory design-of-record) + a "discipline" section making must-load non-negotiable (Justin's ask — kills freelance-trimming).
- **`evryn-team-runtime/docs/ARCHITECTURE.md`** gained a "Why local reasoning often fails here — and why you must load BOTH halves" section (Justin's persist-learnings standing order; Soren owns that doc — **he still needs to be told**). (`evryn-team-runtime` `4eccbfe`→`0c08744`.)
- **ACf1 brief updated + he's re-spun & building item (b).** His brief (`…-acf1-phase1-memory-build-brief.md`) routes reload through `full-team-runtime-cascade.md` and carries the **L9 "refuse-to-boot" build item** (its own trip before item-(b)'s migration deploys). Lane-state front-door fixed + L7 marked closed.
- **Website + dashboard cascades authored** (via AC subagents ACw → ACsite/ACdash independent-pass validation): the design solution is "stable spine + drifting surface, re-verified live," with humility scoped to the drifting surface as **additive verification, never a license to under-load**. Website = runtime-free. Dashboard = a CONTRACT REGISTER (§3).
- **persist-learnings standing order** added to CLAUDE.md (Documentation Approach): build learnings route to their right home (arch/build/sprint), never trapped in a handoff.
- **Dashboard-rewire findings D1–D3** in the tracker (see below).

## 🔄 IN FLIGHT (as of 2026-07-21T16:19)
- **ACdash re-spin (BACKGROUND).** The dashboard cascade doc's FIRST pass self-scoped its load (my brief under-specified it). It's being re-spun with the **verbatim** orchestration blocks (full "STOP SKIMPING" plea) + **every file enumerated** (both runtimes, both halves, ~110 files, line spans). **When it returns:** verify its two-part receipts against the A–H load list; if the load held, review the doc + commit it (source-of-truth → Justin's go); if receipts show a partial load again, re-run. *(If you're a FRESH session, that background agent is dead — check `git log`/the file's reconcile-marker date to see whether the re-spun dashboard doc landed + committed; if not, re-spin per the same brief.)*
- **ACf1** is building item (b) (shared-digest re-key, shape v3) in his own worktree. You conduct: answer his questions, gate his merge. His L9 refuse-to-boot trip is queued before item-(b)'s migration deploys.

## ▶ OPEN / QUEUED — the protocol-sharpening pass (Justin's calls LOCKED; do this while ACdash runs)
Edit these, then present to Justin for commit (source-of-truth):
1. **CLAUDE.md full-cascade section:** add a **CHANGELOG skim on load-in — FULL-CASCADE-ONLY** (Justin locked: not Light/every-session). Shape: skim the CHANGELOG's **top ~100 lines** (it's newest-first — recent ships in full) + skim the `## date (agent — headline)` section-headers below. Rationale: current-state can lag the changelog, so shipped things get missed. **The real fix is current-state currency; this is cheap insurance on top.**
2. **`#lock` protocol (`docs/protocols/lock-protocol.md`):** sharpen the doc-currency gate to include (a) the **persist-learnings** check ("did this session's learnings land in their architectural/build/sprint home, not just a handoff?"), and (b) **current-state currency vs the changelog AND the runtime** ("does current-state reflect what the changelog + the runtime/git show shipped?" — Justin added "AND runtime" — runtime/git is ground truth).
3. **`#sweep` protocol (`evryn-team-workspace/shared/protocols/sweep-protocol.md`):** add the **cross-pollination steal-pass** (product ↔ team-runtime, both directions — read our stuff, read the other runtime's, what should we steal; e.g. the "runtime does the bookkeeping" win that saved ~$2k/mo) + the **current-state-vs-changelog-AND-runtime reconcile**.
4. **My handoff (this file) + the Autonomous Work Protocol:** the persist-learnings + steal-pass are also set-down reminders.
5. **Cross-pollination process** also gets a one-line pointer in both ARCHITECTURE docs (product + team-runtime).

## ⏳ AWAITING JUSTIN
- **Commit-go** for the re-spun dashboard doc (when ACdash returns) — source-of-truth.
- **Soren** needs to be told about the `evryn-team-runtime/ARCHITECTURE.md` edit (his doc of record).
- Any changes to the protocol-sharpening edits before/after I make them (source-of-truth → he reviews).

## 🩹 LESSONS PAID THIS SESSION (don't re-pay)
- **⚠ AC UNDER-SPECIFYING A SUBAGENT BRIEF = the subagent self-lobotomizes, and it's AC's fault.** I spun ACdash with a trimmed `<mandatory_load>` (dropped the verbatim "STOP SKIMPING" plea) and wrote "load the product runtime per full-product-cascade" instead of **enumerating every file with its line span.** ACdash then self-scoped (read contract files, skipped ~54 "non-contract" runtime files). **The orchestration protocol's HARD RULE is literal: paste the blocks VERBATIM (all the pleas), and list EVERY file — never "the runtime" or "per cascade X."** Justin: *"subagents love nothing more than lobotomizing themselves"* — the verbatim pleas + the exhaustive file list are the ONLY things that stop it. Re-read the protocol before every spin.
- **Ping `#team-alerts` LEADING WITH `ACf:`** (not "Need you:" first) — Justin runs many agents; the name must lead. `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`, Node fetch. Pinger at `$TEMP/ping_acf.mjs` (session-specific — recreate from the webhook if absent).
- **Commit only MY files, by explicit path** — `current-state.md` was modified by another agent this session; I never staged it. ACf1 works in shared repos — verify branch/status before editing team-runtime files.
- **Linear is being retired** — don't route new items there; "a line in our sprint/tracker" is Justin's preferred capture.

## Working agreements (this session)
- Source-of-truth commits (CLAUDE.md, ARCHITECTURE, protocol docs, cascade docs) need Justin's explicit go. Lane-brief/mailbox traffic is commit-exempt.
- Full-cascade loads = non-negotiable when Justin calls them; the must-load set is never trimmed.
- s3 spike worktree: AC0's, not mine — off my plate.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
