# ACf → ACf Handoff — 2026-07-21 session (cascade refactor + protocol sharpening + team-runtime conducting)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file:** you are (probably) a fresh ACf continuing this session's work, or a compacted ACf re-sharpening. This session's thrust was **org-level architecture** (the load-cascade refactor + protocol sharpening) plus **conducting the team-runtime memory lane** (ACf1). Read this WHOLE file, execute the RELOAD MANIFEST, then pick up the OPEN items. **This is the current continuity doc; it points to the team-runtime memory-lane handoff (below) for that lane's conducting context.** If chat memory and this doc disagree, trust this doc.

---

## 🔄 2026-07-22 CONTINUATION (ACf) — read FIRST; supersedes the STATE in the 07-21 blocks below (those stay valid for context + lessons)

**🔒 STANDING ORDER (Justin, 2026-07-22 — carry forward, NOT session-scoped): keep `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` current.** It is the team-runtime status source of truth; the conducting ACf updates it at every merge/checkpoint — **a merge isn't "done" until the tracker reflects it.** *(A more durable home may be the tracker's own "how to use this" header; kept here per Justin's explicit instruction — propose promoting it to him if it keeps needing restating.)*

**What changed this continuation:**
- **Full team-runtime cascade re-loaded** (both halves — Justin called it: all ~48 `src/` files + migrations + identity half [team CLAUDE.md, runtime-ops, Lucas def+memory] + ARCH/tracker/ADR-050/052 + the memory design-of-record in full).
- **s3 spike REAPED** — verified superseded: its goal ("on-demand skill loading on both wake surfaces") shipped as **Skills v1** (registry, `config/skills.ts`; the runtime deliberately chose a registry over native `.claude/skills`, 2026-07-13). Local worktree + branch removed; **`origin/spike/s3-protocols-as-skills` still exists** (recoverable).
- **SPRINT tracker fixed** (`team-runtime ba32ad6`): **L9 boot-schema guard → ✅ DONE+MERGED (`815f117`)**; DEPLOY/MERGE footer now notes L9 makes migration-then-code **self-enforcing** (boot refuses until migration 015 is applied).
- **ACf1 brief SHA reconciled** (`evryn-team-workspace 97b85af`): item-(b) worktree base → **verify-HEAD, not a hardcoded SHA** (main is moving) + the L9→`thread_digests` carry-note. ACf1 then marked the L9 near-term-build section **DONE ("DO NOT rebuild")** — **reviewed + agreed** (it prevents a fresh re-spin from rebuilding merged work). Brief is **re-spin-ready**.
- **Corrected picture (supersedes 07-21 line "ACf1 re-spun & building item (b)"):** ACf1 built **L9 first**, then set down. **Item (b) (shared-digest re-key, shape v3) is the LIVE next build** — Justin re-spins ACf1 **fresh** for it (largest Phase-1 item); **ACf conducts** (answer his questions, vet vs. live runtime, gate merges — ACf holds team-runtime merge authority; deploy + migration-apply stay Justin's).
- **ACf2 (Phase 2 consolidation) = HELD.** Gate unmet — Phase 1 not merged, and ACf2 collides with ACf1's remaining items on `composer/`/`memory-writer`/`scheduler/`/`digest/`. Spins **after Phase 1 merges**, scheduling with **ACU**. Brief (`…-acf2-phase2-consolidation-brief.md`) **not yet read by ACf** — read before it spins.
- **Cascade files RENAMED** by Justin's rebuild (`full-X-cascade.md` → `X-full-cascade.md`); inbound refs already swept (verified — no broken links). **Justin's dedicated cascade agent owns those files — do NOT edit them.**
- **Open finding (parked for ACf to place — NOT built):** emergency-notify on **boot refusals** (schema-guard AND env-config) → product's `#emergency-alerts`; its own small trip later (Justin ok'd "(b)").

**Live next-action:** conduct ACf1's **item (b)** build once Justin spins him.

---

## Who you are
**ACf** — Justin's architect for the team-runtime work AND related org-level architecture. This session you did two things: (1) refactored the load-cascade system + started a protocol-sharpening pass (org-level, `_evryn-meta`); (2) conducted the team-runtime memory lane — ACf1 is a SEPARATE top-level AC Justin runs; you prep his brief + gate merges. You are NOT the builder; DC builds, QC reviews, ACf1 conducts his own lane, you conduct at the org/team-runtime-architecture altitude.

## RELOAD MANIFEST (execute in full on spin/compact)
1. Light Startup Cascade: `_evryn-meta/CLAUDE.md` (auto) · `_evryn-meta/docs/current-state.md` · Hub `_evryn-meta/docs/hub/roadmap.md`.
2. **This session's artifacts (read to know what shipped):** the new `_evryn-meta/docs/protocols/load-cascades/` dir (README-less; router lives in CLAUDE.md) — `product-full-cascade.md`, `team-runtime-full-cascade.md`, `conductor-cascade.md`, `website-full-cascade.md`, `dashboard-full-cascade.md`; and CLAUDE.md's collapsed "Full Startup Context Cascade" router + the new "persist-learnings" standing order (in Documentation Approach).
3. **The team-runtime memory-lane conducting context:** the still-live handoff `evryn-team-workspace/shared/projects/ops/team-runtime/sessions/2026.07.11-acf-acf-handoff.md` (its 2026-07-21 top block is current) + the tracker `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` (status source of truth; note the new "Dashboard ↔ team-runtime control surface" D1–D4 section).
4. `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` (full, 634 lines) — **before spinning ANY subagent.** This session's hard lesson: an under-specified brief self-lobotomizes the subagent (see LESSONS).
5. `_evryn-meta/docs/protocols/ac-writing-protocol.md` (full) — before writing any doc.
6. `_evryn-meta/docs/protocols/lock-protocol.md` + `evryn-team-workspace/shared/protocols/sweep-protocol.md` + `align-protocol.md` — the queued protocol-sharpening pass edits these.

## ✅ WHAT THIS SESSION SHIPPED (all committed + PUSHED at end of 2026-07-21 — ACf's night batten-down pushed every repo/branch)
- **Load-cascade refactor.** CLAUDE.md's Full + Conductor cascade sections collapsed to a **router table** → per-build cascade files in `_evryn-meta/docs/protocols/load-cascades/`. Light cascade stays inline; the universal principles (two-halves, enumeration trap, two-modes, which-runtime, "when Justin calls the full cascade, don't trim it") stay in CLAUDE.md. Per-build FILE LISTS moved to the cascade files. (`_evryn-meta` commits through `d441395`→`4cf2c49`.)
- **`team-runtime-full-cascade.md`** restructured into **MUST-LOAD** (whole `src`+migrations both halves, the live agent = Lucas, ARCH/tracker/ADRs) vs **LOAD-DEPENDING-ON-WORK** (other 7 agents, skills, design v2, BUILD-PHASE-1, the memory design-of-record) + a "discipline" section making must-load non-negotiable (Justin's ask — kills freelance-trimming).
- **`evryn-team-runtime/docs/ARCHITECTURE.md`** gained a "Why local reasoning often fails here — and why you must load BOTH halves" section (Justin's persist-learnings standing order; Soren owns that doc — **he still needs to be told**). (`evryn-team-runtime` `4eccbfe`→`0c08744`.)
- **ACf1 brief updated + he's re-spun & building item (b).** His brief (`…-acf1-phase1-memory-build-brief.md`) routes reload through `team-runtime-full-cascade.md` and carries the **L9 "refuse-to-boot" build item** (its own trip before item-(b)'s migration deploys). Lane-state front-door fixed + L7 marked closed.
- **Website + dashboard cascades authored** (via AC subagents ACw → ACsite/ACdash independent-pass validation): the design solution is "stable spine + drifting surface, re-verified live," with humility scoped to the drifting surface as **additive verification, never a license to under-load**. Website = runtime-free. Dashboard = a CONTRACT REGISTER (§3).
- **persist-learnings standing order** added to CLAUDE.md (Documentation Approach): build learnings route to their right home (arch/build/sprint), never trapped in a handoff.
- **Dashboard-rewire findings D1–D4** in the tracker (see below).

## 🔄 DONE THIS TURN / IN FLIGHT
- **ACdash re-spin — ✅ DONE + COMMITTED (locally, unpushed).** The FIRST pass self-scoped (my brief under-specified it); the re-spin used the **verbatim** orchestration blocks + **every file enumerated** (~110 files, both runtimes, both halves) and loaded clean. **The full load caught a real miss the self-scoped pass made:** migration 015 seeds `tool:scratchpad_write` into `capability_catalog` (dashboard LEFT-JOINs it), so the register (stopped at 014) understated the capability universe — now corrected to "re-derive from the migration set." Also caught the `agent_messages` name-collision + re-confirmed the D2 `state_reason` bug + the now-stale `api/team.ts` comment. `dashboard-full-cascade.md` is **committed** (`_evryn-meta 87234e3`, unpushed). *(FRESH session: that background agent is dead; the doc is committed on disk — nothing to re-run.)*
- **ACf1 — L9 refuse-to-boot fix MERGED to team-runtime `main`** (`ed224d8` schema-assertion guard: refuse to boot on a missing DB relation; + QC-blocker fixes `61b996a`/`815f117`). This is the "ACf1 has merged" from Justin's 2026-07-21 ballot — the **correct pre-item-(b) sequencing** (L9 lands before item-(b)'s `thread_digests` migration). **Item (b) (shared-digest re-key, shape v3) is still NEXT, not yet merged.** **Deploy HELD** (runtime dormant; migration-then-code sequencing not yet exercised). ⚠ **ACf1's lane-state doc (`sessions/2026.07.16-acf1-acf1-lane-state.md`) is STALE** — its LIVE-NEXT-ACTION still shows the pre-L9 status; trust the team-runtime `main` git log as ground truth. You conduct: answer ACf1's questions, gate his merges.

## ✅ PROTOCOL-SHARPENING PASS — DONE + COMMITTED + PUSHED (night batten-down)
Justin's calls LOCKED; edits made + committed:
1. ✅ **CLAUDE.md:** full-cascade-only CHANGELOG-skim on load-in. Committed WITH Justin's own tweaks swept in (his "describe the mechanism, not the label" comms bullet + a full-cascade rewording) — he authorized it. (`_evryn-meta 87234e3`.)
2. ✅ **`#lock` (`lock-protocol.md`):** step 3 missed-ship reconcile (current-state vs changelog AND runtime, both directions); step 5d persist-learnings check. (`_evryn-meta 87234e3`.)
3. ✅ **`#sweep` (`sweep-protocol.md`) — LUCAS'S doc:** step 6 current-state-vs-changelog-AND-runtime reconcile + NEW step 11 cross-runtime steal-pass (the "runtime-does-the-bookkeeping / ~$2k-mo" example baked in). (`evryn-team-workspace ddc638b`.)
4. ✅ **Tracker dashboard set:** D1–D3 + **new D4** (emailmgr status-pill gap) + D2/D3 amendments (stale `api/team.ts` comment; `agent_messages` name-collision rewire trap). (`evryn-team-runtime 8936df1`.)
5. **STILL TODO (small, not started):** the cross-pollination one-line pointer in both ARCHITECTURE docs (product + team-runtime).

## ⏳ OPEN / AWAITING
- **PUSHES — night batten-down done, with ONE unresolved (Justin, 2026-07-21).** Justin reversed "AC0 handles pushes" and had ACf push everything. **✅ Pushed + safe on remote:** all `_evryn-meta` / `evryn-team-workspace` / `evryn-team-runtime` main-branch work (incl. the L9 merge), + **Lucas's `lucas/sweep-align-2026-07`** (was never-pushed, now on origin in both repos). **🔴 NOT pushed — flagged to Justin:** `evryn-backend ac2/step57-runtime-bookkeeping` (AC2's cost-lane work) has **22 local commits that DIVERGED from origin** (non-ff — origin also has commits local lacks). ACf did **NOT** force-push or rebase (it's AC2's live branch; force/rebase risks destroying his or origin's work). Those 22 commits are on local disk only — **AC2/Justin must reconcile** (pull --rebase or merge, deciding the canonical line). **⚠ 4 uncommitted files in `evryn-backend`** (another agent's in-progress) also left as-is (can't commit others' unstaged work).
- **Steal-pass HOME (#sweep vs #align): ✅ RESOLVED** — stays in `#sweep`; Lucas moves it to `#align` in his sweep if he wants. **No action** (don't loop Lucas in — he catches it in #sweep).
- **Soren: ✅ RESOLVED** — he reads the changelog and will see the `evryn-team-runtime/ARCHITECTURE.md` edit there (named in the 2026-07-21 CHANGELOG entry). No separate notification needed.
- **STILL-TODO (small):** the ARCHITECTURE cross-pollination pointers (protocol-pass item 5) — not started.
- **ACf1 merge — ✅ RESOLVED:** the merge was the **L9 refuse-to-boot fix** (not item (b)) — Justin's go, merged clean. **Deploy HELD** (dormant runtime). **ACf1 did his own current-state + changelog** for it; ACf did his own 2026-07-21 changelog entry. Item (b) remains the next big lane item.
- **🔎 s3 SPIKE — INVESTIGATE NEXT TIME (Justin, 2026-07-21).** AC0 is convinced the `spike/s3-protocols-as-skills` branch/worktree was **ACf's**, not AC0's (contradicting the earlier read that it was a stale Justin-era Phase-0 spike). **Look back through ACf's previous handoffs (`evryn-team-workspace/.../sessions/2026.07.11-acf-acf-handoff.md` and earlier) + the CHANGELOG** to figure out what it actually is and whose it is before anyone reaps it. Don't assume; the ownership is genuinely unresolved.

## 🩹 LESSONS PAID THIS SESSION (don't re-pay)
- **⚠ AC UNDER-SPECIFYING A SUBAGENT BRIEF = the subagent self-lobotomizes, and it's AC's fault.** I spun ACdash with a trimmed `<mandatory_load>` (dropped the verbatim "STOP SKIMPING" plea) and wrote "load the product runtime per product-full-cascade" instead of **enumerating every file with its line span.** ACdash then self-scoped (read contract files, skipped ~54 "non-contract" runtime files). **The orchestration protocol's HARD RULE is literal: paste the blocks VERBATIM (all the pleas), and list EVERY file — never "the runtime" or "per cascade X."** Justin: *"subagents love nothing more than lobotomizing themselves"* — the verbatim pleas + the exhaustive file list are the ONLY things that stop it. Re-read the protocol before every spin.
- **Ping `#team-alerts` LEADING WITH `ACf:`** (not "Need you:" first) — Justin runs many agents; the name must lead. `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`, Node fetch. Pinger at `$TEMP/ping_acf.mjs` (session-specific — recreate from the webhook if absent).
- **Commit only MY files, by explicit path** — `current-state.md` was modified by another agent this session; I never staged it. ACf1 works in shared repos — verify branch/status before editing team-runtime files.
- **Linear is being retired** — don't route new items there; "a line in our sprint/tracker" is Justin's preferred capture.

## Working agreements (this session)
- Source-of-truth commits (CLAUDE.md, ARCHITECTURE, protocol docs, cascade docs) need Justin's explicit go. Lane-brief/mailbox traffic is commit-exempt.
- Full-cascade loads = non-negotiable when Justin calls them; the must-load set is never trimmed.
- s3 spike: **ownership UNRESOLVED** (AC0 thinks it's ACf's) — see the 🔎 s3 investigate-next-time note in OPEN/AWAITING.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
