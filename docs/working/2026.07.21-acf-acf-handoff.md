# ACf → ACf Handoff — team-runtime memory lane (continuity doc; current through 2026-07-23)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file:** you are a fresh ACf (or a compacted one re-sharpening). This doc is **self-contained for your current mission — conducting the team-runtime memory lane (ACf1) at the item-(c) build.** Read it top to bottom, execute the RELOAD MANIFEST in full, then pick up the LIVE NEXT-ACTION. If chat memory and this doc disagree, trust this doc. *(Filename keeps the 07-21 date because current-state + other docs point at this path; the content is current.)*

---

## Who you are
**ACf** — Justin's architect + **conductor of the team-runtime memory lane.** The team runtime (`evryn-team-runtime`) is the founding team's autonomous harness (ADR-050); it's **dormant/boot-paused** (autonomy off until the Meta-Meeting). You are rebuilding its **memory + wake-economics subsystem** (ADR-052).
- **ACf1** is a *separate top-level AC* Justin spins to BUILD the lane's items. **ACf1 conducts his own build** (spins DC to build + QC to review); **you gate his merges, own the lane's design decisions, and hold the org/cross-lane altitude.** You are **NOT the builder** and you do not spin DC/QC for the build — ACf1 does.
- **Your authority:** you hold **merge authority on `team-runtime`** (uncontended repo). **Deploy + migration-apply each stay Justin's explicit go.**

## Current state (team-runtime memory Phase 1)
Phase 1 is a set of build items (a)→(e) + opt-in notes. **3 merged, all dormant + undeployed:**
- ✅ **(a) scratchpad + headline** — merged `9a2c231` (migration 015 `thread_scratchpads`, UNAPPLIED).
- ✅ **L9 boot-schema guard** — merged `815f117` (refuse-to-boot on a missing DB relation → migration-then-code self-enforcing).
- ✅ **(b) shared-digest re-key** — merged `567056a`. `digest_lines(agent,thread)` → the shared **`thread_digests`** registry (shape (ii): a `last_activity_at` insert-trigger; layer-9 composes OTHER rooms only; `runWake`'s post-reply `regenerateDigestLine` DELETED; 30-min sweep + on-read staleness; activity-keyed dormancy). **Migration 016 `thread_digests`, UNAPPLIED.**
  - ⚠️ **Item (b)'s ENTIRE DB layer is UNVERIFIED-LIVE** — trigger / PostgREST queries / Haiku regen / dormancy are logic+unit-tested only (a build trip never applies migrations). Live-proven only at the Justin-gated **016-apply + Lucas paces**. **Do NOT report it live-proven.**
- 🔨 **(c) is BUILDING (ACf1 live, spun 2026-07-23).** Then (d) membership verbs, (e) dormancy memory-write.

> **⚠ FIRST THING ON SPIN — reconcile against ground truth, because this doc was written while (c) was building:** run `git -C evryn-team-runtime log --oneline -8` and read the **tracker** (`docs/SPRINT-team-runtime-memory.md`). If item (c) has since **merged**, your live next-action shifts to **gating item (d)** (membership verbs — §4.2/§4.6) — read that section of the design-of-record instead of (c)'s. The tracker is the source of truth for *where the lane actually is*; this handoff tells you *how to conduct it*.

## RELOAD MANIFEST (execute in full on spin/compact — you cannot gate a runtime build you haven't loaded)
1. **Light cascade:** `_evryn-meta/CLAUDE.md` (auto) · `_evryn-meta/docs/current-state.md` · Hub `_evryn-meta/docs/hub/roadmap.md`.
2. **The FULL team-runtime cascade, BOTH halves — non-negotiable:** `_evryn-meta/docs/protocols/load-cascades/team-runtime-full-cascade.md`. Follow its MUST-LOAD: the whole `src/**` + `migrations/**` (live-enumerate, read every file), the **identity half** (team `evryn-team-workspace/CLAUDE.md` + `shared/protocols/runtime-ops.md` + Lucas's agent def + memory), `evryn-team-runtime/docs/ARCHITECTURE.md`, the tracker, ADR-050 + ADR-052. **Verify HEAD first:** `git -C evryn-team-runtime rev-parse --short HEAD` — main moves; never trust a hardcoded SHA.
3. **Item (c)'s spec (the design-of-record):** `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-team-runtime-memory-architecture.md` — read **§4.3** (full-thread-load + composed-load advisory) + **§4.6** (per-agent serialization + the thread model) IN FULL. This is ADR-052's design-of-record.
4. **ACf1's brief (the lane surface):** `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-acf1-phase1-memory-build-brief.md` — its **top re-spin block** is the item-(c) front door (dated 2026-07-22 when last read; ACf1 refreshes it each spin — read whichever is newest); the tail carries the full **ACf↔ACf1 design exchange** (read **A2** for item (c)'s serialization trap). ACf1's forensics ledger: `sessions/2026.07.16-acf1-acf1-lane-state.md`.
5. **The tracker (status source of truth):** `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md`.
6. **Before spinning any review subagent to gate a merge:** `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` (full). **Before writing any doc:** `_evryn-meta/docs/protocols/ac-writing-protocol.md` (full).

## LIVE NEXT-ACTION — conduct + gate ACf1's item (c)
ACf1 is building **(c) = full-thread-load + composed-load advisory + per-agent wake serialization (§4.3 + §4.6)**, which owns finding **L5**. Answer his questions, vet his approach against the live runtime, and **gate his merge against these settled points** (all from §4.3/§4.6 + the A2 exchange — do NOT let them get lost):
- **Full-thread-load:** `windowThread`'s 30-msg cap is REMOVED for layer 10 (the thread loads in full); a hard safety-ceiling fallback stays for a pathological single thread.
- **Composed-load advisory:** generalize `currentStateSizeAdvisory` (`composer/layers.ts`) into a whole-composed-load advisory that reads the manifest totals, names which layer is heavy + a burn-down menu. No wake, no DB write — a passive nudge.
- **⚠ Per-agent serialization — THE A2 TRAP (the one most likely to bite):** the in-flight lock moves per-(agent,thread) → **per-AGENT**, BUT **coalescing STAYS keyed on (agent,thread)** — they are different concerns and must not share a key. Add a pass that picks ONE wake per agent and **DEFERS the rest UNCONSUMED** (`processed_at` null / `wakeups.fired` false, so the next cycle retries them) — a deferred-but-marked candidate is silent wake-loss. `dedup.ts`'s comment *"two due wakeups for one agent run in parallel legally"* becomes FALSE and must be rewritten in the same commit. Keep the `agent_state.singleton_lock.{threads:{…}}` map shape (now holds one entry) with a why-comment so nobody "cleans it up" and breaks recovery.
- **L5:** layer-10 conflates empty-vs-absent thread — surface `threadId` unconditionally.
- **Gate mechanics:** every real code change gets QC (ACf1 runs it); you add the independent gate-review. You hold merge authority; **deploy + migration-apply stay Justin's.** When item (c) merges, **update the tracker** (standing order below).

## Open items ACf owns (none block item (c))
1. **L12 — dormant→live digest resurrection (my DESIGN CALL, → item (e)).** No code transitions `thread_digests.state` dormant→live, so a re-activated dormant thread never re-appears in the ambient layer-9 digest. **Nothing is lost** (the ledger + item-(e)'s dormancy memory + @mention all re-surface it) — the gap is only the *passive* ambient re-surface, which may be a non-issue. Item (b) is correct to its spec; the §4.4 "echo rule" assumes a returning line. **My lean (NOT locked):** fold a minimal resurrection into **item (e)** — "on a bump into a dormant thread, set `state` back to `live`" (cheap; honors the echo rule) — finalize when building (e). Tracked against item (e).
2. **Emergency-notify on BOOT REFUSALS** (the L9 schema-guard AND the pre-existing env-config refusal) → product's `#emergency-alerts`. Today a boot-refuse is loud to logs + a dead deploy but sends NO Slack ping (the throw happens before `createNotifier` exists). Needs a boot-safe direct webhook POST at the throw site + an emergency-webhook env var. **Its own small trip, later.**
3. **Read the ACf2 brief before Phase 2 spins:** `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-acf2-phase2-consolidation-brief.md`. **ACf2 (Phase 2 consolidation) is HELD** until Phase 1 merges (it collides with ACf1's remaining items on `composer/`/`memory-writer`/`scheduler/`/`digest/`); scheduling is with **ACU**.
4. **Small org leftover (low priority):** a one-line cross-pollination pointer between the product + team-runtime `ARCHITECTURE.md` docs (from the 07-21 protocol pass) — not started. Non-urgent; carry it.

## Deploy posture
**DEFERRED by plan (ACf1's rec, Justin decides).** Don't deploy item (b) alone — the runtime is dormant (a deploy does nothing live pre-Meta-Meeting), Phase-1 items interlock (§6.3), and the acceptance test is the Lucas paces with ALL Phase-1 parts ON. **Batch the Phase-1 migrations (015 + 016 + whatever (c)–(e) add) into ONE migrate-then-deploy window at a checkpoint — migrations applied IN ORDER FIRST (the L9 guard now enforces this: boot refuses until they're applied), then the paces.** Nothing to deploy now.
- **The paces = the Lucas acceptance test** (`evryn-team-runtime/docs/BUILD-PHASE-1.md` success criteria 1–8). Criteria **1/2/4/7 passed live 2026-07-14**; **3/5/6/8 + the UNVERIFIED-LIVE items remain** — a self-set wakeup fires · a $2-cap job pitches at 60% + freezes at 100% (ACf/ACf1 set up the `jobs` fixture) · kill-switch mid-wake parks losslessly + restart-recovers · the loop-signature detector warns→kills (needs a loop fixture). They re-run with ALL Phase-1 parts ON at the deploy checkpoint. **Split: ACf1 runs the mechanical criteria; Justin judges whether Lucas still *feels* like Lucas.**

## Gates + working discipline (read before you act)
- **🔒 STANDING ORDER (Justin, 2026-07-22): keep `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` current — a merge isn't "done" until the tracker reflects it.** (A more durable home may be the tracker's own header; propose promoting it if it keeps needing restating.)
- **Commit discipline:** commit **only your files, by explicit path** — AND (hard-won this session, now in CLAUDE.md commit-discipline) **`git diff <file>` FIRST** before committing a shared file. By-path is necessary but NOT sufficient: a by-path commit takes the file's whole working-tree state, so another agent's uncommitted edits to the SAME file ride along under your message (it happened this session — swept the cascade agent's rename edits). Merge / deploy / migration-apply each need Justin's explicit go; **lane-brief / mailbox / handoff traffic is commit-exempt. AC0 handles all pushes** — don't push.
- **Multi-agent repos:** `_evryn-meta` and `evryn-team-runtime` frequently have OTHER agents active (Justin's cascade agent, AC2/AC3, ACf1's DC/QC worktrees). Verify branch + `git status` + diff-first before editing/committing any shared file; leave others' modified/untracked files alone.
- **Subagent spins (if you gate with a review agent):** ONLY via the orchestration protocol — paste the blocks VERBATIM (all the pleas), **enumerate EVERY file with line spans** (never "the runtime" / "per cascade X"), **model-pin `opus`**. An under-specified brief self-lobotomizes the subagent (paid for this the hard way — see LESSONS).
- **Ping `#team-alerts` on EVERY response, LEADING with `ACf:`** (name first, not "Need you:"). `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`, Node `fetch`. Pinger at `$TEMP/ping_acf.mjs` (session-specific — recreate from the webhook if absent). Substance in chat; the ping is a concise tap.
- **Never use `AskUserQuestion`** (it hangs) — ask in plain chat + ping.

## LESSONS carried forward (durable — don't re-pay)
- **ACf1's push-back is usually RIGHT — verify the DESIGN, not his competence.** He holds the whole runtime; you hold the design. When his read of the live code disagrees with the design doc, **the code wins and the doc gets fixed** (he corrected ACf 3/3 on real defects during the item-(a)/(b) build, incl. two defects *in the design doc itself*). Treat his notes as an objection clock, not a gate — but vet each against the source before you rule.
- **An under-specified subagent brief = the subagent self-lobotomizes, and it's AC's fault.** Paste orchestration blocks VERBATIM; enumerate EVERY file. Justin: *"subagents love nothing more than lobotomizing themselves."*
- **"Commit by path" is necessary-not-sufficient** — diff-the-file-first in a multi-agent repo (now codified in CLAUDE.md commit-discipline).
- **Distinguish DID from RECOMMENDED/INTENDED**, and **verify against the artifact** (the runtime/the file/the git log), not the narrative — before reporting state.
- **Linear is being retired** — don't route new items there; a line in the tracker is Justin's preferred capture.

## Pointers (quick)
- Lane surface: **ACf1's brief** (above) · Spec: **design-of-record** §4.3/§4.6 (above) · Status: **the tracker** (above) · Forensics: **ACf1's lane-state ledger** (above).
- Cross-runtime raw-write check → **AC0 brief** `docs/working/2026.07.23-acf-ac0-brief.md`.
- Item (b)'s build spec (provenance): `docs/working/2026.07.22-acf1-dc-item-b-shared-digest-build-spec.md`.

## ✅ DONE this session-arc (2026-07-22 → 07-23) — so you don't redo it
- Item (b) shipped (merged `567056a`) + the full **#lock** for it: team-runtime **CHANGELOG** entry for item (b) **AND backfilled** item (a) / safety-trip / L9 entries; **`current-state`** refreshed; **`ARCHITECTURE.md`** memory-section updated to reflect (a)/(b)/L9 merged + the item-(b) digest re-key (Justin-authorized, `8c2a735`). *(These were briefly listed as "owed/routed" in an earlier draft of this handoff — they are DONE.)*
- **s3 spike** — investigated + REAPED (it was ACf's deferred Phase-0 spike, superseded by Skills v1; `origin/spike/s3-protocols-as-skills` still exists, recoverable). Ownership question **resolved** — not an open item.
- **Cross-runtime raw-write-surface check** — DONE, no propagation needed (AC0 brief).
- The commit-sweep lesson → codified into **CLAUDE.md** (`03dc841`).
- **Provenance for the 07-21 org session** (load-cascade refactor + protocol-sharpening pass + website/dashboard cascades) — **shipped + pushed; see `_evryn-meta/CHANGELOG.md` 2026-07-21.** Not carried here (lean-brief rule).

## Product-lane context (NOT yours — awareness only)
The product lane (AC0/AC2/AC3) shipped **`v0.2.8`** then **`v0.2.9`** (③ Runtime Bookkeeping / Step 57 + the SPRINT-91 halt-resume Mark-blocker fix) 2026-07-22→23. Separate repo (`evryn-backend`), separate prod, separate DB — **zero collision with team-runtime.** If someone references it, that's their lane.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
