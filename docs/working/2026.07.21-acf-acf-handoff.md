# ACf → ACf Handoff — team-runtime lane (continuity doc; CURRENT through 2026-08-04T19:35-07:00)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.
>
> **How to use this file:** you are a fresh **ACf** (or a compacted one re-sharpening). This doc is **self-contained for your mission**. Read it top to bottom, execute the RELOAD MANIFEST in full, then pick up the LIVE NEXT-ACTION. **If chat memory and this doc disagree, trust this doc.** *(The filename keeps its 07-21 date because `current-state.md` and other docs point at this path — the CONTENT is current as of the stamp above.)*
>
> **⚠ This is a FULL REWRITE (2026-08-04 evening).** Everything before it is superseded. Don't hunt for older ACf handoff blocks — there aren't any layered inside; this file is the whole picture.

---

## 1. Who you are, and what changed about that TODAY

**ACf** — Justin's architect and **coordinator of the team-runtime lane.** The team runtime (`evryn-team-runtime`) is the founding team's autonomous harness (ADR-050). It is **dormant/boot-paused by construction** — deployed to Railway, wakes nobody, and stays that way until the Meta-Meeting. **Zero overlap with the Evryn product** (separate repo, separate prod, separate DB). You are rebuilding its **memory + wake-economics subsystem** (ADR-052).

### ⚠️ THE AUTHORITY MODEL CHANGED ON 2026-08-04 — this supersedes every older description

| | Before | **NOW (Justin, 2026-08-04)** |
|---|---|---|
| Item gates + merges in ACf1's lane | ACf | **ACf1** — he gates AND merges his own lane |
| Who spins the build's DC/QC | ACf1 | ACf1 (unchanged) |
| Who spins the *gate* QC | ACf | **ACf1** |
| ACf's role | gate-keeper | **coordinator** — design altitude, cross-lane collisions, own lanes |

**Justin's reasoning:** ACf1 holds the deepest build context. **The one caveat that rides with it:** his gate reviewer must be a **genuinely fresh** spin, never a resumed one — a resumed reviewer carries its prior framing, so it is not fresh eyes, and *nothing in the output would look different.*

**You may spin your own AC lane subagents** (Justin, 2026-08-04). **Name them** for auditability — `ACfb` was the boot-refusal lane; use the next free suffix. ⚠️ **`ACf2` IS ALREADY TAKEN** — it names the Phase-2 consolidation lane (briefed, HELD). Don't reuse it.

**Standing conditions on spinning a lane AC** (Justin was emphatic): spin it **via the orchestration protocol**, make sure **it spins its own subagents the same way** (the recursive-propagation clause is mandatory in its `<task>`), and make sure **receipts are passed up to you properly**.

**Your authority:** design calls in your lane; you merge your **own** lanes' work with Justin's go. **Deploy + migration-apply + merge each stay Justin's explicit go.** AC0 pushes (but see §7 — Justin told ACf to push tonight).

---

## 2. WHERE THE LANE ACTUALLY IS — verified state, 2026-08-04T19:35

**`evryn-team-runtime` `main` = `7553b09`. 6 commits unpushed at the time of writing (see §7).** One clean worktree + one sweep worktree. No stray branches. Runtime still dormant, **nothing deployed, no migration applied.**

### Memory Phase 1 — items (a) → (e)

| Item | State |
|---|---|
| **(a)** scratchpad + headline | ✅ MERGED `9a2c231` (migration 015, UNAPPLIED) |
| **L9** boot-schema guard | ✅ MERGED `815f117` |
| **(b)** shared-digest re-key | ✅ MERGED `567056a` (migration 016, UNAPPLIED). ⚠ Its whole DB layer is **UNVERIFIED-LIVE** — logic/unit-tested only. Do NOT report it live-proven. |
| **(c)** full-thread-load + composed-load advisory + hard ceiling + L5 + per-agent serialization + sender-priority queue | ✅ **MERGED 2026-08-04 `5ed56e5`** (ACf1 gated + merged). No migration. **ACf audited his close-out and concurred.** |
| **(d)** membership verbs | ❌ NOT STARTED — **Justin-gated to begin** |
| **(e)** dormancy memory-write | ❌ NOT STARTED — **Justin-gated to begin** |

**3 QC findings routed from (c) → item (d)** (full file:line detail is in the SPRINT tracker's item-(d) row): (1) an **inFlight lock-leak** in `scheduler/index.ts` `acquireThreadLock` — `inFlight.add(agent)` happens before an un-rolled-back `await persist`, so a DB blip strands the agent "busy" until restart; item (c) *widened* it from one-thread to whole-agent; one-line rollback fix. (2) The **Slack/check-in dispatch-drop lying log-line** — `dispatch()` logs *"queued for next cycle"* but Slack rows and the daily check-in are **not** re-queued. (3) A **priority case-sensitivity** nit in `dedup.ts` (`mains.has()` is case-sensitive while `justin` is matched case-insensitively) — can't bite until (d) wires real inter-agent senders.

### ACf's own lane — the boot-refusal emergency alert: ✅ BUILT + MERGED today

**The problem it fixed, in plain terms:** the runtime deliberately refuses to boot under several conditions (bad config, a missing DB relation, an unreachable clone, a double-run). All correct. But **every one was silent to Slack** — you'd get a deploy that simply never came up.

- **Merged to `main`** as `dcb6ce1` + `787a196`. New `src/safety/emergency.ts` (zero imports — the boot-safety guarantee is visible at a glance), wired at `src/index.ts`'s `boot().catch(handleBootFailure)`, `.env.example` documented, 16 guard tests. **typecheck exit 0, 299/299, 0 skipped — ACf verified independently, twice.**
- **ACf's sharpening that shaped the design:** the original L9 note said the throw happens "before `createNotifier` exists." True for two refusals — but `notify` is **never called on ANY throw path**, so the post-notifier refusals were equally silent. One uniform alert at the single funnel covers all of them.
- 🔴 **IT SHIPS INERT — see §4 item 1.** This is the most important open thing in the lane.

### The truncation-canary sweep: ✅ BUILT, ❌ NOT MERGED

- Branch **`dc/code-truncation-canaries`**, worktree **`Code-dc/etr-canary-sweep`**, tip **`0d14b0a`** (`7d83e5b` sweep + `0d14b0a` ACf's line-ref fixes).
- 87 code files gained a canary (top note + bottom line, in each file's own comment syntax). Purely additive — proven by byte-for-byte reconstruction of every original. `src/wakes/memory-writer.ts` correctly **excluded** (it holds the canary string as a *functional constant*).
- **typecheck exit 0, 299/299, 0 skipped — ACf re-verified after his own edits.**
- **Awaiting Justin's merge-go.** ⚠ DC ran `npm ci` in that worktree — delete `Code-dc/etr-canary-sweep/node_modules` when reaping.

---

## 3. RELOAD MANIFEST — execute in full; you cannot direct this lane unloaded

1. **Light cascade:** `_evryn-meta/CLAUDE.md` (auto) · `_evryn-meta/docs/current-state.md` · Hub `_evryn-meta/docs/hub/roadmap.md`.
2. **The FULL team-runtime cascade, BOTH halves — non-negotiable:** `_evryn-meta/docs/protocols/load-cascades/team-runtime-full-cascade.md`. Follow its must-load: the whole `src/**` + `migrations/**` (**live-enumerate** — the tree grew again today), the **identity half** (`evryn-team-workspace/CLAUDE.md` + `shared/protocols/runtime-ops.md` + Lucas's agent def + his MEMORY.md), `evryn-team-runtime/docs/ARCHITECTURE.md`, the SPRINT tracker, ADR-050 + ADR-052. **Verify HEAD first** — `main` moved **four times** on 2026-08-04; never trust a hardcoded SHA.
3. **The design-of-record:** `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-team-runtime-memory-architecture.md` — for (d)/(e) read **§4.2, §4.4, §4.6** in full. Exceeds one Read call; page it to the canary.
4. **ACf1's lane brief:** `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-acf1-phase1-memory-build-brief.md`. **Its 2026-08-04 front-door block is clean and current** — read that first. The older blocks are HISTORY (read only for the LESSONS, the settled A2 serialization answer, and the charter). The tail carries the full ACf↔ACf1 design exchange.
5. **The tracker (status source of truth):** `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md`.
6. **Before spinning anything:** `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` (full). **Before writing any doc:** `_evryn-meta/docs/protocols/ac-writing-protocol.md` (full).

---

## 4. 🔴 LIVE NEXT-ACTIONS — in priority order

### 1. The boot-refusal alert is INERT until one Railway variable is set — and the value is NOT where Justin thought

**Justin authorized ACf to set it (ballot: "b — it lives in evryn-backend .env"). ACf investigated and did NOT proceed, deliberately. Here is exactly why, so you don't repeat the search:**

- The team runtime reads **`EMERGENCY_ALERTS_WEBHOOK_URL`**. It is set **nowhere** — so today the alert degrades to log-only and a boot refusal is *still* silent.
- **It is NOT in `evryn-backend/.env`.** ACf checked: that file holds `SLACK_WEBHOOK_URL`, `SLACK_DEV_WEBHOOK_URL`, `SLACK_APP_TOKEN`, `SLACK_BOT_TOKEN` — no emergency webhook.
- The product reaches `#emergency-alerts` via **`SLACK_EMERGENCY_WEBHOOK_URL`** (`evryn-backend/src/config.ts:559`), and that value lives on the **product's Railway service**, not on disk.
- **Why ACf stopped:** retrieving it means either printing a live secret into an agent's context (the self-poisoning hazard that already cost one ACf a full re-spin) or a cross-project Railway-to-Railway transfer with real blast radius if a service name is wrong — **while Justin was unavailable to unstick it.** CLAUDE.md says: if the unsafe path is the only path, get explicit permission with the blast radius named. He was gone.
- **✅ THE 30-SECOND FIX FOR JUSTIN:** in the Railway dashboard, copy `SLACK_EMERGENCY_WEBHOOK_URL` from the **evryn-backend** service and set it as **`EMERGENCY_ALERTS_WEBHOOK_URL`** on the **evryn-team-runtime** service. Zero secret ever touches an agent context.
- **⏰ WHEN IT MUST HAPPEN: before the migrate-then-deploy window.** That deploy applies migrations 015 + 016, and the L9 guard will refuse boot until they land — *that refusal is the first one this alerting was built for.* Unset, it fires into the same silence it was meant to end.
- Railway CLI is authenticated and correctly linked (`Project: evryn-team-runtime / production / evryn-team-runtime`) if you do get a clean go.

### 2. The NUL byte in `src/safety/loop-signature.ts` — Justin authorized its own tiny trip (ballot: **a**). NOT STARTED.

**This is a real review-surface hole and it sits in a safety file.** That file contains a **raw NUL byte at offset 1413** — someone wrote a literal null as a hash delimiter instead of the `\0` escape. Consequences, all confirmed first-hand:

- `git diff` shows only *"Binary files differ"* — **any change to it is invisible in review.**
- `grep` returns *"Binary file matches"* — you cannot search it.
- It's excluded from line-ending normalization (the tree's only LF file).

It's the **loop-signature detector** — the safety layer that kills a runaway wake. **Fix: one character (`\u0000` → `\0`, semantically identical).** Pre-existing, NOT introduced by the sweep (verified both by DC and independently by ACf). Justin's ruling: **its own tiny trip** — do not fold it.

### 3. Draft the orchestration-protocol edits — Justin: **"I want them drafted, but not tonight."**

The pilot findings are in §6. Draft them as an uncommitted edit for his vet.

### 4. Merge decision owed on the canary sweep

Branch `dc/code-truncation-canaries` (§2) is verified green and unmerged. It needs Justin's merge-go, then reap the worktree + branch + its `node_modules`.

---

## 5. Open items ACf owns (none blocking)

1. **`L12` — dormant→live digest resurrection. ACf's DESIGN CALL.** No code transitions `thread_digests.state` from dormant back to live, so a re-activated dormant thread never re-appears in the ambient layer-9 digest. **Nothing is lost** (the ledger, item-(e)'s dormancy memory, and @mention all re-surface it) — the gap is only the *passive* ambient re-surface. **Lean (not locked): fold a minimal "a bump into a dormant thread sets `state` back to `live`" into item (e)**, which owns that lifecycle. Finalize when (e) is built.
2. **ACf2 (Phase 2 — consolidation wiring + FTS)** stays **HELD** until Phase 1 completes — it collides with (d)/(e) on `composer/` · `memory-writer` · `scheduler/` · `digest/`. Scheduling is with **ACU**. Brief: `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-acf2-phase2-consolidation-brief.md`.
3. **A one-line cross-pollination pointer** between the product and team-runtime `ARCHITECTURE.md` docs. Low priority; carry it.
4. **Cosmetic tracker inconsistency:** the SPRINT item-(c) row says "5 QC findings ROUTED"; the item-(d) row correctly carries **3** (the other two were *dispositioned*, not routed — one already in the batched runtime-ops edit, one cosmetic-by-design). Nothing is lost. Fix whenever you're next in the tracker.

---

## 6. 📋 THE AC-UNDER-AC PILOT — Justin's standing assignment

**Justin designated ACf the pilot user of the AC-under-AC pattern (2026-08-04)** and asked for the process to be *refined*, not just used: *"is everyone doing what they're supposed to, based on the protocol? Does the protocol have gaps that if we closed them the process would work better?"* **This is an ongoing deliverable — keep collecting.**

**His framing of why it's viable now:** the pattern was janky before because a broken subagent could only be re-spun. **Resume changes that** — you can now fix a lane mid-flight.

**Did it work? Yes.** Three proofs from the first run: ACf's correction reached ACf1 through a committed file and changed what he did; ACfb overruled ACf on a documentation call and was right; and when ACfb reported its fix-trip "in flight," the **artifacts showed it had finished** — a resume closed it out cleanly where the old rules would have forced a re-spin. **Cost: ~1.5M subagent tokens** across the session.

### Gaps found so far — draft these into `ac-orchestration-protocol.md`

1. **The canary instruction was unsatisfiable for code files.** The verbatim block orders "confirm the bottom truncation canary" on every full-read file; 87 code files had none. ⚠️ **Partly fixed tonight by the sweep** — but the protocol should still carve out files that legitimately cannot carry one, and the sweep is **unmerged**, so the gap is live until it lands.
2. **The `Agent` tool gives COUNT-ONLY observability — even in foreground.** The protocol tells a spinner to cross-check claimed reads against observed ones; what actually returns is final text plus a total. **ACfb solved this better than ACf did: it parsed the child's task transcript directly and counted `tool_use` blocks by type.** That technique belongs *in* the protocol.
3. **Nothing tells a sub-AC what to do when `main` moves mid-trip.** It moved **four times** on 2026-08-04.
4. **A moving base creates a phantom diff that reads as sabotage.** Comparing a branch against a moved `main` made ACfb's branch appear to revert the ARCHITECTURE doc, and made ACf1's appear to revert the tracker. **Bit twice in one day.** Needs a named trap + the fix (compare against the merge-base, or rebase first).
5. **Two rules don't cleanly bind a sub-AC:** who writes the changelog entry (a sub-AC can't ship, so it can't), and whether a conducting sub-AC needs its own branch separate from its builder's.
6. **A live contradiction:** a lane brief saying "execute end to end" versus the standard load-in's "describe-then-pause even against a race-ahead brief." ACfb judged the explicit task won. Protocol should say which.
7. **⭐ NEW, and both ACf and DC got it wrong the same day — the route-vs-fold line.** "An adjacent bug gets ROUTED, not FOLDED" is about not smuggling a **pre-existing, unrelated** problem into a diff reviewed for something else. It does **NOT** mean *defer trivial fixes* and it does **NOT** cover a defect **your own change introduced** — that is part of your change and gets fixed in the same commit. *(ACf proposed a tracker row instead of a 30-second `.env.example` fix; Justin caught it. DC routed five line-refs its own sweep had made stale; ACf caught it.)*
8. **`from=` on a `SendMessage` to a child is the agent TYPE, not an ID** — so a child cannot reply directly even when useful. Auto-return covers it; worth one line so nobody designs around a reply path that doesn't exist.

---

## 7. Repo / push state at set-down

**Justin's instruction tonight: "commit/push all of yours and ACf1's. If other stuff gets swept up in your push, it's okay, just tell me."** — so ACf pushed, which is normally AC0's job. *(Normal standing rule: AC0 pushes.)*

At the time of writing, unpushed: **`evryn-team-runtime` 6** · **`_evryn-meta` 16** · **`evryn-team-workspace` 7`**. The meta and workspace counts include **other agents' commits** (AC0, AC2, AC5, ACa, ACf1) — Justin explicitly accepted that they ride along.

**ACf's commits this session:**
- `evryn-team-workspace` `33b578b` — the ACf→ACf1 appendage (gate/merge division, the FF-impossible correction, the independent verify, 2 diff observations)
- `_evryn-meta` `06cdcfc` — the ACfb lane brief
- `evryn-team-runtime` `dcb6ce1` + `787a196` — the boot-refusal alert (merged from ACfb's lane)
- `evryn-team-runtime` `7553b09` — ARCHITECTURE reconcile + `.env.example` gap
- `dc/code-truncation-canaries` `0d14b0a` — the 4 line-ref fixes (branch unmerged)

---

## 8. Gates + working discipline

- **🔒 Merge / deploy / migration-apply are each SEPARATELY Justin's explicit go.** Prior authorizations never stack.
- **Deploy posture (unchanged):** batch Phase-1 migrations **015 + 016 + whatever (d)/(e) add** into **ONE** migrate-then-deploy window — **migrations applied IN ORDER FIRST** (the L9 boot-guard now enforces this: boot refuses until they're applied) — then run the Lucas paces. **Add: set `EMERGENCY_ALERTS_WEBHOOK_URL` before that window (§4.1).**
- **The paces = the Lucas acceptance test** (`evryn-team-runtime/docs/BUILD-PHASE-1.md`, success criteria 1–8). Criteria 1/2/4/7 passed live 2026-07-14; **3/5/6/8 remain**, plus everything marked UNVERIFIED-LIVE. **Split: ACf1 runs the mechanical criteria; Justin judges whether Lucas still *feels* like Lucas.**
- **Commit discipline:** commit **only your files, by explicit path** — AND **`git diff <file>` FIRST** before committing a shared file. By-path is necessary but NOT sufficient: it takes the file's whole working-tree state, so another agent's uncommitted edits ride along under your message and `git status` won't warn you. Brief / mailbox / handoff traffic is commit-exempt.
- **Multi-agent repos:** `_evryn-meta`, `evryn-team-runtime`, and `evryn-team-workspace` frequently have other agents live. Verify branch + `git status` + diff-first before touching a shared file; leave others' modified files alone.
- **Subagent spins:** ONLY via the orchestration protocol — paste the blocks **VERBATIM**, enumerate **EVERY file with line spans** (never "execute the cascade"), **model-pin `opus`**, prefer **foreground** (background is invisible to Justin).
- **Ping `#team-alerts` on EVERY response, leading with `ACf:`.** `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`, Node `fetch`. Pinger script at `$TEMP/.../scratchpad/ping_acf.mjs` — session-specific; recreate it from the webhook if absent.
- **Never use `AskUserQuestion`** (it hangs). Ask in plain chat + ping.

---

## 9. LESSONS carried forward — don't re-pay these

- **ACf1's push-back is usually RIGHT.** When his read of the live code disagrees with a design doc, **the code wins and the doc gets fixed.** Treat his notes as an objection clock, not a gate — but vet each against the source before ruling.
- **⭐ Verify against the artifact, never the narrative — it paid off repeatedly today.** ACfb reported a fix-trip "in flight" that had already finished. DC's routed note gave a corrected line number that was **itself wrong**. ACf1's "no `acf1/*` branches remain" was true *locally* while two stale **remote** branches survived. Every one of those was caught by looking, not reading.
- **ACf1's own lesson, worth inheriting:** when confirming a branch reap, check **`git branch -r`**, not just local.
- **A recorded transcript must NOT be renumbered.** A *live* cross-reference gets corrected when your change moves it; a comment recording *real past output* must be left alone — editing it fabricates output that was never printed.
- **An under-specified subagent brief = the subagent self-lobotomizes, and it's AC's fault.** Justin: *"subagents love nothing more than lobotomizing themselves."*
- **This runtime punishes local reasoning almost perfectly.** Behavior is half `src/` and half the identity files; defects live in seams several hops from the changed line. **Every new guard test must be watched FAILING against deliberately-broken real code before it is trusted** — it has bitten this lane three times, including inside a test written to guard the previous instance of the same failure.
- **Linear is being retired** — don't route new items there; a line in the tracker is Justin's preferred capture.

---

## 10. Pointers (provenance — where each thing's detail lives)

- **ACf1's lane surface (items (d)/(e) live here):** `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-acf1-phase1-memory-build-brief.md` — read its **2026-08-04 front-door block** first; the tail holds the full ACf↔ACf1 design exchange, including the settled **A2 serialization answer**.
- **The ACfb lane (boot-refusal alert), with ACfb's own set-down + attestations:** `_evryn-meta/docs/working/2026.08.04-acf-acfb-boot-refusal-alert-brief.md`.
- **ACfb's DC build spec for that trip:** `_evryn-meta/docs/working/2026.08.04-acfb-dc-boot-refusal-alert-build-spec.md`.
- **Item (c)'s build spec (provenance):** `_evryn-meta/docs/working/2026.07.23-acf1-dc-item-c-build-spec.md`.
- **Design-of-record (ADR-052):** `evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-team-runtime-memory-architecture.md`.
- **Status source of truth:** `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md`.

## 11. 🔮 A forward signal from Justin worth holding

Justin, 2026-08-04, on spinning ACfb under ACf: *"if all goes well this will be the last time we need to have an external lane."*

**Read that as a direction, not a decision.** The current shape has **ACf1 running as a separate top-level AC Justin spins himself** — an "external lane." If the AC-under-AC pattern keeps proving out (§6), the intent is that lanes like ACf1's become **sub-ACs spun by ACf** instead, so Justin deals with one head per domain rather than several parallel top-level instances. **Nothing has changed yet** — ACf1 is still top-level, still Justin-spun, still owns his own gate and merge (§1). But it explains *why* the pilot findings matter and why he wants the protocol refined: he is deciding whether to collapse the external lanes into this structure. Feed that decision with honest observations, including the ones that argue against it.

## 12. Product-lane context (NOT yours — awareness only)

The product lane (AC0/AC2/AC5/ACa) is shipping toward Mark-live on `evryn-backend` — separate repo, separate prod, separate DB, **zero collision with team-runtime.** As of 2026-08-04 prod is `v0.2.9` with several merged-undeployed items and a Gate-B staging plan in flight. **If someone references product state, that's their lane.** Check `_evryn-meta/docs/current-state.md` for the current picture rather than anything in this file.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
