# Handoff — AC0 → next AC0 — the 4-lane v0.2-hardening WAVE (2026-07-02)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this file:** START HERE if you're a fresh AC0 on the v0.2-hardening wave. You are the **conductor** of a 4-lane parallel build — you orchestrate, you do NOT write code. This is your whole picture: operating model, live board, every lane's state, what's on Justin's plate, and what you owe. **Supersedes** `2026.07.01-ac0-ac0-wave-handoff.md` (that one's operating-model + Rosetta are still valid — this refreshes the board). Companions you should also skim: the 4 lane briefs (`docs/working/2026.06.30-ac0-ac{1,2,3,4}-*.md` — each is the live AC↔AC0 mailbox; **read the tails**), `docs/deploy-log.md` (ship ledger), `docs/current-state.md` (snapshot, I keep it current).
>
> *Authored 2026-07-02 ~morning by the outgoing AC0 (~8% compaction headroom — this is thorough on purpose; next-you can't come back to ask questions).*

---

## ⚡ 2026-07-02 LATE-AFTERNOON — LIVE STATE (fresh AC0: START HERE — authoritative for *current status*)

> Read the **Operating model (§"Operating model — Justin set this")** below — still fully valid. But for *current status*, THIS section supersedes the board + the morning cleanup-addendum (both are now history). Finest-grain live state is always each lane brief's tail.

**Headline: `v0.2.5` SHIPPED + LIVE; AC1 + AC4 lanes CLOSED; env landmine DE-ARMED (AC0-verified).**

- **LIVE in prod = `v0.2.5`** — `evryn-backend` `main` = `0c8c74b` (tag `v0.2.5`), `railway up` SUCCESS + clean boot. Carries: AC4 security **Bundle 2** (constrained `supabase_read` read-lock — per-table scope-filters + `cross_user_notes` column-deny + `find_user_by_name`) + Mira name-lookup beats + **AC3's env-fix (ADR-048)**. Chain: `bca656b`=v0.2.4 (sec Bundle 1) · `83fa367`=v0.2.3 (AC1 cleanup) · `d198ef1`=v0.2.2 (round-2). Prod idle (Mark not forwarding), M1-protected, Healthchecks-monitored. Ledger: `docs/deploy-log.md`.
- **Env landmine DE-ARMED + AC0-verified:** Railway prod has `EVRYN_ENV=production` + `SUPABASE_URL_PROD` + `SUPABASE_SERVICE_KEY_PROD` (I confirmed via `railway variables` — `URL_PROD` == the live prod URL; old unqualified pair KEPT). **Post-`v0.2.5` cleanup owed:** retire the unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` on Railway (v0.2.5 reads the qualified pair — safe to retire).
- **Lanes:**
  - **AC1 (cleanup) — CLOSED.** `v0.2.3` live; pong-warn prod-verify clean (warn gone from live logs). Nothing owed.
  - **AC4 (security) — CLOSED.** Bundle 1 (`v0.2.4`) + Bundle 2 (`v0.2.5`) both live. Reaped `origin/ac4/sec-bundle2` + the Mira namelookup branch.
  - **AC2 (cost) — ACTIVE (Justin driving it live).** Finishing **① Haiku pre-screen** — reconciled onto v0.2.5 `main`, committed `855105f` on `dc/cost-haiku-r2` (LOCAL/UNPUSHED — **AC2's, not yours to push**), ships `off`; deploys **3rd (next `v0.2.N`)**. State = its **Round-9 self-handoff** (AC2 brief tail): dev creds → DEV migration → dev test → QC-verify → merge/deploy on Justin's go. ② clustering HELD; ③ (Step 57 bookkeeping) NOT started.
  - **AC3 (identity/ops) — ACTIVE (Justin driving it live).** env-fix shipped. Next: staging standup (Step 80, gated on Justin's account actions), consolidated watch-list (47), determination-sharing (55), 54b design.
  - **AC6 (ARCH/BUILD doc-reconciliation) — BACKBURNER (slow, Justin's back pocket).** Own branch `ac6/arch-build-reconciliation`. **AC0-owed (standing):** keep its branch rebased as lanes ship + **adjudicate its sprint-candidate list**. `main` advanced (`0c8c74b`, then `03e27e7` sprint) → AC6 owes a rebase.
- **⭐ Integration-test cadence — NEW, now SPRINT Step 88 (AC0 call, Justin delegated + approved "two gates").** Nothing's had a full-integration pass since Phase 6 (6/11) — everything since shipped on per-lane QC + boot-smoke only. **(A)** pre-③ end-to-end baseline on DEV (before AC2's Step-57 hot-path rewrite merges); **(B)** mandatory pre-Mark full create-from-zero live-fire (everything active; **staging/Step-82** or prod-create-from-zero) that doubles as ③'s checkpoint. **Relayed to AC2 (Gate A in front of ③) + AC3 (its Step 43/80/82 = the gate's machinery).** AC0 orchestrates the runs; AC builds fixtures; Justin picks the fictional character (Rachel Blackwood vs Megan Griffiths).
- **Subagent-resume test (unnumbered AC):** at **28h the resume still works** (Justin, 2026-07-02); the instance sits until **Monday's check**, then the test is complete. Updates the 7/01 "TTL open beyond within-session" line — persist the final result Monday (orchestration protocol + current-state).
- **This session (what this AC0 did):** loaded full cascade + entire `src/` runtime; **verified AC3's landmine de-arm against Railway**; relayed go-forward to the AC4/AC2/AC1 briefs; on the `v0.2.5` ship + AC1 closure ran the **persistence pass** — pushed all stranded lane commits (`_evryn-meta` + `evryn-quality`), reaped `origin/ac4/sec-bundle2` + the Mira branch, reconciled `current-state.md` (+ live-deploy bump), closed the pong-verify in CHANGELOG, updated the **team current-state** (stale since 6/29), marked sprint Steps 86/86b/35a/87 shipped, minted **Step 88**, relayed the cadence to AC2/AC3.
- **On Justin's plate:** finish/deploy AC2's ① (his DEV-migration go already given per AC2's handoff → QC-verify → merge/deploy on his go); AC3 staging account setup (Step 80); pick the fictional character for the integration gates. (Mira NB-1/NB-2 already shipped with `v0.2.5`.)
- **Fresh-AC0 first moves:** (1) skim AC2's + AC3's brief tails (Justin's driving both live). (2) Be ready to **gate AC2's ① deploy** when it's QC-GO — it's next in the queue. (3) When AC2 reaches **③**, coordinate **Gate A** before it merges. (4) Keep **AC6's** branch rebased + adjudicate its candidate list. (5) The full formal **#lock lands at the cost-lane end** (Justin's call — this session was the `v0.2.5` checkpoint). (6) Finalize the **subagent-resume test Monday**.

---

## 🧹 2026-07-02 CLEANUP-SESSION ADDENDUM (AC0) — read this first

A fresh AC0 + Justin ran a **cleanup session** on 2026-07-02: we did **not** advance the main show — we cut everyone's re-boot load. (The board below is still the right *shape*, but it's the morning snapshot; a couple lane lines have moved since — e.g. AC4 has re-rebased Bundle 2 onto the env-fix and is holding for Justin's merge/deploy go, per its brief — so **trust each lane brief's tail for live state**, the board's own rule.) What changed:

- **AC6 spun off + LAUNCHED — a live track as of 2026-07-02 (dedicated ARCH/BUILD reconciliation).** `ARCHITECTURE.md`/`BUILD-EVRYN-MVP.md` are stale with un-woven info-dumps — which is *why* every AC reads the whole runtime on load (the docs can't be trusted). AC6's only job: make ARCH + BUILD faithful to the runtime so future ACs load lighter. Brief: `docs/working/2026.07.02-ac0-ac6-arch-build-reconciliation-brief.md`. **AC6 is a SEPARATE TRACK on its own worktree/branch (`ac6/arch-build-reconciliation`) off `main`** — the lanes keep merging code to `main` in parallel; the docs merge back when AC6 is done. AC6 **pitches** the brief to Justin at kickoff (Justin didn't pre-review it — he settles the approach with AC6 directly). **AC0-OWED (standing — yours + future AC0s'): keep AC6's branch rebased against `main` as lanes ship, so it doesn't drift.** AC6 surfaces a "looks-built-but-marked-open" sprint-candidate list → **AC0 adjudicates** (built / in-progress / false-match-on-a-vague-step) and marks the sprint. **Cross-lane dedup you'll need to coordinate:** reading the lane briefs sharpened the AC6 brief — it now names the **three in-flight branch workstreams** (AC4 Bundle-2 read-lock, AC2 Haiku pre-screen, AC2 Step-57 bookkeeping — each carries its own ARCH/BUILD delta when it ships) and flags that **AC6 SUBSUMES AC3's owed ARCH "Separate Supabase Projects" env-scheme doc-sync** (ADR-048/Step-87 made it stale) → **coordinate so AC3 drops it** (AC6-brief staleness item 8), so they don't both edit the ARCH env section.

- **Changelog archived + trailing-month rule.** May (and earlier) → `docs/historical/changelog-historical/changelog-2026-05.md`; CHANGELOG.md dropped **814→557 lines** (June+July kept inline). New rule (in CHANGELOG.md header + CLAUDE.md): **archive the *previous* month at each month-end** — always keep the whole prior month inline so a fresh AC on the 1st isn't blind to the 30th. **Changelog-as-we-go is the top AC0 housekeeping priority** (Justin) — it's the durable record every AC loads. **The morning handoff's "changelog catch-up owed" is CLEARED:** `v0.2.4` was already logged, the env-fix merge is logged by AC3 (`cd7729f`), and this cleanup session is now logged (the 2026-07-02 AC0 entry).

- **Sprint doc — made status-accurate for AC6 (`evryn-backend/docs/SPRINT-V0.2-HARDENING.md`, commit `99538b1`).** Fixed 2 status errors AC6 would have hit: **Step 87** (env-fix) `TODO`→`DONE` (it's merged, `e04af87`); **Step 44** (Haiku) `TODO`→`IN PROGRESS` (built + parked); + Step 35 (35a built in Bundle 2) + a banner note that the env-fix rides `v0.2.5`. **AC0-OWED (housekeeping, DEFERRED — Justin: don't burn context on it now, do it with headroom):** the ~30 already-`DONE` round-2 Steps carry stale "deploy-pending"/branch-ref parentheticals (they shipped as `v0.2.2`; the sprint banner itself flags this as an owed "DONE-status reformat/compaction pass"). The `DONE` **prefixes are correct**, so it does NOT mislead a status read — it's purely cosmetic (strip the stale lane/branch/deploy-pending language from those entries' parentheticals). **Heads-up:** this sprint commit advanced `evryn-backend` `main` (`99538b1`, sprint-doc-only) → a trivial rebase for AC6's branch (so it reads the accurate sprint) and for the eventual `v0.2.5` merge; no code touched.

- **Lean-brief discipline (the re-boot-load fix).** Since the changelog is the durable history everyone loads, briefs go **current going forward** — history *relocates* to the changelog, and a settled section collapses to a one-line pointer ("XYZ shipped → see CHANGELOG 2026-0X-YY") that **re-hydrates** from the changelog the reader already loads. **Two triggers/actors:** (a) at a deploy/key point, **AC0** gangs the changelog entry with a brief-cleanup; (b) at handoff-write, the **outgoing lane instance** consolidates its *own* settled history + **flags AC0 to vet** — so new-lane-AC boots on a lean, AC0-vetted brief. Already added to **AC3's** brief (re-boots soonest). **AC0-OWED: add the block below to each active lane brief (AC1/AC2/AC4) on its next re-boot** (I didn't touch them mid-flight to avoid clobbering live work), and **vet each lane's consolidation when you re-spin it** ("re-spinning AC4 — check his consolidations, make any changes you need"). Full rule: CLAUDE.md "Keeping working-briefs lean."

  **Standard lane-brief consolidation-directive block (paste into each lane brief's how-to-use on re-boot):**
  > **🔻 Before you re-boot — consolidate this brief:** history everyone needs lives in `_evryn-meta/CHANGELOG.md` (every AC loads it). As your LAST act before writing your self-handoff, collapse *settled/shipped* sections here into a one-line pointer to their changelog entry ("XYZ shipped → see CHANGELOG 2026-0X-YY"), keeping only **live state + open items**. Never collapse a section until its changelog entry exists (memorialize first). Then **flag AC0 what you consolidated** — AC0 vets it before your next instance boots. Cross-lane doubt → leave it, flag AC0.

- **CLAUDE.md edits — DONE + committed (`4cc4371`, Justin approved):** deploy-log pointer in the routing table; the trailing-month changelog rule + changelog-as-priority note; the lean-brief practice note (framed as a good practice, not a mandate).

- **Lane briefs — read, not trimmed:** I read all 4 in full (per Justin, to sharpen the AC6 brief) but did NOT heavy-trim them — the go-forward model (each lane self-consolidates at handoff + AC0 vets) handles that sustainably. If a future AC0 wants a one-time deep trim of the existing briefs, do it fresh.

---

## ⚡ What you are doing (one paragraph)

You conduct a **4-lane parallel v0.2-hardening wave** on `evryn-backend`, built on top of the round-2 hardening already live. Justin runs each lane as its own AC instance (AC1/AC2/AC3/AC4). **You do NOT write code** — each lane's DC/QC builds; you read each lane's report (appended to its brief), reconcile/decide the cross-lane collisions + sequencing, reply (append to the brief + commit immediately), gate merges/deploys, keep Justin at CEO altitude, and keep the housekeeping (sprint marks, current-state, **CHANGELOG**, deploy-log) current. Deploys are **staged, one lane at a time** (each a numbered ship `v0.2.N`), NOT a convergence. The endpoint: finish this hardening batch so Mark can onboard (a full live-test precedes his first real forward).

## 🛠 Operating model — Justin set this; follow it EXACTLY

1. **Each lane's brief IS the AC↔AC0 mailbox** (`docs/working/`). Read the tail, append your reply as a `## ═══ AC0 → ACn · date · summary ═══` banner **before the truncation canary**, and **COMMIT + PUSH immediately** — mailbox/brief commits are **pre-authorized**. **Stage by explicit path** (never `git add -A` — the repos have other agents' in-flight files; e.g. Justin edits `CLAUDE.md`, AC3 drops ADRs, AC4 pushes build-specs).
2. **Justin reads ONLY the chat, never the briefs.** Every reply to a lane → a **CEO-altitude plain-English chat summary.** NEVER a bare step number ("Step 86b") — describe the thing ("the security bundle's cross-user read-lock"). He comes in cold from ~8 parallel agents; re-anchor lightly each time.
3. **PING every final response** on `#team-alerts` (he stressed this most, repeatedly — even when he seems present). Mechanism (the CLAUDE.md method — Node `fetch`; **make your OWN helper in YOUR session scratchpad**, mine won't be at your path): read `SLACK_TEAM_WEBHOOK_URL` from `C:/Users/Justin/Evryn/Code/evryn-team-workspace/.env`, POST `{ text }` to it. **Prefix EVERY message with `AC0:`** — Justin runs ~8 agents in parallel, so an un-tagged ping is noise he can't attribute (he caught this 2026-07-02); **auto-prepend it in your helper so it can't be forgotten.** **Gotcha (cost me a landed-looking-but-lost ping): read the env LINE directly (`.split(/\r?\n/).find(l => l.startsWith("SLACK_TEAM_WEBHOOK_URL="))`) — an earlier regex parse silently mangled the URL so it returned HTTP 200 but never posted; print the Slack response body (`ok`) to CONFIRM it landed.** Keep the ping a one-line attention-tap; substance goes in chat.
4. **Commit/deploy gates are Justin's — per-action, they don't stack.** Lanes build/edit freely but pause for his explicit go before commit + before `railway up`. A workflow verb ("handle it") is not the go. **EXCEPTION (this session's standing authorization): housekeeping-doc commits** — sprint DONE-marks, `current-state.md`, `deploy-log.md`, **`CHANGELOG.md`** — you commit freely under Justin's "update as we go." Code/deploy stays per-action gated.
5. **You're a PEER to the lanes on their architecture, not their boss.** They hold deeper context on their exact problem; you hold cross-lane collisions + sequencing + the runtime-level verification. Answer collision/sequencing Qs; let them own design. `AskUserQuestion` is BANNED (hangs) — ask in chat + ping.
6. **⭐ CHANGELOG-as-we-go is the TOP housekeeping priority (Justin, 2026-07-02): "even more valuable than current-state."** Keep `_evryn-meta/CHANGELOG.md` current as things ship/change. **The catch-up is ✓ CLEARED (2026-07-02 — see the addendum):** `v0.2.4` was already logged, the env-fix merge is logged (AC3 `cd7729f`), and the cleanup session is logged. **The ongoing rule stands: add a CHANGELOG entry at EVERY future ship.**
7. **Coordination lesson (Justin's catch, 2026-07-02 — internalize this):** when you move/split a Step OFF a lane's plate, **relay it DIRECTLY to that lane's mailbox**, not just via a sprint annotation. Active instances run off their *last full read*, so a sprint edit is invisible to them until they re-read. (This came up splitting Step 35(a) to AC4 — the direct AC3-brief note is what prevents a double-build.)
8. **Verify before claiming (you have the full runtime loaded).** Cross-lane security/behavior claims get checked against `evryn-backend/src/` — it's been genuinely load-bearing this session (verifying the `cross_user_notes` firewall-bypass, the `original_from_user_id` triage read, that runtime broad-reads like `getRecentMetaMessages` are direct-DB helpers not the `supabase_read` tool). If Justin says "load the full cascade," load ALL of `src/` in full — **enumerate it LIVE (`find src -name '*.ts'`): it's ~32 files now (~11K lines) and grew from ~8, so never trust a hard-coded count** (per AC3's QC-manual un-staleable fix) — non-negotiable.

## 📊 THE BOARD — live state

**Deploy pipeline:** staged `v0.2.N`, one per prod `railway up`, ledger = `docs/deploy-log.md`; git-tags on `evryn-backend`. **You gate + reconcile deploys; the fresh LANE instance EXECUTES each ship (rebase → merge → `railway up`) — you do NOT run `railway up` yourself.** **Rosetta:** "round-1" = discarded 6/17 lobotomy (never shipped); "round-2" = `v0.2.2`; "go-live" = `v0.2.1`. Discipline: a Step's STATUS + the ledger = truth; a "rides wave-X" tag = intent, not proof.

- **LIVE in prod = `v0.2.4`** (`bca656b`, AC4 security Bundle 1 + Mira 41a + AC1's pong fix). Prod is idle (Mark not forwarding) + M1-protected + Healthchecks-monitored.
- **`main` = `99538b1`** (was `e04af87` at the morning snapshot; the 2026-07-02 sprint status-accuracy pass advanced it — sprint-doc-only). It's `v0.2.4` + all sprint-doc edits + **AC3's env-fix (Step 87 / ADR-048) MERGED** (not deployed). So `main` is AHEAD of what's deployed by the sprint/docs + the env-fix.
- **⚠ Deploy landmine ARMED:** the env-fix makes the runtime `required("EVRYN_ENV")` + read qualified `SUPABASE_URL_PROD`/`SUPABASE_SERVICE_KEY_PROD`. So the **next `railway up` (that's `v0.2.5`) boot-fails** unless Railway has `EVRYN_ENV=production` + those two qualified vars set first (fail-safe by design). **Justin + AC3 are handling the Railway env swap** (told to me 2026-07-02) — **do NOT chase it; it's theirs.** The swap is inert to the running v0.2.4 code, so it can be set anytime before the deploy.
- **Deploy order:** env-fix ✓merged → **`v0.2.5` (AC4 Bundle 2)** → **AC2 ①**. (The env-fix is **merged-not-deployed** — it *rides* the `v0.2.5` `railway up`, it does NOT get its own deploy number. So `v0.2.5` carries BOTH AC4 Bundle 2 AND the env-fix → the env swap is a precondition for that one ship.)

### AC1 (cleanup) — DONE, `v0.2.3` LIVE
Deployed + healthy. All its sprint steps marked DONE (I reconciled them: 23/36/39/42/54.d.ii-iii/72/75/76abc/77/83). **One owed:** the Step-23 pong-warn fix rode `v0.2.4` — its **prod-log verify is still owed** (test-green ≠ prod-verified). Re-spin a deploy-AC1 to `railway logs` and confirm the `"pong wasn't received"` warn is gone from live logs now that `v0.2.4` is up.

### AC4 (security bulkheads) — `v0.2.4` LIVE; `v0.2.5` = BUILT + QC-GO, RE-REBASED, holding for Justin's merge/deploy go
> **⚠ STATUS (2026-07-02, per AC4's brief tail — supersedes the "needs re-rebase" detail below):** the re-rebase onto the env-fix is **DONE** (+ test-seed + re-verify green); AC4 is **holding for Justin's merge-go → deploy-go**. "THE CATCH" below is the now-resolved *why*. NOTE: `main` has since advanced to `99538b1` (sprint-doc-only), so a trivial re-rebase onto it precedes the merge.
- Bundle 1 = `v0.2.4`, LIVE, done.
- **Bundle 2 (`v0.2.5`) = BUILT + adversarial-QC-GO @ `8e5a084`** on `ac4/sec-bundle2` (pushed). **EXPANDED per Justin's ruling 1b** into three read-side inner-doors (Rules 7/8/9):
  - **per-table scope-filters** with corrected key-sets — `emailmgr_items`: `original_from_user_id`·`user_id`·`id`·`external_id`; `messages`: `scope_user_id`·`sender_id`·`recipient_id`·`thread_id`·`id`; `users`: `id`/`email`. (The `original_from_user_id` inclusion is load-bearing — it's the ADR-036 prior-triage read Evryn does via the generic tool; omitting it would deny every repeat-sender triage. That was AC4's precondition catch on my earlier 4-key set.)
  - **`cross_user_notes` column-deny** on the generic `users` read (closes a real firewall-bypass — `select:*` was returning the architecturally-firewalled third-party-feedback column; `story`/`profile_jsonb` deliberately NOT denied — Evryn needs the story to broker).
  - **`find_user_by_name`** thin-fields tool (Step-35(a) pulled forward) so name-lookup never touches the generic read.
- **🔴 THE CATCH (why it's not merge-ready as-is):** AC4 rebased + QC'd `8e5a084` onto the OLD `main` (`3a2fb43`) *before* the env-fix merged. The env-fix now sits UNDER its QC'd branch. So AC4 **owes a re-rebase onto `e04af87`** — code is clean (env-fix files disjoint from AC4's), BUT the env-fix changed the **test-bootstrap contract**: every config-importing test now needs an `EVRYN_ENV=dev` seed via `tests/env-bootstrap.ts`, so AC4's 2 new tests (`test-supabase-read-guard.ts`, `test-find-user-by-name.ts`) need it added, then a **light re-verify** (tsc + tests green — NOT a full re-QC; guards unchanged). I told AC4 this in its brief tail.
- **Merge sequence:** re-rebase → add the test-seed → tsc+tests green → **Justin's merge-go** → merge → **Justin's deploy-go + confirm the Railway env swap** → `railway up` = `v0.2.5`.
- **The 2 Mira NBs (NB-1/NB-2 — identity-beat name-lookup repoints in `feedback-guidance.md`/`operator.md`) SHIP WITH `v0.2.5`** — Justin decided this (2026-07-02); route them to Mira.
- **At the `v0.2.5` deploy — AC0-owed (yours):** current-state reconcile · sprint DONE-marks (Step 86b + Step 35a) · reap `origin/ac4/sec-bundle2` (remote — mirror how I reaped `origin/ac4/sec-bundle1`) · deploy-log row · **CHANGELOG entry**. **AC4-owed:** ADR-047 full-1b-expansion update (it has the build-spec §2b context) + CHANGELOG/deploy-log at ship. **Plus:** AC4 surfaced a QC-pattern-to-promote for `evryn-quality/CLAUDE.md` (needs Justin's approval — source-of-truth edit). Full detail: AC4 brief tail (`2026.06.30-ac0-ac4-security-bulkheads-brief.md`).

### AC2 (cost) — ① built + parked; NOW unblocked; deploys 3rd
- **① Haiku pre-screen (Step 44) = BUILT + AC/QC-GO (build pass) + pure-tested 51/51** on `dc/cost-haiku-prescreen` @ `89e8cff`, ships **`off`** (`HAIKU_SCREEN_MODE` unset). 3 observability adds in (dashboard readout / fresh-false-pass `notifyDev` / Haiku's own `llm_usage` tag).
- **UNBLOCKED now** (env-fix on main → its DEV test can run safely). Forward step (once Justin oks the DEV-migration apply): rebase ① onto `e04af87`, apply `backups/add-haiku-screen-shadow-table.sql` to **DEV**, run `tests/test-haiku-screen-dev.ts`. **⚠ Its rebase is SUBSTANTIAL** — base is `1163800` (pre-`v0.2.3`/`v0.2.4`), so real reconcile at `process.ts` (its `runForwardTriage` hook) + `config.ts` (its `HAIKU_SCREEN_MODE` add vs the env-fix restructure) + the test-bootstrap seed.
- **Final convergence (merge + deploy) waits for AC4's `v0.2.5`** — you flag AC2 the moment `v0.2.5` deploys; then a light re-rebase (AC4's read-guard files are disjoint from ①) + the **deferred post-rebase QC-verify (do NOT skip)** → deploy.
- **② (clustering, Step 58) HELD** (Justin: "lock ① first"). **③ (bookkeeping, Step 57)** design co-designs with AC4's now-settled read-lock end-state; **the broader deterministic-`triage_result`-for-gold/edge fix folds INTO ③** (in-lane, don't spin it off — I confirmed). At ③: write an ADR-018 amendment alongside the ADR-033 one; flag Soren for awareness. AC2's open decision for Justin: DEV-test-forward now vs. hold. Full state: AC2 brief tail (Round-6 handoff + my pick-back-up note).

### AC3 (identity/ops) — env-fix DONE; broad lane, Justin-sequenced
- **env-fix (Step 87 / ADR-048) MERGED ✓** (its front-of-queue task — done; branch reaped). **Post-merge doc-syncs (both still document the OLD unqualified `SUPABASE_URL`/`SUPABASE_SERVICE_KEY` scheme):** the `ARCHITECTURE.md` "Separate Supabase Projects" sync is now **AC6's (subsumed — see addendum; coordinate so AC3 drops it, no double-build)**; **AC3 keeps `operator-guide.md:40`** (AC-maintained).
- **Next up (its endorsed priority):** staging standup (Step 80 — the account-actions checklist is fired to Justin; gated on his slow account work: 4th Supabase project + 2nd Railway service + staging Slack app + test inbox) · consolidated watch-list (Step 47) · near-term Mira determination-sharing (Step 55). **Step 53** (reliable identity-module loading) → its OWN dedicated agent later (off AC3's plate, rides Step 50). **54b** (forwards→permanent-`messages`) needs Justin's design call + a 1-page design AC3 owes.
- Full state: AC3 brief tail (its self-handoff to next-AC3).

## ✅ On Justin's plate (surface these; don't chase what's his)
1. **Railway env swap** (the landmine) — **Justin + AC3 own it.** Don't chase.
2. **`v0.2.5`:** after AC4's re-rebase, Justin's merge-go → deploy-go (+ confirm the env swap). Mira NBs ship with it (decided).
3. **AC2:** Justin's go on the DEV-migration apply (unblocks AC2's DEV-test-forward).
4. **AC3 staging account-actions** (Step 80 — his slow clock).

## 📌 AC0-owed (housekeeping — keep current AS YOU GO)

> **⚠ The 2026-07-02 addendum (top) carries the NEWER owed items — read them there:** keep AC6's branch rebased + adjudicate its sprint-candidate list · add the consolidation-directive block to AC1/AC2/AC4 on their next re-boot + vet each lane's consolidation on re-spin · the ~30-entry round-2 sprint **cosmetic** reformat (deferred, low-priority) · **`current-state.md` owes a cleanup-session / AC6-launched line** (I updated the CHANGELOG + this handoff, not current-state). The morning-AC0 items below still stand.

- **⭐ CHANGELOG — going forward** (Justin's top priority). **The catch-up (`v0.2.4` + env-fix) is ✓ DONE** (see addendum). Ongoing: add an entry at EVERY ship.
- **The pong-warn prod-verify** (post-`v0.2.4`) — re-spin a deploy-AC1 to confirm the warn's gone from live logs.
- **At `v0.2.5` ship:** current-state reconcile + sprint DONE-marks (86b + 35a) + reap `origin/ac4/sec-bundle2` + deploy-log row + CHANGELOG entry.
- **From the 07.01 wave handoff, still owed at #lock:** propagate the `v0.2.N` versioning convention (ledger=truth, tag=intent) into AC `CLAUDE.md` + the **team** current-state (the Rosetta — highest value, it's where "round-2" refs live for the crew).
- **Sprint bookkeeping I did this session** (so you know the state): reconciled AC1 + AC4-Bundle-1 DONE-marks; minted **Step 87** (env-fix); expanded **Step 86b** to the full Bundle-2 elevated scope; split **Step 35(a)** → AC4 (with the direct AC3-brief relay).

## 🧭 Key docs (don't re-derive)
- **The 4 lane briefs** (`docs/working/2026.06.30-ac0-ac{1,2,3,4}-*.md`) — the AC↔AC0 mailboxes; **read the tails** for live state.
- **`docs/deploy-log.md`** — ship ledger + `v0.2.N` convention + Rosetta.
- **`docs/current-state.md`** — snapshot (latest stamp = 2026-07-02 morning; **owes a cleanup-session / AC6-launched line** — see AC0-owed).
- **`_evryn-meta/CHANGELOG.md`** — keep current (Justin's priority).
- **`evryn-backend/docs/SPRINT-V0.2-HARDENING.md`** — the Step list.
- **`docs/working/2026.07.01-ac0-ac0-wave-handoff.md`** — the prior handoff (operating model + Rosetta still valid).
- **`_evryn-meta/CLAUDE.md`** — operating manual; **`docs/protocols/ac-orchestration-protocol.md`** — DC/QC subagent spins.
- **Subagent RESUME works** (current-state 2026-07-01 line): a deferred `SendMessage` resumes a completed subagent by its `agentId` with full context — useful for a fix-trip *continuity* (a lane keeps its DC/QC's build context), NOT where independence is the point. Caveat: TTL beyond within-session unproven.

## 🔧 Repo / commit state (as of handoff)
- All active repos on `main`, synced with origin, clean — EXCEPT other agents' in-flight (leave them): last night `evryn-quality` had 1 unpushed QC-manual commit + `evryn-team-workspace` had 2 uncommitted (Mira memory + team current-state); Justin edits `_evryn-meta/CLAUDE.md` live. **Stage by path always.**
- **`evryn-backend` `main` = `99538b1`** (`v0.2.4` deployed + all sprint/docs + env-fix merged, undeployed; the morning snapshot said `e04af87` — the sprint status-accuracy pass advanced it, sprint-doc-only). Origin lane branches (**SHAs are volatile — trust the brief tails**): **`ac4/sec-bundle2`** (Bundle 2 QC-GO, re-rebased per AC4's brief — needs remote-reap after merge), **`dc/cost-haiku-prescreen`** (`89e8cff`, AC2 ①). AC1's + AC3's branches are reaped.
- **`_evryn-meta` `main`** — tip advances as you commit briefs/housekeeping; I've been pushing as I go (AC0 pushes `_evryn-meta` — the standing norm; lanes push their own `evryn-backend` deploy commits).
- **You stay on `main` in the two canonical trees** — no worktrees for AC0. Do NOT read the lane worktrees; the briefs report their state.

## First moves for the new AC0
1. Load per Justin's load-in note (light cascade + this handoff + the 4 brief tails; stay on `main`; full runtime if he calls the full cascade — it's been load-bearing for the cross-lane security brokering).
2. Ping Justin "I'm up, here's the board" + tell him the next move.
3. **Highest-likelihood next actions:** gate **AC4's `v0.2.5`** (a trivial re-rebase onto current `main` `99538b1` → Justin's merge-go → deploy-go + the env swap — all Justin-gated); flag **AC2** the moment `v0.2.5` deploys; the **pong-warn prod-verify**; keep **AC6's** branch rebased + adjudicate any sprint-candidates it surfaces. *(The CHANGELOG catch-up is ✓ done — just keep logging every ship.)*

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
