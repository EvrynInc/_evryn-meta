# ACf → ACf Handoff — team-runtime lane. FRESH standalone, 2026-08-10 evening.

> **Truncation check:** the last line should read `FULL FILE LOADED`.
>
> **You are a fresh ACf.** This **supersedes `2026.08.06-acf-acf-handoff.md` entirely** — that file's live-state sections are now mostly discharged and several of its statements are actively false (it says both lanes are unspun; one is running). **Read this file top to bottom; it is complete and standalone. Nothing above it needs reading.** The durable content from the 08-06 handoff — how to talk to Justin, the gates, the lessons — has been carried forward here in full rather than pointed at, so you never have to open the retired one.
>
> **Written deliberately, not under compaction pressure.** ACf-11 had ample context remaining; Justin asked for a proper set-down before closing for the night.

---

## §0 — ⚡ WHERE YOU ACTUALLY ARE. READ THIS BEFORE ANYTHING ELSE.

**One lane is RUNNING. Everything else is quiet. Nothing is broken and nothing is half-written.**

### The single most important thing: `ACfw` is live and its report may be unreachable

**`ACfw`** — the lane fixing the HTTP-414 wake outage (SPRINT **Step 30**) — was spun 2026-08-10 evening and was **mid-build at set-down**: its DC's work trip running, its QC not yet spun.

🔴 **Subagents do NOT survive a session boundary.** If ACf-11's session ended (crash, re-spin, overnight), **`ACfw`'s returned report is permanently unreachable** — `agentId` resume works indefinitely *within* a session and never across one. **Do not try to resume it. Do not wait for it.**

**📌 PROGRESS AS OF ~20:30 PT: its DC has BUILT AND COMMITTED the fix.** Branch `acfw/in-url-wall` tip **`f21cc00`**, working tree clean, `npx tsc --noEmit` exit 0, **suite 381/381 passing** (354 baseline + 27 new). Eight mutations each flipped exactly the intended test red. **What remained: `ACfw`'s QC review, and its report to ACf.** ⚠️ **The DC's own two flags, which ACf has NOT yet resolved and which gate the merge — see §3.**

**Its work is NOT lost, because the lane was run on the durable-surface discipline.** Everything it established is in three places you can read right now:

1. **`_evryn-meta/docs/working/2026.08.06-acf-acfw-wake-outage-brief.md` §9** — its committed set-down (commits `a1455bd`, `c1e61f9`). **This is the authoritative record of the lane** and it is genuinely complete: corrections to the brief, the design it decided and why, the environment it provisioned, its DC's verified receipts, and what remained.
2. **The worktree `Code/evryn-team-runtime-acfw`** (branch `acfw/in-url-wall`, base `9c1e81b`) — at set-down it held uncommitted work: modified `src/db/paginate.ts`, `src/digest/index.ts`, `tests/ceiling-db.ts`, plus new `src/safety/rate-gate.ts` and `tests/url-414-call-sites.test.ts`.
3. **The sandbox `Code/evryn-team-runtime-acfw-sandbox`** (detached at `9c1e81b`) — a disposable mutation-testing copy ACfw provisioned itself.

**⇒ YOUR FIRST ACTION: go look at that worktree's git state, then read §9 of the brief.** Do not reason from this handoff about where the lane got to — **check the artifacts.** *(This is the standing rule that has repeatedly paid: a set-down was true when written; the worktree is true now.)*

**If the lane is unfinished, you re-spin `ACfw` fresh against its brief** — which is cheap precisely because §9 is complete. Brief it as a **fresh instance** (it has no memory of the prior run), point it at §9 as its inheritance, and tell it to verify the worktree state itself before continuing.

**Worktree lifecycle is YOURS.** If you abandon the lane rather than resuming it, reap both trees (`git -C evryn-team-runtime worktree remove <path>`) and delete the branch. Do not leave them for Justin to find.

### Everything else, stated plainly

- **🔴 `ACfm` IS ALSO RUNNING — as of 2026-08-10 ~20:30 PT, BOTH lanes are live concurrently.** (SPRINT Steps 4 + 5 — membership verbs, then the dormancy memory-write; **the critical path**.) **Justin authorized concurrency tonight**, reversing his own one-lane ruling from earlier the same day, after AC0's parallel experiment showed subagents do not overburden their parent. Its brief is current and carries the overlap analysis.
  - **The two lanes OVERLAP in `src/digest/index.ts` and `src/composer/index.ts`.** They are in separate worktrees on separate branches, so neither can corrupt the other — **the cost is a rebase at merge time, and it is YOURS to sequence.**
  - **⇒ ACf's standing decision, already written into `ACfm`'s brief: `ACfw` merges FIRST** (it started earlier and is further along), **`ACfm` rebases onto the moved `main` and re-runs its gates.** Use `git diff main...branch` — **three dots**; two dots renders `ACfw`'s gains as deletions by `ACfm`'s branch, which has bitten this repo twice.
  - **Same session-boundary rule applies:** if this session ended, `ACfm`'s report is unreachable too. Its durable surface is its brief (`evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-acf1-phase1-memory-build-brief.md`) plus whatever worktree it cut. **Check artifacts, don't wait.**
- **No other lane of yours is running.** No other ACf subagents exist.
- **`evryn-team-runtime` `main` = `e200dd8`** at set-down — **confirm it yourself; never trust a SHA in a doc.** *(This handoff practices what the tracker's new rule 9 preaches: the number is here only as a "was," and the command is the answer.)*
- **NOTHING IS DEPLOYED. Migrations 015 + 016 remain UNAPPLIED.** Never describe any of this as live. *(And note: "unapplied" is inherited from the build trips, which never apply migrations — it is **true but unproven from the repo**. If it ever becomes load-bearing, go look at the database.)*
- **`_evryn-meta` is a HOT repo.** AC0 is running two product lanes (`ACa` atlas, `ACn` test-safety-net) with **seven `evryn-backend` worktrees** live. **Those are not yours** — do not reap them, do not commit near them, and never `git add -A` anywhere. Always commit with a **pathspec on the commit itself**.

---

### ⚠️ UNCOMMITTED WORK IN THE TREE — recover it, don't recreate it

**`evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` holds three NEW Steps (40, 41, 42) that are written but NOT committed** — held in SCM for Justin's vet per the standing source-of-truth rule, while he was asleep. **They are purely additive** (routed findings from `ACfw`'s QC; no status flips, no existing text changed). **If you find them there, they are mine and they are finished — commit them once Justin vets, don't rewrite them.** Content: Step 40 (a wake can still die silently — the compose-failure guard starts one read too late), Step 41 (**the fix removes an accidental limiter**, so two unbounded fan-outs become reachable — weigh before the Meta-Meeting), Step 42 (four unbounded model-supplied tool args).

**Two doc updates are deliberately NOT written yet, and the reason is a discipline worth keeping:** ARCHITECTURE's cardinal invariant 8 still calls the URL wall an open routed item, and SPRINT Step 30 still reads 🔄 IN PROGRESS. **Both become true only when `ACfw` MERGES.** Writing them earlier would put a claim in a source-of-truth doc that `main` does not support — the exact failure this lane spent 2026-08-10 correcting on the model pin. **Land them WITH the merge, in the same pass as the CHANGELOG entry.**

## §1 — Who you are, in one paragraph

**ACf, coordinator of the team-runtime lane.** `evryn-team-runtime` is the autonomous harness for Evryn's 8 founding-team agents (ADR-050). It is **dormant by construction** — deployed to Railway, wakes nobody, until the Meta-Meeting. **Zero overlap with the Evryn product** (separate repo, prod, DB), so nothing here can touch Mark's path. The current build is its **memory + wake-economics subsystem** (ADR-052).

**⚠️ COORDINATION IS THE LANE — internalize this.** Justin, 2026-08-05: *"You're the coordinator — I want you to be able to keep your head at altitude and see the broad perspective. **That** is your lane."* The moment you think of a lane AC's work as *yours*, you start issuing instructions instead of delegating judgment, **and the whole value of the layer evaporates** — the value is that they are closer to the code than you are. **Name every lane AC.** Used so far: `ACfb` `ACfr` `ACfs` `ACfa` `ACfc` (all closed/dead) · `ACfw` (live — §0) · `ACfm` (next, unspun) · `ACf2` (held). `ACf1` is **retired** — that lane is now `ACfm`.

---

## §2 — What ACf-11 did this session (2026-08-10 evening)

**Five commits, all pathspec-scoped, none sweeping another agent's work:**

| Repo | SHA | What |
|---|---|---|
| `evryn-team-runtime` | `9c1e81b` | ARCHITECTURE invariant 5 corrected + the never-switch-blind model rule; SPRINT Step 24 reframed; tracker rule 9 added |
| `_evryn-meta` | `ad3281b` | `CLAUDE.md` — the build-level-code commit standing, finally in the always-loaded manual |
| `_evryn-meta` | `e959bbe` | `ACfw` brief — corrected site table, two new findings, the blindness banner |
| `evryn-team-workspace` | `7ad8ee4` | `ACfm` brief — killed the phantom sibling lane, the hardcoded SHA, and a dead ledger instruction |
| `evryn-team-runtime` | `e200dd8` | Deleted the tracker's `MERGE STATE` block; rule 9 records the reversal |

**Load method: the Conductor Cascade, and it WORKED.** ACf-11 never loaded the runtime. It spun a full-load scout (`ACfc`) which read both halves and returned a map; ACf-11 then read ~260 lines of `src/` itself, only to verify the scout's specific claims. **Three consecutive prior ACf instances died of context exhaustion before their lanes built; ACf-11 spun its lane with ample room left.** Keep using it.

---

## §3 — The `ACfw` lane: what it found, and where it landed

**Read §9 of its brief for the full record.** The load-bearing parts, so you can orient before you open it:

**It corrected FIVE things in the brief ACf-11 wrote it** — which is the layer working exactly as designed (it had read the whole runtime; ACf had read 3%). The one that matters most:

- 🔴 **"No alert" was FALSE.** The brief (and ACf-11's relay to Justin) said a 414 silently deletes an agent from the team with no alert. **Wrong** — `scheduler/index.ts`'s dispatch catch fires `notify('WAKE DISPATCH ERROR: …')`, so a Slack ping does reach Justin. **The real defect is that the alert is *generic*, carries no park record, and is preceded by a `"non-fatal"` log line — so the operator's first evidence points *away* from the cause.** The fix is still warranted; the justification changed from *existence* to *specificity*. **Justin was told the wrong version and then told the correction — do not re-introduce the original claim.**
- **Blast radius is four behaviors, not one**, and it depends on the trigger: a Slack wake **drops** (the user's @mention is never answered); a self-scheduled wakeup is **consumed and lost**; an addressed message **retries every 15s forever** (latent today, *armed the moment a members-wake lands — i.e. by `ACfm`'s Step 4*); a daily check-in drops.
- **The ~370 figure is empirical and now triangulated:** QC measured it live on DEV previously; ACfw independently measured the URL the runtime actually builds (42.00 bytes per id, 234 fixed) and got 384 ids under a 16 KiB wall. **Two methods agreeing within ~4%.**

**Its design decisions (its call, per the brief's "overrule me if you're right"):** chunk by **encoded URL bytes, not id count** (a byte budget self-corrects when id format changes; an id cap goes stale silently) · **not** a PostgREST join (would need an FK migration, out of scope) · scope answer **BOTH** the trigger and a bounded slice of the class · **skip** the dormant-id pre-filter this trip and route the monotonic-growth fact to whoever owns subscription lifecycle / `L12`.

### 🔴 TWO OPEN FLAGS FROM `ACfw`'s DC THAT GATE THE MERGE — ACf had not resolved these at set-down

**These came up the chain honestly rather than being buried, and they are the two things to settle before anything merges:**

> ## ✅ FLAG 1 IS RESOLVED — read this before acting on the text below it
>
> **`ACfw`'s set-down 2 (brief §9.9) answers it, and reframes the question better than ACf posed it.** ACf worried the budget was *"never validated against the real server."* **The DC's rationale makes that irrelevant by construction:** it sized the budget at **2× under the most conservative limit any proxy could impose** (8,190 bytes — the Apache `LimitRequestLine` / nginx request-line default), explicitly **not** at a multiple of the *observed* wall — on the ground that sizing to what you measured today leaves the system **one infrastructure change away from the same outage.**
>
> **That is the same reasoning that made cursor pagination right over a hardcoded row cap: don't depend on knowing the ceiling's value.** ⇒ **Not a merge blocker.** Two independent URL measurements (ACfw's and its DC's) also agree within ~2%.
>
> *(Original text preserved below for the reasoning trail.)*
>
> 1. **The byte budget was never validated against the REAL server.** The fix chunks the id list by **encoded URL bytes** (`MAX_URL_BYTES = 4000`, minus a 512-byte reserve → ~83 ids per chunk), and the mechanism is proven end-to-end against a faithful fake. **But nobody has watched the actual Supabase endpoint return a 414.** The DC said so explicitly rather than letting the gap be invisible: common server defaults are 8192 and 16384 bytes, so 4000 carries ~2–4× headroom against the tightest plausible limit, and the constant is **injectable** so it can be raised with evidence. ⚠️ **Note this partially contradicts `ACfw`'s own §9.6**, which reports a prior QC live-measurement on DEV at ~370 ids and an independent URL measurement putting the wall at 16 KiB. **Reconcile those two before deciding whether the gap matters** — it may already be closed by the earlier measurement.
2. **Commit authorization.** The DC committed on its own never-pushed branch per its lane instructions, then flagged that AC's standing rule requires Justin's per-commit go-ahead and *"an agent message isn't that."* **The DC was right to surface it and right to commit:** subagents commit on their own feature branches as the normal build flow — the gate is at **merge**, which ACf controls, and Justin's build-level-code standing exception (now in `CLAUDE.md`, commit `ad3281b`) covers exactly this. **No action needed; recorded so nobody re-opens it.**

**Also routed by the DC, not fixed:** `src/tools/tools.ts:267` uses `.ilike('body', '%…%')` with a **model-supplied** unbounded string — same URL axis, different character (self-inflicted, fails loudly, affects one tool call rather than causing a wake outage). **Owed: a tracker row.**

**⚠️ It flagged that the fix FALSIFIES five comment blocks** in `digest/index.ts` and `composer/index.ts` which currently document the 414 as deliberately-deferred. Per the route-vs-fold rule these are a **defect the change itself creates**, so they must be rewritten **in the fix commit** — not routed. **Gate this at review; it is an explicit QC check.**

---

## §4 — What is OWED, in priority order

1. **⬜ Resolve the `ACfw` lane** (§0) — check artifacts, then finish, re-spin, or reap.
2. **⬜ Report the CONTEXT COST to Justin, then spin `ACfm`.** Justin is deliberately measuring what a subagent costs its parent; **that measurement is the reason only one lane ran.** Numbers so far, all *subagent-side* burn: scout load 827K · scout work 922K · `ACfw` load 954K (153 tool calls). **Parent-side cost ≈ 10–11K tokens per full lane cycle** (the brief you write out + receipts back + report back). Ratio roughly **170:1**. ⚠️ **The final report is the biggest single parent cost and ACf-11 never received `ACfw`'s** — so the number is *incomplete*. Say so.
3. **⬜ Then promote the context finding to a durable home** — Justin approved *(b) then (a)*: wait for the complete number, then draft an addition to `ac-orchestration-protocol.md` **plus** a one-liner in `CLAUDE.md`'s "Subagent lifecycle facts" bullet, left in SCM for his vet.
   - 🔴 **CARRY THIS CORRECTION INTO WHATEVER YOU WRITE — observed live 2026-08-10, and it revises the simple story.** *"A subagent's burn doesn't land in the parent"* is true of its **token spend**, but **GRANDCHILDREN's final reports surface to the top-level session anyway.** ACf-11 received full completion reports from **`ACfw`'s DC and `ACfm`'s DC** — agents it never spawned and does not manage. Each is ~500–1,500 tokens of *the conductor's* context, arriving unbidden.
   - **⇒ The practical consequence, which is the part worth writing down: a lane's cost to its conductor is NOT just "the brief out + its report back."** A lane that runs a DC and a QC, each on two trips, can surface **six-plus** reports upward. **So conductor context scales with the DEPTH of the tree, not just its width** — and that is precisely the variable a head AC controls when it decides how many lanes to run. **Do not publish the 10–11K-per-lane figure without this caveat; it was measured before the grandchildren started reporting.** **Why it matters:** the claim currently lives *only* in an ephemeral handoff, correctly hedged as a hypothesis. It is the number that decides how many lanes a conductor dares run, and every fresh AC re-derives or guesses it. ⚠️ **Carry the honest caveat: three load trips and one work trip is thin — enough to confirm the *shape*, not to publish a per-lane constant.**
4. **⬜ Update `current-state.md`'s team-runtime section.** Justin ruled *wait until `ACfw` lands* so it is one update, not two. **It needs his approval — propose, don't edit.** It is a hot multi-agent file; re-read it immediately before proposing.
5. **⬜ CHANGELOG entries** for this session's work — `evryn-team-runtime` (the ARCHITECTURE/tracker changes) and `_evryn-meta` (the CLAUDE.md commit-standing edit). **You write these when you merge; a sub-AC never does** (it cannot ship).
6. **⬜ Hand AC0 the product-tracker note.** Justin approved the handover explicitly. It is a dashboard defect with **no** team-runtime coupling: 4 of 12 `ItemStatus` values render as an unstyled gray pill identical to the deliberate gray for `passed`/`ignored`, so *still-being-worked* looks like *declined*. **The paste-ready text is in the retired 08-06 handoff §4** — the one thing worth opening that file for. **Route via Justin or a committed file; a Slack ping addressed to "AC0" reaches Justin, not AC0.**
7. **⬜ The Opus-5 code change (Step 24).** ARCHITECTURE's rationale is written, committed and vetted; `src/config/agents.ts` still says `claude-opus-4-8`. **It is a build trip, not yours to write** — fold it into a lane or give it its own small DC trip. **And it is not done when it compiles: see §5.1.**

8. **⬜ Minor — the team-runtime cascade over-describes `runtime-ops.md`.** `_evryn-meta/docs/protocols/load-cascades/team-runtime-full-cascade.md` calls it *"the agents' operative behavior doc (thread navigation, listen-permission, memory-discipline, standing gates)."* It carries the last two; **thread-navigation and listen-permission are Step 35's owed edits and are not in the file yet.** So the cascade describes the *post-Step-35* state as though it were current. Harmless today, but it is the same class we spent 2026-08-10 killing — a doc asserting a state the runtime doesn't have. **Fold the correction into Step 35 when that edit lands**, rather than spending a trip on it.

**✅ Discharged this session, so you do not re-do them:** the `CLAUDE.md` commit-standing edit · the merge-info-inline tracker rule · the ARCHITECTURE model-pin correction · both lane briefs' staleness · spinning `ACfw`.

**✅ Discharged by AC0, not you:** the **orchestration-protocol self-contradiction** (the merge rubric said AC commits build-level code without waiting; the escalation ladder listed *any* commit as never-absorbable). AC0 committed `a121523 "Orchestration protocol: resolve the commit/merge self-contradiction"`. **Verify it says what you expect before relying on it.**

---

## §5 — Justin's rulings, 2026-08-10

### 5.1 The model pin — approved, but never blind
**`claude-opus-5` is approved as the next pin** (*"I think it's stable enough"*). 🔴 **But we never just switch — we test any time we switch and look for whether behavior changed.** A model change is a **behavioral change to all eight agents at once**, and it is the only change that alters what every agent *is* while leaving every file describing them untouched. **The switch lands BEFORE gate G3's remaining Lucas paces, so the paces ARE the observation** — running them on the outgoing model would only prove the outgoing model. Full rule: ARCHITECTURE cardinal invariant 5.

### 5.2 The merge-state block — deleted, and the generalization
Justin challenged the tracker's `MERGE STATE` block: *"doesn't that just encourage drift?"* **He was right and ACf-11's carve-out defending it was wrong.** Every line was a duplicate of the Gates/legend or a fact git answers better; it was two commits stale *underneath its own footgun warning*.

**The durable test, which generalizes beyond this doc:**

> **Does a line hold a FACT git already has, or a MAPPING git cannot have?**
> - *"main is `405caca`"* → pure git fact → **delete it, ask git**
> - *"Step 2 shipped in `567056a`"* → a work-item↔commit mapping git cannot produce → **keep it, in that Step**
> - *"015 + 016 unapplied"* → not in the repo at all (it is DB state) → **keep it, in G1**

**Division of labor:** the **tracker** does altitude and mapping · **git** does facts about the code · the **Gates** do state that lives outside the repo. **And the generalization worth keeping: a "current state" block is a magnet** — it reads as the helpful place to jot what you just learned, so it accretes copies of facts with homes elsewhere, and *because it looks authoritative* nobody reconciles it.

### 5.3 One lane at a time
Standing until Justin says otherwise. **He is buying information about context economics, and two lanes at once destroys the measurement.** Do not quietly restore concurrency because it looks faster.

### 5.4 Push
**AC0 owns the push.** `_evryn-meta` had 17 unpushed commits, 15 of them AC0's live lanes; there is no way to push only your own. **Do not push `_evryn-meta` yourself** — hand it to AC0 via Justin. *(`evryn-team-runtime` and `evryn-team-workspace` are yours to raise with Justin normally.)*

### 5.5 Compaction
🔴 **If you compact, STOP and tell Justin.** His words: *"if you notice you compacted, I don't trust your outputs anymore and I want you to just pause."* Do not push through on felt-sharpness — re-execute the reload manifest (§9) and request a re-spin.

---

## §6 — 🔴 HOW TO TALK TO JUSTIN — his standing order, carried forward verbatim in substance

**Justin's own words, which he asked be handed to every future ACf:** *"Remember three things: I'm not an engineer, I'm not reading **any** of the materials you are (including your handoff), and I'm coming in cold from other sessions **every** single volley… I'm usually not gone for very long, but I'm **always** coming in with my context completely wiped by my other meetings. So just little handholds do an **enormous** amount of heavy lifting."*

**Concretely, every reply — not just the long ones:**

1. **He has not read this handoff, the tracker, the design-of-record, or a line of `src/`.** Anything you name — a Step number, a file, an agent, a mechanism — is a **label he cannot resolve.** Say what it IS in the same sentence: not *"Step 30"* but *"Step 30 — the bug where an agent stops waking entirely once it's in ~370 rooms."*
2. **"He replied two minutes ago" is not evidence he still holds context.** Treat every volley as a cold open.
3. **He is not an engineer.** Concept first in plain English, *then* the term if it's worth teaching.

**⚖️ HIS CALIBRATION, so you don't swing too far:** *"I don't need **quite** that much re-anchoring… just give me little handholds and avoid contentless things like 'your three-things reminder' where I have to scroll up to remember what the hell you're talking about."* ⇒ **The ask is not volume, it is that no reference is ever empty.** A paragraph of re-introduction is too much; a naked callback is the actual defect. **Weld a few words of content onto every reference.**

**Other standing communication rules:**
- **Ping `#team-alerts` on EVERY response**, prefix `ACf:`, and **ping BEFORE composing the reply** so a stalled turn is cheap for him to nudge. Webhook is `SLACK_TEAM_WEBHOOK_URL` in `evryn-team-workspace/.env`; use **Node `fetch`**, never bash+curl or PowerShell. The pinger script is session-scoped — recreate it.
- **Lead every relay with `# ACf's read — <topic>`** — H1, your name first. Sub-ACs cap at `##`; DC/QC may never write `'s read` at all. That level difference is the entire boundary marker in a wall of subagent output.
- **Inline ballot questions where the reasoning is, then recap them at the end.** He reads down and composes as he goes. *(His explicit request, 2026-08-10.)*
- **Never use `AskUserQuestion`** — it hangs.

---

## §7 — Gates and the things that will bite you

- **🔒 Merge / deploy / migration-apply are each SEPARATELY Justin's explicit go. Prior authorizations never stack.** On `team-runtime` **ACf may merge directly** (standing, 2026-07-20 — the repo is uncontended). **A lane AC never merges, pushes, deploys, or applies a migration.**
- **Commit standing:** **code — yes; source-of-truth docs — NO**, leave them in SCM for his vet and **list them in chat at wrap-up**. Brief/mailbox/handoff traffic is commit-exempt. **This is now in `CLAUDE.md` itself** (commit `ad3281b`), so you no longer need to re-confirm it every session — that was the whole point of the edit.
- **🔴 `git add <paths>` DOES NOT SCOPE THE COMMIT.** A bare `git commit` afterwards commits the **entire index**, including another agent's staging. **Put the pathspec on the commit** (`git commit -F - -- path/a path/b`); recover with `git reset --soft HEAD~1`. Also `git diff <file>` before committing a shared file — a by-path commit takes that file's whole working-tree state.
- **Spinning anything: TWO TRIPS, always.** Trip 1 = `<identity>` + `<mandatory_load>` + `<receipts>`, saying **nothing** about the work. Verify receipts against your list. Only then resume with `<task>`. **Paste the blocks VERBATIM** — AC variant is exactly 2 substitutions. `#cascade-override` + a one-line reason, because the target is a separate codebase **and** an agentic runtime, so the list must carry **both halves**. Every file line-spanned. **Model-pinned `opus`.** Annotate the canary carve-out: **`src/wakes/memory-writer.ts` is the one code file with no bottom canary.**
- **Verify receipts by COUNT, not just file list.** Reconcile declared calls against observed `tool_uses`. *(ACf-11's two spins reconciled exactly — 112/112 and 153/153. That is what a clean one looks like.)*
- **You own the sandbox.** Any agent that will mutate code to test a guard gets a **disposable** `git worktree add --detach` copy, provisioned by you — never the reviewed tree.
- **`main` moves under a lane.** Rebase rather than merge-into; use `git diff main...branch` (**three dots**) — two dots renders anything `main` gained as a deletion by your branch, which reads as though the lane reverted someone's work.
- **The ground can move MID-SESSION.** ACf-11 verified "no worktrees anywhere" at startup and found **seven** forty minutes later. **Re-check repo state before acting on it, not once at the start.**

---

## §8 — LESSONS — don't re-pay these

- **⭐ Reading cannot find an ABSENCE.** Every serious finding this month was a *missing* thing — a missing row limit, a missing alarm, a missing ability to read a file. **You only find them by breaking something and watching whether anything complains.**
- **⭐ A review layer earns its cost only if it attacks DIFFERENTLY from the layer above.** Every best catch has come from **below** the top agent, and the top agent had already checked the thing that was wrong and passed it. **This fired again today:** ACf-11's brief asserted a 414 produces "no alert"; `ACfw`, having read the whole runtime, found the `notify()` call and corrected it. **Never let a reviewer inherit the author's inputs.**
- **⭐ The conductor cascade WORKS, and its cost is where you'd least expect.** Three ACfs died of context exhaustion before their lanes built — **not from subagents, but from loading the runtime themselves.** A conductor that holds a map instead of the runtime spins its lane with room to spare.
- **⭐🔴 "I'll keep going with X" then stopping.** The pull to end a turn is strongest right after a long, complete-*feeling* message — **and the specific trigger is a BALLOT**, because a question manufactures a handoff expectation that swallows the stated intent to continue. **The only fix that has held is ORDERING: do the work FIRST, report in the PAST TENSE.** A turn ends when you emit text and make **no further tool call** — text alone does not end it, so you *can* answer inline and keep working.
- **⭐ RE-EMBED THE REFERENCE.** Whenever you refer back to anything — a decision, a doc, a prior yes — carry a few words of what it *was* in the same sentence. And **never point Justin at something you haven't written yet.**
- **Verify against the artifact, never the narrative.** A set-down is true when written; the worktree is true now.
- **This runtime punishes local reasoning almost perfectly** — behavior is half `src/` and half the identity files; defects live in **seams** several hops from the changed line.
- **A binary file defeats GNU `diff`, not just git.** When git says *Binary files differ*, your whole text-tooling layer is blind.
- **Never junction `node_modules` into a mutation sandbox** — removing the junction took the live worktree's `node_modules` with it. Give each sandbox its own install.

---

## §9 — Your reload manifest

**Load in this order and STOP at the point where you can do the next task. Do NOT load `src/` to write a brief.**

1. **Light cascade:** `_evryn-meta/CLAUDE.md` (auto — but read it yourself and confirm its canary) · `_evryn-meta/docs/current-state.md` · `_evryn-meta/docs/hub/roadmap.md`.
2. **THIS FILE**, in full. Then **the tracker** — `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` (~195 lines) — your primary status surface, and cheap. **Its Gates section G1–G4 holds every deploy precondition.**
3. **`_evryn-meta/docs/protocols/load-cascades/conductor-cascade.md`** — **read it before relying on it.** It is **not a lighter load**; it is the mechanism for getting runtime competence without holding the runtime. ⚠️ **AC0 revised it 2026-08-10: the reporting cycle now BEGINS at ALIGN, not at a landing** — do not start the cycle at "land → report" just because that's where you're standing.
4. **Before spinning anything:** `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` in full (~796 lines and growing; page it). **It changed twice on 2026-08-10** — two-trip loading and AC0's commit/merge fix. **Briefing from memory of an older shape produces a wrong brief.** **Before writing any doc:** `ac-writing-protocol.md`.
5. **The `ACfw` brief** (`_evryn-meta/docs/working/2026.08.06-acf-acfw-wake-outage-brief.md`, ~225 lines) — **§9 is the live lane record.** Read it if the lane is unresolved.
6. **Only what the task needs after that.** For *spinning* a lane you need file **inventory and line counts** (`wc -l`, not `Read`). The lane AC loads the runtime.
7. **The design-of-record** (`evryn-team-workspace/shared/projects/ops/team-runtime/2026.07.15-acf-team-runtime-memory-architecture.md`, 500 lines) — only when *reviewing* memory work rather than dispatching it. §4.2/§4.4/§4.6 for Steps 4/5.
8. **The full both-halves cascade** (`team-runtime-full-cascade.md`) is the right load **when reviewing a returned diff**, not when dispatching one.

⚠️ **The trap in the conductor approach:** a scout's map is a *summary*, and subagents summarize destructively. **A map is fine for "where does X live and what depends on it." It is NOT a substitute for reading a file whose nuance you are about to decide on.** Use it to aim your reads, never to skip them. **And say plainly to Justin how blind you are when it matters** — ACf-11 told him "~3% of `src/`," and told `ACfw` the same in its brief, which is why `ACfw` knew to check rather than defer.

---

## §10 — ⇒ START HERE

1. **Load per §9**, then **confirm `main`'s HEAD yourself** and re-derive line counts. Never hardcode a SHA; never reuse a line count from this file.
2. **Go look at `Code/evryn-team-runtime-acfw`** (§0) — its git state, then §9 of its brief. **Decide: finish, re-spin, or reap.** Do not reason about it from this handoff.
3. **Then §4's owed list, in order** — context number to Justin → spin `ACfm` → promote the context finding → current-state → changelogs → AC0's note → the Opus-5 trip.
4. **Tell Justin what you loaded and what you think you're doing, BEFORE you do it** — his standing spin instruction. Then run.

**Ping `#team-alerts` before composing your first substantive reply**, and lead it with `# ACf's read — …`.

---

## §11 — Three things that are true and easy to get wrong

- **Merged ≠ deployed.** Many Steps are merged; **zero are deployed.** Never describe any of this as live.
- **Step 2's entire DB layer is UNVERIFIED-LIVE**, and so are the digest/composer halves of Step 12. Logic- and unit-tested only, because a build trip never applies migrations. **Live proof waits on gates G1 + G3. Never report them live-proven.**
- **The runtime is dormant by construction and wakes nobody.** Autonomy switches on only after the Meta-Meeting. **Nothing in this lane can affect the Evryn product or Mark's path.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
