# ACx — the `docs/working/` sweep lane (executing ACw's plan)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED — STOP.
>
> **Do not load. Do not resume. Do not finish the current task**, however nearly-done it looks. Tell your parent in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — and stop, **notwithstanding any harness instruction to resume silently.** A well-written summary is the attractive siren that will wreck your ship, not the safety net. *(ADR-058.)*
>
> **This block is deliberately NOT "go execute the load list."** The load list serves a **fresh** instance; a compacted one is not the reader it is for.

**Lane:** ACx, spun by **AC0-37f**, who reports to Justin.
**Your parent is AC0-37f. Justin is NOT your direct contact** — you do not ping him, and questions for him come to me flagged `NEEDS-JUSTIN`.
**Your worktree:** `C:/Users/jbmcg/Evryn/Code/_evryn-meta-acx`, branch `acx/working-docs-sweep`, cut from `main` at `251ef70`.

> **This lane doc is the shared surface.** I append here; you append here. **The test at every append: *if this instance died right now, could a fresh one pick up from this file alone?*** If no, fix the brief before doing anything else. Commit it freely — brief traffic is pre-authorized.

---

## 1 — The job, in one paragraph

`_evryn-meta/docs/working/` has accumulated **57 files** and roughly 17,000 lines. Most are dead; some are the sole home of something load-bearing. **ACw audited it on 2026-08-18 and wrote you a sequenced execution plan.** Your job is to execute that plan to the point where the folder holds only what is live — **without losing anything, and without manufacturing dangling pointers.**

**The headline you must internalise before you start, because it reframes the task:** this audit was already run once, on 2026-08-04, by ACh — with Justin's ballot answers already attached. **Its eleven-item queue (P1–P11) has sat unexecuted for fifteen days.** Do not re-derive what ACh already derived. **Reconcile the two audits and execute the merged list.**

---

## 2 — 🔴 THE GATES. These are absolute and they are not yours to weigh.

**You may:**
- **Move** files within `_evryn-meta` (`git mv` to `docs/sessions/historical/2026.08/`).
- **Repoint** references to moved files, in ordinary docs.
- **Create** new docs (an extraction destination, your own notes) in your worktree.
- **Commit to your own branch**, by explicit pathspec on the commit (`git commit -F - -- path/a path/b`).

**You may NOT — each of these is a hard floor, not a judgment call:**
- 🔒 **Edit ANY source-of-truth doc.** That means: `ARCHITECTURE.md` · any `BUILD-*` doc · the Hub (`docs/hub/**`) · any protocol in `docs/protocols/**` · any agent manual in `.claude/agents/**` · `CLAUDE.md` · `LEARNINGS.md` · `docs/current-state.md` · any `SPRINT-*` doc. **When your plan calls for content to land in one of these, you DRAFT the exact before → after and hand it to me.** I take it to Justin.
- 🔒 **Touch any file under `evryn-backend/src/`.** The `liveness.ts` comment repoint is a **code change** and needs a real review. **Draft it; do not make it.**
- 🔒 **Merge, push, deploy, or run a migration.** Ever. Your ceiling is your own branch.
- 🔒 **Touch anything named `acf*`, or any file in ACw's bucket 4.** That is ACf's lineage. Not yours to move, tidy, or retire — even when its own text says it can be deleted. **If one is genuinely blocking you, surface it; do not resolve it.**
- 🔒 **Edit a frozen record.** Anything under `docs/sessions/historical/**`, `docs/historical/**`, the archived changelogs, or `evryn-team-workspace/shared/current-state/archive/**`. **A corrected path inside a frozen record is a falsification.** The live changelogs are the same: a changelog entry records what was true when it was written.

---

## 3 — What I know that you do not, and could not derive

**Each of these would cost you real time to discover, and one of them would cost you a wrong answer.**

1. 🔴 **`_evryn-meta` IS HOT RIGHT NOW.** A parallel lane (ACp, under ACv) has **uncommitted edits to `.claude/agents/{ac,dc,qc,oc}.md` and `CLAUDE.md`** in the main worktree. **You are in your own worktree, so you are insulated — but do not assume those files are stable, and never commit them.** `ac.md` measured **748** lines at spin time and was 750 an hour earlier. **Read to its canary and report the count you actually saw.**
2. 🔴 **`AGENT_PATTERNS.md` is NOT your job.** ACw's plan Step 5 covers retiring it. **I am doing that.** Do not repoint an `AGENT_PATTERNS` reference, do not touch that file. If your reference map surfaces its referrers, **hand me the list** — that is genuinely useful — but change nothing.
3. ⭐ **The Step-78 health extraction may already be discharged — CHECK BEFORE DOING IT.** ACw's Step 4b says the *halt returns 200, not 503* reasoning must be extracted from `2026.07.15-ac3-step78-real-health-design.md` into `ARCHITECTURE.md`. **But `evryn-backend/docs/atlas/07-safety.md` §4.3 already documents that rule WITH its full reasoning** (halted is tested first; a 503 there plus a watchdog produces an infinite restart loop against M1's no-auto-reset invariant), and `ARCHITECTURE.md`'s Monitoring section covers the `/health` 503-when-wedged half. ⇒ **Determine what, if anything, is genuinely absent before drafting an extraction.** The answer may be "only the repoint is owed."
4. **The folder is 57 files, not the 53 ACw counted.** Four are new since: this file, and three `2026.08.19-acv-*` files belonging to ACv's live lane. **The ACv files are LIVE and not yours.**
5. **`2026.08.18-ac037e-justin-cutover-redirect-drafts.md` may now be discharged** — ACw flagged it as awaiting Justin's vet, and the cutover ran that same night. Verify.
6. **ACw's own destination correction:** three QC patterns were bound for *"Patterns This Role Watches For"* in the retired `evryn-quality/CLAUDE.md`. **That section survives — it is in `_evryn-meta/.claude/agents/qc.md`.** But `qc.md` is a source-of-truth doc **and** is currently being edited by ACp. ⇒ **Draft those patterns for me; do not write them in.**

---

## 4 — Sequencing. Steps 1 and 2 gate everything else.

**Work in this order. Do not move a single file before Steps 1 and 2 are complete.**

| # | What | Output |
|---|---|---|
| **1** | **Build the reference map** (ACw's Step 6 method). One pass over the seven repos, `*.md` + `*.ts`, source→target pairs, sorted unique. **Then look up per file with `awk`.** | the map, committed |
| **2** | **Verify the three pre-conditions** (ACw's Step 1a/1b/1c) at source, and **reconcile ACw's buckets against ACh's P1–P11** — including whether any of P2–P11 have been discharged in the fifteen days since. | one merged work-list |
| **3** | **Verify what ACw could not.** Buckets 1 and 4 rest on banners and the reference map, not complete reads; **twelve of the seventeen bucket-2 files were classified from their own do-not-retire claims and never individually checked.** Those twelve are where an over-confident RETIRE costs the most. | a per-file verdict |
| **4** | **Execute the safe moves** in your worktree, repointing every live referrer in the same commit. | commits on your branch |
| **5** | **Draft everything gated** — the source-of-truth extractions, the `liveness.ts` comment, the QC patterns — as exact before → after blocks. | a section in this file |
| **6** | **One batched ballot for Justin**, recommend-first, each item: the question in one line · your recommendation · why. | a section in this file |

---

## 5 — 🔴 The instrument traps. Every one of these has already fired here.

- **`grep -P` IS BROKEN IN THIS ENVIRONMENT.** It errors with *"-P supports only unibyte and UTF-8 locales"* — and ACw's first reference lookup used it, **failed on all 53 calls, and returned "EXTERNAL refs: 0" for every file.** Had that been trusted, the recommendation would have been mass retirement of the entire folder **including the file cited by shipped runtime source.** It was caught only because all-zeros was absurd. **Use `awk`. And verify your instrument RAN before believing what it did not find.**
- **A grep establishes PRESENCE, never ABSENCE.** *"Nothing references this file"* is a completeness claim and a pattern-match cannot support one. **Close the defeaters explicitly and say you did:** references by partial or reformatted name; references by section title rather than filename; file types outside your scan (`*.json`, `*.sql`, `*.html`); and **a file `grep` refuses to read at all** — this estate has already been bitten by a stray control character making two files binary to `grep`, so every sweep silently skipped them *and reported success*.
- **When two searches disagree, STOP AND READ.** Do not reconcile them by writing a third pattern.
- **Do not loop `grep -r` once per filename** — ACw tried and it timed out at two minutes with no usable output.
- ⚠️ **Writing a `\uXXXX`-style escape in prose EMITS THE REAL CHARACTER**, because the tool argument travels as JSON. A NUL makes a file binary to `grep` and `git diff`. **Every known instance of this happened while someone was documenting the hazard.** If an `Edit` inexplicably fails to match text that is plainly there, that is a NUL symptom — scan, do not retry.
- **`wc -l` under-counts a file whose last line has no trailing newline.** Trust the canary, not the count.

---

## 6 — What "done" looks like

1. **The folder holds only what is live**, with every retired file's inbound references repointed in the same commit that moved it.
2. **A per-file verdict for all 57**, so the next reader can audit the call rather than re-derive it.
3. **Every gated item drafted**, exact before → after, ready for me to take to Justin.
4. **One batched ballot.**
5. **An honest limits section** — what you did not verify, and what would change if you were wrong. **State the instrument on every claim** (*"grepped for X, found Y"* — never *"verified"* or *"there is no…"*).

---

## 7 — Lane log (append below; newest last)

**[2026-08-19 · AC0-37f]** Lane created. Worktree `_evryn-meta-acx` on `acx/working-docs-sweep` off `main` `251ef70`. ACx not yet spun.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED

**[2026-08-19 · ACx]** Lane executed. Sections 8–13 below are my output. **Read §9 first — it is the headline.**

---

# §8 — What I did, and with what instrument

**Every claim below names its instrument.** Where I searched, I say "grepped"; where I ran something, I say "executed"; where I read the file, I say "read at source". No claim below says "verified" without one of those behind it.

**Step 1 — the reference map. DONE and committed** (`docs/working/refmap-raw.txt`, 982 source-to-target pairs, commit `22349c8`).

- **I extended ACw's scan** from `*.md`/`*.ts` to also cover `*.json *.sql *.html *.mjs *.tsx *.yml *.yaml`, and ran it with `--binary-files=text`. Those close two of the four defeaters ACw's map could not: **file types outside the scan**, and **a file `grep` refuses to read**.
- 🔴 **I verified the instrument RAN before believing what it did not find** — the lesson ACw paid for. Three checks: `grep -E` confirmed working on a synthetic input; exit code 0; and **two known-true references came back present** (`evryn-backend/src/safety/liveness.ts` and the watch-list dangling pointer). ACw's `grep -P` returned a false all-clear on all 53 calls.
- **Two defeaters remain open and I did not close them:** a reference by **section title** rather than filename, and a reference by a **reformatted** name. I ran a partial-name cross-check on the three bucket-1 candidates only (it returned the same sets, plus one extra hit that surfaced `docs/mailboxes/` — a directory that did not exist when ACw ran).

**Step 2 — the reconciliation. DONE. See §9.**
**Step 3 — per-file verdicts. DONE for all 55. See §11.**
**Step 4 — safe moves. PARTIALLY DONE, deliberately. See §11 and §12.**

⚠️ **The folder is 55 files on this branch, not the 57 the brief says or the 58 the task message says.** The `2026.08.19-acv-*` files are **untracked in the main worktree**, so they are not on my branch at all. Nothing is missing — the count differs because untracked files do not travel to a worktree. **I did not touch them; they are ACv's live lane.**

---

# §9 — 🔴 THE HEADLINE: MOST OF ACh's P-QUEUE IS ALREADY DISCHARGED. The fifteen-day-old queue is much smaller than it looks.

**This is the single most useful thing I found, and it inverts the framing everyone has been carrying.** The handoff, the lane brief and ACw's plan all treat P2–P11 as eleven live items rotting since 2026-08-04. **They are not.** Checked one at a time, at source, on 2026-08-19:

| P | ACh's item | Status now | How I established it |
|---|---|---|---|
| **P1** | Operator guide wrong about `/health` | ✅ **DISCHARGED** | grepped `operator-guide.md`: `HEALTH_WEDGED_THRESHOLD_MS` now appears **3x** (ACh measured 0), and 10 `halted`/`wedged` mentions. The one surviving `{"status":"ok"` at line 140 is the **current** Step-78 shape — it carries the `checks` block — not the stale pre-Step-78 example. Fixed by AC0-31/DC, 2026-08-07. |
| **P2** | Six un-rehomed items from the identity-ops brief | ⚠️ **1 of 6 discharged; 5 OPEN** | (i) is P1 → done. (ii)(iii)(iv) grepped absent from `qc.md`. (v) grepped absent from `ac.md`. (vi) grepped absent from the orchestration protocol. **Detail + drafts in §10.** |
| **P3** | Step-78 health doc: extract, repoint, retire | ⚠️ **The extraction is MOOT; only the repoint is owed** | See §9a — this is the lane brief's item 3 and it was right to suspect it. |
| **P4** | Relocate two research docs with breadcrumbs | ⚠️ **1 relocated, 1 diverged from Justin's ruling** | `ls`: the outreach research **is** at `evryn-team-workspace/shared/projects/product/research/2026.07.13-acu-outreach-engine-research.md`. But `find`: the **step53 research went to `_evryn-meta/docs/sessions/historical/2026.07/`** — it was *retired*, not *relocated to the research folder*. Justin's ruling was relocate-with-pointers. **Ballot item.** |
| **P5** | Gate-B has no runnable suite | ⚠️ **Storage risk CLOSED; the build half OPEN** | `git ls-files`: all four `tests/_gate-a-*.ts` are **committed** (2026-08-17). So they can no longer be lost. But grepped `run-fixtures.ts` for `megan`/`argv`/`folder`/`subdir` — **zero hits**, so the promote-the-seeder / extend-the-runner half is not built. **And the changelog records the harness's grading logic predates Step 57 and must be re-derived before its verdict is trusted.** |
| **P6** | Delete the diverged `ac2/step57` branch | ❌ **OPEN** | `git branch -r`: `origin/ac2/step57-runtime-bookkeeping` is still present. ACh's analysis (superseded rebuild, safe) stands; nobody executed it. |
| **P7** | Outstanding pushes in three repos | ⚠️ **Superseded by events** | The 2026-08-17/18 batten-down pushed everything and reaped the stale refs. **Re-measure rather than act on ACh's numbers.** Not mine to push. |
| **P8** | Soren's unsettled design batch | ❌ **OPEN, correctly parked** | Soren's own text forbids sweeping it. Unchanged. |
| **P9** | Three never-answered questions | ⚠️ **Partly overtaken** | Ballot 9 (never-pushed branches) is overtaken by the batten-down. Ballot 10 (stale current-state lines) — those specific lines are long gone. Ballot 4.c (product-radar findings) — that file is now **retired to `docs/sessions/historical/2026.07/`**, so the item closed with it. |
| **P10** | Two process flags | ✅ **Read-once, no action** | Nothing to do. |
| **P11** | Canaries missing from 30 of 44 `src/` files | ✅ **DISCHARGED** | **Executed** a per-file `tail -3 | grep 'Truncation canary'` across `evryn-backend/src`: **45 of 45 carry one.** (Note the file count also grew 44 to 45.) |

⇒ **Genuinely open from ACh's queue: P2 (five items), P3 (repoint only), P4 (one doc), P5 (build half), P6, P8.** That is a short, tractable list — not eleven rotting items.

## §9a — Lane-brief item 3 was right: the Step-78 extraction is NOT needed. Only the repoint is.

**Read at source, three files:**

- **`ARCHITECTURE.md` never carried the halted-returns-200 rule.** Grepped `halted` — the hits at lines 1165/1166 are about the *lossless halt* and the cursor-hold, **not** the `/health` status contract. The single `restart`-family hit is line 1164, about the breaker. **So there is nothing there to be stale; the rule was simply never in it.**
- **`evryn-backend/docs/atlas/07-safety.md` line 230 carries the rule AND its full reasoning** — invariant 3, including that halted is tested first, that this is "the single most tempting bug to fix," and that reordering yields an infinite restart loop against M1's no-auto-reset invariant.
- **`src/safety/liveness.ts` lines 118–138 carry the same reasoning in the code itself**, ending "Do NOT reorder or 'fix' to 503 without re-reading the design spec."

⇒ **The content is not at risk of loss. It lives in two durable places, one of them the runtime.** ACh's P3 and ACw's 4b both prescribe extracting it into `ARCHITECTURE.md` — **that prescription is now redundant**, and executing it would add a third copy of a rule that already has two.

**What IS genuinely owed: the `liveness.ts:5` comment cites a path that would dangle if the design doc moves.** That is a **code change** and a hard gate for me. Draft in §10.

⚠️ **Whether `ARCHITECTURE.md` *should* also carry it is a separate editorial question, not a loss-prevention one. I recommend NOT adding it** — the Atlas is where a reader goes for `src/safety/` invariants, and a third copy is a third thing to keep in sync.

---

# §10 — GATED DRAFTS. Exact before → after. I wrote none of these in.

**Every item here sits behind one of the lane's hard floors.** Ordered by what I judge most valuable first.

## 10.1 🔒 CODE — `evryn-backend/src/safety/liveness.ts:5`. Needed ONLY IF the design doc is retired.

**BEFORE (lines 3–5, read at source):**

```
// tell "healthy" from "wedged"). Design: docs/ARCHITECTURE.md (Monitoring &
// Silent-Death Safety, M1) + the AC3 design spec
// _evryn-meta/docs/working/2026.07.15-ac3-step78-real-health-design.md.
```

**AFTER:**

```
// tell "healthy" from "wedged"). Design: docs/ARCHITECTURE.md (Monitoring &
// Silent-Death Safety, M1) + docs/atlas/07-safety.md invariant 3, which carries
// the halted-returns-200 rule and its full reasoning. Original design spec archived
// at _evryn-meta/docs/sessions/historical/2026.07/2026.07.15-ac3-step78-real-health-design.md.
```

**Why this shape:** it repoints at the **live** durable home (the Atlas invariant) *and* preserves the archival path, so neither the rule nor its provenance is lost. **Comment-only — but it is `src/`, so it gets a real review, not a doc tidy.**

⚠️ **If Justin prefers to leave the design doc where it is, this draft is unnecessary** — that one comment is the doc's only live external referrer, so leaving the file costs nothing but a slot in the folder.

## 10.2 🔒 `_evryn-meta/.claude/agents/qc.md` — three QC patterns. One is ALREADY APPROVED and merely unwritten.

**Destination:** the `Patterns This Role Watches For` section (begins line 186). **Matched to the house shape** — Trigger / Failure / **Move:** / *Seen Nx* — with **no build-state breadcrumbs** (no step numbers, dates or ticket IDs), per that section's own stated rule.

⚠️ **`qc.md` is under uncommitted edit by ACp right now. Re-check the live file before applying.**

**(a) — ALREADY APPROVED BY JUSTIN, never written. This needs a writer, not a decision.**

```
- **A handler whose decision logic is extracted for testability while its assembled composition stays in a boot-on-import module.** The decision function gets covered; the wiring that turns it into a response — status code, body shape, headers — is never exercised, because importing the module boots the app. Coverage reads healthy and the wiring is untested. **Move:** when logic has been lifted out of a handler for testability, ask whether the *composition* is reachable by a test without booting the process; if it is not, the extraction only half-solved the problem. *Seen 1x — a status endpoint whose decision function was well covered while the code assembling its HTTP response had no test at all.*
```

**(b) — PENDING JUSTIN'S YES.**

```
- **A client that treats a non-2xx response as "no data" when the error body carries the payload.** Bailing out on a non-ok status throws away exactly the diagnostic state the caller exists to surface: the endpoint signals the fault by status code *and* explains it in the body, and the client discards the explanation — so the display blanks in precisely the state it was built for. **Move:** for any fetch wrapper reading a status, health or diagnostic endpoint, ask "does this signal failure by HTTP status while carrying detail in the body?" If yes, the not-ok bail is a bug — parse the body first, and reserve the null path for genuine transport failure. **Not** a concern where error bodies are empty or opaque. *Seen 1x — a dashboard health client would have blanked every subsystem light on the one response whose body held those very checks.*
```

**(c) — PENDING JUSTIN'S YES.**

```
- **A UI surfacing several subsystem statuses where one subsystem's failure DELIBERATELY causes another's.** The derived failure renders as its own independent alarm, so a single root cause displays as N faults and the operator chases a phantom at 3am. **Move:** enumerate every pair of surfaced signals and ask "can one deliberately cause the other?" For each such pair verify BOTH directions — the derived signal renders as an expected consequence with the causality named, AND that same signal occurring independently still alarms at full severity. A rule that softens a genuine alarm is worse than no rule. Check that presentation precedence matches the backend's decision precedence. *Seen 1x — a deliberate halt stops the poll loop, so poll staleness is caused-and-expected; a stale poll with the breaker untripped is a real wedge and must stay loud.*
```

⚠️ **Do not confuse (c) with the pattern already at `qc.md:219`** — that one is about *phantom controls left behind when a safety layer is removed*. Different shape. I read it to be sure rather than trusting the grep count.

**A further four patterns** from `2026.08.12-qc-sweep-findings.md` §6 are also proposed-and-unapproved. **I deliberately did not draft those** — they live in a bucket-3 KEEP file that is not moving, so they are not at risk of loss, and drafting them was not asked of this lane.

## 10.3 🔒 `_evryn-meta/.claude/agents/ac.md` — the "clean up as we go" rule. PENDING JUSTIN.

Grepped absent. Justin's own ruling, per ACh's P2(v). **Suggested placement: the *Documentation Approach* section, beside the route-an-adjacent-bug / fix-your-own-defect rule, which it complements.**

```
**Clean up as you go, rather than growing a list.** When you are already in a file and see something small and safe to fix — a stale path, a typo, a broken link, a wrong line reference — fix it in that pass. Routing a ten-second fix into a tracker creates the recorded-but-never-delivered failure this org has already been bitten by, and the next person to open the file pays the tax you just declined to pay. **The bound is the adjacent-bug rule's bound: this is for the small and safe. Anything needing a review is still its own change.**
```

⚠️ **MY RECOMMENDATION IS TO ASK, NOT TO WRITE.** This substantially overlaps the bullet already in `ac.md` — *"It does not mean 'defer the trivial.' Routing a ten-second fix into a tracker row creates the recorded-but-never-delivered failure..."* **In substance it is already covered.** Justin named it separately, so he may want it as its own rule; but adding a near-duplicate to the always-loaded manual has a real cost. **This is a yes/no for him, and I lean no.**

## 10.4 🔒 `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` — the line-count-drift lesson. Minor.

Grepped absent. **Suggested placement: part 2 of the brief shape, beside the existing "annotate every file with its line span" instruction, which it directly qualifies.**

```
⚠️ **Line counts drift mid-session in a multi-agent repo — so tell the subagent to trust its own read over your number, and to flag the delta.** Count close to spin time, but treat the count as a hypothesis rather than a fact: the agent holding the file is better placed than you to know. **A handoff that hands you a number should hand you the command that produced it.**
```

*(This absorbs drafted-learning #6 from the 08-17 handoff §9.3 — the one of that set of six that did NOT land in `LEARNINGS.md`. I probed all six; five are there.)*

## 10.5 🔒 `_evryn-meta/.claude/agents/ac.md` — `ac-writing-protocol.md` into the Light cascade. **Approved 2026-08-12; never landed.**

**Confirmed still open, two instruments:** grepped `docs/protocols/load-cascades/` — **zero hits**; grepped `ac.md`'s Light-cascade section — **zero hits**.

**AFTER — add as a fourth bullet to the Light Startup Context Cascade:**

```
- **`_evryn-meta/docs/protocols/ac-writing-protocol.md`** — the craft layer for writing anything another instance will read. **It is mandatory before writing to ANY doc**, and until now no cascade loaded it, so it was reachable only by remembering it existed.
```

⚠️ **This is the highest-value unlanded item I found that is purely a doc edit.** It has been approved for a week, it gates every document any AC writes, and nothing loads it.

## 10.6 🔒 `_evryn-meta/.claude/agents/ac.md` — the branch-enumeration script duplicates per worktree. **ACw's ballot item 3, ANSWERED BY EXECUTION.**

**The question was open. It is now closed.** I ran the enumeration from two different worktrees of `_evryn-meta`:

```
main worktree: acf15/nest-prep  acx/working-docs-sweep  main
acx  worktree: acf15/nest-prep  acx/working-docs-sweep  main
```

**Identical.** Linked worktrees share one `refs/heads`, and the script's `for d in */` loop admits any directory carrying a `.git` entry — a worktree's `.git` is a file, so it passes. ⇒ **With three meta worktrees, a real hit prints three times. The duplication still occurs.**

**Proposed fix — dedupe on the shared git dir, which is identical across a repo's worktrees and different between repos:**

```
seen=""
for d in */; do d=${d%/}; [ -e "$d/.git" ] || continue
  gd=$(git -C "$d" rev-parse --git-common-dir 2>/dev/null) || continue
  gd=$(cd "$d" && cd "$gd" && pwd)      # absolutise so a repo's worktrees collapse to one key
  case " $seen " in *" $gd "*) continue ;; esac
  seen="$seen $gd"
  # ...existing per-repo body unchanged...
done
```

⚠️ **I did not test this replacement, and I am not recording it as confirmed.** The **finding** is executed and certain; the **fix** is reasoned and unproven. Per the standing rule — do not persist a fix as confirmed before testing it — whoever applies this should run it across all eleven repos first.

## 10.7 🔒 ROUTE TO SOREN — the live dangling pointer in the watch-list (ACw's Step 1b).

**Confirmed real, two instruments.** `ls`: no such file at `_evryn-meta/docs/working/2026.06.17-ac3-cache-measurement-plan.md`. `find`: it is at `_evryn-meta/docs/sessions/historical/2026.06/2026.06.17-ac3-cache-measurement-plan.md`. The citation is **`evryn-team-workspace/shared/projects/product/watch-list.md:35`**.

**BEFORE:** ``Plan: `_evryn-meta/docs/working/2026.06.17-ac3-cache-measurement-plan.md` (Q1–Q7).``
**AFTER:** ``Plan: `_evryn-meta/docs/sessions/historical/2026.06/2026.06.17-ac3-cache-measurement-plan.md` (Q1–Q7).``

**Owner: Soren.** It is his file, in another repo, and outside this lane's move permission. **This is a route, not an edit.**
