# Brief: AC2 — post-③ fast-follows

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload.
>
> **How to use this:** Your marching orders now that the cost lane's keystone (③ Runtime Bookkeeping) has SHIPPED. This is deliberately lean — the ③ build history is retired (it's in CHANGELOG `v0.2.8` + the deploy-log + git history of the old cost-lane brief / step57-build-review). Author: AC0-26, 2026-07-22.

---

## §0 — WHERE YOU ARE (read first)

- **You are AC2 — a REGULAR PEER AC** (cost/hardening lane). Sign `AC2`. ⚠️ If you find a doc saying you become a "subagent of AC0" at ③-merge — **that experiment was CANCELED** (Justin, more-complexity-than-it-fixed). You are a peer, coordinated with AC0 through Justin or a committed brief; there is no live AC-to-AC channel.
- **③ (the bookkeeping rewrite — runtime does the record-keeping, Evryn returns a structured verdict) is DEPLOYED to prod as `v0.2.8`** (2026-07-22, verified). The cost lane's build→merge→deploy arc is **DONE.** Your job now: **(A) finish the ③ #lock doc-flips (§5)** and **(B) the fast-follows (§2).** The #lock doc-flips wait on Justin's vet/auth (he's the gate on ARCH/BUILD/current-state/sprint) — so **advance SPRINT 91 (the Mark-blocker) in parallel; do NOT block it on his doc review.**
- **Load:** the **FULL cascade on `main`, both runtime halves** (`evryn-backend/src/**/*.ts` **and** `identity/**/*.md`) per the load-cascade router (`docs/protocols/load-cascades/full-product-cascade.md`) + skim the CHANGELOG. `main` == the deployed `v0.2.8`; the only in-flight code delta is your WebFetch branch. Don't run lean — the docs lag the runtime; the runtime is ground truth.

---

## §1 — WORKING WITH JUSTIN (load-bearing — carry these verbatim)

- **Ping `#team-alerts` (`AC2:`) EVERY response, the whole session** — he runs ~7-8 lanes in parallel; a dropped ping wastes his time. Even when he seems present, assume he isn't. Node `fetch` → `SLACK_TEAM_WEBHOOK_URL` (in `evryn-team-workspace/.env`), **`C:/...` path form in Node, never git-bash `/c/`, never echo the URL.** Attention-tap only; substance in chat.
- **Put your substantive chat response LAST in the turn, AFTER all tool calls** — a response written *between* tool calls gets buried and he misses it.
- **He comes in COLD every volley and reads ONLY the chat (never briefs/docs).** CEO-altitude, concept-first, plain English. **NO jargon-by-number** — "Step 91"/"③"/"NB-2" mean nothing to him; name the thing ("the halt-resume gatekeeper bug"). Handhold him back into the room with a breadcrumb on first mention.
- **He does NOT vet build specs (not an engineer) — YOU own engineering correctness.** Get his go on the *substance* in plain language. Deploy/merge is the real gate he holds on code.
- **You OWN this lane's design; AC0 is a peer** (bounce *collision*/sequencing to AC0; decide your own design calls). Justin gives taste + top-level vetoes.
- **Recommend-first, always.** Every ask to Justin: cold-reader context → your rec + why → alternatives rejected → the numbered question inline → clean recap at the end. Never a bare "what do you want?"; batch asks so he clears you in one dip; keep advancing everything else while one item waits.
- **Commit discipline:** each **code** commit needs his explicit go in his *most-recent* message ("handle it"/"ping me when done" do NOT authorize). Mailbox/brief/CHANGELOG/deploy-log/ADR/handoff commits are pre-authorized. **Stage by explicit path — never `git add -A`** (parallel agents share repos). **Merge-to-main + `railway up` + Mira SCM-review = ALWAYS Justin-gated.** ARCH/BUILD/operator-guide/current-state/sprint edits: he SCM-vets before commit (and may want to own sprint edits himself — offer).
- **Framing to hold:** Evryn is an **independent contractor doing work for Mark, NOT his assistant.** And "trusted" = the **instruction-vs-data wall** (all inbound rides `prompt`, never `systemPrompt`), not vouching — only the Operator (verified Slack ID) is structurally elevated.
- **Model:** stay **Opus** (offer Fable only for a heavy merge-gate review).

---

## §2 — YOUR FAST-FOLLOW QUEUE

Each is its own small DC→QC trip (live-path exercise before any GO on an interactive pathway). Sequence 91 first (Mark-blocker), then WebFetch, then the rest.

1. **🔴 SPRINT 91 — MARK-BLOCKER (do FIRST).** *The halt-resume path skips gatekeeper lane-resolution → phantom lead + empty-profile triage.* `resumeParkedItem` (`src/email/process.ts`, ~L483) `findOrCreateUser`s the raw envelope address blind instead of going through `resolveGatekeeperByInboundAddress` + `decideSenderResolution` (what `processEmail` uses at L366). **Consequence:** on a halt-resume of a forward that arrived via a registered *non-primary* lane, Evryn fails to recognize her own gatekeeper and triages against a blank profile. **Reachability:** the FORWARD half is covered when clustering is ON (a clustered forward re-drains via `triageQueuedItem`, never the resume path) — but **DIRECT messages stay exposed regardless of clustering**, and any clustering-off window exposes the whole forward firehose. **Clean fix closes ALL regimes:** extract the resolution into ONE shared helper both `processEmail` and `resumeParkedItem` call (so a third path can't drift again). Touches `process.ts` — **the same file as 90, so sequence the two together.** Provenance: `docs/working/2026.07.16-acu-ac0-product-radar-findings.md` (R1); SPRINT 91.
2. **WebFetch-timeout fast-follow.** *A hung WebFetch wedges the single-threaded poll loop forever* (no wall-clock cap on `runEvrynQuery`; the polling-dead guard keys on errors, which a hang never throws). BUILT on `dc/webfetch-timeout` (worktree `evryn-backend-webfetch`, `bf23bca`) — an AbortController wall-clock cap. **⚠️ It's on a stale base (~23 behind `v0.2.8`) → REBASE onto `main` FIRST.** Then: **(a)** the DC fix-trip for QC's **NB-1** (upper-bound footgun: a huge `EVRYN_QUERY_TIMEOUT_MS` → Node coerces to 1ms → every query aborts; bound both ends) + **NB-2** (below / §3); **(b)** YOUR live-path hang exercise (breaker off, real model, a hanging host — count EPIPEs); **(c)** the ADR; **(d)** the `EVRYN_QUERY_TIMEOUT_MS` operator-guide row. **The full spec, QC verdict, findings, and DC-resume handle are in `docs/working/2026.07.16-ac2-dc-webfetch-timeout-brief.md` (§7) — KEEP that doc, it's the WebFetch spec.**
3. **SPRINT 90 — reply-item doctrine (design-call-first).** A `reply`-classification `submit_draft` against a pre-verdict / `queued` forward could flip it out of triage (silently dropping it from the batch) — same silent-drop class as the note-turn collision ③ closed, but the reply case isn't built. **Needs a status-boundary design call: when may a reply attach to a forward item vs. get its own placeholder.** Low reachability pre-Mark. Same file as 91 (`process.ts`) → sequence with it. Spec: `docs/sessions/historical/2026.07/2026.07.14-ac2-step57-dc-build-review.md` (retired 2026-07-23); SPRINT 90.
4. **Step 71 — Reflection binding safety-net.** A runtime guard so consolidation can't **silently** clear a still-live `[binding: until-X]` note that didn't make it into the new story (today it leans on the LLM carrying it; a dropped binding = a broken user promise). Direction: **preserve-the-note** (refuse-to-clear + `notifyDev`, not warn-then-drop). Target `src/reflection/consolidate.ts` / `consolidate_profile` RPC. Reflection is live-inert (no enrolled users) → not urgent; lands cleanly with the WebFetch NB-2 Reflection-budget work. SPRINT 71.
5. **Step 35b + 35d — "she fumbles recognizing her own gatekeeper" (Justin routed to you — you hold the ③ tool-surface/identity interplay best).** **35b (structural):** `buildPersonContext` (`src/triage/classify.ts`) renders the person block with **no** "real record / canonical UUID" signal, so a thin gatekeeper *stub* reads as fake — add a legibility signal (mirror `find_user_by_name`'s `is_stub`). **35d (reasoning — Justin's "most concerning"):** even with the real gatekeeper record loaded straight into her prompt, Evryn has dismissed it as "the test account" — a **Mira reasoning beat** (her doc, coordinate): *a record the runtime hands you IS the real person; don't second-guess your own loaded context as a test artifact.* The two pair. SPRINT 35.

---

## §3 — THE WebFetch DESIGN QUESTION (Justin's — recommend-first, THINK don't rubber-stamp)

Justin pushed on the fix's fundamental shape; **his instinct may be a better design than the shipped flat whole-query cap.** Evaluate before you finalize WebFetch:

- **(a) Per-TURN / inter-message-silence watchdog instead of a flat whole-query cap.** A flat cap bites *legit* long multi-turn triages (Gate-A fixtures took ~1-2 min with real research; a deep multi-search isn't far from 3). A per-tool WebFetch timeout is *not* achievable (no SDK knob) — but a **per-turn watchdog IS:** at the `for await (message of query())` level, **reset the abort timer every time the loop yields a message; abort only if no message arrives for N minutes.** A hung tool blocks the yield → silence → caught; a long-but-*progressing* query keeps resetting → never falsely aborted. **Bonus: this largely dissolves NB-2** (a steadily-generating Reflection keeps yielding → won't abort → may make a separate Reflection budget unnecessary). v0.2 trade to name for him: a slow-but-alive 20-turn query occupies the single-threaded loop a while (no concurrency at v0.2) — acceptable at one-gatekeeper scale; the real answer at scale is concurrency, not a tighter cap.
- **(b) Don't just fail-and-stop — note, retry, and on a REPEAT of the same failure escalate/switch, not loop.** A deterministic hang (permanently dead domain) re-hangs every retry → an abort-retry burn loop (same ratchet shape as NB-2). Design the abort path to distinguish "transient → retry" from "deterministic → escalate to the operator."
- **NB-2 detail:** the flat cap contradicts `reflection.md`'s verbatim *"Soft length — never a hard cap … it never cuts it,"* AND ratchets (timeout → nothing written → notes grow → next attempt longer → burns an Opus call hourly forever). Justin RULED: give Reflection its own larger budget (per-call `timeoutMsOverride`; AC0 rec 600s starting) — **but resolve it TOGETHER with (a), which may dissolve it. Do NOT raise the *global* cap** (re-weakens the wedge protection). Note: `checkReflectionConsolidation` runs *inside* the poll-loop checker block, so a hung Reflection query wedges the loop too → Reflection needs *a* cap (large), not none.

**Your call as lane owner** — if you land on the flat-cap-plus-override as simpler-and-good-enough-for-v0.2 with the watchdog as a v0.3 refinement, that's legitimate; bounce your reasoning back to Justin recommend-first. He wants it *thought about*.

---

## §4 — HELD Gate-A DEV STATE (🔒 DO NOT WIPE yet)

The Gate-A DEV state is the WebFetch fix's **live-path evidence — do NOT wipe until the WebFetch live-path QC is done:** the **Megan** gatekeeper (`43a68cfa-d6c6-4088-b86b-38a5d6583669`, `systemtest@evryn.ai`) + the **frozen a12 `processing` row** (the hang preserved in amber) + the 4 untracked `tests/_gate-a-*.ts` on backend `main`. **Run the WebFetch live-path exercise on the `evryn-backend-webfetch` worktree** (it carries its own DEV `.env`; `EVRYN_ENV=dev`, breaker off, real model — against the DEV DB that holds this evidence). **After** the WebFetch live-path exercise, wipe in ONE pass: Megan + the run's items/leads/`llm_usage` rows + the ~60 `Cluster Test Gatekeeper` debris rows (`cl-gk-…@evryn-test.invalid`) + the ephemeral `_gate-a-*.ts` scripts.

---

## §5 — FINISH THE ③ #LOCK (the remaining doc-flips — ALL need Justin's SCM vet; ARCH/BUILD need his prior edit-auth)

Done already: deploy-log `v0.2.8`, ADR-053 (key-split), CHANGELOG, current-state product-half (AC0-26). **Remaining:**
1. **SPRINT `SPRINT-V0.2-HARDENING.md` — flip Step 57 → DONE** (sprint expects you to do this at #lock, then hand the clean doc to AC0).
2. **ARCHITECTURE ③-absorption (biggest):** flip ③ designed/in-flight → shipped across *emailmgr-lifecycle*, *original-sender-reification*, *profile_jsonb-write-mechanism*, *Security→Bulkhead*.
3. **operator-guide** — add `correct_user_field`; update the spend-ceiling note to the Evryn-Production workspace limit.
4. **BUILD-EVRYN-MVP** — flip ③'s "🚧 in flight" markers → shipped.
5. **cross-runtime raw-write-surface check** — ③ removed Evryn's raw `supabase_upsert` write door; route "does `evryn-team-runtime` have an analogous raw-write surface?" to **ACf/Soren** (a flag, not a heavy spin).
6. Founding-team current-state note (short ③-shipped line) · retire ③'s settled docs (**DONE 2026-07-23** → `docs/sessions/historical/2026.07/`: the old cost-lane brief, the step57-build-review, the key-split runbook. The ③ **design doc is PERMANENT** — moved to `evryn-team-workspace/shared/projects/product/v0.2/`, NOT retired; ADR-051 is permanent) · Linear · **archival `pg_dump` ~owed (>1wk).**

---

## §6 — DISCIPLINE + POINTERS

- Full rules: `_evryn-meta/CLAUDE.md`. Verify branch before every edit; QC every real code change (live-path exercise before a GO on an interactive pathway); spin DC/QC per `docs/protocols/ac-orchestration-protocol.md` (verbatim briefs, load-gate, model-pin, verify receipts); **never resume QC for an independence review** (spin fresh); `#lock` per `lock-protocol.md`.
- **Deploy method** (each fast-follow rides its own small deploy): `docs/deploy-log.md` + the recipe in the retired cost-lane brief's §3 (git history). ③ took ZERO migration; a migration needs dev-first + pre/post `pg_dump` (`backups/README.md`, DB URLs by name, never echo).
- **KEEP (in `docs/working/`):** the WebFetch spec `2026.07.16-ac2-dc-webfetch-timeout-brief.md` (cited in §2.2). **PERMANENT:** ADR-051 (③), ADR-053 (key-split), + the ③ design doc — **now at `evryn-team-workspace/shared/projects/product/v0.2/2026.07.14-ac2-step57-runtime-bookkeeping-design.md`** (moved out of `/working` 2026-07-23; a permanent doc doesn't belong in the transient working dir). **RETIRED 2026-07-23 → `docs/sessions/historical/2026.07/`:** the old cost-lane brief, the step57-build-review, the key-split runbook — deep provenance is in git history + CHANGELOG `v0.2.8`.
- **Report to AC0** for gating (merges, cross-lane collisions, the Gate-B sequence). AC0 owns Gate-B gating (a fresh dedicated reviewer + AC3 staging support).

---

## §7 — AC2 RUNNING LOG (appended 2026-07-23 by AC2 — shared record for next-AC2 + AC0)

> Rides §1–§6 above — don't re-read the whole brief to orient; those still stand. This is *what's happened since* + who owes what next.

**✅ SHIPPED this session — SPRINT 91 (the §2.1 Mark-blocker):** designed → DC build → independent fresh-QC **GO / zero blockers** → **DEPLOYED `v0.2.9`**, verified clean boot on prod (Supabase/Slack/polling/health; catch-up deduped correctly). Fix = one shared **`resolveInboundSender`** helper both `processEmail` + `resumeParkedItem` call (so a third path can't drift back into the phantom-lead bug) + escalate-on-resume anomaly handling (a de-registered-lane parked item → transition the EXISTING item to `escalated`, never blind triage). ZERO migration, code-only (`src/email/process.ts` + `tests/test-resolve-inbound-sender.ts`). QC ruled no live-path model exercise required (deterministic pre-triage; the interactive triage is untouched). Full detail: `evryn-backend/CHANGELOG.md` 2026-07-23 + `_evryn-meta/docs/deploy-log.md` `v0.2.9`.

**Live state:** prod = **`v0.2.9`** (`evryn-backend` `main` / tag = `6886417`). Clustering + Haiku pre-screen still dormant.

**⚠️ PUSHES PENDING (Justin/AC0 coordinate — nothing lost, just unpushed):** `evryn-backend` main is +3 vs `origin` — `6886417` (Step 91 fix) · `e57da97` (another agent's changelog-archive merge, docs-only) · `0c498ee` (my `v0.2.9` CHANGELOG) — **+ the `v0.2.9` git tag**; `_evryn-meta` is +1 (`b81f1e8`, deploy-log).

**→ FOR AC0:**
- **Sprint flips are yours** — please flip in `evryn-backend/docs/SPRINT-V0.2-HARDENING.md`: **Step 57 → DONE** (③, shipped `v0.2.8`) and **Step 91 → DONE** (shipped `v0.2.9` this session). Then note here in §7 that you did them.
- **Log a pre-existing non-blocker:** a double-ping on a resume-time transient DB error — `resumeParkedItem`'s resolution catch fires its own `notifyDev` AND rethrows, so poll.ts's rate-limited transient alert also fires (`processEmail` was deliberately de-duped, ITEM 3 FIX B). Untouched by Step 91, rare, useful extra context — a future de-dup pass, not urgent.

**→ #lock progress (this-AC2 chipping at the EASY pieces before running out of room — status here is authoritative; also check `git status` for my uncommitted in-flight edits):**
- ☑ **BUILD-EVRYN-MVP S1 flip** — **DONE (uncommitted, pending Justin's SCM vet):** ~L609 flipped from *"removing the raw `supabase_upsert` write surface (not yet built)"* → shipped `v0.2.8`/③ (write surface REMOVED; correction survives only as `correct_user_field`).
- ☑ **Concurrency note** — **DONE (uncommitted, pending vet):** added a ⚠️-flag to `BUILD-EVRYN-v0.3.md` § "Sender Outreach + Phase I Backlog" — Evryn is strictly one-at-a-time (`poll.ts`), concurrency NOT in the v0.3 plan, v0.3 scaling MUST add parallel processing (~6,000 cast-offs linearly = non-starter; distinct from outbound rate-limiting).
- ☑ **operator-guide** — **DONE (uncommitted, pending vet):** added `correct_user_field` to the "Operator Mode (Slack)" abilities list (edits only display_name/full_name/email/outbound_address, nothing else) + updated the spend-ceiling note (~L159) to the dedicated `Evryn-Production` Anthropic workspace (ADR-053).
- ☐ **ARCHITECTURE ③-absorption (the biggest — LEFT FOR THE RESTED RE-SPIN):** flip ③ "designed-not-started" → shipped in the **emailmgr Status Lifecycle** callout (~L256 / L282), the **profile_jsonb write-mechanism** (~L289 — `supabase_upsert` is REMOVED; it still says *"prefer `create_user` over `supabase_upsert`"*), and **Security → Bulkhead Layers** (~L1081–1089 — add the `supabase_upsert` write-surface removal + `correct_user_field`, the write-side sibling of the read-lock).
- ☐ **Cross-runtime raw-write flag** → route to **ACf/Soren** (does `evryn-team-runtime` have an analogous raw-write surface ③ would remove? A flag, not a spin).
- **ALL #lock edits need Justin's SCM vet; ARCH/BUILD need his prior edit-auth (granted for this batch). My edits sit UNCOMMITTED pending his vet.**

**→ NEXT-AC2 fast-follow queue (unchanged, §2):** WebFetch next (⚠️ rebase the stale `dc/webfetch-timeout` branch onto `main` FIRST; §3 design call — Justin steered **(a) the per-turn "silence" watchdog** — work it up recommend-first before building), then SPRINT 90 (same file `process.ts`), Step 71, Step 35b/35d. **Held Gate-A DEV state 🔒 DO NOT WIPE (§4).** A Step-91 pre-Mark DEV park→resume confidence-run is *recommended* (fold into Gate-B) — NOT a ship gate.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
