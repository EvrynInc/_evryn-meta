# Lane C (cost) — round-2 handoff to AC0

> **Truncation check:** the last line should read `FULL FILE LOADED`. If you don't see it, reload.

> **How to use this file:** AC3a's high-res handoff for AC0's convergence of the round-2 v0.2-hardening wave. It is the complete record of what Lane C (cost) shipped, where it lives, the convergence wiring AC0 must do, the gates that must hold before enablement, and the tracked deferred items. Read it fully before merging `r2/lane-c-cost`. Companion low-res (CEO-altitude) went to Justin in chat.

*Authored 2026-06-23 by AC3a.*

---

## 1. Status — one line

**Lane C is QC-GO + AC-verified + live-tested.** Code committed on `r2/lane-c-cost`. **Nothing merged to `main`, nothing deployed.** Reflection is **live-inert** (runs on zero real users) until Step 66 enables enrollment — by design.

---

## 2. What shipped (tied to SPRINT steps)

- **Step 10 — LEAN Reflection (consolidation).** A periodic per-user cron consolidates a user's `pending_notes` into a re-synthesized `story`, archives the raw to a new `story_versions` table, clears exactly the consolidated notes. Files (all in the `evryn-backend-cost-r2` worktree / branch `r2/lane-c-cost`):
  - NEW `src/reflection/consolidate.ts` — `consolidateUserProfile` (deps-injectable) + pure `validateConsolidatedStory` / `isOversized` / `buildConsolidationPrompt` + env-tunable constants.
  - NEW `backups/add-story-versions-and-consolidate-profile.sql` — the `story_versions` table + the atomic race-safe `consolidate_profile(uuid,text,integer)` plpgsql RPC.
  - `src/triage/classify.ts` — excluded `reflection.md` from `loadCommonPrefix` (so it's $0 on normal queries) + added `loadReflectionPrefix()` (core.md + reflection.md only).
  - `src/db/users.ts` — `consolidateProfile()` RPC wrapper (load-bearing, throws).
  - `src/email/poll.ts` — `checkReflectionConsolidation` cron (additive; hourly-gated; per-user-isolated).
  - `src/db/llm-usage.ts` — `checkReflectionConsolidation → "reflection"` in `PATHWAY_ACTIVITY`.
  - NEW `identity/activities/reflection.md` — **Mira's voice-pass** (started as my placeholder; her rewrite is committed — see §4).
  - Tests: `tests/test-reflection-consolidate.ts` (pure), `tests/test-consolidate-profile-dev.sql` (dev integration), `tests/test-cache-byte-identity.ts` (Step 11b).
  - Decision record: **ADR-043** (lean Reflection — already on `main`, see §6).
- **Step 11b — cache byte-identity guard.** `tests/test-cache-byte-identity.ts` pins that `loadCommonPrefix()` is byte-identical across calls AND that `reflection.md` is absent from it (regression guard for the exclusion).
- **Step 11a — MOOT/dropped** (clustering keeps cache warm by volume; and the "1h-TTL flip" doesn't exist as a one-liner — see Step 59).
- **Step 11c — DONE** (the measurement PLAN: `_evryn-meta/docs/working/2026.06.17-ac3-cache-measurement-plan.md`). The measurement RUN is now tracked as **Step 69** (§7).
- **Step 12 — DEFERRED** (subsumed by Step 57).
- **Step 59 — ANSWERED** (the cache-partitioning SDK check): the Claude Agent SDK exposes **no** cache-breakpoint control (issue #89). Cache "chunking" is only achievable by *placement* (systemPrompt vs. user message) — that's **ADR-044 / Step 70**, gated on the Step-69 measurement.

---

## 3. Branch + commit state

Branch `r2/lane-c-cost` (worktree `evryn-backend-cost-r2`), off `main`:
- `6b60abc` mira: reflection.md voice-pass + truncation canaries
- `c4dea42` mira: reflection.md revision + core.md note-voice + binding reconciliation
- `5f4972c` Lane C (Step 10 + 11b): lean Reflection consolidation + cache byte-identity guard (DC's code, AC-verified)

So the lane branch carries **both** Mira's identity work **and** the Lane C code, all committed. Ready to merge.

---

## 4. Mira's identity changes — reviewed, but note the blast radius

Mira's voice-pass landed in the worktree and is **committed on the lane branch** (not `main` — so not live). It is **more than `reflection.md`**:
- `identity/activities/reflection.md` — full voiced rewrite (preserves all the mechanical constraints QC confirmed).
- `identity/core.md` — **broad-blast-radius change** (core.md is force-loaded on *every* query): a **note-voice beat** ("write your notes/story in your own first-person voice") that governs ALL note-writing, plus a **binding reconciliation** (a still-live `[binding]` is now carried to the *foot* of the story during consolidation, and honored from notes OR the story foot).
- `identity/core.md` / `activities/triage.md` / `situations/gatekeeper.md` — truncation canaries added (the 3 that were missing).

**I ran the identity-file-review checklist (all 7 items pass)** — no real-user info (she used `[User A]`/`[User B]`), runtime-matched, voice-consistent, cross-refs intact, the append-vs-overwrite + binding contradictions reconciled coherently, matches Justin's intent. **Transparency:** I co-shaped `reflection.md`'s requirements (the brief), so for full independence Justin was offered a fresh-AC read on it; the core.md changes I did not shape. **Deploy note:** identity-file changes trigger a Railway redeploy and follow `identity-file-review.md` — coordinate the deploy so the identity layer + the runtime land together.

---

## 5. CONVERGENCE WIRING — what AC0 must do

1. **Point Reflection at Lane A's shared helper.** `checkReflectionConsolidation` (`poll.ts`) currently enrolls via `findUsersByStatus(["active"])` with a `// TODO(convergence, Step 66)`. **AC1a is building `findCareEligibleUsers()` in `src/db/users.ts`** (Lane A) — status `active` AND role not in (system, admin), the dead `'gatekeeper'` token removed. At convergence: **drop my placeholder + TODO, call `findCareEligibleUsers()`.** Same population, defined once. (AC1a confirmed this directly + is documenting it in his handoff.) If Reflection-eligibility ever needs to differ from proactive, AC1a said add a param, not fork.
2. **Apply the migration to PROD** at the convergence deploy: `backups/add-story-versions-and-consolidate-profile.sql` (dev-first method in `backups/README.md`; pre/post `pg_dump`). **It's already applied + verified on DEV** (residue dropped, clean apply, integration test passed — §8). The migration does NOT self-drop the round-1 dev residue (AC owns that; done).
3. **Honor the gates before enablement** (§9).

---

## 6. DOCS ALREADY COMMITTED TO `main` (not the lane) — read this

Per Justin's authorization (2026-06-23), I committed these **source-of-truth doc edits directly to canonical `main`**, NOT the lane branch — deliberately, to keep shared docs from being forked across five lane branches (the lane brief routes ARCH/BUILD edits through AC0 for exactly this reason; Justin had me write them, on `main`, to stop the staleness misleading people *now*). **So do NOT expect these in the lane convergence — they're already on `main`:**
- `evryn-backend` (`main`): ARCH + BUILD "story-is-written-in-v0.2" staleness fix; SPRINT Steps **69, 70** (cache experiment + restructure) and **71, 72** (Reflection/DB hardening).
- `_evryn-meta` (`main`): **ADR-043** (lean Reflection) + **ADR-044** (epithelial prompt architecture). Provisional numbers — reconcile if a parallel lane collided.
- `evryn-quality` (`main`): QC mailbox cleared both directions.

**Collision watch:** only if another lane edited the *same* ARCH story/Reflection sections or the sprint tail. The sprint steps are new numbers (additive). The ARCH/BUILD edits are confined to the story/Reflection paragraphs.

**ARCH updates still OWED at convergence (not done — in-flight, not on main yet):** (a) ARCH's binding subclass section is still v0.3-framed; it should gain a v0.2 note that lean Reflection carries a still-live `[binding]` to the **story foot** during consolidation, and that `core.md` now honors a binding from `pending_notes` OR the story foot (Mira's reconciliation, this lane). (b) ARCH's Data Model + Memory Architecture want the new `story_versions` table + the consolidation pathway documented when it ships (the staleness fix I made only corrected the "story-is-empty-in-v0.2" lines; the table/pathway documentation is the convergence-time addition). ADR-043 is the decision record to anchor both against.

---

## 7. Tracked deferred items (all have SPRINT steps)

- **Step 71 — binding structural safety-net** (Mira flag): consolidation must not silently clear a note bearing a live `[binding]` that didn't make the new story. Pre-enablement. [AC design → DC+QC]
- **Step 72 — `search_path` pin** on all 3 SECURITY DEFINER RPCs (`consolidate_profile` + `append_pending_note` + `append_cross_user_note` — all share the gap, not introduced here) + the `_meta` robustness tweak. Pre-enablement. Low risk at v0.2. [DC+QC]
- **Step 69 — week-one cache-temperature measurement RUN** (high priority; headline verdict by ~day 2–3 of Mark traffic; **leave `pending_notes` IN the systemPrompt** for the measurement). [AC analysis]
- **Step 70 — epithelial prompt restructure** (ADR-044; measurement-gated fast-follow; co-design with Step 57). [AC design → DC+QC + Mira]
- **v0.3 soft-edge** (not a v0.2 step): the validation guard doesn't catch a plausible-length-but-wrong-content LLM output (recoverable via the archive at v0.2). Harden at v0.3.
- **Clustering preferences for the Step-58 owner** (NOT Lane C): Justin leans twice-daily (~10am/~4:30pm) + manual-trigger the first few. Route to whoever owns Step 58.
- **Cache-measurement doc update — DONE** (`_evryn-meta/docs/working/2026.06.17-ac3-cache-measurement-plan.md`): folded in the SDK-no-breakpoints finding (#89) + the ADR-044/Step-69/70 breadcrumbs.
- **`recordLlmUsage` on errored queries** (Lane C audit item from the brief): a query that throws mid-stream doesn't record its spend — *deliberately* kept out of the error path so it can't compete with the billing alert (the live test confirmed it's best-effort: a malformed-UUID write logged-and-swallowed without breaking the run). Fuller error-path accounting belongs to **Step 57** (runtime-bookkeeping). **NOT done in Lane C, by decision** (folding it in conflicts with the billing-alert priority). Low impact at Mark-scale.

---

## 8. Test + verification status

- `npx tsc --noEmit` — **clean** (AC re-ran).
- Pure suites (`test-reflection-consolidate.ts`, `test-cache-byte-identity.ts`) — **all pass** (AC re-ran), including the race-safety assertion (a note appended mid-run survives the clear; N stays the pre-call count).
- Dev integration test (`test-consolidate-profile-dev.sql`) — **passed on real Postgres**: archive created, story overwritten, exactly-first-N cleared, mid-run note survives, missing-user raises, rolls back. Migration was drop-then-applied to DEV first (round-1 residue dropped, clean apply verified: RLS on, `service_role`-only grants, anon/authenticated revoked).
- **Live real-Opus consolidation** (in-memory fake user, persist stubbed so no DB write) against **Mira's** `core.md` + `reflection.md` — **strong pass**: in Evryn's first-person voice, found the through-line, carried the `[binding]` intact to the story foot, held cross-user provenance, carried uncertainty as uncertainty, ~2,900 chars (on the felt target), N captured correctly, no false alerts. ~$0.35 cold. (QC also reviewed independently — GO, full 37-file load including the BUILD section DC had skipped + live DEV queries.)

---

## 9. Gates before Reflection runs against a real populated profile (sequence matters)

1. **Mira's `reflection.md` voice-pass — DONE** (committed + live-verified). This gate half is **closed inside the lane.**
2. **Step 66 enablement** (Lane A + AC0 convergence wiring, §5.1). Once enrollment is live, **re-verify race-safety + the cost win on a genuinely-enrolled, genuinely-bloated profile** — today's tests exercise the logic but no real user has flowed through the cron (live-inert).
3. **Step 71 binding guard + Step 72 search_path** should land before enablement (no rush — inert today).

---

## 10. Cross-lane seams (confirm at merge)

- `src/email/poll.ts` — Lane A owns it; Lane C added ONE additive checker (`checkReflectionConsolidation`), no existing checker touched. Additive merge.
- `src/triage/classify.ts` — Lane B owns the MCP-tools region; Lane C touched only `loadCommonPrefix` + the new `loadReflectionPrefix`. No overlap.
- `src/db/users.ts` — Lane A adds `findCareEligibleUsers()` (+ the enrollment fix); Lane C added `consolidateProfile()`. Additive; wire §5.1 at merge.
- `identity/core.md` — Mira's broad change rides this lane (force-loaded). Identity-file-review done (§4); coordinate the deploy.

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
