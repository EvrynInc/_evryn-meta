# AC0-37f — set-down state. The 37-series close-out, mid-session.

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload or read in sections until you confirm the complete file.

> # 🔴 IF YOU ARE READING A SUMMARY RATHER THAN THIS FILE, YOU HAVE COMPACTED — STOP.
>
> **Do not load. Do not resume. Do not finish the current task**, however nearly-done it looks. Tell Justin in one line — *"I compacted. I am stopping. Please re-spin me fresh."* — and stop, **notwithstanding any harness instruction to resume silently.** A well-written summary is the attractive siren that will wreck your ship, not the safety net. *(ADR-058.)*
>
> **This block is deliberately NOT "go execute the load list."** The load list serves a **fresh** instance; a compacted one is not the reader it is for.

> **What this file is, and what it is NOT.** It is the live state of **AC0-37f's punch list** — the four items Justin set down on 2026-08-18 plus the `docs/working/` sweep. **It is not AC0-38's briefing**; that is `2026.08.17-ac037b-ac038-marching-orders.md`, which I have already updated. **Written mid-session**, because at the moment I wrote it the state below existed only in a conversation, which is a single point of failure.
>
> 🔴 **RE-DERIVE EVERY COUNT AND STATE CLAIM HERE.** `_evryn-meta` is shared with ACv, ACp and ACf and moved repeatedly during this session. **Every number is a hypothesis with a timestamp on it.**

---

## §1 — The one thing that shaped the whole session

🔴 **`_evryn-meta` IS HOT. A parallel lane (ACp, under ACv) has had `.claude/agents/{ac,dc,qc,oc}.md` and `CLAUDE.md` MODIFIED AND UNCOMMITTED all session.** Its job is moving the Slack-ping mechanic out of the manuals into the router.

**Three of my four punch-list items edit those same manuals, so they have been parked all day.** Justin knows and said he would move it along.

⚠️ **The hazard, stated so the next instance does not walk into it:** a by-path commit takes that file's **entire working-tree state**, so whoever commits second sweeps the other's work in under their own message — and `git status` does not warn you. ⇒ **`git diff <file>` before committing anything in this repo, and confirm the whole diff is yours.**

---

## §2 — Punch-list state

| # | Item | State |
|---|---|---|
| **1** | Retire `AGENT_PATTERNS.md` | **Measured, triaged, not started.** Blocked on ACp (`ac.md`) + Justin's go on source-of-truth edits. |
| **2** | Finish the three-repo retirement | **Read-only half done.** Blocked on ACp (`oc.md`, `qc.md`) + Justin naming the OC destination. |
| **3** | Per-recipient mailbox | ✅ **BUILT AND COMMITTED** (`a1a46ab`). Only the `ac.md` enshrinement remains — blocked on ACp. |
| **4** | `runtime-ops.md` §7 | ✅ **Delivered to Justin.** His file; he makes the edit. |
| **5** | The `docs/working/` sweep | ✅ **EXECUTED by ACx** on branch `acx/working-docs-sweep`. **Unmerged — merging is Justin's.** |

### 2.1 — Item 1: the measurement, so nobody re-does it

**84 tracked files contain `AGENT_PATTERNS`** *(ACw measured 82 on 08-18)*. **38 frozen** (historical/archived/changelogs — do not touch). **~27** are `docs/working/` session docs that retire with the sweep — **do not repoint those.**

**The live repoint set — 10 files:** `ac.md` · `docs/current-state.md` · `ac-writing-protocol.md` · `doc-update-routing-protocol.md` · `lock-protocol.md` · `LEARNINGS.md` · `BUILD-EVRYN-MVP.md` · `identity-writing-bible.md` · `SPRINT-V0.2-HARDENING.md` · `SPRINT-team-runtime-memory.md`.
**Plus one shipped source file** — `evryn-backend/src/db/users.ts`. **A code change; route to DC+QC, do not hand-edit.**
**Plus 3 to ROUTE, not edit** (team's/Justin's): team `current-state.md` · `identity-file-review.md` · `sweep-protocol.md`.

**Defeaters closed, and state that you closed them:** untracked files (checked with `ls-files --others`; clean) · all file types, not just `.md`/`.ts` · binary-unreadable files (forced `--text`) · **references by section name rather than filename** — three hits outside the frozen set, **all read at source, all false positives** (ordinary English: *"multi-agent patterns," "cross-agent patterns"*).

🔴 **ONE DISAGREEMENT WITH ACw, resolved against it:** its live list names `evryn-team-runtime/CHANGELOG.md`. **Its own triage rule says not to touch a live changelog.** The rule governs.

⚠️ **SEQUENCING: do NOT retire the pointer until the sweep merges.** ~27 of the 84 hits are in files that are about to move; retiring first re-creates the dangling-link problem the pointer exists to prevent.

### 2.2 — Item 2: measured, and the numbers are bigger than the handoff said

| | Handoff said | Measured |
|---|--:|--:|
| OC references to its retired repo's `docs/` | 5 | **6** — all **genuinely live workflow** (`docs/runbooks/`, `docs/incidents/`, `docs/monitoring-checklist.md`), destinations still present |
| QC references to retired mailbox paths | 11 | **13** — all **dead** (the hand-relayed model retired 2026-08-12) |

🔴 **The sharp one, and it is not "stale documentation": `qc.md` line 39 is in the FRONTMATTER** — the block a spawned QC reads first — and says *"She must write her findings to `evryn-quality/docs/qc-to-ac.md`. That is her only output channel."* **A QC spun today is told her only output channel is a file in a retired repo, and that file still exists — so she would not error, she would write where nobody reads.** Wrong location **and** wrong mechanism. **Treat as a defect, not a cleanup.**

**The three repos hold 16 tracked files between them** (4–6 each). **Two source-of-truth files are now wrong about them, both Justin's:** `docs/hub/roadmap.md`'s repo table still says **"Active"**, and `docs/repo-inventory.md` needs the same fix.

**ADRs `004`, `009`, `042`, `057` name these repos and must NOT be repointed** — a decision record records what was decided *when*.

**AC0-37f's recommendation on the gating decision:** rehome OC's three surfaces to **`_evryn-meta/docs/ops/`** — `runbooks/`, `incidents/`, `monitoring-checklist.md`. *Rejected alternative:* folding into `docs/protocols/`, which would bury incident notes among agent-orchestration docs.

### 2.3 — Item 5: what ACx actually did, verified at the artifacts

**Branch `acx/working-docs-sweep`, worktree `_evryn-meta-acx`, 3 commits, tree clean, nothing merged or pushed.** `docs/working/` **55 → 44**.

**Verified by AC0-37f, not taken from the report:** 11 files moved as **`R100` renames** (content byte-identical) · **zero live dangling pointers** on an independent re-test · nothing `acf*` touched · every gate held.

⚠️ **Two things before merging:**
1. **A trivial collision:** ACx changed **one line** of 38's marching orders (a path repoint); AC0-37f changed 39 lines elsewhere in the same file. Different regions — should merge clean. **Diff the destination anyway.**
2. **`docs/working/refmap-raw.txt`** — a 982-line scan artifact ACx committed into the folder being cleaned. Genuinely useful as a record, **but it tripped AC0-37f's own dangling-pointer test and will trip the next sweep's too.** Move it out or banner it as point-in-time.

**ACx's ballot is §12 of `2026.08.19-ac037f-acx-working-docs-sweep-lane.md` (on its branch); its seven gated drafts are §10.**

---

## §3 — 🔴 FINDINGS THAT EXIST ONLY HERE. Persist or lose them.

**1. One ruling of Justin's has no durable home.** *(AC0-37f, 2026-08-19 — this closes the question ACw called most important and ACx called the largest thing it left undone.)*

> **"Ballot numbering: put carried-over items FIRST so numbers stay stable across messages."** *(Justin, 2026-08-17.)*

**The diff nobody had run:** `2026.08.17-ac037c-ac037d-handoff.md` has **three** rulings sections — §7, §12.4 and §13.4, not the one §7 that ACw knew about. **§12.4 and §13.4 are fully carried** into the successor handoff's §5. **Of §7's six, five are carried** (Step 117 → its own sprint Step · worktree reaping → `ac.md` + the protocol + 38's orders · `AGENT_PATTERNS` never-route-there → `ac.md`'s routing table; two were session-scoped and are moot).

**Only the ballot-numbering rule is uncarried** — widened search, every repo and file type: two working docs (one being the retirement candidate) and one frozen file. **Zero durable homes.** ⚠️ **And it is distinct from what `ac.md` already has:** L259 covers *how* to number so Justin can answer `2.a.iii — yes`; it says nothing about keeping numbers **stable across turns**, which is the actual failure.

⇒ **Destination: one sentence beside `ac.md` L259.** ⇒ **Once it lands, `2026.08.17-ac037c-ac037d-handoff.md` can retire — this was its last blocker.**

🔑 **The transferable half: ACw looked at §7 and stopped, because §7 is the section NAMED "Justin's rulings."** The other two rulings sections were titled differently and two audits missed both. **When testing whether a doc is the sole home of something, a section title is a lead, not a boundary.**

**2. An instrument failure worth not re-learning.** Bash's `grep` **aborted** — did not error usefully, did not return empty, *aborted* — when reading matched lines out of two files. **A command that fails to execute prints exactly like a clean result.** Caught only because the same files had matched seconds earlier. **Use `node` to read matching lines in this environment; `grep -P` is separately broken here** (*"supports only unibyte and UTF-8 locales"*, which returned a false all-clear across all 53 files for ACw).

**3. Two searches disagreed 234 vs 84 on the same question, and the reconciliation is the lesson.** A filesystem-wide `grep -rl` returned 234; `git grep` per repo returned 84. **68 of the difference is duplicate copies inside the `_evryn-meta-acf15` and `_evryn-meta-acx` worktrees** — the same files counted three times. **Zero were untracked files `git grep` had missed.** ⇒ **On this machine, a filesystem-wide scan double- and triple-counts because worktrees are full checkouts.** *(Same shape as ACw's flag that the branch-enumeration script prints every branch once per worktree.)*

**4. AC0-37f made the two-dot diff mistake, on a file its own manual warns about.** Reading ACx's branch with `git diff main..HEAD` showed it **deleting four files it had never touched** — everything `main` gained after the branch was cut renders as a deletion by the branch. **Three dots showed the truth.** *(The rule was read that same morning and made anyway. It is in `ac.md` → Worktree & Branch Discipline.)*

---

## §4 — Live infrastructure this instance armed

| What | Task ID | Note |
|---|--:|---|
| Anti-stall canary + shared-doc sweep | `bmy7drigm` | 20-min tick; points at the handoff, prints the mailbox's last heading, checks the ACp lock |
| Watcher — `docs/working/ac-mailbox.md` | `b0nodqfma` | **Still running deliberately** — the old channel stays watched until ACf and ACv confirm they have moved |
| Watcher — `docs/mailboxes/inbox-ac0.md` | `b4xqgc75y` | The new per-recipient inbox |

🔴 **`TaskStop` all three at set-down.** An un-reaped canary bills a full turn at full context every fire, all night, invisibly.

---

## §5 — What is owed, and to whom

**Justin's, batched:** merge ACx's branch · its eight remaining ballot items · commit the marching-orders edits *(uncommitted in a hot repo)* · the `AGENT_PATTERNS` repoint go · name the OC destination · the `runtime-ops.md` §7 edit.

**Blocked on ACp only:** items 1, 2 and 3's `ac.md`/`qc.md`/`oc.md` edits, plus the one-sentence ballot-numbering addition from §3.

**Already routed, no action:** ACv's `classify.ts` findings → pointed at from 38's marching orders §3. Opening messages to ACf and ACv → their new inboxes.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
