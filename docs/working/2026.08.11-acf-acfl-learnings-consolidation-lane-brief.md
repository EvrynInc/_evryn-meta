# Lane brief — `ACfl`: collapse `AGENT_PATTERNS.md` into `LEARNINGS.md`

> **📌 NOTE ADDED 2026-08-19 by AC0-37g — `AGENT_PATTERNS.md` NO LONGER EXISTS, and this document refers to it.** It was collapsed into `_evryn-meta/LEARNINGS.md` on 2026-08-12 and **retired outright on 2026-08-19** (both Justin's rulings). Its entries live in `LEARNINGS.md`'s Unpromoted section under *"Absorbed from `AGENT_PATTERNS.md`"*; the retired file itself is at `_evryn-meta/docs/historical/AGENT_PATTERNS.md`. **Never route a new learning there.**
>
> ⚠️ **The references in the body below were deliberately NOT edited — this banner is the correction.** This is a point-in-time document, and rewriting its text would falsify what was true when it was written. *(Justin's instruction, 2026-08-19: a note in each one, because we do not yet know which of these docs will be retired. Banner applied to this ACT-lineage file with ACf-15's explicit authorization, 2026-08-19T19:00 — banner only, body untouched.)*

*Written 2026-08-11 by **ACf-12** for a lane to be spun by **ACf-13**. Pure `_evryn-meta` documentation work. **No code. No merge.***

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`.
>
> **How to use this file.** The lane's shared surface — read in full, append as you work, **keep it current enough to re-spin from cold.** Commit freely; brief-class traffic is pre-authorized. The tagged blocks are pasted verbatim by whoever spins the lane; the file list is in the ACf-13 handoff.

---

## §0 — 🔴 How much the author of this brief had read

**ACf-12 wrote this having read `LEARNINGS.md` in full and only `AGENT_PATTERNS.md`'s headings.** It has **not** read the 738-line body you are about to work through, and it has **not** read the product-side manual. **Treat every claim here as a LEAD.** Where this brief is silent about what is *in* `AGENT_PATTERNS`, that is ignorance, not clearance.

---

## §1 — The two files, and why they are being merged

- **`LEARNINGS.md` (146 lines)** is a **queue**. Its own header: *"Temporary holding pen. Learnings land here first, but nothing should stay."* Entries get promoted to where they will actually be read and enacted — a CLAUDE.md, a spoke, a protocol, an ADR — and collapse to a one-line stub. It has a **Promoted Stubs** section (56 numbered entries) and an **Unpromoted** section.
- **`AGENT_PATTERNS.md` (738 lines)** is a **browsable library**, organised into sixteen domains. Its header: *"This file is not loaded by anyone during builds — this is just a message board so that others can learn, cross-repo."*

**Justin's ruling (2026-08-11), verbatim, because his reasoning is the load-bearing part:**

> *"Your plan here describes learnings.md almost to a T. I say we collapse agent_patterns into learnings. The reason is: the notion of a **fixed destination, decided at the point of recording** misunderstands that there are likely **several** places these things want to land — that's what learnings is here for."*

**And his answer to whether `LEARNINGS.md` itself is dying — it is not, and the reason shapes your work:**

> *"at the #lock, we need to put things in learnings, and **not have to figure out where all it goes in that moment** — #sweep is the time for that sort of thinking. But the **whole idea** is that learnings is a short-term home."*

⇒ **`LEARNINGS.md` is deliberately a place to record WITHOUT deciding the destination.** That separation — capture under pressure at `#lock`, distribute with fresh context at `#sweep` — is the point of the file. **Preserve it.**

---

## §2 — 🔴 The task, exactly as Justin specified it

**Three buckets. Every `AGENT_PATTERNS` entry lands in one.**

1. **PROMOTED and verifiably reflected → STRIP IT.** *"If there are things in agent_patterns that have been promoted (and we need to see that they are reflected in team-runtime and **also** in the product domain — then we can strip them out of there."*
   ⚠️ **"Reflected" means you went and LOOKED.** A stub saying *"Promoted → X"* is a **claim**; open X and confirm the substance is actually there. **A doc's claim about another doc is not evidence about that doc** — that exact failure cost this org a wrong finding relayed to Justin as verified on 2026-08-07.
2. **NOT promoted, or promoted but NOT verifiable in both domains → MOVE TO `LEARNINGS.md` as a promotion candidate**, in its Unpromoted section.
3. 🔴 **Verified in ONE domain but not the other → MOVE IT, DO NOT STRIP IT — and mark it explicitly** *(e.g. "product-side unverified")*. **Justin's test is BOTH domains. One is not two.** **Do not resolve a gap in your own knowledge by treating it as a pass.**

**Then: `AGENT_PATTERNS.md` stays in place as a POINTER** saying its content has been absorbed and where it went, **so nothing that links to it dangles.** *(Twenty-plus files reference it, most of them historical.)*

---

## §3 — 🔴 Pre-decided constraints

1. **🚫 DO NOT REDESIGN `LEARNINGS.md`'s STRUCTURE.** Keep Promoted Stubs and Unpromoted exactly as they are; absorbed patterns go into Unpromoted as candidates. **Justin is openly unsure whether that file survives long-term, which makes tonight precisely the wrong night to reshape it.** Absorb; do not re-architect.
2. **🔴 NEAR-LOSSLESS IS THE BAR. This is 738 lines and the failure mode is silent deletion.** Do not compress an entry to "tighten" it — **the phrasing may carry nuance a future reader needs**, and the whole point of these entries is the *mechanism* and the *consequence*, not the label.
3. **🔴 VERIFY THE MOVE PROGRAMMATICALLY — do NOT trust a read-through.** The technique is recorded in `LEARNINGS.md` itself and it caught a real drop last time: **extract every high-signal specific (commit SHAs, `ADR-NNN`, `EVR-NNN`, file paths) from the BEFORE and AFTER versions and set-diff old-against-new**, e.g. `comm -23 <(git show HEAD:AGENT_PATTERNS.md | grep -oE 'ADR-[0-9]+' | sort -u) <(grep -oE 'ADR-[0-9]+' LEARNINGS.md | sort -u)`. **Then triage every old-only token as either an intentional de-dup or a genuine drop.** At this scale a visual read cannot establish that every fact survived. **Report the diff result in your findings.**
4. **These are SOURCE-OF-TRUTH docs.** Both require Justin's approval. **Do the work on your branch, commit it there, and LEAVE IT — never merge.** He vets in source control.
5. **You do NOT edit any file you are verifying against** — the team-runtime manuals, the product manual, QC's or DC's manuals. **Read-only outside your two files.**
6. **If you cannot find the product-side manual or cannot verify a domain, SAY SO and use bucket 3.** Do not guess, and do not strip on a maybe.
7. **Anything genuinely ambiguous: BATCH IT** into one numbered recommend-first list. Keep advancing everything not blocked.

---

## §4 — Definition of done

1. Every `AGENT_PATTERNS` entry assigned to bucket 1, 2 or 3, **with the evidence for bucket 1** (which file, and that you opened it).
2. `LEARNINGS.md` carrying the buckets 2 and 3 material, **structure unchanged**, markers on the one-domain entries.
3. `AGENT_PATTERNS.md` reduced to a pointer that will not dangle.
4. **The set-diff run and its result reported** — every old-only token classified as de-dup or drop, with drops re-added.
5. Both files **committed on your branch, unmerged**, for Justin's vet.
6. This brief updated with your bucket counts and anything you routed.

---

## §5 — Notes appended by the lane

*(`ACfl` appends here. Keep it current.)*

### `ACfl` — lane COMPLETE, 2026-08-12T00:08:59-07:00. Branch `acfl/learnings-consolidation`, committed, UNMERGED.

**Bucket counts — all 89 entries accounted for (1 + 6 + 82 = 89):**

| Bucket | Count | What |
|---|--:|---|
| **1 — STRIPPED** | **1** | *"Memory as Narrative GPS, Not Compressed Log."* The only entry of eighty-nine that met Justin's both-domains bar. |
| **3 — MOVED + marked one-domain** | **6** | 5 marked `ONE DOMAIN VERIFIED`; 1 marked *headline-in-both / mechanism-in-neither*. |
| **2 — MOVED** | **82** | Of these, **2** marked `SUPERSEDED — DO NOT PROMOTE` and **3** marked `OVERLAPS an existing entry`. |

**Why bucket 1 is one and not twenty — this is a structural finding, not a shortfall of effort.** `AGENT_PATTERNS` was mostly **terminal**, not a waypoint: **twelve of `LEARNINGS.md`'s own Promoted Stubs name `AGENT_PATTERNS` as their promotion *destination*** (stubs 34, 35, 37, 39, 47, 48, 51, 52, 53, 54, 55, 56). For those, stripping would have destroyed the only copy. Most of the remainder is agent-building design lore with no manual home at all — *"Modular Context Loading / Soul DNA test"* returns **zero** occurrences anywhere outside these two files.

**The bar applied.** "Reflected" was taken to require the **mechanism** surviving in both domains, not just the headline — per Justin's own standing rule that a description restating the label is nearly content-less. Verification was by **opening destination files**, never by trusting a stub's claim about them.

⚠️ **Methodology limit, stated plainly: bucket 2 is the DEFAULT UNDER UNCERTAINTY, not a verified negative.** Grep can prove presence; it cannot prove absence (this file's own entry on the four-different-answers import census is the proof). So positive evidence was used to strip or to mark, and everything else moved. **Under-verification therefore biases toward moving — the recoverable direction.** Do not read an unmarked entry as "no manual carries it."

**Bucket 1 evidence (the single strip).** [ADR-023](../decisions/023-agent-memory-as-narrative-gps.md) exists under that exact name and is its doc of record; `evryn-team-workspace/CLAUDE.md:206` carries Story + Recent Notes; `evryn-backend/identity/activities/reflection.md` carries the same architecture as Evryn's consolidation behaviour. The entry was a three-line summary that itself said *"See ADR-023."* A stub records the strip in place.

**Two entries are now FACTUALLY FALSE and are marked `SUPERSEDED — DO NOT PROMOTE`:**
1. *"Continuing a finished subagent is gated behind experimental Agent Teams"* — asserts `SendMessage` cannot resume a subagent and *"every subagent AC spins is fresh."* **Reversed 2026-07-01** (`ac-orchestration-protocol.md:762-783`). **This is load-bearing:** resume is the mechanism two-trip loading runs on, so the current spin protocol depends on the capability this entry denies. *(This lane was itself spun by two-trip resume.)*
2. *"The load-failure callout lands harder in `<task>`"* — prescribes the *"we already spun you once and you failed"* fiction, which two-trip loading **deleted** (`ac-orchestration-protocol.md:155`). The placement *finding* underneath it is still true and worth promoting; the device is not.

**Verification run (definition-of-done #4) — programmatic, not a read-through:**
- **Token set-diff**, six classes (`ADR-NNN`, `EVR-NNN`, 7-char SHAs, backticked file paths, bare filenames, ISO dates), old-against-new: **zero old-only tokens.**
- **Line-level set-diff**, all 354 unique non-blank non-heading content lines of the original body: **exactly 1 absent**, and it is the deliberate bucket-1 strip.
- **Heading set-diff**, all 107 headings: **exactly 1 absent**, the same strip.
- **Positive control on my own transformation:** the heading demotion was proven content-preserving by diffing source against output with heading hashes normalised — **byte-identical**.
- **Triage: 1 old-only item, classified as intentional strip. Zero genuine drops. Nothing required re-adding.**
- Original `LEARNINGS.md` lines 1–142 and its tail confirmed **byte-identical**; both files carry their canaries.

**Structure:** `LEARNINGS.md` 146 → **915 lines**, still exactly two top-level sections (`Promoted Stubs`, `Unpromoted`). The absorbed block nests **inside Unpromoted** as `###`, with the source's sixteen domain groupings preserved at `####` and entries at `#####`. *(First pass placed it as a sibling `##` section; corrected to honour the brief's literal "absorbed patterns go into Unpromoted."* Domain groupings were kept because the grouping is itself information — it tells a reader what class of problem a pattern belongs to, and flattening would have been the more transformative, lossier choice.)

**`AGENT_PATTERNS.md` → 40-line pointer**, retaining its H1 and canary so the ~30 live inbound links resolve.

### 🔴 Routed, NOT fixed — outside this lane's write scope

1. **~7 routing documents still name `AGENT_PATTERNS.md` as a live destination for new learnings** — `_evryn-meta/CLAUDE.md`'s routing table, `doc-update-routing-protocol.md`, `lock-protocol.md`, `ac-writing-protocol.md`, `ac-orchestration-protocol.md`, `evryn-dev-workspace/CLAUDE.md`, `evryn-quality/CLAUDE.md`, and `evryn-team-workspace/shared/protocols/sweep-protocol.md`. **They will send a future agent to a collapsed file.** Mitigated meanwhile: the pointer names them and redirects loudly. **Highest-priority follow-up.**
2. **`ac-orchestration-protocol.md:364`** instructs pasting *"the standing ⚠️ load-compliance callout at the top of `<task>`"* as part of the verbatim set — **no such callout exists** in the `<task>` block at lines 323-332. Looks like a stale remnant of the device two-trip loading deleted.
3. **`LEARNINGS.md` stub 34 cites `evryn-backend identity-writing-brief`** — no such file. The real name is **`evryn-backend/docs/identity-writing-bible.md`**. Left as-is (it sits in the untouched Promoted Stubs region).
4. **`_evryn-meta/docs/current-state.md` is stale on the Atlas** — it says the Code Atlas is *"BUILT AND PUSHED BUT NOT MERGED"* on branch `aca/atlas`. `docs/atlas/` (12 sections) **is tracked in `evryn-backend` `main`'s HEAD tree.** Staleness in the good direction, but current-state is the freshness authority and says otherwise.
5. **Collision worth knowing:** the parallel loading-architecture lane is authoring **`_evryn-meta/identity/ac.md`**, which references `AGENT_PATTERNS.md`. That lane is splitting `CLAUDE.md` into per-agent identities; whoever lands it should point at `LEARNINGS.md` instead.

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
