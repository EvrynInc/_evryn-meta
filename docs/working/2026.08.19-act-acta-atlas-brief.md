# ACTa — LANE BRIEF: A Code Atlas and dependency map for `evryn-team-runtime`. **Standalone.**

*Written 2026-08-19 by **ACT**. **Spin with: "You are ACTa. Read and faithfully execute `_evryn-meta/docs/working/2026.08.19-act-acta-atlas-brief.md`."***

> **Truncation check:** the last line should read `FULL FILE LOADED`.

---

# 🛑 IF YOU HAVE COMPACTED, YOU ARE DONE. STOP.
**Working from a summary rather than files you read means you compacted.** 🔴 **DO NOT RELOAD AND CONTINUE — we tried it and the agent sails through and confidently flails.** **Stop, tell ACT in one line, ask to be re-spun.** 🔴 **Do not assess your own sharpness; the faculty you would assess it with is the one under question.**

---

## §1 — WHO YOU ARE

**You are `ACTa` — a LANE AC**, your own Claude Code instance, reporting to **ACT**. 🔴 **If your identity does not load, STOP and report the failure — you are authorized to do nothing else.**

**Your manual is `_evryn-meta/.claude/agents/ac.md`** — read it in full, confirm its bottom canary. ⚠️ **The auto-injected `CLAUDE.md` is a 78-line ROUTER, not a manual.**

---

## §2 — 🔴 WHAT YOU ARE ACTUALLY BEING ASKED FOR TONIGHT: A PLAN, NOT AN ATLAS.

**The `evryn-backend` runtime has a Code Atlas (11 files, ~3,130 lines) and a generated dependency map. `evryn-team-runtime` has NEITHER — there is no map of it at all; reading it IS the map.** ⭐ **Those two artifacts have become, in Justin's words, *"awfully useful to everyone"* — this lane is about giving the team runtime the same.**

🔴 **BUT THE EXPENSIVE HALF IS NOT AUTHORIZED TONIGHT. Your deliverable is a PLAN that ACT and Justin approve before anything fans out.** ⚠️ **Building an atlas wrong costs more than not having one, because a wrong map is trusted.**

### Your trips
- **TRIP 1 — LOAD (§4), then STOP.** Post receipts + plan-of-approach + concerns to your exchange doc. **Wait for ACT.**
- **TRIP 2 — PHASE 0 and PHASE 1 only (§5), then STOP.** Post the result. **Wait.**
- **PHASES 2–4 are NOT authorized. Do not start them.** They are described in §5 so your plan can be judged against where it leads — **not so you can run them.**

🔴 **DO NOT TRIM THE LOAD LIST.** Add freely; **if you feel strongly something should be cut, PAUSE and raise it — do not cut.** **Report every call including failures; never let a retry absorb one.** **Name the tool on every receipt line, and never call a `Grep` a verification.**

---

## §3 — 🔑 THE DESIGN INSIGHT THIS LANE TURNS ON. Read it before you plan.

**The naive approach — one agent reads 10,000 lines and writes twelve sections — compacts and produces junk. The obvious fix — fan out to N readers — produces a partition with overlaps and gaps, because no reader can see the boundaries.**

⭐ **THE RESOLUTION: THE DEPENDENCY MAP IS THE *INPUT* TO THE ATLAS, NOT A PARALLEL DELIVERABLE.**

**Backend's dependency map is GENERATED — `scripts/dependency-map.ts`, run via `npm run depmap`, and its header says never hand-edit it: *"fix the generator, not the output."*** ⇒ **It is a mechanical port, not a reading task, and it can exist BEFORE anyone reads a line of the runtime.**

**And once it exists, the partition stops being a guess.** **Files that import each other belong in the same section; files nothing imports are leaves; files everything imports are HUBS — and hubs are exactly where this runtime's defects live** *(ARCHITECTURE: "defects live in seams — a value minted in one file, stored in a second, given meaning by a third")*.

⇒ **Port the generator first. Derive the partition from its output. Then, and only then, read code.**

---

## §4 — YOUR LOAD

**Rooted at `C:/Users/jbmcg/Evryn/Code/`. This is deliberately LIGHT on the team runtime and HEAVY on the artifacts you are copying** — because tonight you are designing a method, not describing a codebase.

| # | File | Read |
|---|---|---|
| 1 | `_evryn-meta/.claude/agents/ac.md` | **full — 749.** Your manual. |
| 2 | `evryn-team-runtime/docs/ARCHITECTURE.md` | **full — 138.** 🔑 **"Why local reasoning often fails here" is the reason this lane exists — read it twice.** |
| 3 | **`evryn-backend/docs/atlas/00-INDEX.md`** | **full.** The shape you are copying. |
| 4 | **`evryn-backend/docs/atlas/11-verification-and-tooling.md`** | **full.** ⭐ **Read this one closely — it is the section about how the atlas verifies ITSELF.** |
| 5 | **Two more atlas sections of your choosing** | full — **say which you picked and why** |
| 6 | **`evryn-backend/docs/dependency-map.md`** | **header + enough to know its shape.** Do not read all of it. |
| 7 | **`evryn-backend/scripts/dependency-map.ts`** | **full — the thing you are porting** |
| 8 | `evryn-team-runtime/package.json` + `tsconfig.json` | full — **what the port has to fit into** |
| 9 | `_evryn-meta/docs/protocols/ac-orchestration-protocol.md` | **full — 725.** **You will spin sub-agents; this governs how.** |
| 10 | `_evryn-meta/docs/protocols/orchestration/spinning-dc-qc.md` | full — 46. **Core first; this assumes it.** |
| 11 | `_evryn-meta/docs/protocols/load-cascades/team-runtime-full-cascade.md` | full — **the sanctioned load for anyone reading that runtime; your phase-2 briefs must respect it** |
| 12 | `evryn-team-runtime/docs/SPRINT-team-runtime-memory.md` | **skim for shape only** — you need to know it exists and what it tracks, not its contents |

⚠️ **You do NOT read `evryn-team-runtime/src/` tonight.** **That is the point of the design** — the partition comes from the generated graph, not from your reading. **If your plan requires you to read it, say so and explain why; that is a real finding about the method.**

---

## §5 — THE PHASES

### ✅ PHASE 0 — Port the dependency-map generator. *(Authorized tonight.)*
**Get `dependency-map.ts` running against `evryn-team-runtime`.** ⚠️ **It will carry backend-specific assumptions — finding them is the work.** **Run it. Confirm the output is real.** 🔴 **Then VERIFY IT BY EXECUTION, not by reading it: pick a file you know is imported and confirm the map says so; pick one nothing imports and confirm it shows as a leaf.** *(A generated map that is subtly wrong is worse than none — its whole value is being machine-derived.)*

### ✅ PHASE 1 — Derive a proposed partition. *(Authorized tonight. Then STOP.)*
**From the generated graph plus `git ls-files src` and line counts — NOT from reading the code — propose N sections where every file appears in EXACTLY ONE.** **Deliver:**
- **The partition**, file by file.
- **The rationale** — why these boundaries and not others.
- 🔴 **The HUBS** — files many sections depend on. **These are the seam risks and they decide how hard phase 3 will be.**
- **Your recommended N**, with the trade-off stated: *fewer sections = more per reader; more = more boundaries to reconcile.*
- **What you could NOT determine from the graph alone**, and what reading it would take.

### 🛑 THEN STOP. Phases 2–4 need approval.

### ⬜ PHASE 2 — the fan-out *(described so your plan can be judged; NOT authorized)*
**One sub-agent per section. Each reads ITS OWN section's files IN FULL, plus the dependency map so it knows what crosses its boundary.** 🔴 **Each must report every BOUNDARY CROSSING it finds — "this file of mine is used by something outside my section."** **That crossing data is the raw material for phase 3, and a section that reports none has probably not looked.**

### ⬜ PHASE 3 — the seam synthesis *(NOT authorized)*
**ONE agent reads the N section drafts and the crossing reports. It writes the index and the seam narrative.** 🔴 **It reads NO source — that is what keeps it cheap, and it should be told so explicitly.** *(Justin's steer: give it as little overhead as possible — a QC, or a generic with tight instructions.)*
- ⚠️ **KNOW ITS LIMIT AND WRITE IT DOWN: this agent can catch a CONTRADICTION between two sections, and a stated gap. It CANNOT catch a section that describes its own files wrongly, because it never read them.** **Do not let the synthesis pass be mistaken for a correctness check.**

### ⬜ PHASE 4 — prove it, mechanically *(NOT authorized)*
🔴 **The atlas's central claim is "a complete, non-overlapping partition of every file." That is a COMPLETENESS claim, and this estate has been wrong about completeness claims repeatedly — always by asserting rather than measuring.**
- **A SCRIPT — not an agent — diffs `git ls-files src` against the union of the atlas's own file lists.** **Every file appears exactly once, or it fails loudly.**
- ⭐ **Plus a SAMPLING check, because coverage is not correctness: pick three files at random, have a reader compare the code against what the atlas says about it, and report.** *(Cheap, and it is the only thing that tests whether the sections are TRUE rather than merely complete.)*

---

## §6 — YOUR CHANNEL AND INSTRUMENTS

**Exchange doc: `_evryn-meta/docs/working/2026.08.19-act-acta-exchange.md`.** **The brief is stable; all traffic goes to the exchange.** **Header entries `### ACTa — <ISO timestamp> — <subject>`** *(timestamp from `powershell -Command "Get-Date -Format 'yyyy-MM-ddTHH:mm:sszzz'"` — never guess)*. 🔴 **Commit immediately; an uncommitted entry is invisible.** ⚠️ **`_evryn-meta` is HOT — `git diff` before every commit and put the pathspec on the COMMIT.**

🔴 **AT SPIN-UP, READ YOUR EXCHANGE DOC TOP TO BOTTOM BEFORE RELYING ON A WATCHER.** **Arming sets the baseline to NOW, so anything already there is invisible to it permanently.** ⇒ **Read first, then arm.**

**Arm both:** a **watcher** on your exchange doc *(45s, emits only on change — nearly free)* and a **canary** on yourself *(20–30 min, never sub-5)*. 🔑 **The canary's job is catching YOU stopping without noticing.** 💰 **Every emitted line re-invokes you at full context — poll hard, emit rarely.** ✅ **Do not parse who wrote an entry; emit "CHANGED, read the file" and read it.**

🔴 **When the canary fires: stopped inadvertently → CONTINUE. Waiting on ACT → HOLDING IS CORRECT, do not manufacture output.** **Put that distinction inside the canary's own message.** 🔴 **`TaskStop` both when done.** ⚠️ **Considering a kill-then-re-arm? Check with ACT first.**

### 🔴 WHERE YOUR OUTPUT GOES — and what your CHAT WINDOW should say
**Everything goes in the exchange doc. ACT is your only recipient — you never report to Justin directly.**

⚠️ **AND KEEP YOUR CHAT WINDOW NEARLY EMPTY.** **Justin can see it, and a full report pasted there is noise he has to read past.** ⇒ **Write the report to the exchange doc, then say in chat only: *"Posted <subject> to my exchange doc; waiting on ACT."*** **The document is the deliverable; the chat is a receipt.**

---

## §7 — BOUNDARIES

🔒 **Never:** merge · push · deploy · apply a migration · edit an identity file. 🚫 **No write under any `.claude/` directory** — the gate is **ownership** *(those files are Justin's and ACP's)*, not mechanics.

✅ **Cleared tonight:** reading anything · **Phase 0's port and its verification** *(a new script + a `package.json` script entry, on your own branch in your own worktree)* · Phase 1 · writing your exchange doc.

🔴 **NOT cleared:** phases 2–4 · reading `src/` in bulk · **spinning any sub-agent** *(the fan-out is what needs approving)* · touching `docs/atlas/` in either repo.

🟡 **Anything genuinely Justin's → flag in the exchange doc with a recommendation and keep advancing.** **Over-flag; it costs ACT two seconds.**

---

## §8 — HOW MUCH ACT HAS READ

**ACT has read ARCHITECTURE, the tracker, the protocols and the cascades — and has NOT read `evryn-team-runtime/src/` beyond a handful of targeted regions, nor the backend atlas beyond its file listing.** ⇒ **Treat every claim here as a LEAD.** ⚠️ **Including the central design claim in §3 — that the depmap can precede and inform the partition. If porting the generator proves harder than "mechanical," that changes the plan and I want to know early.**

⭐ **This has already paid off once today: ACT built a sibling lane's load list from the wrong cascade and Justin caught it, not ACT.** **Say so loudly if something here is wrong.**

---

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
