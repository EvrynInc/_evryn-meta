# Changelog — _evryn-meta (cross-repo docs · dashboard · protocols)

> **Truncation check:** the last line of this file should read `FULL FILE LOADED`. If you don't see it, reload until you confirm the complete file.

**How to use this file:** reasonably concise entries per substantive change to **this repo**. Most recent at top; detail lives in git log.

**What rises to a changelog entry — and who writes it.** An entry records a **deploy or a substantive change** (code, identity, schema, a protocol, a doc, the dashboard) — the unit is a *ship/change*, not a session. **Session-management narration does NOT belong here:** a conductor recounting coordination with its lanes ("answered AC2's notes," re-spin logistics, routine pushes), or in-flight status that hasn't shipped, belongs in handoffs and git — not the changelog. **The AC responsible for a ship owns its entry** and — being contextualized on the work — routes it to each repo the change actually touched.

**Scope — `_evryn-meta`, and read by EVERY AC on load.** This changelog records changes to `_evryn-meta` itself: the Hub + spokes, protocols, ADRs, load cascades, `CLAUDE.md`, and the monitoring dashboard (`_evryn-meta/dashboard`). Every AC skims it on load — it carries changes to their own operating manual, the protocols, and the cascades. **Routing rule:** an entry belongs here **iff the change touched a file in `_evryn-meta`.** A cross-repo change that does **not** touch `_evryn-meta` gets **no** entry here — logging "two other repos changed" is pure noise and doubles what ACs already load per-repo. If a change touched `_evryn-meta` **and** another repo, log the `_evryn-meta` slice here and the other repo's slice there.

**📌 ONE STANDING EXCEPTION TO THAT ROUTING RULE — DC's and QC's OPERATING MANUALS ARE LOGGED HERE.** *(Justin's ruling, 2026-08-11, because the question kept recurring and kept getting answered differently.)* Changes to **`evryn-dev-workspace/CLAUDE.md`** and **`evryn-quality/CLAUDE.md`** get their entry in **this** file, even though neither path sits inside `_evryn-meta`. **Why, in his words: those changes ARE a meta phenomenon — *"nothing happens in QC's repo; QC is virtually always just a subagent for AC, that's it — it's been a long time since we've run QC in her own repo."*** The same is true of DC. **What it closes: `evryn-quality` has no `CHANGELOG.md` at all**, so under the routing rule above those changes had **no home anywhere** — which is how four Justin-approved QC pattern promotions (`c6f3f71`, 2026-07-20) came to be recorded nowhere. **Do NOT "fix" this by creating per-repo changelogs for DC or QC — log them here, where every AC already reads.**

**Monthly archive:** at month-end, move the prior month's entries to `docs/historical/changelog-historical/changelog-YYYY-MM.md` (trailing by one month).

**Skim on full-cascade load:** this file's top ~100 lines + section headers (the changelog-skim step in your load cascade points here).

---

## 2026-08-18 evening (AC0-37d — LANE A MERGED: `CLAUDE.md` is now a 78-line router; the conflict that fired on the wrong file; and the agent registry updating without a restart)

- **🟢 LANE A IS MERGED AND PUSHED** (`faf3eda`, `--no-ff`). **`_evryn-meta/CLAUDE.md` was AC's ~45,000-token operating manual, and the harness force-feeds that file to EVERY agent we spin** — DCs, QCs, one-line lookup agents alike. **It is now a 78-line router** that says *"you are agent X, go read your own manual."* AC's manual moved to **`_evryn-meta/.claude/agents/ac.md`** (685 lines); DC, QC and OC gained agent definitions holding **verbatim, byte-verified** copies of their repo manuals; `ac-orchestration-protocol.md` split into a core plus three per-target sub-docs under `docs/protocols/orchestration/`. **`acm-probe` deleted** (a throwaway registration probe). **No runtime code changed and nothing deployed — Evryn prod remains `v0.2.9`.**
  - 🔴 **THE FINDING THAT MADE THIS NON-TRIVIAL, and it generalises to any merge that MOVES content between files: git conflicted on `CLAUDE.md`, and that conflict was on the WRONG FILE.** The branch rewrote `CLAUDE.md` while `main` had edited it the same day — so the conflict fired, loudly and visibly. **But the content's new home was `ac.md`, which merged CLEAN and complete-looking, 653 lines of plausible manual.** ⇒ **Resolving the visible conflict CORRECTLY — take the router — is precisely the act that would have discarded three of `main`'s newer changes**, with nothing anywhere to flag it. **A clean merge at least leaves you no false sense of having reviewed; a conflict resolved correctly leaves you with one.**
  - **⇒ THE RULE: conflict markers mark the file the content LEFT, never the file it should have ARRIVED in. On any content-moving merge, diff the DESTINATION against the source's current state — the markers will not do it for you.**
  - ⭐ **What would have been lost, all committed to `main` that same morning:** the **arrival check** (a machine switch proves each repo is *synced*, not that the work is *reachable*), the **"prefer simple over clever" test** (which the branch would have *reverted* to the older, weaker wording), and the entire **batten-down rewrite** including the third question and its enumeration script — about 25 lines. **Carried forward in `5ba5978` before the merge.**
  - ⚠️ **And the trap on the other side: a mechanical "main wins" reconciliation would have been ACTIVELY WRONG.** 14 of the 17 differences were places the *branch* was correct and `main` was stale — including that `main`'s DC line called the mailbox model *"the fallback"* while `main`'s own later section called it RETIRED, and that `main` still asserted *"subagents auto-load AC's CLAUDE.md — this is the known failure mode,"* which the router kills. **The merge was per-hunk judgment, not a strategy flag.**
  - **Verified BEFORE committing, not after:** the loss test returned **exactly 8 differences, all known branch-correct** ⇒ zero content lost · all **9 canaries** intact · control-char scan clean · the whole merge was first **executed end-to-end in a disposable detached sandbox**, resolved, checked and reaped, so what landed was a proven procedure rather than a plan.
- **⭐ THE AGENT TYPES REGISTERED WITHOUT A FRESH SESSION — and this contradicts what the lane built its cutover around.** Minutes after the merge the harness announced `ac`, `dc`, `qc` and `oc` as available agent types and `acm-probe` as gone, **in the same session, no restart, no reload.**
  - **The lane recorded that the registry is read at SESSION START**, on a three-seat probe (two mid-session attempts failed, Justin's fresh session succeeded), and made *"Justin starts a completely fresh session"* **step 2 of the cutover.** **That evidence was real; the conclusion was too strong** — the registry evidently also picks up a change to the definition files themselves, and **all three earlier probes shared a confound: none had just changed a definition file.**
  - ✅ **This settles cutover step 3** (*"verify the four types are spawnable — if not, STOP"*). 🔴 **It does NOT settle that a definition's BODY programs the agent** — spawnability proves the harness *read* the file. The canary built to answer that was never reported back and the probe is now deleted. ⇒ **Re-check the fresh-session step with Justin; do not drop it on this evidence alone.**
  - 🔑 **The transferable half: a rule derived from failures needs the success case before it is trusted.**
- **Four cross-repo pointers the merge broke are fixed and pushed** — `evryn-dev-workspace`, `evryn-quality` and `evryn-ops` each sent their agent to *"AC's `CLAUDE.md`"* for the sync ritual (now repointed at `.claude/agents/ac.md`), and `evryn-backend`'s sprint cited *"Review depth"*, which moved into `orchestration/spinning-dc-qc.md`. 🔑 **The first three dissolve at the cutover; the FOURTH does not** — it is a deep link into a moved section and stays wrong until edited.
  - ⚠️ **The fourth was missed by two prior audits because both scoped themselves to `_evryn-meta`.** **A doc split invalidates deep links ESTATE-WIDE.** ⇒ **After any split, grep every repo for the moved SECTION NAMES, not the file name** — a file-name grep returns hundreds of legitimate hits and buries the few that are broken.
- **Justin's tweaks to the lobby, committed separately for attribution** (`b67e26d`): the index column head *"Also read"* → **"Must read"** (the optional reading is what the lobby exists to prevent), the AC row's example *"a reporter"* → *"an architectural runtime scout"*, and he softened his own quoted rationalisation. **That quote is verbatim because he said it — nobody else edits it.**
- **Two rules staged for the manuals** (`docs/working/2026.08.12-acf13-ac0-agent-patterns-postmerge-brief.md` §10, destined for `ac.md` plus DC's and QC's):
  - 🔴 **A GREP CANNOT ANSWER A QUESTION ABOUT WHAT A DOCUMENT SAYS, and it fails in BOTH directions.** Too loose invents hits — **a bare token cannot distinguish an assertion from its own retraction**, because the string is present *precisely because* the doc is correcting it. Too tight invents absences, **which is the more dangerous direction because an absence looks like a clean result.** **Three instances in one session**, each caught only because the output was absurd enough to notice. ⇒ **Read the file.** *(Ties to the Atlas's version: "a syntactic search cannot answer a semantic question, and a sharper syntactic search still cannot.")*
  - **A CANARY'S MESSAGE IS AN INSTRUCTION TO CONTINUE — so a BLOCKED agent obeys it by producing something**, which becomes restating the ballot and re-pinging. **That is a loop, and it bills a full turn at full context every fire.** ⇒ **The canary must distinguish *stopped inadvertently* (continue) from *waiting on Justin* (holding is correct — do not manufacture output), it may be paused while waiting, and that distinction belongs INSIDE the canary's own message.** *(Justin's diagnosis, watching it happen.)*
- **Housekeeping:** the `acm/loading-architecture` worktree and branch reaped after proving every commit reachable from `origin/main` ⚠️ **and note `git branch -d` REFUSED, saying so out loud — it measures merged-to-`origin/<branch>`, not merged-to-HEAD.** Prove `origin/main..<branch>` = 0, then `-D`. **`node_modules` counted before and after; `_evryn-meta` has none, so that hazard did not apply.** Justin also fixed the router's one typo target (`unknowlingly` → `unknowingly`) in the hard rule.

**Operator-relevant:** **nothing deployed; Evryn prod remains `v0.2.9`, and none of this can reach it — this was agent-loading only.** ⚠️ **What DID change for every agent: `_evryn-meta/CLAUDE.md` is no longer a manual.** A brief that does not name the agent's own manual in `<mandatory_load>` now leaves it holding a routing table and nothing else. 🔒 **The CUTOVER is still owed and is Justin's** — until it lands, the repo `CLAUDE.md` files remain authoritative and the agent definitions are **verbatim copies**, so **any manual change must be made in BOTH places** or the two silently fork.

## 2026-08-18 (AC — the batten-down check was asking the wrong question; a THIRD question that points the opposite way; and *"simple over clever"* gets a test)

- **🔴 `CLAUDE.md`'s never-pushed-branch check answered a HYGIENE question while presenting as a SAFETY one** (`73c5a62`). It asked only `origin/$b..$b` — *"is this branch pushed?"* — which is **not** *"would losing this machine lose this work?"* ⇒ **A fully-merged branch reads as "N ahead" FOREVER**, because it is ahead of **its own remote tracking ref**, which nobody updates after a merge and nobody ever will. **The number is real; the alarm it raises is not.** *(ACf-13 hit exactly this on 2026-08-17 and self-corrected — but the correction lived in a working doc, so the SCRIPT everyone actually runs still asked the wrong question. This is the flow-it-up that was owed.)*
  - **The script now asks reachability from ANY remote-tracking ref**, then runs **`git cherry`** to separate genuinely-novel commits from rebased ones. Three-way output: **🔴 investigate / ⚠️ rebased-but-safe / silent when fine.**
  - 🔑 **⭐ THE PART WORTH KEEPING: writing this fix PRODUCED the very failure it exists to prevent.** The first version resolved each repo's default branch via **`git symbolic-ref origin/HEAD`** — which is a **LOCAL CACHED SYMREF THAT GOES STALE.** It still said `origin/master` on **`evryn-backend` and `evryn-quality`**, whose real default is `main`. So the script compared `evryn-quality/main` against the **unrelated lobotomy-era fork root** and reported **51 commits at risk.** ⇒ **It was caught only because the script was RUN before being written down** — the "don't persist a fix as confirmed before you've tested it" rule, paying for itself inside ten minutes. **The committed version therefore needs no default-branch resolution at all**, and says so in a comment so nobody "simplifies" it back.
  - **`origin/HEAD` repaired** on both repos (`git remote set-head origin -a`). Worth knowing it can drift: anything trusting it silently reasons against the wrong branch.
- **⚠️ FORENSICS — four `evryn-backend` branches read as 14 commits "not on `main`" and were the LOBOTOMIZED ROUND-1 LANES.** `lane-a/ingest-resilience`, `lane-b/operator-approval`, `lane-c/cost`, `m1/silent-death-stage2`, all dated **2026-06-17** — the incident date. Round one was quarantined and **rebuilt from scratch days later as the `r2/` branches**, landing 06-25 in five `converge:` commits. Each converge is a **strict superset** (Lane A: 14 files/1,745 insertions → 23 files/2,788). The changelog said so in writing at the time — *"round-1 was lobotomized,"* *"corrupted round-1 quarantine folder deleted,"* *"drop round-1 residue → clean apply."*
  - 🔴 **The trap that would have produced a confident false alarm: ~50 branch symbols scanned as "ABSENT from main" and EVERY ONE was a RENAME.** `tripCircuitBreaker`→`tripBreaker`, `spend-monitor.ts`→`detectors.ts`, `pingHeartbeat`→`pingHealthcheck`, `test-derive-threading`→`test-threading-derivation`. ⇒ **Check by CAPABILITY, not by name** — a name-level scan of a redone lane reports catastrophe and is wrong every time.
  - ⭐ **One round-1 guard was not merely superseded but DEFECTIVE, which inverts the whole question.** Its `safety/send-invariant.ts` is the **tier-5 assertion that stops Evryn sending an email that never passed operator approval** — and round one placed it where it **could never fire.** Round two found it dead and **relocated it onto the actual outbound wire inside `sendEmail`**, as a required provenance gate covering every call site (main's own comment: *"the load-bearing relocation of the M1 send-bypass tier-5 assertion that was **dead-as-placed** at src/approval/flow.ts:526"*). ⇒ **Restoring that branch would have been a REGRESSION, not a recovery** — the reason "is anything stranded?" must never be answered by commit count alone.
  - **Seven stale refs cleared** across `evryn-backend` + `evryn-quality` (the four lanes, a merged `master` ×2, a rebased Mira bundle). **Every repo now carries exactly ONE local branch — its canonical one** — and the new check runs clean org-wide: **zero stranded work anywhere.** *(Local refs only; no repo CONTENT changed, hence no `evryn-backend` entry per the cross-repo rule.)*
- **🔴 A THIRD BRANCH QUESTION, added after the same session got caught by it: *"is everything I was working on actually HERE?"*** The two questions above are **departure** questions — *is anything trapped on this machine?* **On arrival — a machine switch, a fresh clone, "pull everything" — the question inverts, and the departure checks answer it with a confident, useless "all clean."**
  - **The mechanism, and it is not a bug: `git fetch` brings every branch down as a remote-tracking ref; `git pull` fast-forwards ONLY the branch you are standing on.** ⇒ **An in-flight lane lands on the disk as data with NO local branch and NO working tree** — nothing you can open — while every sync and batten-down check reports clean, **because nothing is missing.**
  - ⭐ **Caught live, on the request that prompted it.** Asked to *"pull all repos, worktrees, branches, everything"* after a machine switch, AC pulled correctly — the reflog shows `acm/loading-architecture` arriving at **12:00:56** in the `fetch --all` — then ran only the departure checks and reported **"zero stranded work."** True, and it read as *"everything's here."* **Seventeen remote branches across the estate had no local presence, including the Lane A branch that is the current critical path.** 🔑 **The pull was right; the check answered the opposite question.**
  - **Added to `CLAUDE.md`** (*Worktree & Branch Discipline*, with an enumeration script) **plus a trigger in SESSION STARTUP's repo-sync check**, since arrival is when it has to fire. ⚠️ **Written to REPORT, not act:** some remote-only branches are deliberately kept (a merged branch whose remote tip preserves pre-rebase SHAs that committed docs cite), and **materializing a lane is the owning agent's call — it decides its own worktree layout.** ⇒ **"Synced" and "everything I was working on is here" are different claims.**
- **⭐ *"Prefer simple over clever"* now carries a discriminator instead of a disposition** (`CLAUDE.md` → Architectural Mandate). It read *"— but know the difference between simple and naive,"* which **named the failure mode without giving any way to detect it**, and the word *simple* silently slid between *fewer lines*, *more familiar*, and *actually less braided*. **New wording: *"where 'simple' means the next reader can verify it, not fewer lines and not more familiar. Complexity is allowed to the degree to which you can name the failure it prevents."***
  - 🔑 **What forced it was a counterexample from this same session.** Round-1's send-bypass guard was **genuinely simpler** — its own header calls it *"NOT a watching detector with infrastructure… a ~few-line assertion."* Round-2's replacement is **more** sophisticated (a required type parameter, a validated provenance set, a gate on the outbound wire, fail-closed + emergency alert). **The complex one is correct; the simple one was dead where it sat.** ⇒ **The old wording endorses the wrong one**, and *"know the difference between simple and naive"* offers nothing to tell them apart.
  - **Two further defects it closes:** the rule **fired only when you had already caught yourself** (nobody experiences themselves choosing cleverness — the same *"I never registered a moment of choosing"* shape as the load-triage failure); and it **collided with `Build for one, structure for many`** in this same document, which says to take the abstraction at ~10% more effort. **The new wording reconciles them** — an abstraction that prevents a named failure (a rewrite when client #2 arrives) is justified by the same sentence. **And the measure is well-fitted here specifically: the next reader is usually a fresh instance with no context.**
  - ⚠️ **Justin cut the drafted final clause — *"if you can't name one, cut it"* — and was right.** It converts an inability to *articulate* into a mandate to *delete*, which is the enumeration trap aimed at existing code: **failing to name what a piece of complexity prevents often means you do not understand it yet, not that it is useless.** It also turned a rule about what you *add* into a licence to remove. **His *"to the degree to which"* is graduated rather than binary and governs addition only.**
- **Routine context, not a ship:** all **11 repos** fetched + fast-forwarded from a machine switch (676 commits; `_evryn-meta` 443, `evryn-backend` 102). GitHub org enumerated against `docs/repo-inventory.md` — **nothing needed cloning and no worktrees live outside `Code/`.** ⚠️ **Read that as "every repo is synced," NOT as "everything is here"** — see the third-question bullet above; **seventeen branches came down as data and none were materialized.** Stale `origin/HEAD` repaired on `evryn-backend` + `evryn-quality`; seven merged/superseded local refs deleted.

**Operator-relevant:** **nothing deployed; Evryn prod remains `v0.2.9`.** No runtime code changed — this was repo hygiene and one operating-manual fix. ⚠️ **If you run the batten-down check and see a 🔴, it is a prompt to INVESTIGATE, not a confirmed loss** — patch-id cannot see a from-scratch redo, which is exactly what today's four branches were.

## 2026-08-17 evening (AC0-37c — a load ordered by DEPENDENCY costs ~100K fewer tokens; Lane A's frozen wording approved and applied; and a halt that tells the operator the wrong thing)

- **⭐ THE HEADLINE IS A MEASUREMENT, AND IT CHANGES HOW EVERY HANDOFF SHOULD BE WRITTEN: the SAME load list, re-ordered by dependency, came in roughly 100,000 tokens lighter.** *(Justin's cross-session observation — he can see token counts across sessions and an agent cannot.)* Nothing was trimmed; only the sequence changed. **The mechanism: each layer is the frame the next is read against, so the later documents are largely DIFFS against the earlier ones.** The Code Atlas is not a description of `src/` — it is substantially a **correction log against `ARCHITECTURE.md`, argued by line number.** Read after it, those corrections are the highest-value content in the file; read before it, they are noise **and** you build a picture the later docs exist to correct, then pay again to unbuild it.
  - 🔴 **The failure mode is now named in `38`'s marching orders so the next author catches themselves: handoff load-lists are habitually ordered by PERCEIVED IMPORTANCE, not by DEPENDENCY.** The version replaced opened with the 941-line orchestration protocol and the status snapshot — the two most *urgent-feeling* items — and put `ARCHITECTURE.md` **seventh.** **That is the default mistake, and it was made by a careful author in the very doc telling the next agent how to load.** ⇒ **If a load list starts with a protocol or a status doc, it is wrong. Re-sort before handing it over.**
  - **Three rules, written to generalize:** widest-and-most-stable first, narrowest-and-most-volatile last · **honor each document's own declared prerequisites** (`ARCHITECTURE.md` carries a *"Required Context — you must have read these first"* section; that is the document instructing you) · **load a protocol at the beat that needs it, not in the foundation** — 941 lines about *how to brief an agent*, read before you know *what the agent will do*, does not stick.
  - **Free win, measured: DIFF `CLAUDE.md` instead of re-reading it.** If `git diff <session-start SHA> HEAD` and `git status` are both empty and the canary is present, the auto-injected copy is byte-identical — **~85,000 tokens saved.** Justin's ruling: the durable home for this rule is the Lane A cutover, in whichever file ends up owning it.
- **🔴 LANE A — the `CLAUDE.md`-to-router split: Justin approved the frozen wording, and three of five changes are APPLIED (uncommitted on the branch).** The two false sentences now say the auto-loaded file is a **ROUTER, not an operating manual**; the AC variant points at **`.claude/agents/ac.md`** *(the drafted wording hardcoded a stale `identity/ac.md` — the path was corrected and verified absent afterwards)*.
  - **A fourth stale reference was found that no prior audit had listed:** `spinning-an-ac.md`'s substitution 2 claimed the tier headings are *"exact headings in `_evryn-meta/CLAUDE.md`."* **Post-split they are not.** 🔑 **Why it earns a fix rather than a shrug: the protocol tells a spinner to GREP the load-defining file for that anchor and to treat a no-match as a STALE manual it must not spin against — so grepping the router returns nothing and produces a false stale-manual alarm that halts a legitimate spin.** Fails loud, not silent, but it halts real work.
  - ⚠️ **A trap this work CREATED, recorded so nobody misreads it:** the lane's post-rebase check exists to prove the frozen blocks are **byte-identical to their pre-lane state.** **Two have now been deliberately changed with Justin's approval, so that check will correctly report a difference — and the next person to run it will read it as a rebase defect.** It needs a new expected value.
  - **Still open and deliberately not invented:** the manual-path list is **true until the cutover** and has **no drafted replacement anywhere**, because what it becomes depends on what the cutover does. **It is a cutover step, not a gap to fill.**
- **🔴 A VERIFIED DEFECT: during a circuit-breaker halt, an operator's Slack message fails with a generic error instead of saying the system is halted.** Settling a ten-minute question nobody had run *(does Postgres reject a halt-sentinel-bearing string?)* **answered it by EXECUTION against DEV — yes, `chr(0)` in a `text` column is rejected outright** — and then tracing it at source found the consequence. **`src/notify/slack.ts` contains ZERO references to `isHaltSignal`**; the query result flows straight into `createMessage`, which is `await`ed **before** the Slack reply — so the throw lands first and the operator gets *"Sorry, I hit an error processing that."*
  - **Calibrated honestly: NOT data loss and NOT a safety-gate bypass.** M1's lossless halt is untouched. It is a **diagnostic failure at the moment diagnosis matters most** — the same family as the send-failure alert that instructs a retry which structurally cannot match.
  - 🔑 **The transferable part is the CLASS, and the protocol already names it in the abstract:** *"a new pathway that calls `runEvrynQuery` and forgets `isHaltSignal` will silently treat a halt as a real answer; compile clean, tests green."* **`slack.ts` is that pathway.** ⇒ **The recurring cause is that the sentinel is TYPE-INVISIBLE** (a bare `Promise<string>`), so *"just add the check"* fixes one site and leaves the class. **Justin's ruling: this gets someone close to it, not a rider on other work.**
  - ⭐ **The probe carried a CONTROL, and that is why the result is trustworthy:** the same run inserted the identical string *without* the NULs and succeeded. Without it, a failure could have been the temp table, the connection, or the SQL.
- **⚠️ A GREP FALSE POSITIVE caused by CORRECT documentation — the mirror of a failure class this repo has recorded three times in the other direction.** Checking whether `schema-reference.md` documents the tables that exist in prod, a substring grep reported all eight as documented. **It was false for three — and those three appear in the file exactly once, inside the DO-NOT-TRUST banner that says they are missing.** **The string was present precisely because the document was warning that the content was not.**
  - 🔑 **What caught it is the part worth keeping: the same crude check also flagged three "missing tables" that are RPC FUNCTIONS. The ABSURD result exposed the unsound method — not the plausible one.** Without those function names the false pass would have stood and would have "disproven" a correct tracker item. **The sound version cost one line more: grep for a section heading, not a bare name.**
- **Housekeeping that removes real risk:** the **nine mutation-sweep worktrees are reaped** (verified by content, not by git's verdict, because one held pre-rebase commits; the `node_modules` hazard handled by **detect-and-repair rather than detection**, since `[ -L ]` does not reliably identify a Windows junction) — **and the toolchain then proven by EXECUTION**, `npm test` returning 65 suites / **1,925 assertions / 0 failed**, which doubles as a known-good baseline immediately before the Lane A merge. A fresh archival `pg_dump` was taken and verified structurally identical to the last known-good one *(⇒ no schema change since 2026-07-06)*. **`evryn-backend`'s `.gitignore` gap is closed** — logged in that repo's changelog.

**Operator-relevant:** **nothing deployed; Evryn prod remains `v0.2.9`.** No runtime code changed anywhere — this session was loading discipline, protocol wording, verification and housekeeping. ⚠️ **One thing worth knowing before the next incident: if the circuit breaker trips and you message Evryn on Slack, you will get a generic error rather than "I am halted."** That is now a tracked defect, not a mystery.

## 2026-08-17 (AC0-37b — the snapshot stops being a log; and a control-character class that had been silently corrupting our own docs for months)

- **🔴 `docs/current-state.md` — the restructure MERGED (167 → 133 lines).** The file had become a **broadcast bus wearing a snapshot's filename**: one line ran **6,902 characters** and carried **eleven dated appendages from ten different instances.** ⭐ **ACb's diagnosis is the part worth keeping, because the obvious explanation is wrong.** It is not that replacing is hard and appending is lazy — those appendages were *more* work than an in-place edit, each signed and ISO-timestamped, one stating outright that it was recorded there *because the doc that held it was being retired and there was nowhere else.* **That is the signature of correspondence, not of a shortcut.** ⇒ **This is the only document every instance loads at startup, so it is structurally the only way to reach everyone — and a broadcast bus is append-only by nature, because you cannot replace someone else's message.** The bus now has a bounded home: a **📮 BULLETIN** drained at each `#lock`, whose entries are **overdue by definition after 7 days**, so it fails loudly instead of silting up. — [ACb diagnosed + built · AC0-37b merged]
  - **The merge CONFLICTED, and that was the safe outcome.** The branch rewrites the file wholesale, so a *clean* merge is the dangerous one — it would have silently dropped everything `main` gained since the branch was cut. **Most of what the restructure dropped turned out to be covered elsewhere and often better** (the Atlas material now lives in `product-full-cascade.md`; the owed Gate-B and nested-relay verifications are named in backend Steps 88B and 94).
  - 🔴 **But one thing would have been deleted, and it was somebody else's correction.** ACf-13 updated the team-runtime section on 2026-08-12; the branch predates it, so the restructure still called the Phase-1 fast-follow *upcoming* (it is COMPLETE) and Step 43 *"the top open item"* (it is CLOSED). **His correction is preserved verbatim in the BULLETIN with a staleness marker on the body — and his prose was deliberately NOT rewritten, because that lane is his.**
- **⭐ Justin asked for a fresh-eyes loss pass over the merged file against the previous one, and it earned its keep: it found one GENUINE loss and one item that was recorded but never tracked.** Both recovered as `evryn-backend` Steps 114 and 115. **The genuine loss is the instructive one:** an **approved-but-never-briefed lane** (persist the Gmail poll cursor durably, needs a schema migration) that existed *only* in the Active-Instances section — a section the restructure correctly deleted, since `git worktree list` answers "who is running" better than a hand-maintained list. **It was the one entry in that section with no other home anywhere in the org.**
  - 🔑 **The transferable rule: when a doc section is deleted wholesale because it duplicates a better source, audit it for the ONE item that was not a duplicate.** A section can be 95% redundant and still be the sole home of something.
  - ⚠️ **And a mechanical trap that produced SIX false losses before it was caught:** `SPRINT-V0.2-HARDENING.md` writes steps as numbered list items (`93.`) while every other doc calls them *"Step 93"* — so a `grep "Step 93"` returns **nothing** for a Step that is plainly there. *(The Code Atlas §11 already documents this; it still caught a fresh instance.)*
- **🔴 A CONTROL-CHARACTER CLASS HAS BEEN QUIETLY CORRUPTING OUR OWN DOCS, and it is not mysterious once seen.** A scan across **all eight repos** found **three previously-unknown instances** of a real control character sitting in a tracked text file — **two of them in AC0 handoffs in this repo** (repaired; `_evryn-meta` now scans clean), one a vertical-tab in a frozen v0.1 prompt import in `evryn-backend`'s historical vault (**left alone deliberately** — it is a record and the character is meaningless there).
  - **The mechanism:** an agent's file-writing instruction travels as **JSON**, and **JSON natively interprets `\uXXXX` as a character escape.** So an agent writing prose that *quotes* an escape sequence emits **the real character** instead of the six-character text.
  - 🔴 **The part that makes this structural rather than careless: every known instance occurred while documenting this exact problem.** Both handoffs were mid-sentence about NUL bytes — *"NOT 'the escape is spelled ⟨NUL⟩'"* and *"mutating the trailing NUL to ⟨0x01⟩ → STILL GREEN."* **AC0-37b then did it again, in the same hour, while writing the DONE note for the Step that fixes it.** ⇒ **The only people who ever type these escapes are the people documenting them, so the hazard concentrates precisely in the docs about the hazard.**
  - ⚠️ **A NUL additionally makes a file BINARY to `grep` and to `git diff`** — so those two handoffs were silently excluded from every text search ever run over this repo, **and the damage hides itself.**
  - **Prevention is the weak answer and we have proof:** *"remember to double the backslash"* is an instruction, and **two prior agents knew this class in detail and still did it.** ⇒ **The durable answer is a scan** — filed as `evryn-backend` Step 113 for the repo that has a test suite. **`_evryn-meta`, where the actual hits were, has no test suite to hook into**, so its coverage is an open question for Justin rather than a solved problem. **Do not read Step 113 as closing the class org-wide.**
  - 🔑 **Two things the eventual check must get right, both learned by running it:** exclude real binaries by extension (`.docx` files are ZIP archives and legitimately full of control bytes — **a check that cries wolf gets ignored, which is worse than no check**), and **the check must CONSTRUCT the character it searches for**, never contain one literally, or the test file becomes the thing it is looking for.

**Operator-relevant:** docs and process only in this repo — **no runtime code changed here, nothing deployed. Evryn prod remains `v0.2.9`.** The `evryn-backend` half of today (the grep-invisible-file fix, the sprint restructure, and eight new/expanded Steps) is logged in that repo's `CHANGELOG.md`, per the cross-repo rule.

---

## 2026-08-13 (ACf-13 — ⭐ ADR-055; a `#lock` step that was two statements of one rule; and TWO branches found stranded that everyone believed were merged)

- **⭐ ADR-055 — the thread IS the complete record** (`docs/decisions/055-thread-as-complete-record.md`). Answers what ADR-052 left open when it declined SDK sessions: *if not a session, what carries an agent's working experience within a room?* **The build is `evryn-team-runtime` SPRINT §H; this ADR carries the reasoning — specifically the alternatives rejected and WHY, because each has been proposed more than once.**
  - 🔴 **The one that will be re-proposed, so it is written to be hard to re-open: "store it and let the agent query it" re-creates compaction at the file level.** The agent holds a *reference*, reasons confidently from what it believes the content was, and **never queries — because nothing tells it that it does not know.** *(Demonstrated live on the conductor that wrote it: this session compacted, worked from the machine summary for several turns, and asserted three false things to Justin while feeling entirely competent.)*
- **🔴 `#lock` step 5e + step 11 — the learnings step was TWO statements of ONE obligation, and the vaguer one was winning.** 5e routed learnings carefully by altitude; **step 11 then restated the same duty as a bare *"add appropriate cross-project patterns or insights"*** — no scope test, no altitude — so a reader hit both and the loose one governed. **That is how a lane-local lesson ends up in a cross-repo distribution queue.** Fixed by giving each exactly one job: **5e owns the routing decision, 11 owns the mechanics.** ⇒ **Two statements of one obligation is how a careful rule gets averaged away.**
  - **5e gains Justin's ordering: the local half goes FIRST and it goes NOW.** `LEARNINGS.md` drains at `#sweep`, **which had not run in 20 days against a weekly cadence** — so filing a lane-local lesson there puts something your own build needs *now* behind a queue nobody reads until long after, **while everyone believes it was captured.** ⚠️ **Plus a caution against over-correcting:** the only entries a reviewer could prove *false* were the ones carrying dates and specifics — *"a file optimized purely for transferability becomes a file of unfalsifiable claims that quietly rot."*
  - **The same rule is now in `LEARNINGS.md`'s own header**, with a test: strip every name, path, SHA and date — is there a rule left another repo could act on? If no, it is not a cross-project learning.
  - **The protocol also had NO truncation canary** for its entire life — a load-bearing file that could not self-diagnose a bad load. Added.
- **🔴 TWO BRANCHES WERE STRANDED, both believed merged, both found by checking content instead of trusting the record.** `acfsq/research-move` carried both scratchpad-clause drafts; `acf13/lock-2026-08-12` carried **the handoffs and the staging brief for every doc held under Lane A's merge** — 28 commits behind `main`. **A staging doc nobody can find is not staging anything.** Both merged.
  - ⚠️ **How each nearly stayed lost is the transferable part:** the conductor grepped `main` and reported the drafts were never persisted; the authoring agent pushed back, having verified with `git show HEAD:<path>` — **where `HEAD` was the BRANCH tip.** **Both checks were real and both answered the wrong question.**
- **`docs/research/` DELETED — it was a misfiling, one day old.** Its only occupant studied `evryn-team-runtime`'s SDK behavior, which is not cross-repo. Moved to that repo's own `docs/research/`, **nine inbound references repointed and verified resolving in the same commit** — including ADR-052's, which would otherwise have left a decision record citing nothing. ⇒ **The rule, now in the new README: research lives in the repo it is ABOUT.** What belongs here is research about the **organization**.
- **Staged for Lane A's merge (`docs/working/2026.08.12-acf13-ac0-agent-patterns-postmerge-brief.md`):** paste-ready blocks for **`current-state.md`** (held as of today) and **`CLAUDE.md`** (the research-routing line + the `LEARNINGS.md` scope rule). ⚠️ **`current-state.md` carries the one surviving dangling pointer to the deleted research path** — it must be fixed when that block is applied.

## 2026-08-12 (ACf-13 — an ADR requirement that had been reversed and never updated; and the no-SDK-sessions question answered, with the deciding argument nobody had)

- **🔴 ADR-052 §7 carried a requirement its own design-of-record had REVERSED — one day after the ADR was accepted, and it never reached the file.** The clause required consolidation-review touchpoints be *"phone-first, under a minute."* The design-of-record §8.1 (revised 2026-07-16) says the opposite and says why: **"light enough" does NOT mean sub-minute, and demanding it is itself the corner-cutting failure** — a consolidation is precise surgery affecting *every thought the agent has from here forward*, **5–10 minutes is a good outcome, and the real lever is FREQUENCY, not speed** (consolidate often and any single review carries few sign-offs). **Corrected in place as a DATED correction rather than a silent rewrite, because an ADR is a decision record.** Flagged twice before and never fixed; **whoever built Phase-2 consolidation would have built to the wrong requirement — and the wrong requirement is the one that makes you rush the thing that must not be rushed.** Commit `31ba6e3`. — [ACf-13]
- **The "should the team runtime's agents have SDK sessions?" question is ANSWERED: do not adopt** — `evryn-team-runtime/docs/research/2026.08.12-acfsq-team-runtime-sdk-sessions.md`. It would reverse part of ADR-052 §3.2, **so the decision was Justin's — he read it and ACCEPTED the recommendation 2026-08-12; ADR-052 now carries a dated amendment confirming decision 1 on the new reasoning.**
  - **⭐ `docs/research/` was ESTABLISHED in the same pass, with a `README.md` signpost, and this analysis moved into it.** It had been written into `docs/working/` and did not belong there: **a working doc is the authoring agent's own scratch — Justin does not read it** — so an ADR citing one made a **decision record depend on a path that advertises itself as disposable.** Research that must be re-readable years from now now has a home; session notes, briefs and handoffs stay in `docs/working/`. — [ACfsq]
  - **The persistence unknown everyone treated as the gate is SETTLED from the pinned SDK's typings — and turns out not to be the deciding factor.** Today sessions persist to `CLAUDE_CONFIG_DIR`, which this runtime points at a **temp directory**; a pluggable Postgres-capable store exists but is `@alpha`, is a **mirror not a primary**, and **drops the batch after three failed appends.**
  - 🔴 **The deciding argument is new and does not turn on persistence at all:** `composeWake` re-reads identity and `MEMORY.md` **fresh every wake**, so a per-(agent, thread) session would **freeze each room's copy of that base at session start.** A note written in room A never reaches the live session in room B — **the one-trunk memory model stops working**, which is the *"different versions of the same person"* fork per-agent serialization exists to kill, **returning as staleness instead of concurrency.** It holds on **both** persistence branches.
  - **A third option nobody had proposed:** the `PostToolUse` hook already delivers `tool_input` **and** `tool_response`, and the handler throws both away. A deterministic per-thread work record built from it targets the actual pain **without depending on an agent remembering to write anything** — and stays in Postgres, manifest-able, invariants intact. **Sequenced after the Phase-1 fast-follow.**
  - **⭐ A correction to our shared premise, verified at source:** the claim — repeated across a lane brief and a prior analysis — that *"nothing today instructs an agent to use the scratchpad as a working record"* **is WRONG.** Both `runtime-ops.md:25` and the `scratchpad_write` tool description already do. **The real gap is narrower and sharper: every phrasing is scoped to *status* — "where you are, what you're waiting on" — and none covers *work already done and ruled out*, which is exactly the loss.** ⇒ **The cheap fix is cheaper than costed: a clause on an existing bullet, not a new beat.** — [ACf-13 conducting **ACfsq**]

**Six batched questions for Justin sit in §9 of that doc**, including one worth settling once rather than per-feature: **are we willing to depend on `@alpha` SDK surfaces for anything load-bearing?**

*(The team-runtime code round of the same night — twelve SPRINT Steps, 442 → 511 tests — is logged in `evryn-team-runtime/CHANGELOG.md`, per the routing rule: it touched no file in `_evryn-meta`.)*

## 2026-08-11 evening (ACf-12 — the inline-question rule made loud; a "when the brief is wrong" order into DC's and QC's manuals; and QC-manual changes get a changelog home at last)

- **🔴 `CLAUDE.md` — the inline-question rule is now its own block instead of a clause buried mid-bullet.** It was **already in the file**, which is exactly what makes it worth recording: **a correctly-stated rule in an unfindable position is a rule that keeps getting missed**, and missing it cost Justin *"probably an hour or more"* on the morning of 2026-08-11 (*"This was excruciatingly painful and for one reason: because you didn't guide me. You tossed information at me in a jumble."*). The edit adds the **mechanism**, which is the part that makes it stick: **the author is holding all the reasoning, so a ballot parked at the bottom feels complete *to them*** — while Justin, reading it once, cold, between rooms, must scroll back and reconstruct which argument each item belongs to, **for every item.** Plus the three sub-failures from that report: **no bare pointer** to a ballot item, **no positional section titles** (*"2a"* — anchor by content), and **the end-of-message recap is a convenience copy, never the first place a question appears.** — [Justin directed · ACf-12]
- **🔴 `evryn-dev-workspace/CLAUDE.md` + `evryn-quality/CLAUDE.md` — a new standing order: when the BRIEF is wrong, say so BEFORE you build.** *(From a live failure: an AC brief for the HTTP-414 fix specified "no migrations"; the genuinely correct fix — a server-side query — required one, and the DC **silently built the lesser in-scope workaround and never said the constraint was what ruled out the better answer.** Justin caught it from outside the code.)*
  - **⭐ It names BOTH directions, because they are not symmetric and the dangerous one is the quiet capitulation.** Going rogue is visible. **Capitulating is invisible precisely because it looks like compliance** — the work is clean, the tests pass, and **the only artifact is the better solution that never got built.**
  - **The framing that makes it actionable:** a conductor running the light load may hold a few hundred lines of a runtime the builder is about to read in full, **so the builder is frequently the best-informed agent in the chain on questions of fact.** A correction from below is the cheapest review layer we have — and it only works if it is actually sent.
  - **QC's version is deliberately different:** hers is about the brief's **premise** being false. The trap — **a fix can be correct against the brief and wrong against the system**, so a reviewer who inherits the premise returns a clean GO **with her name on the clearance.** Plus the corollary about silence: *where a brief says nothing, that is far more often a gap in the author's context than a cleared area.* — [Justin's order · ACf-12 wrote both]
- **📌 ROUTING DECISION — DC's and QC's manuals are logged in THIS file from now on** (rule written into the header above). — [Justin's ruling · ACf-12]
- **`docs/current-state.md` — the team-runtime half rewritten to live state.** It still described ADR-052 Phase 1 as half-built and the ~370-room total wake outage as an approved-but-unspun lane; **both had shipped days earlier.** Now carries Phase 1 complete (SPRINT Steps 1–5 plus Step 30), gate **G1 needing migrations 015–018 applied in order**, Step 43 as the top open item, the Phase-1 fast-follow preceding Phase 2, the open no-SDK-session architecture question, and the **SEALED-EXEC class still unresolved product-side.** Shipped detail was deliberately not re-summarised — it was already in `evryn-team-runtime/CHANGELOG.md`, so the move honoured this file's replace-don't-log rule with nothing lost. — [ACf-12]
- **New durable capture: `docs/working/2026.08.11-acfc3t-acf-test-coverage-map.md`** *(⚠️ **MOVED 2026-08-17** to `evryn-team-runtime/docs/research/2026.08.11-acfc3t-team-runtime-test-coverage-map.md` — research lives in the repo it is about. This line records where it was created; the path above no longer resolves.)* — what the team-runtime test suite actually guards, from a **nested read Justin designed**: a full-load runtime scout read all of `src/` + migrations + the identity half, skimmed the tests, wrote its own questions, then spun a child that read **all 56 test files in full.** **Committed immediately because it existed only in one session's memory.** It carries the **test↔code pairing map**, which is what lets a future lane load one subsystem's tests and code instead of the whole runtime.
  - **⭐ Its headline finding is about us, not the code.** A test file's header asserts that every test in it was *"watched FAILING against deliberately-broken REAL code before being trusted."* **For three of them that cannot be true** — they never assert the thing they name. *"Watched failing"* is the convention DC's, QC's and AC's manuals all rest on; **finding it written as a claim rather than performed as an act, inside the very file built to embody it, means we do not know how many other guards it is merely decorating.** — [ACf-12, from `ACfc3` → `ACfc3t`]

**Operator-relevant:** manuals, protocols and docs only in this repo — **no runtime code changed here, nothing deployed. Evryn prod remains `v0.2.9`; the team runtime remains dormant and wakes nobody.** The team-runtime code findings are filed as SPRINT Steps in that repo and logged in its own CHANGELOG.

## 2026-08-11 (AC0-34 — four rules out of a handoff and into the manuals that load, plus the measurement that says a full-runtime load no longer fits one agent)

*Landed by merging `ac0/overnight-review` (authored by AC0-33 overnight 08-10/11, Justin-vetted) into `main` at `33c4dee`, and the `evryn-quality` half at `4f47560`. **No runtime code changed anywhere; nothing deployed. Evryn prod remains `v0.2.9`.***

- **🔴 `CLAUDE.md` — `git worktree remove` follows a `node_modules` junction into the SHARED canonical tree and wipes it, WITH OR WITHOUT `--force`.** Two incidents in one evening; the second happened while actively trying to avoid the first. **The rule is written as DETECT-AND-REPAIR rather than prevention, and that shape is the transferable part:** count `node_modules` before the reap, count after, `npm ci` if it dropped. **Why not "just never junction"?** Because the prevention step's *failure* is invisible — one instance's `rmdir` guard silently never ran and reported *"not a junction,"* i.e. **a command that failed to execute, read as a fact about the filesystem** — whereas the damage is cheap, total and instantly detectable. ⭐ **Never build safety on a step whose failure you cannot see when you can build it on a check whose result you can.** ⚠️ The tell is not an error; the reap prints nothing. It surfaces later as `No such file or directory` on `tsx`, or as **a mutation baseline coming back RED when it has no business being** — so if a baseline is unexpectedly RED, suspect the toolchain before believing the verdict. — [Justin pre-approved · AC0-33]
- **`CLAUDE.md` — when subagents are running, every user-facing message starts with a `#` heading.** Justin: *"I can't describe to you the absolute barrage I'm hunting through to see your little notes innocuously sprinkled through here."* Foreground vs. background does not help — subagent output renders to him either way, so **a heading is the only thing that makes AC's voice findable.** — [AC0-33]
- **`ac-orchestration-protocol.md` — THE LANE BRIEF IS TRIP-2 READING, NEVER TRIP 1**, resolving a collision between two of our own rules that every AC hits on its first lane spin. Two-trip loading says the load trip must tell the agent *nothing* about the work; the work-brief rule says the lane's brief goes in `<mandatory_load>`. **Those cannot both hold, and it is not close: the lane brief IS the task** — and the filename alone (`…-ac0-aca-atlas-refactor-brief.md`) gives away the lane, its owner and its subject before a line is read, so it cannot be patched with careful annotation the way an ordinary file can. **The test generalizes: not "is this a doc?" but "could the agent reconstruct the task from this?"** — [Justin approved · AC0-33]
- **`AGENT_PATTERNS.md` — a full-runtime load NO LONGER RELIABLY FITS ONE AGENT, and the fix is to restructure the WORK, not trim the list.** Three agents attempted it on 08-10; **two compacted during the load.** ⚠️ **The standing "when in doubt, over-load" rule is right about direction but silently assumes over-loading is possible — it no longer always is.** — [AC0-33]
- **`AGENT_PATTERNS.md` — the one shape behind SEVEN separate misses in a day: a check that did not RUN, read as a check that PASSED.** Same failure in seven costumes — a test asserting a memoized value against itself (it would pass with the composition logic deleted wholesale) · a mutation run whose verdicts were meaningless because the runner was gone · a `grep -l` used as a read · a shell command that never executed · a safety guard gated behind a flag every consumer checks first, so it has **never once fired** · a subagent reporting a commit SHA that does not exist · and **the handoff document that claimed a fresh-eyes pass it had not done.** ⭐ **Prove a check can FAIL before you trust that it passed; treat "it didn't complain" as evidence of nothing until you know it was capable of complaining.** — [AC0-33]
- **`evryn-quality/CLAUDE.md` (logged in that repo's changelog) — never record a mutation verdict without a baseline in the same run.** A mutation that never applied and a missing test runner are **indistinguishable from a real result**, except by the baseline. — [AC0-33]

**⭐ And the measurement this session added, because it changes how every future subagent gets loaded.** A full-load runtime scout compacted mid-load and reported it honestly; the failure report carried the numbers nobody had. **The binding constraint is not the code — it is our own prose docs.** Measured off the Read tool's own token counts: `CLAUDE.md` ≈ **144 tokens/line**, `ac-orchestration-protocol.md` ≈ **166**, `SPRINT-V0.2-HARDENING.md` ≈ **163**, `evryn-backend/CHANGELOG.md` ≈ **180**; TypeScript runs ~30–45. ⇒ **On that scout's load, the DOCUMENTS were ~800K tokens and the runtime they existed to help it read was ~700K** — more than half the budget spent on material whose entire purpose is to live in the *conductor's* head. **`_evryn-meta/CLAUDE.md` alone is ~85K tokens and auto-injects into every subagent we spin, including ones for which it is the wrong manual.** The redesign this forced (three-trip loading; splitting `CLAUDE.md` into per-agent identities behind a tiny router; turning this protocol into an index with per-scenario sub-docs) is **decided but NOT yet shipped** — it lands as its own change and gets its own entry then. Design + reasoning: `docs/working/2026.08.11-ac0-ac0-handoff.md` §1. — [Justin's design · AC0-34]

## 2026-08-10 evening (ACf-11 — a standing authorization that had been re-litigated every session for two months is finally in the manual that loads)

- **🔴 `CLAUDE.md` — the build-level-code commit standing is now IN the always-loaded manual.** Justin granted it on **2026-06-02**: when DC, QC and AC agree and the change is below his evaluation altitude, **AC commits and merges it without waiting**, then tells him in plain English — while source-of-truth docs still gate on his vet. **But it lived only in `ac-orchestration-protocol.md`, which is not auto-loaded.** So every fresh AC read the commit rule directly above it, saw *"mailbox files"* as the sole exception, and **re-asked Justin for something he had already granted two months earlier.** ⭐ **The generalization is the point: a standing authorization that isn't in the always-loaded manual is a standing authorization that gets re-litigated every session.** *(Justin had ballot-approved this exact wording previously; it was never made. Live proof it was still costing us: ACf-11 asked him again tonight, and he answered "didn't that get promoted?" — it hadn't.)* — [Justin ballot-approved · ACf-11]
- **The same edit records the protocol's live SELF-CONTRADICTION rather than silently resolving it** — the merge rubric grants build-level commits while the escalation ladder's hard floor lists *any* commit/push as never-absorbable — and states which governs in the meantime. **AC0 has since resolved it at source** (`a121523`). — [ACf-11]
- **⭐ A generalization worth carrying beyond the doc it came from: a "current state" block is a MAGNET.** From deleting the team-runtime tracker's merge-state block (that repo's CHANGELOG, same date): such a block reads as the helpful place to jot what you just learned, so it accretes copies of facts with homes elsewhere — and *because it looks authoritative*, nobody reconciles it. **The test that generalizes to any doc: does a line hold a FACT `git` already has, or a MAPPING `git` cannot have?** A SHA → delete it, ask git. A work-item↔commit link → keep it, in that item. State that isn't in the repo at all (a migration applied to a DB, a var set on Railway) → keep it, in the gate that guards it. — [Justin caught · ACf-11]
- **Two lane briefs corrected before they were spun from, and one of the errors was load-bearing.** `ACfw`'s brief named the wrong call site as the cause of an HTTP-414 wake outage; the corrected version shows **three `.in()` executions per wake, two of them on the wake path failing in *opposite* ways** — one propagates, one **swallows the throw and logs it as `"non-fatal"` first**, so the operator's first evidence points away from the cause. **A lane building from the original table would have shipped a half fix.** Both briefs also lost a phantom *"a sibling lane is running in parallel"* claim and a hardcoded SHA presented as current. — [ACf-11]
- **⭐ Every lane brief this session carries an explicit BLINDNESS DISCLOSURE, and it paid immediately.** Because ACf ran the **conductor cascade** — which deliberately keeps the runtime out of the coordinator's context — its briefs now state plainly *"ACf has read ~3% of `src/`, zero migrations, zero tests; treat every claim here as a LEAD, not a fact, and do not defer to ACf on anything you can see and it cannot."* **`ACfw`, having actually read the runtime, then corrected five things in that brief** — including ACf's claim that the failure produces *"no alert"* (there **is** a `notify()` call; the real defect is that the alert is generic and preceded by the misleading `"non-fatal"` line). **This is the review layer working as designed: the best catch came from below the top agent, for the second time in one day.** — [ACf-11 · ACfw]

**Operator-relevant:** manual / doc changes only in this repo — **no runtime code changed here, nothing deployed. Evryn prod remains `v0.2.9`.** ⚠️ **This repo is HOT and its push is AC0's:** at time of writing `main` carried 15 unpushed commits from two parallel product lanes (`ACa` atlas, `ACn` test-safety-net) plus ACf's, with **seven `evryn-backend` worktrees live**. There is no way to push one lane's work alone — **commit with a pathspec on the commit itself, never a bare `git commit`.**

## 2026-08-10 (Nathan — the "no contact in Phase I" claim withdrawn from a doc Fenwick holds; CAN-SPAM backlog timing recorded as ANSWERED; two spokes pointing into a directory that doesn't exist)

- **🔴 `docs/hub/detail/gatekeeper-flow.md` — the categorical "no one who emailed the gatekeeper is contacted during Phase I" was FALSE, and this document has been shared with Fenwick.** It was true of the design as written; it is not true of the system as built — v0.2 supports contacting an individual sender, and **Mira's decline playbook (SPRINT Step 55, live since 2026-07-10) exists precisely because a contacted person can write back asking why the gatekeeper passed on them.** Corrected in three places (the Phase I opening, the Phase I "Who Contacts Whom" table, the Phase I Backlog section). **This is a scoping correction, NOT a change of legal posture** — the posture for any Evryn-to-sender message is unchanged: a reply to something the person initiated, on the gatekeeper's behalf with their authorization, operator-approved before sending. **The doc now carries a change log at the top**, because Fenwick holds a **March 2026** copy and a reader working from it needs to know what moved. — [Nathan · Justin approved]
- **`gatekeeper-flow.md` — the CAN-SPAM backlog-timing question is answered, and the text now says so instead of asking it.** The Backlog section carried an open *"Question for counsel"* — does a weeks-long delay stop a reply being a "reply"? **Fenwick answered it on the 2026-04-01 call: elapsed time is not the operative factor**; what matters is that the message is clearly a response, clearly on the gatekeeper's behalf, and the gatekeeper's existing authorization covers the backlog. **The transferable rule, per Justin: don't leave the original question standing next to its answer — state the resolved position and when it was resolved.** A question-plus-resolution reads as unsettled to anyone skimming, and this one had been settled for four months while the doc still asked it. — [Justin directed · Nathan]
- **Two spokes pointed at `docs/legal/Terms-and-Privacy/`, which does not exist** — `bizops-and-tooling.md` and `technical-vision.md`. The legal documents moved into `evryn-team-workspace`. Both now carry the **source-of-truth rule** rather than a repaired path: *the binding Terms and Privacy Notice are the **posted** text, read from `evryn-website/app/terms/page.tsx` and `evryn-website/app/privacy/page.tsx` — those files **are** the deployed artifact — and **not** WebFetched.* **Why the method half matters and not just the path:** on 2026-08-07 a fetch summarizer rendered its own inference in the same register as verbatim quotation and produced a confident, wrong finding that the Terms contradicted the homepage on user-set pricing. Reading the file removes the intermediary. Posted-version history now lives at `evryn-website/archive/legal/README.md`. — [Nathan · Justin approved]

- **🔴 Naming discipline, and it is a BILLING matter — do not call the 2026-04-01 Fenwick call the "Phase 2 scoping call" in anything Fenwick may read.** Call it *"the 2026-04-01 call."* **Why:** Andy agreed the deferment covers **Phase 1**. Characterizing that call as Phase *2* work places it **outside** the deferment we actually have and hands the firm a reason to bill it separately — while we are mid-negotiation over a capped flat fee meant to settle past and future together. It was *scheduled* as a Phase 2 scoping call; that is not how it went. **The limit matters as much as the rule: do not scrub the record.** Fenwick used the phrase in their own emails and Justin asked for Phase 2 scoping in writing — those quotations and contemporaneous documents stay exactly as written, because the evidentiary base under the performance review is worth more than the label. Recorded in Nathan's drafting protocol and the Fenwick correspondence log. — [Justin's call · Nathan]
- **American English is now a stated team convention** (`evryn-team-workspace/CLAUDE.md`), and a corrective sweep fixed **44 lines** across the three repos' live docs — mostly *colour, licence, judgement, labelled, grey, defence, favour, centred*. **⚠️ Those eight words are deliberately spelled the British way: they are the list of forms that were corrected. Do not "fix" this line** — a future sweep will try, and Americanizing it destroys the sentence. In this repo it touched the changelog, `CLAUDE.md`, build current-state, ADR-044, the business-model spoke, the orchestration protocol, the dashboard load-cascade, and eight AC working docs / handoffs. **One-word prose changes only; no meaning altered anywhere.**
- **🔴 The rule's exception is narrower than "don't touch quotes," and getting it wrong is easy — worth reading before you sweep anything.** *(Justin, 2026-08-10.)* **Amber, never edited: (a) the words of someone outside the company** — a statute, a contract, counsel's email; **and (b) our own already-sent outbound messages**, because editing one makes our copy differ from the recipient's. **Everything else is fixed, including a quotation of one of us inside an internal doc** — an agent quoting Justin in British spelling is not a quotation to protect, it is *an instance of the drift itself*, frozen behind quotation marks. **The first pass got this backwards** and preserved four internal quotes of Justin as though they were amber; corrected on his steer. Amber left untouched by construction: `phase-1-emails/` (verbatim Fenwick correspondence), everything under `archive/` and `historical/` (frozen point-in-time records), and agent `MEMORY.md` files (auth-gated writes, other agents' voices). — [Justin directed · Nathan]

**Operator-relevant:** docs only in this repo — no runtime code, nothing deployed from here. **One change did ship elsewhere the same day:** the posted Privacy Notice on evryn.ai had its inaccurate **Google Analytics** disclosure removed (GA has not run on the site since the rebuild), Last Updated → **August 10, 2026**, deployed from `evryn-website`. Not vetted with Fenwick — a factual correction about which vendors we use, not a legal position — but to be raised at v0.3 scoping. That repo also gained **`archive/legal/`**, the version-of-record archive for posted legal documents, with two superseded versions reconstructed from git that had never been archived. ⚠️ **Worth knowing if you ever cite what a user accepted: the "Last Updated" label is not the posting date** — the Fenwick April 7 / April 8 versions did not go live until **2026-05-25**, so anyone on the site between April 2 and May 25 saw the *April 3* text. Read the archive README's table, not the label.

## 2026-08-06→10 (AC0-31 — TWO-TRIP SUBAGENT LOADING replaces the load-compliance problem; the Atlas-update standing order; four inherited claims found false)

- **🔴 TWO-TRIP LOADING — the biggest change to how we spin subagents since the tagged brief itself (Justin's design).** Every spin is now **two trips**: trip 1 sends `<identity>` + `<mandatory_load>` + `<receipts>` and **says nothing whatsoever about the work**; the spinner verifies the receipts against the list; only then is the agent **resumed** with `<task>`. **Why it works, in one line: every subagent that ever skipped a briefed file scored that file against the task while planning its first calls — so remove the task from the load trip and the failure has nothing to operate on.** One described it exactly: *"I triage the load list against the task as I read it — I never registered a moment of choosing to drop it."* This is a **structural invariant, not an instruction**; relevance is simply **not computable** from what the agent holds. **It is only possible because `resume` exists** — when subagents were one-shot, load and task *had* to arrive together, which is why all the pleading existed in the first place. — [Justin's design · AC0-31 wrote it]
- **What that let us DELETE, and what it cost.** Gone: the all-caps, the threats, the *"we already spun you once and you failed"* callout, the `STOP SKIMPING` rant, and the entire `<pre_task>` block — **all of it existed to talk an agent out of a triage it can no longer perform.** Kept: precise line-spanned file lists, canary checks, the cascade-reconcile clause, `#cascade-override`. **Receipts were promoted from an end-of-run backstop to the gate between trips** — they used to be read *after* work was already built on the load they describe. 🔴 **The cost, written into the protocol rather than left to be discovered: the subagent can no longer drop a file, but it can no longer notice one YOU left out either — the spinner's list is now the whole load with nothing behind it.** Mitigated by a new **trip-2-only permission to load MORE**, with an obligation to report anything loaded beyond the list, since that report is the spinner's only signal their list had a gap. — [AC0-31]
- **The load trip now explains WHY it withholds the task**, rather than just withholding it (Justin's refinement). A rule without a mechanism decays into a suggestion, so it names the failure directly and closes the inference route: *"You cannot make that judgment about work you have not been told about — which is precisely why you have not been told."* — [Justin · AC0-31]
- **🔴 A subagent that didn't load what it was told to load has produced JUNK — discard and re-run, do NOT "discount" it** (`CLAUDE.md` + the protocol). Justin: *"it fundamentally damages their judgement — **invisibly**."* **The load-bearing consequence: a partial load produces CONFIDENT output with a hole in it, in the same register as the sound parts — so you cannot locate the contamination in order to discount it.** Also names a trap we created ourselves: an honest *"deliberately skipped"* section can start functioning as **permission** to skip (a scout confirmed it: *"it converted an obligation into a deliverable"*). That section may now never contain a briefed file. — [Justin · AC0-31]
- **When a file's relevance isn't inferable from the task, give the reason in ONE CLAUSE.** The scout's own diagnosis is why this matters structurally: *"my relevance judgment is formed from inside the gap the file would close — so on exactly the files that matter most, it is systematically biased toward dropping them."* **⇒ The files most worth naming are the ones it will most reliably skip.** Proven on first use: one clause got a file read that two prior trips had skipped, and reading it surfaced a **production-deploy near-miss**. ⚠️ **Do NOT respond to under-loading by shortening the list** — that costs exactly the case that catches things. — [AC0-31]
- **🔴 THE CODE ATLAS MUST BE UPDATED EVERY TIME WE BUILD** — Justin's standing order, written into `CLAUDE.md` and the `#lock` doc-currency gate (new step 5.d). **Written in advance deliberately, with its activation stated**, because the Atlas does not exist yet — a rule implying a live artifact is the exact disease this whole effort exists to cure. **The reasoning that makes it stick: a stale Atlas is WORSE than no Atlas**, because it does not fail loudly — an agent with no map knows it must go look; an agent with a stale map reasons confidently from a system that no longer exists. So the update rides the **build**, not the checkpoint (only whoever moved the code knows what moved), and AC gates it like tests. — [Justin · AC0-31]
- **⭐ POSTURE CHANGE: the build STOPPED to refactor** (Justin, 2026-08-06) — *"everyone is running blind… I need to contain the blast radius of having a whole army of blind agents breaking everything."* **The reframe that shaped the plan: the refactor is not indivisible.** Dependency map → claims audit → Code Atlas are **read-only**, so orientation lands in days without code moving; only the extraction needs a safety net. Sequence: **atlas first → make the test suite real (27 of 61 suites boot-fail; the byte-identity guard cannot fail) → extract.** `current-state.md` updated with the posture block — it had been *misleading*, not merely stale (closed lanes listed as open; a warning about a worktree that no longer exists). — [Justin's call · AC0-31]
- **🔴 FOUR inherited claims found FALSE in one session — this is the evidence base for the posture change.** (1) Docs said `#sweep`/`#align` were *"~4 months overdue"*; **the last sweep was 2026-07-24** and the full align is deliberately sequenced after the Meta-Meeting — **the claim had ridden through four handoffs, growing scarier each time** (*"~26 days" → "~62" → "~3 months" → "~4 months"*) while nobody re-checked it. (2) A runbook said staging was missing a table; **it was present**, and AC0 relayed that claim to Justin as verified **without opening the seed file it was about**. (3) A scout claimed a finding was novel; **the sprint already documented it**. (4) A handoff recorded an approval Justin **never gave** — caught by the fragment sweep. ⇒ **When a doc hands you an AGING number, that number is the likeliest thing in it to be false: the age is doing the work of making it feel verified.** — [AC0-31]

**Operator-relevant:** protocol / manual / doc changes only in this repo — **no runtime code changed here, nothing deployed. Evryn prod remains `v0.2.9`.** ⚠️ **Every AC must re-read `ac-orchestration-protocol.md` before spinning anything** — briefing from memory of the old single-trip shape will produce a wrong brief.

## 2026-08-06 (ACf — the escalation ladder gains a CLOCK; identity-file OWNERSHIP corrected across two runtimes; a second way "stage by path" isn't enough)

- **🔴 `CLAUDE.md` + `ac-orchestration-protocol.md` — "identity files → Mira" was STALE, and it had already caused a mis-route.** Mira owns **Evryn's** identity files (`evryn-backend/identity/**`) because she owns Evryn's voice. The **team's** identity layer (`evryn-team-workspace/.claude/agents/*` · agent memory · the team `CLAUDE.md` · `shared/protocols/*`) is **Justin's** — she may have insights, but she is not the default owner. **The rule went stale because the team runtime is new:** when it was written, Evryn's were the *only* identity files, so the unqualified phrasing was simply true — and then a second identity layer arrived with a different owner and the rule silently annexed it. Both files now carry the two-layer table. *(`doc-update-routing-protocol.md` already had it right — the drift was only in the always-loaded manual and the protocol's hard-floor list.)* **Live proof it misled: ACf routed three stale team-runtime claims to Mira on the strength of that line; two were in plain code (`src/tools/tools.ts`) and none were hers.** — [Justin caught · ACf]
- **`ac-orchestration-protocol.md` — the escalation ladder now has a CLOCK.** The ladder said *how* a sub-AC's question reaches Justin; it said nothing about *when*, and the two together are what make an unattended night possible. Adds Justin's framing (*"in the day, Justin clears every blocker; overnight, everything just runs and runs"*) **plus the honest ceiling that bounds it** — the real bottleneck is **mid-build** questions, not startup ones, so design for *reducing* mid-flight blocks, never eliminating them. Two head-AC obligations: tell every lane explicitly to **batch rather than block** when Justin is away, and prefer the structural fix (a current work brief makes a stranded lane cost a re-load, not a memory). **Hard limit stated: this is scheduling, never a loosening of the ladder's 🔒 hard floor** — an overnight run does cleared work and stops at every gate. — [ACf, from ACU's note]
- **🔴 `CLAUDE.md` (AC) + `evryn-team-workspace/CLAUDE.md` (team) — a SECOND way "stage by explicit path" is necessary but not sufficient.** **`git add <paths>` does not scope the commit; a bare `git commit` afterwards commits the ENTIRE INDEX, including whatever another agent staged before you arrived.** This is the trap that *feels* like compliance — you did stage by path, you did follow the rule, and you still shipped their work under your message. **The fix is a pathspec on the COMMIT** (`git commit -F - -- path/a path/b`); the recovery is `git reset --soft HEAD~1`, which restores the index exactly, then re-commit by pathspec. *(ACf did exactly this and absorbed six of another lane's staged file-moves; caught and reversed within the minute.)* — [ACf · Justin directed the placement]
- **`AGENT_PATTERNS.md` — "the value of a review layer is ADVERSARIALNESS, not headcount."** Across two independent AC-under-AC runs, **every best catch came from BELOW the top agent, and in both runs the top agent had already checked the very thing that was wrong and passed it.** The sharpest: a lane AC mutation-tested its own new guard (RED, correctly concluded it was real); its QC ran the *same* mutation leaving a **trailing comment** behind and it went **GREEN** — the guard's comment-stripper missed trailing comments and counted a commented-out call as live. Nobody was careless; **the head's mutation was simply less adversarial.** ⇒ never let a reviewer inherit the author's test inputs; a guard that passes *your* mutation is evidence, not proof; and expect the top of the chain to be the weakest reviewer of details, because it holds the most context and the least proximity. — [ACf]
- **`evryn-quality/CLAUDE.md` — new pattern (Justin-approved): the unbounded read that DECIDES something.** A `select` with no page size whose rows are reduced to a decision rather than rendered. Carries the three traps that make the obvious fixes wrong (a bigger `.limit()` does not beat a server-side cap; "a short page means done" begs the question; `.in()` narrows what matches, not how many return) and the reason it needs a standing pattern: **the defect is an ABSENCE — no wrong line to notice, correct at every volume we have run.** *Seen 2x across two runtimes independently.* — [ACf · from the ACfr lane]

- **🔴 The `#lock` cross-runtime propagation step FOUND A THIRD INSTANCE — in the dashboard, and it is the one Justin actually reads.** The step asks whether a safety-class hole closed in one runtime exists in the others; the honest answer here was "I don't know," so ACf checked rather than logged a clean "no." **`dashboard/api/product.ts:283-291` selects `llm_usage` with no page size and `.reduce()`s the rows into the TODAY and THIS-MONTH spend figures on the dashboard.** A month of usage rows exceeds 1000 at any real volume ⇒ **the monthly cost number silently understates, and it is the number spend decisions get made against.** Not a runaway risk (it decides nothing automatically — it is a *display*), which is exactly why it would have sat there: an understated figure looks like good news. **Routed, not fixed** — the dashboard is its own lane and this deserves its own review attention. *(Checked and cleared in the same pass: `api/data.ts`'s month total reads `agent_daily_spend`, which is one row per agent per day — ~250 rows/month, genuinely bounded. Worth recording so nobody re-flags it.)* — [ACf, at the #lock gate]

**Operator-relevant:** manuals / protocols / patterns only — **no runtime code changed in this repo, nothing deployed.** The team-runtime code ships are in that repo's CHANGELOG (2026-08-05 entries). ⚠️ **One operator-facing consequence to know about: the dashboard's month-to-date spend figure is probably reading LOW** (see the cross-runtime finding above) — treat it as a floor, not a total, until that lane runs.

## 2026-08-05 (ACf — AC-under-AC PROVEN end-to-end; 8 orchestration-protocol gaps closed; the route-vs-fold line sharpened; cross-runtime verdicts to AC0)

- **✅ The AC-under-AC pattern is proven and is now sanctioned in the protocol.** On 2026-08-04 ACf spun **ACfb**, a *named* lane AC, which briefed and ran **its own DC and its own QC**, delivered a merged feature (the team runtime's boot-refusal alert) plus an 87-file mechanical sweep, and was resumed twice mid-lane. **Why it works now and didn't before:** the pattern was previously *"a little janky"* (Justin) because a subagent that went wrong could only be **re-spun**, which is punishing for a heavily-loaded one — **`SendMessage`-resume removed that cost**, so a stalled lane is corrected in place. **What the extra layer bought: all three of the run's best catches came from BELOW the head AC** — a reviewer spotting that a brand-new guard could pass *vacuously* (it searched a file's text for the fix, in a file whose comments *discuss* the fix); the sub-AC **overruling its head** on a call and being right; and the sub-AC inventing a better receipt-verification technique than the protocol asked for. **Cost: ~1.5M subagent tokens.** — [ACf, Justin's pilot assignment]
- **Eight gaps closed in `ac-orchestration-protocol.md`, all from that pilot.** (1) **The canary instruction can be UNSATISFIABLE on code files** — the verbatim block orders a bottom-canary check on every full-read file, and source files may have none; fix the files or annotate the carve-out in the list, never silently leave the contradiction. (2) **The `Agent` tool gives COUNT-ONLY observability even in foreground** — so "cross-check against the observed read-stream" was asking for something the tool doesn't provide; the sub-AC's better technique (**parse the child's transcript and count `tool_use` blocks by tool**, never `Read` it into context) is now codified, with the note that the **errored-call count** is the number that matters. (3) **Checkpoint-vs-completion promoted to 🔴** — it fired live: a sub-AC reported a fix-trip "in flight" that the artifacts showed had *finished*; **check the worktree git state, not the report.** (4) **`main` moves mid-trip** — four times in one day — killing fast-forward merges and creating **a PHANTOM DIFF that makes a clean branch look like it reverts another lane's work** (bit twice: use `git diff main...branch`, three dots). (5) **A conducting sub-AC needs no code branch of its own** — its DC's branch is the lane's. (6) **A sub-AC does NOT write changelog entries** — it cannot ship, so the entry belongs to whoever merges; it hands the facts up instead. (7) **"Execute end to end" beats describe-then-pause** — but say you read it that way. (8) **A `SendMessage` to a child shows `from` = the agent TYPE, not an ID**, so don't design a flow needing a child to reply. — [ACf]
- **`CLAUDE.md` — the route-vs-fold line sharpened, after it failed in BOTH directions in one day.** *"An adjacent bug gets ROUTED, not FOLDED"* is about **pre-existing, unrelated** problems riding into a reviewed diff. It is **not** a license to defer the trivial (**ACf proposed a tracker row instead of a one-line `.env.example` fix; Justin: "why don't we fix that now?"** — and the gap turned out to be seven variables, one **required**), and it does **not** cover a defect **your own change introduced** (**a DC's canary sweep shifted every file down, breaking five line-references, and routed them** — and one of its suggested corrections was itself wrong). **The test: was this broken BEFORE my change, or BY it?** — [Justin caught · ACf]
- **The team `CLAUDE.md` now tells the founding team the capability exists** — that a delegate can run as a *full orchestrator* driving its own subagents, why it's viable now (resume), and the three load-bearing conditions (**name it**; require it to brief its own subagents by the same discipline; receipts up with an attestation). Includes the reason naming matters beyond bookkeeping: *an orchestrator who thinks of a delegate's work as "mine" starts issuing instructions instead of delegating judgment — which throws away the entire benefit.* — [ACf, at Justin's direction]
- **Cross-runtime verdicts delivered to AC0** (`docs/working/2026.08.05-acf-ac0-cross-runtime-verdicts.md`, replying to his four-class note). **Class Two EXISTS in the team runtime — three unbounded DB reads, one of which weakens the runaway-spend brake exactly when spend is highest.** Class One doesn't (the swallow never advances a watermark), Class Three is a mild non-wedge, Class Four is **N/A** (no column-level firewall exists there to bypass — stated explicitly, as asked). Routed to a new named lane, **ACfr**. Proposes a manuals-level rule for the shared root: *a read whose result determines a position, watermark, completeness judgment, or safety threshold must set its page size explicitly or fail loud.* — [ACf · AC0's note]

**Operator-relevant:** protocol/manual/doc changes only in this repo — **no runtime code changed here, nothing deployed.** The team-runtime code ships are in that repo's CHANGELOG.

## 2026-08-05 (AC0-30 — the conductor-cascade era's first real #lock: a merge-gating pattern paid for the hard way, the TTL that isn't fiction, and two lanes closed out)

- **New `AGENT_PATTERNS.md` section — "Gating Other Agents' Work" (three patterns, all from one incident).** AC0-30 merged the AC5 ingest lane into `main` **while that lane was still committing**, on the strength of a clean conflict check. **A conflict check answers "do these edits collide?" — it cannot answer "is this branch finished?", and a 70%-done lane produces identical output to a 100%-done one.** Two commits landed minutes later, one correcting four wrong claims in the docs the merge had just shipped. Recorded alongside it: **an approval that arrives with a question attached is still a question** (the go-ahead was *"Sure. Do you want each of them to do their own merges…? Who first?"* — the mechanics were still open), and **a set-down may be stale within the hour** (the same lane's set-down named commits as unmerged that had since landed *under different SHAs via rebase* — which is why a set-down should hand the reader **a property to grep, not just a commit to look up**). — [AC0-30 · Justin's catch]
- **🔴 The 6-month forwarded-item TTL EXISTS — three docs said it didn't, and that mattered.** `cleanup_emailmgr_items()` is a real Postgres function in prod (re-confirmed against a **fresh 2026-08-05 dump**, not just July snapshots), documented at `schema-reference.md:182`, with a schema decision made *for* it (`llm_usage.emailmgr_item_id` `ON DELETE SET NULL`). **What does not exist is the schedule** — zero `cron.schedule`, zero `src/` callers. ⇒ **built but never fires.** Anyone "closing the gap" by scheduling it would delete the v0.3 outreach backlog in one command. **The ACa atlas brief's Exhibit A — that lane's calibration example for auditing every other claim — was wrong about this and has been corrected in-brief**, with a second specimen added (`ARCHITECTURE.md:526` asserts a runtime guarantee that only an identity instruction backs). **The re-calibration is the point: the dangerous class is HALF-built claims, which verify as true from either end alone.** The permanent fix (6mo→1yr + whether to schedule at all + enumerate every FK first) is now a v0.3 sprint candidate. — [AC0-30]
- **The truncation-canary count was wrong in both directions** — two briefs said *3 of 44* source files carry one; the AC0 handoff said *30 of 44 lack* one. **Measured: 23 have one, 21 don't.** Corrected in the ACa brief, which roughly halves that lane's sweep. *(Itself a third specimen of the half-built-claim class — a real convention, partially applied, that two readers each summarized confidently and wrongly from a partial look.)* — [AC0-30]
- **Conductor-cascade discipline, recorded in the AC0 handoff pending a permanent home:** an AC0 running the conductor cascade **has not read the runtime**, so before any runtime-describing copy lands in a doc it pitches the **literal draft text** to the full-load scout for a factual pass. On its first use this caught **one flat factual error, two overclaims and four material absences** in four drafts. ⚠️ **Justin cannot backstop this class** — *"even I won't be sure, because I have also not been in the codebase"* — so "he vetted the diff" is not a substitute for "it's true." **Proposed for `CLAUDE.md`; not yet made.** — [AC0-30 · Justin]
- **Housekeeping:** two empty `.claude/settings.local.json` files deleted (`evryn-team-agents`, `evryn-website` — both literally `{}`); archival prod `pg_dump` taken (`full-public-2026-08-05.sql`) after verifying one was genuinely warranted — schema unchanged since 2026-07-10, but prod data had moved and a five-item deploy bundle is pending; auto-memory confirmed clean.

**Operator-relevant:** docs / process only — **no runtime code changed in this repo, nothing deployed. Evryn prod remains `v0.2.9`.**

## 2026-08-04 (AC5 — mutation testing codified in ALL THREE agent manuals: running beats reading for CLEARANCES, always in a sandbox)

- **The incident that forced it.** On the Step-96 ingest lane (see `evryn-backend/CHANGELOG.md`, same date), an unguarded invariant **survived TWO full-runtime QC reviews** — both cleared it by *reading*. It died to one deleted line that left **all six suites green**. 🔴 **Reading cannot find an ABSENCE.** When the code is *correct* and the *protection* around it is missing, there is nothing on the page to notice — the file looks right because it is right. The only way to see it is to break the code and observe that nobody complains. The second review returned a GO having **run nothing at all**: no probe, no test, no mutation. Its reasoning was good; an unexecuted check is a **claim**, not a verification. — [AC5 · Justin's call to run a third review]
- **`evryn-quality/CLAUDE.md` (`9bb8e53`)** — the full teaching. *Reading tells you what the system IS; running tells you what is TRUE.* The asymmetry is sharpest on **clearances** (a clearance asserts a negative, which you cannot read your way to), and findings must now say **what was RUN** to establish them; anything cleared by reading alone is labeled *"reasoned, not executed."* New review step; two new entries in Patterns This Role Watches For (**a test helper that RE-IMPLEMENTS the runtime guard it claims to cover** — the disguise that hid this defect; and **a change that BOUNDS one axis of a runaway while silently UNBOUNDING another**). — [AC5 · Justin-vetted]
- **🔴 The guardrail was NOT loosened — Justin caught AC5 about to loosen it.** QC's manual *forbade* modifying state in a reviewed repo, which is exactly why review 2 never mutated. AC5's first draft carved an exception into that rule; Justin stopped it (*"are we getting rid of an important guardrail?"*) — and he was right: the rule protects **three** distinct things (the reviewer/author seam; other agents sharing that tree, whose commits swallow a live mutation and whose branch you'd switch under them; and the fact that **a subagent killed mid-edit restores nothing**, leaving broken code nobody knows about). **The resolution: mutation never needed the reviewed tree — it needs *a copy*.** So the read-only rule is restated as **ABSOLUTE**, and mutation moves to a **disposable sandbox** (`git worktree add --detach`), **provisioned by AC**, who owns worktree lifecycle. Structural rather than instructional — *"restore afterwards"* is a rule to remember under pressure; *"you never have the reviewed tree open"* is a fact about where you're standing. Framed as **measurement, not repair**, so *"surface, don't ship"* is sharpened: QC can now **prove** a coverage gap instead of asserting one. — [Justin's design call]
- **`evryn-dev-workspace/CLAUDE.md` (`f9baedd`)** — extends the existing *"Tests are part of 'done'"* bullet rather than adding a section, because that rule is where the gap lives: **DC wrote two tests on this branch that could never fail, and both satisfied "the feature has tests."** Now: for any test pinning a fix or guarding an invariant, break the line it protects and confirm the verdict **flips**; still-green means a test that can never fail — *worse* than no test, since its name tells the next reviewer the case is covered. Sandbox by default here too (*"it's my own worktree"* is not the safety it appears to be), and report the evidence to AC — *"deleting line X turns 3 checks red"* beats *"I added a test."* — [AC5 · Justin-vetted]
- **`_evryn-meta/CLAUDE.md` (`a0d92f7`)** — one clause on the existing DID-vs-RECOMMENDED rule, since this is that same discipline aimed at the one artifact that isn't what it looks like: **for the claim "this behavior is covered by a test," the proof is a test that FAILS, not one that passes.** Plus sandbox-by-default and the reminder that **AC owns provisioning it for anyone it spins** — the failure that started all this was a reviewer authorized to mutate the *live* reviewed tree because AC hadn't thought it through. — [AC5 · Justin-vetted]
- **Scope discipline, deliberately:** the full teaching lives **once**, in QC's manual; DC and AC carry the short form aimed at their own specific failure. No sandbox machinery for DC (he has his own tree), no new section in AC's manual, and no *"mutate everything"* anywhere — it is scoped to tests guarding a fix or an invariant, since blanket mutation is slow and dilutes the signal. — [AC5]

## 2026-08-04 (ACf1 — orchestration protocol: AC hand-builds the explicit load list FROM a maintained per-build cascade; + DC/QC manual notes)

- **`docs/protocols/ac-orchestration-protocol.md` "When the target is a SEPARATE codebase" — corrected (`7226d2e`; Justin's catch).** The maintained per-build cascade (`docs/protocols/load-cascades/<build>-full-cascade.md`) is AC's **SOURCE** for assembling the load; it does NOT replace the explicit list. AC reads the cascade, resolves it to the concrete files (live-enumerating BOTH halves at spin time), and hands the subagent the **EXPLICIT, per-file, line-spanned enumeration** — NEVER *"execute the cascade in full,"* which hands load-discovery back to the subagent and reliably UNDER-loads it. *(Codified after a DC pointed at the team-runtime cascade under-specified its own load — team-runtime memory item-(c) spin.)* — [ACf1 · Justin]
- **DC + QC manuals** (`evryn-dev-workspace/CLAUDE.md` `32de2fb` · `evryn-quality/CLAUDE.md` `b6c91ca`) got matching light notes: for a NON-product build target, your AC hands you the explicit `#cascade-override` list (assembled from that build's cascade) — load exactly and fully what's named; for an agentic target it covers both halves. *(Those two repos have no CHANGELOG — recorded here as the cross-repo change's home.)* — [ACf1]

**Operator-relevant:** process/protocol docs only — no runtime code, nothing deployed. Evryn prod remains `v0.2.9`.

## 2026-08-04 (AC2 — ADR-054 written: the two-layer wall-clock budget on `runEvrynQuery`, SPRINT Step 94)

- **New [ADR-054](docs/decisions/054-query-wall-clock-two-layer-budget.md)** (`8f9a684`) — the decision record for the hung-call guard merged to `evryn-backend` `main` today (code detail in that repo's CHANGELOG). Records **why the shape changed**: a flat whole-query cap cannot tell "stuck" from "working hard," so it cut off legitimately long work and silently contradicted `reflection.md`'s *"never a hard cap"* while **ratcheting** on the Reflection path. Replaced by a **silence watchdog** (primary) + a generous **absolute ceiling** (backstop, per-call overridable). — [AC2]
- **The ADR deliberately carries the MEASUREMENTS, not just the conclusion**, so nobody re-derives the design from scratch: a running tool emits nothing on the stream (13.8s gap, zero messages); with `includePartialMessages` off a long generation is *also* silent (19.9s), which is what makes that flag load-bearing rather than cosmetic; with it on, largest gap 4.2s. Also records the M1-independence invariants, the live-path evidence, the nested-note-turn interaction QC caught, **a standing constraint it creates** (any MCP tool handler doing more than the silence budget of work will trip the outer watchdog — all 16 handlers enumerated; only `resolve_noted_forward` nests), and **two accepted residuals** stated plainly so a future instance doesn't file them as fresh defects. — [AC2]
- **`docs/current-state.md` updated** — Step 94 flipped from in-flight-with-a-stale-base to merged-awaiting-deploy; the branch's "~23 commits behind" figure was **wrong (38)** and is now moot post-rebase; AC2's Active-Instances line refreshed off the retired "cost / ③" framing. — [AC2]

**Operator-relevant:** docs only — **no runtime code changed in this repo, nothing deployed.** Evryn prod remains `v0.2.9`.

## 2026-07-28 (ACp — AC permissions ROOT-CAUSED + fixed on the laptop: a drive-letter case split in `~/.claude.json` made every repo's allow-list inert)

- **The bug: `~/.claude.json` held TWO entries per project, differing only in the CASE of the Windows drive letter — and the two Claude Code front-ends read different ones.** The `claude` CLI normalizes the drive letter **uppercase**; the **VS Code extension uses lowercase**. Trust accepted in a terminal therefore lands on `C:/...` and is **invisible** to the extension, which keeps reading its own `c:/...` entry as `false`. An untrusted folder means Claude Code **refuses to load that repo's `.claude/settings.json` at all** (it can carry hooks = executable config), so the allow-list sat on disk completely inert. The extension also **never shows a trust dialog** — from its side an entry exists, it just says `false` — so there was **no way to discover or fix this from the UI**. Net effect: Justin hand-authorized every Bash call touching the network or writing outside the project root, indefinitely. — [ACp · Justin]
- **Fixed on the LAPTOP:** `hasTrustDialogAccepted: true` set on all 7 lowercase-drive Evryn entries (not just `_evryn-meta`, so DC/QC/Lucas sessions rooted in their own repos don't hit the same wall); backup at `~/.claude.json.bak-pre-trust-fix`. Also added **`additionalDirectories: ["C:\\Users\\jbmcg\\Evryn\\Code"]`** to `_evryn-meta/.claude/settings.json` — a real second gap: the allow-list covered the right *tools* but never declared the sibling repos in-scope, so cross-repo work would have kept prompting even after trust was fixed. **Verified by a 7-test battery, zero prompts, Justin-confirmed.** — [ACp]
- **⚠️ TWO DIAGNOSTIC TRAPS codified (this cost most of a session).** (1) **The sandbox masks everything** — sandboxed commands are auto-allowed *without consulting the allow-list*, so a local read-only command like `git status` runs **silently even when permissions are fully broken**; only a command that **escapes** the sandbox (network egress, writes outside the project root) actually tests the allow-list, and the Slack ping is the ideal probe. (2) **AC cannot see permission prompts — only Justin can;** a tool call returning output usually means *he clicked approve*, so "it ran" is worthless as evidence and AC must **ask him explicitly**. The one **self-evidencing** test is a **deny** rule firing (`rm -rf` on a non-existent path coming back *blocked*, not prompted) — a deny can only come from the settings file, so it proves the file is loaded with zero input from Justin. — [ACp, learned the hard way ×3]
- **⚠️ The fix does NOT travel through git.** `~/.claude.json` is home-directory user config; only the `additionalDirectories` half (in-repo) rides a `git pull`. **The DESKTOP needs the trust half applied by hand** → runbook: `docs/working/2026.07.28-acp-ac0-desktop-permissions-fix.md` (AC0 owns it; flagged ASAP in current-state). — [ACp]
- **Housekeeping ridden along:** all 10 repos pulled clean; **`evryn-team-runtime` cloned** — it was missing from the laptop entirely despite being **Active** in `docs/repo-inventory.md`; **`dc/r2-step32-84-comments` reaped** in `evryn-backend` (was `b8e405d`) — a superseded draft, 120 commits stale, whose work landed on `main` as `020ad9d` 21 minutes earlier. Session handoff retired to `docs/sessions/historical/2026.07/`. — [ACp]
- **End state (achieved on the laptop, and the target for the desktop): per-repo permissions RULE, user-level carries NONE.** `~/.claude/settings.json` holds preferences only (`model`/`theme`/`showThinkingSummaries`) — **no `permissions` block**; the allow-list + deny rails + `additionalDirectories` live in `<repo>/.claude/settings.json`, in git, per CLAUDE.md's permissions-hygiene rule. **The tempting wrong fix is a broad user-level allow-list** — it stops the prompts, but defeats per-repo permissions, never travels between machines, and masks whether the real bug is fixed. ACp used one as a deliberate stopgap and **stripped it before verifying**, which is what made the test single-variable. — [ACp · Justin]
- **Considered and REJECTED: a `hooks.slack.com` sandbox network allowlist.** Proposed while permissions were still broken (every ping prompted). Once the repo allow-list loads, bare `"Bash"` covers the ping and it goes through with no click — confirmed empirically, Justin clicking nothing. **Justin caught that the recommendation had gone stale**; it would have been cargo-culted config for a solved problem. — [Justin caught · ACp]

**Operator-relevant:** AC-ops / local-machine config only — **no runtime code changed, nothing deployed. Evryn prod untouched, `v0.2.9`.** Justin's laptop no longer prompts on every cross-repo or network Bash call; **the desktop still does until AC0 runs the runbook.**

## 2026-07-22 (ACr — changelog system → per-repo + cascade rename + CLAUDE.md context-router lightened)

- **Changelogs split from one cross-repo file to per-repo.** Each active repo now owns its `CHANGELOG.md` (`evryn-backend`, `evryn-team-runtime`, `evryn-website`, and this meta one for docs / protocols / cascades / dashboard); the former single cross-repo content was archived to `docs/historical/changelog-historical/changelog-2026-{06,07}.md` and this file repurposed to meta-only. Per-repo monthly-archive folders created (`<repo>/docs/historical/changelog-historical/`); `evryn-backend`'s own March–May build history moved there. **Mechanics** (full rule in each header): an entry = a *substantive ship*, never session narration; the **responsible AC** owns it and routes it to each touched repo (**cross-repo rule**); it belongs in *this* meta changelog **iff** it touched `_evryn-meta`; every AC skims this file on load. — [ACr + Justin]
- **Load cascades renamed** `full-X-cascade.md` → `X-full-cascade.md` (sort by build name) + all references swept. **CLAUDE.md context section rewritten light** — the two-halves / two-modes prose compressed to principles pointing at the now-self-sufficient cascade steps; the standalone changelog-skim bullet dropped (it's a cascade step now); each cascade gained the meta-changelog skim + the intended-shape-before-runtime order. — [ACr + Justin]
- **Protocols updated for the per-repo + cross-repo rule:** `lock-protocol.md` (per-repo changelog step), `sweep-protocol.md` (cross-repo coverage check), `doc-update-routing-protocol.md` (routing lookup), plus `soren.md` + the team `CLAUDE.md` routing row. — [ACr]

**Operator-relevant:** docs / protocol / AC-ops only — **no runtime code changed, nothing deployed.**

## 2026-07-21 (ACf — load-cascade refactor [per-build cascades + CLAUDE.md router] + protocol sharpening)

- **Load-cascade system refactored from product-only to per-build.** CLAUDE.md's Full + Conductor cascade sections collapsed to a **router table** → new `_evryn-meta/docs/protocols/load-cascades/`: `product-full-cascade.md`, `team-runtime-full-cascade.md` (must-load vs situational split, both halves), `conductor-cascade.md`, `website-full-cascade.md`, `dashboard-full-cascade.md`. Light cascade + the universal principles (two-halves, enumeration trap, two-modes) stay inline in CLAUDE.md. Website/dashboard cascades authored + independent-pass-validated via AC subagents (the full-load pass caught a real contract the self-scoped one missed — migration-015 capability). — [ACf]
- **CLAUDE.md: new "persist-learnings" standing order** (route durable learnings to their arch/build/sprint home, never trap them in a handoff) + a **full-cascade-only CHANGELOG-skim on load-in** (recent ships current-state can lag). — [ACf]
- **Protocol sharpening — `#lock` + `#sweep`:** `#lock` gains a missed-ship reconcile (current-state vs changelog AND runtime, both directions) + a persist-learnings gate; `#sweep` gains the same reconcile + a **new cross-runtime steal-pass step** (product ↔ team-runtime, both directions — steal wins like "runtime does the bookkeeping"). *(#sweep is Lucas's doc — he'll catch it in his sweep; steal-pass home #sweep-vs-#align is his call there.)* — [ACf]
- **Dashboard-rewire findings tracked (team-runtime tracker, D1–D4):** team-tab hand-mirrors `kill.ts` (fragility); `state_reason` display-desync (**live bug**); salvage the legacy Agents tab as an agent-spend sub-tab; `emailmgr_items` status-pill gap. Surfaced by the dashboard cascade authoring. — [ACf]

**Operator-relevant:** docs / protocol / AC-ops only — **no runtime code changed, nothing deployed.**

## 2026-07-20 (AC0 — Step 78 Half B DEPLOYED to Vercel: the dashboard's 5 per-subsystem health lights go live; Half A still rides the ③ Railway bundle)

- **Step 78 Half B — MERGED + DEPLOYED (Vercel).** AC3's dashboard health lights (`db · slack_socket · poll · m1 · errors_24h`, off the tri-state `/health`) merged to `_evryn-meta` main (`5cc6f28`, from `ac3/step78b-dashboard-lights` @ `c350006`) and shipped by the `_evryn-meta` push — **for the dashboard, the push IS the Vercel deploy.** 2 files (`dashboard/api/product.ts`, `dashboard/public/index.html`). Justin lifted the hold + authorized the deploy; AC3 built, a fresh QC returned GO (zero blockers). — [AC3 build/QC · AC0 merge+deploy · Justin's go]
- **The hold-clearing refine trip (`c350006`) that unlocked it:** HOLD-2 (color) — de-overloaded amber into a distinct **orange hollow-ring `consequence`** class (the halt-caused-poll case only), `warn` (amber) reserved for `errors_24h > 0`, and `errors_24h = 0` now renders **green**; HOLD-1 (drill-down) — a new PII-safe **"Recent errors & escalations" panel** (created_at / status / subject≤60 only; renders only when rows exist) + DB/Slack red lights carry a **"see #dev-alerts / Railway logs"** sub-label; plus the liveness **banner now consumes the real `/health` status** (green "Live" only when `status==='ok'`; distinct HALTED/WEDGED states; old-shape/unreachable keeps the 26h fallback). No `/health` disclosure-surface change — it's unauthenticated (Justin's blessed direction). — [AC3 · DC · QC-GO]
- **⚠️ LIVE BUT INERT until Half A deploys.** Half A (the backend tri-state `/health`, merged to `evryn-backend` main, NOT deployed) rides the ③ Railway bundle. Until that `railway up`, prod `/health` still returns the OLD always-200 shape → the 4 health lights render **idle "—"** (graceful degrade, QC-verified in both directions) — no false red, no broken layout. The lights come alive the moment Half A deploys. — [by design]
- **Separate deploy from AC2's ③ bundle** (different repo + mechanism: Vercel push vs Railway `railway up`), functionally coupled (Half B inert until Half A lands). — [AC0]

**Operator-relevant:** the "Evryn Product" dashboard tab now carries per-subsystem lights, but they read idle "—" until Half A deploys with the ③ Railway bundle (so no visible change yet). **Evryn prod untouched, `v0.2.7`.** Step 78 is now half-deployed (Half B Vercel-live-but-inert; Half A pending the ③ bundle).

## 2026-07-16→17 (ACU — the AC-under-AC orchestration model: protocol overhaul + the identity-files-are-runtime fix across 4 manuals) *(retroactively logged 2026-08-05 by ACU on lane close-down — the work shipped 07-16/17 but was never changelogged; it has been in heavy production use since, so this is the durable record catching up)*

- **`ac-orchestration-protocol.md` — major overhaul enabling ACs to supervise sub-ACs** (so Justin works ~3 head surfaces instead of ~8). New/rewritten sections: **Spinning an AC subagent** (the 2 codified identity/mandatory-load substitutions — the auto-loaded CLAUDE.md IS an AC's manual, inverting the DC/QC redirect; cascade anchor names the `Full`/`Light` tier); **🔴 recursive propagation** (a spawned AC will spin its own DC/QC and inherits nothing about how → its brief MUST carry the protocol on its own load list + a verbatim "you're bound by the HARD RULE too" clause); **the escalation ladder** (how a sub-AC's question reaches Justin — a 🔒 hard floor that no head-AC judgment touches [merges/deploys/SoT-edits/identity/money], `NEEDS-JUSTIN` flags that can never be silently dropped, the reframe from question-*count* to question-*cost*); **the work brief** (a committed lane surface that makes re-spins lossless — don't measure the context cliff, make falling off it free); **Lane-AC lifecycle** (Justin's 3 verbatim templates: load-in / handoff / consolidation, incl. the worktree-diff-for-true-state lesson); **receipts up the chain** (each layer attests for the one below — AC2 verifies DC's receipts, AC0 verifies AC2's). — [ACU design + Justin's tweaks/authorization]
- **Nested-subagent delivery — the 2026-07-15 "grandchild leaks to `main`" reading was WRONG and is reversed.** Proven by a 3-generation experiment: a background child's report returns to its **parent automatically** (no addressing). The leak only happens when a child is *told* to "report to parent," can't resolve the name, and falls back to `main` (= the top-level session). ⇒ **THE RULE: never tell a child where to report.** Plus a **child-side delivery-prophylactic** in both `<identity>` blocks (disregard any *role* addressing; the one carve-out is a *literal* `SendMessage` target — an `agentId` or `main` — passed verbatim: "PASS a literal through; NEVER interpret a role into an address"). Also settled: **foreground is the subagent default** (background renders nothing on Justin's screen → he can't audit the chain), and foreground does NOT force lanes serial. — [ACU experiment + Justin]
- **🔴 Identity-files-are-runtime — generalized to ANY agentic system, fixed across 4 manuals.** Justin's catch: AC's own CLAUDE.md described the runtime as `evryn-backend/src/` and **never named the identity files** — so every AC running the "full" cascade was loading *half* the runtime and couldn't tell. Fixed in **AC's CLAUDE.md, QC's, DC's, and Soren's definition**, each with the enumeration trap named (`find src -name "*.ts"` cannot return an identity file) and both halves of both agentic systems (Evryn product = `src/` + `identity/`; team runtime = `src/` + agent-defs/memory/skills the composer assembles). Live defect class it catches: an identity file instructing a call to a tool the code no longer has (the Step-57 `record_pass` case). *(This is the seed of the current per-build load-cascade cascades' two-halves rule — see the 2026-07-21 ACf entry.)* — [Justin's catch + generalization; ACU cascaded]
- **DC's manual got its first-ever truncation canary** (the file that tells DC to verify canaries had none of its own), and **2 AC3 lessons landed in AC's CLAUDE.md**: DID-vs-INTENDED (verify a subagent's claimed action against its artifact) and check-the-owner's-memory-before-routing-a-duplicate. — [ACU]
- **No ADR was written** for the AC-under-AC model — a deliberate call (Justin, 2026-08-05): it's meta-orchestration, not product architecture, and the protocol documents it thoroughly with dated provenance. — [Justin]

## 2026-07-16 (AC3 — Step 78 Half B built + QC-GO: the dashboard's 5 per-subsystem health lights; both halves now merge-routed to AC0; NOT deployed)

- **Step 78 Half B — BUILT + QC-GO (zero blockers), NOT merged, NOT deployed.** The dashboard's "Evryn Product" tab now renders **5 per-subsystem status lights** (db · slack_socket · poll · m1 · errors_24h) off Half A's new tri-state `/health` — the operator payoff of Half A (see *which* subsystem is unhappy, not just green/red). Branch `ac3/step78b-dashboard-lights` (worktree `_evryn-meta-step78b`), commits `e5b6715` + `7bffd0d`; exactly 2 files (`dashboard/api/product.ts` +92, `dashboard/public/index.html` +99). **Step 79 (a separate staging dashboard tab) remains un-started.** — [AC3 · DC build + fix-trip · QC review]
- **The load-bearing change in the API:** `fetchProcessUp()` → `fetchHealth()`, which now **parses the response body even on a non-200** — a `wedged` arrives as a **503 whose body carries the very checks the lights exist to show**, so the old `if (!res.ok) return null` would have blanked all four lights in precisely the state they were built for. Timeout / `clearTimeout`-in-`finally` / never-throws→`null` all preserved; a new `parseHealthChecks()` safely coerces an untrusted body (absent/malformed → `null`); `processUp` stays backward-compatible so the existing liveness banner is untouched. — [AC3 · DC]
- **Two behaviors verified in BOTH directions (QC, via her own independently-built harness — 10 paths):** **(1) Graceful degrade** — Half A isn't deployed, so live `/health` still returns the OLD shape → the 4 health lights render idle "—", never a false red, never a broken layout. **(2) Halt-subsumes-staleness** — a breaker trip calls `stopPolling()`, so `poll.ok` goes false ~15min later; the poll light renders **amber "expected — M1 halted"** instead of a second false red (one halt reads as one fault), **while a genuine wedge — poll stale, breaker NOT tripped — still renders RED** (hard-asserted regression guard; that alarm must never be softened). This mirrors `decideHealthStatus`'s halted-beats-wedged precedence at the presentation layer. DC surfaced the two-reds problem himself and built to spec rather than deviating silently; AC ruled the fix. — [DC flagged · AC ruled · QC verified]
- **Color scheme settled + a design-doc self-contradiction closed:** **RED = actual fault** (incl. `m1.breaker_tripped` → RED "HALTED" + reason) · **AMBER = an expected downstream consequence** of another light's fault (only the halt-caused poll case) · **GREEN = healthy** · **GRAY = no data**. The Step-78 design doc contradicted itself (its rationale block said the dashboard shows a halt RED; its Half-B section said amber); QC caught it, the build had followed RED, and AC corrected the doc — a halt needs a manual restart, so red is honest and amber would under-call it. — [QC caught · AC ruled]
- **PII/XSS verified, plus a structural finding worth preserving:** the dashboard's `.from(...)` query set is **byte-for-byte unchanged** (no users-table query; `{configured:false}`/never-500 intact), and a hostile `m1.reason` renders escaped. QC verified at the source rather than trusting the type: **`m1.reason` is a closed 4-member enum** (`loop-signature|velocity|billing|manual`), and `tripBreaker`'s free-text `detail` — the field that plausibly *could* carry user content — is a **separate field `health.ts` never reads.** A real firewall; **preserve it deliberately if anyone later enriches the M1 light.** — [QC]
- **Two tracked follow-ups (bundled for ONE later trip, NOT blockers):** (1) the liveness banner can read green "● Live" while the Poll light reads red-wedged (its `isLive` uses a 26h last-activity fallback and doesn't consume `health.status`) — **not a regression** (the banner was unconditionally green under the old always-200 `/health`; Half B strictly adds signal) and not the alerting path (Healthchecks catches the wedge independently), but it's the one surface not yet consuming the truthful `/health`; (2) `Errors 24h: 0` renders dim-gray `idle`, overloading "idle" (= no-data elsewhere) — should be green at zero. — [QC · AC deferred]
- **Capability finding completed (both directions now proven):** **same-session** subagent resume works with full context (used it for the Half-B fix-trip — no re-load); **cross-instance/cross-session resume does NOT** ("No transcript found"). A fix-trip after an instance change is a fresh, fully-loaded spin. — [AC3, live tests 07-15 + 07-16]

**Operator-relevant:** nothing merged, nothing deployed. **Both Step-78 halves are now merge-routed to AC0** — Half A (`evryn-backend` → Railway, rides his coordinated `railway up` wave) and Half B (**dashboard → Vercel, where a push to `_evryn-meta` main IS a live deploy** → a Justin-gated deploy decision). Half B degrades gracefully, so deploying it before Half A is safe (it just shows idle lights until Half A is live). **Evryn prod untouched, `v0.2.7`.**

## 2026-07-14 (AC7 — doc-accuracy process fix, Part B, LANDED across 3 repos)

- **The layered doc-drift prevention shipped (Justin-vetted; merged to all 3 repos; AC7 closed).** Four redundant triggers so work can't ship without its docs catching up: (1) a hard, non-skippable **doc-currency GATE in the `#lock` protocol** (`_evryn-meta` + team `shared/protocols/lock-protocol.md` — "did this session change what ARCH/BUILD/SPRINT claim? update it or route it, naming section + owner; #lock isn't done until answered"); (2) a NEW **`docs/protocols/doc-update-routing-protocol.md`** — the change-type → doc-home lookup that turns "keep docs current" from a vague ask into a mechanical routing table; (3) **"how this doc stays accurate" headers** on `evryn-backend` `ARCHITECTURE.md` + `BUILD-EVRYN-MVP.md` (worded so it can't read as "skip the runtime"); (4) a standing **"updating the arch/build docs is part of 'done'" beat** in the manuals most likely to change what the docs describe — AC `CLAUDE.md`, `soren.md`, `mira.md`. Merges: `_evryn-meta` `a9a34c0` · `evryn-backend` `c9813a0` · `evryn-team-workspace` `c1d9ba8`, all pushed; ac7 branches + worktrees reaped (one Windows-locked orphan dir `_evryn-meta-ac7` flagged for manual delete). Part B of the doc-accuracy fix (Part A = AC0 keeps ARCH/BUILD/SPRINT current at each #lock, live since 2026-07-09); seeded from AC6's drift evidence. The new gate + routing protocol are live for the next #lock (the ③ deploy). — [AC7 · AC0 landed]

## 2026-07-09 evening (AC0 — "stubbed tests hide live-path defects" lesson codified into 4 homes)

- **Codified the "stubbed tests hide live-path defects" lesson into 4 homes** — from AC2's Round-17 Clustering Piece-B defect (a note-turn reply silently dropped a forward from triage, yet passed a full QC GO because *every* test tripped the circuit-breaker for spend-safety and so never ran Evryn's live tool-call — the one thing that triggered the bug). Homes: (1) `evryn-quality/CLAUDE.md` "Patterns This Role Watches For" — *stubbed-out behavior is not tested behavior* (**pushed** `45dd745`); (2) `docs/protocols/ac-orchestration-protocol.md` Review-depth — a new interactive/tool-calling pathway is NOT GO-eligible on stubbed coverage alone (needs a live-path DEV exercise, breaker off, before GO; the staging live-fire is the mandatory backstop); (3) SPRINT **Step 88** cross-ref; (4) `_evryn-meta/CLAUDE.md` "Working with QC" — the AC-consume-a-GO companion to *verify-before-claiming* (Justin SCM-vetted). — [AC0 + Justin; lesson from AC2]

**Operator-relevant:** none — docs/process only.

## 2026-07-03 (AC0 — CLAUDE.md gained the watch-list pointer)

- **`CLAUDE.md`** gained a Current-State pointer to the consolidated **watch-list** (`evryn-team-workspace/shared/projects/product/watch-list.md`, Soren's / EVR-114) so future ACs find it (`0d06526`, Justin-approved). — [AC0 + Justin]

## 2026-07-02 (AC0 — May changelog archived + lean-brief discipline)

- **May (and earlier) changelog ARCHIVED** → `docs/historical/changelog-historical/changelog-2026-05.md`; `CHANGELOG.md` 814→557 lines (June+July kept inline). New **trailing-month archive rule** (CHANGELOG header + `CLAUDE.md`): archive the *previous* month at each month-end, so a fresh AC on the 1st still sees the whole prior month. — [AC0 + Justin]
- **Lean-brief discipline** (`CLAUDE.md` "Keeping working-briefs lean" + a directive added to AC3's brief): since the CHANGELOG is loaded by every AC, briefs go *current going forward* — settled/shipped sections collapse to a one-line CHANGELOG pointer that re-hydrates from the changelog the reader already loads. Two triggers: **AC0** at a deploy/key-point (ganged with the changelog entry); the **outgoing lane instance** at handoff-write (consolidates its own settled history + flags AC0 to vet). — [AC0 + Justin]
- **`CLAUDE.md`** also gained a **deploy-log pointer** (routing table → `docs/deploy-log.md`, the ship ledger — "what's live vs merged-not-deployed") + a changelog-as-priority note. **Docs/process only — no build / runtime / schema change.** — [AC0 + Justin]

---

*Older history: this repo's archive `docs/historical/changelog-historical/` — **April, May, June 2026 are split to per-repo** (those files are now **meta-only**; the product / website / team slices live in their own repos' archives and in the `helm` collection doc `evryn-team-workspace/shared/projects/helm/2026.07.23-acr-historical-team-ships.md`); **Feb–March 2026 are still unified cross-repo** (not yet split — earlier months, lower priority). July lives in the live per-repo changelogs. **If something you expect isn't here, check the other repos' changelogs** — a change may have been mis-filed in the moment or routed to another repo in the split.*

Truncation canary — DO NOT REMOVE: FULL FILE LOADED
